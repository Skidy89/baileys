/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt, $String = $util.global.String, $Number = $util.global.Number, $Array = $util.global.Array, $Boolean = $util.global.Boolean, $isFinite = $util.global.isFinite;

const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

function longToString(value, unsigned) {
	if (typeof value === "string") {
		return value;
	}
	if (typeof value === "number") {
		return String(value);
	}
	// Fast path: convert Long {low, high} directly via native BigInt
	// BigInt.toString() is a native C++ operation, much faster than Long's pure JS division loops
	if (value && typeof value.low === "number" && typeof value.high === "number") {
		const lo = BigInt(value.low >>> 0);
		const hi = BigInt(value.high >>> 0);
		const combined = (hi << 32n) | lo;
		if (!unsigned && value.high < 0) {
			return (combined - (1n << 64n)).toString();
		}
		return combined.toString();
	}
	return String(value);
}

function longToNumber(value, unsigned) {
	if (typeof value === "number") {
		return value;
	}
	if (typeof value === "string") {
		return Number(value);
	}
	// Fast path: convert Long {low, high} directly via native BigInt
	if (value && typeof value.low === "number" && typeof value.high === "number") {
		const lo = BigInt(value.low >>> 0);
		const hi = BigInt(value.high >>> 0);
		const combined = (hi << 32n) | lo;
		if (!unsigned && value.high < 0) {
			return Number(combined - (1n << 64n));
		}
		return Number(combined);
	}
	return Number(value);
}

export const proto = $root.proto = (() => {

    const proto = {};

    proto.ADVDeviceIdentity = (function() {

        const ADVDeviceIdentity = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ADVDeviceIdentity.prototype.rawId = null;
        ADVDeviceIdentity.prototype.timestamp = null;
        ADVDeviceIdentity.prototype.keyIndex = null;
        ADVDeviceIdentity.prototype.accountType = null;
        ADVDeviceIdentity.prototype.deviceType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVDeviceIdentity.prototype, "_rawId", {
            get: $util.oneOfGetter($oneOfFields = ["rawId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVDeviceIdentity.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVDeviceIdentity.prototype, "_keyIndex", {
            get: $util.oneOfGetter($oneOfFields = ["keyIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVDeviceIdentity.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVDeviceIdentity.prototype, "_deviceType", {
            get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVDeviceIdentity.create = function(properties) {
            return new ADVDeviceIdentity(properties);
        };

        ADVDeviceIdentity.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.rawId != null && $Object.hasOwnProperty.call(m, "rawId"))
                w.uint32(8).uint32(m.rawId);
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).uint64(m.timestamp);
            if (m.keyIndex != null && $Object.hasOwnProperty.call(m, "keyIndex"))
                w.uint32(24).uint32(m.keyIndex);
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(32).int32(m.accountType);
            if (m.deviceType != null && $Object.hasOwnProperty.call(m, "deviceType"))
                w.uint32(40).int32(m.deviceType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ADVDeviceIdentity.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ADVDeviceIdentity(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.rawId = r.uint32();
                        m._rawId = "rawId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.timestamp = r.uint64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.keyIndex = r.uint32();
                        m._keyIndex = "keyIndex";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.accountType = r.int32();
                        m._accountType = "accountType";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.deviceType = r.int32();
                        m._deviceType = "deviceType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ADVDeviceIdentity.fromObject = function (d, q) {
            if (d instanceof $root.proto.ADVDeviceIdentity)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ADVDeviceIdentity: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ADVDeviceIdentity();
            if (d.rawId != null) {
                m.rawId = d.rawId >>> 0;
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, true);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            if (d.keyIndex != null) {
                m.keyIndex = d.keyIndex >>> 0;
            }
            switch (d.accountType) {
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            default:
                if (typeof d.accountType === "number" && (d.accountType | 0) === d.accountType)
                    m.accountType = d.accountType;
            }
            switch (d.deviceType) {
            case "E2EE":
            case 0:
                m.deviceType = 0;
                break;
            case "HOSTED":
            case 1:
                m.deviceType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.deviceType = 2;
                break;
            default:
                if (typeof d.deviceType === "number" && (d.deviceType | 0) === d.deviceType)
                    m.deviceType = d.deviceType;
            }
            return m;
        };

        ADVDeviceIdentity.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.rawId != null && $Object.hasOwnProperty.call(m, "rawId")) {
                d.rawId = m.rawId;
            }
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, true).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
            }
            if (m.keyIndex != null && $Object.hasOwnProperty.call(m, "keyIndex")) {
                d.keyIndex = m.keyIndex;
            }
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType")) {
                d.accountType = o.enums === $String ? $root.proto.ADVEncryptionType[m.accountType] === $undefined ? m.accountType : $root.proto.ADVEncryptionType[m.accountType] : m.accountType;
            }
            if (m.deviceType != null && $Object.hasOwnProperty.call(m, "deviceType")) {
                d.deviceType = o.enums === $String ? $root.proto.ADVEncryptionType[m.deviceType] === $undefined ? m.deviceType : $root.proto.ADVEncryptionType[m.deviceType] : m.deviceType;
            }
            return d;
        };

        ADVDeviceIdentity.prototype.toJSON = function() {
            return ADVDeviceIdentity.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVDeviceIdentity.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ADVDeviceIdentity";
        };

        return ADVDeviceIdentity;
    })();

    proto.ADVEncryptionType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "E2EE"] = 0;
        values[valuesById[1] = "HOSTED"] = 1;
        values[valuesById[2] = "NON_E2EE"] = 2;
        return values;
    })();

    proto.ADVKeyIndexList = (function() {

        const ADVKeyIndexList = function (p) {
            this.validIndexes = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ADVKeyIndexList.prototype.rawId = null;
        ADVKeyIndexList.prototype.timestamp = null;
        ADVKeyIndexList.prototype.currentIndex = null;
        ADVKeyIndexList.prototype.validIndexes = $util.emptyArray;
        ADVKeyIndexList.prototype.accountType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVKeyIndexList.prototype, "_rawId", {
            get: $util.oneOfGetter($oneOfFields = ["rawId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVKeyIndexList.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVKeyIndexList.prototype, "_currentIndex", {
            get: $util.oneOfGetter($oneOfFields = ["currentIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVKeyIndexList.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVKeyIndexList.create = function(properties) {
            return new ADVKeyIndexList(properties);
        };

        ADVKeyIndexList.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.rawId != null && $Object.hasOwnProperty.call(m, "rawId"))
                w.uint32(8).uint32(m.rawId);
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).uint64(m.timestamp);
            if (m.currentIndex != null && $Object.hasOwnProperty.call(m, "currentIndex"))
                w.uint32(24).uint32(m.currentIndex);
            if (m.validIndexes != null && m.validIndexes.length) {
                w.uint32(34).uint32s(m.validIndexes);
            }
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(40).int32(m.accountType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ADVKeyIndexList.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ADVKeyIndexList(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.rawId = r.uint32();
                        m._rawId = "rawId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.timestamp = r.uint64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.currentIndex = r.uint32();
                        m._currentIndex = "currentIndex";
                        continue;
                    }
                case 4: {
                        if (u === 2) {
                            if (!(m.validIndexes && m.validIndexes.length))
                                m.validIndexes = [];
                            r.uint32s(m.validIndexes);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.validIndexes && m.validIndexes.length))
                            m.validIndexes = [];
                        m.validIndexes.push(r.uint32());
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.accountType = r.int32();
                        m._accountType = "accountType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ADVKeyIndexList.fromObject = function (d, q) {
            if (d instanceof $root.proto.ADVKeyIndexList)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ADVKeyIndexList: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ADVKeyIndexList();
            if (d.rawId != null) {
                m.rawId = d.rawId >>> 0;
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, true);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            if (d.currentIndex != null) {
                m.currentIndex = d.currentIndex >>> 0;
            }
            if (d.validIndexes) {
                if (!$Array.isArray(d.validIndexes))
                    throw $TypeError(".proto.ADVKeyIndexList.validIndexes: array expected");
                m.validIndexes = $Array(d.validIndexes.length);
                for (var i = 0; i < d.validIndexes.length; ++i) {
                    m.validIndexes[i] = d.validIndexes[i] >>> 0;
                }
            }
            switch (d.accountType) {
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            default:
                if (typeof d.accountType === "number" && (d.accountType | 0) === d.accountType)
                    m.accountType = d.accountType;
            }
            return m;
        };

        ADVKeyIndexList.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.validIndexes = [];
            }
            if (m.rawId != null && $Object.hasOwnProperty.call(m, "rawId")) {
                d.rawId = m.rawId;
            }
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, true).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
            }
            if (m.currentIndex != null && $Object.hasOwnProperty.call(m, "currentIndex")) {
                d.currentIndex = m.currentIndex;
            }
            if (m.validIndexes && m.validIndexes.length) {
                d.validIndexes = $Array(m.validIndexes.length);
                for (var j = 0; j < m.validIndexes.length; ++j) {
                    d.validIndexes[j] = m.validIndexes[j];
                }
            }
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType")) {
                d.accountType = o.enums === $String ? $root.proto.ADVEncryptionType[m.accountType] === $undefined ? m.accountType : $root.proto.ADVEncryptionType[m.accountType] : m.accountType;
            }
            return d;
        };

        ADVKeyIndexList.prototype.toJSON = function() {
            return ADVKeyIndexList.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVKeyIndexList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ADVKeyIndexList";
        };

        return ADVKeyIndexList;
    })();

    proto.ADVSignedDeviceIdentity = (function() {

        const ADVSignedDeviceIdentity = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ADVSignedDeviceIdentity.prototype.details = null;
        ADVSignedDeviceIdentity.prototype.accountSignatureKey = null;
        ADVSignedDeviceIdentity.prototype.accountSignature = null;
        ADVSignedDeviceIdentity.prototype.deviceSignature = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_accountSignatureKey", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignatureKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_accountSignature", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentity.prototype, "_deviceSignature", {
            get: $util.oneOfGetter($oneOfFields = ["deviceSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVSignedDeviceIdentity.create = function(properties) {
            return new ADVSignedDeviceIdentity(properties);
        };

        ADVSignedDeviceIdentity.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.details != null && $Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.accountSignatureKey != null && $Object.hasOwnProperty.call(m, "accountSignatureKey"))
                w.uint32(18).bytes(m.accountSignatureKey);
            if (m.accountSignature != null && $Object.hasOwnProperty.call(m, "accountSignature"))
                w.uint32(26).bytes(m.accountSignature);
            if (m.deviceSignature != null && $Object.hasOwnProperty.call(m, "deviceSignature"))
                w.uint32(34).bytes(m.deviceSignature);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ADVSignedDeviceIdentity.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ADVSignedDeviceIdentity();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.details = r.bytes();
                        m._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.accountSignatureKey = r.bytes();
                        m._accountSignatureKey = "accountSignatureKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.accountSignature = r.bytes();
                        m._accountSignature = "accountSignature";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.deviceSignature = r.bytes();
                        m._deviceSignature = "deviceSignature";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ADVSignedDeviceIdentity.fromObject = function (d, q) {
            if (d instanceof $root.proto.ADVSignedDeviceIdentity)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ADVSignedDeviceIdentity: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ADVSignedDeviceIdentity();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.accountSignatureKey != null) {
                if (typeof d.accountSignatureKey === "string")
                    $util.base64.decode(d.accountSignatureKey, m.accountSignatureKey = $util.newBuffer($util.base64.length(d.accountSignatureKey)), 0);
                else if (d.accountSignatureKey.length >= 0)
                    m.accountSignatureKey = d.accountSignatureKey;
            }
            if (d.accountSignature != null) {
                if (typeof d.accountSignature === "string")
                    $util.base64.decode(d.accountSignature, m.accountSignature = $util.newBuffer($util.base64.length(d.accountSignature)), 0);
                else if (d.accountSignature.length >= 0)
                    m.accountSignature = d.accountSignature;
            }
            if (d.deviceSignature != null) {
                if (typeof d.deviceSignature === "string")
                    $util.base64.decode(d.deviceSignature, m.deviceSignature = $util.newBuffer($util.base64.length(d.deviceSignature)), 0);
                else if (d.deviceSignature.length >= 0)
                    m.deviceSignature = d.deviceSignature;
            }
            return m;
        };

        ADVSignedDeviceIdentity.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.details != null && $Object.hasOwnProperty.call(m, "details")) {
                d.details = o.bytes === $String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.details) : m.details;
            }
            if (m.accountSignatureKey != null && $Object.hasOwnProperty.call(m, "accountSignatureKey")) {
                d.accountSignatureKey = o.bytes === $String ? $util.base64.encode(m.accountSignatureKey, 0, m.accountSignatureKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.accountSignatureKey) : m.accountSignatureKey;
            }
            if (m.accountSignature != null && $Object.hasOwnProperty.call(m, "accountSignature")) {
                d.accountSignature = o.bytes === $String ? $util.base64.encode(m.accountSignature, 0, m.accountSignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.accountSignature) : m.accountSignature;
            }
            if (m.deviceSignature != null && $Object.hasOwnProperty.call(m, "deviceSignature")) {
                d.deviceSignature = o.bytes === $String ? $util.base64.encode(m.deviceSignature, 0, m.deviceSignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.deviceSignature) : m.deviceSignature;
            }
            return d;
        };

        ADVSignedDeviceIdentity.prototype.toJSON = function() {
            return ADVSignedDeviceIdentity.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVSignedDeviceIdentity.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ADVSignedDeviceIdentity";
        };

        return ADVSignedDeviceIdentity;
    })();

    proto.ADVSignedDeviceIdentityHMAC = (function() {

        const ADVSignedDeviceIdentityHMAC = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ADVSignedDeviceIdentityHMAC.prototype.details = null;
        ADVSignedDeviceIdentityHMAC.prototype.hmac = null;
        ADVSignedDeviceIdentityHMAC.prototype.accountType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_hmac", {
            get: $util.oneOfGetter($oneOfFields = ["hmac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedDeviceIdentityHMAC.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVSignedDeviceIdentityHMAC.create = function(properties) {
            return new ADVSignedDeviceIdentityHMAC(properties);
        };

        ADVSignedDeviceIdentityHMAC.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.details != null && $Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.hmac != null && $Object.hasOwnProperty.call(m, "hmac"))
                w.uint32(18).bytes(m.hmac);
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(24).int32(m.accountType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ADVSignedDeviceIdentityHMAC.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ADVSignedDeviceIdentityHMAC(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.details = r.bytes();
                        m._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.hmac = r.bytes();
                        m._hmac = "hmac";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.accountType = r.int32();
                        m._accountType = "accountType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ADVSignedDeviceIdentityHMAC.fromObject = function (d, q) {
            if (d instanceof $root.proto.ADVSignedDeviceIdentityHMAC)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ADVSignedDeviceIdentityHMAC: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ADVSignedDeviceIdentityHMAC();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.hmac != null) {
                if (typeof d.hmac === "string")
                    $util.base64.decode(d.hmac, m.hmac = $util.newBuffer($util.base64.length(d.hmac)), 0);
                else if (d.hmac.length >= 0)
                    m.hmac = d.hmac;
            }
            switch (d.accountType) {
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            default:
                if (typeof d.accountType === "number" && (d.accountType | 0) === d.accountType)
                    m.accountType = d.accountType;
            }
            return m;
        };

        ADVSignedDeviceIdentityHMAC.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.details != null && $Object.hasOwnProperty.call(m, "details")) {
                d.details = o.bytes === $String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.details) : m.details;
            }
            if (m.hmac != null && $Object.hasOwnProperty.call(m, "hmac")) {
                d.hmac = o.bytes === $String ? $util.base64.encode(m.hmac, 0, m.hmac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.hmac) : m.hmac;
            }
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType")) {
                d.accountType = o.enums === $String ? $root.proto.ADVEncryptionType[m.accountType] === $undefined ? m.accountType : $root.proto.ADVEncryptionType[m.accountType] : m.accountType;
            }
            return d;
        };

        ADVSignedDeviceIdentityHMAC.prototype.toJSON = function() {
            return ADVSignedDeviceIdentityHMAC.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVSignedDeviceIdentityHMAC.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ADVSignedDeviceIdentityHMAC";
        };

        return ADVSignedDeviceIdentityHMAC;
    })();

    proto.ADVSignedKeyIndexList = (function() {

        const ADVSignedKeyIndexList = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ADVSignedKeyIndexList.prototype.details = null;
        ADVSignedKeyIndexList.prototype.accountSignature = null;
        ADVSignedKeyIndexList.prototype.accountSignatureKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedKeyIndexList.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedKeyIndexList.prototype, "_accountSignature", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ADVSignedKeyIndexList.prototype, "_accountSignatureKey", {
            get: $util.oneOfGetter($oneOfFields = ["accountSignatureKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ADVSignedKeyIndexList.create = function(properties) {
            return new ADVSignedKeyIndexList(properties);
        };

        ADVSignedKeyIndexList.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.details != null && $Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.accountSignature != null && $Object.hasOwnProperty.call(m, "accountSignature"))
                w.uint32(18).bytes(m.accountSignature);
            if (m.accountSignatureKey != null && $Object.hasOwnProperty.call(m, "accountSignatureKey"))
                w.uint32(26).bytes(m.accountSignatureKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ADVSignedKeyIndexList.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ADVSignedKeyIndexList();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.details = r.bytes();
                        m._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.accountSignature = r.bytes();
                        m._accountSignature = "accountSignature";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.accountSignatureKey = r.bytes();
                        m._accountSignatureKey = "accountSignatureKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ADVSignedKeyIndexList.fromObject = function (d, q) {
            if (d instanceof $root.proto.ADVSignedKeyIndexList)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ADVSignedKeyIndexList: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ADVSignedKeyIndexList();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.accountSignature != null) {
                if (typeof d.accountSignature === "string")
                    $util.base64.decode(d.accountSignature, m.accountSignature = $util.newBuffer($util.base64.length(d.accountSignature)), 0);
                else if (d.accountSignature.length >= 0)
                    m.accountSignature = d.accountSignature;
            }
            if (d.accountSignatureKey != null) {
                if (typeof d.accountSignatureKey === "string")
                    $util.base64.decode(d.accountSignatureKey, m.accountSignatureKey = $util.newBuffer($util.base64.length(d.accountSignatureKey)), 0);
                else if (d.accountSignatureKey.length >= 0)
                    m.accountSignatureKey = d.accountSignatureKey;
            }
            return m;
        };

        ADVSignedKeyIndexList.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.details != null && $Object.hasOwnProperty.call(m, "details")) {
                d.details = o.bytes === $String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.details) : m.details;
            }
            if (m.accountSignature != null && $Object.hasOwnProperty.call(m, "accountSignature")) {
                d.accountSignature = o.bytes === $String ? $util.base64.encode(m.accountSignature, 0, m.accountSignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.accountSignature) : m.accountSignature;
            }
            if (m.accountSignatureKey != null && $Object.hasOwnProperty.call(m, "accountSignatureKey")) {
                d.accountSignatureKey = o.bytes === $String ? $util.base64.encode(m.accountSignatureKey, 0, m.accountSignatureKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.accountSignatureKey) : m.accountSignatureKey;
            }
            return d;
        };

        ADVSignedKeyIndexList.prototype.toJSON = function() {
            return ADVSignedKeyIndexList.toObject(this, $protobuf.util.toJSONOptions);
        };

        ADVSignedKeyIndexList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ADVSignedKeyIndexList";
        };

        return ADVSignedKeyIndexList;
    })();

    proto.AIHomeState = (function() {

        const AIHomeState = function (p) {
            this.capabilityOptions = [];
            this.conversationOptions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIHomeState.prototype.lastFetchTime = null;
        AIHomeState.prototype.capabilityOptions = $util.emptyArray;
        AIHomeState.prototype.conversationOptions = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIHomeState.prototype, "_lastFetchTime", {
            get: $util.oneOfGetter($oneOfFields = ["lastFetchTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIHomeState.create = function(properties) {
            return new AIHomeState(properties);
        };

        AIHomeState.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.lastFetchTime != null && $Object.hasOwnProperty.call(m, "lastFetchTime"))
                w.uint32(8).int64(m.lastFetchTime);
            if (m.capabilityOptions != null && m.capabilityOptions.length) {
                for (var i = 0; i < m.capabilityOptions.length; ++i)
                    $root.proto.AIHomeState.AIHomeOption.encode(m.capabilityOptions[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.conversationOptions != null && m.conversationOptions.length) {
                for (var i = 0; i < m.conversationOptions.length; ++i)
                    $root.proto.AIHomeState.AIHomeOption.encode(m.conversationOptions[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIHomeState.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIHomeState();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.lastFetchTime = r.int64();
                        m._lastFetchTime = "lastFetchTime";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.capabilityOptions && m.capabilityOptions.length))
                            m.capabilityOptions = [];
                        m.capabilityOptions.push($root.proto.AIHomeState.AIHomeOption.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.conversationOptions && m.conversationOptions.length))
                            m.conversationOptions = [];
                        m.conversationOptions.push($root.proto.AIHomeState.AIHomeOption.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIHomeState.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIHomeState)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIHomeState: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIHomeState();
            if (d.lastFetchTime != null) {
                if ($util.Long)
                    m.lastFetchTime = $util.Long.fromValue(d.lastFetchTime, false);
                else if (typeof d.lastFetchTime === "string")
                    m.lastFetchTime = $parseInt(d.lastFetchTime, 10);
                else if (typeof d.lastFetchTime === "number")
                    m.lastFetchTime = d.lastFetchTime;
                else if (typeof d.lastFetchTime === "object")
                    m.lastFetchTime = new $util.LongBits(d.lastFetchTime.low >>> 0, d.lastFetchTime.high >>> 0).toNumber();
            }
            if (d.capabilityOptions) {
                if (!$Array.isArray(d.capabilityOptions))
                    throw $TypeError(".proto.AIHomeState.capabilityOptions: array expected");
                m.capabilityOptions = $Array(d.capabilityOptions.length);
                for (var i = 0; i < d.capabilityOptions.length; ++i) {
                    if (!$util.isObject(d.capabilityOptions[i]))
                        throw $TypeError(".proto.AIHomeState.capabilityOptions: object expected");
                    m.capabilityOptions[i] = $root.proto.AIHomeState.AIHomeOption.fromObject(d.capabilityOptions[i], q + 1);
                }
            }
            if (d.conversationOptions) {
                if (!$Array.isArray(d.conversationOptions))
                    throw $TypeError(".proto.AIHomeState.conversationOptions: array expected");
                m.conversationOptions = $Array(d.conversationOptions.length);
                for (var i = 0; i < d.conversationOptions.length; ++i) {
                    if (!$util.isObject(d.conversationOptions[i]))
                        throw $TypeError(".proto.AIHomeState.conversationOptions: object expected");
                    m.conversationOptions[i] = $root.proto.AIHomeState.AIHomeOption.fromObject(d.conversationOptions[i], q + 1);
                }
            }
            return m;
        };

        AIHomeState.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.capabilityOptions = [];
                d.conversationOptions = [];
            }
            if (m.lastFetchTime != null && $Object.hasOwnProperty.call(m, "lastFetchTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.lastFetchTime = typeof m.lastFetchTime === "number" ? $BigInt(m.lastFetchTime) : $util.Long.fromBits(m.lastFetchTime.low >>> 0, m.lastFetchTime.high >>> 0, false).toBigInt();
                else if (typeof m.lastFetchTime === "number")
                    d.lastFetchTime = o.longs === $String ? $String(m.lastFetchTime) : m.lastFetchTime;
                else
                    d.lastFetchTime = o.longs === $String ? $util.Long.prototype.toString.call(m.lastFetchTime) : o.longs === $Number ? new $util.LongBits(m.lastFetchTime.low >>> 0, m.lastFetchTime.high >>> 0).toNumber() : m.lastFetchTime;
            }
            if (m.capabilityOptions && m.capabilityOptions.length) {
                d.capabilityOptions = $Array(m.capabilityOptions.length);
                for (var j = 0; j < m.capabilityOptions.length; ++j) {
                    d.capabilityOptions[j] = $root.proto.AIHomeState.AIHomeOption.toObject(m.capabilityOptions[j], o, q + 1);
                }
            }
            if (m.conversationOptions && m.conversationOptions.length) {
                d.conversationOptions = $Array(m.conversationOptions.length);
                for (var j = 0; j < m.conversationOptions.length; ++j) {
                    d.conversationOptions[j] = $root.proto.AIHomeState.AIHomeOption.toObject(m.conversationOptions[j], o, q + 1);
                }
            }
            return d;
        };

        AIHomeState.prototype.toJSON = function() {
            return AIHomeState.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIHomeState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIHomeState";
        };

        AIHomeState.AIHomeOption = (function() {

            const AIHomeOption = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIHomeOption.prototype.type = null;
            AIHomeOption.prototype.title = null;
            AIHomeOption.prototype.promptText = null;
            AIHomeOption.prototype.sessionId = null;
            AIHomeOption.prototype.imageWdsIdentifier = null;
            AIHomeOption.prototype.imageTintColor = null;
            AIHomeOption.prototype.imageBackgroundColor = null;
            AIHomeOption.prototype.cardTypeId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_promptText", {
                get: $util.oneOfGetter($oneOfFields = ["promptText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_sessionId", {
                get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_imageWdsIdentifier", {
                get: $util.oneOfGetter($oneOfFields = ["imageWdsIdentifier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_imageTintColor", {
                get: $util.oneOfGetter($oneOfFields = ["imageTintColor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_imageBackgroundColor", {
                get: $util.oneOfGetter($oneOfFields = ["imageBackgroundColor"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIHomeOption.prototype, "_cardTypeId", {
                get: $util.oneOfGetter($oneOfFields = ["cardTypeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIHomeOption.create = function(properties) {
                return new AIHomeOption(properties);
            };

            AIHomeOption.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(18).string(m.title);
                if (m.promptText != null && $Object.hasOwnProperty.call(m, "promptText"))
                    w.uint32(26).string(m.promptText);
                if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId"))
                    w.uint32(34).string(m.sessionId);
                if (m.imageWdsIdentifier != null && $Object.hasOwnProperty.call(m, "imageWdsIdentifier"))
                    w.uint32(42).string(m.imageWdsIdentifier);
                if (m.imageTintColor != null && $Object.hasOwnProperty.call(m, "imageTintColor"))
                    w.uint32(50).string(m.imageTintColor);
                if (m.imageBackgroundColor != null && $Object.hasOwnProperty.call(m, "imageBackgroundColor"))
                    w.uint32(58).string(m.imageBackgroundColor);
                if (m.cardTypeId != null && $Object.hasOwnProperty.call(m, "cardTypeId"))
                    w.uint32(66).string(m.cardTypeId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIHomeOption.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIHomeState.AIHomeOption(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.promptText = r.stringVerify();
                            m._promptText = "promptText";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.sessionId = r.stringVerify();
                            m._sessionId = "sessionId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.imageWdsIdentifier = r.stringVerify();
                            m._imageWdsIdentifier = "imageWdsIdentifier";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.imageTintColor = r.stringVerify();
                            m._imageTintColor = "imageTintColor";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.imageBackgroundColor = r.stringVerify();
                            m._imageBackgroundColor = "imageBackgroundColor";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.cardTypeId = r.stringVerify();
                            m._cardTypeId = "cardTypeId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIHomeOption.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIHomeState.AIHomeOption)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIHomeState.AIHomeOption: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIHomeState.AIHomeOption();
                switch (d.type) {
                case "PROMPT":
                case 0:
                    m.type = 0;
                    break;
                case "CREATE_IMAGE":
                case 1:
                    m.type = 1;
                    break;
                case "ANIMATE_PHOTO":
                case 2:
                    m.type = 2;
                    break;
                case "ANALYZE_FILE":
                case 3:
                    m.type = 3;
                    break;
                case "COLLABORATE":
                case 4:
                    m.type = 4;
                    break;
                case "OPEN_GREETING_CARD":
                case 5:
                    m.type = 5;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.promptText != null) {
                    m.promptText = $String(d.promptText);
                }
                if (d.sessionId != null) {
                    m.sessionId = $String(d.sessionId);
                }
                if (d.imageWdsIdentifier != null) {
                    m.imageWdsIdentifier = $String(d.imageWdsIdentifier);
                }
                if (d.imageTintColor != null) {
                    m.imageTintColor = $String(d.imageTintColor);
                }
                if (d.imageBackgroundColor != null) {
                    m.imageBackgroundColor = $String(d.imageBackgroundColor);
                }
                if (d.cardTypeId != null) {
                    m.cardTypeId = $String(d.cardTypeId);
                }
                return m;
            };

            AIHomeOption.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.AIHomeState.AIHomeOption.AIHomeActionType[m.type] === $undefined ? m.type : $root.proto.AIHomeState.AIHomeOption.AIHomeActionType[m.type] : m.type;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.promptText != null && $Object.hasOwnProperty.call(m, "promptText")) {
                    d.promptText = m.promptText;
                }
                if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId")) {
                    d.sessionId = m.sessionId;
                }
                if (m.imageWdsIdentifier != null && $Object.hasOwnProperty.call(m, "imageWdsIdentifier")) {
                    d.imageWdsIdentifier = m.imageWdsIdentifier;
                }
                if (m.imageTintColor != null && $Object.hasOwnProperty.call(m, "imageTintColor")) {
                    d.imageTintColor = m.imageTintColor;
                }
                if (m.imageBackgroundColor != null && $Object.hasOwnProperty.call(m, "imageBackgroundColor")) {
                    d.imageBackgroundColor = m.imageBackgroundColor;
                }
                if (m.cardTypeId != null && $Object.hasOwnProperty.call(m, "cardTypeId")) {
                    d.cardTypeId = m.cardTypeId;
                }
                return d;
            };

            AIHomeOption.prototype.toJSON = function() {
                return AIHomeOption.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIHomeOption.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIHomeState.AIHomeOption";
            };

            AIHomeOption.AIHomeActionType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "PROMPT"] = 0;
                values[valuesById[1] = "CREATE_IMAGE"] = 1;
                values[valuesById[2] = "ANIMATE_PHOTO"] = 2;
                values[valuesById[3] = "ANALYZE_FILE"] = 3;
                values[valuesById[4] = "COLLABORATE"] = 4;
                values[valuesById[5] = "OPEN_GREETING_CARD"] = 5;
                return values;
            })();

            return AIHomeOption;
        })();

        return AIHomeState;
    })();

    proto.AIMediaCollectionMessage = (function() {

        const AIMediaCollectionMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIMediaCollectionMessage.prototype.collectionId = null;
        AIMediaCollectionMessage.prototype.expectedMediaCount = null;
        AIMediaCollectionMessage.prototype.hasGlobalCaption = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIMediaCollectionMessage.prototype, "_collectionId", {
            get: $util.oneOfGetter($oneOfFields = ["collectionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIMediaCollectionMessage.prototype, "_expectedMediaCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedMediaCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIMediaCollectionMessage.prototype, "_hasGlobalCaption", {
            get: $util.oneOfGetter($oneOfFields = ["hasGlobalCaption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIMediaCollectionMessage.create = function(properties) {
            return new AIMediaCollectionMessage(properties);
        };

        AIMediaCollectionMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.collectionId != null && $Object.hasOwnProperty.call(m, "collectionId"))
                w.uint32(10).string(m.collectionId);
            if (m.expectedMediaCount != null && $Object.hasOwnProperty.call(m, "expectedMediaCount"))
                w.uint32(16).uint32(m.expectedMediaCount);
            if (m.hasGlobalCaption != null && $Object.hasOwnProperty.call(m, "hasGlobalCaption"))
                w.uint32(24).bool(m.hasGlobalCaption);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIMediaCollectionMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIMediaCollectionMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.collectionId = r.stringVerify();
                        m._collectionId = "collectionId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.expectedMediaCount = r.uint32();
                        m._expectedMediaCount = "expectedMediaCount";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.hasGlobalCaption = r.bool();
                        m._hasGlobalCaption = "hasGlobalCaption";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIMediaCollectionMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIMediaCollectionMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIMediaCollectionMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIMediaCollectionMessage();
            if (d.collectionId != null) {
                m.collectionId = $String(d.collectionId);
            }
            if (d.expectedMediaCount != null) {
                m.expectedMediaCount = d.expectedMediaCount >>> 0;
            }
            if (d.hasGlobalCaption != null) {
                m.hasGlobalCaption = $Boolean(d.hasGlobalCaption);
            }
            return m;
        };

        AIMediaCollectionMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.collectionId != null && $Object.hasOwnProperty.call(m, "collectionId")) {
                d.collectionId = m.collectionId;
            }
            if (m.expectedMediaCount != null && $Object.hasOwnProperty.call(m, "expectedMediaCount")) {
                d.expectedMediaCount = m.expectedMediaCount;
            }
            if (m.hasGlobalCaption != null && $Object.hasOwnProperty.call(m, "hasGlobalCaption")) {
                d.hasGlobalCaption = m.hasGlobalCaption;
            }
            return d;
        };

        AIMediaCollectionMessage.prototype.toJSON = function() {
            return AIMediaCollectionMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIMediaCollectionMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIMediaCollectionMessage";
        };

        return AIMediaCollectionMessage;
    })();

    proto.AIMediaCollectionMetadata = (function() {

        const AIMediaCollectionMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIMediaCollectionMetadata.prototype.collectionId = null;
        AIMediaCollectionMetadata.prototype.uploadOrderIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIMediaCollectionMetadata.prototype, "_collectionId", {
            get: $util.oneOfGetter($oneOfFields = ["collectionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIMediaCollectionMetadata.prototype, "_uploadOrderIndex", {
            get: $util.oneOfGetter($oneOfFields = ["uploadOrderIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIMediaCollectionMetadata.create = function(properties) {
            return new AIMediaCollectionMetadata(properties);
        };

        AIMediaCollectionMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.collectionId != null && $Object.hasOwnProperty.call(m, "collectionId"))
                w.uint32(10).string(m.collectionId);
            if (m.uploadOrderIndex != null && $Object.hasOwnProperty.call(m, "uploadOrderIndex"))
                w.uint32(16).uint32(m.uploadOrderIndex);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIMediaCollectionMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIMediaCollectionMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.collectionId = r.stringVerify();
                        m._collectionId = "collectionId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.uploadOrderIndex = r.uint32();
                        m._uploadOrderIndex = "uploadOrderIndex";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIMediaCollectionMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIMediaCollectionMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIMediaCollectionMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIMediaCollectionMetadata();
            if (d.collectionId != null) {
                m.collectionId = $String(d.collectionId);
            }
            if (d.uploadOrderIndex != null) {
                m.uploadOrderIndex = d.uploadOrderIndex >>> 0;
            }
            return m;
        };

        AIMediaCollectionMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.collectionId != null && $Object.hasOwnProperty.call(m, "collectionId")) {
                d.collectionId = m.collectionId;
            }
            if (m.uploadOrderIndex != null && $Object.hasOwnProperty.call(m, "uploadOrderIndex")) {
                d.uploadOrderIndex = m.uploadOrderIndex;
            }
            return d;
        };

        AIMediaCollectionMetadata.prototype.toJSON = function() {
            return AIMediaCollectionMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIMediaCollectionMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIMediaCollectionMetadata";
        };

        return AIMediaCollectionMetadata;
    })();

    proto.AIMetadataOperation = (function() {

        const AIMetadataOperation = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIMetadataOperation.prototype.hatchMetadataSync = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIMetadataOperation.prototype, "_hatchMetadataSync", {
            get: $util.oneOfGetter($oneOfFields = ["hatchMetadataSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIMetadataOperation.create = function(properties) {
            return new AIMetadataOperation(properties);
        };

        AIMetadataOperation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.hatchMetadataSync != null && $Object.hasOwnProperty.call(m, "hatchMetadataSync"))
                $root.proto.HatchMetadataSync.encode(m.hatchMetadataSync, w.uint32(10).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIMetadataOperation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIMetadataOperation();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.hatchMetadataSync = $root.proto.HatchMetadataSync.decode(r, r.uint32(), $undefined, q + 1, m.hatchMetadataSync);
                        m._hatchMetadataSync = "hatchMetadataSync";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIMetadataOperation.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIMetadataOperation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIMetadataOperation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIMetadataOperation();
            if (d.hatchMetadataSync != null) {
                if (!$util.isObject(d.hatchMetadataSync))
                    throw $TypeError(".proto.AIMetadataOperation.hatchMetadataSync: object expected");
                m.hatchMetadataSync = $root.proto.HatchMetadataSync.fromObject(d.hatchMetadataSync, q + 1);
            }
            return m;
        };

        AIMetadataOperation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.hatchMetadataSync != null && $Object.hasOwnProperty.call(m, "hatchMetadataSync")) {
                d.hatchMetadataSync = $root.proto.HatchMetadataSync.toObject(m.hatchMetadataSync, o, q + 1);
            }
            return d;
        };

        AIMetadataOperation.prototype.toJSON = function() {
            return AIMetadataOperation.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIMetadataOperation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIMetadataOperation";
        };

        return AIMetadataOperation;
    })();

    proto.AIProvenance = (function() {

        const AIProvenance = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIProvenance.prototype.c2PaMetadata = null;
        AIProvenance.prototype.iptcMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIProvenance.prototype, "_c2PaMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["c2PaMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIProvenance.prototype, "_iptcMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["iptcMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIProvenance.create = function(properties) {
            return new AIProvenance(properties);
        };

        AIProvenance.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.c2PaMetadata != null && $Object.hasOwnProperty.call(m, "c2PaMetadata"))
                $root.proto.AIProvenance.Metadata.encode(m.c2PaMetadata, w.uint32(10).fork(), q + 1).ldelim();
            if (m.iptcMetadata != null && $Object.hasOwnProperty.call(m, "iptcMetadata"))
                $root.proto.AIProvenance.Metadata.encode(m.iptcMetadata, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIProvenance.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIProvenance();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.c2PaMetadata = $root.proto.AIProvenance.Metadata.decode(r, r.uint32(), $undefined, q + 1, m.c2PaMetadata);
                        m._c2PaMetadata = "c2PaMetadata";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.iptcMetadata = $root.proto.AIProvenance.Metadata.decode(r, r.uint32(), $undefined, q + 1, m.iptcMetadata);
                        m._iptcMetadata = "iptcMetadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIProvenance.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIProvenance)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIProvenance: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIProvenance();
            if (d.c2PaMetadata != null) {
                if (!$util.isObject(d.c2PaMetadata))
                    throw $TypeError(".proto.AIProvenance.c2PaMetadata: object expected");
                m.c2PaMetadata = $root.proto.AIProvenance.Metadata.fromObject(d.c2PaMetadata, q + 1);
            }
            if (d.iptcMetadata != null) {
                if (!$util.isObject(d.iptcMetadata))
                    throw $TypeError(".proto.AIProvenance.iptcMetadata: object expected");
                m.iptcMetadata = $root.proto.AIProvenance.Metadata.fromObject(d.iptcMetadata, q + 1);
            }
            return m;
        };

        AIProvenance.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.c2PaMetadata != null && $Object.hasOwnProperty.call(m, "c2PaMetadata")) {
                d.c2PaMetadata = $root.proto.AIProvenance.Metadata.toObject(m.c2PaMetadata, o, q + 1);
            }
            if (m.iptcMetadata != null && $Object.hasOwnProperty.call(m, "iptcMetadata")) {
                d.iptcMetadata = $root.proto.AIProvenance.Metadata.toObject(m.iptcMetadata, o, q + 1);
            }
            return d;
        };

        AIProvenance.prototype.toJSON = function() {
            return AIProvenance.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIProvenance.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIProvenance";
        };

        AIProvenance.Metadata = (function() {

            const Metadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Metadata.prototype.createdWithGenAi = null;
            Metadata.prototype.editedWithGenAi = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Metadata.prototype, "_createdWithGenAi", {
                get: $util.oneOfGetter($oneOfFields = ["createdWithGenAi"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Metadata.prototype, "_editedWithGenAi", {
                get: $util.oneOfGetter($oneOfFields = ["editedWithGenAi"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Metadata.create = function(properties) {
                return new Metadata(properties);
            };

            Metadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.createdWithGenAi != null && $Object.hasOwnProperty.call(m, "createdWithGenAi"))
                    w.uint32(8).bool(m.createdWithGenAi);
                if (m.editedWithGenAi != null && $Object.hasOwnProperty.call(m, "editedWithGenAi"))
                    w.uint32(16).bool(m.editedWithGenAi);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Metadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIProvenance.Metadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.createdWithGenAi = r.bool();
                            m._createdWithGenAi = "createdWithGenAi";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.editedWithGenAi = r.bool();
                            m._editedWithGenAi = "editedWithGenAi";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Metadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIProvenance.Metadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIProvenance.Metadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIProvenance.Metadata();
                if (d.createdWithGenAi != null) {
                    m.createdWithGenAi = $Boolean(d.createdWithGenAi);
                }
                if (d.editedWithGenAi != null) {
                    m.editedWithGenAi = $Boolean(d.editedWithGenAi);
                }
                return m;
            };

            Metadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.createdWithGenAi != null && $Object.hasOwnProperty.call(m, "createdWithGenAi")) {
                    d.createdWithGenAi = m.createdWithGenAi;
                }
                if (m.editedWithGenAi != null && $Object.hasOwnProperty.call(m, "editedWithGenAi")) {
                    d.editedWithGenAi = m.editedWithGenAi;
                }
                return d;
            };

            Metadata.prototype.toJSON = function() {
                return Metadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            Metadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIProvenance.Metadata";
            };

            return Metadata;
        })();

        return AIProvenance;
    })();

    proto.AIQueryFanout = (function() {

        const AIQueryFanout = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIQueryFanout.prototype.messageKey = null;
        AIQueryFanout.prototype.message = null;
        AIQueryFanout.prototype.timestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIQueryFanout.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIQueryFanout.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIQueryFanout.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIQueryFanout.create = function(properties) {
            return new AIQueryFanout(properties);
        };

        AIQueryFanout.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey"))
                $root.proto.MessageKey.encode(m.messageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                $root.proto.Message.encode(m.message, w.uint32(18).fork(), q + 1).ldelim();
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(24).int64(m.timestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIQueryFanout.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIQueryFanout();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.messageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.messageKey);
                        m._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                        m._message = "message";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.timestamp = r.int64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIQueryFanout.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIQueryFanout)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIQueryFanout: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIQueryFanout();
            if (d.messageKey != null) {
                if (!$util.isObject(d.messageKey))
                    throw $TypeError(".proto.AIQueryFanout.messageKey: object expected");
                m.messageKey = $root.proto.MessageKey.fromObject(d.messageKey, q + 1);
            }
            if (d.message != null) {
                if (!$util.isObject(d.message))
                    throw $TypeError(".proto.AIQueryFanout.message: object expected");
                m.message = $root.proto.Message.fromObject(d.message, q + 1);
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, false);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
            }
            return m;
        };

        AIQueryFanout.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey")) {
                d.messageKey = $root.proto.MessageKey.toObject(m.messageKey, o, q + 1);
            }
            if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                d.message = $root.proto.Message.toObject(m.message, o, q + 1);
            }
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
            }
            return d;
        };

        AIQueryFanout.prototype.toJSON = function() {
            return AIQueryFanout.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIQueryFanout.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIQueryFanout";
        };

        return AIQueryFanout;
    })();

    proto.AIRegenerateMetadata = (function() {

        const AIRegenerateMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRegenerateMetadata.prototype.messageKey = null;
        AIRegenerateMetadata.prototype.responseTimestampMs = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRegenerateMetadata.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRegenerateMetadata.prototype, "_responseTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["responseTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRegenerateMetadata.create = function(properties) {
            return new AIRegenerateMetadata(properties);
        };

        AIRegenerateMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey"))
                $root.proto.MessageKey.encode(m.messageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.responseTimestampMs != null && $Object.hasOwnProperty.call(m, "responseTimestampMs"))
                w.uint32(16).int64(m.responseTimestampMs);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRegenerateMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRegenerateMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.messageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.messageKey);
                        m._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.responseTimestampMs = r.int64();
                        m._responseTimestampMs = "responseTimestampMs";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRegenerateMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRegenerateMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRegenerateMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRegenerateMetadata();
            if (d.messageKey != null) {
                if (!$util.isObject(d.messageKey))
                    throw $TypeError(".proto.AIRegenerateMetadata.messageKey: object expected");
                m.messageKey = $root.proto.MessageKey.fromObject(d.messageKey, q + 1);
            }
            if (d.responseTimestampMs != null) {
                if ($util.Long)
                    m.responseTimestampMs = $util.Long.fromValue(d.responseTimestampMs, false);
                else if (typeof d.responseTimestampMs === "string")
                    m.responseTimestampMs = $parseInt(d.responseTimestampMs, 10);
                else if (typeof d.responseTimestampMs === "number")
                    m.responseTimestampMs = d.responseTimestampMs;
                else if (typeof d.responseTimestampMs === "object")
                    m.responseTimestampMs = new $util.LongBits(d.responseTimestampMs.low >>> 0, d.responseTimestampMs.high >>> 0).toNumber();
            }
            return m;
        };

        AIRegenerateMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey")) {
                d.messageKey = $root.proto.MessageKey.toObject(m.messageKey, o, q + 1);
            }
            if (m.responseTimestampMs != null && $Object.hasOwnProperty.call(m, "responseTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.responseTimestampMs = typeof m.responseTimestampMs === "number" ? $BigInt(m.responseTimestampMs) : $util.Long.fromBits(m.responseTimestampMs.low >>> 0, m.responseTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.responseTimestampMs === "number")
                    d.responseTimestampMs = o.longs === $String ? $String(m.responseTimestampMs) : m.responseTimestampMs;
                else
                    d.responseTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.responseTimestampMs) : o.longs === $Number ? new $util.LongBits(m.responseTimestampMs.low >>> 0, m.responseTimestampMs.high >>> 0).toNumber() : m.responseTimestampMs;
            }
            return d;
        };

        AIRegenerateMetadata.prototype.toJSON = function() {
            return AIRegenerateMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRegenerateMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRegenerateMetadata";
        };

        return AIRegenerateMetadata;
    })();

    proto.AIRichResponseCodeMetadata = (function() {

        const AIRichResponseCodeMetadata = function (p) {
            this.codeBlocks = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseCodeMetadata.prototype.codeLanguage = null;
        AIRichResponseCodeMetadata.prototype.codeBlocks = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseCodeMetadata.prototype, "_codeLanguage", {
            get: $util.oneOfGetter($oneOfFields = ["codeLanguage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseCodeMetadata.create = function(properties) {
            return new AIRichResponseCodeMetadata(properties);
        };

        AIRichResponseCodeMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.codeLanguage != null && $Object.hasOwnProperty.call(m, "codeLanguage"))
                w.uint32(10).string(m.codeLanguage);
            if (m.codeBlocks != null && m.codeBlocks.length) {
                for (var i = 0; i < m.codeBlocks.length; ++i)
                    $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.encode(m.codeBlocks[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseCodeMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseCodeMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.codeLanguage = r.stringVerify();
                        m._codeLanguage = "codeLanguage";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.codeBlocks && m.codeBlocks.length))
                            m.codeBlocks = [];
                        m.codeBlocks.push($root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseCodeMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseCodeMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseCodeMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseCodeMetadata();
            if (d.codeLanguage != null) {
                m.codeLanguage = $String(d.codeLanguage);
            }
            if (d.codeBlocks) {
                if (!$Array.isArray(d.codeBlocks))
                    throw $TypeError(".proto.AIRichResponseCodeMetadata.codeBlocks: array expected");
                m.codeBlocks = $Array(d.codeBlocks.length);
                for (var i = 0; i < d.codeBlocks.length; ++i) {
                    if (!$util.isObject(d.codeBlocks[i]))
                        throw $TypeError(".proto.AIRichResponseCodeMetadata.codeBlocks: object expected");
                    m.codeBlocks[i] = $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.fromObject(d.codeBlocks[i], q + 1);
                }
            }
            return m;
        };

        AIRichResponseCodeMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.codeBlocks = [];
            }
            if (m.codeLanguage != null && $Object.hasOwnProperty.call(m, "codeLanguage")) {
                d.codeLanguage = m.codeLanguage;
            }
            if (m.codeBlocks && m.codeBlocks.length) {
                d.codeBlocks = $Array(m.codeBlocks.length);
                for (var j = 0; j < m.codeBlocks.length; ++j) {
                    d.codeBlocks[j] = $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock.toObject(m.codeBlocks[j], o, q + 1);
                }
            }
            return d;
        };

        AIRichResponseCodeMetadata.prototype.toJSON = function() {
            return AIRichResponseCodeMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseCodeMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseCodeMetadata";
        };

        AIRichResponseCodeMetadata.AIRichResponseCodeBlock = (function() {

            const AIRichResponseCodeBlock = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIRichResponseCodeBlock.prototype.highlightType = null;
            AIRichResponseCodeBlock.prototype.codeContent = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseCodeBlock.prototype, "_highlightType", {
                get: $util.oneOfGetter($oneOfFields = ["highlightType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseCodeBlock.prototype, "_codeContent", {
                get: $util.oneOfGetter($oneOfFields = ["codeContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseCodeBlock.create = function(properties) {
                return new AIRichResponseCodeBlock(properties);
            };

            AIRichResponseCodeBlock.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.highlightType != null && $Object.hasOwnProperty.call(m, "highlightType"))
                    w.uint32(8).int32(m.highlightType);
                if (m.codeContent != null && $Object.hasOwnProperty.call(m, "codeContent"))
                    w.uint32(18).string(m.codeContent);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIRichResponseCodeBlock.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.highlightType = r.int32();
                            m._highlightType = "highlightType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.codeContent = r.stringVerify();
                            m._codeContent = "codeContent";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIRichResponseCodeBlock.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock();
                switch (d.highlightType) {
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT":
                case 0:
                    m.highlightType = 0;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD":
                case 1:
                    m.highlightType = 1;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD":
                case 2:
                    m.highlightType = 2;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING":
                case 3:
                    m.highlightType = 3;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER":
                case 4:
                    m.highlightType = 4;
                    break;
                case "AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT":
                case 5:
                    m.highlightType = 5;
                    break;
                default:
                    if (typeof d.highlightType === "number" && (d.highlightType | 0) === d.highlightType)
                        m.highlightType = d.highlightType;
                }
                if (d.codeContent != null) {
                    m.codeContent = $String(d.codeContent);
                }
                return m;
            };

            AIRichResponseCodeBlock.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.highlightType != null && $Object.hasOwnProperty.call(m, "highlightType")) {
                    d.highlightType = o.enums === $String ? $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType[m.highlightType] === $undefined ? m.highlightType : $root.proto.AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType[m.highlightType] : m.highlightType;
                }
                if (m.codeContent != null && $Object.hasOwnProperty.call(m, "codeContent")) {
                    d.codeContent = m.codeContent;
                }
                return d;
            };

            AIRichResponseCodeBlock.prototype.toJSON = function() {
                return AIRichResponseCodeBlock.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseCodeBlock.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIRichResponseCodeMetadata.AIRichResponseCodeBlock";
            };

            return AIRichResponseCodeBlock;
        })();

        AIRichResponseCodeMetadata.AIRichResponseCodeHighlightType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_DEFAULT"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_KEYWORD"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_METHOD"] = 2;
            values[valuesById[3] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_STRING"] = 3;
            values[valuesById[4] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_NUMBER"] = 4;
            values[valuesById[5] = "AI_RICH_RESPONSE_CODE_HIGHLIGHT_COMMENT"] = 5;
            return values;
        })();

        return AIRichResponseCodeMetadata;
    })();

    proto.AIRichResponseContentItemsMetadata = (function() {

        const AIRichResponseContentItemsMetadata = function (p) {
            this.itemsMetadata = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseContentItemsMetadata.prototype.itemsMetadata = $util.emptyArray;
        AIRichResponseContentItemsMetadata.prototype.contentType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseContentItemsMetadata.prototype, "_contentType", {
            get: $util.oneOfGetter($oneOfFields = ["contentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseContentItemsMetadata.create = function(properties) {
            return new AIRichResponseContentItemsMetadata(properties);
        };

        AIRichResponseContentItemsMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.itemsMetadata != null && m.itemsMetadata.length) {
                for (var i = 0; i < m.itemsMetadata.length; ++i)
                    $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.encode(m.itemsMetadata[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.contentType != null && $Object.hasOwnProperty.call(m, "contentType"))
                w.uint32(16).int32(m.contentType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseContentItemsMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseContentItemsMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.itemsMetadata && m.itemsMetadata.length))
                            m.itemsMetadata = [];
                        m.itemsMetadata.push($root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.contentType = r.int32();
                        m._contentType = "contentType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseContentItemsMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseContentItemsMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseContentItemsMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseContentItemsMetadata();
            if (d.itemsMetadata) {
                if (!$Array.isArray(d.itemsMetadata))
                    throw $TypeError(".proto.AIRichResponseContentItemsMetadata.itemsMetadata: array expected");
                m.itemsMetadata = $Array(d.itemsMetadata.length);
                for (var i = 0; i < d.itemsMetadata.length; ++i) {
                    if (!$util.isObject(d.itemsMetadata[i]))
                        throw $TypeError(".proto.AIRichResponseContentItemsMetadata.itemsMetadata: object expected");
                    m.itemsMetadata[i] = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.fromObject(d.itemsMetadata[i], q + 1);
                }
            }
            switch (d.contentType) {
            case "DEFAULT":
            case 0:
                m.contentType = 0;
                break;
            case "CAROUSEL":
            case 1:
                m.contentType = 1;
                break;
            default:
                if (typeof d.contentType === "number" && (d.contentType | 0) === d.contentType)
                    m.contentType = d.contentType;
            }
            return m;
        };

        AIRichResponseContentItemsMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.itemsMetadata = [];
            }
            if (m.itemsMetadata && m.itemsMetadata.length) {
                d.itemsMetadata = $Array(m.itemsMetadata.length);
                for (var j = 0; j < m.itemsMetadata.length; ++j) {
                    d.itemsMetadata[j] = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.toObject(m.itemsMetadata[j], o, q + 1);
                }
            }
            if (m.contentType != null && $Object.hasOwnProperty.call(m, "contentType")) {
                d.contentType = o.enums === $String ? $root.proto.AIRichResponseContentItemsMetadata.ContentType[m.contentType] === $undefined ? m.contentType : $root.proto.AIRichResponseContentItemsMetadata.ContentType[m.contentType] : m.contentType;
            }
            return d;
        };

        AIRichResponseContentItemsMetadata.prototype.toJSON = function() {
            return AIRichResponseContentItemsMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseContentItemsMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseContentItemsMetadata";
        };

        AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata = (function() {

            const AIRichResponseContentItemMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIRichResponseContentItemMetadata.prototype.reelItem = null;

            let $oneOfFields;

            $Object.defineProperty(AIRichResponseContentItemMetadata.prototype, "aiRichResponseContentItem", {
                get: $util.oneOfGetter($oneOfFields = ["reelItem"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseContentItemMetadata.create = function(properties) {
                return new AIRichResponseContentItemMetadata(properties);
            };

            AIRichResponseContentItemMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.reelItem != null && $Object.hasOwnProperty.call(m, "reelItem"))
                    $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.encode(m.reelItem, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIRichResponseContentItemMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.reelItem = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.decode(r, r.uint32(), $undefined, q + 1, m.reelItem);
                            m.aiRichResponseContentItem = "reelItem";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIRichResponseContentItemMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata();
                if (d.reelItem != null) {
                    if (!$util.isObject(d.reelItem))
                        throw $TypeError(".proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata.reelItem: object expected");
                    m.reelItem = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.fromObject(d.reelItem, q + 1);
                }
                return m;
            };

            AIRichResponseContentItemMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.reelItem != null && $Object.hasOwnProperty.call(m, "reelItem")) {
                    d.reelItem = $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem.toObject(m.reelItem, o, q + 1);
                    if (o.oneofs)
                        d.aiRichResponseContentItem = "reelItem";
                }
                return d;
            };

            AIRichResponseContentItemMetadata.prototype.toJSON = function() {
                return AIRichResponseContentItemMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseContentItemMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIRichResponseContentItemsMetadata.AIRichResponseContentItemMetadata";
            };

            return AIRichResponseContentItemMetadata;
        })();

        AIRichResponseContentItemsMetadata.AIRichResponseReelItem = (function() {

            const AIRichResponseReelItem = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIRichResponseReelItem.prototype.title = null;
            AIRichResponseReelItem.prototype.profileIconUrl = null;
            AIRichResponseReelItem.prototype.thumbnailUrl = null;
            AIRichResponseReelItem.prototype.videoUrl = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseReelItem.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseReelItem.prototype, "_profileIconUrl", {
                get: $util.oneOfGetter($oneOfFields = ["profileIconUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseReelItem.prototype, "_thumbnailUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseReelItem.prototype, "_videoUrl", {
                get: $util.oneOfGetter($oneOfFields = ["videoUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseReelItem.create = function(properties) {
                return new AIRichResponseReelItem(properties);
            };

            AIRichResponseReelItem.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.profileIconUrl != null && $Object.hasOwnProperty.call(m, "profileIconUrl"))
                    w.uint32(18).string(m.profileIconUrl);
                if (m.thumbnailUrl != null && $Object.hasOwnProperty.call(m, "thumbnailUrl"))
                    w.uint32(26).string(m.thumbnailUrl);
                if (m.videoUrl != null && $Object.hasOwnProperty.call(m, "videoUrl"))
                    w.uint32(34).string(m.videoUrl);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIRichResponseReelItem.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.profileIconUrl = r.stringVerify();
                            m._profileIconUrl = "profileIconUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.thumbnailUrl = r.stringVerify();
                            m._thumbnailUrl = "thumbnailUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.videoUrl = r.stringVerify();
                            m._videoUrl = "videoUrl";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIRichResponseReelItem.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem();
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.profileIconUrl != null) {
                    m.profileIconUrl = $String(d.profileIconUrl);
                }
                if (d.thumbnailUrl != null) {
                    m.thumbnailUrl = $String(d.thumbnailUrl);
                }
                if (d.videoUrl != null) {
                    m.videoUrl = $String(d.videoUrl);
                }
                return m;
            };

            AIRichResponseReelItem.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.profileIconUrl != null && $Object.hasOwnProperty.call(m, "profileIconUrl")) {
                    d.profileIconUrl = m.profileIconUrl;
                }
                if (m.thumbnailUrl != null && $Object.hasOwnProperty.call(m, "thumbnailUrl")) {
                    d.thumbnailUrl = m.thumbnailUrl;
                }
                if (m.videoUrl != null && $Object.hasOwnProperty.call(m, "videoUrl")) {
                    d.videoUrl = m.videoUrl;
                }
                return d;
            };

            AIRichResponseReelItem.prototype.toJSON = function() {
                return AIRichResponseReelItem.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseReelItem.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIRichResponseContentItemsMetadata.AIRichResponseReelItem";
            };

            return AIRichResponseReelItem;
        })();

        AIRichResponseContentItemsMetadata.ContentType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "CAROUSEL"] = 1;
            return values;
        })();

        return AIRichResponseContentItemsMetadata;
    })();

    proto.AIRichResponseDynamicMetadata = (function() {

        const AIRichResponseDynamicMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseDynamicMetadata.prototype.type = null;
        AIRichResponseDynamicMetadata.prototype.version = null;
        AIRichResponseDynamicMetadata.prototype.url = null;
        AIRichResponseDynamicMetadata.prototype.loopCount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_url", {
            get: $util.oneOfGetter($oneOfFields = ["url"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseDynamicMetadata.prototype, "_loopCount", {
            get: $util.oneOfGetter($oneOfFields = ["loopCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseDynamicMetadata.create = function(properties) {
            return new AIRichResponseDynamicMetadata(properties);
        };

        AIRichResponseDynamicMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(16).uint64(m.version);
            if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                w.uint32(26).string(m.url);
            if (m.loopCount != null && $Object.hasOwnProperty.call(m, "loopCount"))
                w.uint32(32).uint32(m.loopCount);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseDynamicMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseDynamicMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.type = r.int32();
                        m._type = "type";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.version = r.uint64();
                        m._version = "version";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.url = r.stringVerify();
                        m._url = "url";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.loopCount = r.uint32();
                        m._loopCount = "loopCount";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseDynamicMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseDynamicMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseDynamicMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseDynamicMetadata();
            switch (d.type) {
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE":
            case 1:
                m.type = 1;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF":
            case 2:
                m.type = 2;
                break;
            default:
                if (typeof d.type === "number" && (d.type | 0) === d.type)
                    m.type = d.type;
            }
            if (d.version != null) {
                if ($util.Long)
                    m.version = $util.Long.fromValue(d.version, true);
                else if (typeof d.version === "string")
                    m.version = $parseInt(d.version, 10);
                else if (typeof d.version === "number")
                    m.version = d.version;
                else if (typeof d.version === "object")
                    m.version = new $util.LongBits(d.version.low >>> 0, d.version.high >>> 0).toNumber(true);
            }
            if (d.url != null) {
                m.url = $String(d.url);
            }
            if (d.loopCount != null) {
                m.loopCount = d.loopCount >>> 0;
            }
            return m;
        };

        AIRichResponseDynamicMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                d.type = o.enums === $String ? $root.proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType[m.type] === $undefined ? m.type : $root.proto.AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType[m.type] : m.type;
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.version = typeof m.version === "number" ? $BigInt(m.version) : $util.Long.fromBits(m.version.low >>> 0, m.version.high >>> 0, true).toBigInt();
                else if (typeof m.version === "number")
                    d.version = o.longs === $String ? $String(m.version) : m.version;
                else
                    d.version = o.longs === $String ? $util.Long.prototype.toString.call(m.version) : o.longs === $Number ? new $util.LongBits(m.version.low >>> 0, m.version.high >>> 0).toNumber(true) : m.version;
            }
            if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                d.url = m.url;
            }
            if (m.loopCount != null && $Object.hasOwnProperty.call(m, "loopCount")) {
                d.loopCount = m.loopCount;
            }
            return d;
        };

        AIRichResponseDynamicMetadata.prototype.toJSON = function() {
            return AIRichResponseDynamicMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseDynamicMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseDynamicMetadata";
        };

        AIRichResponseDynamicMetadata.AIRichResponseDynamicMetadataType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_UNKNOWN"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_IMAGE"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_DYNAMIC_METADATA_TYPE_GIF"] = 2;
            return values;
        })();

        return AIRichResponseDynamicMetadata;
    })();

    proto.AIRichResponseGridImageMetadata = (function() {

        const AIRichResponseGridImageMetadata = function (p) {
            this.imageUrls = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseGridImageMetadata.prototype.gridImageUrl = null;
        AIRichResponseGridImageMetadata.prototype.imageUrls = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseGridImageMetadata.prototype, "_gridImageUrl", {
            get: $util.oneOfGetter($oneOfFields = ["gridImageUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseGridImageMetadata.create = function(properties) {
            return new AIRichResponseGridImageMetadata(properties);
        };

        AIRichResponseGridImageMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.gridImageUrl != null && $Object.hasOwnProperty.call(m, "gridImageUrl"))
                $root.proto.AIRichResponseImageURL.encode(m.gridImageUrl, w.uint32(10).fork(), q + 1).ldelim();
            if (m.imageUrls != null && m.imageUrls.length) {
                for (var i = 0; i < m.imageUrls.length; ++i)
                    $root.proto.AIRichResponseImageURL.encode(m.imageUrls[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseGridImageMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseGridImageMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.gridImageUrl = $root.proto.AIRichResponseImageURL.decode(r, r.uint32(), $undefined, q + 1, m.gridImageUrl);
                        m._gridImageUrl = "gridImageUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.imageUrls && m.imageUrls.length))
                            m.imageUrls = [];
                        m.imageUrls.push($root.proto.AIRichResponseImageURL.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseGridImageMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseGridImageMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseGridImageMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseGridImageMetadata();
            if (d.gridImageUrl != null) {
                if (!$util.isObject(d.gridImageUrl))
                    throw $TypeError(".proto.AIRichResponseGridImageMetadata.gridImageUrl: object expected");
                m.gridImageUrl = $root.proto.AIRichResponseImageURL.fromObject(d.gridImageUrl, q + 1);
            }
            if (d.imageUrls) {
                if (!$Array.isArray(d.imageUrls))
                    throw $TypeError(".proto.AIRichResponseGridImageMetadata.imageUrls: array expected");
                m.imageUrls = $Array(d.imageUrls.length);
                for (var i = 0; i < d.imageUrls.length; ++i) {
                    if (!$util.isObject(d.imageUrls[i]))
                        throw $TypeError(".proto.AIRichResponseGridImageMetadata.imageUrls: object expected");
                    m.imageUrls[i] = $root.proto.AIRichResponseImageURL.fromObject(d.imageUrls[i], q + 1);
                }
            }
            return m;
        };

        AIRichResponseGridImageMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.imageUrls = [];
            }
            if (m.gridImageUrl != null && $Object.hasOwnProperty.call(m, "gridImageUrl")) {
                d.gridImageUrl = $root.proto.AIRichResponseImageURL.toObject(m.gridImageUrl, o, q + 1);
            }
            if (m.imageUrls && m.imageUrls.length) {
                d.imageUrls = $Array(m.imageUrls.length);
                for (var j = 0; j < m.imageUrls.length; ++j) {
                    d.imageUrls[j] = $root.proto.AIRichResponseImageURL.toObject(m.imageUrls[j], o, q + 1);
                }
            }
            return d;
        };

        AIRichResponseGridImageMetadata.prototype.toJSON = function() {
            return AIRichResponseGridImageMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseGridImageMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseGridImageMetadata";
        };

        return AIRichResponseGridImageMetadata;
    })();

    proto.AIRichResponseImageURL = (function() {

        const AIRichResponseImageURL = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseImageURL.prototype.imagePreviewUrl = null;
        AIRichResponseImageURL.prototype.imageHighResUrl = null;
        AIRichResponseImageURL.prototype.sourceUrl = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseImageURL.prototype, "_imagePreviewUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imagePreviewUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseImageURL.prototype, "_imageHighResUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imageHighResUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseImageURL.prototype, "_sourceUrl", {
            get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseImageURL.create = function(properties) {
            return new AIRichResponseImageURL(properties);
        };

        AIRichResponseImageURL.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.imagePreviewUrl != null && $Object.hasOwnProperty.call(m, "imagePreviewUrl"))
                w.uint32(10).string(m.imagePreviewUrl);
            if (m.imageHighResUrl != null && $Object.hasOwnProperty.call(m, "imageHighResUrl"))
                w.uint32(18).string(m.imageHighResUrl);
            if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl"))
                w.uint32(26).string(m.sourceUrl);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseImageURL.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseImageURL();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.imagePreviewUrl = r.stringVerify();
                        m._imagePreviewUrl = "imagePreviewUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.imageHighResUrl = r.stringVerify();
                        m._imageHighResUrl = "imageHighResUrl";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.sourceUrl = r.stringVerify();
                        m._sourceUrl = "sourceUrl";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseImageURL.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseImageURL)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseImageURL: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseImageURL();
            if (d.imagePreviewUrl != null) {
                m.imagePreviewUrl = $String(d.imagePreviewUrl);
            }
            if (d.imageHighResUrl != null) {
                m.imageHighResUrl = $String(d.imageHighResUrl);
            }
            if (d.sourceUrl != null) {
                m.sourceUrl = $String(d.sourceUrl);
            }
            return m;
        };

        AIRichResponseImageURL.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.imagePreviewUrl != null && $Object.hasOwnProperty.call(m, "imagePreviewUrl")) {
                d.imagePreviewUrl = m.imagePreviewUrl;
            }
            if (m.imageHighResUrl != null && $Object.hasOwnProperty.call(m, "imageHighResUrl")) {
                d.imageHighResUrl = m.imageHighResUrl;
            }
            if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl")) {
                d.sourceUrl = m.sourceUrl;
            }
            return d;
        };

        AIRichResponseImageURL.prototype.toJSON = function() {
            return AIRichResponseImageURL.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseImageURL.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseImageURL";
        };

        return AIRichResponseImageURL;
    })();

    proto.AIRichResponseInlineImageMetadata = (function() {

        const AIRichResponseInlineImageMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseInlineImageMetadata.prototype.imageUrl = null;
        AIRichResponseInlineImageMetadata.prototype.imageText = null;
        AIRichResponseInlineImageMetadata.prototype.alignment = null;
        AIRichResponseInlineImageMetadata.prototype.tapLinkUrl = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_imageUrl", {
            get: $util.oneOfGetter($oneOfFields = ["imageUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_imageText", {
            get: $util.oneOfGetter($oneOfFields = ["imageText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_alignment", {
            get: $util.oneOfGetter($oneOfFields = ["alignment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseInlineImageMetadata.prototype, "_tapLinkUrl", {
            get: $util.oneOfGetter($oneOfFields = ["tapLinkUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseInlineImageMetadata.create = function(properties) {
            return new AIRichResponseInlineImageMetadata(properties);
        };

        AIRichResponseInlineImageMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.imageUrl != null && $Object.hasOwnProperty.call(m, "imageUrl"))
                $root.proto.AIRichResponseImageURL.encode(m.imageUrl, w.uint32(10).fork(), q + 1).ldelim();
            if (m.imageText != null && $Object.hasOwnProperty.call(m, "imageText"))
                w.uint32(18).string(m.imageText);
            if (m.alignment != null && $Object.hasOwnProperty.call(m, "alignment"))
                w.uint32(24).int32(m.alignment);
            if (m.tapLinkUrl != null && $Object.hasOwnProperty.call(m, "tapLinkUrl"))
                w.uint32(34).string(m.tapLinkUrl);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseInlineImageMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseInlineImageMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.imageUrl = $root.proto.AIRichResponseImageURL.decode(r, r.uint32(), $undefined, q + 1, m.imageUrl);
                        m._imageUrl = "imageUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.imageText = r.stringVerify();
                        m._imageText = "imageText";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.alignment = r.int32();
                        m._alignment = "alignment";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.tapLinkUrl = r.stringVerify();
                        m._tapLinkUrl = "tapLinkUrl";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseInlineImageMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseInlineImageMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseInlineImageMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseInlineImageMetadata();
            if (d.imageUrl != null) {
                if (!$util.isObject(d.imageUrl))
                    throw $TypeError(".proto.AIRichResponseInlineImageMetadata.imageUrl: object expected");
                m.imageUrl = $root.proto.AIRichResponseImageURL.fromObject(d.imageUrl, q + 1);
            }
            if (d.imageText != null) {
                m.imageText = $String(d.imageText);
            }
            switch (d.alignment) {
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED":
            case 0:
                m.alignment = 0;
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED":
            case 1:
                m.alignment = 1;
                break;
            case "AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED":
            case 2:
                m.alignment = 2;
                break;
            default:
                if (typeof d.alignment === "number" && (d.alignment | 0) === d.alignment)
                    m.alignment = d.alignment;
            }
            if (d.tapLinkUrl != null) {
                m.tapLinkUrl = $String(d.tapLinkUrl);
            }
            return m;
        };

        AIRichResponseInlineImageMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.imageUrl != null && $Object.hasOwnProperty.call(m, "imageUrl")) {
                d.imageUrl = $root.proto.AIRichResponseImageURL.toObject(m.imageUrl, o, q + 1);
            }
            if (m.imageText != null && $Object.hasOwnProperty.call(m, "imageText")) {
                d.imageText = m.imageText;
            }
            if (m.alignment != null && $Object.hasOwnProperty.call(m, "alignment")) {
                d.alignment = o.enums === $String ? $root.proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment[m.alignment] === $undefined ? m.alignment : $root.proto.AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment[m.alignment] : m.alignment;
            }
            if (m.tapLinkUrl != null && $Object.hasOwnProperty.call(m, "tapLinkUrl")) {
                d.tapLinkUrl = m.tapLinkUrl;
            }
            return d;
        };

        AIRichResponseInlineImageMetadata.prototype.toJSON = function() {
            return AIRichResponseInlineImageMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseInlineImageMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseInlineImageMetadata";
        };

        AIRichResponseInlineImageMetadata.AIRichResponseImageAlignment = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_LEADING_ALIGNED"] = 0;
            values[valuesById[1] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_TRAILING_ALIGNED"] = 1;
            values[valuesById[2] = "AI_RICH_RESPONSE_IMAGE_LAYOUT_CENTER_ALIGNED"] = 2;
            return values;
        })();

        return AIRichResponseInlineImageMetadata;
    })();

    proto.AIRichResponseLatexMetadata = (function() {

        const AIRichResponseLatexMetadata = function (p) {
            this.expressions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseLatexMetadata.prototype.text = null;
        AIRichResponseLatexMetadata.prototype.expressions = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseLatexMetadata.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseLatexMetadata.create = function(properties) {
            return new AIRichResponseLatexMetadata(properties);
        };

        AIRichResponseLatexMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                w.uint32(10).string(m.text);
            if (m.expressions != null && m.expressions.length) {
                for (var i = 0; i < m.expressions.length; ++i)
                    $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.encode(m.expressions[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseLatexMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseLatexMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.text = r.stringVerify();
                        m._text = "text";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.expressions && m.expressions.length))
                            m.expressions = [];
                        m.expressions.push($root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseLatexMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseLatexMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseLatexMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseLatexMetadata();
            if (d.text != null) {
                m.text = $String(d.text);
            }
            if (d.expressions) {
                if (!$Array.isArray(d.expressions))
                    throw $TypeError(".proto.AIRichResponseLatexMetadata.expressions: array expected");
                m.expressions = $Array(d.expressions.length);
                for (var i = 0; i < d.expressions.length; ++i) {
                    if (!$util.isObject(d.expressions[i]))
                        throw $TypeError(".proto.AIRichResponseLatexMetadata.expressions: object expected");
                    m.expressions[i] = $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.fromObject(d.expressions[i], q + 1);
                }
            }
            return m;
        };

        AIRichResponseLatexMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.expressions = [];
            }
            if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                d.text = m.text;
            }
            if (m.expressions && m.expressions.length) {
                d.expressions = $Array(m.expressions.length);
                for (var j = 0; j < m.expressions.length; ++j) {
                    d.expressions[j] = $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression.toObject(m.expressions[j], o, q + 1);
                }
            }
            return d;
        };

        AIRichResponseLatexMetadata.prototype.toJSON = function() {
            return AIRichResponseLatexMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseLatexMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseLatexMetadata";
        };

        AIRichResponseLatexMetadata.AIRichResponseLatexExpression = (function() {

            const AIRichResponseLatexExpression = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIRichResponseLatexExpression.prototype.latexExpression = null;
            AIRichResponseLatexExpression.prototype.url = null;
            AIRichResponseLatexExpression.prototype.width = null;
            AIRichResponseLatexExpression.prototype.height = null;
            AIRichResponseLatexExpression.prototype.fontHeight = null;
            AIRichResponseLatexExpression.prototype.imageTopPadding = null;
            AIRichResponseLatexExpression.prototype.imageLeadingPadding = null;
            AIRichResponseLatexExpression.prototype.imageBottomPadding = null;
            AIRichResponseLatexExpression.prototype.imageTrailingPadding = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_latexExpression", {
                get: $util.oneOfGetter($oneOfFields = ["latexExpression"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_fontHeight", {
                get: $util.oneOfGetter($oneOfFields = ["fontHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageTopPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageTopPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageLeadingPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageLeadingPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageBottomPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageBottomPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseLatexExpression.prototype, "_imageTrailingPadding", {
                get: $util.oneOfGetter($oneOfFields = ["imageTrailingPadding"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseLatexExpression.create = function(properties) {
                return new AIRichResponseLatexExpression(properties);
            };

            AIRichResponseLatexExpression.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.latexExpression != null && $Object.hasOwnProperty.call(m, "latexExpression"))
                    w.uint32(10).string(m.latexExpression);
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(18).string(m.url);
                if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                    w.uint32(25).double(m.width);
                if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                    w.uint32(33).double(m.height);
                if (m.fontHeight != null && $Object.hasOwnProperty.call(m, "fontHeight"))
                    w.uint32(41).double(m.fontHeight);
                if (m.imageTopPadding != null && $Object.hasOwnProperty.call(m, "imageTopPadding"))
                    w.uint32(49).double(m.imageTopPadding);
                if (m.imageLeadingPadding != null && $Object.hasOwnProperty.call(m, "imageLeadingPadding"))
                    w.uint32(57).double(m.imageLeadingPadding);
                if (m.imageBottomPadding != null && $Object.hasOwnProperty.call(m, "imageBottomPadding"))
                    w.uint32(65).double(m.imageBottomPadding);
                if (m.imageTrailingPadding != null && $Object.hasOwnProperty.call(m, "imageTrailingPadding"))
                    w.uint32(73).double(m.imageTrailingPadding);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIRichResponseLatexExpression.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.latexExpression = r.stringVerify();
                            m._latexExpression = "latexExpression";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 3: {
                            if (u !== 1)
                                break;
                            m.width = r.double();
                            m._width = "width";
                            continue;
                        }
                    case 4: {
                            if (u !== 1)
                                break;
                            m.height = r.double();
                            m._height = "height";
                            continue;
                        }
                    case 5: {
                            if (u !== 1)
                                break;
                            m.fontHeight = r.double();
                            m._fontHeight = "fontHeight";
                            continue;
                        }
                    case 6: {
                            if (u !== 1)
                                break;
                            m.imageTopPadding = r.double();
                            m._imageTopPadding = "imageTopPadding";
                            continue;
                        }
                    case 7: {
                            if (u !== 1)
                                break;
                            m.imageLeadingPadding = r.double();
                            m._imageLeadingPadding = "imageLeadingPadding";
                            continue;
                        }
                    case 8: {
                            if (u !== 1)
                                break;
                            m.imageBottomPadding = r.double();
                            m._imageBottomPadding = "imageBottomPadding";
                            continue;
                        }
                    case 9: {
                            if (u !== 1)
                                break;
                            m.imageTrailingPadding = r.double();
                            m._imageTrailingPadding = "imageTrailingPadding";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIRichResponseLatexExpression.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression();
                if (d.latexExpression != null) {
                    m.latexExpression = $String(d.latexExpression);
                }
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.width != null) {
                    m.width = $Number(d.width);
                }
                if (d.height != null) {
                    m.height = $Number(d.height);
                }
                if (d.fontHeight != null) {
                    m.fontHeight = $Number(d.fontHeight);
                }
                if (d.imageTopPadding != null) {
                    m.imageTopPadding = $Number(d.imageTopPadding);
                }
                if (d.imageLeadingPadding != null) {
                    m.imageLeadingPadding = $Number(d.imageLeadingPadding);
                }
                if (d.imageBottomPadding != null) {
                    m.imageBottomPadding = $Number(d.imageBottomPadding);
                }
                if (d.imageTrailingPadding != null) {
                    m.imageTrailingPadding = $Number(d.imageTrailingPadding);
                }
                return m;
            };

            AIRichResponseLatexExpression.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.latexExpression != null && $Object.hasOwnProperty.call(m, "latexExpression")) {
                    d.latexExpression = m.latexExpression;
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                    d.width = o.json && !$isFinite(m.width) ? $String(m.width) : m.width;
                }
                if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                    d.height = o.json && !$isFinite(m.height) ? $String(m.height) : m.height;
                }
                if (m.fontHeight != null && $Object.hasOwnProperty.call(m, "fontHeight")) {
                    d.fontHeight = o.json && !$isFinite(m.fontHeight) ? $String(m.fontHeight) : m.fontHeight;
                }
                if (m.imageTopPadding != null && $Object.hasOwnProperty.call(m, "imageTopPadding")) {
                    d.imageTopPadding = o.json && !$isFinite(m.imageTopPadding) ? $String(m.imageTopPadding) : m.imageTopPadding;
                }
                if (m.imageLeadingPadding != null && $Object.hasOwnProperty.call(m, "imageLeadingPadding")) {
                    d.imageLeadingPadding = o.json && !$isFinite(m.imageLeadingPadding) ? $String(m.imageLeadingPadding) : m.imageLeadingPadding;
                }
                if (m.imageBottomPadding != null && $Object.hasOwnProperty.call(m, "imageBottomPadding")) {
                    d.imageBottomPadding = o.json && !$isFinite(m.imageBottomPadding) ? $String(m.imageBottomPadding) : m.imageBottomPadding;
                }
                if (m.imageTrailingPadding != null && $Object.hasOwnProperty.call(m, "imageTrailingPadding")) {
                    d.imageTrailingPadding = o.json && !$isFinite(m.imageTrailingPadding) ? $String(m.imageTrailingPadding) : m.imageTrailingPadding;
                }
                return d;
            };

            AIRichResponseLatexExpression.prototype.toJSON = function() {
                return AIRichResponseLatexExpression.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseLatexExpression.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIRichResponseLatexMetadata.AIRichResponseLatexExpression";
            };

            return AIRichResponseLatexExpression;
        })();

        return AIRichResponseLatexMetadata;
    })();

    proto.AIRichResponseMapMetadata = (function() {

        const AIRichResponseMapMetadata = function (p) {
            this.annotations = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseMapMetadata.prototype.centerLatitude = null;
        AIRichResponseMapMetadata.prototype.centerLongitude = null;
        AIRichResponseMapMetadata.prototype.latitudeDelta = null;
        AIRichResponseMapMetadata.prototype.longitudeDelta = null;
        AIRichResponseMapMetadata.prototype.annotations = $util.emptyArray;
        AIRichResponseMapMetadata.prototype.showInfoList = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMapMetadata.prototype, "_centerLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["centerLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMapMetadata.prototype, "_centerLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["centerLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMapMetadata.prototype, "_latitudeDelta", {
            get: $util.oneOfGetter($oneOfFields = ["latitudeDelta"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMapMetadata.prototype, "_longitudeDelta", {
            get: $util.oneOfGetter($oneOfFields = ["longitudeDelta"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMapMetadata.prototype, "_showInfoList", {
            get: $util.oneOfGetter($oneOfFields = ["showInfoList"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseMapMetadata.create = function(properties) {
            return new AIRichResponseMapMetadata(properties);
        };

        AIRichResponseMapMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.centerLatitude != null && $Object.hasOwnProperty.call(m, "centerLatitude"))
                w.uint32(9).double(m.centerLatitude);
            if (m.centerLongitude != null && $Object.hasOwnProperty.call(m, "centerLongitude"))
                w.uint32(17).double(m.centerLongitude);
            if (m.latitudeDelta != null && $Object.hasOwnProperty.call(m, "latitudeDelta"))
                w.uint32(25).double(m.latitudeDelta);
            if (m.longitudeDelta != null && $Object.hasOwnProperty.call(m, "longitudeDelta"))
                w.uint32(33).double(m.longitudeDelta);
            if (m.annotations != null && m.annotations.length) {
                for (var i = 0; i < m.annotations.length; ++i)
                    $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.encode(m.annotations[i], w.uint32(42).fork(), q + 1).ldelim();
            }
            if (m.showInfoList != null && $Object.hasOwnProperty.call(m, "showInfoList"))
                w.uint32(48).bool(m.showInfoList);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseMapMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseMapMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 1)
                            break;
                        m.centerLatitude = r.double();
                        m._centerLatitude = "centerLatitude";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        m.centerLongitude = r.double();
                        m._centerLongitude = "centerLongitude";
                        continue;
                    }
                case 3: {
                        if (u !== 1)
                            break;
                        m.latitudeDelta = r.double();
                        m._latitudeDelta = "latitudeDelta";
                        continue;
                    }
                case 4: {
                        if (u !== 1)
                            break;
                        m.longitudeDelta = r.double();
                        m._longitudeDelta = "longitudeDelta";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(m.annotations && m.annotations.length))
                            m.annotations = [];
                        m.annotations.push($root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.showInfoList = r.bool();
                        m._showInfoList = "showInfoList";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseMapMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseMapMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseMapMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseMapMetadata();
            if (d.centerLatitude != null) {
                m.centerLatitude = $Number(d.centerLatitude);
            }
            if (d.centerLongitude != null) {
                m.centerLongitude = $Number(d.centerLongitude);
            }
            if (d.latitudeDelta != null) {
                m.latitudeDelta = $Number(d.latitudeDelta);
            }
            if (d.longitudeDelta != null) {
                m.longitudeDelta = $Number(d.longitudeDelta);
            }
            if (d.annotations) {
                if (!$Array.isArray(d.annotations))
                    throw $TypeError(".proto.AIRichResponseMapMetadata.annotations: array expected");
                m.annotations = $Array(d.annotations.length);
                for (var i = 0; i < d.annotations.length; ++i) {
                    if (!$util.isObject(d.annotations[i]))
                        throw $TypeError(".proto.AIRichResponseMapMetadata.annotations: object expected");
                    m.annotations[i] = $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.fromObject(d.annotations[i], q + 1);
                }
            }
            if (d.showInfoList != null) {
                m.showInfoList = $Boolean(d.showInfoList);
            }
            return m;
        };

        AIRichResponseMapMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.annotations = [];
            }
            if (m.centerLatitude != null && $Object.hasOwnProperty.call(m, "centerLatitude")) {
                d.centerLatitude = o.json && !$isFinite(m.centerLatitude) ? $String(m.centerLatitude) : m.centerLatitude;
            }
            if (m.centerLongitude != null && $Object.hasOwnProperty.call(m, "centerLongitude")) {
                d.centerLongitude = o.json && !$isFinite(m.centerLongitude) ? $String(m.centerLongitude) : m.centerLongitude;
            }
            if (m.latitudeDelta != null && $Object.hasOwnProperty.call(m, "latitudeDelta")) {
                d.latitudeDelta = o.json && !$isFinite(m.latitudeDelta) ? $String(m.latitudeDelta) : m.latitudeDelta;
            }
            if (m.longitudeDelta != null && $Object.hasOwnProperty.call(m, "longitudeDelta")) {
                d.longitudeDelta = o.json && !$isFinite(m.longitudeDelta) ? $String(m.longitudeDelta) : m.longitudeDelta;
            }
            if (m.annotations && m.annotations.length) {
                d.annotations = $Array(m.annotations.length);
                for (var j = 0; j < m.annotations.length; ++j) {
                    d.annotations[j] = $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation.toObject(m.annotations[j], o, q + 1);
                }
            }
            if (m.showInfoList != null && $Object.hasOwnProperty.call(m, "showInfoList")) {
                d.showInfoList = m.showInfoList;
            }
            return d;
        };

        AIRichResponseMapMetadata.prototype.toJSON = function() {
            return AIRichResponseMapMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseMapMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseMapMetadata";
        };

        AIRichResponseMapMetadata.AIRichResponseMapAnnotation = (function() {

            const AIRichResponseMapAnnotation = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIRichResponseMapAnnotation.prototype.annotationNumber = null;
            AIRichResponseMapAnnotation.prototype.latitude = null;
            AIRichResponseMapAnnotation.prototype.longitude = null;
            AIRichResponseMapAnnotation.prototype.title = null;
            AIRichResponseMapAnnotation.prototype.body = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_annotationNumber", {
                get: $util.oneOfGetter($oneOfFields = ["annotationNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_latitude", {
                get: $util.oneOfGetter($oneOfFields = ["latitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_longitude", {
                get: $util.oneOfGetter($oneOfFields = ["longitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseMapAnnotation.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseMapAnnotation.create = function(properties) {
                return new AIRichResponseMapAnnotation(properties);
            };

            AIRichResponseMapAnnotation.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.annotationNumber != null && $Object.hasOwnProperty.call(m, "annotationNumber"))
                    w.uint32(8).uint32(m.annotationNumber);
                if (m.latitude != null && $Object.hasOwnProperty.call(m, "latitude"))
                    w.uint32(17).double(m.latitude);
                if (m.longitude != null && $Object.hasOwnProperty.call(m, "longitude"))
                    w.uint32(25).double(m.longitude);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(34).string(m.title);
                if (m.body != null && $Object.hasOwnProperty.call(m, "body"))
                    w.uint32(42).string(m.body);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIRichResponseMapAnnotation.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.annotationNumber = r.uint32();
                            m._annotationNumber = "annotationNumber";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            m.latitude = r.double();
                            m._latitude = "latitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 1)
                                break;
                            m.longitude = r.double();
                            m._longitude = "longitude";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.body = r.stringVerify();
                            m._body = "body";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIRichResponseMapAnnotation.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation();
                if (d.annotationNumber != null) {
                    m.annotationNumber = d.annotationNumber >>> 0;
                }
                if (d.latitude != null) {
                    m.latitude = $Number(d.latitude);
                }
                if (d.longitude != null) {
                    m.longitude = $Number(d.longitude);
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.body != null) {
                    m.body = $String(d.body);
                }
                return m;
            };

            AIRichResponseMapAnnotation.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.annotationNumber != null && $Object.hasOwnProperty.call(m, "annotationNumber")) {
                    d.annotationNumber = m.annotationNumber;
                }
                if (m.latitude != null && $Object.hasOwnProperty.call(m, "latitude")) {
                    d.latitude = o.json && !$isFinite(m.latitude) ? $String(m.latitude) : m.latitude;
                }
                if (m.longitude != null && $Object.hasOwnProperty.call(m, "longitude")) {
                    d.longitude = o.json && !$isFinite(m.longitude) ? $String(m.longitude) : m.longitude;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.body != null && $Object.hasOwnProperty.call(m, "body")) {
                    d.body = m.body;
                }
                return d;
            };

            AIRichResponseMapAnnotation.prototype.toJSON = function() {
                return AIRichResponseMapAnnotation.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseMapAnnotation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIRichResponseMapMetadata.AIRichResponseMapAnnotation";
            };

            return AIRichResponseMapAnnotation;
        })();

        return AIRichResponseMapMetadata;
    })();

    proto.AIRichResponseMessage = (function() {

        const AIRichResponseMessage = function (p) {
            this.submessages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseMessage.prototype.messageType = null;
        AIRichResponseMessage.prototype.submessages = $util.emptyArray;
        AIRichResponseMessage.prototype.unifiedResponse = null;
        AIRichResponseMessage.prototype.contextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMessage.prototype, "_messageType", {
            get: $util.oneOfGetter($oneOfFields = ["messageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMessage.prototype, "_unifiedResponse", {
            get: $util.oneOfGetter($oneOfFields = ["unifiedResponse"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseMessage.prototype, "_contextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseMessage.create = function(properties) {
            return new AIRichResponseMessage(properties);
        };

        AIRichResponseMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageType != null && $Object.hasOwnProperty.call(m, "messageType"))
                w.uint32(8).int32(m.messageType);
            if (m.submessages != null && m.submessages.length) {
                for (var i = 0; i < m.submessages.length; ++i)
                    $root.proto.AIRichResponseSubMessage.encode(m.submessages[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.unifiedResponse != null && $Object.hasOwnProperty.call(m, "unifiedResponse"))
                $root.proto.AIRichResponseUnifiedResponse.encode(m.unifiedResponse, w.uint32(26).fork(), q + 1).ldelim();
            if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(34).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseMessage(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.messageType = r.int32();
                        m._messageType = "messageType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.submessages && m.submessages.length))
                            m.submessages = [];
                        m.submessages.push($root.proto.AIRichResponseSubMessage.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.unifiedResponse = $root.proto.AIRichResponseUnifiedResponse.decode(r, r.uint32(), $undefined, q + 1, m.unifiedResponse);
                        m._unifiedResponse = "unifiedResponse";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                        m._contextInfo = "contextInfo";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseMessage();
            switch (d.messageType) {
            case "AI_RICH_RESPONSE_TYPE_UNKNOWN":
            case 0:
                m.messageType = 0;
                break;
            case "AI_RICH_RESPONSE_TYPE_STANDARD":
            case 1:
                m.messageType = 1;
                break;
            default:
                if (typeof d.messageType === "number" && (d.messageType | 0) === d.messageType)
                    m.messageType = d.messageType;
            }
            if (d.submessages) {
                if (!$Array.isArray(d.submessages))
                    throw $TypeError(".proto.AIRichResponseMessage.submessages: array expected");
                m.submessages = $Array(d.submessages.length);
                for (var i = 0; i < d.submessages.length; ++i) {
                    if (!$util.isObject(d.submessages[i]))
                        throw $TypeError(".proto.AIRichResponseMessage.submessages: object expected");
                    m.submessages[i] = $root.proto.AIRichResponseSubMessage.fromObject(d.submessages[i], q + 1);
                }
            }
            if (d.unifiedResponse != null) {
                if (!$util.isObject(d.unifiedResponse))
                    throw $TypeError(".proto.AIRichResponseMessage.unifiedResponse: object expected");
                m.unifiedResponse = $root.proto.AIRichResponseUnifiedResponse.fromObject(d.unifiedResponse, q + 1);
            }
            if (d.contextInfo != null) {
                if (!$util.isObject(d.contextInfo))
                    throw $TypeError(".proto.AIRichResponseMessage.contextInfo: object expected");
                m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
            }
            return m;
        };

        AIRichResponseMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.submessages = [];
            }
            if (m.messageType != null && $Object.hasOwnProperty.call(m, "messageType")) {
                d.messageType = o.enums === $String ? $root.proto.AIRichResponseMessageType[m.messageType] === $undefined ? m.messageType : $root.proto.AIRichResponseMessageType[m.messageType] : m.messageType;
            }
            if (m.submessages && m.submessages.length) {
                d.submessages = $Array(m.submessages.length);
                for (var j = 0; j < m.submessages.length; ++j) {
                    d.submessages[j] = $root.proto.AIRichResponseSubMessage.toObject(m.submessages[j], o, q + 1);
                }
            }
            if (m.unifiedResponse != null && $Object.hasOwnProperty.call(m, "unifiedResponse")) {
                d.unifiedResponse = $root.proto.AIRichResponseUnifiedResponse.toObject(m.unifiedResponse, o, q + 1);
            }
            if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
            }
            return d;
        };

        AIRichResponseMessage.prototype.toJSON = function() {
            return AIRichResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseMessage";
        };

        return AIRichResponseMessage;
    })();

    proto.AIRichResponseMessageType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "AI_RICH_RESPONSE_TYPE_UNKNOWN"] = 0;
        values[valuesById[1] = "AI_RICH_RESPONSE_TYPE_STANDARD"] = 1;
        return values;
    })();

    proto.AIRichResponseSubMessage = (function() {

        const AIRichResponseSubMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseSubMessage.prototype.messageType = null;
        AIRichResponseSubMessage.prototype.gridImageMetadata = null;
        AIRichResponseSubMessage.prototype.messageText = null;
        AIRichResponseSubMessage.prototype.imageMetadata = null;
        AIRichResponseSubMessage.prototype.codeMetadata = null;
        AIRichResponseSubMessage.prototype.tableMetadata = null;
        AIRichResponseSubMessage.prototype.dynamicMetadata = null;
        AIRichResponseSubMessage.prototype.latexMetadata = null;
        AIRichResponseSubMessage.prototype.mapMetadata = null;
        AIRichResponseSubMessage.prototype.contentItemsMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_messageType", {
            get: $util.oneOfGetter($oneOfFields = ["messageType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_gridImageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["gridImageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_messageText", {
            get: $util.oneOfGetter($oneOfFields = ["messageText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_imageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["imageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_codeMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["codeMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_tableMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["tableMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_dynamicMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["dynamicMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_latexMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["latexMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_mapMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["mapMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseSubMessage.prototype, "_contentItemsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["contentItemsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseSubMessage.create = function(properties) {
            return new AIRichResponseSubMessage(properties);
        };

        AIRichResponseSubMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageType != null && $Object.hasOwnProperty.call(m, "messageType"))
                w.uint32(8).int32(m.messageType);
            if (m.gridImageMetadata != null && $Object.hasOwnProperty.call(m, "gridImageMetadata"))
                $root.proto.AIRichResponseGridImageMetadata.encode(m.gridImageMetadata, w.uint32(18).fork(), q + 1).ldelim();
            if (m.messageText != null && $Object.hasOwnProperty.call(m, "messageText"))
                w.uint32(26).string(m.messageText);
            if (m.imageMetadata != null && $Object.hasOwnProperty.call(m, "imageMetadata"))
                $root.proto.AIRichResponseInlineImageMetadata.encode(m.imageMetadata, w.uint32(34).fork(), q + 1).ldelim();
            if (m.codeMetadata != null && $Object.hasOwnProperty.call(m, "codeMetadata"))
                $root.proto.AIRichResponseCodeMetadata.encode(m.codeMetadata, w.uint32(42).fork(), q + 1).ldelim();
            if (m.tableMetadata != null && $Object.hasOwnProperty.call(m, "tableMetadata"))
                $root.proto.AIRichResponseTableMetadata.encode(m.tableMetadata, w.uint32(50).fork(), q + 1).ldelim();
            if (m.dynamicMetadata != null && $Object.hasOwnProperty.call(m, "dynamicMetadata"))
                $root.proto.AIRichResponseDynamicMetadata.encode(m.dynamicMetadata, w.uint32(58).fork(), q + 1).ldelim();
            if (m.latexMetadata != null && $Object.hasOwnProperty.call(m, "latexMetadata"))
                $root.proto.AIRichResponseLatexMetadata.encode(m.latexMetadata, w.uint32(66).fork(), q + 1).ldelim();
            if (m.mapMetadata != null && $Object.hasOwnProperty.call(m, "mapMetadata"))
                $root.proto.AIRichResponseMapMetadata.encode(m.mapMetadata, w.uint32(74).fork(), q + 1).ldelim();
            if (m.contentItemsMetadata != null && $Object.hasOwnProperty.call(m, "contentItemsMetadata"))
                $root.proto.AIRichResponseContentItemsMetadata.encode(m.contentItemsMetadata, w.uint32(82).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseSubMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseSubMessage(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.messageType = r.int32();
                        m._messageType = "messageType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.gridImageMetadata = $root.proto.AIRichResponseGridImageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.gridImageMetadata);
                        m._gridImageMetadata = "gridImageMetadata";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.messageText = r.stringVerify();
                        m._messageText = "messageText";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.imageMetadata = $root.proto.AIRichResponseInlineImageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.imageMetadata);
                        m._imageMetadata = "imageMetadata";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.codeMetadata = $root.proto.AIRichResponseCodeMetadata.decode(r, r.uint32(), $undefined, q + 1, m.codeMetadata);
                        m._codeMetadata = "codeMetadata";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.tableMetadata = $root.proto.AIRichResponseTableMetadata.decode(r, r.uint32(), $undefined, q + 1, m.tableMetadata);
                        m._tableMetadata = "tableMetadata";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.dynamicMetadata = $root.proto.AIRichResponseDynamicMetadata.decode(r, r.uint32(), $undefined, q + 1, m.dynamicMetadata);
                        m._dynamicMetadata = "dynamicMetadata";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.latexMetadata = $root.proto.AIRichResponseLatexMetadata.decode(r, r.uint32(), $undefined, q + 1, m.latexMetadata);
                        m._latexMetadata = "latexMetadata";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.mapMetadata = $root.proto.AIRichResponseMapMetadata.decode(r, r.uint32(), $undefined, q + 1, m.mapMetadata);
                        m._mapMetadata = "mapMetadata";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.contentItemsMetadata = $root.proto.AIRichResponseContentItemsMetadata.decode(r, r.uint32(), $undefined, q + 1, m.contentItemsMetadata);
                        m._contentItemsMetadata = "contentItemsMetadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseSubMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseSubMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseSubMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseSubMessage();
            switch (d.messageType) {
            case "AI_RICH_RESPONSE_UNKNOWN":
            case 0:
                m.messageType = 0;
                break;
            case "AI_RICH_RESPONSE_GRID_IMAGE":
            case 1:
                m.messageType = 1;
                break;
            case "AI_RICH_RESPONSE_TEXT":
            case 2:
                m.messageType = 2;
                break;
            case "AI_RICH_RESPONSE_INLINE_IMAGE":
            case 3:
                m.messageType = 3;
                break;
            case "AI_RICH_RESPONSE_TABLE":
            case 4:
                m.messageType = 4;
                break;
            case "AI_RICH_RESPONSE_CODE":
            case 5:
                m.messageType = 5;
                break;
            case "AI_RICH_RESPONSE_DYNAMIC":
            case 6:
                m.messageType = 6;
                break;
            case "AI_RICH_RESPONSE_MAP":
            case 7:
                m.messageType = 7;
                break;
            case "AI_RICH_RESPONSE_LATEX":
            case 8:
                m.messageType = 8;
                break;
            case "AI_RICH_RESPONSE_CONTENT_ITEMS":
            case 9:
                m.messageType = 9;
                break;
            default:
                if (typeof d.messageType === "number" && (d.messageType | 0) === d.messageType)
                    m.messageType = d.messageType;
            }
            if (d.gridImageMetadata != null) {
                if (!$util.isObject(d.gridImageMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.gridImageMetadata: object expected");
                m.gridImageMetadata = $root.proto.AIRichResponseGridImageMetadata.fromObject(d.gridImageMetadata, q + 1);
            }
            if (d.messageText != null) {
                m.messageText = $String(d.messageText);
            }
            if (d.imageMetadata != null) {
                if (!$util.isObject(d.imageMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.imageMetadata: object expected");
                m.imageMetadata = $root.proto.AIRichResponseInlineImageMetadata.fromObject(d.imageMetadata, q + 1);
            }
            if (d.codeMetadata != null) {
                if (!$util.isObject(d.codeMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.codeMetadata: object expected");
                m.codeMetadata = $root.proto.AIRichResponseCodeMetadata.fromObject(d.codeMetadata, q + 1);
            }
            if (d.tableMetadata != null) {
                if (!$util.isObject(d.tableMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.tableMetadata: object expected");
                m.tableMetadata = $root.proto.AIRichResponseTableMetadata.fromObject(d.tableMetadata, q + 1);
            }
            if (d.dynamicMetadata != null) {
                if (!$util.isObject(d.dynamicMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.dynamicMetadata: object expected");
                m.dynamicMetadata = $root.proto.AIRichResponseDynamicMetadata.fromObject(d.dynamicMetadata, q + 1);
            }
            if (d.latexMetadata != null) {
                if (!$util.isObject(d.latexMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.latexMetadata: object expected");
                m.latexMetadata = $root.proto.AIRichResponseLatexMetadata.fromObject(d.latexMetadata, q + 1);
            }
            if (d.mapMetadata != null) {
                if (!$util.isObject(d.mapMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.mapMetadata: object expected");
                m.mapMetadata = $root.proto.AIRichResponseMapMetadata.fromObject(d.mapMetadata, q + 1);
            }
            if (d.contentItemsMetadata != null) {
                if (!$util.isObject(d.contentItemsMetadata))
                    throw $TypeError(".proto.AIRichResponseSubMessage.contentItemsMetadata: object expected");
                m.contentItemsMetadata = $root.proto.AIRichResponseContentItemsMetadata.fromObject(d.contentItemsMetadata, q + 1);
            }
            return m;
        };

        AIRichResponseSubMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageType != null && $Object.hasOwnProperty.call(m, "messageType")) {
                d.messageType = o.enums === $String ? $root.proto.AIRichResponseSubMessageType[m.messageType] === $undefined ? m.messageType : $root.proto.AIRichResponseSubMessageType[m.messageType] : m.messageType;
            }
            if (m.gridImageMetadata != null && $Object.hasOwnProperty.call(m, "gridImageMetadata")) {
                d.gridImageMetadata = $root.proto.AIRichResponseGridImageMetadata.toObject(m.gridImageMetadata, o, q + 1);
            }
            if (m.messageText != null && $Object.hasOwnProperty.call(m, "messageText")) {
                d.messageText = m.messageText;
            }
            if (m.imageMetadata != null && $Object.hasOwnProperty.call(m, "imageMetadata")) {
                d.imageMetadata = $root.proto.AIRichResponseInlineImageMetadata.toObject(m.imageMetadata, o, q + 1);
            }
            if (m.codeMetadata != null && $Object.hasOwnProperty.call(m, "codeMetadata")) {
                d.codeMetadata = $root.proto.AIRichResponseCodeMetadata.toObject(m.codeMetadata, o, q + 1);
            }
            if (m.tableMetadata != null && $Object.hasOwnProperty.call(m, "tableMetadata")) {
                d.tableMetadata = $root.proto.AIRichResponseTableMetadata.toObject(m.tableMetadata, o, q + 1);
            }
            if (m.dynamicMetadata != null && $Object.hasOwnProperty.call(m, "dynamicMetadata")) {
                d.dynamicMetadata = $root.proto.AIRichResponseDynamicMetadata.toObject(m.dynamicMetadata, o, q + 1);
            }
            if (m.latexMetadata != null && $Object.hasOwnProperty.call(m, "latexMetadata")) {
                d.latexMetadata = $root.proto.AIRichResponseLatexMetadata.toObject(m.latexMetadata, o, q + 1);
            }
            if (m.mapMetadata != null && $Object.hasOwnProperty.call(m, "mapMetadata")) {
                d.mapMetadata = $root.proto.AIRichResponseMapMetadata.toObject(m.mapMetadata, o, q + 1);
            }
            if (m.contentItemsMetadata != null && $Object.hasOwnProperty.call(m, "contentItemsMetadata")) {
                d.contentItemsMetadata = $root.proto.AIRichResponseContentItemsMetadata.toObject(m.contentItemsMetadata, o, q + 1);
            }
            return d;
        };

        AIRichResponseSubMessage.prototype.toJSON = function() {
            return AIRichResponseSubMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseSubMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseSubMessage";
        };

        return AIRichResponseSubMessage;
    })();

    proto.AIRichResponseSubMessageType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "AI_RICH_RESPONSE_UNKNOWN"] = 0;
        values[valuesById[1] = "AI_RICH_RESPONSE_GRID_IMAGE"] = 1;
        values[valuesById[2] = "AI_RICH_RESPONSE_TEXT"] = 2;
        values[valuesById[3] = "AI_RICH_RESPONSE_INLINE_IMAGE"] = 3;
        values[valuesById[4] = "AI_RICH_RESPONSE_TABLE"] = 4;
        values[valuesById[5] = "AI_RICH_RESPONSE_CODE"] = 5;
        values[valuesById[6] = "AI_RICH_RESPONSE_DYNAMIC"] = 6;
        values[valuesById[7] = "AI_RICH_RESPONSE_MAP"] = 7;
        values[valuesById[8] = "AI_RICH_RESPONSE_LATEX"] = 8;
        values[valuesById[9] = "AI_RICH_RESPONSE_CONTENT_ITEMS"] = 9;
        return values;
    })();

    proto.AIRichResponseTableMetadata = (function() {

        const AIRichResponseTableMetadata = function (p) {
            this.rows = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseTableMetadata.prototype.rows = $util.emptyArray;
        AIRichResponseTableMetadata.prototype.title = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseTableMetadata.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseTableMetadata.create = function(properties) {
            return new AIRichResponseTableMetadata(properties);
        };

        AIRichResponseTableMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.rows != null && m.rows.length) {
                for (var i = 0; i < m.rows.length; ++i)
                    $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.encode(m.rows[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                w.uint32(18).string(m.title);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseTableMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseTableMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.rows && m.rows.length))
                            m.rows = [];
                        m.rows.push($root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.title = r.stringVerify();
                        m._title = "title";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseTableMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseTableMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseTableMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseTableMetadata();
            if (d.rows) {
                if (!$Array.isArray(d.rows))
                    throw $TypeError(".proto.AIRichResponseTableMetadata.rows: array expected");
                m.rows = $Array(d.rows.length);
                for (var i = 0; i < d.rows.length; ++i) {
                    if (!$util.isObject(d.rows[i]))
                        throw $TypeError(".proto.AIRichResponseTableMetadata.rows: object expected");
                    m.rows[i] = $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.fromObject(d.rows[i], q + 1);
                }
            }
            if (d.title != null) {
                m.title = $String(d.title);
            }
            return m;
        };

        AIRichResponseTableMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.rows = [];
            }
            if (m.rows && m.rows.length) {
                d.rows = $Array(m.rows.length);
                for (var j = 0; j < m.rows.length; ++j) {
                    d.rows[j] = $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow.toObject(m.rows[j], o, q + 1);
                }
            }
            if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                d.title = m.title;
            }
            return d;
        };

        AIRichResponseTableMetadata.prototype.toJSON = function() {
            return AIRichResponseTableMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseTableMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseTableMetadata";
        };

        AIRichResponseTableMetadata.AIRichResponseTableRow = (function() {

            const AIRichResponseTableRow = function (p) {
                this.items = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIRichResponseTableRow.prototype.items = $util.emptyArray;
            AIRichResponseTableRow.prototype.isHeading = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIRichResponseTableRow.prototype, "_isHeading", {
                get: $util.oneOfGetter($oneOfFields = ["isHeading"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIRichResponseTableRow.create = function(properties) {
                return new AIRichResponseTableRow(properties);
            };

            AIRichResponseTableRow.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.items != null && m.items.length) {
                    for (var i = 0; i < m.items.length; ++i)
                        w.uint32(10).string(m.items[i]);
                }
                if (m.isHeading != null && $Object.hasOwnProperty.call(m, "isHeading"))
                    w.uint32(16).bool(m.isHeading);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIRichResponseTableRow.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.items && m.items.length))
                                m.items = [];
                            m.items.push(r.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.isHeading = r.bool();
                            m._isHeading = "isHeading";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIRichResponseTableRow.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIRichResponseTableMetadata.AIRichResponseTableRow: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIRichResponseTableMetadata.AIRichResponseTableRow();
                if (d.items) {
                    if (!$Array.isArray(d.items))
                        throw $TypeError(".proto.AIRichResponseTableMetadata.AIRichResponseTableRow.items: array expected");
                    m.items = $Array(d.items.length);
                    for (var i = 0; i < d.items.length; ++i) {
                        m.items[i] = $String(d.items[i]);
                    }
                }
                if (d.isHeading != null) {
                    m.isHeading = $Boolean(d.isHeading);
                }
                return m;
            };

            AIRichResponseTableRow.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.items = [];
                }
                if (m.items && m.items.length) {
                    d.items = $Array(m.items.length);
                    for (var j = 0; j < m.items.length; ++j) {
                        d.items[j] = m.items[j];
                    }
                }
                if (m.isHeading != null && $Object.hasOwnProperty.call(m, "isHeading")) {
                    d.isHeading = m.isHeading;
                }
                return d;
            };

            AIRichResponseTableRow.prototype.toJSON = function() {
                return AIRichResponseTableRow.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIRichResponseTableRow.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIRichResponseTableMetadata.AIRichResponseTableRow";
            };

            return AIRichResponseTableRow;
        })();

        return AIRichResponseTableMetadata;
    })();

    proto.AIRichResponseUnifiedResponse = (function() {

        const AIRichResponseUnifiedResponse = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIRichResponseUnifiedResponse.prototype.data = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIRichResponseUnifiedResponse.prototype, "_data", {
            get: $util.oneOfGetter($oneOfFields = ["data"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIRichResponseUnifiedResponse.create = function(properties) {
            return new AIRichResponseUnifiedResponse(properties);
        };

        AIRichResponseUnifiedResponse.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.data != null && $Object.hasOwnProperty.call(m, "data"))
                w.uint32(10).bytes(m.data);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIRichResponseUnifiedResponse.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIRichResponseUnifiedResponse();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.data = r.bytes();
                        m._data = "data";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIRichResponseUnifiedResponse.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIRichResponseUnifiedResponse)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIRichResponseUnifiedResponse: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIRichResponseUnifiedResponse();
            if (d.data != null) {
                if (typeof d.data === "string")
                    $util.base64.decode(d.data, m.data = $util.newBuffer($util.base64.length(d.data)), 0);
                else if (d.data.length >= 0)
                    m.data = d.data;
            }
            return m;
        };

        AIRichResponseUnifiedResponse.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.data != null && $Object.hasOwnProperty.call(m, "data")) {
                d.data = o.bytes === $String ? $util.base64.encode(m.data, 0, m.data.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.data) : m.data;
            }
            return d;
        };

        AIRichResponseUnifiedResponse.prototype.toJSON = function() {
            return AIRichResponseUnifiedResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIRichResponseUnifiedResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIRichResponseUnifiedResponse";
        };

        return AIRichResponseUnifiedResponse;
    })();

    proto.AISubscriptionRequestType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "UNSPECIFIED"] = 0;
        values[valuesById[1] = "THINK_HARD"] = 1;
        values[valuesById[2] = "IMAGE_GEN"] = 2;
        values[valuesById[3] = "VIDEO_GEN"] = 3;
        return values;
    })();

    proto.AISubscriptionUpsellMetadata = (function() {

        const AISubscriptionUpsellMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AISubscriptionUpsellMetadata.prototype.requestType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AISubscriptionUpsellMetadata.prototype, "_requestType", {
            get: $util.oneOfGetter($oneOfFields = ["requestType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AISubscriptionUpsellMetadata.create = function(properties) {
            return new AISubscriptionUpsellMetadata(properties);
        };

        AISubscriptionUpsellMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.requestType != null && $Object.hasOwnProperty.call(m, "requestType"))
                w.uint32(8).int32(m.requestType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AISubscriptionUpsellMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AISubscriptionUpsellMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.requestType = r.int32();
                        m._requestType = "requestType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AISubscriptionUpsellMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.AISubscriptionUpsellMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AISubscriptionUpsellMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AISubscriptionUpsellMetadata();
            switch (d.requestType) {
            case "UNSPECIFIED":
            case 0:
                m.requestType = 0;
                break;
            case "THINK_HARD":
            case 1:
                m.requestType = 1;
                break;
            case "IMAGE_GEN":
            case 2:
                m.requestType = 2;
                break;
            case "VIDEO_GEN":
            case 3:
                m.requestType = 3;
                break;
            default:
                if (typeof d.requestType === "number" && (d.requestType | 0) === d.requestType)
                    m.requestType = d.requestType;
            }
            return m;
        };

        AISubscriptionUpsellMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.requestType != null && $Object.hasOwnProperty.call(m, "requestType")) {
                d.requestType = o.enums === $String ? $root.proto.AISubscriptionRequestType[m.requestType] === $undefined ? m.requestType : $root.proto.AISubscriptionRequestType[m.requestType] : m.requestType;
            }
            return d;
        };

        AISubscriptionUpsellMetadata.prototype.toJSON = function() {
            return AISubscriptionUpsellMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        AISubscriptionUpsellMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AISubscriptionUpsellMetadata";
        };

        return AISubscriptionUpsellMetadata;
    })();

    proto.AIThreadInfo = (function() {

        const AIThreadInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AIThreadInfo.prototype.serverInfo = null;
        AIThreadInfo.prototype.clientInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIThreadInfo.prototype, "_serverInfo", {
            get: $util.oneOfGetter($oneOfFields = ["serverInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AIThreadInfo.prototype, "_clientInfo", {
            get: $util.oneOfGetter($oneOfFields = ["clientInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AIThreadInfo.create = function(properties) {
            return new AIThreadInfo(properties);
        };

        AIThreadInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.serverInfo != null && $Object.hasOwnProperty.call(m, "serverInfo"))
                $root.proto.AIThreadInfo.AIThreadServerInfo.encode(m.serverInfo, w.uint32(10).fork(), q + 1).ldelim();
            if (m.clientInfo != null && $Object.hasOwnProperty.call(m, "clientInfo"))
                $root.proto.AIThreadInfo.AIThreadClientInfo.encode(m.clientInfo, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AIThreadInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIThreadInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.serverInfo = $root.proto.AIThreadInfo.AIThreadServerInfo.decode(r, r.uint32(), $undefined, q + 1, m.serverInfo);
                        m._serverInfo = "serverInfo";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.clientInfo = $root.proto.AIThreadInfo.AIThreadClientInfo.decode(r, r.uint32(), $undefined, q + 1, m.clientInfo);
                        m._clientInfo = "clientInfo";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AIThreadInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.AIThreadInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AIThreadInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AIThreadInfo();
            if (d.serverInfo != null) {
                if (!$util.isObject(d.serverInfo))
                    throw $TypeError(".proto.AIThreadInfo.serverInfo: object expected");
                m.serverInfo = $root.proto.AIThreadInfo.AIThreadServerInfo.fromObject(d.serverInfo, q + 1);
            }
            if (d.clientInfo != null) {
                if (!$util.isObject(d.clientInfo))
                    throw $TypeError(".proto.AIThreadInfo.clientInfo: object expected");
                m.clientInfo = $root.proto.AIThreadInfo.AIThreadClientInfo.fromObject(d.clientInfo, q + 1);
            }
            return m;
        };

        AIThreadInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.serverInfo != null && $Object.hasOwnProperty.call(m, "serverInfo")) {
                d.serverInfo = $root.proto.AIThreadInfo.AIThreadServerInfo.toObject(m.serverInfo, o, q + 1);
            }
            if (m.clientInfo != null && $Object.hasOwnProperty.call(m, "clientInfo")) {
                d.clientInfo = $root.proto.AIThreadInfo.AIThreadClientInfo.toObject(m.clientInfo, o, q + 1);
            }
            return d;
        };

        AIThreadInfo.prototype.toJSON = function() {
            return AIThreadInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        AIThreadInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AIThreadInfo";
        };

        AIThreadInfo.AIThreadClientInfo = (function() {

            const AIThreadClientInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIThreadClientInfo.prototype.type = null;
            AIThreadClientInfo.prototype.sourceChatJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIThreadClientInfo.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIThreadClientInfo.prototype, "_sourceChatJid", {
                get: $util.oneOfGetter($oneOfFields = ["sourceChatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIThreadClientInfo.create = function(properties) {
                return new AIThreadClientInfo(properties);
            };

            AIThreadClientInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.sourceChatJid != null && $Object.hasOwnProperty.call(m, "sourceChatJid"))
                    w.uint32(18).string(m.sourceChatJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIThreadClientInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIThreadInfo.AIThreadClientInfo(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.sourceChatJid = r.stringVerify();
                            m._sourceChatJid = "sourceChatJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIThreadClientInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIThreadInfo.AIThreadClientInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIThreadInfo.AIThreadClientInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIThreadInfo.AIThreadClientInfo();
                switch (d.type) {
                case "UNKNOWN":
                case 0:
                    m.type = 0;
                    break;
                case "DEFAULT":
                case 1:
                    m.type = 1;
                    break;
                case "INCOGNITO":
                case 2:
                    m.type = 2;
                    break;
                case "SIDE_CHAT":
                case 3:
                    m.type = 3;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.sourceChatJid != null) {
                    m.sourceChatJid = $String(d.sourceChatJid);
                }
                return m;
            };

            AIThreadClientInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.AIThreadInfo.AIThreadClientInfo.AIThreadType[m.type] === $undefined ? m.type : $root.proto.AIThreadInfo.AIThreadClientInfo.AIThreadType[m.type] : m.type;
                }
                if (m.sourceChatJid != null && $Object.hasOwnProperty.call(m, "sourceChatJid")) {
                    d.sourceChatJid = m.sourceChatJid;
                }
                return d;
            };

            AIThreadClientInfo.prototype.toJSON = function() {
                return AIThreadClientInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIThreadClientInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIThreadInfo.AIThreadClientInfo";
            };

            AIThreadClientInfo.AIThreadType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "DEFAULT"] = 1;
                values[valuesById[2] = "INCOGNITO"] = 2;
                values[valuesById[3] = "SIDE_CHAT"] = 3;
                return values;
            })();

            return AIThreadClientInfo;
        })();

        AIThreadInfo.AIThreadServerInfo = (function() {

            const AIThreadServerInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AIThreadServerInfo.prototype.title = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AIThreadServerInfo.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AIThreadServerInfo.create = function(properties) {
                return new AIThreadServerInfo(properties);
            };

            AIThreadServerInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AIThreadServerInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AIThreadInfo.AIThreadServerInfo();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AIThreadServerInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.AIThreadInfo.AIThreadServerInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.AIThreadInfo.AIThreadServerInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.AIThreadInfo.AIThreadServerInfo();
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                return m;
            };

            AIThreadServerInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                return d;
            };

            AIThreadServerInfo.prototype.toJSON = function() {
                return AIThreadServerInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            AIThreadServerInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.AIThreadInfo.AIThreadServerInfo";
            };

            return AIThreadServerInfo;
        })();

        return AIThreadInfo;
    })();

    proto.Account = (function() {

        const Account = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Account.prototype.lid = null;
        Account.prototype.username = null;
        Account.prototype.countryCode = null;
        Account.prototype.isUsernameDeleted = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Account.prototype, "_lid", {
            get: $util.oneOfGetter($oneOfFields = ["lid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Account.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Account.prototype, "_countryCode", {
            get: $util.oneOfGetter($oneOfFields = ["countryCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Account.prototype, "_isUsernameDeleted", {
            get: $util.oneOfGetter($oneOfFields = ["isUsernameDeleted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Account.create = function(properties) {
            return new Account(properties);
        };

        Account.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.lid != null && $Object.hasOwnProperty.call(m, "lid"))
                w.uint32(10).string(m.lid);
            if (m.username != null && $Object.hasOwnProperty.call(m, "username"))
                w.uint32(18).string(m.username);
            if (m.countryCode != null && $Object.hasOwnProperty.call(m, "countryCode"))
                w.uint32(26).string(m.countryCode);
            if (m.isUsernameDeleted != null && $Object.hasOwnProperty.call(m, "isUsernameDeleted"))
                w.uint32(32).bool(m.isUsernameDeleted);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Account.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Account();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.lid = r.stringVerify();
                        m._lid = "lid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.username = r.stringVerify();
                        m._username = "username";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.countryCode = r.stringVerify();
                        m._countryCode = "countryCode";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.isUsernameDeleted = r.bool();
                        m._isUsernameDeleted = "isUsernameDeleted";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Account.fromObject = function (d, q) {
            if (d instanceof $root.proto.Account)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Account: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Account();
            if (d.lid != null) {
                m.lid = $String(d.lid);
            }
            if (d.username != null) {
                m.username = $String(d.username);
            }
            if (d.countryCode != null) {
                m.countryCode = $String(d.countryCode);
            }
            if (d.isUsernameDeleted != null) {
                m.isUsernameDeleted = $Boolean(d.isUsernameDeleted);
            }
            return m;
        };

        Account.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.lid != null && $Object.hasOwnProperty.call(m, "lid")) {
                d.lid = m.lid;
            }
            if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                d.username = m.username;
            }
            if (m.countryCode != null && $Object.hasOwnProperty.call(m, "countryCode")) {
                d.countryCode = m.countryCode;
            }
            if (m.isUsernameDeleted != null && $Object.hasOwnProperty.call(m, "isUsernameDeleted")) {
                d.isUsernameDeleted = m.isUsernameDeleted;
            }
            return d;
        };

        Account.prototype.toJSON = function() {
            return Account.toObject(this, $protobuf.util.toJSONOptions);
        };

        Account.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Account";
        };

        return Account;
    })();

    proto.AccountLinkingOpaqueData = (function() {

        const AccountLinkingOpaqueData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AccountLinkingOpaqueData.prototype.accesstoken = null;
        AccountLinkingOpaqueData.prototype.fbid = null;
        AccountLinkingOpaqueData.prototype.nonce = null;
        AccountLinkingOpaqueData.prototype.encryptedPassword = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AccountLinkingOpaqueData.prototype, "_accesstoken", {
            get: $util.oneOfGetter($oneOfFields = ["accesstoken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AccountLinkingOpaqueData.prototype, "_fbid", {
            get: $util.oneOfGetter($oneOfFields = ["fbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AccountLinkingOpaqueData.prototype, "_nonce", {
            get: $util.oneOfGetter($oneOfFields = ["nonce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AccountLinkingOpaqueData.prototype, "_encryptedPassword", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedPassword"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AccountLinkingOpaqueData.create = function(properties) {
            return new AccountLinkingOpaqueData(properties);
        };

        AccountLinkingOpaqueData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.accesstoken != null && $Object.hasOwnProperty.call(m, "accesstoken"))
                w.uint32(10).string(m.accesstoken);
            if (m.fbid != null && $Object.hasOwnProperty.call(m, "fbid"))
                w.uint32(18).string(m.fbid);
            if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce"))
                w.uint32(26).string(m.nonce);
            if (m.encryptedPassword != null && $Object.hasOwnProperty.call(m, "encryptedPassword"))
                w.uint32(34).string(m.encryptedPassword);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AccountLinkingOpaqueData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AccountLinkingOpaqueData();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.accesstoken = r.stringVerify();
                        m._accesstoken = "accesstoken";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.fbid = r.stringVerify();
                        m._fbid = "fbid";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.nonce = r.stringVerify();
                        m._nonce = "nonce";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.encryptedPassword = r.stringVerify();
                        m._encryptedPassword = "encryptedPassword";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AccountLinkingOpaqueData.fromObject = function (d, q) {
            if (d instanceof $root.proto.AccountLinkingOpaqueData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AccountLinkingOpaqueData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AccountLinkingOpaqueData();
            if (d.accesstoken != null) {
                m.accesstoken = $String(d.accesstoken);
            }
            if (d.fbid != null) {
                m.fbid = $String(d.fbid);
            }
            if (d.nonce != null) {
                m.nonce = $String(d.nonce);
            }
            if (d.encryptedPassword != null) {
                m.encryptedPassword = $String(d.encryptedPassword);
            }
            return m;
        };

        AccountLinkingOpaqueData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.accesstoken != null && $Object.hasOwnProperty.call(m, "accesstoken")) {
                d.accesstoken = m.accesstoken;
            }
            if (m.fbid != null && $Object.hasOwnProperty.call(m, "fbid")) {
                d.fbid = m.fbid;
            }
            if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce")) {
                d.nonce = m.nonce;
            }
            if (m.encryptedPassword != null && $Object.hasOwnProperty.call(m, "encryptedPassword")) {
                d.encryptedPassword = m.encryptedPassword;
            }
            return d;
        };

        AccountLinkingOpaqueData.prototype.toJSON = function() {
            return AccountLinkingOpaqueData.toObject(this, $protobuf.util.toJSONOptions);
        };

        AccountLinkingOpaqueData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AccountLinkingOpaqueData";
        };

        return AccountLinkingOpaqueData;
    })();

    proto.ActionLink = (function() {

        const ActionLink = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ActionLink.prototype.url = null;
        ActionLink.prototype.buttonTitle = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ActionLink.prototype, "_url", {
            get: $util.oneOfGetter($oneOfFields = ["url"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ActionLink.prototype, "_buttonTitle", {
            get: $util.oneOfGetter($oneOfFields = ["buttonTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ActionLink.create = function(properties) {
            return new ActionLink(properties);
        };

        ActionLink.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                w.uint32(10).string(m.url);
            if (m.buttonTitle != null && $Object.hasOwnProperty.call(m, "buttonTitle"))
                w.uint32(18).string(m.buttonTitle);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ActionLink.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ActionLink();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.url = r.stringVerify();
                        m._url = "url";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.buttonTitle = r.stringVerify();
                        m._buttonTitle = "buttonTitle";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ActionLink.fromObject = function (d, q) {
            if (d instanceof $root.proto.ActionLink)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ActionLink: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ActionLink();
            if (d.url != null) {
                m.url = $String(d.url);
            }
            if (d.buttonTitle != null) {
                m.buttonTitle = $String(d.buttonTitle);
            }
            return m;
        };

        ActionLink.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                d.url = m.url;
            }
            if (m.buttonTitle != null && $Object.hasOwnProperty.call(m, "buttonTitle")) {
                d.buttonTitle = m.buttonTitle;
            }
            return d;
        };

        ActionLink.prototype.toJSON = function() {
            return ActionLink.toObject(this, $protobuf.util.toJSONOptions);
        };

        ActionLink.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ActionLink";
        };

        return ActionLink;
    })();

    proto.AutoDownloadSettings = (function() {

        const AutoDownloadSettings = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AutoDownloadSettings.prototype.downloadImages = null;
        AutoDownloadSettings.prototype.downloadAudio = null;
        AutoDownloadSettings.prototype.downloadVideo = null;
        AutoDownloadSettings.prototype.downloadDocuments = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AutoDownloadSettings.prototype, "_downloadImages", {
            get: $util.oneOfGetter($oneOfFields = ["downloadImages"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AutoDownloadSettings.prototype, "_downloadAudio", {
            get: $util.oneOfGetter($oneOfFields = ["downloadAudio"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AutoDownloadSettings.prototype, "_downloadVideo", {
            get: $util.oneOfGetter($oneOfFields = ["downloadVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AutoDownloadSettings.prototype, "_downloadDocuments", {
            get: $util.oneOfGetter($oneOfFields = ["downloadDocuments"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AutoDownloadSettings.create = function(properties) {
            return new AutoDownloadSettings(properties);
        };

        AutoDownloadSettings.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.downloadImages != null && $Object.hasOwnProperty.call(m, "downloadImages"))
                w.uint32(8).bool(m.downloadImages);
            if (m.downloadAudio != null && $Object.hasOwnProperty.call(m, "downloadAudio"))
                w.uint32(16).bool(m.downloadAudio);
            if (m.downloadVideo != null && $Object.hasOwnProperty.call(m, "downloadVideo"))
                w.uint32(24).bool(m.downloadVideo);
            if (m.downloadDocuments != null && $Object.hasOwnProperty.call(m, "downloadDocuments"))
                w.uint32(32).bool(m.downloadDocuments);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AutoDownloadSettings.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AutoDownloadSettings();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.downloadImages = r.bool();
                        m._downloadImages = "downloadImages";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.downloadAudio = r.bool();
                        m._downloadAudio = "downloadAudio";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.downloadVideo = r.bool();
                        m._downloadVideo = "downloadVideo";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.downloadDocuments = r.bool();
                        m._downloadDocuments = "downloadDocuments";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AutoDownloadSettings.fromObject = function (d, q) {
            if (d instanceof $root.proto.AutoDownloadSettings)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AutoDownloadSettings: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AutoDownloadSettings();
            if (d.downloadImages != null) {
                m.downloadImages = $Boolean(d.downloadImages);
            }
            if (d.downloadAudio != null) {
                m.downloadAudio = $Boolean(d.downloadAudio);
            }
            if (d.downloadVideo != null) {
                m.downloadVideo = $Boolean(d.downloadVideo);
            }
            if (d.downloadDocuments != null) {
                m.downloadDocuments = $Boolean(d.downloadDocuments);
            }
            return m;
        };

        AutoDownloadSettings.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.downloadImages != null && $Object.hasOwnProperty.call(m, "downloadImages")) {
                d.downloadImages = m.downloadImages;
            }
            if (m.downloadAudio != null && $Object.hasOwnProperty.call(m, "downloadAudio")) {
                d.downloadAudio = m.downloadAudio;
            }
            if (m.downloadVideo != null && $Object.hasOwnProperty.call(m, "downloadVideo")) {
                d.downloadVideo = m.downloadVideo;
            }
            if (m.downloadDocuments != null && $Object.hasOwnProperty.call(m, "downloadDocuments")) {
                d.downloadDocuments = m.downloadDocuments;
            }
            return d;
        };

        AutoDownloadSettings.prototype.toJSON = function() {
            return AutoDownloadSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        AutoDownloadSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AutoDownloadSettings";
        };

        return AutoDownloadSettings;
    })();

    proto.AvatarUserSettings = (function() {

        const AvatarUserSettings = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        AvatarUserSettings.prototype.fbid = null;
        AvatarUserSettings.prototype.password = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AvatarUserSettings.prototype, "_fbid", {
            get: $util.oneOfGetter($oneOfFields = ["fbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(AvatarUserSettings.prototype, "_password", {
            get: $util.oneOfGetter($oneOfFields = ["password"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        AvatarUserSettings.create = function(properties) {
            return new AvatarUserSettings(properties);
        };

        AvatarUserSettings.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.fbid != null && $Object.hasOwnProperty.call(m, "fbid"))
                w.uint32(10).string(m.fbid);
            if (m.password != null && $Object.hasOwnProperty.call(m, "password"))
                w.uint32(18).string(m.password);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        AvatarUserSettings.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.AvatarUserSettings();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.fbid = r.stringVerify();
                        m._fbid = "fbid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.password = r.stringVerify();
                        m._password = "password";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        AvatarUserSettings.fromObject = function (d, q) {
            if (d instanceof $root.proto.AvatarUserSettings)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.AvatarUserSettings: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.AvatarUserSettings();
            if (d.fbid != null) {
                m.fbid = $String(d.fbid);
            }
            if (d.password != null) {
                m.password = $String(d.password);
            }
            return m;
        };

        AvatarUserSettings.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.fbid != null && $Object.hasOwnProperty.call(m, "fbid")) {
                d.fbid = m.fbid;
            }
            if (m.password != null && $Object.hasOwnProperty.call(m, "password")) {
                d.password = m.password;
            }
            return d;
        };

        AvatarUserSettings.prototype.toJSON = function() {
            return AvatarUserSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        AvatarUserSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.AvatarUserSettings";
        };

        return AvatarUserSettings;
    })();

    proto.BackwardEdge = (function() {

        const BackwardEdge = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BackwardEdge.prototype.encryptedPrevEpochAnonId = null;
        BackwardEdge.prototype.encryptedPrevEpochRootKey = null;
        BackwardEdge.prototype.prevEpochRootKeyFingerprint = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BackwardEdge.prototype, "_encryptedPrevEpochAnonId", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedPrevEpochAnonId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BackwardEdge.prototype, "_encryptedPrevEpochRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedPrevEpochRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BackwardEdge.prototype, "_prevEpochRootKeyFingerprint", {
            get: $util.oneOfGetter($oneOfFields = ["prevEpochRootKeyFingerprint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BackwardEdge.create = function(properties) {
            return new BackwardEdge(properties);
        };

        BackwardEdge.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.encryptedPrevEpochAnonId != null && $Object.hasOwnProperty.call(m, "encryptedPrevEpochAnonId"))
                w.uint32(10).bytes(m.encryptedPrevEpochAnonId);
            if (m.encryptedPrevEpochRootKey != null && $Object.hasOwnProperty.call(m, "encryptedPrevEpochRootKey"))
                w.uint32(18).bytes(m.encryptedPrevEpochRootKey);
            if (m.prevEpochRootKeyFingerprint != null && $Object.hasOwnProperty.call(m, "prevEpochRootKeyFingerprint"))
                w.uint32(26).bytes(m.prevEpochRootKeyFingerprint);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BackwardEdge.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BackwardEdge();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.encryptedPrevEpochAnonId = r.bytes();
                        m._encryptedPrevEpochAnonId = "encryptedPrevEpochAnonId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.encryptedPrevEpochRootKey = r.bytes();
                        m._encryptedPrevEpochRootKey = "encryptedPrevEpochRootKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.prevEpochRootKeyFingerprint = r.bytes();
                        m._prevEpochRootKeyFingerprint = "prevEpochRootKeyFingerprint";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BackwardEdge.fromObject = function (d, q) {
            if (d instanceof $root.proto.BackwardEdge)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BackwardEdge: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BackwardEdge();
            if (d.encryptedPrevEpochAnonId != null) {
                if (typeof d.encryptedPrevEpochAnonId === "string")
                    $util.base64.decode(d.encryptedPrevEpochAnonId, m.encryptedPrevEpochAnonId = $util.newBuffer($util.base64.length(d.encryptedPrevEpochAnonId)), 0);
                else if (d.encryptedPrevEpochAnonId.length >= 0)
                    m.encryptedPrevEpochAnonId = d.encryptedPrevEpochAnonId;
            }
            if (d.encryptedPrevEpochRootKey != null) {
                if (typeof d.encryptedPrevEpochRootKey === "string")
                    $util.base64.decode(d.encryptedPrevEpochRootKey, m.encryptedPrevEpochRootKey = $util.newBuffer($util.base64.length(d.encryptedPrevEpochRootKey)), 0);
                else if (d.encryptedPrevEpochRootKey.length >= 0)
                    m.encryptedPrevEpochRootKey = d.encryptedPrevEpochRootKey;
            }
            if (d.prevEpochRootKeyFingerprint != null) {
                if (typeof d.prevEpochRootKeyFingerprint === "string")
                    $util.base64.decode(d.prevEpochRootKeyFingerprint, m.prevEpochRootKeyFingerprint = $util.newBuffer($util.base64.length(d.prevEpochRootKeyFingerprint)), 0);
                else if (d.prevEpochRootKeyFingerprint.length >= 0)
                    m.prevEpochRootKeyFingerprint = d.prevEpochRootKeyFingerprint;
            }
            return m;
        };

        BackwardEdge.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.encryptedPrevEpochAnonId != null && $Object.hasOwnProperty.call(m, "encryptedPrevEpochAnonId")) {
                d.encryptedPrevEpochAnonId = o.bytes === $String ? $util.base64.encode(m.encryptedPrevEpochAnonId, 0, m.encryptedPrevEpochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedPrevEpochAnonId) : m.encryptedPrevEpochAnonId;
            }
            if (m.encryptedPrevEpochRootKey != null && $Object.hasOwnProperty.call(m, "encryptedPrevEpochRootKey")) {
                d.encryptedPrevEpochRootKey = o.bytes === $String ? $util.base64.encode(m.encryptedPrevEpochRootKey, 0, m.encryptedPrevEpochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedPrevEpochRootKey) : m.encryptedPrevEpochRootKey;
            }
            if (m.prevEpochRootKeyFingerprint != null && $Object.hasOwnProperty.call(m, "prevEpochRootKeyFingerprint")) {
                d.prevEpochRootKeyFingerprint = o.bytes === $String ? $util.base64.encode(m.prevEpochRootKeyFingerprint, 0, m.prevEpochRootKeyFingerprint.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.prevEpochRootKeyFingerprint) : m.prevEpochRootKeyFingerprint;
            }
            return d;
        };

        BackwardEdge.prototype.toJSON = function() {
            return BackwardEdge.toObject(this, $protobuf.util.toJSONOptions);
        };

        BackwardEdge.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BackwardEdge";
        };

        return BackwardEdge;
    })();

    proto.BizAccountLinkInfo = (function() {

        const BizAccountLinkInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BizAccountLinkInfo.prototype.whatsappBizAcctFbid = null;
        BizAccountLinkInfo.prototype.whatsappAcctNumber = null;
        BizAccountLinkInfo.prototype.issueTime = null;
        BizAccountLinkInfo.prototype.hostStorage = null;
        BizAccountLinkInfo.prototype.accountType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountLinkInfo.prototype, "_whatsappBizAcctFbid", {
            get: $util.oneOfGetter($oneOfFields = ["whatsappBizAcctFbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountLinkInfo.prototype, "_whatsappAcctNumber", {
            get: $util.oneOfGetter($oneOfFields = ["whatsappAcctNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountLinkInfo.prototype, "_issueTime", {
            get: $util.oneOfGetter($oneOfFields = ["issueTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountLinkInfo.prototype, "_hostStorage", {
            get: $util.oneOfGetter($oneOfFields = ["hostStorage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountLinkInfo.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BizAccountLinkInfo.create = function(properties) {
            return new BizAccountLinkInfo(properties);
        };

        BizAccountLinkInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.whatsappBizAcctFbid != null && $Object.hasOwnProperty.call(m, "whatsappBizAcctFbid"))
                w.uint32(8).uint64(m.whatsappBizAcctFbid);
            if (m.whatsappAcctNumber != null && $Object.hasOwnProperty.call(m, "whatsappAcctNumber"))
                w.uint32(18).string(m.whatsappAcctNumber);
            if (m.issueTime != null && $Object.hasOwnProperty.call(m, "issueTime"))
                w.uint32(24).uint64(m.issueTime);
            if (m.hostStorage != null && $Object.hasOwnProperty.call(m, "hostStorage"))
                w.uint32(32).int32(m.hostStorage);
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(40).int32(m.accountType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BizAccountLinkInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BizAccountLinkInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.whatsappBizAcctFbid = r.uint64();
                        m._whatsappBizAcctFbid = "whatsappBizAcctFbid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.whatsappAcctNumber = r.stringVerify();
                        m._whatsappAcctNumber = "whatsappAcctNumber";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.issueTime = r.uint64();
                        m._issueTime = "issueTime";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.hostStorage = r.int32();
                        m._hostStorage = "hostStorage";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.accountType = r.int32();
                        m._accountType = "accountType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BizAccountLinkInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.BizAccountLinkInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BizAccountLinkInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BizAccountLinkInfo();
            if (d.whatsappBizAcctFbid != null) {
                if ($util.Long)
                    m.whatsappBizAcctFbid = $util.Long.fromValue(d.whatsappBizAcctFbid, true);
                else if (typeof d.whatsappBizAcctFbid === "string")
                    m.whatsappBizAcctFbid = $parseInt(d.whatsappBizAcctFbid, 10);
                else if (typeof d.whatsappBizAcctFbid === "number")
                    m.whatsappBizAcctFbid = d.whatsappBizAcctFbid;
                else if (typeof d.whatsappBizAcctFbid === "object")
                    m.whatsappBizAcctFbid = new $util.LongBits(d.whatsappBizAcctFbid.low >>> 0, d.whatsappBizAcctFbid.high >>> 0).toNumber(true);
            }
            if (d.whatsappAcctNumber != null) {
                m.whatsappAcctNumber = $String(d.whatsappAcctNumber);
            }
            if (d.issueTime != null) {
                if ($util.Long)
                    m.issueTime = $util.Long.fromValue(d.issueTime, true);
                else if (typeof d.issueTime === "string")
                    m.issueTime = $parseInt(d.issueTime, 10);
                else if (typeof d.issueTime === "number")
                    m.issueTime = d.issueTime;
                else if (typeof d.issueTime === "object")
                    m.issueTime = new $util.LongBits(d.issueTime.low >>> 0, d.issueTime.high >>> 0).toNumber(true);
            }
            switch (d.hostStorage) {
            case "ON_PREMISE":
            case 0:
                m.hostStorage = 0;
                break;
            case "FACEBOOK":
            case 1:
                m.hostStorage = 1;
                break;
            default:
                if (typeof d.hostStorage === "number" && (d.hostStorage | 0) === d.hostStorage)
                    m.hostStorage = d.hostStorage;
            }
            switch (d.accountType) {
            case "ENTERPRISE":
            case 0:
                m.accountType = 0;
                break;
            default:
                if (typeof d.accountType === "number" && (d.accountType | 0) === d.accountType)
                    m.accountType = d.accountType;
            }
            return m;
        };

        BizAccountLinkInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.whatsappBizAcctFbid != null && $Object.hasOwnProperty.call(m, "whatsappBizAcctFbid")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.whatsappBizAcctFbid = typeof m.whatsappBizAcctFbid === "number" ? $BigInt(m.whatsappBizAcctFbid) : $util.Long.fromBits(m.whatsappBizAcctFbid.low >>> 0, m.whatsappBizAcctFbid.high >>> 0, true).toBigInt();
                else if (typeof m.whatsappBizAcctFbid === "number")
                    d.whatsappBizAcctFbid = o.longs === $String ? $String(m.whatsappBizAcctFbid) : m.whatsappBizAcctFbid;
                else
                    d.whatsappBizAcctFbid = o.longs === $String ? $util.Long.prototype.toString.call(m.whatsappBizAcctFbid) : o.longs === $Number ? new $util.LongBits(m.whatsappBizAcctFbid.low >>> 0, m.whatsappBizAcctFbid.high >>> 0).toNumber(true) : m.whatsappBizAcctFbid;
            }
            if (m.whatsappAcctNumber != null && $Object.hasOwnProperty.call(m, "whatsappAcctNumber")) {
                d.whatsappAcctNumber = m.whatsappAcctNumber;
            }
            if (m.issueTime != null && $Object.hasOwnProperty.call(m, "issueTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.issueTime = typeof m.issueTime === "number" ? $BigInt(m.issueTime) : $util.Long.fromBits(m.issueTime.low >>> 0, m.issueTime.high >>> 0, true).toBigInt();
                else if (typeof m.issueTime === "number")
                    d.issueTime = o.longs === $String ? $String(m.issueTime) : m.issueTime;
                else
                    d.issueTime = o.longs === $String ? $util.Long.prototype.toString.call(m.issueTime) : o.longs === $Number ? new $util.LongBits(m.issueTime.low >>> 0, m.issueTime.high >>> 0).toNumber(true) : m.issueTime;
            }
            if (m.hostStorage != null && $Object.hasOwnProperty.call(m, "hostStorage")) {
                d.hostStorage = o.enums === $String ? $root.proto.BizAccountLinkInfo.HostStorageType[m.hostStorage] === $undefined ? m.hostStorage : $root.proto.BizAccountLinkInfo.HostStorageType[m.hostStorage] : m.hostStorage;
            }
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType")) {
                d.accountType = o.enums === $String ? $root.proto.BizAccountLinkInfo.AccountType[m.accountType] === $undefined ? m.accountType : $root.proto.BizAccountLinkInfo.AccountType[m.accountType] : m.accountType;
            }
            return d;
        };

        BizAccountLinkInfo.prototype.toJSON = function() {
            return BizAccountLinkInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        BizAccountLinkInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BizAccountLinkInfo";
        };

        BizAccountLinkInfo.AccountType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ENTERPRISE"] = 0;
            return values;
        })();

        BizAccountLinkInfo.HostStorageType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ON_PREMISE"] = 0;
            values[valuesById[1] = "FACEBOOK"] = 1;
            return values;
        })();

        return BizAccountLinkInfo;
    })();

    proto.BizAccountPayload = (function() {

        const BizAccountPayload = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BizAccountPayload.prototype.vnameCert = null;
        BizAccountPayload.prototype.bizAcctLinkInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountPayload.prototype, "_vnameCert", {
            get: $util.oneOfGetter($oneOfFields = ["vnameCert"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizAccountPayload.prototype, "_bizAcctLinkInfo", {
            get: $util.oneOfGetter($oneOfFields = ["bizAcctLinkInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BizAccountPayload.create = function(properties) {
            return new BizAccountPayload(properties);
        };

        BizAccountPayload.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.vnameCert != null && $Object.hasOwnProperty.call(m, "vnameCert"))
                $root.proto.VerifiedNameCertificate.encode(m.vnameCert, w.uint32(10).fork(), q + 1).ldelim();
            if (m.bizAcctLinkInfo != null && $Object.hasOwnProperty.call(m, "bizAcctLinkInfo"))
                w.uint32(18).bytes(m.bizAcctLinkInfo);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BizAccountPayload.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BizAccountPayload();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.vnameCert = $root.proto.VerifiedNameCertificate.decode(r, r.uint32(), $undefined, q + 1, m.vnameCert);
                        m._vnameCert = "vnameCert";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.bizAcctLinkInfo = r.bytes();
                        m._bizAcctLinkInfo = "bizAcctLinkInfo";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BizAccountPayload.fromObject = function (d, q) {
            if (d instanceof $root.proto.BizAccountPayload)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BizAccountPayload: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BizAccountPayload();
            if (d.vnameCert != null) {
                if (!$util.isObject(d.vnameCert))
                    throw $TypeError(".proto.BizAccountPayload.vnameCert: object expected");
                m.vnameCert = $root.proto.VerifiedNameCertificate.fromObject(d.vnameCert, q + 1);
            }
            if (d.bizAcctLinkInfo != null) {
                if (typeof d.bizAcctLinkInfo === "string")
                    $util.base64.decode(d.bizAcctLinkInfo, m.bizAcctLinkInfo = $util.newBuffer($util.base64.length(d.bizAcctLinkInfo)), 0);
                else if (d.bizAcctLinkInfo.length >= 0)
                    m.bizAcctLinkInfo = d.bizAcctLinkInfo;
            }
            return m;
        };

        BizAccountPayload.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.vnameCert != null && $Object.hasOwnProperty.call(m, "vnameCert")) {
                d.vnameCert = $root.proto.VerifiedNameCertificate.toObject(m.vnameCert, o, q + 1);
            }
            if (m.bizAcctLinkInfo != null && $Object.hasOwnProperty.call(m, "bizAcctLinkInfo")) {
                d.bizAcctLinkInfo = o.bytes === $String ? $util.base64.encode(m.bizAcctLinkInfo, 0, m.bizAcctLinkInfo.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.bizAcctLinkInfo) : m.bizAcctLinkInfo;
            }
            return d;
        };

        BizAccountPayload.prototype.toJSON = function() {
            return BizAccountPayload.toObject(this, $protobuf.util.toJSONOptions);
        };

        BizAccountPayload.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BizAccountPayload";
        };

        return BizAccountPayload;
    })();

    proto.BizIdentityInfo = (function() {

        const BizIdentityInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BizIdentityInfo.prototype.vlevel = null;
        BizIdentityInfo.prototype.vnameCert = null;
        BizIdentityInfo.prototype.signed = null;
        BizIdentityInfo.prototype.revoked = null;
        BizIdentityInfo.prototype.hostStorage = null;
        BizIdentityInfo.prototype.actualActors = null;
        BizIdentityInfo.prototype.privacyModeTs = null;
        BizIdentityInfo.prototype.featureControls = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_vlevel", {
            get: $util.oneOfGetter($oneOfFields = ["vlevel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_vnameCert", {
            get: $util.oneOfGetter($oneOfFields = ["vnameCert"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_signed", {
            get: $util.oneOfGetter($oneOfFields = ["signed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_revoked", {
            get: $util.oneOfGetter($oneOfFields = ["revoked"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_hostStorage", {
            get: $util.oneOfGetter($oneOfFields = ["hostStorage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_actualActors", {
            get: $util.oneOfGetter($oneOfFields = ["actualActors"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_privacyModeTs", {
            get: $util.oneOfGetter($oneOfFields = ["privacyModeTs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BizIdentityInfo.prototype, "_featureControls", {
            get: $util.oneOfGetter($oneOfFields = ["featureControls"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BizIdentityInfo.create = function(properties) {
            return new BizIdentityInfo(properties);
        };

        BizIdentityInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.vlevel != null && $Object.hasOwnProperty.call(m, "vlevel"))
                w.uint32(8).int32(m.vlevel);
            if (m.vnameCert != null && $Object.hasOwnProperty.call(m, "vnameCert"))
                $root.proto.VerifiedNameCertificate.encode(m.vnameCert, w.uint32(18).fork(), q + 1).ldelim();
            if (m.signed != null && $Object.hasOwnProperty.call(m, "signed"))
                w.uint32(24).bool(m.signed);
            if (m.revoked != null && $Object.hasOwnProperty.call(m, "revoked"))
                w.uint32(32).bool(m.revoked);
            if (m.hostStorage != null && $Object.hasOwnProperty.call(m, "hostStorage"))
                w.uint32(40).int32(m.hostStorage);
            if (m.actualActors != null && $Object.hasOwnProperty.call(m, "actualActors"))
                w.uint32(48).int32(m.actualActors);
            if (m.privacyModeTs != null && $Object.hasOwnProperty.call(m, "privacyModeTs"))
                w.uint32(56).uint64(m.privacyModeTs);
            if (m.featureControls != null && $Object.hasOwnProperty.call(m, "featureControls"))
                w.uint32(64).uint64(m.featureControls);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BizIdentityInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BizIdentityInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.vlevel = r.int32();
                        m._vlevel = "vlevel";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.vnameCert = $root.proto.VerifiedNameCertificate.decode(r, r.uint32(), $undefined, q + 1, m.vnameCert);
                        m._vnameCert = "vnameCert";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.signed = r.bool();
                        m._signed = "signed";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.revoked = r.bool();
                        m._revoked = "revoked";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.hostStorage = r.int32();
                        m._hostStorage = "hostStorage";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.actualActors = r.int32();
                        m._actualActors = "actualActors";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.privacyModeTs = r.uint64();
                        m._privacyModeTs = "privacyModeTs";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.featureControls = r.uint64();
                        m._featureControls = "featureControls";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BizIdentityInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.BizIdentityInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BizIdentityInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BizIdentityInfo();
            switch (d.vlevel) {
            case "UNKNOWN":
            case 0:
                m.vlevel = 0;
                break;
            case "LOW":
            case 1:
                m.vlevel = 1;
                break;
            case "HIGH":
            case 2:
                m.vlevel = 2;
                break;
            default:
                if (typeof d.vlevel === "number" && (d.vlevel | 0) === d.vlevel)
                    m.vlevel = d.vlevel;
            }
            if (d.vnameCert != null) {
                if (!$util.isObject(d.vnameCert))
                    throw $TypeError(".proto.BizIdentityInfo.vnameCert: object expected");
                m.vnameCert = $root.proto.VerifiedNameCertificate.fromObject(d.vnameCert, q + 1);
            }
            if (d.signed != null) {
                m.signed = $Boolean(d.signed);
            }
            if (d.revoked != null) {
                m.revoked = $Boolean(d.revoked);
            }
            switch (d.hostStorage) {
            case "ON_PREMISE":
            case 0:
                m.hostStorage = 0;
                break;
            case "FACEBOOK":
            case 1:
                m.hostStorage = 1;
                break;
            default:
                if (typeof d.hostStorage === "number" && (d.hostStorage | 0) === d.hostStorage)
                    m.hostStorage = d.hostStorage;
            }
            switch (d.actualActors) {
            case "SELF":
            case 0:
                m.actualActors = 0;
                break;
            case "BSP":
            case 1:
                m.actualActors = 1;
                break;
            default:
                if (typeof d.actualActors === "number" && (d.actualActors | 0) === d.actualActors)
                    m.actualActors = d.actualActors;
            }
            if (d.privacyModeTs != null) {
                if ($util.Long)
                    m.privacyModeTs = $util.Long.fromValue(d.privacyModeTs, true);
                else if (typeof d.privacyModeTs === "string")
                    m.privacyModeTs = $parseInt(d.privacyModeTs, 10);
                else if (typeof d.privacyModeTs === "number")
                    m.privacyModeTs = d.privacyModeTs;
                else if (typeof d.privacyModeTs === "object")
                    m.privacyModeTs = new $util.LongBits(d.privacyModeTs.low >>> 0, d.privacyModeTs.high >>> 0).toNumber(true);
            }
            if (d.featureControls != null) {
                if ($util.Long)
                    m.featureControls = $util.Long.fromValue(d.featureControls, true);
                else if (typeof d.featureControls === "string")
                    m.featureControls = $parseInt(d.featureControls, 10);
                else if (typeof d.featureControls === "number")
                    m.featureControls = d.featureControls;
                else if (typeof d.featureControls === "object")
                    m.featureControls = new $util.LongBits(d.featureControls.low >>> 0, d.featureControls.high >>> 0).toNumber(true);
            }
            return m;
        };

        BizIdentityInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.vlevel != null && $Object.hasOwnProperty.call(m, "vlevel")) {
                d.vlevel = o.enums === $String ? $root.proto.BizIdentityInfo.VerifiedLevelValue[m.vlevel] === $undefined ? m.vlevel : $root.proto.BizIdentityInfo.VerifiedLevelValue[m.vlevel] : m.vlevel;
            }
            if (m.vnameCert != null && $Object.hasOwnProperty.call(m, "vnameCert")) {
                d.vnameCert = $root.proto.VerifiedNameCertificate.toObject(m.vnameCert, o, q + 1);
            }
            if (m.signed != null && $Object.hasOwnProperty.call(m, "signed")) {
                d.signed = m.signed;
            }
            if (m.revoked != null && $Object.hasOwnProperty.call(m, "revoked")) {
                d.revoked = m.revoked;
            }
            if (m.hostStorage != null && $Object.hasOwnProperty.call(m, "hostStorage")) {
                d.hostStorage = o.enums === $String ? $root.proto.BizIdentityInfo.HostStorageType[m.hostStorage] === $undefined ? m.hostStorage : $root.proto.BizIdentityInfo.HostStorageType[m.hostStorage] : m.hostStorage;
            }
            if (m.actualActors != null && $Object.hasOwnProperty.call(m, "actualActors")) {
                d.actualActors = o.enums === $String ? $root.proto.BizIdentityInfo.ActualActorsType[m.actualActors] === $undefined ? m.actualActors : $root.proto.BizIdentityInfo.ActualActorsType[m.actualActors] : m.actualActors;
            }
            if (m.privacyModeTs != null && $Object.hasOwnProperty.call(m, "privacyModeTs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.privacyModeTs = typeof m.privacyModeTs === "number" ? $BigInt(m.privacyModeTs) : $util.Long.fromBits(m.privacyModeTs.low >>> 0, m.privacyModeTs.high >>> 0, true).toBigInt();
                else if (typeof m.privacyModeTs === "number")
                    d.privacyModeTs = o.longs === $String ? $String(m.privacyModeTs) : m.privacyModeTs;
                else
                    d.privacyModeTs = o.longs === $String ? $util.Long.prototype.toString.call(m.privacyModeTs) : o.longs === $Number ? new $util.LongBits(m.privacyModeTs.low >>> 0, m.privacyModeTs.high >>> 0).toNumber(true) : m.privacyModeTs;
            }
            if (m.featureControls != null && $Object.hasOwnProperty.call(m, "featureControls")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.featureControls = typeof m.featureControls === "number" ? $BigInt(m.featureControls) : $util.Long.fromBits(m.featureControls.low >>> 0, m.featureControls.high >>> 0, true).toBigInt();
                else if (typeof m.featureControls === "number")
                    d.featureControls = o.longs === $String ? $String(m.featureControls) : m.featureControls;
                else
                    d.featureControls = o.longs === $String ? $util.Long.prototype.toString.call(m.featureControls) : o.longs === $Number ? new $util.LongBits(m.featureControls.low >>> 0, m.featureControls.high >>> 0).toNumber(true) : m.featureControls;
            }
            return d;
        };

        BizIdentityInfo.prototype.toJSON = function() {
            return BizIdentityInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        BizIdentityInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BizIdentityInfo";
        };

        BizIdentityInfo.ActualActorsType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "SELF"] = 0;
            values[valuesById[1] = "BSP"] = 1;
            return values;
        })();

        BizIdentityInfo.HostStorageType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ON_PREMISE"] = 0;
            values[valuesById[1] = "FACEBOOK"] = 1;
            return values;
        })();

        BizIdentityInfo.VerifiedLevelValue = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "LOW"] = 1;
            values[valuesById[2] = "HIGH"] = 2;
            return values;
        })();

        return BizIdentityInfo;
    })();

    proto.BotAgeCollectionMetadata = (function() {

        const BotAgeCollectionMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotAgeCollectionMetadata.prototype.ageCollectionEligible = null;
        BotAgeCollectionMetadata.prototype.shouldTriggerAgeCollectionOnClient = null;
        BotAgeCollectionMetadata.prototype.ageCollectionType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotAgeCollectionMetadata.prototype, "_ageCollectionEligible", {
            get: $util.oneOfGetter($oneOfFields = ["ageCollectionEligible"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotAgeCollectionMetadata.prototype, "_shouldTriggerAgeCollectionOnClient", {
            get: $util.oneOfGetter($oneOfFields = ["shouldTriggerAgeCollectionOnClient"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotAgeCollectionMetadata.prototype, "_ageCollectionType", {
            get: $util.oneOfGetter($oneOfFields = ["ageCollectionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotAgeCollectionMetadata.create = function(properties) {
            return new BotAgeCollectionMetadata(properties);
        };

        BotAgeCollectionMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.ageCollectionEligible != null && $Object.hasOwnProperty.call(m, "ageCollectionEligible"))
                w.uint32(8).bool(m.ageCollectionEligible);
            if (m.shouldTriggerAgeCollectionOnClient != null && $Object.hasOwnProperty.call(m, "shouldTriggerAgeCollectionOnClient"))
                w.uint32(16).bool(m.shouldTriggerAgeCollectionOnClient);
            if (m.ageCollectionType != null && $Object.hasOwnProperty.call(m, "ageCollectionType"))
                w.uint32(24).int32(m.ageCollectionType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotAgeCollectionMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotAgeCollectionMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.ageCollectionEligible = r.bool();
                        m._ageCollectionEligible = "ageCollectionEligible";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.shouldTriggerAgeCollectionOnClient = r.bool();
                        m._shouldTriggerAgeCollectionOnClient = "shouldTriggerAgeCollectionOnClient";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.ageCollectionType = r.int32();
                        m._ageCollectionType = "ageCollectionType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotAgeCollectionMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotAgeCollectionMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotAgeCollectionMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotAgeCollectionMetadata();
            if (d.ageCollectionEligible != null) {
                m.ageCollectionEligible = $Boolean(d.ageCollectionEligible);
            }
            if (d.shouldTriggerAgeCollectionOnClient != null) {
                m.shouldTriggerAgeCollectionOnClient = $Boolean(d.shouldTriggerAgeCollectionOnClient);
            }
            switch (d.ageCollectionType) {
            case "O18_BINARY":
            case 0:
                m.ageCollectionType = 0;
                break;
            case "WAFFLE":
            case 1:
                m.ageCollectionType = 1;
                break;
            default:
                if (typeof d.ageCollectionType === "number" && (d.ageCollectionType | 0) === d.ageCollectionType)
                    m.ageCollectionType = d.ageCollectionType;
            }
            return m;
        };

        BotAgeCollectionMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.ageCollectionEligible != null && $Object.hasOwnProperty.call(m, "ageCollectionEligible")) {
                d.ageCollectionEligible = m.ageCollectionEligible;
            }
            if (m.shouldTriggerAgeCollectionOnClient != null && $Object.hasOwnProperty.call(m, "shouldTriggerAgeCollectionOnClient")) {
                d.shouldTriggerAgeCollectionOnClient = m.shouldTriggerAgeCollectionOnClient;
            }
            if (m.ageCollectionType != null && $Object.hasOwnProperty.call(m, "ageCollectionType")) {
                d.ageCollectionType = o.enums === $String ? $root.proto.BotAgeCollectionMetadata.AgeCollectionType[m.ageCollectionType] === $undefined ? m.ageCollectionType : $root.proto.BotAgeCollectionMetadata.AgeCollectionType[m.ageCollectionType] : m.ageCollectionType;
            }
            return d;
        };

        BotAgeCollectionMetadata.prototype.toJSON = function() {
            return BotAgeCollectionMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotAgeCollectionMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotAgeCollectionMetadata";
        };

        BotAgeCollectionMetadata.AgeCollectionType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "O18_BINARY"] = 0;
            values[valuesById[1] = "WAFFLE"] = 1;
            return values;
        })();

        return BotAgeCollectionMetadata;
    })();

    proto.BotAgentDeepLinkMetadata = (function() {

        const BotAgentDeepLinkMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotAgentDeepLinkMetadata.prototype.token = null;
        BotAgentDeepLinkMetadata.prototype.clientPublicKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotAgentDeepLinkMetadata.prototype, "_token", {
            get: $util.oneOfGetter($oneOfFields = ["token"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotAgentDeepLinkMetadata.prototype, "_clientPublicKey", {
            get: $util.oneOfGetter($oneOfFields = ["clientPublicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotAgentDeepLinkMetadata.create = function(properties) {
            return new BotAgentDeepLinkMetadata(properties);
        };

        BotAgentDeepLinkMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.token != null && $Object.hasOwnProperty.call(m, "token"))
                w.uint32(10).string(m.token);
            if (m.clientPublicKey != null && $Object.hasOwnProperty.call(m, "clientPublicKey"))
                w.uint32(18).bytes(m.clientPublicKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotAgentDeepLinkMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotAgentDeepLinkMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.token = r.stringVerify();
                        m._token = "token";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.clientPublicKey = r.bytes();
                        m._clientPublicKey = "clientPublicKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotAgentDeepLinkMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotAgentDeepLinkMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotAgentDeepLinkMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotAgentDeepLinkMetadata();
            if (d.token != null) {
                m.token = $String(d.token);
            }
            if (d.clientPublicKey != null) {
                if (typeof d.clientPublicKey === "string")
                    $util.base64.decode(d.clientPublicKey, m.clientPublicKey = $util.newBuffer($util.base64.length(d.clientPublicKey)), 0);
                else if (d.clientPublicKey.length >= 0)
                    m.clientPublicKey = d.clientPublicKey;
            }
            return m;
        };

        BotAgentDeepLinkMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.token != null && $Object.hasOwnProperty.call(m, "token")) {
                d.token = m.token;
            }
            if (m.clientPublicKey != null && $Object.hasOwnProperty.call(m, "clientPublicKey")) {
                d.clientPublicKey = o.bytes === $String ? $util.base64.encode(m.clientPublicKey, 0, m.clientPublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.clientPublicKey) : m.clientPublicKey;
            }
            return d;
        };

        BotAgentDeepLinkMetadata.prototype.toJSON = function() {
            return BotAgentDeepLinkMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotAgentDeepLinkMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotAgentDeepLinkMetadata";
        };

        return BotAgentDeepLinkMetadata;
    })();

    proto.BotAgentMetadata = (function() {

        const BotAgentMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotAgentMetadata.prototype.deepLinkMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotAgentMetadata.prototype, "_deepLinkMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["deepLinkMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotAgentMetadata.create = function(properties) {
            return new BotAgentMetadata(properties);
        };

        BotAgentMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.deepLinkMetadata != null && $Object.hasOwnProperty.call(m, "deepLinkMetadata"))
                $root.proto.BotAgentDeepLinkMetadata.encode(m.deepLinkMetadata, w.uint32(10).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotAgentMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotAgentMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.deepLinkMetadata = $root.proto.BotAgentDeepLinkMetadata.decode(r, r.uint32(), $undefined, q + 1, m.deepLinkMetadata);
                        m._deepLinkMetadata = "deepLinkMetadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotAgentMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotAgentMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotAgentMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotAgentMetadata();
            if (d.deepLinkMetadata != null) {
                if (!$util.isObject(d.deepLinkMetadata))
                    throw $TypeError(".proto.BotAgentMetadata.deepLinkMetadata: object expected");
                m.deepLinkMetadata = $root.proto.BotAgentDeepLinkMetadata.fromObject(d.deepLinkMetadata, q + 1);
            }
            return m;
        };

        BotAgentMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.deepLinkMetadata != null && $Object.hasOwnProperty.call(m, "deepLinkMetadata")) {
                d.deepLinkMetadata = $root.proto.BotAgentDeepLinkMetadata.toObject(m.deepLinkMetadata, o, q + 1);
            }
            return d;
        };

        BotAgentMetadata.prototype.toJSON = function() {
            return BotAgentMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotAgentMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotAgentMetadata";
        };

        return BotAgentMetadata;
    })();

    proto.BotCapabilityMetadata = (function() {

        const BotCapabilityMetadata = function (p) {
            this.capabilities = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotCapabilityMetadata.prototype.capabilities = $util.emptyArray;

        BotCapabilityMetadata.create = function(properties) {
            return new BotCapabilityMetadata(properties);
        };

        BotCapabilityMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.capabilities != null && m.capabilities.length) {
                w.uint32(10).int32s(m.capabilities);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotCapabilityMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotCapabilityMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u === 2) {
                            if (!(m.capabilities && m.capabilities.length))
                                m.capabilities = [];
                            r.int32s(m.capabilities);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.capabilities && m.capabilities.length))
                            m.capabilities = [];
                        m.capabilities.push(r.int32());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotCapabilityMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotCapabilityMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotCapabilityMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotCapabilityMetadata();
            if (d.capabilities) {
                if (!$Array.isArray(d.capabilities))
                    throw $TypeError(".proto.BotCapabilityMetadata.capabilities: array expected");
                m.capabilities = [];
                for (var i = 0; i < d.capabilities.length; ++i) {
                    switch (d.capabilities[i]) {
                    case "UNKNOWN":
                    case 0:
                        m.capabilities[m.capabilities.length] = 0;
                        break;
                    case "PROGRESS_INDICATOR":
                    case 1:
                        m.capabilities[m.capabilities.length] = 1;
                        break;
                    case "RICH_RESPONSE_HEADING":
                    case 2:
                        m.capabilities[m.capabilities.length] = 2;
                        break;
                    case "RICH_RESPONSE_NESTED_LIST":
                    case 3:
                        m.capabilities[m.capabilities.length] = 3;
                        break;
                    case "AI_MEMORY":
                    case 4:
                        m.capabilities[m.capabilities.length] = 4;
                        break;
                    case "RICH_RESPONSE_THREAD_SURFING":
                    case 5:
                        m.capabilities[m.capabilities.length] = 5;
                        break;
                    case "RICH_RESPONSE_TABLE":
                    case 6:
                        m.capabilities[m.capabilities.length] = 6;
                        break;
                    case "RICH_RESPONSE_CODE":
                    case 7:
                        m.capabilities[m.capabilities.length] = 7;
                        break;
                    case "RICH_RESPONSE_STRUCTURED_RESPONSE":
                    case 8:
                        m.capabilities[m.capabilities.length] = 8;
                        break;
                    case "RICH_RESPONSE_INLINE_IMAGE":
                    case 9:
                        m.capabilities[m.capabilities.length] = 9;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_CONTROL":
                    case 10:
                        m.capabilities[m.capabilities.length] = 10;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_1":
                    case 11:
                        m.capabilities[m.capabilities.length] = 11;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_2":
                    case 12:
                        m.capabilities[m.capabilities.length] = 12;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_3":
                    case 13:
                        m.capabilities[m.capabilities.length] = 13;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_4":
                    case 14:
                        m.capabilities[m.capabilities.length] = 14;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_5":
                    case 15:
                        m.capabilities[m.capabilities.length] = 15;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_6":
                    case 16:
                        m.capabilities[m.capabilities.length] = 16;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_7":
                    case 17:
                        m.capabilities[m.capabilities.length] = 17;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_8":
                    case 18:
                        m.capabilities[m.capabilities.length] = 18;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_9":
                    case 19:
                        m.capabilities[m.capabilities.length] = 19;
                        break;
                    case "WA_IG_1P_PLUGIN_RANKING_UPDATE_10":
                    case 20:
                        m.capabilities[m.capabilities.length] = 20;
                        break;
                    case "RICH_RESPONSE_SUB_HEADING":
                    case 21:
                        m.capabilities[m.capabilities.length] = 21;
                        break;
                    case "RICH_RESPONSE_GRID_IMAGE":
                    case 22:
                        m.capabilities[m.capabilities.length] = 22;
                        break;
                    case "AI_STUDIO_UGC_MEMORY":
                    case 23:
                        m.capabilities[m.capabilities.length] = 23;
                        break;
                    case "RICH_RESPONSE_LATEX":
                    case 24:
                        m.capabilities[m.capabilities.length] = 24;
                        break;
                    case "RICH_RESPONSE_MAPS":
                    case 25:
                        m.capabilities[m.capabilities.length] = 25;
                        break;
                    case "RICH_RESPONSE_INLINE_REELS":
                    case 26:
                        m.capabilities[m.capabilities.length] = 26;
                        break;
                    case "AGENTIC_PLANNING":
                    case 27:
                        m.capabilities[m.capabilities.length] = 27;
                        break;
                    case "ACCOUNT_LINKING":
                    case 28:
                        m.capabilities[m.capabilities.length] = 28;
                        break;
                    case "STREAMING_DISAGGREGATION":
                    case 29:
                        m.capabilities[m.capabilities.length] = 29;
                        break;
                    case "RICH_RESPONSE_GRID_IMAGE_3P":
                    case 30:
                        m.capabilities[m.capabilities.length] = 30;
                        break;
                    case "RICH_RESPONSE_LATEX_INLINE":
                    case 31:
                        m.capabilities[m.capabilities.length] = 31;
                        break;
                    case "QUERY_PLAN":
                    case 32:
                        m.capabilities[m.capabilities.length] = 32;
                        break;
                    case "PROACTIVE_MESSAGE":
                    case 33:
                        m.capabilities[m.capabilities.length] = 33;
                        break;
                    case "RICH_RESPONSE_UNIFIED_RESPONSE":
                    case 34:
                        m.capabilities[m.capabilities.length] = 34;
                        break;
                    case "PROMOTION_MESSAGE":
                    case 35:
                        m.capabilities[m.capabilities.length] = 35;
                        break;
                    case "SIMPLIFIED_PROFILE_PAGE":
                    case 36:
                        m.capabilities[m.capabilities.length] = 36;
                        break;
                    case "RICH_RESPONSE_SOURCES_IN_MESSAGE":
                    case 37:
                        m.capabilities[m.capabilities.length] = 37;
                        break;
                    case "RICH_RESPONSE_SIDE_BY_SIDE_SURVEY":
                    case 38:
                        m.capabilities[m.capabilities.length] = 38;
                        break;
                    case "RICH_RESPONSE_UNIFIED_TEXT_COMPONENT":
                    case 39:
                        m.capabilities[m.capabilities.length] = 39;
                        break;
                    case "AI_SHARED_MEMORY":
                    case 40:
                        m.capabilities[m.capabilities.length] = 40;
                        break;
                    case "RICH_RESPONSE_UNIFIED_SOURCES":
                    case 41:
                        m.capabilities[m.capabilities.length] = 41;
                        break;
                    case "RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS":
                    case 42:
                        m.capabilities[m.capabilities.length] = 42;
                        break;
                    case "RICH_RESPONSE_UR_INLINE_REELS_ENABLED":
                    case 43:
                        m.capabilities[m.capabilities.length] = 43;
                        break;
                    case "RICH_RESPONSE_UR_MEDIA_GRID_ENABLED":
                    case 44:
                        m.capabilities[m.capabilities.length] = 44;
                        break;
                    case "RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER":
                    case 45:
                        m.capabilities[m.capabilities.length] = 45;
                        break;
                    case "RICH_RESPONSE_IN_APP_SURVEY":
                    case 46:
                        m.capabilities[m.capabilities.length] = 46;
                        break;
                    case "AI_RESPONSE_MODEL_BRANDING":
                    case 47:
                        m.capabilities[m.capabilities.length] = 47;
                        break;
                    case "SESSION_TRANSPARENCY_SYSTEM_MESSAGE":
                    case 48:
                        m.capabilities[m.capabilities.length] = 48;
                        break;
                    case "RICH_RESPONSE_UR_REASONING":
                    case 49:
                        m.capabilities[m.capabilities.length] = 49;
                        break;
                    case "RICH_RESPONSE_UR_ZEITGEIST_CITATIONS":
                    case 50:
                        m.capabilities[m.capabilities.length] = 50;
                        break;
                    case "RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL":
                    case 51:
                        m.capabilities[m.capabilities.length] = 51;
                        break;
                    case "AI_IMAGINE_LOADING_INDICATOR":
                    case 52:
                        m.capabilities[m.capabilities.length] = 52;
                        break;
                    case "RICH_RESPONSE_UR_IMAGINE":
                    case 53:
                        m.capabilities[m.capabilities.length] = 53;
                        break;
                    case "AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR":
                    case 54:
                        m.capabilities[m.capabilities.length] = 54;
                        break;
                    case "RICH_RESPONSE_UR_BLOKS_ENABLED":
                    case 55:
                        m.capabilities[m.capabilities.length] = 55;
                        break;
                    case "RICH_RESPONSE_INLINE_LINKS_ENABLED":
                    case 56:
                        m.capabilities[m.capabilities.length] = 56;
                        break;
                    case "RICH_RESPONSE_UR_IMAGINE_VIDEO":
                    case 57:
                        m.capabilities[m.capabilities.length] = 57;
                        break;
                    case "JSON_PATCH_STREAMING":
                    case 58:
                        m.capabilities[m.capabilities.length] = 58;
                        break;
                    case "AI_TAB_FORCE_CLIPPY":
                    case 59:
                        m.capabilities[m.capabilities.length] = 59;
                        break;
                    case "UNIFIED_RESPONSE_EMBEDDED_SCREENS":
                    case 60:
                        m.capabilities[m.capabilities.length] = 60;
                        break;
                    case "AI_SUBSCRIPTION_ENABLED":
                    case 61:
                        m.capabilities[m.capabilities.length] = 61;
                        break;
                    case "UNIFIED_RESPONSE_AI_CONTENT_SEARCH_ENABLED":
                    case 62:
                        m.capabilities[m.capabilities.length] = 62;
                        break;
                    case "UNIFIED_RESPONSE_MARKDOWN_LINKS_ENABLED":
                    case 63:
                        m.capabilities[m.capabilities.length] = 63;
                        break;
                    case "AI_RICH_RESPONSE_MAPS_V2_ENABLED":
                    case 64:
                        m.capabilities[m.capabilities.length] = 64;
                        break;
                    case "AI_SUBSCRIPTION_METERING_ENABLED":
                    case 65:
                        m.capabilities[m.capabilities.length] = 65;
                        break;
                    case "RICH_RESPONSE_SPORTS_WIDGET_ENABLED":
                    case 66:
                        m.capabilities[m.capabilities.length] = 66;
                        break;
                    case "AI_RICH_RESPONSE_ARTIFACTS_ENABLED":
                    case 67:
                        m.capabilities[m.capabilities.length] = 67;
                        break;
                    case "AI_RICH_RESPONSE_EMAIL_CALENDAR_ENABLED":
                    case 68:
                        m.capabilities[m.capabilities.length] = 68;
                        break;
                    case "AI_RICH_RESPONSE_REMINDERS_ENABLED":
                    case 69:
                        m.capabilities[m.capabilities.length] = 69;
                        break;
                    default:
                        if (typeof d.capabilities[i] === "number" && (d.capabilities[i] | 0) === d.capabilities[i])
                            m.capabilities[m.capabilities.length] = d.capabilities[i];
                    }
                }
            }
            return m;
        };

        BotCapabilityMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.capabilities = [];
            }
            if (m.capabilities && m.capabilities.length) {
                d.capabilities = $Array(m.capabilities.length);
                for (var j = 0; j < m.capabilities.length; ++j) {
                    d.capabilities[j] = o.enums === $String ? $root.proto.BotCapabilityMetadata.BotCapabilityType[m.capabilities[j]] === $undefined ? m.capabilities[j] : $root.proto.BotCapabilityMetadata.BotCapabilityType[m.capabilities[j]] : m.capabilities[j];
                }
            }
            return d;
        };

        BotCapabilityMetadata.prototype.toJSON = function() {
            return BotCapabilityMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotCapabilityMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotCapabilityMetadata";
        };

        BotCapabilityMetadata.BotCapabilityType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "PROGRESS_INDICATOR"] = 1;
            values[valuesById[2] = "RICH_RESPONSE_HEADING"] = 2;
            values[valuesById[3] = "RICH_RESPONSE_NESTED_LIST"] = 3;
            values[valuesById[4] = "AI_MEMORY"] = 4;
            values[valuesById[5] = "RICH_RESPONSE_THREAD_SURFING"] = 5;
            values[valuesById[6] = "RICH_RESPONSE_TABLE"] = 6;
            values[valuesById[7] = "RICH_RESPONSE_CODE"] = 7;
            values[valuesById[8] = "RICH_RESPONSE_STRUCTURED_RESPONSE"] = 8;
            values[valuesById[9] = "RICH_RESPONSE_INLINE_IMAGE"] = 9;
            values[valuesById[10] = "WA_IG_1P_PLUGIN_RANKING_CONTROL"] = 10;
            values[valuesById[11] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_1"] = 11;
            values[valuesById[12] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_2"] = 12;
            values[valuesById[13] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_3"] = 13;
            values[valuesById[14] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_4"] = 14;
            values[valuesById[15] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_5"] = 15;
            values[valuesById[16] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_6"] = 16;
            values[valuesById[17] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_7"] = 17;
            values[valuesById[18] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_8"] = 18;
            values[valuesById[19] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_9"] = 19;
            values[valuesById[20] = "WA_IG_1P_PLUGIN_RANKING_UPDATE_10"] = 20;
            values[valuesById[21] = "RICH_RESPONSE_SUB_HEADING"] = 21;
            values[valuesById[22] = "RICH_RESPONSE_GRID_IMAGE"] = 22;
            values[valuesById[23] = "AI_STUDIO_UGC_MEMORY"] = 23;
            values[valuesById[24] = "RICH_RESPONSE_LATEX"] = 24;
            values[valuesById[25] = "RICH_RESPONSE_MAPS"] = 25;
            values[valuesById[26] = "RICH_RESPONSE_INLINE_REELS"] = 26;
            values[valuesById[27] = "AGENTIC_PLANNING"] = 27;
            values[valuesById[28] = "ACCOUNT_LINKING"] = 28;
            values[valuesById[29] = "STREAMING_DISAGGREGATION"] = 29;
            values[valuesById[30] = "RICH_RESPONSE_GRID_IMAGE_3P"] = 30;
            values[valuesById[31] = "RICH_RESPONSE_LATEX_INLINE"] = 31;
            values[valuesById[32] = "QUERY_PLAN"] = 32;
            values[valuesById[33] = "PROACTIVE_MESSAGE"] = 33;
            values[valuesById[34] = "RICH_RESPONSE_UNIFIED_RESPONSE"] = 34;
            values[valuesById[35] = "PROMOTION_MESSAGE"] = 35;
            values[valuesById[36] = "SIMPLIFIED_PROFILE_PAGE"] = 36;
            values[valuesById[37] = "RICH_RESPONSE_SOURCES_IN_MESSAGE"] = 37;
            values[valuesById[38] = "RICH_RESPONSE_SIDE_BY_SIDE_SURVEY"] = 38;
            values[valuesById[39] = "RICH_RESPONSE_UNIFIED_TEXT_COMPONENT"] = 39;
            values[valuesById[40] = "AI_SHARED_MEMORY"] = 40;
            values[valuesById[41] = "RICH_RESPONSE_UNIFIED_SOURCES"] = 41;
            values[valuesById[42] = "RICH_RESPONSE_UNIFIED_DOMAIN_CITATIONS"] = 42;
            values[valuesById[43] = "RICH_RESPONSE_UR_INLINE_REELS_ENABLED"] = 43;
            values[valuesById[44] = "RICH_RESPONSE_UR_MEDIA_GRID_ENABLED"] = 44;
            values[valuesById[45] = "RICH_RESPONSE_UR_TIMESTAMP_PLACEHOLDER"] = 45;
            values[valuesById[46] = "RICH_RESPONSE_IN_APP_SURVEY"] = 46;
            values[valuesById[47] = "AI_RESPONSE_MODEL_BRANDING"] = 47;
            values[valuesById[48] = "SESSION_TRANSPARENCY_SYSTEM_MESSAGE"] = 48;
            values[valuesById[49] = "RICH_RESPONSE_UR_REASONING"] = 49;
            values[valuesById[50] = "RICH_RESPONSE_UR_ZEITGEIST_CITATIONS"] = 50;
            values[valuesById[51] = "RICH_RESPONSE_UR_ZEITGEIST_CAROUSEL"] = 51;
            values[valuesById[52] = "AI_IMAGINE_LOADING_INDICATOR"] = 52;
            values[valuesById[53] = "RICH_RESPONSE_UR_IMAGINE"] = 53;
            values[valuesById[54] = "AI_IMAGINE_UR_TO_NATIVE_LOADING_INDICATOR"] = 54;
            values[valuesById[55] = "RICH_RESPONSE_UR_BLOKS_ENABLED"] = 55;
            values[valuesById[56] = "RICH_RESPONSE_INLINE_LINKS_ENABLED"] = 56;
            values[valuesById[57] = "RICH_RESPONSE_UR_IMAGINE_VIDEO"] = 57;
            values[valuesById[58] = "JSON_PATCH_STREAMING"] = 58;
            values[valuesById[59] = "AI_TAB_FORCE_CLIPPY"] = 59;
            values[valuesById[60] = "UNIFIED_RESPONSE_EMBEDDED_SCREENS"] = 60;
            values[valuesById[61] = "AI_SUBSCRIPTION_ENABLED"] = 61;
            values[valuesById[62] = "UNIFIED_RESPONSE_AI_CONTENT_SEARCH_ENABLED"] = 62;
            values[valuesById[63] = "UNIFIED_RESPONSE_MARKDOWN_LINKS_ENABLED"] = 63;
            values[valuesById[64] = "AI_RICH_RESPONSE_MAPS_V2_ENABLED"] = 64;
            values[valuesById[65] = "AI_SUBSCRIPTION_METERING_ENABLED"] = 65;
            values[valuesById[66] = "RICH_RESPONSE_SPORTS_WIDGET_ENABLED"] = 66;
            values[valuesById[67] = "AI_RICH_RESPONSE_ARTIFACTS_ENABLED"] = 67;
            values[valuesById[68] = "AI_RICH_RESPONSE_EMAIL_CALENDAR_ENABLED"] = 68;
            values[valuesById[69] = "AI_RICH_RESPONSE_REMINDERS_ENABLED"] = 69;
            return values;
        })();

        return BotCapabilityMetadata;
    })();

    proto.BotCommandMetadata = (function() {

        const BotCommandMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotCommandMetadata.prototype.commandName = null;
        BotCommandMetadata.prototype.commandDescription = null;
        BotCommandMetadata.prototype.commandPrompt = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotCommandMetadata.prototype, "_commandName", {
            get: $util.oneOfGetter($oneOfFields = ["commandName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotCommandMetadata.prototype, "_commandDescription", {
            get: $util.oneOfGetter($oneOfFields = ["commandDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotCommandMetadata.prototype, "_commandPrompt", {
            get: $util.oneOfGetter($oneOfFields = ["commandPrompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotCommandMetadata.create = function(properties) {
            return new BotCommandMetadata(properties);
        };

        BotCommandMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.commandName != null && $Object.hasOwnProperty.call(m, "commandName"))
                w.uint32(10).string(m.commandName);
            if (m.commandDescription != null && $Object.hasOwnProperty.call(m, "commandDescription"))
                w.uint32(18).string(m.commandDescription);
            if (m.commandPrompt != null && $Object.hasOwnProperty.call(m, "commandPrompt"))
                w.uint32(26).string(m.commandPrompt);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotCommandMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotCommandMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.commandName = r.stringVerify();
                        m._commandName = "commandName";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.commandDescription = r.stringVerify();
                        m._commandDescription = "commandDescription";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.commandPrompt = r.stringVerify();
                        m._commandPrompt = "commandPrompt";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotCommandMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotCommandMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotCommandMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotCommandMetadata();
            if (d.commandName != null) {
                m.commandName = $String(d.commandName);
            }
            if (d.commandDescription != null) {
                m.commandDescription = $String(d.commandDescription);
            }
            if (d.commandPrompt != null) {
                m.commandPrompt = $String(d.commandPrompt);
            }
            return m;
        };

        BotCommandMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.commandName != null && $Object.hasOwnProperty.call(m, "commandName")) {
                d.commandName = m.commandName;
            }
            if (m.commandDescription != null && $Object.hasOwnProperty.call(m, "commandDescription")) {
                d.commandDescription = m.commandDescription;
            }
            if (m.commandPrompt != null && $Object.hasOwnProperty.call(m, "commandPrompt")) {
                d.commandPrompt = m.commandPrompt;
            }
            return d;
        };

        BotCommandMetadata.prototype.toJSON = function() {
            return BotCommandMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotCommandMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotCommandMetadata";
        };

        return BotCommandMetadata;
    })();

    proto.BotDocumentMessageMetadata = (function() {

        const BotDocumentMessageMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotDocumentMessageMetadata.prototype.pluginType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotDocumentMessageMetadata.prototype, "_pluginType", {
            get: $util.oneOfGetter($oneOfFields = ["pluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotDocumentMessageMetadata.create = function(properties) {
            return new BotDocumentMessageMetadata(properties);
        };

        BotDocumentMessageMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.pluginType != null && $Object.hasOwnProperty.call(m, "pluginType"))
                w.uint32(8).int32(m.pluginType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotDocumentMessageMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotDocumentMessageMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.pluginType = r.int32();
                        m._pluginType = "pluginType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotDocumentMessageMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotDocumentMessageMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotDocumentMessageMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotDocumentMessageMetadata();
            switch (d.pluginType) {
            case "TEXT_EXTRACTION":
            case 0:
                m.pluginType = 0;
                break;
            case "OCR_AND_IMAGES":
            case 1:
                m.pluginType = 1;
                break;
            default:
                if (typeof d.pluginType === "number" && (d.pluginType | 0) === d.pluginType)
                    m.pluginType = d.pluginType;
            }
            return m;
        };

        BotDocumentMessageMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.pluginType != null && $Object.hasOwnProperty.call(m, "pluginType")) {
                d.pluginType = o.enums === $String ? $root.proto.BotDocumentMessageMetadata.DocumentPluginType[m.pluginType] === $undefined ? m.pluginType : $root.proto.BotDocumentMessageMetadata.DocumentPluginType[m.pluginType] : m.pluginType;
            }
            return d;
        };

        BotDocumentMessageMetadata.prototype.toJSON = function() {
            return BotDocumentMessageMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotDocumentMessageMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotDocumentMessageMetadata";
        };

        BotDocumentMessageMetadata.DocumentPluginType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "TEXT_EXTRACTION"] = 0;
            values[valuesById[1] = "OCR_AND_IMAGES"] = 1;
            return values;
        })();

        return BotDocumentMessageMetadata;
    })();

    proto.BotFeedbackMessage = (function() {

        const BotFeedbackMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotFeedbackMessage.prototype.messageKey = null;
        BotFeedbackMessage.prototype.kind = null;
        BotFeedbackMessage.prototype.text = null;
        BotFeedbackMessage.prototype.kindNegative = null;
        BotFeedbackMessage.prototype.kindPositive = null;
        BotFeedbackMessage.prototype.kindReport = null;
        BotFeedbackMessage.prototype.sideBySideSurveyMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_kind", {
            get: $util.oneOfGetter($oneOfFields = ["kind"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_kindNegative", {
            get: $util.oneOfGetter($oneOfFields = ["kindNegative"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_kindPositive", {
            get: $util.oneOfGetter($oneOfFields = ["kindPositive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_kindReport", {
            get: $util.oneOfGetter($oneOfFields = ["kindReport"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotFeedbackMessage.prototype, "_sideBySideSurveyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sideBySideSurveyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotFeedbackMessage.create = function(properties) {
            return new BotFeedbackMessage(properties);
        };

        BotFeedbackMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey"))
                $root.proto.MessageKey.encode(m.messageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.kind != null && $Object.hasOwnProperty.call(m, "kind"))
                w.uint32(16).int32(m.kind);
            if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                w.uint32(26).string(m.text);
            if (m.kindNegative != null && $Object.hasOwnProperty.call(m, "kindNegative"))
                w.uint32(32).uint64(m.kindNegative);
            if (m.kindPositive != null && $Object.hasOwnProperty.call(m, "kindPositive"))
                w.uint32(40).uint64(m.kindPositive);
            if (m.kindReport != null && $Object.hasOwnProperty.call(m, "kindReport"))
                w.uint32(48).int32(m.kindReport);
            if (m.sideBySideSurveyMetadata != null && $Object.hasOwnProperty.call(m, "sideBySideSurveyMetadata"))
                $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.encode(m.sideBySideSurveyMetadata, w.uint32(58).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotFeedbackMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.messageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.messageKey);
                        m._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.kind = r.int32();
                        m._kind = "kind";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.text = r.stringVerify();
                        m._text = "text";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.kindNegative = r.uint64();
                        m._kindNegative = "kindNegative";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.kindPositive = r.uint64();
                        m._kindPositive = "kindPositive";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.kindReport = r.int32();
                        m._kindReport = "kindReport";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.sideBySideSurveyMetadata = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.decode(r, r.uint32(), $undefined, q + 1, m.sideBySideSurveyMetadata);
                        m._sideBySideSurveyMetadata = "sideBySideSurveyMetadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotFeedbackMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotFeedbackMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotFeedbackMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotFeedbackMessage();
            if (d.messageKey != null) {
                if (!$util.isObject(d.messageKey))
                    throw $TypeError(".proto.BotFeedbackMessage.messageKey: object expected");
                m.messageKey = $root.proto.MessageKey.fromObject(d.messageKey, q + 1);
            }
            switch (d.kind) {
            case "BOT_FEEDBACK_POSITIVE":
            case 0:
                m.kind = 0;
                break;
            case "BOT_FEEDBACK_NEGATIVE_GENERIC":
            case 1:
                m.kind = 1;
                break;
            case "BOT_FEEDBACK_NEGATIVE_HELPFUL":
            case 2:
                m.kind = 2;
                break;
            case "BOT_FEEDBACK_NEGATIVE_INTERESTING":
            case 3:
                m.kind = 3;
                break;
            case "BOT_FEEDBACK_NEGATIVE_ACCURATE":
            case 4:
                m.kind = 4;
                break;
            case "BOT_FEEDBACK_NEGATIVE_SAFE":
            case 5:
                m.kind = 5;
                break;
            case "BOT_FEEDBACK_NEGATIVE_OTHER":
            case 6:
                m.kind = 6;
                break;
            case "BOT_FEEDBACK_NEGATIVE_REFUSED":
            case 7:
                m.kind = 7;
                break;
            case "BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING":
            case 8:
                m.kind = 8;
                break;
            case "BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT":
            case 9:
                m.kind = 9;
                break;
            case "BOT_FEEDBACK_NEGATIVE_PERSONALIZED":
            case 10:
                m.kind = 10;
                break;
            case "BOT_FEEDBACK_NEGATIVE_CLARITY":
            case 11:
                m.kind = 11;
                break;
            case "BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON":
            case 12:
                m.kind = 12;
                break;
            case "BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY":
            case 13:
                m.kind = 13;
                break;
            case "BOT_FEEDBACK_NEGATIVE":
            case 14:
                m.kind = 14;
                break;
            default:
                if (typeof d.kind === "number" && (d.kind | 0) === d.kind)
                    m.kind = d.kind;
            }
            if (d.text != null) {
                m.text = $String(d.text);
            }
            if (d.kindNegative != null) {
                if ($util.Long)
                    m.kindNegative = $util.Long.fromValue(d.kindNegative, true);
                else if (typeof d.kindNegative === "string")
                    m.kindNegative = $parseInt(d.kindNegative, 10);
                else if (typeof d.kindNegative === "number")
                    m.kindNegative = d.kindNegative;
                else if (typeof d.kindNegative === "object")
                    m.kindNegative = new $util.LongBits(d.kindNegative.low >>> 0, d.kindNegative.high >>> 0).toNumber(true);
            }
            if (d.kindPositive != null) {
                if ($util.Long)
                    m.kindPositive = $util.Long.fromValue(d.kindPositive, true);
                else if (typeof d.kindPositive === "string")
                    m.kindPositive = $parseInt(d.kindPositive, 10);
                else if (typeof d.kindPositive === "number")
                    m.kindPositive = d.kindPositive;
                else if (typeof d.kindPositive === "object")
                    m.kindPositive = new $util.LongBits(d.kindPositive.low >>> 0, d.kindPositive.high >>> 0).toNumber(true);
            }
            switch (d.kindReport) {
            case "NONE":
            case 0:
                m.kindReport = 0;
                break;
            case "GENERIC":
            case 1:
                m.kindReport = 1;
                break;
            default:
                if (typeof d.kindReport === "number" && (d.kindReport | 0) === d.kindReport)
                    m.kindReport = d.kindReport;
            }
            if (d.sideBySideSurveyMetadata != null) {
                if (!$util.isObject(d.sideBySideSurveyMetadata))
                    throw $TypeError(".proto.BotFeedbackMessage.sideBySideSurveyMetadata: object expected");
                m.sideBySideSurveyMetadata = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.fromObject(d.sideBySideSurveyMetadata, q + 1);
            }
            return m;
        };

        BotFeedbackMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey")) {
                d.messageKey = $root.proto.MessageKey.toObject(m.messageKey, o, q + 1);
            }
            if (m.kind != null && $Object.hasOwnProperty.call(m, "kind")) {
                d.kind = o.enums === $String ? $root.proto.BotFeedbackMessage.BotFeedbackKind[m.kind] === $undefined ? m.kind : $root.proto.BotFeedbackMessage.BotFeedbackKind[m.kind] : m.kind;
            }
            if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                d.text = m.text;
            }
            if (m.kindNegative != null && $Object.hasOwnProperty.call(m, "kindNegative")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.kindNegative = typeof m.kindNegative === "number" ? $BigInt(m.kindNegative) : $util.Long.fromBits(m.kindNegative.low >>> 0, m.kindNegative.high >>> 0, true).toBigInt();
                else if (typeof m.kindNegative === "number")
                    d.kindNegative = o.longs === $String ? $String(m.kindNegative) : m.kindNegative;
                else
                    d.kindNegative = o.longs === $String ? $util.Long.prototype.toString.call(m.kindNegative) : o.longs === $Number ? new $util.LongBits(m.kindNegative.low >>> 0, m.kindNegative.high >>> 0).toNumber(true) : m.kindNegative;
            }
            if (m.kindPositive != null && $Object.hasOwnProperty.call(m, "kindPositive")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.kindPositive = typeof m.kindPositive === "number" ? $BigInt(m.kindPositive) : $util.Long.fromBits(m.kindPositive.low >>> 0, m.kindPositive.high >>> 0, true).toBigInt();
                else if (typeof m.kindPositive === "number")
                    d.kindPositive = o.longs === $String ? $String(m.kindPositive) : m.kindPositive;
                else
                    d.kindPositive = o.longs === $String ? $util.Long.prototype.toString.call(m.kindPositive) : o.longs === $Number ? new $util.LongBits(m.kindPositive.low >>> 0, m.kindPositive.high >>> 0).toNumber(true) : m.kindPositive;
            }
            if (m.kindReport != null && $Object.hasOwnProperty.call(m, "kindReport")) {
                d.kindReport = o.enums === $String ? $root.proto.BotFeedbackMessage.ReportKind[m.kindReport] === $undefined ? m.kindReport : $root.proto.BotFeedbackMessage.ReportKind[m.kindReport] : m.kindReport;
            }
            if (m.sideBySideSurveyMetadata != null && $Object.hasOwnProperty.call(m, "sideBySideSurveyMetadata")) {
                d.sideBySideSurveyMetadata = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.toObject(m.sideBySideSurveyMetadata, o, q + 1);
            }
            return d;
        };

        BotFeedbackMessage.prototype.toJSON = function() {
            return BotFeedbackMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotFeedbackMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotFeedbackMessage";
        };

        BotFeedbackMessage.BotFeedbackKind = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "BOT_FEEDBACK_POSITIVE"] = 0;
            values[valuesById[1] = "BOT_FEEDBACK_NEGATIVE_GENERIC"] = 1;
            values[valuesById[2] = "BOT_FEEDBACK_NEGATIVE_HELPFUL"] = 2;
            values[valuesById[3] = "BOT_FEEDBACK_NEGATIVE_INTERESTING"] = 3;
            values[valuesById[4] = "BOT_FEEDBACK_NEGATIVE_ACCURATE"] = 4;
            values[valuesById[5] = "BOT_FEEDBACK_NEGATIVE_SAFE"] = 5;
            values[valuesById[6] = "BOT_FEEDBACK_NEGATIVE_OTHER"] = 6;
            values[valuesById[7] = "BOT_FEEDBACK_NEGATIVE_REFUSED"] = 7;
            values[valuesById[8] = "BOT_FEEDBACK_NEGATIVE_NOT_VISUALLY_APPEALING"] = 8;
            values[valuesById[9] = "BOT_FEEDBACK_NEGATIVE_NOT_RELEVANT_TO_TEXT"] = 9;
            values[valuesById[10] = "BOT_FEEDBACK_NEGATIVE_PERSONALIZED"] = 10;
            values[valuesById[11] = "BOT_FEEDBACK_NEGATIVE_CLARITY"] = 11;
            values[valuesById[12] = "BOT_FEEDBACK_NEGATIVE_DOESNT_LOOK_LIKE_THE_PERSON"] = 12;
            values[valuesById[13] = "BOT_FEEDBACK_NEGATIVE_HALLUCINATION_INTERNAL_ONLY"] = 13;
            values[valuesById[14] = "BOT_FEEDBACK_NEGATIVE"] = 14;
            return values;
        })();

        BotFeedbackMessage.BotFeedbackKindMultipleNegative = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_GENERIC"] = 1;
            values[valuesById[2] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_HELPFUL"] = 2;
            values[valuesById[4] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_INTERESTING"] = 4;
            values[valuesById[8] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_ACCURATE"] = 8;
            values[valuesById[16] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_SAFE"] = 16;
            values[valuesById[32] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_OTHER"] = 32;
            values[valuesById[64] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_REFUSED"] = 64;
            values[valuesById[128] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_VISUALLY_APPEALING"] = 128;
            values[valuesById[256] = "BOT_FEEDBACK_MULTIPLE_NEGATIVE_NOT_RELEVANT_TO_TEXT"] = 256;
            return values;
        })();

        BotFeedbackMessage.BotFeedbackKindMultiplePositive = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "BOT_FEEDBACK_MULTIPLE_POSITIVE_GENERIC"] = 1;
            return values;
        })();

        BotFeedbackMessage.ReportKind = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "GENERIC"] = 1;
            return values;
        })();

        BotFeedbackMessage.SideBySideSurveyMetadata = (function() {

            const SideBySideSurveyMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SideBySideSurveyMetadata.prototype.selectedRequestId = null;
            SideBySideSurveyMetadata.prototype.surveyId = null;
            SideBySideSurveyMetadata.prototype.simonSessionFbid = null;
            SideBySideSurveyMetadata.prototype.responseOtid = null;
            SideBySideSurveyMetadata.prototype.responseTimestampMsString = null;
            SideBySideSurveyMetadata.prototype.isSelectedResponsePrimary = null;
            SideBySideSurveyMetadata.prototype.messageIdToEdit = null;
            SideBySideSurveyMetadata.prototype.analyticsData = null;
            SideBySideSurveyMetadata.prototype.metaAiAnalyticsData = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_selectedRequestId", {
                get: $util.oneOfGetter($oneOfFields = ["selectedRequestId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_surveyId", {
                get: $util.oneOfGetter($oneOfFields = ["surveyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_simonSessionFbid", {
                get: $util.oneOfGetter($oneOfFields = ["simonSessionFbid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_responseOtid", {
                get: $util.oneOfGetter($oneOfFields = ["responseOtid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_responseTimestampMsString", {
                get: $util.oneOfGetter($oneOfFields = ["responseTimestampMsString"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_isSelectedResponsePrimary", {
                get: $util.oneOfGetter($oneOfFields = ["isSelectedResponsePrimary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_messageIdToEdit", {
                get: $util.oneOfGetter($oneOfFields = ["messageIdToEdit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_analyticsData", {
                get: $util.oneOfGetter($oneOfFields = ["analyticsData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideSurveyMetadata.prototype, "_metaAiAnalyticsData", {
                get: $util.oneOfGetter($oneOfFields = ["metaAiAnalyticsData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SideBySideSurveyMetadata.create = function(properties) {
                return new SideBySideSurveyMetadata(properties);
            };

            SideBySideSurveyMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.selectedRequestId != null && $Object.hasOwnProperty.call(m, "selectedRequestId"))
                    w.uint32(10).string(m.selectedRequestId);
                if (m.surveyId != null && $Object.hasOwnProperty.call(m, "surveyId"))
                    w.uint32(16).uint32(m.surveyId);
                if (m.simonSessionFbid != null && $Object.hasOwnProperty.call(m, "simonSessionFbid"))
                    w.uint32(26).string(m.simonSessionFbid);
                if (m.responseOtid != null && $Object.hasOwnProperty.call(m, "responseOtid"))
                    w.uint32(34).string(m.responseOtid);
                if (m.responseTimestampMsString != null && $Object.hasOwnProperty.call(m, "responseTimestampMsString"))
                    w.uint32(42).string(m.responseTimestampMsString);
                if (m.isSelectedResponsePrimary != null && $Object.hasOwnProperty.call(m, "isSelectedResponsePrimary"))
                    w.uint32(48).bool(m.isSelectedResponsePrimary);
                if (m.messageIdToEdit != null && $Object.hasOwnProperty.call(m, "messageIdToEdit"))
                    w.uint32(58).string(m.messageIdToEdit);
                if (m.analyticsData != null && $Object.hasOwnProperty.call(m, "analyticsData"))
                    $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.encode(m.analyticsData, w.uint32(66).fork(), q + 1).ldelim();
                if (m.metaAiAnalyticsData != null && $Object.hasOwnProperty.call(m, "metaAiAnalyticsData"))
                    $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.encode(m.metaAiAnalyticsData, w.uint32(74).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SideBySideSurveyMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.selectedRequestId = r.stringVerify();
                            m._selectedRequestId = "selectedRequestId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.surveyId = r.uint32();
                            m._surveyId = "surveyId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.simonSessionFbid = r.stringVerify();
                            m._simonSessionFbid = "simonSessionFbid";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.responseOtid = r.stringVerify();
                            m._responseOtid = "responseOtid";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.responseTimestampMsString = r.stringVerify();
                            m._responseTimestampMsString = "responseTimestampMsString";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.isSelectedResponsePrimary = r.bool();
                            m._isSelectedResponsePrimary = "isSelectedResponsePrimary";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.messageIdToEdit = r.stringVerify();
                            m._messageIdToEdit = "messageIdToEdit";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.analyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.decode(r, r.uint32(), $undefined, q + 1, m.analyticsData);
                            m._analyticsData = "analyticsData";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.metaAiAnalyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.decode(r, r.uint32(), $undefined, q + 1, m.metaAiAnalyticsData);
                            m._metaAiAnalyticsData = "metaAiAnalyticsData";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SideBySideSurveyMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata();
                if (d.selectedRequestId != null) {
                    m.selectedRequestId = $String(d.selectedRequestId);
                }
                if (d.surveyId != null) {
                    m.surveyId = d.surveyId >>> 0;
                }
                if (d.simonSessionFbid != null) {
                    m.simonSessionFbid = $String(d.simonSessionFbid);
                }
                if (d.responseOtid != null) {
                    m.responseOtid = $String(d.responseOtid);
                }
                if (d.responseTimestampMsString != null) {
                    m.responseTimestampMsString = $String(d.responseTimestampMsString);
                }
                if (d.isSelectedResponsePrimary != null) {
                    m.isSelectedResponsePrimary = $Boolean(d.isSelectedResponsePrimary);
                }
                if (d.messageIdToEdit != null) {
                    m.messageIdToEdit = $String(d.messageIdToEdit);
                }
                if (d.analyticsData != null) {
                    if (!$util.isObject(d.analyticsData))
                        throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.analyticsData: object expected");
                    m.analyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.fromObject(d.analyticsData, q + 1);
                }
                if (d.metaAiAnalyticsData != null) {
                    if (!$util.isObject(d.metaAiAnalyticsData))
                        throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.metaAiAnalyticsData: object expected");
                    m.metaAiAnalyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.fromObject(d.metaAiAnalyticsData, q + 1);
                }
                return m;
            };

            SideBySideSurveyMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.selectedRequestId != null && $Object.hasOwnProperty.call(m, "selectedRequestId")) {
                    d.selectedRequestId = m.selectedRequestId;
                }
                if (m.surveyId != null && $Object.hasOwnProperty.call(m, "surveyId")) {
                    d.surveyId = m.surveyId;
                }
                if (m.simonSessionFbid != null && $Object.hasOwnProperty.call(m, "simonSessionFbid")) {
                    d.simonSessionFbid = m.simonSessionFbid;
                }
                if (m.responseOtid != null && $Object.hasOwnProperty.call(m, "responseOtid")) {
                    d.responseOtid = m.responseOtid;
                }
                if (m.responseTimestampMsString != null && $Object.hasOwnProperty.call(m, "responseTimestampMsString")) {
                    d.responseTimestampMsString = m.responseTimestampMsString;
                }
                if (m.isSelectedResponsePrimary != null && $Object.hasOwnProperty.call(m, "isSelectedResponsePrimary")) {
                    d.isSelectedResponsePrimary = m.isSelectedResponsePrimary;
                }
                if (m.messageIdToEdit != null && $Object.hasOwnProperty.call(m, "messageIdToEdit")) {
                    d.messageIdToEdit = m.messageIdToEdit;
                }
                if (m.analyticsData != null && $Object.hasOwnProperty.call(m, "analyticsData")) {
                    d.analyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData.toObject(m.analyticsData, o, q + 1);
                }
                if (m.metaAiAnalyticsData != null && $Object.hasOwnProperty.call(m, "metaAiAnalyticsData")) {
                    d.metaAiAnalyticsData = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.toObject(m.metaAiAnalyticsData, o, q + 1);
                }
                return d;
            };

            SideBySideSurveyMetadata.prototype.toJSON = function() {
                return SideBySideSurveyMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            SideBySideSurveyMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata";
            };

            SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData = (function() {

                const SideBySideSurveyAnalyticsData = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                SideBySideSurveyAnalyticsData.prototype.tessaEvent = null;
                SideBySideSurveyAnalyticsData.prototype.tessaSessionFbid = null;
                SideBySideSurveyAnalyticsData.prototype.simonSessionFbid = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_tessaEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["tessaEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_tessaSessionFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["tessaSessionFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SideBySideSurveyAnalyticsData.prototype, "_simonSessionFbid", {
                    get: $util.oneOfGetter($oneOfFields = ["simonSessionFbid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SideBySideSurveyAnalyticsData.create = function(properties) {
                    return new SideBySideSurveyAnalyticsData(properties);
                };

                SideBySideSurveyAnalyticsData.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.tessaEvent != null && $Object.hasOwnProperty.call(m, "tessaEvent"))
                        w.uint32(10).string(m.tessaEvent);
                    if (m.tessaSessionFbid != null && $Object.hasOwnProperty.call(m, "tessaSessionFbid"))
                        w.uint32(18).string(m.tessaSessionFbid);
                    if (m.simonSessionFbid != null && $Object.hasOwnProperty.call(m, "simonSessionFbid"))
                        w.uint32(26).string(m.simonSessionFbid);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                SideBySideSurveyAnalyticsData.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.tessaEvent = r.stringVerify();
                                m._tessaEvent = "tessaEvent";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.tessaSessionFbid = r.stringVerify();
                                m._tessaSessionFbid = "tessaSessionFbid";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.simonSessionFbid = r.stringVerify();
                                m._simonSessionFbid = "simonSessionFbid";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                SideBySideSurveyAnalyticsData.fromObject = function (d, q) {
                    if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData();
                    if (d.tessaEvent != null) {
                        m.tessaEvent = $String(d.tessaEvent);
                    }
                    if (d.tessaSessionFbid != null) {
                        m.tessaSessionFbid = $String(d.tessaSessionFbid);
                    }
                    if (d.simonSessionFbid != null) {
                        m.simonSessionFbid = $String(d.simonSessionFbid);
                    }
                    return m;
                };

                SideBySideSurveyAnalyticsData.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.tessaEvent != null && $Object.hasOwnProperty.call(m, "tessaEvent")) {
                        d.tessaEvent = m.tessaEvent;
                    }
                    if (m.tessaSessionFbid != null && $Object.hasOwnProperty.call(m, "tessaSessionFbid")) {
                        d.tessaSessionFbid = m.tessaSessionFbid;
                    }
                    if (m.simonSessionFbid != null && $Object.hasOwnProperty.call(m, "simonSessionFbid")) {
                        d.simonSessionFbid = m.simonSessionFbid;
                    }
                    return d;
                };

                SideBySideSurveyAnalyticsData.prototype.toJSON = function() {
                    return SideBySideSurveyAnalyticsData.toObject(this, $protobuf.util.toJSONOptions);
                };

                SideBySideSurveyAnalyticsData.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SideBySideSurveyAnalyticsData";
                };

                return SideBySideSurveyAnalyticsData;
            })();

            SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData = (function() {

                const SidebySideSurveyMetaAiAnalyticsData = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                SidebySideSurveyMetaAiAnalyticsData.prototype.surveyId = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.primaryResponseId = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.testArmName = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.timestampMsString = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.ctaImpressionEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.ctaClickEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.cardImpressionEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.responseEvent = null;
                SidebySideSurveyMetaAiAnalyticsData.prototype.abandonEvent = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_surveyId", {
                    get: $util.oneOfGetter($oneOfFields = ["surveyId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_primaryResponseId", {
                    get: $util.oneOfGetter($oneOfFields = ["primaryResponseId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_testArmName", {
                    get: $util.oneOfGetter($oneOfFields = ["testArmName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_timestampMsString", {
                    get: $util.oneOfGetter($oneOfFields = ["timestampMsString"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_ctaImpressionEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["ctaImpressionEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_ctaClickEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["ctaClickEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_cardImpressionEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["cardImpressionEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_responseEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["responseEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SidebySideSurveyMetaAiAnalyticsData.prototype, "_abandonEvent", {
                    get: $util.oneOfGetter($oneOfFields = ["abandonEvent"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SidebySideSurveyMetaAiAnalyticsData.create = function(properties) {
                    return new SidebySideSurveyMetaAiAnalyticsData(properties);
                };

                SidebySideSurveyMetaAiAnalyticsData.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.surveyId != null && $Object.hasOwnProperty.call(m, "surveyId"))
                        w.uint32(8).uint32(m.surveyId);
                    if (m.primaryResponseId != null && $Object.hasOwnProperty.call(m, "primaryResponseId"))
                        w.uint32(18).string(m.primaryResponseId);
                    if (m.testArmName != null && $Object.hasOwnProperty.call(m, "testArmName"))
                        w.uint32(26).string(m.testArmName);
                    if (m.timestampMsString != null && $Object.hasOwnProperty.call(m, "timestampMsString"))
                        w.uint32(34).string(m.timestampMsString);
                    if (m.ctaImpressionEvent != null && $Object.hasOwnProperty.call(m, "ctaImpressionEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.encode(m.ctaImpressionEvent, w.uint32(42).fork(), q + 1).ldelim();
                    if (m.ctaClickEvent != null && $Object.hasOwnProperty.call(m, "ctaClickEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.encode(m.ctaClickEvent, w.uint32(50).fork(), q + 1).ldelim();
                    if (m.cardImpressionEvent != null && $Object.hasOwnProperty.call(m, "cardImpressionEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.encode(m.cardImpressionEvent, w.uint32(58).fork(), q + 1).ldelim();
                    if (m.responseEvent != null && $Object.hasOwnProperty.call(m, "responseEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.encode(m.responseEvent, w.uint32(66).fork(), q + 1).ldelim();
                    if (m.abandonEvent != null && $Object.hasOwnProperty.call(m, "abandonEvent"))
                        $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.encode(m.abandonEvent, w.uint32(74).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                SidebySideSurveyMetaAiAnalyticsData.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.surveyId = r.uint32();
                                m._surveyId = "surveyId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.primaryResponseId = r.stringVerify();
                                m._primaryResponseId = "primaryResponseId";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.testArmName = r.stringVerify();
                                m._testArmName = "testArmName";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.timestampMsString = r.stringVerify();
                                m._timestampMsString = "timestampMsString";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.ctaImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.decode(r, r.uint32(), $undefined, q + 1, m.ctaImpressionEvent);
                                m._ctaImpressionEvent = "ctaImpressionEvent";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.ctaClickEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.decode(r, r.uint32(), $undefined, q + 1, m.ctaClickEvent);
                                m._ctaClickEvent = "ctaClickEvent";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.cardImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.decode(r, r.uint32(), $undefined, q + 1, m.cardImpressionEvent);
                                m._cardImpressionEvent = "cardImpressionEvent";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                m.responseEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.decode(r, r.uint32(), $undefined, q + 1, m.responseEvent);
                                m._responseEvent = "responseEvent";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                m.abandonEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.decode(r, r.uint32(), $undefined, q + 1, m.abandonEvent);
                                m._abandonEvent = "abandonEvent";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                SidebySideSurveyMetaAiAnalyticsData.fromObject = function (d, q) {
                    if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData();
                    if (d.surveyId != null) {
                        m.surveyId = d.surveyId >>> 0;
                    }
                    if (d.primaryResponseId != null) {
                        m.primaryResponseId = $String(d.primaryResponseId);
                    }
                    if (d.testArmName != null) {
                        m.testArmName = $String(d.testArmName);
                    }
                    if (d.timestampMsString != null) {
                        m.timestampMsString = $String(d.timestampMsString);
                    }
                    if (d.ctaImpressionEvent != null) {
                        if (!$util.isObject(d.ctaImpressionEvent))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ctaImpressionEvent: object expected");
                        m.ctaImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.fromObject(d.ctaImpressionEvent, q + 1);
                    }
                    if (d.ctaClickEvent != null) {
                        if (!$util.isObject(d.ctaClickEvent))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.ctaClickEvent: object expected");
                        m.ctaClickEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.fromObject(d.ctaClickEvent, q + 1);
                    }
                    if (d.cardImpressionEvent != null) {
                        if (!$util.isObject(d.cardImpressionEvent))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.cardImpressionEvent: object expected");
                        m.cardImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.fromObject(d.cardImpressionEvent, q + 1);
                    }
                    if (d.responseEvent != null) {
                        if (!$util.isObject(d.responseEvent))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.responseEvent: object expected");
                        m.responseEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.fromObject(d.responseEvent, q + 1);
                    }
                    if (d.abandonEvent != null) {
                        if (!$util.isObject(d.abandonEvent))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.abandonEvent: object expected");
                        m.abandonEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.fromObject(d.abandonEvent, q + 1);
                    }
                    return m;
                };

                SidebySideSurveyMetaAiAnalyticsData.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.surveyId != null && $Object.hasOwnProperty.call(m, "surveyId")) {
                        d.surveyId = m.surveyId;
                    }
                    if (m.primaryResponseId != null && $Object.hasOwnProperty.call(m, "primaryResponseId")) {
                        d.primaryResponseId = m.primaryResponseId;
                    }
                    if (m.testArmName != null && $Object.hasOwnProperty.call(m, "testArmName")) {
                        d.testArmName = m.testArmName;
                    }
                    if (m.timestampMsString != null && $Object.hasOwnProperty.call(m, "timestampMsString")) {
                        d.timestampMsString = m.timestampMsString;
                    }
                    if (m.ctaImpressionEvent != null && $Object.hasOwnProperty.call(m, "ctaImpressionEvent")) {
                        d.ctaImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData.toObject(m.ctaImpressionEvent, o, q + 1);
                    }
                    if (m.ctaClickEvent != null && $Object.hasOwnProperty.call(m, "ctaClickEvent")) {
                        d.ctaClickEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData.toObject(m.ctaClickEvent, o, q + 1);
                    }
                    if (m.cardImpressionEvent != null && $Object.hasOwnProperty.call(m, "cardImpressionEvent")) {
                        d.cardImpressionEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData.toObject(m.cardImpressionEvent, o, q + 1);
                    }
                    if (m.responseEvent != null && $Object.hasOwnProperty.call(m, "responseEvent")) {
                        d.responseEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData.toObject(m.responseEvent, o, q + 1);
                    }
                    if (m.abandonEvent != null && $Object.hasOwnProperty.call(m, "abandonEvent")) {
                        d.abandonEvent = $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData.toObject(m.abandonEvent, o, q + 1);
                    }
                    return d;
                };

                SidebySideSurveyMetaAiAnalyticsData.prototype.toJSON = function() {
                    return SidebySideSurveyMetaAiAnalyticsData.toObject(this, $protobuf.util.toJSONOptions);
                };

                SidebySideSurveyMetaAiAnalyticsData.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData";
                };

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData = (function() {

                    const SideBySideSurveyAbandonEventData = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    SideBySideSurveyAbandonEventData.prototype.abandonDwellTimeMsString = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SideBySideSurveyAbandonEventData.prototype, "_abandonDwellTimeMsString", {
                        get: $util.oneOfGetter($oneOfFields = ["abandonDwellTimeMsString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyAbandonEventData.create = function(properties) {
                        return new SideBySideSurveyAbandonEventData(properties);
                    };

                    SideBySideSurveyAbandonEventData.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.abandonDwellTimeMsString != null && $Object.hasOwnProperty.call(m, "abandonDwellTimeMsString"))
                            w.uint32(10).string(m.abandonDwellTimeMsString);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    SideBySideSurveyAbandonEventData.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.abandonDwellTimeMsString = r.stringVerify();
                                    m._abandonDwellTimeMsString = "abandonDwellTimeMsString";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    SideBySideSurveyAbandonEventData.fromObject = function (d, q) {
                        if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData();
                        if (d.abandonDwellTimeMsString != null) {
                            m.abandonDwellTimeMsString = $String(d.abandonDwellTimeMsString);
                        }
                        return m;
                    };

                    SideBySideSurveyAbandonEventData.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.abandonDwellTimeMsString != null && $Object.hasOwnProperty.call(m, "abandonDwellTimeMsString")) {
                            d.abandonDwellTimeMsString = m.abandonDwellTimeMsString;
                        }
                        return d;
                    };

                    SideBySideSurveyAbandonEventData.prototype.toJSON = function() {
                        return SideBySideSurveyAbandonEventData.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyAbandonEventData.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyAbandonEventData";
                    };

                    return SideBySideSurveyAbandonEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData = (function() {

                    const SideBySideSurveyCTAClickEventData = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    SideBySideSurveyCTAClickEventData.prototype.isSurveyExpired = null;
                    SideBySideSurveyCTAClickEventData.prototype.clickDwellTimeMsString = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SideBySideSurveyCTAClickEventData.prototype, "_isSurveyExpired", {
                        get: $util.oneOfGetter($oneOfFields = ["isSurveyExpired"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SideBySideSurveyCTAClickEventData.prototype, "_clickDwellTimeMsString", {
                        get: $util.oneOfGetter($oneOfFields = ["clickDwellTimeMsString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyCTAClickEventData.create = function(properties) {
                        return new SideBySideSurveyCTAClickEventData(properties);
                    };

                    SideBySideSurveyCTAClickEventData.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.isSurveyExpired != null && $Object.hasOwnProperty.call(m, "isSurveyExpired"))
                            w.uint32(8).bool(m.isSurveyExpired);
                        if (m.clickDwellTimeMsString != null && $Object.hasOwnProperty.call(m, "clickDwellTimeMsString"))
                            w.uint32(18).string(m.clickDwellTimeMsString);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    SideBySideSurveyCTAClickEventData.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 0)
                                        break;
                                    m.isSurveyExpired = r.bool();
                                    m._isSurveyExpired = "isSurveyExpired";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.clickDwellTimeMsString = r.stringVerify();
                                    m._clickDwellTimeMsString = "clickDwellTimeMsString";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    SideBySideSurveyCTAClickEventData.fromObject = function (d, q) {
                        if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData();
                        if (d.isSurveyExpired != null) {
                            m.isSurveyExpired = $Boolean(d.isSurveyExpired);
                        }
                        if (d.clickDwellTimeMsString != null) {
                            m.clickDwellTimeMsString = $String(d.clickDwellTimeMsString);
                        }
                        return m;
                    };

                    SideBySideSurveyCTAClickEventData.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.isSurveyExpired != null && $Object.hasOwnProperty.call(m, "isSurveyExpired")) {
                            d.isSurveyExpired = m.isSurveyExpired;
                        }
                        if (m.clickDwellTimeMsString != null && $Object.hasOwnProperty.call(m, "clickDwellTimeMsString")) {
                            d.clickDwellTimeMsString = m.clickDwellTimeMsString;
                        }
                        return d;
                    };

                    SideBySideSurveyCTAClickEventData.prototype.toJSON = function() {
                        return SideBySideSurveyCTAClickEventData.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyCTAClickEventData.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAClickEventData";
                    };

                    return SideBySideSurveyCTAClickEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData = (function() {

                    const SideBySideSurveyCTAImpressionEventData = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    SideBySideSurveyCTAImpressionEventData.prototype.isSurveyExpired = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SideBySideSurveyCTAImpressionEventData.prototype, "_isSurveyExpired", {
                        get: $util.oneOfGetter($oneOfFields = ["isSurveyExpired"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyCTAImpressionEventData.create = function(properties) {
                        return new SideBySideSurveyCTAImpressionEventData(properties);
                    };

                    SideBySideSurveyCTAImpressionEventData.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.isSurveyExpired != null && $Object.hasOwnProperty.call(m, "isSurveyExpired"))
                            w.uint32(8).bool(m.isSurveyExpired);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    SideBySideSurveyCTAImpressionEventData.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 0)
                                        break;
                                    m.isSurveyExpired = r.bool();
                                    m._isSurveyExpired = "isSurveyExpired";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    SideBySideSurveyCTAImpressionEventData.fromObject = function (d, q) {
                        if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData();
                        if (d.isSurveyExpired != null) {
                            m.isSurveyExpired = $Boolean(d.isSurveyExpired);
                        }
                        return m;
                    };

                    SideBySideSurveyCTAImpressionEventData.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.isSurveyExpired != null && $Object.hasOwnProperty.call(m, "isSurveyExpired")) {
                            d.isSurveyExpired = m.isSurveyExpired;
                        }
                        return d;
                    };

                    SideBySideSurveyCTAImpressionEventData.prototype.toJSON = function() {
                        return SideBySideSurveyCTAImpressionEventData.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyCTAImpressionEventData.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCTAImpressionEventData";
                    };

                    return SideBySideSurveyCTAImpressionEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData = (function() {

                    const SideBySideSurveyCardImpressionEventData = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    SideBySideSurveyCardImpressionEventData.create = function(properties) {
                        return new SideBySideSurveyCardImpressionEventData(properties);
                    };

                    SideBySideSurveyCardImpressionEventData.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    SideBySideSurveyCardImpressionEventData.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            r.skipType(t & 7, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    SideBySideSurveyCardImpressionEventData.fromObject = function (d, q) {
                        if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        return new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData();
                    };

                    SideBySideSurveyCardImpressionEventData.toObject = function () {
                        return {};
                    };

                    SideBySideSurveyCardImpressionEventData.prototype.toJSON = function() {
                        return SideBySideSurveyCardImpressionEventData.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyCardImpressionEventData.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyCardImpressionEventData";
                    };

                    return SideBySideSurveyCardImpressionEventData;
                })();

                SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData = (function() {

                    const SideBySideSurveyResponseEventData = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    SideBySideSurveyResponseEventData.prototype.responseDwellTimeMsString = null;
                    SideBySideSurveyResponseEventData.prototype.selectedResponseId = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SideBySideSurveyResponseEventData.prototype, "_responseDwellTimeMsString", {
                        get: $util.oneOfGetter($oneOfFields = ["responseDwellTimeMsString"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SideBySideSurveyResponseEventData.prototype, "_selectedResponseId", {
                        get: $util.oneOfGetter($oneOfFields = ["selectedResponseId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SideBySideSurveyResponseEventData.create = function(properties) {
                        return new SideBySideSurveyResponseEventData(properties);
                    };

                    SideBySideSurveyResponseEventData.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.responseDwellTimeMsString != null && $Object.hasOwnProperty.call(m, "responseDwellTimeMsString"))
                            w.uint32(10).string(m.responseDwellTimeMsString);
                        if (m.selectedResponseId != null && $Object.hasOwnProperty.call(m, "selectedResponseId"))
                            w.uint32(18).string(m.selectedResponseId);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    SideBySideSurveyResponseEventData.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.responseDwellTimeMsString = r.stringVerify();
                                    m._responseDwellTimeMsString = "responseDwellTimeMsString";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.selectedResponseId = r.stringVerify();
                                    m._selectedResponseId = "selectedResponseId";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    SideBySideSurveyResponseEventData.fromObject = function (d, q) {
                        if (d instanceof $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData();
                        if (d.responseDwellTimeMsString != null) {
                            m.responseDwellTimeMsString = $String(d.responseDwellTimeMsString);
                        }
                        if (d.selectedResponseId != null) {
                            m.selectedResponseId = $String(d.selectedResponseId);
                        }
                        return m;
                    };

                    SideBySideSurveyResponseEventData.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.responseDwellTimeMsString != null && $Object.hasOwnProperty.call(m, "responseDwellTimeMsString")) {
                            d.responseDwellTimeMsString = m.responseDwellTimeMsString;
                        }
                        if (m.selectedResponseId != null && $Object.hasOwnProperty.call(m, "selectedResponseId")) {
                            d.selectedResponseId = m.selectedResponseId;
                        }
                        return d;
                    };

                    SideBySideSurveyResponseEventData.prototype.toJSON = function() {
                        return SideBySideSurveyResponseEventData.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SideBySideSurveyResponseEventData.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.BotFeedbackMessage.SideBySideSurveyMetadata.SidebySideSurveyMetaAiAnalyticsData.SideBySideSurveyResponseEventData";
                    };

                    return SideBySideSurveyResponseEventData;
                })();

                return SidebySideSurveyMetaAiAnalyticsData;
            })();

            return SideBySideSurveyMetadata;
        })();

        return BotFeedbackMessage;
    })();

    proto.BotGroupMetadata = (function() {

        const BotGroupMetadata = function (p) {
            this.participantsMetadata = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotGroupMetadata.prototype.participantsMetadata = $util.emptyArray;

        BotGroupMetadata.create = function(properties) {
            return new BotGroupMetadata(properties);
        };

        BotGroupMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.participantsMetadata != null && m.participantsMetadata.length) {
                for (var i = 0; i < m.participantsMetadata.length; ++i)
                    $root.proto.BotGroupParticipantMetadata.encode(m.participantsMetadata[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotGroupMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotGroupMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.participantsMetadata && m.participantsMetadata.length))
                            m.participantsMetadata = [];
                        m.participantsMetadata.push($root.proto.BotGroupParticipantMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotGroupMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotGroupMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotGroupMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotGroupMetadata();
            if (d.participantsMetadata) {
                if (!$Array.isArray(d.participantsMetadata))
                    throw $TypeError(".proto.BotGroupMetadata.participantsMetadata: array expected");
                m.participantsMetadata = $Array(d.participantsMetadata.length);
                for (var i = 0; i < d.participantsMetadata.length; ++i) {
                    if (!$util.isObject(d.participantsMetadata[i]))
                        throw $TypeError(".proto.BotGroupMetadata.participantsMetadata: object expected");
                    m.participantsMetadata[i] = $root.proto.BotGroupParticipantMetadata.fromObject(d.participantsMetadata[i], q + 1);
                }
            }
            return m;
        };

        BotGroupMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.participantsMetadata = [];
            }
            if (m.participantsMetadata && m.participantsMetadata.length) {
                d.participantsMetadata = $Array(m.participantsMetadata.length);
                for (var j = 0; j < m.participantsMetadata.length; ++j) {
                    d.participantsMetadata[j] = $root.proto.BotGroupParticipantMetadata.toObject(m.participantsMetadata[j], o, q + 1);
                }
            }
            return d;
        };

        BotGroupMetadata.prototype.toJSON = function() {
            return BotGroupMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotGroupMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotGroupMetadata";
        };

        return BotGroupMetadata;
    })();

    proto.BotGroupParticipantMetadata = (function() {

        const BotGroupParticipantMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotGroupParticipantMetadata.prototype.botFbid = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotGroupParticipantMetadata.prototype, "_botFbid", {
            get: $util.oneOfGetter($oneOfFields = ["botFbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotGroupParticipantMetadata.create = function(properties) {
            return new BotGroupParticipantMetadata(properties);
        };

        BotGroupParticipantMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.botFbid != null && $Object.hasOwnProperty.call(m, "botFbid"))
                w.uint32(10).string(m.botFbid);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotGroupParticipantMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotGroupParticipantMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.botFbid = r.stringVerify();
                        m._botFbid = "botFbid";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotGroupParticipantMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotGroupParticipantMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotGroupParticipantMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotGroupParticipantMetadata();
            if (d.botFbid != null) {
                m.botFbid = $String(d.botFbid);
            }
            return m;
        };

        BotGroupParticipantMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.botFbid != null && $Object.hasOwnProperty.call(m, "botFbid")) {
                d.botFbid = m.botFbid;
            }
            return d;
        };

        BotGroupParticipantMetadata.prototype.toJSON = function() {
            return BotGroupParticipantMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotGroupParticipantMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotGroupParticipantMetadata";
        };

        return BotGroupParticipantMetadata;
    })();

    proto.BotHistoryShareMetadata = (function() {

        const BotHistoryShareMetadata = function (p) {
            this.participantsMetadata = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotHistoryShareMetadata.prototype.participantsMetadata = $util.emptyArray;

        BotHistoryShareMetadata.create = function(properties) {
            return new BotHistoryShareMetadata(properties);
        };

        BotHistoryShareMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.participantsMetadata != null && m.participantsMetadata.length) {
                for (var i = 0; i < m.participantsMetadata.length; ++i)
                    $root.proto.BotGroupParticipantMetadata.encode(m.participantsMetadata[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotHistoryShareMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotHistoryShareMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.participantsMetadata && m.participantsMetadata.length))
                            m.participantsMetadata = [];
                        m.participantsMetadata.push($root.proto.BotGroupParticipantMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotHistoryShareMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotHistoryShareMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotHistoryShareMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotHistoryShareMetadata();
            if (d.participantsMetadata) {
                if (!$Array.isArray(d.participantsMetadata))
                    throw $TypeError(".proto.BotHistoryShareMetadata.participantsMetadata: array expected");
                m.participantsMetadata = $Array(d.participantsMetadata.length);
                for (var i = 0; i < d.participantsMetadata.length; ++i) {
                    if (!$util.isObject(d.participantsMetadata[i]))
                        throw $TypeError(".proto.BotHistoryShareMetadata.participantsMetadata: object expected");
                    m.participantsMetadata[i] = $root.proto.BotGroupParticipantMetadata.fromObject(d.participantsMetadata[i], q + 1);
                }
            }
            return m;
        };

        BotHistoryShareMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.participantsMetadata = [];
            }
            if (m.participantsMetadata && m.participantsMetadata.length) {
                d.participantsMetadata = $Array(m.participantsMetadata.length);
                for (var j = 0; j < m.participantsMetadata.length; ++j) {
                    d.participantsMetadata[j] = $root.proto.BotGroupParticipantMetadata.toObject(m.participantsMetadata[j], o, q + 1);
                }
            }
            return d;
        };

        BotHistoryShareMetadata.prototype.toJSON = function() {
            return BotHistoryShareMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotHistoryShareMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotHistoryShareMetadata";
        };

        return BotHistoryShareMetadata;
    })();

    proto.BotImagineMetadata = (function() {

        const BotImagineMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotImagineMetadata.prototype.imagineType = null;
        BotImagineMetadata.prototype.shortPrompt = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotImagineMetadata.prototype, "_imagineType", {
            get: $util.oneOfGetter($oneOfFields = ["imagineType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotImagineMetadata.prototype, "_shortPrompt", {
            get: $util.oneOfGetter($oneOfFields = ["shortPrompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotImagineMetadata.create = function(properties) {
            return new BotImagineMetadata(properties);
        };

        BotImagineMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.imagineType != null && $Object.hasOwnProperty.call(m, "imagineType"))
                w.uint32(8).int32(m.imagineType);
            if (m.shortPrompt != null && $Object.hasOwnProperty.call(m, "shortPrompt"))
                w.uint32(18).string(m.shortPrompt);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotImagineMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotImagineMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.imagineType = r.int32();
                        m._imagineType = "imagineType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.shortPrompt = r.stringVerify();
                        m._shortPrompt = "shortPrompt";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotImagineMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotImagineMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotImagineMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotImagineMetadata();
            switch (d.imagineType) {
            case "UNKNOWN":
            case 0:
                m.imagineType = 0;
                break;
            case "IMAGINE":
            case 1:
                m.imagineType = 1;
                break;
            case "MEMU":
            case 2:
                m.imagineType = 2;
                break;
            case "FLASH":
            case 3:
                m.imagineType = 3;
                break;
            case "EDIT":
            case 4:
                m.imagineType = 4;
                break;
            default:
                if (typeof d.imagineType === "number" && (d.imagineType | 0) === d.imagineType)
                    m.imagineType = d.imagineType;
            }
            if (d.shortPrompt != null) {
                m.shortPrompt = $String(d.shortPrompt);
            }
            return m;
        };

        BotImagineMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.imagineType != null && $Object.hasOwnProperty.call(m, "imagineType")) {
                d.imagineType = o.enums === $String ? $root.proto.BotImagineMetadata.ImagineType[m.imagineType] === $undefined ? m.imagineType : $root.proto.BotImagineMetadata.ImagineType[m.imagineType] : m.imagineType;
            }
            if (m.shortPrompt != null && $Object.hasOwnProperty.call(m, "shortPrompt")) {
                d.shortPrompt = m.shortPrompt;
            }
            return d;
        };

        BotImagineMetadata.prototype.toJSON = function() {
            return BotImagineMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotImagineMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotImagineMetadata";
        };

        BotImagineMetadata.ImagineType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "IMAGINE"] = 1;
            values[valuesById[2] = "MEMU"] = 2;
            values[valuesById[3] = "FLASH"] = 3;
            values[valuesById[4] = "EDIT"] = 4;
            return values;
        })();

        return BotImagineMetadata;
    })();

    proto.BotInfrastructureDiagnostics = (function() {

        const BotInfrastructureDiagnostics = function (p) {
            this.toolsUsed = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotInfrastructureDiagnostics.prototype.botBackend = null;
        BotInfrastructureDiagnostics.prototype.toolsUsed = $util.emptyArray;
        BotInfrastructureDiagnostics.prototype.isThinking = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotInfrastructureDiagnostics.prototype, "_botBackend", {
            get: $util.oneOfGetter($oneOfFields = ["botBackend"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotInfrastructureDiagnostics.prototype, "_isThinking", {
            get: $util.oneOfGetter($oneOfFields = ["isThinking"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotInfrastructureDiagnostics.create = function(properties) {
            return new BotInfrastructureDiagnostics(properties);
        };

        BotInfrastructureDiagnostics.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.botBackend != null && $Object.hasOwnProperty.call(m, "botBackend"))
                w.uint32(8).int32(m.botBackend);
            if (m.toolsUsed != null && m.toolsUsed.length) {
                for (var i = 0; i < m.toolsUsed.length; ++i)
                    w.uint32(18).string(m.toolsUsed[i]);
            }
            if (m.isThinking != null && $Object.hasOwnProperty.call(m, "isThinking"))
                w.uint32(24).bool(m.isThinking);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotInfrastructureDiagnostics.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotInfrastructureDiagnostics(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.botBackend = r.int32();
                        m._botBackend = "botBackend";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.toolsUsed && m.toolsUsed.length))
                            m.toolsUsed = [];
                        m.toolsUsed.push(r.stringVerify());
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.isThinking = r.bool();
                        m._isThinking = "isThinking";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotInfrastructureDiagnostics.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotInfrastructureDiagnostics)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotInfrastructureDiagnostics: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotInfrastructureDiagnostics();
            switch (d.botBackend) {
            case "AAPI":
            case 0:
                m.botBackend = 0;
                break;
            case "CLIPPY":
            case 1:
                m.botBackend = 1;
                break;
            default:
                if (typeof d.botBackend === "number" && (d.botBackend | 0) === d.botBackend)
                    m.botBackend = d.botBackend;
            }
            if (d.toolsUsed) {
                if (!$Array.isArray(d.toolsUsed))
                    throw $TypeError(".proto.BotInfrastructureDiagnostics.toolsUsed: array expected");
                m.toolsUsed = $Array(d.toolsUsed.length);
                for (var i = 0; i < d.toolsUsed.length; ++i) {
                    m.toolsUsed[i] = $String(d.toolsUsed[i]);
                }
            }
            if (d.isThinking != null) {
                m.isThinking = $Boolean(d.isThinking);
            }
            return m;
        };

        BotInfrastructureDiagnostics.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.toolsUsed = [];
            }
            if (m.botBackend != null && $Object.hasOwnProperty.call(m, "botBackend")) {
                d.botBackend = o.enums === $String ? $root.proto.BotInfrastructureDiagnostics.BotBackend[m.botBackend] === $undefined ? m.botBackend : $root.proto.BotInfrastructureDiagnostics.BotBackend[m.botBackend] : m.botBackend;
            }
            if (m.toolsUsed && m.toolsUsed.length) {
                d.toolsUsed = $Array(m.toolsUsed.length);
                for (var j = 0; j < m.toolsUsed.length; ++j) {
                    d.toolsUsed[j] = m.toolsUsed[j];
                }
            }
            if (m.isThinking != null && $Object.hasOwnProperty.call(m, "isThinking")) {
                d.isThinking = m.isThinking;
            }
            return d;
        };

        BotInfrastructureDiagnostics.prototype.toJSON = function() {
            return BotInfrastructureDiagnostics.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotInfrastructureDiagnostics.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotInfrastructureDiagnostics";
        };

        BotInfrastructureDiagnostics.BotBackend = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "AAPI"] = 0;
            values[valuesById[1] = "CLIPPY"] = 1;
            return values;
        })();

        return BotInfrastructureDiagnostics;
    })();

    proto.BotLinkedAccount = (function() {

        const BotLinkedAccount = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotLinkedAccount.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotLinkedAccount.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotLinkedAccount.create = function(properties) {
            return new BotLinkedAccount(properties);
        };

        BotLinkedAccount.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotLinkedAccount.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotLinkedAccount(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.type = r.int32();
                        m._type = "type";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotLinkedAccount.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotLinkedAccount)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotLinkedAccount: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotLinkedAccount();
            switch (d.type) {
            case "BOT_LINKED_ACCOUNT_TYPE_1P":
            case 0:
                m.type = 0;
                break;
            default:
                if (typeof d.type === "number" && (d.type | 0) === d.type)
                    m.type = d.type;
            }
            return m;
        };

        BotLinkedAccount.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                d.type = o.enums === $String ? $root.proto.BotLinkedAccount.BotLinkedAccountType[m.type] === $undefined ? m.type : $root.proto.BotLinkedAccount.BotLinkedAccountType[m.type] : m.type;
            }
            return d;
        };

        BotLinkedAccount.prototype.toJSON = function() {
            return BotLinkedAccount.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotLinkedAccount.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotLinkedAccount";
        };

        BotLinkedAccount.BotLinkedAccountType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "BOT_LINKED_ACCOUNT_TYPE_1P"] = 0;
            return values;
        })();

        return BotLinkedAccount;
    })();

    proto.BotLinkedAccountsMetadata = (function() {

        const BotLinkedAccountsMetadata = function (p) {
            this.accounts = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotLinkedAccountsMetadata.prototype.accounts = $util.emptyArray;
        BotLinkedAccountsMetadata.prototype.acAuthTokens = null;
        BotLinkedAccountsMetadata.prototype.acErrorCode = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotLinkedAccountsMetadata.prototype, "_acAuthTokens", {
            get: $util.oneOfGetter($oneOfFields = ["acAuthTokens"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotLinkedAccountsMetadata.prototype, "_acErrorCode", {
            get: $util.oneOfGetter($oneOfFields = ["acErrorCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotLinkedAccountsMetadata.create = function(properties) {
            return new BotLinkedAccountsMetadata(properties);
        };

        BotLinkedAccountsMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.accounts != null && m.accounts.length) {
                for (var i = 0; i < m.accounts.length; ++i)
                    $root.proto.BotLinkedAccount.encode(m.accounts[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.acAuthTokens != null && $Object.hasOwnProperty.call(m, "acAuthTokens"))
                w.uint32(18).bytes(m.acAuthTokens);
            if (m.acErrorCode != null && $Object.hasOwnProperty.call(m, "acErrorCode"))
                w.uint32(24).int32(m.acErrorCode);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotLinkedAccountsMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotLinkedAccountsMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.accounts && m.accounts.length))
                            m.accounts = [];
                        m.accounts.push($root.proto.BotLinkedAccount.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.acAuthTokens = r.bytes();
                        m._acAuthTokens = "acAuthTokens";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.acErrorCode = r.int32();
                        m._acErrorCode = "acErrorCode";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotLinkedAccountsMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotLinkedAccountsMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotLinkedAccountsMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotLinkedAccountsMetadata();
            if (d.accounts) {
                if (!$Array.isArray(d.accounts))
                    throw $TypeError(".proto.BotLinkedAccountsMetadata.accounts: array expected");
                m.accounts = $Array(d.accounts.length);
                for (var i = 0; i < d.accounts.length; ++i) {
                    if (!$util.isObject(d.accounts[i]))
                        throw $TypeError(".proto.BotLinkedAccountsMetadata.accounts: object expected");
                    m.accounts[i] = $root.proto.BotLinkedAccount.fromObject(d.accounts[i], q + 1);
                }
            }
            if (d.acAuthTokens != null) {
                if (typeof d.acAuthTokens === "string")
                    $util.base64.decode(d.acAuthTokens, m.acAuthTokens = $util.newBuffer($util.base64.length(d.acAuthTokens)), 0);
                else if (d.acAuthTokens.length >= 0)
                    m.acAuthTokens = d.acAuthTokens;
            }
            if (d.acErrorCode != null) {
                m.acErrorCode = d.acErrorCode | 0;
            }
            return m;
        };

        BotLinkedAccountsMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.accounts = [];
            }
            if (m.accounts && m.accounts.length) {
                d.accounts = $Array(m.accounts.length);
                for (var j = 0; j < m.accounts.length; ++j) {
                    d.accounts[j] = $root.proto.BotLinkedAccount.toObject(m.accounts[j], o, q + 1);
                }
            }
            if (m.acAuthTokens != null && $Object.hasOwnProperty.call(m, "acAuthTokens")) {
                d.acAuthTokens = o.bytes === $String ? $util.base64.encode(m.acAuthTokens, 0, m.acAuthTokens.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.acAuthTokens) : m.acAuthTokens;
            }
            if (m.acErrorCode != null && $Object.hasOwnProperty.call(m, "acErrorCode")) {
                d.acErrorCode = m.acErrorCode;
            }
            return d;
        };

        BotLinkedAccountsMetadata.prototype.toJSON = function() {
            return BotLinkedAccountsMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotLinkedAccountsMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotLinkedAccountsMetadata";
        };

        return BotLinkedAccountsMetadata;
    })();

    proto.BotMediaMetadata = (function() {

        const BotMediaMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMediaMetadata.prototype.fileSha256 = null;
        BotMediaMetadata.prototype.mediaKey = null;
        BotMediaMetadata.prototype.fileEncSha256 = null;
        BotMediaMetadata.prototype.directPath = null;
        BotMediaMetadata.prototype.mediaKeyTimestamp = null;
        BotMediaMetadata.prototype.mimetype = null;
        BotMediaMetadata.prototype.orientationType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMediaMetadata.prototype, "_orientationType", {
            get: $util.oneOfGetter($oneOfFields = ["orientationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMediaMetadata.create = function(properties) {
            return new BotMediaMetadata(properties);
        };

        BotMediaMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                w.uint32(10).string(m.fileSha256);
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(18).string(m.mediaKey);
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                w.uint32(26).string(m.fileEncSha256);
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(34).string(m.directPath);
            if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(40).int64(m.mediaKeyTimestamp);
            if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(50).string(m.mimetype);
            if (m.orientationType != null && $Object.hasOwnProperty.call(m, "orientationType"))
                w.uint32(56).int32(m.orientationType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMediaMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMediaMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.fileSha256 = r.stringVerify();
                        m._fileSha256 = "fileSha256";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.mediaKey = r.stringVerify();
                        m._mediaKey = "mediaKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.fileEncSha256 = r.stringVerify();
                        m._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.directPath = r.stringVerify();
                        m._directPath = "directPath";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.mediaKeyTimestamp = r.int64();
                        m._mediaKeyTimestamp = "mediaKeyTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.mimetype = r.stringVerify();
                        m._mimetype = "mimetype";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.orientationType = r.int32();
                        m._orientationType = "orientationType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMediaMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMediaMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMediaMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMediaMetadata();
            if (d.fileSha256 != null) {
                m.fileSha256 = $String(d.fileSha256);
            }
            if (d.mediaKey != null) {
                m.mediaKey = $String(d.mediaKey);
            }
            if (d.fileEncSha256 != null) {
                m.fileEncSha256 = $String(d.fileEncSha256);
            }
            if (d.directPath != null) {
                m.directPath = $String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.mimetype != null) {
                m.mimetype = $String(d.mimetype);
            }
            switch (d.orientationType) {
            case "CENTER":
            case 1:
                m.orientationType = 1;
                break;
            case "LEFT":
            case 2:
                m.orientationType = 2;
                break;
            case "RIGHT":
            case 3:
                m.orientationType = 3;
                break;
            default:
                if (typeof d.orientationType === "number" && (d.orientationType | 0) === d.orientationType)
                    m.orientationType = d.orientationType;
            }
            return m;
        };

        BotMediaMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                d.fileSha256 = m.fileSha256;
            }
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                d.mediaKey = m.mediaKey;
            }
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                d.fileEncSha256 = m.fileEncSha256;
            }
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                d.directPath = m.directPath;
            }
            if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
            }
            if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                d.mimetype = m.mimetype;
            }
            if (m.orientationType != null && $Object.hasOwnProperty.call(m, "orientationType")) {
                d.orientationType = o.enums === $String ? $root.proto.BotMediaMetadata.OrientationType[m.orientationType] === $undefined ? m.orientationType : $root.proto.BotMediaMetadata.OrientationType[m.orientationType] : m.orientationType;
            }
            return d;
        };

        BotMediaMetadata.prototype.toJSON = function() {
            return BotMediaMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMediaMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMediaMetadata";
        };

        BotMediaMetadata.OrientationType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "CENTER"] = 1;
            values[valuesById[2] = "LEFT"] = 2;
            values[valuesById[3] = "RIGHT"] = 3;
            return values;
        })();

        return BotMediaMetadata;
    })();

    proto.BotMemoryFact = (function() {

        const BotMemoryFact = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMemoryFact.prototype.fact = null;
        BotMemoryFact.prototype.factId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMemoryFact.prototype, "_fact", {
            get: $util.oneOfGetter($oneOfFields = ["fact"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMemoryFact.prototype, "_factId", {
            get: $util.oneOfGetter($oneOfFields = ["factId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMemoryFact.create = function(properties) {
            return new BotMemoryFact(properties);
        };

        BotMemoryFact.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.fact != null && $Object.hasOwnProperty.call(m, "fact"))
                w.uint32(10).string(m.fact);
            if (m.factId != null && $Object.hasOwnProperty.call(m, "factId"))
                w.uint32(18).string(m.factId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMemoryFact.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMemoryFact();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.fact = r.stringVerify();
                        m._fact = "fact";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.factId = r.stringVerify();
                        m._factId = "factId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMemoryFact.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMemoryFact)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMemoryFact: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMemoryFact();
            if (d.fact != null) {
                m.fact = $String(d.fact);
            }
            if (d.factId != null) {
                m.factId = $String(d.factId);
            }
            return m;
        };

        BotMemoryFact.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.fact != null && $Object.hasOwnProperty.call(m, "fact")) {
                d.fact = m.fact;
            }
            if (m.factId != null && $Object.hasOwnProperty.call(m, "factId")) {
                d.factId = m.factId;
            }
            return d;
        };

        BotMemoryFact.prototype.toJSON = function() {
            return BotMemoryFact.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMemoryFact.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMemoryFact";
        };

        return BotMemoryFact;
    })();

    proto.BotMemoryMetadata = (function() {

        const BotMemoryMetadata = function (p) {
            this.addedFacts = [];
            this.removedFacts = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMemoryMetadata.prototype.addedFacts = $util.emptyArray;
        BotMemoryMetadata.prototype.removedFacts = $util.emptyArray;
        BotMemoryMetadata.prototype.disclaimer = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMemoryMetadata.prototype, "_disclaimer", {
            get: $util.oneOfGetter($oneOfFields = ["disclaimer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMemoryMetadata.create = function(properties) {
            return new BotMemoryMetadata(properties);
        };

        BotMemoryMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.addedFacts != null && m.addedFacts.length) {
                for (var i = 0; i < m.addedFacts.length; ++i)
                    $root.proto.BotMemoryFact.encode(m.addedFacts[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.removedFacts != null && m.removedFacts.length) {
                for (var i = 0; i < m.removedFacts.length; ++i)
                    $root.proto.BotMemoryFact.encode(m.removedFacts[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.disclaimer != null && $Object.hasOwnProperty.call(m, "disclaimer"))
                w.uint32(26).string(m.disclaimer);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMemoryMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMemoryMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.addedFacts && m.addedFacts.length))
                            m.addedFacts = [];
                        m.addedFacts.push($root.proto.BotMemoryFact.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.removedFacts && m.removedFacts.length))
                            m.removedFacts = [];
                        m.removedFacts.push($root.proto.BotMemoryFact.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.disclaimer = r.stringVerify();
                        m._disclaimer = "disclaimer";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMemoryMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMemoryMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMemoryMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMemoryMetadata();
            if (d.addedFacts) {
                if (!$Array.isArray(d.addedFacts))
                    throw $TypeError(".proto.BotMemoryMetadata.addedFacts: array expected");
                m.addedFacts = $Array(d.addedFacts.length);
                for (var i = 0; i < d.addedFacts.length; ++i) {
                    if (!$util.isObject(d.addedFacts[i]))
                        throw $TypeError(".proto.BotMemoryMetadata.addedFacts: object expected");
                    m.addedFacts[i] = $root.proto.BotMemoryFact.fromObject(d.addedFacts[i], q + 1);
                }
            }
            if (d.removedFacts) {
                if (!$Array.isArray(d.removedFacts))
                    throw $TypeError(".proto.BotMemoryMetadata.removedFacts: array expected");
                m.removedFacts = $Array(d.removedFacts.length);
                for (var i = 0; i < d.removedFacts.length; ++i) {
                    if (!$util.isObject(d.removedFacts[i]))
                        throw $TypeError(".proto.BotMemoryMetadata.removedFacts: object expected");
                    m.removedFacts[i] = $root.proto.BotMemoryFact.fromObject(d.removedFacts[i], q + 1);
                }
            }
            if (d.disclaimer != null) {
                m.disclaimer = $String(d.disclaimer);
            }
            return m;
        };

        BotMemoryMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.addedFacts = [];
                d.removedFacts = [];
            }
            if (m.addedFacts && m.addedFacts.length) {
                d.addedFacts = $Array(m.addedFacts.length);
                for (var j = 0; j < m.addedFacts.length; ++j) {
                    d.addedFacts[j] = $root.proto.BotMemoryFact.toObject(m.addedFacts[j], o, q + 1);
                }
            }
            if (m.removedFacts && m.removedFacts.length) {
                d.removedFacts = $Array(m.removedFacts.length);
                for (var j = 0; j < m.removedFacts.length; ++j) {
                    d.removedFacts[j] = $root.proto.BotMemoryFact.toObject(m.removedFacts[j], o, q + 1);
                }
            }
            if (m.disclaimer != null && $Object.hasOwnProperty.call(m, "disclaimer")) {
                d.disclaimer = m.disclaimer;
            }
            return d;
        };

        BotMemoryMetadata.prototype.toJSON = function() {
            return BotMemoryMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMemoryMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMemoryMetadata";
        };

        return BotMemoryMetadata;
    })();

    proto.BotMemuMetadata = (function() {

        const BotMemuMetadata = function (p) {
            this.faceImages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMemuMetadata.prototype.faceImages = $util.emptyArray;

        BotMemuMetadata.create = function(properties) {
            return new BotMemuMetadata(properties);
        };

        BotMemuMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.faceImages != null && m.faceImages.length) {
                for (var i = 0; i < m.faceImages.length; ++i)
                    $root.proto.BotMediaMetadata.encode(m.faceImages[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMemuMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMemuMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.faceImages && m.faceImages.length))
                            m.faceImages = [];
                        m.faceImages.push($root.proto.BotMediaMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMemuMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMemuMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMemuMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMemuMetadata();
            if (d.faceImages) {
                if (!$Array.isArray(d.faceImages))
                    throw $TypeError(".proto.BotMemuMetadata.faceImages: array expected");
                m.faceImages = $Array(d.faceImages.length);
                for (var i = 0; i < d.faceImages.length; ++i) {
                    if (!$util.isObject(d.faceImages[i]))
                        throw $TypeError(".proto.BotMemuMetadata.faceImages: object expected");
                    m.faceImages[i] = $root.proto.BotMediaMetadata.fromObject(d.faceImages[i], q + 1);
                }
            }
            return m;
        };

        BotMemuMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.faceImages = [];
            }
            if (m.faceImages && m.faceImages.length) {
                d.faceImages = $Array(m.faceImages.length);
                for (var j = 0; j < m.faceImages.length; ++j) {
                    d.faceImages[j] = $root.proto.BotMediaMetadata.toObject(m.faceImages[j], o, q + 1);
                }
            }
            return d;
        };

        BotMemuMetadata.prototype.toJSON = function() {
            return BotMemuMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMemuMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMemuMetadata";
        };

        return BotMemuMetadata;
    })();

    proto.BotMessageOrigin = (function() {

        const BotMessageOrigin = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMessageOrigin.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMessageOrigin.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMessageOrigin.create = function(properties) {
            return new BotMessageOrigin(properties);
        };

        BotMessageOrigin.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMessageOrigin.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMessageOrigin(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.type = r.int32();
                        m._type = "type";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMessageOrigin.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMessageOrigin)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMessageOrigin: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMessageOrigin();
            switch (d.type) {
            case "BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED":
            case 0:
                m.type = 0;
                break;
            default:
                if (typeof d.type === "number" && (d.type | 0) === d.type)
                    m.type = d.type;
            }
            return m;
        };

        BotMessageOrigin.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                d.type = o.enums === $String ? $root.proto.BotMessageOrigin.BotMessageOriginType[m.type] === $undefined ? m.type : $root.proto.BotMessageOrigin.BotMessageOriginType[m.type] : m.type;
            }
            return d;
        };

        BotMessageOrigin.prototype.toJSON = function() {
            return BotMessageOrigin.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMessageOrigin.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMessageOrigin";
        };

        BotMessageOrigin.BotMessageOriginType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "BOT_MESSAGE_ORIGIN_TYPE_AI_INITIATED"] = 0;
            return values;
        })();

        return BotMessageOrigin;
    })();

    proto.BotMessageOriginMetadata = (function() {

        const BotMessageOriginMetadata = function (p) {
            this.origins = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMessageOriginMetadata.prototype.origins = $util.emptyArray;

        BotMessageOriginMetadata.create = function(properties) {
            return new BotMessageOriginMetadata(properties);
        };

        BotMessageOriginMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.origins != null && m.origins.length) {
                for (var i = 0; i < m.origins.length; ++i)
                    $root.proto.BotMessageOrigin.encode(m.origins[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMessageOriginMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMessageOriginMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.origins && m.origins.length))
                            m.origins = [];
                        m.origins.push($root.proto.BotMessageOrigin.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMessageOriginMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMessageOriginMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMessageOriginMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMessageOriginMetadata();
            if (d.origins) {
                if (!$Array.isArray(d.origins))
                    throw $TypeError(".proto.BotMessageOriginMetadata.origins: array expected");
                m.origins = $Array(d.origins.length);
                for (var i = 0; i < d.origins.length; ++i) {
                    if (!$util.isObject(d.origins[i]))
                        throw $TypeError(".proto.BotMessageOriginMetadata.origins: object expected");
                    m.origins[i] = $root.proto.BotMessageOrigin.fromObject(d.origins[i], q + 1);
                }
            }
            return m;
        };

        BotMessageOriginMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.origins = [];
            }
            if (m.origins && m.origins.length) {
                d.origins = $Array(m.origins.length);
                for (var j = 0; j < m.origins.length; ++j) {
                    d.origins[j] = $root.proto.BotMessageOrigin.toObject(m.origins[j], o, q + 1);
                }
            }
            return d;
        };

        BotMessageOriginMetadata.prototype.toJSON = function() {
            return BotMessageOriginMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMessageOriginMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMessageOriginMetadata";
        };

        return BotMessageOriginMetadata;
    })();

    proto.BotMessageSharingInfo = (function() {

        const BotMessageSharingInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMessageSharingInfo.prototype.botEntryPointOrigin = null;
        BotMessageSharingInfo.prototype.forwardScore = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMessageSharingInfo.prototype, "_botEntryPointOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["botEntryPointOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMessageSharingInfo.prototype, "_forwardScore", {
            get: $util.oneOfGetter($oneOfFields = ["forwardScore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMessageSharingInfo.create = function(properties) {
            return new BotMessageSharingInfo(properties);
        };

        BotMessageSharingInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.botEntryPointOrigin != null && $Object.hasOwnProperty.call(m, "botEntryPointOrigin"))
                w.uint32(8).int32(m.botEntryPointOrigin);
            if (m.forwardScore != null && $Object.hasOwnProperty.call(m, "forwardScore"))
                w.uint32(16).uint32(m.forwardScore);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMessageSharingInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMessageSharingInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.botEntryPointOrigin = r.int32();
                        m._botEntryPointOrigin = "botEntryPointOrigin";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.forwardScore = r.uint32();
                        m._forwardScore = "forwardScore";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMessageSharingInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMessageSharingInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMessageSharingInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMessageSharingInfo();
            switch (d.botEntryPointOrigin) {
            case "UNDEFINED_ENTRY_POINT":
            case 0:
                m.botEntryPointOrigin = 0;
                break;
            case "FAVICON":
            case 1:
                m.botEntryPointOrigin = 1;
                break;
            case "CHATLIST":
            case 2:
                m.botEntryPointOrigin = 2;
                break;
            case "AISEARCH_NULL_STATE_PAPER_PLANE":
            case 3:
                m.botEntryPointOrigin = 3;
                break;
            case "AISEARCH_NULL_STATE_SUGGESTION":
            case 4:
                m.botEntryPointOrigin = 4;
                break;
            case "AISEARCH_TYPE_AHEAD_SUGGESTION":
            case 5:
                m.botEntryPointOrigin = 5;
                break;
            case "AISEARCH_TYPE_AHEAD_PAPER_PLANE":
            case 6:
                m.botEntryPointOrigin = 6;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST":
            case 7:
                m.botEntryPointOrigin = 7;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES":
            case 8:
                m.botEntryPointOrigin = 8;
                break;
            case "AIVOICE_SEARCH_BAR":
            case 9:
                m.botEntryPointOrigin = 9;
                break;
            case "AIVOICE_FAVICON":
            case 10:
                m.botEntryPointOrigin = 10;
                break;
            case "AISTUDIO":
            case 11:
                m.botEntryPointOrigin = 11;
                break;
            case "DEEPLINK":
            case 12:
                m.botEntryPointOrigin = 12;
                break;
            case "NOTIFICATION":
            case 13:
                m.botEntryPointOrigin = 13;
                break;
            case "PROFILE_MESSAGE_BUTTON":
            case 14:
                m.botEntryPointOrigin = 14;
                break;
            case "FORWARD":
            case 15:
                m.botEntryPointOrigin = 15;
                break;
            case "APP_SHORTCUT":
            case 16:
                m.botEntryPointOrigin = 16;
                break;
            case "FF_FAMILY":
            case 17:
                m.botEntryPointOrigin = 17;
                break;
            case "AI_TAB":
            case 18:
                m.botEntryPointOrigin = 18;
                break;
            case "AI_HOME":
            case 19:
                m.botEntryPointOrigin = 19;
                break;
            case "AI_DEEPLINK_IMMERSIVE":
            case 20:
                m.botEntryPointOrigin = 20;
                break;
            case "AI_DEEPLINK":
            case 21:
                m.botEntryPointOrigin = 21;
                break;
            case "META_AI_CHAT_SHORTCUT_AI_STUDIO":
            case 22:
                m.botEntryPointOrigin = 22;
                break;
            case "UGC_CHAT_SHORTCUT_AI_STUDIO":
            case 23:
                m.botEntryPointOrigin = 23;
                break;
            case "NEW_CHAT_AI_STUDIO":
            case 24:
                m.botEntryPointOrigin = 24;
                break;
            case "AIVOICE_FAVICON_CALL_HISTORY":
            case 25:
                m.botEntryPointOrigin = 25;
                break;
            case "ASK_META_AI_CONTEXT_MENU":
            case 26:
                m.botEntryPointOrigin = 26;
                break;
            case "ASK_META_AI_CONTEXT_MENU_1ON1":
            case 27:
                m.botEntryPointOrigin = 27;
                break;
            case "ASK_META_AI_CONTEXT_MENU_GROUP":
            case 28:
                m.botEntryPointOrigin = 28;
                break;
            case "INVOKE_META_AI_1ON1":
            case 29:
                m.botEntryPointOrigin = 29;
                break;
            case "INVOKE_META_AI_GROUP":
            case 30:
                m.botEntryPointOrigin = 30;
                break;
            case "META_AI_FORWARD":
            case 31:
                m.botEntryPointOrigin = 31;
                break;
            case "NEW_CHAT_AI_CONTACT":
            case 32:
                m.botEntryPointOrigin = 32;
                break;
            case "MESSAGE_QUICK_ACTION_1_ON_1_CHAT":
            case 33:
                m.botEntryPointOrigin = 33;
                break;
            case "MESSAGE_QUICK_ACTION_GROUP_CHAT":
            case 34:
                m.botEntryPointOrigin = 34;
                break;
            case "ATTACHMENT_TRAY_1_ON_1_CHAT":
            case 35:
                m.botEntryPointOrigin = 35;
                break;
            case "ATTACHMENT_TRAY_GROUP_CHAT":
            case 36:
                m.botEntryPointOrigin = 36;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_1ON1":
            case 37:
                m.botEntryPointOrigin = 37;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_GROUP":
            case 38:
                m.botEntryPointOrigin = 38;
                break;
            case "MEDIA_PICKER_1_ON_1_CHAT":
            case 39:
                m.botEntryPointOrigin = 39;
                break;
            case "MEDIA_PICKER_GROUP_CHAT":
            case 40:
                m.botEntryPointOrigin = 40;
                break;
            case "ASK_META_AI_NO_SEARCH_RESULTS":
            case 41:
                m.botEntryPointOrigin = 41;
                break;
            case "META_AI_SETTINGS":
            case 45:
                m.botEntryPointOrigin = 45;
                break;
            case "WEB_INTRO_PANEL":
            case 46:
                m.botEntryPointOrigin = 46;
                break;
            case "WEB_NAVIGATION_BAR":
            case 47:
                m.botEntryPointOrigin = 47;
                break;
            case "GROUP_MEMBER":
            case 54:
                m.botEntryPointOrigin = 54;
                break;
            case "CHATLIST_SEARCH":
            case 55:
                m.botEntryPointOrigin = 55;
                break;
            case "NEW_CHAT_LIST":
            case 56:
                m.botEntryPointOrigin = 56;
                break;
            case "CONTACTS_TAB":
            case 57:
                m.botEntryPointOrigin = 57;
                break;
            default:
                if (typeof d.botEntryPointOrigin === "number" && (d.botEntryPointOrigin | 0) === d.botEntryPointOrigin)
                    m.botEntryPointOrigin = d.botEntryPointOrigin;
            }
            if (d.forwardScore != null) {
                m.forwardScore = d.forwardScore >>> 0;
            }
            return m;
        };

        BotMessageSharingInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.botEntryPointOrigin != null && $Object.hasOwnProperty.call(m, "botEntryPointOrigin")) {
                d.botEntryPointOrigin = o.enums === $String ? $root.proto.BotMetricsEntryPoint[m.botEntryPointOrigin] === $undefined ? m.botEntryPointOrigin : $root.proto.BotMetricsEntryPoint[m.botEntryPointOrigin] : m.botEntryPointOrigin;
            }
            if (m.forwardScore != null && $Object.hasOwnProperty.call(m, "forwardScore")) {
                d.forwardScore = m.forwardScore;
            }
            return d;
        };

        BotMessageSharingInfo.prototype.toJSON = function() {
            return BotMessageSharingInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMessageSharingInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMessageSharingInfo";
        };

        return BotMessageSharingInfo;
    })();

    proto.BotMetadata = (function() {

        const BotMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMetadata.prototype.personaId = null;
        BotMetadata.prototype.pluginMetadata = null;
        BotMetadata.prototype.suggestedPromptMetadata = null;
        BotMetadata.prototype.invokerJid = null;
        BotMetadata.prototype.sessionMetadata = null;
        BotMetadata.prototype.memuMetadata = null;
        BotMetadata.prototype.timezone = null;
        BotMetadata.prototype.reminderMetadata = null;
        BotMetadata.prototype.modelMetadata = null;
        BotMetadata.prototype.messageDisclaimerText = null;
        BotMetadata.prototype.progressIndicatorMetadata = null;
        BotMetadata.prototype.capabilityMetadata = null;
        BotMetadata.prototype.imagineMetadata = null;
        BotMetadata.prototype.memoryMetadata = null;
        BotMetadata.prototype.renderingMetadata = null;
        BotMetadata.prototype.botMetricsMetadata = null;
        BotMetadata.prototype.botLinkedAccountsMetadata = null;
        BotMetadata.prototype.richResponseSourcesMetadata = null;
        BotMetadata.prototype.aiConversationContext = null;
        BotMetadata.prototype.botPromotionMessageMetadata = null;
        BotMetadata.prototype.botModeSelectionMetadata = null;
        BotMetadata.prototype.botQuotaMetadata = null;
        BotMetadata.prototype.botAgeCollectionMetadata = null;
        BotMetadata.prototype.conversationStarterPromptId = null;
        BotMetadata.prototype.botResponseId = null;
        BotMetadata.prototype.verificationMetadata = null;
        BotMetadata.prototype.unifiedResponseMutation = null;
        BotMetadata.prototype.botMessageOriginMetadata = null;
        BotMetadata.prototype.inThreadSurveyMetadata = null;
        BotMetadata.prototype.botThreadInfo = null;
        BotMetadata.prototype.regenerateMetadata = null;
        BotMetadata.prototype.sessionTransparencyMetadata = null;
        BotMetadata.prototype.botDocumentMessageMetadata = null;
        BotMetadata.prototype.botGroupMetadata = null;
        BotMetadata.prototype.botRenderingConfigMetadata = null;
        BotMetadata.prototype.botInfrastructureDiagnostics = null;
        BotMetadata.prototype.aiMediaCollectionMetadata = null;
        BotMetadata.prototype.commandMetadata = null;
        BotMetadata.prototype.resolvedToolCallMetadata = null;
        BotMetadata.prototype.subscriptionUpsellMetadata = null;
        BotMetadata.prototype.pttPromptMetadata = null;
        BotMetadata.prototype.botHistoryShareMetadata = null;
        BotMetadata.prototype.internalMetadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_personaId", {
            get: $util.oneOfGetter($oneOfFields = ["personaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_pluginMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pluginMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_suggestedPromptMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["suggestedPromptMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_invokerJid", {
            get: $util.oneOfGetter($oneOfFields = ["invokerJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_sessionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sessionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_memuMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["memuMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_timezone", {
            get: $util.oneOfGetter($oneOfFields = ["timezone"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_reminderMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["reminderMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_modelMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["modelMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_messageDisclaimerText", {
            get: $util.oneOfGetter($oneOfFields = ["messageDisclaimerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_progressIndicatorMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["progressIndicatorMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_capabilityMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["capabilityMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_imagineMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["imagineMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_memoryMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["memoryMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_renderingMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["renderingMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botMetricsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMetricsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botLinkedAccountsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botLinkedAccountsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_richResponseSourcesMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["richResponseSourcesMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_aiConversationContext", {
            get: $util.oneOfGetter($oneOfFields = ["aiConversationContext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botPromotionMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botPromotionMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botModeSelectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botModeSelectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botQuotaMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botQuotaMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botAgeCollectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botAgeCollectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_conversationStarterPromptId", {
            get: $util.oneOfGetter($oneOfFields = ["conversationStarterPromptId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botResponseId", {
            get: $util.oneOfGetter($oneOfFields = ["botResponseId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_verificationMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["verificationMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_unifiedResponseMutation", {
            get: $util.oneOfGetter($oneOfFields = ["unifiedResponseMutation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botMessageOriginMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageOriginMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_inThreadSurveyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["inThreadSurveyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botThreadInfo", {
            get: $util.oneOfGetter($oneOfFields = ["botThreadInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_regenerateMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["regenerateMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_sessionTransparencyMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sessionTransparencyMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botDocumentMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botDocumentMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botGroupMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botGroupMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botRenderingConfigMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botRenderingConfigMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botInfrastructureDiagnostics", {
            get: $util.oneOfGetter($oneOfFields = ["botInfrastructureDiagnostics"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_aiMediaCollectionMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["aiMediaCollectionMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_commandMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["commandMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_resolvedToolCallMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["resolvedToolCallMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_subscriptionUpsellMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionUpsellMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_pttPromptMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pttPromptMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_botHistoryShareMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botHistoryShareMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetadata.prototype, "_internalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["internalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMetadata.create = function(properties) {
            return new BotMetadata(properties);
        };

        BotMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.personaId != null && $Object.hasOwnProperty.call(m, "personaId"))
                w.uint32(18).string(m.personaId);
            if (m.pluginMetadata != null && $Object.hasOwnProperty.call(m, "pluginMetadata"))
                $root.proto.BotPluginMetadata.encode(m.pluginMetadata, w.uint32(26).fork(), q + 1).ldelim();
            if (m.suggestedPromptMetadata != null && $Object.hasOwnProperty.call(m, "suggestedPromptMetadata"))
                $root.proto.BotSuggestedPromptMetadata.encode(m.suggestedPromptMetadata, w.uint32(34).fork(), q + 1).ldelim();
            if (m.invokerJid != null && $Object.hasOwnProperty.call(m, "invokerJid"))
                w.uint32(42).string(m.invokerJid);
            if (m.sessionMetadata != null && $Object.hasOwnProperty.call(m, "sessionMetadata"))
                $root.proto.BotSessionMetadata.encode(m.sessionMetadata, w.uint32(50).fork(), q + 1).ldelim();
            if (m.memuMetadata != null && $Object.hasOwnProperty.call(m, "memuMetadata"))
                $root.proto.BotMemuMetadata.encode(m.memuMetadata, w.uint32(58).fork(), q + 1).ldelim();
            if (m.timezone != null && $Object.hasOwnProperty.call(m, "timezone"))
                w.uint32(66).string(m.timezone);
            if (m.reminderMetadata != null && $Object.hasOwnProperty.call(m, "reminderMetadata"))
                $root.proto.BotReminderMetadata.encode(m.reminderMetadata, w.uint32(74).fork(), q + 1).ldelim();
            if (m.modelMetadata != null && $Object.hasOwnProperty.call(m, "modelMetadata"))
                $root.proto.BotModelMetadata.encode(m.modelMetadata, w.uint32(82).fork(), q + 1).ldelim();
            if (m.messageDisclaimerText != null && $Object.hasOwnProperty.call(m, "messageDisclaimerText"))
                w.uint32(90).string(m.messageDisclaimerText);
            if (m.progressIndicatorMetadata != null && $Object.hasOwnProperty.call(m, "progressIndicatorMetadata"))
                $root.proto.BotProgressIndicatorMetadata.encode(m.progressIndicatorMetadata, w.uint32(98).fork(), q + 1).ldelim();
            if (m.capabilityMetadata != null && $Object.hasOwnProperty.call(m, "capabilityMetadata"))
                $root.proto.BotCapabilityMetadata.encode(m.capabilityMetadata, w.uint32(106).fork(), q + 1).ldelim();
            if (m.imagineMetadata != null && $Object.hasOwnProperty.call(m, "imagineMetadata"))
                $root.proto.BotImagineMetadata.encode(m.imagineMetadata, w.uint32(114).fork(), q + 1).ldelim();
            if (m.memoryMetadata != null && $Object.hasOwnProperty.call(m, "memoryMetadata"))
                $root.proto.BotMemoryMetadata.encode(m.memoryMetadata, w.uint32(122).fork(), q + 1).ldelim();
            if (m.renderingMetadata != null && $Object.hasOwnProperty.call(m, "renderingMetadata"))
                $root.proto.BotRenderingMetadata.encode(m.renderingMetadata, w.uint32(130).fork(), q + 1).ldelim();
            if (m.botMetricsMetadata != null && $Object.hasOwnProperty.call(m, "botMetricsMetadata"))
                $root.proto.BotMetricsMetadata.encode(m.botMetricsMetadata, w.uint32(138).fork(), q + 1).ldelim();
            if (m.botLinkedAccountsMetadata != null && $Object.hasOwnProperty.call(m, "botLinkedAccountsMetadata"))
                $root.proto.BotLinkedAccountsMetadata.encode(m.botLinkedAccountsMetadata, w.uint32(146).fork(), q + 1).ldelim();
            if (m.richResponseSourcesMetadata != null && $Object.hasOwnProperty.call(m, "richResponseSourcesMetadata"))
                $root.proto.BotSourcesMetadata.encode(m.richResponseSourcesMetadata, w.uint32(154).fork(), q + 1).ldelim();
            if (m.aiConversationContext != null && $Object.hasOwnProperty.call(m, "aiConversationContext"))
                w.uint32(162).bytes(m.aiConversationContext);
            if (m.botPromotionMessageMetadata != null && $Object.hasOwnProperty.call(m, "botPromotionMessageMetadata"))
                $root.proto.BotPromotionMessageMetadata.encode(m.botPromotionMessageMetadata, w.uint32(170).fork(), q + 1).ldelim();
            if (m.botModeSelectionMetadata != null && $Object.hasOwnProperty.call(m, "botModeSelectionMetadata"))
                $root.proto.BotModeSelectionMetadata.encode(m.botModeSelectionMetadata, w.uint32(178).fork(), q + 1).ldelim();
            if (m.botQuotaMetadata != null && $Object.hasOwnProperty.call(m, "botQuotaMetadata"))
                $root.proto.BotQuotaMetadata.encode(m.botQuotaMetadata, w.uint32(186).fork(), q + 1).ldelim();
            if (m.botAgeCollectionMetadata != null && $Object.hasOwnProperty.call(m, "botAgeCollectionMetadata"))
                $root.proto.BotAgeCollectionMetadata.encode(m.botAgeCollectionMetadata, w.uint32(194).fork(), q + 1).ldelim();
            if (m.conversationStarterPromptId != null && $Object.hasOwnProperty.call(m, "conversationStarterPromptId"))
                w.uint32(202).string(m.conversationStarterPromptId);
            if (m.botResponseId != null && $Object.hasOwnProperty.call(m, "botResponseId"))
                w.uint32(210).string(m.botResponseId);
            if (m.verificationMetadata != null && $Object.hasOwnProperty.call(m, "verificationMetadata"))
                $root.proto.BotSignatureVerificationMetadata.encode(m.verificationMetadata, w.uint32(218).fork(), q + 1).ldelim();
            if (m.unifiedResponseMutation != null && $Object.hasOwnProperty.call(m, "unifiedResponseMutation"))
                $root.proto.BotUnifiedResponseMutation.encode(m.unifiedResponseMutation, w.uint32(226).fork(), q + 1).ldelim();
            if (m.botMessageOriginMetadata != null && $Object.hasOwnProperty.call(m, "botMessageOriginMetadata"))
                $root.proto.BotMessageOriginMetadata.encode(m.botMessageOriginMetadata, w.uint32(234).fork(), q + 1).ldelim();
            if (m.inThreadSurveyMetadata != null && $Object.hasOwnProperty.call(m, "inThreadSurveyMetadata"))
                $root.proto.InThreadSurveyMetadata.encode(m.inThreadSurveyMetadata, w.uint32(242).fork(), q + 1).ldelim();
            if (m.botThreadInfo != null && $Object.hasOwnProperty.call(m, "botThreadInfo"))
                $root.proto.AIThreadInfo.encode(m.botThreadInfo, w.uint32(250).fork(), q + 1).ldelim();
            if (m.regenerateMetadata != null && $Object.hasOwnProperty.call(m, "regenerateMetadata"))
                $root.proto.AIRegenerateMetadata.encode(m.regenerateMetadata, w.uint32(258).fork(), q + 1).ldelim();
            if (m.sessionTransparencyMetadata != null && $Object.hasOwnProperty.call(m, "sessionTransparencyMetadata"))
                $root.proto.SessionTransparencyMetadata.encode(m.sessionTransparencyMetadata, w.uint32(266).fork(), q + 1).ldelim();
            if (m.botDocumentMessageMetadata != null && $Object.hasOwnProperty.call(m, "botDocumentMessageMetadata"))
                $root.proto.BotDocumentMessageMetadata.encode(m.botDocumentMessageMetadata, w.uint32(274).fork(), q + 1).ldelim();
            if (m.botGroupMetadata != null && $Object.hasOwnProperty.call(m, "botGroupMetadata"))
                $root.proto.BotGroupMetadata.encode(m.botGroupMetadata, w.uint32(282).fork(), q + 1).ldelim();
            if (m.botRenderingConfigMetadata != null && $Object.hasOwnProperty.call(m, "botRenderingConfigMetadata"))
                $root.proto.BotRenderingConfigMetadata.encode(m.botRenderingConfigMetadata, w.uint32(290).fork(), q + 1).ldelim();
            if (m.botInfrastructureDiagnostics != null && $Object.hasOwnProperty.call(m, "botInfrastructureDiagnostics"))
                $root.proto.BotInfrastructureDiagnostics.encode(m.botInfrastructureDiagnostics, w.uint32(298).fork(), q + 1).ldelim();
            if (m.aiMediaCollectionMetadata != null && $Object.hasOwnProperty.call(m, "aiMediaCollectionMetadata"))
                $root.proto.AIMediaCollectionMetadata.encode(m.aiMediaCollectionMetadata, w.uint32(306).fork(), q + 1).ldelim();
            if (m.commandMetadata != null && $Object.hasOwnProperty.call(m, "commandMetadata"))
                $root.proto.BotCommandMetadata.encode(m.commandMetadata, w.uint32(314).fork(), q + 1).ldelim();
            if (m.resolvedToolCallMetadata != null && $Object.hasOwnProperty.call(m, "resolvedToolCallMetadata"))
                $root.proto.BotResolvedToolCallMetadata.encode(m.resolvedToolCallMetadata, w.uint32(322).fork(), q + 1).ldelim();
            if (m.subscriptionUpsellMetadata != null && $Object.hasOwnProperty.call(m, "subscriptionUpsellMetadata"))
                $root.proto.AISubscriptionUpsellMetadata.encode(m.subscriptionUpsellMetadata, w.uint32(330).fork(), q + 1).ldelim();
            if (m.pttPromptMetadata != null && $Object.hasOwnProperty.call(m, "pttPromptMetadata"))
                $root.proto.BotPttPromptMetadata.encode(m.pttPromptMetadata, w.uint32(338).fork(), q + 1).ldelim();
            if (m.botHistoryShareMetadata != null && $Object.hasOwnProperty.call(m, "botHistoryShareMetadata"))
                $root.proto.BotHistoryShareMetadata.encode(m.botHistoryShareMetadata, w.uint32(346).fork(), q + 1).ldelim();
            if (m.internalMetadata != null && $Object.hasOwnProperty.call(m, "internalMetadata"))
                w.uint32(7994).bytes(m.internalMetadata);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 2: {
                        if (u !== 2)
                            break;
                        m.personaId = r.stringVerify();
                        m._personaId = "personaId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.pluginMetadata = $root.proto.BotPluginMetadata.decode(r, r.uint32(), $undefined, q + 1, m.pluginMetadata);
                        m._pluginMetadata = "pluginMetadata";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.suggestedPromptMetadata = $root.proto.BotSuggestedPromptMetadata.decode(r, r.uint32(), $undefined, q + 1, m.suggestedPromptMetadata);
                        m._suggestedPromptMetadata = "suggestedPromptMetadata";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.invokerJid = r.stringVerify();
                        m._invokerJid = "invokerJid";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.sessionMetadata = $root.proto.BotSessionMetadata.decode(r, r.uint32(), $undefined, q + 1, m.sessionMetadata);
                        m._sessionMetadata = "sessionMetadata";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.memuMetadata = $root.proto.BotMemuMetadata.decode(r, r.uint32(), $undefined, q + 1, m.memuMetadata);
                        m._memuMetadata = "memuMetadata";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.timezone = r.stringVerify();
                        m._timezone = "timezone";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.reminderMetadata = $root.proto.BotReminderMetadata.decode(r, r.uint32(), $undefined, q + 1, m.reminderMetadata);
                        m._reminderMetadata = "reminderMetadata";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.modelMetadata = $root.proto.BotModelMetadata.decode(r, r.uint32(), $undefined, q + 1, m.modelMetadata);
                        m._modelMetadata = "modelMetadata";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.messageDisclaimerText = r.stringVerify();
                        m._messageDisclaimerText = "messageDisclaimerText";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.progressIndicatorMetadata = $root.proto.BotProgressIndicatorMetadata.decode(r, r.uint32(), $undefined, q + 1, m.progressIndicatorMetadata);
                        m._progressIndicatorMetadata = "progressIndicatorMetadata";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.capabilityMetadata = $root.proto.BotCapabilityMetadata.decode(r, r.uint32(), $undefined, q + 1, m.capabilityMetadata);
                        m._capabilityMetadata = "capabilityMetadata";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.imagineMetadata = $root.proto.BotImagineMetadata.decode(r, r.uint32(), $undefined, q + 1, m.imagineMetadata);
                        m._imagineMetadata = "imagineMetadata";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.memoryMetadata = $root.proto.BotMemoryMetadata.decode(r, r.uint32(), $undefined, q + 1, m.memoryMetadata);
                        m._memoryMetadata = "memoryMetadata";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        m.renderingMetadata = $root.proto.BotRenderingMetadata.decode(r, r.uint32(), $undefined, q + 1, m.renderingMetadata);
                        m._renderingMetadata = "renderingMetadata";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.botMetricsMetadata = $root.proto.BotMetricsMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botMetricsMetadata);
                        m._botMetricsMetadata = "botMetricsMetadata";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        m.botLinkedAccountsMetadata = $root.proto.BotLinkedAccountsMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botLinkedAccountsMetadata);
                        m._botLinkedAccountsMetadata = "botLinkedAccountsMetadata";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.richResponseSourcesMetadata = $root.proto.BotSourcesMetadata.decode(r, r.uint32(), $undefined, q + 1, m.richResponseSourcesMetadata);
                        m._richResponseSourcesMetadata = "richResponseSourcesMetadata";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        m.aiConversationContext = r.bytes();
                        m._aiConversationContext = "aiConversationContext";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        m.botPromotionMessageMetadata = $root.proto.BotPromotionMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botPromotionMessageMetadata);
                        m._botPromotionMessageMetadata = "botPromotionMessageMetadata";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        m.botModeSelectionMetadata = $root.proto.BotModeSelectionMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botModeSelectionMetadata);
                        m._botModeSelectionMetadata = "botModeSelectionMetadata";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        m.botQuotaMetadata = $root.proto.BotQuotaMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botQuotaMetadata);
                        m._botQuotaMetadata = "botQuotaMetadata";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        m.botAgeCollectionMetadata = $root.proto.BotAgeCollectionMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botAgeCollectionMetadata);
                        m._botAgeCollectionMetadata = "botAgeCollectionMetadata";
                        continue;
                    }
                case 25: {
                        if (u !== 2)
                            break;
                        m.conversationStarterPromptId = r.stringVerify();
                        m._conversationStarterPromptId = "conversationStarterPromptId";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        m.botResponseId = r.stringVerify();
                        m._botResponseId = "botResponseId";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        m.verificationMetadata = $root.proto.BotSignatureVerificationMetadata.decode(r, r.uint32(), $undefined, q + 1, m.verificationMetadata);
                        m._verificationMetadata = "verificationMetadata";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        m.unifiedResponseMutation = $root.proto.BotUnifiedResponseMutation.decode(r, r.uint32(), $undefined, q + 1, m.unifiedResponseMutation);
                        m._unifiedResponseMutation = "unifiedResponseMutation";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        m.botMessageOriginMetadata = $root.proto.BotMessageOriginMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botMessageOriginMetadata);
                        m._botMessageOriginMetadata = "botMessageOriginMetadata";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        m.inThreadSurveyMetadata = $root.proto.InThreadSurveyMetadata.decode(r, r.uint32(), $undefined, q + 1, m.inThreadSurveyMetadata);
                        m._inThreadSurveyMetadata = "inThreadSurveyMetadata";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        m.botThreadInfo = $root.proto.AIThreadInfo.decode(r, r.uint32(), $undefined, q + 1, m.botThreadInfo);
                        m._botThreadInfo = "botThreadInfo";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        m.regenerateMetadata = $root.proto.AIRegenerateMetadata.decode(r, r.uint32(), $undefined, q + 1, m.regenerateMetadata);
                        m._regenerateMetadata = "regenerateMetadata";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        m.sessionTransparencyMetadata = $root.proto.SessionTransparencyMetadata.decode(r, r.uint32(), $undefined, q + 1, m.sessionTransparencyMetadata);
                        m._sessionTransparencyMetadata = "sessionTransparencyMetadata";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        m.botDocumentMessageMetadata = $root.proto.BotDocumentMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botDocumentMessageMetadata);
                        m._botDocumentMessageMetadata = "botDocumentMessageMetadata";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        m.botGroupMetadata = $root.proto.BotGroupMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botGroupMetadata);
                        m._botGroupMetadata = "botGroupMetadata";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        m.botRenderingConfigMetadata = $root.proto.BotRenderingConfigMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botRenderingConfigMetadata);
                        m._botRenderingConfigMetadata = "botRenderingConfigMetadata";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        m.botInfrastructureDiagnostics = $root.proto.BotInfrastructureDiagnostics.decode(r, r.uint32(), $undefined, q + 1, m.botInfrastructureDiagnostics);
                        m._botInfrastructureDiagnostics = "botInfrastructureDiagnostics";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.aiMediaCollectionMetadata = $root.proto.AIMediaCollectionMetadata.decode(r, r.uint32(), $undefined, q + 1, m.aiMediaCollectionMetadata);
                        m._aiMediaCollectionMetadata = "aiMediaCollectionMetadata";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        m.commandMetadata = $root.proto.BotCommandMetadata.decode(r, r.uint32(), $undefined, q + 1, m.commandMetadata);
                        m._commandMetadata = "commandMetadata";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        m.resolvedToolCallMetadata = $root.proto.BotResolvedToolCallMetadata.decode(r, r.uint32(), $undefined, q + 1, m.resolvedToolCallMetadata);
                        m._resolvedToolCallMetadata = "resolvedToolCallMetadata";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        m.subscriptionUpsellMetadata = $root.proto.AISubscriptionUpsellMetadata.decode(r, r.uint32(), $undefined, q + 1, m.subscriptionUpsellMetadata);
                        m._subscriptionUpsellMetadata = "subscriptionUpsellMetadata";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        m.pttPromptMetadata = $root.proto.BotPttPromptMetadata.decode(r, r.uint32(), $undefined, q + 1, m.pttPromptMetadata);
                        m._pttPromptMetadata = "pttPromptMetadata";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.botHistoryShareMetadata = $root.proto.BotHistoryShareMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botHistoryShareMetadata);
                        m._botHistoryShareMetadata = "botHistoryShareMetadata";
                        continue;
                    }
                case 999: {
                        if (u !== 2)
                            break;
                        m.internalMetadata = r.bytes();
                        m._internalMetadata = "internalMetadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMetadata();
            if (d.personaId != null) {
                m.personaId = $String(d.personaId);
            }
            if (d.pluginMetadata != null) {
                if (!$util.isObject(d.pluginMetadata))
                    throw $TypeError(".proto.BotMetadata.pluginMetadata: object expected");
                m.pluginMetadata = $root.proto.BotPluginMetadata.fromObject(d.pluginMetadata, q + 1);
            }
            if (d.suggestedPromptMetadata != null) {
                if (!$util.isObject(d.suggestedPromptMetadata))
                    throw $TypeError(".proto.BotMetadata.suggestedPromptMetadata: object expected");
                m.suggestedPromptMetadata = $root.proto.BotSuggestedPromptMetadata.fromObject(d.suggestedPromptMetadata, q + 1);
            }
            if (d.invokerJid != null) {
                m.invokerJid = $String(d.invokerJid);
            }
            if (d.sessionMetadata != null) {
                if (!$util.isObject(d.sessionMetadata))
                    throw $TypeError(".proto.BotMetadata.sessionMetadata: object expected");
                m.sessionMetadata = $root.proto.BotSessionMetadata.fromObject(d.sessionMetadata, q + 1);
            }
            if (d.memuMetadata != null) {
                if (!$util.isObject(d.memuMetadata))
                    throw $TypeError(".proto.BotMetadata.memuMetadata: object expected");
                m.memuMetadata = $root.proto.BotMemuMetadata.fromObject(d.memuMetadata, q + 1);
            }
            if (d.timezone != null) {
                m.timezone = $String(d.timezone);
            }
            if (d.reminderMetadata != null) {
                if (!$util.isObject(d.reminderMetadata))
                    throw $TypeError(".proto.BotMetadata.reminderMetadata: object expected");
                m.reminderMetadata = $root.proto.BotReminderMetadata.fromObject(d.reminderMetadata, q + 1);
            }
            if (d.modelMetadata != null) {
                if (!$util.isObject(d.modelMetadata))
                    throw $TypeError(".proto.BotMetadata.modelMetadata: object expected");
                m.modelMetadata = $root.proto.BotModelMetadata.fromObject(d.modelMetadata, q + 1);
            }
            if (d.messageDisclaimerText != null) {
                m.messageDisclaimerText = $String(d.messageDisclaimerText);
            }
            if (d.progressIndicatorMetadata != null) {
                if (!$util.isObject(d.progressIndicatorMetadata))
                    throw $TypeError(".proto.BotMetadata.progressIndicatorMetadata: object expected");
                m.progressIndicatorMetadata = $root.proto.BotProgressIndicatorMetadata.fromObject(d.progressIndicatorMetadata, q + 1);
            }
            if (d.capabilityMetadata != null) {
                if (!$util.isObject(d.capabilityMetadata))
                    throw $TypeError(".proto.BotMetadata.capabilityMetadata: object expected");
                m.capabilityMetadata = $root.proto.BotCapabilityMetadata.fromObject(d.capabilityMetadata, q + 1);
            }
            if (d.imagineMetadata != null) {
                if (!$util.isObject(d.imagineMetadata))
                    throw $TypeError(".proto.BotMetadata.imagineMetadata: object expected");
                m.imagineMetadata = $root.proto.BotImagineMetadata.fromObject(d.imagineMetadata, q + 1);
            }
            if (d.memoryMetadata != null) {
                if (!$util.isObject(d.memoryMetadata))
                    throw $TypeError(".proto.BotMetadata.memoryMetadata: object expected");
                m.memoryMetadata = $root.proto.BotMemoryMetadata.fromObject(d.memoryMetadata, q + 1);
            }
            if (d.renderingMetadata != null) {
                if (!$util.isObject(d.renderingMetadata))
                    throw $TypeError(".proto.BotMetadata.renderingMetadata: object expected");
                m.renderingMetadata = $root.proto.BotRenderingMetadata.fromObject(d.renderingMetadata, q + 1);
            }
            if (d.botMetricsMetadata != null) {
                if (!$util.isObject(d.botMetricsMetadata))
                    throw $TypeError(".proto.BotMetadata.botMetricsMetadata: object expected");
                m.botMetricsMetadata = $root.proto.BotMetricsMetadata.fromObject(d.botMetricsMetadata, q + 1);
            }
            if (d.botLinkedAccountsMetadata != null) {
                if (!$util.isObject(d.botLinkedAccountsMetadata))
                    throw $TypeError(".proto.BotMetadata.botLinkedAccountsMetadata: object expected");
                m.botLinkedAccountsMetadata = $root.proto.BotLinkedAccountsMetadata.fromObject(d.botLinkedAccountsMetadata, q + 1);
            }
            if (d.richResponseSourcesMetadata != null) {
                if (!$util.isObject(d.richResponseSourcesMetadata))
                    throw $TypeError(".proto.BotMetadata.richResponseSourcesMetadata: object expected");
                m.richResponseSourcesMetadata = $root.proto.BotSourcesMetadata.fromObject(d.richResponseSourcesMetadata, q + 1);
            }
            if (d.aiConversationContext != null) {
                if (typeof d.aiConversationContext === "string")
                    $util.base64.decode(d.aiConversationContext, m.aiConversationContext = $util.newBuffer($util.base64.length(d.aiConversationContext)), 0);
                else if (d.aiConversationContext.length >= 0)
                    m.aiConversationContext = d.aiConversationContext;
            }
            if (d.botPromotionMessageMetadata != null) {
                if (!$util.isObject(d.botPromotionMessageMetadata))
                    throw $TypeError(".proto.BotMetadata.botPromotionMessageMetadata: object expected");
                m.botPromotionMessageMetadata = $root.proto.BotPromotionMessageMetadata.fromObject(d.botPromotionMessageMetadata, q + 1);
            }
            if (d.botModeSelectionMetadata != null) {
                if (!$util.isObject(d.botModeSelectionMetadata))
                    throw $TypeError(".proto.BotMetadata.botModeSelectionMetadata: object expected");
                m.botModeSelectionMetadata = $root.proto.BotModeSelectionMetadata.fromObject(d.botModeSelectionMetadata, q + 1);
            }
            if (d.botQuotaMetadata != null) {
                if (!$util.isObject(d.botQuotaMetadata))
                    throw $TypeError(".proto.BotMetadata.botQuotaMetadata: object expected");
                m.botQuotaMetadata = $root.proto.BotQuotaMetadata.fromObject(d.botQuotaMetadata, q + 1);
            }
            if (d.botAgeCollectionMetadata != null) {
                if (!$util.isObject(d.botAgeCollectionMetadata))
                    throw $TypeError(".proto.BotMetadata.botAgeCollectionMetadata: object expected");
                m.botAgeCollectionMetadata = $root.proto.BotAgeCollectionMetadata.fromObject(d.botAgeCollectionMetadata, q + 1);
            }
            if (d.conversationStarterPromptId != null) {
                m.conversationStarterPromptId = $String(d.conversationStarterPromptId);
            }
            if (d.botResponseId != null) {
                m.botResponseId = $String(d.botResponseId);
            }
            if (d.verificationMetadata != null) {
                if (!$util.isObject(d.verificationMetadata))
                    throw $TypeError(".proto.BotMetadata.verificationMetadata: object expected");
                m.verificationMetadata = $root.proto.BotSignatureVerificationMetadata.fromObject(d.verificationMetadata, q + 1);
            }
            if (d.unifiedResponseMutation != null) {
                if (!$util.isObject(d.unifiedResponseMutation))
                    throw $TypeError(".proto.BotMetadata.unifiedResponseMutation: object expected");
                m.unifiedResponseMutation = $root.proto.BotUnifiedResponseMutation.fromObject(d.unifiedResponseMutation, q + 1);
            }
            if (d.botMessageOriginMetadata != null) {
                if (!$util.isObject(d.botMessageOriginMetadata))
                    throw $TypeError(".proto.BotMetadata.botMessageOriginMetadata: object expected");
                m.botMessageOriginMetadata = $root.proto.BotMessageOriginMetadata.fromObject(d.botMessageOriginMetadata, q + 1);
            }
            if (d.inThreadSurveyMetadata != null) {
                if (!$util.isObject(d.inThreadSurveyMetadata))
                    throw $TypeError(".proto.BotMetadata.inThreadSurveyMetadata: object expected");
                m.inThreadSurveyMetadata = $root.proto.InThreadSurveyMetadata.fromObject(d.inThreadSurveyMetadata, q + 1);
            }
            if (d.botThreadInfo != null) {
                if (!$util.isObject(d.botThreadInfo))
                    throw $TypeError(".proto.BotMetadata.botThreadInfo: object expected");
                m.botThreadInfo = $root.proto.AIThreadInfo.fromObject(d.botThreadInfo, q + 1);
            }
            if (d.regenerateMetadata != null) {
                if (!$util.isObject(d.regenerateMetadata))
                    throw $TypeError(".proto.BotMetadata.regenerateMetadata: object expected");
                m.regenerateMetadata = $root.proto.AIRegenerateMetadata.fromObject(d.regenerateMetadata, q + 1);
            }
            if (d.sessionTransparencyMetadata != null) {
                if (!$util.isObject(d.sessionTransparencyMetadata))
                    throw $TypeError(".proto.BotMetadata.sessionTransparencyMetadata: object expected");
                m.sessionTransparencyMetadata = $root.proto.SessionTransparencyMetadata.fromObject(d.sessionTransparencyMetadata, q + 1);
            }
            if (d.botDocumentMessageMetadata != null) {
                if (!$util.isObject(d.botDocumentMessageMetadata))
                    throw $TypeError(".proto.BotMetadata.botDocumentMessageMetadata: object expected");
                m.botDocumentMessageMetadata = $root.proto.BotDocumentMessageMetadata.fromObject(d.botDocumentMessageMetadata, q + 1);
            }
            if (d.botGroupMetadata != null) {
                if (!$util.isObject(d.botGroupMetadata))
                    throw $TypeError(".proto.BotMetadata.botGroupMetadata: object expected");
                m.botGroupMetadata = $root.proto.BotGroupMetadata.fromObject(d.botGroupMetadata, q + 1);
            }
            if (d.botRenderingConfigMetadata != null) {
                if (!$util.isObject(d.botRenderingConfigMetadata))
                    throw $TypeError(".proto.BotMetadata.botRenderingConfigMetadata: object expected");
                m.botRenderingConfigMetadata = $root.proto.BotRenderingConfigMetadata.fromObject(d.botRenderingConfigMetadata, q + 1);
            }
            if (d.botInfrastructureDiagnostics != null) {
                if (!$util.isObject(d.botInfrastructureDiagnostics))
                    throw $TypeError(".proto.BotMetadata.botInfrastructureDiagnostics: object expected");
                m.botInfrastructureDiagnostics = $root.proto.BotInfrastructureDiagnostics.fromObject(d.botInfrastructureDiagnostics, q + 1);
            }
            if (d.aiMediaCollectionMetadata != null) {
                if (!$util.isObject(d.aiMediaCollectionMetadata))
                    throw $TypeError(".proto.BotMetadata.aiMediaCollectionMetadata: object expected");
                m.aiMediaCollectionMetadata = $root.proto.AIMediaCollectionMetadata.fromObject(d.aiMediaCollectionMetadata, q + 1);
            }
            if (d.commandMetadata != null) {
                if (!$util.isObject(d.commandMetadata))
                    throw $TypeError(".proto.BotMetadata.commandMetadata: object expected");
                m.commandMetadata = $root.proto.BotCommandMetadata.fromObject(d.commandMetadata, q + 1);
            }
            if (d.resolvedToolCallMetadata != null) {
                if (!$util.isObject(d.resolvedToolCallMetadata))
                    throw $TypeError(".proto.BotMetadata.resolvedToolCallMetadata: object expected");
                m.resolvedToolCallMetadata = $root.proto.BotResolvedToolCallMetadata.fromObject(d.resolvedToolCallMetadata, q + 1);
            }
            if (d.subscriptionUpsellMetadata != null) {
                if (!$util.isObject(d.subscriptionUpsellMetadata))
                    throw $TypeError(".proto.BotMetadata.subscriptionUpsellMetadata: object expected");
                m.subscriptionUpsellMetadata = $root.proto.AISubscriptionUpsellMetadata.fromObject(d.subscriptionUpsellMetadata, q + 1);
            }
            if (d.pttPromptMetadata != null) {
                if (!$util.isObject(d.pttPromptMetadata))
                    throw $TypeError(".proto.BotMetadata.pttPromptMetadata: object expected");
                m.pttPromptMetadata = $root.proto.BotPttPromptMetadata.fromObject(d.pttPromptMetadata, q + 1);
            }
            if (d.botHistoryShareMetadata != null) {
                if (!$util.isObject(d.botHistoryShareMetadata))
                    throw $TypeError(".proto.BotMetadata.botHistoryShareMetadata: object expected");
                m.botHistoryShareMetadata = $root.proto.BotHistoryShareMetadata.fromObject(d.botHistoryShareMetadata, q + 1);
            }
            if (d.internalMetadata != null) {
                if (typeof d.internalMetadata === "string")
                    $util.base64.decode(d.internalMetadata, m.internalMetadata = $util.newBuffer($util.base64.length(d.internalMetadata)), 0);
                else if (d.internalMetadata.length >= 0)
                    m.internalMetadata = d.internalMetadata;
            }
            return m;
        };

        BotMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.personaId != null && $Object.hasOwnProperty.call(m, "personaId")) {
                d.personaId = m.personaId;
            }
            if (m.pluginMetadata != null && $Object.hasOwnProperty.call(m, "pluginMetadata")) {
                d.pluginMetadata = $root.proto.BotPluginMetadata.toObject(m.pluginMetadata, o, q + 1);
            }
            if (m.suggestedPromptMetadata != null && $Object.hasOwnProperty.call(m, "suggestedPromptMetadata")) {
                d.suggestedPromptMetadata = $root.proto.BotSuggestedPromptMetadata.toObject(m.suggestedPromptMetadata, o, q + 1);
            }
            if (m.invokerJid != null && $Object.hasOwnProperty.call(m, "invokerJid")) {
                d.invokerJid = m.invokerJid;
            }
            if (m.sessionMetadata != null && $Object.hasOwnProperty.call(m, "sessionMetadata")) {
                d.sessionMetadata = $root.proto.BotSessionMetadata.toObject(m.sessionMetadata, o, q + 1);
            }
            if (m.memuMetadata != null && $Object.hasOwnProperty.call(m, "memuMetadata")) {
                d.memuMetadata = $root.proto.BotMemuMetadata.toObject(m.memuMetadata, o, q + 1);
            }
            if (m.timezone != null && $Object.hasOwnProperty.call(m, "timezone")) {
                d.timezone = m.timezone;
            }
            if (m.reminderMetadata != null && $Object.hasOwnProperty.call(m, "reminderMetadata")) {
                d.reminderMetadata = $root.proto.BotReminderMetadata.toObject(m.reminderMetadata, o, q + 1);
            }
            if (m.modelMetadata != null && $Object.hasOwnProperty.call(m, "modelMetadata")) {
                d.modelMetadata = $root.proto.BotModelMetadata.toObject(m.modelMetadata, o, q + 1);
            }
            if (m.messageDisclaimerText != null && $Object.hasOwnProperty.call(m, "messageDisclaimerText")) {
                d.messageDisclaimerText = m.messageDisclaimerText;
            }
            if (m.progressIndicatorMetadata != null && $Object.hasOwnProperty.call(m, "progressIndicatorMetadata")) {
                d.progressIndicatorMetadata = $root.proto.BotProgressIndicatorMetadata.toObject(m.progressIndicatorMetadata, o, q + 1);
            }
            if (m.capabilityMetadata != null && $Object.hasOwnProperty.call(m, "capabilityMetadata")) {
                d.capabilityMetadata = $root.proto.BotCapabilityMetadata.toObject(m.capabilityMetadata, o, q + 1);
            }
            if (m.imagineMetadata != null && $Object.hasOwnProperty.call(m, "imagineMetadata")) {
                d.imagineMetadata = $root.proto.BotImagineMetadata.toObject(m.imagineMetadata, o, q + 1);
            }
            if (m.memoryMetadata != null && $Object.hasOwnProperty.call(m, "memoryMetadata")) {
                d.memoryMetadata = $root.proto.BotMemoryMetadata.toObject(m.memoryMetadata, o, q + 1);
            }
            if (m.renderingMetadata != null && $Object.hasOwnProperty.call(m, "renderingMetadata")) {
                d.renderingMetadata = $root.proto.BotRenderingMetadata.toObject(m.renderingMetadata, o, q + 1);
            }
            if (m.botMetricsMetadata != null && $Object.hasOwnProperty.call(m, "botMetricsMetadata")) {
                d.botMetricsMetadata = $root.proto.BotMetricsMetadata.toObject(m.botMetricsMetadata, o, q + 1);
            }
            if (m.botLinkedAccountsMetadata != null && $Object.hasOwnProperty.call(m, "botLinkedAccountsMetadata")) {
                d.botLinkedAccountsMetadata = $root.proto.BotLinkedAccountsMetadata.toObject(m.botLinkedAccountsMetadata, o, q + 1);
            }
            if (m.richResponseSourcesMetadata != null && $Object.hasOwnProperty.call(m, "richResponseSourcesMetadata")) {
                d.richResponseSourcesMetadata = $root.proto.BotSourcesMetadata.toObject(m.richResponseSourcesMetadata, o, q + 1);
            }
            if (m.aiConversationContext != null && $Object.hasOwnProperty.call(m, "aiConversationContext")) {
                d.aiConversationContext = o.bytes === $String ? $util.base64.encode(m.aiConversationContext, 0, m.aiConversationContext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.aiConversationContext) : m.aiConversationContext;
            }
            if (m.botPromotionMessageMetadata != null && $Object.hasOwnProperty.call(m, "botPromotionMessageMetadata")) {
                d.botPromotionMessageMetadata = $root.proto.BotPromotionMessageMetadata.toObject(m.botPromotionMessageMetadata, o, q + 1);
            }
            if (m.botModeSelectionMetadata != null && $Object.hasOwnProperty.call(m, "botModeSelectionMetadata")) {
                d.botModeSelectionMetadata = $root.proto.BotModeSelectionMetadata.toObject(m.botModeSelectionMetadata, o, q + 1);
            }
            if (m.botQuotaMetadata != null && $Object.hasOwnProperty.call(m, "botQuotaMetadata")) {
                d.botQuotaMetadata = $root.proto.BotQuotaMetadata.toObject(m.botQuotaMetadata, o, q + 1);
            }
            if (m.botAgeCollectionMetadata != null && $Object.hasOwnProperty.call(m, "botAgeCollectionMetadata")) {
                d.botAgeCollectionMetadata = $root.proto.BotAgeCollectionMetadata.toObject(m.botAgeCollectionMetadata, o, q + 1);
            }
            if (m.conversationStarterPromptId != null && $Object.hasOwnProperty.call(m, "conversationStarterPromptId")) {
                d.conversationStarterPromptId = m.conversationStarterPromptId;
            }
            if (m.botResponseId != null && $Object.hasOwnProperty.call(m, "botResponseId")) {
                d.botResponseId = m.botResponseId;
            }
            if (m.verificationMetadata != null && $Object.hasOwnProperty.call(m, "verificationMetadata")) {
                d.verificationMetadata = $root.proto.BotSignatureVerificationMetadata.toObject(m.verificationMetadata, o, q + 1);
            }
            if (m.unifiedResponseMutation != null && $Object.hasOwnProperty.call(m, "unifiedResponseMutation")) {
                d.unifiedResponseMutation = $root.proto.BotUnifiedResponseMutation.toObject(m.unifiedResponseMutation, o, q + 1);
            }
            if (m.botMessageOriginMetadata != null && $Object.hasOwnProperty.call(m, "botMessageOriginMetadata")) {
                d.botMessageOriginMetadata = $root.proto.BotMessageOriginMetadata.toObject(m.botMessageOriginMetadata, o, q + 1);
            }
            if (m.inThreadSurveyMetadata != null && $Object.hasOwnProperty.call(m, "inThreadSurveyMetadata")) {
                d.inThreadSurveyMetadata = $root.proto.InThreadSurveyMetadata.toObject(m.inThreadSurveyMetadata, o, q + 1);
            }
            if (m.botThreadInfo != null && $Object.hasOwnProperty.call(m, "botThreadInfo")) {
                d.botThreadInfo = $root.proto.AIThreadInfo.toObject(m.botThreadInfo, o, q + 1);
            }
            if (m.regenerateMetadata != null && $Object.hasOwnProperty.call(m, "regenerateMetadata")) {
                d.regenerateMetadata = $root.proto.AIRegenerateMetadata.toObject(m.regenerateMetadata, o, q + 1);
            }
            if (m.sessionTransparencyMetadata != null && $Object.hasOwnProperty.call(m, "sessionTransparencyMetadata")) {
                d.sessionTransparencyMetadata = $root.proto.SessionTransparencyMetadata.toObject(m.sessionTransparencyMetadata, o, q + 1);
            }
            if (m.botDocumentMessageMetadata != null && $Object.hasOwnProperty.call(m, "botDocumentMessageMetadata")) {
                d.botDocumentMessageMetadata = $root.proto.BotDocumentMessageMetadata.toObject(m.botDocumentMessageMetadata, o, q + 1);
            }
            if (m.botGroupMetadata != null && $Object.hasOwnProperty.call(m, "botGroupMetadata")) {
                d.botGroupMetadata = $root.proto.BotGroupMetadata.toObject(m.botGroupMetadata, o, q + 1);
            }
            if (m.botRenderingConfigMetadata != null && $Object.hasOwnProperty.call(m, "botRenderingConfigMetadata")) {
                d.botRenderingConfigMetadata = $root.proto.BotRenderingConfigMetadata.toObject(m.botRenderingConfigMetadata, o, q + 1);
            }
            if (m.botInfrastructureDiagnostics != null && $Object.hasOwnProperty.call(m, "botInfrastructureDiagnostics")) {
                d.botInfrastructureDiagnostics = $root.proto.BotInfrastructureDiagnostics.toObject(m.botInfrastructureDiagnostics, o, q + 1);
            }
            if (m.aiMediaCollectionMetadata != null && $Object.hasOwnProperty.call(m, "aiMediaCollectionMetadata")) {
                d.aiMediaCollectionMetadata = $root.proto.AIMediaCollectionMetadata.toObject(m.aiMediaCollectionMetadata, o, q + 1);
            }
            if (m.commandMetadata != null && $Object.hasOwnProperty.call(m, "commandMetadata")) {
                d.commandMetadata = $root.proto.BotCommandMetadata.toObject(m.commandMetadata, o, q + 1);
            }
            if (m.resolvedToolCallMetadata != null && $Object.hasOwnProperty.call(m, "resolvedToolCallMetadata")) {
                d.resolvedToolCallMetadata = $root.proto.BotResolvedToolCallMetadata.toObject(m.resolvedToolCallMetadata, o, q + 1);
            }
            if (m.subscriptionUpsellMetadata != null && $Object.hasOwnProperty.call(m, "subscriptionUpsellMetadata")) {
                d.subscriptionUpsellMetadata = $root.proto.AISubscriptionUpsellMetadata.toObject(m.subscriptionUpsellMetadata, o, q + 1);
            }
            if (m.pttPromptMetadata != null && $Object.hasOwnProperty.call(m, "pttPromptMetadata")) {
                d.pttPromptMetadata = $root.proto.BotPttPromptMetadata.toObject(m.pttPromptMetadata, o, q + 1);
            }
            if (m.botHistoryShareMetadata != null && $Object.hasOwnProperty.call(m, "botHistoryShareMetadata")) {
                d.botHistoryShareMetadata = $root.proto.BotHistoryShareMetadata.toObject(m.botHistoryShareMetadata, o, q + 1);
            }
            if (m.internalMetadata != null && $Object.hasOwnProperty.call(m, "internalMetadata")) {
                d.internalMetadata = o.bytes === $String ? $util.base64.encode(m.internalMetadata, 0, m.internalMetadata.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.internalMetadata) : m.internalMetadata;
            }
            return d;
        };

        BotMetadata.prototype.toJSON = function() {
            return BotMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMetadata";
        };

        return BotMetadata;
    })();

    proto.BotMetricsEntryPoint = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "UNDEFINED_ENTRY_POINT"] = 0;
        values[valuesById[1] = "FAVICON"] = 1;
        values[valuesById[2] = "CHATLIST"] = 2;
        values[valuesById[3] = "AISEARCH_NULL_STATE_PAPER_PLANE"] = 3;
        values[valuesById[4] = "AISEARCH_NULL_STATE_SUGGESTION"] = 4;
        values[valuesById[5] = "AISEARCH_TYPE_AHEAD_SUGGESTION"] = 5;
        values[valuesById[6] = "AISEARCH_TYPE_AHEAD_PAPER_PLANE"] = 6;
        values[valuesById[7] = "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST"] = 7;
        values[valuesById[8] = "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES"] = 8;
        values[valuesById[9] = "AIVOICE_SEARCH_BAR"] = 9;
        values[valuesById[10] = "AIVOICE_FAVICON"] = 10;
        values[valuesById[11] = "AISTUDIO"] = 11;
        values[valuesById[12] = "DEEPLINK"] = 12;
        values[valuesById[13] = "NOTIFICATION"] = 13;
        values[valuesById[14] = "PROFILE_MESSAGE_BUTTON"] = 14;
        values[valuesById[15] = "FORWARD"] = 15;
        values[valuesById[16] = "APP_SHORTCUT"] = 16;
        values[valuesById[17] = "FF_FAMILY"] = 17;
        values[valuesById[18] = "AI_TAB"] = 18;
        values[valuesById[19] = "AI_HOME"] = 19;
        values[valuesById[20] = "AI_DEEPLINK_IMMERSIVE"] = 20;
        values[valuesById[21] = "AI_DEEPLINK"] = 21;
        values[valuesById[22] = "META_AI_CHAT_SHORTCUT_AI_STUDIO"] = 22;
        values[valuesById[23] = "UGC_CHAT_SHORTCUT_AI_STUDIO"] = 23;
        values[valuesById[24] = "NEW_CHAT_AI_STUDIO"] = 24;
        values[valuesById[25] = "AIVOICE_FAVICON_CALL_HISTORY"] = 25;
        values[valuesById[26] = "ASK_META_AI_CONTEXT_MENU"] = 26;
        values[valuesById[27] = "ASK_META_AI_CONTEXT_MENU_1ON1"] = 27;
        values[valuesById[28] = "ASK_META_AI_CONTEXT_MENU_GROUP"] = 28;
        values[valuesById[29] = "INVOKE_META_AI_1ON1"] = 29;
        values[valuesById[30] = "INVOKE_META_AI_GROUP"] = 30;
        values[valuesById[31] = "META_AI_FORWARD"] = 31;
        values[valuesById[32] = "NEW_CHAT_AI_CONTACT"] = 32;
        values[valuesById[33] = "MESSAGE_QUICK_ACTION_1_ON_1_CHAT"] = 33;
        values[valuesById[34] = "MESSAGE_QUICK_ACTION_GROUP_CHAT"] = 34;
        values[valuesById[35] = "ATTACHMENT_TRAY_1_ON_1_CHAT"] = 35;
        values[valuesById[36] = "ATTACHMENT_TRAY_GROUP_CHAT"] = 36;
        values[valuesById[37] = "ASK_META_AI_MEDIA_VIEWER_1ON1"] = 37;
        values[valuesById[38] = "ASK_META_AI_MEDIA_VIEWER_GROUP"] = 38;
        values[valuesById[39] = "MEDIA_PICKER_1_ON_1_CHAT"] = 39;
        values[valuesById[40] = "MEDIA_PICKER_GROUP_CHAT"] = 40;
        values[valuesById[41] = "ASK_META_AI_NO_SEARCH_RESULTS"] = 41;
        values[valuesById[45] = "META_AI_SETTINGS"] = 45;
        values[valuesById[46] = "WEB_INTRO_PANEL"] = 46;
        values[valuesById[47] = "WEB_NAVIGATION_BAR"] = 47;
        values[valuesById[54] = "GROUP_MEMBER"] = 54;
        values[valuesById[55] = "CHATLIST_SEARCH"] = 55;
        values[valuesById[56] = "NEW_CHAT_LIST"] = 56;
        values[valuesById[57] = "CONTACTS_TAB"] = 57;
        return values;
    })();

    proto.BotMetricsMetadata = (function() {

        const BotMetricsMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotMetricsMetadata.prototype.destinationId = null;
        BotMetricsMetadata.prototype.destinationEntryPoint = null;
        BotMetricsMetadata.prototype.threadOrigin = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetricsMetadata.prototype, "_destinationId", {
            get: $util.oneOfGetter($oneOfFields = ["destinationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetricsMetadata.prototype, "_destinationEntryPoint", {
            get: $util.oneOfGetter($oneOfFields = ["destinationEntryPoint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotMetricsMetadata.prototype, "_threadOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["threadOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotMetricsMetadata.create = function(properties) {
            return new BotMetricsMetadata(properties);
        };

        BotMetricsMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.destinationId != null && $Object.hasOwnProperty.call(m, "destinationId"))
                w.uint32(10).string(m.destinationId);
            if (m.destinationEntryPoint != null && $Object.hasOwnProperty.call(m, "destinationEntryPoint"))
                w.uint32(16).int32(m.destinationEntryPoint);
            if (m.threadOrigin != null && $Object.hasOwnProperty.call(m, "threadOrigin"))
                w.uint32(24).int32(m.threadOrigin);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotMetricsMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotMetricsMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.destinationId = r.stringVerify();
                        m._destinationId = "destinationId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.destinationEntryPoint = r.int32();
                        m._destinationEntryPoint = "destinationEntryPoint";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.threadOrigin = r.int32();
                        m._threadOrigin = "threadOrigin";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotMetricsMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotMetricsMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotMetricsMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotMetricsMetadata();
            if (d.destinationId != null) {
                m.destinationId = $String(d.destinationId);
            }
            switch (d.destinationEntryPoint) {
            case "UNDEFINED_ENTRY_POINT":
            case 0:
                m.destinationEntryPoint = 0;
                break;
            case "FAVICON":
            case 1:
                m.destinationEntryPoint = 1;
                break;
            case "CHATLIST":
            case 2:
                m.destinationEntryPoint = 2;
                break;
            case "AISEARCH_NULL_STATE_PAPER_PLANE":
            case 3:
                m.destinationEntryPoint = 3;
                break;
            case "AISEARCH_NULL_STATE_SUGGESTION":
            case 4:
                m.destinationEntryPoint = 4;
                break;
            case "AISEARCH_TYPE_AHEAD_SUGGESTION":
            case 5:
                m.destinationEntryPoint = 5;
                break;
            case "AISEARCH_TYPE_AHEAD_PAPER_PLANE":
            case 6:
                m.destinationEntryPoint = 6;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_CHATLIST":
            case 7:
                m.destinationEntryPoint = 7;
                break;
            case "AISEARCH_TYPE_AHEAD_RESULT_MESSAGES":
            case 8:
                m.destinationEntryPoint = 8;
                break;
            case "AIVOICE_SEARCH_BAR":
            case 9:
                m.destinationEntryPoint = 9;
                break;
            case "AIVOICE_FAVICON":
            case 10:
                m.destinationEntryPoint = 10;
                break;
            case "AISTUDIO":
            case 11:
                m.destinationEntryPoint = 11;
                break;
            case "DEEPLINK":
            case 12:
                m.destinationEntryPoint = 12;
                break;
            case "NOTIFICATION":
            case 13:
                m.destinationEntryPoint = 13;
                break;
            case "PROFILE_MESSAGE_BUTTON":
            case 14:
                m.destinationEntryPoint = 14;
                break;
            case "FORWARD":
            case 15:
                m.destinationEntryPoint = 15;
                break;
            case "APP_SHORTCUT":
            case 16:
                m.destinationEntryPoint = 16;
                break;
            case "FF_FAMILY":
            case 17:
                m.destinationEntryPoint = 17;
                break;
            case "AI_TAB":
            case 18:
                m.destinationEntryPoint = 18;
                break;
            case "AI_HOME":
            case 19:
                m.destinationEntryPoint = 19;
                break;
            case "AI_DEEPLINK_IMMERSIVE":
            case 20:
                m.destinationEntryPoint = 20;
                break;
            case "AI_DEEPLINK":
            case 21:
                m.destinationEntryPoint = 21;
                break;
            case "META_AI_CHAT_SHORTCUT_AI_STUDIO":
            case 22:
                m.destinationEntryPoint = 22;
                break;
            case "UGC_CHAT_SHORTCUT_AI_STUDIO":
            case 23:
                m.destinationEntryPoint = 23;
                break;
            case "NEW_CHAT_AI_STUDIO":
            case 24:
                m.destinationEntryPoint = 24;
                break;
            case "AIVOICE_FAVICON_CALL_HISTORY":
            case 25:
                m.destinationEntryPoint = 25;
                break;
            case "ASK_META_AI_CONTEXT_MENU":
            case 26:
                m.destinationEntryPoint = 26;
                break;
            case "ASK_META_AI_CONTEXT_MENU_1ON1":
            case 27:
                m.destinationEntryPoint = 27;
                break;
            case "ASK_META_AI_CONTEXT_MENU_GROUP":
            case 28:
                m.destinationEntryPoint = 28;
                break;
            case "INVOKE_META_AI_1ON1":
            case 29:
                m.destinationEntryPoint = 29;
                break;
            case "INVOKE_META_AI_GROUP":
            case 30:
                m.destinationEntryPoint = 30;
                break;
            case "META_AI_FORWARD":
            case 31:
                m.destinationEntryPoint = 31;
                break;
            case "NEW_CHAT_AI_CONTACT":
            case 32:
                m.destinationEntryPoint = 32;
                break;
            case "MESSAGE_QUICK_ACTION_1_ON_1_CHAT":
            case 33:
                m.destinationEntryPoint = 33;
                break;
            case "MESSAGE_QUICK_ACTION_GROUP_CHAT":
            case 34:
                m.destinationEntryPoint = 34;
                break;
            case "ATTACHMENT_TRAY_1_ON_1_CHAT":
            case 35:
                m.destinationEntryPoint = 35;
                break;
            case "ATTACHMENT_TRAY_GROUP_CHAT":
            case 36:
                m.destinationEntryPoint = 36;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_1ON1":
            case 37:
                m.destinationEntryPoint = 37;
                break;
            case "ASK_META_AI_MEDIA_VIEWER_GROUP":
            case 38:
                m.destinationEntryPoint = 38;
                break;
            case "MEDIA_PICKER_1_ON_1_CHAT":
            case 39:
                m.destinationEntryPoint = 39;
                break;
            case "MEDIA_PICKER_GROUP_CHAT":
            case 40:
                m.destinationEntryPoint = 40;
                break;
            case "ASK_META_AI_NO_SEARCH_RESULTS":
            case 41:
                m.destinationEntryPoint = 41;
                break;
            case "META_AI_SETTINGS":
            case 45:
                m.destinationEntryPoint = 45;
                break;
            case "WEB_INTRO_PANEL":
            case 46:
                m.destinationEntryPoint = 46;
                break;
            case "WEB_NAVIGATION_BAR":
            case 47:
                m.destinationEntryPoint = 47;
                break;
            case "GROUP_MEMBER":
            case 54:
                m.destinationEntryPoint = 54;
                break;
            case "CHATLIST_SEARCH":
            case 55:
                m.destinationEntryPoint = 55;
                break;
            case "NEW_CHAT_LIST":
            case 56:
                m.destinationEntryPoint = 56;
                break;
            case "CONTACTS_TAB":
            case 57:
                m.destinationEntryPoint = 57;
                break;
            default:
                if (typeof d.destinationEntryPoint === "number" && (d.destinationEntryPoint | 0) === d.destinationEntryPoint)
                    m.destinationEntryPoint = d.destinationEntryPoint;
            }
            switch (d.threadOrigin) {
            case "AI_TAB_THREAD":
            case 1:
                m.threadOrigin = 1;
                break;
            case "AI_HOME_THREAD":
            case 2:
                m.threadOrigin = 2;
                break;
            case "AI_DEEPLINK_IMMERSIVE_THREAD":
            case 3:
                m.threadOrigin = 3;
                break;
            case "AI_DEEPLINK_THREAD":
            case 4:
                m.threadOrigin = 4;
                break;
            case "ASK_META_AI_CONTEXT_MENU_THREAD":
            case 5:
                m.threadOrigin = 5;
                break;
            default:
                if (typeof d.threadOrigin === "number" && (d.threadOrigin | 0) === d.threadOrigin)
                    m.threadOrigin = d.threadOrigin;
            }
            return m;
        };

        BotMetricsMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.destinationId != null && $Object.hasOwnProperty.call(m, "destinationId")) {
                d.destinationId = m.destinationId;
            }
            if (m.destinationEntryPoint != null && $Object.hasOwnProperty.call(m, "destinationEntryPoint")) {
                d.destinationEntryPoint = o.enums === $String ? $root.proto.BotMetricsEntryPoint[m.destinationEntryPoint] === $undefined ? m.destinationEntryPoint : $root.proto.BotMetricsEntryPoint[m.destinationEntryPoint] : m.destinationEntryPoint;
            }
            if (m.threadOrigin != null && $Object.hasOwnProperty.call(m, "threadOrigin")) {
                d.threadOrigin = o.enums === $String ? $root.proto.BotMetricsThreadEntryPoint[m.threadOrigin] === $undefined ? m.threadOrigin : $root.proto.BotMetricsThreadEntryPoint[m.threadOrigin] : m.threadOrigin;
            }
            return d;
        };

        BotMetricsMetadata.prototype.toJSON = function() {
            return BotMetricsMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotMetricsMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotMetricsMetadata";
        };

        return BotMetricsMetadata;
    })();

    proto.BotMetricsThreadEntryPoint = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[1] = "AI_TAB_THREAD"] = 1;
        values[valuesById[2] = "AI_HOME_THREAD"] = 2;
        values[valuesById[3] = "AI_DEEPLINK_IMMERSIVE_THREAD"] = 3;
        values[valuesById[4] = "AI_DEEPLINK_THREAD"] = 4;
        values[valuesById[5] = "ASK_META_AI_CONTEXT_MENU_THREAD"] = 5;
        return values;
    })();

    proto.BotModeSelectionMetadata = (function() {

        const BotModeSelectionMetadata = function (p) {
            this.mode = [];
            this.overrideMode = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotModeSelectionMetadata.prototype.mode = $util.emptyArray;
        BotModeSelectionMetadata.prototype.overrideMode = $util.emptyArray;

        BotModeSelectionMetadata.create = function(properties) {
            return new BotModeSelectionMetadata(properties);
        };

        BotModeSelectionMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.mode != null && m.mode.length) {
                w.uint32(10).int32s(m.mode);
            }
            if (m.overrideMode != null && m.overrideMode.length) {
                w.uint32(18).uint32s(m.overrideMode);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotModeSelectionMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotModeSelectionMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u === 2) {
                            if (!(m.mode && m.mode.length))
                                m.mode = [];
                            r.int32s(m.mode);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.mode && m.mode.length))
                            m.mode = [];
                        m.mode.push(r.int32());
                        continue;
                    }
                case 2: {
                        if (u === 2) {
                            if (!(m.overrideMode && m.overrideMode.length))
                                m.overrideMode = [];
                            r.uint32s(m.overrideMode);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.overrideMode && m.overrideMode.length))
                            m.overrideMode = [];
                        m.overrideMode.push(r.uint32());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotModeSelectionMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotModeSelectionMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotModeSelectionMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotModeSelectionMetadata();
            if (d.mode) {
                if (!$Array.isArray(d.mode))
                    throw $TypeError(".proto.BotModeSelectionMetadata.mode: array expected");
                m.mode = [];
                for (var i = 0; i < d.mode.length; ++i) {
                    switch (d.mode[i]) {
                    case "DEFAULT_MODE":
                    case 0:
                        m.mode[m.mode.length] = 0;
                        break;
                    case "THINK_HARD_MODE":
                    case 1:
                        m.mode[m.mode.length] = 1;
                        break;
                    default:
                        if (typeof d.mode[i] === "number" && (d.mode[i] | 0) === d.mode[i])
                            m.mode[m.mode.length] = d.mode[i];
                    }
                }
            }
            if (d.overrideMode) {
                if (!$Array.isArray(d.overrideMode))
                    throw $TypeError(".proto.BotModeSelectionMetadata.overrideMode: array expected");
                m.overrideMode = $Array(d.overrideMode.length);
                for (var i = 0; i < d.overrideMode.length; ++i) {
                    m.overrideMode[i] = d.overrideMode[i] >>> 0;
                }
            }
            return m;
        };

        BotModeSelectionMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mode = [];
                d.overrideMode = [];
            }
            if (m.mode && m.mode.length) {
                d.mode = $Array(m.mode.length);
                for (var j = 0; j < m.mode.length; ++j) {
                    d.mode[j] = o.enums === $String ? $root.proto.BotModeSelectionMetadata.BotUserSelectionMode[m.mode[j]] === $undefined ? m.mode[j] : $root.proto.BotModeSelectionMetadata.BotUserSelectionMode[m.mode[j]] : m.mode[j];
                }
            }
            if (m.overrideMode && m.overrideMode.length) {
                d.overrideMode = $Array(m.overrideMode.length);
                for (var j = 0; j < m.overrideMode.length; ++j) {
                    d.overrideMode[j] = m.overrideMode[j];
                }
            }
            return d;
        };

        BotModeSelectionMetadata.prototype.toJSON = function() {
            return BotModeSelectionMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotModeSelectionMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotModeSelectionMetadata";
        };

        BotModeSelectionMetadata.BotUserSelectionMode = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DEFAULT_MODE"] = 0;
            values[valuesById[1] = "THINK_HARD_MODE"] = 1;
            return values;
        })();

        return BotModeSelectionMetadata;
    })();

    proto.BotModelMetadata = (function() {

        const BotModelMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotModelMetadata.prototype.modelType = null;
        BotModelMetadata.prototype.premiumModelStatus = null;
        BotModelMetadata.prototype.modelNameOverride = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotModelMetadata.prototype, "_modelType", {
            get: $util.oneOfGetter($oneOfFields = ["modelType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotModelMetadata.prototype, "_premiumModelStatus", {
            get: $util.oneOfGetter($oneOfFields = ["premiumModelStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotModelMetadata.prototype, "_modelNameOverride", {
            get: $util.oneOfGetter($oneOfFields = ["modelNameOverride"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotModelMetadata.create = function(properties) {
            return new BotModelMetadata(properties);
        };

        BotModelMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.modelType != null && $Object.hasOwnProperty.call(m, "modelType"))
                w.uint32(8).int32(m.modelType);
            if (m.premiumModelStatus != null && $Object.hasOwnProperty.call(m, "premiumModelStatus"))
                w.uint32(16).int32(m.premiumModelStatus);
            if (m.modelNameOverride != null && $Object.hasOwnProperty.call(m, "modelNameOverride"))
                w.uint32(26).string(m.modelNameOverride);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotModelMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotModelMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.modelType = r.int32();
                        m._modelType = "modelType";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.premiumModelStatus = r.int32();
                        m._premiumModelStatus = "premiumModelStatus";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.modelNameOverride = r.stringVerify();
                        m._modelNameOverride = "modelNameOverride";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotModelMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotModelMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotModelMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotModelMetadata();
            switch (d.modelType) {
            case "UNKNOWN_TYPE":
            case 0:
                m.modelType = 0;
                break;
            case "LLAMA_PROD":
            case 1:
                m.modelType = 1;
                break;
            case "LLAMA_PROD_PREMIUM":
            case 2:
                m.modelType = 2;
                break;
            default:
                if (typeof d.modelType === "number" && (d.modelType | 0) === d.modelType)
                    m.modelType = d.modelType;
            }
            switch (d.premiumModelStatus) {
            case "UNKNOWN_STATUS":
            case 0:
                m.premiumModelStatus = 0;
                break;
            case "AVAILABLE":
            case 1:
                m.premiumModelStatus = 1;
                break;
            case "QUOTA_EXCEED_LIMIT":
            case 2:
                m.premiumModelStatus = 2;
                break;
            default:
                if (typeof d.premiumModelStatus === "number" && (d.premiumModelStatus | 0) === d.premiumModelStatus)
                    m.premiumModelStatus = d.premiumModelStatus;
            }
            if (d.modelNameOverride != null) {
                m.modelNameOverride = $String(d.modelNameOverride);
            }
            return m;
        };

        BotModelMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.modelType != null && $Object.hasOwnProperty.call(m, "modelType")) {
                d.modelType = o.enums === $String ? $root.proto.BotModelMetadata.ModelType[m.modelType] === $undefined ? m.modelType : $root.proto.BotModelMetadata.ModelType[m.modelType] : m.modelType;
            }
            if (m.premiumModelStatus != null && $Object.hasOwnProperty.call(m, "premiumModelStatus")) {
                d.premiumModelStatus = o.enums === $String ? $root.proto.BotModelMetadata.PremiumModelStatus[m.premiumModelStatus] === $undefined ? m.premiumModelStatus : $root.proto.BotModelMetadata.PremiumModelStatus[m.premiumModelStatus] : m.premiumModelStatus;
            }
            if (m.modelNameOverride != null && $Object.hasOwnProperty.call(m, "modelNameOverride")) {
                d.modelNameOverride = m.modelNameOverride;
            }
            return d;
        };

        BotModelMetadata.prototype.toJSON = function() {
            return BotModelMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotModelMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotModelMetadata";
        };

        BotModelMetadata.ModelType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "LLAMA_PROD"] = 1;
            values[valuesById[2] = "LLAMA_PROD_PREMIUM"] = 2;
            return values;
        })();

        BotModelMetadata.PremiumModelStatus = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATUS"] = 0;
            values[valuesById[1] = "AVAILABLE"] = 1;
            values[valuesById[2] = "QUOTA_EXCEED_LIMIT"] = 2;
            return values;
        })();

        return BotModelMetadata;
    })();

    proto.BotPluginMetadata = (function() {

        const BotPluginMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotPluginMetadata.prototype.provider = null;
        BotPluginMetadata.prototype.pluginType = null;
        BotPluginMetadata.prototype.thumbnailCdnUrl = null;
        BotPluginMetadata.prototype.profilePhotoCdnUrl = null;
        BotPluginMetadata.prototype.searchProviderUrl = null;
        BotPluginMetadata.prototype.referenceIndex = null;
        BotPluginMetadata.prototype.expectedLinksCount = null;
        BotPluginMetadata.prototype.searchQuery = null;
        BotPluginMetadata.prototype.parentPluginMessageKey = null;
        BotPluginMetadata.prototype.deprecatedField = null;
        BotPluginMetadata.prototype.parentPluginType = null;
        BotPluginMetadata.prototype.faviconCdnUrl = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_provider", {
            get: $util.oneOfGetter($oneOfFields = ["provider"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_pluginType", {
            get: $util.oneOfGetter($oneOfFields = ["pluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_thumbnailCdnUrl", {
            get: $util.oneOfGetter($oneOfFields = ["thumbnailCdnUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_profilePhotoCdnUrl", {
            get: $util.oneOfGetter($oneOfFields = ["profilePhotoCdnUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_searchProviderUrl", {
            get: $util.oneOfGetter($oneOfFields = ["searchProviderUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_referenceIndex", {
            get: $util.oneOfGetter($oneOfFields = ["referenceIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_expectedLinksCount", {
            get: $util.oneOfGetter($oneOfFields = ["expectedLinksCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_searchQuery", {
            get: $util.oneOfGetter($oneOfFields = ["searchQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_parentPluginMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["parentPluginMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_deprecatedField", {
            get: $util.oneOfGetter($oneOfFields = ["deprecatedField"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_parentPluginType", {
            get: $util.oneOfGetter($oneOfFields = ["parentPluginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPluginMetadata.prototype, "_faviconCdnUrl", {
            get: $util.oneOfGetter($oneOfFields = ["faviconCdnUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPluginMetadata.create = function(properties) {
            return new BotPluginMetadata(properties);
        };

        BotPluginMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.provider != null && $Object.hasOwnProperty.call(m, "provider"))
                w.uint32(8).int32(m.provider);
            if (m.pluginType != null && $Object.hasOwnProperty.call(m, "pluginType"))
                w.uint32(16).int32(m.pluginType);
            if (m.thumbnailCdnUrl != null && $Object.hasOwnProperty.call(m, "thumbnailCdnUrl"))
                w.uint32(26).string(m.thumbnailCdnUrl);
            if (m.profilePhotoCdnUrl != null && $Object.hasOwnProperty.call(m, "profilePhotoCdnUrl"))
                w.uint32(34).string(m.profilePhotoCdnUrl);
            if (m.searchProviderUrl != null && $Object.hasOwnProperty.call(m, "searchProviderUrl"))
                w.uint32(42).string(m.searchProviderUrl);
            if (m.referenceIndex != null && $Object.hasOwnProperty.call(m, "referenceIndex"))
                w.uint32(48).uint32(m.referenceIndex);
            if (m.expectedLinksCount != null && $Object.hasOwnProperty.call(m, "expectedLinksCount"))
                w.uint32(56).uint32(m.expectedLinksCount);
            if (m.searchQuery != null && $Object.hasOwnProperty.call(m, "searchQuery"))
                w.uint32(74).string(m.searchQuery);
            if (m.parentPluginMessageKey != null && $Object.hasOwnProperty.call(m, "parentPluginMessageKey"))
                $root.proto.MessageKey.encode(m.parentPluginMessageKey, w.uint32(82).fork(), q + 1).ldelim();
            if (m.deprecatedField != null && $Object.hasOwnProperty.call(m, "deprecatedField"))
                w.uint32(88).int32(m.deprecatedField);
            if (m.parentPluginType != null && $Object.hasOwnProperty.call(m, "parentPluginType"))
                w.uint32(96).int32(m.parentPluginType);
            if (m.faviconCdnUrl != null && $Object.hasOwnProperty.call(m, "faviconCdnUrl"))
                w.uint32(106).string(m.faviconCdnUrl);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotPluginMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotPluginMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.provider = r.int32();
                        m._provider = "provider";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.pluginType = r.int32();
                        m._pluginType = "pluginType";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.thumbnailCdnUrl = r.stringVerify();
                        m._thumbnailCdnUrl = "thumbnailCdnUrl";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.profilePhotoCdnUrl = r.stringVerify();
                        m._profilePhotoCdnUrl = "profilePhotoCdnUrl";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.searchProviderUrl = r.stringVerify();
                        m._searchProviderUrl = "searchProviderUrl";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.referenceIndex = r.uint32();
                        m._referenceIndex = "referenceIndex";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.expectedLinksCount = r.uint32();
                        m._expectedLinksCount = "expectedLinksCount";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.searchQuery = r.stringVerify();
                        m._searchQuery = "searchQuery";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.parentPluginMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.parentPluginMessageKey);
                        m._parentPluginMessageKey = "parentPluginMessageKey";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.deprecatedField = r.int32();
                        m._deprecatedField = "deprecatedField";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.parentPluginType = r.int32();
                        m._parentPluginType = "parentPluginType";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.faviconCdnUrl = r.stringVerify();
                        m._faviconCdnUrl = "faviconCdnUrl";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotPluginMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotPluginMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotPluginMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotPluginMetadata();
            switch (d.provider) {
            case "UNKNOWN":
            case 0:
                m.provider = 0;
                break;
            case "BING":
            case 1:
                m.provider = 1;
                break;
            case "GOOGLE":
            case 2:
                m.provider = 2;
                break;
            case "SUPPORT":
            case 3:
                m.provider = 3;
                break;
            default:
                if (typeof d.provider === "number" && (d.provider | 0) === d.provider)
                    m.provider = d.provider;
            }
            switch (d.pluginType) {
            case "UNKNOWN_PLUGIN":
            case 0:
                m.pluginType = 0;
                break;
            case "REELS":
            case 1:
                m.pluginType = 1;
                break;
            case "SEARCH":
            case 2:
                m.pluginType = 2;
                break;
            default:
                if (typeof d.pluginType === "number" && (d.pluginType | 0) === d.pluginType)
                    m.pluginType = d.pluginType;
            }
            if (d.thumbnailCdnUrl != null) {
                m.thumbnailCdnUrl = $String(d.thumbnailCdnUrl);
            }
            if (d.profilePhotoCdnUrl != null) {
                m.profilePhotoCdnUrl = $String(d.profilePhotoCdnUrl);
            }
            if (d.searchProviderUrl != null) {
                m.searchProviderUrl = $String(d.searchProviderUrl);
            }
            if (d.referenceIndex != null) {
                m.referenceIndex = d.referenceIndex >>> 0;
            }
            if (d.expectedLinksCount != null) {
                m.expectedLinksCount = d.expectedLinksCount >>> 0;
            }
            if (d.searchQuery != null) {
                m.searchQuery = $String(d.searchQuery);
            }
            if (d.parentPluginMessageKey != null) {
                if (!$util.isObject(d.parentPluginMessageKey))
                    throw $TypeError(".proto.BotPluginMetadata.parentPluginMessageKey: object expected");
                m.parentPluginMessageKey = $root.proto.MessageKey.fromObject(d.parentPluginMessageKey, q + 1);
            }
            switch (d.deprecatedField) {
            case "UNKNOWN_PLUGIN":
            case 0:
                m.deprecatedField = 0;
                break;
            case "REELS":
            case 1:
                m.deprecatedField = 1;
                break;
            case "SEARCH":
            case 2:
                m.deprecatedField = 2;
                break;
            default:
                if (typeof d.deprecatedField === "number" && (d.deprecatedField | 0) === d.deprecatedField)
                    m.deprecatedField = d.deprecatedField;
            }
            switch (d.parentPluginType) {
            case "UNKNOWN_PLUGIN":
            case 0:
                m.parentPluginType = 0;
                break;
            case "REELS":
            case 1:
                m.parentPluginType = 1;
                break;
            case "SEARCH":
            case 2:
                m.parentPluginType = 2;
                break;
            default:
                if (typeof d.parentPluginType === "number" && (d.parentPluginType | 0) === d.parentPluginType)
                    m.parentPluginType = d.parentPluginType;
            }
            if (d.faviconCdnUrl != null) {
                m.faviconCdnUrl = $String(d.faviconCdnUrl);
            }
            return m;
        };

        BotPluginMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.provider != null && $Object.hasOwnProperty.call(m, "provider")) {
                d.provider = o.enums === $String ? $root.proto.BotPluginMetadata.SearchProvider[m.provider] === $undefined ? m.provider : $root.proto.BotPluginMetadata.SearchProvider[m.provider] : m.provider;
            }
            if (m.pluginType != null && $Object.hasOwnProperty.call(m, "pluginType")) {
                d.pluginType = o.enums === $String ? $root.proto.BotPluginMetadata.PluginType[m.pluginType] === $undefined ? m.pluginType : $root.proto.BotPluginMetadata.PluginType[m.pluginType] : m.pluginType;
            }
            if (m.thumbnailCdnUrl != null && $Object.hasOwnProperty.call(m, "thumbnailCdnUrl")) {
                d.thumbnailCdnUrl = m.thumbnailCdnUrl;
            }
            if (m.profilePhotoCdnUrl != null && $Object.hasOwnProperty.call(m, "profilePhotoCdnUrl")) {
                d.profilePhotoCdnUrl = m.profilePhotoCdnUrl;
            }
            if (m.searchProviderUrl != null && $Object.hasOwnProperty.call(m, "searchProviderUrl")) {
                d.searchProviderUrl = m.searchProviderUrl;
            }
            if (m.referenceIndex != null && $Object.hasOwnProperty.call(m, "referenceIndex")) {
                d.referenceIndex = m.referenceIndex;
            }
            if (m.expectedLinksCount != null && $Object.hasOwnProperty.call(m, "expectedLinksCount")) {
                d.expectedLinksCount = m.expectedLinksCount;
            }
            if (m.searchQuery != null && $Object.hasOwnProperty.call(m, "searchQuery")) {
                d.searchQuery = m.searchQuery;
            }
            if (m.parentPluginMessageKey != null && $Object.hasOwnProperty.call(m, "parentPluginMessageKey")) {
                d.parentPluginMessageKey = $root.proto.MessageKey.toObject(m.parentPluginMessageKey, o, q + 1);
            }
            if (m.deprecatedField != null && $Object.hasOwnProperty.call(m, "deprecatedField")) {
                d.deprecatedField = o.enums === $String ? $root.proto.BotPluginMetadata.PluginType[m.deprecatedField] === $undefined ? m.deprecatedField : $root.proto.BotPluginMetadata.PluginType[m.deprecatedField] : m.deprecatedField;
            }
            if (m.parentPluginType != null && $Object.hasOwnProperty.call(m, "parentPluginType")) {
                d.parentPluginType = o.enums === $String ? $root.proto.BotPluginMetadata.PluginType[m.parentPluginType] === $undefined ? m.parentPluginType : $root.proto.BotPluginMetadata.PluginType[m.parentPluginType] : m.parentPluginType;
            }
            if (m.faviconCdnUrl != null && $Object.hasOwnProperty.call(m, "faviconCdnUrl")) {
                d.faviconCdnUrl = m.faviconCdnUrl;
            }
            return d;
        };

        BotPluginMetadata.prototype.toJSON = function() {
            return BotPluginMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPluginMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotPluginMetadata";
        };

        BotPluginMetadata.PluginType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_PLUGIN"] = 0;
            values[valuesById[1] = "REELS"] = 1;
            values[valuesById[2] = "SEARCH"] = 2;
            return values;
        })();

        BotPluginMetadata.SearchProvider = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "BING"] = 1;
            values[valuesById[2] = "GOOGLE"] = 2;
            values[valuesById[3] = "SUPPORT"] = 3;
            return values;
        })();

        return BotPluginMetadata;
    })();

    proto.BotProgressIndicatorMetadata = (function() {

        const BotProgressIndicatorMetadata = function (p) {
            this.stepsMetadata = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotProgressIndicatorMetadata.prototype.progressDescription = null;
        BotProgressIndicatorMetadata.prototype.stepsMetadata = $util.emptyArray;
        BotProgressIndicatorMetadata.prototype.estimatedCompletionTime = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotProgressIndicatorMetadata.prototype, "_progressDescription", {
            get: $util.oneOfGetter($oneOfFields = ["progressDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotProgressIndicatorMetadata.prototype, "_estimatedCompletionTime", {
            get: $util.oneOfGetter($oneOfFields = ["estimatedCompletionTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotProgressIndicatorMetadata.create = function(properties) {
            return new BotProgressIndicatorMetadata(properties);
        };

        BotProgressIndicatorMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.progressDescription != null && $Object.hasOwnProperty.call(m, "progressDescription"))
                w.uint32(10).string(m.progressDescription);
            if (m.stepsMetadata != null && m.stepsMetadata.length) {
                for (var i = 0; i < m.stepsMetadata.length; ++i)
                    $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.encode(m.stepsMetadata[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.estimatedCompletionTime != null && $Object.hasOwnProperty.call(m, "estimatedCompletionTime"))
                w.uint32(24).int64(m.estimatedCompletionTime);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotProgressIndicatorMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotProgressIndicatorMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.progressDescription = r.stringVerify();
                        m._progressDescription = "progressDescription";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.stepsMetadata && m.stepsMetadata.length))
                            m.stepsMetadata = [];
                        m.stepsMetadata.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.estimatedCompletionTime = r.int64();
                        m._estimatedCompletionTime = "estimatedCompletionTime";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotProgressIndicatorMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotProgressIndicatorMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotProgressIndicatorMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotProgressIndicatorMetadata();
            if (d.progressDescription != null) {
                m.progressDescription = $String(d.progressDescription);
            }
            if (d.stepsMetadata) {
                if (!$Array.isArray(d.stepsMetadata))
                    throw $TypeError(".proto.BotProgressIndicatorMetadata.stepsMetadata: array expected");
                m.stepsMetadata = $Array(d.stepsMetadata.length);
                for (var i = 0; i < d.stepsMetadata.length; ++i) {
                    if (!$util.isObject(d.stepsMetadata[i]))
                        throw $TypeError(".proto.BotProgressIndicatorMetadata.stepsMetadata: object expected");
                    m.stepsMetadata[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.fromObject(d.stepsMetadata[i], q + 1);
                }
            }
            if (d.estimatedCompletionTime != null) {
                if ($util.Long)
                    m.estimatedCompletionTime = $util.Long.fromValue(d.estimatedCompletionTime, false);
                else if (typeof d.estimatedCompletionTime === "string")
                    m.estimatedCompletionTime = $parseInt(d.estimatedCompletionTime, 10);
                else if (typeof d.estimatedCompletionTime === "number")
                    m.estimatedCompletionTime = d.estimatedCompletionTime;
                else if (typeof d.estimatedCompletionTime === "object")
                    m.estimatedCompletionTime = new $util.LongBits(d.estimatedCompletionTime.low >>> 0, d.estimatedCompletionTime.high >>> 0).toNumber();
            }
            return m;
        };

        BotProgressIndicatorMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.stepsMetadata = [];
            }
            if (m.progressDescription != null && $Object.hasOwnProperty.call(m, "progressDescription")) {
                d.progressDescription = m.progressDescription;
            }
            if (m.stepsMetadata && m.stepsMetadata.length) {
                d.stepsMetadata = $Array(m.stepsMetadata.length);
                for (var j = 0; j < m.stepsMetadata.length; ++j) {
                    d.stepsMetadata[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.toObject(m.stepsMetadata[j], o, q + 1);
                }
            }
            if (m.estimatedCompletionTime != null && $Object.hasOwnProperty.call(m, "estimatedCompletionTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.estimatedCompletionTime = typeof m.estimatedCompletionTime === "number" ? $BigInt(m.estimatedCompletionTime) : $util.Long.fromBits(m.estimatedCompletionTime.low >>> 0, m.estimatedCompletionTime.high >>> 0, false).toBigInt();
                else if (typeof m.estimatedCompletionTime === "number")
                    d.estimatedCompletionTime = o.longs === $String ? $String(m.estimatedCompletionTime) : m.estimatedCompletionTime;
                else
                    d.estimatedCompletionTime = o.longs === $String ? $util.Long.prototype.toString.call(m.estimatedCompletionTime) : o.longs === $Number ? new $util.LongBits(m.estimatedCompletionTime.low >>> 0, m.estimatedCompletionTime.high >>> 0).toNumber() : m.estimatedCompletionTime;
            }
            return d;
        };

        BotProgressIndicatorMetadata.prototype.toJSON = function() {
            return BotProgressIndicatorMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotProgressIndicatorMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotProgressIndicatorMetadata";
        };

        BotProgressIndicatorMetadata.BotPlanningStepMetadata = (function() {

            const BotPlanningStepMetadata = function (p) {
                this.sourcesMetadata = [];
                this.sections = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BotPlanningStepMetadata.prototype.statusTitle = null;
            BotPlanningStepMetadata.prototype.statusBody = null;
            BotPlanningStepMetadata.prototype.sourcesMetadata = $util.emptyArray;
            BotPlanningStepMetadata.prototype.status = null;
            BotPlanningStepMetadata.prototype.isReasoning = null;
            BotPlanningStepMetadata.prototype.isEnhancedSearch = null;
            BotPlanningStepMetadata.prototype.sections = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotPlanningStepMetadata.prototype, "_statusTitle", {
                get: $util.oneOfGetter($oneOfFields = ["statusTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotPlanningStepMetadata.prototype, "_statusBody", {
                get: $util.oneOfGetter($oneOfFields = ["statusBody"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotPlanningStepMetadata.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotPlanningStepMetadata.prototype, "_isReasoning", {
                get: $util.oneOfGetter($oneOfFields = ["isReasoning"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotPlanningStepMetadata.prototype, "_isEnhancedSearch", {
                get: $util.oneOfGetter($oneOfFields = ["isEnhancedSearch"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotPlanningStepMetadata.create = function(properties) {
                return new BotPlanningStepMetadata(properties);
            };

            BotPlanningStepMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.statusTitle != null && $Object.hasOwnProperty.call(m, "statusTitle"))
                    w.uint32(10).string(m.statusTitle);
                if (m.statusBody != null && $Object.hasOwnProperty.call(m, "statusBody"))
                    w.uint32(18).string(m.statusBody);
                if (m.sourcesMetadata != null && m.sourcesMetadata.length) {
                    for (var i = 0; i < m.sourcesMetadata.length; ++i)
                        $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.encode(m.sourcesMetadata[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                    w.uint32(32).int32(m.status);
                if (m.isReasoning != null && $Object.hasOwnProperty.call(m, "isReasoning"))
                    w.uint32(40).bool(m.isReasoning);
                if (m.isEnhancedSearch != null && $Object.hasOwnProperty.call(m, "isEnhancedSearch"))
                    w.uint32(48).bool(m.isEnhancedSearch);
                if (m.sections != null && m.sections.length) {
                    for (var i = 0; i < m.sections.length; ++i)
                        $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.encode(m.sections[i], w.uint32(58).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BotPlanningStepMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.statusTitle = r.stringVerify();
                            m._statusTitle = "statusTitle";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.statusBody = r.stringVerify();
                            m._statusBody = "statusBody";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.sourcesMetadata && m.sourcesMetadata.length))
                                m.sourcesMetadata = [];
                            m.sourcesMetadata.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            m._status = "status";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.isReasoning = r.bool();
                            m._isReasoning = "isReasoning";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.isEnhancedSearch = r.bool();
                            m._isEnhancedSearch = "isEnhancedSearch";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            if (!(m.sections && m.sections.length))
                                m.sections = [];
                            m.sections.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BotPlanningStepMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata();
                if (d.statusTitle != null) {
                    m.statusTitle = $String(d.statusTitle);
                }
                if (d.statusBody != null) {
                    m.statusBody = $String(d.statusBody);
                }
                if (d.sourcesMetadata) {
                    if (!$Array.isArray(d.sourcesMetadata))
                        throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sourcesMetadata: array expected");
                    m.sourcesMetadata = $Array(d.sourcesMetadata.length);
                    for (var i = 0; i < d.sourcesMetadata.length; ++i) {
                        if (!$util.isObject(d.sourcesMetadata[i]))
                            throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sourcesMetadata: object expected");
                        m.sourcesMetadata[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.fromObject(d.sourcesMetadata[i], q + 1);
                    }
                }
                switch (d.status) {
                case "UNKNOWN":
                case 0:
                    m.status = 0;
                    break;
                case "PLANNED":
                case 1:
                    m.status = 1;
                    break;
                case "EXECUTING":
                case 2:
                    m.status = 2;
                    break;
                case "FINISHED":
                case 3:
                    m.status = 3;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                if (d.isReasoning != null) {
                    m.isReasoning = $Boolean(d.isReasoning);
                }
                if (d.isEnhancedSearch != null) {
                    m.isEnhancedSearch = $Boolean(d.isEnhancedSearch);
                }
                if (d.sections) {
                    if (!$Array.isArray(d.sections))
                        throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sections: array expected");
                    m.sections = $Array(d.sections.length);
                    for (var i = 0; i < d.sections.length; ++i) {
                        if (!$util.isObject(d.sections[i]))
                            throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.sections: object expected");
                        m.sections[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.fromObject(d.sections[i], q + 1);
                    }
                }
                return m;
            };

            BotPlanningStepMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.sourcesMetadata = [];
                    d.sections = [];
                }
                if (m.statusTitle != null && $Object.hasOwnProperty.call(m, "statusTitle")) {
                    d.statusTitle = m.statusTitle;
                }
                if (m.statusBody != null && $Object.hasOwnProperty.call(m, "statusBody")) {
                    d.statusBody = m.statusBody;
                }
                if (m.sourcesMetadata && m.sourcesMetadata.length) {
                    d.sourcesMetadata = $Array(m.sourcesMetadata.length);
                    for (var j = 0; j < m.sourcesMetadata.length; ++j) {
                        d.sourcesMetadata[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.toObject(m.sourcesMetadata[j], o, q + 1);
                    }
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus[m.status] === $undefined ? m.status : $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.PlanningStepStatus[m.status] : m.status;
                }
                if (m.isReasoning != null && $Object.hasOwnProperty.call(m, "isReasoning")) {
                    d.isReasoning = m.isReasoning;
                }
                if (m.isEnhancedSearch != null && $Object.hasOwnProperty.call(m, "isEnhancedSearch")) {
                    d.isEnhancedSearch = m.isEnhancedSearch;
                }
                if (m.sections && m.sections.length) {
                    d.sections = $Array(m.sections.length);
                    for (var j = 0; j < m.sections.length; ++j) {
                        d.sections[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.toObject(m.sections[j], o, q + 1);
                    }
                }
                return d;
            };

            BotPlanningStepMetadata.prototype.toJSON = function() {
                return BotPlanningStepMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotPlanningStepMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata";
            };

            BotPlanningStepMetadata.BotPlanningSearchSourceMetadata = (function() {

                const BotPlanningSearchSourceMetadata = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                BotPlanningSearchSourceMetadata.prototype.title = null;
                BotPlanningSearchSourceMetadata.prototype.provider = null;
                BotPlanningSearchSourceMetadata.prototype.sourceUrl = null;
                BotPlanningSearchSourceMetadata.prototype.favIconUrl = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_provider", {
                    get: $util.oneOfGetter($oneOfFields = ["provider"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourceMetadata.prototype, "_favIconUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["favIconUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BotPlanningSearchSourceMetadata.create = function(properties) {
                    return new BotPlanningSearchSourceMetadata(properties);
                };

                BotPlanningSearchSourceMetadata.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(10).string(m.title);
                    if (m.provider != null && $Object.hasOwnProperty.call(m, "provider"))
                        w.uint32(16).int32(m.provider);
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl"))
                        w.uint32(26).string(m.sourceUrl);
                    if (m.favIconUrl != null && $Object.hasOwnProperty.call(m, "favIconUrl"))
                        w.uint32(34).string(m.favIconUrl);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                BotPlanningSearchSourceMetadata.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.provider = r.int32();
                                m._provider = "provider";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.sourceUrl = r.stringVerify();
                                m._sourceUrl = "sourceUrl";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.favIconUrl = r.stringVerify();
                                m._favIconUrl = "favIconUrl";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                BotPlanningSearchSourceMetadata.fromObject = function (d, q) {
                    if (d instanceof $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata();
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    switch (d.provider) {
                    case "UNKNOWN_PROVIDER":
                    case 0:
                        m.provider = 0;
                        break;
                    case "OTHER":
                    case 1:
                        m.provider = 1;
                        break;
                    case "GOOGLE":
                    case 2:
                        m.provider = 2;
                        break;
                    case "BING":
                    case 3:
                        m.provider = 3;
                        break;
                    default:
                        if (typeof d.provider === "number" && (d.provider | 0) === d.provider)
                            m.provider = d.provider;
                    }
                    if (d.sourceUrl != null) {
                        m.sourceUrl = $String(d.sourceUrl);
                    }
                    if (d.favIconUrl != null) {
                        m.favIconUrl = $String(d.favIconUrl);
                    }
                    return m;
                };

                BotPlanningSearchSourceMetadata.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.provider != null && $Object.hasOwnProperty.call(m, "provider")) {
                        d.provider = o.enums === $String ? $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider[m.provider] === $undefined ? m.provider : $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotSearchSourceProvider[m.provider] : m.provider;
                    }
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl")) {
                        d.sourceUrl = m.sourceUrl;
                    }
                    if (m.favIconUrl != null && $Object.hasOwnProperty.call(m, "favIconUrl")) {
                        d.favIconUrl = m.favIconUrl;
                    }
                    return d;
                };

                BotPlanningSearchSourceMetadata.prototype.toJSON = function() {
                    return BotPlanningSearchSourceMetadata.toObject(this, $protobuf.util.toJSONOptions);
                };

                BotPlanningSearchSourceMetadata.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata";
                };

                return BotPlanningSearchSourceMetadata;
            })();

            BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata = (function() {

                const BotPlanningSearchSourcesMetadata = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                BotPlanningSearchSourcesMetadata.prototype.sourceTitle = null;
                BotPlanningSearchSourcesMetadata.prototype.provider = null;
                BotPlanningSearchSourcesMetadata.prototype.sourceUrl = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_sourceTitle", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceTitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_provider", {
                    get: $util.oneOfGetter($oneOfFields = ["provider"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningSearchSourcesMetadata.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BotPlanningSearchSourcesMetadata.create = function(properties) {
                    return new BotPlanningSearchSourcesMetadata(properties);
                };

                BotPlanningSearchSourcesMetadata.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.sourceTitle != null && $Object.hasOwnProperty.call(m, "sourceTitle"))
                        w.uint32(10).string(m.sourceTitle);
                    if (m.provider != null && $Object.hasOwnProperty.call(m, "provider"))
                        w.uint32(16).int32(m.provider);
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl"))
                        w.uint32(26).string(m.sourceUrl);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                BotPlanningSearchSourcesMetadata.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.sourceTitle = r.stringVerify();
                                m._sourceTitle = "sourceTitle";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.provider = r.int32();
                                m._provider = "provider";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.sourceUrl = r.stringVerify();
                                m._sourceUrl = "sourceUrl";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                BotPlanningSearchSourcesMetadata.fromObject = function (d, q) {
                    if (d instanceof $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata();
                    if (d.sourceTitle != null) {
                        m.sourceTitle = $String(d.sourceTitle);
                    }
                    switch (d.provider) {
                    case "UNKNOWN":
                    case 0:
                        m.provider = 0;
                        break;
                    case "OTHER":
                    case 1:
                        m.provider = 1;
                        break;
                    case "GOOGLE":
                    case 2:
                        m.provider = 2;
                        break;
                    case "BING":
                    case 3:
                        m.provider = 3;
                        break;
                    default:
                        if (typeof d.provider === "number" && (d.provider | 0) === d.provider)
                            m.provider = d.provider;
                    }
                    if (d.sourceUrl != null) {
                        m.sourceUrl = $String(d.sourceUrl);
                    }
                    return m;
                };

                BotPlanningSearchSourcesMetadata.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.sourceTitle != null && $Object.hasOwnProperty.call(m, "sourceTitle")) {
                        d.sourceTitle = m.sourceTitle;
                    }
                    if (m.provider != null && $Object.hasOwnProperty.call(m, "provider")) {
                        d.provider = o.enums === $String ? $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider[m.provider] === $undefined ? m.provider : $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider[m.provider] : m.provider;
                    }
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl")) {
                        d.sourceUrl = m.sourceUrl;
                    }
                    return d;
                };

                BotPlanningSearchSourcesMetadata.prototype.toJSON = function() {
                    return BotPlanningSearchSourcesMetadata.toObject(this, $protobuf.util.toJSONOptions);
                };

                BotPlanningSearchSourcesMetadata.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourcesMetadata";
                };

                BotPlanningSearchSourcesMetadata.BotPlanningSearchSourceProvider = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "OTHER"] = 1;
                    values[valuesById[2] = "GOOGLE"] = 2;
                    values[valuesById[3] = "BING"] = 3;
                    return values;
                })();

                return BotPlanningSearchSourcesMetadata;
            })();

            BotPlanningStepMetadata.BotPlanningStepSectionMetadata = (function() {

                const BotPlanningStepSectionMetadata = function (p) {
                    this.sourcesMetadata = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                BotPlanningStepSectionMetadata.prototype.sectionTitle = null;
                BotPlanningStepSectionMetadata.prototype.sectionBody = null;
                BotPlanningStepSectionMetadata.prototype.sourcesMetadata = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningStepSectionMetadata.prototype, "_sectionTitle", {
                    get: $util.oneOfGetter($oneOfFields = ["sectionTitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BotPlanningStepSectionMetadata.prototype, "_sectionBody", {
                    get: $util.oneOfGetter($oneOfFields = ["sectionBody"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BotPlanningStepSectionMetadata.create = function(properties) {
                    return new BotPlanningStepSectionMetadata(properties);
                };

                BotPlanningStepSectionMetadata.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.sectionTitle != null && $Object.hasOwnProperty.call(m, "sectionTitle"))
                        w.uint32(10).string(m.sectionTitle);
                    if (m.sectionBody != null && $Object.hasOwnProperty.call(m, "sectionBody"))
                        w.uint32(18).string(m.sectionBody);
                    if (m.sourcesMetadata != null && m.sourcesMetadata.length) {
                        for (var i = 0; i < m.sourcesMetadata.length; ++i)
                            $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.encode(m.sourcesMetadata[i], w.uint32(26).fork(), q + 1).ldelim();
                    }
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                BotPlanningStepSectionMetadata.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.sectionTitle = r.stringVerify();
                                m._sectionTitle = "sectionTitle";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.sectionBody = r.stringVerify();
                                m._sectionBody = "sectionBody";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                if (!(m.sourcesMetadata && m.sourcesMetadata.length))
                                    m.sourcesMetadata = [];
                                m.sourcesMetadata.push($root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                BotPlanningStepSectionMetadata.fromObject = function (d, q) {
                    if (d instanceof $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata();
                    if (d.sectionTitle != null) {
                        m.sectionTitle = $String(d.sectionTitle);
                    }
                    if (d.sectionBody != null) {
                        m.sectionBody = $String(d.sectionBody);
                    }
                    if (d.sourcesMetadata) {
                        if (!$Array.isArray(d.sourcesMetadata))
                            throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.sourcesMetadata: array expected");
                        m.sourcesMetadata = $Array(d.sourcesMetadata.length);
                        for (var i = 0; i < d.sourcesMetadata.length; ++i) {
                            if (!$util.isObject(d.sourcesMetadata[i]))
                                throw $TypeError(".proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata.sourcesMetadata: object expected");
                            m.sourcesMetadata[i] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.fromObject(d.sourcesMetadata[i], q + 1);
                        }
                    }
                    return m;
                };

                BotPlanningStepSectionMetadata.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.sourcesMetadata = [];
                    }
                    if (m.sectionTitle != null && $Object.hasOwnProperty.call(m, "sectionTitle")) {
                        d.sectionTitle = m.sectionTitle;
                    }
                    if (m.sectionBody != null && $Object.hasOwnProperty.call(m, "sectionBody")) {
                        d.sectionBody = m.sectionBody;
                    }
                    if (m.sourcesMetadata && m.sourcesMetadata.length) {
                        d.sourcesMetadata = $Array(m.sourcesMetadata.length);
                        for (var j = 0; j < m.sourcesMetadata.length; ++j) {
                            d.sourcesMetadata[j] = $root.proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningSearchSourceMetadata.toObject(m.sourcesMetadata[j], o, q + 1);
                        }
                    }
                    return d;
                };

                BotPlanningStepSectionMetadata.prototype.toJSON = function() {
                    return BotPlanningStepSectionMetadata.toObject(this, $protobuf.util.toJSONOptions);
                };

                BotPlanningStepSectionMetadata.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.BotProgressIndicatorMetadata.BotPlanningStepMetadata.BotPlanningStepSectionMetadata";
                };

                return BotPlanningStepSectionMetadata;
            })();

            BotPlanningStepMetadata.BotSearchSourceProvider = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_PROVIDER"] = 0;
                values[valuesById[1] = "OTHER"] = 1;
                values[valuesById[2] = "GOOGLE"] = 2;
                values[valuesById[3] = "BING"] = 3;
                return values;
            })();

            BotPlanningStepMetadata.PlanningStepStatus = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "PLANNED"] = 1;
                values[valuesById[2] = "EXECUTING"] = 2;
                values[valuesById[3] = "FINISHED"] = 3;
                return values;
            })();

            return BotPlanningStepMetadata;
        })();

        return BotProgressIndicatorMetadata;
    })();

    proto.BotPromotionMessageMetadata = (function() {

        const BotPromotionMessageMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotPromotionMessageMetadata.prototype.promotionType = null;
        BotPromotionMessageMetadata.prototype.buttonTitle = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPromotionMessageMetadata.prototype, "_promotionType", {
            get: $util.oneOfGetter($oneOfFields = ["promotionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPromotionMessageMetadata.prototype, "_buttonTitle", {
            get: $util.oneOfGetter($oneOfFields = ["buttonTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPromotionMessageMetadata.create = function(properties) {
            return new BotPromotionMessageMetadata(properties);
        };

        BotPromotionMessageMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.promotionType != null && $Object.hasOwnProperty.call(m, "promotionType"))
                w.uint32(8).int32(m.promotionType);
            if (m.buttonTitle != null && $Object.hasOwnProperty.call(m, "buttonTitle"))
                w.uint32(18).string(m.buttonTitle);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotPromotionMessageMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotPromotionMessageMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.promotionType = r.int32();
                        m._promotionType = "promotionType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.buttonTitle = r.stringVerify();
                        m._buttonTitle = "buttonTitle";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotPromotionMessageMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotPromotionMessageMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotPromotionMessageMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotPromotionMessageMetadata();
            switch (d.promotionType) {
            case "UNKNOWN_TYPE":
            case 0:
                m.promotionType = 0;
                break;
            case "C50":
            case 1:
                m.promotionType = 1;
                break;
            case "SURVEY_PLATFORM":
            case 2:
                m.promotionType = 2;
                break;
            default:
                if (typeof d.promotionType === "number" && (d.promotionType | 0) === d.promotionType)
                    m.promotionType = d.promotionType;
            }
            if (d.buttonTitle != null) {
                m.buttonTitle = $String(d.buttonTitle);
            }
            return m;
        };

        BotPromotionMessageMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.promotionType != null && $Object.hasOwnProperty.call(m, "promotionType")) {
                d.promotionType = o.enums === $String ? $root.proto.BotPromotionMessageMetadata.BotPromotionType[m.promotionType] === $undefined ? m.promotionType : $root.proto.BotPromotionMessageMetadata.BotPromotionType[m.promotionType] : m.promotionType;
            }
            if (m.buttonTitle != null && $Object.hasOwnProperty.call(m, "buttonTitle")) {
                d.buttonTitle = m.buttonTitle;
            }
            return d;
        };

        BotPromotionMessageMetadata.prototype.toJSON = function() {
            return BotPromotionMessageMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPromotionMessageMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotPromotionMessageMetadata";
        };

        BotPromotionMessageMetadata.BotPromotionType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "C50"] = 1;
            values[valuesById[2] = "SURVEY_PLATFORM"] = 2;
            return values;
        })();

        return BotPromotionMessageMetadata;
    })();

    proto.BotPromptSuggestion = (function() {

        const BotPromptSuggestion = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotPromptSuggestion.prototype.prompt = null;
        BotPromptSuggestion.prototype.promptId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPromptSuggestion.prototype, "_prompt", {
            get: $util.oneOfGetter($oneOfFields = ["prompt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPromptSuggestion.prototype, "_promptId", {
            get: $util.oneOfGetter($oneOfFields = ["promptId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPromptSuggestion.create = function(properties) {
            return new BotPromptSuggestion(properties);
        };

        BotPromptSuggestion.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.prompt != null && $Object.hasOwnProperty.call(m, "prompt"))
                w.uint32(10).string(m.prompt);
            if (m.promptId != null && $Object.hasOwnProperty.call(m, "promptId"))
                w.uint32(18).string(m.promptId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotPromptSuggestion.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotPromptSuggestion();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.prompt = r.stringVerify();
                        m._prompt = "prompt";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.promptId = r.stringVerify();
                        m._promptId = "promptId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotPromptSuggestion.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotPromptSuggestion)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotPromptSuggestion: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotPromptSuggestion();
            if (d.prompt != null) {
                m.prompt = $String(d.prompt);
            }
            if (d.promptId != null) {
                m.promptId = $String(d.promptId);
            }
            return m;
        };

        BotPromptSuggestion.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.prompt != null && $Object.hasOwnProperty.call(m, "prompt")) {
                d.prompt = m.prompt;
            }
            if (m.promptId != null && $Object.hasOwnProperty.call(m, "promptId")) {
                d.promptId = m.promptId;
            }
            return d;
        };

        BotPromptSuggestion.prototype.toJSON = function() {
            return BotPromptSuggestion.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPromptSuggestion.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotPromptSuggestion";
        };

        return BotPromptSuggestion;
    })();

    proto.BotPromptSuggestions = (function() {

        const BotPromptSuggestions = function (p) {
            this.suggestions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotPromptSuggestions.prototype.suggestions = $util.emptyArray;

        BotPromptSuggestions.create = function(properties) {
            return new BotPromptSuggestions(properties);
        };

        BotPromptSuggestions.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.suggestions != null && m.suggestions.length) {
                for (var i = 0; i < m.suggestions.length; ++i)
                    $root.proto.BotPromptSuggestion.encode(m.suggestions[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotPromptSuggestions.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotPromptSuggestions();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.suggestions && m.suggestions.length))
                            m.suggestions = [];
                        m.suggestions.push($root.proto.BotPromptSuggestion.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotPromptSuggestions.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotPromptSuggestions)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotPromptSuggestions: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotPromptSuggestions();
            if (d.suggestions) {
                if (!$Array.isArray(d.suggestions))
                    throw $TypeError(".proto.BotPromptSuggestions.suggestions: array expected");
                m.suggestions = $Array(d.suggestions.length);
                for (var i = 0; i < d.suggestions.length; ++i) {
                    if (!$util.isObject(d.suggestions[i]))
                        throw $TypeError(".proto.BotPromptSuggestions.suggestions: object expected");
                    m.suggestions[i] = $root.proto.BotPromptSuggestion.fromObject(d.suggestions[i], q + 1);
                }
            }
            return m;
        };

        BotPromptSuggestions.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.suggestions = [];
            }
            if (m.suggestions && m.suggestions.length) {
                d.suggestions = $Array(m.suggestions.length);
                for (var j = 0; j < m.suggestions.length; ++j) {
                    d.suggestions[j] = $root.proto.BotPromptSuggestion.toObject(m.suggestions[j], o, q + 1);
                }
            }
            return d;
        };

        BotPromptSuggestions.prototype.toJSON = function() {
            return BotPromptSuggestions.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPromptSuggestions.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotPromptSuggestions";
        };

        return BotPromptSuggestions;
    })();

    proto.BotPttPromptMetadata = (function() {

        const BotPttPromptMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotPttPromptMetadata.prototype.transcript = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotPttPromptMetadata.prototype, "_transcript", {
            get: $util.oneOfGetter($oneOfFields = ["transcript"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotPttPromptMetadata.create = function(properties) {
            return new BotPttPromptMetadata(properties);
        };

        BotPttPromptMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.transcript != null && $Object.hasOwnProperty.call(m, "transcript"))
                w.uint32(10).string(m.transcript);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotPttPromptMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotPttPromptMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.transcript = r.stringVerify();
                        m._transcript = "transcript";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotPttPromptMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotPttPromptMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotPttPromptMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotPttPromptMetadata();
            if (d.transcript != null) {
                m.transcript = $String(d.transcript);
            }
            return m;
        };

        BotPttPromptMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.transcript != null && $Object.hasOwnProperty.call(m, "transcript")) {
                d.transcript = m.transcript;
            }
            return d;
        };

        BotPttPromptMetadata.prototype.toJSON = function() {
            return BotPttPromptMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotPttPromptMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotPttPromptMetadata";
        };

        return BotPttPromptMetadata;
    })();

    proto.BotQuotaMetadata = (function() {

        const BotQuotaMetadata = function (p) {
            this.botFeatureQuotaMetadata = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotQuotaMetadata.prototype.botFeatureQuotaMetadata = $util.emptyArray;

        BotQuotaMetadata.create = function(properties) {
            return new BotQuotaMetadata(properties);
        };

        BotQuotaMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.botFeatureQuotaMetadata != null && m.botFeatureQuotaMetadata.length) {
                for (var i = 0; i < m.botFeatureQuotaMetadata.length; ++i)
                    $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.encode(m.botFeatureQuotaMetadata[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotQuotaMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotQuotaMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.botFeatureQuotaMetadata && m.botFeatureQuotaMetadata.length))
                            m.botFeatureQuotaMetadata = [];
                        m.botFeatureQuotaMetadata.push($root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotQuotaMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotQuotaMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotQuotaMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotQuotaMetadata();
            if (d.botFeatureQuotaMetadata) {
                if (!$Array.isArray(d.botFeatureQuotaMetadata))
                    throw $TypeError(".proto.BotQuotaMetadata.botFeatureQuotaMetadata: array expected");
                m.botFeatureQuotaMetadata = $Array(d.botFeatureQuotaMetadata.length);
                for (var i = 0; i < d.botFeatureQuotaMetadata.length; ++i) {
                    if (!$util.isObject(d.botFeatureQuotaMetadata[i]))
                        throw $TypeError(".proto.BotQuotaMetadata.botFeatureQuotaMetadata: object expected");
                    m.botFeatureQuotaMetadata[i] = $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.fromObject(d.botFeatureQuotaMetadata[i], q + 1);
                }
            }
            return m;
        };

        BotQuotaMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.botFeatureQuotaMetadata = [];
            }
            if (m.botFeatureQuotaMetadata && m.botFeatureQuotaMetadata.length) {
                d.botFeatureQuotaMetadata = $Array(m.botFeatureQuotaMetadata.length);
                for (var j = 0; j < m.botFeatureQuotaMetadata.length; ++j) {
                    d.botFeatureQuotaMetadata[j] = $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.toObject(m.botFeatureQuotaMetadata[j], o, q + 1);
                }
            }
            return d;
        };

        BotQuotaMetadata.prototype.toJSON = function() {
            return BotQuotaMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotQuotaMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotQuotaMetadata";
        };

        BotQuotaMetadata.BotFeatureQuotaMetadata = (function() {

            const BotFeatureQuotaMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BotFeatureQuotaMetadata.prototype.featureType = null;
            BotFeatureQuotaMetadata.prototype.remainingQuota = null;
            BotFeatureQuotaMetadata.prototype.expirationTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_featureType", {
                get: $util.oneOfGetter($oneOfFields = ["featureType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_remainingQuota", {
                get: $util.oneOfGetter($oneOfFields = ["remainingQuota"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotFeatureQuotaMetadata.prototype, "_expirationTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expirationTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotFeatureQuotaMetadata.create = function(properties) {
                return new BotFeatureQuotaMetadata(properties);
            };

            BotFeatureQuotaMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.featureType != null && $Object.hasOwnProperty.call(m, "featureType"))
                    w.uint32(8).int32(m.featureType);
                if (m.remainingQuota != null && $Object.hasOwnProperty.call(m, "remainingQuota"))
                    w.uint32(16).uint32(m.remainingQuota);
                if (m.expirationTimestamp != null && $Object.hasOwnProperty.call(m, "expirationTimestamp"))
                    w.uint32(24).uint64(m.expirationTimestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BotFeatureQuotaMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.featureType = r.int32();
                            m._featureType = "featureType";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.remainingQuota = r.uint32();
                            m._remainingQuota = "remainingQuota";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.expirationTimestamp = r.uint64();
                            m._expirationTimestamp = "expirationTimestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BotFeatureQuotaMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotQuotaMetadata.BotFeatureQuotaMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata();
                switch (d.featureType) {
                case "UNKNOWN_FEATURE":
                case 0:
                    m.featureType = 0;
                    break;
                case "REASONING_FEATURE":
                case 1:
                    m.featureType = 1;
                    break;
                default:
                    if (typeof d.featureType === "number" && (d.featureType | 0) === d.featureType)
                        m.featureType = d.featureType;
                }
                if (d.remainingQuota != null) {
                    m.remainingQuota = d.remainingQuota >>> 0;
                }
                if (d.expirationTimestamp != null) {
                    if ($util.Long)
                        m.expirationTimestamp = $util.Long.fromValue(d.expirationTimestamp, true);
                    else if (typeof d.expirationTimestamp === "string")
                        m.expirationTimestamp = $parseInt(d.expirationTimestamp, 10);
                    else if (typeof d.expirationTimestamp === "number")
                        m.expirationTimestamp = d.expirationTimestamp;
                    else if (typeof d.expirationTimestamp === "object")
                        m.expirationTimestamp = new $util.LongBits(d.expirationTimestamp.low >>> 0, d.expirationTimestamp.high >>> 0).toNumber(true);
                }
                return m;
            };

            BotFeatureQuotaMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.featureType != null && $Object.hasOwnProperty.call(m, "featureType")) {
                    d.featureType = o.enums === $String ? $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType[m.featureType] === $undefined ? m.featureType : $root.proto.BotQuotaMetadata.BotFeatureQuotaMetadata.BotFeatureType[m.featureType] : m.featureType;
                }
                if (m.remainingQuota != null && $Object.hasOwnProperty.call(m, "remainingQuota")) {
                    d.remainingQuota = m.remainingQuota;
                }
                if (m.expirationTimestamp != null && $Object.hasOwnProperty.call(m, "expirationTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.expirationTimestamp = typeof m.expirationTimestamp === "number" ? $BigInt(m.expirationTimestamp) : $util.Long.fromBits(m.expirationTimestamp.low >>> 0, m.expirationTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof m.expirationTimestamp === "number")
                        d.expirationTimestamp = o.longs === $String ? $String(m.expirationTimestamp) : m.expirationTimestamp;
                    else
                        d.expirationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.expirationTimestamp) : o.longs === $Number ? new $util.LongBits(m.expirationTimestamp.low >>> 0, m.expirationTimestamp.high >>> 0).toNumber(true) : m.expirationTimestamp;
                }
                return d;
            };

            BotFeatureQuotaMetadata.prototype.toJSON = function() {
                return BotFeatureQuotaMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotFeatureQuotaMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotQuotaMetadata.BotFeatureQuotaMetadata";
            };

            BotFeatureQuotaMetadata.BotFeatureType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_FEATURE"] = 0;
                values[valuesById[1] = "REASONING_FEATURE"] = 1;
                return values;
            })();

            return BotFeatureQuotaMetadata;
        })();

        return BotQuotaMetadata;
    })();

    proto.BotReminderMetadata = (function() {

        const BotReminderMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotReminderMetadata.prototype.requestMessageKey = null;
        BotReminderMetadata.prototype.action = null;
        BotReminderMetadata.prototype.name = null;
        BotReminderMetadata.prototype.nextTriggerTimestamp = null;
        BotReminderMetadata.prototype.frequency = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotReminderMetadata.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotReminderMetadata.prototype, "_action", {
            get: $util.oneOfGetter($oneOfFields = ["action"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotReminderMetadata.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotReminderMetadata.prototype, "_nextTriggerTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["nextTriggerTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotReminderMetadata.prototype, "_frequency", {
            get: $util.oneOfGetter($oneOfFields = ["frequency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotReminderMetadata.create = function(properties) {
            return new BotReminderMetadata(properties);
        };

        BotReminderMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.requestMessageKey != null && $Object.hasOwnProperty.call(m, "requestMessageKey"))
                $root.proto.MessageKey.encode(m.requestMessageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.action != null && $Object.hasOwnProperty.call(m, "action"))
                w.uint32(16).int32(m.action);
            if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                w.uint32(26).string(m.name);
            if (m.nextTriggerTimestamp != null && $Object.hasOwnProperty.call(m, "nextTriggerTimestamp"))
                w.uint32(32).uint64(m.nextTriggerTimestamp);
            if (m.frequency != null && $Object.hasOwnProperty.call(m, "frequency"))
                w.uint32(40).int32(m.frequency);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotReminderMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotReminderMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.requestMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.requestMessageKey);
                        m._requestMessageKey = "requestMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.action = r.int32();
                        m._action = "action";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.name = r.stringVerify();
                        m._name = "name";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.nextTriggerTimestamp = r.uint64();
                        m._nextTriggerTimestamp = "nextTriggerTimestamp";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.frequency = r.int32();
                        m._frequency = "frequency";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotReminderMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotReminderMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotReminderMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotReminderMetadata();
            if (d.requestMessageKey != null) {
                if (!$util.isObject(d.requestMessageKey))
                    throw $TypeError(".proto.BotReminderMetadata.requestMessageKey: object expected");
                m.requestMessageKey = $root.proto.MessageKey.fromObject(d.requestMessageKey, q + 1);
            }
            switch (d.action) {
            case "NOTIFY":
            case 1:
                m.action = 1;
                break;
            case "CREATE":
            case 2:
                m.action = 2;
                break;
            case "DELETE":
            case 3:
                m.action = 3;
                break;
            case "UPDATE":
            case 4:
                m.action = 4;
                break;
            default:
                if (typeof d.action === "number" && (d.action | 0) === d.action)
                    m.action = d.action;
            }
            if (d.name != null) {
                m.name = $String(d.name);
            }
            if (d.nextTriggerTimestamp != null) {
                if ($util.Long)
                    m.nextTriggerTimestamp = $util.Long.fromValue(d.nextTriggerTimestamp, true);
                else if (typeof d.nextTriggerTimestamp === "string")
                    m.nextTriggerTimestamp = $parseInt(d.nextTriggerTimestamp, 10);
                else if (typeof d.nextTriggerTimestamp === "number")
                    m.nextTriggerTimestamp = d.nextTriggerTimestamp;
                else if (typeof d.nextTriggerTimestamp === "object")
                    m.nextTriggerTimestamp = new $util.LongBits(d.nextTriggerTimestamp.low >>> 0, d.nextTriggerTimestamp.high >>> 0).toNumber(true);
            }
            switch (d.frequency) {
            case "ONCE":
            case 1:
                m.frequency = 1;
                break;
            case "DAILY":
            case 2:
                m.frequency = 2;
                break;
            case "WEEKLY":
            case 3:
                m.frequency = 3;
                break;
            case "BIWEEKLY":
            case 4:
                m.frequency = 4;
                break;
            case "MONTHLY":
            case 5:
                m.frequency = 5;
                break;
            default:
                if (typeof d.frequency === "number" && (d.frequency | 0) === d.frequency)
                    m.frequency = d.frequency;
            }
            return m;
        };

        BotReminderMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.requestMessageKey != null && $Object.hasOwnProperty.call(m, "requestMessageKey")) {
                d.requestMessageKey = $root.proto.MessageKey.toObject(m.requestMessageKey, o, q + 1);
            }
            if (m.action != null && $Object.hasOwnProperty.call(m, "action")) {
                d.action = o.enums === $String ? $root.proto.BotReminderMetadata.ReminderAction[m.action] === $undefined ? m.action : $root.proto.BotReminderMetadata.ReminderAction[m.action] : m.action;
            }
            if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                d.name = m.name;
            }
            if (m.nextTriggerTimestamp != null && $Object.hasOwnProperty.call(m, "nextTriggerTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.nextTriggerTimestamp = typeof m.nextTriggerTimestamp === "number" ? $BigInt(m.nextTriggerTimestamp) : $util.Long.fromBits(m.nextTriggerTimestamp.low >>> 0, m.nextTriggerTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.nextTriggerTimestamp === "number")
                    d.nextTriggerTimestamp = o.longs === $String ? $String(m.nextTriggerTimestamp) : m.nextTriggerTimestamp;
                else
                    d.nextTriggerTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.nextTriggerTimestamp) : o.longs === $Number ? new $util.LongBits(m.nextTriggerTimestamp.low >>> 0, m.nextTriggerTimestamp.high >>> 0).toNumber(true) : m.nextTriggerTimestamp;
            }
            if (m.frequency != null && $Object.hasOwnProperty.call(m, "frequency")) {
                d.frequency = o.enums === $String ? $root.proto.BotReminderMetadata.ReminderFrequency[m.frequency] === $undefined ? m.frequency : $root.proto.BotReminderMetadata.ReminderFrequency[m.frequency] : m.frequency;
            }
            return d;
        };

        BotReminderMetadata.prototype.toJSON = function() {
            return BotReminderMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotReminderMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotReminderMetadata";
        };

        BotReminderMetadata.ReminderAction = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "NOTIFY"] = 1;
            values[valuesById[2] = "CREATE"] = 2;
            values[valuesById[3] = "DELETE"] = 3;
            values[valuesById[4] = "UPDATE"] = 4;
            return values;
        })();

        BotReminderMetadata.ReminderFrequency = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "ONCE"] = 1;
            values[valuesById[2] = "DAILY"] = 2;
            values[valuesById[3] = "WEEKLY"] = 3;
            values[valuesById[4] = "BIWEEKLY"] = 4;
            values[valuesById[5] = "MONTHLY"] = 5;
            return values;
        })();

        return BotReminderMetadata;
    })();

    proto.BotRenderingConfigMetadata = (function() {

        const BotRenderingConfigMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotRenderingConfigMetadata.prototype.bloksVersioningId = null;
        BotRenderingConfigMetadata.prototype.pixelDensity = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotRenderingConfigMetadata.prototype, "_bloksVersioningId", {
            get: $util.oneOfGetter($oneOfFields = ["bloksVersioningId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotRenderingConfigMetadata.prototype, "_pixelDensity", {
            get: $util.oneOfGetter($oneOfFields = ["pixelDensity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotRenderingConfigMetadata.create = function(properties) {
            return new BotRenderingConfigMetadata(properties);
        };

        BotRenderingConfigMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.bloksVersioningId != null && $Object.hasOwnProperty.call(m, "bloksVersioningId"))
                w.uint32(10).string(m.bloksVersioningId);
            if (m.pixelDensity != null && $Object.hasOwnProperty.call(m, "pixelDensity"))
                w.uint32(17).double(m.pixelDensity);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotRenderingConfigMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotRenderingConfigMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.bloksVersioningId = r.stringVerify();
                        m._bloksVersioningId = "bloksVersioningId";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        m.pixelDensity = r.double();
                        m._pixelDensity = "pixelDensity";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotRenderingConfigMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotRenderingConfigMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotRenderingConfigMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotRenderingConfigMetadata();
            if (d.bloksVersioningId != null) {
                m.bloksVersioningId = $String(d.bloksVersioningId);
            }
            if (d.pixelDensity != null) {
                m.pixelDensity = $Number(d.pixelDensity);
            }
            return m;
        };

        BotRenderingConfigMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.bloksVersioningId != null && $Object.hasOwnProperty.call(m, "bloksVersioningId")) {
                d.bloksVersioningId = m.bloksVersioningId;
            }
            if (m.pixelDensity != null && $Object.hasOwnProperty.call(m, "pixelDensity")) {
                d.pixelDensity = o.json && !$isFinite(m.pixelDensity) ? $String(m.pixelDensity) : m.pixelDensity;
            }
            return d;
        };

        BotRenderingConfigMetadata.prototype.toJSON = function() {
            return BotRenderingConfigMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotRenderingConfigMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotRenderingConfigMetadata";
        };

        return BotRenderingConfigMetadata;
    })();

    proto.BotRenderingMetadata = (function() {

        const BotRenderingMetadata = function (p) {
            this.keywords = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotRenderingMetadata.prototype.keywords = $util.emptyArray;

        BotRenderingMetadata.create = function(properties) {
            return new BotRenderingMetadata(properties);
        };

        BotRenderingMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.keywords != null && m.keywords.length) {
                for (var i = 0; i < m.keywords.length; ++i)
                    $root.proto.BotRenderingMetadata.Keyword.encode(m.keywords[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotRenderingMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotRenderingMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.keywords && m.keywords.length))
                            m.keywords = [];
                        m.keywords.push($root.proto.BotRenderingMetadata.Keyword.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotRenderingMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotRenderingMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotRenderingMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotRenderingMetadata();
            if (d.keywords) {
                if (!$Array.isArray(d.keywords))
                    throw $TypeError(".proto.BotRenderingMetadata.keywords: array expected");
                m.keywords = $Array(d.keywords.length);
                for (var i = 0; i < d.keywords.length; ++i) {
                    if (!$util.isObject(d.keywords[i]))
                        throw $TypeError(".proto.BotRenderingMetadata.keywords: object expected");
                    m.keywords[i] = $root.proto.BotRenderingMetadata.Keyword.fromObject(d.keywords[i], q + 1);
                }
            }
            return m;
        };

        BotRenderingMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.keywords = [];
            }
            if (m.keywords && m.keywords.length) {
                d.keywords = $Array(m.keywords.length);
                for (var j = 0; j < m.keywords.length; ++j) {
                    d.keywords[j] = $root.proto.BotRenderingMetadata.Keyword.toObject(m.keywords[j], o, q + 1);
                }
            }
            return d;
        };

        BotRenderingMetadata.prototype.toJSON = function() {
            return BotRenderingMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotRenderingMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotRenderingMetadata";
        };

        BotRenderingMetadata.Keyword = (function() {

            const Keyword = function (p) {
                this.associatedPrompts = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Keyword.prototype.value = null;
            Keyword.prototype.associatedPrompts = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Keyword.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Keyword.create = function(properties) {
                return new Keyword(properties);
            };

            Keyword.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                    w.uint32(10).string(m.value);
                if (m.associatedPrompts != null && m.associatedPrompts.length) {
                    for (var i = 0; i < m.associatedPrompts.length; ++i)
                        w.uint32(18).string(m.associatedPrompts[i]);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Keyword.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotRenderingMetadata.Keyword();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.value = r.stringVerify();
                            m._value = "value";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.associatedPrompts && m.associatedPrompts.length))
                                m.associatedPrompts = [];
                            m.associatedPrompts.push(r.stringVerify());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Keyword.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotRenderingMetadata.Keyword)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotRenderingMetadata.Keyword: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotRenderingMetadata.Keyword();
                if (d.value != null) {
                    m.value = $String(d.value);
                }
                if (d.associatedPrompts) {
                    if (!$Array.isArray(d.associatedPrompts))
                        throw $TypeError(".proto.BotRenderingMetadata.Keyword.associatedPrompts: array expected");
                    m.associatedPrompts = $Array(d.associatedPrompts.length);
                    for (var i = 0; i < d.associatedPrompts.length; ++i) {
                        m.associatedPrompts[i] = $String(d.associatedPrompts[i]);
                    }
                }
                return m;
            };

            Keyword.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.associatedPrompts = [];
                }
                if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                    d.value = m.value;
                }
                if (m.associatedPrompts && m.associatedPrompts.length) {
                    d.associatedPrompts = $Array(m.associatedPrompts.length);
                    for (var j = 0; j < m.associatedPrompts.length; ++j) {
                        d.associatedPrompts[j] = m.associatedPrompts[j];
                    }
                }
                return d;
            };

            Keyword.prototype.toJSON = function() {
                return Keyword.toObject(this, $protobuf.util.toJSONOptions);
            };

            Keyword.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotRenderingMetadata.Keyword";
            };

            return Keyword;
        })();

        return BotRenderingMetadata;
    })();

    proto.BotResolvedToolCallMetadata = (function() {

        const BotResolvedToolCallMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotResolvedToolCallMetadata.prototype.toolCallId = null;
        BotResolvedToolCallMetadata.prototype.resolutionDataSerialized = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotResolvedToolCallMetadata.prototype, "_toolCallId", {
            get: $util.oneOfGetter($oneOfFields = ["toolCallId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotResolvedToolCallMetadata.prototype, "_resolutionDataSerialized", {
            get: $util.oneOfGetter($oneOfFields = ["resolutionDataSerialized"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotResolvedToolCallMetadata.create = function(properties) {
            return new BotResolvedToolCallMetadata(properties);
        };

        BotResolvedToolCallMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.toolCallId != null && $Object.hasOwnProperty.call(m, "toolCallId"))
                w.uint32(10).string(m.toolCallId);
            if (m.resolutionDataSerialized != null && $Object.hasOwnProperty.call(m, "resolutionDataSerialized"))
                w.uint32(18).string(m.resolutionDataSerialized);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotResolvedToolCallMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotResolvedToolCallMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.toolCallId = r.stringVerify();
                        m._toolCallId = "toolCallId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.resolutionDataSerialized = r.stringVerify();
                        m._resolutionDataSerialized = "resolutionDataSerialized";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotResolvedToolCallMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotResolvedToolCallMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotResolvedToolCallMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotResolvedToolCallMetadata();
            if (d.toolCallId != null) {
                m.toolCallId = $String(d.toolCallId);
            }
            if (d.resolutionDataSerialized != null) {
                m.resolutionDataSerialized = $String(d.resolutionDataSerialized);
            }
            return m;
        };

        BotResolvedToolCallMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.toolCallId != null && $Object.hasOwnProperty.call(m, "toolCallId")) {
                d.toolCallId = m.toolCallId;
            }
            if (m.resolutionDataSerialized != null && $Object.hasOwnProperty.call(m, "resolutionDataSerialized")) {
                d.resolutionDataSerialized = m.resolutionDataSerialized;
            }
            return d;
        };

        BotResolvedToolCallMetadata.prototype.toJSON = function() {
            return BotResolvedToolCallMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotResolvedToolCallMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotResolvedToolCallMetadata";
        };

        return BotResolvedToolCallMetadata;
    })();

    proto.BotSessionMetadata = (function() {

        const BotSessionMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotSessionMetadata.prototype.sessionId = null;
        BotSessionMetadata.prototype.sessionSource = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSessionMetadata.prototype, "_sessionId", {
            get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSessionMetadata.prototype, "_sessionSource", {
            get: $util.oneOfGetter($oneOfFields = ["sessionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotSessionMetadata.create = function(properties) {
            return new BotSessionMetadata(properties);
        };

        BotSessionMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId"))
                w.uint32(10).string(m.sessionId);
            if (m.sessionSource != null && $Object.hasOwnProperty.call(m, "sessionSource"))
                w.uint32(16).int32(m.sessionSource);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotSessionMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSessionMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.sessionId = r.stringVerify();
                        m._sessionId = "sessionId";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.sessionSource = r.int32();
                        m._sessionSource = "sessionSource";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotSessionMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotSessionMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotSessionMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotSessionMetadata();
            if (d.sessionId != null) {
                m.sessionId = $String(d.sessionId);
            }
            switch (d.sessionSource) {
            case "NONE":
            case 0:
                m.sessionSource = 0;
                break;
            case "NULL_STATE":
            case 1:
                m.sessionSource = 1;
                break;
            case "TYPEAHEAD":
            case 2:
                m.sessionSource = 2;
                break;
            case "USER_INPUT":
            case 3:
                m.sessionSource = 3;
                break;
            case "EMU_FLASH":
            case 4:
                m.sessionSource = 4;
                break;
            case "EMU_FLASH_FOLLOWUP":
            case 5:
                m.sessionSource = 5;
                break;
            case "VOICE":
            case 6:
                m.sessionSource = 6;
                break;
            case "AI_HOME_SESSION":
            case 7:
                m.sessionSource = 7;
                break;
            default:
                if (typeof d.sessionSource === "number" && (d.sessionSource | 0) === d.sessionSource)
                    m.sessionSource = d.sessionSource;
            }
            return m;
        };

        BotSessionMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId")) {
                d.sessionId = m.sessionId;
            }
            if (m.sessionSource != null && $Object.hasOwnProperty.call(m, "sessionSource")) {
                d.sessionSource = o.enums === $String ? $root.proto.BotSessionSource[m.sessionSource] === $undefined ? m.sessionSource : $root.proto.BotSessionSource[m.sessionSource] : m.sessionSource;
            }
            return d;
        };

        BotSessionMetadata.prototype.toJSON = function() {
            return BotSessionMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSessionMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotSessionMetadata";
        };

        return BotSessionMetadata;
    })();

    proto.BotSessionSource = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "NONE"] = 0;
        values[valuesById[1] = "NULL_STATE"] = 1;
        values[valuesById[2] = "TYPEAHEAD"] = 2;
        values[valuesById[3] = "USER_INPUT"] = 3;
        values[valuesById[4] = "EMU_FLASH"] = 4;
        values[valuesById[5] = "EMU_FLASH_FOLLOWUP"] = 5;
        values[valuesById[6] = "VOICE"] = 6;
        values[valuesById[7] = "AI_HOME_SESSION"] = 7;
        return values;
    })();

    proto.BotSignatureVerificationMetadata = (function() {

        const BotSignatureVerificationMetadata = function (p) {
            this.proofs = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotSignatureVerificationMetadata.prototype.proofs = $util.emptyArray;

        BotSignatureVerificationMetadata.create = function(properties) {
            return new BotSignatureVerificationMetadata(properties);
        };

        BotSignatureVerificationMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.proofs != null && m.proofs.length) {
                for (var i = 0; i < m.proofs.length; ++i)
                    $root.proto.BotSignatureVerificationUseCaseProof.encode(m.proofs[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotSignatureVerificationMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSignatureVerificationMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.proofs && m.proofs.length))
                            m.proofs = [];
                        m.proofs.push($root.proto.BotSignatureVerificationUseCaseProof.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotSignatureVerificationMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotSignatureVerificationMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotSignatureVerificationMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotSignatureVerificationMetadata();
            if (d.proofs) {
                if (!$Array.isArray(d.proofs))
                    throw $TypeError(".proto.BotSignatureVerificationMetadata.proofs: array expected");
                m.proofs = $Array(d.proofs.length);
                for (var i = 0; i < d.proofs.length; ++i) {
                    if (!$util.isObject(d.proofs[i]))
                        throw $TypeError(".proto.BotSignatureVerificationMetadata.proofs: object expected");
                    m.proofs[i] = $root.proto.BotSignatureVerificationUseCaseProof.fromObject(d.proofs[i], q + 1);
                }
            }
            return m;
        };

        BotSignatureVerificationMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.proofs = [];
            }
            if (m.proofs && m.proofs.length) {
                d.proofs = $Array(m.proofs.length);
                for (var j = 0; j < m.proofs.length; ++j) {
                    d.proofs[j] = $root.proto.BotSignatureVerificationUseCaseProof.toObject(m.proofs[j], o, q + 1);
                }
            }
            return d;
        };

        BotSignatureVerificationMetadata.prototype.toJSON = function() {
            return BotSignatureVerificationMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSignatureVerificationMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotSignatureVerificationMetadata";
        };

        return BotSignatureVerificationMetadata;
    })();

    proto.BotSignatureVerificationUseCaseProof = (function() {

        const BotSignatureVerificationUseCaseProof = function (p) {
            this.certificateChain = [];
            this.certificateChainSki = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotSignatureVerificationUseCaseProof.prototype.version = null;
        BotSignatureVerificationUseCaseProof.prototype.useCase = null;
        BotSignatureVerificationUseCaseProof.prototype.signature = null;
        BotSignatureVerificationUseCaseProof.prototype.certificateChain = $util.emptyArray;
        BotSignatureVerificationUseCaseProof.prototype.certificateChainSki = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_useCase", {
            get: $util.oneOfGetter($oneOfFields = ["useCase"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSignatureVerificationUseCaseProof.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotSignatureVerificationUseCaseProof.create = function(properties) {
            return new BotSignatureVerificationUseCaseProof(properties);
        };

        BotSignatureVerificationUseCaseProof.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(8).int32(m.version);
            if (m.useCase != null && $Object.hasOwnProperty.call(m, "useCase"))
                w.uint32(16).int32(m.useCase);
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature"))
                w.uint32(26).bytes(m.signature);
            if (m.certificateChain != null && m.certificateChain.length) {
                for (var i = 0; i < m.certificateChain.length; ++i)
                    w.uint32(34).bytes(m.certificateChain[i]);
            }
            if (m.certificateChainSki != null && m.certificateChainSki.length) {
                for (var i = 0; i < m.certificateChainSki.length; ++i)
                    $root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI.encode(m.certificateChainSki[i], w.uint32(42).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotSignatureVerificationUseCaseProof.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSignatureVerificationUseCaseProof(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.version = r.int32();
                        m._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.useCase = r.int32();
                        m._useCase = "useCase";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.signature = r.bytes();
                        m._signature = "signature";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(m.certificateChain && m.certificateChain.length))
                            m.certificateChain = [];
                        m.certificateChain.push(r.bytes());
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(m.certificateChainSki && m.certificateChainSki.length))
                            m.certificateChainSki = [];
                        m.certificateChainSki.push($root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotSignatureVerificationUseCaseProof.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotSignatureVerificationUseCaseProof)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotSignatureVerificationUseCaseProof: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotSignatureVerificationUseCaseProof();
            if (d.version != null) {
                m.version = d.version | 0;
            }
            switch (d.useCase) {
            case "UNSPECIFIED":
            case 0:
                m.useCase = 0;
                break;
            case "WA_BOT_MSG":
            case 1:
                m.useCase = 1;
                break;
            case "WA_TEE_BOT_MSG":
            case 2:
                m.useCase = 2;
                break;
            case "P2P_PILLS":
            case 3:
                m.useCase = 3;
                break;
            case "WA_WAFFLE":
            case 4:
                m.useCase = 4;
                break;
            case "WA_FEATURE_PKI":
            case 5:
                m.useCase = 5;
                break;
            default:
                if (typeof d.useCase === "number" && (d.useCase | 0) === d.useCase)
                    m.useCase = d.useCase;
            }
            if (d.signature != null) {
                if (typeof d.signature === "string")
                    $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                else if (d.signature.length >= 0)
                    m.signature = d.signature;
            }
            if (d.certificateChain) {
                if (!$Array.isArray(d.certificateChain))
                    throw $TypeError(".proto.BotSignatureVerificationUseCaseProof.certificateChain: array expected");
                m.certificateChain = $Array(d.certificateChain.length);
                for (var i = 0; i < d.certificateChain.length; ++i) {
                    if (typeof d.certificateChain[i] === "string")
                        $util.base64.decode(d.certificateChain[i], m.certificateChain[i] = $util.newBuffer($util.base64.length(d.certificateChain[i])), 0);
                    else if (d.certificateChain[i].length >= 0)
                        m.certificateChain[i] = d.certificateChain[i];
                }
            }
            if (d.certificateChainSki) {
                if (!$Array.isArray(d.certificateChainSki))
                    throw $TypeError(".proto.BotSignatureVerificationUseCaseProof.certificateChainSki: array expected");
                m.certificateChainSki = $Array(d.certificateChainSki.length);
                for (var i = 0; i < d.certificateChainSki.length; ++i) {
                    if (!$util.isObject(d.certificateChainSki[i]))
                        throw $TypeError(".proto.BotSignatureVerificationUseCaseProof.certificateChainSki: object expected");
                    m.certificateChainSki[i] = $root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI.fromObject(d.certificateChainSki[i], q + 1);
                }
            }
            return m;
        };

        BotSignatureVerificationUseCaseProof.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.certificateChain = [];
                d.certificateChainSki = [];
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = m.version;
            }
            if (m.useCase != null && $Object.hasOwnProperty.call(m, "useCase")) {
                d.useCase = o.enums === $String ? $root.proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[m.useCase] === $undefined ? m.useCase : $root.proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[m.useCase] : m.useCase;
            }
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature")) {
                d.signature = o.bytes === $String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signature) : m.signature;
            }
            if (m.certificateChain && m.certificateChain.length) {
                d.certificateChain = $Array(m.certificateChain.length);
                for (var j = 0; j < m.certificateChain.length; ++j) {
                    d.certificateChain[j] = o.bytes === $String ? $util.base64.encode(m.certificateChain[j], 0, m.certificateChain[j].length) : o.bytes === $Array ? $Array.prototype.slice.call(m.certificateChain[j]) : m.certificateChain[j];
                }
            }
            if (m.certificateChainSki && m.certificateChainSki.length) {
                d.certificateChainSki = $Array(m.certificateChainSki.length);
                for (var j = 0; j < m.certificateChainSki.length; ++j) {
                    d.certificateChainSki[j] = $root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI.toObject(m.certificateChainSki[j], o, q + 1);
                }
            }
            return d;
        };

        BotSignatureVerificationUseCaseProof.prototype.toJSON = function() {
            return BotSignatureVerificationUseCaseProof.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSignatureVerificationUseCaseProof.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotSignatureVerificationUseCaseProof";
        };

        BotSignatureVerificationUseCaseProof.BotSignatureUseCase = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNSPECIFIED"] = 0;
            values[valuesById[1] = "WA_BOT_MSG"] = 1;
            values[valuesById[2] = "WA_TEE_BOT_MSG"] = 2;
            values[valuesById[3] = "P2P_PILLS"] = 3;
            values[valuesById[4] = "WA_WAFFLE"] = 4;
            values[valuesById[5] = "WA_FEATURE_PKI"] = 5;
            return values;
        })();

        BotSignatureVerificationUseCaseProof.CertificateSKI = (function() {

            const CertificateSKI = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CertificateSKI.prototype.useCase = null;
            CertificateSKI.prototype.ski = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CertificateSKI.prototype, "_useCase", {
                get: $util.oneOfGetter($oneOfFields = ["useCase"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CertificateSKI.prototype, "_ski", {
                get: $util.oneOfGetter($oneOfFields = ["ski"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CertificateSKI.create = function(properties) {
                return new CertificateSKI(properties);
            };

            CertificateSKI.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.useCase != null && $Object.hasOwnProperty.call(m, "useCase"))
                    w.uint32(8).int32(m.useCase);
                if (m.ski != null && $Object.hasOwnProperty.call(m, "ski"))
                    w.uint32(18).bytes(m.ski);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CertificateSKI.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.useCase = r.int32();
                            m._useCase = "useCase";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.ski = r.bytes();
                            m._ski = "ski";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CertificateSKI.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotSignatureVerificationUseCaseProof.CertificateSKI: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotSignatureVerificationUseCaseProof.CertificateSKI();
                switch (d.useCase) {
                case "UNSPECIFIED":
                case 0:
                    m.useCase = 0;
                    break;
                case "WA_BOT_MSG":
                case 1:
                    m.useCase = 1;
                    break;
                case "WA_TEE_BOT_MSG":
                case 2:
                    m.useCase = 2;
                    break;
                case "P2P_PILLS":
                case 3:
                    m.useCase = 3;
                    break;
                case "WA_WAFFLE":
                case 4:
                    m.useCase = 4;
                    break;
                case "WA_FEATURE_PKI":
                case 5:
                    m.useCase = 5;
                    break;
                default:
                    if (typeof d.useCase === "number" && (d.useCase | 0) === d.useCase)
                        m.useCase = d.useCase;
                }
                if (d.ski != null) {
                    if (typeof d.ski === "string")
                        $util.base64.decode(d.ski, m.ski = $util.newBuffer($util.base64.length(d.ski)), 0);
                    else if (d.ski.length >= 0)
                        m.ski = d.ski;
                }
                return m;
            };

            CertificateSKI.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.useCase != null && $Object.hasOwnProperty.call(m, "useCase")) {
                    d.useCase = o.enums === $String ? $root.proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[m.useCase] === $undefined ? m.useCase : $root.proto.BotSignatureVerificationUseCaseProof.BotSignatureUseCase[m.useCase] : m.useCase;
                }
                if (m.ski != null && $Object.hasOwnProperty.call(m, "ski")) {
                    d.ski = o.bytes === $String ? $util.base64.encode(m.ski, 0, m.ski.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ski) : m.ski;
                }
                return d;
            };

            CertificateSKI.prototype.toJSON = function() {
                return CertificateSKI.toObject(this, $protobuf.util.toJSONOptions);
            };

            CertificateSKI.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotSignatureVerificationUseCaseProof.CertificateSKI";
            };

            return CertificateSKI;
        })();

        return BotSignatureVerificationUseCaseProof;
    })();

    proto.BotSourcesMetadata = (function() {

        const BotSourcesMetadata = function (p) {
            this.sources = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotSourcesMetadata.prototype.sources = $util.emptyArray;

        BotSourcesMetadata.create = function(properties) {
            return new BotSourcesMetadata(properties);
        };

        BotSourcesMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.sources != null && m.sources.length) {
                for (var i = 0; i < m.sources.length; ++i)
                    $root.proto.BotSourcesMetadata.BotSourceItem.encode(m.sources[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotSourcesMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSourcesMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.sources && m.sources.length))
                            m.sources = [];
                        m.sources.push($root.proto.BotSourcesMetadata.BotSourceItem.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotSourcesMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotSourcesMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotSourcesMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotSourcesMetadata();
            if (d.sources) {
                if (!$Array.isArray(d.sources))
                    throw $TypeError(".proto.BotSourcesMetadata.sources: array expected");
                m.sources = $Array(d.sources.length);
                for (var i = 0; i < d.sources.length; ++i) {
                    if (!$util.isObject(d.sources[i]))
                        throw $TypeError(".proto.BotSourcesMetadata.sources: object expected");
                    m.sources[i] = $root.proto.BotSourcesMetadata.BotSourceItem.fromObject(d.sources[i], q + 1);
                }
            }
            return m;
        };

        BotSourcesMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.sources = [];
            }
            if (m.sources && m.sources.length) {
                d.sources = $Array(m.sources.length);
                for (var j = 0; j < m.sources.length; ++j) {
                    d.sources[j] = $root.proto.BotSourcesMetadata.BotSourceItem.toObject(m.sources[j], o, q + 1);
                }
            }
            return d;
        };

        BotSourcesMetadata.prototype.toJSON = function() {
            return BotSourcesMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSourcesMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotSourcesMetadata";
        };

        BotSourcesMetadata.BotSourceItem = (function() {

            const BotSourceItem = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BotSourceItem.prototype.provider = null;
            BotSourceItem.prototype.thumbnailCdnUrl = null;
            BotSourceItem.prototype.sourceProviderUrl = null;
            BotSourceItem.prototype.sourceQuery = null;
            BotSourceItem.prototype.faviconCdnUrl = null;
            BotSourceItem.prototype.citationNumber = null;
            BotSourceItem.prototype.sourceTitle = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_provider", {
                get: $util.oneOfGetter($oneOfFields = ["provider"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_thumbnailCdnUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailCdnUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_sourceProviderUrl", {
                get: $util.oneOfGetter($oneOfFields = ["sourceProviderUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_sourceQuery", {
                get: $util.oneOfGetter($oneOfFields = ["sourceQuery"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_faviconCdnUrl", {
                get: $util.oneOfGetter($oneOfFields = ["faviconCdnUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_citationNumber", {
                get: $util.oneOfGetter($oneOfFields = ["citationNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotSourceItem.prototype, "_sourceTitle", {
                get: $util.oneOfGetter($oneOfFields = ["sourceTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotSourceItem.create = function(properties) {
                return new BotSourceItem(properties);
            };

            BotSourceItem.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.provider != null && $Object.hasOwnProperty.call(m, "provider"))
                    w.uint32(8).int32(m.provider);
                if (m.thumbnailCdnUrl != null && $Object.hasOwnProperty.call(m, "thumbnailCdnUrl"))
                    w.uint32(18).string(m.thumbnailCdnUrl);
                if (m.sourceProviderUrl != null && $Object.hasOwnProperty.call(m, "sourceProviderUrl"))
                    w.uint32(26).string(m.sourceProviderUrl);
                if (m.sourceQuery != null && $Object.hasOwnProperty.call(m, "sourceQuery"))
                    w.uint32(34).string(m.sourceQuery);
                if (m.faviconCdnUrl != null && $Object.hasOwnProperty.call(m, "faviconCdnUrl"))
                    w.uint32(42).string(m.faviconCdnUrl);
                if (m.citationNumber != null && $Object.hasOwnProperty.call(m, "citationNumber"))
                    w.uint32(48).uint32(m.citationNumber);
                if (m.sourceTitle != null && $Object.hasOwnProperty.call(m, "sourceTitle"))
                    w.uint32(58).string(m.sourceTitle);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BotSourceItem.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSourcesMetadata.BotSourceItem(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.provider = r.int32();
                            m._provider = "provider";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.thumbnailCdnUrl = r.stringVerify();
                            m._thumbnailCdnUrl = "thumbnailCdnUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.sourceProviderUrl = r.stringVerify();
                            m._sourceProviderUrl = "sourceProviderUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.sourceQuery = r.stringVerify();
                            m._sourceQuery = "sourceQuery";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.faviconCdnUrl = r.stringVerify();
                            m._faviconCdnUrl = "faviconCdnUrl";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.citationNumber = r.uint32();
                            m._citationNumber = "citationNumber";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.sourceTitle = r.stringVerify();
                            m._sourceTitle = "sourceTitle";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BotSourceItem.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotSourcesMetadata.BotSourceItem)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotSourcesMetadata.BotSourceItem: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotSourcesMetadata.BotSourceItem();
                switch (d.provider) {
                case "UNKNOWN":
                case 0:
                    m.provider = 0;
                    break;
                case "BING":
                case 1:
                    m.provider = 1;
                    break;
                case "GOOGLE":
                case 2:
                    m.provider = 2;
                    break;
                case "SUPPORT":
                case 3:
                    m.provider = 3;
                    break;
                case "OTHER":
                case 4:
                    m.provider = 4;
                    break;
                default:
                    if (typeof d.provider === "number" && (d.provider | 0) === d.provider)
                        m.provider = d.provider;
                }
                if (d.thumbnailCdnUrl != null) {
                    m.thumbnailCdnUrl = $String(d.thumbnailCdnUrl);
                }
                if (d.sourceProviderUrl != null) {
                    m.sourceProviderUrl = $String(d.sourceProviderUrl);
                }
                if (d.sourceQuery != null) {
                    m.sourceQuery = $String(d.sourceQuery);
                }
                if (d.faviconCdnUrl != null) {
                    m.faviconCdnUrl = $String(d.faviconCdnUrl);
                }
                if (d.citationNumber != null) {
                    m.citationNumber = d.citationNumber >>> 0;
                }
                if (d.sourceTitle != null) {
                    m.sourceTitle = $String(d.sourceTitle);
                }
                return m;
            };

            BotSourceItem.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.provider != null && $Object.hasOwnProperty.call(m, "provider")) {
                    d.provider = o.enums === $String ? $root.proto.BotSourcesMetadata.BotSourceItem.SourceProvider[m.provider] === $undefined ? m.provider : $root.proto.BotSourcesMetadata.BotSourceItem.SourceProvider[m.provider] : m.provider;
                }
                if (m.thumbnailCdnUrl != null && $Object.hasOwnProperty.call(m, "thumbnailCdnUrl")) {
                    d.thumbnailCdnUrl = m.thumbnailCdnUrl;
                }
                if (m.sourceProviderUrl != null && $Object.hasOwnProperty.call(m, "sourceProviderUrl")) {
                    d.sourceProviderUrl = m.sourceProviderUrl;
                }
                if (m.sourceQuery != null && $Object.hasOwnProperty.call(m, "sourceQuery")) {
                    d.sourceQuery = m.sourceQuery;
                }
                if (m.faviconCdnUrl != null && $Object.hasOwnProperty.call(m, "faviconCdnUrl")) {
                    d.faviconCdnUrl = m.faviconCdnUrl;
                }
                if (m.citationNumber != null && $Object.hasOwnProperty.call(m, "citationNumber")) {
                    d.citationNumber = m.citationNumber;
                }
                if (m.sourceTitle != null && $Object.hasOwnProperty.call(m, "sourceTitle")) {
                    d.sourceTitle = m.sourceTitle;
                }
                return d;
            };

            BotSourceItem.prototype.toJSON = function() {
                return BotSourceItem.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotSourceItem.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotSourcesMetadata.BotSourceItem";
            };

            BotSourceItem.SourceProvider = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "BING"] = 1;
                values[valuesById[2] = "GOOGLE"] = 2;
                values[valuesById[3] = "SUPPORT"] = 3;
                values[valuesById[4] = "OTHER"] = 4;
                return values;
            })();

            return BotSourceItem;
        })();

        return BotSourcesMetadata;
    })();

    proto.BotSuggestedPromptMetadata = (function() {

        const BotSuggestedPromptMetadata = function (p) {
            this.suggestedPrompts = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotSuggestedPromptMetadata.prototype.suggestedPrompts = $util.emptyArray;
        BotSuggestedPromptMetadata.prototype.selectedPromptIndex = null;
        BotSuggestedPromptMetadata.prototype.promptSuggestions = null;
        BotSuggestedPromptMetadata.prototype.selectedPromptId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_selectedPromptIndex", {
            get: $util.oneOfGetter($oneOfFields = ["selectedPromptIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_promptSuggestions", {
            get: $util.oneOfGetter($oneOfFields = ["promptSuggestions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotSuggestedPromptMetadata.prototype, "_selectedPromptId", {
            get: $util.oneOfGetter($oneOfFields = ["selectedPromptId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotSuggestedPromptMetadata.create = function(properties) {
            return new BotSuggestedPromptMetadata(properties);
        };

        BotSuggestedPromptMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.suggestedPrompts != null && m.suggestedPrompts.length) {
                for (var i = 0; i < m.suggestedPrompts.length; ++i)
                    w.uint32(10).string(m.suggestedPrompts[i]);
            }
            if (m.selectedPromptIndex != null && $Object.hasOwnProperty.call(m, "selectedPromptIndex"))
                w.uint32(16).uint32(m.selectedPromptIndex);
            if (m.promptSuggestions != null && $Object.hasOwnProperty.call(m, "promptSuggestions"))
                $root.proto.BotPromptSuggestions.encode(m.promptSuggestions, w.uint32(26).fork(), q + 1).ldelim();
            if (m.selectedPromptId != null && $Object.hasOwnProperty.call(m, "selectedPromptId"))
                w.uint32(34).string(m.selectedPromptId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotSuggestedPromptMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotSuggestedPromptMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.suggestedPrompts && m.suggestedPrompts.length))
                            m.suggestedPrompts = [];
                        m.suggestedPrompts.push(r.stringVerify());
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.selectedPromptIndex = r.uint32();
                        m._selectedPromptIndex = "selectedPromptIndex";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.promptSuggestions = $root.proto.BotPromptSuggestions.decode(r, r.uint32(), $undefined, q + 1, m.promptSuggestions);
                        m._promptSuggestions = "promptSuggestions";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.selectedPromptId = r.stringVerify();
                        m._selectedPromptId = "selectedPromptId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotSuggestedPromptMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotSuggestedPromptMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotSuggestedPromptMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotSuggestedPromptMetadata();
            if (d.suggestedPrompts) {
                if (!$Array.isArray(d.suggestedPrompts))
                    throw $TypeError(".proto.BotSuggestedPromptMetadata.suggestedPrompts: array expected");
                m.suggestedPrompts = $Array(d.suggestedPrompts.length);
                for (var i = 0; i < d.suggestedPrompts.length; ++i) {
                    m.suggestedPrompts[i] = $String(d.suggestedPrompts[i]);
                }
            }
            if (d.selectedPromptIndex != null) {
                m.selectedPromptIndex = d.selectedPromptIndex >>> 0;
            }
            if (d.promptSuggestions != null) {
                if (!$util.isObject(d.promptSuggestions))
                    throw $TypeError(".proto.BotSuggestedPromptMetadata.promptSuggestions: object expected");
                m.promptSuggestions = $root.proto.BotPromptSuggestions.fromObject(d.promptSuggestions, q + 1);
            }
            if (d.selectedPromptId != null) {
                m.selectedPromptId = $String(d.selectedPromptId);
            }
            return m;
        };

        BotSuggestedPromptMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.suggestedPrompts = [];
            }
            if (m.suggestedPrompts && m.suggestedPrompts.length) {
                d.suggestedPrompts = $Array(m.suggestedPrompts.length);
                for (var j = 0; j < m.suggestedPrompts.length; ++j) {
                    d.suggestedPrompts[j] = m.suggestedPrompts[j];
                }
            }
            if (m.selectedPromptIndex != null && $Object.hasOwnProperty.call(m, "selectedPromptIndex")) {
                d.selectedPromptIndex = m.selectedPromptIndex;
            }
            if (m.promptSuggestions != null && $Object.hasOwnProperty.call(m, "promptSuggestions")) {
                d.promptSuggestions = $root.proto.BotPromptSuggestions.toObject(m.promptSuggestions, o, q + 1);
            }
            if (m.selectedPromptId != null && $Object.hasOwnProperty.call(m, "selectedPromptId")) {
                d.selectedPromptId = m.selectedPromptId;
            }
            return d;
        };

        BotSuggestedPromptMetadata.prototype.toJSON = function() {
            return BotSuggestedPromptMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotSuggestedPromptMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotSuggestedPromptMetadata";
        };

        return BotSuggestedPromptMetadata;
    })();

    proto.BotUnifiedResponseMutation = (function() {

        const BotUnifiedResponseMutation = function (p) {
            this.mediaDetailsMetadataList = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        BotUnifiedResponseMutation.prototype.sbsMetadata = null;
        BotUnifiedResponseMutation.prototype.mediaDetailsMetadataList = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(BotUnifiedResponseMutation.prototype, "_sbsMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["sbsMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        BotUnifiedResponseMutation.create = function(properties) {
            return new BotUnifiedResponseMutation(properties);
        };

        BotUnifiedResponseMutation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.sbsMetadata != null && $Object.hasOwnProperty.call(m, "sbsMetadata"))
                $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.encode(m.sbsMetadata, w.uint32(10).fork(), q + 1).ldelim();
            if (m.mediaDetailsMetadataList != null && m.mediaDetailsMetadataList.length) {
                for (var i = 0; i < m.mediaDetailsMetadataList.length; ++i)
                    $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.encode(m.mediaDetailsMetadataList[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        BotUnifiedResponseMutation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotUnifiedResponseMutation();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.sbsMetadata = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.decode(r, r.uint32(), $undefined, q + 1, m.sbsMetadata);
                        m._sbsMetadata = "sbsMetadata";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.mediaDetailsMetadataList && m.mediaDetailsMetadataList.length))
                            m.mediaDetailsMetadataList = [];
                        m.mediaDetailsMetadataList.push($root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        BotUnifiedResponseMutation.fromObject = function (d, q) {
            if (d instanceof $root.proto.BotUnifiedResponseMutation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.BotUnifiedResponseMutation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.BotUnifiedResponseMutation();
            if (d.sbsMetadata != null) {
                if (!$util.isObject(d.sbsMetadata))
                    throw $TypeError(".proto.BotUnifiedResponseMutation.sbsMetadata: object expected");
                m.sbsMetadata = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.fromObject(d.sbsMetadata, q + 1);
            }
            if (d.mediaDetailsMetadataList) {
                if (!$Array.isArray(d.mediaDetailsMetadataList))
                    throw $TypeError(".proto.BotUnifiedResponseMutation.mediaDetailsMetadataList: array expected");
                m.mediaDetailsMetadataList = $Array(d.mediaDetailsMetadataList.length);
                for (var i = 0; i < d.mediaDetailsMetadataList.length; ++i) {
                    if (!$util.isObject(d.mediaDetailsMetadataList[i]))
                        throw $TypeError(".proto.BotUnifiedResponseMutation.mediaDetailsMetadataList: object expected");
                    m.mediaDetailsMetadataList[i] = $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.fromObject(d.mediaDetailsMetadataList[i], q + 1);
                }
            }
            return m;
        };

        BotUnifiedResponseMutation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mediaDetailsMetadataList = [];
            }
            if (m.sbsMetadata != null && $Object.hasOwnProperty.call(m, "sbsMetadata")) {
                d.sbsMetadata = $root.proto.BotUnifiedResponseMutation.SideBySideMetadata.toObject(m.sbsMetadata, o, q + 1);
            }
            if (m.mediaDetailsMetadataList && m.mediaDetailsMetadataList.length) {
                d.mediaDetailsMetadataList = $Array(m.mediaDetailsMetadataList.length);
                for (var j = 0; j < m.mediaDetailsMetadataList.length; ++j) {
                    d.mediaDetailsMetadataList[j] = $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata.toObject(m.mediaDetailsMetadataList[j], o, q + 1);
                }
            }
            return d;
        };

        BotUnifiedResponseMutation.prototype.toJSON = function() {
            return BotUnifiedResponseMutation.toObject(this, $protobuf.util.toJSONOptions);
        };

        BotUnifiedResponseMutation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.BotUnifiedResponseMutation";
        };

        BotUnifiedResponseMutation.MediaDetailsMetadata = (function() {

            const MediaDetailsMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MediaDetailsMetadata.prototype.id = null;
            MediaDetailsMetadata.prototype.highResMedia = null;
            MediaDetailsMetadata.prototype.previewMedia = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaDetailsMetadata.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaDetailsMetadata.prototype, "_highResMedia", {
                get: $util.oneOfGetter($oneOfFields = ["highResMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaDetailsMetadata.prototype, "_previewMedia", {
                get: $util.oneOfGetter($oneOfFields = ["previewMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MediaDetailsMetadata.create = function(properties) {
                return new MediaDetailsMetadata(properties);
            };

            MediaDetailsMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                    w.uint32(10).string(m.id);
                if (m.highResMedia != null && $Object.hasOwnProperty.call(m, "highResMedia"))
                    $root.proto.BotMediaMetadata.encode(m.highResMedia, w.uint32(18).fork(), q + 1).ldelim();
                if (m.previewMedia != null && $Object.hasOwnProperty.call(m, "previewMedia"))
                    $root.proto.BotMediaMetadata.encode(m.previewMedia, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MediaDetailsMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.id = r.stringVerify();
                            m._id = "id";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.highResMedia = $root.proto.BotMediaMetadata.decode(r, r.uint32(), $undefined, q + 1, m.highResMedia);
                            m._highResMedia = "highResMedia";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.previewMedia = $root.proto.BotMediaMetadata.decode(r, r.uint32(), $undefined, q + 1, m.previewMedia);
                            m._previewMedia = "previewMedia";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MediaDetailsMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotUnifiedResponseMutation.MediaDetailsMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotUnifiedResponseMutation.MediaDetailsMetadata();
                if (d.id != null) {
                    m.id = $String(d.id);
                }
                if (d.highResMedia != null) {
                    if (!$util.isObject(d.highResMedia))
                        throw $TypeError(".proto.BotUnifiedResponseMutation.MediaDetailsMetadata.highResMedia: object expected");
                    m.highResMedia = $root.proto.BotMediaMetadata.fromObject(d.highResMedia, q + 1);
                }
                if (d.previewMedia != null) {
                    if (!$util.isObject(d.previewMedia))
                        throw $TypeError(".proto.BotUnifiedResponseMutation.MediaDetailsMetadata.previewMedia: object expected");
                    m.previewMedia = $root.proto.BotMediaMetadata.fromObject(d.previewMedia, q + 1);
                }
                return m;
            };

            MediaDetailsMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                    d.id = m.id;
                }
                if (m.highResMedia != null && $Object.hasOwnProperty.call(m, "highResMedia")) {
                    d.highResMedia = $root.proto.BotMediaMetadata.toObject(m.highResMedia, o, q + 1);
                }
                if (m.previewMedia != null && $Object.hasOwnProperty.call(m, "previewMedia")) {
                    d.previewMedia = $root.proto.BotMediaMetadata.toObject(m.previewMedia, o, q + 1);
                }
                return d;
            };

            MediaDetailsMetadata.prototype.toJSON = function() {
                return MediaDetailsMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            MediaDetailsMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotUnifiedResponseMutation.MediaDetailsMetadata";
            };

            return MediaDetailsMetadata;
        })();

        BotUnifiedResponseMutation.SideBySideMetadata = (function() {

            const SideBySideMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SideBySideMetadata.prototype.primaryResponseId = null;
            SideBySideMetadata.prototype.surveyCtaHasRendered = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideMetadata.prototype, "_primaryResponseId", {
                get: $util.oneOfGetter($oneOfFields = ["primaryResponseId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SideBySideMetadata.prototype, "_surveyCtaHasRendered", {
                get: $util.oneOfGetter($oneOfFields = ["surveyCtaHasRendered"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SideBySideMetadata.create = function(properties) {
                return new SideBySideMetadata(properties);
            };

            SideBySideMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.primaryResponseId != null && $Object.hasOwnProperty.call(m, "primaryResponseId"))
                    w.uint32(10).string(m.primaryResponseId);
                if (m.surveyCtaHasRendered != null && $Object.hasOwnProperty.call(m, "surveyCtaHasRendered"))
                    w.uint32(16).bool(m.surveyCtaHasRendered);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SideBySideMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.BotUnifiedResponseMutation.SideBySideMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.primaryResponseId = r.stringVerify();
                            m._primaryResponseId = "primaryResponseId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.surveyCtaHasRendered = r.bool();
                            m._surveyCtaHasRendered = "surveyCtaHasRendered";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SideBySideMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.BotUnifiedResponseMutation.SideBySideMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.BotUnifiedResponseMutation.SideBySideMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.BotUnifiedResponseMutation.SideBySideMetadata();
                if (d.primaryResponseId != null) {
                    m.primaryResponseId = $String(d.primaryResponseId);
                }
                if (d.surveyCtaHasRendered != null) {
                    m.surveyCtaHasRendered = $Boolean(d.surveyCtaHasRendered);
                }
                return m;
            };

            SideBySideMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.primaryResponseId != null && $Object.hasOwnProperty.call(m, "primaryResponseId")) {
                    d.primaryResponseId = m.primaryResponseId;
                }
                if (m.surveyCtaHasRendered != null && $Object.hasOwnProperty.call(m, "surveyCtaHasRendered")) {
                    d.surveyCtaHasRendered = m.surveyCtaHasRendered;
                }
                return d;
            };

            SideBySideMetadata.prototype.toJSON = function() {
                return SideBySideMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            SideBySideMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.BotUnifiedResponseMutation.SideBySideMetadata";
            };

            return SideBySideMetadata;
        })();

        return BotUnifiedResponseMutation;
    })();

    proto.COMMAND_COMMAND_TYPE = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[1] = "EVERYONE"] = 1;
        values[valuesById[2] = "SILENT"] = 2;
        values[valuesById[3] = "AI"] = 3;
        values[valuesById[4] = "AI_IMAGINE"] = 4;
        return values;
    })();

    proto.CONSUMER_APPLICATION_EXTENDED_TEXT_MESSAGE_PREVIEW_TYPE = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "NONE"] = 0;
        values[valuesById[1] = "VIDEO"] = 1;
        return values;
    })();

    proto.CONSUMER_APPLICATION_METADATA_SPECIAL_TEXT_SIZE = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[1] = "SMALL"] = 1;
        values[valuesById[2] = "MEDIUM"] = 2;
        values[valuesById[3] = "LARGE"] = 3;
        return values;
    })();

    proto.CONSUMER_APPLICATION_STATUS_TEXT_MESAGE_FONT_TYPE = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "SANS_SERIF"] = 0;
        values[valuesById[1] = "SERIF"] = 1;
        values[valuesById[2] = "NORICAN_REGULAR"] = 2;
        values[valuesById[3] = "BRYNDAN_WRITE"] = 3;
        values[valuesById[4] = "BEBASNEUE_REGULAR"] = 4;
        values[valuesById[5] = "OSWALD_HEAVY"] = 5;
        return values;
    })();

    proto.CallLogRecord = (function() {

        const CallLogRecord = function (p) {
            this.participants = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CallLogRecord.prototype.callResult = null;
        CallLogRecord.prototype.isDndMode = null;
        CallLogRecord.prototype.silenceReason = null;
        CallLogRecord.prototype.duration = null;
        CallLogRecord.prototype.startTime = null;
        CallLogRecord.prototype.isIncoming = null;
        CallLogRecord.prototype.isVideo = null;
        CallLogRecord.prototype.isCallLink = null;
        CallLogRecord.prototype.callLinkToken = null;
        CallLogRecord.prototype.scheduledCallId = null;
        CallLogRecord.prototype.callId = null;
        CallLogRecord.prototype.callCreatorJid = null;
        CallLogRecord.prototype.groupJid = null;
        CallLogRecord.prototype.participants = $util.emptyArray;
        CallLogRecord.prototype.callType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_callResult", {
            get: $util.oneOfGetter($oneOfFields = ["callResult"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_isDndMode", {
            get: $util.oneOfGetter($oneOfFields = ["isDndMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_silenceReason", {
            get: $util.oneOfGetter($oneOfFields = ["silenceReason"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_startTime", {
            get: $util.oneOfGetter($oneOfFields = ["startTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_isIncoming", {
            get: $util.oneOfGetter($oneOfFields = ["isIncoming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_isVideo", {
            get: $util.oneOfGetter($oneOfFields = ["isVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_isCallLink", {
            get: $util.oneOfGetter($oneOfFields = ["isCallLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_callLinkToken", {
            get: $util.oneOfGetter($oneOfFields = ["callLinkToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_scheduledCallId", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_callId", {
            get: $util.oneOfGetter($oneOfFields = ["callId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_callCreatorJid", {
            get: $util.oneOfGetter($oneOfFields = ["callCreatorJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_groupJid", {
            get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CallLogRecord.prototype, "_callType", {
            get: $util.oneOfGetter($oneOfFields = ["callType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CallLogRecord.create = function(properties) {
            return new CallLogRecord(properties);
        };

        CallLogRecord.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.callResult != null && $Object.hasOwnProperty.call(m, "callResult"))
                w.uint32(8).int32(m.callResult);
            if (m.isDndMode != null && $Object.hasOwnProperty.call(m, "isDndMode"))
                w.uint32(16).bool(m.isDndMode);
            if (m.silenceReason != null && $Object.hasOwnProperty.call(m, "silenceReason"))
                w.uint32(24).int32(m.silenceReason);
            if (m.duration != null && $Object.hasOwnProperty.call(m, "duration"))
                w.uint32(32).int64(m.duration);
            if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime"))
                w.uint32(40).int64(m.startTime);
            if (m.isIncoming != null && $Object.hasOwnProperty.call(m, "isIncoming"))
                w.uint32(48).bool(m.isIncoming);
            if (m.isVideo != null && $Object.hasOwnProperty.call(m, "isVideo"))
                w.uint32(56).bool(m.isVideo);
            if (m.isCallLink != null && $Object.hasOwnProperty.call(m, "isCallLink"))
                w.uint32(64).bool(m.isCallLink);
            if (m.callLinkToken != null && $Object.hasOwnProperty.call(m, "callLinkToken"))
                w.uint32(74).string(m.callLinkToken);
            if (m.scheduledCallId != null && $Object.hasOwnProperty.call(m, "scheduledCallId"))
                w.uint32(82).string(m.scheduledCallId);
            if (m.callId != null && $Object.hasOwnProperty.call(m, "callId"))
                w.uint32(90).string(m.callId);
            if (m.callCreatorJid != null && $Object.hasOwnProperty.call(m, "callCreatorJid"))
                w.uint32(98).string(m.callCreatorJid);
            if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid"))
                w.uint32(106).string(m.groupJid);
            if (m.participants != null && m.participants.length) {
                for (var i = 0; i < m.participants.length; ++i)
                    $root.proto.CallLogRecord.ParticipantInfo.encode(m.participants[i], w.uint32(114).fork(), q + 1).ldelim();
            }
            if (m.callType != null && $Object.hasOwnProperty.call(m, "callType"))
                w.uint32(120).int32(m.callType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CallLogRecord.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CallLogRecord(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.callResult = r.int32();
                        m._callResult = "callResult";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.isDndMode = r.bool();
                        m._isDndMode = "isDndMode";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.silenceReason = r.int32();
                        m._silenceReason = "silenceReason";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.duration = r.int64();
                        m._duration = "duration";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.startTime = r.int64();
                        m._startTime = "startTime";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.isIncoming = r.bool();
                        m._isIncoming = "isIncoming";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.isVideo = r.bool();
                        m._isVideo = "isVideo";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.isCallLink = r.bool();
                        m._isCallLink = "isCallLink";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.callLinkToken = r.stringVerify();
                        m._callLinkToken = "callLinkToken";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.scheduledCallId = r.stringVerify();
                        m._scheduledCallId = "scheduledCallId";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.callId = r.stringVerify();
                        m._callId = "callId";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.callCreatorJid = r.stringVerify();
                        m._callCreatorJid = "callCreatorJid";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.groupJid = r.stringVerify();
                        m._groupJid = "groupJid";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        if (!(m.participants && m.participants.length))
                            m.participants = [];
                        m.participants.push($root.proto.CallLogRecord.ParticipantInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        m.callType = r.int32();
                        m._callType = "callType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CallLogRecord.fromObject = function (d, q) {
            if (d instanceof $root.proto.CallLogRecord)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CallLogRecord: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CallLogRecord();
            switch (d.callResult) {
            case "CONNECTED":
            case 0:
                m.callResult = 0;
                break;
            case "REJECTED":
            case 1:
                m.callResult = 1;
                break;
            case "CANCELLED":
            case 2:
                m.callResult = 2;
                break;
            case "ACCEPTEDELSEWHERE":
            case 3:
                m.callResult = 3;
                break;
            case "MISSED":
            case 4:
                m.callResult = 4;
                break;
            case "INVALID":
            case 5:
                m.callResult = 5;
                break;
            case "UNAVAILABLE":
            case 6:
                m.callResult = 6;
                break;
            case "UPCOMING":
            case 7:
                m.callResult = 7;
                break;
            case "FAILED":
            case 8:
                m.callResult = 8;
                break;
            case "ABANDONED":
            case 9:
                m.callResult = 9;
                break;
            case "ONGOING":
            case 10:
                m.callResult = 10;
                break;
            default:
                if (typeof d.callResult === "number" && (d.callResult | 0) === d.callResult)
                    m.callResult = d.callResult;
            }
            if (d.isDndMode != null) {
                m.isDndMode = $Boolean(d.isDndMode);
            }
            switch (d.silenceReason) {
            case "NONE":
            case 0:
                m.silenceReason = 0;
                break;
            case "SCHEDULED":
            case 1:
                m.silenceReason = 1;
                break;
            case "PRIVACY":
            case 2:
                m.silenceReason = 2;
                break;
            case "LIGHTWEIGHT":
            case 3:
                m.silenceReason = 3;
                break;
            default:
                if (typeof d.silenceReason === "number" && (d.silenceReason | 0) === d.silenceReason)
                    m.silenceReason = d.silenceReason;
            }
            if (d.duration != null) {
                if ($util.Long)
                    m.duration = $util.Long.fromValue(d.duration, false);
                else if (typeof d.duration === "string")
                    m.duration = $parseInt(d.duration, 10);
                else if (typeof d.duration === "number")
                    m.duration = d.duration;
                else if (typeof d.duration === "object")
                    m.duration = new $util.LongBits(d.duration.low >>> 0, d.duration.high >>> 0).toNumber();
            }
            if (d.startTime != null) {
                if ($util.Long)
                    m.startTime = $util.Long.fromValue(d.startTime, false);
                else if (typeof d.startTime === "string")
                    m.startTime = $parseInt(d.startTime, 10);
                else if (typeof d.startTime === "number")
                    m.startTime = d.startTime;
                else if (typeof d.startTime === "object")
                    m.startTime = new $util.LongBits(d.startTime.low >>> 0, d.startTime.high >>> 0).toNumber();
            }
            if (d.isIncoming != null) {
                m.isIncoming = $Boolean(d.isIncoming);
            }
            if (d.isVideo != null) {
                m.isVideo = $Boolean(d.isVideo);
            }
            if (d.isCallLink != null) {
                m.isCallLink = $Boolean(d.isCallLink);
            }
            if (d.callLinkToken != null) {
                m.callLinkToken = $String(d.callLinkToken);
            }
            if (d.scheduledCallId != null) {
                m.scheduledCallId = $String(d.scheduledCallId);
            }
            if (d.callId != null) {
                m.callId = $String(d.callId);
            }
            if (d.callCreatorJid != null) {
                m.callCreatorJid = $String(d.callCreatorJid);
            }
            if (d.groupJid != null) {
                m.groupJid = $String(d.groupJid);
            }
            if (d.participants) {
                if (!$Array.isArray(d.participants))
                    throw $TypeError(".proto.CallLogRecord.participants: array expected");
                m.participants = $Array(d.participants.length);
                for (var i = 0; i < d.participants.length; ++i) {
                    if (!$util.isObject(d.participants[i]))
                        throw $TypeError(".proto.CallLogRecord.participants: object expected");
                    m.participants[i] = $root.proto.CallLogRecord.ParticipantInfo.fromObject(d.participants[i], q + 1);
                }
            }
            switch (d.callType) {
            case "REGULAR":
            case 0:
                m.callType = 0;
                break;
            case "SCHEDULED_CALL":
            case 1:
                m.callType = 1;
                break;
            case "VOICE_CHAT":
            case 2:
                m.callType = 2;
                break;
            default:
                if (typeof d.callType === "number" && (d.callType | 0) === d.callType)
                    m.callType = d.callType;
            }
            return m;
        };

        CallLogRecord.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.participants = [];
            }
            if (m.callResult != null && $Object.hasOwnProperty.call(m, "callResult")) {
                d.callResult = o.enums === $String ? $root.proto.CallLogRecord.CallResult[m.callResult] === $undefined ? m.callResult : $root.proto.CallLogRecord.CallResult[m.callResult] : m.callResult;
            }
            if (m.isDndMode != null && $Object.hasOwnProperty.call(m, "isDndMode")) {
                d.isDndMode = m.isDndMode;
            }
            if (m.silenceReason != null && $Object.hasOwnProperty.call(m, "silenceReason")) {
                d.silenceReason = o.enums === $String ? $root.proto.CallLogRecord.SilenceReason[m.silenceReason] === $undefined ? m.silenceReason : $root.proto.CallLogRecord.SilenceReason[m.silenceReason] : m.silenceReason;
            }
            if (m.duration != null && $Object.hasOwnProperty.call(m, "duration")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.duration = typeof m.duration === "number" ? $BigInt(m.duration) : $util.Long.fromBits(m.duration.low >>> 0, m.duration.high >>> 0, false).toBigInt();
                else if (typeof m.duration === "number")
                    d.duration = o.longs === $String ? $String(m.duration) : m.duration;
                else
                    d.duration = o.longs === $String ? $util.Long.prototype.toString.call(m.duration) : o.longs === $Number ? new $util.LongBits(m.duration.low >>> 0, m.duration.high >>> 0).toNumber() : m.duration;
            }
            if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.startTime = typeof m.startTime === "number" ? $BigInt(m.startTime) : $util.Long.fromBits(m.startTime.low >>> 0, m.startTime.high >>> 0, false).toBigInt();
                else if (typeof m.startTime === "number")
                    d.startTime = o.longs === $String ? $String(m.startTime) : m.startTime;
                else
                    d.startTime = o.longs === $String ? $util.Long.prototype.toString.call(m.startTime) : o.longs === $Number ? new $util.LongBits(m.startTime.low >>> 0, m.startTime.high >>> 0).toNumber() : m.startTime;
            }
            if (m.isIncoming != null && $Object.hasOwnProperty.call(m, "isIncoming")) {
                d.isIncoming = m.isIncoming;
            }
            if (m.isVideo != null && $Object.hasOwnProperty.call(m, "isVideo")) {
                d.isVideo = m.isVideo;
            }
            if (m.isCallLink != null && $Object.hasOwnProperty.call(m, "isCallLink")) {
                d.isCallLink = m.isCallLink;
            }
            if (m.callLinkToken != null && $Object.hasOwnProperty.call(m, "callLinkToken")) {
                d.callLinkToken = m.callLinkToken;
            }
            if (m.scheduledCallId != null && $Object.hasOwnProperty.call(m, "scheduledCallId")) {
                d.scheduledCallId = m.scheduledCallId;
            }
            if (m.callId != null && $Object.hasOwnProperty.call(m, "callId")) {
                d.callId = m.callId;
            }
            if (m.callCreatorJid != null && $Object.hasOwnProperty.call(m, "callCreatorJid")) {
                d.callCreatorJid = m.callCreatorJid;
            }
            if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid")) {
                d.groupJid = m.groupJid;
            }
            if (m.participants && m.participants.length) {
                d.participants = $Array(m.participants.length);
                for (var j = 0; j < m.participants.length; ++j) {
                    d.participants[j] = $root.proto.CallLogRecord.ParticipantInfo.toObject(m.participants[j], o, q + 1);
                }
            }
            if (m.callType != null && $Object.hasOwnProperty.call(m, "callType")) {
                d.callType = o.enums === $String ? $root.proto.CallLogRecord.CallType[m.callType] === $undefined ? m.callType : $root.proto.CallLogRecord.CallType[m.callType] : m.callType;
            }
            return d;
        };

        CallLogRecord.prototype.toJSON = function() {
            return CallLogRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        CallLogRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CallLogRecord";
        };

        CallLogRecord.CallResult = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "CONNECTED"] = 0;
            values[valuesById[1] = "REJECTED"] = 1;
            values[valuesById[2] = "CANCELLED"] = 2;
            values[valuesById[3] = "ACCEPTEDELSEWHERE"] = 3;
            values[valuesById[4] = "MISSED"] = 4;
            values[valuesById[5] = "INVALID"] = 5;
            values[valuesById[6] = "UNAVAILABLE"] = 6;
            values[valuesById[7] = "UPCOMING"] = 7;
            values[valuesById[8] = "FAILED"] = 8;
            values[valuesById[9] = "ABANDONED"] = 9;
            values[valuesById[10] = "ONGOING"] = 10;
            return values;
        })();

        CallLogRecord.CallType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "REGULAR"] = 0;
            values[valuesById[1] = "SCHEDULED_CALL"] = 1;
            values[valuesById[2] = "VOICE_CHAT"] = 2;
            return values;
        })();

        CallLogRecord.ParticipantInfo = (function() {

            const ParticipantInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ParticipantInfo.prototype.userJid = null;
            ParticipantInfo.prototype.callResult = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ParticipantInfo.prototype, "_userJid", {
                get: $util.oneOfGetter($oneOfFields = ["userJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ParticipantInfo.prototype, "_callResult", {
                get: $util.oneOfGetter($oneOfFields = ["callResult"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ParticipantInfo.create = function(properties) {
                return new ParticipantInfo(properties);
            };

            ParticipantInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.userJid != null && $Object.hasOwnProperty.call(m, "userJid"))
                    w.uint32(10).string(m.userJid);
                if (m.callResult != null && $Object.hasOwnProperty.call(m, "callResult"))
                    w.uint32(16).int32(m.callResult);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ParticipantInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CallLogRecord.ParticipantInfo(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.userJid = r.stringVerify();
                            m._userJid = "userJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.callResult = r.int32();
                            m._callResult = "callResult";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ParticipantInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.CallLogRecord.ParticipantInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.CallLogRecord.ParticipantInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.CallLogRecord.ParticipantInfo();
                if (d.userJid != null) {
                    m.userJid = $String(d.userJid);
                }
                switch (d.callResult) {
                case "CONNECTED":
                case 0:
                    m.callResult = 0;
                    break;
                case "REJECTED":
                case 1:
                    m.callResult = 1;
                    break;
                case "CANCELLED":
                case 2:
                    m.callResult = 2;
                    break;
                case "ACCEPTEDELSEWHERE":
                case 3:
                    m.callResult = 3;
                    break;
                case "MISSED":
                case 4:
                    m.callResult = 4;
                    break;
                case "INVALID":
                case 5:
                    m.callResult = 5;
                    break;
                case "UNAVAILABLE":
                case 6:
                    m.callResult = 6;
                    break;
                case "UPCOMING":
                case 7:
                    m.callResult = 7;
                    break;
                case "FAILED":
                case 8:
                    m.callResult = 8;
                    break;
                case "ABANDONED":
                case 9:
                    m.callResult = 9;
                    break;
                case "ONGOING":
                case 10:
                    m.callResult = 10;
                    break;
                default:
                    if (typeof d.callResult === "number" && (d.callResult | 0) === d.callResult)
                        m.callResult = d.callResult;
                }
                return m;
            };

            ParticipantInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.userJid != null && $Object.hasOwnProperty.call(m, "userJid")) {
                    d.userJid = m.userJid;
                }
                if (m.callResult != null && $Object.hasOwnProperty.call(m, "callResult")) {
                    d.callResult = o.enums === $String ? $root.proto.CallLogRecord.CallResult[m.callResult] === $undefined ? m.callResult : $root.proto.CallLogRecord.CallResult[m.callResult] : m.callResult;
                }
                return d;
            };

            ParticipantInfo.prototype.toJSON = function() {
                return ParticipantInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            ParticipantInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.CallLogRecord.ParticipantInfo";
            };

            return ParticipantInfo;
        })();

        CallLogRecord.SilenceReason = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "SCHEDULED"] = 1;
            values[valuesById[2] = "PRIVACY"] = 2;
            values[valuesById[3] = "LIGHTWEIGHT"] = 3;
            return values;
        })();

        return CallLogRecord;
    })();

    proto.CertChain = (function() {

        const CertChain = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CertChain.prototype.leaf = null;
        CertChain.prototype.intermediate = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CertChain.prototype, "_leaf", {
            get: $util.oneOfGetter($oneOfFields = ["leaf"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CertChain.prototype, "_intermediate", {
            get: $util.oneOfGetter($oneOfFields = ["intermediate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CertChain.create = function(properties) {
            return new CertChain(properties);
        };

        CertChain.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.leaf != null && $Object.hasOwnProperty.call(m, "leaf"))
                $root.proto.CertChain.NoiseCertificate.encode(m.leaf, w.uint32(10).fork(), q + 1).ldelim();
            if (m.intermediate != null && $Object.hasOwnProperty.call(m, "intermediate"))
                $root.proto.CertChain.NoiseCertificate.encode(m.intermediate, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CertChain.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CertChain();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.leaf = $root.proto.CertChain.NoiseCertificate.decode(r, r.uint32(), $undefined, q + 1, m.leaf);
                        m._leaf = "leaf";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.intermediate = $root.proto.CertChain.NoiseCertificate.decode(r, r.uint32(), $undefined, q + 1, m.intermediate);
                        m._intermediate = "intermediate";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CertChain.fromObject = function (d, q) {
            if (d instanceof $root.proto.CertChain)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CertChain: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CertChain();
            if (d.leaf != null) {
                if (!$util.isObject(d.leaf))
                    throw $TypeError(".proto.CertChain.leaf: object expected");
                m.leaf = $root.proto.CertChain.NoiseCertificate.fromObject(d.leaf, q + 1);
            }
            if (d.intermediate != null) {
                if (!$util.isObject(d.intermediate))
                    throw $TypeError(".proto.CertChain.intermediate: object expected");
                m.intermediate = $root.proto.CertChain.NoiseCertificate.fromObject(d.intermediate, q + 1);
            }
            return m;
        };

        CertChain.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.leaf != null && $Object.hasOwnProperty.call(m, "leaf")) {
                d.leaf = $root.proto.CertChain.NoiseCertificate.toObject(m.leaf, o, q + 1);
            }
            if (m.intermediate != null && $Object.hasOwnProperty.call(m, "intermediate")) {
                d.intermediate = $root.proto.CertChain.NoiseCertificate.toObject(m.intermediate, o, q + 1);
            }
            return d;
        };

        CertChain.prototype.toJSON = function() {
            return CertChain.toObject(this, $protobuf.util.toJSONOptions);
        };

        CertChain.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CertChain";
        };

        CertChain.NoiseCertificate = (function() {

            const NoiseCertificate = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NoiseCertificate.prototype.details = null;
            NoiseCertificate.prototype.signature = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoiseCertificate.prototype, "_details", {
                get: $util.oneOfGetter($oneOfFields = ["details"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoiseCertificate.prototype, "_signature", {
                get: $util.oneOfGetter($oneOfFields = ["signature"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NoiseCertificate.create = function(properties) {
                return new NoiseCertificate(properties);
            };

            NoiseCertificate.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.details != null && $Object.hasOwnProperty.call(m, "details"))
                    w.uint32(10).bytes(m.details);
                if (m.signature != null && $Object.hasOwnProperty.call(m, "signature"))
                    w.uint32(18).bytes(m.signature);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NoiseCertificate.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CertChain.NoiseCertificate();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.details = r.bytes();
                            m._details = "details";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.signature = r.bytes();
                            m._signature = "signature";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NoiseCertificate.fromObject = function (d, q) {
                if (d instanceof $root.proto.CertChain.NoiseCertificate)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.CertChain.NoiseCertificate: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.CertChain.NoiseCertificate();
                if (d.details != null) {
                    if (typeof d.details === "string")
                        $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                    else if (d.details.length >= 0)
                        m.details = d.details;
                }
                if (d.signature != null) {
                    if (typeof d.signature === "string")
                        $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                    else if (d.signature.length >= 0)
                        m.signature = d.signature;
                }
                return m;
            };

            NoiseCertificate.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.details != null && $Object.hasOwnProperty.call(m, "details")) {
                    d.details = o.bytes === $String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.details) : m.details;
                }
                if (m.signature != null && $Object.hasOwnProperty.call(m, "signature")) {
                    d.signature = o.bytes === $String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signature) : m.signature;
                }
                return d;
            };

            NoiseCertificate.prototype.toJSON = function() {
                return NoiseCertificate.toObject(this, $protobuf.util.toJSONOptions);
            };

            NoiseCertificate.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.CertChain.NoiseCertificate";
            };

            NoiseCertificate.Details = (function() {

                const Details = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Details.prototype.serial = null;
                Details.prototype.issuerSerial = null;
                Details.prototype.key = null;
                Details.prototype.notBefore = null;
                Details.prototype.notAfter = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Details.prototype, "_serial", {
                    get: $util.oneOfGetter($oneOfFields = ["serial"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Details.prototype, "_issuerSerial", {
                    get: $util.oneOfGetter($oneOfFields = ["issuerSerial"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Details.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Details.prototype, "_notBefore", {
                    get: $util.oneOfGetter($oneOfFields = ["notBefore"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Details.prototype, "_notAfter", {
                    get: $util.oneOfGetter($oneOfFields = ["notAfter"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Details.create = function(properties) {
                    return new Details(properties);
                };

                Details.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.serial != null && $Object.hasOwnProperty.call(m, "serial"))
                        w.uint32(8).uint32(m.serial);
                    if (m.issuerSerial != null && $Object.hasOwnProperty.call(m, "issuerSerial"))
                        w.uint32(16).uint32(m.issuerSerial);
                    if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                        w.uint32(26).bytes(m.key);
                    if (m.notBefore != null && $Object.hasOwnProperty.call(m, "notBefore"))
                        w.uint32(32).uint64(m.notBefore);
                    if (m.notAfter != null && $Object.hasOwnProperty.call(m, "notAfter"))
                        w.uint32(40).uint64(m.notAfter);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Details.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CertChain.NoiseCertificate.Details();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.serial = r.uint32();
                                m._serial = "serial";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.issuerSerial = r.uint32();
                                m._issuerSerial = "issuerSerial";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.key = r.bytes();
                                m._key = "key";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.notBefore = r.uint64();
                                m._notBefore = "notBefore";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.notAfter = r.uint64();
                                m._notAfter = "notAfter";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Details.fromObject = function (d, q) {
                    if (d instanceof $root.proto.CertChain.NoiseCertificate.Details)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.CertChain.NoiseCertificate.Details: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.CertChain.NoiseCertificate.Details();
                    if (d.serial != null) {
                        m.serial = d.serial >>> 0;
                    }
                    if (d.issuerSerial != null) {
                        m.issuerSerial = d.issuerSerial >>> 0;
                    }
                    if (d.key != null) {
                        if (typeof d.key === "string")
                            $util.base64.decode(d.key, m.key = $util.newBuffer($util.base64.length(d.key)), 0);
                        else if (d.key.length >= 0)
                            m.key = d.key;
                    }
                    if (d.notBefore != null) {
                        if ($util.Long)
                            m.notBefore = $util.Long.fromValue(d.notBefore, true);
                        else if (typeof d.notBefore === "string")
                            m.notBefore = $parseInt(d.notBefore, 10);
                        else if (typeof d.notBefore === "number")
                            m.notBefore = d.notBefore;
                        else if (typeof d.notBefore === "object")
                            m.notBefore = new $util.LongBits(d.notBefore.low >>> 0, d.notBefore.high >>> 0).toNumber(true);
                    }
                    if (d.notAfter != null) {
                        if ($util.Long)
                            m.notAfter = $util.Long.fromValue(d.notAfter, true);
                        else if (typeof d.notAfter === "string")
                            m.notAfter = $parseInt(d.notAfter, 10);
                        else if (typeof d.notAfter === "number")
                            m.notAfter = d.notAfter;
                        else if (typeof d.notAfter === "object")
                            m.notAfter = new $util.LongBits(d.notAfter.low >>> 0, d.notAfter.high >>> 0).toNumber(true);
                    }
                    return m;
                };

                Details.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.serial != null && $Object.hasOwnProperty.call(m, "serial")) {
                        d.serial = m.serial;
                    }
                    if (m.issuerSerial != null && $Object.hasOwnProperty.call(m, "issuerSerial")) {
                        d.issuerSerial = m.issuerSerial;
                    }
                    if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                        d.key = o.bytes === $String ? $util.base64.encode(m.key, 0, m.key.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.key) : m.key;
                    }
                    if (m.notBefore != null && $Object.hasOwnProperty.call(m, "notBefore")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.notBefore = typeof m.notBefore === "number" ? $BigInt(m.notBefore) : $util.Long.fromBits(m.notBefore.low >>> 0, m.notBefore.high >>> 0, true).toBigInt();
                        else if (typeof m.notBefore === "number")
                            d.notBefore = o.longs === $String ? $String(m.notBefore) : m.notBefore;
                        else
                            d.notBefore = o.longs === $String ? $util.Long.prototype.toString.call(m.notBefore) : o.longs === $Number ? new $util.LongBits(m.notBefore.low >>> 0, m.notBefore.high >>> 0).toNumber(true) : m.notBefore;
                    }
                    if (m.notAfter != null && $Object.hasOwnProperty.call(m, "notAfter")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.notAfter = typeof m.notAfter === "number" ? $BigInt(m.notAfter) : $util.Long.fromBits(m.notAfter.low >>> 0, m.notAfter.high >>> 0, true).toBigInt();
                        else if (typeof m.notAfter === "number")
                            d.notAfter = o.longs === $String ? $String(m.notAfter) : m.notAfter;
                        else
                            d.notAfter = o.longs === $String ? $util.Long.prototype.toString.call(m.notAfter) : o.longs === $Number ? new $util.LongBits(m.notAfter.low >>> 0, m.notAfter.high >>> 0).toNumber(true) : m.notAfter;
                    }
                    return d;
                };

                Details.prototype.toJSON = function() {
                    return Details.toObject(this, $protobuf.util.toJSONOptions);
                };

                Details.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.CertChain.NoiseCertificate.Details";
                };

                return Details;
            })();

            return NoiseCertificate;
        })();

        return CertChain;
    })();

    proto.ChatLockSettings = (function() {

        const ChatLockSettings = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ChatLockSettings.prototype.hideLockedChats = null;
        ChatLockSettings.prototype.secretCode = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ChatLockSettings.prototype, "_hideLockedChats", {
            get: $util.oneOfGetter($oneOfFields = ["hideLockedChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ChatLockSettings.prototype, "_secretCode", {
            get: $util.oneOfGetter($oneOfFields = ["secretCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatLockSettings.create = function(properties) {
            return new ChatLockSettings(properties);
        };

        ChatLockSettings.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.hideLockedChats != null && $Object.hasOwnProperty.call(m, "hideLockedChats"))
                w.uint32(8).bool(m.hideLockedChats);
            if (m.secretCode != null && $Object.hasOwnProperty.call(m, "secretCode"))
                $root.proto.UserPassword.encode(m.secretCode, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ChatLockSettings.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ChatLockSettings();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.hideLockedChats = r.bool();
                        m._hideLockedChats = "hideLockedChats";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.secretCode = $root.proto.UserPassword.decode(r, r.uint32(), $undefined, q + 1, m.secretCode);
                        m._secretCode = "secretCode";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ChatLockSettings.fromObject = function (d, q) {
            if (d instanceof $root.proto.ChatLockSettings)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ChatLockSettings: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ChatLockSettings();
            if (d.hideLockedChats != null) {
                m.hideLockedChats = $Boolean(d.hideLockedChats);
            }
            if (d.secretCode != null) {
                if (!$util.isObject(d.secretCode))
                    throw $TypeError(".proto.ChatLockSettings.secretCode: object expected");
                m.secretCode = $root.proto.UserPassword.fromObject(d.secretCode, q + 1);
            }
            return m;
        };

        ChatLockSettings.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.hideLockedChats != null && $Object.hasOwnProperty.call(m, "hideLockedChats")) {
                d.hideLockedChats = m.hideLockedChats;
            }
            if (m.secretCode != null && $Object.hasOwnProperty.call(m, "secretCode")) {
                d.secretCode = $root.proto.UserPassword.toObject(m.secretCode, o, q + 1);
            }
            return d;
        };

        ChatLockSettings.prototype.toJSON = function() {
            return ChatLockSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatLockSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ChatLockSettings";
        };

        return ChatLockSettings;
    })();

    proto.ChatRowOpaqueData = (function() {

        const ChatRowOpaqueData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ChatRowOpaqueData.prototype.draftMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ChatRowOpaqueData.prototype, "_draftMessage", {
            get: $util.oneOfGetter($oneOfFields = ["draftMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ChatRowOpaqueData.create = function(properties) {
            return new ChatRowOpaqueData(properties);
        };

        ChatRowOpaqueData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.draftMessage != null && $Object.hasOwnProperty.call(m, "draftMessage"))
                $root.proto.ChatRowOpaqueData.DraftMessage.encode(m.draftMessage, w.uint32(10).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ChatRowOpaqueData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ChatRowOpaqueData();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.draftMessage = $root.proto.ChatRowOpaqueData.DraftMessage.decode(r, r.uint32(), $undefined, q + 1, m.draftMessage);
                        m._draftMessage = "draftMessage";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ChatRowOpaqueData.fromObject = function (d, q) {
            if (d instanceof $root.proto.ChatRowOpaqueData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ChatRowOpaqueData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ChatRowOpaqueData();
            if (d.draftMessage != null) {
                if (!$util.isObject(d.draftMessage))
                    throw $TypeError(".proto.ChatRowOpaqueData.draftMessage: object expected");
                m.draftMessage = $root.proto.ChatRowOpaqueData.DraftMessage.fromObject(d.draftMessage, q + 1);
            }
            return m;
        };

        ChatRowOpaqueData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.draftMessage != null && $Object.hasOwnProperty.call(m, "draftMessage")) {
                d.draftMessage = $root.proto.ChatRowOpaqueData.DraftMessage.toObject(m.draftMessage, o, q + 1);
            }
            return d;
        };

        ChatRowOpaqueData.prototype.toJSON = function() {
            return ChatRowOpaqueData.toObject(this, $protobuf.util.toJSONOptions);
        };

        ChatRowOpaqueData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ChatRowOpaqueData";
        };

        ChatRowOpaqueData.DraftMessage = (function() {

            const DraftMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DraftMessage.prototype.text = null;
            DraftMessage.prototype.omittedUrl = null;
            DraftMessage.prototype.ctwaContextLinkData = null;
            DraftMessage.prototype.ctwaContext = null;
            DraftMessage.prototype.timestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DraftMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DraftMessage.prototype, "_omittedUrl", {
                get: $util.oneOfGetter($oneOfFields = ["omittedUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DraftMessage.prototype, "_ctwaContextLinkData", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaContextLinkData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DraftMessage.prototype, "_ctwaContext", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaContext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DraftMessage.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DraftMessage.create = function(properties) {
                return new DraftMessage(properties);
            };

            DraftMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.omittedUrl != null && $Object.hasOwnProperty.call(m, "omittedUrl"))
                    w.uint32(18).string(m.omittedUrl);
                if (m.ctwaContextLinkData != null && $Object.hasOwnProperty.call(m, "ctwaContextLinkData"))
                    $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.encode(m.ctwaContextLinkData, w.uint32(26).fork(), q + 1).ldelim();
                if (m.ctwaContext != null && $Object.hasOwnProperty.call(m, "ctwaContext"))
                    $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.encode(m.ctwaContext, w.uint32(34).fork(), q + 1).ldelim();
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                    w.uint32(40).int64(m.timestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DraftMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ChatRowOpaqueData.DraftMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.omittedUrl = r.stringVerify();
                            m._omittedUrl = "omittedUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.ctwaContextLinkData = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.decode(r, r.uint32(), $undefined, q + 1, m.ctwaContextLinkData);
                            m._ctwaContextLinkData = "ctwaContextLinkData";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.ctwaContext = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.decode(r, r.uint32(), $undefined, q + 1, m.ctwaContext);
                            m._ctwaContext = "ctwaContext";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.timestamp = r.int64();
                            m._timestamp = "timestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DraftMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ChatRowOpaqueData.DraftMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ChatRowOpaqueData.DraftMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ChatRowOpaqueData.DraftMessage();
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.omittedUrl != null) {
                    m.omittedUrl = $String(d.omittedUrl);
                }
                if (d.ctwaContextLinkData != null) {
                    if (!$util.isObject(d.ctwaContextLinkData))
                        throw $TypeError(".proto.ChatRowOpaqueData.DraftMessage.ctwaContextLinkData: object expected");
                    m.ctwaContextLinkData = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.fromObject(d.ctwaContextLinkData, q + 1);
                }
                if (d.ctwaContext != null) {
                    if (!$util.isObject(d.ctwaContext))
                        throw $TypeError(".proto.ChatRowOpaqueData.DraftMessage.ctwaContext: object expected");
                    m.ctwaContext = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.fromObject(d.ctwaContext, q + 1);
                }
                if (d.timestamp != null) {
                    if ($util.Long)
                        m.timestamp = $util.Long.fromValue(d.timestamp, false);
                    else if (typeof d.timestamp === "string")
                        m.timestamp = $parseInt(d.timestamp, 10);
                    else if (typeof d.timestamp === "number")
                        m.timestamp = d.timestamp;
                    else if (typeof d.timestamp === "object")
                        m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                }
                return m;
            };

            DraftMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                if (m.omittedUrl != null && $Object.hasOwnProperty.call(m, "omittedUrl")) {
                    d.omittedUrl = m.omittedUrl;
                }
                if (m.ctwaContextLinkData != null && $Object.hasOwnProperty.call(m, "ctwaContextLinkData")) {
                    d.ctwaContextLinkData = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData.toObject(m.ctwaContextLinkData, o, q + 1);
                }
                if (m.ctwaContext != null && $Object.hasOwnProperty.call(m, "ctwaContext")) {
                    d.ctwaContext = $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.toObject(m.ctwaContext, o, q + 1);
                }
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.timestamp === "number")
                        d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                    else
                        d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                }
                return d;
            };

            DraftMessage.prototype.toJSON = function() {
                return DraftMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            DraftMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ChatRowOpaqueData.DraftMessage";
            };

            DraftMessage.CtwaContextData = (function() {

                const CtwaContextData = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CtwaContextData.prototype.conversionSource = null;
                CtwaContextData.prototype.conversionData = null;
                CtwaContextData.prototype.sourceUrl = null;
                CtwaContextData.prototype.sourceId = null;
                CtwaContextData.prototype.sourceType = null;
                CtwaContextData.prototype.title = null;
                CtwaContextData.prototype.description = null;
                CtwaContextData.prototype.thumbnail = null;
                CtwaContextData.prototype.thumbnailUrl = null;
                CtwaContextData.prototype.mediaType = null;
                CtwaContextData.prototype.mediaUrl = null;
                CtwaContextData.prototype.isSuspiciousLink = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_conversionSource", {
                    get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_conversionData", {
                    get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_sourceId", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_sourceType", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_thumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_thumbnailUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["thumbnailUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_mediaType", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_mediaUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextData.prototype, "_isSuspiciousLink", {
                    get: $util.oneOfGetter($oneOfFields = ["isSuspiciousLink"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CtwaContextData.create = function(properties) {
                    return new CtwaContextData(properties);
                };

                CtwaContextData.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.conversionSource != null && $Object.hasOwnProperty.call(m, "conversionSource"))
                        w.uint32(10).string(m.conversionSource);
                    if (m.conversionData != null && $Object.hasOwnProperty.call(m, "conversionData"))
                        w.uint32(18).bytes(m.conversionData);
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl"))
                        w.uint32(26).string(m.sourceUrl);
                    if (m.sourceId != null && $Object.hasOwnProperty.call(m, "sourceId"))
                        w.uint32(34).string(m.sourceId);
                    if (m.sourceType != null && $Object.hasOwnProperty.call(m, "sourceType"))
                        w.uint32(42).string(m.sourceType);
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(50).string(m.title);
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                        w.uint32(58).string(m.description);
                    if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail"))
                        w.uint32(66).string(m.thumbnail);
                    if (m.thumbnailUrl != null && $Object.hasOwnProperty.call(m, "thumbnailUrl"))
                        w.uint32(74).string(m.thumbnailUrl);
                    if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType"))
                        w.uint32(80).int32(m.mediaType);
                    if (m.mediaUrl != null && $Object.hasOwnProperty.call(m, "mediaUrl"))
                        w.uint32(90).string(m.mediaUrl);
                    if (m.isSuspiciousLink != null && $Object.hasOwnProperty.call(m, "isSuspiciousLink"))
                        w.uint32(96).bool(m.isSuspiciousLink);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CtwaContextData.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.conversionSource = r.stringVerify();
                                m._conversionSource = "conversionSource";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.conversionData = r.bytes();
                                m._conversionData = "conversionData";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.sourceUrl = r.stringVerify();
                                m._sourceUrl = "sourceUrl";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.sourceId = r.stringVerify();
                                m._sourceId = "sourceId";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.sourceType = r.stringVerify();
                                m._sourceType = "sourceType";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.description = r.stringVerify();
                                m._description = "description";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                m.thumbnail = r.stringVerify();
                                m._thumbnail = "thumbnail";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                m.thumbnailUrl = r.stringVerify();
                                m._thumbnailUrl = "thumbnailUrl";
                                continue;
                            }
                        case 10: {
                                if (u !== 0)
                                    break;
                                m.mediaType = r.int32();
                                m._mediaType = "mediaType";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                m.mediaUrl = r.stringVerify();
                                m._mediaUrl = "mediaUrl";
                                continue;
                            }
                        case 12: {
                                if (u !== 0)
                                    break;
                                m.isSuspiciousLink = r.bool();
                                m._isSuspiciousLink = "isSuspiciousLink";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CtwaContextData.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ChatRowOpaqueData.DraftMessage.CtwaContextData: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData();
                    if (d.conversionSource != null) {
                        m.conversionSource = $String(d.conversionSource);
                    }
                    if (d.conversionData != null) {
                        if (typeof d.conversionData === "string")
                            $util.base64.decode(d.conversionData, m.conversionData = $util.newBuffer($util.base64.length(d.conversionData)), 0);
                        else if (d.conversionData.length >= 0)
                            m.conversionData = d.conversionData;
                    }
                    if (d.sourceUrl != null) {
                        m.sourceUrl = $String(d.sourceUrl);
                    }
                    if (d.sourceId != null) {
                        m.sourceId = $String(d.sourceId);
                    }
                    if (d.sourceType != null) {
                        m.sourceType = $String(d.sourceType);
                    }
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.description != null) {
                        m.description = $String(d.description);
                    }
                    if (d.thumbnail != null) {
                        m.thumbnail = $String(d.thumbnail);
                    }
                    if (d.thumbnailUrl != null) {
                        m.thumbnailUrl = $String(d.thumbnailUrl);
                    }
                    switch (d.mediaType) {
                    case "NONE":
                    case 0:
                        m.mediaType = 0;
                        break;
                    case "IMAGE":
                    case 1:
                        m.mediaType = 1;
                        break;
                    case "VIDEO":
                    case 2:
                        m.mediaType = 2;
                        break;
                    default:
                        if (typeof d.mediaType === "number" && (d.mediaType | 0) === d.mediaType)
                            m.mediaType = d.mediaType;
                    }
                    if (d.mediaUrl != null) {
                        m.mediaUrl = $String(d.mediaUrl);
                    }
                    if (d.isSuspiciousLink != null) {
                        m.isSuspiciousLink = $Boolean(d.isSuspiciousLink);
                    }
                    return m;
                };

                CtwaContextData.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.conversionSource != null && $Object.hasOwnProperty.call(m, "conversionSource")) {
                        d.conversionSource = m.conversionSource;
                    }
                    if (m.conversionData != null && $Object.hasOwnProperty.call(m, "conversionData")) {
                        d.conversionData = o.bytes === $String ? $util.base64.encode(m.conversionData, 0, m.conversionData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.conversionData) : m.conversionData;
                    }
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl")) {
                        d.sourceUrl = m.sourceUrl;
                    }
                    if (m.sourceId != null && $Object.hasOwnProperty.call(m, "sourceId")) {
                        d.sourceId = m.sourceId;
                    }
                    if (m.sourceType != null && $Object.hasOwnProperty.call(m, "sourceType")) {
                        d.sourceType = m.sourceType;
                    }
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                        d.description = m.description;
                    }
                    if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail")) {
                        d.thumbnail = m.thumbnail;
                    }
                    if (m.thumbnailUrl != null && $Object.hasOwnProperty.call(m, "thumbnailUrl")) {
                        d.thumbnailUrl = m.thumbnailUrl;
                    }
                    if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType")) {
                        d.mediaType = o.enums === $String ? $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType[m.mediaType] === $undefined ? m.mediaType : $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextData.ContextInfoExternalAdReplyInfoMediaType[m.mediaType] : m.mediaType;
                    }
                    if (m.mediaUrl != null && $Object.hasOwnProperty.call(m, "mediaUrl")) {
                        d.mediaUrl = m.mediaUrl;
                    }
                    if (m.isSuspiciousLink != null && $Object.hasOwnProperty.call(m, "isSuspiciousLink")) {
                        d.isSuspiciousLink = m.isSuspiciousLink;
                    }
                    return d;
                };

                CtwaContextData.prototype.toJSON = function() {
                    return CtwaContextData.toObject(this, $protobuf.util.toJSONOptions);
                };

                CtwaContextData.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ChatRowOpaqueData.DraftMessage.CtwaContextData";
                };

                CtwaContextData.ContextInfoExternalAdReplyInfoMediaType = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "NONE"] = 0;
                    values[valuesById[1] = "IMAGE"] = 1;
                    values[valuesById[2] = "VIDEO"] = 2;
                    return values;
                })();

                return CtwaContextData;
            })();

            DraftMessage.CtwaContextLinkData = (function() {

                const CtwaContextLinkData = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CtwaContextLinkData.prototype.context = null;
                CtwaContextLinkData.prototype.sourceUrl = null;
                CtwaContextLinkData.prototype.icebreaker = null;
                CtwaContextLinkData.prototype.phone = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextLinkData.prototype, "_context", {
                    get: $util.oneOfGetter($oneOfFields = ["context"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextLinkData.prototype, "_sourceUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextLinkData.prototype, "_icebreaker", {
                    get: $util.oneOfGetter($oneOfFields = ["icebreaker"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CtwaContextLinkData.prototype, "_phone", {
                    get: $util.oneOfGetter($oneOfFields = ["phone"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CtwaContextLinkData.create = function(properties) {
                    return new CtwaContextLinkData(properties);
                };

                CtwaContextLinkData.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.context != null && $Object.hasOwnProperty.call(m, "context"))
                        w.uint32(10).string(m.context);
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl"))
                        w.uint32(18).string(m.sourceUrl);
                    if (m.icebreaker != null && $Object.hasOwnProperty.call(m, "icebreaker"))
                        w.uint32(26).string(m.icebreaker);
                    if (m.phone != null && $Object.hasOwnProperty.call(m, "phone"))
                        w.uint32(34).string(m.phone);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CtwaContextLinkData.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.context = r.stringVerify();
                                m._context = "context";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.sourceUrl = r.stringVerify();
                                m._sourceUrl = "sourceUrl";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.icebreaker = r.stringVerify();
                                m._icebreaker = "icebreaker";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.phone = r.stringVerify();
                                m._phone = "phone";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CtwaContextLinkData.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData();
                    if (d.context != null) {
                        m.context = $String(d.context);
                    }
                    if (d.sourceUrl != null) {
                        m.sourceUrl = $String(d.sourceUrl);
                    }
                    if (d.icebreaker != null) {
                        m.icebreaker = $String(d.icebreaker);
                    }
                    if (d.phone != null) {
                        m.phone = $String(d.phone);
                    }
                    return m;
                };

                CtwaContextLinkData.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.context != null && $Object.hasOwnProperty.call(m, "context")) {
                        d.context = m.context;
                    }
                    if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl")) {
                        d.sourceUrl = m.sourceUrl;
                    }
                    if (m.icebreaker != null && $Object.hasOwnProperty.call(m, "icebreaker")) {
                        d.icebreaker = m.icebreaker;
                    }
                    if (m.phone != null && $Object.hasOwnProperty.call(m, "phone")) {
                        d.phone = m.phone;
                    }
                    return d;
                };

                CtwaContextLinkData.prototype.toJSON = function() {
                    return CtwaContextLinkData.toObject(this, $protobuf.util.toJSONOptions);
                };

                CtwaContextLinkData.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ChatRowOpaqueData.DraftMessage.CtwaContextLinkData";
                };

                return CtwaContextLinkData;
            })();

            return DraftMessage;
        })();

        return ChatRowOpaqueData;
    })();

    proto.Citation = (function() {

        const Citation = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Citation.prototype.title = "";
        Citation.prototype.subtitle = "";
        Citation.prototype.cmsId = "";
        Citation.prototype.imageUrl = "";

        Citation.create = function(properties) {
            return new Citation(properties);
        };

        Citation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).string(m.title);
            w.uint32(18).string(m.subtitle);
            w.uint32(26).string(m.cmsId);
            w.uint32(34).string(m.imageUrl);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Citation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Citation();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.title = r.stringVerify();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.subtitle = r.stringVerify();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.cmsId = r.stringVerify();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.imageUrl = r.stringVerify();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "title"))
                throw $util.ProtocolError("missing required 'title'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "subtitle"))
                throw $util.ProtocolError("missing required 'subtitle'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "cmsId"))
                throw $util.ProtocolError("missing required 'cmsId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "imageUrl"))
                throw $util.ProtocolError("missing required 'imageUrl'", { instance: m });
            return m;
        };

        Citation.fromObject = function (d, q) {
            if (d instanceof $root.proto.Citation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Citation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Citation();
            if (d.title != null) {
                m.title = $String(d.title);
            }
            if (d.subtitle != null) {
                m.subtitle = $String(d.subtitle);
            }
            if (d.cmsId != null) {
                m.cmsId = $String(d.cmsId);
            }
            if (d.imageUrl != null) {
                m.imageUrl = $String(d.imageUrl);
            }
            return m;
        };

        Citation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                d.title = "";
                d.subtitle = "";
                d.cmsId = "";
                d.imageUrl = "";
            }
            if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                d.title = m.title;
            }
            if (m.subtitle != null && $Object.hasOwnProperty.call(m, "subtitle")) {
                d.subtitle = m.subtitle;
            }
            if (m.cmsId != null && $Object.hasOwnProperty.call(m, "cmsId")) {
                d.cmsId = m.cmsId;
            }
            if (m.imageUrl != null && $Object.hasOwnProperty.call(m, "imageUrl")) {
                d.imageUrl = m.imageUrl;
            }
            return d;
        };

        Citation.prototype.toJSON = function() {
            return Citation.toObject(this, $protobuf.util.toJSONOptions);
        };

        Citation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Citation";
        };

        return Citation;
    })();

    proto.ClientPairingProps = (function() {

        const ClientPairingProps = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ClientPairingProps.prototype.isChatDbLidMigrated = null;
        ClientPairingProps.prototype.isSyncdPureLidSession = null;
        ClientPairingProps.prototype.isSyncdSnapshotRecoveryEnabled = null;
        ClientPairingProps.prototype.isHsThumbnailSyncEnabled = null;
        ClientPairingProps.prototype.subscriptionSyncPayload = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPairingProps.prototype, "_isChatDbLidMigrated", {
            get: $util.oneOfGetter($oneOfFields = ["isChatDbLidMigrated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPairingProps.prototype, "_isSyncdPureLidSession", {
            get: $util.oneOfGetter($oneOfFields = ["isSyncdPureLidSession"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPairingProps.prototype, "_isSyncdSnapshotRecoveryEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isSyncdSnapshotRecoveryEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPairingProps.prototype, "_isHsThumbnailSyncEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["isHsThumbnailSyncEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPairingProps.prototype, "_subscriptionSyncPayload", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionSyncPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ClientPairingProps.create = function(properties) {
            return new ClientPairingProps(properties);
        };

        ClientPairingProps.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.isChatDbLidMigrated != null && $Object.hasOwnProperty.call(m, "isChatDbLidMigrated"))
                w.uint32(8).bool(m.isChatDbLidMigrated);
            if (m.isSyncdPureLidSession != null && $Object.hasOwnProperty.call(m, "isSyncdPureLidSession"))
                w.uint32(16).bool(m.isSyncdPureLidSession);
            if (m.isSyncdSnapshotRecoveryEnabled != null && $Object.hasOwnProperty.call(m, "isSyncdSnapshotRecoveryEnabled"))
                w.uint32(24).bool(m.isSyncdSnapshotRecoveryEnabled);
            if (m.isHsThumbnailSyncEnabled != null && $Object.hasOwnProperty.call(m, "isHsThumbnailSyncEnabled"))
                w.uint32(32).bool(m.isHsThumbnailSyncEnabled);
            if (m.subscriptionSyncPayload != null && $Object.hasOwnProperty.call(m, "subscriptionSyncPayload"))
                w.uint32(42).bytes(m.subscriptionSyncPayload);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ClientPairingProps.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPairingProps();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.isChatDbLidMigrated = r.bool();
                        m._isChatDbLidMigrated = "isChatDbLidMigrated";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.isSyncdPureLidSession = r.bool();
                        m._isSyncdPureLidSession = "isSyncdPureLidSession";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.isSyncdSnapshotRecoveryEnabled = r.bool();
                        m._isSyncdSnapshotRecoveryEnabled = "isSyncdSnapshotRecoveryEnabled";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.isHsThumbnailSyncEnabled = r.bool();
                        m._isHsThumbnailSyncEnabled = "isHsThumbnailSyncEnabled";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.subscriptionSyncPayload = r.bytes();
                        m._subscriptionSyncPayload = "subscriptionSyncPayload";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ClientPairingProps.fromObject = function (d, q) {
            if (d instanceof $root.proto.ClientPairingProps)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ClientPairingProps: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ClientPairingProps();
            if (d.isChatDbLidMigrated != null) {
                m.isChatDbLidMigrated = $Boolean(d.isChatDbLidMigrated);
            }
            if (d.isSyncdPureLidSession != null) {
                m.isSyncdPureLidSession = $Boolean(d.isSyncdPureLidSession);
            }
            if (d.isSyncdSnapshotRecoveryEnabled != null) {
                m.isSyncdSnapshotRecoveryEnabled = $Boolean(d.isSyncdSnapshotRecoveryEnabled);
            }
            if (d.isHsThumbnailSyncEnabled != null) {
                m.isHsThumbnailSyncEnabled = $Boolean(d.isHsThumbnailSyncEnabled);
            }
            if (d.subscriptionSyncPayload != null) {
                if (typeof d.subscriptionSyncPayload === "string")
                    $util.base64.decode(d.subscriptionSyncPayload, m.subscriptionSyncPayload = $util.newBuffer($util.base64.length(d.subscriptionSyncPayload)), 0);
                else if (d.subscriptionSyncPayload.length >= 0)
                    m.subscriptionSyncPayload = d.subscriptionSyncPayload;
            }
            return m;
        };

        ClientPairingProps.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.isChatDbLidMigrated != null && $Object.hasOwnProperty.call(m, "isChatDbLidMigrated")) {
                d.isChatDbLidMigrated = m.isChatDbLidMigrated;
            }
            if (m.isSyncdPureLidSession != null && $Object.hasOwnProperty.call(m, "isSyncdPureLidSession")) {
                d.isSyncdPureLidSession = m.isSyncdPureLidSession;
            }
            if (m.isSyncdSnapshotRecoveryEnabled != null && $Object.hasOwnProperty.call(m, "isSyncdSnapshotRecoveryEnabled")) {
                d.isSyncdSnapshotRecoveryEnabled = m.isSyncdSnapshotRecoveryEnabled;
            }
            if (m.isHsThumbnailSyncEnabled != null && $Object.hasOwnProperty.call(m, "isHsThumbnailSyncEnabled")) {
                d.isHsThumbnailSyncEnabled = m.isHsThumbnailSyncEnabled;
            }
            if (m.subscriptionSyncPayload != null && $Object.hasOwnProperty.call(m, "subscriptionSyncPayload")) {
                d.subscriptionSyncPayload = o.bytes === $String ? $util.base64.encode(m.subscriptionSyncPayload, 0, m.subscriptionSyncPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.subscriptionSyncPayload) : m.subscriptionSyncPayload;
            }
            return d;
        };

        ClientPairingProps.prototype.toJSON = function() {
            return ClientPairingProps.toObject(this, $protobuf.util.toJSONOptions);
        };

        ClientPairingProps.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ClientPairingProps";
        };

        return ClientPairingProps;
    })();

    proto.ClientPayload = (function() {

        const ClientPayload = function (p) {
            this.shards = [];
            this.pairedPeripherals = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ClientPayload.prototype.username = null;
        ClientPayload.prototype.passive = null;
        ClientPayload.prototype.userAgent = null;
        ClientPayload.prototype.webInfo = null;
        ClientPayload.prototype.pushName = null;
        ClientPayload.prototype.sessionId = null;
        ClientPayload.prototype.shortConnect = null;
        ClientPayload.prototype.connectType = null;
        ClientPayload.prototype.connectReason = null;
        ClientPayload.prototype.shards = $util.emptyArray;
        ClientPayload.prototype.dnsSource = null;
        ClientPayload.prototype.connectAttemptCount = null;
        ClientPayload.prototype.device = null;
        ClientPayload.prototype.devicePairingData = null;
        ClientPayload.prototype.product = null;
        ClientPayload.prototype.fbCat = null;
        ClientPayload.prototype.fbUserAgent = null;
        ClientPayload.prototype.oc = null;
        ClientPayload.prototype.lc = null;
        ClientPayload.prototype.iosAppExtension = null;
        ClientPayload.prototype.fbAppId = null;
        ClientPayload.prototype.fbDeviceId = null;
        ClientPayload.prototype.pull = null;
        ClientPayload.prototype.paddingBytes = null;
        ClientPayload.prototype.yearClass = null;
        ClientPayload.prototype.memClass = null;
        ClientPayload.prototype.interopData = null;
        ClientPayload.prototype.trafficAnonymization = null;
        ClientPayload.prototype.lidDbMigrated = null;
        ClientPayload.prototype.accountType = null;
        ClientPayload.prototype.connectionSequenceInfo = null;
        ClientPayload.prototype.paaLink = null;
        ClientPayload.prototype.preacksCount = null;
        ClientPayload.prototype.processingQueueSize = null;
        ClientPayload.prototype.pairedPeripherals = $util.emptyArray;
        ClientPayload.prototype.testIsolationId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_passive", {
            get: $util.oneOfGetter($oneOfFields = ["passive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_userAgent", {
            get: $util.oneOfGetter($oneOfFields = ["userAgent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_webInfo", {
            get: $util.oneOfGetter($oneOfFields = ["webInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_pushName", {
            get: $util.oneOfGetter($oneOfFields = ["pushName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_sessionId", {
            get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_shortConnect", {
            get: $util.oneOfGetter($oneOfFields = ["shortConnect"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_connectType", {
            get: $util.oneOfGetter($oneOfFields = ["connectType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_connectReason", {
            get: $util.oneOfGetter($oneOfFields = ["connectReason"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_dnsSource", {
            get: $util.oneOfGetter($oneOfFields = ["dnsSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_connectAttemptCount", {
            get: $util.oneOfGetter($oneOfFields = ["connectAttemptCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_device", {
            get: $util.oneOfGetter($oneOfFields = ["device"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_devicePairingData", {
            get: $util.oneOfGetter($oneOfFields = ["devicePairingData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_product", {
            get: $util.oneOfGetter($oneOfFields = ["product"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_fbCat", {
            get: $util.oneOfGetter($oneOfFields = ["fbCat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_fbUserAgent", {
            get: $util.oneOfGetter($oneOfFields = ["fbUserAgent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_oc", {
            get: $util.oneOfGetter($oneOfFields = ["oc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_lc", {
            get: $util.oneOfGetter($oneOfFields = ["lc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_iosAppExtension", {
            get: $util.oneOfGetter($oneOfFields = ["iosAppExtension"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_fbAppId", {
            get: $util.oneOfGetter($oneOfFields = ["fbAppId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_fbDeviceId", {
            get: $util.oneOfGetter($oneOfFields = ["fbDeviceId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_pull", {
            get: $util.oneOfGetter($oneOfFields = ["pull"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_paddingBytes", {
            get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_yearClass", {
            get: $util.oneOfGetter($oneOfFields = ["yearClass"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_memClass", {
            get: $util.oneOfGetter($oneOfFields = ["memClass"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_interopData", {
            get: $util.oneOfGetter($oneOfFields = ["interopData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_trafficAnonymization", {
            get: $util.oneOfGetter($oneOfFields = ["trafficAnonymization"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_lidDbMigrated", {
            get: $util.oneOfGetter($oneOfFields = ["lidDbMigrated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_connectionSequenceInfo", {
            get: $util.oneOfGetter($oneOfFields = ["connectionSequenceInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_paaLink", {
            get: $util.oneOfGetter($oneOfFields = ["paaLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_preacksCount", {
            get: $util.oneOfGetter($oneOfFields = ["preacksCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_processingQueueSize", {
            get: $util.oneOfGetter($oneOfFields = ["processingQueueSize"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ClientPayload.prototype, "_testIsolationId", {
            get: $util.oneOfGetter($oneOfFields = ["testIsolationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ClientPayload.create = function(properties) {
            return new ClientPayload(properties);
        };

        ClientPayload.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.username != null && $Object.hasOwnProperty.call(m, "username"))
                w.uint32(8).uint64(m.username);
            if (m.passive != null && $Object.hasOwnProperty.call(m, "passive"))
                w.uint32(24).bool(m.passive);
            if (m.userAgent != null && $Object.hasOwnProperty.call(m, "userAgent"))
                $root.proto.ClientPayload.UserAgent.encode(m.userAgent, w.uint32(42).fork(), q + 1).ldelim();
            if (m.webInfo != null && $Object.hasOwnProperty.call(m, "webInfo"))
                $root.proto.ClientPayload.WebInfo.encode(m.webInfo, w.uint32(50).fork(), q + 1).ldelim();
            if (m.pushName != null && $Object.hasOwnProperty.call(m, "pushName"))
                w.uint32(58).string(m.pushName);
            if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId"))
                w.uint32(77).sfixed32(m.sessionId);
            if (m.shortConnect != null && $Object.hasOwnProperty.call(m, "shortConnect"))
                w.uint32(80).bool(m.shortConnect);
            if (m.connectType != null && $Object.hasOwnProperty.call(m, "connectType"))
                w.uint32(96).int32(m.connectType);
            if (m.connectReason != null && $Object.hasOwnProperty.call(m, "connectReason"))
                w.uint32(104).int32(m.connectReason);
            if (m.shards != null && m.shards.length) {
                w.uint32(114).int32s(m.shards);
            }
            if (m.dnsSource != null && $Object.hasOwnProperty.call(m, "dnsSource"))
                $root.proto.ClientPayload.DNSSource.encode(m.dnsSource, w.uint32(122).fork(), q + 1).ldelim();
            if (m.connectAttemptCount != null && $Object.hasOwnProperty.call(m, "connectAttemptCount"))
                w.uint32(128).uint32(m.connectAttemptCount);
            if (m.device != null && $Object.hasOwnProperty.call(m, "device"))
                w.uint32(144).uint32(m.device);
            if (m.devicePairingData != null && $Object.hasOwnProperty.call(m, "devicePairingData"))
                $root.proto.ClientPayload.DevicePairingRegistrationData.encode(m.devicePairingData, w.uint32(154).fork(), q + 1).ldelim();
            if (m.product != null && $Object.hasOwnProperty.call(m, "product"))
                w.uint32(160).int32(m.product);
            if (m.fbCat != null && $Object.hasOwnProperty.call(m, "fbCat"))
                w.uint32(170).bytes(m.fbCat);
            if (m.fbUserAgent != null && $Object.hasOwnProperty.call(m, "fbUserAgent"))
                w.uint32(178).bytes(m.fbUserAgent);
            if (m.oc != null && $Object.hasOwnProperty.call(m, "oc"))
                w.uint32(184).bool(m.oc);
            if (m.lc != null && $Object.hasOwnProperty.call(m, "lc"))
                w.uint32(192).int32(m.lc);
            if (m.iosAppExtension != null && $Object.hasOwnProperty.call(m, "iosAppExtension"))
                w.uint32(240).int32(m.iosAppExtension);
            if (m.fbAppId != null && $Object.hasOwnProperty.call(m, "fbAppId"))
                w.uint32(248).uint64(m.fbAppId);
            if (m.fbDeviceId != null && $Object.hasOwnProperty.call(m, "fbDeviceId"))
                w.uint32(258).bytes(m.fbDeviceId);
            if (m.pull != null && $Object.hasOwnProperty.call(m, "pull"))
                w.uint32(264).bool(m.pull);
            if (m.paddingBytes != null && $Object.hasOwnProperty.call(m, "paddingBytes"))
                w.uint32(274).bytes(m.paddingBytes);
            if (m.yearClass != null && $Object.hasOwnProperty.call(m, "yearClass"))
                w.uint32(288).int32(m.yearClass);
            if (m.memClass != null && $Object.hasOwnProperty.call(m, "memClass"))
                w.uint32(296).int32(m.memClass);
            if (m.interopData != null && $Object.hasOwnProperty.call(m, "interopData"))
                $root.proto.ClientPayload.InteropData.encode(m.interopData, w.uint32(306).fork(), q + 1).ldelim();
            if (m.trafficAnonymization != null && $Object.hasOwnProperty.call(m, "trafficAnonymization"))
                w.uint32(320).int32(m.trafficAnonymization);
            if (m.lidDbMigrated != null && $Object.hasOwnProperty.call(m, "lidDbMigrated"))
                w.uint32(328).bool(m.lidDbMigrated);
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(336).int32(m.accountType);
            if (m.connectionSequenceInfo != null && $Object.hasOwnProperty.call(m, "connectionSequenceInfo"))
                w.uint32(349).sfixed32(m.connectionSequenceInfo);
            if (m.paaLink != null && $Object.hasOwnProperty.call(m, "paaLink"))
                w.uint32(352).bool(m.paaLink);
            if (m.preacksCount != null && $Object.hasOwnProperty.call(m, "preacksCount"))
                w.uint32(360).int32(m.preacksCount);
            if (m.processingQueueSize != null && $Object.hasOwnProperty.call(m, "processingQueueSize"))
                w.uint32(368).int32(m.processingQueueSize);
            if (m.pairedPeripherals != null && m.pairedPeripherals.length) {
                for (var i = 0; i < m.pairedPeripherals.length; ++i)
                    w.uint32(378).string(m.pairedPeripherals[i]);
            }
            if (m.testIsolationId != null && $Object.hasOwnProperty.call(m, "testIsolationId"))
                w.uint32(386).bytes(m.testIsolationId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ClientPayload.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.username = r.uint64();
                        m._username = "username";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.passive = r.bool();
                        m._passive = "passive";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.userAgent = $root.proto.ClientPayload.UserAgent.decode(r, r.uint32(), $undefined, q + 1, m.userAgent);
                        m._userAgent = "userAgent";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.webInfo = $root.proto.ClientPayload.WebInfo.decode(r, r.uint32(), $undefined, q + 1, m.webInfo);
                        m._webInfo = "webInfo";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.pushName = r.stringVerify();
                        m._pushName = "pushName";
                        continue;
                    }
                case 9: {
                        if (u !== 5)
                            break;
                        m.sessionId = r.sfixed32();
                        m._sessionId = "sessionId";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.shortConnect = r.bool();
                        m._shortConnect = "shortConnect";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.connectType = r.int32();
                        m._connectType = "connectType";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        m.connectReason = r.int32();
                        m._connectReason = "connectReason";
                        continue;
                    }
                case 14: {
                        if (u === 2) {
                            if (!(m.shards && m.shards.length))
                                m.shards = [];
                            r.int32s(m.shards);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.shards && m.shards.length))
                            m.shards = [];
                        m.shards.push(r.int32());
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.dnsSource = $root.proto.ClientPayload.DNSSource.decode(r, r.uint32(), $undefined, q + 1, m.dnsSource);
                        m._dnsSource = "dnsSource";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        m.connectAttemptCount = r.uint32();
                        m._connectAttemptCount = "connectAttemptCount";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        m.device = r.uint32();
                        m._device = "device";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.devicePairingData = $root.proto.ClientPayload.DevicePairingRegistrationData.decode(r, r.uint32(), $undefined, q + 1, m.devicePairingData);
                        m._devicePairingData = "devicePairingData";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        m.product = r.int32();
                        m._product = "product";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        m.fbCat = r.bytes();
                        m._fbCat = "fbCat";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        m.fbUserAgent = r.bytes();
                        m._fbUserAgent = "fbUserAgent";
                        continue;
                    }
                case 23: {
                        if (u !== 0)
                            break;
                        m.oc = r.bool();
                        m._oc = "oc";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        m.lc = r.int32();
                        m._lc = "lc";
                        continue;
                    }
                case 30: {
                        if (u !== 0)
                            break;
                        m.iosAppExtension = r.int32();
                        m._iosAppExtension = "iosAppExtension";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        m.fbAppId = r.uint64();
                        m._fbAppId = "fbAppId";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        m.fbDeviceId = r.bytes();
                        m._fbDeviceId = "fbDeviceId";
                        continue;
                    }
                case 33: {
                        if (u !== 0)
                            break;
                        m.pull = r.bool();
                        m._pull = "pull";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        m.paddingBytes = r.bytes();
                        m._paddingBytes = "paddingBytes";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        m.yearClass = r.int32();
                        m._yearClass = "yearClass";
                        continue;
                    }
                case 37: {
                        if (u !== 0)
                            break;
                        m.memClass = r.int32();
                        m._memClass = "memClass";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.interopData = $root.proto.ClientPayload.InteropData.decode(r, r.uint32(), $undefined, q + 1, m.interopData);
                        m._interopData = "interopData";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        m.trafficAnonymization = r.int32();
                        m._trafficAnonymization = "trafficAnonymization";
                        continue;
                    }
                case 41: {
                        if (u !== 0)
                            break;
                        m.lidDbMigrated = r.bool();
                        m._lidDbMigrated = "lidDbMigrated";
                        continue;
                    }
                case 42: {
                        if (u !== 0)
                            break;
                        m.accountType = r.int32();
                        m._accountType = "accountType";
                        continue;
                    }
                case 43: {
                        if (u !== 5)
                            break;
                        m.connectionSequenceInfo = r.sfixed32();
                        m._connectionSequenceInfo = "connectionSequenceInfo";
                        continue;
                    }
                case 44: {
                        if (u !== 0)
                            break;
                        m.paaLink = r.bool();
                        m._paaLink = "paaLink";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        m.preacksCount = r.int32();
                        m._preacksCount = "preacksCount";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        m.processingQueueSize = r.int32();
                        m._processingQueueSize = "processingQueueSize";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        if (!(m.pairedPeripherals && m.pairedPeripherals.length))
                            m.pairedPeripherals = [];
                        m.pairedPeripherals.push(r.stringVerify());
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        m.testIsolationId = r.bytes();
                        m._testIsolationId = "testIsolationId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ClientPayload.fromObject = function (d, q) {
            if (d instanceof $root.proto.ClientPayload)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ClientPayload: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ClientPayload();
            if (d.username != null) {
                if ($util.Long)
                    m.username = $util.Long.fromValue(d.username, true);
                else if (typeof d.username === "string")
                    m.username = $parseInt(d.username, 10);
                else if (typeof d.username === "number")
                    m.username = d.username;
                else if (typeof d.username === "object")
                    m.username = new $util.LongBits(d.username.low >>> 0, d.username.high >>> 0).toNumber(true);
            }
            if (d.passive != null) {
                m.passive = $Boolean(d.passive);
            }
            if (d.userAgent != null) {
                if (!$util.isObject(d.userAgent))
                    throw $TypeError(".proto.ClientPayload.userAgent: object expected");
                m.userAgent = $root.proto.ClientPayload.UserAgent.fromObject(d.userAgent, q + 1);
            }
            if (d.webInfo != null) {
                if (!$util.isObject(d.webInfo))
                    throw $TypeError(".proto.ClientPayload.webInfo: object expected");
                m.webInfo = $root.proto.ClientPayload.WebInfo.fromObject(d.webInfo, q + 1);
            }
            if (d.pushName != null) {
                m.pushName = $String(d.pushName);
            }
            if (d.sessionId != null) {
                m.sessionId = d.sessionId | 0;
            }
            if (d.shortConnect != null) {
                m.shortConnect = $Boolean(d.shortConnect);
            }
            switch (d.connectType) {
            case "CELLULAR_UNKNOWN":
            case 0:
                m.connectType = 0;
                break;
            case "WIFI_UNKNOWN":
            case 1:
                m.connectType = 1;
                break;
            case "CELLULAR_EDGE":
            case 100:
                m.connectType = 100;
                break;
            case "CELLULAR_IDEN":
            case 101:
                m.connectType = 101;
                break;
            case "CELLULAR_UMTS":
            case 102:
                m.connectType = 102;
                break;
            case "CELLULAR_EVDO":
            case 103:
                m.connectType = 103;
                break;
            case "CELLULAR_GPRS":
            case 104:
                m.connectType = 104;
                break;
            case "CELLULAR_HSDPA":
            case 105:
                m.connectType = 105;
                break;
            case "CELLULAR_HSUPA":
            case 106:
                m.connectType = 106;
                break;
            case "CELLULAR_HSPA":
            case 107:
                m.connectType = 107;
                break;
            case "CELLULAR_CDMA":
            case 108:
                m.connectType = 108;
                break;
            case "CELLULAR_1XRTT":
            case 109:
                m.connectType = 109;
                break;
            case "CELLULAR_EHRPD":
            case 110:
                m.connectType = 110;
                break;
            case "CELLULAR_LTE":
            case 111:
                m.connectType = 111;
                break;
            case "CELLULAR_HSPAP":
            case 112:
                m.connectType = 112;
                break;
            default:
                if (typeof d.connectType === "number" && (d.connectType | 0) === d.connectType)
                    m.connectType = d.connectType;
            }
            switch (d.connectReason) {
            case "PUSH":
            case 0:
                m.connectReason = 0;
                break;
            case "USER_ACTIVATED":
            case 1:
                m.connectReason = 1;
                break;
            case "SCHEDULED":
            case 2:
                m.connectReason = 2;
                break;
            case "ERROR_RECONNECT":
            case 3:
                m.connectReason = 3;
                break;
            case "NETWORK_SWITCH":
            case 4:
                m.connectReason = 4;
                break;
            case "PING_RECONNECT":
            case 5:
                m.connectReason = 5;
                break;
            case "UNKNOWN":
            case 6:
                m.connectReason = 6;
                break;
            default:
                if (typeof d.connectReason === "number" && (d.connectReason | 0) === d.connectReason)
                    m.connectReason = d.connectReason;
            }
            if (d.shards) {
                if (!$Array.isArray(d.shards))
                    throw $TypeError(".proto.ClientPayload.shards: array expected");
                m.shards = $Array(d.shards.length);
                for (var i = 0; i < d.shards.length; ++i) {
                    m.shards[i] = d.shards[i] | 0;
                }
            }
            if (d.dnsSource != null) {
                if (!$util.isObject(d.dnsSource))
                    throw $TypeError(".proto.ClientPayload.dnsSource: object expected");
                m.dnsSource = $root.proto.ClientPayload.DNSSource.fromObject(d.dnsSource, q + 1);
            }
            if (d.connectAttemptCount != null) {
                m.connectAttemptCount = d.connectAttemptCount >>> 0;
            }
            if (d.device != null) {
                m.device = d.device >>> 0;
            }
            if (d.devicePairingData != null) {
                if (!$util.isObject(d.devicePairingData))
                    throw $TypeError(".proto.ClientPayload.devicePairingData: object expected");
                m.devicePairingData = $root.proto.ClientPayload.DevicePairingRegistrationData.fromObject(d.devicePairingData, q + 1);
            }
            switch (d.product) {
            case "WHATSAPP":
            case 0:
                m.product = 0;
                break;
            case "MESSENGER":
            case 1:
                m.product = 1;
                break;
            case "INTEROP":
            case 2:
                m.product = 2;
                break;
            case "INTEROP_MSGR":
            case 3:
                m.product = 3;
                break;
            case "WHATSAPP_LID":
            case 4:
                m.product = 4;
                break;
            default:
                if (typeof d.product === "number" && (d.product | 0) === d.product)
                    m.product = d.product;
            }
            if (d.fbCat != null) {
                if (typeof d.fbCat === "string")
                    $util.base64.decode(d.fbCat, m.fbCat = $util.newBuffer($util.base64.length(d.fbCat)), 0);
                else if (d.fbCat.length >= 0)
                    m.fbCat = d.fbCat;
            }
            if (d.fbUserAgent != null) {
                if (typeof d.fbUserAgent === "string")
                    $util.base64.decode(d.fbUserAgent, m.fbUserAgent = $util.newBuffer($util.base64.length(d.fbUserAgent)), 0);
                else if (d.fbUserAgent.length >= 0)
                    m.fbUserAgent = d.fbUserAgent;
            }
            if (d.oc != null) {
                m.oc = $Boolean(d.oc);
            }
            if (d.lc != null) {
                m.lc = d.lc | 0;
            }
            switch (d.iosAppExtension) {
            case "SHARE_EXTENSION":
            case 0:
                m.iosAppExtension = 0;
                break;
            case "SERVICE_EXTENSION":
            case 1:
                m.iosAppExtension = 1;
                break;
            case "INTENTS_EXTENSION":
            case 2:
                m.iosAppExtension = 2;
                break;
            default:
                if (typeof d.iosAppExtension === "number" && (d.iosAppExtension | 0) === d.iosAppExtension)
                    m.iosAppExtension = d.iosAppExtension;
            }
            if (d.fbAppId != null) {
                if ($util.Long)
                    m.fbAppId = $util.Long.fromValue(d.fbAppId, true);
                else if (typeof d.fbAppId === "string")
                    m.fbAppId = $parseInt(d.fbAppId, 10);
                else if (typeof d.fbAppId === "number")
                    m.fbAppId = d.fbAppId;
                else if (typeof d.fbAppId === "object")
                    m.fbAppId = new $util.LongBits(d.fbAppId.low >>> 0, d.fbAppId.high >>> 0).toNumber(true);
            }
            if (d.fbDeviceId != null) {
                if (typeof d.fbDeviceId === "string")
                    $util.base64.decode(d.fbDeviceId, m.fbDeviceId = $util.newBuffer($util.base64.length(d.fbDeviceId)), 0);
                else if (d.fbDeviceId.length >= 0)
                    m.fbDeviceId = d.fbDeviceId;
            }
            if (d.pull != null) {
                m.pull = $Boolean(d.pull);
            }
            if (d.paddingBytes != null) {
                if (typeof d.paddingBytes === "string")
                    $util.base64.decode(d.paddingBytes, m.paddingBytes = $util.newBuffer($util.base64.length(d.paddingBytes)), 0);
                else if (d.paddingBytes.length >= 0)
                    m.paddingBytes = d.paddingBytes;
            }
            if (d.yearClass != null) {
                m.yearClass = d.yearClass | 0;
            }
            if (d.memClass != null) {
                m.memClass = d.memClass | 0;
            }
            if (d.interopData != null) {
                if (!$util.isObject(d.interopData))
                    throw $TypeError(".proto.ClientPayload.interopData: object expected");
                m.interopData = $root.proto.ClientPayload.InteropData.fromObject(d.interopData, q + 1);
            }
            switch (d.trafficAnonymization) {
            case "OFF":
            case 0:
                m.trafficAnonymization = 0;
                break;
            case "STANDARD":
            case 1:
                m.trafficAnonymization = 1;
                break;
            default:
                if (typeof d.trafficAnonymization === "number" && (d.trafficAnonymization | 0) === d.trafficAnonymization)
                    m.trafficAnonymization = d.trafficAnonymization;
            }
            if (d.lidDbMigrated != null) {
                m.lidDbMigrated = $Boolean(d.lidDbMigrated);
            }
            switch (d.accountType) {
            case "DEFAULT":
            case 0:
                m.accountType = 0;
                break;
            case "GUEST":
            case 1:
                m.accountType = 1;
                break;
            default:
                if (typeof d.accountType === "number" && (d.accountType | 0) === d.accountType)
                    m.accountType = d.accountType;
            }
            if (d.connectionSequenceInfo != null) {
                m.connectionSequenceInfo = d.connectionSequenceInfo | 0;
            }
            if (d.paaLink != null) {
                m.paaLink = $Boolean(d.paaLink);
            }
            if (d.preacksCount != null) {
                m.preacksCount = d.preacksCount | 0;
            }
            if (d.processingQueueSize != null) {
                m.processingQueueSize = d.processingQueueSize | 0;
            }
            if (d.pairedPeripherals) {
                if (!$Array.isArray(d.pairedPeripherals))
                    throw $TypeError(".proto.ClientPayload.pairedPeripherals: array expected");
                m.pairedPeripherals = $Array(d.pairedPeripherals.length);
                for (var i = 0; i < d.pairedPeripherals.length; ++i) {
                    m.pairedPeripherals[i] = $String(d.pairedPeripherals[i]);
                }
            }
            if (d.testIsolationId != null) {
                if (typeof d.testIsolationId === "string")
                    $util.base64.decode(d.testIsolationId, m.testIsolationId = $util.newBuffer($util.base64.length(d.testIsolationId)), 0);
                else if (d.testIsolationId.length >= 0)
                    m.testIsolationId = d.testIsolationId;
            }
            return m;
        };

        ClientPayload.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.shards = [];
                d.pairedPeripherals = [];
            }
            if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.username = typeof m.username === "number" ? $BigInt(m.username) : $util.Long.fromBits(m.username.low >>> 0, m.username.high >>> 0, true).toBigInt();
                else if (typeof m.username === "number")
                    d.username = o.longs === $String ? $String(m.username) : m.username;
                else
                    d.username = o.longs === $String ? $util.Long.prototype.toString.call(m.username) : o.longs === $Number ? new $util.LongBits(m.username.low >>> 0, m.username.high >>> 0).toNumber(true) : m.username;
            }
            if (m.passive != null && $Object.hasOwnProperty.call(m, "passive")) {
                d.passive = m.passive;
            }
            if (m.userAgent != null && $Object.hasOwnProperty.call(m, "userAgent")) {
                d.userAgent = $root.proto.ClientPayload.UserAgent.toObject(m.userAgent, o, q + 1);
            }
            if (m.webInfo != null && $Object.hasOwnProperty.call(m, "webInfo")) {
                d.webInfo = $root.proto.ClientPayload.WebInfo.toObject(m.webInfo, o, q + 1);
            }
            if (m.pushName != null && $Object.hasOwnProperty.call(m, "pushName")) {
                d.pushName = m.pushName;
            }
            if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId")) {
                d.sessionId = m.sessionId;
            }
            if (m.shortConnect != null && $Object.hasOwnProperty.call(m, "shortConnect")) {
                d.shortConnect = m.shortConnect;
            }
            if (m.connectType != null && $Object.hasOwnProperty.call(m, "connectType")) {
                d.connectType = o.enums === $String ? $root.proto.ClientPayload.ConnectType[m.connectType] === $undefined ? m.connectType : $root.proto.ClientPayload.ConnectType[m.connectType] : m.connectType;
            }
            if (m.connectReason != null && $Object.hasOwnProperty.call(m, "connectReason")) {
                d.connectReason = o.enums === $String ? $root.proto.ClientPayload.ConnectReason[m.connectReason] === $undefined ? m.connectReason : $root.proto.ClientPayload.ConnectReason[m.connectReason] : m.connectReason;
            }
            if (m.shards && m.shards.length) {
                d.shards = $Array(m.shards.length);
                for (var j = 0; j < m.shards.length; ++j) {
                    d.shards[j] = m.shards[j];
                }
            }
            if (m.dnsSource != null && $Object.hasOwnProperty.call(m, "dnsSource")) {
                d.dnsSource = $root.proto.ClientPayload.DNSSource.toObject(m.dnsSource, o, q + 1);
            }
            if (m.connectAttemptCount != null && $Object.hasOwnProperty.call(m, "connectAttemptCount")) {
                d.connectAttemptCount = m.connectAttemptCount;
            }
            if (m.device != null && $Object.hasOwnProperty.call(m, "device")) {
                d.device = m.device;
            }
            if (m.devicePairingData != null && $Object.hasOwnProperty.call(m, "devicePairingData")) {
                d.devicePairingData = $root.proto.ClientPayload.DevicePairingRegistrationData.toObject(m.devicePairingData, o, q + 1);
            }
            if (m.product != null && $Object.hasOwnProperty.call(m, "product")) {
                d.product = o.enums === $String ? $root.proto.ClientPayload.Product[m.product] === $undefined ? m.product : $root.proto.ClientPayload.Product[m.product] : m.product;
            }
            if (m.fbCat != null && $Object.hasOwnProperty.call(m, "fbCat")) {
                d.fbCat = o.bytes === $String ? $util.base64.encode(m.fbCat, 0, m.fbCat.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fbCat) : m.fbCat;
            }
            if (m.fbUserAgent != null && $Object.hasOwnProperty.call(m, "fbUserAgent")) {
                d.fbUserAgent = o.bytes === $String ? $util.base64.encode(m.fbUserAgent, 0, m.fbUserAgent.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fbUserAgent) : m.fbUserAgent;
            }
            if (m.oc != null && $Object.hasOwnProperty.call(m, "oc")) {
                d.oc = m.oc;
            }
            if (m.lc != null && $Object.hasOwnProperty.call(m, "lc")) {
                d.lc = m.lc;
            }
            if (m.iosAppExtension != null && $Object.hasOwnProperty.call(m, "iosAppExtension")) {
                d.iosAppExtension = o.enums === $String ? $root.proto.ClientPayload.IOSAppExtension[m.iosAppExtension] === $undefined ? m.iosAppExtension : $root.proto.ClientPayload.IOSAppExtension[m.iosAppExtension] : m.iosAppExtension;
            }
            if (m.fbAppId != null && $Object.hasOwnProperty.call(m, "fbAppId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.fbAppId = typeof m.fbAppId === "number" ? $BigInt(m.fbAppId) : $util.Long.fromBits(m.fbAppId.low >>> 0, m.fbAppId.high >>> 0, true).toBigInt();
                else if (typeof m.fbAppId === "number")
                    d.fbAppId = o.longs === $String ? $String(m.fbAppId) : m.fbAppId;
                else
                    d.fbAppId = o.longs === $String ? $util.Long.prototype.toString.call(m.fbAppId) : o.longs === $Number ? new $util.LongBits(m.fbAppId.low >>> 0, m.fbAppId.high >>> 0).toNumber(true) : m.fbAppId;
            }
            if (m.fbDeviceId != null && $Object.hasOwnProperty.call(m, "fbDeviceId")) {
                d.fbDeviceId = o.bytes === $String ? $util.base64.encode(m.fbDeviceId, 0, m.fbDeviceId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fbDeviceId) : m.fbDeviceId;
            }
            if (m.pull != null && $Object.hasOwnProperty.call(m, "pull")) {
                d.pull = m.pull;
            }
            if (m.paddingBytes != null && $Object.hasOwnProperty.call(m, "paddingBytes")) {
                d.paddingBytes = o.bytes === $String ? $util.base64.encode(m.paddingBytes, 0, m.paddingBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.paddingBytes) : m.paddingBytes;
            }
            if (m.yearClass != null && $Object.hasOwnProperty.call(m, "yearClass")) {
                d.yearClass = m.yearClass;
            }
            if (m.memClass != null && $Object.hasOwnProperty.call(m, "memClass")) {
                d.memClass = m.memClass;
            }
            if (m.interopData != null && $Object.hasOwnProperty.call(m, "interopData")) {
                d.interopData = $root.proto.ClientPayload.InteropData.toObject(m.interopData, o, q + 1);
            }
            if (m.trafficAnonymization != null && $Object.hasOwnProperty.call(m, "trafficAnonymization")) {
                d.trafficAnonymization = o.enums === $String ? $root.proto.ClientPayload.TrafficAnonymization[m.trafficAnonymization] === $undefined ? m.trafficAnonymization : $root.proto.ClientPayload.TrafficAnonymization[m.trafficAnonymization] : m.trafficAnonymization;
            }
            if (m.lidDbMigrated != null && $Object.hasOwnProperty.call(m, "lidDbMigrated")) {
                d.lidDbMigrated = m.lidDbMigrated;
            }
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType")) {
                d.accountType = o.enums === $String ? $root.proto.ClientPayload.AccountType[m.accountType] === $undefined ? m.accountType : $root.proto.ClientPayload.AccountType[m.accountType] : m.accountType;
            }
            if (m.connectionSequenceInfo != null && $Object.hasOwnProperty.call(m, "connectionSequenceInfo")) {
                d.connectionSequenceInfo = m.connectionSequenceInfo;
            }
            if (m.paaLink != null && $Object.hasOwnProperty.call(m, "paaLink")) {
                d.paaLink = m.paaLink;
            }
            if (m.preacksCount != null && $Object.hasOwnProperty.call(m, "preacksCount")) {
                d.preacksCount = m.preacksCount;
            }
            if (m.processingQueueSize != null && $Object.hasOwnProperty.call(m, "processingQueueSize")) {
                d.processingQueueSize = m.processingQueueSize;
            }
            if (m.pairedPeripherals && m.pairedPeripherals.length) {
                d.pairedPeripherals = $Array(m.pairedPeripherals.length);
                for (var j = 0; j < m.pairedPeripherals.length; ++j) {
                    d.pairedPeripherals[j] = m.pairedPeripherals[j];
                }
            }
            if (m.testIsolationId != null && $Object.hasOwnProperty.call(m, "testIsolationId")) {
                d.testIsolationId = o.bytes === $String ? $util.base64.encode(m.testIsolationId, 0, m.testIsolationId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.testIsolationId) : m.testIsolationId;
            }
            return d;
        };

        ClientPayload.prototype.toJSON = function() {
            return ClientPayload.toObject(this, $protobuf.util.toJSONOptions);
        };

        ClientPayload.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ClientPayload";
        };

        ClientPayload.AccountType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DEFAULT"] = 0;
            values[valuesById[1] = "GUEST"] = 1;
            return values;
        })();

        ClientPayload.ConnectReason = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "PUSH"] = 0;
            values[valuesById[1] = "USER_ACTIVATED"] = 1;
            values[valuesById[2] = "SCHEDULED"] = 2;
            values[valuesById[3] = "ERROR_RECONNECT"] = 3;
            values[valuesById[4] = "NETWORK_SWITCH"] = 4;
            values[valuesById[5] = "PING_RECONNECT"] = 5;
            values[valuesById[6] = "UNKNOWN"] = 6;
            return values;
        })();

        ClientPayload.ConnectType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "CELLULAR_UNKNOWN"] = 0;
            values[valuesById[1] = "WIFI_UNKNOWN"] = 1;
            values[valuesById[100] = "CELLULAR_EDGE"] = 100;
            values[valuesById[101] = "CELLULAR_IDEN"] = 101;
            values[valuesById[102] = "CELLULAR_UMTS"] = 102;
            values[valuesById[103] = "CELLULAR_EVDO"] = 103;
            values[valuesById[104] = "CELLULAR_GPRS"] = 104;
            values[valuesById[105] = "CELLULAR_HSDPA"] = 105;
            values[valuesById[106] = "CELLULAR_HSUPA"] = 106;
            values[valuesById[107] = "CELLULAR_HSPA"] = 107;
            values[valuesById[108] = "CELLULAR_CDMA"] = 108;
            values[valuesById[109] = "CELLULAR_1XRTT"] = 109;
            values[valuesById[110] = "CELLULAR_EHRPD"] = 110;
            values[valuesById[111] = "CELLULAR_LTE"] = 111;
            values[valuesById[112] = "CELLULAR_HSPAP"] = 112;
            return values;
        })();

        ClientPayload.DNSSource = (function() {

            const DNSSource = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DNSSource.prototype.dnsMethod = null;
            DNSSource.prototype.appCached = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DNSSource.prototype, "_dnsMethod", {
                get: $util.oneOfGetter($oneOfFields = ["dnsMethod"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DNSSource.prototype, "_appCached", {
                get: $util.oneOfGetter($oneOfFields = ["appCached"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DNSSource.create = function(properties) {
                return new DNSSource(properties);
            };

            DNSSource.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.dnsMethod != null && $Object.hasOwnProperty.call(m, "dnsMethod"))
                    w.uint32(120).int32(m.dnsMethod);
                if (m.appCached != null && $Object.hasOwnProperty.call(m, "appCached"))
                    w.uint32(128).bool(m.appCached);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DNSSource.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.DNSSource(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 15: {
                            if (u !== 0)
                                break;
                            m.dnsMethod = r.int32();
                            m._dnsMethod = "dnsMethod";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            m.appCached = r.bool();
                            m._appCached = "appCached";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DNSSource.fromObject = function (d, q) {
                if (d instanceof $root.proto.ClientPayload.DNSSource)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ClientPayload.DNSSource: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ClientPayload.DNSSource();
                switch (d.dnsMethod) {
                case "SYSTEM":
                case 0:
                    m.dnsMethod = 0;
                    break;
                case "GOOGLE":
                case 1:
                    m.dnsMethod = 1;
                    break;
                case "HARDCODED":
                case 2:
                    m.dnsMethod = 2;
                    break;
                case "OVERRIDE":
                case 3:
                    m.dnsMethod = 3;
                    break;
                case "FALLBACK":
                case 4:
                    m.dnsMethod = 4;
                    break;
                case "MNS":
                case 5:
                    m.dnsMethod = 5;
                    break;
                case "MNS_SECONDARY":
                case 6:
                    m.dnsMethod = 6;
                    break;
                case "SOCKS_PROXY":
                case 7:
                    m.dnsMethod = 7;
                    break;
                default:
                    if (typeof d.dnsMethod === "number" && (d.dnsMethod | 0) === d.dnsMethod)
                        m.dnsMethod = d.dnsMethod;
                }
                if (d.appCached != null) {
                    m.appCached = $Boolean(d.appCached);
                }
                return m;
            };

            DNSSource.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.dnsMethod != null && $Object.hasOwnProperty.call(m, "dnsMethod")) {
                    d.dnsMethod = o.enums === $String ? $root.proto.ClientPayload.DNSSource.DNSResolutionMethod[m.dnsMethod] === $undefined ? m.dnsMethod : $root.proto.ClientPayload.DNSSource.DNSResolutionMethod[m.dnsMethod] : m.dnsMethod;
                }
                if (m.appCached != null && $Object.hasOwnProperty.call(m, "appCached")) {
                    d.appCached = m.appCached;
                }
                return d;
            };

            DNSSource.prototype.toJSON = function() {
                return DNSSource.toObject(this, $protobuf.util.toJSONOptions);
            };

            DNSSource.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ClientPayload.DNSSource";
            };

            DNSSource.DNSResolutionMethod = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "SYSTEM"] = 0;
                values[valuesById[1] = "GOOGLE"] = 1;
                values[valuesById[2] = "HARDCODED"] = 2;
                values[valuesById[3] = "OVERRIDE"] = 3;
                values[valuesById[4] = "FALLBACK"] = 4;
                values[valuesById[5] = "MNS"] = 5;
                values[valuesById[6] = "MNS_SECONDARY"] = 6;
                values[valuesById[7] = "SOCKS_PROXY"] = 7;
                return values;
            })();

            return DNSSource;
        })();

        ClientPayload.DevicePairingRegistrationData = (function() {

            const DevicePairingRegistrationData = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DevicePairingRegistrationData.prototype.eRegid = null;
            DevicePairingRegistrationData.prototype.eKeytype = null;
            DevicePairingRegistrationData.prototype.eIdent = null;
            DevicePairingRegistrationData.prototype.eSkeyId = null;
            DevicePairingRegistrationData.prototype.eSkeyVal = null;
            DevicePairingRegistrationData.prototype.eSkeySig = null;
            DevicePairingRegistrationData.prototype.buildHash = null;
            DevicePairingRegistrationData.prototype.deviceProps = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_eRegid", {
                get: $util.oneOfGetter($oneOfFields = ["eRegid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_eKeytype", {
                get: $util.oneOfGetter($oneOfFields = ["eKeytype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_eIdent", {
                get: $util.oneOfGetter($oneOfFields = ["eIdent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_eSkeyId", {
                get: $util.oneOfGetter($oneOfFields = ["eSkeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_eSkeyVal", {
                get: $util.oneOfGetter($oneOfFields = ["eSkeyVal"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_eSkeySig", {
                get: $util.oneOfGetter($oneOfFields = ["eSkeySig"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_buildHash", {
                get: $util.oneOfGetter($oneOfFields = ["buildHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DevicePairingRegistrationData.prototype, "_deviceProps", {
                get: $util.oneOfGetter($oneOfFields = ["deviceProps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DevicePairingRegistrationData.create = function(properties) {
                return new DevicePairingRegistrationData(properties);
            };

            DevicePairingRegistrationData.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.eRegid != null && $Object.hasOwnProperty.call(m, "eRegid"))
                    w.uint32(10).bytes(m.eRegid);
                if (m.eKeytype != null && $Object.hasOwnProperty.call(m, "eKeytype"))
                    w.uint32(18).bytes(m.eKeytype);
                if (m.eIdent != null && $Object.hasOwnProperty.call(m, "eIdent"))
                    w.uint32(26).bytes(m.eIdent);
                if (m.eSkeyId != null && $Object.hasOwnProperty.call(m, "eSkeyId"))
                    w.uint32(34).bytes(m.eSkeyId);
                if (m.eSkeyVal != null && $Object.hasOwnProperty.call(m, "eSkeyVal"))
                    w.uint32(42).bytes(m.eSkeyVal);
                if (m.eSkeySig != null && $Object.hasOwnProperty.call(m, "eSkeySig"))
                    w.uint32(50).bytes(m.eSkeySig);
                if (m.buildHash != null && $Object.hasOwnProperty.call(m, "buildHash"))
                    w.uint32(58).bytes(m.buildHash);
                if (m.deviceProps != null && $Object.hasOwnProperty.call(m, "deviceProps"))
                    w.uint32(66).bytes(m.deviceProps);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DevicePairingRegistrationData.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.DevicePairingRegistrationData();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.eRegid = r.bytes();
                            m._eRegid = "eRegid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.eKeytype = r.bytes();
                            m._eKeytype = "eKeytype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.eIdent = r.bytes();
                            m._eIdent = "eIdent";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.eSkeyId = r.bytes();
                            m._eSkeyId = "eSkeyId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.eSkeyVal = r.bytes();
                            m._eSkeyVal = "eSkeyVal";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.eSkeySig = r.bytes();
                            m._eSkeySig = "eSkeySig";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.buildHash = r.bytes();
                            m._buildHash = "buildHash";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.deviceProps = r.bytes();
                            m._deviceProps = "deviceProps";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DevicePairingRegistrationData.fromObject = function (d, q) {
                if (d instanceof $root.proto.ClientPayload.DevicePairingRegistrationData)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ClientPayload.DevicePairingRegistrationData: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ClientPayload.DevicePairingRegistrationData();
                if (d.eRegid != null) {
                    if (typeof d.eRegid === "string")
                        $util.base64.decode(d.eRegid, m.eRegid = $util.newBuffer($util.base64.length(d.eRegid)), 0);
                    else if (d.eRegid.length >= 0)
                        m.eRegid = d.eRegid;
                }
                if (d.eKeytype != null) {
                    if (typeof d.eKeytype === "string")
                        $util.base64.decode(d.eKeytype, m.eKeytype = $util.newBuffer($util.base64.length(d.eKeytype)), 0);
                    else if (d.eKeytype.length >= 0)
                        m.eKeytype = d.eKeytype;
                }
                if (d.eIdent != null) {
                    if (typeof d.eIdent === "string")
                        $util.base64.decode(d.eIdent, m.eIdent = $util.newBuffer($util.base64.length(d.eIdent)), 0);
                    else if (d.eIdent.length >= 0)
                        m.eIdent = d.eIdent;
                }
                if (d.eSkeyId != null) {
                    if (typeof d.eSkeyId === "string")
                        $util.base64.decode(d.eSkeyId, m.eSkeyId = $util.newBuffer($util.base64.length(d.eSkeyId)), 0);
                    else if (d.eSkeyId.length >= 0)
                        m.eSkeyId = d.eSkeyId;
                }
                if (d.eSkeyVal != null) {
                    if (typeof d.eSkeyVal === "string")
                        $util.base64.decode(d.eSkeyVal, m.eSkeyVal = $util.newBuffer($util.base64.length(d.eSkeyVal)), 0);
                    else if (d.eSkeyVal.length >= 0)
                        m.eSkeyVal = d.eSkeyVal;
                }
                if (d.eSkeySig != null) {
                    if (typeof d.eSkeySig === "string")
                        $util.base64.decode(d.eSkeySig, m.eSkeySig = $util.newBuffer($util.base64.length(d.eSkeySig)), 0);
                    else if (d.eSkeySig.length >= 0)
                        m.eSkeySig = d.eSkeySig;
                }
                if (d.buildHash != null) {
                    if (typeof d.buildHash === "string")
                        $util.base64.decode(d.buildHash, m.buildHash = $util.newBuffer($util.base64.length(d.buildHash)), 0);
                    else if (d.buildHash.length >= 0)
                        m.buildHash = d.buildHash;
                }
                if (d.deviceProps != null) {
                    if (typeof d.deviceProps === "string")
                        $util.base64.decode(d.deviceProps, m.deviceProps = $util.newBuffer($util.base64.length(d.deviceProps)), 0);
                    else if (d.deviceProps.length >= 0)
                        m.deviceProps = d.deviceProps;
                }
                return m;
            };

            DevicePairingRegistrationData.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.eRegid != null && $Object.hasOwnProperty.call(m, "eRegid")) {
                    d.eRegid = o.bytes === $String ? $util.base64.encode(m.eRegid, 0, m.eRegid.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.eRegid) : m.eRegid;
                }
                if (m.eKeytype != null && $Object.hasOwnProperty.call(m, "eKeytype")) {
                    d.eKeytype = o.bytes === $String ? $util.base64.encode(m.eKeytype, 0, m.eKeytype.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.eKeytype) : m.eKeytype;
                }
                if (m.eIdent != null && $Object.hasOwnProperty.call(m, "eIdent")) {
                    d.eIdent = o.bytes === $String ? $util.base64.encode(m.eIdent, 0, m.eIdent.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.eIdent) : m.eIdent;
                }
                if (m.eSkeyId != null && $Object.hasOwnProperty.call(m, "eSkeyId")) {
                    d.eSkeyId = o.bytes === $String ? $util.base64.encode(m.eSkeyId, 0, m.eSkeyId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.eSkeyId) : m.eSkeyId;
                }
                if (m.eSkeyVal != null && $Object.hasOwnProperty.call(m, "eSkeyVal")) {
                    d.eSkeyVal = o.bytes === $String ? $util.base64.encode(m.eSkeyVal, 0, m.eSkeyVal.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.eSkeyVal) : m.eSkeyVal;
                }
                if (m.eSkeySig != null && $Object.hasOwnProperty.call(m, "eSkeySig")) {
                    d.eSkeySig = o.bytes === $String ? $util.base64.encode(m.eSkeySig, 0, m.eSkeySig.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.eSkeySig) : m.eSkeySig;
                }
                if (m.buildHash != null && $Object.hasOwnProperty.call(m, "buildHash")) {
                    d.buildHash = o.bytes === $String ? $util.base64.encode(m.buildHash, 0, m.buildHash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.buildHash) : m.buildHash;
                }
                if (m.deviceProps != null && $Object.hasOwnProperty.call(m, "deviceProps")) {
                    d.deviceProps = o.bytes === $String ? $util.base64.encode(m.deviceProps, 0, m.deviceProps.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.deviceProps) : m.deviceProps;
                }
                return d;
            };

            DevicePairingRegistrationData.prototype.toJSON = function() {
                return DevicePairingRegistrationData.toObject(this, $protobuf.util.toJSONOptions);
            };

            DevicePairingRegistrationData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ClientPayload.DevicePairingRegistrationData";
            };

            return DevicePairingRegistrationData;
        })();

        ClientPayload.IOSAppExtension = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "SHARE_EXTENSION"] = 0;
            values[valuesById[1] = "SERVICE_EXTENSION"] = 1;
            values[valuesById[2] = "INTENTS_EXTENSION"] = 2;
            return values;
        })();

        ClientPayload.InteropData = (function() {

            const InteropData = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InteropData.prototype.accountId = null;
            InteropData.prototype.token = null;
            InteropData.prototype.enableReadReceipts = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteropData.prototype, "_accountId", {
                get: $util.oneOfGetter($oneOfFields = ["accountId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteropData.prototype, "_token", {
                get: $util.oneOfGetter($oneOfFields = ["token"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteropData.prototype, "_enableReadReceipts", {
                get: $util.oneOfGetter($oneOfFields = ["enableReadReceipts"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InteropData.create = function(properties) {
                return new InteropData(properties);
            };

            InteropData.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.accountId != null && $Object.hasOwnProperty.call(m, "accountId"))
                    w.uint32(8).uint64(m.accountId);
                if (m.token != null && $Object.hasOwnProperty.call(m, "token"))
                    w.uint32(18).bytes(m.token);
                if (m.enableReadReceipts != null && $Object.hasOwnProperty.call(m, "enableReadReceipts"))
                    w.uint32(24).bool(m.enableReadReceipts);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InteropData.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.InteropData();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.accountId = r.uint64();
                            m._accountId = "accountId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.token = r.bytes();
                            m._token = "token";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.enableReadReceipts = r.bool();
                            m._enableReadReceipts = "enableReadReceipts";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InteropData.fromObject = function (d, q) {
                if (d instanceof $root.proto.ClientPayload.InteropData)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ClientPayload.InteropData: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ClientPayload.InteropData();
                if (d.accountId != null) {
                    if ($util.Long)
                        m.accountId = $util.Long.fromValue(d.accountId, true);
                    else if (typeof d.accountId === "string")
                        m.accountId = $parseInt(d.accountId, 10);
                    else if (typeof d.accountId === "number")
                        m.accountId = d.accountId;
                    else if (typeof d.accountId === "object")
                        m.accountId = new $util.LongBits(d.accountId.low >>> 0, d.accountId.high >>> 0).toNumber(true);
                }
                if (d.token != null) {
                    if (typeof d.token === "string")
                        $util.base64.decode(d.token, m.token = $util.newBuffer($util.base64.length(d.token)), 0);
                    else if (d.token.length >= 0)
                        m.token = d.token;
                }
                if (d.enableReadReceipts != null) {
                    m.enableReadReceipts = $Boolean(d.enableReadReceipts);
                }
                return m;
            };

            InteropData.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.accountId != null && $Object.hasOwnProperty.call(m, "accountId")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.accountId = typeof m.accountId === "number" ? $BigInt(m.accountId) : $util.Long.fromBits(m.accountId.low >>> 0, m.accountId.high >>> 0, true).toBigInt();
                    else if (typeof m.accountId === "number")
                        d.accountId = o.longs === $String ? $String(m.accountId) : m.accountId;
                    else
                        d.accountId = o.longs === $String ? $util.Long.prototype.toString.call(m.accountId) : o.longs === $Number ? new $util.LongBits(m.accountId.low >>> 0, m.accountId.high >>> 0).toNumber(true) : m.accountId;
                }
                if (m.token != null && $Object.hasOwnProperty.call(m, "token")) {
                    d.token = o.bytes === $String ? $util.base64.encode(m.token, 0, m.token.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.token) : m.token;
                }
                if (m.enableReadReceipts != null && $Object.hasOwnProperty.call(m, "enableReadReceipts")) {
                    d.enableReadReceipts = m.enableReadReceipts;
                }
                return d;
            };

            InteropData.prototype.toJSON = function() {
                return InteropData.toObject(this, $protobuf.util.toJSONOptions);
            };

            InteropData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ClientPayload.InteropData";
            };

            return InteropData;
        })();

        ClientPayload.Product = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "WHATSAPP"] = 0;
            values[valuesById[1] = "MESSENGER"] = 1;
            values[valuesById[2] = "INTEROP"] = 2;
            values[valuesById[3] = "INTEROP_MSGR"] = 3;
            values[valuesById[4] = "WHATSAPP_LID"] = 4;
            return values;
        })();

        ClientPayload.TrafficAnonymization = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "OFF"] = 0;
            values[valuesById[1] = "STANDARD"] = 1;
            return values;
        })();

        ClientPayload.UserAgent = (function() {

            const UserAgent = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UserAgent.prototype.platform = null;
            UserAgent.prototype.appVersion = null;
            UserAgent.prototype.mcc = null;
            UserAgent.prototype.mnc = null;
            UserAgent.prototype.osVersion = null;
            UserAgent.prototype.manufacturer = null;
            UserAgent.prototype.device = null;
            UserAgent.prototype.osBuildNumber = null;
            UserAgent.prototype.phoneId = null;
            UserAgent.prototype.releaseChannel = null;
            UserAgent.prototype.localeLanguageIso6391 = null;
            UserAgent.prototype.localeCountryIso31661Alpha2 = null;
            UserAgent.prototype.deviceBoard = null;
            UserAgent.prototype.deviceExpId = null;
            UserAgent.prototype.deviceType = null;
            UserAgent.prototype.deviceModelType = null;
            UserAgent.prototype.distributionChannel = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_platform", {
                get: $util.oneOfGetter($oneOfFields = ["platform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_appVersion", {
                get: $util.oneOfGetter($oneOfFields = ["appVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_mcc", {
                get: $util.oneOfGetter($oneOfFields = ["mcc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_mnc", {
                get: $util.oneOfGetter($oneOfFields = ["mnc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_osVersion", {
                get: $util.oneOfGetter($oneOfFields = ["osVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_manufacturer", {
                get: $util.oneOfGetter($oneOfFields = ["manufacturer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_device", {
                get: $util.oneOfGetter($oneOfFields = ["device"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_osBuildNumber", {
                get: $util.oneOfGetter($oneOfFields = ["osBuildNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_phoneId", {
                get: $util.oneOfGetter($oneOfFields = ["phoneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_releaseChannel", {
                get: $util.oneOfGetter($oneOfFields = ["releaseChannel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_localeLanguageIso6391", {
                get: $util.oneOfGetter($oneOfFields = ["localeLanguageIso6391"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_localeCountryIso31661Alpha2", {
                get: $util.oneOfGetter($oneOfFields = ["localeCountryIso31661Alpha2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_deviceBoard", {
                get: $util.oneOfGetter($oneOfFields = ["deviceBoard"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_deviceExpId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceExpId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_deviceType", {
                get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_deviceModelType", {
                get: $util.oneOfGetter($oneOfFields = ["deviceModelType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserAgent.prototype, "_distributionChannel", {
                get: $util.oneOfGetter($oneOfFields = ["distributionChannel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UserAgent.create = function(properties) {
                return new UserAgent(properties);
            };

            UserAgent.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.platform != null && $Object.hasOwnProperty.call(m, "platform"))
                    w.uint32(8).int32(m.platform);
                if (m.appVersion != null && $Object.hasOwnProperty.call(m, "appVersion"))
                    $root.proto.ClientPayload.UserAgent.AppVersion.encode(m.appVersion, w.uint32(18).fork(), q + 1).ldelim();
                if (m.mcc != null && $Object.hasOwnProperty.call(m, "mcc"))
                    w.uint32(26).string(m.mcc);
                if (m.mnc != null && $Object.hasOwnProperty.call(m, "mnc"))
                    w.uint32(34).string(m.mnc);
                if (m.osVersion != null && $Object.hasOwnProperty.call(m, "osVersion"))
                    w.uint32(42).string(m.osVersion);
                if (m.manufacturer != null && $Object.hasOwnProperty.call(m, "manufacturer"))
                    w.uint32(50).string(m.manufacturer);
                if (m.device != null && $Object.hasOwnProperty.call(m, "device"))
                    w.uint32(58).string(m.device);
                if (m.osBuildNumber != null && $Object.hasOwnProperty.call(m, "osBuildNumber"))
                    w.uint32(66).string(m.osBuildNumber);
                if (m.phoneId != null && $Object.hasOwnProperty.call(m, "phoneId"))
                    w.uint32(74).string(m.phoneId);
                if (m.releaseChannel != null && $Object.hasOwnProperty.call(m, "releaseChannel"))
                    w.uint32(80).int32(m.releaseChannel);
                if (m.localeLanguageIso6391 != null && $Object.hasOwnProperty.call(m, "localeLanguageIso6391"))
                    w.uint32(90).string(m.localeLanguageIso6391);
                if (m.localeCountryIso31661Alpha2 != null && $Object.hasOwnProperty.call(m, "localeCountryIso31661Alpha2"))
                    w.uint32(98).string(m.localeCountryIso31661Alpha2);
                if (m.deviceBoard != null && $Object.hasOwnProperty.call(m, "deviceBoard"))
                    w.uint32(106).string(m.deviceBoard);
                if (m.deviceExpId != null && $Object.hasOwnProperty.call(m, "deviceExpId"))
                    w.uint32(114).string(m.deviceExpId);
                if (m.deviceType != null && $Object.hasOwnProperty.call(m, "deviceType"))
                    w.uint32(120).int32(m.deviceType);
                if (m.deviceModelType != null && $Object.hasOwnProperty.call(m, "deviceModelType"))
                    w.uint32(130).string(m.deviceModelType);
                if (m.distributionChannel != null && $Object.hasOwnProperty.call(m, "distributionChannel"))
                    w.uint32(136).int32(m.distributionChannel);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UserAgent.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.UserAgent(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.platform = r.int32();
                            m._platform = "platform";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.appVersion = $root.proto.ClientPayload.UserAgent.AppVersion.decode(r, r.uint32(), $undefined, q + 1, m.appVersion);
                            m._appVersion = "appVersion";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.mcc = r.stringVerify();
                            m._mcc = "mcc";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.mnc = r.stringVerify();
                            m._mnc = "mnc";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.osVersion = r.stringVerify();
                            m._osVersion = "osVersion";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.manufacturer = r.stringVerify();
                            m._manufacturer = "manufacturer";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.device = r.stringVerify();
                            m._device = "device";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.osBuildNumber = r.stringVerify();
                            m._osBuildNumber = "osBuildNumber";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.phoneId = r.stringVerify();
                            m._phoneId = "phoneId";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.releaseChannel = r.int32();
                            m._releaseChannel = "releaseChannel";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.localeLanguageIso6391 = r.stringVerify();
                            m._localeLanguageIso6391 = "localeLanguageIso6391";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.localeCountryIso31661Alpha2 = r.stringVerify();
                            m._localeCountryIso31661Alpha2 = "localeCountryIso31661Alpha2";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.deviceBoard = r.stringVerify();
                            m._deviceBoard = "deviceBoard";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.deviceExpId = r.stringVerify();
                            m._deviceExpId = "deviceExpId";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            m.deviceType = r.int32();
                            m._deviceType = "deviceType";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.deviceModelType = r.stringVerify();
                            m._deviceModelType = "deviceModelType";
                            continue;
                        }
                    case 17: {
                            if (u !== 0)
                                break;
                            m.distributionChannel = r.int32();
                            m._distributionChannel = "distributionChannel";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UserAgent.fromObject = function (d, q) {
                if (d instanceof $root.proto.ClientPayload.UserAgent)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ClientPayload.UserAgent: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ClientPayload.UserAgent();
                switch (d.platform) {
                case "ANDROID":
                case 0:
                    m.platform = 0;
                    break;
                case "IOS":
                case 1:
                    m.platform = 1;
                    break;
                case "WINDOWS_PHONE":
                case 2:
                    m.platform = 2;
                    break;
                case "BLACKBERRY":
                case 3:
                    m.platform = 3;
                    break;
                case "BLACKBERRYX":
                case 4:
                    m.platform = 4;
                    break;
                case "S40":
                case 5:
                    m.platform = 5;
                    break;
                case "S60":
                case 6:
                    m.platform = 6;
                    break;
                case "PYTHON_CLIENT":
                case 7:
                    m.platform = 7;
                    break;
                case "TIZEN":
                case 8:
                    m.platform = 8;
                    break;
                case "ENTERPRISE":
                case 9:
                    m.platform = 9;
                    break;
                case "SMB_ANDROID":
                case 10:
                    m.platform = 10;
                    break;
                case "KAIOS":
                case 11:
                    m.platform = 11;
                    break;
                case "SMB_IOS":
                case 12:
                    m.platform = 12;
                    break;
                case "WINDOWS":
                case 13:
                    m.platform = 13;
                    break;
                case "WEB":
                case 14:
                    m.platform = 14;
                    break;
                case "PORTAL":
                case 15:
                    m.platform = 15;
                    break;
                case "GREEN_ANDROID":
                case 16:
                    m.platform = 16;
                    break;
                case "GREEN_IPHONE":
                case 17:
                    m.platform = 17;
                    break;
                case "BLUE_ANDROID":
                case 18:
                    m.platform = 18;
                    break;
                case "BLUE_IPHONE":
                case 19:
                    m.platform = 19;
                    break;
                case "FBLITE_ANDROID":
                case 20:
                    m.platform = 20;
                    break;
                case "MLITE_ANDROID":
                case 21:
                    m.platform = 21;
                    break;
                case "IGLITE_ANDROID":
                case 22:
                    m.platform = 22;
                    break;
                case "PAGE":
                case 23:
                    m.platform = 23;
                    break;
                case "MACOS":
                case 24:
                    m.platform = 24;
                    break;
                case "OCULUS_MSG":
                case 25:
                    m.platform = 25;
                    break;
                case "OCULUS_CALL":
                case 26:
                    m.platform = 26;
                    break;
                case "MILAN":
                case 27:
                    m.platform = 27;
                    break;
                case "CAPI":
                case 28:
                    m.platform = 28;
                    break;
                case "WEAROS":
                case 29:
                    m.platform = 29;
                    break;
                case "ARDEVICE":
                case 30:
                    m.platform = 30;
                    break;
                case "VRDEVICE":
                case 31:
                    m.platform = 31;
                    break;
                case "BLUE_WEB":
                case 32:
                    m.platform = 32;
                    break;
                case "IPAD":
                case 33:
                    m.platform = 33;
                    break;
                case "TEST":
                case 34:
                    m.platform = 34;
                    break;
                case "SMART_GLASSES":
                case 35:
                    m.platform = 35;
                    break;
                case "BLUE_VR":
                case 36:
                    m.platform = 36;
                    break;
                case "AR_WRIST":
                case 37:
                    m.platform = 37;
                    break;
                case "WAIL":
                case 38:
                    m.platform = 38;
                    break;
                default:
                    if (typeof d.platform === "number" && (d.platform | 0) === d.platform)
                        m.platform = d.platform;
                }
                if (d.appVersion != null) {
                    if (!$util.isObject(d.appVersion))
                        throw $TypeError(".proto.ClientPayload.UserAgent.appVersion: object expected");
                    m.appVersion = $root.proto.ClientPayload.UserAgent.AppVersion.fromObject(d.appVersion, q + 1);
                }
                if (d.mcc != null) {
                    m.mcc = $String(d.mcc);
                }
                if (d.mnc != null) {
                    m.mnc = $String(d.mnc);
                }
                if (d.osVersion != null) {
                    m.osVersion = $String(d.osVersion);
                }
                if (d.manufacturer != null) {
                    m.manufacturer = $String(d.manufacturer);
                }
                if (d.device != null) {
                    m.device = $String(d.device);
                }
                if (d.osBuildNumber != null) {
                    m.osBuildNumber = $String(d.osBuildNumber);
                }
                if (d.phoneId != null) {
                    m.phoneId = $String(d.phoneId);
                }
                switch (d.releaseChannel) {
                case "RELEASE":
                case 0:
                    m.releaseChannel = 0;
                    break;
                case "BETA":
                case 1:
                    m.releaseChannel = 1;
                    break;
                case "ALPHA":
                case 2:
                    m.releaseChannel = 2;
                    break;
                case "DEBUG":
                case 3:
                    m.releaseChannel = 3;
                    break;
                default:
                    if (typeof d.releaseChannel === "number" && (d.releaseChannel | 0) === d.releaseChannel)
                        m.releaseChannel = d.releaseChannel;
                }
                if (d.localeLanguageIso6391 != null) {
                    m.localeLanguageIso6391 = $String(d.localeLanguageIso6391);
                }
                if (d.localeCountryIso31661Alpha2 != null) {
                    m.localeCountryIso31661Alpha2 = $String(d.localeCountryIso31661Alpha2);
                }
                if (d.deviceBoard != null) {
                    m.deviceBoard = $String(d.deviceBoard);
                }
                if (d.deviceExpId != null) {
                    m.deviceExpId = $String(d.deviceExpId);
                }
                switch (d.deviceType) {
                case "PHONE":
                case 0:
                    m.deviceType = 0;
                    break;
                case "TABLET":
                case 1:
                    m.deviceType = 1;
                    break;
                case "DESKTOP":
                case 2:
                    m.deviceType = 2;
                    break;
                case "WEARABLE":
                case 3:
                    m.deviceType = 3;
                    break;
                case "VR":
                case 4:
                    m.deviceType = 4;
                    break;
                default:
                    if (typeof d.deviceType === "number" && (d.deviceType | 0) === d.deviceType)
                        m.deviceType = d.deviceType;
                }
                if (d.deviceModelType != null) {
                    m.deviceModelType = $String(d.deviceModelType);
                }
                switch (d.distributionChannel) {
                case "APPSTORE":
                case 0:
                    m.distributionChannel = 0;
                    break;
                case "WEBSITE":
                case 1:
                    m.distributionChannel = 1;
                    break;
                case "TESTFLIGHT":
                case 2:
                    m.distributionChannel = 2;
                    break;
                case "INTERNAL":
                case 3:
                    m.distributionChannel = 3;
                    break;
                default:
                    if (typeof d.distributionChannel === "number" && (d.distributionChannel | 0) === d.distributionChannel)
                        m.distributionChannel = d.distributionChannel;
                }
                return m;
            };

            UserAgent.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.platform != null && $Object.hasOwnProperty.call(m, "platform")) {
                    d.platform = o.enums === $String ? $root.proto.ClientPayload.UserAgent.Platform[m.platform] === $undefined ? m.platform : $root.proto.ClientPayload.UserAgent.Platform[m.platform] : m.platform;
                }
                if (m.appVersion != null && $Object.hasOwnProperty.call(m, "appVersion")) {
                    d.appVersion = $root.proto.ClientPayload.UserAgent.AppVersion.toObject(m.appVersion, o, q + 1);
                }
                if (m.mcc != null && $Object.hasOwnProperty.call(m, "mcc")) {
                    d.mcc = m.mcc;
                }
                if (m.mnc != null && $Object.hasOwnProperty.call(m, "mnc")) {
                    d.mnc = m.mnc;
                }
                if (m.osVersion != null && $Object.hasOwnProperty.call(m, "osVersion")) {
                    d.osVersion = m.osVersion;
                }
                if (m.manufacturer != null && $Object.hasOwnProperty.call(m, "manufacturer")) {
                    d.manufacturer = m.manufacturer;
                }
                if (m.device != null && $Object.hasOwnProperty.call(m, "device")) {
                    d.device = m.device;
                }
                if (m.osBuildNumber != null && $Object.hasOwnProperty.call(m, "osBuildNumber")) {
                    d.osBuildNumber = m.osBuildNumber;
                }
                if (m.phoneId != null && $Object.hasOwnProperty.call(m, "phoneId")) {
                    d.phoneId = m.phoneId;
                }
                if (m.releaseChannel != null && $Object.hasOwnProperty.call(m, "releaseChannel")) {
                    d.releaseChannel = o.enums === $String ? $root.proto.ClientPayload.UserAgent.ReleaseChannel[m.releaseChannel] === $undefined ? m.releaseChannel : $root.proto.ClientPayload.UserAgent.ReleaseChannel[m.releaseChannel] : m.releaseChannel;
                }
                if (m.localeLanguageIso6391 != null && $Object.hasOwnProperty.call(m, "localeLanguageIso6391")) {
                    d.localeLanguageIso6391 = m.localeLanguageIso6391;
                }
                if (m.localeCountryIso31661Alpha2 != null && $Object.hasOwnProperty.call(m, "localeCountryIso31661Alpha2")) {
                    d.localeCountryIso31661Alpha2 = m.localeCountryIso31661Alpha2;
                }
                if (m.deviceBoard != null && $Object.hasOwnProperty.call(m, "deviceBoard")) {
                    d.deviceBoard = m.deviceBoard;
                }
                if (m.deviceExpId != null && $Object.hasOwnProperty.call(m, "deviceExpId")) {
                    d.deviceExpId = m.deviceExpId;
                }
                if (m.deviceType != null && $Object.hasOwnProperty.call(m, "deviceType")) {
                    d.deviceType = o.enums === $String ? $root.proto.ClientPayload.UserAgent.DeviceType[m.deviceType] === $undefined ? m.deviceType : $root.proto.ClientPayload.UserAgent.DeviceType[m.deviceType] : m.deviceType;
                }
                if (m.deviceModelType != null && $Object.hasOwnProperty.call(m, "deviceModelType")) {
                    d.deviceModelType = m.deviceModelType;
                }
                if (m.distributionChannel != null && $Object.hasOwnProperty.call(m, "distributionChannel")) {
                    d.distributionChannel = o.enums === $String ? $root.proto.ClientPayload.UserAgent.DistributionChannel[m.distributionChannel] === $undefined ? m.distributionChannel : $root.proto.ClientPayload.UserAgent.DistributionChannel[m.distributionChannel] : m.distributionChannel;
                }
                return d;
            };

            UserAgent.prototype.toJSON = function() {
                return UserAgent.toObject(this, $protobuf.util.toJSONOptions);
            };

            UserAgent.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ClientPayload.UserAgent";
            };

            UserAgent.AppVersion = (function() {

                const AppVersion = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                AppVersion.prototype.primary = null;
                AppVersion.prototype.secondary = null;
                AppVersion.prototype.tertiary = null;
                AppVersion.prototype.quaternary = null;
                AppVersion.prototype.quinary = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(AppVersion.prototype, "_primary", {
                    get: $util.oneOfGetter($oneOfFields = ["primary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(AppVersion.prototype, "_secondary", {
                    get: $util.oneOfGetter($oneOfFields = ["secondary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(AppVersion.prototype, "_tertiary", {
                    get: $util.oneOfGetter($oneOfFields = ["tertiary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(AppVersion.prototype, "_quaternary", {
                    get: $util.oneOfGetter($oneOfFields = ["quaternary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(AppVersion.prototype, "_quinary", {
                    get: $util.oneOfGetter($oneOfFields = ["quinary"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                AppVersion.create = function(properties) {
                    return new AppVersion(properties);
                };

                AppVersion.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.primary != null && $Object.hasOwnProperty.call(m, "primary"))
                        w.uint32(8).uint32(m.primary);
                    if (m.secondary != null && $Object.hasOwnProperty.call(m, "secondary"))
                        w.uint32(16).uint32(m.secondary);
                    if (m.tertiary != null && $Object.hasOwnProperty.call(m, "tertiary"))
                        w.uint32(24).uint32(m.tertiary);
                    if (m.quaternary != null && $Object.hasOwnProperty.call(m, "quaternary"))
                        w.uint32(32).uint32(m.quaternary);
                    if (m.quinary != null && $Object.hasOwnProperty.call(m, "quinary"))
                        w.uint32(40).uint32(m.quinary);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                AppVersion.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.UserAgent.AppVersion();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.primary = r.uint32();
                                m._primary = "primary";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.secondary = r.uint32();
                                m._secondary = "secondary";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.tertiary = r.uint32();
                                m._tertiary = "tertiary";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.quaternary = r.uint32();
                                m._quaternary = "quaternary";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.quinary = r.uint32();
                                m._quinary = "quinary";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                AppVersion.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ClientPayload.UserAgent.AppVersion)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ClientPayload.UserAgent.AppVersion: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ClientPayload.UserAgent.AppVersion();
                    if (d.primary != null) {
                        m.primary = d.primary >>> 0;
                    }
                    if (d.secondary != null) {
                        m.secondary = d.secondary >>> 0;
                    }
                    if (d.tertiary != null) {
                        m.tertiary = d.tertiary >>> 0;
                    }
                    if (d.quaternary != null) {
                        m.quaternary = d.quaternary >>> 0;
                    }
                    if (d.quinary != null) {
                        m.quinary = d.quinary >>> 0;
                    }
                    return m;
                };

                AppVersion.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.primary != null && $Object.hasOwnProperty.call(m, "primary")) {
                        d.primary = m.primary;
                    }
                    if (m.secondary != null && $Object.hasOwnProperty.call(m, "secondary")) {
                        d.secondary = m.secondary;
                    }
                    if (m.tertiary != null && $Object.hasOwnProperty.call(m, "tertiary")) {
                        d.tertiary = m.tertiary;
                    }
                    if (m.quaternary != null && $Object.hasOwnProperty.call(m, "quaternary")) {
                        d.quaternary = m.quaternary;
                    }
                    if (m.quinary != null && $Object.hasOwnProperty.call(m, "quinary")) {
                        d.quinary = m.quinary;
                    }
                    return d;
                };

                AppVersion.prototype.toJSON = function() {
                    return AppVersion.toObject(this, $protobuf.util.toJSONOptions);
                };

                AppVersion.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ClientPayload.UserAgent.AppVersion";
                };

                return AppVersion;
            })();

            UserAgent.DeviceType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "PHONE"] = 0;
                values[valuesById[1] = "TABLET"] = 1;
                values[valuesById[2] = "DESKTOP"] = 2;
                values[valuesById[3] = "WEARABLE"] = 3;
                values[valuesById[4] = "VR"] = 4;
                return values;
            })();

            UserAgent.DistributionChannel = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "APPSTORE"] = 0;
                values[valuesById[1] = "WEBSITE"] = 1;
                values[valuesById[2] = "TESTFLIGHT"] = 2;
                values[valuesById[3] = "INTERNAL"] = 3;
                return values;
            })();

            UserAgent.Platform = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "ANDROID"] = 0;
                values[valuesById[1] = "IOS"] = 1;
                values[valuesById[2] = "WINDOWS_PHONE"] = 2;
                values[valuesById[3] = "BLACKBERRY"] = 3;
                values[valuesById[4] = "BLACKBERRYX"] = 4;
                values[valuesById[5] = "S40"] = 5;
                values[valuesById[6] = "S60"] = 6;
                values[valuesById[7] = "PYTHON_CLIENT"] = 7;
                values[valuesById[8] = "TIZEN"] = 8;
                values[valuesById[9] = "ENTERPRISE"] = 9;
                values[valuesById[10] = "SMB_ANDROID"] = 10;
                values[valuesById[11] = "KAIOS"] = 11;
                values[valuesById[12] = "SMB_IOS"] = 12;
                values[valuesById[13] = "WINDOWS"] = 13;
                values[valuesById[14] = "WEB"] = 14;
                values[valuesById[15] = "PORTAL"] = 15;
                values[valuesById[16] = "GREEN_ANDROID"] = 16;
                values[valuesById[17] = "GREEN_IPHONE"] = 17;
                values[valuesById[18] = "BLUE_ANDROID"] = 18;
                values[valuesById[19] = "BLUE_IPHONE"] = 19;
                values[valuesById[20] = "FBLITE_ANDROID"] = 20;
                values[valuesById[21] = "MLITE_ANDROID"] = 21;
                values[valuesById[22] = "IGLITE_ANDROID"] = 22;
                values[valuesById[23] = "PAGE"] = 23;
                values[valuesById[24] = "MACOS"] = 24;
                values[valuesById[25] = "OCULUS_MSG"] = 25;
                values[valuesById[26] = "OCULUS_CALL"] = 26;
                values[valuesById[27] = "MILAN"] = 27;
                values[valuesById[28] = "CAPI"] = 28;
                values[valuesById[29] = "WEAROS"] = 29;
                values[valuesById[30] = "ARDEVICE"] = 30;
                values[valuesById[31] = "VRDEVICE"] = 31;
                values[valuesById[32] = "BLUE_WEB"] = 32;
                values[valuesById[33] = "IPAD"] = 33;
                values[valuesById[34] = "TEST"] = 34;
                values[valuesById[35] = "SMART_GLASSES"] = 35;
                values[valuesById[36] = "BLUE_VR"] = 36;
                values[valuesById[37] = "AR_WRIST"] = 37;
                values[valuesById[38] = "WAIL"] = 38;
                return values;
            })();

            UserAgent.ReleaseChannel = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "RELEASE"] = 0;
                values[valuesById[1] = "BETA"] = 1;
                values[valuesById[2] = "ALPHA"] = 2;
                values[valuesById[3] = "DEBUG"] = 3;
                return values;
            })();

            return UserAgent;
        })();

        ClientPayload.WebInfo = (function() {

            const WebInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            WebInfo.prototype.refToken = null;
            WebInfo.prototype.version = null;
            WebInfo.prototype.webdPayload = null;
            WebInfo.prototype.webSubPlatform = null;
            WebInfo.prototype.browser = null;
            WebInfo.prototype.browserVersion = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WebInfo.prototype, "_refToken", {
                get: $util.oneOfGetter($oneOfFields = ["refToken"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WebInfo.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WebInfo.prototype, "_webdPayload", {
                get: $util.oneOfGetter($oneOfFields = ["webdPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WebInfo.prototype, "_webSubPlatform", {
                get: $util.oneOfGetter($oneOfFields = ["webSubPlatform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WebInfo.prototype, "_browser", {
                get: $util.oneOfGetter($oneOfFields = ["browser"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WebInfo.prototype, "_browserVersion", {
                get: $util.oneOfGetter($oneOfFields = ["browserVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            WebInfo.create = function(properties) {
                return new WebInfo(properties);
            };

            WebInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.refToken != null && $Object.hasOwnProperty.call(m, "refToken"))
                    w.uint32(10).string(m.refToken);
                if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                    w.uint32(18).string(m.version);
                if (m.webdPayload != null && $Object.hasOwnProperty.call(m, "webdPayload"))
                    $root.proto.ClientPayload.WebInfo.WebdPayload.encode(m.webdPayload, w.uint32(26).fork(), q + 1).ldelim();
                if (m.webSubPlatform != null && $Object.hasOwnProperty.call(m, "webSubPlatform"))
                    w.uint32(32).int32(m.webSubPlatform);
                if (m.browser != null && $Object.hasOwnProperty.call(m, "browser"))
                    w.uint32(42).string(m.browser);
                if (m.browserVersion != null && $Object.hasOwnProperty.call(m, "browserVersion"))
                    w.uint32(50).string(m.browserVersion);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            WebInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.WebInfo(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.refToken = r.stringVerify();
                            m._refToken = "refToken";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.version = r.stringVerify();
                            m._version = "version";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.webdPayload = $root.proto.ClientPayload.WebInfo.WebdPayload.decode(r, r.uint32(), $undefined, q + 1, m.webdPayload);
                            m._webdPayload = "webdPayload";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.webSubPlatform = r.int32();
                            m._webSubPlatform = "webSubPlatform";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.browser = r.stringVerify();
                            m._browser = "browser";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.browserVersion = r.stringVerify();
                            m._browserVersion = "browserVersion";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            WebInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.ClientPayload.WebInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ClientPayload.WebInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ClientPayload.WebInfo();
                if (d.refToken != null) {
                    m.refToken = $String(d.refToken);
                }
                if (d.version != null) {
                    m.version = $String(d.version);
                }
                if (d.webdPayload != null) {
                    if (!$util.isObject(d.webdPayload))
                        throw $TypeError(".proto.ClientPayload.WebInfo.webdPayload: object expected");
                    m.webdPayload = $root.proto.ClientPayload.WebInfo.WebdPayload.fromObject(d.webdPayload, q + 1);
                }
                switch (d.webSubPlatform) {
                case "WEB_BROWSER":
                case 0:
                    m.webSubPlatform = 0;
                    break;
                case "APP_STORE":
                case 1:
                    m.webSubPlatform = 1;
                    break;
                case "WIN_STORE":
                case 2:
                    m.webSubPlatform = 2;
                    break;
                case "DARWIN":
                case 3:
                    m.webSubPlatform = 3;
                    break;
                case "WIN32":
                case 4:
                    m.webSubPlatform = 4;
                    break;
                case "WIN_HYBRID":
                case 5:
                    m.webSubPlatform = 5;
                    break;
                default:
                    if (typeof d.webSubPlatform === "number" && (d.webSubPlatform | 0) === d.webSubPlatform)
                        m.webSubPlatform = d.webSubPlatform;
                }
                if (d.browser != null) {
                    m.browser = $String(d.browser);
                }
                if (d.browserVersion != null) {
                    m.browserVersion = $String(d.browserVersion);
                }
                return m;
            };

            WebInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.refToken != null && $Object.hasOwnProperty.call(m, "refToken")) {
                    d.refToken = m.refToken;
                }
                if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                    d.version = m.version;
                }
                if (m.webdPayload != null && $Object.hasOwnProperty.call(m, "webdPayload")) {
                    d.webdPayload = $root.proto.ClientPayload.WebInfo.WebdPayload.toObject(m.webdPayload, o, q + 1);
                }
                if (m.webSubPlatform != null && $Object.hasOwnProperty.call(m, "webSubPlatform")) {
                    d.webSubPlatform = o.enums === $String ? $root.proto.ClientPayload.WebInfo.WebSubPlatform[m.webSubPlatform] === $undefined ? m.webSubPlatform : $root.proto.ClientPayload.WebInfo.WebSubPlatform[m.webSubPlatform] : m.webSubPlatform;
                }
                if (m.browser != null && $Object.hasOwnProperty.call(m, "browser")) {
                    d.browser = m.browser;
                }
                if (m.browserVersion != null && $Object.hasOwnProperty.call(m, "browserVersion")) {
                    d.browserVersion = m.browserVersion;
                }
                return d;
            };

            WebInfo.prototype.toJSON = function() {
                return WebInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            WebInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ClientPayload.WebInfo";
            };

            WebInfo.WebSubPlatform = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "WEB_BROWSER"] = 0;
                values[valuesById[1] = "APP_STORE"] = 1;
                values[valuesById[2] = "WIN_STORE"] = 2;
                values[valuesById[3] = "DARWIN"] = 3;
                values[valuesById[4] = "WIN32"] = 4;
                values[valuesById[5] = "WIN_HYBRID"] = 5;
                return values;
            })();

            WebInfo.WebdPayload = (function() {

                const WebdPayload = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                WebdPayload.prototype.usesParticipantInKey = null;
                WebdPayload.prototype.supportsStarredMessages = null;
                WebdPayload.prototype.supportsDocumentMessages = null;
                WebdPayload.prototype.supportsUrlMessages = null;
                WebdPayload.prototype.supportsMediaRetry = null;
                WebdPayload.prototype.supportsE2EImage = null;
                WebdPayload.prototype.supportsE2EVideo = null;
                WebdPayload.prototype.supportsE2EAudio = null;
                WebdPayload.prototype.supportsE2EDocument = null;
                WebdPayload.prototype.documentTypes = null;
                WebdPayload.prototype.features = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_usesParticipantInKey", {
                    get: $util.oneOfGetter($oneOfFields = ["usesParticipantInKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsStarredMessages", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsStarredMessages"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsDocumentMessages", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsDocumentMessages"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsUrlMessages", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsUrlMessages"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsMediaRetry", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsMediaRetry"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsE2EImage", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsE2EVideo", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EVideo"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsE2EAudio", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EAudio"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_supportsE2EDocument", {
                    get: $util.oneOfGetter($oneOfFields = ["supportsE2EDocument"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_documentTypes", {
                    get: $util.oneOfGetter($oneOfFields = ["documentTypes"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(WebdPayload.prototype, "_features", {
                    get: $util.oneOfGetter($oneOfFields = ["features"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                WebdPayload.create = function(properties) {
                    return new WebdPayload(properties);
                };

                WebdPayload.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.usesParticipantInKey != null && $Object.hasOwnProperty.call(m, "usesParticipantInKey"))
                        w.uint32(8).bool(m.usesParticipantInKey);
                    if (m.supportsStarredMessages != null && $Object.hasOwnProperty.call(m, "supportsStarredMessages"))
                        w.uint32(16).bool(m.supportsStarredMessages);
                    if (m.supportsDocumentMessages != null && $Object.hasOwnProperty.call(m, "supportsDocumentMessages"))
                        w.uint32(24).bool(m.supportsDocumentMessages);
                    if (m.supportsUrlMessages != null && $Object.hasOwnProperty.call(m, "supportsUrlMessages"))
                        w.uint32(32).bool(m.supportsUrlMessages);
                    if (m.supportsMediaRetry != null && $Object.hasOwnProperty.call(m, "supportsMediaRetry"))
                        w.uint32(40).bool(m.supportsMediaRetry);
                    if (m.supportsE2EImage != null && $Object.hasOwnProperty.call(m, "supportsE2EImage"))
                        w.uint32(48).bool(m.supportsE2EImage);
                    if (m.supportsE2EVideo != null && $Object.hasOwnProperty.call(m, "supportsE2EVideo"))
                        w.uint32(56).bool(m.supportsE2EVideo);
                    if (m.supportsE2EAudio != null && $Object.hasOwnProperty.call(m, "supportsE2EAudio"))
                        w.uint32(64).bool(m.supportsE2EAudio);
                    if (m.supportsE2EDocument != null && $Object.hasOwnProperty.call(m, "supportsE2EDocument"))
                        w.uint32(72).bool(m.supportsE2EDocument);
                    if (m.documentTypes != null && $Object.hasOwnProperty.call(m, "documentTypes"))
                        w.uint32(82).string(m.documentTypes);
                    if (m.features != null && $Object.hasOwnProperty.call(m, "features"))
                        w.uint32(90).bytes(m.features);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                WebdPayload.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ClientPayload.WebInfo.WebdPayload();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.usesParticipantInKey = r.bool();
                                m._usesParticipantInKey = "usesParticipantInKey";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.supportsStarredMessages = r.bool();
                                m._supportsStarredMessages = "supportsStarredMessages";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.supportsDocumentMessages = r.bool();
                                m._supportsDocumentMessages = "supportsDocumentMessages";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.supportsUrlMessages = r.bool();
                                m._supportsUrlMessages = "supportsUrlMessages";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.supportsMediaRetry = r.bool();
                                m._supportsMediaRetry = "supportsMediaRetry";
                                continue;
                            }
                        case 6: {
                                if (u !== 0)
                                    break;
                                m.supportsE2EImage = r.bool();
                                m._supportsE2EImage = "supportsE2EImage";
                                continue;
                            }
                        case 7: {
                                if (u !== 0)
                                    break;
                                m.supportsE2EVideo = r.bool();
                                m._supportsE2EVideo = "supportsE2EVideo";
                                continue;
                            }
                        case 8: {
                                if (u !== 0)
                                    break;
                                m.supportsE2EAudio = r.bool();
                                m._supportsE2EAudio = "supportsE2EAudio";
                                continue;
                            }
                        case 9: {
                                if (u !== 0)
                                    break;
                                m.supportsE2EDocument = r.bool();
                                m._supportsE2EDocument = "supportsE2EDocument";
                                continue;
                            }
                        case 10: {
                                if (u !== 2)
                                    break;
                                m.documentTypes = r.stringVerify();
                                m._documentTypes = "documentTypes";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                m.features = r.bytes();
                                m._features = "features";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                WebdPayload.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ClientPayload.WebInfo.WebdPayload)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ClientPayload.WebInfo.WebdPayload: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ClientPayload.WebInfo.WebdPayload();
                    if (d.usesParticipantInKey != null) {
                        m.usesParticipantInKey = $Boolean(d.usesParticipantInKey);
                    }
                    if (d.supportsStarredMessages != null) {
                        m.supportsStarredMessages = $Boolean(d.supportsStarredMessages);
                    }
                    if (d.supportsDocumentMessages != null) {
                        m.supportsDocumentMessages = $Boolean(d.supportsDocumentMessages);
                    }
                    if (d.supportsUrlMessages != null) {
                        m.supportsUrlMessages = $Boolean(d.supportsUrlMessages);
                    }
                    if (d.supportsMediaRetry != null) {
                        m.supportsMediaRetry = $Boolean(d.supportsMediaRetry);
                    }
                    if (d.supportsE2EImage != null) {
                        m.supportsE2EImage = $Boolean(d.supportsE2EImage);
                    }
                    if (d.supportsE2EVideo != null) {
                        m.supportsE2EVideo = $Boolean(d.supportsE2EVideo);
                    }
                    if (d.supportsE2EAudio != null) {
                        m.supportsE2EAudio = $Boolean(d.supportsE2EAudio);
                    }
                    if (d.supportsE2EDocument != null) {
                        m.supportsE2EDocument = $Boolean(d.supportsE2EDocument);
                    }
                    if (d.documentTypes != null) {
                        m.documentTypes = $String(d.documentTypes);
                    }
                    if (d.features != null) {
                        if (typeof d.features === "string")
                            $util.base64.decode(d.features, m.features = $util.newBuffer($util.base64.length(d.features)), 0);
                        else if (d.features.length >= 0)
                            m.features = d.features;
                    }
                    return m;
                };

                WebdPayload.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.usesParticipantInKey != null && $Object.hasOwnProperty.call(m, "usesParticipantInKey")) {
                        d.usesParticipantInKey = m.usesParticipantInKey;
                    }
                    if (m.supportsStarredMessages != null && $Object.hasOwnProperty.call(m, "supportsStarredMessages")) {
                        d.supportsStarredMessages = m.supportsStarredMessages;
                    }
                    if (m.supportsDocumentMessages != null && $Object.hasOwnProperty.call(m, "supportsDocumentMessages")) {
                        d.supportsDocumentMessages = m.supportsDocumentMessages;
                    }
                    if (m.supportsUrlMessages != null && $Object.hasOwnProperty.call(m, "supportsUrlMessages")) {
                        d.supportsUrlMessages = m.supportsUrlMessages;
                    }
                    if (m.supportsMediaRetry != null && $Object.hasOwnProperty.call(m, "supportsMediaRetry")) {
                        d.supportsMediaRetry = m.supportsMediaRetry;
                    }
                    if (m.supportsE2EImage != null && $Object.hasOwnProperty.call(m, "supportsE2EImage")) {
                        d.supportsE2EImage = m.supportsE2EImage;
                    }
                    if (m.supportsE2EVideo != null && $Object.hasOwnProperty.call(m, "supportsE2EVideo")) {
                        d.supportsE2EVideo = m.supportsE2EVideo;
                    }
                    if (m.supportsE2EAudio != null && $Object.hasOwnProperty.call(m, "supportsE2EAudio")) {
                        d.supportsE2EAudio = m.supportsE2EAudio;
                    }
                    if (m.supportsE2EDocument != null && $Object.hasOwnProperty.call(m, "supportsE2EDocument")) {
                        d.supportsE2EDocument = m.supportsE2EDocument;
                    }
                    if (m.documentTypes != null && $Object.hasOwnProperty.call(m, "documentTypes")) {
                        d.documentTypes = m.documentTypes;
                    }
                    if (m.features != null && $Object.hasOwnProperty.call(m, "features")) {
                        d.features = o.bytes === $String ? $util.base64.encode(m.features, 0, m.features.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.features) : m.features;
                    }
                    return d;
                };

                WebdPayload.prototype.toJSON = function() {
                    return WebdPayload.toObject(this, $protobuf.util.toJSONOptions);
                };

                WebdPayload.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ClientPayload.WebInfo.WebdPayload";
                };

                return WebdPayload;
            })();

            return WebInfo;
        })();

        return ClientPayload;
    })();

    proto.CoexStateSync = (function() {

        const CoexStateSync = function (p) {
            this.collectionMutations = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CoexStateSync.prototype.collectionMutations = $util.emptyArray;

        CoexStateSync.create = function(properties) {
            return new CoexStateSync(properties);
        };

        CoexStateSync.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.collectionMutations != null && m.collectionMutations.length) {
                for (var i = 0; i < m.collectionMutations.length; ++i)
                    $root.proto.CoexStateSync.CollectionMutations.encode(m.collectionMutations[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CoexStateSync.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CoexStateSync();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.collectionMutations && m.collectionMutations.length))
                            m.collectionMutations = [];
                        m.collectionMutations.push($root.proto.CoexStateSync.CollectionMutations.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CoexStateSync.fromObject = function (d, q) {
            if (d instanceof $root.proto.CoexStateSync)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CoexStateSync: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CoexStateSync();
            if (d.collectionMutations) {
                if (!$Array.isArray(d.collectionMutations))
                    throw $TypeError(".proto.CoexStateSync.collectionMutations: array expected");
                m.collectionMutations = $Array(d.collectionMutations.length);
                for (var i = 0; i < d.collectionMutations.length; ++i) {
                    if (!$util.isObject(d.collectionMutations[i]))
                        throw $TypeError(".proto.CoexStateSync.collectionMutations: object expected");
                    m.collectionMutations[i] = $root.proto.CoexStateSync.CollectionMutations.fromObject(d.collectionMutations[i], q + 1);
                }
            }
            return m;
        };

        CoexStateSync.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.collectionMutations = [];
            }
            if (m.collectionMutations && m.collectionMutations.length) {
                d.collectionMutations = $Array(m.collectionMutations.length);
                for (var j = 0; j < m.collectionMutations.length; ++j) {
                    d.collectionMutations[j] = $root.proto.CoexStateSync.CollectionMutations.toObject(m.collectionMutations[j], o, q + 1);
                }
            }
            return d;
        };

        CoexStateSync.prototype.toJSON = function() {
            return CoexStateSync.toObject(this, $protobuf.util.toJSONOptions);
        };

        CoexStateSync.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CoexStateSync";
        };

        CoexStateSync.CollectionMutations = (function() {

            const CollectionMutations = function (p) {
                this.mutations = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CollectionMutations.prototype.collection = null;
            CollectionMutations.prototype.mutations = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CollectionMutations.prototype, "_collection", {
                get: $util.oneOfGetter($oneOfFields = ["collection"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CollectionMutations.create = function(properties) {
                return new CollectionMutations(properties);
            };

            CollectionMutations.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.collection != null && $Object.hasOwnProperty.call(m, "collection"))
                    w.uint32(10).string(m.collection);
                if (m.mutations != null && m.mutations.length) {
                    for (var i = 0; i < m.mutations.length; ++i)
                        $root.proto.CoexStateSync.Mutation.encode(m.mutations[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CollectionMutations.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CoexStateSync.CollectionMutations();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.collection = r.stringVerify();
                            m._collection = "collection";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.mutations && m.mutations.length))
                                m.mutations = [];
                            m.mutations.push($root.proto.CoexStateSync.Mutation.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CollectionMutations.fromObject = function (d, q) {
                if (d instanceof $root.proto.CoexStateSync.CollectionMutations)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.CoexStateSync.CollectionMutations: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.CoexStateSync.CollectionMutations();
                if (d.collection != null) {
                    m.collection = $String(d.collection);
                }
                if (d.mutations) {
                    if (!$Array.isArray(d.mutations))
                        throw $TypeError(".proto.CoexStateSync.CollectionMutations.mutations: array expected");
                    m.mutations = $Array(d.mutations.length);
                    for (var i = 0; i < d.mutations.length; ++i) {
                        if (!$util.isObject(d.mutations[i]))
                            throw $TypeError(".proto.CoexStateSync.CollectionMutations.mutations: object expected");
                        m.mutations[i] = $root.proto.CoexStateSync.Mutation.fromObject(d.mutations[i], q + 1);
                    }
                }
                return m;
            };

            CollectionMutations.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.mutations = [];
                }
                if (m.collection != null && $Object.hasOwnProperty.call(m, "collection")) {
                    d.collection = m.collection;
                }
                if (m.mutations && m.mutations.length) {
                    d.mutations = $Array(m.mutations.length);
                    for (var j = 0; j < m.mutations.length; ++j) {
                        d.mutations[j] = $root.proto.CoexStateSync.Mutation.toObject(m.mutations[j], o, q + 1);
                    }
                }
                return d;
            };

            CollectionMutations.prototype.toJSON = function() {
                return CollectionMutations.toObject(this, $protobuf.util.toJSONOptions);
            };

            CollectionMutations.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.CoexStateSync.CollectionMutations";
            };

            return CollectionMutations;
        })();

        CoexStateSync.Mutation = (function() {

            const Mutation = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Mutation.prototype.index = null;
            Mutation.prototype.value = null;
            Mutation.prototype.dirtyVersion = null;
            Mutation.prototype.operation = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Mutation.prototype, "_index", {
                get: $util.oneOfGetter($oneOfFields = ["index"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Mutation.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Mutation.prototype, "_dirtyVersion", {
                get: $util.oneOfGetter($oneOfFields = ["dirtyVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Mutation.prototype, "_operation", {
                get: $util.oneOfGetter($oneOfFields = ["operation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Mutation.create = function(properties) {
                return new Mutation(properties);
            };

            Mutation.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                    $root.proto.SyncdIndex.encode(m.index, w.uint32(10).fork(), q + 1).ldelim();
                if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                    $root.proto.SyncdValue.encode(m.value, w.uint32(18).fork(), q + 1).ldelim();
                if (m.dirtyVersion != null && $Object.hasOwnProperty.call(m, "dirtyVersion"))
                    w.uint32(24).uint64(m.dirtyVersion);
                if (m.operation != null && $Object.hasOwnProperty.call(m, "operation"))
                    w.uint32(32).int32(m.operation);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Mutation.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CoexStateSync.Mutation(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.index = $root.proto.SyncdIndex.decode(r, r.uint32(), $undefined, q + 1, m.index);
                            m._index = "index";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.value = $root.proto.SyncdValue.decode(r, r.uint32(), $undefined, q + 1, m.value);
                            m._value = "value";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.dirtyVersion = r.uint64();
                            m._dirtyVersion = "dirtyVersion";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.operation = r.int32();
                            m._operation = "operation";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Mutation.fromObject = function (d, q) {
                if (d instanceof $root.proto.CoexStateSync.Mutation)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.CoexStateSync.Mutation: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.CoexStateSync.Mutation();
                if (d.index != null) {
                    if (!$util.isObject(d.index))
                        throw $TypeError(".proto.CoexStateSync.Mutation.index: object expected");
                    m.index = $root.proto.SyncdIndex.fromObject(d.index, q + 1);
                }
                if (d.value != null) {
                    if (!$util.isObject(d.value))
                        throw $TypeError(".proto.CoexStateSync.Mutation.value: object expected");
                    m.value = $root.proto.SyncdValue.fromObject(d.value, q + 1);
                }
                if (d.dirtyVersion != null) {
                    if ($util.Long)
                        m.dirtyVersion = $util.Long.fromValue(d.dirtyVersion, true);
                    else if (typeof d.dirtyVersion === "string")
                        m.dirtyVersion = $parseInt(d.dirtyVersion, 10);
                    else if (typeof d.dirtyVersion === "number")
                        m.dirtyVersion = d.dirtyVersion;
                    else if (typeof d.dirtyVersion === "object")
                        m.dirtyVersion = new $util.LongBits(d.dirtyVersion.low >>> 0, d.dirtyVersion.high >>> 0).toNumber(true);
                }
                switch (d.operation) {
                case "SET":
                case 0:
                    m.operation = 0;
                    break;
                case "REMOVE":
                case 1:
                    m.operation = 1;
                    break;
                default:
                    if (typeof d.operation === "number" && (d.operation | 0) === d.operation)
                        m.operation = d.operation;
                }
                return m;
            };

            Mutation.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                    d.index = $root.proto.SyncdIndex.toObject(m.index, o, q + 1);
                }
                if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                    d.value = $root.proto.SyncdValue.toObject(m.value, o, q + 1);
                }
                if (m.dirtyVersion != null && $Object.hasOwnProperty.call(m, "dirtyVersion")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.dirtyVersion = typeof m.dirtyVersion === "number" ? $BigInt(m.dirtyVersion) : $util.Long.fromBits(m.dirtyVersion.low >>> 0, m.dirtyVersion.high >>> 0, true).toBigInt();
                    else if (typeof m.dirtyVersion === "number")
                        d.dirtyVersion = o.longs === $String ? $String(m.dirtyVersion) : m.dirtyVersion;
                    else
                        d.dirtyVersion = o.longs === $String ? $util.Long.prototype.toString.call(m.dirtyVersion) : o.longs === $Number ? new $util.LongBits(m.dirtyVersion.low >>> 0, m.dirtyVersion.high >>> 0).toNumber(true) : m.dirtyVersion;
                }
                if (m.operation != null && $Object.hasOwnProperty.call(m, "operation")) {
                    d.operation = o.enums === $String ? $root.proto.SyncdMutation.SyncdOperation[m.operation] === $undefined ? m.operation : $root.proto.SyncdMutation.SyncdOperation[m.operation] : m.operation;
                }
                return d;
            };

            Mutation.prototype.toJSON = function() {
                return Mutation.toObject(this, $protobuf.util.toJSONOptions);
            };

            Mutation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.CoexStateSync.Mutation";
            };

            return Mutation;
        })();

        return CoexStateSync;
    })();

    proto.CollectionName = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "COLLECTION_NAME_UNKNOWN"] = 0;
        values[valuesById[1] = "REGULAR"] = 1;
        values[valuesById[2] = "REGULAR_LOW"] = 2;
        values[valuesById[3] = "REGULAR_HIGH"] = 3;
        values[valuesById[4] = "CRITICAL_BLOCK"] = 4;
        values[valuesById[5] = "CRITICAL_UNBLOCK_LOW"] = 5;
        return values;
    })();

    proto.CombinedFingerprint = (function() {

        const CombinedFingerprint = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CombinedFingerprint.prototype.version = null;
        CombinedFingerprint.prototype.localFingerprint = null;
        CombinedFingerprint.prototype.remoteFingerprint = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CombinedFingerprint.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CombinedFingerprint.prototype, "_localFingerprint", {
            get: $util.oneOfGetter($oneOfFields = ["localFingerprint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CombinedFingerprint.prototype, "_remoteFingerprint", {
            get: $util.oneOfGetter($oneOfFields = ["remoteFingerprint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CombinedFingerprint.create = function(properties) {
            return new CombinedFingerprint(properties);
        };

        CombinedFingerprint.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(8).uint32(m.version);
            if (m.localFingerprint != null && $Object.hasOwnProperty.call(m, "localFingerprint"))
                $root.proto.FingerprintData.encode(m.localFingerprint, w.uint32(18).fork(), q + 1).ldelim();
            if (m.remoteFingerprint != null && $Object.hasOwnProperty.call(m, "remoteFingerprint"))
                $root.proto.FingerprintData.encode(m.remoteFingerprint, w.uint32(26).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CombinedFingerprint.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CombinedFingerprint();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.version = r.uint32();
                        m._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.localFingerprint = $root.proto.FingerprintData.decode(r, r.uint32(), $undefined, q + 1, m.localFingerprint);
                        m._localFingerprint = "localFingerprint";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.remoteFingerprint = $root.proto.FingerprintData.decode(r, r.uint32(), $undefined, q + 1, m.remoteFingerprint);
                        m._remoteFingerprint = "remoteFingerprint";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CombinedFingerprint.fromObject = function (d, q) {
            if (d instanceof $root.proto.CombinedFingerprint)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CombinedFingerprint: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CombinedFingerprint();
            if (d.version != null) {
                m.version = d.version >>> 0;
            }
            if (d.localFingerprint != null) {
                if (!$util.isObject(d.localFingerprint))
                    throw $TypeError(".proto.CombinedFingerprint.localFingerprint: object expected");
                m.localFingerprint = $root.proto.FingerprintData.fromObject(d.localFingerprint, q + 1);
            }
            if (d.remoteFingerprint != null) {
                if (!$util.isObject(d.remoteFingerprint))
                    throw $TypeError(".proto.CombinedFingerprint.remoteFingerprint: object expected");
                m.remoteFingerprint = $root.proto.FingerprintData.fromObject(d.remoteFingerprint, q + 1);
            }
            return m;
        };

        CombinedFingerprint.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = m.version;
            }
            if (m.localFingerprint != null && $Object.hasOwnProperty.call(m, "localFingerprint")) {
                d.localFingerprint = $root.proto.FingerprintData.toObject(m.localFingerprint, o, q + 1);
            }
            if (m.remoteFingerprint != null && $Object.hasOwnProperty.call(m, "remoteFingerprint")) {
                d.remoteFingerprint = $root.proto.FingerprintData.toObject(m.remoteFingerprint, o, q + 1);
            }
            return d;
        };

        CombinedFingerprint.prototype.toJSON = function() {
            return CombinedFingerprint.toObject(this, $protobuf.util.toJSONOptions);
        };

        CombinedFingerprint.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CombinedFingerprint";
        };

        return CombinedFingerprint;
    })();

    proto.Command = (function() {

        const Command = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Command.prototype.commandType = null;
        Command.prototype.offset = null;
        Command.prototype.length = null;
        Command.prototype.validationToken = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Command.prototype, "_commandType", {
            get: $util.oneOfGetter($oneOfFields = ["commandType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Command.prototype, "_offset", {
            get: $util.oneOfGetter($oneOfFields = ["offset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Command.prototype, "_length", {
            get: $util.oneOfGetter($oneOfFields = ["length"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Command.prototype, "_validationToken", {
            get: $util.oneOfGetter($oneOfFields = ["validationToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Command.create = function(properties) {
            return new Command(properties);
        };

        Command.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.commandType != null && $Object.hasOwnProperty.call(m, "commandType"))
                w.uint32(8).int32(m.commandType);
            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset"))
                w.uint32(16).uint32(m.offset);
            if (m.length != null && $Object.hasOwnProperty.call(m, "length"))
                w.uint32(24).uint32(m.length);
            if (m.validationToken != null && $Object.hasOwnProperty.call(m, "validationToken"))
                w.uint32(34).string(m.validationToken);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Command.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Command(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.commandType = r.int32();
                        m._commandType = "commandType";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.offset = r.uint32();
                        m._offset = "offset";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.length = r.uint32();
                        m._length = "length";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.validationToken = r.stringVerify();
                        m._validationToken = "validationToken";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Command.fromObject = function (d, q) {
            if (d instanceof $root.proto.Command)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Command: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Command();
            switch (d.commandType) {
            case "EVERYONE":
            case 1:
                m.commandType = 1;
                break;
            case "SILENT":
            case 2:
                m.commandType = 2;
                break;
            case "AI":
            case 3:
                m.commandType = 3;
                break;
            case "AI_IMAGINE":
            case 4:
                m.commandType = 4;
                break;
            default:
                if (typeof d.commandType === "number" && (d.commandType | 0) === d.commandType)
                    m.commandType = d.commandType;
            }
            if (d.offset != null) {
                m.offset = d.offset >>> 0;
            }
            if (d.length != null) {
                m.length = d.length >>> 0;
            }
            if (d.validationToken != null) {
                m.validationToken = $String(d.validationToken);
            }
            return m;
        };

        Command.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.commandType != null && $Object.hasOwnProperty.call(m, "commandType")) {
                d.commandType = o.enums === $String ? $root.proto.COMMAND_COMMAND_TYPE[m.commandType] === $undefined ? m.commandType : $root.proto.COMMAND_COMMAND_TYPE[m.commandType] : m.commandType;
            }
            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset")) {
                d.offset = m.offset;
            }
            if (m.length != null && $Object.hasOwnProperty.call(m, "length")) {
                d.length = m.length;
            }
            if (m.validationToken != null && $Object.hasOwnProperty.call(m, "validationToken")) {
                d.validationToken = m.validationToken;
            }
            return d;
        };

        Command.prototype.toJSON = function() {
            return Command.toObject(this, $protobuf.util.toJSONOptions);
        };

        Command.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Command";
        };

        return Command;
    })();

    proto.CommentMetadata = (function() {

        const CommentMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CommentMetadata.prototype.commentParentKey = null;
        CommentMetadata.prototype.replyCount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CommentMetadata.prototype, "_commentParentKey", {
            get: $util.oneOfGetter($oneOfFields = ["commentParentKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CommentMetadata.prototype, "_replyCount", {
            get: $util.oneOfGetter($oneOfFields = ["replyCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CommentMetadata.create = function(properties) {
            return new CommentMetadata(properties);
        };

        CommentMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.commentParentKey != null && $Object.hasOwnProperty.call(m, "commentParentKey"))
                $root.proto.MessageKey.encode(m.commentParentKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.replyCount != null && $Object.hasOwnProperty.call(m, "replyCount"))
                w.uint32(16).uint32(m.replyCount);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CommentMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CommentMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.commentParentKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.commentParentKey);
                        m._commentParentKey = "commentParentKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.replyCount = r.uint32();
                        m._replyCount = "replyCount";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CommentMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.CommentMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CommentMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CommentMetadata();
            if (d.commentParentKey != null) {
                if (!$util.isObject(d.commentParentKey))
                    throw $TypeError(".proto.CommentMetadata.commentParentKey: object expected");
                m.commentParentKey = $root.proto.MessageKey.fromObject(d.commentParentKey, q + 1);
            }
            if (d.replyCount != null) {
                m.replyCount = d.replyCount >>> 0;
            }
            return m;
        };

        CommentMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.commentParentKey != null && $Object.hasOwnProperty.call(m, "commentParentKey")) {
                d.commentParentKey = $root.proto.MessageKey.toObject(m.commentParentKey, o, q + 1);
            }
            if (m.replyCount != null && $Object.hasOwnProperty.call(m, "replyCount")) {
                d.replyCount = m.replyCount;
            }
            return d;
        };

        CommentMetadata.prototype.toJSON = function() {
            return CommentMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        CommentMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CommentMetadata";
        };

        return CommentMetadata;
    })();

    proto.CompanionCommitment = (function() {

        const CompanionCommitment = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CompanionCommitment.prototype.hash = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CompanionCommitment.prototype, "_hash", {
            get: $util.oneOfGetter($oneOfFields = ["hash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CompanionCommitment.create = function(properties) {
            return new CompanionCommitment(properties);
        };

        CompanionCommitment.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.hash != null && $Object.hasOwnProperty.call(m, "hash"))
                w.uint32(10).bytes(m.hash);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CompanionCommitment.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CompanionCommitment();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.hash = r.bytes();
                        m._hash = "hash";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CompanionCommitment.fromObject = function (d, q) {
            if (d instanceof $root.proto.CompanionCommitment)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CompanionCommitment: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CompanionCommitment();
            if (d.hash != null) {
                if (typeof d.hash === "string")
                    $util.base64.decode(d.hash, m.hash = $util.newBuffer($util.base64.length(d.hash)), 0);
                else if (d.hash.length >= 0)
                    m.hash = d.hash;
            }
            return m;
        };

        CompanionCommitment.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.hash != null && $Object.hasOwnProperty.call(m, "hash")) {
                d.hash = o.bytes === $String ? $util.base64.encode(m.hash, 0, m.hash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.hash) : m.hash;
            }
            return d;
        };

        CompanionCommitment.prototype.toJSON = function() {
            return CompanionCommitment.toObject(this, $protobuf.util.toJSONOptions);
        };

        CompanionCommitment.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CompanionCommitment";
        };

        return CompanionCommitment;
    })();

    proto.CompanionEphemeralIdentity = (function() {

        const CompanionEphemeralIdentity = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CompanionEphemeralIdentity.prototype.publicKey = null;
        CompanionEphemeralIdentity.prototype.deviceType = null;
        CompanionEphemeralIdentity.prototype.ref = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CompanionEphemeralIdentity.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CompanionEphemeralIdentity.prototype, "_deviceType", {
            get: $util.oneOfGetter($oneOfFields = ["deviceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CompanionEphemeralIdentity.prototype, "_ref", {
            get: $util.oneOfGetter($oneOfFields = ["ref"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CompanionEphemeralIdentity.create = function(properties) {
            return new CompanionEphemeralIdentity(properties);
        };

        CompanionEphemeralIdentity.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(10).bytes(m.publicKey);
            if (m.deviceType != null && $Object.hasOwnProperty.call(m, "deviceType"))
                w.uint32(16).int32(m.deviceType);
            if (m.ref != null && $Object.hasOwnProperty.call(m, "ref"))
                w.uint32(26).string(m.ref);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CompanionEphemeralIdentity.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CompanionEphemeralIdentity(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        m._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.deviceType = r.int32();
                        m._deviceType = "deviceType";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.ref = r.stringVerify();
                        m._ref = "ref";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CompanionEphemeralIdentity.fromObject = function (d, q) {
            if (d instanceof $root.proto.CompanionEphemeralIdentity)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CompanionEphemeralIdentity: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CompanionEphemeralIdentity();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            switch (d.deviceType) {
            case "UNKNOWN":
            case 0:
                m.deviceType = 0;
                break;
            case "CHROME":
            case 1:
                m.deviceType = 1;
                break;
            case "FIREFOX":
            case 2:
                m.deviceType = 2;
                break;
            case "IE":
            case 3:
                m.deviceType = 3;
                break;
            case "OPERA":
            case 4:
                m.deviceType = 4;
                break;
            case "SAFARI":
            case 5:
                m.deviceType = 5;
                break;
            case "EDGE":
            case 6:
                m.deviceType = 6;
                break;
            case "DESKTOP":
            case 7:
                m.deviceType = 7;
                break;
            case "IPAD":
            case 8:
                m.deviceType = 8;
                break;
            case "ANDROID_TABLET":
            case 9:
                m.deviceType = 9;
                break;
            case "OHANA":
            case 10:
                m.deviceType = 10;
                break;
            case "ALOHA":
            case 11:
                m.deviceType = 11;
                break;
            case "CATALINA":
            case 12:
                m.deviceType = 12;
                break;
            case "TCL_TV":
            case 13:
                m.deviceType = 13;
                break;
            case "IOS_PHONE":
            case 14:
                m.deviceType = 14;
                break;
            case "IOS_CATALYST":
            case 15:
                m.deviceType = 15;
                break;
            case "ANDROID_PHONE":
            case 16:
                m.deviceType = 16;
                break;
            case "ANDROID_AMBIGUOUS":
            case 17:
                m.deviceType = 17;
                break;
            case "WEAR_OS":
            case 18:
                m.deviceType = 18;
                break;
            case "AR_WRIST":
            case 19:
                m.deviceType = 19;
                break;
            case "AR_DEVICE":
            case 20:
                m.deviceType = 20;
                break;
            case "UWP":
            case 21:
                m.deviceType = 21;
                break;
            case "VR":
            case 22:
                m.deviceType = 22;
                break;
            case "CLOUD_API":
            case 23:
                m.deviceType = 23;
                break;
            case "SMARTGLASSES":
            case 24:
                m.deviceType = 24;
                break;
            case "WAIL":
            case 25:
                m.deviceType = 25;
                break;
            default:
                if (typeof d.deviceType === "number" && (d.deviceType | 0) === d.deviceType)
                    m.deviceType = d.deviceType;
            }
            if (d.ref != null) {
                m.ref = $String(d.ref);
            }
            return m;
        };

        CompanionEphemeralIdentity.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.deviceType != null && $Object.hasOwnProperty.call(m, "deviceType")) {
                d.deviceType = o.enums === $String ? $root.proto.DeviceProps.PlatformType[m.deviceType] === $undefined ? m.deviceType : $root.proto.DeviceProps.PlatformType[m.deviceType] : m.deviceType;
            }
            if (m.ref != null && $Object.hasOwnProperty.call(m, "ref")) {
                d.ref = m.ref;
            }
            return d;
        };

        CompanionEphemeralIdentity.prototype.toJSON = function() {
            return CompanionEphemeralIdentity.toObject(this, $protobuf.util.toJSONOptions);
        };

        CompanionEphemeralIdentity.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CompanionEphemeralIdentity";
        };

        return CompanionEphemeralIdentity;
    })();

    proto.Config = (function() {

        const Config = function (p) {
            this.field = {};
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Config.prototype.field = $util.emptyObject;
        Config.prototype.version = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Config.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Config.create = function(properties) {
            return new Config(properties);
        };

        Config.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.field != null && $Object.hasOwnProperty.call(m, "field")) {
                for (var ks = $Object.keys(m.field), i = 0; i < ks.length; ++i) {
                    w.uint32(10).fork().uint32(8).uint32(ks[i]);
                    $root.proto.Field.encode(m.field[ks[i]], w.uint32(18).fork(), q + 1).ldelim().ldelim();
                }
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(16).uint32(m.version);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Config.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Config(), k, v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (m.field === $util.emptyObject)
                            m.field = {};
                        var c2 = r.uint32() + r.pos;
                        k = 0;
                        v = null;
                        while (r.pos < c2) {
                            var t2 = r.tag();
                            u = t2 & 7;
                            switch (t2 >>>= 3) {
                            case 1:
                                if (u !== 0)
                                    break;
                                k = r.uint32();
                                continue;
                            case 2:
                                if (u !== 2)
                                    break;
                                v = $root.proto.Field.decode(r, r.uint32(), $undefined, q + 1, v);
                                continue;
                            }
                            r.skipType(u, q, t2);
                        }
                        m.field[k] = v || new $root.proto.Field();
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.version = r.uint32();
                        m._version = "version";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Config.fromObject = function (d, q) {
            if (d instanceof $root.proto.Config)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Config: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Config();
            if (d.field) {
                if (!$util.isObject(d.field))
                    throw $TypeError(".proto.Config.field: object expected");
                m.field = {};
                for (var ks = $Object.keys(d.field), i = 0; i < ks.length; ++i) {
                    if (ks[i] === "__proto__")
                        $util.makeProp(m.field, ks[i]);
                    if (!$util.isObject(d.field[ks[i]]))
                        throw $TypeError(".proto.Config.field: object expected");
                    m.field[ks[i]] = $root.proto.Field.fromObject(d.field[ks[i]], q + 1);
                }
            }
            if (d.version != null) {
                m.version = d.version >>> 0;
            }
            return m;
        };

        Config.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.objects || o.defaults) {
                d.field = {};
            }
            var ks2;
            if (m.field && (ks2 = $Object.keys(m.field)).length) {
                d.field = {};
                for (var j = 0; j < ks2.length; ++j) {
                    if (ks2[j] === "__proto__")
                        $util.makeProp(d.field, ks2[j]);
                    d.field[ks2[j]] = $root.proto.Field.toObject(m.field[ks2[j]], o, q + 1);
                }
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = m.version;
            }
            return d;
        };

        Config.prototype.toJSON = function() {
            return Config.toObject(this, $protobuf.util.toJSONOptions);
        };

        Config.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Config";
        };

        return Config;
    })();

    proto.ConsumerApplication = (function() {

        const ConsumerApplication = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ConsumerApplication.prototype.payload = null;
        ConsumerApplication.prototype.metadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ConsumerApplication.prototype, "_payload", {
            get: $util.oneOfGetter($oneOfFields = ["payload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ConsumerApplication.prototype, "_metadata", {
            get: $util.oneOfGetter($oneOfFields = ["metadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ConsumerApplication.create = function(properties) {
            return new ConsumerApplication(properties);
        };

        ConsumerApplication.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.payload != null && $Object.hasOwnProperty.call(m, "payload"))
                $root.proto.ConsumerApplication.Payload.encode(m.payload, w.uint32(10).fork(), q + 1).ldelim();
            if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata"))
                $root.proto.ConsumerApplication.Metadata.encode(m.metadata, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ConsumerApplication.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.payload = $root.proto.ConsumerApplication.Payload.decode(r, r.uint32(), $undefined, q + 1, m.payload);
                        m._payload = "payload";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.metadata = $root.proto.ConsumerApplication.Metadata.decode(r, r.uint32(), $undefined, q + 1, m.metadata);
                        m._metadata = "metadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ConsumerApplication.fromObject = function (d, q) {
            if (d instanceof $root.proto.ConsumerApplication)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ConsumerApplication: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ConsumerApplication();
            if (d.payload != null) {
                if (!$util.isObject(d.payload))
                    throw $TypeError(".proto.ConsumerApplication.payload: object expected");
                m.payload = $root.proto.ConsumerApplication.Payload.fromObject(d.payload, q + 1);
            }
            if (d.metadata != null) {
                if (!$util.isObject(d.metadata))
                    throw $TypeError(".proto.ConsumerApplication.metadata: object expected");
                m.metadata = $root.proto.ConsumerApplication.Metadata.fromObject(d.metadata, q + 1);
            }
            return m;
        };

        ConsumerApplication.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.payload != null && $Object.hasOwnProperty.call(m, "payload")) {
                d.payload = $root.proto.ConsumerApplication.Payload.toObject(m.payload, o, q + 1);
            }
            if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata")) {
                d.metadata = $root.proto.ConsumerApplication.Metadata.toObject(m.metadata, o, q + 1);
            }
            return d;
        };

        ConsumerApplication.prototype.toJSON = function() {
            return ConsumerApplication.toObject(this, $protobuf.util.toJSONOptions);
        };

        ConsumerApplication.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ConsumerApplication";
        };

        ConsumerApplication.ApplicationData = (function() {

            const ApplicationData = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ApplicationData.prototype.revoke = null;

            let $oneOfFields;

            $Object.defineProperty(ApplicationData.prototype, "applicationContent", {
                get: $util.oneOfGetter($oneOfFields = ["revoke"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ApplicationData.create = function(properties) {
                return new ApplicationData(properties);
            };

            ApplicationData.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.revoke != null && $Object.hasOwnProperty.call(m, "revoke"))
                    $root.proto.ConsumerApplication.RevokeMessage.encode(m.revoke, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ApplicationData.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ApplicationData();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.revoke = $root.proto.ConsumerApplication.RevokeMessage.decode(r, r.uint32(), $undefined, q + 1, m.revoke);
                            m.applicationContent = "revoke";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ApplicationData.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ApplicationData)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ApplicationData: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ApplicationData();
                if (d.revoke != null) {
                    if (!$util.isObject(d.revoke))
                        throw $TypeError(".proto.ConsumerApplication.ApplicationData.revoke: object expected");
                    m.revoke = $root.proto.ConsumerApplication.RevokeMessage.fromObject(d.revoke, q + 1);
                }
                return m;
            };

            ApplicationData.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.revoke != null && $Object.hasOwnProperty.call(m, "revoke")) {
                    d.revoke = $root.proto.ConsumerApplication.RevokeMessage.toObject(m.revoke, o, q + 1);
                    if (o.oneofs)
                        d.applicationContent = "revoke";
                }
                return d;
            };

            ApplicationData.prototype.toJSON = function() {
                return ApplicationData.toObject(this, $protobuf.util.toJSONOptions);
            };

            ApplicationData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ApplicationData";
            };

            return ApplicationData;
        })();

        ConsumerApplication.AudioMessage = (function() {

            const AudioMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AudioMessage.prototype.audio = null;
            AudioMessage.prototype.ptt = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_audio", {
                get: $util.oneOfGetter($oneOfFields = ["audio"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_ptt", {
                get: $util.oneOfGetter($oneOfFields = ["ptt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AudioMessage.create = function(properties) {
                return new AudioMessage(properties);
            };

            AudioMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.audio != null && $Object.hasOwnProperty.call(m, "audio"))
                    $root.proto.SubProtocol.encode(m.audio, w.uint32(10).fork(), q + 1).ldelim();
                if (m.ptt != null && $Object.hasOwnProperty.call(m, "ptt"))
                    w.uint32(16).bool(m.ptt);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AudioMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.AudioMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.audio = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.audio);
                            m._audio = "audio";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.ptt = r.bool();
                            m._ptt = "ptt";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AudioMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.AudioMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.AudioMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.AudioMessage();
                if (d.audio != null) {
                    if (!$util.isObject(d.audio))
                        throw $TypeError(".proto.ConsumerApplication.AudioMessage.audio: object expected");
                    m.audio = $root.proto.SubProtocol.fromObject(d.audio, q + 1);
                }
                if (d.ptt != null) {
                    m.ptt = $Boolean(d.ptt);
                }
                return m;
            };

            AudioMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.audio != null && $Object.hasOwnProperty.call(m, "audio")) {
                    d.audio = $root.proto.SubProtocol.toObject(m.audio, o, q + 1);
                }
                if (m.ptt != null && $Object.hasOwnProperty.call(m, "ptt")) {
                    d.ptt = m.ptt;
                }
                return d;
            };

            AudioMessage.prototype.toJSON = function() {
                return AudioMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            AudioMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.AudioMessage";
            };

            return AudioMessage;
        })();

        ConsumerApplication.ContactMessage = (function() {

            const ContactMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactMessage.prototype.contact = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactMessage.prototype, "_contact", {
                get: $util.oneOfGetter($oneOfFields = ["contact"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactMessage.create = function(properties) {
                return new ContactMessage(properties);
            };

            ContactMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.contact != null && $Object.hasOwnProperty.call(m, "contact"))
                    $root.proto.SubProtocol.encode(m.contact, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ContactMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.contact = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.contact);
                            m._contact = "contact";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ContactMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ContactMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ContactMessage();
                if (d.contact != null) {
                    if (!$util.isObject(d.contact))
                        throw $TypeError(".proto.ConsumerApplication.ContactMessage.contact: object expected");
                    m.contact = $root.proto.SubProtocol.fromObject(d.contact, q + 1);
                }
                return m;
            };

            ContactMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.contact != null && $Object.hasOwnProperty.call(m, "contact")) {
                    d.contact = $root.proto.SubProtocol.toObject(m.contact, o, q + 1);
                }
                return d;
            };

            ContactMessage.prototype.toJSON = function() {
                return ContactMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ContactMessage";
            };

            return ContactMessage;
        })();

        ConsumerApplication.ContactsArrayMessage = (function() {

            const ContactsArrayMessage = function (p) {
                this.contacts = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactsArrayMessage.prototype.displayName = null;
            ContactsArrayMessage.prototype.contacts = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactsArrayMessage.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactsArrayMessage.create = function(properties) {
                return new ContactsArrayMessage(properties);
            };

            ContactsArrayMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName"))
                    w.uint32(10).string(m.displayName);
                if (m.contacts != null && m.contacts.length) {
                    for (var i = 0; i < m.contacts.length; ++i)
                        $root.proto.ConsumerApplication.ContactMessage.encode(m.contacts[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactsArrayMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ContactsArrayMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayName = r.stringVerify();
                            m._displayName = "displayName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.contacts && m.contacts.length))
                                m.contacts = [];
                            m.contacts.push($root.proto.ConsumerApplication.ContactMessage.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactsArrayMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ContactsArrayMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ContactsArrayMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ContactsArrayMessage();
                if (d.displayName != null) {
                    m.displayName = $String(d.displayName);
                }
                if (d.contacts) {
                    if (!$Array.isArray(d.contacts))
                        throw $TypeError(".proto.ConsumerApplication.ContactsArrayMessage.contacts: array expected");
                    m.contacts = $Array(d.contacts.length);
                    for (var i = 0; i < d.contacts.length; ++i) {
                        if (!$util.isObject(d.contacts[i]))
                            throw $TypeError(".proto.ConsumerApplication.ContactsArrayMessage.contacts: object expected");
                        m.contacts[i] = $root.proto.ConsumerApplication.ContactMessage.fromObject(d.contacts[i], q + 1);
                    }
                }
                return m;
            };

            ContactsArrayMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.contacts = [];
                }
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName")) {
                    d.displayName = m.displayName;
                }
                if (m.contacts && m.contacts.length) {
                    d.contacts = $Array(m.contacts.length);
                    for (var j = 0; j < m.contacts.length; ++j) {
                        d.contacts[j] = $root.proto.ConsumerApplication.ContactMessage.toObject(m.contacts[j], o, q + 1);
                    }
                }
                return d;
            };

            ContactsArrayMessage.prototype.toJSON = function() {
                return ContactsArrayMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactsArrayMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ContactsArrayMessage";
            };

            return ContactsArrayMessage;
        })();

        ConsumerApplication.Content = (function() {

            const Content = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Content.prototype.messageText = null;
            Content.prototype.imageMessage = null;
            Content.prototype.contactMessage = null;
            Content.prototype.locationMessage = null;
            Content.prototype.extendedTextMessage = null;
            Content.prototype.statusTextMessage = null;
            Content.prototype.documentMessage = null;
            Content.prototype.audioMessage = null;
            Content.prototype.videoMessage = null;
            Content.prototype.contactsArrayMessage = null;
            Content.prototype.liveLocationMessage = null;
            Content.prototype.stickerMessage = null;
            Content.prototype.groupInviteMessage = null;
            Content.prototype.viewOnceMessage = null;
            Content.prototype.reactionMessage = null;
            Content.prototype.pollCreationMessage = null;
            Content.prototype.pollUpdateMessage = null;
            Content.prototype.editMessage = null;

            let $oneOfFields;

            $Object.defineProperty(Content.prototype, "content", {
                get: $util.oneOfGetter($oneOfFields = ["messageText", "imageMessage", "contactMessage", "locationMessage", "extendedTextMessage", "statusTextMessage", "documentMessage", "audioMessage", "videoMessage", "contactsArrayMessage", "liveLocationMessage", "stickerMessage", "groupInviteMessage", "viewOnceMessage", "reactionMessage", "pollCreationMessage", "pollUpdateMessage", "editMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Content.create = function(properties) {
                return new Content(properties);
            };

            Content.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.messageText != null && $Object.hasOwnProperty.call(m, "messageText"))
                    $root.proto.MessageText.encode(m.messageText, w.uint32(10).fork(), q + 1).ldelim();
                if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                    $root.proto.ConsumerApplication.ImageMessage.encode(m.imageMessage, w.uint32(18).fork(), q + 1).ldelim();
                if (m.contactMessage != null && $Object.hasOwnProperty.call(m, "contactMessage"))
                    $root.proto.ConsumerApplication.ContactMessage.encode(m.contactMessage, w.uint32(26).fork(), q + 1).ldelim();
                if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage"))
                    $root.proto.ConsumerApplication.LocationMessage.encode(m.locationMessage, w.uint32(34).fork(), q + 1).ldelim();
                if (m.extendedTextMessage != null && $Object.hasOwnProperty.call(m, "extendedTextMessage"))
                    $root.proto.ConsumerApplication.ExtendedTextMessage.encode(m.extendedTextMessage, w.uint32(42).fork(), q + 1).ldelim();
                if (m.statusTextMessage != null && $Object.hasOwnProperty.call(m, "statusTextMessage"))
                    $root.proto.ConsumerApplication.StatusTextMesage.encode(m.statusTextMessage, w.uint32(50).fork(), q + 1).ldelim();
                if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage"))
                    $root.proto.ConsumerApplication.DocumentMessage.encode(m.documentMessage, w.uint32(58).fork(), q + 1).ldelim();
                if (m.audioMessage != null && $Object.hasOwnProperty.call(m, "audioMessage"))
                    $root.proto.ConsumerApplication.AudioMessage.encode(m.audioMessage, w.uint32(66).fork(), q + 1).ldelim();
                if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                    $root.proto.ConsumerApplication.VideoMessage.encode(m.videoMessage, w.uint32(74).fork(), q + 1).ldelim();
                if (m.contactsArrayMessage != null && $Object.hasOwnProperty.call(m, "contactsArrayMessage"))
                    $root.proto.ConsumerApplication.ContactsArrayMessage.encode(m.contactsArrayMessage, w.uint32(82).fork(), q + 1).ldelim();
                if (m.liveLocationMessage != null && $Object.hasOwnProperty.call(m, "liveLocationMessage"))
                    $root.proto.ConsumerApplication.LiveLocationMessage.encode(m.liveLocationMessage, w.uint32(90).fork(), q + 1).ldelim();
                if (m.stickerMessage != null && $Object.hasOwnProperty.call(m, "stickerMessage"))
                    $root.proto.ConsumerApplication.StickerMessage.encode(m.stickerMessage, w.uint32(98).fork(), q + 1).ldelim();
                if (m.groupInviteMessage != null && $Object.hasOwnProperty.call(m, "groupInviteMessage"))
                    $root.proto.ConsumerApplication.GroupInviteMessage.encode(m.groupInviteMessage, w.uint32(106).fork(), q + 1).ldelim();
                if (m.viewOnceMessage != null && $Object.hasOwnProperty.call(m, "viewOnceMessage"))
                    $root.proto.ConsumerApplication.ViewOnceMessage.encode(m.viewOnceMessage, w.uint32(114).fork(), q + 1).ldelim();
                if (m.reactionMessage != null && $Object.hasOwnProperty.call(m, "reactionMessage"))
                    $root.proto.ConsumerApplication.ReactionMessage.encode(m.reactionMessage, w.uint32(130).fork(), q + 1).ldelim();
                if (m.pollCreationMessage != null && $Object.hasOwnProperty.call(m, "pollCreationMessage"))
                    $root.proto.ConsumerApplication.PollCreationMessage.encode(m.pollCreationMessage, w.uint32(138).fork(), q + 1).ldelim();
                if (m.pollUpdateMessage != null && $Object.hasOwnProperty.call(m, "pollUpdateMessage"))
                    $root.proto.ConsumerApplication.PollUpdateMessage.encode(m.pollUpdateMessage, w.uint32(146).fork(), q + 1).ldelim();
                if (m.editMessage != null && $Object.hasOwnProperty.call(m, "editMessage"))
                    $root.proto.ConsumerApplication.EditMessage.encode(m.editMessage, w.uint32(154).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Content.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Content();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.messageText = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.messageText);
                            m.content = "messageText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.imageMessage = $root.proto.ConsumerApplication.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                            m.content = "imageMessage";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.contactMessage = $root.proto.ConsumerApplication.ContactMessage.decode(r, r.uint32(), $undefined, q + 1, m.contactMessage);
                            m.content = "contactMessage";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.locationMessage = $root.proto.ConsumerApplication.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.locationMessage);
                            m.content = "locationMessage";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.extendedTextMessage = $root.proto.ConsumerApplication.ExtendedTextMessage.decode(r, r.uint32(), $undefined, q + 1, m.extendedTextMessage);
                            m.content = "extendedTextMessage";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.statusTextMessage = $root.proto.ConsumerApplication.StatusTextMesage.decode(r, r.uint32(), $undefined, q + 1, m.statusTextMessage);
                            m.content = "statusTextMessage";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.documentMessage = $root.proto.ConsumerApplication.DocumentMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentMessage);
                            m.content = "documentMessage";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.audioMessage = $root.proto.ConsumerApplication.AudioMessage.decode(r, r.uint32(), $undefined, q + 1, m.audioMessage);
                            m.content = "audioMessage";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.videoMessage = $root.proto.ConsumerApplication.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                            m.content = "videoMessage";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.contactsArrayMessage = $root.proto.ConsumerApplication.ContactsArrayMessage.decode(r, r.uint32(), $undefined, q + 1, m.contactsArrayMessage);
                            m.content = "contactsArrayMessage";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.liveLocationMessage = $root.proto.ConsumerApplication.LiveLocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.liveLocationMessage);
                            m.content = "liveLocationMessage";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.stickerMessage = $root.proto.ConsumerApplication.StickerMessage.decode(r, r.uint32(), $undefined, q + 1, m.stickerMessage);
                            m.content = "stickerMessage";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.groupInviteMessage = $root.proto.ConsumerApplication.GroupInviteMessage.decode(r, r.uint32(), $undefined, q + 1, m.groupInviteMessage);
                            m.content = "groupInviteMessage";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.viewOnceMessage = $root.proto.ConsumerApplication.ViewOnceMessage.decode(r, r.uint32(), $undefined, q + 1, m.viewOnceMessage);
                            m.content = "viewOnceMessage";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.reactionMessage = $root.proto.ConsumerApplication.ReactionMessage.decode(r, r.uint32(), $undefined, q + 1, m.reactionMessage);
                            m.content = "reactionMessage";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.pollCreationMessage = $root.proto.ConsumerApplication.PollCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessage);
                            m.content = "pollCreationMessage";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            m.pollUpdateMessage = $root.proto.ConsumerApplication.PollUpdateMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollUpdateMessage);
                            m.content = "pollUpdateMessage";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            m.editMessage = $root.proto.ConsumerApplication.EditMessage.decode(r, r.uint32(), $undefined, q + 1, m.editMessage);
                            m.content = "editMessage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Content.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Content)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Content: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.Content();
                if (d.messageText != null) {
                    if (!$util.isObject(d.messageText))
                        throw $TypeError(".proto.ConsumerApplication.Content.messageText: object expected");
                    m.messageText = $root.proto.MessageText.fromObject(d.messageText, q + 1);
                }
                if (d.imageMessage != null) {
                    if (!$util.isObject(d.imageMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.imageMessage: object expected");
                    m.imageMessage = $root.proto.ConsumerApplication.ImageMessage.fromObject(d.imageMessage, q + 1);
                }
                if (d.contactMessage != null) {
                    if (!$util.isObject(d.contactMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.contactMessage: object expected");
                    m.contactMessage = $root.proto.ConsumerApplication.ContactMessage.fromObject(d.contactMessage, q + 1);
                }
                if (d.locationMessage != null) {
                    if (!$util.isObject(d.locationMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.locationMessage: object expected");
                    m.locationMessage = $root.proto.ConsumerApplication.LocationMessage.fromObject(d.locationMessage, q + 1);
                }
                if (d.extendedTextMessage != null) {
                    if (!$util.isObject(d.extendedTextMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.extendedTextMessage: object expected");
                    m.extendedTextMessage = $root.proto.ConsumerApplication.ExtendedTextMessage.fromObject(d.extendedTextMessage, q + 1);
                }
                if (d.statusTextMessage != null) {
                    if (!$util.isObject(d.statusTextMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.statusTextMessage: object expected");
                    m.statusTextMessage = $root.proto.ConsumerApplication.StatusTextMesage.fromObject(d.statusTextMessage, q + 1);
                }
                if (d.documentMessage != null) {
                    if (!$util.isObject(d.documentMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.documentMessage: object expected");
                    m.documentMessage = $root.proto.ConsumerApplication.DocumentMessage.fromObject(d.documentMessage, q + 1);
                }
                if (d.audioMessage != null) {
                    if (!$util.isObject(d.audioMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.audioMessage: object expected");
                    m.audioMessage = $root.proto.ConsumerApplication.AudioMessage.fromObject(d.audioMessage, q + 1);
                }
                if (d.videoMessage != null) {
                    if (!$util.isObject(d.videoMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.videoMessage: object expected");
                    m.videoMessage = $root.proto.ConsumerApplication.VideoMessage.fromObject(d.videoMessage, q + 1);
                }
                if (d.contactsArrayMessage != null) {
                    if (!$util.isObject(d.contactsArrayMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.contactsArrayMessage: object expected");
                    m.contactsArrayMessage = $root.proto.ConsumerApplication.ContactsArrayMessage.fromObject(d.contactsArrayMessage, q + 1);
                }
                if (d.liveLocationMessage != null) {
                    if (!$util.isObject(d.liveLocationMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.liveLocationMessage: object expected");
                    m.liveLocationMessage = $root.proto.ConsumerApplication.LiveLocationMessage.fromObject(d.liveLocationMessage, q + 1);
                }
                if (d.stickerMessage != null) {
                    if (!$util.isObject(d.stickerMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.stickerMessage: object expected");
                    m.stickerMessage = $root.proto.ConsumerApplication.StickerMessage.fromObject(d.stickerMessage, q + 1);
                }
                if (d.groupInviteMessage != null) {
                    if (!$util.isObject(d.groupInviteMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.groupInviteMessage: object expected");
                    m.groupInviteMessage = $root.proto.ConsumerApplication.GroupInviteMessage.fromObject(d.groupInviteMessage, q + 1);
                }
                if (d.viewOnceMessage != null) {
                    if (!$util.isObject(d.viewOnceMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.viewOnceMessage: object expected");
                    m.viewOnceMessage = $root.proto.ConsumerApplication.ViewOnceMessage.fromObject(d.viewOnceMessage, q + 1);
                }
                if (d.reactionMessage != null) {
                    if (!$util.isObject(d.reactionMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.reactionMessage: object expected");
                    m.reactionMessage = $root.proto.ConsumerApplication.ReactionMessage.fromObject(d.reactionMessage, q + 1);
                }
                if (d.pollCreationMessage != null) {
                    if (!$util.isObject(d.pollCreationMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.pollCreationMessage: object expected");
                    m.pollCreationMessage = $root.proto.ConsumerApplication.PollCreationMessage.fromObject(d.pollCreationMessage, q + 1);
                }
                if (d.pollUpdateMessage != null) {
                    if (!$util.isObject(d.pollUpdateMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.pollUpdateMessage: object expected");
                    m.pollUpdateMessage = $root.proto.ConsumerApplication.PollUpdateMessage.fromObject(d.pollUpdateMessage, q + 1);
                }
                if (d.editMessage != null) {
                    if (!$util.isObject(d.editMessage))
                        throw $TypeError(".proto.ConsumerApplication.Content.editMessage: object expected");
                    m.editMessage = $root.proto.ConsumerApplication.EditMessage.fromObject(d.editMessage, q + 1);
                }
                return m;
            };

            Content.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.messageText != null && $Object.hasOwnProperty.call(m, "messageText")) {
                    d.messageText = $root.proto.MessageText.toObject(m.messageText, o, q + 1);
                    if (o.oneofs)
                        d.content = "messageText";
                }
                if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                    d.imageMessage = $root.proto.ConsumerApplication.ImageMessage.toObject(m.imageMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "imageMessage";
                }
                if (m.contactMessage != null && $Object.hasOwnProperty.call(m, "contactMessage")) {
                    d.contactMessage = $root.proto.ConsumerApplication.ContactMessage.toObject(m.contactMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "contactMessage";
                }
                if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage")) {
                    d.locationMessage = $root.proto.ConsumerApplication.LocationMessage.toObject(m.locationMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "locationMessage";
                }
                if (m.extendedTextMessage != null && $Object.hasOwnProperty.call(m, "extendedTextMessage")) {
                    d.extendedTextMessage = $root.proto.ConsumerApplication.ExtendedTextMessage.toObject(m.extendedTextMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "extendedTextMessage";
                }
                if (m.statusTextMessage != null && $Object.hasOwnProperty.call(m, "statusTextMessage")) {
                    d.statusTextMessage = $root.proto.ConsumerApplication.StatusTextMesage.toObject(m.statusTextMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "statusTextMessage";
                }
                if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage")) {
                    d.documentMessage = $root.proto.ConsumerApplication.DocumentMessage.toObject(m.documentMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "documentMessage";
                }
                if (m.audioMessage != null && $Object.hasOwnProperty.call(m, "audioMessage")) {
                    d.audioMessage = $root.proto.ConsumerApplication.AudioMessage.toObject(m.audioMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "audioMessage";
                }
                if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                    d.videoMessage = $root.proto.ConsumerApplication.VideoMessage.toObject(m.videoMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "videoMessage";
                }
                if (m.contactsArrayMessage != null && $Object.hasOwnProperty.call(m, "contactsArrayMessage")) {
                    d.contactsArrayMessage = $root.proto.ConsumerApplication.ContactsArrayMessage.toObject(m.contactsArrayMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "contactsArrayMessage";
                }
                if (m.liveLocationMessage != null && $Object.hasOwnProperty.call(m, "liveLocationMessage")) {
                    d.liveLocationMessage = $root.proto.ConsumerApplication.LiveLocationMessage.toObject(m.liveLocationMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "liveLocationMessage";
                }
                if (m.stickerMessage != null && $Object.hasOwnProperty.call(m, "stickerMessage")) {
                    d.stickerMessage = $root.proto.ConsumerApplication.StickerMessage.toObject(m.stickerMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "stickerMessage";
                }
                if (m.groupInviteMessage != null && $Object.hasOwnProperty.call(m, "groupInviteMessage")) {
                    d.groupInviteMessage = $root.proto.ConsumerApplication.GroupInviteMessage.toObject(m.groupInviteMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "groupInviteMessage";
                }
                if (m.viewOnceMessage != null && $Object.hasOwnProperty.call(m, "viewOnceMessage")) {
                    d.viewOnceMessage = $root.proto.ConsumerApplication.ViewOnceMessage.toObject(m.viewOnceMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "viewOnceMessage";
                }
                if (m.reactionMessage != null && $Object.hasOwnProperty.call(m, "reactionMessage")) {
                    d.reactionMessage = $root.proto.ConsumerApplication.ReactionMessage.toObject(m.reactionMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "reactionMessage";
                }
                if (m.pollCreationMessage != null && $Object.hasOwnProperty.call(m, "pollCreationMessage")) {
                    d.pollCreationMessage = $root.proto.ConsumerApplication.PollCreationMessage.toObject(m.pollCreationMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "pollCreationMessage";
                }
                if (m.pollUpdateMessage != null && $Object.hasOwnProperty.call(m, "pollUpdateMessage")) {
                    d.pollUpdateMessage = $root.proto.ConsumerApplication.PollUpdateMessage.toObject(m.pollUpdateMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "pollUpdateMessage";
                }
                if (m.editMessage != null && $Object.hasOwnProperty.call(m, "editMessage")) {
                    d.editMessage = $root.proto.ConsumerApplication.EditMessage.toObject(m.editMessage, o, q + 1);
                    if (o.oneofs)
                        d.content = "editMessage";
                }
                return d;
            };

            Content.prototype.toJSON = function() {
                return Content.toObject(this, $protobuf.util.toJSONOptions);
            };

            Content.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Content";
            };

            return Content;
        })();

        ConsumerApplication.DocumentMessage = (function() {

            const DocumentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DocumentMessage.prototype.document = null;
            DocumentMessage.prototype.fileName = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_document", {
                get: $util.oneOfGetter($oneOfFields = ["document"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_fileName", {
                get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DocumentMessage.create = function(properties) {
                return new DocumentMessage(properties);
            };

            DocumentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.document != null && $Object.hasOwnProperty.call(m, "document"))
                    $root.proto.SubProtocol.encode(m.document, w.uint32(10).fork(), q + 1).ldelim();
                if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName"))
                    w.uint32(18).string(m.fileName);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DocumentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.DocumentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.document = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.document);
                            m._document = "document";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.fileName = r.stringVerify();
                            m._fileName = "fileName";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DocumentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.DocumentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.DocumentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.DocumentMessage();
                if (d.document != null) {
                    if (!$util.isObject(d.document))
                        throw $TypeError(".proto.ConsumerApplication.DocumentMessage.document: object expected");
                    m.document = $root.proto.SubProtocol.fromObject(d.document, q + 1);
                }
                if (d.fileName != null) {
                    m.fileName = $String(d.fileName);
                }
                return m;
            };

            DocumentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.document != null && $Object.hasOwnProperty.call(m, "document")) {
                    d.document = $root.proto.SubProtocol.toObject(m.document, o, q + 1);
                }
                if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName")) {
                    d.fileName = m.fileName;
                }
                return d;
            };

            DocumentMessage.prototype.toJSON = function() {
                return DocumentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            DocumentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.DocumentMessage";
            };

            return DocumentMessage;
        })();

        ConsumerApplication.EditMessage = (function() {

            const EditMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EditMessage.prototype.key = null;
            EditMessage.prototype.message = null;
            EditMessage.prototype.timestampMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EditMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EditMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EditMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EditMessage.create = function(properties) {
                return new EditMessage(properties);
            };

            EditMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    $root.proto.MessageText.encode(m.message, w.uint32(18).fork(), q + 1).ldelim();
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                    w.uint32(24).int64(m.timestampMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EditMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.EditMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.message = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.message);
                            m._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.timestampMs = r.int64();
                            m._timestampMs = "timestampMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EditMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.EditMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.EditMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.EditMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.ConsumerApplication.EditMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.message != null) {
                    if (!$util.isObject(d.message))
                        throw $TypeError(".proto.ConsumerApplication.EditMessage.message: object expected");
                    m.message = $root.proto.MessageText.fromObject(d.message, q + 1);
                }
                if (d.timestampMs != null) {
                    if ($util.Long)
                        m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                    else if (typeof d.timestampMs === "string")
                        m.timestampMs = $parseInt(d.timestampMs, 10);
                    else if (typeof d.timestampMs === "number")
                        m.timestampMs = d.timestampMs;
                    else if (typeof d.timestampMs === "object")
                        m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
                }
                return m;
            };

            EditMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = $root.proto.MessageText.toObject(m.message, o, q + 1);
                }
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.timestampMs === "number")
                        d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                    else
                        d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
                }
                return d;
            };

            EditMessage.prototype.toJSON = function() {
                return EditMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EditMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.EditMessage";
            };

            return EditMessage;
        })();

        ConsumerApplication.ExtendedTextMessage = (function() {

            const ExtendedTextMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ExtendedTextMessage.prototype.text = null;
            ExtendedTextMessage.prototype.matchedText = null;
            ExtendedTextMessage.prototype.canonicalUrl = null;
            ExtendedTextMessage.prototype.description = null;
            ExtendedTextMessage.prototype.title = null;
            ExtendedTextMessage.prototype.thumbnail = null;
            ExtendedTextMessage.prototype.previewType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_matchedText", {
                get: $util.oneOfGetter($oneOfFields = ["matchedText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_canonicalUrl", {
                get: $util.oneOfGetter($oneOfFields = ["canonicalUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_previewType", {
                get: $util.oneOfGetter($oneOfFields = ["previewType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExtendedTextMessage.create = function(properties) {
                return new ExtendedTextMessage(properties);
            };

            ExtendedTextMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    $root.proto.MessageText.encode(m.text, w.uint32(10).fork(), q + 1).ldelim();
                if (m.matchedText != null && $Object.hasOwnProperty.call(m, "matchedText"))
                    w.uint32(18).string(m.matchedText);
                if (m.canonicalUrl != null && $Object.hasOwnProperty.call(m, "canonicalUrl"))
                    w.uint32(26).string(m.canonicalUrl);
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(34).string(m.description);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(42).string(m.title);
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail"))
                    $root.proto.SubProtocol.encode(m.thumbnail, w.uint32(50).fork(), q + 1).ldelim();
                if (m.previewType != null && $Object.hasOwnProperty.call(m, "previewType"))
                    w.uint32(56).int32(m.previewType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ExtendedTextMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ExtendedTextMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.text);
                            m._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.matchedText = r.stringVerify();
                            m._matchedText = "matchedText";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.canonicalUrl = r.stringVerify();
                            m._canonicalUrl = "canonicalUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.thumbnail = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.thumbnail);
                            m._thumbnail = "thumbnail";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.previewType = r.int32();
                            m._previewType = "previewType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ExtendedTextMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ExtendedTextMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ExtendedTextMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ExtendedTextMessage();
                if (d.text != null) {
                    if (!$util.isObject(d.text))
                        throw $TypeError(".proto.ConsumerApplication.ExtendedTextMessage.text: object expected");
                    m.text = $root.proto.MessageText.fromObject(d.text, q + 1);
                }
                if (d.matchedText != null) {
                    m.matchedText = $String(d.matchedText);
                }
                if (d.canonicalUrl != null) {
                    m.canonicalUrl = $String(d.canonicalUrl);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.thumbnail != null) {
                    if (!$util.isObject(d.thumbnail))
                        throw $TypeError(".proto.ConsumerApplication.ExtendedTextMessage.thumbnail: object expected");
                    m.thumbnail = $root.proto.SubProtocol.fromObject(d.thumbnail, q + 1);
                }
                switch (d.previewType) {
                case "NONE":
                case 0:
                    m.previewType = 0;
                    break;
                case "VIDEO":
                case 1:
                    m.previewType = 1;
                    break;
                default:
                    if (typeof d.previewType === "number" && (d.previewType | 0) === d.previewType)
                        m.previewType = d.previewType;
                }
                return m;
            };

            ExtendedTextMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = $root.proto.MessageText.toObject(m.text, o, q + 1);
                }
                if (m.matchedText != null && $Object.hasOwnProperty.call(m, "matchedText")) {
                    d.matchedText = m.matchedText;
                }
                if (m.canonicalUrl != null && $Object.hasOwnProperty.call(m, "canonicalUrl")) {
                    d.canonicalUrl = m.canonicalUrl;
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail")) {
                    d.thumbnail = $root.proto.SubProtocol.toObject(m.thumbnail, o, q + 1);
                }
                if (m.previewType != null && $Object.hasOwnProperty.call(m, "previewType")) {
                    d.previewType = o.enums === $String ? $root.proto.CONSUMER_APPLICATION_EXTENDED_TEXT_MESSAGE_PREVIEW_TYPE[m.previewType] === $undefined ? m.previewType : $root.proto.CONSUMER_APPLICATION_EXTENDED_TEXT_MESSAGE_PREVIEW_TYPE[m.previewType] : m.previewType;
                }
                return d;
            };

            ExtendedTextMessage.prototype.toJSON = function() {
                return ExtendedTextMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExtendedTextMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ExtendedTextMessage";
            };

            return ExtendedTextMessage;
        })();

        ConsumerApplication.GroupInviteMessage = (function() {

            const GroupInviteMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            GroupInviteMessage.prototype.groupJid = null;
            GroupInviteMessage.prototype.inviteCode = null;
            GroupInviteMessage.prototype.inviteExpiration = null;
            GroupInviteMessage.prototype.groupName = null;
            GroupInviteMessage.prototype.jpegThumbnail = null;
            GroupInviteMessage.prototype.caption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_groupJid", {
                get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_inviteCode", {
                get: $util.oneOfGetter($oneOfFields = ["inviteCode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_inviteExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_groupName", {
                get: $util.oneOfGetter($oneOfFields = ["groupName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            GroupInviteMessage.create = function(properties) {
                return new GroupInviteMessage(properties);
            };

            GroupInviteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid"))
                    w.uint32(10).string(m.groupJid);
                if (m.inviteCode != null && $Object.hasOwnProperty.call(m, "inviteCode"))
                    w.uint32(18).string(m.inviteCode);
                if (m.inviteExpiration != null && $Object.hasOwnProperty.call(m, "inviteExpiration"))
                    w.uint32(24).int64(m.inviteExpiration);
                if (m.groupName != null && $Object.hasOwnProperty.call(m, "groupName"))
                    w.uint32(34).string(m.groupName);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(42).bytes(m.jpegThumbnail);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    $root.proto.MessageText.encode(m.caption, w.uint32(50).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            GroupInviteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.GroupInviteMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.groupJid = r.stringVerify();
                            m._groupJid = "groupJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.inviteCode = r.stringVerify();
                            m._inviteCode = "inviteCode";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.inviteExpiration = r.int64();
                            m._inviteExpiration = "inviteExpiration";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.groupName = r.stringVerify();
                            m._groupName = "groupName";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.caption = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.caption);
                            m._caption = "caption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            GroupInviteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.GroupInviteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.GroupInviteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.GroupInviteMessage();
                if (d.groupJid != null) {
                    m.groupJid = $String(d.groupJid);
                }
                if (d.inviteCode != null) {
                    m.inviteCode = $String(d.inviteCode);
                }
                if (d.inviteExpiration != null) {
                    if ($util.Long)
                        m.inviteExpiration = $util.Long.fromValue(d.inviteExpiration, false);
                    else if (typeof d.inviteExpiration === "string")
                        m.inviteExpiration = $parseInt(d.inviteExpiration, 10);
                    else if (typeof d.inviteExpiration === "number")
                        m.inviteExpiration = d.inviteExpiration;
                    else if (typeof d.inviteExpiration === "object")
                        m.inviteExpiration = new $util.LongBits(d.inviteExpiration.low >>> 0, d.inviteExpiration.high >>> 0).toNumber();
                }
                if (d.groupName != null) {
                    m.groupName = $String(d.groupName);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.caption != null) {
                    if (!$util.isObject(d.caption))
                        throw $TypeError(".proto.ConsumerApplication.GroupInviteMessage.caption: object expected");
                    m.caption = $root.proto.MessageText.fromObject(d.caption, q + 1);
                }
                return m;
            };

            GroupInviteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid")) {
                    d.groupJid = m.groupJid;
                }
                if (m.inviteCode != null && $Object.hasOwnProperty.call(m, "inviteCode")) {
                    d.inviteCode = m.inviteCode;
                }
                if (m.inviteExpiration != null && $Object.hasOwnProperty.call(m, "inviteExpiration")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.inviteExpiration = typeof m.inviteExpiration === "number" ? $BigInt(m.inviteExpiration) : $util.Long.fromBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0, false).toBigInt();
                    else if (typeof m.inviteExpiration === "number")
                        d.inviteExpiration = o.longs === $String ? $String(m.inviteExpiration) : m.inviteExpiration;
                    else
                        d.inviteExpiration = o.longs === $String ? $util.Long.prototype.toString.call(m.inviteExpiration) : o.longs === $Number ? new $util.LongBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0).toNumber() : m.inviteExpiration;
                }
                if (m.groupName != null && $Object.hasOwnProperty.call(m, "groupName")) {
                    d.groupName = m.groupName;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = $root.proto.MessageText.toObject(m.caption, o, q + 1);
                }
                return d;
            };

            GroupInviteMessage.prototype.toJSON = function() {
                return GroupInviteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            GroupInviteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.GroupInviteMessage";
            };

            return GroupInviteMessage;
        })();

        ConsumerApplication.ImageMessage = (function() {

            const ImageMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ImageMessage.prototype.image = null;
            ImageMessage.prototype.caption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_image", {
                get: $util.oneOfGetter($oneOfFields = ["image"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ImageMessage.create = function(properties) {
                return new ImageMessage(properties);
            };

            ImageMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.image != null && $Object.hasOwnProperty.call(m, "image"))
                    $root.proto.SubProtocol.encode(m.image, w.uint32(10).fork(), q + 1).ldelim();
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    $root.proto.MessageText.encode(m.caption, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ImageMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ImageMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.image = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.image);
                            m._image = "image";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.caption = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.caption);
                            m._caption = "caption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ImageMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ImageMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ImageMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ImageMessage();
                if (d.image != null) {
                    if (!$util.isObject(d.image))
                        throw $TypeError(".proto.ConsumerApplication.ImageMessage.image: object expected");
                    m.image = $root.proto.SubProtocol.fromObject(d.image, q + 1);
                }
                if (d.caption != null) {
                    if (!$util.isObject(d.caption))
                        throw $TypeError(".proto.ConsumerApplication.ImageMessage.caption: object expected");
                    m.caption = $root.proto.MessageText.fromObject(d.caption, q + 1);
                }
                return m;
            };

            ImageMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.image != null && $Object.hasOwnProperty.call(m, "image")) {
                    d.image = $root.proto.SubProtocol.toObject(m.image, o, q + 1);
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = $root.proto.MessageText.toObject(m.caption, o, q + 1);
                }
                return d;
            };

            ImageMessage.prototype.toJSON = function() {
                return ImageMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ImageMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ImageMessage";
            };

            return ImageMessage;
        })();

        ConsumerApplication.InteractiveAnnotation = (function() {

            const InteractiveAnnotation = function (p) {
                this.polygonVertices = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InteractiveAnnotation.prototype.polygonVertices = $util.emptyArray;
            InteractiveAnnotation.prototype.location = null;

            let $oneOfFields;

            $Object.defineProperty(InteractiveAnnotation.prototype, "action", {
                get: $util.oneOfGetter($oneOfFields = ["location"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InteractiveAnnotation.create = function(properties) {
                return new InteractiveAnnotation(properties);
            };

            InteractiveAnnotation.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.polygonVertices != null && m.polygonVertices.length) {
                    for (var i = 0; i < m.polygonVertices.length; ++i)
                        $root.proto.ConsumerApplication.Point.encode(m.polygonVertices[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.location != null && $Object.hasOwnProperty.call(m, "location"))
                    $root.proto.ConsumerApplication.Location.encode(m.location, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InteractiveAnnotation.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.InteractiveAnnotation();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.polygonVertices && m.polygonVertices.length))
                                m.polygonVertices = [];
                            m.polygonVertices.push($root.proto.ConsumerApplication.Point.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.location = $root.proto.ConsumerApplication.Location.decode(r, r.uint32(), $undefined, q + 1, m.location);
                            m.action = "location";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InteractiveAnnotation.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.InteractiveAnnotation)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.InteractiveAnnotation: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.InteractiveAnnotation();
                if (d.polygonVertices) {
                    if (!$Array.isArray(d.polygonVertices))
                        throw $TypeError(".proto.ConsumerApplication.InteractiveAnnotation.polygonVertices: array expected");
                    m.polygonVertices = $Array(d.polygonVertices.length);
                    for (var i = 0; i < d.polygonVertices.length; ++i) {
                        if (!$util.isObject(d.polygonVertices[i]))
                            throw $TypeError(".proto.ConsumerApplication.InteractiveAnnotation.polygonVertices: object expected");
                        m.polygonVertices[i] = $root.proto.ConsumerApplication.Point.fromObject(d.polygonVertices[i], q + 1);
                    }
                }
                if (d.location != null) {
                    if (!$util.isObject(d.location))
                        throw $TypeError(".proto.ConsumerApplication.InteractiveAnnotation.location: object expected");
                    m.location = $root.proto.ConsumerApplication.Location.fromObject(d.location, q + 1);
                }
                return m;
            };

            InteractiveAnnotation.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.polygonVertices = [];
                }
                if (m.polygonVertices && m.polygonVertices.length) {
                    d.polygonVertices = $Array(m.polygonVertices.length);
                    for (var j = 0; j < m.polygonVertices.length; ++j) {
                        d.polygonVertices[j] = $root.proto.ConsumerApplication.Point.toObject(m.polygonVertices[j], o, q + 1);
                    }
                }
                if (m.location != null && $Object.hasOwnProperty.call(m, "location")) {
                    d.location = $root.proto.ConsumerApplication.Location.toObject(m.location, o, q + 1);
                    if (o.oneofs)
                        d.action = "location";
                }
                return d;
            };

            InteractiveAnnotation.prototype.toJSON = function() {
                return InteractiveAnnotation.toObject(this, $protobuf.util.toJSONOptions);
            };

            InteractiveAnnotation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.InteractiveAnnotation";
            };

            return InteractiveAnnotation;
        })();

        ConsumerApplication.LiveLocationMessage = (function() {

            const LiveLocationMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LiveLocationMessage.prototype.location = null;
            LiveLocationMessage.prototype.accuracyInMeters = null;
            LiveLocationMessage.prototype.speedInMps = null;
            LiveLocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;
            LiveLocationMessage.prototype.caption = null;
            LiveLocationMessage.prototype.sequenceNumber = null;
            LiveLocationMessage.prototype.timeOffset = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_location", {
                get: $util.oneOfGetter($oneOfFields = ["location"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_accuracyInMeters", {
                get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_speedInMps", {
                get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
                get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_sequenceNumber", {
                get: $util.oneOfGetter($oneOfFields = ["sequenceNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_timeOffset", {
                get: $util.oneOfGetter($oneOfFields = ["timeOffset"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LiveLocationMessage.create = function(properties) {
                return new LiveLocationMessage(properties);
            };

            LiveLocationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.location != null && $Object.hasOwnProperty.call(m, "location"))
                    $root.proto.ConsumerApplication.Location.encode(m.location, w.uint32(10).fork(), q + 1).ldelim();
                if (m.accuracyInMeters != null && $Object.hasOwnProperty.call(m, "accuracyInMeters"))
                    w.uint32(16).uint32(m.accuracyInMeters);
                if (m.speedInMps != null && $Object.hasOwnProperty.call(m, "speedInMps"))
                    w.uint32(29).float(m.speedInMps);
                if (m.degreesClockwiseFromMagneticNorth != null && $Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth"))
                    w.uint32(32).uint32(m.degreesClockwiseFromMagneticNorth);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    $root.proto.MessageText.encode(m.caption, w.uint32(42).fork(), q + 1).ldelim();
                if (m.sequenceNumber != null && $Object.hasOwnProperty.call(m, "sequenceNumber"))
                    w.uint32(48).int64(m.sequenceNumber);
                if (m.timeOffset != null && $Object.hasOwnProperty.call(m, "timeOffset"))
                    w.uint32(56).uint32(m.timeOffset);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LiveLocationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.LiveLocationMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.location = $root.proto.ConsumerApplication.Location.decode(r, r.uint32(), $undefined, q + 1, m.location);
                            m._location = "location";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.accuracyInMeters = r.uint32();
                            m._accuracyInMeters = "accuracyInMeters";
                            continue;
                        }
                    case 3: {
                            if (u !== 5)
                                break;
                            m.speedInMps = r.float();
                            m._speedInMps = "speedInMps";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.degreesClockwiseFromMagneticNorth = r.uint32();
                            m._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.caption = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.caption);
                            m._caption = "caption";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.sequenceNumber = r.int64();
                            m._sequenceNumber = "sequenceNumber";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.timeOffset = r.uint32();
                            m._timeOffset = "timeOffset";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LiveLocationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.LiveLocationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.LiveLocationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.LiveLocationMessage();
                if (d.location != null) {
                    if (!$util.isObject(d.location))
                        throw $TypeError(".proto.ConsumerApplication.LiveLocationMessage.location: object expected");
                    m.location = $root.proto.ConsumerApplication.Location.fromObject(d.location, q + 1);
                }
                if (d.accuracyInMeters != null) {
                    m.accuracyInMeters = d.accuracyInMeters >>> 0;
                }
                if (d.speedInMps != null) {
                    m.speedInMps = $Number(d.speedInMps);
                }
                if (d.degreesClockwiseFromMagneticNorth != null) {
                    m.degreesClockwiseFromMagneticNorth = d.degreesClockwiseFromMagneticNorth >>> 0;
                }
                if (d.caption != null) {
                    if (!$util.isObject(d.caption))
                        throw $TypeError(".proto.ConsumerApplication.LiveLocationMessage.caption: object expected");
                    m.caption = $root.proto.MessageText.fromObject(d.caption, q + 1);
                }
                if (d.sequenceNumber != null) {
                    if ($util.Long)
                        m.sequenceNumber = $util.Long.fromValue(d.sequenceNumber, false);
                    else if (typeof d.sequenceNumber === "string")
                        m.sequenceNumber = $parseInt(d.sequenceNumber, 10);
                    else if (typeof d.sequenceNumber === "number")
                        m.sequenceNumber = d.sequenceNumber;
                    else if (typeof d.sequenceNumber === "object")
                        m.sequenceNumber = new $util.LongBits(d.sequenceNumber.low >>> 0, d.sequenceNumber.high >>> 0).toNumber();
                }
                if (d.timeOffset != null) {
                    m.timeOffset = d.timeOffset >>> 0;
                }
                return m;
            };

            LiveLocationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.location != null && $Object.hasOwnProperty.call(m, "location")) {
                    d.location = $root.proto.ConsumerApplication.Location.toObject(m.location, o, q + 1);
                }
                if (m.accuracyInMeters != null && $Object.hasOwnProperty.call(m, "accuracyInMeters")) {
                    d.accuracyInMeters = m.accuracyInMeters;
                }
                if (m.speedInMps != null && $Object.hasOwnProperty.call(m, "speedInMps")) {
                    d.speedInMps = o.json && !$isFinite(m.speedInMps) ? $String(m.speedInMps) : m.speedInMps;
                }
                if (m.degreesClockwiseFromMagneticNorth != null && $Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth")) {
                    d.degreesClockwiseFromMagneticNorth = m.degreesClockwiseFromMagneticNorth;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = $root.proto.MessageText.toObject(m.caption, o, q + 1);
                }
                if (m.sequenceNumber != null && $Object.hasOwnProperty.call(m, "sequenceNumber")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.sequenceNumber = typeof m.sequenceNumber === "number" ? $BigInt(m.sequenceNumber) : $util.Long.fromBits(m.sequenceNumber.low >>> 0, m.sequenceNumber.high >>> 0, false).toBigInt();
                    else if (typeof m.sequenceNumber === "number")
                        d.sequenceNumber = o.longs === $String ? $String(m.sequenceNumber) : m.sequenceNumber;
                    else
                        d.sequenceNumber = o.longs === $String ? $util.Long.prototype.toString.call(m.sequenceNumber) : o.longs === $Number ? new $util.LongBits(m.sequenceNumber.low >>> 0, m.sequenceNumber.high >>> 0).toNumber() : m.sequenceNumber;
                }
                if (m.timeOffset != null && $Object.hasOwnProperty.call(m, "timeOffset")) {
                    d.timeOffset = m.timeOffset;
                }
                return d;
            };

            LiveLocationMessage.prototype.toJSON = function() {
                return LiveLocationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            LiveLocationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.LiveLocationMessage";
            };

            return LiveLocationMessage;
        })();

        ConsumerApplication.Location = (function() {

            const Location = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Location.prototype.degreesLatitude = null;
            Location.prototype.degreesLongitude = null;
            Location.prototype.name = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Location.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Location.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Location.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Location.create = function(properties) {
                return new Location(properties);
            };

            Location.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude"))
                    w.uint32(9).double(m.degreesLatitude);
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude"))
                    w.uint32(17).double(m.degreesLongitude);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(26).string(m.name);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Location.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Location();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            m.degreesLatitude = r.double();
                            m._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            m.degreesLongitude = r.double();
                            m._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Location.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Location)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Location: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.Location();
                if (d.degreesLatitude != null) {
                    m.degreesLatitude = $Number(d.degreesLatitude);
                }
                if (d.degreesLongitude != null) {
                    m.degreesLongitude = $Number(d.degreesLongitude);
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                return m;
            };

            Location.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude")) {
                    d.degreesLatitude = o.json && !$isFinite(m.degreesLatitude) ? $String(m.degreesLatitude) : m.degreesLatitude;
                }
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude")) {
                    d.degreesLongitude = o.json && !$isFinite(m.degreesLongitude) ? $String(m.degreesLongitude) : m.degreesLongitude;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                return d;
            };

            Location.prototype.toJSON = function() {
                return Location.toObject(this, $protobuf.util.toJSONOptions);
            };

            Location.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Location";
            };

            return Location;
        })();

        ConsumerApplication.LocationMessage = (function() {

            const LocationMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LocationMessage.prototype.location = null;
            LocationMessage.prototype.address = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_location", {
                get: $util.oneOfGetter($oneOfFields = ["location"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LocationMessage.create = function(properties) {
                return new LocationMessage(properties);
            };

            LocationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.location != null && $Object.hasOwnProperty.call(m, "location"))
                    $root.proto.ConsumerApplication.Location.encode(m.location, w.uint32(10).fork(), q + 1).ldelim();
                if (m.address != null && $Object.hasOwnProperty.call(m, "address"))
                    w.uint32(18).string(m.address);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LocationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.LocationMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.location = $root.proto.ConsumerApplication.Location.decode(r, r.uint32(), $undefined, q + 1, m.location);
                            m._location = "location";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.address = r.stringVerify();
                            m._address = "address";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LocationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.LocationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.LocationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.LocationMessage();
                if (d.location != null) {
                    if (!$util.isObject(d.location))
                        throw $TypeError(".proto.ConsumerApplication.LocationMessage.location: object expected");
                    m.location = $root.proto.ConsumerApplication.Location.fromObject(d.location, q + 1);
                }
                if (d.address != null) {
                    m.address = $String(d.address);
                }
                return m;
            };

            LocationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.location != null && $Object.hasOwnProperty.call(m, "location")) {
                    d.location = $root.proto.ConsumerApplication.Location.toObject(m.location, o, q + 1);
                }
                if (m.address != null && $Object.hasOwnProperty.call(m, "address")) {
                    d.address = m.address;
                }
                return d;
            };

            LocationMessage.prototype.toJSON = function() {
                return LocationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            LocationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.LocationMessage";
            };

            return LocationMessage;
        })();

        ConsumerApplication.MediaPayload = (function() {

            const MediaPayload = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MediaPayload.prototype.protocol = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaPayload.prototype, "_protocol", {
                get: $util.oneOfGetter($oneOfFields = ["protocol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MediaPayload.create = function(properties) {
                return new MediaPayload(properties);
            };

            MediaPayload.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.protocol != null && $Object.hasOwnProperty.call(m, "protocol"))
                    $root.proto.SubProtocol.encode(m.protocol, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MediaPayload.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.MediaPayload();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.protocol = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.protocol);
                            m._protocol = "protocol";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MediaPayload.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.MediaPayload)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.MediaPayload: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.MediaPayload();
                if (d.protocol != null) {
                    if (!$util.isObject(d.protocol))
                        throw $TypeError(".proto.ConsumerApplication.MediaPayload.protocol: object expected");
                    m.protocol = $root.proto.SubProtocol.fromObject(d.protocol, q + 1);
                }
                return m;
            };

            MediaPayload.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.protocol != null && $Object.hasOwnProperty.call(m, "protocol")) {
                    d.protocol = $root.proto.SubProtocol.toObject(m.protocol, o, q + 1);
                }
                return d;
            };

            MediaPayload.prototype.toJSON = function() {
                return MediaPayload.toObject(this, $protobuf.util.toJSONOptions);
            };

            MediaPayload.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.MediaPayload";
            };

            return MediaPayload;
        })();

        ConsumerApplication.Metadata = (function() {

            const Metadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Metadata.prototype.specialTextSize = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Metadata.prototype, "_specialTextSize", {
                get: $util.oneOfGetter($oneOfFields = ["specialTextSize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Metadata.create = function(properties) {
                return new Metadata(properties);
            };

            Metadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.specialTextSize != null && $Object.hasOwnProperty.call(m, "specialTextSize"))
                    w.uint32(8).int32(m.specialTextSize);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Metadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Metadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.specialTextSize = r.int32();
                            m._specialTextSize = "specialTextSize";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Metadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Metadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Metadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.Metadata();
                switch (d.specialTextSize) {
                case "SMALL":
                case 1:
                    m.specialTextSize = 1;
                    break;
                case "MEDIUM":
                case 2:
                    m.specialTextSize = 2;
                    break;
                case "LARGE":
                case 3:
                    m.specialTextSize = 3;
                    break;
                default:
                    if (typeof d.specialTextSize === "number" && (d.specialTextSize | 0) === d.specialTextSize)
                        m.specialTextSize = d.specialTextSize;
                }
                return m;
            };

            Metadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.specialTextSize != null && $Object.hasOwnProperty.call(m, "specialTextSize")) {
                    d.specialTextSize = o.enums === $String ? $root.proto.CONSUMER_APPLICATION_METADATA_SPECIAL_TEXT_SIZE[m.specialTextSize] === $undefined ? m.specialTextSize : $root.proto.CONSUMER_APPLICATION_METADATA_SPECIAL_TEXT_SIZE[m.specialTextSize] : m.specialTextSize;
                }
                return d;
            };

            Metadata.prototype.toJSON = function() {
                return Metadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            Metadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Metadata";
            };

            return Metadata;
        })();

        ConsumerApplication.Option = (function() {

            const Option = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Option.prototype.optionName = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Option.prototype, "_optionName", {
                get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Option.create = function(properties) {
                return new Option(properties);
            };

            Option.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.optionName != null && $Object.hasOwnProperty.call(m, "optionName"))
                    w.uint32(10).string(m.optionName);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Option.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Option();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.optionName = r.stringVerify();
                            m._optionName = "optionName";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Option.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Option)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Option: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.Option();
                if (d.optionName != null) {
                    m.optionName = $String(d.optionName);
                }
                return m;
            };

            Option.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.optionName != null && $Object.hasOwnProperty.call(m, "optionName")) {
                    d.optionName = m.optionName;
                }
                return d;
            };

            Option.prototype.toJSON = function() {
                return Option.toObject(this, $protobuf.util.toJSONOptions);
            };

            Option.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Option";
            };

            return Option;
        })();

        ConsumerApplication.Payload = (function() {

            const Payload = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Payload.prototype.content = null;
            Payload.prototype.applicationData = null;
            Payload.prototype.signal = null;
            Payload.prototype.subProtocol = null;

            let $oneOfFields;

            $Object.defineProperty(Payload.prototype, "payload", {
                get: $util.oneOfGetter($oneOfFields = ["content", "applicationData", "signal", "subProtocol"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Payload.create = function(properties) {
                return new Payload(properties);
            };

            Payload.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.content != null && $Object.hasOwnProperty.call(m, "content"))
                    $root.proto.ConsumerApplication.Content.encode(m.content, w.uint32(10).fork(), q + 1).ldelim();
                if (m.applicationData != null && $Object.hasOwnProperty.call(m, "applicationData"))
                    $root.proto.ConsumerApplication.ApplicationData.encode(m.applicationData, w.uint32(18).fork(), q + 1).ldelim();
                if (m.signal != null && $Object.hasOwnProperty.call(m, "signal"))
                    $root.proto.ConsumerApplication.Signal.encode(m.signal, w.uint32(26).fork(), q + 1).ldelim();
                if (m.subProtocol != null && $Object.hasOwnProperty.call(m, "subProtocol"))
                    $root.proto.ConsumerApplication.SubProtocolPayload.encode(m.subProtocol, w.uint32(34).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Payload.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Payload();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.content = $root.proto.ConsumerApplication.Content.decode(r, r.uint32(), $undefined, q + 1, m.content);
                            m.payload = "content";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.applicationData = $root.proto.ConsumerApplication.ApplicationData.decode(r, r.uint32(), $undefined, q + 1, m.applicationData);
                            m.payload = "applicationData";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.signal = $root.proto.ConsumerApplication.Signal.decode(r, r.uint32(), $undefined, q + 1, m.signal);
                            m.payload = "signal";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.subProtocol = $root.proto.ConsumerApplication.SubProtocolPayload.decode(r, r.uint32(), $undefined, q + 1, m.subProtocol);
                            m.payload = "subProtocol";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Payload.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Payload)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Payload: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.Payload();
                if (d.content != null) {
                    if (!$util.isObject(d.content))
                        throw $TypeError(".proto.ConsumerApplication.Payload.content: object expected");
                    m.content = $root.proto.ConsumerApplication.Content.fromObject(d.content, q + 1);
                }
                if (d.applicationData != null) {
                    if (!$util.isObject(d.applicationData))
                        throw $TypeError(".proto.ConsumerApplication.Payload.applicationData: object expected");
                    m.applicationData = $root.proto.ConsumerApplication.ApplicationData.fromObject(d.applicationData, q + 1);
                }
                if (d.signal != null) {
                    if (!$util.isObject(d.signal))
                        throw $TypeError(".proto.ConsumerApplication.Payload.signal: object expected");
                    m.signal = $root.proto.ConsumerApplication.Signal.fromObject(d.signal, q + 1);
                }
                if (d.subProtocol != null) {
                    if (!$util.isObject(d.subProtocol))
                        throw $TypeError(".proto.ConsumerApplication.Payload.subProtocol: object expected");
                    m.subProtocol = $root.proto.ConsumerApplication.SubProtocolPayload.fromObject(d.subProtocol, q + 1);
                }
                return m;
            };

            Payload.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.content != null && $Object.hasOwnProperty.call(m, "content")) {
                    d.content = $root.proto.ConsumerApplication.Content.toObject(m.content, o, q + 1);
                    if (o.oneofs)
                        d.payload = "content";
                }
                if (m.applicationData != null && $Object.hasOwnProperty.call(m, "applicationData")) {
                    d.applicationData = $root.proto.ConsumerApplication.ApplicationData.toObject(m.applicationData, o, q + 1);
                    if (o.oneofs)
                        d.payload = "applicationData";
                }
                if (m.signal != null && $Object.hasOwnProperty.call(m, "signal")) {
                    d.signal = $root.proto.ConsumerApplication.Signal.toObject(m.signal, o, q + 1);
                    if (o.oneofs)
                        d.payload = "signal";
                }
                if (m.subProtocol != null && $Object.hasOwnProperty.call(m, "subProtocol")) {
                    d.subProtocol = $root.proto.ConsumerApplication.SubProtocolPayload.toObject(m.subProtocol, o, q + 1);
                    if (o.oneofs)
                        d.payload = "subProtocol";
                }
                return d;
            };

            Payload.prototype.toJSON = function() {
                return Payload.toObject(this, $protobuf.util.toJSONOptions);
            };

            Payload.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Payload";
            };

            return Payload;
        })();

        ConsumerApplication.Point = (function() {

            const Point = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Point.prototype.x = null;
            Point.prototype.y = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Point.prototype, "_x", {
                get: $util.oneOfGetter($oneOfFields = ["x"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Point.prototype, "_y", {
                get: $util.oneOfGetter($oneOfFields = ["y"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Point.create = function(properties) {
                return new Point(properties);
            };

            Point.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.x != null && $Object.hasOwnProperty.call(m, "x"))
                    w.uint32(9).double(m.x);
                if (m.y != null && $Object.hasOwnProperty.call(m, "y"))
                    w.uint32(17).double(m.y);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Point.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Point();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            m.x = r.double();
                            m._x = "x";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            m.y = r.double();
                            m._y = "y";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Point.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Point)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Point: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.Point();
                if (d.x != null) {
                    m.x = $Number(d.x);
                }
                if (d.y != null) {
                    m.y = $Number(d.y);
                }
                return m;
            };

            Point.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.x != null && $Object.hasOwnProperty.call(m, "x")) {
                    d.x = o.json && !$isFinite(m.x) ? $String(m.x) : m.x;
                }
                if (m.y != null && $Object.hasOwnProperty.call(m, "y")) {
                    d.y = o.json && !$isFinite(m.y) ? $String(m.y) : m.y;
                }
                return d;
            };

            Point.prototype.toJSON = function() {
                return Point.toObject(this, $protobuf.util.toJSONOptions);
            };

            Point.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Point";
            };

            return Point;
        })();

        ConsumerApplication.PollAddOptionMessage = (function() {

            const PollAddOptionMessage = function (p) {
                this.pollOption = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollAddOptionMessage.prototype.pollOption = $util.emptyArray;

            PollAddOptionMessage.create = function(properties) {
                return new PollAddOptionMessage(properties);
            };

            PollAddOptionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pollOption != null && m.pollOption.length) {
                    for (var i = 0; i < m.pollOption.length; ++i)
                        $root.proto.ConsumerApplication.Option.encode(m.pollOption[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollAddOptionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.PollAddOptionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.pollOption && m.pollOption.length))
                                m.pollOption = [];
                            m.pollOption.push($root.proto.ConsumerApplication.Option.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollAddOptionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.PollAddOptionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.PollAddOptionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.PollAddOptionMessage();
                if (d.pollOption) {
                    if (!$Array.isArray(d.pollOption))
                        throw $TypeError(".proto.ConsumerApplication.PollAddOptionMessage.pollOption: array expected");
                    m.pollOption = $Array(d.pollOption.length);
                    for (var i = 0; i < d.pollOption.length; ++i) {
                        if (!$util.isObject(d.pollOption[i]))
                            throw $TypeError(".proto.ConsumerApplication.PollAddOptionMessage.pollOption: object expected");
                        m.pollOption[i] = $root.proto.ConsumerApplication.Option.fromObject(d.pollOption[i], q + 1);
                    }
                }
                return m;
            };

            PollAddOptionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.pollOption = [];
                }
                if (m.pollOption && m.pollOption.length) {
                    d.pollOption = $Array(m.pollOption.length);
                    for (var j = 0; j < m.pollOption.length; ++j) {
                        d.pollOption[j] = $root.proto.ConsumerApplication.Option.toObject(m.pollOption[j], o, q + 1);
                    }
                }
                return d;
            };

            PollAddOptionMessage.prototype.toJSON = function() {
                return PollAddOptionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollAddOptionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.PollAddOptionMessage";
            };

            return PollAddOptionMessage;
        })();

        ConsumerApplication.PollCreationMessage = (function() {

            const PollCreationMessage = function (p) {
                this.options = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollCreationMessage.prototype.encKey = null;
            PollCreationMessage.prototype.name = null;
            PollCreationMessage.prototype.options = $util.emptyArray;
            PollCreationMessage.prototype.selectableOptionsCount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_encKey", {
                get: $util.oneOfGetter($oneOfFields = ["encKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_selectableOptionsCount", {
                get: $util.oneOfGetter($oneOfFields = ["selectableOptionsCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollCreationMessage.create = function(properties) {
                return new PollCreationMessage(properties);
            };

            PollCreationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.encKey != null && $Object.hasOwnProperty.call(m, "encKey"))
                    w.uint32(10).bytes(m.encKey);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(18).string(m.name);
                if (m.options != null && m.options.length) {
                    for (var i = 0; i < m.options.length; ++i)
                        $root.proto.ConsumerApplication.Option.encode(m.options[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.selectableOptionsCount != null && $Object.hasOwnProperty.call(m, "selectableOptionsCount"))
                    w.uint32(32).uint32(m.selectableOptionsCount);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollCreationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.PollCreationMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.encKey = r.bytes();
                            m._encKey = "encKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.options && m.options.length))
                                m.options = [];
                            m.options.push($root.proto.ConsumerApplication.Option.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.selectableOptionsCount = r.uint32();
                            m._selectableOptionsCount = "selectableOptionsCount";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollCreationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.PollCreationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.PollCreationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.PollCreationMessage();
                if (d.encKey != null) {
                    if (typeof d.encKey === "string")
                        $util.base64.decode(d.encKey, m.encKey = $util.newBuffer($util.base64.length(d.encKey)), 0);
                    else if (d.encKey.length >= 0)
                        m.encKey = d.encKey;
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.options) {
                    if (!$Array.isArray(d.options))
                        throw $TypeError(".proto.ConsumerApplication.PollCreationMessage.options: array expected");
                    m.options = $Array(d.options.length);
                    for (var i = 0; i < d.options.length; ++i) {
                        if (!$util.isObject(d.options[i]))
                            throw $TypeError(".proto.ConsumerApplication.PollCreationMessage.options: object expected");
                        m.options[i] = $root.proto.ConsumerApplication.Option.fromObject(d.options[i], q + 1);
                    }
                }
                if (d.selectableOptionsCount != null) {
                    m.selectableOptionsCount = d.selectableOptionsCount >>> 0;
                }
                return m;
            };

            PollCreationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.options = [];
                }
                if (m.encKey != null && $Object.hasOwnProperty.call(m, "encKey")) {
                    d.encKey = o.bytes === $String ? $util.base64.encode(m.encKey, 0, m.encKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encKey) : m.encKey;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.options && m.options.length) {
                    d.options = $Array(m.options.length);
                    for (var j = 0; j < m.options.length; ++j) {
                        d.options[j] = $root.proto.ConsumerApplication.Option.toObject(m.options[j], o, q + 1);
                    }
                }
                if (m.selectableOptionsCount != null && $Object.hasOwnProperty.call(m, "selectableOptionsCount")) {
                    d.selectableOptionsCount = m.selectableOptionsCount;
                }
                return d;
            };

            PollCreationMessage.prototype.toJSON = function() {
                return PollCreationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollCreationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.PollCreationMessage";
            };

            return PollCreationMessage;
        })();

        ConsumerApplication.PollEncValue = (function() {

            const PollEncValue = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollEncValue.prototype.encPayload = null;
            PollEncValue.prototype.encIv = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollEncValue.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollEncValue.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollEncValue.create = function(properties) {
                return new PollEncValue(properties);
            };

            PollEncValue.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(10).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(18).bytes(m.encIv);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollEncValue.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.PollEncValue();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollEncValue.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.PollEncValue)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.PollEncValue: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.PollEncValue();
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                return m;
            };

            PollEncValue.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                return d;
            };

            PollEncValue.prototype.toJSON = function() {
                return PollEncValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollEncValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.PollEncValue";
            };

            return PollEncValue;
        })();

        ConsumerApplication.PollUpdateMessage = (function() {

            const PollUpdateMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollUpdateMessage.prototype.pollCreationMessageKey = null;
            PollUpdateMessage.prototype.vote = null;
            PollUpdateMessage.prototype.addOption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_pollCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_vote", {
                get: $util.oneOfGetter($oneOfFields = ["vote"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_addOption", {
                get: $util.oneOfGetter($oneOfFields = ["addOption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollUpdateMessage.create = function(properties) {
                return new PollUpdateMessage(properties);
            };

            PollUpdateMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pollCreationMessageKey != null && $Object.hasOwnProperty.call(m, "pollCreationMessageKey"))
                    $root.proto.MessageKey.encode(m.pollCreationMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.vote != null && $Object.hasOwnProperty.call(m, "vote"))
                    $root.proto.ConsumerApplication.PollEncValue.encode(m.vote, w.uint32(18).fork(), q + 1).ldelim();
                if (m.addOption != null && $Object.hasOwnProperty.call(m, "addOption"))
                    $root.proto.ConsumerApplication.PollEncValue.encode(m.addOption, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollUpdateMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.PollUpdateMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.pollCreationMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageKey);
                            m._pollCreationMessageKey = "pollCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.vote = $root.proto.ConsumerApplication.PollEncValue.decode(r, r.uint32(), $undefined, q + 1, m.vote);
                            m._vote = "vote";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.addOption = $root.proto.ConsumerApplication.PollEncValue.decode(r, r.uint32(), $undefined, q + 1, m.addOption);
                            m._addOption = "addOption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollUpdateMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.PollUpdateMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.PollUpdateMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.PollUpdateMessage();
                if (d.pollCreationMessageKey != null) {
                    if (!$util.isObject(d.pollCreationMessageKey))
                        throw $TypeError(".proto.ConsumerApplication.PollUpdateMessage.pollCreationMessageKey: object expected");
                    m.pollCreationMessageKey = $root.proto.MessageKey.fromObject(d.pollCreationMessageKey, q + 1);
                }
                if (d.vote != null) {
                    if (!$util.isObject(d.vote))
                        throw $TypeError(".proto.ConsumerApplication.PollUpdateMessage.vote: object expected");
                    m.vote = $root.proto.ConsumerApplication.PollEncValue.fromObject(d.vote, q + 1);
                }
                if (d.addOption != null) {
                    if (!$util.isObject(d.addOption))
                        throw $TypeError(".proto.ConsumerApplication.PollUpdateMessage.addOption: object expected");
                    m.addOption = $root.proto.ConsumerApplication.PollEncValue.fromObject(d.addOption, q + 1);
                }
                return m;
            };

            PollUpdateMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pollCreationMessageKey != null && $Object.hasOwnProperty.call(m, "pollCreationMessageKey")) {
                    d.pollCreationMessageKey = $root.proto.MessageKey.toObject(m.pollCreationMessageKey, o, q + 1);
                }
                if (m.vote != null && $Object.hasOwnProperty.call(m, "vote")) {
                    d.vote = $root.proto.ConsumerApplication.PollEncValue.toObject(m.vote, o, q + 1);
                }
                if (m.addOption != null && $Object.hasOwnProperty.call(m, "addOption")) {
                    d.addOption = $root.proto.ConsumerApplication.PollEncValue.toObject(m.addOption, o, q + 1);
                }
                return d;
            };

            PollUpdateMessage.prototype.toJSON = function() {
                return PollUpdateMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollUpdateMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.PollUpdateMessage";
            };

            return PollUpdateMessage;
        })();

        ConsumerApplication.PollVoteMessage = (function() {

            const PollVoteMessage = function (p) {
                this.selectedOptions = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollVoteMessage.prototype.selectedOptions = $util.emptyArray;
            PollVoteMessage.prototype.senderTimestampMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollVoteMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollVoteMessage.create = function(properties) {
                return new PollVoteMessage(properties);
            };

            PollVoteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.selectedOptions != null && m.selectedOptions.length) {
                    for (var i = 0; i < m.selectedOptions.length; ++i)
                        w.uint32(10).bytes(m.selectedOptions[i]);
                }
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                    w.uint32(16).int64(m.senderTimestampMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollVoteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.PollVoteMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.selectedOptions && m.selectedOptions.length))
                                m.selectedOptions = [];
                            m.selectedOptions.push(r.bytes());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.senderTimestampMs = r.int64();
                            m._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollVoteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.PollVoteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.PollVoteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.PollVoteMessage();
                if (d.selectedOptions) {
                    if (!$Array.isArray(d.selectedOptions))
                        throw $TypeError(".proto.ConsumerApplication.PollVoteMessage.selectedOptions: array expected");
                    m.selectedOptions = $Array(d.selectedOptions.length);
                    for (var i = 0; i < d.selectedOptions.length; ++i) {
                        if (typeof d.selectedOptions[i] === "string")
                            $util.base64.decode(d.selectedOptions[i], m.selectedOptions[i] = $util.newBuffer($util.base64.length(d.selectedOptions[i])), 0);
                        else if (d.selectedOptions[i].length >= 0)
                            m.selectedOptions[i] = d.selectedOptions[i];
                    }
                }
                if (d.senderTimestampMs != null) {
                    if ($util.Long)
                        m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                    else if (typeof d.senderTimestampMs === "string")
                        m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                    else if (typeof d.senderTimestampMs === "number")
                        m.senderTimestampMs = d.senderTimestampMs;
                    else if (typeof d.senderTimestampMs === "object")
                        m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
                }
                return m;
            };

            PollVoteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.selectedOptions = [];
                }
                if (m.selectedOptions && m.selectedOptions.length) {
                    d.selectedOptions = $Array(m.selectedOptions.length);
                    for (var j = 0; j < m.selectedOptions.length; ++j) {
                        d.selectedOptions[j] = o.bytes === $String ? $util.base64.encode(m.selectedOptions[j], 0, m.selectedOptions[j].length) : o.bytes === $Array ? $Array.prototype.slice.call(m.selectedOptions[j]) : m.selectedOptions[j];
                    }
                }
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.senderTimestampMs === "number")
                        d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                    else
                        d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
                }
                return d;
            };

            PollVoteMessage.prototype.toJSON = function() {
                return PollVoteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollVoteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.PollVoteMessage";
            };

            return PollVoteMessage;
        })();

        ConsumerApplication.ReactionMessage = (function() {

            const ReactionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ReactionMessage.prototype.key = null;
            ReactionMessage.prototype.text = null;
            ReactionMessage.prototype.groupingKey = null;
            ReactionMessage.prototype.senderTimestampMs = null;
            ReactionMessage.prototype.reactionMetadataDataclassData = null;
            ReactionMessage.prototype.style = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_groupingKey", {
                get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_reactionMetadataDataclassData", {
                get: $util.oneOfGetter($oneOfFields = ["reactionMetadataDataclassData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_style", {
                get: $util.oneOfGetter($oneOfFields = ["style"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ReactionMessage.create = function(properties) {
                return new ReactionMessage(properties);
            };

            ReactionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(18).string(m.text);
                if (m.groupingKey != null && $Object.hasOwnProperty.call(m, "groupingKey"))
                    w.uint32(26).string(m.groupingKey);
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                    w.uint32(32).int64(m.senderTimestampMs);
                if (m.reactionMetadataDataclassData != null && $Object.hasOwnProperty.call(m, "reactionMetadataDataclassData"))
                    w.uint32(42).string(m.reactionMetadataDataclassData);
                if (m.style != null && $Object.hasOwnProperty.call(m, "style"))
                    w.uint32(48).int32(m.style);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ReactionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ReactionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.groupingKey = r.stringVerify();
                            m._groupingKey = "groupingKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.senderTimestampMs = r.int64();
                            m._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.reactionMetadataDataclassData = r.stringVerify();
                            m._reactionMetadataDataclassData = "reactionMetadataDataclassData";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.style = r.int32();
                            m._style = "style";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ReactionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ReactionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ReactionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ReactionMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.ConsumerApplication.ReactionMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.groupingKey != null) {
                    m.groupingKey = $String(d.groupingKey);
                }
                if (d.senderTimestampMs != null) {
                    if ($util.Long)
                        m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                    else if (typeof d.senderTimestampMs === "string")
                        m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                    else if (typeof d.senderTimestampMs === "number")
                        m.senderTimestampMs = d.senderTimestampMs;
                    else if (typeof d.senderTimestampMs === "object")
                        m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
                }
                if (d.reactionMetadataDataclassData != null) {
                    m.reactionMetadataDataclassData = $String(d.reactionMetadataDataclassData);
                }
                if (d.style != null) {
                    m.style = d.style | 0;
                }
                return m;
            };

            ReactionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                if (m.groupingKey != null && $Object.hasOwnProperty.call(m, "groupingKey")) {
                    d.groupingKey = m.groupingKey;
                }
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.senderTimestampMs === "number")
                        d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                    else
                        d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
                }
                if (m.reactionMetadataDataclassData != null && $Object.hasOwnProperty.call(m, "reactionMetadataDataclassData")) {
                    d.reactionMetadataDataclassData = m.reactionMetadataDataclassData;
                }
                if (m.style != null && $Object.hasOwnProperty.call(m, "style")) {
                    d.style = m.style;
                }
                return d;
            };

            ReactionMessage.prototype.toJSON = function() {
                return ReactionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ReactionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ReactionMessage";
            };

            return ReactionMessage;
        })();

        ConsumerApplication.RevokeMessage = (function() {

            const RevokeMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RevokeMessage.prototype.key = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RevokeMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RevokeMessage.create = function(properties) {
                return new RevokeMessage(properties);
            };

            RevokeMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RevokeMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.RevokeMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RevokeMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.RevokeMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.RevokeMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.RevokeMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.ConsumerApplication.RevokeMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                return m;
            };

            RevokeMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                return d;
            };

            RevokeMessage.prototype.toJSON = function() {
                return RevokeMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            RevokeMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.RevokeMessage";
            };

            return RevokeMessage;
        })();

        ConsumerApplication.Signal = (function() {

            const Signal = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Signal.create = function(properties) {
                return new Signal(properties);
            };

            Signal.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Signal.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.Signal();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    r.skipType(t & 7, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Signal.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.Signal)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.Signal: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                return new $root.proto.ConsumerApplication.Signal();
            };

            Signal.toObject = function () {
                return {};
            };

            Signal.prototype.toJSON = function() {
                return Signal.toObject(this, $protobuf.util.toJSONOptions);
            };

            Signal.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.Signal";
            };

            return Signal;
        })();

        ConsumerApplication.StatusTextMesage = (function() {

            const StatusTextMesage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusTextMesage.prototype.text = null;
            StatusTextMesage.prototype.textArgb = null;
            StatusTextMesage.prototype.backgroundArgb = null;
            StatusTextMesage.prototype.font = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusTextMesage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusTextMesage.prototype, "_textArgb", {
                get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusTextMesage.prototype, "_backgroundArgb", {
                get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusTextMesage.prototype, "_font", {
                get: $util.oneOfGetter($oneOfFields = ["font"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusTextMesage.create = function(properties) {
                return new StatusTextMesage(properties);
            };

            StatusTextMesage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    $root.proto.ConsumerApplication.ExtendedTextMessage.encode(m.text, w.uint32(10).fork(), q + 1).ldelim();
                if (m.textArgb != null && $Object.hasOwnProperty.call(m, "textArgb"))
                    w.uint32(53).fixed32(m.textArgb);
                if (m.backgroundArgb != null && $Object.hasOwnProperty.call(m, "backgroundArgb"))
                    w.uint32(61).fixed32(m.backgroundArgb);
                if (m.font != null && $Object.hasOwnProperty.call(m, "font"))
                    w.uint32(64).int32(m.font);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusTextMesage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.StatusTextMesage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = $root.proto.ConsumerApplication.ExtendedTextMessage.decode(r, r.uint32(), $undefined, q + 1, m.text);
                            m._text = "text";
                            continue;
                        }
                    case 6: {
                            if (u !== 5)
                                break;
                            m.textArgb = r.fixed32();
                            m._textArgb = "textArgb";
                            continue;
                        }
                    case 7: {
                            if (u !== 5)
                                break;
                            m.backgroundArgb = r.fixed32();
                            m._backgroundArgb = "backgroundArgb";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.font = r.int32();
                            m._font = "font";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusTextMesage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.StatusTextMesage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.StatusTextMesage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.StatusTextMesage();
                if (d.text != null) {
                    if (!$util.isObject(d.text))
                        throw $TypeError(".proto.ConsumerApplication.StatusTextMesage.text: object expected");
                    m.text = $root.proto.ConsumerApplication.ExtendedTextMessage.fromObject(d.text, q + 1);
                }
                if (d.textArgb != null) {
                    m.textArgb = d.textArgb >>> 0;
                }
                if (d.backgroundArgb != null) {
                    m.backgroundArgb = d.backgroundArgb >>> 0;
                }
                switch (d.font) {
                case "SANS_SERIF":
                case 0:
                    m.font = 0;
                    break;
                case "SERIF":
                case 1:
                    m.font = 1;
                    break;
                case "NORICAN_REGULAR":
                case 2:
                    m.font = 2;
                    break;
                case "BRYNDAN_WRITE":
                case 3:
                    m.font = 3;
                    break;
                case "BEBASNEUE_REGULAR":
                case 4:
                    m.font = 4;
                    break;
                case "OSWALD_HEAVY":
                case 5:
                    m.font = 5;
                    break;
                default:
                    if (typeof d.font === "number" && (d.font | 0) === d.font)
                        m.font = d.font;
                }
                return m;
            };

            StatusTextMesage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = $root.proto.ConsumerApplication.ExtendedTextMessage.toObject(m.text, o, q + 1);
                }
                if (m.textArgb != null && $Object.hasOwnProperty.call(m, "textArgb")) {
                    d.textArgb = m.textArgb;
                }
                if (m.backgroundArgb != null && $Object.hasOwnProperty.call(m, "backgroundArgb")) {
                    d.backgroundArgb = m.backgroundArgb;
                }
                if (m.font != null && $Object.hasOwnProperty.call(m, "font")) {
                    d.font = o.enums === $String ? $root.proto.CONSUMER_APPLICATION_STATUS_TEXT_MESAGE_FONT_TYPE[m.font] === $undefined ? m.font : $root.proto.CONSUMER_APPLICATION_STATUS_TEXT_MESAGE_FONT_TYPE[m.font] : m.font;
                }
                return d;
            };

            StatusTextMesage.prototype.toJSON = function() {
                return StatusTextMesage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusTextMesage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.StatusTextMesage";
            };

            return StatusTextMesage;
        })();

        ConsumerApplication.StickerMessage = (function() {

            const StickerMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StickerMessage.prototype.sticker = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_sticker", {
                get: $util.oneOfGetter($oneOfFields = ["sticker"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StickerMessage.create = function(properties) {
                return new StickerMessage(properties);
            };

            StickerMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.sticker != null && $Object.hasOwnProperty.call(m, "sticker"))
                    $root.proto.SubProtocol.encode(m.sticker, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StickerMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.StickerMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.sticker = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.sticker);
                            m._sticker = "sticker";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StickerMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.StickerMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.StickerMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.StickerMessage();
                if (d.sticker != null) {
                    if (!$util.isObject(d.sticker))
                        throw $TypeError(".proto.ConsumerApplication.StickerMessage.sticker: object expected");
                    m.sticker = $root.proto.SubProtocol.fromObject(d.sticker, q + 1);
                }
                return m;
            };

            StickerMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.sticker != null && $Object.hasOwnProperty.call(m, "sticker")) {
                    d.sticker = $root.proto.SubProtocol.toObject(m.sticker, o, q + 1);
                }
                return d;
            };

            StickerMessage.prototype.toJSON = function() {
                return StickerMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StickerMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.StickerMessage";
            };

            return StickerMessage;
        })();

        ConsumerApplication.SubProtocolPayload = (function() {

            const SubProtocolPayload = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SubProtocolPayload.prototype.futureProof = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SubProtocolPayload.prototype, "_futureProof", {
                get: $util.oneOfGetter($oneOfFields = ["futureProof"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SubProtocolPayload.create = function(properties) {
                return new SubProtocolPayload(properties);
            };

            SubProtocolPayload.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.futureProof != null && $Object.hasOwnProperty.call(m, "futureProof"))
                    w.uint32(8).int32(m.futureProof);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SubProtocolPayload.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.SubProtocolPayload(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.futureProof = r.int32();
                            m._futureProof = "futureProof";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SubProtocolPayload.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.SubProtocolPayload)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.SubProtocolPayload: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.SubProtocolPayload();
                switch (d.futureProof) {
                case "PLACEHOLDER":
                case 0:
                    m.futureProof = 0;
                    break;
                case "NO_PLACEHOLDER":
                case 1:
                    m.futureProof = 1;
                    break;
                case "IGNORE":
                case 2:
                    m.futureProof = 2;
                    break;
                default:
                    if (typeof d.futureProof === "number" && (d.futureProof | 0) === d.futureProof)
                        m.futureProof = d.futureProof;
                }
                return m;
            };

            SubProtocolPayload.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.futureProof != null && $Object.hasOwnProperty.call(m, "futureProof")) {
                    d.futureProof = o.enums === $String ? $root.proto.FUTURE_PROOF_BEHAVIOR[m.futureProof] === $undefined ? m.futureProof : $root.proto.FUTURE_PROOF_BEHAVIOR[m.futureProof] : m.futureProof;
                }
                return d;
            };

            SubProtocolPayload.prototype.toJSON = function() {
                return SubProtocolPayload.toObject(this, $protobuf.util.toJSONOptions);
            };

            SubProtocolPayload.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.SubProtocolPayload";
            };

            return SubProtocolPayload;
        })();

        ConsumerApplication.VideoMessage = (function() {

            const VideoMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            VideoMessage.prototype.video = null;
            VideoMessage.prototype.caption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_video", {
                get: $util.oneOfGetter($oneOfFields = ["video"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            VideoMessage.create = function(properties) {
                return new VideoMessage(properties);
            };

            VideoMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.video != null && $Object.hasOwnProperty.call(m, "video"))
                    $root.proto.SubProtocol.encode(m.video, w.uint32(10).fork(), q + 1).ldelim();
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    $root.proto.MessageText.encode(m.caption, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            VideoMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.VideoMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.video = $root.proto.SubProtocol.decode(r, r.uint32(), $undefined, q + 1, m.video);
                            m._video = "video";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.caption = $root.proto.MessageText.decode(r, r.uint32(), $undefined, q + 1, m.caption);
                            m._caption = "caption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            VideoMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.VideoMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.VideoMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.VideoMessage();
                if (d.video != null) {
                    if (!$util.isObject(d.video))
                        throw $TypeError(".proto.ConsumerApplication.VideoMessage.video: object expected");
                    m.video = $root.proto.SubProtocol.fromObject(d.video, q + 1);
                }
                if (d.caption != null) {
                    if (!$util.isObject(d.caption))
                        throw $TypeError(".proto.ConsumerApplication.VideoMessage.caption: object expected");
                    m.caption = $root.proto.MessageText.fromObject(d.caption, q + 1);
                }
                return m;
            };

            VideoMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.video != null && $Object.hasOwnProperty.call(m, "video")) {
                    d.video = $root.proto.SubProtocol.toObject(m.video, o, q + 1);
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = $root.proto.MessageText.toObject(m.caption, o, q + 1);
                }
                return d;
            };

            VideoMessage.prototype.toJSON = function() {
                return VideoMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            VideoMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.VideoMessage";
            };

            return VideoMessage;
        })();

        ConsumerApplication.ViewOnceMessage = (function() {

            const ViewOnceMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ViewOnceMessage.prototype.imageMessage = null;
            ViewOnceMessage.prototype.videoMessage = null;

            let $oneOfFields;

            $Object.defineProperty(ViewOnceMessage.prototype, "viewOnceContent", {
                get: $util.oneOfGetter($oneOfFields = ["imageMessage", "videoMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ViewOnceMessage.create = function(properties) {
                return new ViewOnceMessage(properties);
            };

            ViewOnceMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                    $root.proto.ConsumerApplication.ImageMessage.encode(m.imageMessage, w.uint32(10).fork(), q + 1).ldelim();
                if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                    $root.proto.ConsumerApplication.VideoMessage.encode(m.videoMessage, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ViewOnceMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ConsumerApplication.ViewOnceMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.imageMessage = $root.proto.ConsumerApplication.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                            m.viewOnceContent = "imageMessage";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.videoMessage = $root.proto.ConsumerApplication.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                            m.viewOnceContent = "videoMessage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ViewOnceMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ConsumerApplication.ViewOnceMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ConsumerApplication.ViewOnceMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ConsumerApplication.ViewOnceMessage();
                if (d.imageMessage != null) {
                    if (!$util.isObject(d.imageMessage))
                        throw $TypeError(".proto.ConsumerApplication.ViewOnceMessage.imageMessage: object expected");
                    m.imageMessage = $root.proto.ConsumerApplication.ImageMessage.fromObject(d.imageMessage, q + 1);
                }
                if (d.videoMessage != null) {
                    if (!$util.isObject(d.videoMessage))
                        throw $TypeError(".proto.ConsumerApplication.ViewOnceMessage.videoMessage: object expected");
                    m.videoMessage = $root.proto.ConsumerApplication.VideoMessage.fromObject(d.videoMessage, q + 1);
                }
                return m;
            };

            ViewOnceMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                    d.imageMessage = $root.proto.ConsumerApplication.ImageMessage.toObject(m.imageMessage, o, q + 1);
                    if (o.oneofs)
                        d.viewOnceContent = "imageMessage";
                }
                if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                    d.videoMessage = $root.proto.ConsumerApplication.VideoMessage.toObject(m.videoMessage, o, q + 1);
                    if (o.oneofs)
                        d.viewOnceContent = "videoMessage";
                }
                return d;
            };

            ViewOnceMessage.prototype.toJSON = function() {
                return ViewOnceMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ViewOnceMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ConsumerApplication.ViewOnceMessage";
            };

            return ViewOnceMessage;
        })();

        return ConsumerApplication;
    })();

    proto.ContextInfo = (function() {

        const ContextInfo = function (p) {
            this.mentionedJid = [];
            this.groupMentions = [];
            this.statusAttributions = [];
            this.experienceIds = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ContextInfo.prototype.stanzaId = null;
        ContextInfo.prototype.participant = null;
        ContextInfo.prototype.quotedMessage = null;
        ContextInfo.prototype.remoteJid = null;
        ContextInfo.prototype.mentionedJid = $util.emptyArray;
        ContextInfo.prototype.conversionSource = null;
        ContextInfo.prototype.conversionData = null;
        ContextInfo.prototype.conversionDelaySeconds = null;
        ContextInfo.prototype.forwardingScore = null;
        ContextInfo.prototype.isForwarded = null;
        ContextInfo.prototype.quotedAd = null;
        ContextInfo.prototype.placeholderKey = null;
        ContextInfo.prototype.expiration = null;
        ContextInfo.prototype.ephemeralSettingTimestamp = null;
        ContextInfo.prototype.ephemeralSharedSecret = null;
        ContextInfo.prototype.externalAdReply = null;
        ContextInfo.prototype.entryPointConversionSource = null;
        ContextInfo.prototype.entryPointConversionApp = null;
        ContextInfo.prototype.entryPointConversionDelaySeconds = null;
        ContextInfo.prototype.disappearingMode = null;
        ContextInfo.prototype.actionLink = null;
        ContextInfo.prototype.groupSubject = null;
        ContextInfo.prototype.parentGroupJid = null;
        ContextInfo.prototype.trustBannerType = null;
        ContextInfo.prototype.trustBannerAction = null;
        ContextInfo.prototype.isSampled = null;
        ContextInfo.prototype.groupMentions = $util.emptyArray;
        ContextInfo.prototype.utm = null;
        ContextInfo.prototype.forwardedNewsletterMessageInfo = null;
        ContextInfo.prototype.businessMessageForwardInfo = null;
        ContextInfo.prototype.smbClientCampaignId = null;
        ContextInfo.prototype.smbServerCampaignId = null;
        ContextInfo.prototype.dataSharingContext = null;
        ContextInfo.prototype.alwaysShowAdAttribution = null;
        ContextInfo.prototype.featureEligibilities = null;
        ContextInfo.prototype.entryPointConversionExternalSource = null;
        ContextInfo.prototype.entryPointConversionExternalMedium = null;
        ContextInfo.prototype.ctwaSignals = null;
        ContextInfo.prototype.ctwaPayload = null;
        ContextInfo.prototype.forwardedAiBotMessageInfo = null;
        ContextInfo.prototype.statusAttributionType = null;
        ContextInfo.prototype.urlTrackingMap = null;
        ContextInfo.prototype.pairedMediaType = null;
        ContextInfo.prototype.rankingVersion = null;
        ContextInfo.prototype.memberLabel = null;
        ContextInfo.prototype.isQuestion = null;
        ContextInfo.prototype.statusSourceType = null;
        ContextInfo.prototype.statusAttributions = $util.emptyArray;
        ContextInfo.prototype.isGroupStatus = null;
        ContextInfo.prototype.forwardOrigin = null;
        ContextInfo.prototype.questionReplyQuotedMessage = null;
        ContextInfo.prototype.statusAudienceMetadata = null;
        ContextInfo.prototype.nonJidMentions = null;
        ContextInfo.prototype.quotedType = null;
        ContextInfo.prototype.botMessageSharingInfo = null;
        ContextInfo.prototype.isSpoiler = null;
        ContextInfo.prototype.mediaDomainInfo = null;
        ContextInfo.prototype.partiallySelectedContent = null;
        ContextInfo.prototype.afterReadDuration = null;
        ContextInfo.prototype.crossAppSource = null;
        ContextInfo.prototype.businessInteractionPills = null;
        ContextInfo.prototype.posterStatusId = null;
        ContextInfo.prototype.instagramThreadLink = null;
        ContextInfo.prototype.aiProvenance = null;
        ContextInfo.prototype.experienceIds = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_quotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["quotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_remoteJid", {
            get: $util.oneOfGetter($oneOfFields = ["remoteJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_conversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_conversionData", {
            get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_conversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["conversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_forwardingScore", {
            get: $util.oneOfGetter($oneOfFields = ["forwardingScore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_isForwarded", {
            get: $util.oneOfGetter($oneOfFields = ["isForwarded"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_quotedAd", {
            get: $util.oneOfGetter($oneOfFields = ["quotedAd"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_placeholderKey", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_expiration", {
            get: $util.oneOfGetter($oneOfFields = ["expiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_ephemeralSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_ephemeralSharedSecret", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSharedSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_externalAdReply", {
            get: $util.oneOfGetter($oneOfFields = ["externalAdReply"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_entryPointConversionSource", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_entryPointConversionApp", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionApp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_entryPointConversionDelaySeconds", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionDelaySeconds"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_actionLink", {
            get: $util.oneOfGetter($oneOfFields = ["actionLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_groupSubject", {
            get: $util.oneOfGetter($oneOfFields = ["groupSubject"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_parentGroupJid", {
            get: $util.oneOfGetter($oneOfFields = ["parentGroupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_trustBannerType", {
            get: $util.oneOfGetter($oneOfFields = ["trustBannerType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_trustBannerAction", {
            get: $util.oneOfGetter($oneOfFields = ["trustBannerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_isSampled", {
            get: $util.oneOfGetter($oneOfFields = ["isSampled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_utm", {
            get: $util.oneOfGetter($oneOfFields = ["utm"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_forwardedNewsletterMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["forwardedNewsletterMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_businessMessageForwardInfo", {
            get: $util.oneOfGetter($oneOfFields = ["businessMessageForwardInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_smbClientCampaignId", {
            get: $util.oneOfGetter($oneOfFields = ["smbClientCampaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_smbServerCampaignId", {
            get: $util.oneOfGetter($oneOfFields = ["smbServerCampaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_dataSharingContext", {
            get: $util.oneOfGetter($oneOfFields = ["dataSharingContext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_alwaysShowAdAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["alwaysShowAdAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_featureEligibilities", {
            get: $util.oneOfGetter($oneOfFields = ["featureEligibilities"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_entryPointConversionExternalSource", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionExternalSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_entryPointConversionExternalMedium", {
            get: $util.oneOfGetter($oneOfFields = ["entryPointConversionExternalMedium"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_ctwaSignals", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaSignals"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_ctwaPayload", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_forwardedAiBotMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["forwardedAiBotMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_statusAttributionType", {
            get: $util.oneOfGetter($oneOfFields = ["statusAttributionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_urlTrackingMap", {
            get: $util.oneOfGetter($oneOfFields = ["urlTrackingMap"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_pairedMediaType", {
            get: $util.oneOfGetter($oneOfFields = ["pairedMediaType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_rankingVersion", {
            get: $util.oneOfGetter($oneOfFields = ["rankingVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_memberLabel", {
            get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_isQuestion", {
            get: $util.oneOfGetter($oneOfFields = ["isQuestion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_statusSourceType", {
            get: $util.oneOfGetter($oneOfFields = ["statusSourceType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_isGroupStatus", {
            get: $util.oneOfGetter($oneOfFields = ["isGroupStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_forwardOrigin", {
            get: $util.oneOfGetter($oneOfFields = ["forwardOrigin"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_questionReplyQuotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionReplyQuotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_statusAudienceMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["statusAudienceMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_nonJidMentions", {
            get: $util.oneOfGetter($oneOfFields = ["nonJidMentions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_quotedType", {
            get: $util.oneOfGetter($oneOfFields = ["quotedType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_botMessageSharingInfo", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSharingInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_isSpoiler", {
            get: $util.oneOfGetter($oneOfFields = ["isSpoiler"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_mediaDomainInfo", {
            get: $util.oneOfGetter($oneOfFields = ["mediaDomainInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_partiallySelectedContent", {
            get: $util.oneOfGetter($oneOfFields = ["partiallySelectedContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_afterReadDuration", {
            get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_crossAppSource", {
            get: $util.oneOfGetter($oneOfFields = ["crossAppSource"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_businessInteractionPills", {
            get: $util.oneOfGetter($oneOfFields = ["businessInteractionPills"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_posterStatusId", {
            get: $util.oneOfGetter($oneOfFields = ["posterStatusId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_instagramThreadLink", {
            get: $util.oneOfGetter($oneOfFields = ["instagramThreadLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ContextInfo.prototype, "_aiProvenance", {
            get: $util.oneOfGetter($oneOfFields = ["aiProvenance"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ContextInfo.create = function(properties) {
            return new ContextInfo(properties);
        };

        ContextInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                w.uint32(10).string(m.stanzaId);
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant"))
                w.uint32(18).string(m.participant);
            if (m.quotedMessage != null && $Object.hasOwnProperty.call(m, "quotedMessage"))
                $root.proto.Message.encode(m.quotedMessage, w.uint32(26).fork(), q + 1).ldelim();
            if (m.remoteJid != null && $Object.hasOwnProperty.call(m, "remoteJid"))
                w.uint32(34).string(m.remoteJid);
            if (m.mentionedJid != null && m.mentionedJid.length) {
                for (var i = 0; i < m.mentionedJid.length; ++i)
                    w.uint32(122).string(m.mentionedJid[i]);
            }
            if (m.conversionSource != null && $Object.hasOwnProperty.call(m, "conversionSource"))
                w.uint32(146).string(m.conversionSource);
            if (m.conversionData != null && $Object.hasOwnProperty.call(m, "conversionData"))
                w.uint32(154).bytes(m.conversionData);
            if (m.conversionDelaySeconds != null && $Object.hasOwnProperty.call(m, "conversionDelaySeconds"))
                w.uint32(160).uint32(m.conversionDelaySeconds);
            if (m.forwardingScore != null && $Object.hasOwnProperty.call(m, "forwardingScore"))
                w.uint32(168).uint32(m.forwardingScore);
            if (m.isForwarded != null && $Object.hasOwnProperty.call(m, "isForwarded"))
                w.uint32(176).bool(m.isForwarded);
            if (m.quotedAd != null && $Object.hasOwnProperty.call(m, "quotedAd"))
                $root.proto.ContextInfo.AdReplyInfo.encode(m.quotedAd, w.uint32(186).fork(), q + 1).ldelim();
            if (m.placeholderKey != null && $Object.hasOwnProperty.call(m, "placeholderKey"))
                $root.proto.MessageKey.encode(m.placeholderKey, w.uint32(194).fork(), q + 1).ldelim();
            if (m.expiration != null && $Object.hasOwnProperty.call(m, "expiration"))
                w.uint32(200).uint32(m.expiration);
            if (m.ephemeralSettingTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp"))
                w.uint32(208).int64(m.ephemeralSettingTimestamp);
            if (m.ephemeralSharedSecret != null && $Object.hasOwnProperty.call(m, "ephemeralSharedSecret"))
                w.uint32(218).bytes(m.ephemeralSharedSecret);
            if (m.externalAdReply != null && $Object.hasOwnProperty.call(m, "externalAdReply"))
                $root.proto.ContextInfo.ExternalAdReplyInfo.encode(m.externalAdReply, w.uint32(226).fork(), q + 1).ldelim();
            if (m.entryPointConversionSource != null && $Object.hasOwnProperty.call(m, "entryPointConversionSource"))
                w.uint32(234).string(m.entryPointConversionSource);
            if (m.entryPointConversionApp != null && $Object.hasOwnProperty.call(m, "entryPointConversionApp"))
                w.uint32(242).string(m.entryPointConversionApp);
            if (m.entryPointConversionDelaySeconds != null && $Object.hasOwnProperty.call(m, "entryPointConversionDelaySeconds"))
                w.uint32(248).uint32(m.entryPointConversionDelaySeconds);
            if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode"))
                $root.proto.DisappearingMode.encode(m.disappearingMode, w.uint32(258).fork(), q + 1).ldelim();
            if (m.actionLink != null && $Object.hasOwnProperty.call(m, "actionLink"))
                $root.proto.ActionLink.encode(m.actionLink, w.uint32(266).fork(), q + 1).ldelim();
            if (m.groupSubject != null && $Object.hasOwnProperty.call(m, "groupSubject"))
                w.uint32(274).string(m.groupSubject);
            if (m.parentGroupJid != null && $Object.hasOwnProperty.call(m, "parentGroupJid"))
                w.uint32(282).string(m.parentGroupJid);
            if (m.trustBannerType != null && $Object.hasOwnProperty.call(m, "trustBannerType"))
                w.uint32(298).string(m.trustBannerType);
            if (m.trustBannerAction != null && $Object.hasOwnProperty.call(m, "trustBannerAction"))
                w.uint32(304).uint32(m.trustBannerAction);
            if (m.isSampled != null && $Object.hasOwnProperty.call(m, "isSampled"))
                w.uint32(312).bool(m.isSampled);
            if (m.groupMentions != null && m.groupMentions.length) {
                for (var i = 0; i < m.groupMentions.length; ++i)
                    $root.proto.GroupMention.encode(m.groupMentions[i], w.uint32(322).fork(), q + 1).ldelim();
            }
            if (m.utm != null && $Object.hasOwnProperty.call(m, "utm"))
                $root.proto.ContextInfo.UTMInfo.encode(m.utm, w.uint32(330).fork(), q + 1).ldelim();
            if (m.forwardedNewsletterMessageInfo != null && $Object.hasOwnProperty.call(m, "forwardedNewsletterMessageInfo"))
                $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.encode(m.forwardedNewsletterMessageInfo, w.uint32(346).fork(), q + 1).ldelim();
            if (m.businessMessageForwardInfo != null && $Object.hasOwnProperty.call(m, "businessMessageForwardInfo"))
                $root.proto.ContextInfo.BusinessMessageForwardInfo.encode(m.businessMessageForwardInfo, w.uint32(354).fork(), q + 1).ldelim();
            if (m.smbClientCampaignId != null && $Object.hasOwnProperty.call(m, "smbClientCampaignId"))
                w.uint32(362).string(m.smbClientCampaignId);
            if (m.smbServerCampaignId != null && $Object.hasOwnProperty.call(m, "smbServerCampaignId"))
                w.uint32(370).string(m.smbServerCampaignId);
            if (m.dataSharingContext != null && $Object.hasOwnProperty.call(m, "dataSharingContext"))
                $root.proto.ContextInfo.DataSharingContext.encode(m.dataSharingContext, w.uint32(378).fork(), q + 1).ldelim();
            if (m.alwaysShowAdAttribution != null && $Object.hasOwnProperty.call(m, "alwaysShowAdAttribution"))
                w.uint32(384).bool(m.alwaysShowAdAttribution);
            if (m.featureEligibilities != null && $Object.hasOwnProperty.call(m, "featureEligibilities"))
                $root.proto.ContextInfo.FeatureEligibilities.encode(m.featureEligibilities, w.uint32(394).fork(), q + 1).ldelim();
            if (m.entryPointConversionExternalSource != null && $Object.hasOwnProperty.call(m, "entryPointConversionExternalSource"))
                w.uint32(402).string(m.entryPointConversionExternalSource);
            if (m.entryPointConversionExternalMedium != null && $Object.hasOwnProperty.call(m, "entryPointConversionExternalMedium"))
                w.uint32(410).string(m.entryPointConversionExternalMedium);
            if (m.ctwaSignals != null && $Object.hasOwnProperty.call(m, "ctwaSignals"))
                w.uint32(434).string(m.ctwaSignals);
            if (m.ctwaPayload != null && $Object.hasOwnProperty.call(m, "ctwaPayload"))
                w.uint32(442).bytes(m.ctwaPayload);
            if (m.forwardedAiBotMessageInfo != null && $Object.hasOwnProperty.call(m, "forwardedAiBotMessageInfo"))
                $root.proto.ForwardedAIBotMessageInfo.encode(m.forwardedAiBotMessageInfo, w.uint32(450).fork(), q + 1).ldelim();
            if (m.statusAttributionType != null && $Object.hasOwnProperty.call(m, "statusAttributionType"))
                w.uint32(456).int32(m.statusAttributionType);
            if (m.urlTrackingMap != null && $Object.hasOwnProperty.call(m, "urlTrackingMap"))
                $root.proto.UrlTrackingMap.encode(m.urlTrackingMap, w.uint32(466).fork(), q + 1).ldelim();
            if (m.pairedMediaType != null && $Object.hasOwnProperty.call(m, "pairedMediaType"))
                w.uint32(472).int32(m.pairedMediaType);
            if (m.rankingVersion != null && $Object.hasOwnProperty.call(m, "rankingVersion"))
                w.uint32(480).uint32(m.rankingVersion);
            if (m.memberLabel != null && $Object.hasOwnProperty.call(m, "memberLabel"))
                $root.proto.MemberLabel.encode(m.memberLabel, w.uint32(498).fork(), q + 1).ldelim();
            if (m.isQuestion != null && $Object.hasOwnProperty.call(m, "isQuestion"))
                w.uint32(504).bool(m.isQuestion);
            if (m.statusSourceType != null && $Object.hasOwnProperty.call(m, "statusSourceType"))
                w.uint32(512).int32(m.statusSourceType);
            if (m.statusAttributions != null && m.statusAttributions.length) {
                for (var i = 0; i < m.statusAttributions.length; ++i)
                    $root.proto.StatusAttribution.encode(m.statusAttributions[i], w.uint32(522).fork(), q + 1).ldelim();
            }
            if (m.isGroupStatus != null && $Object.hasOwnProperty.call(m, "isGroupStatus"))
                w.uint32(528).bool(m.isGroupStatus);
            if (m.forwardOrigin != null && $Object.hasOwnProperty.call(m, "forwardOrigin"))
                w.uint32(536).int32(m.forwardOrigin);
            if (m.questionReplyQuotedMessage != null && $Object.hasOwnProperty.call(m, "questionReplyQuotedMessage"))
                $root.proto.ContextInfo.QuestionReplyQuotedMessage.encode(m.questionReplyQuotedMessage, w.uint32(546).fork(), q + 1).ldelim();
            if (m.statusAudienceMetadata != null && $Object.hasOwnProperty.call(m, "statusAudienceMetadata"))
                $root.proto.ContextInfo.StatusAudienceMetadata.encode(m.statusAudienceMetadata, w.uint32(554).fork(), q + 1).ldelim();
            if (m.nonJidMentions != null && $Object.hasOwnProperty.call(m, "nonJidMentions"))
                w.uint32(560).uint32(m.nonJidMentions);
            if (m.quotedType != null && $Object.hasOwnProperty.call(m, "quotedType"))
                w.uint32(568).int32(m.quotedType);
            if (m.botMessageSharingInfo != null && $Object.hasOwnProperty.call(m, "botMessageSharingInfo"))
                $root.proto.BotMessageSharingInfo.encode(m.botMessageSharingInfo, w.uint32(578).fork(), q + 1).ldelim();
            if (m.isSpoiler != null && $Object.hasOwnProperty.call(m, "isSpoiler"))
                w.uint32(584).bool(m.isSpoiler);
            if (m.mediaDomainInfo != null && $Object.hasOwnProperty.call(m, "mediaDomainInfo"))
                $root.proto.MediaDomainInfo.encode(m.mediaDomainInfo, w.uint32(594).fork(), q + 1).ldelim();
            if (m.partiallySelectedContent != null && $Object.hasOwnProperty.call(m, "partiallySelectedContent"))
                $root.proto.ContextInfo.PartiallySelectedContent.encode(m.partiallySelectedContent, w.uint32(602).fork(), q + 1).ldelim();
            if (m.afterReadDuration != null && $Object.hasOwnProperty.call(m, "afterReadDuration"))
                w.uint32(608).uint32(m.afterReadDuration);
            if (m.crossAppSource != null && $Object.hasOwnProperty.call(m, "crossAppSource"))
                w.uint32(616).int32(m.crossAppSource);
            if (m.businessInteractionPills != null && $Object.hasOwnProperty.call(m, "businessInteractionPills"))
                $root.proto.ContextInfo.BusinessInteractionPills.encode(m.businessInteractionPills, w.uint32(626).fork(), q + 1).ldelim();
            if (m.posterStatusId != null && $Object.hasOwnProperty.call(m, "posterStatusId"))
                w.uint32(634).string(m.posterStatusId);
            if (m.instagramThreadLink != null && $Object.hasOwnProperty.call(m, "instagramThreadLink"))
                $root.proto.ContextInfo.InstagramThreadLink.encode(m.instagramThreadLink, w.uint32(642).fork(), q + 1).ldelim();
            if (m.aiProvenance != null && $Object.hasOwnProperty.call(m, "aiProvenance"))
                $root.proto.AIProvenance.encode(m.aiProvenance, w.uint32(650).fork(), q + 1).ldelim();
            if (m.experienceIds != null && m.experienceIds.length) {
                w.uint32(658).uint32s(m.experienceIds);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ContextInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.stanzaId = r.stringVerify();
                        m._stanzaId = "stanzaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.participant = r.stringVerify();
                        m._participant = "participant";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.quotedMessage = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.quotedMessage);
                        m._quotedMessage = "quotedMessage";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.remoteJid = r.stringVerify();
                        m._remoteJid = "remoteJid";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        if (!(m.mentionedJid && m.mentionedJid.length))
                            m.mentionedJid = [];
                        m.mentionedJid.push(r.stringVerify());
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        m.conversionSource = r.stringVerify();
                        m._conversionSource = "conversionSource";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.conversionData = r.bytes();
                        m._conversionData = "conversionData";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        m.conversionDelaySeconds = r.uint32();
                        m._conversionDelaySeconds = "conversionDelaySeconds";
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        m.forwardingScore = r.uint32();
                        m._forwardingScore = "forwardingScore";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        m.isForwarded = r.bool();
                        m._isForwarded = "isForwarded";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        m.quotedAd = $root.proto.ContextInfo.AdReplyInfo.decode(r, r.uint32(), $undefined, q + 1, m.quotedAd);
                        m._quotedAd = "quotedAd";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        m.placeholderKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.placeholderKey);
                        m._placeholderKey = "placeholderKey";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        m.expiration = r.uint32();
                        m._expiration = "expiration";
                        continue;
                    }
                case 26: {
                        if (u !== 0)
                            break;
                        m.ephemeralSettingTimestamp = r.int64();
                        m._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        m.ephemeralSharedSecret = r.bytes();
                        m._ephemeralSharedSecret = "ephemeralSharedSecret";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        m.externalAdReply = $root.proto.ContextInfo.ExternalAdReplyInfo.decode(r, r.uint32(), $undefined, q + 1, m.externalAdReply);
                        m._externalAdReply = "externalAdReply";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        m.entryPointConversionSource = r.stringVerify();
                        m._entryPointConversionSource = "entryPointConversionSource";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        m.entryPointConversionApp = r.stringVerify();
                        m._entryPointConversionApp = "entryPointConversionApp";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        m.entryPointConversionDelaySeconds = r.uint32();
                        m._entryPointConversionDelaySeconds = "entryPointConversionDelaySeconds";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        m.disappearingMode = $root.proto.DisappearingMode.decode(r, r.uint32(), $undefined, q + 1, m.disappearingMode);
                        m._disappearingMode = "disappearingMode";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        m.actionLink = $root.proto.ActionLink.decode(r, r.uint32(), $undefined, q + 1, m.actionLink);
                        m._actionLink = "actionLink";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        m.groupSubject = r.stringVerify();
                        m._groupSubject = "groupSubject";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        m.parentGroupJid = r.stringVerify();
                        m._parentGroupJid = "parentGroupJid";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        m.trustBannerType = r.stringVerify();
                        m._trustBannerType = "trustBannerType";
                        continue;
                    }
                case 38: {
                        if (u !== 0)
                            break;
                        m.trustBannerAction = r.uint32();
                        m._trustBannerAction = "trustBannerAction";
                        continue;
                    }
                case 39: {
                        if (u !== 0)
                            break;
                        m.isSampled = r.bool();
                        m._isSampled = "isSampled";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        if (!(m.groupMentions && m.groupMentions.length))
                            m.groupMentions = [];
                        m.groupMentions.push($root.proto.GroupMention.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        m.utm = $root.proto.ContextInfo.UTMInfo.decode(r, r.uint32(), $undefined, q + 1, m.utm);
                        m._utm = "utm";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.forwardedNewsletterMessageInfo = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.decode(r, r.uint32(), $undefined, q + 1, m.forwardedNewsletterMessageInfo);
                        m._forwardedNewsletterMessageInfo = "forwardedNewsletterMessageInfo";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        m.businessMessageForwardInfo = $root.proto.ContextInfo.BusinessMessageForwardInfo.decode(r, r.uint32(), $undefined, q + 1, m.businessMessageForwardInfo);
                        m._businessMessageForwardInfo = "businessMessageForwardInfo";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        m.smbClientCampaignId = r.stringVerify();
                        m._smbClientCampaignId = "smbClientCampaignId";
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        m.smbServerCampaignId = r.stringVerify();
                        m._smbServerCampaignId = "smbServerCampaignId";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        m.dataSharingContext = $root.proto.ContextInfo.DataSharingContext.decode(r, r.uint32(), $undefined, q + 1, m.dataSharingContext);
                        m._dataSharingContext = "dataSharingContext";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        m.alwaysShowAdAttribution = r.bool();
                        m._alwaysShowAdAttribution = "alwaysShowAdAttribution";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        m.featureEligibilities = $root.proto.ContextInfo.FeatureEligibilities.decode(r, r.uint32(), $undefined, q + 1, m.featureEligibilities);
                        m._featureEligibilities = "featureEligibilities";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        m.entryPointConversionExternalSource = r.stringVerify();
                        m._entryPointConversionExternalSource = "entryPointConversionExternalSource";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        m.entryPointConversionExternalMedium = r.stringVerify();
                        m._entryPointConversionExternalMedium = "entryPointConversionExternalMedium";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        m.ctwaSignals = r.stringVerify();
                        m._ctwaSignals = "ctwaSignals";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        m.ctwaPayload = r.bytes();
                        m._ctwaPayload = "ctwaPayload";
                        continue;
                    }
                case 56: {
                        if (u !== 2)
                            break;
                        m.forwardedAiBotMessageInfo = $root.proto.ForwardedAIBotMessageInfo.decode(r, r.uint32(), $undefined, q + 1, m.forwardedAiBotMessageInfo);
                        m._forwardedAiBotMessageInfo = "forwardedAiBotMessageInfo";
                        continue;
                    }
                case 57: {
                        if (u !== 0)
                            break;
                        m.statusAttributionType = r.int32();
                        m._statusAttributionType = "statusAttributionType";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        m.urlTrackingMap = $root.proto.UrlTrackingMap.decode(r, r.uint32(), $undefined, q + 1, m.urlTrackingMap);
                        m._urlTrackingMap = "urlTrackingMap";
                        continue;
                    }
                case 59: {
                        if (u !== 0)
                            break;
                        m.pairedMediaType = r.int32();
                        m._pairedMediaType = "pairedMediaType";
                        continue;
                    }
                case 60: {
                        if (u !== 0)
                            break;
                        m.rankingVersion = r.uint32();
                        m._rankingVersion = "rankingVersion";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        m.memberLabel = $root.proto.MemberLabel.decode(r, r.uint32(), $undefined, q + 1, m.memberLabel);
                        m._memberLabel = "memberLabel";
                        continue;
                    }
                case 63: {
                        if (u !== 0)
                            break;
                        m.isQuestion = r.bool();
                        m._isQuestion = "isQuestion";
                        continue;
                    }
                case 64: {
                        if (u !== 0)
                            break;
                        m.statusSourceType = r.int32();
                        m._statusSourceType = "statusSourceType";
                        continue;
                    }
                case 65: {
                        if (u !== 2)
                            break;
                        if (!(m.statusAttributions && m.statusAttributions.length))
                            m.statusAttributions = [];
                        m.statusAttributions.push($root.proto.StatusAttribution.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 66: {
                        if (u !== 0)
                            break;
                        m.isGroupStatus = r.bool();
                        m._isGroupStatus = "isGroupStatus";
                        continue;
                    }
                case 67: {
                        if (u !== 0)
                            break;
                        m.forwardOrigin = r.int32();
                        m._forwardOrigin = "forwardOrigin";
                        continue;
                    }
                case 68: {
                        if (u !== 2)
                            break;
                        m.questionReplyQuotedMessage = $root.proto.ContextInfo.QuestionReplyQuotedMessage.decode(r, r.uint32(), $undefined, q + 1, m.questionReplyQuotedMessage);
                        m._questionReplyQuotedMessage = "questionReplyQuotedMessage";
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        m.statusAudienceMetadata = $root.proto.ContextInfo.StatusAudienceMetadata.decode(r, r.uint32(), $undefined, q + 1, m.statusAudienceMetadata);
                        m._statusAudienceMetadata = "statusAudienceMetadata";
                        continue;
                    }
                case 70: {
                        if (u !== 0)
                            break;
                        m.nonJidMentions = r.uint32();
                        m._nonJidMentions = "nonJidMentions";
                        continue;
                    }
                case 71: {
                        if (u !== 0)
                            break;
                        m.quotedType = r.int32();
                        m._quotedType = "quotedType";
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        m.botMessageSharingInfo = $root.proto.BotMessageSharingInfo.decode(r, r.uint32(), $undefined, q + 1, m.botMessageSharingInfo);
                        m._botMessageSharingInfo = "botMessageSharingInfo";
                        continue;
                    }
                case 73: {
                        if (u !== 0)
                            break;
                        m.isSpoiler = r.bool();
                        m._isSpoiler = "isSpoiler";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        m.mediaDomainInfo = $root.proto.MediaDomainInfo.decode(r, r.uint32(), $undefined, q + 1, m.mediaDomainInfo);
                        m._mediaDomainInfo = "mediaDomainInfo";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        m.partiallySelectedContent = $root.proto.ContextInfo.PartiallySelectedContent.decode(r, r.uint32(), $undefined, q + 1, m.partiallySelectedContent);
                        m._partiallySelectedContent = "partiallySelectedContent";
                        continue;
                    }
                case 76: {
                        if (u !== 0)
                            break;
                        m.afterReadDuration = r.uint32();
                        m._afterReadDuration = "afterReadDuration";
                        continue;
                    }
                case 77: {
                        if (u !== 0)
                            break;
                        m.crossAppSource = r.int32();
                        m._crossAppSource = "crossAppSource";
                        continue;
                    }
                case 78: {
                        if (u !== 2)
                            break;
                        m.businessInteractionPills = $root.proto.ContextInfo.BusinessInteractionPills.decode(r, r.uint32(), $undefined, q + 1, m.businessInteractionPills);
                        m._businessInteractionPills = "businessInteractionPills";
                        continue;
                    }
                case 79: {
                        if (u !== 2)
                            break;
                        m.posterStatusId = r.stringVerify();
                        m._posterStatusId = "posterStatusId";
                        continue;
                    }
                case 80: {
                        if (u !== 2)
                            break;
                        m.instagramThreadLink = $root.proto.ContextInfo.InstagramThreadLink.decode(r, r.uint32(), $undefined, q + 1, m.instagramThreadLink);
                        m._instagramThreadLink = "instagramThreadLink";
                        continue;
                    }
                case 81: {
                        if (u !== 2)
                            break;
                        m.aiProvenance = $root.proto.AIProvenance.decode(r, r.uint32(), $undefined, q + 1, m.aiProvenance);
                        m._aiProvenance = "aiProvenance";
                        continue;
                    }
                case 82: {
                        if (u === 2) {
                            if (!(m.experienceIds && m.experienceIds.length))
                                m.experienceIds = [];
                            r.uint32s(m.experienceIds);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.experienceIds && m.experienceIds.length))
                            m.experienceIds = [];
                        m.experienceIds.push(r.uint32());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ContextInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.ContextInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ContextInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ContextInfo();
            if (d.stanzaId != null) {
                m.stanzaId = $String(d.stanzaId);
            }
            if (d.participant != null) {
                m.participant = $String(d.participant);
            }
            if (d.quotedMessage != null) {
                if (!$util.isObject(d.quotedMessage))
                    throw $TypeError(".proto.ContextInfo.quotedMessage: object expected");
                m.quotedMessage = $root.proto.Message.fromObject(d.quotedMessage, q + 1);
            }
            if (d.remoteJid != null) {
                m.remoteJid = $String(d.remoteJid);
            }
            if (d.mentionedJid) {
                if (!$Array.isArray(d.mentionedJid))
                    throw $TypeError(".proto.ContextInfo.mentionedJid: array expected");
                m.mentionedJid = $Array(d.mentionedJid.length);
                for (var i = 0; i < d.mentionedJid.length; ++i) {
                    m.mentionedJid[i] = $String(d.mentionedJid[i]);
                }
            }
            if (d.conversionSource != null) {
                m.conversionSource = $String(d.conversionSource);
            }
            if (d.conversionData != null) {
                if (typeof d.conversionData === "string")
                    $util.base64.decode(d.conversionData, m.conversionData = $util.newBuffer($util.base64.length(d.conversionData)), 0);
                else if (d.conversionData.length >= 0)
                    m.conversionData = d.conversionData;
            }
            if (d.conversionDelaySeconds != null) {
                m.conversionDelaySeconds = d.conversionDelaySeconds >>> 0;
            }
            if (d.forwardingScore != null) {
                m.forwardingScore = d.forwardingScore >>> 0;
            }
            if (d.isForwarded != null) {
                m.isForwarded = $Boolean(d.isForwarded);
            }
            if (d.quotedAd != null) {
                if (!$util.isObject(d.quotedAd))
                    throw $TypeError(".proto.ContextInfo.quotedAd: object expected");
                m.quotedAd = $root.proto.ContextInfo.AdReplyInfo.fromObject(d.quotedAd, q + 1);
            }
            if (d.placeholderKey != null) {
                if (!$util.isObject(d.placeholderKey))
                    throw $TypeError(".proto.ContextInfo.placeholderKey: object expected");
                m.placeholderKey = $root.proto.MessageKey.fromObject(d.placeholderKey, q + 1);
            }
            if (d.expiration != null) {
                m.expiration = d.expiration >>> 0;
            }
            if (d.ephemeralSettingTimestamp != null) {
                if ($util.Long)
                    m.ephemeralSettingTimestamp = $util.Long.fromValue(d.ephemeralSettingTimestamp, false);
                else if (typeof d.ephemeralSettingTimestamp === "string")
                    m.ephemeralSettingTimestamp = $parseInt(d.ephemeralSettingTimestamp, 10);
                else if (typeof d.ephemeralSettingTimestamp === "number")
                    m.ephemeralSettingTimestamp = d.ephemeralSettingTimestamp;
                else if (typeof d.ephemeralSettingTimestamp === "object")
                    m.ephemeralSettingTimestamp = new $util.LongBits(d.ephemeralSettingTimestamp.low >>> 0, d.ephemeralSettingTimestamp.high >>> 0).toNumber();
            }
            if (d.ephemeralSharedSecret != null) {
                if (typeof d.ephemeralSharedSecret === "string")
                    $util.base64.decode(d.ephemeralSharedSecret, m.ephemeralSharedSecret = $util.newBuffer($util.base64.length(d.ephemeralSharedSecret)), 0);
                else if (d.ephemeralSharedSecret.length >= 0)
                    m.ephemeralSharedSecret = d.ephemeralSharedSecret;
            }
            if (d.externalAdReply != null) {
                if (!$util.isObject(d.externalAdReply))
                    throw $TypeError(".proto.ContextInfo.externalAdReply: object expected");
                m.externalAdReply = $root.proto.ContextInfo.ExternalAdReplyInfo.fromObject(d.externalAdReply, q + 1);
            }
            if (d.entryPointConversionSource != null) {
                m.entryPointConversionSource = $String(d.entryPointConversionSource);
            }
            if (d.entryPointConversionApp != null) {
                m.entryPointConversionApp = $String(d.entryPointConversionApp);
            }
            if (d.entryPointConversionDelaySeconds != null) {
                m.entryPointConversionDelaySeconds = d.entryPointConversionDelaySeconds >>> 0;
            }
            if (d.disappearingMode != null) {
                if (!$util.isObject(d.disappearingMode))
                    throw $TypeError(".proto.ContextInfo.disappearingMode: object expected");
                m.disappearingMode = $root.proto.DisappearingMode.fromObject(d.disappearingMode, q + 1);
            }
            if (d.actionLink != null) {
                if (!$util.isObject(d.actionLink))
                    throw $TypeError(".proto.ContextInfo.actionLink: object expected");
                m.actionLink = $root.proto.ActionLink.fromObject(d.actionLink, q + 1);
            }
            if (d.groupSubject != null) {
                m.groupSubject = $String(d.groupSubject);
            }
            if (d.parentGroupJid != null) {
                m.parentGroupJid = $String(d.parentGroupJid);
            }
            if (d.trustBannerType != null) {
                m.trustBannerType = $String(d.trustBannerType);
            }
            if (d.trustBannerAction != null) {
                m.trustBannerAction = d.trustBannerAction >>> 0;
            }
            if (d.isSampled != null) {
                m.isSampled = $Boolean(d.isSampled);
            }
            if (d.groupMentions) {
                if (!$Array.isArray(d.groupMentions))
                    throw $TypeError(".proto.ContextInfo.groupMentions: array expected");
                m.groupMentions = $Array(d.groupMentions.length);
                for (var i = 0; i < d.groupMentions.length; ++i) {
                    if (!$util.isObject(d.groupMentions[i]))
                        throw $TypeError(".proto.ContextInfo.groupMentions: object expected");
                    m.groupMentions[i] = $root.proto.GroupMention.fromObject(d.groupMentions[i], q + 1);
                }
            }
            if (d.utm != null) {
                if (!$util.isObject(d.utm))
                    throw $TypeError(".proto.ContextInfo.utm: object expected");
                m.utm = $root.proto.ContextInfo.UTMInfo.fromObject(d.utm, q + 1);
            }
            if (d.forwardedNewsletterMessageInfo != null) {
                if (!$util.isObject(d.forwardedNewsletterMessageInfo))
                    throw $TypeError(".proto.ContextInfo.forwardedNewsletterMessageInfo: object expected");
                m.forwardedNewsletterMessageInfo = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.fromObject(d.forwardedNewsletterMessageInfo, q + 1);
            }
            if (d.businessMessageForwardInfo != null) {
                if (!$util.isObject(d.businessMessageForwardInfo))
                    throw $TypeError(".proto.ContextInfo.businessMessageForwardInfo: object expected");
                m.businessMessageForwardInfo = $root.proto.ContextInfo.BusinessMessageForwardInfo.fromObject(d.businessMessageForwardInfo, q + 1);
            }
            if (d.smbClientCampaignId != null) {
                m.smbClientCampaignId = $String(d.smbClientCampaignId);
            }
            if (d.smbServerCampaignId != null) {
                m.smbServerCampaignId = $String(d.smbServerCampaignId);
            }
            if (d.dataSharingContext != null) {
                if (!$util.isObject(d.dataSharingContext))
                    throw $TypeError(".proto.ContextInfo.dataSharingContext: object expected");
                m.dataSharingContext = $root.proto.ContextInfo.DataSharingContext.fromObject(d.dataSharingContext, q + 1);
            }
            if (d.alwaysShowAdAttribution != null) {
                m.alwaysShowAdAttribution = $Boolean(d.alwaysShowAdAttribution);
            }
            if (d.featureEligibilities != null) {
                if (!$util.isObject(d.featureEligibilities))
                    throw $TypeError(".proto.ContextInfo.featureEligibilities: object expected");
                m.featureEligibilities = $root.proto.ContextInfo.FeatureEligibilities.fromObject(d.featureEligibilities, q + 1);
            }
            if (d.entryPointConversionExternalSource != null) {
                m.entryPointConversionExternalSource = $String(d.entryPointConversionExternalSource);
            }
            if (d.entryPointConversionExternalMedium != null) {
                m.entryPointConversionExternalMedium = $String(d.entryPointConversionExternalMedium);
            }
            if (d.ctwaSignals != null) {
                m.ctwaSignals = $String(d.ctwaSignals);
            }
            if (d.ctwaPayload != null) {
                if (typeof d.ctwaPayload === "string")
                    $util.base64.decode(d.ctwaPayload, m.ctwaPayload = $util.newBuffer($util.base64.length(d.ctwaPayload)), 0);
                else if (d.ctwaPayload.length >= 0)
                    m.ctwaPayload = d.ctwaPayload;
            }
            if (d.forwardedAiBotMessageInfo != null) {
                if (!$util.isObject(d.forwardedAiBotMessageInfo))
                    throw $TypeError(".proto.ContextInfo.forwardedAiBotMessageInfo: object expected");
                m.forwardedAiBotMessageInfo = $root.proto.ForwardedAIBotMessageInfo.fromObject(d.forwardedAiBotMessageInfo, q + 1);
            }
            switch (d.statusAttributionType) {
            case "NONE":
            case 0:
                m.statusAttributionType = 0;
                break;
            case "RESHARED_FROM_MENTION":
            case 1:
                m.statusAttributionType = 1;
                break;
            case "RESHARED_FROM_POST":
            case 2:
                m.statusAttributionType = 2;
                break;
            case "RESHARED_FROM_POST_MANY_TIMES":
            case 3:
                m.statusAttributionType = 3;
                break;
            case "FORWARDED_FROM_STATUS":
            case 4:
                m.statusAttributionType = 4;
                break;
            default:
                if (typeof d.statusAttributionType === "number" && (d.statusAttributionType | 0) === d.statusAttributionType)
                    m.statusAttributionType = d.statusAttributionType;
            }
            if (d.urlTrackingMap != null) {
                if (!$util.isObject(d.urlTrackingMap))
                    throw $TypeError(".proto.ContextInfo.urlTrackingMap: object expected");
                m.urlTrackingMap = $root.proto.UrlTrackingMap.fromObject(d.urlTrackingMap, q + 1);
            }
            switch (d.pairedMediaType) {
            case "NOT_PAIRED_MEDIA":
            case 0:
                m.pairedMediaType = 0;
                break;
            case "SD_VIDEO_PARENT":
            case 1:
                m.pairedMediaType = 1;
                break;
            case "HD_VIDEO_CHILD":
            case 2:
                m.pairedMediaType = 2;
                break;
            case "SD_IMAGE_PARENT":
            case 3:
                m.pairedMediaType = 3;
                break;
            case "HD_IMAGE_CHILD":
            case 4:
                m.pairedMediaType = 4;
                break;
            case "MOTION_PHOTO_PARENT":
            case 5:
                m.pairedMediaType = 5;
                break;
            case "MOTION_PHOTO_CHILD":
            case 6:
                m.pairedMediaType = 6;
                break;
            case "HEVC_VIDEO_PARENT":
            case 7:
                m.pairedMediaType = 7;
                break;
            case "HEVC_VIDEO_CHILD":
            case 8:
                m.pairedMediaType = 8;
                break;
            default:
                if (typeof d.pairedMediaType === "number" && (d.pairedMediaType | 0) === d.pairedMediaType)
                    m.pairedMediaType = d.pairedMediaType;
            }
            if (d.rankingVersion != null) {
                m.rankingVersion = d.rankingVersion >>> 0;
            }
            if (d.memberLabel != null) {
                if (!$util.isObject(d.memberLabel))
                    throw $TypeError(".proto.ContextInfo.memberLabel: object expected");
                m.memberLabel = $root.proto.MemberLabel.fromObject(d.memberLabel, q + 1);
            }
            if (d.isQuestion != null) {
                m.isQuestion = $Boolean(d.isQuestion);
            }
            switch (d.statusSourceType) {
            case "IMAGE":
            case 0:
                m.statusSourceType = 0;
                break;
            case "VIDEO":
            case 1:
                m.statusSourceType = 1;
                break;
            case "GIF":
            case 2:
                m.statusSourceType = 2;
                break;
            case "AUDIO":
            case 3:
                m.statusSourceType = 3;
                break;
            case "TEXT":
            case 4:
                m.statusSourceType = 4;
                break;
            case "MUSIC_STANDALONE":
            case 5:
                m.statusSourceType = 5;
                break;
            default:
                if (typeof d.statusSourceType === "number" && (d.statusSourceType | 0) === d.statusSourceType)
                    m.statusSourceType = d.statusSourceType;
            }
            if (d.statusAttributions) {
                if (!$Array.isArray(d.statusAttributions))
                    throw $TypeError(".proto.ContextInfo.statusAttributions: array expected");
                m.statusAttributions = $Array(d.statusAttributions.length);
                for (var i = 0; i < d.statusAttributions.length; ++i) {
                    if (!$util.isObject(d.statusAttributions[i]))
                        throw $TypeError(".proto.ContextInfo.statusAttributions: object expected");
                    m.statusAttributions[i] = $root.proto.StatusAttribution.fromObject(d.statusAttributions[i], q + 1);
                }
            }
            if (d.isGroupStatus != null) {
                m.isGroupStatus = $Boolean(d.isGroupStatus);
            }
            switch (d.forwardOrigin) {
            case "UNKNOWN":
            case 0:
                m.forwardOrigin = 0;
                break;
            case "CHAT":
            case 1:
                m.forwardOrigin = 1;
                break;
            case "STATUS":
            case 2:
                m.forwardOrigin = 2;
                break;
            case "CHANNELS":
            case 3:
                m.forwardOrigin = 3;
                break;
            case "META_AI":
            case 4:
                m.forwardOrigin = 4;
                break;
            case "UGC":
            case 5:
                m.forwardOrigin = 5;
                break;
            default:
                if (typeof d.forwardOrigin === "number" && (d.forwardOrigin | 0) === d.forwardOrigin)
                    m.forwardOrigin = d.forwardOrigin;
            }
            if (d.questionReplyQuotedMessage != null) {
                if (!$util.isObject(d.questionReplyQuotedMessage))
                    throw $TypeError(".proto.ContextInfo.questionReplyQuotedMessage: object expected");
                m.questionReplyQuotedMessage = $root.proto.ContextInfo.QuestionReplyQuotedMessage.fromObject(d.questionReplyQuotedMessage, q + 1);
            }
            if (d.statusAudienceMetadata != null) {
                if (!$util.isObject(d.statusAudienceMetadata))
                    throw $TypeError(".proto.ContextInfo.statusAudienceMetadata: object expected");
                m.statusAudienceMetadata = $root.proto.ContextInfo.StatusAudienceMetadata.fromObject(d.statusAudienceMetadata, q + 1);
            }
            if (d.nonJidMentions != null) {
                m.nonJidMentions = d.nonJidMentions >>> 0;
            }
            switch (d.quotedType) {
            case "EXPLICIT":
            case 0:
                m.quotedType = 0;
                break;
            case "AUTO":
            case 1:
                m.quotedType = 1;
                break;
            default:
                if (typeof d.quotedType === "number" && (d.quotedType | 0) === d.quotedType)
                    m.quotedType = d.quotedType;
            }
            if (d.botMessageSharingInfo != null) {
                if (!$util.isObject(d.botMessageSharingInfo))
                    throw $TypeError(".proto.ContextInfo.botMessageSharingInfo: object expected");
                m.botMessageSharingInfo = $root.proto.BotMessageSharingInfo.fromObject(d.botMessageSharingInfo, q + 1);
            }
            if (d.isSpoiler != null) {
                m.isSpoiler = $Boolean(d.isSpoiler);
            }
            if (d.mediaDomainInfo != null) {
                if (!$util.isObject(d.mediaDomainInfo))
                    throw $TypeError(".proto.ContextInfo.mediaDomainInfo: object expected");
                m.mediaDomainInfo = $root.proto.MediaDomainInfo.fromObject(d.mediaDomainInfo, q + 1);
            }
            if (d.partiallySelectedContent != null) {
                if (!$util.isObject(d.partiallySelectedContent))
                    throw $TypeError(".proto.ContextInfo.partiallySelectedContent: object expected");
                m.partiallySelectedContent = $root.proto.ContextInfo.PartiallySelectedContent.fromObject(d.partiallySelectedContent, q + 1);
            }
            if (d.afterReadDuration != null) {
                m.afterReadDuration = d.afterReadDuration >>> 0;
            }
            switch (d.crossAppSource) {
            case "CROSS_APP_SOURCE_UNKNOWN":
            case 0:
                m.crossAppSource = 0;
                break;
            case "CROSS_APP_SOURCE_INSTAGRAM":
            case 1:
                m.crossAppSource = 1;
                break;
            case "CROSS_APP_SOURCE_FACEBOOK":
            case 2:
                m.crossAppSource = 2;
                break;
            default:
                if (typeof d.crossAppSource === "number" && (d.crossAppSource | 0) === d.crossAppSource)
                    m.crossAppSource = d.crossAppSource;
            }
            if (d.businessInteractionPills != null) {
                if (!$util.isObject(d.businessInteractionPills))
                    throw $TypeError(".proto.ContextInfo.businessInteractionPills: object expected");
                m.businessInteractionPills = $root.proto.ContextInfo.BusinessInteractionPills.fromObject(d.businessInteractionPills, q + 1);
            }
            if (d.posterStatusId != null) {
                m.posterStatusId = $String(d.posterStatusId);
            }
            if (d.instagramThreadLink != null) {
                if (!$util.isObject(d.instagramThreadLink))
                    throw $TypeError(".proto.ContextInfo.instagramThreadLink: object expected");
                m.instagramThreadLink = $root.proto.ContextInfo.InstagramThreadLink.fromObject(d.instagramThreadLink, q + 1);
            }
            if (d.aiProvenance != null) {
                if (!$util.isObject(d.aiProvenance))
                    throw $TypeError(".proto.ContextInfo.aiProvenance: object expected");
                m.aiProvenance = $root.proto.AIProvenance.fromObject(d.aiProvenance, q + 1);
            }
            if (d.experienceIds) {
                if (!$Array.isArray(d.experienceIds))
                    throw $TypeError(".proto.ContextInfo.experienceIds: array expected");
                m.experienceIds = $Array(d.experienceIds.length);
                for (var i = 0; i < d.experienceIds.length; ++i) {
                    m.experienceIds[i] = d.experienceIds[i] >>> 0;
                }
            }
            return m;
        };

        ContextInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mentionedJid = [];
                d.groupMentions = [];
                d.statusAttributions = [];
                d.experienceIds = [];
            }
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                d.stanzaId = m.stanzaId;
            }
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant")) {
                d.participant = m.participant;
            }
            if (m.quotedMessage != null && $Object.hasOwnProperty.call(m, "quotedMessage")) {
                d.quotedMessage = $root.proto.Message.toObject(m.quotedMessage, o, q + 1);
            }
            if (m.remoteJid != null && $Object.hasOwnProperty.call(m, "remoteJid")) {
                d.remoteJid = m.remoteJid;
            }
            if (m.mentionedJid && m.mentionedJid.length) {
                d.mentionedJid = $Array(m.mentionedJid.length);
                for (var j = 0; j < m.mentionedJid.length; ++j) {
                    d.mentionedJid[j] = m.mentionedJid[j];
                }
            }
            if (m.conversionSource != null && $Object.hasOwnProperty.call(m, "conversionSource")) {
                d.conversionSource = m.conversionSource;
            }
            if (m.conversionData != null && $Object.hasOwnProperty.call(m, "conversionData")) {
                d.conversionData = o.bytes === $String ? $util.base64.encode(m.conversionData, 0, m.conversionData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.conversionData) : m.conversionData;
            }
            if (m.conversionDelaySeconds != null && $Object.hasOwnProperty.call(m, "conversionDelaySeconds")) {
                d.conversionDelaySeconds = m.conversionDelaySeconds;
            }
            if (m.forwardingScore != null && $Object.hasOwnProperty.call(m, "forwardingScore")) {
                d.forwardingScore = m.forwardingScore;
            }
            if (m.isForwarded != null && $Object.hasOwnProperty.call(m, "isForwarded")) {
                d.isForwarded = m.isForwarded;
            }
            if (m.quotedAd != null && $Object.hasOwnProperty.call(m, "quotedAd")) {
                d.quotedAd = $root.proto.ContextInfo.AdReplyInfo.toObject(m.quotedAd, o, q + 1);
            }
            if (m.placeholderKey != null && $Object.hasOwnProperty.call(m, "placeholderKey")) {
                d.placeholderKey = $root.proto.MessageKey.toObject(m.placeholderKey, o, q + 1);
            }
            if (m.expiration != null && $Object.hasOwnProperty.call(m, "expiration")) {
                d.expiration = m.expiration;
            }
            if (m.ephemeralSettingTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.ephemeralSettingTimestamp = typeof m.ephemeralSettingTimestamp === "number" ? $BigInt(m.ephemeralSettingTimestamp) : $util.Long.fromBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.ephemeralSettingTimestamp === "number")
                    d.ephemeralSettingTimestamp = o.longs === $String ? $String(m.ephemeralSettingTimestamp) : m.ephemeralSettingTimestamp;
                else
                    d.ephemeralSettingTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.ephemeralSettingTimestamp) : o.longs === $Number ? new $util.LongBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0).toNumber() : m.ephemeralSettingTimestamp;
            }
            if (m.ephemeralSharedSecret != null && $Object.hasOwnProperty.call(m, "ephemeralSharedSecret")) {
                d.ephemeralSharedSecret = o.bytes === $String ? $util.base64.encode(m.ephemeralSharedSecret, 0, m.ephemeralSharedSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ephemeralSharedSecret) : m.ephemeralSharedSecret;
            }
            if (m.externalAdReply != null && $Object.hasOwnProperty.call(m, "externalAdReply")) {
                d.externalAdReply = $root.proto.ContextInfo.ExternalAdReplyInfo.toObject(m.externalAdReply, o, q + 1);
            }
            if (m.entryPointConversionSource != null && $Object.hasOwnProperty.call(m, "entryPointConversionSource")) {
                d.entryPointConversionSource = m.entryPointConversionSource;
            }
            if (m.entryPointConversionApp != null && $Object.hasOwnProperty.call(m, "entryPointConversionApp")) {
                d.entryPointConversionApp = m.entryPointConversionApp;
            }
            if (m.entryPointConversionDelaySeconds != null && $Object.hasOwnProperty.call(m, "entryPointConversionDelaySeconds")) {
                d.entryPointConversionDelaySeconds = m.entryPointConversionDelaySeconds;
            }
            if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode")) {
                d.disappearingMode = $root.proto.DisappearingMode.toObject(m.disappearingMode, o, q + 1);
            }
            if (m.actionLink != null && $Object.hasOwnProperty.call(m, "actionLink")) {
                d.actionLink = $root.proto.ActionLink.toObject(m.actionLink, o, q + 1);
            }
            if (m.groupSubject != null && $Object.hasOwnProperty.call(m, "groupSubject")) {
                d.groupSubject = m.groupSubject;
            }
            if (m.parentGroupJid != null && $Object.hasOwnProperty.call(m, "parentGroupJid")) {
                d.parentGroupJid = m.parentGroupJid;
            }
            if (m.trustBannerType != null && $Object.hasOwnProperty.call(m, "trustBannerType")) {
                d.trustBannerType = m.trustBannerType;
            }
            if (m.trustBannerAction != null && $Object.hasOwnProperty.call(m, "trustBannerAction")) {
                d.trustBannerAction = m.trustBannerAction;
            }
            if (m.isSampled != null && $Object.hasOwnProperty.call(m, "isSampled")) {
                d.isSampled = m.isSampled;
            }
            if (m.groupMentions && m.groupMentions.length) {
                d.groupMentions = $Array(m.groupMentions.length);
                for (var j = 0; j < m.groupMentions.length; ++j) {
                    d.groupMentions[j] = $root.proto.GroupMention.toObject(m.groupMentions[j], o, q + 1);
                }
            }
            if (m.utm != null && $Object.hasOwnProperty.call(m, "utm")) {
                d.utm = $root.proto.ContextInfo.UTMInfo.toObject(m.utm, o, q + 1);
            }
            if (m.forwardedNewsletterMessageInfo != null && $Object.hasOwnProperty.call(m, "forwardedNewsletterMessageInfo")) {
                d.forwardedNewsletterMessageInfo = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.toObject(m.forwardedNewsletterMessageInfo, o, q + 1);
            }
            if (m.businessMessageForwardInfo != null && $Object.hasOwnProperty.call(m, "businessMessageForwardInfo")) {
                d.businessMessageForwardInfo = $root.proto.ContextInfo.BusinessMessageForwardInfo.toObject(m.businessMessageForwardInfo, o, q + 1);
            }
            if (m.smbClientCampaignId != null && $Object.hasOwnProperty.call(m, "smbClientCampaignId")) {
                d.smbClientCampaignId = m.smbClientCampaignId;
            }
            if (m.smbServerCampaignId != null && $Object.hasOwnProperty.call(m, "smbServerCampaignId")) {
                d.smbServerCampaignId = m.smbServerCampaignId;
            }
            if (m.dataSharingContext != null && $Object.hasOwnProperty.call(m, "dataSharingContext")) {
                d.dataSharingContext = $root.proto.ContextInfo.DataSharingContext.toObject(m.dataSharingContext, o, q + 1);
            }
            if (m.alwaysShowAdAttribution != null && $Object.hasOwnProperty.call(m, "alwaysShowAdAttribution")) {
                d.alwaysShowAdAttribution = m.alwaysShowAdAttribution;
            }
            if (m.featureEligibilities != null && $Object.hasOwnProperty.call(m, "featureEligibilities")) {
                d.featureEligibilities = $root.proto.ContextInfo.FeatureEligibilities.toObject(m.featureEligibilities, o, q + 1);
            }
            if (m.entryPointConversionExternalSource != null && $Object.hasOwnProperty.call(m, "entryPointConversionExternalSource")) {
                d.entryPointConversionExternalSource = m.entryPointConversionExternalSource;
            }
            if (m.entryPointConversionExternalMedium != null && $Object.hasOwnProperty.call(m, "entryPointConversionExternalMedium")) {
                d.entryPointConversionExternalMedium = m.entryPointConversionExternalMedium;
            }
            if (m.ctwaSignals != null && $Object.hasOwnProperty.call(m, "ctwaSignals")) {
                d.ctwaSignals = m.ctwaSignals;
            }
            if (m.ctwaPayload != null && $Object.hasOwnProperty.call(m, "ctwaPayload")) {
                d.ctwaPayload = o.bytes === $String ? $util.base64.encode(m.ctwaPayload, 0, m.ctwaPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ctwaPayload) : m.ctwaPayload;
            }
            if (m.forwardedAiBotMessageInfo != null && $Object.hasOwnProperty.call(m, "forwardedAiBotMessageInfo")) {
                d.forwardedAiBotMessageInfo = $root.proto.ForwardedAIBotMessageInfo.toObject(m.forwardedAiBotMessageInfo, o, q + 1);
            }
            if (m.statusAttributionType != null && $Object.hasOwnProperty.call(m, "statusAttributionType")) {
                d.statusAttributionType = o.enums === $String ? $root.proto.ContextInfo.StatusAttributionType[m.statusAttributionType] === $undefined ? m.statusAttributionType : $root.proto.ContextInfo.StatusAttributionType[m.statusAttributionType] : m.statusAttributionType;
            }
            if (m.urlTrackingMap != null && $Object.hasOwnProperty.call(m, "urlTrackingMap")) {
                d.urlTrackingMap = $root.proto.UrlTrackingMap.toObject(m.urlTrackingMap, o, q + 1);
            }
            if (m.pairedMediaType != null && $Object.hasOwnProperty.call(m, "pairedMediaType")) {
                d.pairedMediaType = o.enums === $String ? $root.proto.ContextInfo.PairedMediaType[m.pairedMediaType] === $undefined ? m.pairedMediaType : $root.proto.ContextInfo.PairedMediaType[m.pairedMediaType] : m.pairedMediaType;
            }
            if (m.rankingVersion != null && $Object.hasOwnProperty.call(m, "rankingVersion")) {
                d.rankingVersion = m.rankingVersion;
            }
            if (m.memberLabel != null && $Object.hasOwnProperty.call(m, "memberLabel")) {
                d.memberLabel = $root.proto.MemberLabel.toObject(m.memberLabel, o, q + 1);
            }
            if (m.isQuestion != null && $Object.hasOwnProperty.call(m, "isQuestion")) {
                d.isQuestion = m.isQuestion;
            }
            if (m.statusSourceType != null && $Object.hasOwnProperty.call(m, "statusSourceType")) {
                d.statusSourceType = o.enums === $String ? $root.proto.ContextInfo.StatusSourceType[m.statusSourceType] === $undefined ? m.statusSourceType : $root.proto.ContextInfo.StatusSourceType[m.statusSourceType] : m.statusSourceType;
            }
            if (m.statusAttributions && m.statusAttributions.length) {
                d.statusAttributions = $Array(m.statusAttributions.length);
                for (var j = 0; j < m.statusAttributions.length; ++j) {
                    d.statusAttributions[j] = $root.proto.StatusAttribution.toObject(m.statusAttributions[j], o, q + 1);
                }
            }
            if (m.isGroupStatus != null && $Object.hasOwnProperty.call(m, "isGroupStatus")) {
                d.isGroupStatus = m.isGroupStatus;
            }
            if (m.forwardOrigin != null && $Object.hasOwnProperty.call(m, "forwardOrigin")) {
                d.forwardOrigin = o.enums === $String ? $root.proto.ContextInfo.ForwardOrigin[m.forwardOrigin] === $undefined ? m.forwardOrigin : $root.proto.ContextInfo.ForwardOrigin[m.forwardOrigin] : m.forwardOrigin;
            }
            if (m.questionReplyQuotedMessage != null && $Object.hasOwnProperty.call(m, "questionReplyQuotedMessage")) {
                d.questionReplyQuotedMessage = $root.proto.ContextInfo.QuestionReplyQuotedMessage.toObject(m.questionReplyQuotedMessage, o, q + 1);
            }
            if (m.statusAudienceMetadata != null && $Object.hasOwnProperty.call(m, "statusAudienceMetadata")) {
                d.statusAudienceMetadata = $root.proto.ContextInfo.StatusAudienceMetadata.toObject(m.statusAudienceMetadata, o, q + 1);
            }
            if (m.nonJidMentions != null && $Object.hasOwnProperty.call(m, "nonJidMentions")) {
                d.nonJidMentions = m.nonJidMentions;
            }
            if (m.quotedType != null && $Object.hasOwnProperty.call(m, "quotedType")) {
                d.quotedType = o.enums === $String ? $root.proto.ContextInfo.QuotedType[m.quotedType] === $undefined ? m.quotedType : $root.proto.ContextInfo.QuotedType[m.quotedType] : m.quotedType;
            }
            if (m.botMessageSharingInfo != null && $Object.hasOwnProperty.call(m, "botMessageSharingInfo")) {
                d.botMessageSharingInfo = $root.proto.BotMessageSharingInfo.toObject(m.botMessageSharingInfo, o, q + 1);
            }
            if (m.isSpoiler != null && $Object.hasOwnProperty.call(m, "isSpoiler")) {
                d.isSpoiler = m.isSpoiler;
            }
            if (m.mediaDomainInfo != null && $Object.hasOwnProperty.call(m, "mediaDomainInfo")) {
                d.mediaDomainInfo = $root.proto.MediaDomainInfo.toObject(m.mediaDomainInfo, o, q + 1);
            }
            if (m.partiallySelectedContent != null && $Object.hasOwnProperty.call(m, "partiallySelectedContent")) {
                d.partiallySelectedContent = $root.proto.ContextInfo.PartiallySelectedContent.toObject(m.partiallySelectedContent, o, q + 1);
            }
            if (m.afterReadDuration != null && $Object.hasOwnProperty.call(m, "afterReadDuration")) {
                d.afterReadDuration = m.afterReadDuration;
            }
            if (m.crossAppSource != null && $Object.hasOwnProperty.call(m, "crossAppSource")) {
                d.crossAppSource = o.enums === $String ? $root.proto.ContextInfo.CrossAppSource[m.crossAppSource] === $undefined ? m.crossAppSource : $root.proto.ContextInfo.CrossAppSource[m.crossAppSource] : m.crossAppSource;
            }
            if (m.businessInteractionPills != null && $Object.hasOwnProperty.call(m, "businessInteractionPills")) {
                d.businessInteractionPills = $root.proto.ContextInfo.BusinessInteractionPills.toObject(m.businessInteractionPills, o, q + 1);
            }
            if (m.posterStatusId != null && $Object.hasOwnProperty.call(m, "posterStatusId")) {
                d.posterStatusId = m.posterStatusId;
            }
            if (m.instagramThreadLink != null && $Object.hasOwnProperty.call(m, "instagramThreadLink")) {
                d.instagramThreadLink = $root.proto.ContextInfo.InstagramThreadLink.toObject(m.instagramThreadLink, o, q + 1);
            }
            if (m.aiProvenance != null && $Object.hasOwnProperty.call(m, "aiProvenance")) {
                d.aiProvenance = $root.proto.AIProvenance.toObject(m.aiProvenance, o, q + 1);
            }
            if (m.experienceIds && m.experienceIds.length) {
                d.experienceIds = $Array(m.experienceIds.length);
                for (var j = 0; j < m.experienceIds.length; ++j) {
                    d.experienceIds[j] = m.experienceIds[j];
                }
            }
            return d;
        };

        ContextInfo.prototype.toJSON = function() {
            return ContextInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        ContextInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ContextInfo";
        };

        ContextInfo.AdReplyInfo = (function() {

            const AdReplyInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AdReplyInfo.prototype.advertiserName = null;
            AdReplyInfo.prototype.mediaType = null;
            AdReplyInfo.prototype.jpegThumbnail = null;
            AdReplyInfo.prototype.caption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AdReplyInfo.prototype, "_advertiserName", {
                get: $util.oneOfGetter($oneOfFields = ["advertiserName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AdReplyInfo.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AdReplyInfo.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AdReplyInfo.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AdReplyInfo.create = function(properties) {
                return new AdReplyInfo(properties);
            };

            AdReplyInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.advertiserName != null && $Object.hasOwnProperty.call(m, "advertiserName"))
                    w.uint32(10).string(m.advertiserName);
                if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType"))
                    w.uint32(16).int32(m.mediaType);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(138).string(m.caption);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AdReplyInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.AdReplyInfo(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.advertiserName = r.stringVerify();
                            m._advertiserName = "advertiserName";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.mediaType = r.int32();
                            m._mediaType = "mediaType";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AdReplyInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.AdReplyInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.AdReplyInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.AdReplyInfo();
                if (d.advertiserName != null) {
                    m.advertiserName = $String(d.advertiserName);
                }
                switch (d.mediaType) {
                case "NONE":
                case 0:
                    m.mediaType = 0;
                    break;
                case "IMAGE":
                case 1:
                    m.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    m.mediaType = 2;
                    break;
                default:
                    if (typeof d.mediaType === "number" && (d.mediaType | 0) === d.mediaType)
                        m.mediaType = d.mediaType;
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                return m;
            };

            AdReplyInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.advertiserName != null && $Object.hasOwnProperty.call(m, "advertiserName")) {
                    d.advertiserName = m.advertiserName;
                }
                if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType")) {
                    d.mediaType = o.enums === $String ? $root.proto.ContextInfo.AdReplyInfo.MediaType[m.mediaType] === $undefined ? m.mediaType : $root.proto.ContextInfo.AdReplyInfo.MediaType[m.mediaType] : m.mediaType;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                return d;
            };

            AdReplyInfo.prototype.toJSON = function() {
                return AdReplyInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            AdReplyInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.AdReplyInfo";
            };

            AdReplyInfo.MediaType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "IMAGE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return AdReplyInfo;
        })();

        ContextInfo.BusinessInteractionPills = (function() {

            const BusinessInteractionPills = function (p) {
                this.pills = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessInteractionPills.prototype.businessJid = null;
            BusinessInteractionPills.prototype.pills = $util.emptyArray;
            BusinessInteractionPills.prototype.entryPoint = null;
            BusinessInteractionPills.prototype.signedPayload = null;
            BusinessInteractionPills.prototype.signatureEnvelope = null;
            BusinessInteractionPills.prototype.unauthenticatedBusinessMetadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessInteractionPills.prototype, "_businessJid", {
                get: $util.oneOfGetter($oneOfFields = ["businessJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessInteractionPills.prototype, "_entryPoint", {
                get: $util.oneOfGetter($oneOfFields = ["entryPoint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessInteractionPills.prototype, "_signedPayload", {
                get: $util.oneOfGetter($oneOfFields = ["signedPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessInteractionPills.prototype, "_signatureEnvelope", {
                get: $util.oneOfGetter($oneOfFields = ["signatureEnvelope"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessInteractionPills.prototype, "_unauthenticatedBusinessMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["unauthenticatedBusinessMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessInteractionPills.create = function(properties) {
                return new BusinessInteractionPills(properties);
            };

            BusinessInteractionPills.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.businessJid != null && $Object.hasOwnProperty.call(m, "businessJid"))
                    w.uint32(10).string(m.businessJid);
                if (m.pills != null && m.pills.length) {
                    for (var i = 0; i < m.pills.length; ++i)
                        $root.proto.ContextInfo.BusinessInteractionPills.Pill.encode(m.pills[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.entryPoint != null && $Object.hasOwnProperty.call(m, "entryPoint"))
                    w.uint32(24).int32(m.entryPoint);
                if (m.signedPayload != null && $Object.hasOwnProperty.call(m, "signedPayload"))
                    w.uint32(34).bytes(m.signedPayload);
                if (m.signatureEnvelope != null && $Object.hasOwnProperty.call(m, "signatureEnvelope"))
                    $root.proto.BotSignatureVerificationMetadata.encode(m.signatureEnvelope, w.uint32(42).fork(), q + 1).ldelim();
                if (m.unauthenticatedBusinessMetadata != null && $Object.hasOwnProperty.call(m, "unauthenticatedBusinessMetadata"))
                    $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata.encode(m.unauthenticatedBusinessMetadata, w.uint32(50).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessInteractionPills.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.BusinessInteractionPills(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.businessJid = r.stringVerify();
                            m._businessJid = "businessJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.pills && m.pills.length))
                                m.pills = [];
                            m.pills.push($root.proto.ContextInfo.BusinessInteractionPills.Pill.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.entryPoint = r.int32();
                            m._entryPoint = "entryPoint";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.signedPayload = r.bytes();
                            m._signedPayload = "signedPayload";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.signatureEnvelope = $root.proto.BotSignatureVerificationMetadata.decode(r, r.uint32(), $undefined, q + 1, m.signatureEnvelope);
                            m._signatureEnvelope = "signatureEnvelope";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.unauthenticatedBusinessMetadata = $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata.decode(r, r.uint32(), $undefined, q + 1, m.unauthenticatedBusinessMetadata);
                            m._unauthenticatedBusinessMetadata = "unauthenticatedBusinessMetadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessInteractionPills.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.BusinessInteractionPills)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.BusinessInteractionPills: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.BusinessInteractionPills();
                if (d.businessJid != null) {
                    m.businessJid = $String(d.businessJid);
                }
                if (d.pills) {
                    if (!$Array.isArray(d.pills))
                        throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.pills: array expected");
                    m.pills = $Array(d.pills.length);
                    for (var i = 0; i < d.pills.length; ++i) {
                        if (!$util.isObject(d.pills[i]))
                            throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.pills: object expected");
                        m.pills[i] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.fromObject(d.pills[i], q + 1);
                    }
                }
                switch (d.entryPoint) {
                case "ENTRY_POINT_UNKNOWN":
                case 0:
                    m.entryPoint = 0;
                    break;
                case "P2P_LINK_SHARE":
                case 1:
                    m.entryPoint = 1;
                    break;
                case "CONTACT_CARD_SHARING":
                case 2:
                    m.entryPoint = 2;
                    break;
                case "PHONE_NUMBER":
                case 3:
                    m.entryPoint = 3;
                    break;
                case "STATUS":
                case 4:
                    m.entryPoint = 4;
                    break;
                case "IN_THREAD_CONTEXT_CARD":
                case 5:
                    m.entryPoint = 5;
                    break;
                default:
                    if (typeof d.entryPoint === "number" && (d.entryPoint | 0) === d.entryPoint)
                        m.entryPoint = d.entryPoint;
                }
                if (d.signedPayload != null) {
                    if (typeof d.signedPayload === "string")
                        $util.base64.decode(d.signedPayload, m.signedPayload = $util.newBuffer($util.base64.length(d.signedPayload)), 0);
                    else if (d.signedPayload.length >= 0)
                        m.signedPayload = d.signedPayload;
                }
                if (d.signatureEnvelope != null) {
                    if (!$util.isObject(d.signatureEnvelope))
                        throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.signatureEnvelope: object expected");
                    m.signatureEnvelope = $root.proto.BotSignatureVerificationMetadata.fromObject(d.signatureEnvelope, q + 1);
                }
                if (d.unauthenticatedBusinessMetadata != null) {
                    if (!$util.isObject(d.unauthenticatedBusinessMetadata))
                        throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.unauthenticatedBusinessMetadata: object expected");
                    m.unauthenticatedBusinessMetadata = $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata.fromObject(d.unauthenticatedBusinessMetadata, q + 1);
                }
                return m;
            };

            BusinessInteractionPills.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.pills = [];
                }
                if (m.businessJid != null && $Object.hasOwnProperty.call(m, "businessJid")) {
                    d.businessJid = m.businessJid;
                }
                if (m.pills && m.pills.length) {
                    d.pills = $Array(m.pills.length);
                    for (var j = 0; j < m.pills.length; ++j) {
                        d.pills[j] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.toObject(m.pills[j], o, q + 1);
                    }
                }
                if (m.entryPoint != null && $Object.hasOwnProperty.call(m, "entryPoint")) {
                    d.entryPoint = o.enums === $String ? $root.proto.ContextInfo.BusinessInteractionPills.EntryPoint[m.entryPoint] === $undefined ? m.entryPoint : $root.proto.ContextInfo.BusinessInteractionPills.EntryPoint[m.entryPoint] : m.entryPoint;
                }
                if (m.signedPayload != null && $Object.hasOwnProperty.call(m, "signedPayload")) {
                    d.signedPayload = o.bytes === $String ? $util.base64.encode(m.signedPayload, 0, m.signedPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signedPayload) : m.signedPayload;
                }
                if (m.signatureEnvelope != null && $Object.hasOwnProperty.call(m, "signatureEnvelope")) {
                    d.signatureEnvelope = $root.proto.BotSignatureVerificationMetadata.toObject(m.signatureEnvelope, o, q + 1);
                }
                if (m.unauthenticatedBusinessMetadata != null && $Object.hasOwnProperty.call(m, "unauthenticatedBusinessMetadata")) {
                    d.unauthenticatedBusinessMetadata = $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata.toObject(m.unauthenticatedBusinessMetadata, o, q + 1);
                }
                return d;
            };

            BusinessInteractionPills.prototype.toJSON = function() {
                return BusinessInteractionPills.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessInteractionPills.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.BusinessInteractionPills";
            };

            BusinessInteractionPills.EntryPoint = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "ENTRY_POINT_UNKNOWN"] = 0;
                values[valuesById[1] = "P2P_LINK_SHARE"] = 1;
                values[valuesById[2] = "CONTACT_CARD_SHARING"] = 2;
                values[valuesById[3] = "PHONE_NUMBER"] = 3;
                values[valuesById[4] = "STATUS"] = 4;
                values[valuesById[5] = "IN_THREAD_CONTEXT_CARD"] = 5;
                return values;
            })();

            BusinessInteractionPills.Pill = (function() {

                const Pill = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Pill.prototype.pillType = null;
                Pill.prototype.actionUrl = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Pill.prototype, "_pillType", {
                    get: $util.oneOfGetter($oneOfFields = ["pillType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Pill.prototype, "_actionUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["actionUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Pill.create = function(properties) {
                    return new Pill(properties);
                };

                Pill.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.pillType != null && $Object.hasOwnProperty.call(m, "pillType"))
                        w.uint32(8).int32(m.pillType);
                    if (m.actionUrl != null && $Object.hasOwnProperty.call(m, "actionUrl"))
                        w.uint32(18).string(m.actionUrl);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Pill.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.BusinessInteractionPills.Pill(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.pillType = r.int32();
                                m._pillType = "pillType";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.actionUrl = r.stringVerify();
                                m._actionUrl = "actionUrl";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Pill.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ContextInfo.BusinessInteractionPills.Pill)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.Pill: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ContextInfo.BusinessInteractionPills.Pill();
                    switch (d.pillType) {
                    case "UNKNOWN":
                    case 0:
                        m.pillType = 0;
                        break;
                    case "VIEW_BUSINESS":
                    case 1:
                        m.pillType = 1;
                        break;
                    case "CHAT":
                    case 2:
                        m.pillType = 2;
                        break;
                    case "CALL":
                    case 3:
                        m.pillType = 3;
                        break;
                    case "CATALOG":
                    case 4:
                        m.pillType = 4;
                        break;
                    case "CHANNEL":
                    case 5:
                        m.pillType = 5;
                        break;
                    case "BOOK_APPOINTMENT":
                    case 6:
                        m.pillType = 6;
                        break;
                    case "OFFERS":
                    case 7:
                        m.pillType = 7;
                        break;
                    case "BESTSELLERS":
                    case 8:
                        m.pillType = 8;
                        break;
                    case "MENU":
                    case 9:
                        m.pillType = 9;
                        break;
                    case "ABOUT":
                    case 10:
                        m.pillType = 10;
                        break;
                    case "SHOP":
                    case 11:
                        m.pillType = 11;
                        break;
                    case "ORDER":
                    case 12:
                        m.pillType = 12;
                        break;
                    default:
                        if (typeof d.pillType === "number" && (d.pillType | 0) === d.pillType)
                            m.pillType = d.pillType;
                    }
                    if (d.actionUrl != null) {
                        m.actionUrl = $String(d.actionUrl);
                    }
                    return m;
                };

                Pill.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.pillType != null && $Object.hasOwnProperty.call(m, "pillType")) {
                        d.pillType = o.enums === $String ? $root.proto.ContextInfo.BusinessInteractionPills.PillType[m.pillType] === $undefined ? m.pillType : $root.proto.ContextInfo.BusinessInteractionPills.PillType[m.pillType] : m.pillType;
                    }
                    if (m.actionUrl != null && $Object.hasOwnProperty.call(m, "actionUrl")) {
                        d.actionUrl = m.actionUrl;
                    }
                    return d;
                };

                Pill.prototype.toJSON = function() {
                    return Pill.toObject(this, $protobuf.util.toJSONOptions);
                };

                Pill.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ContextInfo.BusinessInteractionPills.Pill";
                };

                return Pill;
            })();

            BusinessInteractionPills.PillType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "VIEW_BUSINESS"] = 1;
                values[valuesById[2] = "CHAT"] = 2;
                values[valuesById[3] = "CALL"] = 3;
                values[valuesById[4] = "CATALOG"] = 4;
                values[valuesById[5] = "CHANNEL"] = 5;
                values[valuesById[6] = "BOOK_APPOINTMENT"] = 6;
                values[valuesById[7] = "OFFERS"] = 7;
                values[valuesById[8] = "BESTSELLERS"] = 8;
                values[valuesById[9] = "MENU"] = 9;
                values[valuesById[10] = "ABOUT"] = 10;
                values[valuesById[11] = "SHOP"] = 11;
                values[valuesById[12] = "ORDER"] = 12;
                return values;
            })();

            BusinessInteractionPills.SignedPayload = (function() {

                const SignedPayload = function (p) {
                    this.pills = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                SignedPayload.prototype.verifiedName = null;
                SignedPayload.prototype.pills = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SignedPayload.prototype, "_verifiedName", {
                    get: $util.oneOfGetter($oneOfFields = ["verifiedName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SignedPayload.create = function(properties) {
                    return new SignedPayload(properties);
                };

                SignedPayload.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.verifiedName != null && $Object.hasOwnProperty.call(m, "verifiedName"))
                        w.uint32(10).string(m.verifiedName);
                    if (m.pills != null && m.pills.length) {
                        for (var i = 0; i < m.pills.length; ++i)
                            $root.proto.ContextInfo.BusinessInteractionPills.Pill.encode(m.pills[i], w.uint32(18).fork(), q + 1).ldelim();
                    }
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                SignedPayload.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.BusinessInteractionPills.SignedPayload();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.verifiedName = r.stringVerify();
                                m._verifiedName = "verifiedName";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                if (!(m.pills && m.pills.length))
                                    m.pills = [];
                                m.pills.push($root.proto.ContextInfo.BusinessInteractionPills.Pill.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                SignedPayload.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ContextInfo.BusinessInteractionPills.SignedPayload)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.SignedPayload: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ContextInfo.BusinessInteractionPills.SignedPayload();
                    if (d.verifiedName != null) {
                        m.verifiedName = $String(d.verifiedName);
                    }
                    if (d.pills) {
                        if (!$Array.isArray(d.pills))
                            throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.SignedPayload.pills: array expected");
                        m.pills = $Array(d.pills.length);
                        for (var i = 0; i < d.pills.length; ++i) {
                            if (!$util.isObject(d.pills[i]))
                                throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.SignedPayload.pills: object expected");
                            m.pills[i] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.fromObject(d.pills[i], q + 1);
                        }
                    }
                    return m;
                };

                SignedPayload.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.pills = [];
                    }
                    if (m.verifiedName != null && $Object.hasOwnProperty.call(m, "verifiedName")) {
                        d.verifiedName = m.verifiedName;
                    }
                    if (m.pills && m.pills.length) {
                        d.pills = $Array(m.pills.length);
                        for (var j = 0; j < m.pills.length; ++j) {
                            d.pills[j] = $root.proto.ContextInfo.BusinessInteractionPills.Pill.toObject(m.pills[j], o, q + 1);
                        }
                    }
                    return d;
                };

                SignedPayload.prototype.toJSON = function() {
                    return SignedPayload.toObject(this, $protobuf.util.toJSONOptions);
                };

                SignedPayload.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ContextInfo.BusinessInteractionPills.SignedPayload";
                };

                return SignedPayload;
            })();

            BusinessInteractionPills.UnauthenticatedBusinessMetadata = (function() {

                const UnauthenticatedBusinessMetadata = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                UnauthenticatedBusinessMetadata.prototype.businessName = null;
                UnauthenticatedBusinessMetadata.prototype.businessCategory = null;
                UnauthenticatedBusinessMetadata.prototype.businessIsOpen = null;
                UnauthenticatedBusinessMetadata.prototype.businessIsOpenSnapshotMs = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(UnauthenticatedBusinessMetadata.prototype, "_businessName", {
                    get: $util.oneOfGetter($oneOfFields = ["businessName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(UnauthenticatedBusinessMetadata.prototype, "_businessCategory", {
                    get: $util.oneOfGetter($oneOfFields = ["businessCategory"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(UnauthenticatedBusinessMetadata.prototype, "_businessIsOpen", {
                    get: $util.oneOfGetter($oneOfFields = ["businessIsOpen"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(UnauthenticatedBusinessMetadata.prototype, "_businessIsOpenSnapshotMs", {
                    get: $util.oneOfGetter($oneOfFields = ["businessIsOpenSnapshotMs"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                UnauthenticatedBusinessMetadata.create = function(properties) {
                    return new UnauthenticatedBusinessMetadata(properties);
                };

                UnauthenticatedBusinessMetadata.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.businessName != null && $Object.hasOwnProperty.call(m, "businessName"))
                        w.uint32(10).string(m.businessName);
                    if (m.businessCategory != null && $Object.hasOwnProperty.call(m, "businessCategory"))
                        w.uint32(18).string(m.businessCategory);
                    if (m.businessIsOpen != null && $Object.hasOwnProperty.call(m, "businessIsOpen"))
                        w.uint32(24).bool(m.businessIsOpen);
                    if (m.businessIsOpenSnapshotMs != null && $Object.hasOwnProperty.call(m, "businessIsOpenSnapshotMs"))
                        w.uint32(32).int64(m.businessIsOpenSnapshotMs);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                UnauthenticatedBusinessMetadata.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.businessName = r.stringVerify();
                                m._businessName = "businessName";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.businessCategory = r.stringVerify();
                                m._businessCategory = "businessCategory";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.businessIsOpen = r.bool();
                                m._businessIsOpen = "businessIsOpen";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.businessIsOpenSnapshotMs = r.int64();
                                m._businessIsOpenSnapshotMs = "businessIsOpenSnapshotMs";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                UnauthenticatedBusinessMetadata.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata();
                    if (d.businessName != null) {
                        m.businessName = $String(d.businessName);
                    }
                    if (d.businessCategory != null) {
                        m.businessCategory = $String(d.businessCategory);
                    }
                    if (d.businessIsOpen != null) {
                        m.businessIsOpen = $Boolean(d.businessIsOpen);
                    }
                    if (d.businessIsOpenSnapshotMs != null) {
                        if ($util.Long)
                            m.businessIsOpenSnapshotMs = $util.Long.fromValue(d.businessIsOpenSnapshotMs, false);
                        else if (typeof d.businessIsOpenSnapshotMs === "string")
                            m.businessIsOpenSnapshotMs = $parseInt(d.businessIsOpenSnapshotMs, 10);
                        else if (typeof d.businessIsOpenSnapshotMs === "number")
                            m.businessIsOpenSnapshotMs = d.businessIsOpenSnapshotMs;
                        else if (typeof d.businessIsOpenSnapshotMs === "object")
                            m.businessIsOpenSnapshotMs = new $util.LongBits(d.businessIsOpenSnapshotMs.low >>> 0, d.businessIsOpenSnapshotMs.high >>> 0).toNumber();
                    }
                    return m;
                };

                UnauthenticatedBusinessMetadata.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.businessName != null && $Object.hasOwnProperty.call(m, "businessName")) {
                        d.businessName = m.businessName;
                    }
                    if (m.businessCategory != null && $Object.hasOwnProperty.call(m, "businessCategory")) {
                        d.businessCategory = m.businessCategory;
                    }
                    if (m.businessIsOpen != null && $Object.hasOwnProperty.call(m, "businessIsOpen")) {
                        d.businessIsOpen = m.businessIsOpen;
                    }
                    if (m.businessIsOpenSnapshotMs != null && $Object.hasOwnProperty.call(m, "businessIsOpenSnapshotMs")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.businessIsOpenSnapshotMs = typeof m.businessIsOpenSnapshotMs === "number" ? $BigInt(m.businessIsOpenSnapshotMs) : $util.Long.fromBits(m.businessIsOpenSnapshotMs.low >>> 0, m.businessIsOpenSnapshotMs.high >>> 0, false).toBigInt();
                        else if (typeof m.businessIsOpenSnapshotMs === "number")
                            d.businessIsOpenSnapshotMs = o.longs === $String ? $String(m.businessIsOpenSnapshotMs) : m.businessIsOpenSnapshotMs;
                        else
                            d.businessIsOpenSnapshotMs = o.longs === $String ? $util.Long.prototype.toString.call(m.businessIsOpenSnapshotMs) : o.longs === $Number ? new $util.LongBits(m.businessIsOpenSnapshotMs.low >>> 0, m.businessIsOpenSnapshotMs.high >>> 0).toNumber() : m.businessIsOpenSnapshotMs;
                    }
                    return d;
                };

                UnauthenticatedBusinessMetadata.prototype.toJSON = function() {
                    return UnauthenticatedBusinessMetadata.toObject(this, $protobuf.util.toJSONOptions);
                };

                UnauthenticatedBusinessMetadata.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ContextInfo.BusinessInteractionPills.UnauthenticatedBusinessMetadata";
                };

                return UnauthenticatedBusinessMetadata;
            })();

            return BusinessInteractionPills;
        })();

        ContextInfo.BusinessMessageForwardInfo = (function() {

            const BusinessMessageForwardInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessMessageForwardInfo.prototype.businessOwnerJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessMessageForwardInfo.prototype, "_businessOwnerJid", {
                get: $util.oneOfGetter($oneOfFields = ["businessOwnerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessMessageForwardInfo.create = function(properties) {
                return new BusinessMessageForwardInfo(properties);
            };

            BusinessMessageForwardInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.businessOwnerJid != null && $Object.hasOwnProperty.call(m, "businessOwnerJid"))
                    w.uint32(10).string(m.businessOwnerJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessMessageForwardInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.BusinessMessageForwardInfo();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.businessOwnerJid = r.stringVerify();
                            m._businessOwnerJid = "businessOwnerJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessMessageForwardInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.BusinessMessageForwardInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.BusinessMessageForwardInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.BusinessMessageForwardInfo();
                if (d.businessOwnerJid != null) {
                    m.businessOwnerJid = $String(d.businessOwnerJid);
                }
                return m;
            };

            BusinessMessageForwardInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.businessOwnerJid != null && $Object.hasOwnProperty.call(m, "businessOwnerJid")) {
                    d.businessOwnerJid = m.businessOwnerJid;
                }
                return d;
            };

            BusinessMessageForwardInfo.prototype.toJSON = function() {
                return BusinessMessageForwardInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessMessageForwardInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.BusinessMessageForwardInfo";
            };

            return BusinessMessageForwardInfo;
        })();

        ContextInfo.CrossAppSource = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "CROSS_APP_SOURCE_UNKNOWN"] = 0;
            values[valuesById[1] = "CROSS_APP_SOURCE_INSTAGRAM"] = 1;
            values[valuesById[2] = "CROSS_APP_SOURCE_FACEBOOK"] = 2;
            return values;
        })();

        ContextInfo.DataSharingContext = (function() {

            const DataSharingContext = function (p) {
                this.parameters = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DataSharingContext.prototype.showMmDisclosure = null;
            DataSharingContext.prototype.encryptedSignalTokenConsented = null;
            DataSharingContext.prototype.parameters = $util.emptyArray;
            DataSharingContext.prototype.dataSharingFlags = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DataSharingContext.prototype, "_showMmDisclosure", {
                get: $util.oneOfGetter($oneOfFields = ["showMmDisclosure"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DataSharingContext.prototype, "_encryptedSignalTokenConsented", {
                get: $util.oneOfGetter($oneOfFields = ["encryptedSignalTokenConsented"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DataSharingContext.prototype, "_dataSharingFlags", {
                get: $util.oneOfGetter($oneOfFields = ["dataSharingFlags"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DataSharingContext.create = function(properties) {
                return new DataSharingContext(properties);
            };

            DataSharingContext.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.showMmDisclosure != null && $Object.hasOwnProperty.call(m, "showMmDisclosure"))
                    w.uint32(8).bool(m.showMmDisclosure);
                if (m.encryptedSignalTokenConsented != null && $Object.hasOwnProperty.call(m, "encryptedSignalTokenConsented"))
                    w.uint32(18).string(m.encryptedSignalTokenConsented);
                if (m.parameters != null && m.parameters.length) {
                    for (var i = 0; i < m.parameters.length; ++i)
                        $root.proto.ContextInfo.DataSharingContext.Parameters.encode(m.parameters[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.dataSharingFlags != null && $Object.hasOwnProperty.call(m, "dataSharingFlags"))
                    w.uint32(32).int32(m.dataSharingFlags);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DataSharingContext.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.DataSharingContext();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.showMmDisclosure = r.bool();
                            m._showMmDisclosure = "showMmDisclosure";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encryptedSignalTokenConsented = r.stringVerify();
                            m._encryptedSignalTokenConsented = "encryptedSignalTokenConsented";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.parameters && m.parameters.length))
                                m.parameters = [];
                            m.parameters.push($root.proto.ContextInfo.DataSharingContext.Parameters.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.dataSharingFlags = r.int32();
                            m._dataSharingFlags = "dataSharingFlags";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DataSharingContext.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.DataSharingContext)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.DataSharingContext: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.DataSharingContext();
                if (d.showMmDisclosure != null) {
                    m.showMmDisclosure = $Boolean(d.showMmDisclosure);
                }
                if (d.encryptedSignalTokenConsented != null) {
                    m.encryptedSignalTokenConsented = $String(d.encryptedSignalTokenConsented);
                }
                if (d.parameters) {
                    if (!$Array.isArray(d.parameters))
                        throw $TypeError(".proto.ContextInfo.DataSharingContext.parameters: array expected");
                    m.parameters = $Array(d.parameters.length);
                    for (var i = 0; i < d.parameters.length; ++i) {
                        if (!$util.isObject(d.parameters[i]))
                            throw $TypeError(".proto.ContextInfo.DataSharingContext.parameters: object expected");
                        m.parameters[i] = $root.proto.ContextInfo.DataSharingContext.Parameters.fromObject(d.parameters[i], q + 1);
                    }
                }
                if (d.dataSharingFlags != null) {
                    m.dataSharingFlags = d.dataSharingFlags | 0;
                }
                return m;
            };

            DataSharingContext.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.parameters = [];
                }
                if (m.showMmDisclosure != null && $Object.hasOwnProperty.call(m, "showMmDisclosure")) {
                    d.showMmDisclosure = m.showMmDisclosure;
                }
                if (m.encryptedSignalTokenConsented != null && $Object.hasOwnProperty.call(m, "encryptedSignalTokenConsented")) {
                    d.encryptedSignalTokenConsented = m.encryptedSignalTokenConsented;
                }
                if (m.parameters && m.parameters.length) {
                    d.parameters = $Array(m.parameters.length);
                    for (var j = 0; j < m.parameters.length; ++j) {
                        d.parameters[j] = $root.proto.ContextInfo.DataSharingContext.Parameters.toObject(m.parameters[j], o, q + 1);
                    }
                }
                if (m.dataSharingFlags != null && $Object.hasOwnProperty.call(m, "dataSharingFlags")) {
                    d.dataSharingFlags = m.dataSharingFlags;
                }
                return d;
            };

            DataSharingContext.prototype.toJSON = function() {
                return DataSharingContext.toObject(this, $protobuf.util.toJSONOptions);
            };

            DataSharingContext.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.DataSharingContext";
            };

            DataSharingContext.DataSharingFlags = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "SHOW_MM_DISCLOSURE_ON_CLICK"] = 1;
                values[valuesById[2] = "SHOW_MM_DISCLOSURE_ON_READ"] = 2;
                return values;
            })();

            DataSharingContext.Parameters = (function() {

                const Parameters = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Parameters.prototype.key = null;
                Parameters.prototype.stringData = null;
                Parameters.prototype.intData = null;
                Parameters.prototype.floatData = null;
                Parameters.prototype.contents = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Parameters.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Parameters.prototype, "_stringData", {
                    get: $util.oneOfGetter($oneOfFields = ["stringData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Parameters.prototype, "_intData", {
                    get: $util.oneOfGetter($oneOfFields = ["intData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Parameters.prototype, "_floatData", {
                    get: $util.oneOfGetter($oneOfFields = ["floatData"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Parameters.prototype, "_contents", {
                    get: $util.oneOfGetter($oneOfFields = ["contents"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Parameters.create = function(properties) {
                    return new Parameters(properties);
                };

                Parameters.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                        w.uint32(10).string(m.key);
                    if (m.stringData != null && $Object.hasOwnProperty.call(m, "stringData"))
                        w.uint32(18).string(m.stringData);
                    if (m.intData != null && $Object.hasOwnProperty.call(m, "intData"))
                        w.uint32(24).int64(m.intData);
                    if (m.floatData != null && $Object.hasOwnProperty.call(m, "floatData"))
                        w.uint32(37).float(m.floatData);
                    if (m.contents != null && $Object.hasOwnProperty.call(m, "contents"))
                        $root.proto.ContextInfo.DataSharingContext.Parameters.encode(m.contents, w.uint32(42).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Parameters.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.DataSharingContext.Parameters();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.key = r.stringVerify();
                                m._key = "key";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.stringData = r.stringVerify();
                                m._stringData = "stringData";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.intData = r.int64();
                                m._intData = "intData";
                                continue;
                            }
                        case 4: {
                                if (u !== 5)
                                    break;
                                m.floatData = r.float();
                                m._floatData = "floatData";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.contents = $root.proto.ContextInfo.DataSharingContext.Parameters.decode(r, r.uint32(), $undefined, q + 1, m.contents);
                                m._contents = "contents";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Parameters.fromObject = function (d, q) {
                    if (d instanceof $root.proto.ContextInfo.DataSharingContext.Parameters)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.ContextInfo.DataSharingContext.Parameters: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.ContextInfo.DataSharingContext.Parameters();
                    if (d.key != null) {
                        m.key = $String(d.key);
                    }
                    if (d.stringData != null) {
                        m.stringData = $String(d.stringData);
                    }
                    if (d.intData != null) {
                        if ($util.Long)
                            m.intData = $util.Long.fromValue(d.intData, false);
                        else if (typeof d.intData === "string")
                            m.intData = $parseInt(d.intData, 10);
                        else if (typeof d.intData === "number")
                            m.intData = d.intData;
                        else if (typeof d.intData === "object")
                            m.intData = new $util.LongBits(d.intData.low >>> 0, d.intData.high >>> 0).toNumber();
                    }
                    if (d.floatData != null) {
                        m.floatData = $Number(d.floatData);
                    }
                    if (d.contents != null) {
                        if (!$util.isObject(d.contents))
                            throw $TypeError(".proto.ContextInfo.DataSharingContext.Parameters.contents: object expected");
                        m.contents = $root.proto.ContextInfo.DataSharingContext.Parameters.fromObject(d.contents, q + 1);
                    }
                    return m;
                };

                Parameters.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                        d.key = m.key;
                    }
                    if (m.stringData != null && $Object.hasOwnProperty.call(m, "stringData")) {
                        d.stringData = m.stringData;
                    }
                    if (m.intData != null && $Object.hasOwnProperty.call(m, "intData")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.intData = typeof m.intData === "number" ? $BigInt(m.intData) : $util.Long.fromBits(m.intData.low >>> 0, m.intData.high >>> 0, false).toBigInt();
                        else if (typeof m.intData === "number")
                            d.intData = o.longs === $String ? $String(m.intData) : m.intData;
                        else
                            d.intData = o.longs === $String ? $util.Long.prototype.toString.call(m.intData) : o.longs === $Number ? new $util.LongBits(m.intData.low >>> 0, m.intData.high >>> 0).toNumber() : m.intData;
                    }
                    if (m.floatData != null && $Object.hasOwnProperty.call(m, "floatData")) {
                        d.floatData = o.json && !$isFinite(m.floatData) ? $String(m.floatData) : m.floatData;
                    }
                    if (m.contents != null && $Object.hasOwnProperty.call(m, "contents")) {
                        d.contents = $root.proto.ContextInfo.DataSharingContext.Parameters.toObject(m.contents, o, q + 1);
                    }
                    return d;
                };

                Parameters.prototype.toJSON = function() {
                    return Parameters.toObject(this, $protobuf.util.toJSONOptions);
                };

                Parameters.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.ContextInfo.DataSharingContext.Parameters";
                };

                return Parameters;
            })();

            return DataSharingContext;
        })();

        ContextInfo.ExternalAdReplyInfo = (function() {

            const ExternalAdReplyInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ExternalAdReplyInfo.prototype.title = null;
            ExternalAdReplyInfo.prototype.body = null;
            ExternalAdReplyInfo.prototype.mediaType = null;
            ExternalAdReplyInfo.prototype.thumbnailUrl = null;
            ExternalAdReplyInfo.prototype.mediaUrl = null;
            ExternalAdReplyInfo.prototype.thumbnail = null;
            ExternalAdReplyInfo.prototype.sourceType = null;
            ExternalAdReplyInfo.prototype.sourceId = null;
            ExternalAdReplyInfo.prototype.sourceUrl = null;
            ExternalAdReplyInfo.prototype.containsAutoReply = null;
            ExternalAdReplyInfo.prototype.renderLargerThumbnail = null;
            ExternalAdReplyInfo.prototype.showAdAttribution = null;
            ExternalAdReplyInfo.prototype.ctwaClid = null;
            ExternalAdReplyInfo.prototype.ref = null;
            ExternalAdReplyInfo.prototype.clickToWhatsappCall = null;
            ExternalAdReplyInfo.prototype.adContextPreviewDismissed = null;
            ExternalAdReplyInfo.prototype.sourceApp = null;
            ExternalAdReplyInfo.prototype.automatedGreetingMessageShown = null;
            ExternalAdReplyInfo.prototype.greetingMessageBody = null;
            ExternalAdReplyInfo.prototype.ctaPayload = null;
            ExternalAdReplyInfo.prototype.disableNudge = null;
            ExternalAdReplyInfo.prototype.originalImageUrl = null;
            ExternalAdReplyInfo.prototype.automatedGreetingMessageCtaType = null;
            ExternalAdReplyInfo.prototype.wtwaAdFormat = null;
            ExternalAdReplyInfo.prototype.adType = null;
            ExternalAdReplyInfo.prototype.wtwaWebsiteUrl = null;
            ExternalAdReplyInfo.prototype.adPreviewUrl = null;
            ExternalAdReplyInfo.prototype.containsCtwaFlowsAutoReply = null;
            ExternalAdReplyInfo.prototype.agmThumbnailStrategy = null;
            ExternalAdReplyInfo.prototype.agmTitleStrategy = null;
            ExternalAdReplyInfo.prototype.agmSubtitleStrategy = null;
            ExternalAdReplyInfo.prototype.agmHeaderInteractionStrategy = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_thumbnailUrl", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_mediaUrl", {
                get: $util.oneOfGetter($oneOfFields = ["mediaUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceType", {
                get: $util.oneOfGetter($oneOfFields = ["sourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceId", {
                get: $util.oneOfGetter($oneOfFields = ["sourceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceUrl", {
                get: $util.oneOfGetter($oneOfFields = ["sourceUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_containsAutoReply", {
                get: $util.oneOfGetter($oneOfFields = ["containsAutoReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_renderLargerThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["renderLargerThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_showAdAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["showAdAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_ctwaClid", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaClid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_ref", {
                get: $util.oneOfGetter($oneOfFields = ["ref"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_clickToWhatsappCall", {
                get: $util.oneOfGetter($oneOfFields = ["clickToWhatsappCall"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_adContextPreviewDismissed", {
                get: $util.oneOfGetter($oneOfFields = ["adContextPreviewDismissed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_sourceApp", {
                get: $util.oneOfGetter($oneOfFields = ["sourceApp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_automatedGreetingMessageShown", {
                get: $util.oneOfGetter($oneOfFields = ["automatedGreetingMessageShown"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_greetingMessageBody", {
                get: $util.oneOfGetter($oneOfFields = ["greetingMessageBody"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_ctaPayload", {
                get: $util.oneOfGetter($oneOfFields = ["ctaPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_disableNudge", {
                get: $util.oneOfGetter($oneOfFields = ["disableNudge"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_originalImageUrl", {
                get: $util.oneOfGetter($oneOfFields = ["originalImageUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_automatedGreetingMessageCtaType", {
                get: $util.oneOfGetter($oneOfFields = ["automatedGreetingMessageCtaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_wtwaAdFormat", {
                get: $util.oneOfGetter($oneOfFields = ["wtwaAdFormat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_adType", {
                get: $util.oneOfGetter($oneOfFields = ["adType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_wtwaWebsiteUrl", {
                get: $util.oneOfGetter($oneOfFields = ["wtwaWebsiteUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_adPreviewUrl", {
                get: $util.oneOfGetter($oneOfFields = ["adPreviewUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_containsCtwaFlowsAutoReply", {
                get: $util.oneOfGetter($oneOfFields = ["containsCtwaFlowsAutoReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmThumbnailStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmThumbnailStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmTitleStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmTitleStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmSubtitleStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmSubtitleStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalAdReplyInfo.prototype, "_agmHeaderInteractionStrategy", {
                get: $util.oneOfGetter($oneOfFields = ["agmHeaderInteractionStrategy"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExternalAdReplyInfo.create = function(properties) {
                return new ExternalAdReplyInfo(properties);
            };

            ExternalAdReplyInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.body != null && $Object.hasOwnProperty.call(m, "body"))
                    w.uint32(18).string(m.body);
                if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType"))
                    w.uint32(24).int32(m.mediaType);
                if (m.thumbnailUrl != null && $Object.hasOwnProperty.call(m, "thumbnailUrl"))
                    w.uint32(34).string(m.thumbnailUrl);
                if (m.mediaUrl != null && $Object.hasOwnProperty.call(m, "mediaUrl"))
                    w.uint32(42).string(m.mediaUrl);
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail"))
                    w.uint32(50).bytes(m.thumbnail);
                if (m.sourceType != null && $Object.hasOwnProperty.call(m, "sourceType"))
                    w.uint32(58).string(m.sourceType);
                if (m.sourceId != null && $Object.hasOwnProperty.call(m, "sourceId"))
                    w.uint32(66).string(m.sourceId);
                if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl"))
                    w.uint32(74).string(m.sourceUrl);
                if (m.containsAutoReply != null && $Object.hasOwnProperty.call(m, "containsAutoReply"))
                    w.uint32(80).bool(m.containsAutoReply);
                if (m.renderLargerThumbnail != null && $Object.hasOwnProperty.call(m, "renderLargerThumbnail"))
                    w.uint32(88).bool(m.renderLargerThumbnail);
                if (m.showAdAttribution != null && $Object.hasOwnProperty.call(m, "showAdAttribution"))
                    w.uint32(96).bool(m.showAdAttribution);
                if (m.ctwaClid != null && $Object.hasOwnProperty.call(m, "ctwaClid"))
                    w.uint32(106).string(m.ctwaClid);
                if (m.ref != null && $Object.hasOwnProperty.call(m, "ref"))
                    w.uint32(114).string(m.ref);
                if (m.clickToWhatsappCall != null && $Object.hasOwnProperty.call(m, "clickToWhatsappCall"))
                    w.uint32(120).bool(m.clickToWhatsappCall);
                if (m.adContextPreviewDismissed != null && $Object.hasOwnProperty.call(m, "adContextPreviewDismissed"))
                    w.uint32(128).bool(m.adContextPreviewDismissed);
                if (m.sourceApp != null && $Object.hasOwnProperty.call(m, "sourceApp"))
                    w.uint32(138).string(m.sourceApp);
                if (m.automatedGreetingMessageShown != null && $Object.hasOwnProperty.call(m, "automatedGreetingMessageShown"))
                    w.uint32(144).bool(m.automatedGreetingMessageShown);
                if (m.greetingMessageBody != null && $Object.hasOwnProperty.call(m, "greetingMessageBody"))
                    w.uint32(154).string(m.greetingMessageBody);
                if (m.ctaPayload != null && $Object.hasOwnProperty.call(m, "ctaPayload"))
                    w.uint32(162).string(m.ctaPayload);
                if (m.disableNudge != null && $Object.hasOwnProperty.call(m, "disableNudge"))
                    w.uint32(168).bool(m.disableNudge);
                if (m.originalImageUrl != null && $Object.hasOwnProperty.call(m, "originalImageUrl"))
                    w.uint32(178).string(m.originalImageUrl);
                if (m.automatedGreetingMessageCtaType != null && $Object.hasOwnProperty.call(m, "automatedGreetingMessageCtaType"))
                    w.uint32(186).string(m.automatedGreetingMessageCtaType);
                if (m.wtwaAdFormat != null && $Object.hasOwnProperty.call(m, "wtwaAdFormat"))
                    w.uint32(192).bool(m.wtwaAdFormat);
                if (m.adType != null && $Object.hasOwnProperty.call(m, "adType"))
                    w.uint32(200).int32(m.adType);
                if (m.wtwaWebsiteUrl != null && $Object.hasOwnProperty.call(m, "wtwaWebsiteUrl"))
                    w.uint32(210).string(m.wtwaWebsiteUrl);
                if (m.adPreviewUrl != null && $Object.hasOwnProperty.call(m, "adPreviewUrl"))
                    w.uint32(218).string(m.adPreviewUrl);
                if (m.containsCtwaFlowsAutoReply != null && $Object.hasOwnProperty.call(m, "containsCtwaFlowsAutoReply"))
                    w.uint32(224).bool(m.containsCtwaFlowsAutoReply);
                if (m.agmThumbnailStrategy != null && $Object.hasOwnProperty.call(m, "agmThumbnailStrategy"))
                    w.uint32(232).int32(m.agmThumbnailStrategy);
                if (m.agmTitleStrategy != null && $Object.hasOwnProperty.call(m, "agmTitleStrategy"))
                    w.uint32(240).int32(m.agmTitleStrategy);
                if (m.agmSubtitleStrategy != null && $Object.hasOwnProperty.call(m, "agmSubtitleStrategy"))
                    w.uint32(248).int32(m.agmSubtitleStrategy);
                if (m.agmHeaderInteractionStrategy != null && $Object.hasOwnProperty.call(m, "agmHeaderInteractionStrategy"))
                    w.uint32(256).int32(m.agmHeaderInteractionStrategy);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ExternalAdReplyInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.ExternalAdReplyInfo(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.body = r.stringVerify();
                            m._body = "body";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.mediaType = r.int32();
                            m._mediaType = "mediaType";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.thumbnailUrl = r.stringVerify();
                            m._thumbnailUrl = "thumbnailUrl";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.mediaUrl = r.stringVerify();
                            m._mediaUrl = "mediaUrl";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.thumbnail = r.bytes();
                            m._thumbnail = "thumbnail";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.sourceType = r.stringVerify();
                            m._sourceType = "sourceType";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.sourceId = r.stringVerify();
                            m._sourceId = "sourceId";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.sourceUrl = r.stringVerify();
                            m._sourceUrl = "sourceUrl";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.containsAutoReply = r.bool();
                            m._containsAutoReply = "containsAutoReply";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.renderLargerThumbnail = r.bool();
                            m._renderLargerThumbnail = "renderLargerThumbnail";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.showAdAttribution = r.bool();
                            m._showAdAttribution = "showAdAttribution";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.ctwaClid = r.stringVerify();
                            m._ctwaClid = "ctwaClid";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.ref = r.stringVerify();
                            m._ref = "ref";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            m.clickToWhatsappCall = r.bool();
                            m._clickToWhatsappCall = "clickToWhatsappCall";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            m.adContextPreviewDismissed = r.bool();
                            m._adContextPreviewDismissed = "adContextPreviewDismissed";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.sourceApp = r.stringVerify();
                            m._sourceApp = "sourceApp";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.automatedGreetingMessageShown = r.bool();
                            m._automatedGreetingMessageShown = "automatedGreetingMessageShown";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            m.greetingMessageBody = r.stringVerify();
                            m._greetingMessageBody = "greetingMessageBody";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            m.ctaPayload = r.stringVerify();
                            m._ctaPayload = "ctaPayload";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            m.disableNudge = r.bool();
                            m._disableNudge = "disableNudge";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            m.originalImageUrl = r.stringVerify();
                            m._originalImageUrl = "originalImageUrl";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            m.automatedGreetingMessageCtaType = r.stringVerify();
                            m._automatedGreetingMessageCtaType = "automatedGreetingMessageCtaType";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            m.wtwaAdFormat = r.bool();
                            m._wtwaAdFormat = "wtwaAdFormat";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            m.adType = r.int32();
                            m._adType = "adType";
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            m.wtwaWebsiteUrl = r.stringVerify();
                            m._wtwaWebsiteUrl = "wtwaWebsiteUrl";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            m.adPreviewUrl = r.stringVerify();
                            m._adPreviewUrl = "adPreviewUrl";
                            continue;
                        }
                    case 28: {
                            if (u !== 0)
                                break;
                            m.containsCtwaFlowsAutoReply = r.bool();
                            m._containsCtwaFlowsAutoReply = "containsCtwaFlowsAutoReply";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            m.agmThumbnailStrategy = r.int32();
                            m._agmThumbnailStrategy = "agmThumbnailStrategy";
                            continue;
                        }
                    case 30: {
                            if (u !== 0)
                                break;
                            m.agmTitleStrategy = r.int32();
                            m._agmTitleStrategy = "agmTitleStrategy";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            m.agmSubtitleStrategy = r.int32();
                            m._agmSubtitleStrategy = "agmSubtitleStrategy";
                            continue;
                        }
                    case 32: {
                            if (u !== 0)
                                break;
                            m.agmHeaderInteractionStrategy = r.int32();
                            m._agmHeaderInteractionStrategy = "agmHeaderInteractionStrategy";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ExternalAdReplyInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.ExternalAdReplyInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.ExternalAdReplyInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.ExternalAdReplyInfo();
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.body != null) {
                    m.body = $String(d.body);
                }
                switch (d.mediaType) {
                case "NONE":
                case 0:
                    m.mediaType = 0;
                    break;
                case "IMAGE":
                case 1:
                    m.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    m.mediaType = 2;
                    break;
                default:
                    if (typeof d.mediaType === "number" && (d.mediaType | 0) === d.mediaType)
                        m.mediaType = d.mediaType;
                }
                if (d.thumbnailUrl != null) {
                    m.thumbnailUrl = $String(d.thumbnailUrl);
                }
                if (d.mediaUrl != null) {
                    m.mediaUrl = $String(d.mediaUrl);
                }
                if (d.thumbnail != null) {
                    if (typeof d.thumbnail === "string")
                        $util.base64.decode(d.thumbnail, m.thumbnail = $util.newBuffer($util.base64.length(d.thumbnail)), 0);
                    else if (d.thumbnail.length >= 0)
                        m.thumbnail = d.thumbnail;
                }
                if (d.sourceType != null) {
                    m.sourceType = $String(d.sourceType);
                }
                if (d.sourceId != null) {
                    m.sourceId = $String(d.sourceId);
                }
                if (d.sourceUrl != null) {
                    m.sourceUrl = $String(d.sourceUrl);
                }
                if (d.containsAutoReply != null) {
                    m.containsAutoReply = $Boolean(d.containsAutoReply);
                }
                if (d.renderLargerThumbnail != null) {
                    m.renderLargerThumbnail = $Boolean(d.renderLargerThumbnail);
                }
                if (d.showAdAttribution != null) {
                    m.showAdAttribution = $Boolean(d.showAdAttribution);
                }
                if (d.ctwaClid != null) {
                    m.ctwaClid = $String(d.ctwaClid);
                }
                if (d.ref != null) {
                    m.ref = $String(d.ref);
                }
                if (d.clickToWhatsappCall != null) {
                    m.clickToWhatsappCall = $Boolean(d.clickToWhatsappCall);
                }
                if (d.adContextPreviewDismissed != null) {
                    m.adContextPreviewDismissed = $Boolean(d.adContextPreviewDismissed);
                }
                if (d.sourceApp != null) {
                    m.sourceApp = $String(d.sourceApp);
                }
                if (d.automatedGreetingMessageShown != null) {
                    m.automatedGreetingMessageShown = $Boolean(d.automatedGreetingMessageShown);
                }
                if (d.greetingMessageBody != null) {
                    m.greetingMessageBody = $String(d.greetingMessageBody);
                }
                if (d.ctaPayload != null) {
                    m.ctaPayload = $String(d.ctaPayload);
                }
                if (d.disableNudge != null) {
                    m.disableNudge = $Boolean(d.disableNudge);
                }
                if (d.originalImageUrl != null) {
                    m.originalImageUrl = $String(d.originalImageUrl);
                }
                if (d.automatedGreetingMessageCtaType != null) {
                    m.automatedGreetingMessageCtaType = $String(d.automatedGreetingMessageCtaType);
                }
                if (d.wtwaAdFormat != null) {
                    m.wtwaAdFormat = $Boolean(d.wtwaAdFormat);
                }
                switch (d.adType) {
                case "CTWA":
                case 0:
                    m.adType = 0;
                    break;
                case "CAWC":
                case 1:
                    m.adType = 1;
                    break;
                default:
                    if (typeof d.adType === "number" && (d.adType | 0) === d.adType)
                        m.adType = d.adType;
                }
                if (d.wtwaWebsiteUrl != null) {
                    m.wtwaWebsiteUrl = $String(d.wtwaWebsiteUrl);
                }
                if (d.adPreviewUrl != null) {
                    m.adPreviewUrl = $String(d.adPreviewUrl);
                }
                if (d.containsCtwaFlowsAutoReply != null) {
                    m.containsCtwaFlowsAutoReply = $Boolean(d.containsCtwaFlowsAutoReply);
                }
                if (d.agmThumbnailStrategy != null) {
                    m.agmThumbnailStrategy = d.agmThumbnailStrategy | 0;
                }
                if (d.agmTitleStrategy != null) {
                    m.agmTitleStrategy = d.agmTitleStrategy | 0;
                }
                if (d.agmSubtitleStrategy != null) {
                    m.agmSubtitleStrategy = d.agmSubtitleStrategy | 0;
                }
                if (d.agmHeaderInteractionStrategy != null) {
                    m.agmHeaderInteractionStrategy = d.agmHeaderInteractionStrategy | 0;
                }
                return m;
            };

            ExternalAdReplyInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.body != null && $Object.hasOwnProperty.call(m, "body")) {
                    d.body = m.body;
                }
                if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType")) {
                    d.mediaType = o.enums === $String ? $root.proto.ContextInfo.ExternalAdReplyInfo.MediaType[m.mediaType] === $undefined ? m.mediaType : $root.proto.ContextInfo.ExternalAdReplyInfo.MediaType[m.mediaType] : m.mediaType;
                }
                if (m.thumbnailUrl != null && $Object.hasOwnProperty.call(m, "thumbnailUrl")) {
                    d.thumbnailUrl = m.thumbnailUrl;
                }
                if (m.mediaUrl != null && $Object.hasOwnProperty.call(m, "mediaUrl")) {
                    d.mediaUrl = m.mediaUrl;
                }
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail")) {
                    d.thumbnail = o.bytes === $String ? $util.base64.encode(m.thumbnail, 0, m.thumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnail) : m.thumbnail;
                }
                if (m.sourceType != null && $Object.hasOwnProperty.call(m, "sourceType")) {
                    d.sourceType = m.sourceType;
                }
                if (m.sourceId != null && $Object.hasOwnProperty.call(m, "sourceId")) {
                    d.sourceId = m.sourceId;
                }
                if (m.sourceUrl != null && $Object.hasOwnProperty.call(m, "sourceUrl")) {
                    d.sourceUrl = m.sourceUrl;
                }
                if (m.containsAutoReply != null && $Object.hasOwnProperty.call(m, "containsAutoReply")) {
                    d.containsAutoReply = m.containsAutoReply;
                }
                if (m.renderLargerThumbnail != null && $Object.hasOwnProperty.call(m, "renderLargerThumbnail")) {
                    d.renderLargerThumbnail = m.renderLargerThumbnail;
                }
                if (m.showAdAttribution != null && $Object.hasOwnProperty.call(m, "showAdAttribution")) {
                    d.showAdAttribution = m.showAdAttribution;
                }
                if (m.ctwaClid != null && $Object.hasOwnProperty.call(m, "ctwaClid")) {
                    d.ctwaClid = m.ctwaClid;
                }
                if (m.ref != null && $Object.hasOwnProperty.call(m, "ref")) {
                    d.ref = m.ref;
                }
                if (m.clickToWhatsappCall != null && $Object.hasOwnProperty.call(m, "clickToWhatsappCall")) {
                    d.clickToWhatsappCall = m.clickToWhatsappCall;
                }
                if (m.adContextPreviewDismissed != null && $Object.hasOwnProperty.call(m, "adContextPreviewDismissed")) {
                    d.adContextPreviewDismissed = m.adContextPreviewDismissed;
                }
                if (m.sourceApp != null && $Object.hasOwnProperty.call(m, "sourceApp")) {
                    d.sourceApp = m.sourceApp;
                }
                if (m.automatedGreetingMessageShown != null && $Object.hasOwnProperty.call(m, "automatedGreetingMessageShown")) {
                    d.automatedGreetingMessageShown = m.automatedGreetingMessageShown;
                }
                if (m.greetingMessageBody != null && $Object.hasOwnProperty.call(m, "greetingMessageBody")) {
                    d.greetingMessageBody = m.greetingMessageBody;
                }
                if (m.ctaPayload != null && $Object.hasOwnProperty.call(m, "ctaPayload")) {
                    d.ctaPayload = m.ctaPayload;
                }
                if (m.disableNudge != null && $Object.hasOwnProperty.call(m, "disableNudge")) {
                    d.disableNudge = m.disableNudge;
                }
                if (m.originalImageUrl != null && $Object.hasOwnProperty.call(m, "originalImageUrl")) {
                    d.originalImageUrl = m.originalImageUrl;
                }
                if (m.automatedGreetingMessageCtaType != null && $Object.hasOwnProperty.call(m, "automatedGreetingMessageCtaType")) {
                    d.automatedGreetingMessageCtaType = m.automatedGreetingMessageCtaType;
                }
                if (m.wtwaAdFormat != null && $Object.hasOwnProperty.call(m, "wtwaAdFormat")) {
                    d.wtwaAdFormat = m.wtwaAdFormat;
                }
                if (m.adType != null && $Object.hasOwnProperty.call(m, "adType")) {
                    d.adType = o.enums === $String ? $root.proto.ContextInfo.ExternalAdReplyInfo.AdType[m.adType] === $undefined ? m.adType : $root.proto.ContextInfo.ExternalAdReplyInfo.AdType[m.adType] : m.adType;
                }
                if (m.wtwaWebsiteUrl != null && $Object.hasOwnProperty.call(m, "wtwaWebsiteUrl")) {
                    d.wtwaWebsiteUrl = m.wtwaWebsiteUrl;
                }
                if (m.adPreviewUrl != null && $Object.hasOwnProperty.call(m, "adPreviewUrl")) {
                    d.adPreviewUrl = m.adPreviewUrl;
                }
                if (m.containsCtwaFlowsAutoReply != null && $Object.hasOwnProperty.call(m, "containsCtwaFlowsAutoReply")) {
                    d.containsCtwaFlowsAutoReply = m.containsCtwaFlowsAutoReply;
                }
                if (m.agmThumbnailStrategy != null && $Object.hasOwnProperty.call(m, "agmThumbnailStrategy")) {
                    d.agmThumbnailStrategy = m.agmThumbnailStrategy;
                }
                if (m.agmTitleStrategy != null && $Object.hasOwnProperty.call(m, "agmTitleStrategy")) {
                    d.agmTitleStrategy = m.agmTitleStrategy;
                }
                if (m.agmSubtitleStrategy != null && $Object.hasOwnProperty.call(m, "agmSubtitleStrategy")) {
                    d.agmSubtitleStrategy = m.agmSubtitleStrategy;
                }
                if (m.agmHeaderInteractionStrategy != null && $Object.hasOwnProperty.call(m, "agmHeaderInteractionStrategy")) {
                    d.agmHeaderInteractionStrategy = m.agmHeaderInteractionStrategy;
                }
                return d;
            };

            ExternalAdReplyInfo.prototype.toJSON = function() {
                return ExternalAdReplyInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExternalAdReplyInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.ExternalAdReplyInfo";
            };

            ExternalAdReplyInfo.AdType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "CTWA"] = 0;
                values[valuesById[1] = "CAWC"] = 1;
                return values;
            })();

            ExternalAdReplyInfo.MediaType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "IMAGE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return ExternalAdReplyInfo;
        })();

        ContextInfo.FeatureEligibilities = (function() {

            const FeatureEligibilities = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FeatureEligibilities.prototype.cannotBeReactedTo = null;
            FeatureEligibilities.prototype.cannotBeRanked = null;
            FeatureEligibilities.prototype.canRequestFeedback = null;
            FeatureEligibilities.prototype.canBeReshared = null;
            FeatureEligibilities.prototype.canReceiveMultiReact = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FeatureEligibilities.prototype, "_cannotBeReactedTo", {
                get: $util.oneOfGetter($oneOfFields = ["cannotBeReactedTo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FeatureEligibilities.prototype, "_cannotBeRanked", {
                get: $util.oneOfGetter($oneOfFields = ["cannotBeRanked"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FeatureEligibilities.prototype, "_canRequestFeedback", {
                get: $util.oneOfGetter($oneOfFields = ["canRequestFeedback"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FeatureEligibilities.prototype, "_canBeReshared", {
                get: $util.oneOfGetter($oneOfFields = ["canBeReshared"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FeatureEligibilities.prototype, "_canReceiveMultiReact", {
                get: $util.oneOfGetter($oneOfFields = ["canReceiveMultiReact"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FeatureEligibilities.create = function(properties) {
                return new FeatureEligibilities(properties);
            };

            FeatureEligibilities.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.cannotBeReactedTo != null && $Object.hasOwnProperty.call(m, "cannotBeReactedTo"))
                    w.uint32(8).bool(m.cannotBeReactedTo);
                if (m.cannotBeRanked != null && $Object.hasOwnProperty.call(m, "cannotBeRanked"))
                    w.uint32(16).bool(m.cannotBeRanked);
                if (m.canRequestFeedback != null && $Object.hasOwnProperty.call(m, "canRequestFeedback"))
                    w.uint32(24).bool(m.canRequestFeedback);
                if (m.canBeReshared != null && $Object.hasOwnProperty.call(m, "canBeReshared"))
                    w.uint32(32).bool(m.canBeReshared);
                if (m.canReceiveMultiReact != null && $Object.hasOwnProperty.call(m, "canReceiveMultiReact"))
                    w.uint32(40).bool(m.canReceiveMultiReact);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FeatureEligibilities.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.FeatureEligibilities();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.cannotBeReactedTo = r.bool();
                            m._cannotBeReactedTo = "cannotBeReactedTo";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.cannotBeRanked = r.bool();
                            m._cannotBeRanked = "cannotBeRanked";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.canRequestFeedback = r.bool();
                            m._canRequestFeedback = "canRequestFeedback";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.canBeReshared = r.bool();
                            m._canBeReshared = "canBeReshared";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.canReceiveMultiReact = r.bool();
                            m._canReceiveMultiReact = "canReceiveMultiReact";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            FeatureEligibilities.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.FeatureEligibilities)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.FeatureEligibilities: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.FeatureEligibilities();
                if (d.cannotBeReactedTo != null) {
                    m.cannotBeReactedTo = $Boolean(d.cannotBeReactedTo);
                }
                if (d.cannotBeRanked != null) {
                    m.cannotBeRanked = $Boolean(d.cannotBeRanked);
                }
                if (d.canRequestFeedback != null) {
                    m.canRequestFeedback = $Boolean(d.canRequestFeedback);
                }
                if (d.canBeReshared != null) {
                    m.canBeReshared = $Boolean(d.canBeReshared);
                }
                if (d.canReceiveMultiReact != null) {
                    m.canReceiveMultiReact = $Boolean(d.canReceiveMultiReact);
                }
                return m;
            };

            FeatureEligibilities.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.cannotBeReactedTo != null && $Object.hasOwnProperty.call(m, "cannotBeReactedTo")) {
                    d.cannotBeReactedTo = m.cannotBeReactedTo;
                }
                if (m.cannotBeRanked != null && $Object.hasOwnProperty.call(m, "cannotBeRanked")) {
                    d.cannotBeRanked = m.cannotBeRanked;
                }
                if (m.canRequestFeedback != null && $Object.hasOwnProperty.call(m, "canRequestFeedback")) {
                    d.canRequestFeedback = m.canRequestFeedback;
                }
                if (m.canBeReshared != null && $Object.hasOwnProperty.call(m, "canBeReshared")) {
                    d.canBeReshared = m.canBeReshared;
                }
                if (m.canReceiveMultiReact != null && $Object.hasOwnProperty.call(m, "canReceiveMultiReact")) {
                    d.canReceiveMultiReact = m.canReceiveMultiReact;
                }
                return d;
            };

            FeatureEligibilities.prototype.toJSON = function() {
                return FeatureEligibilities.toObject(this, $protobuf.util.toJSONOptions);
            };

            FeatureEligibilities.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.FeatureEligibilities";
            };

            return FeatureEligibilities;
        })();

        ContextInfo.ForwardOrigin = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT"] = 1;
            values[valuesById[2] = "STATUS"] = 2;
            values[valuesById[3] = "CHANNELS"] = 3;
            values[valuesById[4] = "META_AI"] = 4;
            values[valuesById[5] = "UGC"] = 5;
            return values;
        })();

        ContextInfo.ForwardedNewsletterMessageInfo = (function() {

            const ForwardedNewsletterMessageInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ForwardedNewsletterMessageInfo.prototype.newsletterJid = null;
            ForwardedNewsletterMessageInfo.prototype.serverMessageId = null;
            ForwardedNewsletterMessageInfo.prototype.newsletterName = null;
            ForwardedNewsletterMessageInfo.prototype.contentType = null;
            ForwardedNewsletterMessageInfo.prototype.accessibilityText = null;
            ForwardedNewsletterMessageInfo.prototype.profileName = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_newsletterJid", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_serverMessageId", {
                get: $util.oneOfGetter($oneOfFields = ["serverMessageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_contentType", {
                get: $util.oneOfGetter($oneOfFields = ["contentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_accessibilityText", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ForwardedNewsletterMessageInfo.prototype, "_profileName", {
                get: $util.oneOfGetter($oneOfFields = ["profileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ForwardedNewsletterMessageInfo.create = function(properties) {
                return new ForwardedNewsletterMessageInfo(properties);
            };

            ForwardedNewsletterMessageInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.newsletterJid != null && $Object.hasOwnProperty.call(m, "newsletterJid"))
                    w.uint32(10).string(m.newsletterJid);
                if (m.serverMessageId != null && $Object.hasOwnProperty.call(m, "serverMessageId"))
                    w.uint32(16).int32(m.serverMessageId);
                if (m.newsletterName != null && $Object.hasOwnProperty.call(m, "newsletterName"))
                    w.uint32(26).string(m.newsletterName);
                if (m.contentType != null && $Object.hasOwnProperty.call(m, "contentType"))
                    w.uint32(32).int32(m.contentType);
                if (m.accessibilityText != null && $Object.hasOwnProperty.call(m, "accessibilityText"))
                    w.uint32(42).string(m.accessibilityText);
                if (m.profileName != null && $Object.hasOwnProperty.call(m, "profileName"))
                    w.uint32(50).string(m.profileName);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ForwardedNewsletterMessageInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.ForwardedNewsletterMessageInfo(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.newsletterJid = r.stringVerify();
                            m._newsletterJid = "newsletterJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.serverMessageId = r.int32();
                            m._serverMessageId = "serverMessageId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.newsletterName = r.stringVerify();
                            m._newsletterName = "newsletterName";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.contentType = r.int32();
                            m._contentType = "contentType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.accessibilityText = r.stringVerify();
                            m._accessibilityText = "accessibilityText";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.profileName = r.stringVerify();
                            m._profileName = "profileName";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ForwardedNewsletterMessageInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.ForwardedNewsletterMessageInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.ForwardedNewsletterMessageInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.ForwardedNewsletterMessageInfo();
                if (d.newsletterJid != null) {
                    m.newsletterJid = $String(d.newsletterJid);
                }
                if (d.serverMessageId != null) {
                    m.serverMessageId = d.serverMessageId | 0;
                }
                if (d.newsletterName != null) {
                    m.newsletterName = $String(d.newsletterName);
                }
                switch (d.contentType) {
                case "UPDATE":
                case 1:
                    m.contentType = 1;
                    break;
                case "UPDATE_CARD":
                case 2:
                    m.contentType = 2;
                    break;
                case "LINK_CARD":
                case 3:
                    m.contentType = 3;
                    break;
                default:
                    if (typeof d.contentType === "number" && (d.contentType | 0) === d.contentType)
                        m.contentType = d.contentType;
                }
                if (d.accessibilityText != null) {
                    m.accessibilityText = $String(d.accessibilityText);
                }
                if (d.profileName != null) {
                    m.profileName = $String(d.profileName);
                }
                return m;
            };

            ForwardedNewsletterMessageInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.newsletterJid != null && $Object.hasOwnProperty.call(m, "newsletterJid")) {
                    d.newsletterJid = m.newsletterJid;
                }
                if (m.serverMessageId != null && $Object.hasOwnProperty.call(m, "serverMessageId")) {
                    d.serverMessageId = m.serverMessageId;
                }
                if (m.newsletterName != null && $Object.hasOwnProperty.call(m, "newsletterName")) {
                    d.newsletterName = m.newsletterName;
                }
                if (m.contentType != null && $Object.hasOwnProperty.call(m, "contentType")) {
                    d.contentType = o.enums === $String ? $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType[m.contentType] === $undefined ? m.contentType : $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.ContentType[m.contentType] : m.contentType;
                }
                if (m.accessibilityText != null && $Object.hasOwnProperty.call(m, "accessibilityText")) {
                    d.accessibilityText = m.accessibilityText;
                }
                if (m.profileName != null && $Object.hasOwnProperty.call(m, "profileName")) {
                    d.profileName = m.profileName;
                }
                return d;
            };

            ForwardedNewsletterMessageInfo.prototype.toJSON = function() {
                return ForwardedNewsletterMessageInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            ForwardedNewsletterMessageInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.ForwardedNewsletterMessageInfo";
            };

            ForwardedNewsletterMessageInfo.ContentType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "UPDATE"] = 1;
                values[valuesById[2] = "UPDATE_CARD"] = 2;
                values[valuesById[3] = "LINK_CARD"] = 3;
                return values;
            })();

            return ForwardedNewsletterMessageInfo;
        })();

        ContextInfo.InstagramThreadLink = (function() {

            const InstagramThreadLink = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InstagramThreadLink.prototype.url = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InstagramThreadLink.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InstagramThreadLink.create = function(properties) {
                return new InstagramThreadLink(properties);
            };

            InstagramThreadLink.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InstagramThreadLink.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.InstagramThreadLink();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InstagramThreadLink.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.InstagramThreadLink)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.InstagramThreadLink: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.InstagramThreadLink();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                return m;
            };

            InstagramThreadLink.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                return d;
            };

            InstagramThreadLink.prototype.toJSON = function() {
                return InstagramThreadLink.toObject(this, $protobuf.util.toJSONOptions);
            };

            InstagramThreadLink.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.InstagramThreadLink";
            };

            return InstagramThreadLink;
        })();

        ContextInfo.PairedMediaType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NOT_PAIRED_MEDIA"] = 0;
            values[valuesById[1] = "SD_VIDEO_PARENT"] = 1;
            values[valuesById[2] = "HD_VIDEO_CHILD"] = 2;
            values[valuesById[3] = "SD_IMAGE_PARENT"] = 3;
            values[valuesById[4] = "HD_IMAGE_CHILD"] = 4;
            values[valuesById[5] = "MOTION_PHOTO_PARENT"] = 5;
            values[valuesById[6] = "MOTION_PHOTO_CHILD"] = 6;
            values[valuesById[7] = "HEVC_VIDEO_PARENT"] = 7;
            values[valuesById[8] = "HEVC_VIDEO_CHILD"] = 8;
            return values;
        })();

        ContextInfo.PartiallySelectedContent = (function() {

            const PartiallySelectedContent = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PartiallySelectedContent.prototype.text = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PartiallySelectedContent.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PartiallySelectedContent.create = function(properties) {
                return new PartiallySelectedContent(properties);
            };

            PartiallySelectedContent.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PartiallySelectedContent.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.PartiallySelectedContent();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PartiallySelectedContent.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.PartiallySelectedContent)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.PartiallySelectedContent: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.PartiallySelectedContent();
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                return m;
            };

            PartiallySelectedContent.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                return d;
            };

            PartiallySelectedContent.prototype.toJSON = function() {
                return PartiallySelectedContent.toObject(this, $protobuf.util.toJSONOptions);
            };

            PartiallySelectedContent.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.PartiallySelectedContent";
            };

            return PartiallySelectedContent;
        })();

        ContextInfo.QuestionReplyQuotedMessage = (function() {

            const QuestionReplyQuotedMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            QuestionReplyQuotedMessage.prototype.serverQuestionId = null;
            QuestionReplyQuotedMessage.prototype.quotedQuestion = null;
            QuestionReplyQuotedMessage.prototype.quotedResponse = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_serverQuestionId", {
                get: $util.oneOfGetter($oneOfFields = ["serverQuestionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_quotedQuestion", {
                get: $util.oneOfGetter($oneOfFields = ["quotedQuestion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuestionReplyQuotedMessage.prototype, "_quotedResponse", {
                get: $util.oneOfGetter($oneOfFields = ["quotedResponse"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            QuestionReplyQuotedMessage.create = function(properties) {
                return new QuestionReplyQuotedMessage(properties);
            };

            QuestionReplyQuotedMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.serverQuestionId != null && $Object.hasOwnProperty.call(m, "serverQuestionId"))
                    w.uint32(8).int32(m.serverQuestionId);
                if (m.quotedQuestion != null && $Object.hasOwnProperty.call(m, "quotedQuestion"))
                    $root.proto.Message.encode(m.quotedQuestion, w.uint32(18).fork(), q + 1).ldelim();
                if (m.quotedResponse != null && $Object.hasOwnProperty.call(m, "quotedResponse"))
                    $root.proto.Message.encode(m.quotedResponse, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            QuestionReplyQuotedMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.QuestionReplyQuotedMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.serverQuestionId = r.int32();
                            m._serverQuestionId = "serverQuestionId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.quotedQuestion = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.quotedQuestion);
                            m._quotedQuestion = "quotedQuestion";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.quotedResponse = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.quotedResponse);
                            m._quotedResponse = "quotedResponse";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            QuestionReplyQuotedMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.QuestionReplyQuotedMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.QuestionReplyQuotedMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.QuestionReplyQuotedMessage();
                if (d.serverQuestionId != null) {
                    m.serverQuestionId = d.serverQuestionId | 0;
                }
                if (d.quotedQuestion != null) {
                    if (!$util.isObject(d.quotedQuestion))
                        throw $TypeError(".proto.ContextInfo.QuestionReplyQuotedMessage.quotedQuestion: object expected");
                    m.quotedQuestion = $root.proto.Message.fromObject(d.quotedQuestion, q + 1);
                }
                if (d.quotedResponse != null) {
                    if (!$util.isObject(d.quotedResponse))
                        throw $TypeError(".proto.ContextInfo.QuestionReplyQuotedMessage.quotedResponse: object expected");
                    m.quotedResponse = $root.proto.Message.fromObject(d.quotedResponse, q + 1);
                }
                return m;
            };

            QuestionReplyQuotedMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.serverQuestionId != null && $Object.hasOwnProperty.call(m, "serverQuestionId")) {
                    d.serverQuestionId = m.serverQuestionId;
                }
                if (m.quotedQuestion != null && $Object.hasOwnProperty.call(m, "quotedQuestion")) {
                    d.quotedQuestion = $root.proto.Message.toObject(m.quotedQuestion, o, q + 1);
                }
                if (m.quotedResponse != null && $Object.hasOwnProperty.call(m, "quotedResponse")) {
                    d.quotedResponse = $root.proto.Message.toObject(m.quotedResponse, o, q + 1);
                }
                return d;
            };

            QuestionReplyQuotedMessage.prototype.toJSON = function() {
                return QuestionReplyQuotedMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuestionReplyQuotedMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.QuestionReplyQuotedMessage";
            };

            return QuestionReplyQuotedMessage;
        })();

        ContextInfo.QuotedType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "EXPLICIT"] = 0;
            values[valuesById[1] = "AUTO"] = 1;
            return values;
        })();

        ContextInfo.StatusAttributionType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "RESHARED_FROM_MENTION"] = 1;
            values[valuesById[2] = "RESHARED_FROM_POST"] = 2;
            values[valuesById[3] = "RESHARED_FROM_POST_MANY_TIMES"] = 3;
            values[valuesById[4] = "FORWARDED_FROM_STATUS"] = 4;
            return values;
        })();

        ContextInfo.StatusAudienceMetadata = (function() {

            const StatusAudienceMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusAudienceMetadata.prototype.audienceType = null;
            StatusAudienceMetadata.prototype.listName = null;
            StatusAudienceMetadata.prototype.listEmoji = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusAudienceMetadata.prototype, "_audienceType", {
                get: $util.oneOfGetter($oneOfFields = ["audienceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusAudienceMetadata.prototype, "_listName", {
                get: $util.oneOfGetter($oneOfFields = ["listName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusAudienceMetadata.prototype, "_listEmoji", {
                get: $util.oneOfGetter($oneOfFields = ["listEmoji"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusAudienceMetadata.create = function(properties) {
                return new StatusAudienceMetadata(properties);
            };

            StatusAudienceMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.audienceType != null && $Object.hasOwnProperty.call(m, "audienceType"))
                    w.uint32(8).int32(m.audienceType);
                if (m.listName != null && $Object.hasOwnProperty.call(m, "listName"))
                    w.uint32(18).string(m.listName);
                if (m.listEmoji != null && $Object.hasOwnProperty.call(m, "listEmoji"))
                    w.uint32(26).string(m.listEmoji);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusAudienceMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.StatusAudienceMetadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.audienceType = r.int32();
                            m._audienceType = "audienceType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.listName = r.stringVerify();
                            m._listName = "listName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.listEmoji = r.stringVerify();
                            m._listEmoji = "listEmoji";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusAudienceMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.StatusAudienceMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.StatusAudienceMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.StatusAudienceMetadata();
                switch (d.audienceType) {
                case "UNKNOWN":
                case 0:
                    m.audienceType = 0;
                    break;
                case "CLOSE_FRIENDS":
                case 1:
                    m.audienceType = 1;
                    break;
                default:
                    if (typeof d.audienceType === "number" && (d.audienceType | 0) === d.audienceType)
                        m.audienceType = d.audienceType;
                }
                if (d.listName != null) {
                    m.listName = $String(d.listName);
                }
                if (d.listEmoji != null) {
                    m.listEmoji = $String(d.listEmoji);
                }
                return m;
            };

            StatusAudienceMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.audienceType != null && $Object.hasOwnProperty.call(m, "audienceType")) {
                    d.audienceType = o.enums === $String ? $root.proto.ContextInfo.StatusAudienceMetadata.AudienceType[m.audienceType] === $undefined ? m.audienceType : $root.proto.ContextInfo.StatusAudienceMetadata.AudienceType[m.audienceType] : m.audienceType;
                }
                if (m.listName != null && $Object.hasOwnProperty.call(m, "listName")) {
                    d.listName = m.listName;
                }
                if (m.listEmoji != null && $Object.hasOwnProperty.call(m, "listEmoji")) {
                    d.listEmoji = m.listEmoji;
                }
                return d;
            };

            StatusAudienceMetadata.prototype.toJSON = function() {
                return StatusAudienceMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusAudienceMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.StatusAudienceMetadata";
            };

            StatusAudienceMetadata.AudienceType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CLOSE_FRIENDS"] = 1;
                return values;
            })();

            return StatusAudienceMetadata;
        })();

        ContextInfo.StatusSourceType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "IMAGE"] = 0;
            values[valuesById[1] = "VIDEO"] = 1;
            values[valuesById[2] = "GIF"] = 2;
            values[valuesById[3] = "AUDIO"] = 3;
            values[valuesById[4] = "TEXT"] = 4;
            values[valuesById[5] = "MUSIC_STANDALONE"] = 5;
            return values;
        })();

        ContextInfo.UTMInfo = (function() {

            const UTMInfo = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UTMInfo.prototype.utmSource = null;
            UTMInfo.prototype.utmCampaign = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UTMInfo.prototype, "_utmSource", {
                get: $util.oneOfGetter($oneOfFields = ["utmSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UTMInfo.prototype, "_utmCampaign", {
                get: $util.oneOfGetter($oneOfFields = ["utmCampaign"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UTMInfo.create = function(properties) {
                return new UTMInfo(properties);
            };

            UTMInfo.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.utmSource != null && $Object.hasOwnProperty.call(m, "utmSource"))
                    w.uint32(10).string(m.utmSource);
                if (m.utmCampaign != null && $Object.hasOwnProperty.call(m, "utmCampaign"))
                    w.uint32(18).string(m.utmCampaign);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UTMInfo.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ContextInfo.UTMInfo();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.utmSource = r.stringVerify();
                            m._utmSource = "utmSource";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.utmCampaign = r.stringVerify();
                            m._utmCampaign = "utmCampaign";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UTMInfo.fromObject = function (d, q) {
                if (d instanceof $root.proto.ContextInfo.UTMInfo)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.ContextInfo.UTMInfo: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.ContextInfo.UTMInfo();
                if (d.utmSource != null) {
                    m.utmSource = $String(d.utmSource);
                }
                if (d.utmCampaign != null) {
                    m.utmCampaign = $String(d.utmCampaign);
                }
                return m;
            };

            UTMInfo.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.utmSource != null && $Object.hasOwnProperty.call(m, "utmSource")) {
                    d.utmSource = m.utmSource;
                }
                if (m.utmCampaign != null && $Object.hasOwnProperty.call(m, "utmCampaign")) {
                    d.utmCampaign = m.utmCampaign;
                }
                return d;
            };

            UTMInfo.prototype.toJSON = function() {
                return UTMInfo.toObject(this, $protobuf.util.toJSONOptions);
            };

            UTMInfo.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.ContextInfo.UTMInfo";
            };

            return UTMInfo;
        })();

        return ContextInfo;
    })();

    proto.Conversation = (function() {

        const Conversation = function (p) {
            this.messages = [];
            this.participant = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Conversation.prototype.id = "";
        Conversation.prototype.messages = $util.emptyArray;
        Conversation.prototype.newJid = null;
        Conversation.prototype.oldJid = null;
        Conversation.prototype.lastMsgTimestamp = null;
        Conversation.prototype.unreadCount = null;
        Conversation.prototype.readOnly = null;
        Conversation.prototype.endOfHistoryTransfer = null;
        Conversation.prototype.ephemeralExpiration = null;
        Conversation.prototype.ephemeralSettingTimestamp = null;
        Conversation.prototype.endOfHistoryTransferType = null;
        Conversation.prototype.conversationTimestamp = null;
        Conversation.prototype.name = null;
        Conversation.prototype.pHash = null;
        Conversation.prototype.notSpam = null;
        Conversation.prototype.archived = null;
        Conversation.prototype.disappearingMode = null;
        Conversation.prototype.unreadMentionCount = null;
        Conversation.prototype.markedAsUnread = null;
        Conversation.prototype.participant = $util.emptyArray;
        Conversation.prototype.tcToken = null;
        Conversation.prototype.tcTokenTimestamp = null;
        Conversation.prototype.contactPrimaryIdentityKey = null;
        Conversation.prototype.pinned = null;
        Conversation.prototype.muteEndTime = null;
        Conversation.prototype.wallpaper = null;
        Conversation.prototype.mediaVisibility = null;
        Conversation.prototype.tcTokenSenderTimestamp = null;
        Conversation.prototype.suspended = null;
        Conversation.prototype.terminated = null;
        Conversation.prototype.createdAt = null;
        Conversation.prototype.createdBy = null;
        Conversation.prototype.description = null;
        Conversation.prototype.support = null;
        Conversation.prototype.isParentGroup = null;
        Conversation.prototype.parentGroupId = null;
        Conversation.prototype.isDefaultSubgroup = null;
        Conversation.prototype.displayName = null;
        Conversation.prototype.pnJid = null;
        Conversation.prototype.shareOwnPn = null;
        Conversation.prototype.pnhDuplicateLidThread = null;
        Conversation.prototype.lidJid = null;
        Conversation.prototype.username = null;
        Conversation.prototype.lidOriginType = null;
        Conversation.prototype.commentsCount = null;
        Conversation.prototype.locked = null;
        Conversation.prototype.systemMessageToInsert = null;
        Conversation.prototype.capiCreatedGroup = null;
        Conversation.prototype.accountLid = null;
        Conversation.prototype.limitSharing = null;
        Conversation.prototype.limitSharingSettingTimestamp = null;
        Conversation.prototype.limitSharingTrigger = null;
        Conversation.prototype.limitSharingInitiatedByMe = null;
        Conversation.prototype.maibaAiThreadEnabled = null;
        Conversation.prototype.isMarketingMessageThread = null;
        Conversation.prototype.isSenderNewAccount = null;
        Conversation.prototype.afterReadDuration = null;
        Conversation.prototype.isSenderSuspicious = null;
        Conversation.prototype.appealStatus = null;
        Conversation.prototype.appealUpdateTime = null;
        Conversation.prototype.authAgentParentCompanyName = null;
        Conversation.prototype.authAgentObaPhoneNumber = null;
        Conversation.prototype.identityVerification = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_newJid", {
            get: $util.oneOfGetter($oneOfFields = ["newJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_oldJid", {
            get: $util.oneOfGetter($oneOfFields = ["oldJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_lastMsgTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["lastMsgTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_unreadCount", {
            get: $util.oneOfGetter($oneOfFields = ["unreadCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_readOnly", {
            get: $util.oneOfGetter($oneOfFields = ["readOnly"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_endOfHistoryTransfer", {
            get: $util.oneOfGetter($oneOfFields = ["endOfHistoryTransfer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_ephemeralExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_ephemeralSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_endOfHistoryTransferType", {
            get: $util.oneOfGetter($oneOfFields = ["endOfHistoryTransferType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_conversationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["conversationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_pHash", {
            get: $util.oneOfGetter($oneOfFields = ["pHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_notSpam", {
            get: $util.oneOfGetter($oneOfFields = ["notSpam"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_archived", {
            get: $util.oneOfGetter($oneOfFields = ["archived"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_unreadMentionCount", {
            get: $util.oneOfGetter($oneOfFields = ["unreadMentionCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_markedAsUnread", {
            get: $util.oneOfGetter($oneOfFields = ["markedAsUnread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_tcToken", {
            get: $util.oneOfGetter($oneOfFields = ["tcToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_tcTokenTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["tcTokenTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_contactPrimaryIdentityKey", {
            get: $util.oneOfGetter($oneOfFields = ["contactPrimaryIdentityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_pinned", {
            get: $util.oneOfGetter($oneOfFields = ["pinned"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_muteEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["muteEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_wallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["wallpaper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_mediaVisibility", {
            get: $util.oneOfGetter($oneOfFields = ["mediaVisibility"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_tcTokenSenderTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["tcTokenSenderTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_suspended", {
            get: $util.oneOfGetter($oneOfFields = ["suspended"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_terminated", {
            get: $util.oneOfGetter($oneOfFields = ["terminated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_createdAt", {
            get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_createdBy", {
            get: $util.oneOfGetter($oneOfFields = ["createdBy"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_support", {
            get: $util.oneOfGetter($oneOfFields = ["support"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_isParentGroup", {
            get: $util.oneOfGetter($oneOfFields = ["isParentGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_parentGroupId", {
            get: $util.oneOfGetter($oneOfFields = ["parentGroupId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_isDefaultSubgroup", {
            get: $util.oneOfGetter($oneOfFields = ["isDefaultSubgroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_displayName", {
            get: $util.oneOfGetter($oneOfFields = ["displayName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_pnJid", {
            get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_shareOwnPn", {
            get: $util.oneOfGetter($oneOfFields = ["shareOwnPn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_pnhDuplicateLidThread", {
            get: $util.oneOfGetter($oneOfFields = ["pnhDuplicateLidThread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_lidJid", {
            get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_lidOriginType", {
            get: $util.oneOfGetter($oneOfFields = ["lidOriginType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_commentsCount", {
            get: $util.oneOfGetter($oneOfFields = ["commentsCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_locked", {
            get: $util.oneOfGetter($oneOfFields = ["locked"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_systemMessageToInsert", {
            get: $util.oneOfGetter($oneOfFields = ["systemMessageToInsert"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_capiCreatedGroup", {
            get: $util.oneOfGetter($oneOfFields = ["capiCreatedGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_accountLid", {
            get: $util.oneOfGetter($oneOfFields = ["accountLid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_limitSharing", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_limitSharingSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_limitSharingTrigger", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingTrigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_limitSharingInitiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingInitiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_maibaAiThreadEnabled", {
            get: $util.oneOfGetter($oneOfFields = ["maibaAiThreadEnabled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_isMarketingMessageThread", {
            get: $util.oneOfGetter($oneOfFields = ["isMarketingMessageThread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_isSenderNewAccount", {
            get: $util.oneOfGetter($oneOfFields = ["isSenderNewAccount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_afterReadDuration", {
            get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_isSenderSuspicious", {
            get: $util.oneOfGetter($oneOfFields = ["isSenderSuspicious"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_appealStatus", {
            get: $util.oneOfGetter($oneOfFields = ["appealStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_appealUpdateTime", {
            get: $util.oneOfGetter($oneOfFields = ["appealUpdateTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_authAgentParentCompanyName", {
            get: $util.oneOfGetter($oneOfFields = ["authAgentParentCompanyName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_authAgentObaPhoneNumber", {
            get: $util.oneOfGetter($oneOfFields = ["authAgentObaPhoneNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Conversation.prototype, "_identityVerification", {
            get: $util.oneOfGetter($oneOfFields = ["identityVerification"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Conversation.create = function(properties) {
            return new Conversation(properties);
        };

        Conversation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).string(m.id);
            if (m.messages != null && m.messages.length) {
                for (var i = 0; i < m.messages.length; ++i)
                    $root.proto.HistorySyncMsg.encode(m.messages[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.newJid != null && $Object.hasOwnProperty.call(m, "newJid"))
                w.uint32(26).string(m.newJid);
            if (m.oldJid != null && $Object.hasOwnProperty.call(m, "oldJid"))
                w.uint32(34).string(m.oldJid);
            if (m.lastMsgTimestamp != null && $Object.hasOwnProperty.call(m, "lastMsgTimestamp"))
                w.uint32(40).uint64(m.lastMsgTimestamp);
            if (m.unreadCount != null && $Object.hasOwnProperty.call(m, "unreadCount"))
                w.uint32(48).uint32(m.unreadCount);
            if (m.readOnly != null && $Object.hasOwnProperty.call(m, "readOnly"))
                w.uint32(56).bool(m.readOnly);
            if (m.endOfHistoryTransfer != null && $Object.hasOwnProperty.call(m, "endOfHistoryTransfer"))
                w.uint32(64).bool(m.endOfHistoryTransfer);
            if (m.ephemeralExpiration != null && $Object.hasOwnProperty.call(m, "ephemeralExpiration"))
                w.uint32(72).uint32(m.ephemeralExpiration);
            if (m.ephemeralSettingTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp"))
                w.uint32(80).int64(m.ephemeralSettingTimestamp);
            if (m.endOfHistoryTransferType != null && $Object.hasOwnProperty.call(m, "endOfHistoryTransferType"))
                w.uint32(88).int32(m.endOfHistoryTransferType);
            if (m.conversationTimestamp != null && $Object.hasOwnProperty.call(m, "conversationTimestamp"))
                w.uint32(96).uint64(m.conversationTimestamp);
            if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                w.uint32(106).string(m.name);
            if (m.pHash != null && $Object.hasOwnProperty.call(m, "pHash"))
                w.uint32(114).string(m.pHash);
            if (m.notSpam != null && $Object.hasOwnProperty.call(m, "notSpam"))
                w.uint32(120).bool(m.notSpam);
            if (m.archived != null && $Object.hasOwnProperty.call(m, "archived"))
                w.uint32(128).bool(m.archived);
            if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode"))
                $root.proto.DisappearingMode.encode(m.disappearingMode, w.uint32(138).fork(), q + 1).ldelim();
            if (m.unreadMentionCount != null && $Object.hasOwnProperty.call(m, "unreadMentionCount"))
                w.uint32(144).uint32(m.unreadMentionCount);
            if (m.markedAsUnread != null && $Object.hasOwnProperty.call(m, "markedAsUnread"))
                w.uint32(152).bool(m.markedAsUnread);
            if (m.participant != null && m.participant.length) {
                for (var i = 0; i < m.participant.length; ++i)
                    $root.proto.GroupParticipant.encode(m.participant[i], w.uint32(162).fork(), q + 1).ldelim();
            }
            if (m.tcToken != null && $Object.hasOwnProperty.call(m, "tcToken"))
                w.uint32(170).bytes(m.tcToken);
            if (m.tcTokenTimestamp != null && $Object.hasOwnProperty.call(m, "tcTokenTimestamp"))
                w.uint32(176).uint64(m.tcTokenTimestamp);
            if (m.contactPrimaryIdentityKey != null && $Object.hasOwnProperty.call(m, "contactPrimaryIdentityKey"))
                w.uint32(186).bytes(m.contactPrimaryIdentityKey);
            if (m.pinned != null && $Object.hasOwnProperty.call(m, "pinned"))
                w.uint32(192).uint32(m.pinned);
            if (m.muteEndTime != null && $Object.hasOwnProperty.call(m, "muteEndTime"))
                w.uint32(200).uint64(m.muteEndTime);
            if (m.wallpaper != null && $Object.hasOwnProperty.call(m, "wallpaper"))
                $root.proto.WallpaperSettings.encode(m.wallpaper, w.uint32(210).fork(), q + 1).ldelim();
            if (m.mediaVisibility != null && $Object.hasOwnProperty.call(m, "mediaVisibility"))
                w.uint32(216).int32(m.mediaVisibility);
            if (m.tcTokenSenderTimestamp != null && $Object.hasOwnProperty.call(m, "tcTokenSenderTimestamp"))
                w.uint32(224).uint64(m.tcTokenSenderTimestamp);
            if (m.suspended != null && $Object.hasOwnProperty.call(m, "suspended"))
                w.uint32(232).bool(m.suspended);
            if (m.terminated != null && $Object.hasOwnProperty.call(m, "terminated"))
                w.uint32(240).bool(m.terminated);
            if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt"))
                w.uint32(248).uint64(m.createdAt);
            if (m.createdBy != null && $Object.hasOwnProperty.call(m, "createdBy"))
                w.uint32(258).string(m.createdBy);
            if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                w.uint32(266).string(m.description);
            if (m.support != null && $Object.hasOwnProperty.call(m, "support"))
                w.uint32(272).bool(m.support);
            if (m.isParentGroup != null && $Object.hasOwnProperty.call(m, "isParentGroup"))
                w.uint32(280).bool(m.isParentGroup);
            if (m.isDefaultSubgroup != null && $Object.hasOwnProperty.call(m, "isDefaultSubgroup"))
                w.uint32(288).bool(m.isDefaultSubgroup);
            if (m.parentGroupId != null && $Object.hasOwnProperty.call(m, "parentGroupId"))
                w.uint32(298).string(m.parentGroupId);
            if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName"))
                w.uint32(306).string(m.displayName);
            if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid"))
                w.uint32(314).string(m.pnJid);
            if (m.shareOwnPn != null && $Object.hasOwnProperty.call(m, "shareOwnPn"))
                w.uint32(320).bool(m.shareOwnPn);
            if (m.pnhDuplicateLidThread != null && $Object.hasOwnProperty.call(m, "pnhDuplicateLidThread"))
                w.uint32(328).bool(m.pnhDuplicateLidThread);
            if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid"))
                w.uint32(338).string(m.lidJid);
            if (m.username != null && $Object.hasOwnProperty.call(m, "username"))
                w.uint32(346).string(m.username);
            if (m.lidOriginType != null && $Object.hasOwnProperty.call(m, "lidOriginType"))
                w.uint32(354).string(m.lidOriginType);
            if (m.commentsCount != null && $Object.hasOwnProperty.call(m, "commentsCount"))
                w.uint32(360).uint32(m.commentsCount);
            if (m.locked != null && $Object.hasOwnProperty.call(m, "locked"))
                w.uint32(368).bool(m.locked);
            if (m.systemMessageToInsert != null && $Object.hasOwnProperty.call(m, "systemMessageToInsert"))
                w.uint32(376).int32(m.systemMessageToInsert);
            if (m.capiCreatedGroup != null && $Object.hasOwnProperty.call(m, "capiCreatedGroup"))
                w.uint32(384).bool(m.capiCreatedGroup);
            if (m.accountLid != null && $Object.hasOwnProperty.call(m, "accountLid"))
                w.uint32(394).string(m.accountLid);
            if (m.limitSharing != null && $Object.hasOwnProperty.call(m, "limitSharing"))
                w.uint32(400).bool(m.limitSharing);
            if (m.limitSharingSettingTimestamp != null && $Object.hasOwnProperty.call(m, "limitSharingSettingTimestamp"))
                w.uint32(408).int64(m.limitSharingSettingTimestamp);
            if (m.limitSharingTrigger != null && $Object.hasOwnProperty.call(m, "limitSharingTrigger"))
                w.uint32(416).int32(m.limitSharingTrigger);
            if (m.limitSharingInitiatedByMe != null && $Object.hasOwnProperty.call(m, "limitSharingInitiatedByMe"))
                w.uint32(424).bool(m.limitSharingInitiatedByMe);
            if (m.maibaAiThreadEnabled != null && $Object.hasOwnProperty.call(m, "maibaAiThreadEnabled"))
                w.uint32(432).bool(m.maibaAiThreadEnabled);
            if (m.isMarketingMessageThread != null && $Object.hasOwnProperty.call(m, "isMarketingMessageThread"))
                w.uint32(440).bool(m.isMarketingMessageThread);
            if (m.isSenderNewAccount != null && $Object.hasOwnProperty.call(m, "isSenderNewAccount"))
                w.uint32(448).bool(m.isSenderNewAccount);
            if (m.afterReadDuration != null && $Object.hasOwnProperty.call(m, "afterReadDuration"))
                w.uint32(456).uint32(m.afterReadDuration);
            if (m.isSenderSuspicious != null && $Object.hasOwnProperty.call(m, "isSenderSuspicious"))
                w.uint32(464).bool(m.isSenderSuspicious);
            if (m.appealStatus != null && $Object.hasOwnProperty.call(m, "appealStatus"))
                w.uint32(472).int32(m.appealStatus);
            if (m.appealUpdateTime != null && $Object.hasOwnProperty.call(m, "appealUpdateTime"))
                w.uint32(480).uint64(m.appealUpdateTime);
            if (m.authAgentParentCompanyName != null && $Object.hasOwnProperty.call(m, "authAgentParentCompanyName"))
                w.uint32(490).string(m.authAgentParentCompanyName);
            if (m.authAgentObaPhoneNumber != null && $Object.hasOwnProperty.call(m, "authAgentObaPhoneNumber"))
                w.uint32(498).string(m.authAgentObaPhoneNumber);
            if (m.identityVerification != null && $Object.hasOwnProperty.call(m, "identityVerification"))
                $root.proto.IdentityVerificationState.encode(m.identityVerification, w.uint32(506).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Conversation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Conversation(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.id = r.stringVerify();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.messages && m.messages.length))
                            m.messages = [];
                        m.messages.push($root.proto.HistorySyncMsg.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.newJid = r.stringVerify();
                        m._newJid = "newJid";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.oldJid = r.stringVerify();
                        m._oldJid = "oldJid";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.lastMsgTimestamp = r.uint64();
                        m._lastMsgTimestamp = "lastMsgTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.unreadCount = r.uint32();
                        m._unreadCount = "unreadCount";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.readOnly = r.bool();
                        m._readOnly = "readOnly";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.endOfHistoryTransfer = r.bool();
                        m._endOfHistoryTransfer = "endOfHistoryTransfer";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.ephemeralExpiration = r.uint32();
                        m._ephemeralExpiration = "ephemeralExpiration";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.ephemeralSettingTimestamp = r.int64();
                        m._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.endOfHistoryTransferType = r.int32();
                        m._endOfHistoryTransferType = "endOfHistoryTransferType";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.conversationTimestamp = r.uint64();
                        m._conversationTimestamp = "conversationTimestamp";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.name = r.stringVerify();
                        m._name = "name";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.pHash = r.stringVerify();
                        m._pHash = "pHash";
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        m.notSpam = r.bool();
                        m._notSpam = "notSpam";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        m.archived = r.bool();
                        m._archived = "archived";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.disappearingMode = $root.proto.DisappearingMode.decode(r, r.uint32(), $undefined, q + 1, m.disappearingMode);
                        m._disappearingMode = "disappearingMode";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        m.unreadMentionCount = r.uint32();
                        m._unreadMentionCount = "unreadMentionCount";
                        continue;
                    }
                case 19: {
                        if (u !== 0)
                            break;
                        m.markedAsUnread = r.bool();
                        m._markedAsUnread = "markedAsUnread";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        if (!(m.participant && m.participant.length))
                            m.participant = [];
                        m.participant.push($root.proto.GroupParticipant.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        m.tcToken = r.bytes();
                        m._tcToken = "tcToken";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        m.tcTokenTimestamp = r.uint64();
                        m._tcTokenTimestamp = "tcTokenTimestamp";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        m.contactPrimaryIdentityKey = r.bytes();
                        m._contactPrimaryIdentityKey = "contactPrimaryIdentityKey";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        m.pinned = r.uint32();
                        m._pinned = "pinned";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        m.muteEndTime = r.uint64();
                        m._muteEndTime = "muteEndTime";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        m.wallpaper = $root.proto.WallpaperSettings.decode(r, r.uint32(), $undefined, q + 1, m.wallpaper);
                        m._wallpaper = "wallpaper";
                        continue;
                    }
                case 27: {
                        if (u !== 0)
                            break;
                        m.mediaVisibility = r.int32();
                        m._mediaVisibility = "mediaVisibility";
                        continue;
                    }
                case 28: {
                        if (u !== 0)
                            break;
                        m.tcTokenSenderTimestamp = r.uint64();
                        m._tcTokenSenderTimestamp = "tcTokenSenderTimestamp";
                        continue;
                    }
                case 29: {
                        if (u !== 0)
                            break;
                        m.suspended = r.bool();
                        m._suspended = "suspended";
                        continue;
                    }
                case 30: {
                        if (u !== 0)
                            break;
                        m.terminated = r.bool();
                        m._terminated = "terminated";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        m.createdAt = r.uint64();
                        m._createdAt = "createdAt";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        m.createdBy = r.stringVerify();
                        m._createdBy = "createdBy";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        m.description = r.stringVerify();
                        m._description = "description";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        m.support = r.bool();
                        m._support = "support";
                        continue;
                    }
                case 35: {
                        if (u !== 0)
                            break;
                        m.isParentGroup = r.bool();
                        m._isParentGroup = "isParentGroup";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        m.parentGroupId = r.stringVerify();
                        m._parentGroupId = "parentGroupId";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        m.isDefaultSubgroup = r.bool();
                        m._isDefaultSubgroup = "isDefaultSubgroup";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.displayName = r.stringVerify();
                        m._displayName = "displayName";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        m.pnJid = r.stringVerify();
                        m._pnJid = "pnJid";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        m.shareOwnPn = r.bool();
                        m._shareOwnPn = "shareOwnPn";
                        continue;
                    }
                case 41: {
                        if (u !== 0)
                            break;
                        m.pnhDuplicateLidThread = r.bool();
                        m._pnhDuplicateLidThread = "pnhDuplicateLidThread";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        m.lidJid = r.stringVerify();
                        m._lidJid = "lidJid";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.username = r.stringVerify();
                        m._username = "username";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        m.lidOriginType = r.stringVerify();
                        m._lidOriginType = "lidOriginType";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        m.commentsCount = r.uint32();
                        m._commentsCount = "commentsCount";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        m.locked = r.bool();
                        m._locked = "locked";
                        continue;
                    }
                case 47: {
                        if (u !== 0)
                            break;
                        m.systemMessageToInsert = r.int32();
                        m._systemMessageToInsert = "systemMessageToInsert";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        m.capiCreatedGroup = r.bool();
                        m._capiCreatedGroup = "capiCreatedGroup";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        m.accountLid = r.stringVerify();
                        m._accountLid = "accountLid";
                        continue;
                    }
                case 50: {
                        if (u !== 0)
                            break;
                        m.limitSharing = r.bool();
                        m._limitSharing = "limitSharing";
                        continue;
                    }
                case 51: {
                        if (u !== 0)
                            break;
                        m.limitSharingSettingTimestamp = r.int64();
                        m._limitSharingSettingTimestamp = "limitSharingSettingTimestamp";
                        continue;
                    }
                case 52: {
                        if (u !== 0)
                            break;
                        m.limitSharingTrigger = r.int32();
                        m._limitSharingTrigger = "limitSharingTrigger";
                        continue;
                    }
                case 53: {
                        if (u !== 0)
                            break;
                        m.limitSharingInitiatedByMe = r.bool();
                        m._limitSharingInitiatedByMe = "limitSharingInitiatedByMe";
                        continue;
                    }
                case 54: {
                        if (u !== 0)
                            break;
                        m.maibaAiThreadEnabled = r.bool();
                        m._maibaAiThreadEnabled = "maibaAiThreadEnabled";
                        continue;
                    }
                case 55: {
                        if (u !== 0)
                            break;
                        m.isMarketingMessageThread = r.bool();
                        m._isMarketingMessageThread = "isMarketingMessageThread";
                        continue;
                    }
                case 56: {
                        if (u !== 0)
                            break;
                        m.isSenderNewAccount = r.bool();
                        m._isSenderNewAccount = "isSenderNewAccount";
                        continue;
                    }
                case 57: {
                        if (u !== 0)
                            break;
                        m.afterReadDuration = r.uint32();
                        m._afterReadDuration = "afterReadDuration";
                        continue;
                    }
                case 58: {
                        if (u !== 0)
                            break;
                        m.isSenderSuspicious = r.bool();
                        m._isSenderSuspicious = "isSenderSuspicious";
                        continue;
                    }
                case 59: {
                        if (u !== 0)
                            break;
                        m.appealStatus = r.int32();
                        m._appealStatus = "appealStatus";
                        continue;
                    }
                case 60: {
                        if (u !== 0)
                            break;
                        m.appealUpdateTime = r.uint64();
                        m._appealUpdateTime = "appealUpdateTime";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        m.authAgentParentCompanyName = r.stringVerify();
                        m._authAgentParentCompanyName = "authAgentParentCompanyName";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        m.authAgentObaPhoneNumber = r.stringVerify();
                        m._authAgentObaPhoneNumber = "authAgentObaPhoneNumber";
                        continue;
                    }
                case 63: {
                        if (u !== 2)
                            break;
                        m.identityVerification = $root.proto.IdentityVerificationState.decode(r, r.uint32(), $undefined, q + 1, m.identityVerification);
                        m._identityVerification = "identityVerification";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "id"))
                throw $util.ProtocolError("missing required 'id'", { instance: m });
            return m;
        };

        Conversation.fromObject = function (d, q) {
            if (d instanceof $root.proto.Conversation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Conversation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Conversation();
            if (d.id != null) {
                m.id = $String(d.id);
            }
            if (d.messages) {
                if (!$Array.isArray(d.messages))
                    throw $TypeError(".proto.Conversation.messages: array expected");
                m.messages = $Array(d.messages.length);
                for (var i = 0; i < d.messages.length; ++i) {
                    if (!$util.isObject(d.messages[i]))
                        throw $TypeError(".proto.Conversation.messages: object expected");
                    m.messages[i] = $root.proto.HistorySyncMsg.fromObject(d.messages[i], q + 1);
                }
            }
            if (d.newJid != null) {
                m.newJid = $String(d.newJid);
            }
            if (d.oldJid != null) {
                m.oldJid = $String(d.oldJid);
            }
            if (d.lastMsgTimestamp != null) {
                if ($util.Long)
                    m.lastMsgTimestamp = $util.Long.fromValue(d.lastMsgTimestamp, true);
                else if (typeof d.lastMsgTimestamp === "string")
                    m.lastMsgTimestamp = $parseInt(d.lastMsgTimestamp, 10);
                else if (typeof d.lastMsgTimestamp === "number")
                    m.lastMsgTimestamp = d.lastMsgTimestamp;
                else if (typeof d.lastMsgTimestamp === "object")
                    m.lastMsgTimestamp = new $util.LongBits(d.lastMsgTimestamp.low >>> 0, d.lastMsgTimestamp.high >>> 0).toNumber(true);
            }
            if (d.unreadCount != null) {
                m.unreadCount = d.unreadCount >>> 0;
            }
            if (d.readOnly != null) {
                m.readOnly = $Boolean(d.readOnly);
            }
            if (d.endOfHistoryTransfer != null) {
                m.endOfHistoryTransfer = $Boolean(d.endOfHistoryTransfer);
            }
            if (d.ephemeralExpiration != null) {
                m.ephemeralExpiration = d.ephemeralExpiration >>> 0;
            }
            if (d.ephemeralSettingTimestamp != null) {
                if ($util.Long)
                    m.ephemeralSettingTimestamp = $util.Long.fromValue(d.ephemeralSettingTimestamp, false);
                else if (typeof d.ephemeralSettingTimestamp === "string")
                    m.ephemeralSettingTimestamp = $parseInt(d.ephemeralSettingTimestamp, 10);
                else if (typeof d.ephemeralSettingTimestamp === "number")
                    m.ephemeralSettingTimestamp = d.ephemeralSettingTimestamp;
                else if (typeof d.ephemeralSettingTimestamp === "object")
                    m.ephemeralSettingTimestamp = new $util.LongBits(d.ephemeralSettingTimestamp.low >>> 0, d.ephemeralSettingTimestamp.high >>> 0).toNumber();
            }
            switch (d.endOfHistoryTransferType) {
            case "COMPLETE_BUT_MORE_MESSAGES_REMAIN_ON_PRIMARY":
            case 0:
                m.endOfHistoryTransferType = 0;
                break;
            case "COMPLETE_AND_NO_MORE_MESSAGE_REMAIN_ON_PRIMARY":
            case 1:
                m.endOfHistoryTransferType = 1;
                break;
            case "COMPLETE_ON_DEMAND_SYNC_BUT_MORE_MSG_REMAIN_ON_PRIMARY":
            case 2:
                m.endOfHistoryTransferType = 2;
                break;
            case "COMPLETE_ON_DEMAND_SYNC_WITH_MORE_MSG_ON_PRIMARY_BUT_NO_ACCESS":
            case 3:
                m.endOfHistoryTransferType = 3;
                break;
            default:
                if (typeof d.endOfHistoryTransferType === "number" && (d.endOfHistoryTransferType | 0) === d.endOfHistoryTransferType)
                    m.endOfHistoryTransferType = d.endOfHistoryTransferType;
            }
            if (d.conversationTimestamp != null) {
                if ($util.Long)
                    m.conversationTimestamp = $util.Long.fromValue(d.conversationTimestamp, true);
                else if (typeof d.conversationTimestamp === "string")
                    m.conversationTimestamp = $parseInt(d.conversationTimestamp, 10);
                else if (typeof d.conversationTimestamp === "number")
                    m.conversationTimestamp = d.conversationTimestamp;
                else if (typeof d.conversationTimestamp === "object")
                    m.conversationTimestamp = new $util.LongBits(d.conversationTimestamp.low >>> 0, d.conversationTimestamp.high >>> 0).toNumber(true);
            }
            if (d.name != null) {
                m.name = $String(d.name);
            }
            if (d.pHash != null) {
                m.pHash = $String(d.pHash);
            }
            if (d.notSpam != null) {
                m.notSpam = $Boolean(d.notSpam);
            }
            if (d.archived != null) {
                m.archived = $Boolean(d.archived);
            }
            if (d.disappearingMode != null) {
                if (!$util.isObject(d.disappearingMode))
                    throw $TypeError(".proto.Conversation.disappearingMode: object expected");
                m.disappearingMode = $root.proto.DisappearingMode.fromObject(d.disappearingMode, q + 1);
            }
            if (d.unreadMentionCount != null) {
                m.unreadMentionCount = d.unreadMentionCount >>> 0;
            }
            if (d.markedAsUnread != null) {
                m.markedAsUnread = $Boolean(d.markedAsUnread);
            }
            if (d.participant) {
                if (!$Array.isArray(d.participant))
                    throw $TypeError(".proto.Conversation.participant: array expected");
                m.participant = $Array(d.participant.length);
                for (var i = 0; i < d.participant.length; ++i) {
                    if (!$util.isObject(d.participant[i]))
                        throw $TypeError(".proto.Conversation.participant: object expected");
                    m.participant[i] = $root.proto.GroupParticipant.fromObject(d.participant[i], q + 1);
                }
            }
            if (d.tcToken != null) {
                if (typeof d.tcToken === "string")
                    $util.base64.decode(d.tcToken, m.tcToken = $util.newBuffer($util.base64.length(d.tcToken)), 0);
                else if (d.tcToken.length >= 0)
                    m.tcToken = d.tcToken;
            }
            if (d.tcTokenTimestamp != null) {
                if ($util.Long)
                    m.tcTokenTimestamp = $util.Long.fromValue(d.tcTokenTimestamp, true);
                else if (typeof d.tcTokenTimestamp === "string")
                    m.tcTokenTimestamp = $parseInt(d.tcTokenTimestamp, 10);
                else if (typeof d.tcTokenTimestamp === "number")
                    m.tcTokenTimestamp = d.tcTokenTimestamp;
                else if (typeof d.tcTokenTimestamp === "object")
                    m.tcTokenTimestamp = new $util.LongBits(d.tcTokenTimestamp.low >>> 0, d.tcTokenTimestamp.high >>> 0).toNumber(true);
            }
            if (d.contactPrimaryIdentityKey != null) {
                if (typeof d.contactPrimaryIdentityKey === "string")
                    $util.base64.decode(d.contactPrimaryIdentityKey, m.contactPrimaryIdentityKey = $util.newBuffer($util.base64.length(d.contactPrimaryIdentityKey)), 0);
                else if (d.contactPrimaryIdentityKey.length >= 0)
                    m.contactPrimaryIdentityKey = d.contactPrimaryIdentityKey;
            }
            if (d.pinned != null) {
                m.pinned = d.pinned >>> 0;
            }
            if (d.muteEndTime != null) {
                if ($util.Long)
                    m.muteEndTime = $util.Long.fromValue(d.muteEndTime, true);
                else if (typeof d.muteEndTime === "string")
                    m.muteEndTime = $parseInt(d.muteEndTime, 10);
                else if (typeof d.muteEndTime === "number")
                    m.muteEndTime = d.muteEndTime;
                else if (typeof d.muteEndTime === "object")
                    m.muteEndTime = new $util.LongBits(d.muteEndTime.low >>> 0, d.muteEndTime.high >>> 0).toNumber(true);
            }
            if (d.wallpaper != null) {
                if (!$util.isObject(d.wallpaper))
                    throw $TypeError(".proto.Conversation.wallpaper: object expected");
                m.wallpaper = $root.proto.WallpaperSettings.fromObject(d.wallpaper, q + 1);
            }
            switch (d.mediaVisibility) {
            case "DEFAULT":
            case 0:
                m.mediaVisibility = 0;
                break;
            case "OFF":
            case 1:
                m.mediaVisibility = 1;
                break;
            case "ON":
            case 2:
                m.mediaVisibility = 2;
                break;
            default:
                if (typeof d.mediaVisibility === "number" && (d.mediaVisibility | 0) === d.mediaVisibility)
                    m.mediaVisibility = d.mediaVisibility;
            }
            if (d.tcTokenSenderTimestamp != null) {
                if ($util.Long)
                    m.tcTokenSenderTimestamp = $util.Long.fromValue(d.tcTokenSenderTimestamp, true);
                else if (typeof d.tcTokenSenderTimestamp === "string")
                    m.tcTokenSenderTimestamp = $parseInt(d.tcTokenSenderTimestamp, 10);
                else if (typeof d.tcTokenSenderTimestamp === "number")
                    m.tcTokenSenderTimestamp = d.tcTokenSenderTimestamp;
                else if (typeof d.tcTokenSenderTimestamp === "object")
                    m.tcTokenSenderTimestamp = new $util.LongBits(d.tcTokenSenderTimestamp.low >>> 0, d.tcTokenSenderTimestamp.high >>> 0).toNumber(true);
            }
            if (d.suspended != null) {
                m.suspended = $Boolean(d.suspended);
            }
            if (d.terminated != null) {
                m.terminated = $Boolean(d.terminated);
            }
            if (d.createdAt != null) {
                if ($util.Long)
                    m.createdAt = $util.Long.fromValue(d.createdAt, true);
                else if (typeof d.createdAt === "string")
                    m.createdAt = $parseInt(d.createdAt, 10);
                else if (typeof d.createdAt === "number")
                    m.createdAt = d.createdAt;
                else if (typeof d.createdAt === "object")
                    m.createdAt = new $util.LongBits(d.createdAt.low >>> 0, d.createdAt.high >>> 0).toNumber(true);
            }
            if (d.createdBy != null) {
                m.createdBy = $String(d.createdBy);
            }
            if (d.description != null) {
                m.description = $String(d.description);
            }
            if (d.support != null) {
                m.support = $Boolean(d.support);
            }
            if (d.isParentGroup != null) {
                m.isParentGroup = $Boolean(d.isParentGroup);
            }
            if (d.parentGroupId != null) {
                m.parentGroupId = $String(d.parentGroupId);
            }
            if (d.isDefaultSubgroup != null) {
                m.isDefaultSubgroup = $Boolean(d.isDefaultSubgroup);
            }
            if (d.displayName != null) {
                m.displayName = $String(d.displayName);
            }
            if (d.pnJid != null) {
                m.pnJid = $String(d.pnJid);
            }
            if (d.shareOwnPn != null) {
                m.shareOwnPn = $Boolean(d.shareOwnPn);
            }
            if (d.pnhDuplicateLidThread != null) {
                m.pnhDuplicateLidThread = $Boolean(d.pnhDuplicateLidThread);
            }
            if (d.lidJid != null) {
                m.lidJid = $String(d.lidJid);
            }
            if (d.username != null) {
                m.username = $String(d.username);
            }
            if (d.lidOriginType != null) {
                m.lidOriginType = $String(d.lidOriginType);
            }
            if (d.commentsCount != null) {
                m.commentsCount = d.commentsCount >>> 0;
            }
            if (d.locked != null) {
                m.locked = $Boolean(d.locked);
            }
            switch (d.systemMessageToInsert) {
            case "E2EE_MSG":
            case 1:
                m.systemMessageToInsert = 1;
                break;
            case "NE2EE_SELF":
            case 2:
                m.systemMessageToInsert = 2;
                break;
            case "NE2EE_OTHER":
            case 3:
                m.systemMessageToInsert = 3;
                break;
            default:
                if (typeof d.systemMessageToInsert === "number" && (d.systemMessageToInsert | 0) === d.systemMessageToInsert)
                    m.systemMessageToInsert = d.systemMessageToInsert;
            }
            if (d.capiCreatedGroup != null) {
                m.capiCreatedGroup = $Boolean(d.capiCreatedGroup);
            }
            if (d.accountLid != null) {
                m.accountLid = $String(d.accountLid);
            }
            if (d.limitSharing != null) {
                m.limitSharing = $Boolean(d.limitSharing);
            }
            if (d.limitSharingSettingTimestamp != null) {
                if ($util.Long)
                    m.limitSharingSettingTimestamp = $util.Long.fromValue(d.limitSharingSettingTimestamp, false);
                else if (typeof d.limitSharingSettingTimestamp === "string")
                    m.limitSharingSettingTimestamp = $parseInt(d.limitSharingSettingTimestamp, 10);
                else if (typeof d.limitSharingSettingTimestamp === "number")
                    m.limitSharingSettingTimestamp = d.limitSharingSettingTimestamp;
                else if (typeof d.limitSharingSettingTimestamp === "object")
                    m.limitSharingSettingTimestamp = new $util.LongBits(d.limitSharingSettingTimestamp.low >>> 0, d.limitSharingSettingTimestamp.high >>> 0).toNumber();
            }
            switch (d.limitSharingTrigger) {
            case "UNKNOWN":
            case 0:
                m.limitSharingTrigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                m.limitSharingTrigger = 1;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 2:
                m.limitSharingTrigger = 2;
                break;
            case "UNKNOWN_GROUP":
            case 3:
                m.limitSharingTrigger = 3;
                break;
            default:
                if (typeof d.limitSharingTrigger === "number" && (d.limitSharingTrigger | 0) === d.limitSharingTrigger)
                    m.limitSharingTrigger = d.limitSharingTrigger;
            }
            if (d.limitSharingInitiatedByMe != null) {
                m.limitSharingInitiatedByMe = $Boolean(d.limitSharingInitiatedByMe);
            }
            if (d.maibaAiThreadEnabled != null) {
                m.maibaAiThreadEnabled = $Boolean(d.maibaAiThreadEnabled);
            }
            if (d.isMarketingMessageThread != null) {
                m.isMarketingMessageThread = $Boolean(d.isMarketingMessageThread);
            }
            if (d.isSenderNewAccount != null) {
                m.isSenderNewAccount = $Boolean(d.isSenderNewAccount);
            }
            if (d.afterReadDuration != null) {
                m.afterReadDuration = d.afterReadDuration >>> 0;
            }
            if (d.isSenderSuspicious != null) {
                m.isSenderSuspicious = $Boolean(d.isSenderSuspicious);
            }
            switch (d.appealStatus) {
            case "NO_APPEAL":
            case 0:
                m.appealStatus = 0;
                break;
            case "APPEAL_IN_REVIEW":
            case 1:
                m.appealStatus = 1;
                break;
            case "APPEAL_APPROVED":
            case 2:
                m.appealStatus = 2;
                break;
            case "APPEAL_REJECTED":
            case 3:
                m.appealStatus = 3;
                break;
            default:
                if (typeof d.appealStatus === "number" && (d.appealStatus | 0) === d.appealStatus)
                    m.appealStatus = d.appealStatus;
            }
            if (d.appealUpdateTime != null) {
                if ($util.Long)
                    m.appealUpdateTime = $util.Long.fromValue(d.appealUpdateTime, true);
                else if (typeof d.appealUpdateTime === "string")
                    m.appealUpdateTime = $parseInt(d.appealUpdateTime, 10);
                else if (typeof d.appealUpdateTime === "number")
                    m.appealUpdateTime = d.appealUpdateTime;
                else if (typeof d.appealUpdateTime === "object")
                    m.appealUpdateTime = new $util.LongBits(d.appealUpdateTime.low >>> 0, d.appealUpdateTime.high >>> 0).toNumber(true);
            }
            if (d.authAgentParentCompanyName != null) {
                m.authAgentParentCompanyName = $String(d.authAgentParentCompanyName);
            }
            if (d.authAgentObaPhoneNumber != null) {
                m.authAgentObaPhoneNumber = $String(d.authAgentObaPhoneNumber);
            }
            if (d.identityVerification != null) {
                if (!$util.isObject(d.identityVerification))
                    throw $TypeError(".proto.Conversation.identityVerification: object expected");
                m.identityVerification = $root.proto.IdentityVerificationState.fromObject(d.identityVerification, q + 1);
            }
            return m;
        };

        Conversation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.messages = [];
                d.participant = [];
            }
            if (o.defaults) {
                d.id = "";
            }
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.messages && m.messages.length) {
                d.messages = $Array(m.messages.length);
                for (var j = 0; j < m.messages.length; ++j) {
                    d.messages[j] = $root.proto.HistorySyncMsg.toObject(m.messages[j], o, q + 1);
                }
            }
            if (m.newJid != null && $Object.hasOwnProperty.call(m, "newJid")) {
                d.newJid = m.newJid;
            }
            if (m.oldJid != null && $Object.hasOwnProperty.call(m, "oldJid")) {
                d.oldJid = m.oldJid;
            }
            if (m.lastMsgTimestamp != null && $Object.hasOwnProperty.call(m, "lastMsgTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.lastMsgTimestamp = typeof m.lastMsgTimestamp === "number" ? $BigInt(m.lastMsgTimestamp) : $util.Long.fromBits(m.lastMsgTimestamp.low >>> 0, m.lastMsgTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.lastMsgTimestamp === "number")
                    d.lastMsgTimestamp = o.longs === $String ? $String(m.lastMsgTimestamp) : m.lastMsgTimestamp;
                else
                    d.lastMsgTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.lastMsgTimestamp) : o.longs === $Number ? new $util.LongBits(m.lastMsgTimestamp.low >>> 0, m.lastMsgTimestamp.high >>> 0).toNumber(true) : m.lastMsgTimestamp;
            }
            if (m.unreadCount != null && $Object.hasOwnProperty.call(m, "unreadCount")) {
                d.unreadCount = m.unreadCount;
            }
            if (m.readOnly != null && $Object.hasOwnProperty.call(m, "readOnly")) {
                d.readOnly = m.readOnly;
            }
            if (m.endOfHistoryTransfer != null && $Object.hasOwnProperty.call(m, "endOfHistoryTransfer")) {
                d.endOfHistoryTransfer = m.endOfHistoryTransfer;
            }
            if (m.ephemeralExpiration != null && $Object.hasOwnProperty.call(m, "ephemeralExpiration")) {
                d.ephemeralExpiration = m.ephemeralExpiration;
            }
            if (m.ephemeralSettingTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.ephemeralSettingTimestamp = typeof m.ephemeralSettingTimestamp === "number" ? $BigInt(m.ephemeralSettingTimestamp) : $util.Long.fromBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.ephemeralSettingTimestamp === "number")
                    d.ephemeralSettingTimestamp = o.longs === $String ? $String(m.ephemeralSettingTimestamp) : m.ephemeralSettingTimestamp;
                else
                    d.ephemeralSettingTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.ephemeralSettingTimestamp) : o.longs === $Number ? new $util.LongBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0).toNumber() : m.ephemeralSettingTimestamp;
            }
            if (m.endOfHistoryTransferType != null && $Object.hasOwnProperty.call(m, "endOfHistoryTransferType")) {
                d.endOfHistoryTransferType = o.enums === $String ? $root.proto.Conversation.EndOfHistoryTransferType[m.endOfHistoryTransferType] === $undefined ? m.endOfHistoryTransferType : $root.proto.Conversation.EndOfHistoryTransferType[m.endOfHistoryTransferType] : m.endOfHistoryTransferType;
            }
            if (m.conversationTimestamp != null && $Object.hasOwnProperty.call(m, "conversationTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.conversationTimestamp = typeof m.conversationTimestamp === "number" ? $BigInt(m.conversationTimestamp) : $util.Long.fromBits(m.conversationTimestamp.low >>> 0, m.conversationTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.conversationTimestamp === "number")
                    d.conversationTimestamp = o.longs === $String ? $String(m.conversationTimestamp) : m.conversationTimestamp;
                else
                    d.conversationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.conversationTimestamp) : o.longs === $Number ? new $util.LongBits(m.conversationTimestamp.low >>> 0, m.conversationTimestamp.high >>> 0).toNumber(true) : m.conversationTimestamp;
            }
            if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                d.name = m.name;
            }
            if (m.pHash != null && $Object.hasOwnProperty.call(m, "pHash")) {
                d.pHash = m.pHash;
            }
            if (m.notSpam != null && $Object.hasOwnProperty.call(m, "notSpam")) {
                d.notSpam = m.notSpam;
            }
            if (m.archived != null && $Object.hasOwnProperty.call(m, "archived")) {
                d.archived = m.archived;
            }
            if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode")) {
                d.disappearingMode = $root.proto.DisappearingMode.toObject(m.disappearingMode, o, q + 1);
            }
            if (m.unreadMentionCount != null && $Object.hasOwnProperty.call(m, "unreadMentionCount")) {
                d.unreadMentionCount = m.unreadMentionCount;
            }
            if (m.markedAsUnread != null && $Object.hasOwnProperty.call(m, "markedAsUnread")) {
                d.markedAsUnread = m.markedAsUnread;
            }
            if (m.participant && m.participant.length) {
                d.participant = $Array(m.participant.length);
                for (var j = 0; j < m.participant.length; ++j) {
                    d.participant[j] = $root.proto.GroupParticipant.toObject(m.participant[j], o, q + 1);
                }
            }
            if (m.tcToken != null && $Object.hasOwnProperty.call(m, "tcToken")) {
                d.tcToken = o.bytes === $String ? $util.base64.encode(m.tcToken, 0, m.tcToken.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.tcToken) : m.tcToken;
            }
            if (m.tcTokenTimestamp != null && $Object.hasOwnProperty.call(m, "tcTokenTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.tcTokenTimestamp = typeof m.tcTokenTimestamp === "number" ? $BigInt(m.tcTokenTimestamp) : $util.Long.fromBits(m.tcTokenTimestamp.low >>> 0, m.tcTokenTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.tcTokenTimestamp === "number")
                    d.tcTokenTimestamp = o.longs === $String ? $String(m.tcTokenTimestamp) : m.tcTokenTimestamp;
                else
                    d.tcTokenTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.tcTokenTimestamp) : o.longs === $Number ? new $util.LongBits(m.tcTokenTimestamp.low >>> 0, m.tcTokenTimestamp.high >>> 0).toNumber(true) : m.tcTokenTimestamp;
            }
            if (m.contactPrimaryIdentityKey != null && $Object.hasOwnProperty.call(m, "contactPrimaryIdentityKey")) {
                d.contactPrimaryIdentityKey = o.bytes === $String ? $util.base64.encode(m.contactPrimaryIdentityKey, 0, m.contactPrimaryIdentityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.contactPrimaryIdentityKey) : m.contactPrimaryIdentityKey;
            }
            if (m.pinned != null && $Object.hasOwnProperty.call(m, "pinned")) {
                d.pinned = m.pinned;
            }
            if (m.muteEndTime != null && $Object.hasOwnProperty.call(m, "muteEndTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.muteEndTime = typeof m.muteEndTime === "number" ? $BigInt(m.muteEndTime) : $util.Long.fromBits(m.muteEndTime.low >>> 0, m.muteEndTime.high >>> 0, true).toBigInt();
                else if (typeof m.muteEndTime === "number")
                    d.muteEndTime = o.longs === $String ? $String(m.muteEndTime) : m.muteEndTime;
                else
                    d.muteEndTime = o.longs === $String ? $util.Long.prototype.toString.call(m.muteEndTime) : o.longs === $Number ? new $util.LongBits(m.muteEndTime.low >>> 0, m.muteEndTime.high >>> 0).toNumber(true) : m.muteEndTime;
            }
            if (m.wallpaper != null && $Object.hasOwnProperty.call(m, "wallpaper")) {
                d.wallpaper = $root.proto.WallpaperSettings.toObject(m.wallpaper, o, q + 1);
            }
            if (m.mediaVisibility != null && $Object.hasOwnProperty.call(m, "mediaVisibility")) {
                d.mediaVisibility = o.enums === $String ? $root.proto.MediaVisibility[m.mediaVisibility] === $undefined ? m.mediaVisibility : $root.proto.MediaVisibility[m.mediaVisibility] : m.mediaVisibility;
            }
            if (m.tcTokenSenderTimestamp != null && $Object.hasOwnProperty.call(m, "tcTokenSenderTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.tcTokenSenderTimestamp = typeof m.tcTokenSenderTimestamp === "number" ? $BigInt(m.tcTokenSenderTimestamp) : $util.Long.fromBits(m.tcTokenSenderTimestamp.low >>> 0, m.tcTokenSenderTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.tcTokenSenderTimestamp === "number")
                    d.tcTokenSenderTimestamp = o.longs === $String ? $String(m.tcTokenSenderTimestamp) : m.tcTokenSenderTimestamp;
                else
                    d.tcTokenSenderTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.tcTokenSenderTimestamp) : o.longs === $Number ? new $util.LongBits(m.tcTokenSenderTimestamp.low >>> 0, m.tcTokenSenderTimestamp.high >>> 0).toNumber(true) : m.tcTokenSenderTimestamp;
            }
            if (m.suspended != null && $Object.hasOwnProperty.call(m, "suspended")) {
                d.suspended = m.suspended;
            }
            if (m.terminated != null && $Object.hasOwnProperty.call(m, "terminated")) {
                d.terminated = m.terminated;
            }
            if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.createdAt = typeof m.createdAt === "number" ? $BigInt(m.createdAt) : $util.Long.fromBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0, true).toBigInt();
                else if (typeof m.createdAt === "number")
                    d.createdAt = o.longs === $String ? $String(m.createdAt) : m.createdAt;
                else
                    d.createdAt = o.longs === $String ? $util.Long.prototype.toString.call(m.createdAt) : o.longs === $Number ? new $util.LongBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0).toNumber(true) : m.createdAt;
            }
            if (m.createdBy != null && $Object.hasOwnProperty.call(m, "createdBy")) {
                d.createdBy = m.createdBy;
            }
            if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                d.description = m.description;
            }
            if (m.support != null && $Object.hasOwnProperty.call(m, "support")) {
                d.support = m.support;
            }
            if (m.isParentGroup != null && $Object.hasOwnProperty.call(m, "isParentGroup")) {
                d.isParentGroup = m.isParentGroup;
            }
            if (m.isDefaultSubgroup != null && $Object.hasOwnProperty.call(m, "isDefaultSubgroup")) {
                d.isDefaultSubgroup = m.isDefaultSubgroup;
            }
            if (m.parentGroupId != null && $Object.hasOwnProperty.call(m, "parentGroupId")) {
                d.parentGroupId = m.parentGroupId;
            }
            if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName")) {
                d.displayName = m.displayName;
            }
            if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid")) {
                d.pnJid = m.pnJid;
            }
            if (m.shareOwnPn != null && $Object.hasOwnProperty.call(m, "shareOwnPn")) {
                d.shareOwnPn = m.shareOwnPn;
            }
            if (m.pnhDuplicateLidThread != null && $Object.hasOwnProperty.call(m, "pnhDuplicateLidThread")) {
                d.pnhDuplicateLidThread = m.pnhDuplicateLidThread;
            }
            if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid")) {
                d.lidJid = m.lidJid;
            }
            if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                d.username = m.username;
            }
            if (m.lidOriginType != null && $Object.hasOwnProperty.call(m, "lidOriginType")) {
                d.lidOriginType = m.lidOriginType;
            }
            if (m.commentsCount != null && $Object.hasOwnProperty.call(m, "commentsCount")) {
                d.commentsCount = m.commentsCount;
            }
            if (m.locked != null && $Object.hasOwnProperty.call(m, "locked")) {
                d.locked = m.locked;
            }
            if (m.systemMessageToInsert != null && $Object.hasOwnProperty.call(m, "systemMessageToInsert")) {
                d.systemMessageToInsert = o.enums === $String ? $root.proto.PrivacySystemMessage[m.systemMessageToInsert] === $undefined ? m.systemMessageToInsert : $root.proto.PrivacySystemMessage[m.systemMessageToInsert] : m.systemMessageToInsert;
            }
            if (m.capiCreatedGroup != null && $Object.hasOwnProperty.call(m, "capiCreatedGroup")) {
                d.capiCreatedGroup = m.capiCreatedGroup;
            }
            if (m.accountLid != null && $Object.hasOwnProperty.call(m, "accountLid")) {
                d.accountLid = m.accountLid;
            }
            if (m.limitSharing != null && $Object.hasOwnProperty.call(m, "limitSharing")) {
                d.limitSharing = m.limitSharing;
            }
            if (m.limitSharingSettingTimestamp != null && $Object.hasOwnProperty.call(m, "limitSharingSettingTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.limitSharingSettingTimestamp = typeof m.limitSharingSettingTimestamp === "number" ? $BigInt(m.limitSharingSettingTimestamp) : $util.Long.fromBits(m.limitSharingSettingTimestamp.low >>> 0, m.limitSharingSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.limitSharingSettingTimestamp === "number")
                    d.limitSharingSettingTimestamp = o.longs === $String ? $String(m.limitSharingSettingTimestamp) : m.limitSharingSettingTimestamp;
                else
                    d.limitSharingSettingTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.limitSharingSettingTimestamp) : o.longs === $Number ? new $util.LongBits(m.limitSharingSettingTimestamp.low >>> 0, m.limitSharingSettingTimestamp.high >>> 0).toNumber() : m.limitSharingSettingTimestamp;
            }
            if (m.limitSharingTrigger != null && $Object.hasOwnProperty.call(m, "limitSharingTrigger")) {
                d.limitSharingTrigger = o.enums === $String ? $root.proto.LimitSharing.TriggerType[m.limitSharingTrigger] === $undefined ? m.limitSharingTrigger : $root.proto.LimitSharing.TriggerType[m.limitSharingTrigger] : m.limitSharingTrigger;
            }
            if (m.limitSharingInitiatedByMe != null && $Object.hasOwnProperty.call(m, "limitSharingInitiatedByMe")) {
                d.limitSharingInitiatedByMe = m.limitSharingInitiatedByMe;
            }
            if (m.maibaAiThreadEnabled != null && $Object.hasOwnProperty.call(m, "maibaAiThreadEnabled")) {
                d.maibaAiThreadEnabled = m.maibaAiThreadEnabled;
            }
            if (m.isMarketingMessageThread != null && $Object.hasOwnProperty.call(m, "isMarketingMessageThread")) {
                d.isMarketingMessageThread = m.isMarketingMessageThread;
            }
            if (m.isSenderNewAccount != null && $Object.hasOwnProperty.call(m, "isSenderNewAccount")) {
                d.isSenderNewAccount = m.isSenderNewAccount;
            }
            if (m.afterReadDuration != null && $Object.hasOwnProperty.call(m, "afterReadDuration")) {
                d.afterReadDuration = m.afterReadDuration;
            }
            if (m.isSenderSuspicious != null && $Object.hasOwnProperty.call(m, "isSenderSuspicious")) {
                d.isSenderSuspicious = m.isSenderSuspicious;
            }
            if (m.appealStatus != null && $Object.hasOwnProperty.call(m, "appealStatus")) {
                d.appealStatus = o.enums === $String ? $root.proto.Conversation.GroupAppealStatus[m.appealStatus] === $undefined ? m.appealStatus : $root.proto.Conversation.GroupAppealStatus[m.appealStatus] : m.appealStatus;
            }
            if (m.appealUpdateTime != null && $Object.hasOwnProperty.call(m, "appealUpdateTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.appealUpdateTime = typeof m.appealUpdateTime === "number" ? $BigInt(m.appealUpdateTime) : $util.Long.fromBits(m.appealUpdateTime.low >>> 0, m.appealUpdateTime.high >>> 0, true).toBigInt();
                else if (typeof m.appealUpdateTime === "number")
                    d.appealUpdateTime = o.longs === $String ? $String(m.appealUpdateTime) : m.appealUpdateTime;
                else
                    d.appealUpdateTime = o.longs === $String ? $util.Long.prototype.toString.call(m.appealUpdateTime) : o.longs === $Number ? new $util.LongBits(m.appealUpdateTime.low >>> 0, m.appealUpdateTime.high >>> 0).toNumber(true) : m.appealUpdateTime;
            }
            if (m.authAgentParentCompanyName != null && $Object.hasOwnProperty.call(m, "authAgentParentCompanyName")) {
                d.authAgentParentCompanyName = m.authAgentParentCompanyName;
            }
            if (m.authAgentObaPhoneNumber != null && $Object.hasOwnProperty.call(m, "authAgentObaPhoneNumber")) {
                d.authAgentObaPhoneNumber = m.authAgentObaPhoneNumber;
            }
            if (m.identityVerification != null && $Object.hasOwnProperty.call(m, "identityVerification")) {
                d.identityVerification = $root.proto.IdentityVerificationState.toObject(m.identityVerification, o, q + 1);
            }
            return d;
        };

        Conversation.prototype.toJSON = function() {
            return Conversation.toObject(this, $protobuf.util.toJSONOptions);
        };

        Conversation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Conversation";
        };

        Conversation.EndOfHistoryTransferType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "COMPLETE_BUT_MORE_MESSAGES_REMAIN_ON_PRIMARY"] = 0;
            values[valuesById[1] = "COMPLETE_AND_NO_MORE_MESSAGE_REMAIN_ON_PRIMARY"] = 1;
            values[valuesById[2] = "COMPLETE_ON_DEMAND_SYNC_BUT_MORE_MSG_REMAIN_ON_PRIMARY"] = 2;
            values[valuesById[3] = "COMPLETE_ON_DEMAND_SYNC_WITH_MORE_MSG_ON_PRIMARY_BUT_NO_ACCESS"] = 3;
            return values;
        })();

        Conversation.GroupAppealStatus = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NO_APPEAL"] = 0;
            values[valuesById[1] = "APPEAL_IN_REVIEW"] = 1;
            values[valuesById[2] = "APPEAL_APPROVED"] = 2;
            values[valuesById[3] = "APPEAL_REJECTED"] = 3;
            return values;
        })();

        return Conversation;
    })();

    proto.CreateBackupInput = (function() {

        const CreateBackupInput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CreateBackupInput.prototype.recoveryCode = "";
        CreateBackupInput.prototype.userId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;

        CreateBackupInput.create = function(properties) {
            return new CreateBackupInput(properties);
        };

        CreateBackupInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).string(m.recoveryCode);
            w.uint32(16).uint64(m.userId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CreateBackupInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CreateBackupInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.recoveryCode = r.stringVerify();
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.userId = r.uint64();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "recoveryCode"))
                throw $util.ProtocolError("missing required 'recoveryCode'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "userId"))
                throw $util.ProtocolError("missing required 'userId'", { instance: m });
            return m;
        };

        CreateBackupInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.CreateBackupInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CreateBackupInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CreateBackupInput();
            if (d.recoveryCode != null) {
                m.recoveryCode = $String(d.recoveryCode);
            }
            if (d.userId != null) {
                if ($util.Long)
                    m.userId = $util.Long.fromValue(d.userId, true);
                else if (typeof d.userId === "string")
                    m.userId = $parseInt(d.userId, 10);
                else if (typeof d.userId === "number")
                    m.userId = d.userId;
                else if (typeof d.userId === "object")
                    m.userId = new $util.LongBits(d.userId.low >>> 0, d.userId.high >>> 0).toNumber(true);
            }
            return m;
        };

        CreateBackupInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                d.recoveryCode = "";
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.userId = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.userId = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (m.recoveryCode != null && $Object.hasOwnProperty.call(m, "recoveryCode")) {
                d.recoveryCode = m.recoveryCode;
            }
            if (m.userId != null && $Object.hasOwnProperty.call(m, "userId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.userId = typeof m.userId === "number" ? $BigInt(m.userId) : $util.Long.fromBits(m.userId.low >>> 0, m.userId.high >>> 0, true).toBigInt();
                else if (typeof m.userId === "number")
                    d.userId = o.longs === $String ? $String(m.userId) : m.userId;
                else
                    d.userId = o.longs === $String ? $util.Long.prototype.toString.call(m.userId) : o.longs === $Number ? new $util.LongBits(m.userId.low >>> 0, m.userId.high >>> 0).toNumber(true) : m.userId;
            }
            return d;
        };

        CreateBackupInput.prototype.toJSON = function() {
            return CreateBackupInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        CreateBackupInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CreateBackupInput";
        };

        return CreateBackupInput;
    })();

    proto.CreateBackupOutput = (function() {

        const CreateBackupOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        CreateBackupOutput.prototype.device = null;
        CreateBackupOutput.prototype.virtualDevice = null;
        CreateBackupOutput.prototype.epoch0 = null;
        CreateBackupOutput.prototype.mailboxRootKey = null;
        CreateBackupOutput.prototype.error = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CreateBackupOutput.prototype, "_device", {
            get: $util.oneOfGetter($oneOfFields = ["device"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CreateBackupOutput.prototype, "_virtualDevice", {
            get: $util.oneOfGetter($oneOfFields = ["virtualDevice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CreateBackupOutput.prototype, "_epoch0", {
            get: $util.oneOfGetter($oneOfFields = ["epoch0"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CreateBackupOutput.prototype, "_mailboxRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["mailboxRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(CreateBackupOutput.prototype, "_error", {
            get: $util.oneOfGetter($oneOfFields = ["error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        CreateBackupOutput.create = function(properties) {
            return new CreateBackupOutput(properties);
        };

        CreateBackupOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.device != null && $Object.hasOwnProperty.call(m, "device"))
                $root.proto.DeviceOutput.encode(m.device, w.uint32(10).fork(), q + 1).ldelim();
            if (m.virtualDevice != null && $Object.hasOwnProperty.call(m, "virtualDevice"))
                $root.proto.VirtualDeviceOutput.encode(m.virtualDevice, w.uint32(18).fork(), q + 1).ldelim();
            if (m.epoch0 != null && $Object.hasOwnProperty.call(m, "epoch0"))
                $root.proto.Epoch0Output.encode(m.epoch0, w.uint32(26).fork(), q + 1).ldelim();
            if (m.mailboxRootKey != null && $Object.hasOwnProperty.call(m, "mailboxRootKey"))
                w.uint32(34).bytes(m.mailboxRootKey);
            if (m.error != null && $Object.hasOwnProperty.call(m, "error"))
                w.uint32(42).string(m.error);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        CreateBackupOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.CreateBackupOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.device = $root.proto.DeviceOutput.decode(r, r.uint32(), $undefined, q + 1, m.device);
                        m._device = "device";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.virtualDevice = $root.proto.VirtualDeviceOutput.decode(r, r.uint32(), $undefined, q + 1, m.virtualDevice);
                        m._virtualDevice = "virtualDevice";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.epoch0 = $root.proto.Epoch0Output.decode(r, r.uint32(), $undefined, q + 1, m.epoch0);
                        m._epoch0 = "epoch0";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.mailboxRootKey = r.bytes();
                        m._mailboxRootKey = "mailboxRootKey";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.error = r.stringVerify();
                        m._error = "error";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        CreateBackupOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.CreateBackupOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.CreateBackupOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.CreateBackupOutput();
            if (d.device != null) {
                if (!$util.isObject(d.device))
                    throw $TypeError(".proto.CreateBackupOutput.device: object expected");
                m.device = $root.proto.DeviceOutput.fromObject(d.device, q + 1);
            }
            if (d.virtualDevice != null) {
                if (!$util.isObject(d.virtualDevice))
                    throw $TypeError(".proto.CreateBackupOutput.virtualDevice: object expected");
                m.virtualDevice = $root.proto.VirtualDeviceOutput.fromObject(d.virtualDevice, q + 1);
            }
            if (d.epoch0 != null) {
                if (!$util.isObject(d.epoch0))
                    throw $TypeError(".proto.CreateBackupOutput.epoch0: object expected");
                m.epoch0 = $root.proto.Epoch0Output.fromObject(d.epoch0, q + 1);
            }
            if (d.mailboxRootKey != null) {
                if (typeof d.mailboxRootKey === "string")
                    $util.base64.decode(d.mailboxRootKey, m.mailboxRootKey = $util.newBuffer($util.base64.length(d.mailboxRootKey)), 0);
                else if (d.mailboxRootKey.length >= 0)
                    m.mailboxRootKey = d.mailboxRootKey;
            }
            if (d.error != null) {
                m.error = $String(d.error);
            }
            return m;
        };

        CreateBackupOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.device != null && $Object.hasOwnProperty.call(m, "device")) {
                d.device = $root.proto.DeviceOutput.toObject(m.device, o, q + 1);
            }
            if (m.virtualDevice != null && $Object.hasOwnProperty.call(m, "virtualDevice")) {
                d.virtualDevice = $root.proto.VirtualDeviceOutput.toObject(m.virtualDevice, o, q + 1);
            }
            if (m.epoch0 != null && $Object.hasOwnProperty.call(m, "epoch0")) {
                d.epoch0 = $root.proto.Epoch0Output.toObject(m.epoch0, o, q + 1);
            }
            if (m.mailboxRootKey != null && $Object.hasOwnProperty.call(m, "mailboxRootKey")) {
                d.mailboxRootKey = o.bytes === $String ? $util.base64.encode(m.mailboxRootKey, 0, m.mailboxRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mailboxRootKey) : m.mailboxRootKey;
            }
            if (m.error != null && $Object.hasOwnProperty.call(m, "error")) {
                d.error = m.error;
            }
            return d;
        };

        CreateBackupOutput.prototype.toJSON = function() {
            return CreateBackupOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        CreateBackupOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.CreateBackupOutput";
        };

        return CreateBackupOutput;
    })();

    proto.DecryptMessageInput = (function() {

        const DecryptMessageInput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DecryptMessageInput.prototype.epochRootKey = $util.newBuffer([]);
        DecryptMessageInput.prototype.epochAnonId = $util.newBuffer([]);
        DecryptMessageInput.prototype.threadId = "";
        DecryptMessageInput.prototype.encryptionVersion = 0;
        DecryptMessageInput.prototype.ciphertext = $util.newBuffer([]);

        DecryptMessageInput.create = function(properties) {
            return new DecryptMessageInput(properties);
        };

        DecryptMessageInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.epochRootKey);
            w.uint32(18).bytes(m.epochAnonId);
            w.uint32(26).string(m.threadId);
            w.uint32(32).int32(m.encryptionVersion);
            w.uint32(42).bytes(m.ciphertext);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DecryptMessageInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DecryptMessageInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.epochRootKey = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.epochAnonId = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.threadId = r.stringVerify();
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.encryptionVersion = r.int32();
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.ciphertext = r.bytes();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "epochRootKey"))
                throw $util.ProtocolError("missing required 'epochRootKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochAnonId"))
                throw $util.ProtocolError("missing required 'epochAnonId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "threadId"))
                throw $util.ProtocolError("missing required 'threadId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptionVersion"))
                throw $util.ProtocolError("missing required 'encryptionVersion'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "ciphertext"))
                throw $util.ProtocolError("missing required 'ciphertext'", { instance: m });
            return m;
        };

        DecryptMessageInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.DecryptMessageInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DecryptMessageInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DecryptMessageInput();
            if (d.epochRootKey != null) {
                if (typeof d.epochRootKey === "string")
                    $util.base64.decode(d.epochRootKey, m.epochRootKey = $util.newBuffer($util.base64.length(d.epochRootKey)), 0);
                else if (d.epochRootKey.length >= 0)
                    m.epochRootKey = d.epochRootKey;
            }
            if (d.epochAnonId != null) {
                if (typeof d.epochAnonId === "string")
                    $util.base64.decode(d.epochAnonId, m.epochAnonId = $util.newBuffer($util.base64.length(d.epochAnonId)), 0);
                else if (d.epochAnonId.length >= 0)
                    m.epochAnonId = d.epochAnonId;
            }
            if (d.threadId != null) {
                m.threadId = $String(d.threadId);
            }
            if (d.encryptionVersion != null) {
                m.encryptionVersion = d.encryptionVersion | 0;
            }
            if (d.ciphertext != null) {
                if (typeof d.ciphertext === "string")
                    $util.base64.decode(d.ciphertext, m.ciphertext = $util.newBuffer($util.base64.length(d.ciphertext)), 0);
                else if (d.ciphertext.length >= 0)
                    m.ciphertext = d.ciphertext;
            }
            return m;
        };

        DecryptMessageInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if (o.bytes === $String)
                    d.epochRootKey = "";
                else {
                    d.epochRootKey = [];
                    if (o.bytes !== $Array)
                        d.epochRootKey = $util.newBuffer(d.epochRootKey);
                }
                if (o.bytes === $String)
                    d.epochAnonId = "";
                else {
                    d.epochAnonId = [];
                    if (o.bytes !== $Array)
                        d.epochAnonId = $util.newBuffer(d.epochAnonId);
                }
                d.threadId = "";
                d.encryptionVersion = 0;
                if (o.bytes === $String)
                    d.ciphertext = "";
                else {
                    d.ciphertext = [];
                    if (o.bytes !== $Array)
                        d.ciphertext = $util.newBuffer(d.ciphertext);
                }
            }
            if (m.epochRootKey != null && $Object.hasOwnProperty.call(m, "epochRootKey")) {
                d.epochRootKey = o.bytes === $String ? $util.base64.encode(m.epochRootKey, 0, m.epochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochRootKey) : m.epochRootKey;
            }
            if (m.epochAnonId != null && $Object.hasOwnProperty.call(m, "epochAnonId")) {
                d.epochAnonId = o.bytes === $String ? $util.base64.encode(m.epochAnonId, 0, m.epochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAnonId) : m.epochAnonId;
            }
            if (m.threadId != null && $Object.hasOwnProperty.call(m, "threadId")) {
                d.threadId = m.threadId;
            }
            if (m.encryptionVersion != null && $Object.hasOwnProperty.call(m, "encryptionVersion")) {
                d.encryptionVersion = m.encryptionVersion;
            }
            if (m.ciphertext != null && $Object.hasOwnProperty.call(m, "ciphertext")) {
                d.ciphertext = o.bytes === $String ? $util.base64.encode(m.ciphertext, 0, m.ciphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ciphertext) : m.ciphertext;
            }
            return d;
        };

        DecryptMessageInput.prototype.toJSON = function() {
            return DecryptMessageInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        DecryptMessageInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DecryptMessageInput";
        };

        return DecryptMessageInput;
    })();

    proto.DecryptMessageOutput = (function() {

        const DecryptMessageOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DecryptMessageOutput.prototype.plaintextPayload = null;
        DecryptMessageOutput.prototype.error = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DecryptMessageOutput.prototype, "_plaintextPayload", {
            get: $util.oneOfGetter($oneOfFields = ["plaintextPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DecryptMessageOutput.prototype, "_error", {
            get: $util.oneOfGetter($oneOfFields = ["error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DecryptMessageOutput.create = function(properties) {
            return new DecryptMessageOutput(properties);
        };

        DecryptMessageOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.plaintextPayload != null && $Object.hasOwnProperty.call(m, "plaintextPayload"))
                w.uint32(10).bytes(m.plaintextPayload);
            if (m.error != null && $Object.hasOwnProperty.call(m, "error"))
                w.uint32(18).string(m.error);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DecryptMessageOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DecryptMessageOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.plaintextPayload = r.bytes();
                        m._plaintextPayload = "plaintextPayload";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.error = r.stringVerify();
                        m._error = "error";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DecryptMessageOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.DecryptMessageOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DecryptMessageOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DecryptMessageOutput();
            if (d.plaintextPayload != null) {
                if (typeof d.plaintextPayload === "string")
                    $util.base64.decode(d.plaintextPayload, m.plaintextPayload = $util.newBuffer($util.base64.length(d.plaintextPayload)), 0);
                else if (d.plaintextPayload.length >= 0)
                    m.plaintextPayload = d.plaintextPayload;
            }
            if (d.error != null) {
                m.error = $String(d.error);
            }
            return m;
        };

        DecryptMessageOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.plaintextPayload != null && $Object.hasOwnProperty.call(m, "plaintextPayload")) {
                d.plaintextPayload = o.bytes === $String ? $util.base64.encode(m.plaintextPayload, 0, m.plaintextPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.plaintextPayload) : m.plaintextPayload;
            }
            if (m.error != null && $Object.hasOwnProperty.call(m, "error")) {
                d.error = m.error;
            }
            return d;
        };

        DecryptMessageOutput.prototype.toJSON = function() {
            return DecryptMessageOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        DecryptMessageOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DecryptMessageOutput";
        };

        return DecryptMessageOutput;
    })();

    proto.DeriveMessageKeyInput = (function() {

        const DeriveMessageKeyInput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeriveMessageKeyInput.prototype.epochRootKey = $util.newBuffer([]);
        DeriveMessageKeyInput.prototype.epochAnonId = $util.newBuffer([]);
        DeriveMessageKeyInput.prototype.threadId = "";

        DeriveMessageKeyInput.create = function(properties) {
            return new DeriveMessageKeyInput(properties);
        };

        DeriveMessageKeyInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.epochRootKey);
            w.uint32(18).bytes(m.epochAnonId);
            w.uint32(26).string(m.threadId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeriveMessageKeyInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeriveMessageKeyInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.epochRootKey = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.epochAnonId = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.threadId = r.stringVerify();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "epochRootKey"))
                throw $util.ProtocolError("missing required 'epochRootKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochAnonId"))
                throw $util.ProtocolError("missing required 'epochAnonId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "threadId"))
                throw $util.ProtocolError("missing required 'threadId'", { instance: m });
            return m;
        };

        DeriveMessageKeyInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeriveMessageKeyInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeriveMessageKeyInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeriveMessageKeyInput();
            if (d.epochRootKey != null) {
                if (typeof d.epochRootKey === "string")
                    $util.base64.decode(d.epochRootKey, m.epochRootKey = $util.newBuffer($util.base64.length(d.epochRootKey)), 0);
                else if (d.epochRootKey.length >= 0)
                    m.epochRootKey = d.epochRootKey;
            }
            if (d.epochAnonId != null) {
                if (typeof d.epochAnonId === "string")
                    $util.base64.decode(d.epochAnonId, m.epochAnonId = $util.newBuffer($util.base64.length(d.epochAnonId)), 0);
                else if (d.epochAnonId.length >= 0)
                    m.epochAnonId = d.epochAnonId;
            }
            if (d.threadId != null) {
                m.threadId = $String(d.threadId);
            }
            return m;
        };

        DeriveMessageKeyInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if (o.bytes === $String)
                    d.epochRootKey = "";
                else {
                    d.epochRootKey = [];
                    if (o.bytes !== $Array)
                        d.epochRootKey = $util.newBuffer(d.epochRootKey);
                }
                if (o.bytes === $String)
                    d.epochAnonId = "";
                else {
                    d.epochAnonId = [];
                    if (o.bytes !== $Array)
                        d.epochAnonId = $util.newBuffer(d.epochAnonId);
                }
                d.threadId = "";
            }
            if (m.epochRootKey != null && $Object.hasOwnProperty.call(m, "epochRootKey")) {
                d.epochRootKey = o.bytes === $String ? $util.base64.encode(m.epochRootKey, 0, m.epochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochRootKey) : m.epochRootKey;
            }
            if (m.epochAnonId != null && $Object.hasOwnProperty.call(m, "epochAnonId")) {
                d.epochAnonId = o.bytes === $String ? $util.base64.encode(m.epochAnonId, 0, m.epochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAnonId) : m.epochAnonId;
            }
            if (m.threadId != null && $Object.hasOwnProperty.call(m, "threadId")) {
                d.threadId = m.threadId;
            }
            return d;
        };

        DeriveMessageKeyInput.prototype.toJSON = function() {
            return DeriveMessageKeyInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeriveMessageKeyInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeriveMessageKeyInput";
        };

        return DeriveMessageKeyInput;
    })();

    proto.DeriveMessageKeyOutput = (function() {

        const DeriveMessageKeyOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeriveMessageKeyOutput.prototype.messageKey = null;
        DeriveMessageKeyOutput.prototype.error = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeriveMessageKeyOutput.prototype, "_messageKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeriveMessageKeyOutput.prototype, "_error", {
            get: $util.oneOfGetter($oneOfFields = ["error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeriveMessageKeyOutput.create = function(properties) {
            return new DeriveMessageKeyOutput(properties);
        };

        DeriveMessageKeyOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey"))
                w.uint32(10).bytes(m.messageKey);
            if (m.error != null && $Object.hasOwnProperty.call(m, "error"))
                w.uint32(18).string(m.error);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeriveMessageKeyOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeriveMessageKeyOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.messageKey = r.bytes();
                        m._messageKey = "messageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.error = r.stringVerify();
                        m._error = "error";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DeriveMessageKeyOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeriveMessageKeyOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeriveMessageKeyOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeriveMessageKeyOutput();
            if (d.messageKey != null) {
                if (typeof d.messageKey === "string")
                    $util.base64.decode(d.messageKey, m.messageKey = $util.newBuffer($util.base64.length(d.messageKey)), 0);
                else if (d.messageKey.length >= 0)
                    m.messageKey = d.messageKey;
            }
            if (d.error != null) {
                m.error = $String(d.error);
            }
            return m;
        };

        DeriveMessageKeyOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey")) {
                d.messageKey = o.bytes === $String ? $util.base64.encode(m.messageKey, 0, m.messageKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageKey) : m.messageKey;
            }
            if (m.error != null && $Object.hasOwnProperty.call(m, "error")) {
                d.error = m.error;
            }
            return d;
        };

        DeriveMessageKeyOutput.prototype.toJSON = function() {
            return DeriveMessageKeyOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeriveMessageKeyOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeriveMessageKeyOutput";
        };

        return DeriveMessageKeyOutput;
    })();

    proto.DeviceCapabilities = (function() {

        const DeviceCapabilities = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeviceCapabilities.prototype.chatLockSupportLevel = null;
        DeviceCapabilities.prototype.lidMigration = null;
        DeviceCapabilities.prototype.businessBroadcast = null;
        DeviceCapabilities.prototype.userHasAvatar = null;
        DeviceCapabilities.prototype.memberNameTagPrimarySupport = null;
        DeviceCapabilities.prototype.aiThread = null;
        DeviceCapabilities.prototype.aiFbidMigration = null;
        DeviceCapabilities.prototype.bizAiSettingsSync = null;
        DeviceCapabilities.prototype.contactRefresh = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_chatLockSupportLevel", {
            get: $util.oneOfGetter($oneOfFields = ["chatLockSupportLevel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_lidMigration", {
            get: $util.oneOfGetter($oneOfFields = ["lidMigration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_businessBroadcast", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_userHasAvatar", {
            get: $util.oneOfGetter($oneOfFields = ["userHasAvatar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_memberNameTagPrimarySupport", {
            get: $util.oneOfGetter($oneOfFields = ["memberNameTagPrimarySupport"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_aiThread", {
            get: $util.oneOfGetter($oneOfFields = ["aiThread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_aiFbidMigration", {
            get: $util.oneOfGetter($oneOfFields = ["aiFbidMigration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_bizAiSettingsSync", {
            get: $util.oneOfGetter($oneOfFields = ["bizAiSettingsSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceCapabilities.prototype, "_contactRefresh", {
            get: $util.oneOfGetter($oneOfFields = ["contactRefresh"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceCapabilities.create = function(properties) {
            return new DeviceCapabilities(properties);
        };

        DeviceCapabilities.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.chatLockSupportLevel != null && $Object.hasOwnProperty.call(m, "chatLockSupportLevel"))
                w.uint32(8).int32(m.chatLockSupportLevel);
            if (m.lidMigration != null && $Object.hasOwnProperty.call(m, "lidMigration"))
                $root.proto.DeviceCapabilities.LIDMigration.encode(m.lidMigration, w.uint32(18).fork(), q + 1).ldelim();
            if (m.businessBroadcast != null && $Object.hasOwnProperty.call(m, "businessBroadcast"))
                $root.proto.DeviceCapabilities.BusinessBroadcast.encode(m.businessBroadcast, w.uint32(26).fork(), q + 1).ldelim();
            if (m.userHasAvatar != null && $Object.hasOwnProperty.call(m, "userHasAvatar"))
                $root.proto.DeviceCapabilities.UserHasAvatar.encode(m.userHasAvatar, w.uint32(34).fork(), q + 1).ldelim();
            if (m.memberNameTagPrimarySupport != null && $Object.hasOwnProperty.call(m, "memberNameTagPrimarySupport"))
                w.uint32(40).int32(m.memberNameTagPrimarySupport);
            if (m.aiThread != null && $Object.hasOwnProperty.call(m, "aiThread"))
                $root.proto.DeviceCapabilities.AiThread.encode(m.aiThread, w.uint32(50).fork(), q + 1).ldelim();
            if (m.aiFbidMigration != null && $Object.hasOwnProperty.call(m, "aiFbidMigration"))
                $root.proto.DeviceCapabilities.AiFbidMigration.encode(m.aiFbidMigration, w.uint32(58).fork(), q + 1).ldelim();
            if (m.bizAiSettingsSync != null && $Object.hasOwnProperty.call(m, "bizAiSettingsSync"))
                $root.proto.DeviceCapabilities.BizAiSettingsSync.encode(m.bizAiSettingsSync, w.uint32(66).fork(), q + 1).ldelim();
            if (m.contactRefresh != null && $Object.hasOwnProperty.call(m, "contactRefresh"))
                $root.proto.DeviceCapabilities.ContactRefresh.encode(m.contactRefresh, w.uint32(74).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeviceCapabilities.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.chatLockSupportLevel = r.int32();
                        m._chatLockSupportLevel = "chatLockSupportLevel";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.lidMigration = $root.proto.DeviceCapabilities.LIDMigration.decode(r, r.uint32(), $undefined, q + 1, m.lidMigration);
                        m._lidMigration = "lidMigration";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.businessBroadcast = $root.proto.DeviceCapabilities.BusinessBroadcast.decode(r, r.uint32(), $undefined, q + 1, m.businessBroadcast);
                        m._businessBroadcast = "businessBroadcast";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.userHasAvatar = $root.proto.DeviceCapabilities.UserHasAvatar.decode(r, r.uint32(), $undefined, q + 1, m.userHasAvatar);
                        m._userHasAvatar = "userHasAvatar";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.memberNameTagPrimarySupport = r.int32();
                        m._memberNameTagPrimarySupport = "memberNameTagPrimarySupport";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.aiThread = $root.proto.DeviceCapabilities.AiThread.decode(r, r.uint32(), $undefined, q + 1, m.aiThread);
                        m._aiThread = "aiThread";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.aiFbidMigration = $root.proto.DeviceCapabilities.AiFbidMigration.decode(r, r.uint32(), $undefined, q + 1, m.aiFbidMigration);
                        m._aiFbidMigration = "aiFbidMigration";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.bizAiSettingsSync = $root.proto.DeviceCapabilities.BizAiSettingsSync.decode(r, r.uint32(), $undefined, q + 1, m.bizAiSettingsSync);
                        m._bizAiSettingsSync = "bizAiSettingsSync";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.contactRefresh = $root.proto.DeviceCapabilities.ContactRefresh.decode(r, r.uint32(), $undefined, q + 1, m.contactRefresh);
                        m._contactRefresh = "contactRefresh";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DeviceCapabilities.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeviceCapabilities)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeviceCapabilities: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeviceCapabilities();
            switch (d.chatLockSupportLevel) {
            case "NONE":
            case 0:
                m.chatLockSupportLevel = 0;
                break;
            case "MINIMAL":
            case 1:
                m.chatLockSupportLevel = 1;
                break;
            case "FULL":
            case 2:
                m.chatLockSupportLevel = 2;
                break;
            default:
                if (typeof d.chatLockSupportLevel === "number" && (d.chatLockSupportLevel | 0) === d.chatLockSupportLevel)
                    m.chatLockSupportLevel = d.chatLockSupportLevel;
            }
            if (d.lidMigration != null) {
                if (!$util.isObject(d.lidMigration))
                    throw $TypeError(".proto.DeviceCapabilities.lidMigration: object expected");
                m.lidMigration = $root.proto.DeviceCapabilities.LIDMigration.fromObject(d.lidMigration, q + 1);
            }
            if (d.businessBroadcast != null) {
                if (!$util.isObject(d.businessBroadcast))
                    throw $TypeError(".proto.DeviceCapabilities.businessBroadcast: object expected");
                m.businessBroadcast = $root.proto.DeviceCapabilities.BusinessBroadcast.fromObject(d.businessBroadcast, q + 1);
            }
            if (d.userHasAvatar != null) {
                if (!$util.isObject(d.userHasAvatar))
                    throw $TypeError(".proto.DeviceCapabilities.userHasAvatar: object expected");
                m.userHasAvatar = $root.proto.DeviceCapabilities.UserHasAvatar.fromObject(d.userHasAvatar, q + 1);
            }
            switch (d.memberNameTagPrimarySupport) {
            case "DISABLED":
            case 0:
                m.memberNameTagPrimarySupport = 0;
                break;
            case "RECEIVER_ENABLED":
            case 1:
                m.memberNameTagPrimarySupport = 1;
                break;
            case "SENDER_ENABLED":
            case 2:
                m.memberNameTagPrimarySupport = 2;
                break;
            default:
                if (typeof d.memberNameTagPrimarySupport === "number" && (d.memberNameTagPrimarySupport | 0) === d.memberNameTagPrimarySupport)
                    m.memberNameTagPrimarySupport = d.memberNameTagPrimarySupport;
            }
            if (d.aiThread != null) {
                if (!$util.isObject(d.aiThread))
                    throw $TypeError(".proto.DeviceCapabilities.aiThread: object expected");
                m.aiThread = $root.proto.DeviceCapabilities.AiThread.fromObject(d.aiThread, q + 1);
            }
            if (d.aiFbidMigration != null) {
                if (!$util.isObject(d.aiFbidMigration))
                    throw $TypeError(".proto.DeviceCapabilities.aiFbidMigration: object expected");
                m.aiFbidMigration = $root.proto.DeviceCapabilities.AiFbidMigration.fromObject(d.aiFbidMigration, q + 1);
            }
            if (d.bizAiSettingsSync != null) {
                if (!$util.isObject(d.bizAiSettingsSync))
                    throw $TypeError(".proto.DeviceCapabilities.bizAiSettingsSync: object expected");
                m.bizAiSettingsSync = $root.proto.DeviceCapabilities.BizAiSettingsSync.fromObject(d.bizAiSettingsSync, q + 1);
            }
            if (d.contactRefresh != null) {
                if (!$util.isObject(d.contactRefresh))
                    throw $TypeError(".proto.DeviceCapabilities.contactRefresh: object expected");
                m.contactRefresh = $root.proto.DeviceCapabilities.ContactRefresh.fromObject(d.contactRefresh, q + 1);
            }
            return m;
        };

        DeviceCapabilities.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.chatLockSupportLevel != null && $Object.hasOwnProperty.call(m, "chatLockSupportLevel")) {
                d.chatLockSupportLevel = o.enums === $String ? $root.proto.DeviceCapabilities.ChatLockSupportLevel[m.chatLockSupportLevel] === $undefined ? m.chatLockSupportLevel : $root.proto.DeviceCapabilities.ChatLockSupportLevel[m.chatLockSupportLevel] : m.chatLockSupportLevel;
            }
            if (m.lidMigration != null && $Object.hasOwnProperty.call(m, "lidMigration")) {
                d.lidMigration = $root.proto.DeviceCapabilities.LIDMigration.toObject(m.lidMigration, o, q + 1);
            }
            if (m.businessBroadcast != null && $Object.hasOwnProperty.call(m, "businessBroadcast")) {
                d.businessBroadcast = $root.proto.DeviceCapabilities.BusinessBroadcast.toObject(m.businessBroadcast, o, q + 1);
            }
            if (m.userHasAvatar != null && $Object.hasOwnProperty.call(m, "userHasAvatar")) {
                d.userHasAvatar = $root.proto.DeviceCapabilities.UserHasAvatar.toObject(m.userHasAvatar, o, q + 1);
            }
            if (m.memberNameTagPrimarySupport != null && $Object.hasOwnProperty.call(m, "memberNameTagPrimarySupport")) {
                d.memberNameTagPrimarySupport = o.enums === $String ? $root.proto.DeviceCapabilities.MemberNameTagPrimarySupport[m.memberNameTagPrimarySupport] === $undefined ? m.memberNameTagPrimarySupport : $root.proto.DeviceCapabilities.MemberNameTagPrimarySupport[m.memberNameTagPrimarySupport] : m.memberNameTagPrimarySupport;
            }
            if (m.aiThread != null && $Object.hasOwnProperty.call(m, "aiThread")) {
                d.aiThread = $root.proto.DeviceCapabilities.AiThread.toObject(m.aiThread, o, q + 1);
            }
            if (m.aiFbidMigration != null && $Object.hasOwnProperty.call(m, "aiFbidMigration")) {
                d.aiFbidMigration = $root.proto.DeviceCapabilities.AiFbidMigration.toObject(m.aiFbidMigration, o, q + 1);
            }
            if (m.bizAiSettingsSync != null && $Object.hasOwnProperty.call(m, "bizAiSettingsSync")) {
                d.bizAiSettingsSync = $root.proto.DeviceCapabilities.BizAiSettingsSync.toObject(m.bizAiSettingsSync, o, q + 1);
            }
            if (m.contactRefresh != null && $Object.hasOwnProperty.call(m, "contactRefresh")) {
                d.contactRefresh = $root.proto.DeviceCapabilities.ContactRefresh.toObject(m.contactRefresh, o, q + 1);
            }
            return d;
        };

        DeviceCapabilities.prototype.toJSON = function() {
            return DeviceCapabilities.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceCapabilities.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeviceCapabilities";
        };

        DeviceCapabilities.AiFbidMigration = (function() {

            const AiFbidMigration = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AiFbidMigration.prototype.chatDbMigrationTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AiFbidMigration.prototype, "_chatDbMigrationTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["chatDbMigrationTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AiFbidMigration.create = function(properties) {
                return new AiFbidMigration(properties);
            };

            AiFbidMigration.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.chatDbMigrationTimestamp != null && $Object.hasOwnProperty.call(m, "chatDbMigrationTimestamp"))
                    w.uint32(8).uint64(m.chatDbMigrationTimestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AiFbidMigration.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.AiFbidMigration();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.chatDbMigrationTimestamp = r.uint64();
                            m._chatDbMigrationTimestamp = "chatDbMigrationTimestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AiFbidMigration.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.AiFbidMigration)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.AiFbidMigration: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.AiFbidMigration();
                if (d.chatDbMigrationTimestamp != null) {
                    if ($util.Long)
                        m.chatDbMigrationTimestamp = $util.Long.fromValue(d.chatDbMigrationTimestamp, true);
                    else if (typeof d.chatDbMigrationTimestamp === "string")
                        m.chatDbMigrationTimestamp = $parseInt(d.chatDbMigrationTimestamp, 10);
                    else if (typeof d.chatDbMigrationTimestamp === "number")
                        m.chatDbMigrationTimestamp = d.chatDbMigrationTimestamp;
                    else if (typeof d.chatDbMigrationTimestamp === "object")
                        m.chatDbMigrationTimestamp = new $util.LongBits(d.chatDbMigrationTimestamp.low >>> 0, d.chatDbMigrationTimestamp.high >>> 0).toNumber(true);
                }
                return m;
            };

            AiFbidMigration.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.chatDbMigrationTimestamp != null && $Object.hasOwnProperty.call(m, "chatDbMigrationTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.chatDbMigrationTimestamp = typeof m.chatDbMigrationTimestamp === "number" ? $BigInt(m.chatDbMigrationTimestamp) : $util.Long.fromBits(m.chatDbMigrationTimestamp.low >>> 0, m.chatDbMigrationTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof m.chatDbMigrationTimestamp === "number")
                        d.chatDbMigrationTimestamp = o.longs === $String ? $String(m.chatDbMigrationTimestamp) : m.chatDbMigrationTimestamp;
                    else
                        d.chatDbMigrationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.chatDbMigrationTimestamp) : o.longs === $Number ? new $util.LongBits(m.chatDbMigrationTimestamp.low >>> 0, m.chatDbMigrationTimestamp.high >>> 0).toNumber(true) : m.chatDbMigrationTimestamp;
                }
                return d;
            };

            AiFbidMigration.prototype.toJSON = function() {
                return AiFbidMigration.toObject(this, $protobuf.util.toJSONOptions);
            };

            AiFbidMigration.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.AiFbidMigration";
            };

            return AiFbidMigration;
        })();

        DeviceCapabilities.AiThread = (function() {

            const AiThread = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AiThread.prototype.supportLevel = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AiThread.prototype, "_supportLevel", {
                get: $util.oneOfGetter($oneOfFields = ["supportLevel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AiThread.create = function(properties) {
                return new AiThread(properties);
            };

            AiThread.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.supportLevel != null && $Object.hasOwnProperty.call(m, "supportLevel"))
                    w.uint32(8).int32(m.supportLevel);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AiThread.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.AiThread(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.supportLevel = r.int32();
                            m._supportLevel = "supportLevel";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AiThread.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.AiThread)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.AiThread: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.AiThread();
                switch (d.supportLevel) {
                case "NONE":
                case 0:
                    m.supportLevel = 0;
                    break;
                case "INFRA":
                case 1:
                    m.supportLevel = 1;
                    break;
                case "FULL":
                case 2:
                    m.supportLevel = 2;
                    break;
                default:
                    if (typeof d.supportLevel === "number" && (d.supportLevel | 0) === d.supportLevel)
                        m.supportLevel = d.supportLevel;
                }
                return m;
            };

            AiThread.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.supportLevel != null && $Object.hasOwnProperty.call(m, "supportLevel")) {
                    d.supportLevel = o.enums === $String ? $root.proto.DeviceCapabilities.AiThread.SupportLevel[m.supportLevel] === $undefined ? m.supportLevel : $root.proto.DeviceCapabilities.AiThread.SupportLevel[m.supportLevel] : m.supportLevel;
                }
                return d;
            };

            AiThread.prototype.toJSON = function() {
                return AiThread.toObject(this, $protobuf.util.toJSONOptions);
            };

            AiThread.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.AiThread";
            };

            AiThread.SupportLevel = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "INFRA"] = 1;
                values[valuesById[2] = "FULL"] = 2;
                return values;
            })();

            return AiThread;
        })();

        DeviceCapabilities.BizAiSettingsSync = (function() {

            const BizAiSettingsSync = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BizAiSettingsSync.prototype.handoffRemovalTimingEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BizAiSettingsSync.prototype, "_handoffRemovalTimingEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["handoffRemovalTimingEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BizAiSettingsSync.create = function(properties) {
                return new BizAiSettingsSync(properties);
            };

            BizAiSettingsSync.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.handoffRemovalTimingEnabled != null && $Object.hasOwnProperty.call(m, "handoffRemovalTimingEnabled"))
                    w.uint32(8).bool(m.handoffRemovalTimingEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BizAiSettingsSync.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.BizAiSettingsSync();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.handoffRemovalTimingEnabled = r.bool();
                            m._handoffRemovalTimingEnabled = "handoffRemovalTimingEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BizAiSettingsSync.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.BizAiSettingsSync)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.BizAiSettingsSync: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.BizAiSettingsSync();
                if (d.handoffRemovalTimingEnabled != null) {
                    m.handoffRemovalTimingEnabled = $Boolean(d.handoffRemovalTimingEnabled);
                }
                return m;
            };

            BizAiSettingsSync.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.handoffRemovalTimingEnabled != null && $Object.hasOwnProperty.call(m, "handoffRemovalTimingEnabled")) {
                    d.handoffRemovalTimingEnabled = m.handoffRemovalTimingEnabled;
                }
                return d;
            };

            BizAiSettingsSync.prototype.toJSON = function() {
                return BizAiSettingsSync.toObject(this, $protobuf.util.toJSONOptions);
            };

            BizAiSettingsSync.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.BizAiSettingsSync";
            };

            return BizAiSettingsSync;
        })();

        DeviceCapabilities.BusinessBroadcast = (function() {

            const BusinessBroadcast = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessBroadcast.prototype.importListEnabled = null;
            BusinessBroadcast.prototype.companionSupportEnabled = null;
            BusinessBroadcast.prototype.campaignSyncEnabled = null;
            BusinessBroadcast.prototype.insightsSyncEnabled = null;
            BusinessBroadcast.prototype.recipientLimit = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcast.prototype, "_importListEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["importListEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcast.prototype, "_companionSupportEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["companionSupportEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcast.prototype, "_campaignSyncEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["campaignSyncEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcast.prototype, "_insightsSyncEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["insightsSyncEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcast.prototype, "_recipientLimit", {
                get: $util.oneOfGetter($oneOfFields = ["recipientLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessBroadcast.create = function(properties) {
                return new BusinessBroadcast(properties);
            };

            BusinessBroadcast.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.importListEnabled != null && $Object.hasOwnProperty.call(m, "importListEnabled"))
                    w.uint32(8).bool(m.importListEnabled);
                if (m.companionSupportEnabled != null && $Object.hasOwnProperty.call(m, "companionSupportEnabled"))
                    w.uint32(16).bool(m.companionSupportEnabled);
                if (m.campaignSyncEnabled != null && $Object.hasOwnProperty.call(m, "campaignSyncEnabled"))
                    w.uint32(24).bool(m.campaignSyncEnabled);
                if (m.insightsSyncEnabled != null && $Object.hasOwnProperty.call(m, "insightsSyncEnabled"))
                    w.uint32(32).bool(m.insightsSyncEnabled);
                if (m.recipientLimit != null && $Object.hasOwnProperty.call(m, "recipientLimit"))
                    w.uint32(40).int32(m.recipientLimit);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessBroadcast.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.BusinessBroadcast();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.importListEnabled = r.bool();
                            m._importListEnabled = "importListEnabled";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.companionSupportEnabled = r.bool();
                            m._companionSupportEnabled = "companionSupportEnabled";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.campaignSyncEnabled = r.bool();
                            m._campaignSyncEnabled = "campaignSyncEnabled";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.insightsSyncEnabled = r.bool();
                            m._insightsSyncEnabled = "insightsSyncEnabled";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.recipientLimit = r.int32();
                            m._recipientLimit = "recipientLimit";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessBroadcast.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.BusinessBroadcast)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.BusinessBroadcast: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.BusinessBroadcast();
                if (d.importListEnabled != null) {
                    m.importListEnabled = $Boolean(d.importListEnabled);
                }
                if (d.companionSupportEnabled != null) {
                    m.companionSupportEnabled = $Boolean(d.companionSupportEnabled);
                }
                if (d.campaignSyncEnabled != null) {
                    m.campaignSyncEnabled = $Boolean(d.campaignSyncEnabled);
                }
                if (d.insightsSyncEnabled != null) {
                    m.insightsSyncEnabled = $Boolean(d.insightsSyncEnabled);
                }
                if (d.recipientLimit != null) {
                    m.recipientLimit = d.recipientLimit | 0;
                }
                return m;
            };

            BusinessBroadcast.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.importListEnabled != null && $Object.hasOwnProperty.call(m, "importListEnabled")) {
                    d.importListEnabled = m.importListEnabled;
                }
                if (m.companionSupportEnabled != null && $Object.hasOwnProperty.call(m, "companionSupportEnabled")) {
                    d.companionSupportEnabled = m.companionSupportEnabled;
                }
                if (m.campaignSyncEnabled != null && $Object.hasOwnProperty.call(m, "campaignSyncEnabled")) {
                    d.campaignSyncEnabled = m.campaignSyncEnabled;
                }
                if (m.insightsSyncEnabled != null && $Object.hasOwnProperty.call(m, "insightsSyncEnabled")) {
                    d.insightsSyncEnabled = m.insightsSyncEnabled;
                }
                if (m.recipientLimit != null && $Object.hasOwnProperty.call(m, "recipientLimit")) {
                    d.recipientLimit = m.recipientLimit;
                }
                return d;
            };

            BusinessBroadcast.prototype.toJSON = function() {
                return BusinessBroadcast.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessBroadcast.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.BusinessBroadcast";
            };

            return BusinessBroadcast;
        })();

        DeviceCapabilities.ChatLockSupportLevel = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "MINIMAL"] = 1;
            values[valuesById[2] = "FULL"] = 2;
            return values;
        })();

        DeviceCapabilities.ContactRefresh = (function() {

            const ContactRefresh = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactRefresh.prototype.refreshSupported = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactRefresh.prototype, "_refreshSupported", {
                get: $util.oneOfGetter($oneOfFields = ["refreshSupported"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactRefresh.create = function(properties) {
                return new ContactRefresh(properties);
            };

            ContactRefresh.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.refreshSupported != null && $Object.hasOwnProperty.call(m, "refreshSupported"))
                    w.uint32(8).bool(m.refreshSupported);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactRefresh.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.ContactRefresh();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.refreshSupported = r.bool();
                            m._refreshSupported = "refreshSupported";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactRefresh.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.ContactRefresh)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.ContactRefresh: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.ContactRefresh();
                if (d.refreshSupported != null) {
                    m.refreshSupported = $Boolean(d.refreshSupported);
                }
                return m;
            };

            ContactRefresh.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.refreshSupported != null && $Object.hasOwnProperty.call(m, "refreshSupported")) {
                    d.refreshSupported = m.refreshSupported;
                }
                return d;
            };

            ContactRefresh.prototype.toJSON = function() {
                return ContactRefresh.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactRefresh.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.ContactRefresh";
            };

            return ContactRefresh;
        })();

        DeviceCapabilities.LIDMigration = (function() {

            const LIDMigration = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LIDMigration.prototype.chatDbMigrationTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LIDMigration.prototype, "_chatDbMigrationTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["chatDbMigrationTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LIDMigration.create = function(properties) {
                return new LIDMigration(properties);
            };

            LIDMigration.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.chatDbMigrationTimestamp != null && $Object.hasOwnProperty.call(m, "chatDbMigrationTimestamp"))
                    w.uint32(8).uint64(m.chatDbMigrationTimestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LIDMigration.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.LIDMigration();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.chatDbMigrationTimestamp = r.uint64();
                            m._chatDbMigrationTimestamp = "chatDbMigrationTimestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LIDMigration.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.LIDMigration)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.LIDMigration: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.LIDMigration();
                if (d.chatDbMigrationTimestamp != null) {
                    if ($util.Long)
                        m.chatDbMigrationTimestamp = $util.Long.fromValue(d.chatDbMigrationTimestamp, true);
                    else if (typeof d.chatDbMigrationTimestamp === "string")
                        m.chatDbMigrationTimestamp = $parseInt(d.chatDbMigrationTimestamp, 10);
                    else if (typeof d.chatDbMigrationTimestamp === "number")
                        m.chatDbMigrationTimestamp = d.chatDbMigrationTimestamp;
                    else if (typeof d.chatDbMigrationTimestamp === "object")
                        m.chatDbMigrationTimestamp = new $util.LongBits(d.chatDbMigrationTimestamp.low >>> 0, d.chatDbMigrationTimestamp.high >>> 0).toNumber(true);
                }
                return m;
            };

            LIDMigration.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.chatDbMigrationTimestamp != null && $Object.hasOwnProperty.call(m, "chatDbMigrationTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.chatDbMigrationTimestamp = typeof m.chatDbMigrationTimestamp === "number" ? $BigInt(m.chatDbMigrationTimestamp) : $util.Long.fromBits(m.chatDbMigrationTimestamp.low >>> 0, m.chatDbMigrationTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof m.chatDbMigrationTimestamp === "number")
                        d.chatDbMigrationTimestamp = o.longs === $String ? $String(m.chatDbMigrationTimestamp) : m.chatDbMigrationTimestamp;
                    else
                        d.chatDbMigrationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.chatDbMigrationTimestamp) : o.longs === $Number ? new $util.LongBits(m.chatDbMigrationTimestamp.low >>> 0, m.chatDbMigrationTimestamp.high >>> 0).toNumber(true) : m.chatDbMigrationTimestamp;
                }
                return d;
            };

            LIDMigration.prototype.toJSON = function() {
                return LIDMigration.toObject(this, $protobuf.util.toJSONOptions);
            };

            LIDMigration.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.LIDMigration";
            };

            return LIDMigration;
        })();

        DeviceCapabilities.MemberNameTagPrimarySupport = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "DISABLED"] = 0;
            values[valuesById[1] = "RECEIVER_ENABLED"] = 1;
            values[valuesById[2] = "SENDER_ENABLED"] = 2;
            return values;
        })();

        DeviceCapabilities.UserHasAvatar = (function() {

            const UserHasAvatar = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UserHasAvatar.prototype.userHasAvatar = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserHasAvatar.prototype, "_userHasAvatar", {
                get: $util.oneOfGetter($oneOfFields = ["userHasAvatar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UserHasAvatar.create = function(properties) {
                return new UserHasAvatar(properties);
            };

            UserHasAvatar.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.userHasAvatar != null && $Object.hasOwnProperty.call(m, "userHasAvatar"))
                    w.uint32(8).bool(m.userHasAvatar);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UserHasAvatar.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceCapabilities.UserHasAvatar();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.userHasAvatar = r.bool();
                            m._userHasAvatar = "userHasAvatar";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UserHasAvatar.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceCapabilities.UserHasAvatar)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceCapabilities.UserHasAvatar: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceCapabilities.UserHasAvatar();
                if (d.userHasAvatar != null) {
                    m.userHasAvatar = $Boolean(d.userHasAvatar);
                }
                return m;
            };

            UserHasAvatar.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.userHasAvatar != null && $Object.hasOwnProperty.call(m, "userHasAvatar")) {
                    d.userHasAvatar = m.userHasAvatar;
                }
                return d;
            };

            UserHasAvatar.prototype.toJSON = function() {
                return UserHasAvatar.toObject(this, $protobuf.util.toJSONOptions);
            };

            UserHasAvatar.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceCapabilities.UserHasAvatar";
            };

            return UserHasAvatar;
        })();

        return DeviceCapabilities;
    })();

    proto.DeviceConsistencyCodeMessage = (function() {

        const DeviceConsistencyCodeMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeviceConsistencyCodeMessage.prototype.generation = null;
        DeviceConsistencyCodeMessage.prototype.signature = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceConsistencyCodeMessage.prototype, "_generation", {
            get: $util.oneOfGetter($oneOfFields = ["generation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceConsistencyCodeMessage.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceConsistencyCodeMessage.create = function(properties) {
            return new DeviceConsistencyCodeMessage(properties);
        };

        DeviceConsistencyCodeMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.generation != null && $Object.hasOwnProperty.call(m, "generation"))
                w.uint32(8).uint32(m.generation);
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature"))
                w.uint32(18).bytes(m.signature);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeviceConsistencyCodeMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceConsistencyCodeMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.generation = r.uint32();
                        m._generation = "generation";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.signature = r.bytes();
                        m._signature = "signature";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DeviceConsistencyCodeMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeviceConsistencyCodeMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeviceConsistencyCodeMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeviceConsistencyCodeMessage();
            if (d.generation != null) {
                m.generation = d.generation >>> 0;
            }
            if (d.signature != null) {
                if (typeof d.signature === "string")
                    $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                else if (d.signature.length >= 0)
                    m.signature = d.signature;
            }
            return m;
        };

        DeviceConsistencyCodeMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.generation != null && $Object.hasOwnProperty.call(m, "generation")) {
                d.generation = m.generation;
            }
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature")) {
                d.signature = o.bytes === $String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signature) : m.signature;
            }
            return d;
        };

        DeviceConsistencyCodeMessage.prototype.toJSON = function() {
            return DeviceConsistencyCodeMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceConsistencyCodeMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeviceConsistencyCodeMessage";
        };

        return DeviceConsistencyCodeMessage;
    })();

    proto.DeviceListMetadata = (function() {

        const DeviceListMetadata = function (p) {
            this.senderKeyIndexes = [];
            this.recipientKeyIndexes = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeviceListMetadata.prototype.senderKeyHash = null;
        DeviceListMetadata.prototype.senderTimestamp = null;
        DeviceListMetadata.prototype.senderKeyIndexes = $util.emptyArray;
        DeviceListMetadata.prototype.senderAccountType = null;
        DeviceListMetadata.prototype.receiverAccountType = null;
        DeviceListMetadata.prototype.recipientKeyHash = null;
        DeviceListMetadata.prototype.recipientTimestamp = null;
        DeviceListMetadata.prototype.recipientKeyIndexes = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceListMetadata.prototype, "_senderKeyHash", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceListMetadata.prototype, "_senderTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceListMetadata.prototype, "_senderAccountType", {
            get: $util.oneOfGetter($oneOfFields = ["senderAccountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceListMetadata.prototype, "_receiverAccountType", {
            get: $util.oneOfGetter($oneOfFields = ["receiverAccountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceListMetadata.prototype, "_recipientKeyHash", {
            get: $util.oneOfGetter($oneOfFields = ["recipientKeyHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceListMetadata.prototype, "_recipientTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["recipientTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceListMetadata.create = function(properties) {
            return new DeviceListMetadata(properties);
        };

        DeviceListMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.senderKeyHash != null && $Object.hasOwnProperty.call(m, "senderKeyHash"))
                w.uint32(10).bytes(m.senderKeyHash);
            if (m.senderTimestamp != null && $Object.hasOwnProperty.call(m, "senderTimestamp"))
                w.uint32(16).uint64(m.senderTimestamp);
            if (m.senderKeyIndexes != null && m.senderKeyIndexes.length) {
                w.uint32(26).uint32s(m.senderKeyIndexes);
            }
            if (m.senderAccountType != null && $Object.hasOwnProperty.call(m, "senderAccountType"))
                w.uint32(32).int32(m.senderAccountType);
            if (m.receiverAccountType != null && $Object.hasOwnProperty.call(m, "receiverAccountType"))
                w.uint32(40).int32(m.receiverAccountType);
            if (m.recipientKeyHash != null && $Object.hasOwnProperty.call(m, "recipientKeyHash"))
                w.uint32(66).bytes(m.recipientKeyHash);
            if (m.recipientTimestamp != null && $Object.hasOwnProperty.call(m, "recipientTimestamp"))
                w.uint32(72).uint64(m.recipientTimestamp);
            if (m.recipientKeyIndexes != null && m.recipientKeyIndexes.length) {
                w.uint32(82).uint32s(m.recipientKeyIndexes);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeviceListMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceListMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.senderKeyHash = r.bytes();
                        m._senderKeyHash = "senderKeyHash";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.senderTimestamp = r.uint64();
                        m._senderTimestamp = "senderTimestamp";
                        continue;
                    }
                case 3: {
                        if (u === 2) {
                            if (!(m.senderKeyIndexes && m.senderKeyIndexes.length))
                                m.senderKeyIndexes = [];
                            r.uint32s(m.senderKeyIndexes);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.senderKeyIndexes && m.senderKeyIndexes.length))
                            m.senderKeyIndexes = [];
                        m.senderKeyIndexes.push(r.uint32());
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.senderAccountType = r.int32();
                        m._senderAccountType = "senderAccountType";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.receiverAccountType = r.int32();
                        m._receiverAccountType = "receiverAccountType";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.recipientKeyHash = r.bytes();
                        m._recipientKeyHash = "recipientKeyHash";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.recipientTimestamp = r.uint64();
                        m._recipientTimestamp = "recipientTimestamp";
                        continue;
                    }
                case 10: {
                        if (u === 2) {
                            if (!(m.recipientKeyIndexes && m.recipientKeyIndexes.length))
                                m.recipientKeyIndexes = [];
                            r.uint32s(m.recipientKeyIndexes);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.recipientKeyIndexes && m.recipientKeyIndexes.length))
                            m.recipientKeyIndexes = [];
                        m.recipientKeyIndexes.push(r.uint32());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DeviceListMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeviceListMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeviceListMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeviceListMetadata();
            if (d.senderKeyHash != null) {
                if (typeof d.senderKeyHash === "string")
                    $util.base64.decode(d.senderKeyHash, m.senderKeyHash = $util.newBuffer($util.base64.length(d.senderKeyHash)), 0);
                else if (d.senderKeyHash.length >= 0)
                    m.senderKeyHash = d.senderKeyHash;
            }
            if (d.senderTimestamp != null) {
                if ($util.Long)
                    m.senderTimestamp = $util.Long.fromValue(d.senderTimestamp, true);
                else if (typeof d.senderTimestamp === "string")
                    m.senderTimestamp = $parseInt(d.senderTimestamp, 10);
                else if (typeof d.senderTimestamp === "number")
                    m.senderTimestamp = d.senderTimestamp;
                else if (typeof d.senderTimestamp === "object")
                    m.senderTimestamp = new $util.LongBits(d.senderTimestamp.low >>> 0, d.senderTimestamp.high >>> 0).toNumber(true);
            }
            if (d.senderKeyIndexes) {
                if (!$Array.isArray(d.senderKeyIndexes))
                    throw $TypeError(".proto.DeviceListMetadata.senderKeyIndexes: array expected");
                m.senderKeyIndexes = $Array(d.senderKeyIndexes.length);
                for (var i = 0; i < d.senderKeyIndexes.length; ++i) {
                    m.senderKeyIndexes[i] = d.senderKeyIndexes[i] >>> 0;
                }
            }
            switch (d.senderAccountType) {
            case "E2EE":
            case 0:
                m.senderAccountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.senderAccountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.senderAccountType = 2;
                break;
            default:
                if (typeof d.senderAccountType === "number" && (d.senderAccountType | 0) === d.senderAccountType)
                    m.senderAccountType = d.senderAccountType;
            }
            switch (d.receiverAccountType) {
            case "E2EE":
            case 0:
                m.receiverAccountType = 0;
                break;
            case "HOSTED":
            case 1:
                m.receiverAccountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.receiverAccountType = 2;
                break;
            default:
                if (typeof d.receiverAccountType === "number" && (d.receiverAccountType | 0) === d.receiverAccountType)
                    m.receiverAccountType = d.receiverAccountType;
            }
            if (d.recipientKeyHash != null) {
                if (typeof d.recipientKeyHash === "string")
                    $util.base64.decode(d.recipientKeyHash, m.recipientKeyHash = $util.newBuffer($util.base64.length(d.recipientKeyHash)), 0);
                else if (d.recipientKeyHash.length >= 0)
                    m.recipientKeyHash = d.recipientKeyHash;
            }
            if (d.recipientTimestamp != null) {
                if ($util.Long)
                    m.recipientTimestamp = $util.Long.fromValue(d.recipientTimestamp, true);
                else if (typeof d.recipientTimestamp === "string")
                    m.recipientTimestamp = $parseInt(d.recipientTimestamp, 10);
                else if (typeof d.recipientTimestamp === "number")
                    m.recipientTimestamp = d.recipientTimestamp;
                else if (typeof d.recipientTimestamp === "object")
                    m.recipientTimestamp = new $util.LongBits(d.recipientTimestamp.low >>> 0, d.recipientTimestamp.high >>> 0).toNumber(true);
            }
            if (d.recipientKeyIndexes) {
                if (!$Array.isArray(d.recipientKeyIndexes))
                    throw $TypeError(".proto.DeviceListMetadata.recipientKeyIndexes: array expected");
                m.recipientKeyIndexes = $Array(d.recipientKeyIndexes.length);
                for (var i = 0; i < d.recipientKeyIndexes.length; ++i) {
                    m.recipientKeyIndexes[i] = d.recipientKeyIndexes[i] >>> 0;
                }
            }
            return m;
        };

        DeviceListMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.senderKeyIndexes = [];
                d.recipientKeyIndexes = [];
            }
            if (m.senderKeyHash != null && $Object.hasOwnProperty.call(m, "senderKeyHash")) {
                d.senderKeyHash = o.bytes === $String ? $util.base64.encode(m.senderKeyHash, 0, m.senderKeyHash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.senderKeyHash) : m.senderKeyHash;
            }
            if (m.senderTimestamp != null && $Object.hasOwnProperty.call(m, "senderTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.senderTimestamp = typeof m.senderTimestamp === "number" ? $BigInt(m.senderTimestamp) : $util.Long.fromBits(m.senderTimestamp.low >>> 0, m.senderTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.senderTimestamp === "number")
                    d.senderTimestamp = o.longs === $String ? $String(m.senderTimestamp) : m.senderTimestamp;
                else
                    d.senderTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestamp) : o.longs === $Number ? new $util.LongBits(m.senderTimestamp.low >>> 0, m.senderTimestamp.high >>> 0).toNumber(true) : m.senderTimestamp;
            }
            if (m.senderKeyIndexes && m.senderKeyIndexes.length) {
                d.senderKeyIndexes = $Array(m.senderKeyIndexes.length);
                for (var j = 0; j < m.senderKeyIndexes.length; ++j) {
                    d.senderKeyIndexes[j] = m.senderKeyIndexes[j];
                }
            }
            if (m.senderAccountType != null && $Object.hasOwnProperty.call(m, "senderAccountType")) {
                d.senderAccountType = o.enums === $String ? $root.proto.ADVEncryptionType[m.senderAccountType] === $undefined ? m.senderAccountType : $root.proto.ADVEncryptionType[m.senderAccountType] : m.senderAccountType;
            }
            if (m.receiverAccountType != null && $Object.hasOwnProperty.call(m, "receiverAccountType")) {
                d.receiverAccountType = o.enums === $String ? $root.proto.ADVEncryptionType[m.receiverAccountType] === $undefined ? m.receiverAccountType : $root.proto.ADVEncryptionType[m.receiverAccountType] : m.receiverAccountType;
            }
            if (m.recipientKeyHash != null && $Object.hasOwnProperty.call(m, "recipientKeyHash")) {
                d.recipientKeyHash = o.bytes === $String ? $util.base64.encode(m.recipientKeyHash, 0, m.recipientKeyHash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.recipientKeyHash) : m.recipientKeyHash;
            }
            if (m.recipientTimestamp != null && $Object.hasOwnProperty.call(m, "recipientTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.recipientTimestamp = typeof m.recipientTimestamp === "number" ? $BigInt(m.recipientTimestamp) : $util.Long.fromBits(m.recipientTimestamp.low >>> 0, m.recipientTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.recipientTimestamp === "number")
                    d.recipientTimestamp = o.longs === $String ? $String(m.recipientTimestamp) : m.recipientTimestamp;
                else
                    d.recipientTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.recipientTimestamp) : o.longs === $Number ? new $util.LongBits(m.recipientTimestamp.low >>> 0, m.recipientTimestamp.high >>> 0).toNumber(true) : m.recipientTimestamp;
            }
            if (m.recipientKeyIndexes && m.recipientKeyIndexes.length) {
                d.recipientKeyIndexes = $Array(m.recipientKeyIndexes.length);
                for (var j = 0; j < m.recipientKeyIndexes.length; ++j) {
                    d.recipientKeyIndexes[j] = m.recipientKeyIndexes[j];
                }
            }
            return d;
        };

        DeviceListMetadata.prototype.toJSON = function() {
            return DeviceListMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceListMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeviceListMetadata";
        };

        return DeviceListMetadata;
    })();

    proto.DeviceOutput = (function() {

        const DeviceOutput = function (p) {
            this.supportedEncryptionVersions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeviceOutput.prototype.publicKey = $util.newBuffer([]);
        DeviceOutput.prototype.epochAuthPublicKey = $util.newBuffer([]);
        DeviceOutput.prototype.epochAuthPublicKeySig = $util.newBuffer([]);
        DeviceOutput.prototype.epochStoragePublicKey = $util.newBuffer([]);
        DeviceOutput.prototype.epochStoragePublicKeySig = $util.newBuffer([]);
        DeviceOutput.prototype.supportedEncryptionVersions = $util.emptyArray;
        DeviceOutput.prototype.encryptionVersionSignature = $util.newBuffer([]);
        DeviceOutput.prototype.clientVersion = 0;
        DeviceOutput.prototype.ocmfClientState = $util.newBuffer([]);
        DeviceOutput.prototype.epochStoragePrivateKey = $util.newBuffer([]);

        DeviceOutput.create = function(properties) {
            return new DeviceOutput(properties);
        };

        DeviceOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.publicKey);
            w.uint32(18).bytes(m.epochAuthPublicKey);
            w.uint32(26).bytes(m.epochAuthPublicKeySig);
            w.uint32(34).bytes(m.epochStoragePublicKey);
            w.uint32(42).bytes(m.epochStoragePublicKeySig);
            if (m.supportedEncryptionVersions != null && m.supportedEncryptionVersions.length) {
                w.uint32(50).int32s(m.supportedEncryptionVersions);
            }
            w.uint32(58).bytes(m.encryptionVersionSignature);
            w.uint32(64).int32(m.clientVersion);
            w.uint32(74).bytes(m.ocmfClientState);
            w.uint32(82).bytes(m.epochStoragePrivateKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeviceOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.epochAuthPublicKey = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.epochAuthPublicKeySig = r.bytes();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.epochStoragePublicKey = r.bytes();
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.epochStoragePublicKeySig = r.bytes();
                        continue;
                    }
                case 6: {
                        if (u === 2) {
                            if (!(m.supportedEncryptionVersions && m.supportedEncryptionVersions.length))
                                m.supportedEncryptionVersions = [];
                            r.int32s(m.supportedEncryptionVersions);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.supportedEncryptionVersions && m.supportedEncryptionVersions.length))
                            m.supportedEncryptionVersions = [];
                        m.supportedEncryptionVersions.push(r.int32());
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.encryptionVersionSignature = r.bytes();
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.clientVersion = r.int32();
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.ocmfClientState = r.bytes();
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.epochStoragePrivateKey = r.bytes();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "publicKey"))
                throw $util.ProtocolError("missing required 'publicKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochAuthPublicKey"))
                throw $util.ProtocolError("missing required 'epochAuthPublicKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochAuthPublicKeySig"))
                throw $util.ProtocolError("missing required 'epochAuthPublicKeySig'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochStoragePublicKey"))
                throw $util.ProtocolError("missing required 'epochStoragePublicKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochStoragePublicKeySig"))
                throw $util.ProtocolError("missing required 'epochStoragePublicKeySig'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptionVersionSignature"))
                throw $util.ProtocolError("missing required 'encryptionVersionSignature'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "clientVersion"))
                throw $util.ProtocolError("missing required 'clientVersion'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "ocmfClientState"))
                throw $util.ProtocolError("missing required 'ocmfClientState'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochStoragePrivateKey"))
                throw $util.ProtocolError("missing required 'epochStoragePrivateKey'", { instance: m });
            return m;
        };

        DeviceOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeviceOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeviceOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeviceOutput();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.epochAuthPublicKey != null) {
                if (typeof d.epochAuthPublicKey === "string")
                    $util.base64.decode(d.epochAuthPublicKey, m.epochAuthPublicKey = $util.newBuffer($util.base64.length(d.epochAuthPublicKey)), 0);
                else if (d.epochAuthPublicKey.length >= 0)
                    m.epochAuthPublicKey = d.epochAuthPublicKey;
            }
            if (d.epochAuthPublicKeySig != null) {
                if (typeof d.epochAuthPublicKeySig === "string")
                    $util.base64.decode(d.epochAuthPublicKeySig, m.epochAuthPublicKeySig = $util.newBuffer($util.base64.length(d.epochAuthPublicKeySig)), 0);
                else if (d.epochAuthPublicKeySig.length >= 0)
                    m.epochAuthPublicKeySig = d.epochAuthPublicKeySig;
            }
            if (d.epochStoragePublicKey != null) {
                if (typeof d.epochStoragePublicKey === "string")
                    $util.base64.decode(d.epochStoragePublicKey, m.epochStoragePublicKey = $util.newBuffer($util.base64.length(d.epochStoragePublicKey)), 0);
                else if (d.epochStoragePublicKey.length >= 0)
                    m.epochStoragePublicKey = d.epochStoragePublicKey;
            }
            if (d.epochStoragePublicKeySig != null) {
                if (typeof d.epochStoragePublicKeySig === "string")
                    $util.base64.decode(d.epochStoragePublicKeySig, m.epochStoragePublicKeySig = $util.newBuffer($util.base64.length(d.epochStoragePublicKeySig)), 0);
                else if (d.epochStoragePublicKeySig.length >= 0)
                    m.epochStoragePublicKeySig = d.epochStoragePublicKeySig;
            }
            if (d.supportedEncryptionVersions) {
                if (!$Array.isArray(d.supportedEncryptionVersions))
                    throw $TypeError(".proto.DeviceOutput.supportedEncryptionVersions: array expected");
                m.supportedEncryptionVersions = $Array(d.supportedEncryptionVersions.length);
                for (var i = 0; i < d.supportedEncryptionVersions.length; ++i) {
                    m.supportedEncryptionVersions[i] = d.supportedEncryptionVersions[i] | 0;
                }
            }
            if (d.encryptionVersionSignature != null) {
                if (typeof d.encryptionVersionSignature === "string")
                    $util.base64.decode(d.encryptionVersionSignature, m.encryptionVersionSignature = $util.newBuffer($util.base64.length(d.encryptionVersionSignature)), 0);
                else if (d.encryptionVersionSignature.length >= 0)
                    m.encryptionVersionSignature = d.encryptionVersionSignature;
            }
            if (d.clientVersion != null) {
                m.clientVersion = d.clientVersion | 0;
            }
            if (d.ocmfClientState != null) {
                if (typeof d.ocmfClientState === "string")
                    $util.base64.decode(d.ocmfClientState, m.ocmfClientState = $util.newBuffer($util.base64.length(d.ocmfClientState)), 0);
                else if (d.ocmfClientState.length >= 0)
                    m.ocmfClientState = d.ocmfClientState;
            }
            if (d.epochStoragePrivateKey != null) {
                if (typeof d.epochStoragePrivateKey === "string")
                    $util.base64.decode(d.epochStoragePrivateKey, m.epochStoragePrivateKey = $util.newBuffer($util.base64.length(d.epochStoragePrivateKey)), 0);
                else if (d.epochStoragePrivateKey.length >= 0)
                    m.epochStoragePrivateKey = d.epochStoragePrivateKey;
            }
            return m;
        };

        DeviceOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.supportedEncryptionVersions = [];
            }
            if (o.defaults) {
                if (o.bytes === $String)
                    d.publicKey = "";
                else {
                    d.publicKey = [];
                    if (o.bytes !== $Array)
                        d.publicKey = $util.newBuffer(d.publicKey);
                }
                if (o.bytes === $String)
                    d.epochAuthPublicKey = "";
                else {
                    d.epochAuthPublicKey = [];
                    if (o.bytes !== $Array)
                        d.epochAuthPublicKey = $util.newBuffer(d.epochAuthPublicKey);
                }
                if (o.bytes === $String)
                    d.epochAuthPublicKeySig = "";
                else {
                    d.epochAuthPublicKeySig = [];
                    if (o.bytes !== $Array)
                        d.epochAuthPublicKeySig = $util.newBuffer(d.epochAuthPublicKeySig);
                }
                if (o.bytes === $String)
                    d.epochStoragePublicKey = "";
                else {
                    d.epochStoragePublicKey = [];
                    if (o.bytes !== $Array)
                        d.epochStoragePublicKey = $util.newBuffer(d.epochStoragePublicKey);
                }
                if (o.bytes === $String)
                    d.epochStoragePublicKeySig = "";
                else {
                    d.epochStoragePublicKeySig = [];
                    if (o.bytes !== $Array)
                        d.epochStoragePublicKeySig = $util.newBuffer(d.epochStoragePublicKeySig);
                }
                if (o.bytes === $String)
                    d.encryptionVersionSignature = "";
                else {
                    d.encryptionVersionSignature = [];
                    if (o.bytes !== $Array)
                        d.encryptionVersionSignature = $util.newBuffer(d.encryptionVersionSignature);
                }
                d.clientVersion = 0;
                if (o.bytes === $String)
                    d.ocmfClientState = "";
                else {
                    d.ocmfClientState = [];
                    if (o.bytes !== $Array)
                        d.ocmfClientState = $util.newBuffer(d.ocmfClientState);
                }
                if (o.bytes === $String)
                    d.epochStoragePrivateKey = "";
                else {
                    d.epochStoragePrivateKey = [];
                    if (o.bytes !== $Array)
                        d.epochStoragePrivateKey = $util.newBuffer(d.epochStoragePrivateKey);
                }
            }
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.epochAuthPublicKey != null && $Object.hasOwnProperty.call(m, "epochAuthPublicKey")) {
                d.epochAuthPublicKey = o.bytes === $String ? $util.base64.encode(m.epochAuthPublicKey, 0, m.epochAuthPublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAuthPublicKey) : m.epochAuthPublicKey;
            }
            if (m.epochAuthPublicKeySig != null && $Object.hasOwnProperty.call(m, "epochAuthPublicKeySig")) {
                d.epochAuthPublicKeySig = o.bytes === $String ? $util.base64.encode(m.epochAuthPublicKeySig, 0, m.epochAuthPublicKeySig.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAuthPublicKeySig) : m.epochAuthPublicKeySig;
            }
            if (m.epochStoragePublicKey != null && $Object.hasOwnProperty.call(m, "epochStoragePublicKey")) {
                d.epochStoragePublicKey = o.bytes === $String ? $util.base64.encode(m.epochStoragePublicKey, 0, m.epochStoragePublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochStoragePublicKey) : m.epochStoragePublicKey;
            }
            if (m.epochStoragePublicKeySig != null && $Object.hasOwnProperty.call(m, "epochStoragePublicKeySig")) {
                d.epochStoragePublicKeySig = o.bytes === $String ? $util.base64.encode(m.epochStoragePublicKeySig, 0, m.epochStoragePublicKeySig.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochStoragePublicKeySig) : m.epochStoragePublicKeySig;
            }
            if (m.supportedEncryptionVersions && m.supportedEncryptionVersions.length) {
                d.supportedEncryptionVersions = $Array(m.supportedEncryptionVersions.length);
                for (var j = 0; j < m.supportedEncryptionVersions.length; ++j) {
                    d.supportedEncryptionVersions[j] = m.supportedEncryptionVersions[j];
                }
            }
            if (m.encryptionVersionSignature != null && $Object.hasOwnProperty.call(m, "encryptionVersionSignature")) {
                d.encryptionVersionSignature = o.bytes === $String ? $util.base64.encode(m.encryptionVersionSignature, 0, m.encryptionVersionSignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptionVersionSignature) : m.encryptionVersionSignature;
            }
            if (m.clientVersion != null && $Object.hasOwnProperty.call(m, "clientVersion")) {
                d.clientVersion = m.clientVersion;
            }
            if (m.ocmfClientState != null && $Object.hasOwnProperty.call(m, "ocmfClientState")) {
                d.ocmfClientState = o.bytes === $String ? $util.base64.encode(m.ocmfClientState, 0, m.ocmfClientState.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ocmfClientState) : m.ocmfClientState;
            }
            if (m.epochStoragePrivateKey != null && $Object.hasOwnProperty.call(m, "epochStoragePrivateKey")) {
                d.epochStoragePrivateKey = o.bytes === $String ? $util.base64.encode(m.epochStoragePrivateKey, 0, m.epochStoragePrivateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochStoragePrivateKey) : m.epochStoragePrivateKey;
            }
            return d;
        };

        DeviceOutput.prototype.toJSON = function() {
            return DeviceOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeviceOutput";
        };

        return DeviceOutput;
    })();

    proto.DeviceProps = (function() {

        const DeviceProps = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DeviceProps.prototype.os = null;
        DeviceProps.prototype.version = null;
        DeviceProps.prototype.platformType = null;
        DeviceProps.prototype.requireFullSync = null;
        DeviceProps.prototype.historySyncConfig = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceProps.prototype, "_os", {
            get: $util.oneOfGetter($oneOfFields = ["os"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceProps.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceProps.prototype, "_platformType", {
            get: $util.oneOfGetter($oneOfFields = ["platformType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceProps.prototype, "_requireFullSync", {
            get: $util.oneOfGetter($oneOfFields = ["requireFullSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DeviceProps.prototype, "_historySyncConfig", {
            get: $util.oneOfGetter($oneOfFields = ["historySyncConfig"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DeviceProps.create = function(properties) {
            return new DeviceProps(properties);
        };

        DeviceProps.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.os != null && $Object.hasOwnProperty.call(m, "os"))
                w.uint32(10).string(m.os);
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                $root.proto.DeviceProps.AppVersion.encode(m.version, w.uint32(18).fork(), q + 1).ldelim();
            if (m.platformType != null && $Object.hasOwnProperty.call(m, "platformType"))
                w.uint32(24).int32(m.platformType);
            if (m.requireFullSync != null && $Object.hasOwnProperty.call(m, "requireFullSync"))
                w.uint32(32).bool(m.requireFullSync);
            if (m.historySyncConfig != null && $Object.hasOwnProperty.call(m, "historySyncConfig"))
                $root.proto.DeviceProps.HistorySyncConfig.encode(m.historySyncConfig, w.uint32(42).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DeviceProps.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceProps(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.os = r.stringVerify();
                        m._os = "os";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.version = $root.proto.DeviceProps.AppVersion.decode(r, r.uint32(), $undefined, q + 1, m.version);
                        m._version = "version";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.platformType = r.int32();
                        m._platformType = "platformType";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.requireFullSync = r.bool();
                        m._requireFullSync = "requireFullSync";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.decode(r, r.uint32(), $undefined, q + 1, m.historySyncConfig);
                        m._historySyncConfig = "historySyncConfig";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DeviceProps.fromObject = function (d, q) {
            if (d instanceof $root.proto.DeviceProps)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DeviceProps: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DeviceProps();
            if (d.os != null) {
                m.os = $String(d.os);
            }
            if (d.version != null) {
                if (!$util.isObject(d.version))
                    throw $TypeError(".proto.DeviceProps.version: object expected");
                m.version = $root.proto.DeviceProps.AppVersion.fromObject(d.version, q + 1);
            }
            switch (d.platformType) {
            case "UNKNOWN":
            case 0:
                m.platformType = 0;
                break;
            case "CHROME":
            case 1:
                m.platformType = 1;
                break;
            case "FIREFOX":
            case 2:
                m.platformType = 2;
                break;
            case "IE":
            case 3:
                m.platformType = 3;
                break;
            case "OPERA":
            case 4:
                m.platformType = 4;
                break;
            case "SAFARI":
            case 5:
                m.platformType = 5;
                break;
            case "EDGE":
            case 6:
                m.platformType = 6;
                break;
            case "DESKTOP":
            case 7:
                m.platformType = 7;
                break;
            case "IPAD":
            case 8:
                m.platformType = 8;
                break;
            case "ANDROID_TABLET":
            case 9:
                m.platformType = 9;
                break;
            case "OHANA":
            case 10:
                m.platformType = 10;
                break;
            case "ALOHA":
            case 11:
                m.platformType = 11;
                break;
            case "CATALINA":
            case 12:
                m.platformType = 12;
                break;
            case "TCL_TV":
            case 13:
                m.platformType = 13;
                break;
            case "IOS_PHONE":
            case 14:
                m.platformType = 14;
                break;
            case "IOS_CATALYST":
            case 15:
                m.platformType = 15;
                break;
            case "ANDROID_PHONE":
            case 16:
                m.platformType = 16;
                break;
            case "ANDROID_AMBIGUOUS":
            case 17:
                m.platformType = 17;
                break;
            case "WEAR_OS":
            case 18:
                m.platformType = 18;
                break;
            case "AR_WRIST":
            case 19:
                m.platformType = 19;
                break;
            case "AR_DEVICE":
            case 20:
                m.platformType = 20;
                break;
            case "UWP":
            case 21:
                m.platformType = 21;
                break;
            case "VR":
            case 22:
                m.platformType = 22;
                break;
            case "CLOUD_API":
            case 23:
                m.platformType = 23;
                break;
            case "SMARTGLASSES":
            case 24:
                m.platformType = 24;
                break;
            case "WAIL":
            case 25:
                m.platformType = 25;
                break;
            default:
                if (typeof d.platformType === "number" && (d.platformType | 0) === d.platformType)
                    m.platformType = d.platformType;
            }
            if (d.requireFullSync != null) {
                m.requireFullSync = $Boolean(d.requireFullSync);
            }
            if (d.historySyncConfig != null) {
                if (!$util.isObject(d.historySyncConfig))
                    throw $TypeError(".proto.DeviceProps.historySyncConfig: object expected");
                m.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.fromObject(d.historySyncConfig, q + 1);
            }
            return m;
        };

        DeviceProps.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.os != null && $Object.hasOwnProperty.call(m, "os")) {
                d.os = m.os;
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = $root.proto.DeviceProps.AppVersion.toObject(m.version, o, q + 1);
            }
            if (m.platformType != null && $Object.hasOwnProperty.call(m, "platformType")) {
                d.platformType = o.enums === $String ? $root.proto.DeviceProps.PlatformType[m.platformType] === $undefined ? m.platformType : $root.proto.DeviceProps.PlatformType[m.platformType] : m.platformType;
            }
            if (m.requireFullSync != null && $Object.hasOwnProperty.call(m, "requireFullSync")) {
                d.requireFullSync = m.requireFullSync;
            }
            if (m.historySyncConfig != null && $Object.hasOwnProperty.call(m, "historySyncConfig")) {
                d.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.toObject(m.historySyncConfig, o, q + 1);
            }
            return d;
        };

        DeviceProps.prototype.toJSON = function() {
            return DeviceProps.toObject(this, $protobuf.util.toJSONOptions);
        };

        DeviceProps.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DeviceProps";
        };

        DeviceProps.AppVersion = (function() {

            const AppVersion = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppVersion.prototype.primary = null;
            AppVersion.prototype.secondary = null;
            AppVersion.prototype.tertiary = null;
            AppVersion.prototype.quaternary = null;
            AppVersion.prototype.quinary = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppVersion.prototype, "_primary", {
                get: $util.oneOfGetter($oneOfFields = ["primary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppVersion.prototype, "_secondary", {
                get: $util.oneOfGetter($oneOfFields = ["secondary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppVersion.prototype, "_tertiary", {
                get: $util.oneOfGetter($oneOfFields = ["tertiary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppVersion.prototype, "_quaternary", {
                get: $util.oneOfGetter($oneOfFields = ["quaternary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppVersion.prototype, "_quinary", {
                get: $util.oneOfGetter($oneOfFields = ["quinary"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppVersion.create = function(properties) {
                return new AppVersion(properties);
            };

            AppVersion.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.primary != null && $Object.hasOwnProperty.call(m, "primary"))
                    w.uint32(8).uint32(m.primary);
                if (m.secondary != null && $Object.hasOwnProperty.call(m, "secondary"))
                    w.uint32(16).uint32(m.secondary);
                if (m.tertiary != null && $Object.hasOwnProperty.call(m, "tertiary"))
                    w.uint32(24).uint32(m.tertiary);
                if (m.quaternary != null && $Object.hasOwnProperty.call(m, "quaternary"))
                    w.uint32(32).uint32(m.quaternary);
                if (m.quinary != null && $Object.hasOwnProperty.call(m, "quinary"))
                    w.uint32(40).uint32(m.quinary);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppVersion.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceProps.AppVersion();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.primary = r.uint32();
                            m._primary = "primary";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.secondary = r.uint32();
                            m._secondary = "secondary";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.tertiary = r.uint32();
                            m._tertiary = "tertiary";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.quaternary = r.uint32();
                            m._quaternary = "quaternary";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.quinary = r.uint32();
                            m._quinary = "quinary";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppVersion.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceProps.AppVersion)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceProps.AppVersion: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceProps.AppVersion();
                if (d.primary != null) {
                    m.primary = d.primary >>> 0;
                }
                if (d.secondary != null) {
                    m.secondary = d.secondary >>> 0;
                }
                if (d.tertiary != null) {
                    m.tertiary = d.tertiary >>> 0;
                }
                if (d.quaternary != null) {
                    m.quaternary = d.quaternary >>> 0;
                }
                if (d.quinary != null) {
                    m.quinary = d.quinary >>> 0;
                }
                return m;
            };

            AppVersion.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.primary != null && $Object.hasOwnProperty.call(m, "primary")) {
                    d.primary = m.primary;
                }
                if (m.secondary != null && $Object.hasOwnProperty.call(m, "secondary")) {
                    d.secondary = m.secondary;
                }
                if (m.tertiary != null && $Object.hasOwnProperty.call(m, "tertiary")) {
                    d.tertiary = m.tertiary;
                }
                if (m.quaternary != null && $Object.hasOwnProperty.call(m, "quaternary")) {
                    d.quaternary = m.quaternary;
                }
                if (m.quinary != null && $Object.hasOwnProperty.call(m, "quinary")) {
                    d.quinary = m.quinary;
                }
                return d;
            };

            AppVersion.prototype.toJSON = function() {
                return AppVersion.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppVersion.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceProps.AppVersion";
            };

            return AppVersion;
        })();

        DeviceProps.HistorySyncConfig = (function() {

            const HistorySyncConfig = function (p) {
                this.supportedBotChannelFbids = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HistorySyncConfig.prototype.fullSyncDaysLimit = null;
            HistorySyncConfig.prototype.fullSyncSizeMbLimit = null;
            HistorySyncConfig.prototype.storageQuotaMb = null;
            HistorySyncConfig.prototype.inlineInitialPayloadInE2EeMsg = null;
            HistorySyncConfig.prototype.recentSyncDaysLimit = null;
            HistorySyncConfig.prototype.supportCallLogHistory = null;
            HistorySyncConfig.prototype.supportBotUserAgentChatHistory = null;
            HistorySyncConfig.prototype.supportCagReactionsAndPolls = null;
            HistorySyncConfig.prototype.supportBizHostedMsg = null;
            HistorySyncConfig.prototype.supportRecentSyncChunkMessageCountTuning = null;
            HistorySyncConfig.prototype.supportHostedGroupMsg = null;
            HistorySyncConfig.prototype.supportFbidBotChatHistory = null;
            HistorySyncConfig.prototype.supportAddOnHistorySyncMigration = null;
            HistorySyncConfig.prototype.supportMessageAssociation = null;
            HistorySyncConfig.prototype.supportGroupHistory = null;
            HistorySyncConfig.prototype.onDemandReady = null;
            HistorySyncConfig.prototype.supportGuestChat = null;
            HistorySyncConfig.prototype.completeOnDemandReady = null;
            HistorySyncConfig.prototype.thumbnailSyncDaysLimit = null;
            HistorySyncConfig.prototype.initialSyncMaxMessagesPerChat = null;
            HistorySyncConfig.prototype.supportManusHistory = null;
            HistorySyncConfig.prototype.supportHatchHistory = null;
            HistorySyncConfig.prototype.supportedBotChannelFbids = $util.emptyArray;
            HistorySyncConfig.prototype.supportInlineContacts = null;
            HistorySyncConfig.prototype.supportNewsletter = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_fullSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["fullSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_fullSyncSizeMbLimit", {
                get: $util.oneOfGetter($oneOfFields = ["fullSyncSizeMbLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_storageQuotaMb", {
                get: $util.oneOfGetter($oneOfFields = ["storageQuotaMb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_inlineInitialPayloadInE2EeMsg", {
                get: $util.oneOfGetter($oneOfFields = ["inlineInitialPayloadInE2EeMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_recentSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["recentSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportCallLogHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportCallLogHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportBotUserAgentChatHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportBotUserAgentChatHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportCagReactionsAndPolls", {
                get: $util.oneOfGetter($oneOfFields = ["supportCagReactionsAndPolls"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportBizHostedMsg", {
                get: $util.oneOfGetter($oneOfFields = ["supportBizHostedMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportRecentSyncChunkMessageCountTuning", {
                get: $util.oneOfGetter($oneOfFields = ["supportRecentSyncChunkMessageCountTuning"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportHostedGroupMsg", {
                get: $util.oneOfGetter($oneOfFields = ["supportHostedGroupMsg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportFbidBotChatHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportFbidBotChatHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportAddOnHistorySyncMigration", {
                get: $util.oneOfGetter($oneOfFields = ["supportAddOnHistorySyncMigration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportMessageAssociation", {
                get: $util.oneOfGetter($oneOfFields = ["supportMessageAssociation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportGroupHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportGroupHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_onDemandReady", {
                get: $util.oneOfGetter($oneOfFields = ["onDemandReady"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportGuestChat", {
                get: $util.oneOfGetter($oneOfFields = ["supportGuestChat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_completeOnDemandReady", {
                get: $util.oneOfGetter($oneOfFields = ["completeOnDemandReady"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_thumbnailSyncDaysLimit", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSyncDaysLimit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_initialSyncMaxMessagesPerChat", {
                get: $util.oneOfGetter($oneOfFields = ["initialSyncMaxMessagesPerChat"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportManusHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportManusHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportHatchHistory", {
                get: $util.oneOfGetter($oneOfFields = ["supportHatchHistory"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportInlineContacts", {
                get: $util.oneOfGetter($oneOfFields = ["supportInlineContacts"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncConfig.prototype, "_supportNewsletter", {
                get: $util.oneOfGetter($oneOfFields = ["supportNewsletter"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistorySyncConfig.create = function(properties) {
                return new HistorySyncConfig(properties);
            };

            HistorySyncConfig.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fullSyncDaysLimit != null && $Object.hasOwnProperty.call(m, "fullSyncDaysLimit"))
                    w.uint32(8).uint32(m.fullSyncDaysLimit);
                if (m.fullSyncSizeMbLimit != null && $Object.hasOwnProperty.call(m, "fullSyncSizeMbLimit"))
                    w.uint32(16).uint32(m.fullSyncSizeMbLimit);
                if (m.storageQuotaMb != null && $Object.hasOwnProperty.call(m, "storageQuotaMb"))
                    w.uint32(24).uint32(m.storageQuotaMb);
                if (m.inlineInitialPayloadInE2EeMsg != null && $Object.hasOwnProperty.call(m, "inlineInitialPayloadInE2EeMsg"))
                    w.uint32(32).bool(m.inlineInitialPayloadInE2EeMsg);
                if (m.recentSyncDaysLimit != null && $Object.hasOwnProperty.call(m, "recentSyncDaysLimit"))
                    w.uint32(40).uint32(m.recentSyncDaysLimit);
                if (m.supportCallLogHistory != null && $Object.hasOwnProperty.call(m, "supportCallLogHistory"))
                    w.uint32(48).bool(m.supportCallLogHistory);
                if (m.supportBotUserAgentChatHistory != null && $Object.hasOwnProperty.call(m, "supportBotUserAgentChatHistory"))
                    w.uint32(56).bool(m.supportBotUserAgentChatHistory);
                if (m.supportCagReactionsAndPolls != null && $Object.hasOwnProperty.call(m, "supportCagReactionsAndPolls"))
                    w.uint32(64).bool(m.supportCagReactionsAndPolls);
                if (m.supportBizHostedMsg != null && $Object.hasOwnProperty.call(m, "supportBizHostedMsg"))
                    w.uint32(72).bool(m.supportBizHostedMsg);
                if (m.supportRecentSyncChunkMessageCountTuning != null && $Object.hasOwnProperty.call(m, "supportRecentSyncChunkMessageCountTuning"))
                    w.uint32(80).bool(m.supportRecentSyncChunkMessageCountTuning);
                if (m.supportHostedGroupMsg != null && $Object.hasOwnProperty.call(m, "supportHostedGroupMsg"))
                    w.uint32(88).bool(m.supportHostedGroupMsg);
                if (m.supportFbidBotChatHistory != null && $Object.hasOwnProperty.call(m, "supportFbidBotChatHistory"))
                    w.uint32(96).bool(m.supportFbidBotChatHistory);
                if (m.supportAddOnHistorySyncMigration != null && $Object.hasOwnProperty.call(m, "supportAddOnHistorySyncMigration"))
                    w.uint32(104).bool(m.supportAddOnHistorySyncMigration);
                if (m.supportMessageAssociation != null && $Object.hasOwnProperty.call(m, "supportMessageAssociation"))
                    w.uint32(112).bool(m.supportMessageAssociation);
                if (m.supportGroupHistory != null && $Object.hasOwnProperty.call(m, "supportGroupHistory"))
                    w.uint32(120).bool(m.supportGroupHistory);
                if (m.onDemandReady != null && $Object.hasOwnProperty.call(m, "onDemandReady"))
                    w.uint32(128).bool(m.onDemandReady);
                if (m.supportGuestChat != null && $Object.hasOwnProperty.call(m, "supportGuestChat"))
                    w.uint32(136).bool(m.supportGuestChat);
                if (m.completeOnDemandReady != null && $Object.hasOwnProperty.call(m, "completeOnDemandReady"))
                    w.uint32(144).bool(m.completeOnDemandReady);
                if (m.thumbnailSyncDaysLimit != null && $Object.hasOwnProperty.call(m, "thumbnailSyncDaysLimit"))
                    w.uint32(152).uint32(m.thumbnailSyncDaysLimit);
                if (m.initialSyncMaxMessagesPerChat != null && $Object.hasOwnProperty.call(m, "initialSyncMaxMessagesPerChat"))
                    w.uint32(160).uint32(m.initialSyncMaxMessagesPerChat);
                if (m.supportManusHistory != null && $Object.hasOwnProperty.call(m, "supportManusHistory"))
                    w.uint32(168).bool(m.supportManusHistory);
                if (m.supportHatchHistory != null && $Object.hasOwnProperty.call(m, "supportHatchHistory"))
                    w.uint32(176).bool(m.supportHatchHistory);
                if (m.supportedBotChannelFbids != null && m.supportedBotChannelFbids.length) {
                    for (var i = 0; i < m.supportedBotChannelFbids.length; ++i)
                        w.uint32(186).string(m.supportedBotChannelFbids[i]);
                }
                if (m.supportInlineContacts != null && $Object.hasOwnProperty.call(m, "supportInlineContacts"))
                    w.uint32(192).bool(m.supportInlineContacts);
                if (m.supportNewsletter != null && $Object.hasOwnProperty.call(m, "supportNewsletter"))
                    w.uint32(200).bool(m.supportNewsletter);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HistorySyncConfig.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DeviceProps.HistorySyncConfig();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.fullSyncDaysLimit = r.uint32();
                            m._fullSyncDaysLimit = "fullSyncDaysLimit";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.fullSyncSizeMbLimit = r.uint32();
                            m._fullSyncSizeMbLimit = "fullSyncSizeMbLimit";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.storageQuotaMb = r.uint32();
                            m._storageQuotaMb = "storageQuotaMb";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.inlineInitialPayloadInE2EeMsg = r.bool();
                            m._inlineInitialPayloadInE2EeMsg = "inlineInitialPayloadInE2EeMsg";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.recentSyncDaysLimit = r.uint32();
                            m._recentSyncDaysLimit = "recentSyncDaysLimit";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.supportCallLogHistory = r.bool();
                            m._supportCallLogHistory = "supportCallLogHistory";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.supportBotUserAgentChatHistory = r.bool();
                            m._supportBotUserAgentChatHistory = "supportBotUserAgentChatHistory";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.supportCagReactionsAndPolls = r.bool();
                            m._supportCagReactionsAndPolls = "supportCagReactionsAndPolls";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.supportBizHostedMsg = r.bool();
                            m._supportBizHostedMsg = "supportBizHostedMsg";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.supportRecentSyncChunkMessageCountTuning = r.bool();
                            m._supportRecentSyncChunkMessageCountTuning = "supportRecentSyncChunkMessageCountTuning";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.supportHostedGroupMsg = r.bool();
                            m._supportHostedGroupMsg = "supportHostedGroupMsg";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.supportFbidBotChatHistory = r.bool();
                            m._supportFbidBotChatHistory = "supportFbidBotChatHistory";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            m.supportAddOnHistorySyncMigration = r.bool();
                            m._supportAddOnHistorySyncMigration = "supportAddOnHistorySyncMigration";
                            continue;
                        }
                    case 14: {
                            if (u !== 0)
                                break;
                            m.supportMessageAssociation = r.bool();
                            m._supportMessageAssociation = "supportMessageAssociation";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            m.supportGroupHistory = r.bool();
                            m._supportGroupHistory = "supportGroupHistory";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            m.onDemandReady = r.bool();
                            m._onDemandReady = "onDemandReady";
                            continue;
                        }
                    case 17: {
                            if (u !== 0)
                                break;
                            m.supportGuestChat = r.bool();
                            m._supportGuestChat = "supportGuestChat";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.completeOnDemandReady = r.bool();
                            m._completeOnDemandReady = "completeOnDemandReady";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.thumbnailSyncDaysLimit = r.uint32();
                            m._thumbnailSyncDaysLimit = "thumbnailSyncDaysLimit";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            m.initialSyncMaxMessagesPerChat = r.uint32();
                            m._initialSyncMaxMessagesPerChat = "initialSyncMaxMessagesPerChat";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            m.supportManusHistory = r.bool();
                            m._supportManusHistory = "supportManusHistory";
                            continue;
                        }
                    case 22: {
                            if (u !== 0)
                                break;
                            m.supportHatchHistory = r.bool();
                            m._supportHatchHistory = "supportHatchHistory";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            if (!(m.supportedBotChannelFbids && m.supportedBotChannelFbids.length))
                                m.supportedBotChannelFbids = [];
                            m.supportedBotChannelFbids.push(r.stringVerify());
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            m.supportInlineContacts = r.bool();
                            m._supportInlineContacts = "supportInlineContacts";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            m.supportNewsletter = r.bool();
                            m._supportNewsletter = "supportNewsletter";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HistorySyncConfig.fromObject = function (d, q) {
                if (d instanceof $root.proto.DeviceProps.HistorySyncConfig)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.DeviceProps.HistorySyncConfig: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.DeviceProps.HistorySyncConfig();
                if (d.fullSyncDaysLimit != null) {
                    m.fullSyncDaysLimit = d.fullSyncDaysLimit >>> 0;
                }
                if (d.fullSyncSizeMbLimit != null) {
                    m.fullSyncSizeMbLimit = d.fullSyncSizeMbLimit >>> 0;
                }
                if (d.storageQuotaMb != null) {
                    m.storageQuotaMb = d.storageQuotaMb >>> 0;
                }
                if (d.inlineInitialPayloadInE2EeMsg != null) {
                    m.inlineInitialPayloadInE2EeMsg = $Boolean(d.inlineInitialPayloadInE2EeMsg);
                }
                if (d.recentSyncDaysLimit != null) {
                    m.recentSyncDaysLimit = d.recentSyncDaysLimit >>> 0;
                }
                if (d.supportCallLogHistory != null) {
                    m.supportCallLogHistory = $Boolean(d.supportCallLogHistory);
                }
                if (d.supportBotUserAgentChatHistory != null) {
                    m.supportBotUserAgentChatHistory = $Boolean(d.supportBotUserAgentChatHistory);
                }
                if (d.supportCagReactionsAndPolls != null) {
                    m.supportCagReactionsAndPolls = $Boolean(d.supportCagReactionsAndPolls);
                }
                if (d.supportBizHostedMsg != null) {
                    m.supportBizHostedMsg = $Boolean(d.supportBizHostedMsg);
                }
                if (d.supportRecentSyncChunkMessageCountTuning != null) {
                    m.supportRecentSyncChunkMessageCountTuning = $Boolean(d.supportRecentSyncChunkMessageCountTuning);
                }
                if (d.supportHostedGroupMsg != null) {
                    m.supportHostedGroupMsg = $Boolean(d.supportHostedGroupMsg);
                }
                if (d.supportFbidBotChatHistory != null) {
                    m.supportFbidBotChatHistory = $Boolean(d.supportFbidBotChatHistory);
                }
                if (d.supportAddOnHistorySyncMigration != null) {
                    m.supportAddOnHistorySyncMigration = $Boolean(d.supportAddOnHistorySyncMigration);
                }
                if (d.supportMessageAssociation != null) {
                    m.supportMessageAssociation = $Boolean(d.supportMessageAssociation);
                }
                if (d.supportGroupHistory != null) {
                    m.supportGroupHistory = $Boolean(d.supportGroupHistory);
                }
                if (d.onDemandReady != null) {
                    m.onDemandReady = $Boolean(d.onDemandReady);
                }
                if (d.supportGuestChat != null) {
                    m.supportGuestChat = $Boolean(d.supportGuestChat);
                }
                if (d.completeOnDemandReady != null) {
                    m.completeOnDemandReady = $Boolean(d.completeOnDemandReady);
                }
                if (d.thumbnailSyncDaysLimit != null) {
                    m.thumbnailSyncDaysLimit = d.thumbnailSyncDaysLimit >>> 0;
                }
                if (d.initialSyncMaxMessagesPerChat != null) {
                    m.initialSyncMaxMessagesPerChat = d.initialSyncMaxMessagesPerChat >>> 0;
                }
                if (d.supportManusHistory != null) {
                    m.supportManusHistory = $Boolean(d.supportManusHistory);
                }
                if (d.supportHatchHistory != null) {
                    m.supportHatchHistory = $Boolean(d.supportHatchHistory);
                }
                if (d.supportedBotChannelFbids) {
                    if (!$Array.isArray(d.supportedBotChannelFbids))
                        throw $TypeError(".proto.DeviceProps.HistorySyncConfig.supportedBotChannelFbids: array expected");
                    m.supportedBotChannelFbids = $Array(d.supportedBotChannelFbids.length);
                    for (var i = 0; i < d.supportedBotChannelFbids.length; ++i) {
                        m.supportedBotChannelFbids[i] = $String(d.supportedBotChannelFbids[i]);
                    }
                }
                if (d.supportInlineContacts != null) {
                    m.supportInlineContacts = $Boolean(d.supportInlineContacts);
                }
                if (d.supportNewsletter != null) {
                    m.supportNewsletter = $Boolean(d.supportNewsletter);
                }
                return m;
            };

            HistorySyncConfig.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.supportedBotChannelFbids = [];
                }
                if (m.fullSyncDaysLimit != null && $Object.hasOwnProperty.call(m, "fullSyncDaysLimit")) {
                    d.fullSyncDaysLimit = m.fullSyncDaysLimit;
                }
                if (m.fullSyncSizeMbLimit != null && $Object.hasOwnProperty.call(m, "fullSyncSizeMbLimit")) {
                    d.fullSyncSizeMbLimit = m.fullSyncSizeMbLimit;
                }
                if (m.storageQuotaMb != null && $Object.hasOwnProperty.call(m, "storageQuotaMb")) {
                    d.storageQuotaMb = m.storageQuotaMb;
                }
                if (m.inlineInitialPayloadInE2EeMsg != null && $Object.hasOwnProperty.call(m, "inlineInitialPayloadInE2EeMsg")) {
                    d.inlineInitialPayloadInE2EeMsg = m.inlineInitialPayloadInE2EeMsg;
                }
                if (m.recentSyncDaysLimit != null && $Object.hasOwnProperty.call(m, "recentSyncDaysLimit")) {
                    d.recentSyncDaysLimit = m.recentSyncDaysLimit;
                }
                if (m.supportCallLogHistory != null && $Object.hasOwnProperty.call(m, "supportCallLogHistory")) {
                    d.supportCallLogHistory = m.supportCallLogHistory;
                }
                if (m.supportBotUserAgentChatHistory != null && $Object.hasOwnProperty.call(m, "supportBotUserAgentChatHistory")) {
                    d.supportBotUserAgentChatHistory = m.supportBotUserAgentChatHistory;
                }
                if (m.supportCagReactionsAndPolls != null && $Object.hasOwnProperty.call(m, "supportCagReactionsAndPolls")) {
                    d.supportCagReactionsAndPolls = m.supportCagReactionsAndPolls;
                }
                if (m.supportBizHostedMsg != null && $Object.hasOwnProperty.call(m, "supportBizHostedMsg")) {
                    d.supportBizHostedMsg = m.supportBizHostedMsg;
                }
                if (m.supportRecentSyncChunkMessageCountTuning != null && $Object.hasOwnProperty.call(m, "supportRecentSyncChunkMessageCountTuning")) {
                    d.supportRecentSyncChunkMessageCountTuning = m.supportRecentSyncChunkMessageCountTuning;
                }
                if (m.supportHostedGroupMsg != null && $Object.hasOwnProperty.call(m, "supportHostedGroupMsg")) {
                    d.supportHostedGroupMsg = m.supportHostedGroupMsg;
                }
                if (m.supportFbidBotChatHistory != null && $Object.hasOwnProperty.call(m, "supportFbidBotChatHistory")) {
                    d.supportFbidBotChatHistory = m.supportFbidBotChatHistory;
                }
                if (m.supportAddOnHistorySyncMigration != null && $Object.hasOwnProperty.call(m, "supportAddOnHistorySyncMigration")) {
                    d.supportAddOnHistorySyncMigration = m.supportAddOnHistorySyncMigration;
                }
                if (m.supportMessageAssociation != null && $Object.hasOwnProperty.call(m, "supportMessageAssociation")) {
                    d.supportMessageAssociation = m.supportMessageAssociation;
                }
                if (m.supportGroupHistory != null && $Object.hasOwnProperty.call(m, "supportGroupHistory")) {
                    d.supportGroupHistory = m.supportGroupHistory;
                }
                if (m.onDemandReady != null && $Object.hasOwnProperty.call(m, "onDemandReady")) {
                    d.onDemandReady = m.onDemandReady;
                }
                if (m.supportGuestChat != null && $Object.hasOwnProperty.call(m, "supportGuestChat")) {
                    d.supportGuestChat = m.supportGuestChat;
                }
                if (m.completeOnDemandReady != null && $Object.hasOwnProperty.call(m, "completeOnDemandReady")) {
                    d.completeOnDemandReady = m.completeOnDemandReady;
                }
                if (m.thumbnailSyncDaysLimit != null && $Object.hasOwnProperty.call(m, "thumbnailSyncDaysLimit")) {
                    d.thumbnailSyncDaysLimit = m.thumbnailSyncDaysLimit;
                }
                if (m.initialSyncMaxMessagesPerChat != null && $Object.hasOwnProperty.call(m, "initialSyncMaxMessagesPerChat")) {
                    d.initialSyncMaxMessagesPerChat = m.initialSyncMaxMessagesPerChat;
                }
                if (m.supportManusHistory != null && $Object.hasOwnProperty.call(m, "supportManusHistory")) {
                    d.supportManusHistory = m.supportManusHistory;
                }
                if (m.supportHatchHistory != null && $Object.hasOwnProperty.call(m, "supportHatchHistory")) {
                    d.supportHatchHistory = m.supportHatchHistory;
                }
                if (m.supportedBotChannelFbids && m.supportedBotChannelFbids.length) {
                    d.supportedBotChannelFbids = $Array(m.supportedBotChannelFbids.length);
                    for (var j = 0; j < m.supportedBotChannelFbids.length; ++j) {
                        d.supportedBotChannelFbids[j] = m.supportedBotChannelFbids[j];
                    }
                }
                if (m.supportInlineContacts != null && $Object.hasOwnProperty.call(m, "supportInlineContacts")) {
                    d.supportInlineContacts = m.supportInlineContacts;
                }
                if (m.supportNewsletter != null && $Object.hasOwnProperty.call(m, "supportNewsletter")) {
                    d.supportNewsletter = m.supportNewsletter;
                }
                return d;
            };

            HistorySyncConfig.prototype.toJSON = function() {
                return HistorySyncConfig.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistorySyncConfig.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.DeviceProps.HistorySyncConfig";
            };

            return HistorySyncConfig;
        })();

        DeviceProps.PlatformType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHROME"] = 1;
            values[valuesById[2] = "FIREFOX"] = 2;
            values[valuesById[3] = "IE"] = 3;
            values[valuesById[4] = "OPERA"] = 4;
            values[valuesById[5] = "SAFARI"] = 5;
            values[valuesById[6] = "EDGE"] = 6;
            values[valuesById[7] = "DESKTOP"] = 7;
            values[valuesById[8] = "IPAD"] = 8;
            values[valuesById[9] = "ANDROID_TABLET"] = 9;
            values[valuesById[10] = "OHANA"] = 10;
            values[valuesById[11] = "ALOHA"] = 11;
            values[valuesById[12] = "CATALINA"] = 12;
            values[valuesById[13] = "TCL_TV"] = 13;
            values[valuesById[14] = "IOS_PHONE"] = 14;
            values[valuesById[15] = "IOS_CATALYST"] = 15;
            values[valuesById[16] = "ANDROID_PHONE"] = 16;
            values[valuesById[17] = "ANDROID_AMBIGUOUS"] = 17;
            values[valuesById[18] = "WEAR_OS"] = 18;
            values[valuesById[19] = "AR_WRIST"] = 19;
            values[valuesById[20] = "AR_DEVICE"] = 20;
            values[valuesById[21] = "UWP"] = 21;
            values[valuesById[22] = "VR"] = 22;
            values[valuesById[23] = "CLOUD_API"] = 23;
            values[valuesById[24] = "SMARTGLASSES"] = 24;
            values[valuesById[25] = "WAIL"] = 25;
            return values;
        })();

        return DeviceProps;
    })();

    proto.DisappearingMode = (function() {

        const DisappearingMode = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        DisappearingMode.prototype.initiator = null;
        DisappearingMode.prototype.trigger = null;
        DisappearingMode.prototype.initiatorDeviceJid = null;
        DisappearingMode.prototype.initiatedByMe = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DisappearingMode.prototype, "_initiator", {
            get: $util.oneOfGetter($oneOfFields = ["initiator"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DisappearingMode.prototype, "_trigger", {
            get: $util.oneOfGetter($oneOfFields = ["trigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DisappearingMode.prototype, "_initiatorDeviceJid", {
            get: $util.oneOfGetter($oneOfFields = ["initiatorDeviceJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(DisappearingMode.prototype, "_initiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["initiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        DisappearingMode.create = function(properties) {
            return new DisappearingMode(properties);
        };

        DisappearingMode.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.initiator != null && $Object.hasOwnProperty.call(m, "initiator"))
                w.uint32(8).int32(m.initiator);
            if (m.trigger != null && $Object.hasOwnProperty.call(m, "trigger"))
                w.uint32(16).int32(m.trigger);
            if (m.initiatorDeviceJid != null && $Object.hasOwnProperty.call(m, "initiatorDeviceJid"))
                w.uint32(26).string(m.initiatorDeviceJid);
            if (m.initiatedByMe != null && $Object.hasOwnProperty.call(m, "initiatedByMe"))
                w.uint32(32).bool(m.initiatedByMe);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        DisappearingMode.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.DisappearingMode(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.initiator = r.int32();
                        m._initiator = "initiator";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.trigger = r.int32();
                        m._trigger = "trigger";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.initiatorDeviceJid = r.stringVerify();
                        m._initiatorDeviceJid = "initiatorDeviceJid";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.initiatedByMe = r.bool();
                        m._initiatedByMe = "initiatedByMe";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        DisappearingMode.fromObject = function (d, q) {
            if (d instanceof $root.proto.DisappearingMode)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.DisappearingMode: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.DisappearingMode();
            switch (d.initiator) {
            case "CHANGED_IN_CHAT":
            case 0:
                m.initiator = 0;
                break;
            case "INITIATED_BY_ME":
            case 1:
                m.initiator = 1;
                break;
            case "INITIATED_BY_OTHER":
            case 2:
                m.initiator = 2;
                break;
            case "BIZ_UPGRADE_FB_HOSTING":
            case 3:
                m.initiator = 3;
                break;
            default:
                if (typeof d.initiator === "number" && (d.initiator | 0) === d.initiator)
                    m.initiator = d.initiator;
            }
            switch (d.trigger) {
            case "UNKNOWN":
            case 0:
                m.trigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                m.trigger = 1;
                break;
            case "ACCOUNT_SETTING":
            case 2:
                m.trigger = 2;
                break;
            case "BULK_CHANGE":
            case 3:
                m.trigger = 3;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 4:
                m.trigger = 4;
                break;
            case "UNKNOWN_GROUPS":
            case 5:
                m.trigger = 5;
                break;
            default:
                if (typeof d.trigger === "number" && (d.trigger | 0) === d.trigger)
                    m.trigger = d.trigger;
            }
            if (d.initiatorDeviceJid != null) {
                m.initiatorDeviceJid = $String(d.initiatorDeviceJid);
            }
            if (d.initiatedByMe != null) {
                m.initiatedByMe = $Boolean(d.initiatedByMe);
            }
            return m;
        };

        DisappearingMode.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.initiator != null && $Object.hasOwnProperty.call(m, "initiator")) {
                d.initiator = o.enums === $String ? $root.proto.DisappearingMode.Initiator[m.initiator] === $undefined ? m.initiator : $root.proto.DisappearingMode.Initiator[m.initiator] : m.initiator;
            }
            if (m.trigger != null && $Object.hasOwnProperty.call(m, "trigger")) {
                d.trigger = o.enums === $String ? $root.proto.DisappearingMode.Trigger[m.trigger] === $undefined ? m.trigger : $root.proto.DisappearingMode.Trigger[m.trigger] : m.trigger;
            }
            if (m.initiatorDeviceJid != null && $Object.hasOwnProperty.call(m, "initiatorDeviceJid")) {
                d.initiatorDeviceJid = m.initiatorDeviceJid;
            }
            if (m.initiatedByMe != null && $Object.hasOwnProperty.call(m, "initiatedByMe")) {
                d.initiatedByMe = m.initiatedByMe;
            }
            return d;
        };

        DisappearingMode.prototype.toJSON = function() {
            return DisappearingMode.toObject(this, $protobuf.util.toJSONOptions);
        };

        DisappearingMode.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.DisappearingMode";
        };

        DisappearingMode.Initiator = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "CHANGED_IN_CHAT"] = 0;
            values[valuesById[1] = "INITIATED_BY_ME"] = 1;
            values[valuesById[2] = "INITIATED_BY_OTHER"] = 2;
            values[valuesById[3] = "BIZ_UPGRADE_FB_HOSTING"] = 3;
            return values;
        })();

        DisappearingMode.Trigger = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT_SETTING"] = 1;
            values[valuesById[2] = "ACCOUNT_SETTING"] = 2;
            values[valuesById[3] = "BULK_CHANGE"] = 3;
            values[valuesById[4] = "BIZ_SUPPORTS_FB_HOSTING"] = 4;
            values[valuesById[5] = "UNKNOWN_GROUPS"] = 5;
            return values;
        })();

        return DisappearingMode;
    })();

    proto.EmbeddedContent = (function() {

        const EmbeddedContent = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EmbeddedContent.prototype.embeddedMessage = null;
        EmbeddedContent.prototype.embeddedMusic = null;

        let $oneOfFields;

        $Object.defineProperty(EmbeddedContent.prototype, "content", {
            get: $util.oneOfGetter($oneOfFields = ["embeddedMessage", "embeddedMusic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EmbeddedContent.create = function(properties) {
            return new EmbeddedContent(properties);
        };

        EmbeddedContent.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.embeddedMessage != null && $Object.hasOwnProperty.call(m, "embeddedMessage"))
                $root.proto.EmbeddedMessage.encode(m.embeddedMessage, w.uint32(10).fork(), q + 1).ldelim();
            if (m.embeddedMusic != null && $Object.hasOwnProperty.call(m, "embeddedMusic"))
                $root.proto.EmbeddedMusic.encode(m.embeddedMusic, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EmbeddedContent.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EmbeddedContent();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.embeddedMessage = $root.proto.EmbeddedMessage.decode(r, r.uint32(), $undefined, q + 1, m.embeddedMessage);
                        m.content = "embeddedMessage";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.embeddedMusic = $root.proto.EmbeddedMusic.decode(r, r.uint32(), $undefined, q + 1, m.embeddedMusic);
                        m.content = "embeddedMusic";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EmbeddedContent.fromObject = function (d, q) {
            if (d instanceof $root.proto.EmbeddedContent)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EmbeddedContent: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EmbeddedContent();
            if (d.embeddedMessage != null) {
                if (!$util.isObject(d.embeddedMessage))
                    throw $TypeError(".proto.EmbeddedContent.embeddedMessage: object expected");
                m.embeddedMessage = $root.proto.EmbeddedMessage.fromObject(d.embeddedMessage, q + 1);
            }
            if (d.embeddedMusic != null) {
                if (!$util.isObject(d.embeddedMusic))
                    throw $TypeError(".proto.EmbeddedContent.embeddedMusic: object expected");
                m.embeddedMusic = $root.proto.EmbeddedMusic.fromObject(d.embeddedMusic, q + 1);
            }
            return m;
        };

        EmbeddedContent.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.embeddedMessage != null && $Object.hasOwnProperty.call(m, "embeddedMessage")) {
                d.embeddedMessage = $root.proto.EmbeddedMessage.toObject(m.embeddedMessage, o, q + 1);
                if (o.oneofs)
                    d.content = "embeddedMessage";
            }
            if (m.embeddedMusic != null && $Object.hasOwnProperty.call(m, "embeddedMusic")) {
                d.embeddedMusic = $root.proto.EmbeddedMusic.toObject(m.embeddedMusic, o, q + 1);
                if (o.oneofs)
                    d.content = "embeddedMusic";
            }
            return d;
        };

        EmbeddedContent.prototype.toJSON = function() {
            return EmbeddedContent.toObject(this, $protobuf.util.toJSONOptions);
        };

        EmbeddedContent.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EmbeddedContent";
        };

        return EmbeddedContent;
    })();

    proto.EmbeddedMessage = (function() {

        const EmbeddedMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EmbeddedMessage.prototype.stanzaId = null;
        EmbeddedMessage.prototype.message = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMessage.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EmbeddedMessage.create = function(properties) {
            return new EmbeddedMessage(properties);
        };

        EmbeddedMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                w.uint32(10).string(m.stanzaId);
            if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                $root.proto.Message.encode(m.message, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EmbeddedMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EmbeddedMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.stanzaId = r.stringVerify();
                        m._stanzaId = "stanzaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                        m._message = "message";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EmbeddedMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.EmbeddedMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EmbeddedMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EmbeddedMessage();
            if (d.stanzaId != null) {
                m.stanzaId = $String(d.stanzaId);
            }
            if (d.message != null) {
                if (!$util.isObject(d.message))
                    throw $TypeError(".proto.EmbeddedMessage.message: object expected");
                m.message = $root.proto.Message.fromObject(d.message, q + 1);
            }
            return m;
        };

        EmbeddedMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                d.stanzaId = m.stanzaId;
            }
            if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                d.message = $root.proto.Message.toObject(m.message, o, q + 1);
            }
            return d;
        };

        EmbeddedMessage.prototype.toJSON = function() {
            return EmbeddedMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        EmbeddedMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EmbeddedMessage";
        };

        return EmbeddedMessage;
    })();

    proto.EmbeddedMusic = (function() {

        const EmbeddedMusic = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EmbeddedMusic.prototype.musicContentMediaId = null;
        EmbeddedMusic.prototype.songId = null;
        EmbeddedMusic.prototype.author = null;
        EmbeddedMusic.prototype.title = null;
        EmbeddedMusic.prototype.artworkDirectPath = null;
        EmbeddedMusic.prototype.artworkSha256 = null;
        EmbeddedMusic.prototype.artworkEncSha256 = null;
        EmbeddedMusic.prototype.artistAttribution = null;
        EmbeddedMusic.prototype.countryBlocklist = null;
        EmbeddedMusic.prototype.isExplicit = null;
        EmbeddedMusic.prototype.artworkMediaKey = null;
        EmbeddedMusic.prototype.musicSongStartTimeInMs = null;
        EmbeddedMusic.prototype.derivedContentStartTimeInMs = null;
        EmbeddedMusic.prototype.overlapDurationInMs = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_musicContentMediaId", {
            get: $util.oneOfGetter($oneOfFields = ["musicContentMediaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_songId", {
            get: $util.oneOfGetter($oneOfFields = ["songId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_author", {
            get: $util.oneOfGetter($oneOfFields = ["author"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_artworkDirectPath", {
            get: $util.oneOfGetter($oneOfFields = ["artworkDirectPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_artworkSha256", {
            get: $util.oneOfGetter($oneOfFields = ["artworkSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_artworkEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["artworkEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_artistAttribution", {
            get: $util.oneOfGetter($oneOfFields = ["artistAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_countryBlocklist", {
            get: $util.oneOfGetter($oneOfFields = ["countryBlocklist"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_isExplicit", {
            get: $util.oneOfGetter($oneOfFields = ["isExplicit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_artworkMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["artworkMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_musicSongStartTimeInMs", {
            get: $util.oneOfGetter($oneOfFields = ["musicSongStartTimeInMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_derivedContentStartTimeInMs", {
            get: $util.oneOfGetter($oneOfFields = ["derivedContentStartTimeInMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EmbeddedMusic.prototype, "_overlapDurationInMs", {
            get: $util.oneOfGetter($oneOfFields = ["overlapDurationInMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EmbeddedMusic.create = function(properties) {
            return new EmbeddedMusic(properties);
        };

        EmbeddedMusic.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.musicContentMediaId != null && $Object.hasOwnProperty.call(m, "musicContentMediaId"))
                w.uint32(10).string(m.musicContentMediaId);
            if (m.songId != null && $Object.hasOwnProperty.call(m, "songId"))
                w.uint32(18).string(m.songId);
            if (m.author != null && $Object.hasOwnProperty.call(m, "author"))
                w.uint32(26).string(m.author);
            if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                w.uint32(34).string(m.title);
            if (m.artworkDirectPath != null && $Object.hasOwnProperty.call(m, "artworkDirectPath"))
                w.uint32(42).string(m.artworkDirectPath);
            if (m.artworkSha256 != null && $Object.hasOwnProperty.call(m, "artworkSha256"))
                w.uint32(50).bytes(m.artworkSha256);
            if (m.artworkEncSha256 != null && $Object.hasOwnProperty.call(m, "artworkEncSha256"))
                w.uint32(58).bytes(m.artworkEncSha256);
            if (m.artistAttribution != null && $Object.hasOwnProperty.call(m, "artistAttribution"))
                w.uint32(66).string(m.artistAttribution);
            if (m.countryBlocklist != null && $Object.hasOwnProperty.call(m, "countryBlocklist"))
                w.uint32(74).bytes(m.countryBlocklist);
            if (m.isExplicit != null && $Object.hasOwnProperty.call(m, "isExplicit"))
                w.uint32(80).bool(m.isExplicit);
            if (m.artworkMediaKey != null && $Object.hasOwnProperty.call(m, "artworkMediaKey"))
                w.uint32(90).bytes(m.artworkMediaKey);
            if (m.musicSongStartTimeInMs != null && $Object.hasOwnProperty.call(m, "musicSongStartTimeInMs"))
                w.uint32(96).int64(m.musicSongStartTimeInMs);
            if (m.derivedContentStartTimeInMs != null && $Object.hasOwnProperty.call(m, "derivedContentStartTimeInMs"))
                w.uint32(104).int64(m.derivedContentStartTimeInMs);
            if (m.overlapDurationInMs != null && $Object.hasOwnProperty.call(m, "overlapDurationInMs"))
                w.uint32(112).int64(m.overlapDurationInMs);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EmbeddedMusic.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EmbeddedMusic();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.musicContentMediaId = r.stringVerify();
                        m._musicContentMediaId = "musicContentMediaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.songId = r.stringVerify();
                        m._songId = "songId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.author = r.stringVerify();
                        m._author = "author";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.title = r.stringVerify();
                        m._title = "title";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.artworkDirectPath = r.stringVerify();
                        m._artworkDirectPath = "artworkDirectPath";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.artworkSha256 = r.bytes();
                        m._artworkSha256 = "artworkSha256";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.artworkEncSha256 = r.bytes();
                        m._artworkEncSha256 = "artworkEncSha256";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.artistAttribution = r.stringVerify();
                        m._artistAttribution = "artistAttribution";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.countryBlocklist = r.bytes();
                        m._countryBlocklist = "countryBlocklist";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.isExplicit = r.bool();
                        m._isExplicit = "isExplicit";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.artworkMediaKey = r.bytes();
                        m._artworkMediaKey = "artworkMediaKey";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.musicSongStartTimeInMs = r.int64();
                        m._musicSongStartTimeInMs = "musicSongStartTimeInMs";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        m.derivedContentStartTimeInMs = r.int64();
                        m._derivedContentStartTimeInMs = "derivedContentStartTimeInMs";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        m.overlapDurationInMs = r.int64();
                        m._overlapDurationInMs = "overlapDurationInMs";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EmbeddedMusic.fromObject = function (d, q) {
            if (d instanceof $root.proto.EmbeddedMusic)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EmbeddedMusic: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EmbeddedMusic();
            if (d.musicContentMediaId != null) {
                m.musicContentMediaId = $String(d.musicContentMediaId);
            }
            if (d.songId != null) {
                m.songId = $String(d.songId);
            }
            if (d.author != null) {
                m.author = $String(d.author);
            }
            if (d.title != null) {
                m.title = $String(d.title);
            }
            if (d.artworkDirectPath != null) {
                m.artworkDirectPath = $String(d.artworkDirectPath);
            }
            if (d.artworkSha256 != null) {
                if (typeof d.artworkSha256 === "string")
                    $util.base64.decode(d.artworkSha256, m.artworkSha256 = $util.newBuffer($util.base64.length(d.artworkSha256)), 0);
                else if (d.artworkSha256.length >= 0)
                    m.artworkSha256 = d.artworkSha256;
            }
            if (d.artworkEncSha256 != null) {
                if (typeof d.artworkEncSha256 === "string")
                    $util.base64.decode(d.artworkEncSha256, m.artworkEncSha256 = $util.newBuffer($util.base64.length(d.artworkEncSha256)), 0);
                else if (d.artworkEncSha256.length >= 0)
                    m.artworkEncSha256 = d.artworkEncSha256;
            }
            if (d.artistAttribution != null) {
                m.artistAttribution = $String(d.artistAttribution);
            }
            if (d.countryBlocklist != null) {
                if (typeof d.countryBlocklist === "string")
                    $util.base64.decode(d.countryBlocklist, m.countryBlocklist = $util.newBuffer($util.base64.length(d.countryBlocklist)), 0);
                else if (d.countryBlocklist.length >= 0)
                    m.countryBlocklist = d.countryBlocklist;
            }
            if (d.isExplicit != null) {
                m.isExplicit = $Boolean(d.isExplicit);
            }
            if (d.artworkMediaKey != null) {
                if (typeof d.artworkMediaKey === "string")
                    $util.base64.decode(d.artworkMediaKey, m.artworkMediaKey = $util.newBuffer($util.base64.length(d.artworkMediaKey)), 0);
                else if (d.artworkMediaKey.length >= 0)
                    m.artworkMediaKey = d.artworkMediaKey;
            }
            if (d.musicSongStartTimeInMs != null) {
                if ($util.Long)
                    m.musicSongStartTimeInMs = $util.Long.fromValue(d.musicSongStartTimeInMs, false);
                else if (typeof d.musicSongStartTimeInMs === "string")
                    m.musicSongStartTimeInMs = $parseInt(d.musicSongStartTimeInMs, 10);
                else if (typeof d.musicSongStartTimeInMs === "number")
                    m.musicSongStartTimeInMs = d.musicSongStartTimeInMs;
                else if (typeof d.musicSongStartTimeInMs === "object")
                    m.musicSongStartTimeInMs = new $util.LongBits(d.musicSongStartTimeInMs.low >>> 0, d.musicSongStartTimeInMs.high >>> 0).toNumber();
            }
            if (d.derivedContentStartTimeInMs != null) {
                if ($util.Long)
                    m.derivedContentStartTimeInMs = $util.Long.fromValue(d.derivedContentStartTimeInMs, false);
                else if (typeof d.derivedContentStartTimeInMs === "string")
                    m.derivedContentStartTimeInMs = $parseInt(d.derivedContentStartTimeInMs, 10);
                else if (typeof d.derivedContentStartTimeInMs === "number")
                    m.derivedContentStartTimeInMs = d.derivedContentStartTimeInMs;
                else if (typeof d.derivedContentStartTimeInMs === "object")
                    m.derivedContentStartTimeInMs = new $util.LongBits(d.derivedContentStartTimeInMs.low >>> 0, d.derivedContentStartTimeInMs.high >>> 0).toNumber();
            }
            if (d.overlapDurationInMs != null) {
                if ($util.Long)
                    m.overlapDurationInMs = $util.Long.fromValue(d.overlapDurationInMs, false);
                else if (typeof d.overlapDurationInMs === "string")
                    m.overlapDurationInMs = $parseInt(d.overlapDurationInMs, 10);
                else if (typeof d.overlapDurationInMs === "number")
                    m.overlapDurationInMs = d.overlapDurationInMs;
                else if (typeof d.overlapDurationInMs === "object")
                    m.overlapDurationInMs = new $util.LongBits(d.overlapDurationInMs.low >>> 0, d.overlapDurationInMs.high >>> 0).toNumber();
            }
            return m;
        };

        EmbeddedMusic.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.musicContentMediaId != null && $Object.hasOwnProperty.call(m, "musicContentMediaId")) {
                d.musicContentMediaId = m.musicContentMediaId;
            }
            if (m.songId != null && $Object.hasOwnProperty.call(m, "songId")) {
                d.songId = m.songId;
            }
            if (m.author != null && $Object.hasOwnProperty.call(m, "author")) {
                d.author = m.author;
            }
            if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                d.title = m.title;
            }
            if (m.artworkDirectPath != null && $Object.hasOwnProperty.call(m, "artworkDirectPath")) {
                d.artworkDirectPath = m.artworkDirectPath;
            }
            if (m.artworkSha256 != null && $Object.hasOwnProperty.call(m, "artworkSha256")) {
                d.artworkSha256 = o.bytes === $String ? $util.base64.encode(m.artworkSha256, 0, m.artworkSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.artworkSha256) : m.artworkSha256;
            }
            if (m.artworkEncSha256 != null && $Object.hasOwnProperty.call(m, "artworkEncSha256")) {
                d.artworkEncSha256 = o.bytes === $String ? $util.base64.encode(m.artworkEncSha256, 0, m.artworkEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.artworkEncSha256) : m.artworkEncSha256;
            }
            if (m.artistAttribution != null && $Object.hasOwnProperty.call(m, "artistAttribution")) {
                d.artistAttribution = m.artistAttribution;
            }
            if (m.countryBlocklist != null && $Object.hasOwnProperty.call(m, "countryBlocklist")) {
                d.countryBlocklist = o.bytes === $String ? $util.base64.encode(m.countryBlocklist, 0, m.countryBlocklist.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.countryBlocklist) : m.countryBlocklist;
            }
            if (m.isExplicit != null && $Object.hasOwnProperty.call(m, "isExplicit")) {
                d.isExplicit = m.isExplicit;
            }
            if (m.artworkMediaKey != null && $Object.hasOwnProperty.call(m, "artworkMediaKey")) {
                d.artworkMediaKey = o.bytes === $String ? $util.base64.encode(m.artworkMediaKey, 0, m.artworkMediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.artworkMediaKey) : m.artworkMediaKey;
            }
            if (m.musicSongStartTimeInMs != null && $Object.hasOwnProperty.call(m, "musicSongStartTimeInMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.musicSongStartTimeInMs = typeof m.musicSongStartTimeInMs === "number" ? $BigInt(m.musicSongStartTimeInMs) : $util.Long.fromBits(m.musicSongStartTimeInMs.low >>> 0, m.musicSongStartTimeInMs.high >>> 0, false).toBigInt();
                else if (typeof m.musicSongStartTimeInMs === "number")
                    d.musicSongStartTimeInMs = o.longs === $String ? $String(m.musicSongStartTimeInMs) : m.musicSongStartTimeInMs;
                else
                    d.musicSongStartTimeInMs = o.longs === $String ? $util.Long.prototype.toString.call(m.musicSongStartTimeInMs) : o.longs === $Number ? new $util.LongBits(m.musicSongStartTimeInMs.low >>> 0, m.musicSongStartTimeInMs.high >>> 0).toNumber() : m.musicSongStartTimeInMs;
            }
            if (m.derivedContentStartTimeInMs != null && $Object.hasOwnProperty.call(m, "derivedContentStartTimeInMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.derivedContentStartTimeInMs = typeof m.derivedContentStartTimeInMs === "number" ? $BigInt(m.derivedContentStartTimeInMs) : $util.Long.fromBits(m.derivedContentStartTimeInMs.low >>> 0, m.derivedContentStartTimeInMs.high >>> 0, false).toBigInt();
                else if (typeof m.derivedContentStartTimeInMs === "number")
                    d.derivedContentStartTimeInMs = o.longs === $String ? $String(m.derivedContentStartTimeInMs) : m.derivedContentStartTimeInMs;
                else
                    d.derivedContentStartTimeInMs = o.longs === $String ? $util.Long.prototype.toString.call(m.derivedContentStartTimeInMs) : o.longs === $Number ? new $util.LongBits(m.derivedContentStartTimeInMs.low >>> 0, m.derivedContentStartTimeInMs.high >>> 0).toNumber() : m.derivedContentStartTimeInMs;
            }
            if (m.overlapDurationInMs != null && $Object.hasOwnProperty.call(m, "overlapDurationInMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.overlapDurationInMs = typeof m.overlapDurationInMs === "number" ? $BigInt(m.overlapDurationInMs) : $util.Long.fromBits(m.overlapDurationInMs.low >>> 0, m.overlapDurationInMs.high >>> 0, false).toBigInt();
                else if (typeof m.overlapDurationInMs === "number")
                    d.overlapDurationInMs = o.longs === $String ? $String(m.overlapDurationInMs) : m.overlapDurationInMs;
                else
                    d.overlapDurationInMs = o.longs === $String ? $util.Long.prototype.toString.call(m.overlapDurationInMs) : o.longs === $Number ? new $util.LongBits(m.overlapDurationInMs.low >>> 0, m.overlapDurationInMs.high >>> 0).toNumber() : m.overlapDurationInMs;
            }
            return d;
        };

        EmbeddedMusic.prototype.toJSON = function() {
            return EmbeddedMusic.toObject(this, $protobuf.util.toJSONOptions);
        };

        EmbeddedMusic.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EmbeddedMusic";
        };

        return EmbeddedMusic;
    })();

    proto.EncryptMessageInput = (function() {

        const EncryptMessageInput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EncryptMessageInput.prototype.epochRootKey = $util.newBuffer([]);
        EncryptMessageInput.prototype.mailboxRootKey = $util.newBuffer([]);
        EncryptMessageInput.prototype.orfClientState = $util.newBuffer([]);
        EncryptMessageInput.prototype.epochAnonId = $util.newBuffer([]);
        EncryptMessageInput.prototype.epochId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        EncryptMessageInput.prototype.threadId = "";
        EncryptMessageInput.prototype.waCanonicalUserFbid = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        EncryptMessageInput.prototype.timestampMs = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        EncryptMessageInput.prototype.backupId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        EncryptMessageInput.prototype.plaintextPayload = $util.newBuffer([]);
        EncryptMessageInput.prototype.stanzaId = "";

        EncryptMessageInput.create = function(properties) {
            return new EncryptMessageInput(properties);
        };

        EncryptMessageInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.epochRootKey);
            w.uint32(18).bytes(m.mailboxRootKey);
            w.uint32(26).bytes(m.orfClientState);
            w.uint32(34).bytes(m.epochAnonId);
            w.uint32(40).uint64(m.epochId);
            w.uint32(50).string(m.threadId);
            w.uint32(56).uint64(m.waCanonicalUserFbid);
            w.uint32(64).uint64(m.timestampMs);
            w.uint32(72).uint64(m.backupId);
            w.uint32(82).bytes(m.plaintextPayload);
            w.uint32(90).string(m.stanzaId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EncryptMessageInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EncryptMessageInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.epochRootKey = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.mailboxRootKey = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.orfClientState = r.bytes();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.epochAnonId = r.bytes();
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.epochId = r.uint64();
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.threadId = r.stringVerify();
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.waCanonicalUserFbid = r.uint64();
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.timestampMs = r.uint64();
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.backupId = r.uint64();
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.plaintextPayload = r.bytes();
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.stanzaId = r.stringVerify();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "epochRootKey"))
                throw $util.ProtocolError("missing required 'epochRootKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "mailboxRootKey"))
                throw $util.ProtocolError("missing required 'mailboxRootKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "orfClientState"))
                throw $util.ProtocolError("missing required 'orfClientState'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochAnonId"))
                throw $util.ProtocolError("missing required 'epochAnonId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochId"))
                throw $util.ProtocolError("missing required 'epochId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "threadId"))
                throw $util.ProtocolError("missing required 'threadId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "waCanonicalUserFbid"))
                throw $util.ProtocolError("missing required 'waCanonicalUserFbid'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "timestampMs"))
                throw $util.ProtocolError("missing required 'timestampMs'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "backupId"))
                throw $util.ProtocolError("missing required 'backupId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "plaintextPayload"))
                throw $util.ProtocolError("missing required 'plaintextPayload'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "stanzaId"))
                throw $util.ProtocolError("missing required 'stanzaId'", { instance: m });
            return m;
        };

        EncryptMessageInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.EncryptMessageInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EncryptMessageInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EncryptMessageInput();
            if (d.epochRootKey != null) {
                if (typeof d.epochRootKey === "string")
                    $util.base64.decode(d.epochRootKey, m.epochRootKey = $util.newBuffer($util.base64.length(d.epochRootKey)), 0);
                else if (d.epochRootKey.length >= 0)
                    m.epochRootKey = d.epochRootKey;
            }
            if (d.mailboxRootKey != null) {
                if (typeof d.mailboxRootKey === "string")
                    $util.base64.decode(d.mailboxRootKey, m.mailboxRootKey = $util.newBuffer($util.base64.length(d.mailboxRootKey)), 0);
                else if (d.mailboxRootKey.length >= 0)
                    m.mailboxRootKey = d.mailboxRootKey;
            }
            if (d.orfClientState != null) {
                if (typeof d.orfClientState === "string")
                    $util.base64.decode(d.orfClientState, m.orfClientState = $util.newBuffer($util.base64.length(d.orfClientState)), 0);
                else if (d.orfClientState.length >= 0)
                    m.orfClientState = d.orfClientState;
            }
            if (d.epochAnonId != null) {
                if (typeof d.epochAnonId === "string")
                    $util.base64.decode(d.epochAnonId, m.epochAnonId = $util.newBuffer($util.base64.length(d.epochAnonId)), 0);
                else if (d.epochAnonId.length >= 0)
                    m.epochAnonId = d.epochAnonId;
            }
            if (d.epochId != null) {
                if ($util.Long)
                    m.epochId = $util.Long.fromValue(d.epochId, true);
                else if (typeof d.epochId === "string")
                    m.epochId = $parseInt(d.epochId, 10);
                else if (typeof d.epochId === "number")
                    m.epochId = d.epochId;
                else if (typeof d.epochId === "object")
                    m.epochId = new $util.LongBits(d.epochId.low >>> 0, d.epochId.high >>> 0).toNumber(true);
            }
            if (d.threadId != null) {
                m.threadId = $String(d.threadId);
            }
            if (d.waCanonicalUserFbid != null) {
                if ($util.Long)
                    m.waCanonicalUserFbid = $util.Long.fromValue(d.waCanonicalUserFbid, true);
                else if (typeof d.waCanonicalUserFbid === "string")
                    m.waCanonicalUserFbid = $parseInt(d.waCanonicalUserFbid, 10);
                else if (typeof d.waCanonicalUserFbid === "number")
                    m.waCanonicalUserFbid = d.waCanonicalUserFbid;
                else if (typeof d.waCanonicalUserFbid === "object")
                    m.waCanonicalUserFbid = new $util.LongBits(d.waCanonicalUserFbid.low >>> 0, d.waCanonicalUserFbid.high >>> 0).toNumber(true);
            }
            if (d.timestampMs != null) {
                if ($util.Long)
                    m.timestampMs = $util.Long.fromValue(d.timestampMs, true);
                else if (typeof d.timestampMs === "string")
                    m.timestampMs = $parseInt(d.timestampMs, 10);
                else if (typeof d.timestampMs === "number")
                    m.timestampMs = d.timestampMs;
                else if (typeof d.timestampMs === "object")
                    m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber(true);
            }
            if (d.backupId != null) {
                if ($util.Long)
                    m.backupId = $util.Long.fromValue(d.backupId, true);
                else if (typeof d.backupId === "string")
                    m.backupId = $parseInt(d.backupId, 10);
                else if (typeof d.backupId === "number")
                    m.backupId = d.backupId;
                else if (typeof d.backupId === "object")
                    m.backupId = new $util.LongBits(d.backupId.low >>> 0, d.backupId.high >>> 0).toNumber(true);
            }
            if (d.plaintextPayload != null) {
                if (typeof d.plaintextPayload === "string")
                    $util.base64.decode(d.plaintextPayload, m.plaintextPayload = $util.newBuffer($util.base64.length(d.plaintextPayload)), 0);
                else if (d.plaintextPayload.length >= 0)
                    m.plaintextPayload = d.plaintextPayload;
            }
            if (d.stanzaId != null) {
                m.stanzaId = $String(d.stanzaId);
            }
            return m;
        };

        EncryptMessageInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if (o.bytes === $String)
                    d.epochRootKey = "";
                else {
                    d.epochRootKey = [];
                    if (o.bytes !== $Array)
                        d.epochRootKey = $util.newBuffer(d.epochRootKey);
                }
                if (o.bytes === $String)
                    d.mailboxRootKey = "";
                else {
                    d.mailboxRootKey = [];
                    if (o.bytes !== $Array)
                        d.mailboxRootKey = $util.newBuffer(d.mailboxRootKey);
                }
                if (o.bytes === $String)
                    d.orfClientState = "";
                else {
                    d.orfClientState = [];
                    if (o.bytes !== $Array)
                        d.orfClientState = $util.newBuffer(d.orfClientState);
                }
                if (o.bytes === $String)
                    d.epochAnonId = "";
                else {
                    d.epochAnonId = [];
                    if (o.bytes !== $Array)
                        d.epochAnonId = $util.newBuffer(d.epochAnonId);
                }
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.epochId = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.epochId = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                d.threadId = "";
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.waCanonicalUserFbid = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.waCanonicalUserFbid = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.timestampMs = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.timestampMs = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.backupId = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.backupId = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                if (o.bytes === $String)
                    d.plaintextPayload = "";
                else {
                    d.plaintextPayload = [];
                    if (o.bytes !== $Array)
                        d.plaintextPayload = $util.newBuffer(d.plaintextPayload);
                }
                d.stanzaId = "";
            }
            if (m.epochRootKey != null && $Object.hasOwnProperty.call(m, "epochRootKey")) {
                d.epochRootKey = o.bytes === $String ? $util.base64.encode(m.epochRootKey, 0, m.epochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochRootKey) : m.epochRootKey;
            }
            if (m.mailboxRootKey != null && $Object.hasOwnProperty.call(m, "mailboxRootKey")) {
                d.mailboxRootKey = o.bytes === $String ? $util.base64.encode(m.mailboxRootKey, 0, m.mailboxRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mailboxRootKey) : m.mailboxRootKey;
            }
            if (m.orfClientState != null && $Object.hasOwnProperty.call(m, "orfClientState")) {
                d.orfClientState = o.bytes === $String ? $util.base64.encode(m.orfClientState, 0, m.orfClientState.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.orfClientState) : m.orfClientState;
            }
            if (m.epochAnonId != null && $Object.hasOwnProperty.call(m, "epochAnonId")) {
                d.epochAnonId = o.bytes === $String ? $util.base64.encode(m.epochAnonId, 0, m.epochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAnonId) : m.epochAnonId;
            }
            if (m.epochId != null && $Object.hasOwnProperty.call(m, "epochId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.epochId = typeof m.epochId === "number" ? $BigInt(m.epochId) : $util.Long.fromBits(m.epochId.low >>> 0, m.epochId.high >>> 0, true).toBigInt();
                else if (typeof m.epochId === "number")
                    d.epochId = o.longs === $String ? $String(m.epochId) : m.epochId;
                else
                    d.epochId = o.longs === $String ? $util.Long.prototype.toString.call(m.epochId) : o.longs === $Number ? new $util.LongBits(m.epochId.low >>> 0, m.epochId.high >>> 0).toNumber(true) : m.epochId;
            }
            if (m.threadId != null && $Object.hasOwnProperty.call(m, "threadId")) {
                d.threadId = m.threadId;
            }
            if (m.waCanonicalUserFbid != null && $Object.hasOwnProperty.call(m, "waCanonicalUserFbid")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.waCanonicalUserFbid = typeof m.waCanonicalUserFbid === "number" ? $BigInt(m.waCanonicalUserFbid) : $util.Long.fromBits(m.waCanonicalUserFbid.low >>> 0, m.waCanonicalUserFbid.high >>> 0, true).toBigInt();
                else if (typeof m.waCanonicalUserFbid === "number")
                    d.waCanonicalUserFbid = o.longs === $String ? $String(m.waCanonicalUserFbid) : m.waCanonicalUserFbid;
                else
                    d.waCanonicalUserFbid = o.longs === $String ? $util.Long.prototype.toString.call(m.waCanonicalUserFbid) : o.longs === $Number ? new $util.LongBits(m.waCanonicalUserFbid.low >>> 0, m.waCanonicalUserFbid.high >>> 0).toNumber(true) : m.waCanonicalUserFbid;
            }
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, true).toBigInt();
                else if (typeof m.timestampMs === "number")
                    d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                else
                    d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber(true) : m.timestampMs;
            }
            if (m.backupId != null && $Object.hasOwnProperty.call(m, "backupId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.backupId = typeof m.backupId === "number" ? $BigInt(m.backupId) : $util.Long.fromBits(m.backupId.low >>> 0, m.backupId.high >>> 0, true).toBigInt();
                else if (typeof m.backupId === "number")
                    d.backupId = o.longs === $String ? $String(m.backupId) : m.backupId;
                else
                    d.backupId = o.longs === $String ? $util.Long.prototype.toString.call(m.backupId) : o.longs === $Number ? new $util.LongBits(m.backupId.low >>> 0, m.backupId.high >>> 0).toNumber(true) : m.backupId;
            }
            if (m.plaintextPayload != null && $Object.hasOwnProperty.call(m, "plaintextPayload")) {
                d.plaintextPayload = o.bytes === $String ? $util.base64.encode(m.plaintextPayload, 0, m.plaintextPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.plaintextPayload) : m.plaintextPayload;
            }
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                d.stanzaId = m.stanzaId;
            }
            return d;
        };

        EncryptMessageInput.prototype.toJSON = function() {
            return EncryptMessageInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncryptMessageInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EncryptMessageInput";
        };

        return EncryptMessageInput;
    })();

    proto.EncryptMessageOutput = (function() {

        const EncryptMessageOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EncryptMessageOutput.prototype.encryptedProtobuf = null;
        EncryptMessageOutput.prototype.orfThreadId = null;
        EncryptMessageOutput.prototype.valueSecretRef = null;
        EncryptMessageOutput.prototype.offlineThreadingId = null;
        EncryptMessageOutput.prototype.timestampMs = null;
        EncryptMessageOutput.prototype.error = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptMessageOutput.prototype, "_encryptedProtobuf", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedProtobuf"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptMessageOutput.prototype, "_orfThreadId", {
            get: $util.oneOfGetter($oneOfFields = ["orfThreadId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptMessageOutput.prototype, "_valueSecretRef", {
            get: $util.oneOfGetter($oneOfFields = ["valueSecretRef"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptMessageOutput.prototype, "_offlineThreadingId", {
            get: $util.oneOfGetter($oneOfFields = ["offlineThreadingId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptMessageOutput.prototype, "_timestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptMessageOutput.prototype, "_error", {
            get: $util.oneOfGetter($oneOfFields = ["error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncryptMessageOutput.create = function(properties) {
            return new EncryptMessageOutput(properties);
        };

        EncryptMessageOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.encryptedProtobuf != null && $Object.hasOwnProperty.call(m, "encryptedProtobuf"))
                w.uint32(10).bytes(m.encryptedProtobuf);
            if (m.orfThreadId != null && $Object.hasOwnProperty.call(m, "orfThreadId"))
                w.uint32(18).bytes(m.orfThreadId);
            if (m.valueSecretRef != null && $Object.hasOwnProperty.call(m, "valueSecretRef"))
                w.uint32(26).string(m.valueSecretRef);
            if (m.offlineThreadingId != null && $Object.hasOwnProperty.call(m, "offlineThreadingId"))
                w.uint32(32).uint64(m.offlineThreadingId);
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                w.uint32(40).uint64(m.timestampMs);
            if (m.error != null && $Object.hasOwnProperty.call(m, "error"))
                w.uint32(58).string(m.error);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EncryptMessageOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EncryptMessageOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.encryptedProtobuf = r.bytes();
                        m._encryptedProtobuf = "encryptedProtobuf";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.orfThreadId = r.bytes();
                        m._orfThreadId = "orfThreadId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.valueSecretRef = r.stringVerify();
                        m._valueSecretRef = "valueSecretRef";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.offlineThreadingId = r.uint64();
                        m._offlineThreadingId = "offlineThreadingId";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.timestampMs = r.uint64();
                        m._timestampMs = "timestampMs";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.error = r.stringVerify();
                        m._error = "error";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EncryptMessageOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.EncryptMessageOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EncryptMessageOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EncryptMessageOutput();
            if (d.encryptedProtobuf != null) {
                if (typeof d.encryptedProtobuf === "string")
                    $util.base64.decode(d.encryptedProtobuf, m.encryptedProtobuf = $util.newBuffer($util.base64.length(d.encryptedProtobuf)), 0);
                else if (d.encryptedProtobuf.length >= 0)
                    m.encryptedProtobuf = d.encryptedProtobuf;
            }
            if (d.orfThreadId != null) {
                if (typeof d.orfThreadId === "string")
                    $util.base64.decode(d.orfThreadId, m.orfThreadId = $util.newBuffer($util.base64.length(d.orfThreadId)), 0);
                else if (d.orfThreadId.length >= 0)
                    m.orfThreadId = d.orfThreadId;
            }
            if (d.valueSecretRef != null) {
                m.valueSecretRef = $String(d.valueSecretRef);
            }
            if (d.offlineThreadingId != null) {
                if ($util.Long)
                    m.offlineThreadingId = $util.Long.fromValue(d.offlineThreadingId, true);
                else if (typeof d.offlineThreadingId === "string")
                    m.offlineThreadingId = $parseInt(d.offlineThreadingId, 10);
                else if (typeof d.offlineThreadingId === "number")
                    m.offlineThreadingId = d.offlineThreadingId;
                else if (typeof d.offlineThreadingId === "object")
                    m.offlineThreadingId = new $util.LongBits(d.offlineThreadingId.low >>> 0, d.offlineThreadingId.high >>> 0).toNumber(true);
            }
            if (d.timestampMs != null) {
                if ($util.Long)
                    m.timestampMs = $util.Long.fromValue(d.timestampMs, true);
                else if (typeof d.timestampMs === "string")
                    m.timestampMs = $parseInt(d.timestampMs, 10);
                else if (typeof d.timestampMs === "number")
                    m.timestampMs = d.timestampMs;
                else if (typeof d.timestampMs === "object")
                    m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber(true);
            }
            if (d.error != null) {
                m.error = $String(d.error);
            }
            return m;
        };

        EncryptMessageOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.encryptedProtobuf != null && $Object.hasOwnProperty.call(m, "encryptedProtobuf")) {
                d.encryptedProtobuf = o.bytes === $String ? $util.base64.encode(m.encryptedProtobuf, 0, m.encryptedProtobuf.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedProtobuf) : m.encryptedProtobuf;
            }
            if (m.orfThreadId != null && $Object.hasOwnProperty.call(m, "orfThreadId")) {
                d.orfThreadId = o.bytes === $String ? $util.base64.encode(m.orfThreadId, 0, m.orfThreadId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.orfThreadId) : m.orfThreadId;
            }
            if (m.valueSecretRef != null && $Object.hasOwnProperty.call(m, "valueSecretRef")) {
                d.valueSecretRef = m.valueSecretRef;
            }
            if (m.offlineThreadingId != null && $Object.hasOwnProperty.call(m, "offlineThreadingId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.offlineThreadingId = typeof m.offlineThreadingId === "number" ? $BigInt(m.offlineThreadingId) : $util.Long.fromBits(m.offlineThreadingId.low >>> 0, m.offlineThreadingId.high >>> 0, true).toBigInt();
                else if (typeof m.offlineThreadingId === "number")
                    d.offlineThreadingId = o.longs === $String ? $String(m.offlineThreadingId) : m.offlineThreadingId;
                else
                    d.offlineThreadingId = o.longs === $String ? $util.Long.prototype.toString.call(m.offlineThreadingId) : o.longs === $Number ? new $util.LongBits(m.offlineThreadingId.low >>> 0, m.offlineThreadingId.high >>> 0).toNumber(true) : m.offlineThreadingId;
            }
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, true).toBigInt();
                else if (typeof m.timestampMs === "number")
                    d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                else
                    d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber(true) : m.timestampMs;
            }
            if (m.error != null && $Object.hasOwnProperty.call(m, "error")) {
                d.error = m.error;
            }
            return d;
        };

        EncryptMessageOutput.prototype.toJSON = function() {
            return EncryptMessageOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncryptMessageOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EncryptMessageOutput";
        };

        return EncryptMessageOutput;
    })();

    proto.EncryptedPairingRequest = (function() {

        const EncryptedPairingRequest = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EncryptedPairingRequest.prototype.encryptedPayload = null;
        EncryptedPairingRequest.prototype.iv = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptedPairingRequest.prototype, "_encryptedPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptedPairingRequest.prototype, "_iv", {
            get: $util.oneOfGetter($oneOfFields = ["iv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncryptedPairingRequest.create = function(properties) {
            return new EncryptedPairingRequest(properties);
        };

        EncryptedPairingRequest.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.encryptedPayload != null && $Object.hasOwnProperty.call(m, "encryptedPayload"))
                w.uint32(10).bytes(m.encryptedPayload);
            if (m.iv != null && $Object.hasOwnProperty.call(m, "iv"))
                w.uint32(18).bytes(m.iv);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EncryptedPairingRequest.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EncryptedPairingRequest();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.encryptedPayload = r.bytes();
                        m._encryptedPayload = "encryptedPayload";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.iv = r.bytes();
                        m._iv = "iv";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EncryptedPairingRequest.fromObject = function (d, q) {
            if (d instanceof $root.proto.EncryptedPairingRequest)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EncryptedPairingRequest: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EncryptedPairingRequest();
            if (d.encryptedPayload != null) {
                if (typeof d.encryptedPayload === "string")
                    $util.base64.decode(d.encryptedPayload, m.encryptedPayload = $util.newBuffer($util.base64.length(d.encryptedPayload)), 0);
                else if (d.encryptedPayload.length >= 0)
                    m.encryptedPayload = d.encryptedPayload;
            }
            if (d.iv != null) {
                if (typeof d.iv === "string")
                    $util.base64.decode(d.iv, m.iv = $util.newBuffer($util.base64.length(d.iv)), 0);
                else if (d.iv.length >= 0)
                    m.iv = d.iv;
            }
            return m;
        };

        EncryptedPairingRequest.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.encryptedPayload != null && $Object.hasOwnProperty.call(m, "encryptedPayload")) {
                d.encryptedPayload = o.bytes === $String ? $util.base64.encode(m.encryptedPayload, 0, m.encryptedPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedPayload) : m.encryptedPayload;
            }
            if (m.iv != null && $Object.hasOwnProperty.call(m, "iv")) {
                d.iv = o.bytes === $String ? $util.base64.encode(m.iv, 0, m.iv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.iv) : m.iv;
            }
            return d;
        };

        EncryptedPairingRequest.prototype.toJSON = function() {
            return EncryptedPairingRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncryptedPairingRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EncryptedPairingRequest";
        };

        return EncryptedPairingRequest;
    })();

    proto.EncryptedSecretValuesOutput = (function() {

        const EncryptedSecretValuesOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EncryptedSecretValuesOutput.prototype.encryptedDevicePrivateKey = $util.newBuffer([]);
        EncryptedSecretValuesOutput.prototype.encryptedObliviousValidationTokenBlob = $util.newBuffer([]);
        EncryptedSecretValuesOutput.prototype.encryptedEpochStoragePrivateKey = $util.newBuffer([]);
        EncryptedSecretValuesOutput.prototype.encryptedOcmfClientState = $util.newBuffer([]);
        EncryptedSecretValuesOutput.prototype.encryptedOrfClientStateV2 = null;
        EncryptedSecretValuesOutput.prototype.encryptedMailboxRootKeyBlob = $util.newBuffer([]);
        EncryptedSecretValuesOutput.prototype.encryptedEpochAnonId = $util.newBuffer([]);
        EncryptedSecretValuesOutput.prototype.encryptedEpochRootKey = $util.newBuffer([]);

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EncryptedSecretValuesOutput.prototype, "_encryptedOrfClientStateV2", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedOrfClientStateV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EncryptedSecretValuesOutput.create = function(properties) {
            return new EncryptedSecretValuesOutput(properties);
        };

        EncryptedSecretValuesOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.encryptedDevicePrivateKey);
            w.uint32(18).bytes(m.encryptedObliviousValidationTokenBlob);
            w.uint32(26).bytes(m.encryptedEpochStoragePrivateKey);
            w.uint32(34).bytes(m.encryptedOcmfClientState);
            if (m.encryptedOrfClientStateV2 != null && $Object.hasOwnProperty.call(m, "encryptedOrfClientStateV2"))
                w.uint32(42).bytes(m.encryptedOrfClientStateV2);
            w.uint32(50).bytes(m.encryptedMailboxRootKeyBlob);
            w.uint32(58).bytes(m.encryptedEpochAnonId);
            w.uint32(66).bytes(m.encryptedEpochRootKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EncryptedSecretValuesOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EncryptedSecretValuesOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.encryptedDevicePrivateKey = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.encryptedObliviousValidationTokenBlob = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.encryptedEpochStoragePrivateKey = r.bytes();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.encryptedOcmfClientState = r.bytes();
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.encryptedOrfClientStateV2 = r.bytes();
                        m._encryptedOrfClientStateV2 = "encryptedOrfClientStateV2";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.encryptedMailboxRootKeyBlob = r.bytes();
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.encryptedEpochAnonId = r.bytes();
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.encryptedEpochRootKey = r.bytes();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "encryptedDevicePrivateKey"))
                throw $util.ProtocolError("missing required 'encryptedDevicePrivateKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedObliviousValidationTokenBlob"))
                throw $util.ProtocolError("missing required 'encryptedObliviousValidationTokenBlob'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedEpochStoragePrivateKey"))
                throw $util.ProtocolError("missing required 'encryptedEpochStoragePrivateKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedOcmfClientState"))
                throw $util.ProtocolError("missing required 'encryptedOcmfClientState'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedMailboxRootKeyBlob"))
                throw $util.ProtocolError("missing required 'encryptedMailboxRootKeyBlob'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedEpochAnonId"))
                throw $util.ProtocolError("missing required 'encryptedEpochAnonId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedEpochRootKey"))
                throw $util.ProtocolError("missing required 'encryptedEpochRootKey'", { instance: m });
            return m;
        };

        EncryptedSecretValuesOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.EncryptedSecretValuesOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EncryptedSecretValuesOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EncryptedSecretValuesOutput();
            if (d.encryptedDevicePrivateKey != null) {
                if (typeof d.encryptedDevicePrivateKey === "string")
                    $util.base64.decode(d.encryptedDevicePrivateKey, m.encryptedDevicePrivateKey = $util.newBuffer($util.base64.length(d.encryptedDevicePrivateKey)), 0);
                else if (d.encryptedDevicePrivateKey.length >= 0)
                    m.encryptedDevicePrivateKey = d.encryptedDevicePrivateKey;
            }
            if (d.encryptedObliviousValidationTokenBlob != null) {
                if (typeof d.encryptedObliviousValidationTokenBlob === "string")
                    $util.base64.decode(d.encryptedObliviousValidationTokenBlob, m.encryptedObliviousValidationTokenBlob = $util.newBuffer($util.base64.length(d.encryptedObliviousValidationTokenBlob)), 0);
                else if (d.encryptedObliviousValidationTokenBlob.length >= 0)
                    m.encryptedObliviousValidationTokenBlob = d.encryptedObliviousValidationTokenBlob;
            }
            if (d.encryptedEpochStoragePrivateKey != null) {
                if (typeof d.encryptedEpochStoragePrivateKey === "string")
                    $util.base64.decode(d.encryptedEpochStoragePrivateKey, m.encryptedEpochStoragePrivateKey = $util.newBuffer($util.base64.length(d.encryptedEpochStoragePrivateKey)), 0);
                else if (d.encryptedEpochStoragePrivateKey.length >= 0)
                    m.encryptedEpochStoragePrivateKey = d.encryptedEpochStoragePrivateKey;
            }
            if (d.encryptedOcmfClientState != null) {
                if (typeof d.encryptedOcmfClientState === "string")
                    $util.base64.decode(d.encryptedOcmfClientState, m.encryptedOcmfClientState = $util.newBuffer($util.base64.length(d.encryptedOcmfClientState)), 0);
                else if (d.encryptedOcmfClientState.length >= 0)
                    m.encryptedOcmfClientState = d.encryptedOcmfClientState;
            }
            if (d.encryptedOrfClientStateV2 != null) {
                if (typeof d.encryptedOrfClientStateV2 === "string")
                    $util.base64.decode(d.encryptedOrfClientStateV2, m.encryptedOrfClientStateV2 = $util.newBuffer($util.base64.length(d.encryptedOrfClientStateV2)), 0);
                else if (d.encryptedOrfClientStateV2.length >= 0)
                    m.encryptedOrfClientStateV2 = d.encryptedOrfClientStateV2;
            }
            if (d.encryptedMailboxRootKeyBlob != null) {
                if (typeof d.encryptedMailboxRootKeyBlob === "string")
                    $util.base64.decode(d.encryptedMailboxRootKeyBlob, m.encryptedMailboxRootKeyBlob = $util.newBuffer($util.base64.length(d.encryptedMailboxRootKeyBlob)), 0);
                else if (d.encryptedMailboxRootKeyBlob.length >= 0)
                    m.encryptedMailboxRootKeyBlob = d.encryptedMailboxRootKeyBlob;
            }
            if (d.encryptedEpochAnonId != null) {
                if (typeof d.encryptedEpochAnonId === "string")
                    $util.base64.decode(d.encryptedEpochAnonId, m.encryptedEpochAnonId = $util.newBuffer($util.base64.length(d.encryptedEpochAnonId)), 0);
                else if (d.encryptedEpochAnonId.length >= 0)
                    m.encryptedEpochAnonId = d.encryptedEpochAnonId;
            }
            if (d.encryptedEpochRootKey != null) {
                if (typeof d.encryptedEpochRootKey === "string")
                    $util.base64.decode(d.encryptedEpochRootKey, m.encryptedEpochRootKey = $util.newBuffer($util.base64.length(d.encryptedEpochRootKey)), 0);
                else if (d.encryptedEpochRootKey.length >= 0)
                    m.encryptedEpochRootKey = d.encryptedEpochRootKey;
            }
            return m;
        };

        EncryptedSecretValuesOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if (o.bytes === $String)
                    d.encryptedDevicePrivateKey = "";
                else {
                    d.encryptedDevicePrivateKey = [];
                    if (o.bytes !== $Array)
                        d.encryptedDevicePrivateKey = $util.newBuffer(d.encryptedDevicePrivateKey);
                }
                if (o.bytes === $String)
                    d.encryptedObliviousValidationTokenBlob = "";
                else {
                    d.encryptedObliviousValidationTokenBlob = [];
                    if (o.bytes !== $Array)
                        d.encryptedObliviousValidationTokenBlob = $util.newBuffer(d.encryptedObliviousValidationTokenBlob);
                }
                if (o.bytes === $String)
                    d.encryptedEpochStoragePrivateKey = "";
                else {
                    d.encryptedEpochStoragePrivateKey = [];
                    if (o.bytes !== $Array)
                        d.encryptedEpochStoragePrivateKey = $util.newBuffer(d.encryptedEpochStoragePrivateKey);
                }
                if (o.bytes === $String)
                    d.encryptedOcmfClientState = "";
                else {
                    d.encryptedOcmfClientState = [];
                    if (o.bytes !== $Array)
                        d.encryptedOcmfClientState = $util.newBuffer(d.encryptedOcmfClientState);
                }
                if (o.bytes === $String)
                    d.encryptedMailboxRootKeyBlob = "";
                else {
                    d.encryptedMailboxRootKeyBlob = [];
                    if (o.bytes !== $Array)
                        d.encryptedMailboxRootKeyBlob = $util.newBuffer(d.encryptedMailboxRootKeyBlob);
                }
                if (o.bytes === $String)
                    d.encryptedEpochAnonId = "";
                else {
                    d.encryptedEpochAnonId = [];
                    if (o.bytes !== $Array)
                        d.encryptedEpochAnonId = $util.newBuffer(d.encryptedEpochAnonId);
                }
                if (o.bytes === $String)
                    d.encryptedEpochRootKey = "";
                else {
                    d.encryptedEpochRootKey = [];
                    if (o.bytes !== $Array)
                        d.encryptedEpochRootKey = $util.newBuffer(d.encryptedEpochRootKey);
                }
            }
            if (m.encryptedDevicePrivateKey != null && $Object.hasOwnProperty.call(m, "encryptedDevicePrivateKey")) {
                d.encryptedDevicePrivateKey = o.bytes === $String ? $util.base64.encode(m.encryptedDevicePrivateKey, 0, m.encryptedDevicePrivateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedDevicePrivateKey) : m.encryptedDevicePrivateKey;
            }
            if (m.encryptedObliviousValidationTokenBlob != null && $Object.hasOwnProperty.call(m, "encryptedObliviousValidationTokenBlob")) {
                d.encryptedObliviousValidationTokenBlob = o.bytes === $String ? $util.base64.encode(m.encryptedObliviousValidationTokenBlob, 0, m.encryptedObliviousValidationTokenBlob.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedObliviousValidationTokenBlob) : m.encryptedObliviousValidationTokenBlob;
            }
            if (m.encryptedEpochStoragePrivateKey != null && $Object.hasOwnProperty.call(m, "encryptedEpochStoragePrivateKey")) {
                d.encryptedEpochStoragePrivateKey = o.bytes === $String ? $util.base64.encode(m.encryptedEpochStoragePrivateKey, 0, m.encryptedEpochStoragePrivateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedEpochStoragePrivateKey) : m.encryptedEpochStoragePrivateKey;
            }
            if (m.encryptedOcmfClientState != null && $Object.hasOwnProperty.call(m, "encryptedOcmfClientState")) {
                d.encryptedOcmfClientState = o.bytes === $String ? $util.base64.encode(m.encryptedOcmfClientState, 0, m.encryptedOcmfClientState.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedOcmfClientState) : m.encryptedOcmfClientState;
            }
            if (m.encryptedOrfClientStateV2 != null && $Object.hasOwnProperty.call(m, "encryptedOrfClientStateV2")) {
                d.encryptedOrfClientStateV2 = o.bytes === $String ? $util.base64.encode(m.encryptedOrfClientStateV2, 0, m.encryptedOrfClientStateV2.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedOrfClientStateV2) : m.encryptedOrfClientStateV2;
            }
            if (m.encryptedMailboxRootKeyBlob != null && $Object.hasOwnProperty.call(m, "encryptedMailboxRootKeyBlob")) {
                d.encryptedMailboxRootKeyBlob = o.bytes === $String ? $util.base64.encode(m.encryptedMailboxRootKeyBlob, 0, m.encryptedMailboxRootKeyBlob.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedMailboxRootKeyBlob) : m.encryptedMailboxRootKeyBlob;
            }
            if (m.encryptedEpochAnonId != null && $Object.hasOwnProperty.call(m, "encryptedEpochAnonId")) {
                d.encryptedEpochAnonId = o.bytes === $String ? $util.base64.encode(m.encryptedEpochAnonId, 0, m.encryptedEpochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedEpochAnonId) : m.encryptedEpochAnonId;
            }
            if (m.encryptedEpochRootKey != null && $Object.hasOwnProperty.call(m, "encryptedEpochRootKey")) {
                d.encryptedEpochRootKey = o.bytes === $String ? $util.base64.encode(m.encryptedEpochRootKey, 0, m.encryptedEpochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedEpochRootKey) : m.encryptedEpochRootKey;
            }
            return d;
        };

        EncryptedSecretValuesOutput.prototype.toJSON = function() {
            return EncryptedSecretValuesOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        EncryptedSecretValuesOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EncryptedSecretValuesOutput";
        };

        return EncryptedSecretValuesOutput;
    })();

    proto.EphemeralSetting = (function() {

        const EphemeralSetting = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EphemeralSetting.prototype.duration = null;
        EphemeralSetting.prototype.timestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EphemeralSetting.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EphemeralSetting.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EphemeralSetting.create = function(properties) {
            return new EphemeralSetting(properties);
        };

        EphemeralSetting.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.duration != null && $Object.hasOwnProperty.call(m, "duration"))
                w.uint32(13).sfixed32(m.duration);
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(17).sfixed64(m.timestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EphemeralSetting.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EphemeralSetting();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 5)
                            break;
                        m.duration = r.sfixed32();
                        m._duration = "duration";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        m.timestamp = r.sfixed64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EphemeralSetting.fromObject = function (d, q) {
            if (d instanceof $root.proto.EphemeralSetting)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EphemeralSetting: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EphemeralSetting();
            if (d.duration != null) {
                m.duration = d.duration | 0;
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, false);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
            }
            return m;
        };

        EphemeralSetting.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.duration != null && $Object.hasOwnProperty.call(m, "duration")) {
                d.duration = m.duration;
            }
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
            }
            return d;
        };

        EphemeralSetting.prototype.toJSON = function() {
            return EphemeralSetting.toObject(this, $protobuf.util.toJSONOptions);
        };

        EphemeralSetting.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EphemeralSetting";
        };

        return EphemeralSetting;
    })();

    proto.Epoch0Output = (function() {

        const Epoch0Output = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Epoch0Output.prototype.epochFbid = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        Epoch0Output.prototype.epochAnonId = $util.newBuffer([]);
        Epoch0Output.prototype.epochData = $util.newBuffer([]);
        Epoch0Output.prototype.wrappedRootKeyForSelf = $util.newBuffer([]);
        Epoch0Output.prototype.epochSignature = $util.newBuffer([]);
        Epoch0Output.prototype.epochRootKeyFingerprint = $util.newBuffer([]);
        Epoch0Output.prototype.epochRootKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Epoch0Output.prototype, "_epochRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["epochRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Epoch0Output.create = function(properties) {
            return new Epoch0Output(properties);
        };

        Epoch0Output.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(8).uint64(m.epochFbid);
            w.uint32(18).bytes(m.epochAnonId);
            w.uint32(26).bytes(m.epochData);
            w.uint32(34).bytes(m.wrappedRootKeyForSelf);
            w.uint32(42).bytes(m.epochSignature);
            w.uint32(50).bytes(m.epochRootKeyFingerprint);
            if (m.epochRootKey != null && $Object.hasOwnProperty.call(m, "epochRootKey"))
                w.uint32(58).bytes(m.epochRootKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Epoch0Output.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Epoch0Output();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.epochFbid = r.uint64();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.epochAnonId = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.epochData = r.bytes();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.wrappedRootKeyForSelf = r.bytes();
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.epochSignature = r.bytes();
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.epochRootKeyFingerprint = r.bytes();
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.epochRootKey = r.bytes();
                        m._epochRootKey = "epochRootKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "epochFbid"))
                throw $util.ProtocolError("missing required 'epochFbid'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochAnonId"))
                throw $util.ProtocolError("missing required 'epochAnonId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochData"))
                throw $util.ProtocolError("missing required 'epochData'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "wrappedRootKeyForSelf"))
                throw $util.ProtocolError("missing required 'wrappedRootKeyForSelf'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochSignature"))
                throw $util.ProtocolError("missing required 'epochSignature'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochRootKeyFingerprint"))
                throw $util.ProtocolError("missing required 'epochRootKeyFingerprint'", { instance: m });
            return m;
        };

        Epoch0Output.fromObject = function (d, q) {
            if (d instanceof $root.proto.Epoch0Output)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Epoch0Output: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Epoch0Output();
            if (d.epochFbid != null) {
                if ($util.Long)
                    m.epochFbid = $util.Long.fromValue(d.epochFbid, true);
                else if (typeof d.epochFbid === "string")
                    m.epochFbid = $parseInt(d.epochFbid, 10);
                else if (typeof d.epochFbid === "number")
                    m.epochFbid = d.epochFbid;
                else if (typeof d.epochFbid === "object")
                    m.epochFbid = new $util.LongBits(d.epochFbid.low >>> 0, d.epochFbid.high >>> 0).toNumber(true);
            }
            if (d.epochAnonId != null) {
                if (typeof d.epochAnonId === "string")
                    $util.base64.decode(d.epochAnonId, m.epochAnonId = $util.newBuffer($util.base64.length(d.epochAnonId)), 0);
                else if (d.epochAnonId.length >= 0)
                    m.epochAnonId = d.epochAnonId;
            }
            if (d.epochData != null) {
                if (typeof d.epochData === "string")
                    $util.base64.decode(d.epochData, m.epochData = $util.newBuffer($util.base64.length(d.epochData)), 0);
                else if (d.epochData.length >= 0)
                    m.epochData = d.epochData;
            }
            if (d.wrappedRootKeyForSelf != null) {
                if (typeof d.wrappedRootKeyForSelf === "string")
                    $util.base64.decode(d.wrappedRootKeyForSelf, m.wrappedRootKeyForSelf = $util.newBuffer($util.base64.length(d.wrappedRootKeyForSelf)), 0);
                else if (d.wrappedRootKeyForSelf.length >= 0)
                    m.wrappedRootKeyForSelf = d.wrappedRootKeyForSelf;
            }
            if (d.epochSignature != null) {
                if (typeof d.epochSignature === "string")
                    $util.base64.decode(d.epochSignature, m.epochSignature = $util.newBuffer($util.base64.length(d.epochSignature)), 0);
                else if (d.epochSignature.length >= 0)
                    m.epochSignature = d.epochSignature;
            }
            if (d.epochRootKeyFingerprint != null) {
                if (typeof d.epochRootKeyFingerprint === "string")
                    $util.base64.decode(d.epochRootKeyFingerprint, m.epochRootKeyFingerprint = $util.newBuffer($util.base64.length(d.epochRootKeyFingerprint)), 0);
                else if (d.epochRootKeyFingerprint.length >= 0)
                    m.epochRootKeyFingerprint = d.epochRootKeyFingerprint;
            }
            if (d.epochRootKey != null) {
                if (typeof d.epochRootKey === "string")
                    $util.base64.decode(d.epochRootKey, m.epochRootKey = $util.newBuffer($util.base64.length(d.epochRootKey)), 0);
                else if (d.epochRootKey.length >= 0)
                    m.epochRootKey = d.epochRootKey;
            }
            return m;
        };

        Epoch0Output.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.epochFbid = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.epochFbid = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                if (o.bytes === $String)
                    d.epochAnonId = "";
                else {
                    d.epochAnonId = [];
                    if (o.bytes !== $Array)
                        d.epochAnonId = $util.newBuffer(d.epochAnonId);
                }
                if (o.bytes === $String)
                    d.epochData = "";
                else {
                    d.epochData = [];
                    if (o.bytes !== $Array)
                        d.epochData = $util.newBuffer(d.epochData);
                }
                if (o.bytes === $String)
                    d.wrappedRootKeyForSelf = "";
                else {
                    d.wrappedRootKeyForSelf = [];
                    if (o.bytes !== $Array)
                        d.wrappedRootKeyForSelf = $util.newBuffer(d.wrappedRootKeyForSelf);
                }
                if (o.bytes === $String)
                    d.epochSignature = "";
                else {
                    d.epochSignature = [];
                    if (o.bytes !== $Array)
                        d.epochSignature = $util.newBuffer(d.epochSignature);
                }
                if (o.bytes === $String)
                    d.epochRootKeyFingerprint = "";
                else {
                    d.epochRootKeyFingerprint = [];
                    if (o.bytes !== $Array)
                        d.epochRootKeyFingerprint = $util.newBuffer(d.epochRootKeyFingerprint);
                }
            }
            if (m.epochFbid != null && $Object.hasOwnProperty.call(m, "epochFbid")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.epochFbid = typeof m.epochFbid === "number" ? $BigInt(m.epochFbid) : $util.Long.fromBits(m.epochFbid.low >>> 0, m.epochFbid.high >>> 0, true).toBigInt();
                else if (typeof m.epochFbid === "number")
                    d.epochFbid = o.longs === $String ? $String(m.epochFbid) : m.epochFbid;
                else
                    d.epochFbid = o.longs === $String ? $util.Long.prototype.toString.call(m.epochFbid) : o.longs === $Number ? new $util.LongBits(m.epochFbid.low >>> 0, m.epochFbid.high >>> 0).toNumber(true) : m.epochFbid;
            }
            if (m.epochAnonId != null && $Object.hasOwnProperty.call(m, "epochAnonId")) {
                d.epochAnonId = o.bytes === $String ? $util.base64.encode(m.epochAnonId, 0, m.epochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAnonId) : m.epochAnonId;
            }
            if (m.epochData != null && $Object.hasOwnProperty.call(m, "epochData")) {
                d.epochData = o.bytes === $String ? $util.base64.encode(m.epochData, 0, m.epochData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochData) : m.epochData;
            }
            if (m.wrappedRootKeyForSelf != null && $Object.hasOwnProperty.call(m, "wrappedRootKeyForSelf")) {
                d.wrappedRootKeyForSelf = o.bytes === $String ? $util.base64.encode(m.wrappedRootKeyForSelf, 0, m.wrappedRootKeyForSelf.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.wrappedRootKeyForSelf) : m.wrappedRootKeyForSelf;
            }
            if (m.epochSignature != null && $Object.hasOwnProperty.call(m, "epochSignature")) {
                d.epochSignature = o.bytes === $String ? $util.base64.encode(m.epochSignature, 0, m.epochSignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochSignature) : m.epochSignature;
            }
            if (m.epochRootKeyFingerprint != null && $Object.hasOwnProperty.call(m, "epochRootKeyFingerprint")) {
                d.epochRootKeyFingerprint = o.bytes === $String ? $util.base64.encode(m.epochRootKeyFingerprint, 0, m.epochRootKeyFingerprint.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochRootKeyFingerprint) : m.epochRootKeyFingerprint;
            }
            if (m.epochRootKey != null && $Object.hasOwnProperty.call(m, "epochRootKey")) {
                d.epochRootKey = o.bytes === $String ? $util.base64.encode(m.epochRootKey, 0, m.epochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochRootKey) : m.epochRootKey;
            }
            return d;
        };

        Epoch0Output.prototype.toJSON = function() {
            return Epoch0Output.toObject(this, $protobuf.util.toJSONOptions);
        };

        Epoch0Output.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Epoch0Output";
        };

        return Epoch0Output;
    })();

    proto.EventAdditionalMetadata = (function() {

        const EventAdditionalMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EventAdditionalMetadata.prototype.isStale = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EventAdditionalMetadata.prototype, "_isStale", {
            get: $util.oneOfGetter($oneOfFields = ["isStale"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventAdditionalMetadata.create = function(properties) {
            return new EventAdditionalMetadata(properties);
        };

        EventAdditionalMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.isStale != null && $Object.hasOwnProperty.call(m, "isStale"))
                w.uint32(8).bool(m.isStale);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EventAdditionalMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EventAdditionalMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.isStale = r.bool();
                        m._isStale = "isStale";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EventAdditionalMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.EventAdditionalMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EventAdditionalMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EventAdditionalMetadata();
            if (d.isStale != null) {
                m.isStale = $Boolean(d.isStale);
            }
            return m;
        };

        EventAdditionalMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.isStale != null && $Object.hasOwnProperty.call(m, "isStale")) {
                d.isStale = m.isStale;
            }
            return d;
        };

        EventAdditionalMetadata.prototype.toJSON = function() {
            return EventAdditionalMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventAdditionalMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EventAdditionalMetadata";
        };

        return EventAdditionalMetadata;
    })();

    proto.EventResponse = (function() {

        const EventResponse = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        EventResponse.prototype.eventResponseMessageKey = null;
        EventResponse.prototype.timestampMs = null;
        EventResponse.prototype.eventResponseMessage = null;
        EventResponse.prototype.unread = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EventResponse.prototype, "_eventResponseMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EventResponse.prototype, "_timestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EventResponse.prototype, "_eventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(EventResponse.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        EventResponse.create = function(properties) {
            return new EventResponse(properties);
        };

        EventResponse.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.eventResponseMessageKey != null && $Object.hasOwnProperty.call(m, "eventResponseMessageKey"))
                $root.proto.MessageKey.encode(m.eventResponseMessageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                w.uint32(16).int64(m.timestampMs);
            if (m.eventResponseMessage != null && $Object.hasOwnProperty.call(m, "eventResponseMessage"))
                $root.proto.Message.EventResponseMessage.encode(m.eventResponseMessage, w.uint32(26).fork(), q + 1).ldelim();
            if (m.unread != null && $Object.hasOwnProperty.call(m, "unread"))
                w.uint32(32).bool(m.unread);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        EventResponse.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.EventResponse();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.eventResponseMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.eventResponseMessageKey);
                        m._eventResponseMessageKey = "eventResponseMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.timestampMs = r.int64();
                        m._timestampMs = "timestampMs";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.eventResponseMessage = $root.proto.Message.EventResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.eventResponseMessage);
                        m._eventResponseMessage = "eventResponseMessage";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.unread = r.bool();
                        m._unread = "unread";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        EventResponse.fromObject = function (d, q) {
            if (d instanceof $root.proto.EventResponse)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.EventResponse: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.EventResponse();
            if (d.eventResponseMessageKey != null) {
                if (!$util.isObject(d.eventResponseMessageKey))
                    throw $TypeError(".proto.EventResponse.eventResponseMessageKey: object expected");
                m.eventResponseMessageKey = $root.proto.MessageKey.fromObject(d.eventResponseMessageKey, q + 1);
            }
            if (d.timestampMs != null) {
                if ($util.Long)
                    m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                else if (typeof d.timestampMs === "string")
                    m.timestampMs = $parseInt(d.timestampMs, 10);
                else if (typeof d.timestampMs === "number")
                    m.timestampMs = d.timestampMs;
                else if (typeof d.timestampMs === "object")
                    m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
            }
            if (d.eventResponseMessage != null) {
                if (!$util.isObject(d.eventResponseMessage))
                    throw $TypeError(".proto.EventResponse.eventResponseMessage: object expected");
                m.eventResponseMessage = $root.proto.Message.EventResponseMessage.fromObject(d.eventResponseMessage, q + 1);
            }
            if (d.unread != null) {
                m.unread = $Boolean(d.unread);
            }
            return m;
        };

        EventResponse.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.eventResponseMessageKey != null && $Object.hasOwnProperty.call(m, "eventResponseMessageKey")) {
                d.eventResponseMessageKey = $root.proto.MessageKey.toObject(m.eventResponseMessageKey, o, q + 1);
            }
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.timestampMs === "number")
                    d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                else
                    d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
            }
            if (m.eventResponseMessage != null && $Object.hasOwnProperty.call(m, "eventResponseMessage")) {
                d.eventResponseMessage = $root.proto.Message.EventResponseMessage.toObject(m.eventResponseMessage, o, q + 1);
            }
            if (m.unread != null && $Object.hasOwnProperty.call(m, "unread")) {
                d.unread = m.unread;
            }
            return d;
        };

        EventResponse.prototype.toJSON = function() {
            return EventResponse.toObject(this, $protobuf.util.toJSONOptions);
        };

        EventResponse.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.EventResponse";
        };

        return EventResponse;
    })();

    proto.ExitCode = (function() {

        const ExitCode = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ExitCode.prototype.code = null;
        ExitCode.prototype.text = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExitCode.prototype, "_code", {
            get: $util.oneOfGetter($oneOfFields = ["code"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExitCode.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ExitCode.create = function(properties) {
            return new ExitCode(properties);
        };

        ExitCode.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.code != null && $Object.hasOwnProperty.call(m, "code"))
                w.uint32(8).uint64(m.code);
            if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ExitCode.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ExitCode();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.code = r.uint64();
                        m._code = "code";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.text = r.stringVerify();
                        m._text = "text";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ExitCode.fromObject = function (d, q) {
            if (d instanceof $root.proto.ExitCode)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ExitCode: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ExitCode();
            if (d.code != null) {
                if ($util.Long)
                    m.code = $util.Long.fromValue(d.code, true);
                else if (typeof d.code === "string")
                    m.code = $parseInt(d.code, 10);
                else if (typeof d.code === "number")
                    m.code = d.code;
                else if (typeof d.code === "object")
                    m.code = new $util.LongBits(d.code.low >>> 0, d.code.high >>> 0).toNumber(true);
            }
            if (d.text != null) {
                m.text = $String(d.text);
            }
            return m;
        };

        ExitCode.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.code != null && $Object.hasOwnProperty.call(m, "code")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.code = typeof m.code === "number" ? $BigInt(m.code) : $util.Long.fromBits(m.code.low >>> 0, m.code.high >>> 0, true).toBigInt();
                else if (typeof m.code === "number")
                    d.code = o.longs === $String ? $String(m.code) : m.code;
                else
                    d.code = o.longs === $String ? $util.Long.prototype.toString.call(m.code) : o.longs === $Number ? new $util.LongBits(m.code.low >>> 0, m.code.high >>> 0).toNumber(true) : m.code;
            }
            if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                d.text = m.text;
            }
            return d;
        };

        ExitCode.prototype.toJSON = function() {
            return ExitCode.toObject(this, $protobuf.util.toJSONOptions);
        };

        ExitCode.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ExitCode";
        };

        return ExitCode;
    })();

    proto.ExternalBlobReference = (function() {

        const ExternalBlobReference = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ExternalBlobReference.prototype.mediaKey = null;
        ExternalBlobReference.prototype.directPath = null;
        ExternalBlobReference.prototype.handle = null;
        ExternalBlobReference.prototype.fileSizeBytes = null;
        ExternalBlobReference.prototype.fileSha256 = null;
        ExternalBlobReference.prototype.fileEncSha256 = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExternalBlobReference.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExternalBlobReference.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExternalBlobReference.prototype, "_handle", {
            get: $util.oneOfGetter($oneOfFields = ["handle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExternalBlobReference.prototype, "_fileSizeBytes", {
            get: $util.oneOfGetter($oneOfFields = ["fileSizeBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExternalBlobReference.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ExternalBlobReference.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ExternalBlobReference.create = function(properties) {
            return new ExternalBlobReference(properties);
        };

        ExternalBlobReference.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(10).bytes(m.mediaKey);
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(18).string(m.directPath);
            if (m.handle != null && $Object.hasOwnProperty.call(m, "handle"))
                w.uint32(26).string(m.handle);
            if (m.fileSizeBytes != null && $Object.hasOwnProperty.call(m, "fileSizeBytes"))
                w.uint32(32).uint64(m.fileSizeBytes);
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                w.uint32(42).bytes(m.fileSha256);
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                w.uint32(50).bytes(m.fileEncSha256);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ExternalBlobReference.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ExternalBlobReference();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.mediaKey = r.bytes();
                        m._mediaKey = "mediaKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.directPath = r.stringVerify();
                        m._directPath = "directPath";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.handle = r.stringVerify();
                        m._handle = "handle";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.fileSizeBytes = r.uint64();
                        m._fileSizeBytes = "fileSizeBytes";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.fileSha256 = r.bytes();
                        m._fileSha256 = "fileSha256";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.fileEncSha256 = r.bytes();
                        m._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ExternalBlobReference.fromObject = function (d, q) {
            if (d instanceof $root.proto.ExternalBlobReference)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ExternalBlobReference: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ExternalBlobReference();
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.directPath != null) {
                m.directPath = $String(d.directPath);
            }
            if (d.handle != null) {
                m.handle = $String(d.handle);
            }
            if (d.fileSizeBytes != null) {
                if ($util.Long)
                    m.fileSizeBytes = $util.Long.fromValue(d.fileSizeBytes, true);
                else if (typeof d.fileSizeBytes === "string")
                    m.fileSizeBytes = $parseInt(d.fileSizeBytes, 10);
                else if (typeof d.fileSizeBytes === "number")
                    m.fileSizeBytes = d.fileSizeBytes;
                else if (typeof d.fileSizeBytes === "object")
                    m.fileSizeBytes = new $util.LongBits(d.fileSizeBytes.low >>> 0, d.fileSizeBytes.high >>> 0).toNumber(true);
            }
            if (d.fileSha256 != null) {
                if (typeof d.fileSha256 === "string")
                    $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                else if (d.fileSha256.length >= 0)
                    m.fileSha256 = d.fileSha256;
            }
            if (d.fileEncSha256 != null) {
                if (typeof d.fileEncSha256 === "string")
                    $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                else if (d.fileEncSha256.length >= 0)
                    m.fileEncSha256 = d.fileEncSha256;
            }
            return m;
        };

        ExternalBlobReference.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
            }
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                d.directPath = m.directPath;
            }
            if (m.handle != null && $Object.hasOwnProperty.call(m, "handle")) {
                d.handle = m.handle;
            }
            if (m.fileSizeBytes != null && $Object.hasOwnProperty.call(m, "fileSizeBytes")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.fileSizeBytes = typeof m.fileSizeBytes === "number" ? $BigInt(m.fileSizeBytes) : $util.Long.fromBits(m.fileSizeBytes.low >>> 0, m.fileSizeBytes.high >>> 0, true).toBigInt();
                else if (typeof m.fileSizeBytes === "number")
                    d.fileSizeBytes = o.longs === $String ? $String(m.fileSizeBytes) : m.fileSizeBytes;
                else
                    d.fileSizeBytes = o.longs === $String ? $util.Long.prototype.toString.call(m.fileSizeBytes) : o.longs === $Number ? new $util.LongBits(m.fileSizeBytes.low >>> 0, m.fileSizeBytes.high >>> 0).toNumber(true) : m.fileSizeBytes;
            }
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
            }
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
            }
            return d;
        };

        ExternalBlobReference.prototype.toJSON = function() {
            return ExternalBlobReference.toObject(this, $protobuf.util.toJSONOptions);
        };

        ExternalBlobReference.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ExternalBlobReference";
        };

        return ExternalBlobReference;
    })();

    proto.FUTURE_PROOF_BEHAVIOR = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "PLACEHOLDER"] = 0;
        values[valuesById[1] = "NO_PLACEHOLDER"] = 1;
        values[valuesById[2] = "IGNORE"] = 2;
        return values;
    })();

    proto.Field = (function() {

        const Field = function (p) {
            this.subfield = {};
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Field.prototype.minVersion = null;
        Field.prototype.maxVersion = null;
        Field.prototype.notReportableMinVersion = null;
        Field.prototype.isMessage = null;
        Field.prototype.subfield = $util.emptyObject;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Field.prototype, "_minVersion", {
            get: $util.oneOfGetter($oneOfFields = ["minVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Field.prototype, "_maxVersion", {
            get: $util.oneOfGetter($oneOfFields = ["maxVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Field.prototype, "_notReportableMinVersion", {
            get: $util.oneOfGetter($oneOfFields = ["notReportableMinVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Field.prototype, "_isMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Field.create = function(properties) {
            return new Field(properties);
        };

        Field.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.minVersion != null && $Object.hasOwnProperty.call(m, "minVersion"))
                w.uint32(8).uint32(m.minVersion);
            if (m.maxVersion != null && $Object.hasOwnProperty.call(m, "maxVersion"))
                w.uint32(16).uint32(m.maxVersion);
            if (m.notReportableMinVersion != null && $Object.hasOwnProperty.call(m, "notReportableMinVersion"))
                w.uint32(24).uint32(m.notReportableMinVersion);
            if (m.isMessage != null && $Object.hasOwnProperty.call(m, "isMessage"))
                w.uint32(32).bool(m.isMessage);
            if (m.subfield != null && $Object.hasOwnProperty.call(m, "subfield")) {
                for (var ks = $Object.keys(m.subfield), i = 0; i < ks.length; ++i) {
                    w.uint32(42).fork().uint32(8).uint32(ks[i]);
                    $root.proto.Field.encode(m.subfield[ks[i]], w.uint32(18).fork(), q + 1).ldelim().ldelim();
                }
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Field.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Field(), k, v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.minVersion = r.uint32();
                        m._minVersion = "minVersion";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.maxVersion = r.uint32();
                        m._maxVersion = "maxVersion";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.notReportableMinVersion = r.uint32();
                        m._notReportableMinVersion = "notReportableMinVersion";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.isMessage = r.bool();
                        m._isMessage = "isMessage";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (m.subfield === $util.emptyObject)
                            m.subfield = {};
                        var c2 = r.uint32() + r.pos;
                        k = 0;
                        v = null;
                        while (r.pos < c2) {
                            var t2 = r.tag();
                            u = t2 & 7;
                            switch (t2 >>>= 3) {
                            case 1:
                                if (u !== 0)
                                    break;
                                k = r.uint32();
                                continue;
                            case 2:
                                if (u !== 2)
                                    break;
                                v = $root.proto.Field.decode(r, r.uint32(), $undefined, q + 1, v);
                                continue;
                            }
                            r.skipType(u, q, t2);
                        }
                        m.subfield[k] = v || new $root.proto.Field();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Field.fromObject = function (d, q) {
            if (d instanceof $root.proto.Field)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Field: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Field();
            if (d.minVersion != null) {
                m.minVersion = d.minVersion >>> 0;
            }
            if (d.maxVersion != null) {
                m.maxVersion = d.maxVersion >>> 0;
            }
            if (d.notReportableMinVersion != null) {
                m.notReportableMinVersion = d.notReportableMinVersion >>> 0;
            }
            if (d.isMessage != null) {
                m.isMessage = $Boolean(d.isMessage);
            }
            if (d.subfield) {
                if (!$util.isObject(d.subfield))
                    throw $TypeError(".proto.Field.subfield: object expected");
                m.subfield = {};
                for (var ks = $Object.keys(d.subfield), i = 0; i < ks.length; ++i) {
                    if (ks[i] === "__proto__")
                        $util.makeProp(m.subfield, ks[i]);
                    if (!$util.isObject(d.subfield[ks[i]]))
                        throw $TypeError(".proto.Field.subfield: object expected");
                    m.subfield[ks[i]] = $root.proto.Field.fromObject(d.subfield[ks[i]], q + 1);
                }
            }
            return m;
        };

        Field.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.objects || o.defaults) {
                d.subfield = {};
            }
            if (m.minVersion != null && $Object.hasOwnProperty.call(m, "minVersion")) {
                d.minVersion = m.minVersion;
            }
            if (m.maxVersion != null && $Object.hasOwnProperty.call(m, "maxVersion")) {
                d.maxVersion = m.maxVersion;
            }
            if (m.notReportableMinVersion != null && $Object.hasOwnProperty.call(m, "notReportableMinVersion")) {
                d.notReportableMinVersion = m.notReportableMinVersion;
            }
            if (m.isMessage != null && $Object.hasOwnProperty.call(m, "isMessage")) {
                d.isMessage = m.isMessage;
            }
            var ks2;
            if (m.subfield && (ks2 = $Object.keys(m.subfield)).length) {
                d.subfield = {};
                for (var j = 0; j < ks2.length; ++j) {
                    if (ks2[j] === "__proto__")
                        $util.makeProp(d.subfield, ks2[j]);
                    d.subfield[ks2[j]] = $root.proto.Field.toObject(m.subfield[ks2[j]], o, q + 1);
                }
            }
            return d;
        };

        Field.prototype.toJSON = function() {
            return Field.toObject(this, $protobuf.util.toJSONOptions);
        };

        Field.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Field";
        };

        return Field;
    })();

    proto.FingerprintData = (function() {

        const FingerprintData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        FingerprintData.prototype.publicKey = null;
        FingerprintData.prototype.pnIdentifier = null;
        FingerprintData.prototype.lidIdentifier = null;
        FingerprintData.prototype.usernameIdentifier = null;
        FingerprintData.prototype.hostedState = null;
        FingerprintData.prototype.hashedPublicKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(FingerprintData.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(FingerprintData.prototype, "_pnIdentifier", {
            get: $util.oneOfGetter($oneOfFields = ["pnIdentifier"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(FingerprintData.prototype, "_lidIdentifier", {
            get: $util.oneOfGetter($oneOfFields = ["lidIdentifier"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(FingerprintData.prototype, "_usernameIdentifier", {
            get: $util.oneOfGetter($oneOfFields = ["usernameIdentifier"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(FingerprintData.prototype, "_hostedState", {
            get: $util.oneOfGetter($oneOfFields = ["hostedState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(FingerprintData.prototype, "_hashedPublicKey", {
            get: $util.oneOfGetter($oneOfFields = ["hashedPublicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        FingerprintData.create = function(properties) {
            return new FingerprintData(properties);
        };

        FingerprintData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(10).bytes(m.publicKey);
            if (m.pnIdentifier != null && $Object.hasOwnProperty.call(m, "pnIdentifier"))
                w.uint32(18).bytes(m.pnIdentifier);
            if (m.lidIdentifier != null && $Object.hasOwnProperty.call(m, "lidIdentifier"))
                w.uint32(26).bytes(m.lidIdentifier);
            if (m.usernameIdentifier != null && $Object.hasOwnProperty.call(m, "usernameIdentifier"))
                w.uint32(34).bytes(m.usernameIdentifier);
            if (m.hostedState != null && $Object.hasOwnProperty.call(m, "hostedState"))
                w.uint32(40).int32(m.hostedState);
            if (m.hashedPublicKey != null && $Object.hasOwnProperty.call(m, "hashedPublicKey"))
                w.uint32(50).bytes(m.hashedPublicKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        FingerprintData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.FingerprintData(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        m._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.pnIdentifier = r.bytes();
                        m._pnIdentifier = "pnIdentifier";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.lidIdentifier = r.bytes();
                        m._lidIdentifier = "lidIdentifier";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.usernameIdentifier = r.bytes();
                        m._usernameIdentifier = "usernameIdentifier";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.hostedState = r.int32();
                        m._hostedState = "hostedState";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.hashedPublicKey = r.bytes();
                        m._hashedPublicKey = "hashedPublicKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        FingerprintData.fromObject = function (d, q) {
            if (d instanceof $root.proto.FingerprintData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.FingerprintData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.FingerprintData();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.pnIdentifier != null) {
                if (typeof d.pnIdentifier === "string")
                    $util.base64.decode(d.pnIdentifier, m.pnIdentifier = $util.newBuffer($util.base64.length(d.pnIdentifier)), 0);
                else if (d.pnIdentifier.length >= 0)
                    m.pnIdentifier = d.pnIdentifier;
            }
            if (d.lidIdentifier != null) {
                if (typeof d.lidIdentifier === "string")
                    $util.base64.decode(d.lidIdentifier, m.lidIdentifier = $util.newBuffer($util.base64.length(d.lidIdentifier)), 0);
                else if (d.lidIdentifier.length >= 0)
                    m.lidIdentifier = d.lidIdentifier;
            }
            if (d.usernameIdentifier != null) {
                if (typeof d.usernameIdentifier === "string")
                    $util.base64.decode(d.usernameIdentifier, m.usernameIdentifier = $util.newBuffer($util.base64.length(d.usernameIdentifier)), 0);
                else if (d.usernameIdentifier.length >= 0)
                    m.usernameIdentifier = d.usernameIdentifier;
            }
            switch (d.hostedState) {
            case "E2EE":
            case 0:
                m.hostedState = 0;
                break;
            case "HOSTED":
            case 1:
                m.hostedState = 1;
                break;
            default:
                if (typeof d.hostedState === "number" && (d.hostedState | 0) === d.hostedState)
                    m.hostedState = d.hostedState;
            }
            if (d.hashedPublicKey != null) {
                if (typeof d.hashedPublicKey === "string")
                    $util.base64.decode(d.hashedPublicKey, m.hashedPublicKey = $util.newBuffer($util.base64.length(d.hashedPublicKey)), 0);
                else if (d.hashedPublicKey.length >= 0)
                    m.hashedPublicKey = d.hashedPublicKey;
            }
            return m;
        };

        FingerprintData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.pnIdentifier != null && $Object.hasOwnProperty.call(m, "pnIdentifier")) {
                d.pnIdentifier = o.bytes === $String ? $util.base64.encode(m.pnIdentifier, 0, m.pnIdentifier.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.pnIdentifier) : m.pnIdentifier;
            }
            if (m.lidIdentifier != null && $Object.hasOwnProperty.call(m, "lidIdentifier")) {
                d.lidIdentifier = o.bytes === $String ? $util.base64.encode(m.lidIdentifier, 0, m.lidIdentifier.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.lidIdentifier) : m.lidIdentifier;
            }
            if (m.usernameIdentifier != null && $Object.hasOwnProperty.call(m, "usernameIdentifier")) {
                d.usernameIdentifier = o.bytes === $String ? $util.base64.encode(m.usernameIdentifier, 0, m.usernameIdentifier.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.usernameIdentifier) : m.usernameIdentifier;
            }
            if (m.hostedState != null && $Object.hasOwnProperty.call(m, "hostedState")) {
                d.hostedState = o.enums === $String ? $root.proto.HostedState[m.hostedState] === $undefined ? m.hostedState : $root.proto.HostedState[m.hostedState] : m.hostedState;
            }
            if (m.hashedPublicKey != null && $Object.hasOwnProperty.call(m, "hashedPublicKey")) {
                d.hashedPublicKey = o.bytes === $String ? $util.base64.encode(m.hashedPublicKey, 0, m.hashedPublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.hashedPublicKey) : m.hashedPublicKey;
            }
            return d;
        };

        FingerprintData.prototype.toJSON = function() {
            return FingerprintData.toObject(this, $protobuf.util.toJSONOptions);
        };

        FingerprintData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.FingerprintData";
        };

        return FingerprintData;
    })();

    proto.ForwardedAIBotMessageInfo = (function() {

        const ForwardedAIBotMessageInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ForwardedAIBotMessageInfo.prototype.botName = null;
        ForwardedAIBotMessageInfo.prototype.botJid = null;
        ForwardedAIBotMessageInfo.prototype.creatorName = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_botName", {
            get: $util.oneOfGetter($oneOfFields = ["botName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_botJid", {
            get: $util.oneOfGetter($oneOfFields = ["botJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ForwardedAIBotMessageInfo.prototype, "_creatorName", {
            get: $util.oneOfGetter($oneOfFields = ["creatorName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ForwardedAIBotMessageInfo.create = function(properties) {
            return new ForwardedAIBotMessageInfo(properties);
        };

        ForwardedAIBotMessageInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.botName != null && $Object.hasOwnProperty.call(m, "botName"))
                w.uint32(10).string(m.botName);
            if (m.botJid != null && $Object.hasOwnProperty.call(m, "botJid"))
                w.uint32(18).string(m.botJid);
            if (m.creatorName != null && $Object.hasOwnProperty.call(m, "creatorName"))
                w.uint32(26).string(m.creatorName);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ForwardedAIBotMessageInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ForwardedAIBotMessageInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.botName = r.stringVerify();
                        m._botName = "botName";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.botJid = r.stringVerify();
                        m._botJid = "botJid";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.creatorName = r.stringVerify();
                        m._creatorName = "creatorName";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ForwardedAIBotMessageInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.ForwardedAIBotMessageInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ForwardedAIBotMessageInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ForwardedAIBotMessageInfo();
            if (d.botName != null) {
                m.botName = $String(d.botName);
            }
            if (d.botJid != null) {
                m.botJid = $String(d.botJid);
            }
            if (d.creatorName != null) {
                m.creatorName = $String(d.creatorName);
            }
            return m;
        };

        ForwardedAIBotMessageInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.botName != null && $Object.hasOwnProperty.call(m, "botName")) {
                d.botName = m.botName;
            }
            if (m.botJid != null && $Object.hasOwnProperty.call(m, "botJid")) {
                d.botJid = m.botJid;
            }
            if (m.creatorName != null && $Object.hasOwnProperty.call(m, "creatorName")) {
                d.creatorName = m.creatorName;
            }
            return d;
        };

        ForwardedAIBotMessageInfo.prototype.toJSON = function() {
            return ForwardedAIBotMessageInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        ForwardedAIBotMessageInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ForwardedAIBotMessageInfo";
        };

        return ForwardedAIBotMessageInfo;
    })();

    proto.GlobalSettings = (function() {

        const GlobalSettings = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GlobalSettings.prototype.lightThemeWallpaper = null;
        GlobalSettings.prototype.mediaVisibility = null;
        GlobalSettings.prototype.darkThemeWallpaper = null;
        GlobalSettings.prototype.autoDownloadWiFi = null;
        GlobalSettings.prototype.autoDownloadCellular = null;
        GlobalSettings.prototype.autoDownloadRoaming = null;
        GlobalSettings.prototype.showIndividualNotificationsPreview = null;
        GlobalSettings.prototype.showGroupNotificationsPreview = null;
        GlobalSettings.prototype.disappearingModeDuration = null;
        GlobalSettings.prototype.disappearingModeTimestamp = null;
        GlobalSettings.prototype.avatarUserSettings = null;
        GlobalSettings.prototype.fontSize = null;
        GlobalSettings.prototype.securityNotifications = null;
        GlobalSettings.prototype.autoUnarchiveChats = null;
        GlobalSettings.prototype.videoQualityMode = null;
        GlobalSettings.prototype.photoQualityMode = null;
        GlobalSettings.prototype.individualNotificationSettings = null;
        GlobalSettings.prototype.groupNotificationSettings = null;
        GlobalSettings.prototype.chatLockSettings = null;
        GlobalSettings.prototype.chatDbLidMigrationTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_lightThemeWallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["lightThemeWallpaper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_mediaVisibility", {
            get: $util.oneOfGetter($oneOfFields = ["mediaVisibility"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_darkThemeWallpaper", {
            get: $util.oneOfGetter($oneOfFields = ["darkThemeWallpaper"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_autoDownloadWiFi", {
            get: $util.oneOfGetter($oneOfFields = ["autoDownloadWiFi"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_autoDownloadCellular", {
            get: $util.oneOfGetter($oneOfFields = ["autoDownloadCellular"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_autoDownloadRoaming", {
            get: $util.oneOfGetter($oneOfFields = ["autoDownloadRoaming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_showIndividualNotificationsPreview", {
            get: $util.oneOfGetter($oneOfFields = ["showIndividualNotificationsPreview"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_showGroupNotificationsPreview", {
            get: $util.oneOfGetter($oneOfFields = ["showGroupNotificationsPreview"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_disappearingModeDuration", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingModeDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_disappearingModeTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingModeTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_avatarUserSettings", {
            get: $util.oneOfGetter($oneOfFields = ["avatarUserSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_fontSize", {
            get: $util.oneOfGetter($oneOfFields = ["fontSize"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_securityNotifications", {
            get: $util.oneOfGetter($oneOfFields = ["securityNotifications"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_autoUnarchiveChats", {
            get: $util.oneOfGetter($oneOfFields = ["autoUnarchiveChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_videoQualityMode", {
            get: $util.oneOfGetter($oneOfFields = ["videoQualityMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_photoQualityMode", {
            get: $util.oneOfGetter($oneOfFields = ["photoQualityMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_individualNotificationSettings", {
            get: $util.oneOfGetter($oneOfFields = ["individualNotificationSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_groupNotificationSettings", {
            get: $util.oneOfGetter($oneOfFields = ["groupNotificationSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_chatLockSettings", {
            get: $util.oneOfGetter($oneOfFields = ["chatLockSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GlobalSettings.prototype, "_chatDbLidMigrationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["chatDbLidMigrationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GlobalSettings.create = function(properties) {
            return new GlobalSettings(properties);
        };

        GlobalSettings.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.lightThemeWallpaper != null && $Object.hasOwnProperty.call(m, "lightThemeWallpaper"))
                $root.proto.WallpaperSettings.encode(m.lightThemeWallpaper, w.uint32(10).fork(), q + 1).ldelim();
            if (m.mediaVisibility != null && $Object.hasOwnProperty.call(m, "mediaVisibility"))
                w.uint32(16).int32(m.mediaVisibility);
            if (m.darkThemeWallpaper != null && $Object.hasOwnProperty.call(m, "darkThemeWallpaper"))
                $root.proto.WallpaperSettings.encode(m.darkThemeWallpaper, w.uint32(26).fork(), q + 1).ldelim();
            if (m.autoDownloadWiFi != null && $Object.hasOwnProperty.call(m, "autoDownloadWiFi"))
                $root.proto.AutoDownloadSettings.encode(m.autoDownloadWiFi, w.uint32(34).fork(), q + 1).ldelim();
            if (m.autoDownloadCellular != null && $Object.hasOwnProperty.call(m, "autoDownloadCellular"))
                $root.proto.AutoDownloadSettings.encode(m.autoDownloadCellular, w.uint32(42).fork(), q + 1).ldelim();
            if (m.autoDownloadRoaming != null && $Object.hasOwnProperty.call(m, "autoDownloadRoaming"))
                $root.proto.AutoDownloadSettings.encode(m.autoDownloadRoaming, w.uint32(50).fork(), q + 1).ldelim();
            if (m.showIndividualNotificationsPreview != null && $Object.hasOwnProperty.call(m, "showIndividualNotificationsPreview"))
                w.uint32(56).bool(m.showIndividualNotificationsPreview);
            if (m.showGroupNotificationsPreview != null && $Object.hasOwnProperty.call(m, "showGroupNotificationsPreview"))
                w.uint32(64).bool(m.showGroupNotificationsPreview);
            if (m.disappearingModeDuration != null && $Object.hasOwnProperty.call(m, "disappearingModeDuration"))
                w.uint32(72).int32(m.disappearingModeDuration);
            if (m.disappearingModeTimestamp != null && $Object.hasOwnProperty.call(m, "disappearingModeTimestamp"))
                w.uint32(80).int64(m.disappearingModeTimestamp);
            if (m.avatarUserSettings != null && $Object.hasOwnProperty.call(m, "avatarUserSettings"))
                $root.proto.AvatarUserSettings.encode(m.avatarUserSettings, w.uint32(90).fork(), q + 1).ldelim();
            if (m.fontSize != null && $Object.hasOwnProperty.call(m, "fontSize"))
                w.uint32(96).int32(m.fontSize);
            if (m.securityNotifications != null && $Object.hasOwnProperty.call(m, "securityNotifications"))
                w.uint32(104).bool(m.securityNotifications);
            if (m.autoUnarchiveChats != null && $Object.hasOwnProperty.call(m, "autoUnarchiveChats"))
                w.uint32(112).bool(m.autoUnarchiveChats);
            if (m.videoQualityMode != null && $Object.hasOwnProperty.call(m, "videoQualityMode"))
                w.uint32(120).int32(m.videoQualityMode);
            if (m.photoQualityMode != null && $Object.hasOwnProperty.call(m, "photoQualityMode"))
                w.uint32(128).int32(m.photoQualityMode);
            if (m.individualNotificationSettings != null && $Object.hasOwnProperty.call(m, "individualNotificationSettings"))
                $root.proto.NotificationSettings.encode(m.individualNotificationSettings, w.uint32(138).fork(), q + 1).ldelim();
            if (m.groupNotificationSettings != null && $Object.hasOwnProperty.call(m, "groupNotificationSettings"))
                $root.proto.NotificationSettings.encode(m.groupNotificationSettings, w.uint32(146).fork(), q + 1).ldelim();
            if (m.chatLockSettings != null && $Object.hasOwnProperty.call(m, "chatLockSettings"))
                $root.proto.ChatLockSettings.encode(m.chatLockSettings, w.uint32(154).fork(), q + 1).ldelim();
            if (m.chatDbLidMigrationTimestamp != null && $Object.hasOwnProperty.call(m, "chatDbLidMigrationTimestamp"))
                w.uint32(160).int64(m.chatDbLidMigrationTimestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GlobalSettings.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GlobalSettings(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.lightThemeWallpaper = $root.proto.WallpaperSettings.decode(r, r.uint32(), $undefined, q + 1, m.lightThemeWallpaper);
                        m._lightThemeWallpaper = "lightThemeWallpaper";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.mediaVisibility = r.int32();
                        m._mediaVisibility = "mediaVisibility";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.darkThemeWallpaper = $root.proto.WallpaperSettings.decode(r, r.uint32(), $undefined, q + 1, m.darkThemeWallpaper);
                        m._darkThemeWallpaper = "darkThemeWallpaper";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.autoDownloadWiFi = $root.proto.AutoDownloadSettings.decode(r, r.uint32(), $undefined, q + 1, m.autoDownloadWiFi);
                        m._autoDownloadWiFi = "autoDownloadWiFi";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.autoDownloadCellular = $root.proto.AutoDownloadSettings.decode(r, r.uint32(), $undefined, q + 1, m.autoDownloadCellular);
                        m._autoDownloadCellular = "autoDownloadCellular";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.autoDownloadRoaming = $root.proto.AutoDownloadSettings.decode(r, r.uint32(), $undefined, q + 1, m.autoDownloadRoaming);
                        m._autoDownloadRoaming = "autoDownloadRoaming";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.showIndividualNotificationsPreview = r.bool();
                        m._showIndividualNotificationsPreview = "showIndividualNotificationsPreview";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.showGroupNotificationsPreview = r.bool();
                        m._showGroupNotificationsPreview = "showGroupNotificationsPreview";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.disappearingModeDuration = r.int32();
                        m._disappearingModeDuration = "disappearingModeDuration";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.disappearingModeTimestamp = r.int64();
                        m._disappearingModeTimestamp = "disappearingModeTimestamp";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.avatarUserSettings = $root.proto.AvatarUserSettings.decode(r, r.uint32(), $undefined, q + 1, m.avatarUserSettings);
                        m._avatarUserSettings = "avatarUserSettings";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.fontSize = r.int32();
                        m._fontSize = "fontSize";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        m.securityNotifications = r.bool();
                        m._securityNotifications = "securityNotifications";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        m.autoUnarchiveChats = r.bool();
                        m._autoUnarchiveChats = "autoUnarchiveChats";
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        m.videoQualityMode = r.int32();
                        m._videoQualityMode = "videoQualityMode";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        m.photoQualityMode = r.int32();
                        m._photoQualityMode = "photoQualityMode";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.individualNotificationSettings = $root.proto.NotificationSettings.decode(r, r.uint32(), $undefined, q + 1, m.individualNotificationSettings);
                        m._individualNotificationSettings = "individualNotificationSettings";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        m.groupNotificationSettings = $root.proto.NotificationSettings.decode(r, r.uint32(), $undefined, q + 1, m.groupNotificationSettings);
                        m._groupNotificationSettings = "groupNotificationSettings";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.chatLockSettings = $root.proto.ChatLockSettings.decode(r, r.uint32(), $undefined, q + 1, m.chatLockSettings);
                        m._chatLockSettings = "chatLockSettings";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        m.chatDbLidMigrationTimestamp = r.int64();
                        m._chatDbLidMigrationTimestamp = "chatDbLidMigrationTimestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GlobalSettings.fromObject = function (d, q) {
            if (d instanceof $root.proto.GlobalSettings)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GlobalSettings: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GlobalSettings();
            if (d.lightThemeWallpaper != null) {
                if (!$util.isObject(d.lightThemeWallpaper))
                    throw $TypeError(".proto.GlobalSettings.lightThemeWallpaper: object expected");
                m.lightThemeWallpaper = $root.proto.WallpaperSettings.fromObject(d.lightThemeWallpaper, q + 1);
            }
            switch (d.mediaVisibility) {
            case "DEFAULT":
            case 0:
                m.mediaVisibility = 0;
                break;
            case "OFF":
            case 1:
                m.mediaVisibility = 1;
                break;
            case "ON":
            case 2:
                m.mediaVisibility = 2;
                break;
            default:
                if (typeof d.mediaVisibility === "number" && (d.mediaVisibility | 0) === d.mediaVisibility)
                    m.mediaVisibility = d.mediaVisibility;
            }
            if (d.darkThemeWallpaper != null) {
                if (!$util.isObject(d.darkThemeWallpaper))
                    throw $TypeError(".proto.GlobalSettings.darkThemeWallpaper: object expected");
                m.darkThemeWallpaper = $root.proto.WallpaperSettings.fromObject(d.darkThemeWallpaper, q + 1);
            }
            if (d.autoDownloadWiFi != null) {
                if (!$util.isObject(d.autoDownloadWiFi))
                    throw $TypeError(".proto.GlobalSettings.autoDownloadWiFi: object expected");
                m.autoDownloadWiFi = $root.proto.AutoDownloadSettings.fromObject(d.autoDownloadWiFi, q + 1);
            }
            if (d.autoDownloadCellular != null) {
                if (!$util.isObject(d.autoDownloadCellular))
                    throw $TypeError(".proto.GlobalSettings.autoDownloadCellular: object expected");
                m.autoDownloadCellular = $root.proto.AutoDownloadSettings.fromObject(d.autoDownloadCellular, q + 1);
            }
            if (d.autoDownloadRoaming != null) {
                if (!$util.isObject(d.autoDownloadRoaming))
                    throw $TypeError(".proto.GlobalSettings.autoDownloadRoaming: object expected");
                m.autoDownloadRoaming = $root.proto.AutoDownloadSettings.fromObject(d.autoDownloadRoaming, q + 1);
            }
            if (d.showIndividualNotificationsPreview != null) {
                m.showIndividualNotificationsPreview = $Boolean(d.showIndividualNotificationsPreview);
            }
            if (d.showGroupNotificationsPreview != null) {
                m.showGroupNotificationsPreview = $Boolean(d.showGroupNotificationsPreview);
            }
            if (d.disappearingModeDuration != null) {
                m.disappearingModeDuration = d.disappearingModeDuration | 0;
            }
            if (d.disappearingModeTimestamp != null) {
                if ($util.Long)
                    m.disappearingModeTimestamp = $util.Long.fromValue(d.disappearingModeTimestamp, false);
                else if (typeof d.disappearingModeTimestamp === "string")
                    m.disappearingModeTimestamp = $parseInt(d.disappearingModeTimestamp, 10);
                else if (typeof d.disappearingModeTimestamp === "number")
                    m.disappearingModeTimestamp = d.disappearingModeTimestamp;
                else if (typeof d.disappearingModeTimestamp === "object")
                    m.disappearingModeTimestamp = new $util.LongBits(d.disappearingModeTimestamp.low >>> 0, d.disappearingModeTimestamp.high >>> 0).toNumber();
            }
            if (d.avatarUserSettings != null) {
                if (!$util.isObject(d.avatarUserSettings))
                    throw $TypeError(".proto.GlobalSettings.avatarUserSettings: object expected");
                m.avatarUserSettings = $root.proto.AvatarUserSettings.fromObject(d.avatarUserSettings, q + 1);
            }
            if (d.fontSize != null) {
                m.fontSize = d.fontSize | 0;
            }
            if (d.securityNotifications != null) {
                m.securityNotifications = $Boolean(d.securityNotifications);
            }
            if (d.autoUnarchiveChats != null) {
                m.autoUnarchiveChats = $Boolean(d.autoUnarchiveChats);
            }
            if (d.videoQualityMode != null) {
                m.videoQualityMode = d.videoQualityMode | 0;
            }
            if (d.photoQualityMode != null) {
                m.photoQualityMode = d.photoQualityMode | 0;
            }
            if (d.individualNotificationSettings != null) {
                if (!$util.isObject(d.individualNotificationSettings))
                    throw $TypeError(".proto.GlobalSettings.individualNotificationSettings: object expected");
                m.individualNotificationSettings = $root.proto.NotificationSettings.fromObject(d.individualNotificationSettings, q + 1);
            }
            if (d.groupNotificationSettings != null) {
                if (!$util.isObject(d.groupNotificationSettings))
                    throw $TypeError(".proto.GlobalSettings.groupNotificationSettings: object expected");
                m.groupNotificationSettings = $root.proto.NotificationSettings.fromObject(d.groupNotificationSettings, q + 1);
            }
            if (d.chatLockSettings != null) {
                if (!$util.isObject(d.chatLockSettings))
                    throw $TypeError(".proto.GlobalSettings.chatLockSettings: object expected");
                m.chatLockSettings = $root.proto.ChatLockSettings.fromObject(d.chatLockSettings, q + 1);
            }
            if (d.chatDbLidMigrationTimestamp != null) {
                if ($util.Long)
                    m.chatDbLidMigrationTimestamp = $util.Long.fromValue(d.chatDbLidMigrationTimestamp, false);
                else if (typeof d.chatDbLidMigrationTimestamp === "string")
                    m.chatDbLidMigrationTimestamp = $parseInt(d.chatDbLidMigrationTimestamp, 10);
                else if (typeof d.chatDbLidMigrationTimestamp === "number")
                    m.chatDbLidMigrationTimestamp = d.chatDbLidMigrationTimestamp;
                else if (typeof d.chatDbLidMigrationTimestamp === "object")
                    m.chatDbLidMigrationTimestamp = new $util.LongBits(d.chatDbLidMigrationTimestamp.low >>> 0, d.chatDbLidMigrationTimestamp.high >>> 0).toNumber();
            }
            return m;
        };

        GlobalSettings.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.lightThemeWallpaper != null && $Object.hasOwnProperty.call(m, "lightThemeWallpaper")) {
                d.lightThemeWallpaper = $root.proto.WallpaperSettings.toObject(m.lightThemeWallpaper, o, q + 1);
            }
            if (m.mediaVisibility != null && $Object.hasOwnProperty.call(m, "mediaVisibility")) {
                d.mediaVisibility = o.enums === $String ? $root.proto.MediaVisibility[m.mediaVisibility] === $undefined ? m.mediaVisibility : $root.proto.MediaVisibility[m.mediaVisibility] : m.mediaVisibility;
            }
            if (m.darkThemeWallpaper != null && $Object.hasOwnProperty.call(m, "darkThemeWallpaper")) {
                d.darkThemeWallpaper = $root.proto.WallpaperSettings.toObject(m.darkThemeWallpaper, o, q + 1);
            }
            if (m.autoDownloadWiFi != null && $Object.hasOwnProperty.call(m, "autoDownloadWiFi")) {
                d.autoDownloadWiFi = $root.proto.AutoDownloadSettings.toObject(m.autoDownloadWiFi, o, q + 1);
            }
            if (m.autoDownloadCellular != null && $Object.hasOwnProperty.call(m, "autoDownloadCellular")) {
                d.autoDownloadCellular = $root.proto.AutoDownloadSettings.toObject(m.autoDownloadCellular, o, q + 1);
            }
            if (m.autoDownloadRoaming != null && $Object.hasOwnProperty.call(m, "autoDownloadRoaming")) {
                d.autoDownloadRoaming = $root.proto.AutoDownloadSettings.toObject(m.autoDownloadRoaming, o, q + 1);
            }
            if (m.showIndividualNotificationsPreview != null && $Object.hasOwnProperty.call(m, "showIndividualNotificationsPreview")) {
                d.showIndividualNotificationsPreview = m.showIndividualNotificationsPreview;
            }
            if (m.showGroupNotificationsPreview != null && $Object.hasOwnProperty.call(m, "showGroupNotificationsPreview")) {
                d.showGroupNotificationsPreview = m.showGroupNotificationsPreview;
            }
            if (m.disappearingModeDuration != null && $Object.hasOwnProperty.call(m, "disappearingModeDuration")) {
                d.disappearingModeDuration = m.disappearingModeDuration;
            }
            if (m.disappearingModeTimestamp != null && $Object.hasOwnProperty.call(m, "disappearingModeTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.disappearingModeTimestamp = typeof m.disappearingModeTimestamp === "number" ? $BigInt(m.disappearingModeTimestamp) : $util.Long.fromBits(m.disappearingModeTimestamp.low >>> 0, m.disappearingModeTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.disappearingModeTimestamp === "number")
                    d.disappearingModeTimestamp = o.longs === $String ? $String(m.disappearingModeTimestamp) : m.disappearingModeTimestamp;
                else
                    d.disappearingModeTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.disappearingModeTimestamp) : o.longs === $Number ? new $util.LongBits(m.disappearingModeTimestamp.low >>> 0, m.disappearingModeTimestamp.high >>> 0).toNumber() : m.disappearingModeTimestamp;
            }
            if (m.avatarUserSettings != null && $Object.hasOwnProperty.call(m, "avatarUserSettings")) {
                d.avatarUserSettings = $root.proto.AvatarUserSettings.toObject(m.avatarUserSettings, o, q + 1);
            }
            if (m.fontSize != null && $Object.hasOwnProperty.call(m, "fontSize")) {
                d.fontSize = m.fontSize;
            }
            if (m.securityNotifications != null && $Object.hasOwnProperty.call(m, "securityNotifications")) {
                d.securityNotifications = m.securityNotifications;
            }
            if (m.autoUnarchiveChats != null && $Object.hasOwnProperty.call(m, "autoUnarchiveChats")) {
                d.autoUnarchiveChats = m.autoUnarchiveChats;
            }
            if (m.videoQualityMode != null && $Object.hasOwnProperty.call(m, "videoQualityMode")) {
                d.videoQualityMode = m.videoQualityMode;
            }
            if (m.photoQualityMode != null && $Object.hasOwnProperty.call(m, "photoQualityMode")) {
                d.photoQualityMode = m.photoQualityMode;
            }
            if (m.individualNotificationSettings != null && $Object.hasOwnProperty.call(m, "individualNotificationSettings")) {
                d.individualNotificationSettings = $root.proto.NotificationSettings.toObject(m.individualNotificationSettings, o, q + 1);
            }
            if (m.groupNotificationSettings != null && $Object.hasOwnProperty.call(m, "groupNotificationSettings")) {
                d.groupNotificationSettings = $root.proto.NotificationSettings.toObject(m.groupNotificationSettings, o, q + 1);
            }
            if (m.chatLockSettings != null && $Object.hasOwnProperty.call(m, "chatLockSettings")) {
                d.chatLockSettings = $root.proto.ChatLockSettings.toObject(m.chatLockSettings, o, q + 1);
            }
            if (m.chatDbLidMigrationTimestamp != null && $Object.hasOwnProperty.call(m, "chatDbLidMigrationTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.chatDbLidMigrationTimestamp = typeof m.chatDbLidMigrationTimestamp === "number" ? $BigInt(m.chatDbLidMigrationTimestamp) : $util.Long.fromBits(m.chatDbLidMigrationTimestamp.low >>> 0, m.chatDbLidMigrationTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.chatDbLidMigrationTimestamp === "number")
                    d.chatDbLidMigrationTimestamp = o.longs === $String ? $String(m.chatDbLidMigrationTimestamp) : m.chatDbLidMigrationTimestamp;
                else
                    d.chatDbLidMigrationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.chatDbLidMigrationTimestamp) : o.longs === $Number ? new $util.LongBits(m.chatDbLidMigrationTimestamp.low >>> 0, m.chatDbLidMigrationTimestamp.high >>> 0).toNumber() : m.chatDbLidMigrationTimestamp;
            }
            return d;
        };

        GlobalSettings.prototype.toJSON = function() {
            return GlobalSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        GlobalSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GlobalSettings";
        };

        return GlobalSettings;
    })();

    proto.GroupHistory = (function() {

        const GroupHistory = function (p) {
            this.messages = [];
            this.uncountedAssociatedMessageLists = [];
            this.commentMessages = [];
            this.outOfWindowPinnedMessages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupHistory.prototype.messages = $util.emptyArray;
        GroupHistory.prototype.uncountedAssociatedMessageLists = $util.emptyArray;
        GroupHistory.prototype.commentMessages = $util.emptyArray;
        GroupHistory.prototype.outOfWindowPinnedMessages = $util.emptyArray;

        GroupHistory.create = function(properties) {
            return new GroupHistory(properties);
        };

        GroupHistory.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messages != null && m.messages.length) {
                for (var i = 0; i < m.messages.length; ++i)
                    $root.proto.WebMessageInfo.encode(m.messages[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.uncountedAssociatedMessageLists != null && m.uncountedAssociatedMessageLists.length) {
                for (var i = 0; i < m.uncountedAssociatedMessageLists.length; ++i)
                    $root.proto.UnCountedAssociatedMessageList.encode(m.uncountedAssociatedMessageLists[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.commentMessages != null && m.commentMessages.length) {
                for (var i = 0; i < m.commentMessages.length; ++i)
                    $root.proto.WebMessageInfo.encode(m.commentMessages[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.outOfWindowPinnedMessages != null && m.outOfWindowPinnedMessages.length) {
                for (var i = 0; i < m.outOfWindowPinnedMessages.length; ++i)
                    $root.proto.WebMessageInfo.encode(m.outOfWindowPinnedMessages[i], w.uint32(34).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupHistory.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupHistory();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.messages && m.messages.length))
                            m.messages = [];
                        m.messages.push($root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.uncountedAssociatedMessageLists && m.uncountedAssociatedMessageLists.length))
                            m.uncountedAssociatedMessageLists = [];
                        m.uncountedAssociatedMessageLists.push($root.proto.UnCountedAssociatedMessageList.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.commentMessages && m.commentMessages.length))
                            m.commentMessages = [];
                        m.commentMessages.push($root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(m.outOfWindowPinnedMessages && m.outOfWindowPinnedMessages.length))
                            m.outOfWindowPinnedMessages = [];
                        m.outOfWindowPinnedMessages.push($root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupHistory.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupHistory)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupHistory: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupHistory();
            if (d.messages) {
                if (!$Array.isArray(d.messages))
                    throw $TypeError(".proto.GroupHistory.messages: array expected");
                m.messages = $Array(d.messages.length);
                for (var i = 0; i < d.messages.length; ++i) {
                    if (!$util.isObject(d.messages[i]))
                        throw $TypeError(".proto.GroupHistory.messages: object expected");
                    m.messages[i] = $root.proto.WebMessageInfo.fromObject(d.messages[i], q + 1);
                }
            }
            if (d.uncountedAssociatedMessageLists) {
                if (!$Array.isArray(d.uncountedAssociatedMessageLists))
                    throw $TypeError(".proto.GroupHistory.uncountedAssociatedMessageLists: array expected");
                m.uncountedAssociatedMessageLists = $Array(d.uncountedAssociatedMessageLists.length);
                for (var i = 0; i < d.uncountedAssociatedMessageLists.length; ++i) {
                    if (!$util.isObject(d.uncountedAssociatedMessageLists[i]))
                        throw $TypeError(".proto.GroupHistory.uncountedAssociatedMessageLists: object expected");
                    m.uncountedAssociatedMessageLists[i] = $root.proto.UnCountedAssociatedMessageList.fromObject(d.uncountedAssociatedMessageLists[i], q + 1);
                }
            }
            if (d.commentMessages) {
                if (!$Array.isArray(d.commentMessages))
                    throw $TypeError(".proto.GroupHistory.commentMessages: array expected");
                m.commentMessages = $Array(d.commentMessages.length);
                for (var i = 0; i < d.commentMessages.length; ++i) {
                    if (!$util.isObject(d.commentMessages[i]))
                        throw $TypeError(".proto.GroupHistory.commentMessages: object expected");
                    m.commentMessages[i] = $root.proto.WebMessageInfo.fromObject(d.commentMessages[i], q + 1);
                }
            }
            if (d.outOfWindowPinnedMessages) {
                if (!$Array.isArray(d.outOfWindowPinnedMessages))
                    throw $TypeError(".proto.GroupHistory.outOfWindowPinnedMessages: array expected");
                m.outOfWindowPinnedMessages = $Array(d.outOfWindowPinnedMessages.length);
                for (var i = 0; i < d.outOfWindowPinnedMessages.length; ++i) {
                    if (!$util.isObject(d.outOfWindowPinnedMessages[i]))
                        throw $TypeError(".proto.GroupHistory.outOfWindowPinnedMessages: object expected");
                    m.outOfWindowPinnedMessages[i] = $root.proto.WebMessageInfo.fromObject(d.outOfWindowPinnedMessages[i], q + 1);
                }
            }
            return m;
        };

        GroupHistory.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.messages = [];
                d.uncountedAssociatedMessageLists = [];
                d.commentMessages = [];
                d.outOfWindowPinnedMessages = [];
            }
            if (m.messages && m.messages.length) {
                d.messages = $Array(m.messages.length);
                for (var j = 0; j < m.messages.length; ++j) {
                    d.messages[j] = $root.proto.WebMessageInfo.toObject(m.messages[j], o, q + 1);
                }
            }
            if (m.uncountedAssociatedMessageLists && m.uncountedAssociatedMessageLists.length) {
                d.uncountedAssociatedMessageLists = $Array(m.uncountedAssociatedMessageLists.length);
                for (var j = 0; j < m.uncountedAssociatedMessageLists.length; ++j) {
                    d.uncountedAssociatedMessageLists[j] = $root.proto.UnCountedAssociatedMessageList.toObject(m.uncountedAssociatedMessageLists[j], o, q + 1);
                }
            }
            if (m.commentMessages && m.commentMessages.length) {
                d.commentMessages = $Array(m.commentMessages.length);
                for (var j = 0; j < m.commentMessages.length; ++j) {
                    d.commentMessages[j] = $root.proto.WebMessageInfo.toObject(m.commentMessages[j], o, q + 1);
                }
            }
            if (m.outOfWindowPinnedMessages && m.outOfWindowPinnedMessages.length) {
                d.outOfWindowPinnedMessages = $Array(m.outOfWindowPinnedMessages.length);
                for (var j = 0; j < m.outOfWindowPinnedMessages.length; ++j) {
                    d.outOfWindowPinnedMessages[j] = $root.proto.WebMessageInfo.toObject(m.outOfWindowPinnedMessages[j], o, q + 1);
                }
            }
            return d;
        };

        GroupHistory.prototype.toJSON = function() {
            return GroupHistory.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupHistory.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupHistory";
        };

        return GroupHistory;
    })();

    proto.GroupHistoryBundleInfo = (function() {

        const GroupHistoryBundleInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupHistoryBundleInfo.prototype.deprecatedMessageHistoryBundle = null;
        GroupHistoryBundleInfo.prototype.processState = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupHistoryBundleInfo.prototype, "_deprecatedMessageHistoryBundle", {
            get: $util.oneOfGetter($oneOfFields = ["deprecatedMessageHistoryBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupHistoryBundleInfo.prototype, "_processState", {
            get: $util.oneOfGetter($oneOfFields = ["processState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupHistoryBundleInfo.create = function(properties) {
            return new GroupHistoryBundleInfo(properties);
        };

        GroupHistoryBundleInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.deprecatedMessageHistoryBundle != null && $Object.hasOwnProperty.call(m, "deprecatedMessageHistoryBundle"))
                $root.proto.Message.MessageHistoryBundle.encode(m.deprecatedMessageHistoryBundle, w.uint32(10).fork(), q + 1).ldelim();
            if (m.processState != null && $Object.hasOwnProperty.call(m, "processState"))
                w.uint32(16).int32(m.processState);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupHistoryBundleInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupHistoryBundleInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.deprecatedMessageHistoryBundle = $root.proto.Message.MessageHistoryBundle.decode(r, r.uint32(), $undefined, q + 1, m.deprecatedMessageHistoryBundle);
                        m._deprecatedMessageHistoryBundle = "deprecatedMessageHistoryBundle";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.processState = r.int32();
                        m._processState = "processState";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupHistoryBundleInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupHistoryBundleInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupHistoryBundleInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupHistoryBundleInfo();
            if (d.deprecatedMessageHistoryBundle != null) {
                if (!$util.isObject(d.deprecatedMessageHistoryBundle))
                    throw $TypeError(".proto.GroupHistoryBundleInfo.deprecatedMessageHistoryBundle: object expected");
                m.deprecatedMessageHistoryBundle = $root.proto.Message.MessageHistoryBundle.fromObject(d.deprecatedMessageHistoryBundle, q + 1);
            }
            switch (d.processState) {
            case "NOT_INJECTED":
            case 0:
                m.processState = 0;
                break;
            case "INJECTED":
            case 1:
                m.processState = 1;
                break;
            case "INJECTED_PARTIAL":
            case 2:
                m.processState = 2;
                break;
            case "INJECTION_FAILED":
            case 3:
                m.processState = 3;
                break;
            case "INJECTION_FAILED_NO_RETRY":
            case 4:
                m.processState = 4;
                break;
            case "DEDUPED":
            case 5:
                m.processState = 5;
                break;
            default:
                if (typeof d.processState === "number" && (d.processState | 0) === d.processState)
                    m.processState = d.processState;
            }
            return m;
        };

        GroupHistoryBundleInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.deprecatedMessageHistoryBundle != null && $Object.hasOwnProperty.call(m, "deprecatedMessageHistoryBundle")) {
                d.deprecatedMessageHistoryBundle = $root.proto.Message.MessageHistoryBundle.toObject(m.deprecatedMessageHistoryBundle, o, q + 1);
            }
            if (m.processState != null && $Object.hasOwnProperty.call(m, "processState")) {
                d.processState = o.enums === $String ? $root.proto.GroupHistoryBundleInfo.ProcessState[m.processState] === $undefined ? m.processState : $root.proto.GroupHistoryBundleInfo.ProcessState[m.processState] : m.processState;
            }
            return d;
        };

        GroupHistoryBundleInfo.prototype.toJSON = function() {
            return GroupHistoryBundleInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupHistoryBundleInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupHistoryBundleInfo";
        };

        GroupHistoryBundleInfo.ProcessState = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NOT_INJECTED"] = 0;
            values[valuesById[1] = "INJECTED"] = 1;
            values[valuesById[2] = "INJECTED_PARTIAL"] = 2;
            values[valuesById[3] = "INJECTION_FAILED"] = 3;
            values[valuesById[4] = "INJECTION_FAILED_NO_RETRY"] = 4;
            values[valuesById[5] = "DEDUPED"] = 5;
            return values;
        })();

        return GroupHistoryBundleInfo;
    })();

    proto.GroupHistoryIndividualMessageInfo = (function() {

        const GroupHistoryIndividualMessageInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupHistoryIndividualMessageInfo.prototype.bundleMessageKey = null;
        GroupHistoryIndividualMessageInfo.prototype.editedAfterReceivedAsHistory = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupHistoryIndividualMessageInfo.prototype, "_bundleMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["bundleMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupHistoryIndividualMessageInfo.prototype, "_editedAfterReceivedAsHistory", {
            get: $util.oneOfGetter($oneOfFields = ["editedAfterReceivedAsHistory"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupHistoryIndividualMessageInfo.create = function(properties) {
            return new GroupHistoryIndividualMessageInfo(properties);
        };

        GroupHistoryIndividualMessageInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.bundleMessageKey != null && $Object.hasOwnProperty.call(m, "bundleMessageKey"))
                $root.proto.MessageKey.encode(m.bundleMessageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.editedAfterReceivedAsHistory != null && $Object.hasOwnProperty.call(m, "editedAfterReceivedAsHistory"))
                w.uint32(16).bool(m.editedAfterReceivedAsHistory);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupHistoryIndividualMessageInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupHistoryIndividualMessageInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.bundleMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.bundleMessageKey);
                        m._bundleMessageKey = "bundleMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.editedAfterReceivedAsHistory = r.bool();
                        m._editedAfterReceivedAsHistory = "editedAfterReceivedAsHistory";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupHistoryIndividualMessageInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupHistoryIndividualMessageInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupHistoryIndividualMessageInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupHistoryIndividualMessageInfo();
            if (d.bundleMessageKey != null) {
                if (!$util.isObject(d.bundleMessageKey))
                    throw $TypeError(".proto.GroupHistoryIndividualMessageInfo.bundleMessageKey: object expected");
                m.bundleMessageKey = $root.proto.MessageKey.fromObject(d.bundleMessageKey, q + 1);
            }
            if (d.editedAfterReceivedAsHistory != null) {
                m.editedAfterReceivedAsHistory = $Boolean(d.editedAfterReceivedAsHistory);
            }
            return m;
        };

        GroupHistoryIndividualMessageInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.bundleMessageKey != null && $Object.hasOwnProperty.call(m, "bundleMessageKey")) {
                d.bundleMessageKey = $root.proto.MessageKey.toObject(m.bundleMessageKey, o, q + 1);
            }
            if (m.editedAfterReceivedAsHistory != null && $Object.hasOwnProperty.call(m, "editedAfterReceivedAsHistory")) {
                d.editedAfterReceivedAsHistory = m.editedAfterReceivedAsHistory;
            }
            return d;
        };

        GroupHistoryIndividualMessageInfo.prototype.toJSON = function() {
            return GroupHistoryIndividualMessageInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupHistoryIndividualMessageInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupHistoryIndividualMessageInfo";
        };

        return GroupHistoryIndividualMessageInfo;
    })();

    proto.GroupHistoryWithMessageBytes = (function() {

        const GroupHistoryWithMessageBytes = function (p) {
            this.messages = [];
            this.uncountedAssociatedMessageLists = [];
            this.commentMessages = [];
            this.outOfWindowPinnedMessages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupHistoryWithMessageBytes.prototype.messages = $util.emptyArray;
        GroupHistoryWithMessageBytes.prototype.uncountedAssociatedMessageLists = $util.emptyArray;
        GroupHistoryWithMessageBytes.prototype.commentMessages = $util.emptyArray;
        GroupHistoryWithMessageBytes.prototype.outOfWindowPinnedMessages = $util.emptyArray;

        GroupHistoryWithMessageBytes.create = function(properties) {
            return new GroupHistoryWithMessageBytes(properties);
        };

        GroupHistoryWithMessageBytes.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messages != null && m.messages.length) {
                for (var i = 0; i < m.messages.length; ++i)
                    $root.proto.WebMessageInfoWithMessageBytes.encode(m.messages[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.uncountedAssociatedMessageLists != null && m.uncountedAssociatedMessageLists.length) {
                for (var i = 0; i < m.uncountedAssociatedMessageLists.length; ++i)
                    $root.proto.UnCountedAssociatedMessageListWithMessageBytes.encode(m.uncountedAssociatedMessageLists[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.commentMessages != null && m.commentMessages.length) {
                for (var i = 0; i < m.commentMessages.length; ++i)
                    $root.proto.WebMessageInfoWithMessageBytes.encode(m.commentMessages[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.outOfWindowPinnedMessages != null && m.outOfWindowPinnedMessages.length) {
                for (var i = 0; i < m.outOfWindowPinnedMessages.length; ++i)
                    $root.proto.WebMessageInfoWithMessageBytes.encode(m.outOfWindowPinnedMessages[i], w.uint32(34).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupHistoryWithMessageBytes.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupHistoryWithMessageBytes();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.messages && m.messages.length))
                            m.messages = [];
                        m.messages.push($root.proto.WebMessageInfoWithMessageBytes.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.uncountedAssociatedMessageLists && m.uncountedAssociatedMessageLists.length))
                            m.uncountedAssociatedMessageLists = [];
                        m.uncountedAssociatedMessageLists.push($root.proto.UnCountedAssociatedMessageListWithMessageBytes.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.commentMessages && m.commentMessages.length))
                            m.commentMessages = [];
                        m.commentMessages.push($root.proto.WebMessageInfoWithMessageBytes.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(m.outOfWindowPinnedMessages && m.outOfWindowPinnedMessages.length))
                            m.outOfWindowPinnedMessages = [];
                        m.outOfWindowPinnedMessages.push($root.proto.WebMessageInfoWithMessageBytes.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupHistoryWithMessageBytes.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupHistoryWithMessageBytes)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupHistoryWithMessageBytes: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupHistoryWithMessageBytes();
            if (d.messages) {
                if (!$Array.isArray(d.messages))
                    throw $TypeError(".proto.GroupHistoryWithMessageBytes.messages: array expected");
                m.messages = $Array(d.messages.length);
                for (var i = 0; i < d.messages.length; ++i) {
                    if (!$util.isObject(d.messages[i]))
                        throw $TypeError(".proto.GroupHistoryWithMessageBytes.messages: object expected");
                    m.messages[i] = $root.proto.WebMessageInfoWithMessageBytes.fromObject(d.messages[i], q + 1);
                }
            }
            if (d.uncountedAssociatedMessageLists) {
                if (!$Array.isArray(d.uncountedAssociatedMessageLists))
                    throw $TypeError(".proto.GroupHistoryWithMessageBytes.uncountedAssociatedMessageLists: array expected");
                m.uncountedAssociatedMessageLists = $Array(d.uncountedAssociatedMessageLists.length);
                for (var i = 0; i < d.uncountedAssociatedMessageLists.length; ++i) {
                    if (!$util.isObject(d.uncountedAssociatedMessageLists[i]))
                        throw $TypeError(".proto.GroupHistoryWithMessageBytes.uncountedAssociatedMessageLists: object expected");
                    m.uncountedAssociatedMessageLists[i] = $root.proto.UnCountedAssociatedMessageListWithMessageBytes.fromObject(d.uncountedAssociatedMessageLists[i], q + 1);
                }
            }
            if (d.commentMessages) {
                if (!$Array.isArray(d.commentMessages))
                    throw $TypeError(".proto.GroupHistoryWithMessageBytes.commentMessages: array expected");
                m.commentMessages = $Array(d.commentMessages.length);
                for (var i = 0; i < d.commentMessages.length; ++i) {
                    if (!$util.isObject(d.commentMessages[i]))
                        throw $TypeError(".proto.GroupHistoryWithMessageBytes.commentMessages: object expected");
                    m.commentMessages[i] = $root.proto.WebMessageInfoWithMessageBytes.fromObject(d.commentMessages[i], q + 1);
                }
            }
            if (d.outOfWindowPinnedMessages) {
                if (!$Array.isArray(d.outOfWindowPinnedMessages))
                    throw $TypeError(".proto.GroupHistoryWithMessageBytes.outOfWindowPinnedMessages: array expected");
                m.outOfWindowPinnedMessages = $Array(d.outOfWindowPinnedMessages.length);
                for (var i = 0; i < d.outOfWindowPinnedMessages.length; ++i) {
                    if (!$util.isObject(d.outOfWindowPinnedMessages[i]))
                        throw $TypeError(".proto.GroupHistoryWithMessageBytes.outOfWindowPinnedMessages: object expected");
                    m.outOfWindowPinnedMessages[i] = $root.proto.WebMessageInfoWithMessageBytes.fromObject(d.outOfWindowPinnedMessages[i], q + 1);
                }
            }
            return m;
        };

        GroupHistoryWithMessageBytes.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.messages = [];
                d.uncountedAssociatedMessageLists = [];
                d.commentMessages = [];
                d.outOfWindowPinnedMessages = [];
            }
            if (m.messages && m.messages.length) {
                d.messages = $Array(m.messages.length);
                for (var j = 0; j < m.messages.length; ++j) {
                    d.messages[j] = $root.proto.WebMessageInfoWithMessageBytes.toObject(m.messages[j], o, q + 1);
                }
            }
            if (m.uncountedAssociatedMessageLists && m.uncountedAssociatedMessageLists.length) {
                d.uncountedAssociatedMessageLists = $Array(m.uncountedAssociatedMessageLists.length);
                for (var j = 0; j < m.uncountedAssociatedMessageLists.length; ++j) {
                    d.uncountedAssociatedMessageLists[j] = $root.proto.UnCountedAssociatedMessageListWithMessageBytes.toObject(m.uncountedAssociatedMessageLists[j], o, q + 1);
                }
            }
            if (m.commentMessages && m.commentMessages.length) {
                d.commentMessages = $Array(m.commentMessages.length);
                for (var j = 0; j < m.commentMessages.length; ++j) {
                    d.commentMessages[j] = $root.proto.WebMessageInfoWithMessageBytes.toObject(m.commentMessages[j], o, q + 1);
                }
            }
            if (m.outOfWindowPinnedMessages && m.outOfWindowPinnedMessages.length) {
                d.outOfWindowPinnedMessages = $Array(m.outOfWindowPinnedMessages.length);
                for (var j = 0; j < m.outOfWindowPinnedMessages.length; ++j) {
                    d.outOfWindowPinnedMessages[j] = $root.proto.WebMessageInfoWithMessageBytes.toObject(m.outOfWindowPinnedMessages[j], o, q + 1);
                }
            }
            return d;
        };

        GroupHistoryWithMessageBytes.prototype.toJSON = function() {
            return GroupHistoryWithMessageBytes.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupHistoryWithMessageBytes.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupHistoryWithMessageBytes";
        };

        return GroupHistoryWithMessageBytes;
    })();

    proto.GroupMention = (function() {

        const GroupMention = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupMention.prototype.groupJid = null;
        GroupMention.prototype.groupSubject = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupMention.prototype, "_groupJid", {
            get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupMention.prototype, "_groupSubject", {
            get: $util.oneOfGetter($oneOfFields = ["groupSubject"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupMention.create = function(properties) {
            return new GroupMention(properties);
        };

        GroupMention.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid"))
                w.uint32(10).string(m.groupJid);
            if (m.groupSubject != null && $Object.hasOwnProperty.call(m, "groupSubject"))
                w.uint32(18).string(m.groupSubject);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupMention.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupMention();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.groupJid = r.stringVerify();
                        m._groupJid = "groupJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.groupSubject = r.stringVerify();
                        m._groupSubject = "groupSubject";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupMention.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupMention)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupMention: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupMention();
            if (d.groupJid != null) {
                m.groupJid = $String(d.groupJid);
            }
            if (d.groupSubject != null) {
                m.groupSubject = $String(d.groupSubject);
            }
            return m;
        };

        GroupMention.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid")) {
                d.groupJid = m.groupJid;
            }
            if (m.groupSubject != null && $Object.hasOwnProperty.call(m, "groupSubject")) {
                d.groupSubject = m.groupSubject;
            }
            return d;
        };

        GroupMention.prototype.toJSON = function() {
            return GroupMention.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupMention.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupMention";
        };

        return GroupMention;
    })();

    proto.GroupParticipant = (function() {

        const GroupParticipant = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupParticipant.prototype.userJid = "";
        GroupParticipant.prototype.rank = null;
        GroupParticipant.prototype.memberLabel = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupParticipant.prototype, "_rank", {
            get: $util.oneOfGetter($oneOfFields = ["rank"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupParticipant.prototype, "_memberLabel", {
            get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupParticipant.create = function(properties) {
            return new GroupParticipant(properties);
        };

        GroupParticipant.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).string(m.userJid);
            if (m.rank != null && $Object.hasOwnProperty.call(m, "rank"))
                w.uint32(16).int32(m.rank);
            if (m.memberLabel != null && $Object.hasOwnProperty.call(m, "memberLabel"))
                $root.proto.MemberLabel.encode(m.memberLabel, w.uint32(26).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupParticipant.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupParticipant(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.userJid = r.stringVerify();
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.rank = r.int32();
                        m._rank = "rank";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.memberLabel = $root.proto.MemberLabel.decode(r, r.uint32(), $undefined, q + 1, m.memberLabel);
                        m._memberLabel = "memberLabel";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "userJid"))
                throw $util.ProtocolError("missing required 'userJid'", { instance: m });
            return m;
        };

        GroupParticipant.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupParticipant)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupParticipant: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupParticipant();
            if (d.userJid != null) {
                m.userJid = $String(d.userJid);
            }
            switch (d.rank) {
            case "REGULAR":
            case 0:
                m.rank = 0;
                break;
            case "ADMIN":
            case 1:
                m.rank = 1;
                break;
            case "SUPERADMIN":
            case 2:
                m.rank = 2;
                break;
            default:
                if (typeof d.rank === "number" && (d.rank | 0) === d.rank)
                    m.rank = d.rank;
            }
            if (d.memberLabel != null) {
                if (!$util.isObject(d.memberLabel))
                    throw $TypeError(".proto.GroupParticipant.memberLabel: object expected");
                m.memberLabel = $root.proto.MemberLabel.fromObject(d.memberLabel, q + 1);
            }
            return m;
        };

        GroupParticipant.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                d.userJid = "";
            }
            if (m.userJid != null && $Object.hasOwnProperty.call(m, "userJid")) {
                d.userJid = m.userJid;
            }
            if (m.rank != null && $Object.hasOwnProperty.call(m, "rank")) {
                d.rank = o.enums === $String ? $root.proto.GroupParticipant.Rank[m.rank] === $undefined ? m.rank : $root.proto.GroupParticipant.Rank[m.rank] : m.rank;
            }
            if (m.memberLabel != null && $Object.hasOwnProperty.call(m, "memberLabel")) {
                d.memberLabel = $root.proto.MemberLabel.toObject(m.memberLabel, o, q + 1);
            }
            return d;
        };

        GroupParticipant.prototype.toJSON = function() {
            return GroupParticipant.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupParticipant.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupParticipant";
        };

        GroupParticipant.Rank = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "REGULAR"] = 0;
            values[valuesById[1] = "ADMIN"] = 1;
            values[valuesById[2] = "SUPERADMIN"] = 2;
            return values;
        })();

        return GroupParticipant;
    })();

    proto.GroupRootKeyShare = (function() {

        const GroupRootKeyShare = function (p) {
            this.keys = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupRootKeyShare.prototype.keys = $util.emptyArray;

        GroupRootKeyShare.create = function(properties) {
            return new GroupRootKeyShare(properties);
        };

        GroupRootKeyShare.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.keys != null && m.keys.length) {
                for (var i = 0; i < m.keys.length; ++i)
                    $root.proto.GroupRootKeyShareEntry.encode(m.keys[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupRootKeyShare.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupRootKeyShare();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.keys && m.keys.length))
                            m.keys = [];
                        m.keys.push($root.proto.GroupRootKeyShareEntry.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupRootKeyShare.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupRootKeyShare)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupRootKeyShare: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupRootKeyShare();
            if (d.keys) {
                if (!$Array.isArray(d.keys))
                    throw $TypeError(".proto.GroupRootKeyShare.keys: array expected");
                m.keys = $Array(d.keys.length);
                for (var i = 0; i < d.keys.length; ++i) {
                    if (!$util.isObject(d.keys[i]))
                        throw $TypeError(".proto.GroupRootKeyShare.keys: object expected");
                    m.keys[i] = $root.proto.GroupRootKeyShareEntry.fromObject(d.keys[i], q + 1);
                }
            }
            return m;
        };

        GroupRootKeyShare.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.keys = [];
            }
            if (m.keys && m.keys.length) {
                d.keys = $Array(m.keys.length);
                for (var j = 0; j < m.keys.length; ++j) {
                    d.keys[j] = $root.proto.GroupRootKeyShareEntry.toObject(m.keys[j], o, q + 1);
                }
            }
            return d;
        };

        GroupRootKeyShare.prototype.toJSON = function() {
            return GroupRootKeyShare.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupRootKeyShare.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupRootKeyShare";
        };

        return GroupRootKeyShare;
    })();

    proto.GroupRootKeyShareEntry = (function() {

        const GroupRootKeyShareEntry = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        GroupRootKeyShareEntry.prototype.groupRootKey = null;
        GroupRootKeyShareEntry.prototype.keyId = null;
        GroupRootKeyShareEntry.prototype.expiryTimestampMs = null;
        GroupRootKeyShareEntry.prototype.createdTimestampMs = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupRootKeyShareEntry.prototype, "_groupRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupRootKeyShareEntry.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupRootKeyShareEntry.prototype, "_expiryTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(GroupRootKeyShareEntry.prototype, "_createdTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["createdTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        GroupRootKeyShareEntry.create = function(properties) {
            return new GroupRootKeyShareEntry(properties);
        };

        GroupRootKeyShareEntry.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.groupRootKey != null && $Object.hasOwnProperty.call(m, "groupRootKey"))
                w.uint32(10).bytes(m.groupRootKey);
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                w.uint32(18).string(m.keyId);
            if (m.expiryTimestampMs != null && $Object.hasOwnProperty.call(m, "expiryTimestampMs"))
                w.uint32(24).int64(m.expiryTimestampMs);
            if (m.createdTimestampMs != null && $Object.hasOwnProperty.call(m, "createdTimestampMs"))
                w.uint32(32).int64(m.createdTimestampMs);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        GroupRootKeyShareEntry.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.GroupRootKeyShareEntry();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.groupRootKey = r.bytes();
                        m._groupRootKey = "groupRootKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.keyId = r.stringVerify();
                        m._keyId = "keyId";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.expiryTimestampMs = r.int64();
                        m._expiryTimestampMs = "expiryTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.createdTimestampMs = r.int64();
                        m._createdTimestampMs = "createdTimestampMs";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        GroupRootKeyShareEntry.fromObject = function (d, q) {
            if (d instanceof $root.proto.GroupRootKeyShareEntry)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.GroupRootKeyShareEntry: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.GroupRootKeyShareEntry();
            if (d.groupRootKey != null) {
                if (typeof d.groupRootKey === "string")
                    $util.base64.decode(d.groupRootKey, m.groupRootKey = $util.newBuffer($util.base64.length(d.groupRootKey)), 0);
                else if (d.groupRootKey.length >= 0)
                    m.groupRootKey = d.groupRootKey;
            }
            if (d.keyId != null) {
                m.keyId = $String(d.keyId);
            }
            if (d.expiryTimestampMs != null) {
                if ($util.Long)
                    m.expiryTimestampMs = $util.Long.fromValue(d.expiryTimestampMs, false);
                else if (typeof d.expiryTimestampMs === "string")
                    m.expiryTimestampMs = $parseInt(d.expiryTimestampMs, 10);
                else if (typeof d.expiryTimestampMs === "number")
                    m.expiryTimestampMs = d.expiryTimestampMs;
                else if (typeof d.expiryTimestampMs === "object")
                    m.expiryTimestampMs = new $util.LongBits(d.expiryTimestampMs.low >>> 0, d.expiryTimestampMs.high >>> 0).toNumber();
            }
            if (d.createdTimestampMs != null) {
                if ($util.Long)
                    m.createdTimestampMs = $util.Long.fromValue(d.createdTimestampMs, false);
                else if (typeof d.createdTimestampMs === "string")
                    m.createdTimestampMs = $parseInt(d.createdTimestampMs, 10);
                else if (typeof d.createdTimestampMs === "number")
                    m.createdTimestampMs = d.createdTimestampMs;
                else if (typeof d.createdTimestampMs === "object")
                    m.createdTimestampMs = new $util.LongBits(d.createdTimestampMs.low >>> 0, d.createdTimestampMs.high >>> 0).toNumber();
            }
            return m;
        };

        GroupRootKeyShareEntry.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.groupRootKey != null && $Object.hasOwnProperty.call(m, "groupRootKey")) {
                d.groupRootKey = o.bytes === $String ? $util.base64.encode(m.groupRootKey, 0, m.groupRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.groupRootKey) : m.groupRootKey;
            }
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                d.keyId = m.keyId;
            }
            if (m.expiryTimestampMs != null && $Object.hasOwnProperty.call(m, "expiryTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.expiryTimestampMs = typeof m.expiryTimestampMs === "number" ? $BigInt(m.expiryTimestampMs) : $util.Long.fromBits(m.expiryTimestampMs.low >>> 0, m.expiryTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.expiryTimestampMs === "number")
                    d.expiryTimestampMs = o.longs === $String ? $String(m.expiryTimestampMs) : m.expiryTimestampMs;
                else
                    d.expiryTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.expiryTimestampMs) : o.longs === $Number ? new $util.LongBits(m.expiryTimestampMs.low >>> 0, m.expiryTimestampMs.high >>> 0).toNumber() : m.expiryTimestampMs;
            }
            if (m.createdTimestampMs != null && $Object.hasOwnProperty.call(m, "createdTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.createdTimestampMs = typeof m.createdTimestampMs === "number" ? $BigInt(m.createdTimestampMs) : $util.Long.fromBits(m.createdTimestampMs.low >>> 0, m.createdTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.createdTimestampMs === "number")
                    d.createdTimestampMs = o.longs === $String ? $String(m.createdTimestampMs) : m.createdTimestampMs;
                else
                    d.createdTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.createdTimestampMs) : o.longs === $Number ? new $util.LongBits(m.createdTimestampMs.low >>> 0, m.createdTimestampMs.high >>> 0).toNumber() : m.createdTimestampMs;
            }
            return d;
        };

        GroupRootKeyShareEntry.prototype.toJSON = function() {
            return GroupRootKeyShareEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        GroupRootKeyShareEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.GroupRootKeyShareEntry";
        };

        return GroupRootKeyShareEntry;
    })();

    proto.HandshakeMessage = (function() {

        const HandshakeMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        HandshakeMessage.prototype.clientHello = null;
        HandshakeMessage.prototype.serverHello = null;
        HandshakeMessage.prototype.clientFinish = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HandshakeMessage.prototype, "_clientHello", {
            get: $util.oneOfGetter($oneOfFields = ["clientHello"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HandshakeMessage.prototype, "_serverHello", {
            get: $util.oneOfGetter($oneOfFields = ["serverHello"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HandshakeMessage.prototype, "_clientFinish", {
            get: $util.oneOfGetter($oneOfFields = ["clientFinish"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HandshakeMessage.create = function(properties) {
            return new HandshakeMessage(properties);
        };

        HandshakeMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.clientHello != null && $Object.hasOwnProperty.call(m, "clientHello"))
                $root.proto.HandshakeMessage.ClientHello.encode(m.clientHello, w.uint32(18).fork(), q + 1).ldelim();
            if (m.serverHello != null && $Object.hasOwnProperty.call(m, "serverHello"))
                $root.proto.HandshakeMessage.ServerHello.encode(m.serverHello, w.uint32(26).fork(), q + 1).ldelim();
            if (m.clientFinish != null && $Object.hasOwnProperty.call(m, "clientFinish"))
                $root.proto.HandshakeMessage.ClientFinish.encode(m.clientFinish, w.uint32(34).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        HandshakeMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HandshakeMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 2: {
                        if (u !== 2)
                            break;
                        m.clientHello = $root.proto.HandshakeMessage.ClientHello.decode(r, r.uint32(), $undefined, q + 1, m.clientHello);
                        m._clientHello = "clientHello";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.serverHello = $root.proto.HandshakeMessage.ServerHello.decode(r, r.uint32(), $undefined, q + 1, m.serverHello);
                        m._serverHello = "serverHello";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.clientFinish = $root.proto.HandshakeMessage.ClientFinish.decode(r, r.uint32(), $undefined, q + 1, m.clientFinish);
                        m._clientFinish = "clientFinish";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        HandshakeMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.HandshakeMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.HandshakeMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.HandshakeMessage();
            if (d.clientHello != null) {
                if (!$util.isObject(d.clientHello))
                    throw $TypeError(".proto.HandshakeMessage.clientHello: object expected");
                m.clientHello = $root.proto.HandshakeMessage.ClientHello.fromObject(d.clientHello, q + 1);
            }
            if (d.serverHello != null) {
                if (!$util.isObject(d.serverHello))
                    throw $TypeError(".proto.HandshakeMessage.serverHello: object expected");
                m.serverHello = $root.proto.HandshakeMessage.ServerHello.fromObject(d.serverHello, q + 1);
            }
            if (d.clientFinish != null) {
                if (!$util.isObject(d.clientFinish))
                    throw $TypeError(".proto.HandshakeMessage.clientFinish: object expected");
                m.clientFinish = $root.proto.HandshakeMessage.ClientFinish.fromObject(d.clientFinish, q + 1);
            }
            return m;
        };

        HandshakeMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.clientHello != null && $Object.hasOwnProperty.call(m, "clientHello")) {
                d.clientHello = $root.proto.HandshakeMessage.ClientHello.toObject(m.clientHello, o, q + 1);
            }
            if (m.serverHello != null && $Object.hasOwnProperty.call(m, "serverHello")) {
                d.serverHello = $root.proto.HandshakeMessage.ServerHello.toObject(m.serverHello, o, q + 1);
            }
            if (m.clientFinish != null && $Object.hasOwnProperty.call(m, "clientFinish")) {
                d.clientFinish = $root.proto.HandshakeMessage.ClientFinish.toObject(m.clientFinish, o, q + 1);
            }
            return d;
        };

        HandshakeMessage.prototype.toJSON = function() {
            return HandshakeMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        HandshakeMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HandshakeMessage";
        };

        HandshakeMessage.ClientFinish = (function() {

            const ClientFinish = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ClientFinish.prototype["static"] = null;
            ClientFinish.prototype.payload = null;
            ClientFinish.prototype.extendedCiphertext = null;
            ClientFinish.prototype.paddedBytes = null;
            ClientFinish.prototype.simulateXxkemFs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientFinish.prototype, "_static", {
                get: $util.oneOfGetter($oneOfFields = ["static"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientFinish.prototype, "_payload", {
                get: $util.oneOfGetter($oneOfFields = ["payload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientFinish.prototype, "_extendedCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["extendedCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientFinish.prototype, "_paddedBytes", {
                get: $util.oneOfGetter($oneOfFields = ["paddedBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientFinish.prototype, "_simulateXxkemFs", {
                get: $util.oneOfGetter($oneOfFields = ["simulateXxkemFs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ClientFinish.create = function(properties) {
                return new ClientFinish(properties);
            };

            ClientFinish.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m["static"] != null && $Object.hasOwnProperty.call(m, "static"))
                    w.uint32(10).bytes(m["static"]);
                if (m.payload != null && $Object.hasOwnProperty.call(m, "payload"))
                    w.uint32(18).bytes(m.payload);
                if (m.extendedCiphertext != null && $Object.hasOwnProperty.call(m, "extendedCiphertext"))
                    w.uint32(26).bytes(m.extendedCiphertext);
                if (m.paddedBytes != null && $Object.hasOwnProperty.call(m, "paddedBytes"))
                    w.uint32(34).bytes(m.paddedBytes);
                if (m.simulateXxkemFs != null && $Object.hasOwnProperty.call(m, "simulateXxkemFs"))
                    w.uint32(40).bool(m.simulateXxkemFs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ClientFinish.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HandshakeMessage.ClientFinish();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m["static"] = r.bytes();
                            m._static = "static";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.payload = r.bytes();
                            m._payload = "payload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.extendedCiphertext = r.bytes();
                            m._extendedCiphertext = "extendedCiphertext";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.paddedBytes = r.bytes();
                            m._paddedBytes = "paddedBytes";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.simulateXxkemFs = r.bool();
                            m._simulateXxkemFs = "simulateXxkemFs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ClientFinish.fromObject = function (d, q) {
                if (d instanceof $root.proto.HandshakeMessage.ClientFinish)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.HandshakeMessage.ClientFinish: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.HandshakeMessage.ClientFinish();
                if (d["static"] != null) {
                    if (typeof d["static"] === "string")
                        $util.base64.decode(d["static"], m["static"] = $util.newBuffer($util.base64.length(d["static"])), 0);
                    else if (d["static"].length >= 0)
                        m["static"] = d["static"];
                }
                if (d.payload != null) {
                    if (typeof d.payload === "string")
                        $util.base64.decode(d.payload, m.payload = $util.newBuffer($util.base64.length(d.payload)), 0);
                    else if (d.payload.length >= 0)
                        m.payload = d.payload;
                }
                if (d.extendedCiphertext != null) {
                    if (typeof d.extendedCiphertext === "string")
                        $util.base64.decode(d.extendedCiphertext, m.extendedCiphertext = $util.newBuffer($util.base64.length(d.extendedCiphertext)), 0);
                    else if (d.extendedCiphertext.length >= 0)
                        m.extendedCiphertext = d.extendedCiphertext;
                }
                if (d.paddedBytes != null) {
                    if (typeof d.paddedBytes === "string")
                        $util.base64.decode(d.paddedBytes, m.paddedBytes = $util.newBuffer($util.base64.length(d.paddedBytes)), 0);
                    else if (d.paddedBytes.length >= 0)
                        m.paddedBytes = d.paddedBytes;
                }
                if (d.simulateXxkemFs != null) {
                    m.simulateXxkemFs = $Boolean(d.simulateXxkemFs);
                }
                return m;
            };

            ClientFinish.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m["static"] != null && $Object.hasOwnProperty.call(m, "static")) {
                    d["static"] = o.bytes === $String ? $util.base64.encode(m["static"], 0, m["static"].length) : o.bytes === $Array ? $Array.prototype.slice.call(m["static"]) : m["static"];
                }
                if (m.payload != null && $Object.hasOwnProperty.call(m, "payload")) {
                    d.payload = o.bytes === $String ? $util.base64.encode(m.payload, 0, m.payload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.payload) : m.payload;
                }
                if (m.extendedCiphertext != null && $Object.hasOwnProperty.call(m, "extendedCiphertext")) {
                    d.extendedCiphertext = o.bytes === $String ? $util.base64.encode(m.extendedCiphertext, 0, m.extendedCiphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.extendedCiphertext) : m.extendedCiphertext;
                }
                if (m.paddedBytes != null && $Object.hasOwnProperty.call(m, "paddedBytes")) {
                    d.paddedBytes = o.bytes === $String ? $util.base64.encode(m.paddedBytes, 0, m.paddedBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.paddedBytes) : m.paddedBytes;
                }
                if (m.simulateXxkemFs != null && $Object.hasOwnProperty.call(m, "simulateXxkemFs")) {
                    d.simulateXxkemFs = m.simulateXxkemFs;
                }
                return d;
            };

            ClientFinish.prototype.toJSON = function() {
                return ClientFinish.toObject(this, $protobuf.util.toJSONOptions);
            };

            ClientFinish.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.HandshakeMessage.ClientFinish";
            };

            return ClientFinish;
        })();

        HandshakeMessage.ClientHello = (function() {

            const ClientHello = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ClientHello.prototype.ephemeral = null;
            ClientHello.prototype["static"] = null;
            ClientHello.prototype.payload = null;
            ClientHello.prototype.useExtended = null;
            ClientHello.prototype.extendedCiphertext = null;
            ClientHello.prototype.paddedBytes = null;
            ClientHello.prototype.sendServerHelloPaddedBytes = null;
            ClientHello.prototype.simulateXxkemFs = null;
            ClientHello.prototype.pqMode = null;
            ClientHello.prototype.extendedEphemeral = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_ephemeral", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeral"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_static", {
                get: $util.oneOfGetter($oneOfFields = ["static"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_payload", {
                get: $util.oneOfGetter($oneOfFields = ["payload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_useExtended", {
                get: $util.oneOfGetter($oneOfFields = ["useExtended"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_extendedCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["extendedCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_paddedBytes", {
                get: $util.oneOfGetter($oneOfFields = ["paddedBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_sendServerHelloPaddedBytes", {
                get: $util.oneOfGetter($oneOfFields = ["sendServerHelloPaddedBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_simulateXxkemFs", {
                get: $util.oneOfGetter($oneOfFields = ["simulateXxkemFs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_pqMode", {
                get: $util.oneOfGetter($oneOfFields = ["pqMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClientHello.prototype, "_extendedEphemeral", {
                get: $util.oneOfGetter($oneOfFields = ["extendedEphemeral"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ClientHello.create = function(properties) {
                return new ClientHello(properties);
            };

            ClientHello.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.ephemeral != null && $Object.hasOwnProperty.call(m, "ephemeral"))
                    w.uint32(10).bytes(m.ephemeral);
                if (m["static"] != null && $Object.hasOwnProperty.call(m, "static"))
                    w.uint32(18).bytes(m["static"]);
                if (m.payload != null && $Object.hasOwnProperty.call(m, "payload"))
                    w.uint32(26).bytes(m.payload);
                if (m.useExtended != null && $Object.hasOwnProperty.call(m, "useExtended"))
                    w.uint32(32).bool(m.useExtended);
                if (m.extendedCiphertext != null && $Object.hasOwnProperty.call(m, "extendedCiphertext"))
                    w.uint32(42).bytes(m.extendedCiphertext);
                if (m.paddedBytes != null && $Object.hasOwnProperty.call(m, "paddedBytes"))
                    w.uint32(50).bytes(m.paddedBytes);
                if (m.sendServerHelloPaddedBytes != null && $Object.hasOwnProperty.call(m, "sendServerHelloPaddedBytes"))
                    w.uint32(56).bool(m.sendServerHelloPaddedBytes);
                if (m.simulateXxkemFs != null && $Object.hasOwnProperty.call(m, "simulateXxkemFs"))
                    w.uint32(64).bool(m.simulateXxkemFs);
                if (m.pqMode != null && $Object.hasOwnProperty.call(m, "pqMode"))
                    w.uint32(72).int32(m.pqMode);
                if (m.extendedEphemeral != null && $Object.hasOwnProperty.call(m, "extendedEphemeral"))
                    w.uint32(82).bytes(m.extendedEphemeral);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ClientHello.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HandshakeMessage.ClientHello(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.ephemeral = r.bytes();
                            m._ephemeral = "ephemeral";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m["static"] = r.bytes();
                            m._static = "static";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.payload = r.bytes();
                            m._payload = "payload";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.useExtended = r.bool();
                            m._useExtended = "useExtended";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.extendedCiphertext = r.bytes();
                            m._extendedCiphertext = "extendedCiphertext";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.paddedBytes = r.bytes();
                            m._paddedBytes = "paddedBytes";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.sendServerHelloPaddedBytes = r.bool();
                            m._sendServerHelloPaddedBytes = "sendServerHelloPaddedBytes";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.simulateXxkemFs = r.bool();
                            m._simulateXxkemFs = "simulateXxkemFs";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.pqMode = r.int32();
                            m._pqMode = "pqMode";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.extendedEphemeral = r.bytes();
                            m._extendedEphemeral = "extendedEphemeral";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ClientHello.fromObject = function (d, q) {
                if (d instanceof $root.proto.HandshakeMessage.ClientHello)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.HandshakeMessage.ClientHello: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.HandshakeMessage.ClientHello();
                if (d.ephemeral != null) {
                    if (typeof d.ephemeral === "string")
                        $util.base64.decode(d.ephemeral, m.ephemeral = $util.newBuffer($util.base64.length(d.ephemeral)), 0);
                    else if (d.ephemeral.length >= 0)
                        m.ephemeral = d.ephemeral;
                }
                if (d["static"] != null) {
                    if (typeof d["static"] === "string")
                        $util.base64.decode(d["static"], m["static"] = $util.newBuffer($util.base64.length(d["static"])), 0);
                    else if (d["static"].length >= 0)
                        m["static"] = d["static"];
                }
                if (d.payload != null) {
                    if (typeof d.payload === "string")
                        $util.base64.decode(d.payload, m.payload = $util.newBuffer($util.base64.length(d.payload)), 0);
                    else if (d.payload.length >= 0)
                        m.payload = d.payload;
                }
                if (d.useExtended != null) {
                    m.useExtended = $Boolean(d.useExtended);
                }
                if (d.extendedCiphertext != null) {
                    if (typeof d.extendedCiphertext === "string")
                        $util.base64.decode(d.extendedCiphertext, m.extendedCiphertext = $util.newBuffer($util.base64.length(d.extendedCiphertext)), 0);
                    else if (d.extendedCiphertext.length >= 0)
                        m.extendedCiphertext = d.extendedCiphertext;
                }
                if (d.paddedBytes != null) {
                    if (typeof d.paddedBytes === "string")
                        $util.base64.decode(d.paddedBytes, m.paddedBytes = $util.newBuffer($util.base64.length(d.paddedBytes)), 0);
                    else if (d.paddedBytes.length >= 0)
                        m.paddedBytes = d.paddedBytes;
                }
                if (d.sendServerHelloPaddedBytes != null) {
                    m.sendServerHelloPaddedBytes = $Boolean(d.sendServerHelloPaddedBytes);
                }
                if (d.simulateXxkemFs != null) {
                    m.simulateXxkemFs = $Boolean(d.simulateXxkemFs);
                }
                switch (d.pqMode) {
                case "HANDSHAKE_PQ_MODE_UNKNOWN":
                case 0:
                    m.pqMode = 0;
                    break;
                case "XXKEM":
                case 1:
                    m.pqMode = 1;
                    break;
                case "XXKEM_FS":
                case 2:
                    m.pqMode = 2;
                    break;
                case "XXKEM_EPH":
                case 9:
                    m.pqMode = 9;
                    break;
                case "WA_CLASSICAL":
                case 3:
                    m.pqMode = 3;
                    break;
                case "WA_PQ":
                case 4:
                    m.pqMode = 4;
                    break;
                case "IKKEM":
                case 5:
                    m.pqMode = 5;
                    break;
                case "IKKEM_FS":
                case 6:
                    m.pqMode = 6;
                    break;
                case "XXKEM_2":
                case 7:
                    m.pqMode = 7;
                    break;
                case "IKKEM_2":
                case 8:
                    m.pqMode = 8;
                    break;
                default:
                    if (typeof d.pqMode === "number" && (d.pqMode | 0) === d.pqMode)
                        m.pqMode = d.pqMode;
                }
                if (d.extendedEphemeral != null) {
                    if (typeof d.extendedEphemeral === "string")
                        $util.base64.decode(d.extendedEphemeral, m.extendedEphemeral = $util.newBuffer($util.base64.length(d.extendedEphemeral)), 0);
                    else if (d.extendedEphemeral.length >= 0)
                        m.extendedEphemeral = d.extendedEphemeral;
                }
                return m;
            };

            ClientHello.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.ephemeral != null && $Object.hasOwnProperty.call(m, "ephemeral")) {
                    d.ephemeral = o.bytes === $String ? $util.base64.encode(m.ephemeral, 0, m.ephemeral.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ephemeral) : m.ephemeral;
                }
                if (m["static"] != null && $Object.hasOwnProperty.call(m, "static")) {
                    d["static"] = o.bytes === $String ? $util.base64.encode(m["static"], 0, m["static"].length) : o.bytes === $Array ? $Array.prototype.slice.call(m["static"]) : m["static"];
                }
                if (m.payload != null && $Object.hasOwnProperty.call(m, "payload")) {
                    d.payload = o.bytes === $String ? $util.base64.encode(m.payload, 0, m.payload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.payload) : m.payload;
                }
                if (m.useExtended != null && $Object.hasOwnProperty.call(m, "useExtended")) {
                    d.useExtended = m.useExtended;
                }
                if (m.extendedCiphertext != null && $Object.hasOwnProperty.call(m, "extendedCiphertext")) {
                    d.extendedCiphertext = o.bytes === $String ? $util.base64.encode(m.extendedCiphertext, 0, m.extendedCiphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.extendedCiphertext) : m.extendedCiphertext;
                }
                if (m.paddedBytes != null && $Object.hasOwnProperty.call(m, "paddedBytes")) {
                    d.paddedBytes = o.bytes === $String ? $util.base64.encode(m.paddedBytes, 0, m.paddedBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.paddedBytes) : m.paddedBytes;
                }
                if (m.sendServerHelloPaddedBytes != null && $Object.hasOwnProperty.call(m, "sendServerHelloPaddedBytes")) {
                    d.sendServerHelloPaddedBytes = m.sendServerHelloPaddedBytes;
                }
                if (m.simulateXxkemFs != null && $Object.hasOwnProperty.call(m, "simulateXxkemFs")) {
                    d.simulateXxkemFs = m.simulateXxkemFs;
                }
                if (m.pqMode != null && $Object.hasOwnProperty.call(m, "pqMode")) {
                    d.pqMode = o.enums === $String ? $root.proto.HandshakeMessage.HandshakePqMode[m.pqMode] === $undefined ? m.pqMode : $root.proto.HandshakeMessage.HandshakePqMode[m.pqMode] : m.pqMode;
                }
                if (m.extendedEphemeral != null && $Object.hasOwnProperty.call(m, "extendedEphemeral")) {
                    d.extendedEphemeral = o.bytes === $String ? $util.base64.encode(m.extendedEphemeral, 0, m.extendedEphemeral.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.extendedEphemeral) : m.extendedEphemeral;
                }
                return d;
            };

            ClientHello.prototype.toJSON = function() {
                return ClientHello.toObject(this, $protobuf.util.toJSONOptions);
            };

            ClientHello.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.HandshakeMessage.ClientHello";
            };

            return ClientHello;
        })();

        HandshakeMessage.HandshakePqMode = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "HANDSHAKE_PQ_MODE_UNKNOWN"] = 0;
            values[valuesById[1] = "XXKEM"] = 1;
            values[valuesById[2] = "XXKEM_FS"] = 2;
            values[valuesById[9] = "XXKEM_EPH"] = 9;
            values[valuesById[3] = "WA_CLASSICAL"] = 3;
            values[valuesById[4] = "WA_PQ"] = 4;
            values[valuesById[5] = "IKKEM"] = 5;
            values[valuesById[6] = "IKKEM_FS"] = 6;
            values[valuesById[7] = "XXKEM_2"] = 7;
            values[valuesById[8] = "IKKEM_2"] = 8;
            return values;
        })();

        HandshakeMessage.ServerHello = (function() {

            const ServerHello = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ServerHello.prototype.ephemeral = null;
            ServerHello.prototype["static"] = null;
            ServerHello.prototype.payload = null;
            ServerHello.prototype.extendedStatic = null;
            ServerHello.prototype.paddingBytes = null;
            ServerHello.prototype.extendedCiphertext = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ServerHello.prototype, "_ephemeral", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeral"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ServerHello.prototype, "_static", {
                get: $util.oneOfGetter($oneOfFields = ["static"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ServerHello.prototype, "_payload", {
                get: $util.oneOfGetter($oneOfFields = ["payload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ServerHello.prototype, "_extendedStatic", {
                get: $util.oneOfGetter($oneOfFields = ["extendedStatic"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ServerHello.prototype, "_paddingBytes", {
                get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ServerHello.prototype, "_extendedCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["extendedCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ServerHello.create = function(properties) {
                return new ServerHello(properties);
            };

            ServerHello.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.ephemeral != null && $Object.hasOwnProperty.call(m, "ephemeral"))
                    w.uint32(10).bytes(m.ephemeral);
                if (m["static"] != null && $Object.hasOwnProperty.call(m, "static"))
                    w.uint32(18).bytes(m["static"]);
                if (m.payload != null && $Object.hasOwnProperty.call(m, "payload"))
                    w.uint32(26).bytes(m.payload);
                if (m.extendedStatic != null && $Object.hasOwnProperty.call(m, "extendedStatic"))
                    w.uint32(34).bytes(m.extendedStatic);
                if (m.paddingBytes != null && $Object.hasOwnProperty.call(m, "paddingBytes"))
                    w.uint32(42).bytes(m.paddingBytes);
                if (m.extendedCiphertext != null && $Object.hasOwnProperty.call(m, "extendedCiphertext"))
                    w.uint32(50).bytes(m.extendedCiphertext);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ServerHello.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HandshakeMessage.ServerHello();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.ephemeral = r.bytes();
                            m._ephemeral = "ephemeral";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m["static"] = r.bytes();
                            m._static = "static";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.payload = r.bytes();
                            m._payload = "payload";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.extendedStatic = r.bytes();
                            m._extendedStatic = "extendedStatic";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.paddingBytes = r.bytes();
                            m._paddingBytes = "paddingBytes";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.extendedCiphertext = r.bytes();
                            m._extendedCiphertext = "extendedCiphertext";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ServerHello.fromObject = function (d, q) {
                if (d instanceof $root.proto.HandshakeMessage.ServerHello)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.HandshakeMessage.ServerHello: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.HandshakeMessage.ServerHello();
                if (d.ephemeral != null) {
                    if (typeof d.ephemeral === "string")
                        $util.base64.decode(d.ephemeral, m.ephemeral = $util.newBuffer($util.base64.length(d.ephemeral)), 0);
                    else if (d.ephemeral.length >= 0)
                        m.ephemeral = d.ephemeral;
                }
                if (d["static"] != null) {
                    if (typeof d["static"] === "string")
                        $util.base64.decode(d["static"], m["static"] = $util.newBuffer($util.base64.length(d["static"])), 0);
                    else if (d["static"].length >= 0)
                        m["static"] = d["static"];
                }
                if (d.payload != null) {
                    if (typeof d.payload === "string")
                        $util.base64.decode(d.payload, m.payload = $util.newBuffer($util.base64.length(d.payload)), 0);
                    else if (d.payload.length >= 0)
                        m.payload = d.payload;
                }
                if (d.extendedStatic != null) {
                    if (typeof d.extendedStatic === "string")
                        $util.base64.decode(d.extendedStatic, m.extendedStatic = $util.newBuffer($util.base64.length(d.extendedStatic)), 0);
                    else if (d.extendedStatic.length >= 0)
                        m.extendedStatic = d.extendedStatic;
                }
                if (d.paddingBytes != null) {
                    if (typeof d.paddingBytes === "string")
                        $util.base64.decode(d.paddingBytes, m.paddingBytes = $util.newBuffer($util.base64.length(d.paddingBytes)), 0);
                    else if (d.paddingBytes.length >= 0)
                        m.paddingBytes = d.paddingBytes;
                }
                if (d.extendedCiphertext != null) {
                    if (typeof d.extendedCiphertext === "string")
                        $util.base64.decode(d.extendedCiphertext, m.extendedCiphertext = $util.newBuffer($util.base64.length(d.extendedCiphertext)), 0);
                    else if (d.extendedCiphertext.length >= 0)
                        m.extendedCiphertext = d.extendedCiphertext;
                }
                return m;
            };

            ServerHello.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.ephemeral != null && $Object.hasOwnProperty.call(m, "ephemeral")) {
                    d.ephemeral = o.bytes === $String ? $util.base64.encode(m.ephemeral, 0, m.ephemeral.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ephemeral) : m.ephemeral;
                }
                if (m["static"] != null && $Object.hasOwnProperty.call(m, "static")) {
                    d["static"] = o.bytes === $String ? $util.base64.encode(m["static"], 0, m["static"].length) : o.bytes === $Array ? $Array.prototype.slice.call(m["static"]) : m["static"];
                }
                if (m.payload != null && $Object.hasOwnProperty.call(m, "payload")) {
                    d.payload = o.bytes === $String ? $util.base64.encode(m.payload, 0, m.payload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.payload) : m.payload;
                }
                if (m.extendedStatic != null && $Object.hasOwnProperty.call(m, "extendedStatic")) {
                    d.extendedStatic = o.bytes === $String ? $util.base64.encode(m.extendedStatic, 0, m.extendedStatic.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.extendedStatic) : m.extendedStatic;
                }
                if (m.paddingBytes != null && $Object.hasOwnProperty.call(m, "paddingBytes")) {
                    d.paddingBytes = o.bytes === $String ? $util.base64.encode(m.paddingBytes, 0, m.paddingBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.paddingBytes) : m.paddingBytes;
                }
                if (m.extendedCiphertext != null && $Object.hasOwnProperty.call(m, "extendedCiphertext")) {
                    d.extendedCiphertext = o.bytes === $String ? $util.base64.encode(m.extendedCiphertext, 0, m.extendedCiphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.extendedCiphertext) : m.extendedCiphertext;
                }
                return d;
            };

            ServerHello.prototype.toJSON = function() {
                return ServerHello.toObject(this, $protobuf.util.toJSONOptions);
            };

            ServerHello.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.HandshakeMessage.ServerHello";
            };

            return ServerHello;
        })();

        return HandshakeMessage;
    })();

    proto.HatchMetadataSync = (function() {

        const HatchMetadataSync = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        HatchMetadataSync.prototype.data = null;
        HatchMetadataSync.prototype.timestampMs = null;
        HatchMetadataSync.prototype.requestId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HatchMetadataSync.prototype, "_data", {
            get: $util.oneOfGetter($oneOfFields = ["data"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HatchMetadataSync.prototype, "_timestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HatchMetadataSync.prototype, "_requestId", {
            get: $util.oneOfGetter($oneOfFields = ["requestId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HatchMetadataSync.create = function(properties) {
            return new HatchMetadataSync(properties);
        };

        HatchMetadataSync.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.data != null && $Object.hasOwnProperty.call(m, "data"))
                w.uint32(10).bytes(m.data);
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                w.uint32(16).int64(m.timestampMs);
            if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId"))
                w.uint32(26).string(m.requestId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        HatchMetadataSync.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HatchMetadataSync();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.data = r.bytes();
                        m._data = "data";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.timestampMs = r.int64();
                        m._timestampMs = "timestampMs";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.requestId = r.stringVerify();
                        m._requestId = "requestId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        HatchMetadataSync.fromObject = function (d, q) {
            if (d instanceof $root.proto.HatchMetadataSync)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.HatchMetadataSync: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.HatchMetadataSync();
            if (d.data != null) {
                if (typeof d.data === "string")
                    $util.base64.decode(d.data, m.data = $util.newBuffer($util.base64.length(d.data)), 0);
                else if (d.data.length >= 0)
                    m.data = d.data;
            }
            if (d.timestampMs != null) {
                if ($util.Long)
                    m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                else if (typeof d.timestampMs === "string")
                    m.timestampMs = $parseInt(d.timestampMs, 10);
                else if (typeof d.timestampMs === "number")
                    m.timestampMs = d.timestampMs;
                else if (typeof d.timestampMs === "object")
                    m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
            }
            if (d.requestId != null) {
                m.requestId = $String(d.requestId);
            }
            return m;
        };

        HatchMetadataSync.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.data != null && $Object.hasOwnProperty.call(m, "data")) {
                d.data = o.bytes === $String ? $util.base64.encode(m.data, 0, m.data.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.data) : m.data;
            }
            if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.timestampMs === "number")
                    d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                else
                    d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
            }
            if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId")) {
                d.requestId = m.requestId;
            }
            return d;
        };

        HatchMetadataSync.prototype.toJSON = function() {
            return HatchMetadataSync.toObject(this, $protobuf.util.toJSONOptions);
        };

        HatchMetadataSync.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HatchMetadataSync";
        };

        return HatchMetadataSync;
    })();

    proto.HistorySync = (function() {

        const HistorySync = function (p) {
            this.conversations = [];
            this.statusV3Messages = [];
            this.pushnames = [];
            this.recentStickers = [];
            this.pastParticipants = [];
            this.callLogRecords = [];
            this.phoneNumberToLidMappings = [];
            this.accounts = [];
            this.inlineContacts = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        HistorySync.prototype.syncType = 0;
        HistorySync.prototype.conversations = $util.emptyArray;
        HistorySync.prototype.statusV3Messages = $util.emptyArray;
        HistorySync.prototype.chunkOrder = null;
        HistorySync.prototype.progress = null;
        HistorySync.prototype.pushnames = $util.emptyArray;
        HistorySync.prototype.globalSettings = null;
        HistorySync.prototype.threadIdUserSecret = null;
        HistorySync.prototype.threadDsTimeframeOffset = null;
        HistorySync.prototype.recentStickers = $util.emptyArray;
        HistorySync.prototype.pastParticipants = $util.emptyArray;
        HistorySync.prototype.callLogRecords = $util.emptyArray;
        HistorySync.prototype.aiWaitListState = null;
        HistorySync.prototype.phoneNumberToLidMappings = $util.emptyArray;
        HistorySync.prototype.companionMetaNonce = null;
        HistorySync.prototype.shareableChatIdentifierEncryptionKey = null;
        HistorySync.prototype.accounts = $util.emptyArray;
        HistorySync.prototype.nctSalt = null;
        HistorySync.prototype.inlineContacts = $util.emptyArray;
        HistorySync.prototype.inlineContactsProvided = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_chunkOrder", {
            get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_progress", {
            get: $util.oneOfGetter($oneOfFields = ["progress"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_globalSettings", {
            get: $util.oneOfGetter($oneOfFields = ["globalSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_threadIdUserSecret", {
            get: $util.oneOfGetter($oneOfFields = ["threadIdUserSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_threadDsTimeframeOffset", {
            get: $util.oneOfGetter($oneOfFields = ["threadDsTimeframeOffset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_aiWaitListState", {
            get: $util.oneOfGetter($oneOfFields = ["aiWaitListState"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_companionMetaNonce", {
            get: $util.oneOfGetter($oneOfFields = ["companionMetaNonce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_shareableChatIdentifierEncryptionKey", {
            get: $util.oneOfGetter($oneOfFields = ["shareableChatIdentifierEncryptionKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_nctSalt", {
            get: $util.oneOfGetter($oneOfFields = ["nctSalt"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySync.prototype, "_inlineContactsProvided", {
            get: $util.oneOfGetter($oneOfFields = ["inlineContactsProvided"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HistorySync.create = function(properties) {
            return new HistorySync(properties);
        };

        HistorySync.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(8).int32(m.syncType);
            if (m.conversations != null && m.conversations.length) {
                for (var i = 0; i < m.conversations.length; ++i)
                    $root.proto.Conversation.encode(m.conversations[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.statusV3Messages != null && m.statusV3Messages.length) {
                for (var i = 0; i < m.statusV3Messages.length; ++i)
                    $root.proto.WebMessageInfo.encode(m.statusV3Messages[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder"))
                w.uint32(40).uint32(m.chunkOrder);
            if (m.progress != null && $Object.hasOwnProperty.call(m, "progress"))
                w.uint32(48).uint32(m.progress);
            if (m.pushnames != null && m.pushnames.length) {
                for (var i = 0; i < m.pushnames.length; ++i)
                    $root.proto.Pushname.encode(m.pushnames[i], w.uint32(58).fork(), q + 1).ldelim();
            }
            if (m.globalSettings != null && $Object.hasOwnProperty.call(m, "globalSettings"))
                $root.proto.GlobalSettings.encode(m.globalSettings, w.uint32(66).fork(), q + 1).ldelim();
            if (m.threadIdUserSecret != null && $Object.hasOwnProperty.call(m, "threadIdUserSecret"))
                w.uint32(74).bytes(m.threadIdUserSecret);
            if (m.threadDsTimeframeOffset != null && $Object.hasOwnProperty.call(m, "threadDsTimeframeOffset"))
                w.uint32(80).uint32(m.threadDsTimeframeOffset);
            if (m.recentStickers != null && m.recentStickers.length) {
                for (var i = 0; i < m.recentStickers.length; ++i)
                    $root.proto.StickerMetadata.encode(m.recentStickers[i], w.uint32(90).fork(), q + 1).ldelim();
            }
            if (m.pastParticipants != null && m.pastParticipants.length) {
                for (var i = 0; i < m.pastParticipants.length; ++i)
                    $root.proto.PastParticipants.encode(m.pastParticipants[i], w.uint32(98).fork(), q + 1).ldelim();
            }
            if (m.callLogRecords != null && m.callLogRecords.length) {
                for (var i = 0; i < m.callLogRecords.length; ++i)
                    $root.proto.CallLogRecord.encode(m.callLogRecords[i], w.uint32(106).fork(), q + 1).ldelim();
            }
            if (m.aiWaitListState != null && $Object.hasOwnProperty.call(m, "aiWaitListState"))
                w.uint32(112).int32(m.aiWaitListState);
            if (m.phoneNumberToLidMappings != null && m.phoneNumberToLidMappings.length) {
                for (var i = 0; i < m.phoneNumberToLidMappings.length; ++i)
                    $root.proto.PhoneNumberToLIDMapping.encode(m.phoneNumberToLidMappings[i], w.uint32(122).fork(), q + 1).ldelim();
            }
            if (m.companionMetaNonce != null && $Object.hasOwnProperty.call(m, "companionMetaNonce"))
                w.uint32(130).string(m.companionMetaNonce);
            if (m.shareableChatIdentifierEncryptionKey != null && $Object.hasOwnProperty.call(m, "shareableChatIdentifierEncryptionKey"))
                w.uint32(138).bytes(m.shareableChatIdentifierEncryptionKey);
            if (m.accounts != null && m.accounts.length) {
                for (var i = 0; i < m.accounts.length; ++i)
                    $root.proto.Account.encode(m.accounts[i], w.uint32(146).fork(), q + 1).ldelim();
            }
            if (m.nctSalt != null && $Object.hasOwnProperty.call(m, "nctSalt"))
                w.uint32(154).bytes(m.nctSalt);
            if (m.inlineContacts != null && m.inlineContacts.length) {
                for (var i = 0; i < m.inlineContacts.length; ++i)
                    $root.proto.InlineContact.encode(m.inlineContacts[i], w.uint32(162).fork(), q + 1).ldelim();
            }
            if (m.inlineContactsProvided != null && $Object.hasOwnProperty.call(m, "inlineContactsProvided"))
                w.uint32(168).bool(m.inlineContactsProvided);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        HistorySync.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HistorySync(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.syncType = r.int32();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.conversations && m.conversations.length))
                            m.conversations = [];
                        m.conversations.push($root.proto.Conversation.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.statusV3Messages && m.statusV3Messages.length))
                            m.statusV3Messages = [];
                        m.statusV3Messages.push($root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.chunkOrder = r.uint32();
                        m._chunkOrder = "chunkOrder";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.progress = r.uint32();
                        m._progress = "progress";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        if (!(m.pushnames && m.pushnames.length))
                            m.pushnames = [];
                        m.pushnames.push($root.proto.Pushname.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.globalSettings = $root.proto.GlobalSettings.decode(r, r.uint32(), $undefined, q + 1, m.globalSettings);
                        m._globalSettings = "globalSettings";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.threadIdUserSecret = r.bytes();
                        m._threadIdUserSecret = "threadIdUserSecret";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.threadDsTimeframeOffset = r.uint32();
                        m._threadDsTimeframeOffset = "threadDsTimeframeOffset";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        if (!(m.recentStickers && m.recentStickers.length))
                            m.recentStickers = [];
                        m.recentStickers.push($root.proto.StickerMetadata.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        if (!(m.pastParticipants && m.pastParticipants.length))
                            m.pastParticipants = [];
                        m.pastParticipants.push($root.proto.PastParticipants.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        if (!(m.callLogRecords && m.callLogRecords.length))
                            m.callLogRecords = [];
                        m.callLogRecords.push($root.proto.CallLogRecord.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        m.aiWaitListState = r.int32();
                        m._aiWaitListState = "aiWaitListState";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        if (!(m.phoneNumberToLidMappings && m.phoneNumberToLidMappings.length))
                            m.phoneNumberToLidMappings = [];
                        m.phoneNumberToLidMappings.push($root.proto.PhoneNumberToLIDMapping.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        m.companionMetaNonce = r.stringVerify();
                        m._companionMetaNonce = "companionMetaNonce";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.shareableChatIdentifierEncryptionKey = r.bytes();
                        m._shareableChatIdentifierEncryptionKey = "shareableChatIdentifierEncryptionKey";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        if (!(m.accounts && m.accounts.length))
                            m.accounts = [];
                        m.accounts.push($root.proto.Account.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.nctSalt = r.bytes();
                        m._nctSalt = "nctSalt";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        if (!(m.inlineContacts && m.inlineContacts.length))
                            m.inlineContacts = [];
                        m.inlineContacts.push($root.proto.InlineContact.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        m.inlineContactsProvided = r.bool();
                        m._inlineContactsProvided = "inlineContactsProvided";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "syncType"))
                throw $util.ProtocolError("missing required 'syncType'", { instance: m });
            return m;
        };

        HistorySync.fromObject = function (d, q) {
            if (d instanceof $root.proto.HistorySync)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.HistorySync: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.HistorySync();
            switch (d.syncType) {
            case "INITIAL_BOOTSTRAP":
            case 0:
                m.syncType = 0;
                break;
            case "INITIAL_STATUS_V3":
            case 1:
                m.syncType = 1;
                break;
            case "FULL":
            case 2:
                m.syncType = 2;
                break;
            case "RECENT":
            case 3:
                m.syncType = 3;
                break;
            case "PUSH_NAME":
            case 4:
                m.syncType = 4;
                break;
            case "NON_BLOCKING_DATA":
            case 5:
                m.syncType = 5;
                break;
            case "ON_DEMAND":
            case 6:
                m.syncType = 6;
                break;
            default:
                if (typeof d.syncType === "number" && (d.syncType | 0) === d.syncType)
                    m.syncType = d.syncType;
            }
            if (d.conversations) {
                if (!$Array.isArray(d.conversations))
                    throw $TypeError(".proto.HistorySync.conversations: array expected");
                m.conversations = $Array(d.conversations.length);
                for (var i = 0; i < d.conversations.length; ++i) {
                    if (!$util.isObject(d.conversations[i]))
                        throw $TypeError(".proto.HistorySync.conversations: object expected");
                    m.conversations[i] = $root.proto.Conversation.fromObject(d.conversations[i], q + 1);
                }
            }
            if (d.statusV3Messages) {
                if (!$Array.isArray(d.statusV3Messages))
                    throw $TypeError(".proto.HistorySync.statusV3Messages: array expected");
                m.statusV3Messages = $Array(d.statusV3Messages.length);
                for (var i = 0; i < d.statusV3Messages.length; ++i) {
                    if (!$util.isObject(d.statusV3Messages[i]))
                        throw $TypeError(".proto.HistorySync.statusV3Messages: object expected");
                    m.statusV3Messages[i] = $root.proto.WebMessageInfo.fromObject(d.statusV3Messages[i], q + 1);
                }
            }
            if (d.chunkOrder != null) {
                m.chunkOrder = d.chunkOrder >>> 0;
            }
            if (d.progress != null) {
                m.progress = d.progress >>> 0;
            }
            if (d.pushnames) {
                if (!$Array.isArray(d.pushnames))
                    throw $TypeError(".proto.HistorySync.pushnames: array expected");
                m.pushnames = $Array(d.pushnames.length);
                for (var i = 0; i < d.pushnames.length; ++i) {
                    if (!$util.isObject(d.pushnames[i]))
                        throw $TypeError(".proto.HistorySync.pushnames: object expected");
                    m.pushnames[i] = $root.proto.Pushname.fromObject(d.pushnames[i], q + 1);
                }
            }
            if (d.globalSettings != null) {
                if (!$util.isObject(d.globalSettings))
                    throw $TypeError(".proto.HistorySync.globalSettings: object expected");
                m.globalSettings = $root.proto.GlobalSettings.fromObject(d.globalSettings, q + 1);
            }
            if (d.threadIdUserSecret != null) {
                if (typeof d.threadIdUserSecret === "string")
                    $util.base64.decode(d.threadIdUserSecret, m.threadIdUserSecret = $util.newBuffer($util.base64.length(d.threadIdUserSecret)), 0);
                else if (d.threadIdUserSecret.length >= 0)
                    m.threadIdUserSecret = d.threadIdUserSecret;
            }
            if (d.threadDsTimeframeOffset != null) {
                m.threadDsTimeframeOffset = d.threadDsTimeframeOffset >>> 0;
            }
            if (d.recentStickers) {
                if (!$Array.isArray(d.recentStickers))
                    throw $TypeError(".proto.HistorySync.recentStickers: array expected");
                m.recentStickers = $Array(d.recentStickers.length);
                for (var i = 0; i < d.recentStickers.length; ++i) {
                    if (!$util.isObject(d.recentStickers[i]))
                        throw $TypeError(".proto.HistorySync.recentStickers: object expected");
                    m.recentStickers[i] = $root.proto.StickerMetadata.fromObject(d.recentStickers[i], q + 1);
                }
            }
            if (d.pastParticipants) {
                if (!$Array.isArray(d.pastParticipants))
                    throw $TypeError(".proto.HistorySync.pastParticipants: array expected");
                m.pastParticipants = $Array(d.pastParticipants.length);
                for (var i = 0; i < d.pastParticipants.length; ++i) {
                    if (!$util.isObject(d.pastParticipants[i]))
                        throw $TypeError(".proto.HistorySync.pastParticipants: object expected");
                    m.pastParticipants[i] = $root.proto.PastParticipants.fromObject(d.pastParticipants[i], q + 1);
                }
            }
            if (d.callLogRecords) {
                if (!$Array.isArray(d.callLogRecords))
                    throw $TypeError(".proto.HistorySync.callLogRecords: array expected");
                m.callLogRecords = $Array(d.callLogRecords.length);
                for (var i = 0; i < d.callLogRecords.length; ++i) {
                    if (!$util.isObject(d.callLogRecords[i]))
                        throw $TypeError(".proto.HistorySync.callLogRecords: object expected");
                    m.callLogRecords[i] = $root.proto.CallLogRecord.fromObject(d.callLogRecords[i], q + 1);
                }
            }
            switch (d.aiWaitListState) {
            case "IN_WAITLIST":
            case 0:
                m.aiWaitListState = 0;
                break;
            case "AI_AVAILABLE":
            case 1:
                m.aiWaitListState = 1;
                break;
            default:
                if (typeof d.aiWaitListState === "number" && (d.aiWaitListState | 0) === d.aiWaitListState)
                    m.aiWaitListState = d.aiWaitListState;
            }
            if (d.phoneNumberToLidMappings) {
                if (!$Array.isArray(d.phoneNumberToLidMappings))
                    throw $TypeError(".proto.HistorySync.phoneNumberToLidMappings: array expected");
                m.phoneNumberToLidMappings = $Array(d.phoneNumberToLidMappings.length);
                for (var i = 0; i < d.phoneNumberToLidMappings.length; ++i) {
                    if (!$util.isObject(d.phoneNumberToLidMappings[i]))
                        throw $TypeError(".proto.HistorySync.phoneNumberToLidMappings: object expected");
                    m.phoneNumberToLidMappings[i] = $root.proto.PhoneNumberToLIDMapping.fromObject(d.phoneNumberToLidMappings[i], q + 1);
                }
            }
            if (d.companionMetaNonce != null) {
                m.companionMetaNonce = $String(d.companionMetaNonce);
            }
            if (d.shareableChatIdentifierEncryptionKey != null) {
                if (typeof d.shareableChatIdentifierEncryptionKey === "string")
                    $util.base64.decode(d.shareableChatIdentifierEncryptionKey, m.shareableChatIdentifierEncryptionKey = $util.newBuffer($util.base64.length(d.shareableChatIdentifierEncryptionKey)), 0);
                else if (d.shareableChatIdentifierEncryptionKey.length >= 0)
                    m.shareableChatIdentifierEncryptionKey = d.shareableChatIdentifierEncryptionKey;
            }
            if (d.accounts) {
                if (!$Array.isArray(d.accounts))
                    throw $TypeError(".proto.HistorySync.accounts: array expected");
                m.accounts = $Array(d.accounts.length);
                for (var i = 0; i < d.accounts.length; ++i) {
                    if (!$util.isObject(d.accounts[i]))
                        throw $TypeError(".proto.HistorySync.accounts: object expected");
                    m.accounts[i] = $root.proto.Account.fromObject(d.accounts[i], q + 1);
                }
            }
            if (d.nctSalt != null) {
                if (typeof d.nctSalt === "string")
                    $util.base64.decode(d.nctSalt, m.nctSalt = $util.newBuffer($util.base64.length(d.nctSalt)), 0);
                else if (d.nctSalt.length >= 0)
                    m.nctSalt = d.nctSalt;
            }
            if (d.inlineContacts) {
                if (!$Array.isArray(d.inlineContacts))
                    throw $TypeError(".proto.HistorySync.inlineContacts: array expected");
                m.inlineContacts = $Array(d.inlineContacts.length);
                for (var i = 0; i < d.inlineContacts.length; ++i) {
                    if (!$util.isObject(d.inlineContacts[i]))
                        throw $TypeError(".proto.HistorySync.inlineContacts: object expected");
                    m.inlineContacts[i] = $root.proto.InlineContact.fromObject(d.inlineContacts[i], q + 1);
                }
            }
            if (d.inlineContactsProvided != null) {
                m.inlineContactsProvided = $Boolean(d.inlineContactsProvided);
            }
            return m;
        };

        HistorySync.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.conversations = [];
                d.statusV3Messages = [];
                d.pushnames = [];
                d.recentStickers = [];
                d.pastParticipants = [];
                d.callLogRecords = [];
                d.phoneNumberToLidMappings = [];
                d.accounts = [];
                d.inlineContacts = [];
            }
            if (o.defaults) {
                d.syncType = o.enums === $String ? "INITIAL_BOOTSTRAP" : 0;
            }
            if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType")) {
                d.syncType = o.enums === $String ? $root.proto.HistorySync.HistorySyncType[m.syncType] === $undefined ? m.syncType : $root.proto.HistorySync.HistorySyncType[m.syncType] : m.syncType;
            }
            if (m.conversations && m.conversations.length) {
                d.conversations = $Array(m.conversations.length);
                for (var j = 0; j < m.conversations.length; ++j) {
                    d.conversations[j] = $root.proto.Conversation.toObject(m.conversations[j], o, q + 1);
                }
            }
            if (m.statusV3Messages && m.statusV3Messages.length) {
                d.statusV3Messages = $Array(m.statusV3Messages.length);
                for (var j = 0; j < m.statusV3Messages.length; ++j) {
                    d.statusV3Messages[j] = $root.proto.WebMessageInfo.toObject(m.statusV3Messages[j], o, q + 1);
                }
            }
            if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder")) {
                d.chunkOrder = m.chunkOrder;
            }
            if (m.progress != null && $Object.hasOwnProperty.call(m, "progress")) {
                d.progress = m.progress;
            }
            if (m.pushnames && m.pushnames.length) {
                d.pushnames = $Array(m.pushnames.length);
                for (var j = 0; j < m.pushnames.length; ++j) {
                    d.pushnames[j] = $root.proto.Pushname.toObject(m.pushnames[j], o, q + 1);
                }
            }
            if (m.globalSettings != null && $Object.hasOwnProperty.call(m, "globalSettings")) {
                d.globalSettings = $root.proto.GlobalSettings.toObject(m.globalSettings, o, q + 1);
            }
            if (m.threadIdUserSecret != null && $Object.hasOwnProperty.call(m, "threadIdUserSecret")) {
                d.threadIdUserSecret = o.bytes === $String ? $util.base64.encode(m.threadIdUserSecret, 0, m.threadIdUserSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.threadIdUserSecret) : m.threadIdUserSecret;
            }
            if (m.threadDsTimeframeOffset != null && $Object.hasOwnProperty.call(m, "threadDsTimeframeOffset")) {
                d.threadDsTimeframeOffset = m.threadDsTimeframeOffset;
            }
            if (m.recentStickers && m.recentStickers.length) {
                d.recentStickers = $Array(m.recentStickers.length);
                for (var j = 0; j < m.recentStickers.length; ++j) {
                    d.recentStickers[j] = $root.proto.StickerMetadata.toObject(m.recentStickers[j], o, q + 1);
                }
            }
            if (m.pastParticipants && m.pastParticipants.length) {
                d.pastParticipants = $Array(m.pastParticipants.length);
                for (var j = 0; j < m.pastParticipants.length; ++j) {
                    d.pastParticipants[j] = $root.proto.PastParticipants.toObject(m.pastParticipants[j], o, q + 1);
                }
            }
            if (m.callLogRecords && m.callLogRecords.length) {
                d.callLogRecords = $Array(m.callLogRecords.length);
                for (var j = 0; j < m.callLogRecords.length; ++j) {
                    d.callLogRecords[j] = $root.proto.CallLogRecord.toObject(m.callLogRecords[j], o, q + 1);
                }
            }
            if (m.aiWaitListState != null && $Object.hasOwnProperty.call(m, "aiWaitListState")) {
                d.aiWaitListState = o.enums === $String ? $root.proto.HistorySync.BotAIWaitListState[m.aiWaitListState] === $undefined ? m.aiWaitListState : $root.proto.HistorySync.BotAIWaitListState[m.aiWaitListState] : m.aiWaitListState;
            }
            if (m.phoneNumberToLidMappings && m.phoneNumberToLidMappings.length) {
                d.phoneNumberToLidMappings = $Array(m.phoneNumberToLidMappings.length);
                for (var j = 0; j < m.phoneNumberToLidMappings.length; ++j) {
                    d.phoneNumberToLidMappings[j] = $root.proto.PhoneNumberToLIDMapping.toObject(m.phoneNumberToLidMappings[j], o, q + 1);
                }
            }
            if (m.companionMetaNonce != null && $Object.hasOwnProperty.call(m, "companionMetaNonce")) {
                d.companionMetaNonce = m.companionMetaNonce;
            }
            if (m.shareableChatIdentifierEncryptionKey != null && $Object.hasOwnProperty.call(m, "shareableChatIdentifierEncryptionKey")) {
                d.shareableChatIdentifierEncryptionKey = o.bytes === $String ? $util.base64.encode(m.shareableChatIdentifierEncryptionKey, 0, m.shareableChatIdentifierEncryptionKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.shareableChatIdentifierEncryptionKey) : m.shareableChatIdentifierEncryptionKey;
            }
            if (m.accounts && m.accounts.length) {
                d.accounts = $Array(m.accounts.length);
                for (var j = 0; j < m.accounts.length; ++j) {
                    d.accounts[j] = $root.proto.Account.toObject(m.accounts[j], o, q + 1);
                }
            }
            if (m.nctSalt != null && $Object.hasOwnProperty.call(m, "nctSalt")) {
                d.nctSalt = o.bytes === $String ? $util.base64.encode(m.nctSalt, 0, m.nctSalt.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.nctSalt) : m.nctSalt;
            }
            if (m.inlineContacts && m.inlineContacts.length) {
                d.inlineContacts = $Array(m.inlineContacts.length);
                for (var j = 0; j < m.inlineContacts.length; ++j) {
                    d.inlineContacts[j] = $root.proto.InlineContact.toObject(m.inlineContacts[j], o, q + 1);
                }
            }
            if (m.inlineContactsProvided != null && $Object.hasOwnProperty.call(m, "inlineContactsProvided")) {
                d.inlineContactsProvided = m.inlineContactsProvided;
            }
            return d;
        };

        HistorySync.prototype.toJSON = function() {
            return HistorySync.toObject(this, $protobuf.util.toJSONOptions);
        };

        HistorySync.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HistorySync";
        };

        HistorySync.BotAIWaitListState = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "IN_WAITLIST"] = 0;
            values[valuesById[1] = "AI_AVAILABLE"] = 1;
            return values;
        })();

        HistorySync.HistorySyncType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "INITIAL_BOOTSTRAP"] = 0;
            values[valuesById[1] = "INITIAL_STATUS_V3"] = 1;
            values[valuesById[2] = "FULL"] = 2;
            values[valuesById[3] = "RECENT"] = 3;
            values[valuesById[4] = "PUSH_NAME"] = 4;
            values[valuesById[5] = "NON_BLOCKING_DATA"] = 5;
            values[valuesById[6] = "ON_DEMAND"] = 6;
            return values;
        })();

        return HistorySync;
    })();

    proto.HistorySyncMsg = (function() {

        const HistorySyncMsg = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        HistorySyncMsg.prototype.message = null;
        HistorySyncMsg.prototype.msgOrderId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySyncMsg.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HistorySyncMsg.prototype, "_msgOrderId", {
            get: $util.oneOfGetter($oneOfFields = ["msgOrderId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HistorySyncMsg.create = function(properties) {
            return new HistorySyncMsg(properties);
        };

        HistorySyncMsg.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                $root.proto.WebMessageInfo.encode(m.message, w.uint32(10).fork(), q + 1).ldelim();
            if (m.msgOrderId != null && $Object.hasOwnProperty.call(m, "msgOrderId"))
                w.uint32(16).uint64(m.msgOrderId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        HistorySyncMsg.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HistorySyncMsg();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.message = $root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1, m.message);
                        m._message = "message";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.msgOrderId = r.uint64();
                        m._msgOrderId = "msgOrderId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        HistorySyncMsg.fromObject = function (d, q) {
            if (d instanceof $root.proto.HistorySyncMsg)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.HistorySyncMsg: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.HistorySyncMsg();
            if (d.message != null) {
                if (!$util.isObject(d.message))
                    throw $TypeError(".proto.HistorySyncMsg.message: object expected");
                m.message = $root.proto.WebMessageInfo.fromObject(d.message, q + 1);
            }
            if (d.msgOrderId != null) {
                if ($util.Long)
                    m.msgOrderId = $util.Long.fromValue(d.msgOrderId, true);
                else if (typeof d.msgOrderId === "string")
                    m.msgOrderId = $parseInt(d.msgOrderId, 10);
                else if (typeof d.msgOrderId === "number")
                    m.msgOrderId = d.msgOrderId;
                else if (typeof d.msgOrderId === "object")
                    m.msgOrderId = new $util.LongBits(d.msgOrderId.low >>> 0, d.msgOrderId.high >>> 0).toNumber(true);
            }
            return m;
        };

        HistorySyncMsg.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                d.message = $root.proto.WebMessageInfo.toObject(m.message, o, q + 1);
            }
            if (m.msgOrderId != null && $Object.hasOwnProperty.call(m, "msgOrderId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.msgOrderId = typeof m.msgOrderId === "number" ? $BigInt(m.msgOrderId) : $util.Long.fromBits(m.msgOrderId.low >>> 0, m.msgOrderId.high >>> 0, true).toBigInt();
                else if (typeof m.msgOrderId === "number")
                    d.msgOrderId = o.longs === $String ? $String(m.msgOrderId) : m.msgOrderId;
                else
                    d.msgOrderId = o.longs === $String ? $util.Long.prototype.toString.call(m.msgOrderId) : o.longs === $Number ? new $util.LongBits(m.msgOrderId.low >>> 0, m.msgOrderId.high >>> 0).toNumber(true) : m.msgOrderId;
            }
            return d;
        };

        HistorySyncMsg.prototype.toJSON = function() {
            return HistorySyncMsg.toObject(this, $protobuf.util.toJSONOptions);
        };

        HistorySyncMsg.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HistorySyncMsg";
        };

        return HistorySyncMsg;
    })();

    proto.HostedState = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "E2EE"] = 0;
        values[valuesById[1] = "HOSTED"] = 1;
        return values;
    })();

    proto.HydratedTemplateButton = (function() {

        const HydratedTemplateButton = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        HydratedTemplateButton.prototype.index = null;
        HydratedTemplateButton.prototype.quickReplyButton = null;
        HydratedTemplateButton.prototype.urlButton = null;
        HydratedTemplateButton.prototype.callButton = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(HydratedTemplateButton.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        $Object.defineProperty(HydratedTemplateButton.prototype, "hydratedButton", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyButton", "urlButton", "callButton"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        HydratedTemplateButton.create = function(properties) {
            return new HydratedTemplateButton(properties);
        };

        HydratedTemplateButton.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.quickReplyButton != null && $Object.hasOwnProperty.call(m, "quickReplyButton"))
                $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.encode(m.quickReplyButton, w.uint32(10).fork(), q + 1).ldelim();
            if (m.urlButton != null && $Object.hasOwnProperty.call(m, "urlButton"))
                $root.proto.HydratedTemplateButton.HydratedURLButton.encode(m.urlButton, w.uint32(18).fork(), q + 1).ldelim();
            if (m.callButton != null && $Object.hasOwnProperty.call(m, "callButton"))
                $root.proto.HydratedTemplateButton.HydratedCallButton.encode(m.callButton, w.uint32(26).fork(), q + 1).ldelim();
            if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                w.uint32(32).uint32(m.index);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        HydratedTemplateButton.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HydratedTemplateButton();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 4: {
                        if (u !== 0)
                            break;
                        m.index = r.uint32();
                        m._index = "index";
                        continue;
                    }
                case 1: {
                        if (u !== 2)
                            break;
                        m.quickReplyButton = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.decode(r, r.uint32(), $undefined, q + 1, m.quickReplyButton);
                        m.hydratedButton = "quickReplyButton";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.urlButton = $root.proto.HydratedTemplateButton.HydratedURLButton.decode(r, r.uint32(), $undefined, q + 1, m.urlButton);
                        m.hydratedButton = "urlButton";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.callButton = $root.proto.HydratedTemplateButton.HydratedCallButton.decode(r, r.uint32(), $undefined, q + 1, m.callButton);
                        m.hydratedButton = "callButton";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        HydratedTemplateButton.fromObject = function (d, q) {
            if (d instanceof $root.proto.HydratedTemplateButton)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.HydratedTemplateButton: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.HydratedTemplateButton();
            if (d.index != null) {
                m.index = d.index >>> 0;
            }
            if (d.quickReplyButton != null) {
                if (!$util.isObject(d.quickReplyButton))
                    throw $TypeError(".proto.HydratedTemplateButton.quickReplyButton: object expected");
                m.quickReplyButton = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.fromObject(d.quickReplyButton, q + 1);
            }
            if (d.urlButton != null) {
                if (!$util.isObject(d.urlButton))
                    throw $TypeError(".proto.HydratedTemplateButton.urlButton: object expected");
                m.urlButton = $root.proto.HydratedTemplateButton.HydratedURLButton.fromObject(d.urlButton, q + 1);
            }
            if (d.callButton != null) {
                if (!$util.isObject(d.callButton))
                    throw $TypeError(".proto.HydratedTemplateButton.callButton: object expected");
                m.callButton = $root.proto.HydratedTemplateButton.HydratedCallButton.fromObject(d.callButton, q + 1);
            }
            return m;
        };

        HydratedTemplateButton.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.quickReplyButton != null && $Object.hasOwnProperty.call(m, "quickReplyButton")) {
                d.quickReplyButton = $root.proto.HydratedTemplateButton.HydratedQuickReplyButton.toObject(m.quickReplyButton, o, q + 1);
                if (o.oneofs)
                    d.hydratedButton = "quickReplyButton";
            }
            if (m.urlButton != null && $Object.hasOwnProperty.call(m, "urlButton")) {
                d.urlButton = $root.proto.HydratedTemplateButton.HydratedURLButton.toObject(m.urlButton, o, q + 1);
                if (o.oneofs)
                    d.hydratedButton = "urlButton";
            }
            if (m.callButton != null && $Object.hasOwnProperty.call(m, "callButton")) {
                d.callButton = $root.proto.HydratedTemplateButton.HydratedCallButton.toObject(m.callButton, o, q + 1);
                if (o.oneofs)
                    d.hydratedButton = "callButton";
            }
            if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                d.index = m.index;
            }
            return d;
        };

        HydratedTemplateButton.prototype.toJSON = function() {
            return HydratedTemplateButton.toObject(this, $protobuf.util.toJSONOptions);
        };

        HydratedTemplateButton.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.HydratedTemplateButton";
        };

        HydratedTemplateButton.HydratedCallButton = (function() {

            const HydratedCallButton = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HydratedCallButton.prototype.displayText = null;
            HydratedCallButton.prototype.phoneNumber = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedCallButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedCallButton.prototype, "_phoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["phoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedCallButton.create = function(properties) {
                return new HydratedCallButton(properties);
            };

            HydratedCallButton.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                if (m.phoneNumber != null && $Object.hasOwnProperty.call(m, "phoneNumber"))
                    w.uint32(18).string(m.phoneNumber);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HydratedCallButton.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HydratedTemplateButton.HydratedCallButton();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayText = r.stringVerify();
                            m._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.phoneNumber = r.stringVerify();
                            m._phoneNumber = "phoneNumber";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HydratedCallButton.fromObject = function (d, q) {
                if (d instanceof $root.proto.HydratedTemplateButton.HydratedCallButton)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.HydratedTemplateButton.HydratedCallButton: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.HydratedTemplateButton.HydratedCallButton();
                if (d.displayText != null) {
                    m.displayText = $String(d.displayText);
                }
                if (d.phoneNumber != null) {
                    m.phoneNumber = $String(d.phoneNumber);
                }
                return m;
            };

            HydratedCallButton.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                    d.displayText = m.displayText;
                }
                if (m.phoneNumber != null && $Object.hasOwnProperty.call(m, "phoneNumber")) {
                    d.phoneNumber = m.phoneNumber;
                }
                return d;
            };

            HydratedCallButton.prototype.toJSON = function() {
                return HydratedCallButton.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedCallButton.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.HydratedTemplateButton.HydratedCallButton";
            };

            return HydratedCallButton;
        })();

        HydratedTemplateButton.HydratedQuickReplyButton = (function() {

            const HydratedQuickReplyButton = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HydratedQuickReplyButton.prototype.displayText = null;
            HydratedQuickReplyButton.prototype.id = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedQuickReplyButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedQuickReplyButton.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedQuickReplyButton.create = function(properties) {
                return new HydratedQuickReplyButton(properties);
            };

            HydratedQuickReplyButton.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                    w.uint32(18).string(m.id);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HydratedQuickReplyButton.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HydratedTemplateButton.HydratedQuickReplyButton();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayText = r.stringVerify();
                            m._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.id = r.stringVerify();
                            m._id = "id";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HydratedQuickReplyButton.fromObject = function (d, q) {
                if (d instanceof $root.proto.HydratedTemplateButton.HydratedQuickReplyButton)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.HydratedTemplateButton.HydratedQuickReplyButton: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.HydratedTemplateButton.HydratedQuickReplyButton();
                if (d.displayText != null) {
                    m.displayText = $String(d.displayText);
                }
                if (d.id != null) {
                    m.id = $String(d.id);
                }
                return m;
            };

            HydratedQuickReplyButton.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                    d.displayText = m.displayText;
                }
                if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                    d.id = m.id;
                }
                return d;
            };

            HydratedQuickReplyButton.prototype.toJSON = function() {
                return HydratedQuickReplyButton.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedQuickReplyButton.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.HydratedTemplateButton.HydratedQuickReplyButton";
            };

            return HydratedQuickReplyButton;
        })();

        HydratedTemplateButton.HydratedURLButton = (function() {

            const HydratedURLButton = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HydratedURLButton.prototype.displayText = null;
            HydratedURLButton.prototype.url = null;
            HydratedURLButton.prototype.consentedUsersUrl = null;
            HydratedURLButton.prototype.webviewPresentation = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedURLButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedURLButton.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedURLButton.prototype, "_consentedUsersUrl", {
                get: $util.oneOfGetter($oneOfFields = ["consentedUsersUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HydratedURLButton.prototype, "_webviewPresentation", {
                get: $util.oneOfGetter($oneOfFields = ["webviewPresentation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HydratedURLButton.create = function(properties) {
                return new HydratedURLButton(properties);
            };

            HydratedURLButton.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                    w.uint32(10).string(m.displayText);
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(18).string(m.url);
                if (m.consentedUsersUrl != null && $Object.hasOwnProperty.call(m, "consentedUsersUrl"))
                    w.uint32(26).string(m.consentedUsersUrl);
                if (m.webviewPresentation != null && $Object.hasOwnProperty.call(m, "webviewPresentation"))
                    w.uint32(32).int32(m.webviewPresentation);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HydratedURLButton.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.HydratedTemplateButton.HydratedURLButton(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayText = r.stringVerify();
                            m._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.consentedUsersUrl = r.stringVerify();
                            m._consentedUsersUrl = "consentedUsersUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.webviewPresentation = r.int32();
                            m._webviewPresentation = "webviewPresentation";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HydratedURLButton.fromObject = function (d, q) {
                if (d instanceof $root.proto.HydratedTemplateButton.HydratedURLButton)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.HydratedTemplateButton.HydratedURLButton: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.HydratedTemplateButton.HydratedURLButton();
                if (d.displayText != null) {
                    m.displayText = $String(d.displayText);
                }
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.consentedUsersUrl != null) {
                    m.consentedUsersUrl = $String(d.consentedUsersUrl);
                }
                switch (d.webviewPresentation) {
                case "FULL":
                case 1:
                    m.webviewPresentation = 1;
                    break;
                case "TALL":
                case 2:
                    m.webviewPresentation = 2;
                    break;
                case "COMPACT":
                case 3:
                    m.webviewPresentation = 3;
                    break;
                default:
                    if (typeof d.webviewPresentation === "number" && (d.webviewPresentation | 0) === d.webviewPresentation)
                        m.webviewPresentation = d.webviewPresentation;
                }
                return m;
            };

            HydratedURLButton.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                    d.displayText = m.displayText;
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.consentedUsersUrl != null && $Object.hasOwnProperty.call(m, "consentedUsersUrl")) {
                    d.consentedUsersUrl = m.consentedUsersUrl;
                }
                if (m.webviewPresentation != null && $Object.hasOwnProperty.call(m, "webviewPresentation")) {
                    d.webviewPresentation = o.enums === $String ? $root.proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType[m.webviewPresentation] === $undefined ? m.webviewPresentation : $root.proto.HydratedTemplateButton.HydratedURLButton.WebviewPresentationType[m.webviewPresentation] : m.webviewPresentation;
                }
                return d;
            };

            HydratedURLButton.prototype.toJSON = function() {
                return HydratedURLButton.toObject(this, $protobuf.util.toJSONOptions);
            };

            HydratedURLButton.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.HydratedTemplateButton.HydratedURLButton";
            };

            HydratedURLButton.WebviewPresentationType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "FULL"] = 1;
                values[valuesById[2] = "TALL"] = 2;
                values[valuesById[3] = "COMPACT"] = 3;
                return values;
            })();

            return HydratedURLButton;
        })();

        return HydratedTemplateButton;
    })();

    proto.IdentityKeyPairStructure = (function() {

        const IdentityKeyPairStructure = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        IdentityKeyPairStructure.prototype.publicKey = null;
        IdentityKeyPairStructure.prototype.privateKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(IdentityKeyPairStructure.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(IdentityKeyPairStructure.prototype, "_privateKey", {
            get: $util.oneOfGetter($oneOfFields = ["privateKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        IdentityKeyPairStructure.create = function(properties) {
            return new IdentityKeyPairStructure(properties);
        };

        IdentityKeyPairStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(10).bytes(m.publicKey);
            if (m.privateKey != null && $Object.hasOwnProperty.call(m, "privateKey"))
                w.uint32(18).bytes(m.privateKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        IdentityKeyPairStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.IdentityKeyPairStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        m._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.privateKey = r.bytes();
                        m._privateKey = "privateKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        IdentityKeyPairStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.IdentityKeyPairStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.IdentityKeyPairStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.IdentityKeyPairStructure();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.privateKey != null) {
                if (typeof d.privateKey === "string")
                    $util.base64.decode(d.privateKey, m.privateKey = $util.newBuffer($util.base64.length(d.privateKey)), 0);
                else if (d.privateKey.length >= 0)
                    m.privateKey = d.privateKey;
            }
            return m;
        };

        IdentityKeyPairStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.privateKey != null && $Object.hasOwnProperty.call(m, "privateKey")) {
                d.privateKey = o.bytes === $String ? $util.base64.encode(m.privateKey, 0, m.privateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.privateKey) : m.privateKey;
            }
            return d;
        };

        IdentityKeyPairStructure.prototype.toJSON = function() {
            return IdentityKeyPairStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        IdentityKeyPairStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.IdentityKeyPairStructure";
        };

        return IdentityKeyPairStructure;
    })();

    proto.IdentityVerificationState = (function() {

        const IdentityVerificationState = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        IdentityVerificationState.prototype.verified = null;
        IdentityVerificationState.prototype.actionSeq = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(IdentityVerificationState.prototype, "_verified", {
            get: $util.oneOfGetter($oneOfFields = ["verified"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(IdentityVerificationState.prototype, "_actionSeq", {
            get: $util.oneOfGetter($oneOfFields = ["actionSeq"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        IdentityVerificationState.create = function(properties) {
            return new IdentityVerificationState(properties);
        };

        IdentityVerificationState.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.verified != null && $Object.hasOwnProperty.call(m, "verified"))
                w.uint32(8).bool(m.verified);
            if (m.actionSeq != null && $Object.hasOwnProperty.call(m, "actionSeq"))
                w.uint32(16).uint64(m.actionSeq);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        IdentityVerificationState.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.IdentityVerificationState();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.verified = r.bool();
                        m._verified = "verified";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.actionSeq = r.uint64();
                        m._actionSeq = "actionSeq";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        IdentityVerificationState.fromObject = function (d, q) {
            if (d instanceof $root.proto.IdentityVerificationState)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.IdentityVerificationState: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.IdentityVerificationState();
            if (d.verified != null) {
                m.verified = $Boolean(d.verified);
            }
            if (d.actionSeq != null) {
                if ($util.Long)
                    m.actionSeq = $util.Long.fromValue(d.actionSeq, true);
                else if (typeof d.actionSeq === "string")
                    m.actionSeq = $parseInt(d.actionSeq, 10);
                else if (typeof d.actionSeq === "number")
                    m.actionSeq = d.actionSeq;
                else if (typeof d.actionSeq === "object")
                    m.actionSeq = new $util.LongBits(d.actionSeq.low >>> 0, d.actionSeq.high >>> 0).toNumber(true);
            }
            return m;
        };

        IdentityVerificationState.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.verified != null && $Object.hasOwnProperty.call(m, "verified")) {
                d.verified = m.verified;
            }
            if (m.actionSeq != null && $Object.hasOwnProperty.call(m, "actionSeq")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.actionSeq = typeof m.actionSeq === "number" ? $BigInt(m.actionSeq) : $util.Long.fromBits(m.actionSeq.low >>> 0, m.actionSeq.high >>> 0, true).toBigInt();
                else if (typeof m.actionSeq === "number")
                    d.actionSeq = o.longs === $String ? $String(m.actionSeq) : m.actionSeq;
                else
                    d.actionSeq = o.longs === $String ? $util.Long.prototype.toString.call(m.actionSeq) : o.longs === $Number ? new $util.LongBits(m.actionSeq.low >>> 0, m.actionSeq.high >>> 0).toNumber(true) : m.actionSeq;
            }
            return d;
        };

        IdentityVerificationState.prototype.toJSON = function() {
            return IdentityVerificationState.toObject(this, $protobuf.util.toJSONOptions);
        };

        IdentityVerificationState.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.IdentityVerificationState";
        };

        return IdentityVerificationState;
    })();

    proto.InThreadSurveyMetadata = (function() {

        const InThreadSurveyMetadata = function (p) {
            this.questions = [];
            this.privacyStatementParts = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        InThreadSurveyMetadata.prototype.tessaSessionId = null;
        InThreadSurveyMetadata.prototype.simonSessionId = null;
        InThreadSurveyMetadata.prototype.simonSurveyId = null;
        InThreadSurveyMetadata.prototype.tessaRootId = null;
        InThreadSurveyMetadata.prototype.requestId = null;
        InThreadSurveyMetadata.prototype.tessaEvent = null;
        InThreadSurveyMetadata.prototype.invitationHeaderText = null;
        InThreadSurveyMetadata.prototype.invitationBodyText = null;
        InThreadSurveyMetadata.prototype.invitationCtaText = null;
        InThreadSurveyMetadata.prototype.invitationCtaUrl = null;
        InThreadSurveyMetadata.prototype.surveyTitle = null;
        InThreadSurveyMetadata.prototype.questions = $util.emptyArray;
        InThreadSurveyMetadata.prototype.surveyContinueButtonText = null;
        InThreadSurveyMetadata.prototype.surveySubmitButtonText = null;
        InThreadSurveyMetadata.prototype.privacyStatementFull = null;
        InThreadSurveyMetadata.prototype.privacyStatementParts = $util.emptyArray;
        InThreadSurveyMetadata.prototype.feedbackToastText = null;
        InThreadSurveyMetadata.prototype.startQuestionIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaSessionId", {
            get: $util.oneOfGetter($oneOfFields = ["tessaSessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_simonSessionId", {
            get: $util.oneOfGetter($oneOfFields = ["simonSessionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_simonSurveyId", {
            get: $util.oneOfGetter($oneOfFields = ["simonSurveyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaRootId", {
            get: $util.oneOfGetter($oneOfFields = ["tessaRootId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_requestId", {
            get: $util.oneOfGetter($oneOfFields = ["requestId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_tessaEvent", {
            get: $util.oneOfGetter($oneOfFields = ["tessaEvent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationHeaderText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationHeaderText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationBodyText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationBodyText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationCtaText", {
            get: $util.oneOfGetter($oneOfFields = ["invitationCtaText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_invitationCtaUrl", {
            get: $util.oneOfGetter($oneOfFields = ["invitationCtaUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveyTitle", {
            get: $util.oneOfGetter($oneOfFields = ["surveyTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveyContinueButtonText", {
            get: $util.oneOfGetter($oneOfFields = ["surveyContinueButtonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_surveySubmitButtonText", {
            get: $util.oneOfGetter($oneOfFields = ["surveySubmitButtonText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_privacyStatementFull", {
            get: $util.oneOfGetter($oneOfFields = ["privacyStatementFull"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_feedbackToastText", {
            get: $util.oneOfGetter($oneOfFields = ["feedbackToastText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InThreadSurveyMetadata.prototype, "_startQuestionIndex", {
            get: $util.oneOfGetter($oneOfFields = ["startQuestionIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InThreadSurveyMetadata.create = function(properties) {
            return new InThreadSurveyMetadata(properties);
        };

        InThreadSurveyMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.tessaSessionId != null && $Object.hasOwnProperty.call(m, "tessaSessionId"))
                w.uint32(10).string(m.tessaSessionId);
            if (m.simonSessionId != null && $Object.hasOwnProperty.call(m, "simonSessionId"))
                w.uint32(18).string(m.simonSessionId);
            if (m.simonSurveyId != null && $Object.hasOwnProperty.call(m, "simonSurveyId"))
                w.uint32(26).string(m.simonSurveyId);
            if (m.tessaRootId != null && $Object.hasOwnProperty.call(m, "tessaRootId"))
                w.uint32(34).string(m.tessaRootId);
            if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId"))
                w.uint32(42).string(m.requestId);
            if (m.tessaEvent != null && $Object.hasOwnProperty.call(m, "tessaEvent"))
                w.uint32(50).string(m.tessaEvent);
            if (m.invitationHeaderText != null && $Object.hasOwnProperty.call(m, "invitationHeaderText"))
                w.uint32(58).string(m.invitationHeaderText);
            if (m.invitationBodyText != null && $Object.hasOwnProperty.call(m, "invitationBodyText"))
                w.uint32(66).string(m.invitationBodyText);
            if (m.invitationCtaText != null && $Object.hasOwnProperty.call(m, "invitationCtaText"))
                w.uint32(74).string(m.invitationCtaText);
            if (m.invitationCtaUrl != null && $Object.hasOwnProperty.call(m, "invitationCtaUrl"))
                w.uint32(82).string(m.invitationCtaUrl);
            if (m.surveyTitle != null && $Object.hasOwnProperty.call(m, "surveyTitle"))
                w.uint32(90).string(m.surveyTitle);
            if (m.questions != null && m.questions.length) {
                for (var i = 0; i < m.questions.length; ++i)
                    $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.encode(m.questions[i], w.uint32(98).fork(), q + 1).ldelim();
            }
            if (m.surveyContinueButtonText != null && $Object.hasOwnProperty.call(m, "surveyContinueButtonText"))
                w.uint32(106).string(m.surveyContinueButtonText);
            if (m.surveySubmitButtonText != null && $Object.hasOwnProperty.call(m, "surveySubmitButtonText"))
                w.uint32(114).string(m.surveySubmitButtonText);
            if (m.privacyStatementFull != null && $Object.hasOwnProperty.call(m, "privacyStatementFull"))
                w.uint32(122).string(m.privacyStatementFull);
            if (m.privacyStatementParts != null && m.privacyStatementParts.length) {
                for (var i = 0; i < m.privacyStatementParts.length; ++i)
                    $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.encode(m.privacyStatementParts[i], w.uint32(130).fork(), q + 1).ldelim();
            }
            if (m.feedbackToastText != null && $Object.hasOwnProperty.call(m, "feedbackToastText"))
                w.uint32(138).string(m.feedbackToastText);
            if (m.startQuestionIndex != null && $Object.hasOwnProperty.call(m, "startQuestionIndex"))
                w.uint32(144).int32(m.startQuestionIndex);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        InThreadSurveyMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InThreadSurveyMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.tessaSessionId = r.stringVerify();
                        m._tessaSessionId = "tessaSessionId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.simonSessionId = r.stringVerify();
                        m._simonSessionId = "simonSessionId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.simonSurveyId = r.stringVerify();
                        m._simonSurveyId = "simonSurveyId";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.tessaRootId = r.stringVerify();
                        m._tessaRootId = "tessaRootId";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.requestId = r.stringVerify();
                        m._requestId = "requestId";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.tessaEvent = r.stringVerify();
                        m._tessaEvent = "tessaEvent";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.invitationHeaderText = r.stringVerify();
                        m._invitationHeaderText = "invitationHeaderText";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.invitationBodyText = r.stringVerify();
                        m._invitationBodyText = "invitationBodyText";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.invitationCtaText = r.stringVerify();
                        m._invitationCtaText = "invitationCtaText";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.invitationCtaUrl = r.stringVerify();
                        m._invitationCtaUrl = "invitationCtaUrl";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.surveyTitle = r.stringVerify();
                        m._surveyTitle = "surveyTitle";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        if (!(m.questions && m.questions.length))
                            m.questions = [];
                        m.questions.push($root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.surveyContinueButtonText = r.stringVerify();
                        m._surveyContinueButtonText = "surveyContinueButtonText";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.surveySubmitButtonText = r.stringVerify();
                        m._surveySubmitButtonText = "surveySubmitButtonText";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.privacyStatementFull = r.stringVerify();
                        m._privacyStatementFull = "privacyStatementFull";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        if (!(m.privacyStatementParts && m.privacyStatementParts.length))
                            m.privacyStatementParts = [];
                        m.privacyStatementParts.push($root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.feedbackToastText = r.stringVerify();
                        m._feedbackToastText = "feedbackToastText";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        m.startQuestionIndex = r.int32();
                        m._startQuestionIndex = "startQuestionIndex";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        InThreadSurveyMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.InThreadSurveyMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.InThreadSurveyMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.InThreadSurveyMetadata();
            if (d.tessaSessionId != null) {
                m.tessaSessionId = $String(d.tessaSessionId);
            }
            if (d.simonSessionId != null) {
                m.simonSessionId = $String(d.simonSessionId);
            }
            if (d.simonSurveyId != null) {
                m.simonSurveyId = $String(d.simonSurveyId);
            }
            if (d.tessaRootId != null) {
                m.tessaRootId = $String(d.tessaRootId);
            }
            if (d.requestId != null) {
                m.requestId = $String(d.requestId);
            }
            if (d.tessaEvent != null) {
                m.tessaEvent = $String(d.tessaEvent);
            }
            if (d.invitationHeaderText != null) {
                m.invitationHeaderText = $String(d.invitationHeaderText);
            }
            if (d.invitationBodyText != null) {
                m.invitationBodyText = $String(d.invitationBodyText);
            }
            if (d.invitationCtaText != null) {
                m.invitationCtaText = $String(d.invitationCtaText);
            }
            if (d.invitationCtaUrl != null) {
                m.invitationCtaUrl = $String(d.invitationCtaUrl);
            }
            if (d.surveyTitle != null) {
                m.surveyTitle = $String(d.surveyTitle);
            }
            if (d.questions) {
                if (!$Array.isArray(d.questions))
                    throw $TypeError(".proto.InThreadSurveyMetadata.questions: array expected");
                m.questions = $Array(d.questions.length);
                for (var i = 0; i < d.questions.length; ++i) {
                    if (!$util.isObject(d.questions[i]))
                        throw $TypeError(".proto.InThreadSurveyMetadata.questions: object expected");
                    m.questions[i] = $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.fromObject(d.questions[i], q + 1);
                }
            }
            if (d.surveyContinueButtonText != null) {
                m.surveyContinueButtonText = $String(d.surveyContinueButtonText);
            }
            if (d.surveySubmitButtonText != null) {
                m.surveySubmitButtonText = $String(d.surveySubmitButtonText);
            }
            if (d.privacyStatementFull != null) {
                m.privacyStatementFull = $String(d.privacyStatementFull);
            }
            if (d.privacyStatementParts) {
                if (!$Array.isArray(d.privacyStatementParts))
                    throw $TypeError(".proto.InThreadSurveyMetadata.privacyStatementParts: array expected");
                m.privacyStatementParts = $Array(d.privacyStatementParts.length);
                for (var i = 0; i < d.privacyStatementParts.length; ++i) {
                    if (!$util.isObject(d.privacyStatementParts[i]))
                        throw $TypeError(".proto.InThreadSurveyMetadata.privacyStatementParts: object expected");
                    m.privacyStatementParts[i] = $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.fromObject(d.privacyStatementParts[i], q + 1);
                }
            }
            if (d.feedbackToastText != null) {
                m.feedbackToastText = $String(d.feedbackToastText);
            }
            if (d.startQuestionIndex != null) {
                m.startQuestionIndex = d.startQuestionIndex | 0;
            }
            return m;
        };

        InThreadSurveyMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.questions = [];
                d.privacyStatementParts = [];
            }
            if (m.tessaSessionId != null && $Object.hasOwnProperty.call(m, "tessaSessionId")) {
                d.tessaSessionId = m.tessaSessionId;
            }
            if (m.simonSessionId != null && $Object.hasOwnProperty.call(m, "simonSessionId")) {
                d.simonSessionId = m.simonSessionId;
            }
            if (m.simonSurveyId != null && $Object.hasOwnProperty.call(m, "simonSurveyId")) {
                d.simonSurveyId = m.simonSurveyId;
            }
            if (m.tessaRootId != null && $Object.hasOwnProperty.call(m, "tessaRootId")) {
                d.tessaRootId = m.tessaRootId;
            }
            if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId")) {
                d.requestId = m.requestId;
            }
            if (m.tessaEvent != null && $Object.hasOwnProperty.call(m, "tessaEvent")) {
                d.tessaEvent = m.tessaEvent;
            }
            if (m.invitationHeaderText != null && $Object.hasOwnProperty.call(m, "invitationHeaderText")) {
                d.invitationHeaderText = m.invitationHeaderText;
            }
            if (m.invitationBodyText != null && $Object.hasOwnProperty.call(m, "invitationBodyText")) {
                d.invitationBodyText = m.invitationBodyText;
            }
            if (m.invitationCtaText != null && $Object.hasOwnProperty.call(m, "invitationCtaText")) {
                d.invitationCtaText = m.invitationCtaText;
            }
            if (m.invitationCtaUrl != null && $Object.hasOwnProperty.call(m, "invitationCtaUrl")) {
                d.invitationCtaUrl = m.invitationCtaUrl;
            }
            if (m.surveyTitle != null && $Object.hasOwnProperty.call(m, "surveyTitle")) {
                d.surveyTitle = m.surveyTitle;
            }
            if (m.questions && m.questions.length) {
                d.questions = $Array(m.questions.length);
                for (var j = 0; j < m.questions.length; ++j) {
                    d.questions[j] = $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion.toObject(m.questions[j], o, q + 1);
                }
            }
            if (m.surveyContinueButtonText != null && $Object.hasOwnProperty.call(m, "surveyContinueButtonText")) {
                d.surveyContinueButtonText = m.surveyContinueButtonText;
            }
            if (m.surveySubmitButtonText != null && $Object.hasOwnProperty.call(m, "surveySubmitButtonText")) {
                d.surveySubmitButtonText = m.surveySubmitButtonText;
            }
            if (m.privacyStatementFull != null && $Object.hasOwnProperty.call(m, "privacyStatementFull")) {
                d.privacyStatementFull = m.privacyStatementFull;
            }
            if (m.privacyStatementParts && m.privacyStatementParts.length) {
                d.privacyStatementParts = $Array(m.privacyStatementParts.length);
                for (var j = 0; j < m.privacyStatementParts.length; ++j) {
                    d.privacyStatementParts[j] = $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart.toObject(m.privacyStatementParts[j], o, q + 1);
                }
            }
            if (m.feedbackToastText != null && $Object.hasOwnProperty.call(m, "feedbackToastText")) {
                d.feedbackToastText = m.feedbackToastText;
            }
            if (m.startQuestionIndex != null && $Object.hasOwnProperty.call(m, "startQuestionIndex")) {
                d.startQuestionIndex = m.startQuestionIndex;
            }
            return d;
        };

        InThreadSurveyMetadata.prototype.toJSON = function() {
            return InThreadSurveyMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        InThreadSurveyMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.InThreadSurveyMetadata";
        };

        InThreadSurveyMetadata.InThreadSurveyOption = (function() {

            const InThreadSurveyOption = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InThreadSurveyOption.prototype.stringValue = null;
            InThreadSurveyOption.prototype.numericValue = null;
            InThreadSurveyOption.prototype.textTranslated = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyOption.prototype, "_stringValue", {
                get: $util.oneOfGetter($oneOfFields = ["stringValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyOption.prototype, "_numericValue", {
                get: $util.oneOfGetter($oneOfFields = ["numericValue"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyOption.prototype, "_textTranslated", {
                get: $util.oneOfGetter($oneOfFields = ["textTranslated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InThreadSurveyOption.create = function(properties) {
                return new InThreadSurveyOption(properties);
            };

            InThreadSurveyOption.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.stringValue != null && $Object.hasOwnProperty.call(m, "stringValue"))
                    w.uint32(10).string(m.stringValue);
                if (m.numericValue != null && $Object.hasOwnProperty.call(m, "numericValue"))
                    w.uint32(16).uint32(m.numericValue);
                if (m.textTranslated != null && $Object.hasOwnProperty.call(m, "textTranslated"))
                    w.uint32(26).string(m.textTranslated);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InThreadSurveyOption.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InThreadSurveyMetadata.InThreadSurveyOption();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.stringValue = r.stringVerify();
                            m._stringValue = "stringValue";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.numericValue = r.uint32();
                            m._numericValue = "numericValue";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.textTranslated = r.stringVerify();
                            m._textTranslated = "textTranslated";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InThreadSurveyOption.fromObject = function (d, q) {
                if (d instanceof $root.proto.InThreadSurveyMetadata.InThreadSurveyOption)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyOption: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.InThreadSurveyMetadata.InThreadSurveyOption();
                if (d.stringValue != null) {
                    m.stringValue = $String(d.stringValue);
                }
                if (d.numericValue != null) {
                    m.numericValue = d.numericValue >>> 0;
                }
                if (d.textTranslated != null) {
                    m.textTranslated = $String(d.textTranslated);
                }
                return m;
            };

            InThreadSurveyOption.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.stringValue != null && $Object.hasOwnProperty.call(m, "stringValue")) {
                    d.stringValue = m.stringValue;
                }
                if (m.numericValue != null && $Object.hasOwnProperty.call(m, "numericValue")) {
                    d.numericValue = m.numericValue;
                }
                if (m.textTranslated != null && $Object.hasOwnProperty.call(m, "textTranslated")) {
                    d.textTranslated = m.textTranslated;
                }
                return d;
            };

            InThreadSurveyOption.prototype.toJSON = function() {
                return InThreadSurveyOption.toObject(this, $protobuf.util.toJSONOptions);
            };

            InThreadSurveyOption.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.InThreadSurveyMetadata.InThreadSurveyOption";
            };

            return InThreadSurveyOption;
        })();

        InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart = (function() {

            const InThreadSurveyPrivacyStatementPart = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InThreadSurveyPrivacyStatementPart.prototype.text = null;
            InThreadSurveyPrivacyStatementPart.prototype.url = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyPrivacyStatementPart.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyPrivacyStatementPart.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InThreadSurveyPrivacyStatementPart.create = function(properties) {
                return new InThreadSurveyPrivacyStatementPart(properties);
            };

            InThreadSurveyPrivacyStatementPart.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(18).string(m.url);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InThreadSurveyPrivacyStatementPart.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InThreadSurveyPrivacyStatementPart.fromObject = function (d, q) {
                if (d instanceof $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart();
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                return m;
            };

            InThreadSurveyPrivacyStatementPart.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                return d;
            };

            InThreadSurveyPrivacyStatementPart.prototype.toJSON = function() {
                return InThreadSurveyPrivacyStatementPart.toObject(this, $protobuf.util.toJSONOptions);
            };

            InThreadSurveyPrivacyStatementPart.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.InThreadSurveyMetadata.InThreadSurveyPrivacyStatementPart";
            };

            return InThreadSurveyPrivacyStatementPart;
        })();

        InThreadSurveyMetadata.InThreadSurveyQuestion = (function() {

            const InThreadSurveyQuestion = function (p) {
                this.questionOptions = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InThreadSurveyQuestion.prototype.questionText = null;
            InThreadSurveyQuestion.prototype.questionId = null;
            InThreadSurveyQuestion.prototype.questionOptions = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyQuestion.prototype, "_questionText", {
                get: $util.oneOfGetter($oneOfFields = ["questionText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InThreadSurveyQuestion.prototype, "_questionId", {
                get: $util.oneOfGetter($oneOfFields = ["questionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InThreadSurveyQuestion.create = function(properties) {
                return new InThreadSurveyQuestion(properties);
            };

            InThreadSurveyQuestion.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.questionText != null && $Object.hasOwnProperty.call(m, "questionText"))
                    w.uint32(10).string(m.questionText);
                if (m.questionId != null && $Object.hasOwnProperty.call(m, "questionId"))
                    w.uint32(18).string(m.questionId);
                if (m.questionOptions != null && m.questionOptions.length) {
                    for (var i = 0; i < m.questionOptions.length; ++i)
                        $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.encode(m.questionOptions[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InThreadSurveyQuestion.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.questionText = r.stringVerify();
                            m._questionText = "questionText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.questionId = r.stringVerify();
                            m._questionId = "questionId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.questionOptions && m.questionOptions.length))
                                m.questionOptions = [];
                            m.questionOptions.push($root.proto.InThreadSurveyMetadata.InThreadSurveyOption.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InThreadSurveyQuestion.fromObject = function (d, q) {
                if (d instanceof $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyQuestion: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.InThreadSurveyMetadata.InThreadSurveyQuestion();
                if (d.questionText != null) {
                    m.questionText = $String(d.questionText);
                }
                if (d.questionId != null) {
                    m.questionId = $String(d.questionId);
                }
                if (d.questionOptions) {
                    if (!$Array.isArray(d.questionOptions))
                        throw $TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyQuestion.questionOptions: array expected");
                    m.questionOptions = $Array(d.questionOptions.length);
                    for (var i = 0; i < d.questionOptions.length; ++i) {
                        if (!$util.isObject(d.questionOptions[i]))
                            throw $TypeError(".proto.InThreadSurveyMetadata.InThreadSurveyQuestion.questionOptions: object expected");
                        m.questionOptions[i] = $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.fromObject(d.questionOptions[i], q + 1);
                    }
                }
                return m;
            };

            InThreadSurveyQuestion.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.questionOptions = [];
                }
                if (m.questionText != null && $Object.hasOwnProperty.call(m, "questionText")) {
                    d.questionText = m.questionText;
                }
                if (m.questionId != null && $Object.hasOwnProperty.call(m, "questionId")) {
                    d.questionId = m.questionId;
                }
                if (m.questionOptions && m.questionOptions.length) {
                    d.questionOptions = $Array(m.questionOptions.length);
                    for (var j = 0; j < m.questionOptions.length; ++j) {
                        d.questionOptions[j] = $root.proto.InThreadSurveyMetadata.InThreadSurveyOption.toObject(m.questionOptions[j], o, q + 1);
                    }
                }
                return d;
            };

            InThreadSurveyQuestion.prototype.toJSON = function() {
                return InThreadSurveyQuestion.toObject(this, $protobuf.util.toJSONOptions);
            };

            InThreadSurveyQuestion.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.InThreadSurveyMetadata.InThreadSurveyQuestion";
            };

            return InThreadSurveyQuestion;
        })();

        return InThreadSurveyMetadata;
    })();

    proto.InlineContact = (function() {

        const InlineContact = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        InlineContact.prototype.pnJid = null;
        InlineContact.prototype.lidJid = null;
        InlineContact.prototype.fullName = null;
        InlineContact.prototype.firstName = null;
        InlineContact.prototype.username = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InlineContact.prototype, "_pnJid", {
            get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InlineContact.prototype, "_lidJid", {
            get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InlineContact.prototype, "_fullName", {
            get: $util.oneOfGetter($oneOfFields = ["fullName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InlineContact.prototype, "_firstName", {
            get: $util.oneOfGetter($oneOfFields = ["firstName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InlineContact.prototype, "_username", {
            get: $util.oneOfGetter($oneOfFields = ["username"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InlineContact.create = function(properties) {
            return new InlineContact(properties);
        };

        InlineContact.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid"))
                w.uint32(10).string(m.pnJid);
            if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid"))
                w.uint32(18).string(m.lidJid);
            if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName"))
                w.uint32(26).string(m.fullName);
            if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName"))
                w.uint32(34).string(m.firstName);
            if (m.username != null && $Object.hasOwnProperty.call(m, "username"))
                w.uint32(42).string(m.username);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        InlineContact.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InlineContact();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.pnJid = r.stringVerify();
                        m._pnJid = "pnJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.lidJid = r.stringVerify();
                        m._lidJid = "lidJid";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.fullName = r.stringVerify();
                        m._fullName = "fullName";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.firstName = r.stringVerify();
                        m._firstName = "firstName";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.username = r.stringVerify();
                        m._username = "username";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        InlineContact.fromObject = function (d, q) {
            if (d instanceof $root.proto.InlineContact)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.InlineContact: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.InlineContact();
            if (d.pnJid != null) {
                m.pnJid = $String(d.pnJid);
            }
            if (d.lidJid != null) {
                m.lidJid = $String(d.lidJid);
            }
            if (d.fullName != null) {
                m.fullName = $String(d.fullName);
            }
            if (d.firstName != null) {
                m.firstName = $String(d.firstName);
            }
            if (d.username != null) {
                m.username = $String(d.username);
            }
            return m;
        };

        InlineContact.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid")) {
                d.pnJid = m.pnJid;
            }
            if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid")) {
                d.lidJid = m.lidJid;
            }
            if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName")) {
                d.fullName = m.fullName;
            }
            if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName")) {
                d.firstName = m.firstName;
            }
            if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                d.username = m.username;
            }
            return d;
        };

        InlineContact.prototype.toJSON = function() {
            return InlineContact.toObject(this, $protobuf.util.toJSONOptions);
        };

        InlineContact.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.InlineContact";
        };

        return InlineContact;
    })();

    proto.InteractiveAnnotation = (function() {

        const InteractiveAnnotation = function (p) {
            this.polygonVertices = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        InteractiveAnnotation.prototype.polygonVertices = $util.emptyArray;
        InteractiveAnnotation.prototype.shouldSkipConfirmation = null;
        InteractiveAnnotation.prototype.embeddedContent = null;
        InteractiveAnnotation.prototype.statusLinkType = null;
        InteractiveAnnotation.prototype.location = null;
        InteractiveAnnotation.prototype.newsletter = null;
        InteractiveAnnotation.prototype.embeddedAction = null;
        InteractiveAnnotation.prototype.tapAction = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InteractiveAnnotation.prototype, "_shouldSkipConfirmation", {
            get: $util.oneOfGetter($oneOfFields = ["shouldSkipConfirmation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InteractiveAnnotation.prototype, "_embeddedContent", {
            get: $util.oneOfGetter($oneOfFields = ["embeddedContent"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InteractiveAnnotation.prototype, "_statusLinkType", {
            get: $util.oneOfGetter($oneOfFields = ["statusLinkType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        $Object.defineProperty(InteractiveAnnotation.prototype, "action", {
            get: $util.oneOfGetter($oneOfFields = ["location", "newsletter", "embeddedAction", "tapAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InteractiveAnnotation.create = function(properties) {
            return new InteractiveAnnotation(properties);
        };

        InteractiveAnnotation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.polygonVertices != null && m.polygonVertices.length) {
                for (var i = 0; i < m.polygonVertices.length; ++i)
                    $root.proto.Point.encode(m.polygonVertices[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.location != null && $Object.hasOwnProperty.call(m, "location"))
                $root.proto.Location.encode(m.location, w.uint32(18).fork(), q + 1).ldelim();
            if (m.newsletter != null && $Object.hasOwnProperty.call(m, "newsletter"))
                $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.encode(m.newsletter, w.uint32(26).fork(), q + 1).ldelim();
            if (m.shouldSkipConfirmation != null && $Object.hasOwnProperty.call(m, "shouldSkipConfirmation"))
                w.uint32(32).bool(m.shouldSkipConfirmation);
            if (m.embeddedContent != null && $Object.hasOwnProperty.call(m, "embeddedContent"))
                $root.proto.EmbeddedContent.encode(m.embeddedContent, w.uint32(42).fork(), q + 1).ldelim();
            if (m.embeddedAction != null && $Object.hasOwnProperty.call(m, "embeddedAction"))
                w.uint32(48).bool(m.embeddedAction);
            if (m.tapAction != null && $Object.hasOwnProperty.call(m, "tapAction"))
                $root.proto.TapLinkAction.encode(m.tapAction, w.uint32(58).fork(), q + 1).ldelim();
            if (m.statusLinkType != null && $Object.hasOwnProperty.call(m, "statusLinkType"))
                w.uint32(64).int32(m.statusLinkType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        InteractiveAnnotation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InteractiveAnnotation(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.polygonVertices && m.polygonVertices.length))
                            m.polygonVertices = [];
                        m.polygonVertices.push($root.proto.Point.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.shouldSkipConfirmation = r.bool();
                        m._shouldSkipConfirmation = "shouldSkipConfirmation";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.embeddedContent = $root.proto.EmbeddedContent.decode(r, r.uint32(), $undefined, q + 1, m.embeddedContent);
                        m._embeddedContent = "embeddedContent";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.statusLinkType = r.int32();
                        m._statusLinkType = "statusLinkType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.location = $root.proto.Location.decode(r, r.uint32(), $undefined, q + 1, m.location);
                        m.action = "location";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.newsletter = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.decode(r, r.uint32(), $undefined, q + 1, m.newsletter);
                        m.action = "newsletter";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.embeddedAction = r.bool();
                        m.action = "embeddedAction";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.tapAction = $root.proto.TapLinkAction.decode(r, r.uint32(), $undefined, q + 1, m.tapAction);
                        m.action = "tapAction";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        InteractiveAnnotation.fromObject = function (d, q) {
            if (d instanceof $root.proto.InteractiveAnnotation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.InteractiveAnnotation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.InteractiveAnnotation();
            if (d.polygonVertices) {
                if (!$Array.isArray(d.polygonVertices))
                    throw $TypeError(".proto.InteractiveAnnotation.polygonVertices: array expected");
                m.polygonVertices = $Array(d.polygonVertices.length);
                for (var i = 0; i < d.polygonVertices.length; ++i) {
                    if (!$util.isObject(d.polygonVertices[i]))
                        throw $TypeError(".proto.InteractiveAnnotation.polygonVertices: object expected");
                    m.polygonVertices[i] = $root.proto.Point.fromObject(d.polygonVertices[i], q + 1);
                }
            }
            if (d.shouldSkipConfirmation != null) {
                m.shouldSkipConfirmation = $Boolean(d.shouldSkipConfirmation);
            }
            if (d.embeddedContent != null) {
                if (!$util.isObject(d.embeddedContent))
                    throw $TypeError(".proto.InteractiveAnnotation.embeddedContent: object expected");
                m.embeddedContent = $root.proto.EmbeddedContent.fromObject(d.embeddedContent, q + 1);
            }
            switch (d.statusLinkType) {
            case "RASTERIZED_LINK_PREVIEW":
            case 1:
                m.statusLinkType = 1;
                break;
            case "RASTERIZED_LINK_TRUNCATED":
            case 2:
                m.statusLinkType = 2;
                break;
            case "RASTERIZED_LINK_FULL_URL":
            case 3:
                m.statusLinkType = 3;
                break;
            default:
                if (typeof d.statusLinkType === "number" && (d.statusLinkType | 0) === d.statusLinkType)
                    m.statusLinkType = d.statusLinkType;
            }
            if (d.location != null) {
                if (!$util.isObject(d.location))
                    throw $TypeError(".proto.InteractiveAnnotation.location: object expected");
                m.location = $root.proto.Location.fromObject(d.location, q + 1);
            }
            if (d.newsletter != null) {
                if (!$util.isObject(d.newsletter))
                    throw $TypeError(".proto.InteractiveAnnotation.newsletter: object expected");
                m.newsletter = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.fromObject(d.newsletter, q + 1);
            }
            if (d.embeddedAction != null) {
                m.embeddedAction = $Boolean(d.embeddedAction);
            }
            if (d.tapAction != null) {
                if (!$util.isObject(d.tapAction))
                    throw $TypeError(".proto.InteractiveAnnotation.tapAction: object expected");
                m.tapAction = $root.proto.TapLinkAction.fromObject(d.tapAction, q + 1);
            }
            return m;
        };

        InteractiveAnnotation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.polygonVertices = [];
            }
            if (m.polygonVertices && m.polygonVertices.length) {
                d.polygonVertices = $Array(m.polygonVertices.length);
                for (var j = 0; j < m.polygonVertices.length; ++j) {
                    d.polygonVertices[j] = $root.proto.Point.toObject(m.polygonVertices[j], o, q + 1);
                }
            }
            if (m.location != null && $Object.hasOwnProperty.call(m, "location")) {
                d.location = $root.proto.Location.toObject(m.location, o, q + 1);
                if (o.oneofs)
                    d.action = "location";
            }
            if (m.newsletter != null && $Object.hasOwnProperty.call(m, "newsletter")) {
                d.newsletter = $root.proto.ContextInfo.ForwardedNewsletterMessageInfo.toObject(m.newsletter, o, q + 1);
                if (o.oneofs)
                    d.action = "newsletter";
            }
            if (m.shouldSkipConfirmation != null && $Object.hasOwnProperty.call(m, "shouldSkipConfirmation")) {
                d.shouldSkipConfirmation = m.shouldSkipConfirmation;
            }
            if (m.embeddedContent != null && $Object.hasOwnProperty.call(m, "embeddedContent")) {
                d.embeddedContent = $root.proto.EmbeddedContent.toObject(m.embeddedContent, o, q + 1);
            }
            if (m.embeddedAction != null && $Object.hasOwnProperty.call(m, "embeddedAction")) {
                d.embeddedAction = m.embeddedAction;
                if (o.oneofs)
                    d.action = "embeddedAction";
            }
            if (m.tapAction != null && $Object.hasOwnProperty.call(m, "tapAction")) {
                d.tapAction = $root.proto.TapLinkAction.toObject(m.tapAction, o, q + 1);
                if (o.oneofs)
                    d.action = "tapAction";
            }
            if (m.statusLinkType != null && $Object.hasOwnProperty.call(m, "statusLinkType")) {
                d.statusLinkType = o.enums === $String ? $root.proto.InteractiveAnnotation.StatusLinkType[m.statusLinkType] === $undefined ? m.statusLinkType : $root.proto.InteractiveAnnotation.StatusLinkType[m.statusLinkType] : m.statusLinkType;
            }
            return d;
        };

        InteractiveAnnotation.prototype.toJSON = function() {
            return InteractiveAnnotation.toObject(this, $protobuf.util.toJSONOptions);
        };

        InteractiveAnnotation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.InteractiveAnnotation";
        };

        InteractiveAnnotation.StatusLinkType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "RASTERIZED_LINK_PREVIEW"] = 1;
            values[valuesById[2] = "RASTERIZED_LINK_TRUNCATED"] = 2;
            values[valuesById[3] = "RASTERIZED_LINK_FULL_URL"] = 3;
            return values;
        })();

        return InteractiveAnnotation;
    })();

    proto.InteractiveMessageAdditionalMetadata = (function() {

        const InteractiveMessageAdditionalMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        InteractiveMessageAdditionalMetadata.prototype.isGalaxyFlowCompleted = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(InteractiveMessageAdditionalMetadata.prototype, "_isGalaxyFlowCompleted", {
            get: $util.oneOfGetter($oneOfFields = ["isGalaxyFlowCompleted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        InteractiveMessageAdditionalMetadata.create = function(properties) {
            return new InteractiveMessageAdditionalMetadata(properties);
        };

        InteractiveMessageAdditionalMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.isGalaxyFlowCompleted != null && $Object.hasOwnProperty.call(m, "isGalaxyFlowCompleted"))
                w.uint32(8).bool(m.isGalaxyFlowCompleted);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        InteractiveMessageAdditionalMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.InteractiveMessageAdditionalMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.isGalaxyFlowCompleted = r.bool();
                        m._isGalaxyFlowCompleted = "isGalaxyFlowCompleted";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        InteractiveMessageAdditionalMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.InteractiveMessageAdditionalMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.InteractiveMessageAdditionalMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.InteractiveMessageAdditionalMetadata();
            if (d.isGalaxyFlowCompleted != null) {
                m.isGalaxyFlowCompleted = $Boolean(d.isGalaxyFlowCompleted);
            }
            return m;
        };

        InteractiveMessageAdditionalMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.isGalaxyFlowCompleted != null && $Object.hasOwnProperty.call(m, "isGalaxyFlowCompleted")) {
                d.isGalaxyFlowCompleted = m.isGalaxyFlowCompleted;
            }
            return d;
        };

        InteractiveMessageAdditionalMetadata.prototype.toJSON = function() {
            return InteractiveMessageAdditionalMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        InteractiveMessageAdditionalMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.InteractiveMessageAdditionalMetadata";
        };

        return InteractiveMessageAdditionalMetadata;
    })();

    proto.KeepInChat = (function() {

        const KeepInChat = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        KeepInChat.prototype.keepType = null;
        KeepInChat.prototype.serverTimestamp = null;
        KeepInChat.prototype.key = null;
        KeepInChat.prototype.deviceJid = null;
        KeepInChat.prototype.clientTimestampMs = null;
        KeepInChat.prototype.serverTimestampMs = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeepInChat.prototype, "_keepType", {
            get: $util.oneOfGetter($oneOfFields = ["keepType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeepInChat.prototype, "_serverTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeepInChat.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeepInChat.prototype, "_deviceJid", {
            get: $util.oneOfGetter($oneOfFields = ["deviceJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeepInChat.prototype, "_clientTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["clientTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeepInChat.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        KeepInChat.create = function(properties) {
            return new KeepInChat(properties);
        };

        KeepInChat.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.keepType != null && $Object.hasOwnProperty.call(m, "keepType"))
                w.uint32(8).int32(m.keepType);
            if (m.serverTimestamp != null && $Object.hasOwnProperty.call(m, "serverTimestamp"))
                w.uint32(16).int64(m.serverTimestamp);
            if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                $root.proto.MessageKey.encode(m.key, w.uint32(26).fork(), q + 1).ldelim();
            if (m.deviceJid != null && $Object.hasOwnProperty.call(m, "deviceJid"))
                w.uint32(34).string(m.deviceJid);
            if (m.clientTimestampMs != null && $Object.hasOwnProperty.call(m, "clientTimestampMs"))
                w.uint32(40).int64(m.clientTimestampMs);
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs"))
                w.uint32(48).int64(m.serverTimestampMs);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        KeepInChat.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.KeepInChat(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.keepType = r.int32();
                        m._keepType = "keepType";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.serverTimestamp = r.int64();
                        m._serverTimestamp = "serverTimestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                        m._key = "key";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.deviceJid = r.stringVerify();
                        m._deviceJid = "deviceJid";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.clientTimestampMs = r.int64();
                        m._clientTimestampMs = "clientTimestampMs";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.serverTimestampMs = r.int64();
                        m._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        KeepInChat.fromObject = function (d, q) {
            if (d instanceof $root.proto.KeepInChat)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.KeepInChat: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.KeepInChat();
            switch (d.keepType) {
            case "UNKNOWN":
            case 0:
                m.keepType = 0;
                break;
            case "KEEP_FOR_ALL":
            case 1:
                m.keepType = 1;
                break;
            case "UNDO_KEEP_FOR_ALL":
            case 2:
                m.keepType = 2;
                break;
            default:
                if (typeof d.keepType === "number" && (d.keepType | 0) === d.keepType)
                    m.keepType = d.keepType;
            }
            if (d.serverTimestamp != null) {
                if ($util.Long)
                    m.serverTimestamp = $util.Long.fromValue(d.serverTimestamp, false);
                else if (typeof d.serverTimestamp === "string")
                    m.serverTimestamp = $parseInt(d.serverTimestamp, 10);
                else if (typeof d.serverTimestamp === "number")
                    m.serverTimestamp = d.serverTimestamp;
                else if (typeof d.serverTimestamp === "object")
                    m.serverTimestamp = new $util.LongBits(d.serverTimestamp.low >>> 0, d.serverTimestamp.high >>> 0).toNumber();
            }
            if (d.key != null) {
                if (!$util.isObject(d.key))
                    throw $TypeError(".proto.KeepInChat.key: object expected");
                m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
            }
            if (d.deviceJid != null) {
                m.deviceJid = $String(d.deviceJid);
            }
            if (d.clientTimestampMs != null) {
                if ($util.Long)
                    m.clientTimestampMs = $util.Long.fromValue(d.clientTimestampMs, false);
                else if (typeof d.clientTimestampMs === "string")
                    m.clientTimestampMs = $parseInt(d.clientTimestampMs, 10);
                else if (typeof d.clientTimestampMs === "number")
                    m.clientTimestampMs = d.clientTimestampMs;
                else if (typeof d.clientTimestampMs === "object")
                    m.clientTimestampMs = new $util.LongBits(d.clientTimestampMs.low >>> 0, d.clientTimestampMs.high >>> 0).toNumber();
            }
            if (d.serverTimestampMs != null) {
                if ($util.Long)
                    m.serverTimestampMs = $util.Long.fromValue(d.serverTimestampMs, false);
                else if (typeof d.serverTimestampMs === "string")
                    m.serverTimestampMs = $parseInt(d.serverTimestampMs, 10);
                else if (typeof d.serverTimestampMs === "number")
                    m.serverTimestampMs = d.serverTimestampMs;
                else if (typeof d.serverTimestampMs === "object")
                    m.serverTimestampMs = new $util.LongBits(d.serverTimestampMs.low >>> 0, d.serverTimestampMs.high >>> 0).toNumber();
            }
            return m;
        };

        KeepInChat.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.keepType != null && $Object.hasOwnProperty.call(m, "keepType")) {
                d.keepType = o.enums === $String ? $root.proto.KeepType[m.keepType] === $undefined ? m.keepType : $root.proto.KeepType[m.keepType] : m.keepType;
            }
            if (m.serverTimestamp != null && $Object.hasOwnProperty.call(m, "serverTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.serverTimestamp = typeof m.serverTimestamp === "number" ? $BigInt(m.serverTimestamp) : $util.Long.fromBits(m.serverTimestamp.low >>> 0, m.serverTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.serverTimestamp === "number")
                    d.serverTimestamp = o.longs === $String ? $String(m.serverTimestamp) : m.serverTimestamp;
                else
                    d.serverTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.serverTimestamp) : o.longs === $Number ? new $util.LongBits(m.serverTimestamp.low >>> 0, m.serverTimestamp.high >>> 0).toNumber() : m.serverTimestamp;
            }
            if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
            }
            if (m.deviceJid != null && $Object.hasOwnProperty.call(m, "deviceJid")) {
                d.deviceJid = m.deviceJid;
            }
            if (m.clientTimestampMs != null && $Object.hasOwnProperty.call(m, "clientTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.clientTimestampMs = typeof m.clientTimestampMs === "number" ? $BigInt(m.clientTimestampMs) : $util.Long.fromBits(m.clientTimestampMs.low >>> 0, m.clientTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.clientTimestampMs === "number")
                    d.clientTimestampMs = o.longs === $String ? $String(m.clientTimestampMs) : m.clientTimestampMs;
                else
                    d.clientTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.clientTimestampMs) : o.longs === $Number ? new $util.LongBits(m.clientTimestampMs.low >>> 0, m.clientTimestampMs.high >>> 0).toNumber() : m.clientTimestampMs;
            }
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.serverTimestampMs = typeof m.serverTimestampMs === "number" ? $BigInt(m.serverTimestampMs) : $util.Long.fromBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.serverTimestampMs === "number")
                    d.serverTimestampMs = o.longs === $String ? $String(m.serverTimestampMs) : m.serverTimestampMs;
                else
                    d.serverTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.serverTimestampMs) : o.longs === $Number ? new $util.LongBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0).toNumber() : m.serverTimestampMs;
            }
            return d;
        };

        KeepInChat.prototype.toJSON = function() {
            return KeepInChat.toObject(this, $protobuf.util.toJSONOptions);
        };

        KeepInChat.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.KeepInChat";
        };

        return KeepInChat;
    })();

    proto.KeepType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN"] = 0;
        values[valuesById[1] = "KEEP_FOR_ALL"] = 1;
        values[valuesById[2] = "UNDO_KEEP_FOR_ALL"] = 2;
        return values;
    })();

    proto.KeyExchangeMessage = (function() {

        const KeyExchangeMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        KeyExchangeMessage.prototype.id = null;
        KeyExchangeMessage.prototype.baseKey = null;
        KeyExchangeMessage.prototype.ratchetKey = null;
        KeyExchangeMessage.prototype.identityKey = null;
        KeyExchangeMessage.prototype.baseKeySignature = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeyExchangeMessage.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeyExchangeMessage.prototype, "_baseKey", {
            get: $util.oneOfGetter($oneOfFields = ["baseKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeyExchangeMessage.prototype, "_ratchetKey", {
            get: $util.oneOfGetter($oneOfFields = ["ratchetKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeyExchangeMessage.prototype, "_identityKey", {
            get: $util.oneOfGetter($oneOfFields = ["identityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeyExchangeMessage.prototype, "_baseKeySignature", {
            get: $util.oneOfGetter($oneOfFields = ["baseKeySignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        KeyExchangeMessage.create = function(properties) {
            return new KeyExchangeMessage(properties);
        };

        KeyExchangeMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(8).uint32(m.id);
            if (m.baseKey != null && $Object.hasOwnProperty.call(m, "baseKey"))
                w.uint32(18).bytes(m.baseKey);
            if (m.ratchetKey != null && $Object.hasOwnProperty.call(m, "ratchetKey"))
                w.uint32(26).bytes(m.ratchetKey);
            if (m.identityKey != null && $Object.hasOwnProperty.call(m, "identityKey"))
                w.uint32(34).bytes(m.identityKey);
            if (m.baseKeySignature != null && $Object.hasOwnProperty.call(m, "baseKeySignature"))
                w.uint32(42).bytes(m.baseKeySignature);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        KeyExchangeMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.KeyExchangeMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.id = r.uint32();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.baseKey = r.bytes();
                        m._baseKey = "baseKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.ratchetKey = r.bytes();
                        m._ratchetKey = "ratchetKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.identityKey = r.bytes();
                        m._identityKey = "identityKey";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.baseKeySignature = r.bytes();
                        m._baseKeySignature = "baseKeySignature";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        KeyExchangeMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.KeyExchangeMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.KeyExchangeMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.KeyExchangeMessage();
            if (d.id != null) {
                m.id = d.id >>> 0;
            }
            if (d.baseKey != null) {
                if (typeof d.baseKey === "string")
                    $util.base64.decode(d.baseKey, m.baseKey = $util.newBuffer($util.base64.length(d.baseKey)), 0);
                else if (d.baseKey.length >= 0)
                    m.baseKey = d.baseKey;
            }
            if (d.ratchetKey != null) {
                if (typeof d.ratchetKey === "string")
                    $util.base64.decode(d.ratchetKey, m.ratchetKey = $util.newBuffer($util.base64.length(d.ratchetKey)), 0);
                else if (d.ratchetKey.length >= 0)
                    m.ratchetKey = d.ratchetKey;
            }
            if (d.identityKey != null) {
                if (typeof d.identityKey === "string")
                    $util.base64.decode(d.identityKey, m.identityKey = $util.newBuffer($util.base64.length(d.identityKey)), 0);
                else if (d.identityKey.length >= 0)
                    m.identityKey = d.identityKey;
            }
            if (d.baseKeySignature != null) {
                if (typeof d.baseKeySignature === "string")
                    $util.base64.decode(d.baseKeySignature, m.baseKeySignature = $util.newBuffer($util.base64.length(d.baseKeySignature)), 0);
                else if (d.baseKeySignature.length >= 0)
                    m.baseKeySignature = d.baseKeySignature;
            }
            return m;
        };

        KeyExchangeMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.baseKey != null && $Object.hasOwnProperty.call(m, "baseKey")) {
                d.baseKey = o.bytes === $String ? $util.base64.encode(m.baseKey, 0, m.baseKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.baseKey) : m.baseKey;
            }
            if (m.ratchetKey != null && $Object.hasOwnProperty.call(m, "ratchetKey")) {
                d.ratchetKey = o.bytes === $String ? $util.base64.encode(m.ratchetKey, 0, m.ratchetKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ratchetKey) : m.ratchetKey;
            }
            if (m.identityKey != null && $Object.hasOwnProperty.call(m, "identityKey")) {
                d.identityKey = o.bytes === $String ? $util.base64.encode(m.identityKey, 0, m.identityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.identityKey) : m.identityKey;
            }
            if (m.baseKeySignature != null && $Object.hasOwnProperty.call(m, "baseKeySignature")) {
                d.baseKeySignature = o.bytes === $String ? $util.base64.encode(m.baseKeySignature, 0, m.baseKeySignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.baseKeySignature) : m.baseKeySignature;
            }
            return d;
        };

        KeyExchangeMessage.prototype.toJSON = function() {
            return KeyExchangeMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        KeyExchangeMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.KeyExchangeMessage";
        };

        return KeyExchangeMessage;
    })();

    proto.KeyId = (function() {

        const KeyId = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        KeyId.prototype.id = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(KeyId.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        KeyId.create = function(properties) {
            return new KeyId(properties);
        };

        KeyId.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(10).bytes(m.id);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        KeyId.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.KeyId();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.id = r.bytes();
                        m._id = "id";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        KeyId.fromObject = function (d, q) {
            if (d instanceof $root.proto.KeyId)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.KeyId: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.KeyId();
            if (d.id != null) {
                if (typeof d.id === "string")
                    $util.base64.decode(d.id, m.id = $util.newBuffer($util.base64.length(d.id)), 0);
                else if (d.id.length >= 0)
                    m.id = d.id;
            }
            return m;
        };

        KeyId.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = o.bytes === $String ? $util.base64.encode(m.id, 0, m.id.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.id) : m.id;
            }
            return d;
        };

        KeyId.prototype.toJSON = function() {
            return KeyId.toObject(this, $protobuf.util.toJSONOptions);
        };

        KeyId.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.KeyId";
        };

        return KeyId;
    })();

    proto.LIDMigrationMappingSyncMessage = (function() {

        const LIDMigrationMappingSyncMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        LIDMigrationMappingSyncMessage.prototype.encodedMappingPayload = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LIDMigrationMappingSyncMessage.prototype, "_encodedMappingPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encodedMappingPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LIDMigrationMappingSyncMessage.create = function(properties) {
            return new LIDMigrationMappingSyncMessage(properties);
        };

        LIDMigrationMappingSyncMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.encodedMappingPayload != null && $Object.hasOwnProperty.call(m, "encodedMappingPayload"))
                w.uint32(10).bytes(m.encodedMappingPayload);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        LIDMigrationMappingSyncMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.LIDMigrationMappingSyncMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.encodedMappingPayload = r.bytes();
                        m._encodedMappingPayload = "encodedMappingPayload";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        LIDMigrationMappingSyncMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.LIDMigrationMappingSyncMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.LIDMigrationMappingSyncMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.LIDMigrationMappingSyncMessage();
            if (d.encodedMappingPayload != null) {
                if (typeof d.encodedMappingPayload === "string")
                    $util.base64.decode(d.encodedMappingPayload, m.encodedMappingPayload = $util.newBuffer($util.base64.length(d.encodedMappingPayload)), 0);
                else if (d.encodedMappingPayload.length >= 0)
                    m.encodedMappingPayload = d.encodedMappingPayload;
            }
            return m;
        };

        LIDMigrationMappingSyncMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.encodedMappingPayload != null && $Object.hasOwnProperty.call(m, "encodedMappingPayload")) {
                d.encodedMappingPayload = o.bytes === $String ? $util.base64.encode(m.encodedMappingPayload, 0, m.encodedMappingPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encodedMappingPayload) : m.encodedMappingPayload;
            }
            return d;
        };

        LIDMigrationMappingSyncMessage.prototype.toJSON = function() {
            return LIDMigrationMappingSyncMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        LIDMigrationMappingSyncMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.LIDMigrationMappingSyncMessage";
        };

        return LIDMigrationMappingSyncMessage;
    })();

    proto.LabyrinthWaCommand = (function() {

        const LabyrinthWaCommand = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        LabyrinthWaCommand.prototype.createBackupInput = null;
        LabyrinthWaCommand.prototype.encryptMessageInput = null;
        LabyrinthWaCommand.prototype.decryptMessageInput = null;
        LabyrinthWaCommand.prototype.orfThreadIdInput = null;
        LabyrinthWaCommand.prototype.deriveMessageKeyInput = null;
        LabyrinthWaCommand.prototype.rotateEpochInput = null;

        let $oneOfFields;

        $Object.defineProperty(LabyrinthWaCommand.prototype, "commandInput", {
            get: $util.oneOfGetter($oneOfFields = ["createBackupInput", "encryptMessageInput", "decryptMessageInput", "orfThreadIdInput", "deriveMessageKeyInput", "rotateEpochInput"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LabyrinthWaCommand.create = function(properties) {
            return new LabyrinthWaCommand(properties);
        };

        LabyrinthWaCommand.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.createBackupInput != null && $Object.hasOwnProperty.call(m, "createBackupInput"))
                $root.proto.CreateBackupInput.encode(m.createBackupInput, w.uint32(10).fork(), q + 1).ldelim();
            if (m.encryptMessageInput != null && $Object.hasOwnProperty.call(m, "encryptMessageInput"))
                $root.proto.EncryptMessageInput.encode(m.encryptMessageInput, w.uint32(18).fork(), q + 1).ldelim();
            if (m.decryptMessageInput != null && $Object.hasOwnProperty.call(m, "decryptMessageInput"))
                $root.proto.DecryptMessageInput.encode(m.decryptMessageInput, w.uint32(26).fork(), q + 1).ldelim();
            if (m.orfThreadIdInput != null && $Object.hasOwnProperty.call(m, "orfThreadIdInput"))
                $root.proto.OrfThreadIdInput.encode(m.orfThreadIdInput, w.uint32(34).fork(), q + 1).ldelim();
            if (m.deriveMessageKeyInput != null && $Object.hasOwnProperty.call(m, "deriveMessageKeyInput"))
                $root.proto.DeriveMessageKeyInput.encode(m.deriveMessageKeyInput, w.uint32(42).fork(), q + 1).ldelim();
            if (m.rotateEpochInput != null && $Object.hasOwnProperty.call(m, "rotateEpochInput"))
                $root.proto.RotateEpochInput.encode(m.rotateEpochInput, w.uint32(50).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        LabyrinthWaCommand.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.LabyrinthWaCommand();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.createBackupInput = $root.proto.CreateBackupInput.decode(r, r.uint32(), $undefined, q + 1, m.createBackupInput);
                        m.commandInput = "createBackupInput";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.encryptMessageInput = $root.proto.EncryptMessageInput.decode(r, r.uint32(), $undefined, q + 1, m.encryptMessageInput);
                        m.commandInput = "encryptMessageInput";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.decryptMessageInput = $root.proto.DecryptMessageInput.decode(r, r.uint32(), $undefined, q + 1, m.decryptMessageInput);
                        m.commandInput = "decryptMessageInput";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.orfThreadIdInput = $root.proto.OrfThreadIdInput.decode(r, r.uint32(), $undefined, q + 1, m.orfThreadIdInput);
                        m.commandInput = "orfThreadIdInput";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.deriveMessageKeyInput = $root.proto.DeriveMessageKeyInput.decode(r, r.uint32(), $undefined, q + 1, m.deriveMessageKeyInput);
                        m.commandInput = "deriveMessageKeyInput";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.rotateEpochInput = $root.proto.RotateEpochInput.decode(r, r.uint32(), $undefined, q + 1, m.rotateEpochInput);
                        m.commandInput = "rotateEpochInput";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        LabyrinthWaCommand.fromObject = function (d, q) {
            if (d instanceof $root.proto.LabyrinthWaCommand)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.LabyrinthWaCommand: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.LabyrinthWaCommand();
            if (d.createBackupInput != null) {
                if (!$util.isObject(d.createBackupInput))
                    throw $TypeError(".proto.LabyrinthWaCommand.createBackupInput: object expected");
                m.createBackupInput = $root.proto.CreateBackupInput.fromObject(d.createBackupInput, q + 1);
            }
            if (d.encryptMessageInput != null) {
                if (!$util.isObject(d.encryptMessageInput))
                    throw $TypeError(".proto.LabyrinthWaCommand.encryptMessageInput: object expected");
                m.encryptMessageInput = $root.proto.EncryptMessageInput.fromObject(d.encryptMessageInput, q + 1);
            }
            if (d.decryptMessageInput != null) {
                if (!$util.isObject(d.decryptMessageInput))
                    throw $TypeError(".proto.LabyrinthWaCommand.decryptMessageInput: object expected");
                m.decryptMessageInput = $root.proto.DecryptMessageInput.fromObject(d.decryptMessageInput, q + 1);
            }
            if (d.orfThreadIdInput != null) {
                if (!$util.isObject(d.orfThreadIdInput))
                    throw $TypeError(".proto.LabyrinthWaCommand.orfThreadIdInput: object expected");
                m.orfThreadIdInput = $root.proto.OrfThreadIdInput.fromObject(d.orfThreadIdInput, q + 1);
            }
            if (d.deriveMessageKeyInput != null) {
                if (!$util.isObject(d.deriveMessageKeyInput))
                    throw $TypeError(".proto.LabyrinthWaCommand.deriveMessageKeyInput: object expected");
                m.deriveMessageKeyInput = $root.proto.DeriveMessageKeyInput.fromObject(d.deriveMessageKeyInput, q + 1);
            }
            if (d.rotateEpochInput != null) {
                if (!$util.isObject(d.rotateEpochInput))
                    throw $TypeError(".proto.LabyrinthWaCommand.rotateEpochInput: object expected");
                m.rotateEpochInput = $root.proto.RotateEpochInput.fromObject(d.rotateEpochInput, q + 1);
            }
            return m;
        };

        LabyrinthWaCommand.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.createBackupInput != null && $Object.hasOwnProperty.call(m, "createBackupInput")) {
                d.createBackupInput = $root.proto.CreateBackupInput.toObject(m.createBackupInput, o, q + 1);
                if (o.oneofs)
                    d.commandInput = "createBackupInput";
            }
            if (m.encryptMessageInput != null && $Object.hasOwnProperty.call(m, "encryptMessageInput")) {
                d.encryptMessageInput = $root.proto.EncryptMessageInput.toObject(m.encryptMessageInput, o, q + 1);
                if (o.oneofs)
                    d.commandInput = "encryptMessageInput";
            }
            if (m.decryptMessageInput != null && $Object.hasOwnProperty.call(m, "decryptMessageInput")) {
                d.decryptMessageInput = $root.proto.DecryptMessageInput.toObject(m.decryptMessageInput, o, q + 1);
                if (o.oneofs)
                    d.commandInput = "decryptMessageInput";
            }
            if (m.orfThreadIdInput != null && $Object.hasOwnProperty.call(m, "orfThreadIdInput")) {
                d.orfThreadIdInput = $root.proto.OrfThreadIdInput.toObject(m.orfThreadIdInput, o, q + 1);
                if (o.oneofs)
                    d.commandInput = "orfThreadIdInput";
            }
            if (m.deriveMessageKeyInput != null && $Object.hasOwnProperty.call(m, "deriveMessageKeyInput")) {
                d.deriveMessageKeyInput = $root.proto.DeriveMessageKeyInput.toObject(m.deriveMessageKeyInput, o, q + 1);
                if (o.oneofs)
                    d.commandInput = "deriveMessageKeyInput";
            }
            if (m.rotateEpochInput != null && $Object.hasOwnProperty.call(m, "rotateEpochInput")) {
                d.rotateEpochInput = $root.proto.RotateEpochInput.toObject(m.rotateEpochInput, o, q + 1);
                if (o.oneofs)
                    d.commandInput = "rotateEpochInput";
            }
            return d;
        };

        LabyrinthWaCommand.prototype.toJSON = function() {
            return LabyrinthWaCommand.toObject(this, $protobuf.util.toJSONOptions);
        };

        LabyrinthWaCommand.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.LabyrinthWaCommand";
        };

        return LabyrinthWaCommand;
    })();

    proto.LegacyMessage = (function() {

        const LegacyMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        LegacyMessage.prototype.eventResponseMessage = null;
        LegacyMessage.prototype.pollVote = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LegacyMessage.prototype, "_eventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LegacyMessage.prototype, "_pollVote", {
            get: $util.oneOfGetter($oneOfFields = ["pollVote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LegacyMessage.create = function(properties) {
            return new LegacyMessage(properties);
        };

        LegacyMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.eventResponseMessage != null && $Object.hasOwnProperty.call(m, "eventResponseMessage"))
                $root.proto.Message.EventResponseMessage.encode(m.eventResponseMessage, w.uint32(10).fork(), q + 1).ldelim();
            if (m.pollVote != null && $Object.hasOwnProperty.call(m, "pollVote"))
                $root.proto.Message.PollVoteMessage.encode(m.pollVote, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        LegacyMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.LegacyMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.eventResponseMessage = $root.proto.Message.EventResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.eventResponseMessage);
                        m._eventResponseMessage = "eventResponseMessage";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.pollVote = $root.proto.Message.PollVoteMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollVote);
                        m._pollVote = "pollVote";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        LegacyMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.LegacyMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.LegacyMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.LegacyMessage();
            if (d.eventResponseMessage != null) {
                if (!$util.isObject(d.eventResponseMessage))
                    throw $TypeError(".proto.LegacyMessage.eventResponseMessage: object expected");
                m.eventResponseMessage = $root.proto.Message.EventResponseMessage.fromObject(d.eventResponseMessage, q + 1);
            }
            if (d.pollVote != null) {
                if (!$util.isObject(d.pollVote))
                    throw $TypeError(".proto.LegacyMessage.pollVote: object expected");
                m.pollVote = $root.proto.Message.PollVoteMessage.fromObject(d.pollVote, q + 1);
            }
            return m;
        };

        LegacyMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.eventResponseMessage != null && $Object.hasOwnProperty.call(m, "eventResponseMessage")) {
                d.eventResponseMessage = $root.proto.Message.EventResponseMessage.toObject(m.eventResponseMessage, o, q + 1);
            }
            if (m.pollVote != null && $Object.hasOwnProperty.call(m, "pollVote")) {
                d.pollVote = $root.proto.Message.PollVoteMessage.toObject(m.pollVote, o, q + 1);
            }
            return d;
        };

        LegacyMessage.prototype.toJSON = function() {
            return LegacyMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        LegacyMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.LegacyMessage";
        };

        return LegacyMessage;
    })();

    proto.LimitSharing = (function() {

        const LimitSharing = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        LimitSharing.prototype.sharingLimited = null;
        LimitSharing.prototype.trigger = null;
        LimitSharing.prototype.limitSharingSettingTimestamp = null;
        LimitSharing.prototype.initiatedByMe = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LimitSharing.prototype, "_sharingLimited", {
            get: $util.oneOfGetter($oneOfFields = ["sharingLimited"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LimitSharing.prototype, "_trigger", {
            get: $util.oneOfGetter($oneOfFields = ["trigger"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LimitSharing.prototype, "_limitSharingSettingTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingSettingTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LimitSharing.prototype, "_initiatedByMe", {
            get: $util.oneOfGetter($oneOfFields = ["initiatedByMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LimitSharing.create = function(properties) {
            return new LimitSharing(properties);
        };

        LimitSharing.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.sharingLimited != null && $Object.hasOwnProperty.call(m, "sharingLimited"))
                w.uint32(8).bool(m.sharingLimited);
            if (m.trigger != null && $Object.hasOwnProperty.call(m, "trigger"))
                w.uint32(16).int32(m.trigger);
            if (m.limitSharingSettingTimestamp != null && $Object.hasOwnProperty.call(m, "limitSharingSettingTimestamp"))
                w.uint32(24).int64(m.limitSharingSettingTimestamp);
            if (m.initiatedByMe != null && $Object.hasOwnProperty.call(m, "initiatedByMe"))
                w.uint32(32).bool(m.initiatedByMe);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        LimitSharing.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.LimitSharing(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.sharingLimited = r.bool();
                        m._sharingLimited = "sharingLimited";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.trigger = r.int32();
                        m._trigger = "trigger";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.limitSharingSettingTimestamp = r.int64();
                        m._limitSharingSettingTimestamp = "limitSharingSettingTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.initiatedByMe = r.bool();
                        m._initiatedByMe = "initiatedByMe";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        LimitSharing.fromObject = function (d, q) {
            if (d instanceof $root.proto.LimitSharing)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.LimitSharing: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.LimitSharing();
            if (d.sharingLimited != null) {
                m.sharingLimited = $Boolean(d.sharingLimited);
            }
            switch (d.trigger) {
            case "UNKNOWN":
            case 0:
                m.trigger = 0;
                break;
            case "CHAT_SETTING":
            case 1:
                m.trigger = 1;
                break;
            case "BIZ_SUPPORTS_FB_HOSTING":
            case 2:
                m.trigger = 2;
                break;
            case "UNKNOWN_GROUP":
            case 3:
                m.trigger = 3;
                break;
            default:
                if (typeof d.trigger === "number" && (d.trigger | 0) === d.trigger)
                    m.trigger = d.trigger;
            }
            if (d.limitSharingSettingTimestamp != null) {
                if ($util.Long)
                    m.limitSharingSettingTimestamp = $util.Long.fromValue(d.limitSharingSettingTimestamp, false);
                else if (typeof d.limitSharingSettingTimestamp === "string")
                    m.limitSharingSettingTimestamp = $parseInt(d.limitSharingSettingTimestamp, 10);
                else if (typeof d.limitSharingSettingTimestamp === "number")
                    m.limitSharingSettingTimestamp = d.limitSharingSettingTimestamp;
                else if (typeof d.limitSharingSettingTimestamp === "object")
                    m.limitSharingSettingTimestamp = new $util.LongBits(d.limitSharingSettingTimestamp.low >>> 0, d.limitSharingSettingTimestamp.high >>> 0).toNumber();
            }
            if (d.initiatedByMe != null) {
                m.initiatedByMe = $Boolean(d.initiatedByMe);
            }
            return m;
        };

        LimitSharing.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.sharingLimited != null && $Object.hasOwnProperty.call(m, "sharingLimited")) {
                d.sharingLimited = m.sharingLimited;
            }
            if (m.trigger != null && $Object.hasOwnProperty.call(m, "trigger")) {
                d.trigger = o.enums === $String ? $root.proto.LimitSharing.TriggerType[m.trigger] === $undefined ? m.trigger : $root.proto.LimitSharing.TriggerType[m.trigger] : m.trigger;
            }
            if (m.limitSharingSettingTimestamp != null && $Object.hasOwnProperty.call(m, "limitSharingSettingTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.limitSharingSettingTimestamp = typeof m.limitSharingSettingTimestamp === "number" ? $BigInt(m.limitSharingSettingTimestamp) : $util.Long.fromBits(m.limitSharingSettingTimestamp.low >>> 0, m.limitSharingSettingTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.limitSharingSettingTimestamp === "number")
                    d.limitSharingSettingTimestamp = o.longs === $String ? $String(m.limitSharingSettingTimestamp) : m.limitSharingSettingTimestamp;
                else
                    d.limitSharingSettingTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.limitSharingSettingTimestamp) : o.longs === $Number ? new $util.LongBits(m.limitSharingSettingTimestamp.low >>> 0, m.limitSharingSettingTimestamp.high >>> 0).toNumber() : m.limitSharingSettingTimestamp;
            }
            if (m.initiatedByMe != null && $Object.hasOwnProperty.call(m, "initiatedByMe")) {
                d.initiatedByMe = m.initiatedByMe;
            }
            return d;
        };

        LimitSharing.prototype.toJSON = function() {
            return LimitSharing.toObject(this, $protobuf.util.toJSONOptions);
        };

        LimitSharing.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.LimitSharing";
        };

        LimitSharing.TriggerType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "CHAT_SETTING"] = 1;
            values[valuesById[2] = "BIZ_SUPPORTS_FB_HOSTING"] = 2;
            values[valuesById[3] = "UNKNOWN_GROUP"] = 3;
            return values;
        })();

        return LimitSharing;
    })();

    proto.LocalizedName = (function() {

        const LocalizedName = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        LocalizedName.prototype.lg = null;
        LocalizedName.prototype.lc = null;
        LocalizedName.prototype.verifiedName = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LocalizedName.prototype, "_lg", {
            get: $util.oneOfGetter($oneOfFields = ["lg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LocalizedName.prototype, "_lc", {
            get: $util.oneOfGetter($oneOfFields = ["lc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(LocalizedName.prototype, "_verifiedName", {
            get: $util.oneOfGetter($oneOfFields = ["verifiedName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        LocalizedName.create = function(properties) {
            return new LocalizedName(properties);
        };

        LocalizedName.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.lg != null && $Object.hasOwnProperty.call(m, "lg"))
                w.uint32(10).string(m.lg);
            if (m.lc != null && $Object.hasOwnProperty.call(m, "lc"))
                w.uint32(18).string(m.lc);
            if (m.verifiedName != null && $Object.hasOwnProperty.call(m, "verifiedName"))
                w.uint32(26).string(m.verifiedName);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        LocalizedName.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.LocalizedName();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.lg = r.stringVerify();
                        m._lg = "lg";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.lc = r.stringVerify();
                        m._lc = "lc";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.verifiedName = r.stringVerify();
                        m._verifiedName = "verifiedName";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        LocalizedName.fromObject = function (d, q) {
            if (d instanceof $root.proto.LocalizedName)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.LocalizedName: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.LocalizedName();
            if (d.lg != null) {
                m.lg = $String(d.lg);
            }
            if (d.lc != null) {
                m.lc = $String(d.lc);
            }
            if (d.verifiedName != null) {
                m.verifiedName = $String(d.verifiedName);
            }
            return m;
        };

        LocalizedName.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.lg != null && $Object.hasOwnProperty.call(m, "lg")) {
                d.lg = m.lg;
            }
            if (m.lc != null && $Object.hasOwnProperty.call(m, "lc")) {
                d.lc = m.lc;
            }
            if (m.verifiedName != null && $Object.hasOwnProperty.call(m, "verifiedName")) {
                d.verifiedName = m.verifiedName;
            }
            return d;
        };

        LocalizedName.prototype.toJSON = function() {
            return LocalizedName.toObject(this, $protobuf.util.toJSONOptions);
        };

        LocalizedName.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.LocalizedName";
        };

        return LocalizedName;
    })();

    proto.Location = (function() {

        const Location = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Location.prototype.degreesLatitude = null;
        Location.prototype.degreesLongitude = null;
        Location.prototype.name = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Location.prototype, "_degreesLatitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Location.prototype, "_degreesLongitude", {
            get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Location.prototype, "_name", {
            get: $util.oneOfGetter($oneOfFields = ["name"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Location.create = function(properties) {
            return new Location(properties);
        };

        Location.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude"))
                w.uint32(9).double(m.degreesLatitude);
            if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude"))
                w.uint32(17).double(m.degreesLongitude);
            if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                w.uint32(26).string(m.name);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Location.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Location();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 1)
                            break;
                        m.degreesLatitude = r.double();
                        m._degreesLatitude = "degreesLatitude";
                        continue;
                    }
                case 2: {
                        if (u !== 1)
                            break;
                        m.degreesLongitude = r.double();
                        m._degreesLongitude = "degreesLongitude";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.name = r.stringVerify();
                        m._name = "name";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Location.fromObject = function (d, q) {
            if (d instanceof $root.proto.Location)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Location: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Location();
            if (d.degreesLatitude != null) {
                m.degreesLatitude = $Number(d.degreesLatitude);
            }
            if (d.degreesLongitude != null) {
                m.degreesLongitude = $Number(d.degreesLongitude);
            }
            if (d.name != null) {
                m.name = $String(d.name);
            }
            return m;
        };

        Location.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude")) {
                d.degreesLatitude = o.json && !$isFinite(m.degreesLatitude) ? $String(m.degreesLatitude) : m.degreesLatitude;
            }
            if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude")) {
                d.degreesLongitude = o.json && !$isFinite(m.degreesLongitude) ? $String(m.degreesLongitude) : m.degreesLongitude;
            }
            if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                d.name = m.name;
            }
            return d;
        };

        Location.prototype.toJSON = function() {
            return Location.toObject(this, $protobuf.util.toJSONOptions);
        };

        Location.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Location";
        };

        return Location;
    })();

    proto.MENTION_MENTION_TYPE = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "PROFILE"] = 0;
        return values;
    })();

    proto.MediaData = (function() {

        const MediaData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MediaData.prototype.localPath = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaData.prototype, "_localPath", {
            get: $util.oneOfGetter($oneOfFields = ["localPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaData.create = function(properties) {
            return new MediaData(properties);
        };

        MediaData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.localPath != null && $Object.hasOwnProperty.call(m, "localPath"))
                w.uint32(10).string(m.localPath);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MediaData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaData();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.localPath = r.stringVerify();
                        m._localPath = "localPath";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MediaData.fromObject = function (d, q) {
            if (d instanceof $root.proto.MediaData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MediaData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MediaData();
            if (d.localPath != null) {
                m.localPath = $String(d.localPath);
            }
            return m;
        };

        MediaData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.localPath != null && $Object.hasOwnProperty.call(m, "localPath")) {
                d.localPath = m.localPath;
            }
            return d;
        };

        MediaData.prototype.toJSON = function() {
            return MediaData.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MediaData";
        };

        return MediaData;
    })();

    proto.MediaDomainInfo = (function() {

        const MediaDomainInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MediaDomainInfo.prototype.mediaKeyDomain = null;
        MediaDomainInfo.prototype.e2EeMediaKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaDomainInfo.prototype, "_mediaKeyDomain", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyDomain"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaDomainInfo.prototype, "_e2EeMediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["e2EeMediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaDomainInfo.create = function(properties) {
            return new MediaDomainInfo(properties);
        };

        MediaDomainInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.mediaKeyDomain != null && $Object.hasOwnProperty.call(m, "mediaKeyDomain"))
                w.uint32(8).int32(m.mediaKeyDomain);
            if (m.e2EeMediaKey != null && $Object.hasOwnProperty.call(m, "e2EeMediaKey"))
                w.uint32(18).bytes(m.e2EeMediaKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MediaDomainInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaDomainInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.mediaKeyDomain = r.int32();
                        m._mediaKeyDomain = "mediaKeyDomain";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.e2EeMediaKey = r.bytes();
                        m._e2EeMediaKey = "e2EeMediaKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MediaDomainInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.MediaDomainInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MediaDomainInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MediaDomainInfo();
            switch (d.mediaKeyDomain) {
            case "MEDIA_KEY_DOMAIN_UNKNOWN":
            case 0:
                m.mediaKeyDomain = 0;
                break;
            case "MEDIA_KEY_DOMAIN_E2EE":
            case 1:
                m.mediaKeyDomain = 1;
                break;
            case "MEDIA_KEY_DOMAIN_NON_E2EE":
            case 2:
                m.mediaKeyDomain = 2;
                break;
            default:
                if (typeof d.mediaKeyDomain === "number" && (d.mediaKeyDomain | 0) === d.mediaKeyDomain)
                    m.mediaKeyDomain = d.mediaKeyDomain;
            }
            if (d.e2EeMediaKey != null) {
                if (typeof d.e2EeMediaKey === "string")
                    $util.base64.decode(d.e2EeMediaKey, m.e2EeMediaKey = $util.newBuffer($util.base64.length(d.e2EeMediaKey)), 0);
                else if (d.e2EeMediaKey.length >= 0)
                    m.e2EeMediaKey = d.e2EeMediaKey;
            }
            return m;
        };

        MediaDomainInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.mediaKeyDomain != null && $Object.hasOwnProperty.call(m, "mediaKeyDomain")) {
                d.mediaKeyDomain = o.enums === $String ? $root.proto.MediaKeyDomain[m.mediaKeyDomain] === $undefined ? m.mediaKeyDomain : $root.proto.MediaKeyDomain[m.mediaKeyDomain] : m.mediaKeyDomain;
            }
            if (m.e2EeMediaKey != null && $Object.hasOwnProperty.call(m, "e2EeMediaKey")) {
                d.e2EeMediaKey = o.bytes === $String ? $util.base64.encode(m.e2EeMediaKey, 0, m.e2EeMediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.e2EeMediaKey) : m.e2EeMediaKey;
            }
            return d;
        };

        MediaDomainInfo.prototype.toJSON = function() {
            return MediaDomainInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaDomainInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MediaDomainInfo";
        };

        return MediaDomainInfo;
    })();

    proto.MediaEntry = (function() {

        const MediaEntry = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MediaEntry.prototype.fileSha256 = null;
        MediaEntry.prototype.mediaKey = null;
        MediaEntry.prototype.fileEncSha256 = null;
        MediaEntry.prototype.directPath = null;
        MediaEntry.prototype.mediaKeyTimestamp = null;
        MediaEntry.prototype.serverMediaType = null;
        MediaEntry.prototype.uploadToken = null;
        MediaEntry.prototype.validatedTimestamp = null;
        MediaEntry.prototype.sidecar = null;
        MediaEntry.prototype.objectId = null;
        MediaEntry.prototype.fbid = null;
        MediaEntry.prototype.downloadableThumbnail = null;
        MediaEntry.prototype.handle = null;
        MediaEntry.prototype.filename = null;
        MediaEntry.prototype.progressiveJpegDetails = null;
        MediaEntry.prototype.size = null;
        MediaEntry.prototype.lastDownloadAttemptTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_mediaKeyTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_serverMediaType", {
            get: $util.oneOfGetter($oneOfFields = ["serverMediaType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_uploadToken", {
            get: $util.oneOfGetter($oneOfFields = ["uploadToken"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_validatedTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["validatedTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_sidecar", {
            get: $util.oneOfGetter($oneOfFields = ["sidecar"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_objectId", {
            get: $util.oneOfGetter($oneOfFields = ["objectId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_fbid", {
            get: $util.oneOfGetter($oneOfFields = ["fbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_downloadableThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["downloadableThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_handle", {
            get: $util.oneOfGetter($oneOfFields = ["handle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_filename", {
            get: $util.oneOfGetter($oneOfFields = ["filename"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_progressiveJpegDetails", {
            get: $util.oneOfGetter($oneOfFields = ["progressiveJpegDetails"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_size", {
            get: $util.oneOfGetter($oneOfFields = ["size"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaEntry.prototype, "_lastDownloadAttemptTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["lastDownloadAttemptTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaEntry.create = function(properties) {
            return new MediaEntry(properties);
        };

        MediaEntry.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                w.uint32(10).bytes(m.fileSha256);
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(18).bytes(m.mediaKey);
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                w.uint32(26).bytes(m.fileEncSha256);
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(34).string(m.directPath);
            if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                w.uint32(40).int64(m.mediaKeyTimestamp);
            if (m.serverMediaType != null && $Object.hasOwnProperty.call(m, "serverMediaType"))
                w.uint32(50).string(m.serverMediaType);
            if (m.uploadToken != null && $Object.hasOwnProperty.call(m, "uploadToken"))
                w.uint32(58).bytes(m.uploadToken);
            if (m.validatedTimestamp != null && $Object.hasOwnProperty.call(m, "validatedTimestamp"))
                w.uint32(66).bytes(m.validatedTimestamp);
            if (m.sidecar != null && $Object.hasOwnProperty.call(m, "sidecar"))
                w.uint32(74).bytes(m.sidecar);
            if (m.objectId != null && $Object.hasOwnProperty.call(m, "objectId"))
                w.uint32(82).string(m.objectId);
            if (m.fbid != null && $Object.hasOwnProperty.call(m, "fbid"))
                w.uint32(90).string(m.fbid);
            if (m.downloadableThumbnail != null && $Object.hasOwnProperty.call(m, "downloadableThumbnail"))
                $root.proto.MediaEntry.DownloadableThumbnail.encode(m.downloadableThumbnail, w.uint32(98).fork(), q + 1).ldelim();
            if (m.handle != null && $Object.hasOwnProperty.call(m, "handle"))
                w.uint32(106).string(m.handle);
            if (m.filename != null && $Object.hasOwnProperty.call(m, "filename"))
                w.uint32(114).string(m.filename);
            if (m.progressiveJpegDetails != null && $Object.hasOwnProperty.call(m, "progressiveJpegDetails"))
                $root.proto.MediaEntry.ProgressiveJpegDetails.encode(m.progressiveJpegDetails, w.uint32(122).fork(), q + 1).ldelim();
            if (m.size != null && $Object.hasOwnProperty.call(m, "size"))
                w.uint32(128).int64(m.size);
            if (m.lastDownloadAttemptTimestamp != null && $Object.hasOwnProperty.call(m, "lastDownloadAttemptTimestamp"))
                w.uint32(136).int64(m.lastDownloadAttemptTimestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MediaEntry.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaEntry();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.fileSha256 = r.bytes();
                        m._fileSha256 = "fileSha256";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.mediaKey = r.bytes();
                        m._mediaKey = "mediaKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.fileEncSha256 = r.bytes();
                        m._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.directPath = r.stringVerify();
                        m._directPath = "directPath";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.mediaKeyTimestamp = r.int64();
                        m._mediaKeyTimestamp = "mediaKeyTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.serverMediaType = r.stringVerify();
                        m._serverMediaType = "serverMediaType";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.uploadToken = r.bytes();
                        m._uploadToken = "uploadToken";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.validatedTimestamp = r.bytes();
                        m._validatedTimestamp = "validatedTimestamp";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.sidecar = r.bytes();
                        m._sidecar = "sidecar";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.objectId = r.stringVerify();
                        m._objectId = "objectId";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.fbid = r.stringVerify();
                        m._fbid = "fbid";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.downloadableThumbnail = $root.proto.MediaEntry.DownloadableThumbnail.decode(r, r.uint32(), $undefined, q + 1, m.downloadableThumbnail);
                        m._downloadableThumbnail = "downloadableThumbnail";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.handle = r.stringVerify();
                        m._handle = "handle";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.filename = r.stringVerify();
                        m._filename = "filename";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.progressiveJpegDetails = $root.proto.MediaEntry.ProgressiveJpegDetails.decode(r, r.uint32(), $undefined, q + 1, m.progressiveJpegDetails);
                        m._progressiveJpegDetails = "progressiveJpegDetails";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        m.size = r.int64();
                        m._size = "size";
                        continue;
                    }
                case 17: {
                        if (u !== 0)
                            break;
                        m.lastDownloadAttemptTimestamp = r.int64();
                        m._lastDownloadAttemptTimestamp = "lastDownloadAttemptTimestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MediaEntry.fromObject = function (d, q) {
            if (d instanceof $root.proto.MediaEntry)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MediaEntry: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MediaEntry();
            if (d.fileSha256 != null) {
                if (typeof d.fileSha256 === "string")
                    $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                else if (d.fileSha256.length >= 0)
                    m.fileSha256 = d.fileSha256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.fileEncSha256 != null) {
                if (typeof d.fileEncSha256 === "string")
                    $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                else if (d.fileEncSha256.length >= 0)
                    m.fileEncSha256 = d.fileEncSha256;
            }
            if (d.directPath != null) {
                m.directPath = $String(d.directPath);
            }
            if (d.mediaKeyTimestamp != null) {
                if ($util.Long)
                    m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                else if (typeof d.mediaKeyTimestamp === "string")
                    m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                else if (typeof d.mediaKeyTimestamp === "number")
                    m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                else if (typeof d.mediaKeyTimestamp === "object")
                    m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
            }
            if (d.serverMediaType != null) {
                m.serverMediaType = $String(d.serverMediaType);
            }
            if (d.uploadToken != null) {
                if (typeof d.uploadToken === "string")
                    $util.base64.decode(d.uploadToken, m.uploadToken = $util.newBuffer($util.base64.length(d.uploadToken)), 0);
                else if (d.uploadToken.length >= 0)
                    m.uploadToken = d.uploadToken;
            }
            if (d.validatedTimestamp != null) {
                if (typeof d.validatedTimestamp === "string")
                    $util.base64.decode(d.validatedTimestamp, m.validatedTimestamp = $util.newBuffer($util.base64.length(d.validatedTimestamp)), 0);
                else if (d.validatedTimestamp.length >= 0)
                    m.validatedTimestamp = d.validatedTimestamp;
            }
            if (d.sidecar != null) {
                if (typeof d.sidecar === "string")
                    $util.base64.decode(d.sidecar, m.sidecar = $util.newBuffer($util.base64.length(d.sidecar)), 0);
                else if (d.sidecar.length >= 0)
                    m.sidecar = d.sidecar;
            }
            if (d.objectId != null) {
                m.objectId = $String(d.objectId);
            }
            if (d.fbid != null) {
                m.fbid = $String(d.fbid);
            }
            if (d.downloadableThumbnail != null) {
                if (!$util.isObject(d.downloadableThumbnail))
                    throw $TypeError(".proto.MediaEntry.downloadableThumbnail: object expected");
                m.downloadableThumbnail = $root.proto.MediaEntry.DownloadableThumbnail.fromObject(d.downloadableThumbnail, q + 1);
            }
            if (d.handle != null) {
                m.handle = $String(d.handle);
            }
            if (d.filename != null) {
                m.filename = $String(d.filename);
            }
            if (d.progressiveJpegDetails != null) {
                if (!$util.isObject(d.progressiveJpegDetails))
                    throw $TypeError(".proto.MediaEntry.progressiveJpegDetails: object expected");
                m.progressiveJpegDetails = $root.proto.MediaEntry.ProgressiveJpegDetails.fromObject(d.progressiveJpegDetails, q + 1);
            }
            if (d.size != null) {
                if ($util.Long)
                    m.size = $util.Long.fromValue(d.size, false);
                else if (typeof d.size === "string")
                    m.size = $parseInt(d.size, 10);
                else if (typeof d.size === "number")
                    m.size = d.size;
                else if (typeof d.size === "object")
                    m.size = new $util.LongBits(d.size.low >>> 0, d.size.high >>> 0).toNumber();
            }
            if (d.lastDownloadAttemptTimestamp != null) {
                if ($util.Long)
                    m.lastDownloadAttemptTimestamp = $util.Long.fromValue(d.lastDownloadAttemptTimestamp, false);
                else if (typeof d.lastDownloadAttemptTimestamp === "string")
                    m.lastDownloadAttemptTimestamp = $parseInt(d.lastDownloadAttemptTimestamp, 10);
                else if (typeof d.lastDownloadAttemptTimestamp === "number")
                    m.lastDownloadAttemptTimestamp = d.lastDownloadAttemptTimestamp;
                else if (typeof d.lastDownloadAttemptTimestamp === "object")
                    m.lastDownloadAttemptTimestamp = new $util.LongBits(d.lastDownloadAttemptTimestamp.low >>> 0, d.lastDownloadAttemptTimestamp.high >>> 0).toNumber();
            }
            return m;
        };

        MediaEntry.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
            }
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
            }
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
            }
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                d.directPath = m.directPath;
            }
            if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.mediaKeyTimestamp === "number")
                    d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                else
                    d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
            }
            if (m.serverMediaType != null && $Object.hasOwnProperty.call(m, "serverMediaType")) {
                d.serverMediaType = m.serverMediaType;
            }
            if (m.uploadToken != null && $Object.hasOwnProperty.call(m, "uploadToken")) {
                d.uploadToken = o.bytes === $String ? $util.base64.encode(m.uploadToken, 0, m.uploadToken.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.uploadToken) : m.uploadToken;
            }
            if (m.validatedTimestamp != null && $Object.hasOwnProperty.call(m, "validatedTimestamp")) {
                d.validatedTimestamp = o.bytes === $String ? $util.base64.encode(m.validatedTimestamp, 0, m.validatedTimestamp.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.validatedTimestamp) : m.validatedTimestamp;
            }
            if (m.sidecar != null && $Object.hasOwnProperty.call(m, "sidecar")) {
                d.sidecar = o.bytes === $String ? $util.base64.encode(m.sidecar, 0, m.sidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.sidecar) : m.sidecar;
            }
            if (m.objectId != null && $Object.hasOwnProperty.call(m, "objectId")) {
                d.objectId = m.objectId;
            }
            if (m.fbid != null && $Object.hasOwnProperty.call(m, "fbid")) {
                d.fbid = m.fbid;
            }
            if (m.downloadableThumbnail != null && $Object.hasOwnProperty.call(m, "downloadableThumbnail")) {
                d.downloadableThumbnail = $root.proto.MediaEntry.DownloadableThumbnail.toObject(m.downloadableThumbnail, o, q + 1);
            }
            if (m.handle != null && $Object.hasOwnProperty.call(m, "handle")) {
                d.handle = m.handle;
            }
            if (m.filename != null && $Object.hasOwnProperty.call(m, "filename")) {
                d.filename = m.filename;
            }
            if (m.progressiveJpegDetails != null && $Object.hasOwnProperty.call(m, "progressiveJpegDetails")) {
                d.progressiveJpegDetails = $root.proto.MediaEntry.ProgressiveJpegDetails.toObject(m.progressiveJpegDetails, o, q + 1);
            }
            if (m.size != null && $Object.hasOwnProperty.call(m, "size")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.size = typeof m.size === "number" ? $BigInt(m.size) : $util.Long.fromBits(m.size.low >>> 0, m.size.high >>> 0, false).toBigInt();
                else if (typeof m.size === "number")
                    d.size = o.longs === $String ? $String(m.size) : m.size;
                else
                    d.size = o.longs === $String ? $util.Long.prototype.toString.call(m.size) : o.longs === $Number ? new $util.LongBits(m.size.low >>> 0, m.size.high >>> 0).toNumber() : m.size;
            }
            if (m.lastDownloadAttemptTimestamp != null && $Object.hasOwnProperty.call(m, "lastDownloadAttemptTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.lastDownloadAttemptTimestamp = typeof m.lastDownloadAttemptTimestamp === "number" ? $BigInt(m.lastDownloadAttemptTimestamp) : $util.Long.fromBits(m.lastDownloadAttemptTimestamp.low >>> 0, m.lastDownloadAttemptTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.lastDownloadAttemptTimestamp === "number")
                    d.lastDownloadAttemptTimestamp = o.longs === $String ? $String(m.lastDownloadAttemptTimestamp) : m.lastDownloadAttemptTimestamp;
                else
                    d.lastDownloadAttemptTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.lastDownloadAttemptTimestamp) : o.longs === $Number ? new $util.LongBits(m.lastDownloadAttemptTimestamp.low >>> 0, m.lastDownloadAttemptTimestamp.high >>> 0).toNumber() : m.lastDownloadAttemptTimestamp;
            }
            return d;
        };

        MediaEntry.prototype.toJSON = function() {
            return MediaEntry.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaEntry.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MediaEntry";
        };

        MediaEntry.DownloadableThumbnail = (function() {

            const DownloadableThumbnail = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DownloadableThumbnail.prototype.fileSha256 = null;
            DownloadableThumbnail.prototype.fileEncSha256 = null;
            DownloadableThumbnail.prototype.directPath = null;
            DownloadableThumbnail.prototype.mediaKey = null;
            DownloadableThumbnail.prototype.mediaKeyTimestamp = null;
            DownloadableThumbnail.prototype.objectId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DownloadableThumbnail.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DownloadableThumbnail.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DownloadableThumbnail.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DownloadableThumbnail.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DownloadableThumbnail.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DownloadableThumbnail.prototype, "_objectId", {
                get: $util.oneOfGetter($oneOfFields = ["objectId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DownloadableThumbnail.create = function(properties) {
                return new DownloadableThumbnail(properties);
            };

            DownloadableThumbnail.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(10).bytes(m.fileSha256);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(18).bytes(m.fileEncSha256);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(26).string(m.directPath);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(34).bytes(m.mediaKey);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(40).int64(m.mediaKeyTimestamp);
                if (m.objectId != null && $Object.hasOwnProperty.call(m, "objectId"))
                    w.uint32(50).string(m.objectId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DownloadableThumbnail.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaEntry.DownloadableThumbnail();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.objectId = r.stringVerify();
                            m._objectId = "objectId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DownloadableThumbnail.fromObject = function (d, q) {
                if (d instanceof $root.proto.MediaEntry.DownloadableThumbnail)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.MediaEntry.DownloadableThumbnail: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.MediaEntry.DownloadableThumbnail();
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.objectId != null) {
                    m.objectId = $String(d.objectId);
                }
                return m;
            };

            DownloadableThumbnail.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.objectId != null && $Object.hasOwnProperty.call(m, "objectId")) {
                    d.objectId = m.objectId;
                }
                return d;
            };

            DownloadableThumbnail.prototype.toJSON = function() {
                return DownloadableThumbnail.toObject(this, $protobuf.util.toJSONOptions);
            };

            DownloadableThumbnail.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.MediaEntry.DownloadableThumbnail";
            };

            return DownloadableThumbnail;
        })();

        MediaEntry.ProgressiveJpegDetails = (function() {

            const ProgressiveJpegDetails = function (p) {
                this.scanLengths = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ProgressiveJpegDetails.prototype.scanLengths = $util.emptyArray;
            ProgressiveJpegDetails.prototype.sidecar = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProgressiveJpegDetails.prototype, "_sidecar", {
                get: $util.oneOfGetter($oneOfFields = ["sidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProgressiveJpegDetails.create = function(properties) {
                return new ProgressiveJpegDetails(properties);
            };

            ProgressiveJpegDetails.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.scanLengths != null && m.scanLengths.length) {
                    w.uint32(10).uint32s(m.scanLengths);
                }
                if (m.sidecar != null && $Object.hasOwnProperty.call(m, "sidecar"))
                    w.uint32(18).bytes(m.sidecar);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ProgressiveJpegDetails.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaEntry.ProgressiveJpegDetails();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u === 2) {
                                if (!(m.scanLengths && m.scanLengths.length))
                                    m.scanLengths = [];
                                r.uint32s(m.scanLengths);
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(m.scanLengths && m.scanLengths.length))
                                m.scanLengths = [];
                            m.scanLengths.push(r.uint32());
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.sidecar = r.bytes();
                            m._sidecar = "sidecar";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ProgressiveJpegDetails.fromObject = function (d, q) {
                if (d instanceof $root.proto.MediaEntry.ProgressiveJpegDetails)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.MediaEntry.ProgressiveJpegDetails: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.MediaEntry.ProgressiveJpegDetails();
                if (d.scanLengths) {
                    if (!$Array.isArray(d.scanLengths))
                        throw $TypeError(".proto.MediaEntry.ProgressiveJpegDetails.scanLengths: array expected");
                    m.scanLengths = $Array(d.scanLengths.length);
                    for (var i = 0; i < d.scanLengths.length; ++i) {
                        m.scanLengths[i] = d.scanLengths[i] >>> 0;
                    }
                }
                if (d.sidecar != null) {
                    if (typeof d.sidecar === "string")
                        $util.base64.decode(d.sidecar, m.sidecar = $util.newBuffer($util.base64.length(d.sidecar)), 0);
                    else if (d.sidecar.length >= 0)
                        m.sidecar = d.sidecar;
                }
                return m;
            };

            ProgressiveJpegDetails.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.scanLengths = [];
                }
                if (m.scanLengths && m.scanLengths.length) {
                    d.scanLengths = $Array(m.scanLengths.length);
                    for (var j = 0; j < m.scanLengths.length; ++j) {
                        d.scanLengths[j] = m.scanLengths[j];
                    }
                }
                if (m.sidecar != null && $Object.hasOwnProperty.call(m, "sidecar")) {
                    d.sidecar = o.bytes === $String ? $util.base64.encode(m.sidecar, 0, m.sidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.sidecar) : m.sidecar;
                }
                return d;
            };

            ProgressiveJpegDetails.prototype.toJSON = function() {
                return ProgressiveJpegDetails.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProgressiveJpegDetails.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.MediaEntry.ProgressiveJpegDetails";
            };

            return ProgressiveJpegDetails;
        })();

        return MediaEntry;
    })();

    proto.MediaKeyDomain = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "MEDIA_KEY_DOMAIN_UNKNOWN"] = 0;
        values[valuesById[1] = "MEDIA_KEY_DOMAIN_E2EE"] = 1;
        values[valuesById[2] = "MEDIA_KEY_DOMAIN_NON_E2EE"] = 2;
        return values;
    })();

    proto.MediaNotifyMessage = (function() {

        const MediaNotifyMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MediaNotifyMessage.prototype.expressPathUrl = null;
        MediaNotifyMessage.prototype.fileEncSha256 = null;
        MediaNotifyMessage.prototype.fileLength = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaNotifyMessage.prototype, "_expressPathUrl", {
            get: $util.oneOfGetter($oneOfFields = ["expressPathUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaNotifyMessage.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaNotifyMessage.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaNotifyMessage.create = function(properties) {
            return new MediaNotifyMessage(properties);
        };

        MediaNotifyMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.expressPathUrl != null && $Object.hasOwnProperty.call(m, "expressPathUrl"))
                w.uint32(10).string(m.expressPathUrl);
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                w.uint32(18).bytes(m.fileEncSha256);
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(24).uint64(m.fileLength);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MediaNotifyMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaNotifyMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.expressPathUrl = r.stringVerify();
                        m._expressPathUrl = "expressPathUrl";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.fileEncSha256 = r.bytes();
                        m._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.fileLength = r.uint64();
                        m._fileLength = "fileLength";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MediaNotifyMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.MediaNotifyMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MediaNotifyMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MediaNotifyMessage();
            if (d.expressPathUrl != null) {
                m.expressPathUrl = $String(d.expressPathUrl);
            }
            if (d.fileEncSha256 != null) {
                if (typeof d.fileEncSha256 === "string")
                    $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                else if (d.fileEncSha256.length >= 0)
                    m.fileEncSha256 = d.fileEncSha256;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    m.fileLength = $util.Long.fromValue(d.fileLength, true);
                else if (typeof d.fileLength === "string")
                    m.fileLength = $parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            return m;
        };

        MediaNotifyMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.expressPathUrl != null && $Object.hasOwnProperty.call(m, "expressPathUrl")) {
                d.expressPathUrl = m.expressPathUrl;
            }
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
            }
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                else if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
            }
            return d;
        };

        MediaNotifyMessage.prototype.toJSON = function() {
            return MediaNotifyMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaNotifyMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MediaNotifyMessage";
        };

        return MediaNotifyMessage;
    })();

    proto.MediaRetryNotification = (function() {

        const MediaRetryNotification = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MediaRetryNotification.prototype.stanzaId = null;
        MediaRetryNotification.prototype.directPath = null;
        MediaRetryNotification.prototype.result = null;
        MediaRetryNotification.prototype.messageSecret = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaRetryNotification.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaRetryNotification.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaRetryNotification.prototype, "_result", {
            get: $util.oneOfGetter($oneOfFields = ["result"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MediaRetryNotification.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MediaRetryNotification.create = function(properties) {
            return new MediaRetryNotification(properties);
        };

        MediaRetryNotification.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                w.uint32(10).string(m.stanzaId);
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(18).string(m.directPath);
            if (m.result != null && $Object.hasOwnProperty.call(m, "result"))
                w.uint32(24).int32(m.result);
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(34).bytes(m.messageSecret);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MediaRetryNotification.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MediaRetryNotification(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.stanzaId = r.stringVerify();
                        m._stanzaId = "stanzaId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.directPath = r.stringVerify();
                        m._directPath = "directPath";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.result = r.int32();
                        m._result = "result";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.messageSecret = r.bytes();
                        m._messageSecret = "messageSecret";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MediaRetryNotification.fromObject = function (d, q) {
            if (d instanceof $root.proto.MediaRetryNotification)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MediaRetryNotification: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MediaRetryNotification();
            if (d.stanzaId != null) {
                m.stanzaId = $String(d.stanzaId);
            }
            if (d.directPath != null) {
                m.directPath = $String(d.directPath);
            }
            switch (d.result) {
            case "GENERAL_ERROR":
            case 0:
                m.result = 0;
                break;
            case "SUCCESS":
            case 1:
                m.result = 1;
                break;
            case "NOT_FOUND":
            case 2:
                m.result = 2;
                break;
            case "DECRYPTION_ERROR":
            case 3:
                m.result = 3;
                break;
            default:
                if (typeof d.result === "number" && (d.result | 0) === d.result)
                    m.result = d.result;
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            return m;
        };

        MediaRetryNotification.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                d.stanzaId = m.stanzaId;
            }
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                d.directPath = m.directPath;
            }
            if (m.result != null && $Object.hasOwnProperty.call(m, "result")) {
                d.result = o.enums === $String ? $root.proto.MediaRetryNotification.ResultType[m.result] === $undefined ? m.result : $root.proto.MediaRetryNotification.ResultType[m.result] : m.result;
            }
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret")) {
                d.messageSecret = o.bytes === $String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
            }
            return d;
        };

        MediaRetryNotification.prototype.toJSON = function() {
            return MediaRetryNotification.toObject(this, $protobuf.util.toJSONOptions);
        };

        MediaRetryNotification.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MediaRetryNotification";
        };

        MediaRetryNotification.ResultType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "GENERAL_ERROR"] = 0;
            values[valuesById[1] = "SUCCESS"] = 1;
            values[valuesById[2] = "NOT_FOUND"] = 2;
            values[valuesById[3] = "DECRYPTION_ERROR"] = 3;
            return values;
        })();

        return MediaRetryNotification;
    })();

    proto.MediaVisibility = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "DEFAULT"] = 0;
        values[valuesById[1] = "OFF"] = 1;
        values[valuesById[2] = "ON"] = 2;
        return values;
    })();

    proto.MemberLabel = (function() {

        const MemberLabel = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MemberLabel.prototype.label = null;
        MemberLabel.prototype.labelTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MemberLabel.prototype, "_label", {
            get: $util.oneOfGetter($oneOfFields = ["label"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MemberLabel.prototype, "_labelTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["labelTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MemberLabel.create = function(properties) {
            return new MemberLabel(properties);
        };

        MemberLabel.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.label != null && $Object.hasOwnProperty.call(m, "label"))
                w.uint32(10).string(m.label);
            if (m.labelTimestamp != null && $Object.hasOwnProperty.call(m, "labelTimestamp"))
                w.uint32(16).int64(m.labelTimestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MemberLabel.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MemberLabel();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.label = r.stringVerify();
                        m._label = "label";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.labelTimestamp = r.int64();
                        m._labelTimestamp = "labelTimestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MemberLabel.fromObject = function (d, q) {
            if (d instanceof $root.proto.MemberLabel)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MemberLabel: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MemberLabel();
            if (d.label != null) {
                m.label = $String(d.label);
            }
            if (d.labelTimestamp != null) {
                if ($util.Long)
                    m.labelTimestamp = $util.Long.fromValue(d.labelTimestamp, false);
                else if (typeof d.labelTimestamp === "string")
                    m.labelTimestamp = $parseInt(d.labelTimestamp, 10);
                else if (typeof d.labelTimestamp === "number")
                    m.labelTimestamp = d.labelTimestamp;
                else if (typeof d.labelTimestamp === "object")
                    m.labelTimestamp = new $util.LongBits(d.labelTimestamp.low >>> 0, d.labelTimestamp.high >>> 0).toNumber();
            }
            return m;
        };

        MemberLabel.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.label != null && $Object.hasOwnProperty.call(m, "label")) {
                d.label = m.label;
            }
            if (m.labelTimestamp != null && $Object.hasOwnProperty.call(m, "labelTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.labelTimestamp = typeof m.labelTimestamp === "number" ? $BigInt(m.labelTimestamp) : $util.Long.fromBits(m.labelTimestamp.low >>> 0, m.labelTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.labelTimestamp === "number")
                    d.labelTimestamp = o.longs === $String ? $String(m.labelTimestamp) : m.labelTimestamp;
                else
                    d.labelTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.labelTimestamp) : o.longs === $Number ? new $util.LongBits(m.labelTimestamp.low >>> 0, m.labelTimestamp.high >>> 0).toNumber() : m.labelTimestamp;
            }
            return d;
        };

        MemberLabel.prototype.toJSON = function() {
            return MemberLabel.toObject(this, $protobuf.util.toJSONOptions);
        };

        MemberLabel.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MemberLabel";
        };

        return MemberLabel;
    })();

    proto.Mention = (function() {

        const Mention = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Mention.prototype.mentionType = null;
        Mention.prototype.mentionedJid = null;
        Mention.prototype.offset = null;
        Mention.prototype.length = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Mention.prototype, "_mentionType", {
            get: $util.oneOfGetter($oneOfFields = ["mentionType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Mention.prototype, "_mentionedJid", {
            get: $util.oneOfGetter($oneOfFields = ["mentionedJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Mention.prototype, "_offset", {
            get: $util.oneOfGetter($oneOfFields = ["offset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Mention.prototype, "_length", {
            get: $util.oneOfGetter($oneOfFields = ["length"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Mention.create = function(properties) {
            return new Mention(properties);
        };

        Mention.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.mentionType != null && $Object.hasOwnProperty.call(m, "mentionType"))
                w.uint32(8).int32(m.mentionType);
            if (m.mentionedJid != null && $Object.hasOwnProperty.call(m, "mentionedJid"))
                w.uint32(18).string(m.mentionedJid);
            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset"))
                w.uint32(24).uint32(m.offset);
            if (m.length != null && $Object.hasOwnProperty.call(m, "length"))
                w.uint32(32).uint32(m.length);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Mention.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Mention(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.mentionType = r.int32();
                        m._mentionType = "mentionType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.mentionedJid = r.stringVerify();
                        m._mentionedJid = "mentionedJid";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.offset = r.uint32();
                        m._offset = "offset";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.length = r.uint32();
                        m._length = "length";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Mention.fromObject = function (d, q) {
            if (d instanceof $root.proto.Mention)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Mention: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Mention();
            switch (d.mentionType) {
            case "PROFILE":
            case 0:
                m.mentionType = 0;
                break;
            default:
                if (typeof d.mentionType === "number" && (d.mentionType | 0) === d.mentionType)
                    m.mentionType = d.mentionType;
            }
            if (d.mentionedJid != null) {
                m.mentionedJid = $String(d.mentionedJid);
            }
            if (d.offset != null) {
                m.offset = d.offset >>> 0;
            }
            if (d.length != null) {
                m.length = d.length >>> 0;
            }
            return m;
        };

        Mention.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.mentionType != null && $Object.hasOwnProperty.call(m, "mentionType")) {
                d.mentionType = o.enums === $String ? $root.proto.MENTION_MENTION_TYPE[m.mentionType] === $undefined ? m.mentionType : $root.proto.MENTION_MENTION_TYPE[m.mentionType] : m.mentionType;
            }
            if (m.mentionedJid != null && $Object.hasOwnProperty.call(m, "mentionedJid")) {
                d.mentionedJid = m.mentionedJid;
            }
            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset")) {
                d.offset = m.offset;
            }
            if (m.length != null && $Object.hasOwnProperty.call(m, "length")) {
                d.length = m.length;
            }
            return d;
        };

        Mention.prototype.toJSON = function() {
            return Mention.toObject(this, $protobuf.util.toJSONOptions);
        };

        Mention.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Mention";
        };

        return Mention;
    })();

    proto.Message = (function() {

        const Message = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Message.prototype.conversation = null;
        Message.prototype.senderKeyDistributionMessage = null;
        Message.prototype.imageMessage = null;
        Message.prototype.contactMessage = null;
        Message.prototype.locationMessage = null;
        Message.prototype.extendedTextMessage = null;
        Message.prototype.documentMessage = null;
        Message.prototype.audioMessage = null;
        Message.prototype.videoMessage = null;
        Message.prototype.call = null;
        Message.prototype.chat = null;
        Message.prototype.protocolMessage = null;
        Message.prototype.contactsArrayMessage = null;
        Message.prototype.highlyStructuredMessage = null;
        Message.prototype.fastRatchetKeySenderKeyDistributionMessage = null;
        Message.prototype.sendPaymentMessage = null;
        Message.prototype.liveLocationMessage = null;
        Message.prototype.requestPaymentMessage = null;
        Message.prototype.declinePaymentRequestMessage = null;
        Message.prototype.cancelPaymentRequestMessage = null;
        Message.prototype.templateMessage = null;
        Message.prototype.stickerMessage = null;
        Message.prototype.groupInviteMessage = null;
        Message.prototype.templateButtonReplyMessage = null;
        Message.prototype.productMessage = null;
        Message.prototype.deviceSentMessage = null;
        Message.prototype.messageContextInfo = null;
        Message.prototype.listMessage = null;
        Message.prototype.viewOnceMessage = null;
        Message.prototype.orderMessage = null;
        Message.prototype.listResponseMessage = null;
        Message.prototype.ephemeralMessage = null;
        Message.prototype.invoiceMessage = null;
        Message.prototype.buttonsMessage = null;
        Message.prototype.buttonsResponseMessage = null;
        Message.prototype.paymentInviteMessage = null;
        Message.prototype.interactiveMessage = null;
        Message.prototype.reactionMessage = null;
        Message.prototype.stickerSyncRmrMessage = null;
        Message.prototype.interactiveResponseMessage = null;
        Message.prototype.pollCreationMessage = null;
        Message.prototype.pollUpdateMessage = null;
        Message.prototype.keepInChatMessage = null;
        Message.prototype.documentWithCaptionMessage = null;
        Message.prototype.requestPhoneNumberMessage = null;
        Message.prototype.viewOnceMessageV2 = null;
        Message.prototype.encReactionMessage = null;
        Message.prototype.editedMessage = null;
        Message.prototype.viewOnceMessageV2Extension = null;
        Message.prototype.pollCreationMessageV2 = null;
        Message.prototype.scheduledCallCreationMessage = null;
        Message.prototype.groupMentionedMessage = null;
        Message.prototype.pinInChatMessage = null;
        Message.prototype.pollCreationMessageV3 = null;
        Message.prototype.scheduledCallEditMessage = null;
        Message.prototype.ptvMessage = null;
        Message.prototype.botInvokeMessage = null;
        Message.prototype.callLogMesssage = null;
        Message.prototype.messageHistoryBundle = null;
        Message.prototype.encCommentMessage = null;
        Message.prototype.bcallMessage = null;
        Message.prototype.lottieStickerMessage = null;
        Message.prototype.eventMessage = null;
        Message.prototype.encEventResponseMessage = null;
        Message.prototype.commentMessage = null;
        Message.prototype.newsletterAdminInviteMessage = null;
        Message.prototype.placeholderMessage = null;
        Message.prototype.secretEncryptedMessage = null;
        Message.prototype.albumMessage = null;
        Message.prototype.eventCoverImage = null;
        Message.prototype.stickerPackMessage = null;
        Message.prototype.statusMentionMessage = null;
        Message.prototype.pollResultSnapshotMessage = null;
        Message.prototype.pollCreationOptionImageMessage = null;
        Message.prototype.associatedChildMessage = null;
        Message.prototype.groupStatusMentionMessage = null;
        Message.prototype.pollCreationMessageV4 = null;
        Message.prototype.statusAddYours = null;
        Message.prototype.groupStatusMessage = null;
        Message.prototype.richResponseMessage = null;
        Message.prototype.statusNotificationMessage = null;
        Message.prototype.limitSharingMessage = null;
        Message.prototype.botTaskMessage = null;
        Message.prototype.questionMessage = null;
        Message.prototype.messageHistoryNotice = null;
        Message.prototype.groupStatusMessageV2 = null;
        Message.prototype.botForwardedMessage = null;
        Message.prototype.statusQuestionAnswerMessage = null;
        Message.prototype.questionReplyMessage = null;
        Message.prototype.questionResponseMessage = null;
        Message.prototype.statusQuotedMessage = null;
        Message.prototype.statusStickerInteractionMessage = null;
        Message.prototype.pollCreationMessageV5 = null;
        Message.prototype.newsletterFollowerInviteMessageV2 = null;
        Message.prototype.pollResultSnapshotMessageV3 = null;
        Message.prototype.newsletterAdminProfileMessage = null;
        Message.prototype.spoilerMessage = null;
        Message.prototype.pollCreationMessageV6 = null;
        Message.prototype.conditionalRevealMessage = null;
        Message.prototype.pollAddOptionMessage = null;
        Message.prototype.eventInviteMessage = null;
        Message.prototype.groupRootKeyShare = null;
        Message.prototype.paymentReminderMessage = null;
        Message.prototype.splitPaymentMessage = null;
        Message.prototype.newsletterAdminProfileStatusMessage = null;
        Message.prototype.rootSecretDistributeMessage = null;
        Message.prototype.splitPaymentUpdateMessage = null;
        Message.prototype.musicMessage = null;
        Message.prototype.statusLinkPreviewMetadata = null;
        Message.prototype.botPlatformRegistrationSuccessMessage = null;
        Message.prototype.newsletterScheduledMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_conversation", {
            get: $util.oneOfGetter($oneOfFields = ["conversation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_senderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_imageMessage", {
            get: $util.oneOfGetter($oneOfFields = ["imageMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_contactMessage", {
            get: $util.oneOfGetter($oneOfFields = ["contactMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_locationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["locationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_extendedTextMessage", {
            get: $util.oneOfGetter($oneOfFields = ["extendedTextMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_documentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["documentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_audioMessage", {
            get: $util.oneOfGetter($oneOfFields = ["audioMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_videoMessage", {
            get: $util.oneOfGetter($oneOfFields = ["videoMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_call", {
            get: $util.oneOfGetter($oneOfFields = ["call"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_chat", {
            get: $util.oneOfGetter($oneOfFields = ["chat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_protocolMessage", {
            get: $util.oneOfGetter($oneOfFields = ["protocolMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_contactsArrayMessage", {
            get: $util.oneOfGetter($oneOfFields = ["contactsArrayMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_highlyStructuredMessage", {
            get: $util.oneOfGetter($oneOfFields = ["highlyStructuredMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_fastRatchetKeySenderKeyDistributionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["fastRatchetKeySenderKeyDistributionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_sendPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["sendPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_liveLocationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_requestPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["requestPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_declinePaymentRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["declinePaymentRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_cancelPaymentRequestMessage", {
            get: $util.oneOfGetter($oneOfFields = ["cancelPaymentRequestMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_templateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_stickerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_groupInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_templateButtonReplyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateButtonReplyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_productMessage", {
            get: $util.oneOfGetter($oneOfFields = ["productMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_deviceSentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["deviceSentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_messageContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_listMessage", {
            get: $util.oneOfGetter($oneOfFields = ["listMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_viewOnceMessage", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_orderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["orderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_listResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["listResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_ephemeralMessage", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_invoiceMessage", {
            get: $util.oneOfGetter($oneOfFields = ["invoiceMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_buttonsMessage", {
            get: $util.oneOfGetter($oneOfFields = ["buttonsMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_buttonsResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["buttonsResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_paymentInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_interactiveMessage", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_reactionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["reactionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_stickerSyncRmrMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerSyncRmrMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_interactiveResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollUpdateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_keepInChatMessage", {
            get: $util.oneOfGetter($oneOfFields = ["keepInChatMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_documentWithCaptionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["documentWithCaptionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_requestPhoneNumberMessage", {
            get: $util.oneOfGetter($oneOfFields = ["requestPhoneNumberMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_viewOnceMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_encReactionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_editedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["editedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_viewOnceMessageV2Extension", {
            get: $util.oneOfGetter($oneOfFields = ["viewOnceMessageV2Extension"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_scheduledCallCreationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallCreationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_groupMentionedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupMentionedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pinInChatMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pinInChatMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationMessageV3", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_scheduledCallEditMessage", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledCallEditMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_ptvMessage", {
            get: $util.oneOfGetter($oneOfFields = ["ptvMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_botInvokeMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botInvokeMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_callLogMesssage", {
            get: $util.oneOfGetter($oneOfFields = ["callLogMesssage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_messageHistoryBundle", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryBundle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_encCommentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encCommentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_bcallMessage", {
            get: $util.oneOfGetter($oneOfFields = ["bcallMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_lottieStickerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["lottieStickerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_eventMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_encEventResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["encEventResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_commentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["commentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_newsletterAdminInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_placeholderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_secretEncryptedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["secretEncryptedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_albumMessage", {
            get: $util.oneOfGetter($oneOfFields = ["albumMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_eventCoverImage", {
            get: $util.oneOfGetter($oneOfFields = ["eventCoverImage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_stickerPackMessage", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusMentionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusMentionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollResultSnapshotMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollResultSnapshotMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationOptionImageMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationOptionImageMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_associatedChildMessage", {
            get: $util.oneOfGetter($oneOfFields = ["associatedChildMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_groupStatusMentionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMentionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationMessageV4", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV4"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusAddYours", {
            get: $util.oneOfGetter($oneOfFields = ["statusAddYours"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_groupStatusMessage", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_richResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["richResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusNotificationMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusNotificationMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_limitSharingMessage", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_botTaskMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botTaskMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_questionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_messageHistoryNotice", {
            get: $util.oneOfGetter($oneOfFields = ["messageHistoryNotice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_groupStatusMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["groupStatusMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_botForwardedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botForwardedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusQuestionAnswerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusQuestionAnswerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_questionReplyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionReplyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_questionResponseMessage", {
            get: $util.oneOfGetter($oneOfFields = ["questionResponseMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusQuotedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusQuotedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusStickerInteractionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["statusStickerInteractionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationMessageV5", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV5"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_newsletterFollowerInviteMessageV2", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterFollowerInviteMessageV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollResultSnapshotMessageV3", {
            get: $util.oneOfGetter($oneOfFields = ["pollResultSnapshotMessageV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_newsletterAdminProfileMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_spoilerMessage", {
            get: $util.oneOfGetter($oneOfFields = ["spoilerMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollCreationMessageV6", {
            get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageV6"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_conditionalRevealMessage", {
            get: $util.oneOfGetter($oneOfFields = ["conditionalRevealMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_pollAddOptionMessage", {
            get: $util.oneOfGetter($oneOfFields = ["pollAddOptionMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_eventInviteMessage", {
            get: $util.oneOfGetter($oneOfFields = ["eventInviteMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_groupRootKeyShare", {
            get: $util.oneOfGetter($oneOfFields = ["groupRootKeyShare"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_paymentReminderMessage", {
            get: $util.oneOfGetter($oneOfFields = ["paymentReminderMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_splitPaymentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["splitPaymentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_newsletterAdminProfileStatusMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterAdminProfileStatusMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_rootSecretDistributeMessage", {
            get: $util.oneOfGetter($oneOfFields = ["rootSecretDistributeMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_splitPaymentUpdateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["splitPaymentUpdateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_musicMessage", {
            get: $util.oneOfGetter($oneOfFields = ["musicMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_statusLinkPreviewMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["statusLinkPreviewMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_botPlatformRegistrationSuccessMessage", {
            get: $util.oneOfGetter($oneOfFields = ["botPlatformRegistrationSuccessMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Message.prototype, "_newsletterScheduledMessage", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterScheduledMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Message.create = function(properties) {
            return new Message(properties);
        };

        Message.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.conversation != null && $Object.hasOwnProperty.call(m, "conversation"))
                w.uint32(10).string(m.conversation);
            if (m.senderKeyDistributionMessage != null && $Object.hasOwnProperty.call(m, "senderKeyDistributionMessage"))
                $root.proto.Message.SenderKeyDistributionMessage.encode(m.senderKeyDistributionMessage, w.uint32(18).fork(), q + 1).ldelim();
            if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                $root.proto.Message.ImageMessage.encode(m.imageMessage, w.uint32(26).fork(), q + 1).ldelim();
            if (m.contactMessage != null && $Object.hasOwnProperty.call(m, "contactMessage"))
                $root.proto.Message.ContactMessage.encode(m.contactMessage, w.uint32(34).fork(), q + 1).ldelim();
            if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage"))
                $root.proto.Message.LocationMessage.encode(m.locationMessage, w.uint32(42).fork(), q + 1).ldelim();
            if (m.extendedTextMessage != null && $Object.hasOwnProperty.call(m, "extendedTextMessage"))
                $root.proto.Message.ExtendedTextMessage.encode(m.extendedTextMessage, w.uint32(50).fork(), q + 1).ldelim();
            if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage"))
                $root.proto.Message.DocumentMessage.encode(m.documentMessage, w.uint32(58).fork(), q + 1).ldelim();
            if (m.audioMessage != null && $Object.hasOwnProperty.call(m, "audioMessage"))
                $root.proto.Message.AudioMessage.encode(m.audioMessage, w.uint32(66).fork(), q + 1).ldelim();
            if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                $root.proto.Message.VideoMessage.encode(m.videoMessage, w.uint32(74).fork(), q + 1).ldelim();
            if (m.call != null && $Object.hasOwnProperty.call(m, "call"))
                $root.proto.Message.Call.encode(m.call, w.uint32(82).fork(), q + 1).ldelim();
            if (m.chat != null && $Object.hasOwnProperty.call(m, "chat"))
                $root.proto.Message.Chat.encode(m.chat, w.uint32(90).fork(), q + 1).ldelim();
            if (m.protocolMessage != null && $Object.hasOwnProperty.call(m, "protocolMessage"))
                $root.proto.Message.ProtocolMessage.encode(m.protocolMessage, w.uint32(98).fork(), q + 1).ldelim();
            if (m.contactsArrayMessage != null && $Object.hasOwnProperty.call(m, "contactsArrayMessage"))
                $root.proto.Message.ContactsArrayMessage.encode(m.contactsArrayMessage, w.uint32(106).fork(), q + 1).ldelim();
            if (m.highlyStructuredMessage != null && $Object.hasOwnProperty.call(m, "highlyStructuredMessage"))
                $root.proto.Message.HighlyStructuredMessage.encode(m.highlyStructuredMessage, w.uint32(114).fork(), q + 1).ldelim();
            if (m.fastRatchetKeySenderKeyDistributionMessage != null && $Object.hasOwnProperty.call(m, "fastRatchetKeySenderKeyDistributionMessage"))
                $root.proto.Message.SenderKeyDistributionMessage.encode(m.fastRatchetKeySenderKeyDistributionMessage, w.uint32(122).fork(), q + 1).ldelim();
            if (m.sendPaymentMessage != null && $Object.hasOwnProperty.call(m, "sendPaymentMessage"))
                $root.proto.Message.SendPaymentMessage.encode(m.sendPaymentMessage, w.uint32(130).fork(), q + 1).ldelim();
            if (m.liveLocationMessage != null && $Object.hasOwnProperty.call(m, "liveLocationMessage"))
                $root.proto.Message.LiveLocationMessage.encode(m.liveLocationMessage, w.uint32(146).fork(), q + 1).ldelim();
            if (m.requestPaymentMessage != null && $Object.hasOwnProperty.call(m, "requestPaymentMessage"))
                $root.proto.Message.RequestPaymentMessage.encode(m.requestPaymentMessage, w.uint32(178).fork(), q + 1).ldelim();
            if (m.declinePaymentRequestMessage != null && $Object.hasOwnProperty.call(m, "declinePaymentRequestMessage"))
                $root.proto.Message.DeclinePaymentRequestMessage.encode(m.declinePaymentRequestMessage, w.uint32(186).fork(), q + 1).ldelim();
            if (m.cancelPaymentRequestMessage != null && $Object.hasOwnProperty.call(m, "cancelPaymentRequestMessage"))
                $root.proto.Message.CancelPaymentRequestMessage.encode(m.cancelPaymentRequestMessage, w.uint32(194).fork(), q + 1).ldelim();
            if (m.templateMessage != null && $Object.hasOwnProperty.call(m, "templateMessage"))
                $root.proto.Message.TemplateMessage.encode(m.templateMessage, w.uint32(202).fork(), q + 1).ldelim();
            if (m.stickerMessage != null && $Object.hasOwnProperty.call(m, "stickerMessage"))
                $root.proto.Message.StickerMessage.encode(m.stickerMessage, w.uint32(210).fork(), q + 1).ldelim();
            if (m.groupInviteMessage != null && $Object.hasOwnProperty.call(m, "groupInviteMessage"))
                $root.proto.Message.GroupInviteMessage.encode(m.groupInviteMessage, w.uint32(226).fork(), q + 1).ldelim();
            if (m.templateButtonReplyMessage != null && $Object.hasOwnProperty.call(m, "templateButtonReplyMessage"))
                $root.proto.Message.TemplateButtonReplyMessage.encode(m.templateButtonReplyMessage, w.uint32(234).fork(), q + 1).ldelim();
            if (m.productMessage != null && $Object.hasOwnProperty.call(m, "productMessage"))
                $root.proto.Message.ProductMessage.encode(m.productMessage, w.uint32(242).fork(), q + 1).ldelim();
            if (m.deviceSentMessage != null && $Object.hasOwnProperty.call(m, "deviceSentMessage"))
                $root.proto.Message.DeviceSentMessage.encode(m.deviceSentMessage, w.uint32(250).fork(), q + 1).ldelim();
            if (m.messageContextInfo != null && $Object.hasOwnProperty.call(m, "messageContextInfo"))
                $root.proto.MessageContextInfo.encode(m.messageContextInfo, w.uint32(282).fork(), q + 1).ldelim();
            if (m.listMessage != null && $Object.hasOwnProperty.call(m, "listMessage"))
                $root.proto.Message.ListMessage.encode(m.listMessage, w.uint32(290).fork(), q + 1).ldelim();
            if (m.viewOnceMessage != null && $Object.hasOwnProperty.call(m, "viewOnceMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.viewOnceMessage, w.uint32(298).fork(), q + 1).ldelim();
            if (m.orderMessage != null && $Object.hasOwnProperty.call(m, "orderMessage"))
                $root.proto.Message.OrderMessage.encode(m.orderMessage, w.uint32(306).fork(), q + 1).ldelim();
            if (m.listResponseMessage != null && $Object.hasOwnProperty.call(m, "listResponseMessage"))
                $root.proto.Message.ListResponseMessage.encode(m.listResponseMessage, w.uint32(314).fork(), q + 1).ldelim();
            if (m.ephemeralMessage != null && $Object.hasOwnProperty.call(m, "ephemeralMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.ephemeralMessage, w.uint32(322).fork(), q + 1).ldelim();
            if (m.invoiceMessage != null && $Object.hasOwnProperty.call(m, "invoiceMessage"))
                $root.proto.Message.InvoiceMessage.encode(m.invoiceMessage, w.uint32(330).fork(), q + 1).ldelim();
            if (m.buttonsMessage != null && $Object.hasOwnProperty.call(m, "buttonsMessage"))
                $root.proto.Message.ButtonsMessage.encode(m.buttonsMessage, w.uint32(338).fork(), q + 1).ldelim();
            if (m.buttonsResponseMessage != null && $Object.hasOwnProperty.call(m, "buttonsResponseMessage"))
                $root.proto.Message.ButtonsResponseMessage.encode(m.buttonsResponseMessage, w.uint32(346).fork(), q + 1).ldelim();
            if (m.paymentInviteMessage != null && $Object.hasOwnProperty.call(m, "paymentInviteMessage"))
                $root.proto.Message.PaymentInviteMessage.encode(m.paymentInviteMessage, w.uint32(354).fork(), q + 1).ldelim();
            if (m.interactiveMessage != null && $Object.hasOwnProperty.call(m, "interactiveMessage"))
                $root.proto.Message.InteractiveMessage.encode(m.interactiveMessage, w.uint32(362).fork(), q + 1).ldelim();
            if (m.reactionMessage != null && $Object.hasOwnProperty.call(m, "reactionMessage"))
                $root.proto.Message.ReactionMessage.encode(m.reactionMessage, w.uint32(370).fork(), q + 1).ldelim();
            if (m.stickerSyncRmrMessage != null && $Object.hasOwnProperty.call(m, "stickerSyncRmrMessage"))
                $root.proto.Message.StickerSyncRMRMessage.encode(m.stickerSyncRmrMessage, w.uint32(378).fork(), q + 1).ldelim();
            if (m.interactiveResponseMessage != null && $Object.hasOwnProperty.call(m, "interactiveResponseMessage"))
                $root.proto.Message.InteractiveResponseMessage.encode(m.interactiveResponseMessage, w.uint32(386).fork(), q + 1).ldelim();
            if (m.pollCreationMessage != null && $Object.hasOwnProperty.call(m, "pollCreationMessage"))
                $root.proto.Message.PollCreationMessage.encode(m.pollCreationMessage, w.uint32(394).fork(), q + 1).ldelim();
            if (m.pollUpdateMessage != null && $Object.hasOwnProperty.call(m, "pollUpdateMessage"))
                $root.proto.Message.PollUpdateMessage.encode(m.pollUpdateMessage, w.uint32(402).fork(), q + 1).ldelim();
            if (m.keepInChatMessage != null && $Object.hasOwnProperty.call(m, "keepInChatMessage"))
                $root.proto.Message.KeepInChatMessage.encode(m.keepInChatMessage, w.uint32(410).fork(), q + 1).ldelim();
            if (m.documentWithCaptionMessage != null && $Object.hasOwnProperty.call(m, "documentWithCaptionMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.documentWithCaptionMessage, w.uint32(426).fork(), q + 1).ldelim();
            if (m.requestPhoneNumberMessage != null && $Object.hasOwnProperty.call(m, "requestPhoneNumberMessage"))
                $root.proto.Message.RequestPhoneNumberMessage.encode(m.requestPhoneNumberMessage, w.uint32(434).fork(), q + 1).ldelim();
            if (m.viewOnceMessageV2 != null && $Object.hasOwnProperty.call(m, "viewOnceMessageV2"))
                $root.proto.Message.FutureProofMessage.encode(m.viewOnceMessageV2, w.uint32(442).fork(), q + 1).ldelim();
            if (m.encReactionMessage != null && $Object.hasOwnProperty.call(m, "encReactionMessage"))
                $root.proto.Message.EncReactionMessage.encode(m.encReactionMessage, w.uint32(450).fork(), q + 1).ldelim();
            if (m.editedMessage != null && $Object.hasOwnProperty.call(m, "editedMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.editedMessage, w.uint32(466).fork(), q + 1).ldelim();
            if (m.viewOnceMessageV2Extension != null && $Object.hasOwnProperty.call(m, "viewOnceMessageV2Extension"))
                $root.proto.Message.FutureProofMessage.encode(m.viewOnceMessageV2Extension, w.uint32(474).fork(), q + 1).ldelim();
            if (m.pollCreationMessageV2 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV2"))
                $root.proto.Message.PollCreationMessage.encode(m.pollCreationMessageV2, w.uint32(482).fork(), q + 1).ldelim();
            if (m.scheduledCallCreationMessage != null && $Object.hasOwnProperty.call(m, "scheduledCallCreationMessage"))
                $root.proto.Message.ScheduledCallCreationMessage.encode(m.scheduledCallCreationMessage, w.uint32(490).fork(), q + 1).ldelim();
            if (m.groupMentionedMessage != null && $Object.hasOwnProperty.call(m, "groupMentionedMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.groupMentionedMessage, w.uint32(498).fork(), q + 1).ldelim();
            if (m.pinInChatMessage != null && $Object.hasOwnProperty.call(m, "pinInChatMessage"))
                $root.proto.Message.PinInChatMessage.encode(m.pinInChatMessage, w.uint32(506).fork(), q + 1).ldelim();
            if (m.pollCreationMessageV3 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV3"))
                $root.proto.Message.PollCreationMessage.encode(m.pollCreationMessageV3, w.uint32(514).fork(), q + 1).ldelim();
            if (m.scheduledCallEditMessage != null && $Object.hasOwnProperty.call(m, "scheduledCallEditMessage"))
                $root.proto.Message.ScheduledCallEditMessage.encode(m.scheduledCallEditMessage, w.uint32(522).fork(), q + 1).ldelim();
            if (m.ptvMessage != null && $Object.hasOwnProperty.call(m, "ptvMessage"))
                $root.proto.Message.VideoMessage.encode(m.ptvMessage, w.uint32(530).fork(), q + 1).ldelim();
            if (m.botInvokeMessage != null && $Object.hasOwnProperty.call(m, "botInvokeMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.botInvokeMessage, w.uint32(538).fork(), q + 1).ldelim();
            if (m.callLogMesssage != null && $Object.hasOwnProperty.call(m, "callLogMesssage"))
                $root.proto.Message.CallLogMessage.encode(m.callLogMesssage, w.uint32(554).fork(), q + 1).ldelim();
            if (m.messageHistoryBundle != null && $Object.hasOwnProperty.call(m, "messageHistoryBundle"))
                $root.proto.Message.MessageHistoryBundle.encode(m.messageHistoryBundle, w.uint32(562).fork(), q + 1).ldelim();
            if (m.encCommentMessage != null && $Object.hasOwnProperty.call(m, "encCommentMessage"))
                $root.proto.Message.EncCommentMessage.encode(m.encCommentMessage, w.uint32(570).fork(), q + 1).ldelim();
            if (m.bcallMessage != null && $Object.hasOwnProperty.call(m, "bcallMessage"))
                $root.proto.Message.BCallMessage.encode(m.bcallMessage, w.uint32(578).fork(), q + 1).ldelim();
            if (m.lottieStickerMessage != null && $Object.hasOwnProperty.call(m, "lottieStickerMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.lottieStickerMessage, w.uint32(594).fork(), q + 1).ldelim();
            if (m.eventMessage != null && $Object.hasOwnProperty.call(m, "eventMessage"))
                $root.proto.Message.EventMessage.encode(m.eventMessage, w.uint32(602).fork(), q + 1).ldelim();
            if (m.encEventResponseMessage != null && $Object.hasOwnProperty.call(m, "encEventResponseMessage"))
                $root.proto.Message.EncEventResponseMessage.encode(m.encEventResponseMessage, w.uint32(610).fork(), q + 1).ldelim();
            if (m.commentMessage != null && $Object.hasOwnProperty.call(m, "commentMessage"))
                $root.proto.Message.CommentMessage.encode(m.commentMessage, w.uint32(618).fork(), q + 1).ldelim();
            if (m.newsletterAdminInviteMessage != null && $Object.hasOwnProperty.call(m, "newsletterAdminInviteMessage"))
                $root.proto.Message.NewsletterAdminInviteMessage.encode(m.newsletterAdminInviteMessage, w.uint32(626).fork(), q + 1).ldelim();
            if (m.placeholderMessage != null && $Object.hasOwnProperty.call(m, "placeholderMessage"))
                $root.proto.Message.PlaceholderMessage.encode(m.placeholderMessage, w.uint32(642).fork(), q + 1).ldelim();
            if (m.secretEncryptedMessage != null && $Object.hasOwnProperty.call(m, "secretEncryptedMessage"))
                $root.proto.Message.SecretEncryptedMessage.encode(m.secretEncryptedMessage, w.uint32(658).fork(), q + 1).ldelim();
            if (m.albumMessage != null && $Object.hasOwnProperty.call(m, "albumMessage"))
                $root.proto.Message.AlbumMessage.encode(m.albumMessage, w.uint32(666).fork(), q + 1).ldelim();
            if (m.eventCoverImage != null && $Object.hasOwnProperty.call(m, "eventCoverImage"))
                $root.proto.Message.FutureProofMessage.encode(m.eventCoverImage, w.uint32(682).fork(), q + 1).ldelim();
            if (m.stickerPackMessage != null && $Object.hasOwnProperty.call(m, "stickerPackMessage"))
                $root.proto.Message.StickerPackMessage.encode(m.stickerPackMessage, w.uint32(690).fork(), q + 1).ldelim();
            if (m.statusMentionMessage != null && $Object.hasOwnProperty.call(m, "statusMentionMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.statusMentionMessage, w.uint32(698).fork(), q + 1).ldelim();
            if (m.pollResultSnapshotMessage != null && $Object.hasOwnProperty.call(m, "pollResultSnapshotMessage"))
                $root.proto.Message.PollResultSnapshotMessage.encode(m.pollResultSnapshotMessage, w.uint32(706).fork(), q + 1).ldelim();
            if (m.pollCreationOptionImageMessage != null && $Object.hasOwnProperty.call(m, "pollCreationOptionImageMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.pollCreationOptionImageMessage, w.uint32(722).fork(), q + 1).ldelim();
            if (m.associatedChildMessage != null && $Object.hasOwnProperty.call(m, "associatedChildMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.associatedChildMessage, w.uint32(730).fork(), q + 1).ldelim();
            if (m.groupStatusMentionMessage != null && $Object.hasOwnProperty.call(m, "groupStatusMentionMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.groupStatusMentionMessage, w.uint32(738).fork(), q + 1).ldelim();
            if (m.pollCreationMessageV4 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV4"))
                $root.proto.Message.FutureProofMessage.encode(m.pollCreationMessageV4, w.uint32(746).fork(), q + 1).ldelim();
            if (m.statusAddYours != null && $Object.hasOwnProperty.call(m, "statusAddYours"))
                $root.proto.Message.FutureProofMessage.encode(m.statusAddYours, w.uint32(762).fork(), q + 1).ldelim();
            if (m.groupStatusMessage != null && $Object.hasOwnProperty.call(m, "groupStatusMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.groupStatusMessage, w.uint32(770).fork(), q + 1).ldelim();
            if (m.richResponseMessage != null && $Object.hasOwnProperty.call(m, "richResponseMessage"))
                $root.proto.AIRichResponseMessage.encode(m.richResponseMessage, w.uint32(778).fork(), q + 1).ldelim();
            if (m.statusNotificationMessage != null && $Object.hasOwnProperty.call(m, "statusNotificationMessage"))
                $root.proto.Message.StatusNotificationMessage.encode(m.statusNotificationMessage, w.uint32(786).fork(), q + 1).ldelim();
            if (m.limitSharingMessage != null && $Object.hasOwnProperty.call(m, "limitSharingMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.limitSharingMessage, w.uint32(794).fork(), q + 1).ldelim();
            if (m.botTaskMessage != null && $Object.hasOwnProperty.call(m, "botTaskMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.botTaskMessage, w.uint32(802).fork(), q + 1).ldelim();
            if (m.questionMessage != null && $Object.hasOwnProperty.call(m, "questionMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.questionMessage, w.uint32(810).fork(), q + 1).ldelim();
            if (m.messageHistoryNotice != null && $Object.hasOwnProperty.call(m, "messageHistoryNotice"))
                $root.proto.Message.MessageHistoryNotice.encode(m.messageHistoryNotice, w.uint32(818).fork(), q + 1).ldelim();
            if (m.groupStatusMessageV2 != null && $Object.hasOwnProperty.call(m, "groupStatusMessageV2"))
                $root.proto.Message.FutureProofMessage.encode(m.groupStatusMessageV2, w.uint32(826).fork(), q + 1).ldelim();
            if (m.botForwardedMessage != null && $Object.hasOwnProperty.call(m, "botForwardedMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.botForwardedMessage, w.uint32(834).fork(), q + 1).ldelim();
            if (m.statusQuestionAnswerMessage != null && $Object.hasOwnProperty.call(m, "statusQuestionAnswerMessage"))
                $root.proto.Message.StatusQuestionAnswerMessage.encode(m.statusQuestionAnswerMessage, w.uint32(842).fork(), q + 1).ldelim();
            if (m.questionReplyMessage != null && $Object.hasOwnProperty.call(m, "questionReplyMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.questionReplyMessage, w.uint32(850).fork(), q + 1).ldelim();
            if (m.questionResponseMessage != null && $Object.hasOwnProperty.call(m, "questionResponseMessage"))
                $root.proto.Message.QuestionResponseMessage.encode(m.questionResponseMessage, w.uint32(858).fork(), q + 1).ldelim();
            if (m.statusQuotedMessage != null && $Object.hasOwnProperty.call(m, "statusQuotedMessage"))
                $root.proto.Message.StatusQuotedMessage.encode(m.statusQuotedMessage, w.uint32(874).fork(), q + 1).ldelim();
            if (m.statusStickerInteractionMessage != null && $Object.hasOwnProperty.call(m, "statusStickerInteractionMessage"))
                $root.proto.Message.StatusStickerInteractionMessage.encode(m.statusStickerInteractionMessage, w.uint32(882).fork(), q + 1).ldelim();
            if (m.pollCreationMessageV5 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV5"))
                $root.proto.Message.PollCreationMessage.encode(m.pollCreationMessageV5, w.uint32(890).fork(), q + 1).ldelim();
            if (m.newsletterFollowerInviteMessageV2 != null && $Object.hasOwnProperty.call(m, "newsletterFollowerInviteMessageV2"))
                $root.proto.Message.NewsletterFollowerInviteMessage.encode(m.newsletterFollowerInviteMessageV2, w.uint32(906).fork(), q + 1).ldelim();
            if (m.pollResultSnapshotMessageV3 != null && $Object.hasOwnProperty.call(m, "pollResultSnapshotMessageV3"))
                $root.proto.Message.PollResultSnapshotMessage.encode(m.pollResultSnapshotMessageV3, w.uint32(922).fork(), q + 1).ldelim();
            if (m.newsletterAdminProfileMessage != null && $Object.hasOwnProperty.call(m, "newsletterAdminProfileMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.newsletterAdminProfileMessage, w.uint32(930).fork(), q + 1).ldelim();
            if (m.spoilerMessage != null && $Object.hasOwnProperty.call(m, "spoilerMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.spoilerMessage, w.uint32(946).fork(), q + 1).ldelim();
            if (m.pollCreationMessageV6 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV6"))
                $root.proto.Message.PollCreationMessage.encode(m.pollCreationMessageV6, w.uint32(954).fork(), q + 1).ldelim();
            if (m.conditionalRevealMessage != null && $Object.hasOwnProperty.call(m, "conditionalRevealMessage"))
                $root.proto.Message.ConditionalRevealMessage.encode(m.conditionalRevealMessage, w.uint32(962).fork(), q + 1).ldelim();
            if (m.pollAddOptionMessage != null && $Object.hasOwnProperty.call(m, "pollAddOptionMessage"))
                $root.proto.Message.PollAddOptionMessage.encode(m.pollAddOptionMessage, w.uint32(970).fork(), q + 1).ldelim();
            if (m.eventInviteMessage != null && $Object.hasOwnProperty.call(m, "eventInviteMessage"))
                $root.proto.Message.EventInviteMessage.encode(m.eventInviteMessage, w.uint32(978).fork(), q + 1).ldelim();
            if (m.groupRootKeyShare != null && $Object.hasOwnProperty.call(m, "groupRootKeyShare"))
                $root.proto.GroupRootKeyShare.encode(m.groupRootKeyShare, w.uint32(986).fork(), q + 1).ldelim();
            if (m.paymentReminderMessage != null && $Object.hasOwnProperty.call(m, "paymentReminderMessage"))
                $root.proto.Message.PaymentReminderMessage.encode(m.paymentReminderMessage, w.uint32(994).fork(), q + 1).ldelim();
            if (m.splitPaymentMessage != null && $Object.hasOwnProperty.call(m, "splitPaymentMessage"))
                $root.proto.Message.SplitPaymentMessage.encode(m.splitPaymentMessage, w.uint32(1002).fork(), q + 1).ldelim();
            if (m.newsletterAdminProfileStatusMessage != null && $Object.hasOwnProperty.call(m, "newsletterAdminProfileStatusMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.newsletterAdminProfileStatusMessage, w.uint32(1010).fork(), q + 1).ldelim();
            if (m.rootSecretDistributeMessage != null && $Object.hasOwnProperty.call(m, "rootSecretDistributeMessage"))
                $root.proto.Message.RootSecretDistributeMessage.encode(m.rootSecretDistributeMessage, w.uint32(1018).fork(), q + 1).ldelim();
            if (m.splitPaymentUpdateMessage != null && $Object.hasOwnProperty.call(m, "splitPaymentUpdateMessage"))
                $root.proto.Message.SplitPaymentUpdateMessage.encode(m.splitPaymentUpdateMessage, w.uint32(1026).fork(), q + 1).ldelim();
            if (m.musicMessage != null && $Object.hasOwnProperty.call(m, "musicMessage"))
                $root.proto.Message.MusicMessage.encode(m.musicMessage, w.uint32(1034).fork(), q + 1).ldelim();
            if (m.statusLinkPreviewMetadata != null && $Object.hasOwnProperty.call(m, "statusLinkPreviewMetadata"))
                $root.proto.Message.StatusLinkPreviewMetadata.encode(m.statusLinkPreviewMetadata, w.uint32(1042).fork(), q + 1).ldelim();
            if (m.botPlatformRegistrationSuccessMessage != null && $Object.hasOwnProperty.call(m, "botPlatformRegistrationSuccessMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.botPlatformRegistrationSuccessMessage, w.uint32(1050).fork(), q + 1).ldelim();
            if (m.newsletterScheduledMessage != null && $Object.hasOwnProperty.call(m, "newsletterScheduledMessage"))
                $root.proto.Message.FutureProofMessage.encode(m.newsletterScheduledMessage, w.uint32(1058).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Message.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.conversation = r.stringVerify();
                        m._conversation = "conversation";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.senderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.decode(r, r.uint32(), $undefined, q + 1, m.senderKeyDistributionMessage);
                        m._senderKeyDistributionMessage = "senderKeyDistributionMessage";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.imageMessage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                        m._imageMessage = "imageMessage";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.contactMessage = $root.proto.Message.ContactMessage.decode(r, r.uint32(), $undefined, q + 1, m.contactMessage);
                        m._contactMessage = "contactMessage";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.locationMessage = $root.proto.Message.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.locationMessage);
                        m._locationMessage = "locationMessage";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.extendedTextMessage = $root.proto.Message.ExtendedTextMessage.decode(r, r.uint32(), $undefined, q + 1, m.extendedTextMessage);
                        m._extendedTextMessage = "extendedTextMessage";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.documentMessage = $root.proto.Message.DocumentMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentMessage);
                        m._documentMessage = "documentMessage";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.audioMessage = $root.proto.Message.AudioMessage.decode(r, r.uint32(), $undefined, q + 1, m.audioMessage);
                        m._audioMessage = "audioMessage";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.videoMessage = $root.proto.Message.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                        m._videoMessage = "videoMessage";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.call = $root.proto.Message.Call.decode(r, r.uint32(), $undefined, q + 1, m.call);
                        m._call = "call";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.chat = $root.proto.Message.Chat.decode(r, r.uint32(), $undefined, q + 1, m.chat);
                        m._chat = "chat";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.protocolMessage = $root.proto.Message.ProtocolMessage.decode(r, r.uint32(), $undefined, q + 1, m.protocolMessage);
                        m._protocolMessage = "protocolMessage";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.contactsArrayMessage = $root.proto.Message.ContactsArrayMessage.decode(r, r.uint32(), $undefined, q + 1, m.contactsArrayMessage);
                        m._contactsArrayMessage = "contactsArrayMessage";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.highlyStructuredMessage);
                        m._highlyStructuredMessage = "highlyStructuredMessage";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.fastRatchetKeySenderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.decode(r, r.uint32(), $undefined, q + 1, m.fastRatchetKeySenderKeyDistributionMessage);
                        m._fastRatchetKeySenderKeyDistributionMessage = "fastRatchetKeySenderKeyDistributionMessage";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        m.sendPaymentMessage = $root.proto.Message.SendPaymentMessage.decode(r, r.uint32(), $undefined, q + 1, m.sendPaymentMessage);
                        m._sendPaymentMessage = "sendPaymentMessage";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        m.liveLocationMessage = $root.proto.Message.LiveLocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.liveLocationMessage);
                        m._liveLocationMessage = "liveLocationMessage";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        m.requestPaymentMessage = $root.proto.Message.RequestPaymentMessage.decode(r, r.uint32(), $undefined, q + 1, m.requestPaymentMessage);
                        m._requestPaymentMessage = "requestPaymentMessage";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        m.declinePaymentRequestMessage = $root.proto.Message.DeclinePaymentRequestMessage.decode(r, r.uint32(), $undefined, q + 1, m.declinePaymentRequestMessage);
                        m._declinePaymentRequestMessage = "declinePaymentRequestMessage";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        m.cancelPaymentRequestMessage = $root.proto.Message.CancelPaymentRequestMessage.decode(r, r.uint32(), $undefined, q + 1, m.cancelPaymentRequestMessage);
                        m._cancelPaymentRequestMessage = "cancelPaymentRequestMessage";
                        continue;
                    }
                case 25: {
                        if (u !== 2)
                            break;
                        m.templateMessage = $root.proto.Message.TemplateMessage.decode(r, r.uint32(), $undefined, q + 1, m.templateMessage);
                        m._templateMessage = "templateMessage";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        m.stickerMessage = $root.proto.Message.StickerMessage.decode(r, r.uint32(), $undefined, q + 1, m.stickerMessage);
                        m._stickerMessage = "stickerMessage";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        m.groupInviteMessage = $root.proto.Message.GroupInviteMessage.decode(r, r.uint32(), $undefined, q + 1, m.groupInviteMessage);
                        m._groupInviteMessage = "groupInviteMessage";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        m.templateButtonReplyMessage = $root.proto.Message.TemplateButtonReplyMessage.decode(r, r.uint32(), $undefined, q + 1, m.templateButtonReplyMessage);
                        m._templateButtonReplyMessage = "templateButtonReplyMessage";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        m.productMessage = $root.proto.Message.ProductMessage.decode(r, r.uint32(), $undefined, q + 1, m.productMessage);
                        m._productMessage = "productMessage";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        m.deviceSentMessage = $root.proto.Message.DeviceSentMessage.decode(r, r.uint32(), $undefined, q + 1, m.deviceSentMessage);
                        m._deviceSentMessage = "deviceSentMessage";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        m.messageContextInfo = $root.proto.MessageContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.messageContextInfo);
                        m._messageContextInfo = "messageContextInfo";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        m.listMessage = $root.proto.Message.ListMessage.decode(r, r.uint32(), $undefined, q + 1, m.listMessage);
                        m._listMessage = "listMessage";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        m.viewOnceMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.viewOnceMessage);
                        m._viewOnceMessage = "viewOnceMessage";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.orderMessage = $root.proto.Message.OrderMessage.decode(r, r.uint32(), $undefined, q + 1, m.orderMessage);
                        m._orderMessage = "orderMessage";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        m.listResponseMessage = $root.proto.Message.ListResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.listResponseMessage);
                        m._listResponseMessage = "listResponseMessage";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        m.ephemeralMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.ephemeralMessage);
                        m._ephemeralMessage = "ephemeralMessage";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        m.invoiceMessage = $root.proto.Message.InvoiceMessage.decode(r, r.uint32(), $undefined, q + 1, m.invoiceMessage);
                        m._invoiceMessage = "invoiceMessage";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        m.buttonsMessage = $root.proto.Message.ButtonsMessage.decode(r, r.uint32(), $undefined, q + 1, m.buttonsMessage);
                        m._buttonsMessage = "buttonsMessage";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.buttonsResponseMessage = $root.proto.Message.ButtonsResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.buttonsResponseMessage);
                        m._buttonsResponseMessage = "buttonsResponseMessage";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        m.paymentInviteMessage = $root.proto.Message.PaymentInviteMessage.decode(r, r.uint32(), $undefined, q + 1, m.paymentInviteMessage);
                        m._paymentInviteMessage = "paymentInviteMessage";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        m.interactiveMessage = $root.proto.Message.InteractiveMessage.decode(r, r.uint32(), $undefined, q + 1, m.interactiveMessage);
                        m._interactiveMessage = "interactiveMessage";
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        m.reactionMessage = $root.proto.Message.ReactionMessage.decode(r, r.uint32(), $undefined, q + 1, m.reactionMessage);
                        m._reactionMessage = "reactionMessage";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        m.stickerSyncRmrMessage = $root.proto.Message.StickerSyncRMRMessage.decode(r, r.uint32(), $undefined, q + 1, m.stickerSyncRmrMessage);
                        m._stickerSyncRmrMessage = "stickerSyncRmrMessage";
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        m.interactiveResponseMessage = $root.proto.Message.InteractiveResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.interactiveResponseMessage);
                        m._interactiveResponseMessage = "interactiveResponseMessage";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        m.pollCreationMessage = $root.proto.Message.PollCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessage);
                        m._pollCreationMessage = "pollCreationMessage";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        m.pollUpdateMessage = $root.proto.Message.PollUpdateMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollUpdateMessage);
                        m._pollUpdateMessage = "pollUpdateMessage";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        m.keepInChatMessage = $root.proto.Message.KeepInChatMessage.decode(r, r.uint32(), $undefined, q + 1, m.keepInChatMessage);
                        m._keepInChatMessage = "keepInChatMessage";
                        continue;
                    }
                case 53: {
                        if (u !== 2)
                            break;
                        m.documentWithCaptionMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentWithCaptionMessage);
                        m._documentWithCaptionMessage = "documentWithCaptionMessage";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        m.requestPhoneNumberMessage = $root.proto.Message.RequestPhoneNumberMessage.decode(r, r.uint32(), $undefined, q + 1, m.requestPhoneNumberMessage);
                        m._requestPhoneNumberMessage = "requestPhoneNumberMessage";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        m.viewOnceMessageV2 = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.viewOnceMessageV2);
                        m._viewOnceMessageV2 = "viewOnceMessageV2";
                        continue;
                    }
                case 56: {
                        if (u !== 2)
                            break;
                        m.encReactionMessage = $root.proto.Message.EncReactionMessage.decode(r, r.uint32(), $undefined, q + 1, m.encReactionMessage);
                        m._encReactionMessage = "encReactionMessage";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        m.editedMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.editedMessage);
                        m._editedMessage = "editedMessage";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        m.viewOnceMessageV2Extension = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.viewOnceMessageV2Extension);
                        m._viewOnceMessageV2Extension = "viewOnceMessageV2Extension";
                        continue;
                    }
                case 60: {
                        if (u !== 2)
                            break;
                        m.pollCreationMessageV2 = $root.proto.Message.PollCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageV2);
                        m._pollCreationMessageV2 = "pollCreationMessageV2";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        m.scheduledCallCreationMessage = $root.proto.Message.ScheduledCallCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.scheduledCallCreationMessage);
                        m._scheduledCallCreationMessage = "scheduledCallCreationMessage";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        m.groupMentionedMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.groupMentionedMessage);
                        m._groupMentionedMessage = "groupMentionedMessage";
                        continue;
                    }
                case 63: {
                        if (u !== 2)
                            break;
                        m.pinInChatMessage = $root.proto.Message.PinInChatMessage.decode(r, r.uint32(), $undefined, q + 1, m.pinInChatMessage);
                        m._pinInChatMessage = "pinInChatMessage";
                        continue;
                    }
                case 64: {
                        if (u !== 2)
                            break;
                        m.pollCreationMessageV3 = $root.proto.Message.PollCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageV3);
                        m._pollCreationMessageV3 = "pollCreationMessageV3";
                        continue;
                    }
                case 65: {
                        if (u !== 2)
                            break;
                        m.scheduledCallEditMessage = $root.proto.Message.ScheduledCallEditMessage.decode(r, r.uint32(), $undefined, q + 1, m.scheduledCallEditMessage);
                        m._scheduledCallEditMessage = "scheduledCallEditMessage";
                        continue;
                    }
                case 66: {
                        if (u !== 2)
                            break;
                        m.ptvMessage = $root.proto.Message.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.ptvMessage);
                        m._ptvMessage = "ptvMessage";
                        continue;
                    }
                case 67: {
                        if (u !== 2)
                            break;
                        m.botInvokeMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.botInvokeMessage);
                        m._botInvokeMessage = "botInvokeMessage";
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        m.callLogMesssage = $root.proto.Message.CallLogMessage.decode(r, r.uint32(), $undefined, q + 1, m.callLogMesssage);
                        m._callLogMesssage = "callLogMesssage";
                        continue;
                    }
                case 70: {
                        if (u !== 2)
                            break;
                        m.messageHistoryBundle = $root.proto.Message.MessageHistoryBundle.decode(r, r.uint32(), $undefined, q + 1, m.messageHistoryBundle);
                        m._messageHistoryBundle = "messageHistoryBundle";
                        continue;
                    }
                case 71: {
                        if (u !== 2)
                            break;
                        m.encCommentMessage = $root.proto.Message.EncCommentMessage.decode(r, r.uint32(), $undefined, q + 1, m.encCommentMessage);
                        m._encCommentMessage = "encCommentMessage";
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        m.bcallMessage = $root.proto.Message.BCallMessage.decode(r, r.uint32(), $undefined, q + 1, m.bcallMessage);
                        m._bcallMessage = "bcallMessage";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        m.lottieStickerMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.lottieStickerMessage);
                        m._lottieStickerMessage = "lottieStickerMessage";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        m.eventMessage = $root.proto.Message.EventMessage.decode(r, r.uint32(), $undefined, q + 1, m.eventMessage);
                        m._eventMessage = "eventMessage";
                        continue;
                    }
                case 76: {
                        if (u !== 2)
                            break;
                        m.encEventResponseMessage = $root.proto.Message.EncEventResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.encEventResponseMessage);
                        m._encEventResponseMessage = "encEventResponseMessage";
                        continue;
                    }
                case 77: {
                        if (u !== 2)
                            break;
                        m.commentMessage = $root.proto.Message.CommentMessage.decode(r, r.uint32(), $undefined, q + 1, m.commentMessage);
                        m._commentMessage = "commentMessage";
                        continue;
                    }
                case 78: {
                        if (u !== 2)
                            break;
                        m.newsletterAdminInviteMessage = $root.proto.Message.NewsletterAdminInviteMessage.decode(r, r.uint32(), $undefined, q + 1, m.newsletterAdminInviteMessage);
                        m._newsletterAdminInviteMessage = "newsletterAdminInviteMessage";
                        continue;
                    }
                case 80: {
                        if (u !== 2)
                            break;
                        m.placeholderMessage = $root.proto.Message.PlaceholderMessage.decode(r, r.uint32(), $undefined, q + 1, m.placeholderMessage);
                        m._placeholderMessage = "placeholderMessage";
                        continue;
                    }
                case 82: {
                        if (u !== 2)
                            break;
                        m.secretEncryptedMessage = $root.proto.Message.SecretEncryptedMessage.decode(r, r.uint32(), $undefined, q + 1, m.secretEncryptedMessage);
                        m._secretEncryptedMessage = "secretEncryptedMessage";
                        continue;
                    }
                case 83: {
                        if (u !== 2)
                            break;
                        m.albumMessage = $root.proto.Message.AlbumMessage.decode(r, r.uint32(), $undefined, q + 1, m.albumMessage);
                        m._albumMessage = "albumMessage";
                        continue;
                    }
                case 85: {
                        if (u !== 2)
                            break;
                        m.eventCoverImage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.eventCoverImage);
                        m._eventCoverImage = "eventCoverImage";
                        continue;
                    }
                case 86: {
                        if (u !== 2)
                            break;
                        m.stickerPackMessage = $root.proto.Message.StickerPackMessage.decode(r, r.uint32(), $undefined, q + 1, m.stickerPackMessage);
                        m._stickerPackMessage = "stickerPackMessage";
                        continue;
                    }
                case 87: {
                        if (u !== 2)
                            break;
                        m.statusMentionMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusMentionMessage);
                        m._statusMentionMessage = "statusMentionMessage";
                        continue;
                    }
                case 88: {
                        if (u !== 2)
                            break;
                        m.pollResultSnapshotMessage = $root.proto.Message.PollResultSnapshotMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollResultSnapshotMessage);
                        m._pollResultSnapshotMessage = "pollResultSnapshotMessage";
                        continue;
                    }
                case 90: {
                        if (u !== 2)
                            break;
                        m.pollCreationOptionImageMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationOptionImageMessage);
                        m._pollCreationOptionImageMessage = "pollCreationOptionImageMessage";
                        continue;
                    }
                case 91: {
                        if (u !== 2)
                            break;
                        m.associatedChildMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.associatedChildMessage);
                        m._associatedChildMessage = "associatedChildMessage";
                        continue;
                    }
                case 92: {
                        if (u !== 2)
                            break;
                        m.groupStatusMentionMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.groupStatusMentionMessage);
                        m._groupStatusMentionMessage = "groupStatusMentionMessage";
                        continue;
                    }
                case 93: {
                        if (u !== 2)
                            break;
                        m.pollCreationMessageV4 = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageV4);
                        m._pollCreationMessageV4 = "pollCreationMessageV4";
                        continue;
                    }
                case 95: {
                        if (u !== 2)
                            break;
                        m.statusAddYours = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusAddYours);
                        m._statusAddYours = "statusAddYours";
                        continue;
                    }
                case 96: {
                        if (u !== 2)
                            break;
                        m.groupStatusMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.groupStatusMessage);
                        m._groupStatusMessage = "groupStatusMessage";
                        continue;
                    }
                case 97: {
                        if (u !== 2)
                            break;
                        m.richResponseMessage = $root.proto.AIRichResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.richResponseMessage);
                        m._richResponseMessage = "richResponseMessage";
                        continue;
                    }
                case 98: {
                        if (u !== 2)
                            break;
                        m.statusNotificationMessage = $root.proto.Message.StatusNotificationMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusNotificationMessage);
                        m._statusNotificationMessage = "statusNotificationMessage";
                        continue;
                    }
                case 99: {
                        if (u !== 2)
                            break;
                        m.limitSharingMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.limitSharingMessage);
                        m._limitSharingMessage = "limitSharingMessage";
                        continue;
                    }
                case 100: {
                        if (u !== 2)
                            break;
                        m.botTaskMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.botTaskMessage);
                        m._botTaskMessage = "botTaskMessage";
                        continue;
                    }
                case 101: {
                        if (u !== 2)
                            break;
                        m.questionMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.questionMessage);
                        m._questionMessage = "questionMessage";
                        continue;
                    }
                case 102: {
                        if (u !== 2)
                            break;
                        m.messageHistoryNotice = $root.proto.Message.MessageHistoryNotice.decode(r, r.uint32(), $undefined, q + 1, m.messageHistoryNotice);
                        m._messageHistoryNotice = "messageHistoryNotice";
                        continue;
                    }
                case 103: {
                        if (u !== 2)
                            break;
                        m.groupStatusMessageV2 = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.groupStatusMessageV2);
                        m._groupStatusMessageV2 = "groupStatusMessageV2";
                        continue;
                    }
                case 104: {
                        if (u !== 2)
                            break;
                        m.botForwardedMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.botForwardedMessage);
                        m._botForwardedMessage = "botForwardedMessage";
                        continue;
                    }
                case 105: {
                        if (u !== 2)
                            break;
                        m.statusQuestionAnswerMessage = $root.proto.Message.StatusQuestionAnswerMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusQuestionAnswerMessage);
                        m._statusQuestionAnswerMessage = "statusQuestionAnswerMessage";
                        continue;
                    }
                case 106: {
                        if (u !== 2)
                            break;
                        m.questionReplyMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.questionReplyMessage);
                        m._questionReplyMessage = "questionReplyMessage";
                        continue;
                    }
                case 107: {
                        if (u !== 2)
                            break;
                        m.questionResponseMessage = $root.proto.Message.QuestionResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.questionResponseMessage);
                        m._questionResponseMessage = "questionResponseMessage";
                        continue;
                    }
                case 109: {
                        if (u !== 2)
                            break;
                        m.statusQuotedMessage = $root.proto.Message.StatusQuotedMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusQuotedMessage);
                        m._statusQuotedMessage = "statusQuotedMessage";
                        continue;
                    }
                case 110: {
                        if (u !== 2)
                            break;
                        m.statusStickerInteractionMessage = $root.proto.Message.StatusStickerInteractionMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusStickerInteractionMessage);
                        m._statusStickerInteractionMessage = "statusStickerInteractionMessage";
                        continue;
                    }
                case 111: {
                        if (u !== 2)
                            break;
                        m.pollCreationMessageV5 = $root.proto.Message.PollCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageV5);
                        m._pollCreationMessageV5 = "pollCreationMessageV5";
                        continue;
                    }
                case 113: {
                        if (u !== 2)
                            break;
                        m.newsletterFollowerInviteMessageV2 = $root.proto.Message.NewsletterFollowerInviteMessage.decode(r, r.uint32(), $undefined, q + 1, m.newsletterFollowerInviteMessageV2);
                        m._newsletterFollowerInviteMessageV2 = "newsletterFollowerInviteMessageV2";
                        continue;
                    }
                case 115: {
                        if (u !== 2)
                            break;
                        m.pollResultSnapshotMessageV3 = $root.proto.Message.PollResultSnapshotMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollResultSnapshotMessageV3);
                        m._pollResultSnapshotMessageV3 = "pollResultSnapshotMessageV3";
                        continue;
                    }
                case 116: {
                        if (u !== 2)
                            break;
                        m.newsletterAdminProfileMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.newsletterAdminProfileMessage);
                        m._newsletterAdminProfileMessage = "newsletterAdminProfileMessage";
                        continue;
                    }
                case 118: {
                        if (u !== 2)
                            break;
                        m.spoilerMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.spoilerMessage);
                        m._spoilerMessage = "spoilerMessage";
                        continue;
                    }
                case 119: {
                        if (u !== 2)
                            break;
                        m.pollCreationMessageV6 = $root.proto.Message.PollCreationMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageV6);
                        m._pollCreationMessageV6 = "pollCreationMessageV6";
                        continue;
                    }
                case 120: {
                        if (u !== 2)
                            break;
                        m.conditionalRevealMessage = $root.proto.Message.ConditionalRevealMessage.decode(r, r.uint32(), $undefined, q + 1, m.conditionalRevealMessage);
                        m._conditionalRevealMessage = "conditionalRevealMessage";
                        continue;
                    }
                case 121: {
                        if (u !== 2)
                            break;
                        m.pollAddOptionMessage = $root.proto.Message.PollAddOptionMessage.decode(r, r.uint32(), $undefined, q + 1, m.pollAddOptionMessage);
                        m._pollAddOptionMessage = "pollAddOptionMessage";
                        continue;
                    }
                case 122: {
                        if (u !== 2)
                            break;
                        m.eventInviteMessage = $root.proto.Message.EventInviteMessage.decode(r, r.uint32(), $undefined, q + 1, m.eventInviteMessage);
                        m._eventInviteMessage = "eventInviteMessage";
                        continue;
                    }
                case 123: {
                        if (u !== 2)
                            break;
                        m.groupRootKeyShare = $root.proto.GroupRootKeyShare.decode(r, r.uint32(), $undefined, q + 1, m.groupRootKeyShare);
                        m._groupRootKeyShare = "groupRootKeyShare";
                        continue;
                    }
                case 124: {
                        if (u !== 2)
                            break;
                        m.paymentReminderMessage = $root.proto.Message.PaymentReminderMessage.decode(r, r.uint32(), $undefined, q + 1, m.paymentReminderMessage);
                        m._paymentReminderMessage = "paymentReminderMessage";
                        continue;
                    }
                case 125: {
                        if (u !== 2)
                            break;
                        m.splitPaymentMessage = $root.proto.Message.SplitPaymentMessage.decode(r, r.uint32(), $undefined, q + 1, m.splitPaymentMessage);
                        m._splitPaymentMessage = "splitPaymentMessage";
                        continue;
                    }
                case 126: {
                        if (u !== 2)
                            break;
                        m.newsletterAdminProfileStatusMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.newsletterAdminProfileStatusMessage);
                        m._newsletterAdminProfileStatusMessage = "newsletterAdminProfileStatusMessage";
                        continue;
                    }
                case 127: {
                        if (u !== 2)
                            break;
                        m.rootSecretDistributeMessage = $root.proto.Message.RootSecretDistributeMessage.decode(r, r.uint32(), $undefined, q + 1, m.rootSecretDistributeMessage);
                        m._rootSecretDistributeMessage = "rootSecretDistributeMessage";
                        continue;
                    }
                case 128: {
                        if (u !== 2)
                            break;
                        m.splitPaymentUpdateMessage = $root.proto.Message.SplitPaymentUpdateMessage.decode(r, r.uint32(), $undefined, q + 1, m.splitPaymentUpdateMessage);
                        m._splitPaymentUpdateMessage = "splitPaymentUpdateMessage";
                        continue;
                    }
                case 129: {
                        if (u !== 2)
                            break;
                        m.musicMessage = $root.proto.Message.MusicMessage.decode(r, r.uint32(), $undefined, q + 1, m.musicMessage);
                        m._musicMessage = "musicMessage";
                        continue;
                    }
                case 130: {
                        if (u !== 2)
                            break;
                        m.statusLinkPreviewMetadata = $root.proto.Message.StatusLinkPreviewMetadata.decode(r, r.uint32(), $undefined, q + 1, m.statusLinkPreviewMetadata);
                        m._statusLinkPreviewMetadata = "statusLinkPreviewMetadata";
                        continue;
                    }
                case 131: {
                        if (u !== 2)
                            break;
                        m.botPlatformRegistrationSuccessMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.botPlatformRegistrationSuccessMessage);
                        m._botPlatformRegistrationSuccessMessage = "botPlatformRegistrationSuccessMessage";
                        continue;
                    }
                case 132: {
                        if (u !== 2)
                            break;
                        m.newsletterScheduledMessage = $root.proto.Message.FutureProofMessage.decode(r, r.uint32(), $undefined, q + 1, m.newsletterScheduledMessage);
                        m._newsletterScheduledMessage = "newsletterScheduledMessage";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Message.fromObject = function (d, q) {
            if (d instanceof $root.proto.Message)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Message: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Message();
            if (d.conversation != null) {
                m.conversation = $String(d.conversation);
            }
            if (d.senderKeyDistributionMessage != null) {
                if (!$util.isObject(d.senderKeyDistributionMessage))
                    throw $TypeError(".proto.Message.senderKeyDistributionMessage: object expected");
                m.senderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.fromObject(d.senderKeyDistributionMessage, q + 1);
            }
            if (d.imageMessage != null) {
                if (!$util.isObject(d.imageMessage))
                    throw $TypeError(".proto.Message.imageMessage: object expected");
                m.imageMessage = $root.proto.Message.ImageMessage.fromObject(d.imageMessage, q + 1);
            }
            if (d.contactMessage != null) {
                if (!$util.isObject(d.contactMessage))
                    throw $TypeError(".proto.Message.contactMessage: object expected");
                m.contactMessage = $root.proto.Message.ContactMessage.fromObject(d.contactMessage, q + 1);
            }
            if (d.locationMessage != null) {
                if (!$util.isObject(d.locationMessage))
                    throw $TypeError(".proto.Message.locationMessage: object expected");
                m.locationMessage = $root.proto.Message.LocationMessage.fromObject(d.locationMessage, q + 1);
            }
            if (d.extendedTextMessage != null) {
                if (!$util.isObject(d.extendedTextMessage))
                    throw $TypeError(".proto.Message.extendedTextMessage: object expected");
                m.extendedTextMessage = $root.proto.Message.ExtendedTextMessage.fromObject(d.extendedTextMessage, q + 1);
            }
            if (d.documentMessage != null) {
                if (!$util.isObject(d.documentMessage))
                    throw $TypeError(".proto.Message.documentMessage: object expected");
                m.documentMessage = $root.proto.Message.DocumentMessage.fromObject(d.documentMessage, q + 1);
            }
            if (d.audioMessage != null) {
                if (!$util.isObject(d.audioMessage))
                    throw $TypeError(".proto.Message.audioMessage: object expected");
                m.audioMessage = $root.proto.Message.AudioMessage.fromObject(d.audioMessage, q + 1);
            }
            if (d.videoMessage != null) {
                if (!$util.isObject(d.videoMessage))
                    throw $TypeError(".proto.Message.videoMessage: object expected");
                m.videoMessage = $root.proto.Message.VideoMessage.fromObject(d.videoMessage, q + 1);
            }
            if (d.call != null) {
                if (!$util.isObject(d.call))
                    throw $TypeError(".proto.Message.call: object expected");
                m.call = $root.proto.Message.Call.fromObject(d.call, q + 1);
            }
            if (d.chat != null) {
                if (!$util.isObject(d.chat))
                    throw $TypeError(".proto.Message.chat: object expected");
                m.chat = $root.proto.Message.Chat.fromObject(d.chat, q + 1);
            }
            if (d.protocolMessage != null) {
                if (!$util.isObject(d.protocolMessage))
                    throw $TypeError(".proto.Message.protocolMessage: object expected");
                m.protocolMessage = $root.proto.Message.ProtocolMessage.fromObject(d.protocolMessage, q + 1);
            }
            if (d.contactsArrayMessage != null) {
                if (!$util.isObject(d.contactsArrayMessage))
                    throw $TypeError(".proto.Message.contactsArrayMessage: object expected");
                m.contactsArrayMessage = $root.proto.Message.ContactsArrayMessage.fromObject(d.contactsArrayMessage, q + 1);
            }
            if (d.highlyStructuredMessage != null) {
                if (!$util.isObject(d.highlyStructuredMessage))
                    throw $TypeError(".proto.Message.highlyStructuredMessage: object expected");
                m.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.fromObject(d.highlyStructuredMessage, q + 1);
            }
            if (d.fastRatchetKeySenderKeyDistributionMessage != null) {
                if (!$util.isObject(d.fastRatchetKeySenderKeyDistributionMessage))
                    throw $TypeError(".proto.Message.fastRatchetKeySenderKeyDistributionMessage: object expected");
                m.fastRatchetKeySenderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.fromObject(d.fastRatchetKeySenderKeyDistributionMessage, q + 1);
            }
            if (d.sendPaymentMessage != null) {
                if (!$util.isObject(d.sendPaymentMessage))
                    throw $TypeError(".proto.Message.sendPaymentMessage: object expected");
                m.sendPaymentMessage = $root.proto.Message.SendPaymentMessage.fromObject(d.sendPaymentMessage, q + 1);
            }
            if (d.liveLocationMessage != null) {
                if (!$util.isObject(d.liveLocationMessage))
                    throw $TypeError(".proto.Message.liveLocationMessage: object expected");
                m.liveLocationMessage = $root.proto.Message.LiveLocationMessage.fromObject(d.liveLocationMessage, q + 1);
            }
            if (d.requestPaymentMessage != null) {
                if (!$util.isObject(d.requestPaymentMessage))
                    throw $TypeError(".proto.Message.requestPaymentMessage: object expected");
                m.requestPaymentMessage = $root.proto.Message.RequestPaymentMessage.fromObject(d.requestPaymentMessage, q + 1);
            }
            if (d.declinePaymentRequestMessage != null) {
                if (!$util.isObject(d.declinePaymentRequestMessage))
                    throw $TypeError(".proto.Message.declinePaymentRequestMessage: object expected");
                m.declinePaymentRequestMessage = $root.proto.Message.DeclinePaymentRequestMessage.fromObject(d.declinePaymentRequestMessage, q + 1);
            }
            if (d.cancelPaymentRequestMessage != null) {
                if (!$util.isObject(d.cancelPaymentRequestMessage))
                    throw $TypeError(".proto.Message.cancelPaymentRequestMessage: object expected");
                m.cancelPaymentRequestMessage = $root.proto.Message.CancelPaymentRequestMessage.fromObject(d.cancelPaymentRequestMessage, q + 1);
            }
            if (d.templateMessage != null) {
                if (!$util.isObject(d.templateMessage))
                    throw $TypeError(".proto.Message.templateMessage: object expected");
                m.templateMessage = $root.proto.Message.TemplateMessage.fromObject(d.templateMessage, q + 1);
            }
            if (d.stickerMessage != null) {
                if (!$util.isObject(d.stickerMessage))
                    throw $TypeError(".proto.Message.stickerMessage: object expected");
                m.stickerMessage = $root.proto.Message.StickerMessage.fromObject(d.stickerMessage, q + 1);
            }
            if (d.groupInviteMessage != null) {
                if (!$util.isObject(d.groupInviteMessage))
                    throw $TypeError(".proto.Message.groupInviteMessage: object expected");
                m.groupInviteMessage = $root.proto.Message.GroupInviteMessage.fromObject(d.groupInviteMessage, q + 1);
            }
            if (d.templateButtonReplyMessage != null) {
                if (!$util.isObject(d.templateButtonReplyMessage))
                    throw $TypeError(".proto.Message.templateButtonReplyMessage: object expected");
                m.templateButtonReplyMessage = $root.proto.Message.TemplateButtonReplyMessage.fromObject(d.templateButtonReplyMessage, q + 1);
            }
            if (d.productMessage != null) {
                if (!$util.isObject(d.productMessage))
                    throw $TypeError(".proto.Message.productMessage: object expected");
                m.productMessage = $root.proto.Message.ProductMessage.fromObject(d.productMessage, q + 1);
            }
            if (d.deviceSentMessage != null) {
                if (!$util.isObject(d.deviceSentMessage))
                    throw $TypeError(".proto.Message.deviceSentMessage: object expected");
                m.deviceSentMessage = $root.proto.Message.DeviceSentMessage.fromObject(d.deviceSentMessage, q + 1);
            }
            if (d.messageContextInfo != null) {
                if (!$util.isObject(d.messageContextInfo))
                    throw $TypeError(".proto.Message.messageContextInfo: object expected");
                m.messageContextInfo = $root.proto.MessageContextInfo.fromObject(d.messageContextInfo, q + 1);
            }
            if (d.listMessage != null) {
                if (!$util.isObject(d.listMessage))
                    throw $TypeError(".proto.Message.listMessage: object expected");
                m.listMessage = $root.proto.Message.ListMessage.fromObject(d.listMessage, q + 1);
            }
            if (d.viewOnceMessage != null) {
                if (!$util.isObject(d.viewOnceMessage))
                    throw $TypeError(".proto.Message.viewOnceMessage: object expected");
                m.viewOnceMessage = $root.proto.Message.FutureProofMessage.fromObject(d.viewOnceMessage, q + 1);
            }
            if (d.orderMessage != null) {
                if (!$util.isObject(d.orderMessage))
                    throw $TypeError(".proto.Message.orderMessage: object expected");
                m.orderMessage = $root.proto.Message.OrderMessage.fromObject(d.orderMessage, q + 1);
            }
            if (d.listResponseMessage != null) {
                if (!$util.isObject(d.listResponseMessage))
                    throw $TypeError(".proto.Message.listResponseMessage: object expected");
                m.listResponseMessage = $root.proto.Message.ListResponseMessage.fromObject(d.listResponseMessage, q + 1);
            }
            if (d.ephemeralMessage != null) {
                if (!$util.isObject(d.ephemeralMessage))
                    throw $TypeError(".proto.Message.ephemeralMessage: object expected");
                m.ephemeralMessage = $root.proto.Message.FutureProofMessage.fromObject(d.ephemeralMessage, q + 1);
            }
            if (d.invoiceMessage != null) {
                if (!$util.isObject(d.invoiceMessage))
                    throw $TypeError(".proto.Message.invoiceMessage: object expected");
                m.invoiceMessage = $root.proto.Message.InvoiceMessage.fromObject(d.invoiceMessage, q + 1);
            }
            if (d.buttonsMessage != null) {
                if (!$util.isObject(d.buttonsMessage))
                    throw $TypeError(".proto.Message.buttonsMessage: object expected");
                m.buttonsMessage = $root.proto.Message.ButtonsMessage.fromObject(d.buttonsMessage, q + 1);
            }
            if (d.buttonsResponseMessage != null) {
                if (!$util.isObject(d.buttonsResponseMessage))
                    throw $TypeError(".proto.Message.buttonsResponseMessage: object expected");
                m.buttonsResponseMessage = $root.proto.Message.ButtonsResponseMessage.fromObject(d.buttonsResponseMessage, q + 1);
            }
            if (d.paymentInviteMessage != null) {
                if (!$util.isObject(d.paymentInviteMessage))
                    throw $TypeError(".proto.Message.paymentInviteMessage: object expected");
                m.paymentInviteMessage = $root.proto.Message.PaymentInviteMessage.fromObject(d.paymentInviteMessage, q + 1);
            }
            if (d.interactiveMessage != null) {
                if (!$util.isObject(d.interactiveMessage))
                    throw $TypeError(".proto.Message.interactiveMessage: object expected");
                m.interactiveMessage = $root.proto.Message.InteractiveMessage.fromObject(d.interactiveMessage, q + 1);
            }
            if (d.reactionMessage != null) {
                if (!$util.isObject(d.reactionMessage))
                    throw $TypeError(".proto.Message.reactionMessage: object expected");
                m.reactionMessage = $root.proto.Message.ReactionMessage.fromObject(d.reactionMessage, q + 1);
            }
            if (d.stickerSyncRmrMessage != null) {
                if (!$util.isObject(d.stickerSyncRmrMessage))
                    throw $TypeError(".proto.Message.stickerSyncRmrMessage: object expected");
                m.stickerSyncRmrMessage = $root.proto.Message.StickerSyncRMRMessage.fromObject(d.stickerSyncRmrMessage, q + 1);
            }
            if (d.interactiveResponseMessage != null) {
                if (!$util.isObject(d.interactiveResponseMessage))
                    throw $TypeError(".proto.Message.interactiveResponseMessage: object expected");
                m.interactiveResponseMessage = $root.proto.Message.InteractiveResponseMessage.fromObject(d.interactiveResponseMessage, q + 1);
            }
            if (d.pollCreationMessage != null) {
                if (!$util.isObject(d.pollCreationMessage))
                    throw $TypeError(".proto.Message.pollCreationMessage: object expected");
                m.pollCreationMessage = $root.proto.Message.PollCreationMessage.fromObject(d.pollCreationMessage, q + 1);
            }
            if (d.pollUpdateMessage != null) {
                if (!$util.isObject(d.pollUpdateMessage))
                    throw $TypeError(".proto.Message.pollUpdateMessage: object expected");
                m.pollUpdateMessage = $root.proto.Message.PollUpdateMessage.fromObject(d.pollUpdateMessage, q + 1);
            }
            if (d.keepInChatMessage != null) {
                if (!$util.isObject(d.keepInChatMessage))
                    throw $TypeError(".proto.Message.keepInChatMessage: object expected");
                m.keepInChatMessage = $root.proto.Message.KeepInChatMessage.fromObject(d.keepInChatMessage, q + 1);
            }
            if (d.documentWithCaptionMessage != null) {
                if (!$util.isObject(d.documentWithCaptionMessage))
                    throw $TypeError(".proto.Message.documentWithCaptionMessage: object expected");
                m.documentWithCaptionMessage = $root.proto.Message.FutureProofMessage.fromObject(d.documentWithCaptionMessage, q + 1);
            }
            if (d.requestPhoneNumberMessage != null) {
                if (!$util.isObject(d.requestPhoneNumberMessage))
                    throw $TypeError(".proto.Message.requestPhoneNumberMessage: object expected");
                m.requestPhoneNumberMessage = $root.proto.Message.RequestPhoneNumberMessage.fromObject(d.requestPhoneNumberMessage, q + 1);
            }
            if (d.viewOnceMessageV2 != null) {
                if (!$util.isObject(d.viewOnceMessageV2))
                    throw $TypeError(".proto.Message.viewOnceMessageV2: object expected");
                m.viewOnceMessageV2 = $root.proto.Message.FutureProofMessage.fromObject(d.viewOnceMessageV2, q + 1);
            }
            if (d.encReactionMessage != null) {
                if (!$util.isObject(d.encReactionMessage))
                    throw $TypeError(".proto.Message.encReactionMessage: object expected");
                m.encReactionMessage = $root.proto.Message.EncReactionMessage.fromObject(d.encReactionMessage, q + 1);
            }
            if (d.editedMessage != null) {
                if (!$util.isObject(d.editedMessage))
                    throw $TypeError(".proto.Message.editedMessage: object expected");
                m.editedMessage = $root.proto.Message.FutureProofMessage.fromObject(d.editedMessage, q + 1);
            }
            if (d.viewOnceMessageV2Extension != null) {
                if (!$util.isObject(d.viewOnceMessageV2Extension))
                    throw $TypeError(".proto.Message.viewOnceMessageV2Extension: object expected");
                m.viewOnceMessageV2Extension = $root.proto.Message.FutureProofMessage.fromObject(d.viewOnceMessageV2Extension, q + 1);
            }
            if (d.pollCreationMessageV2 != null) {
                if (!$util.isObject(d.pollCreationMessageV2))
                    throw $TypeError(".proto.Message.pollCreationMessageV2: object expected");
                m.pollCreationMessageV2 = $root.proto.Message.PollCreationMessage.fromObject(d.pollCreationMessageV2, q + 1);
            }
            if (d.scheduledCallCreationMessage != null) {
                if (!$util.isObject(d.scheduledCallCreationMessage))
                    throw $TypeError(".proto.Message.scheduledCallCreationMessage: object expected");
                m.scheduledCallCreationMessage = $root.proto.Message.ScheduledCallCreationMessage.fromObject(d.scheduledCallCreationMessage, q + 1);
            }
            if (d.groupMentionedMessage != null) {
                if (!$util.isObject(d.groupMentionedMessage))
                    throw $TypeError(".proto.Message.groupMentionedMessage: object expected");
                m.groupMentionedMessage = $root.proto.Message.FutureProofMessage.fromObject(d.groupMentionedMessage, q + 1);
            }
            if (d.pinInChatMessage != null) {
                if (!$util.isObject(d.pinInChatMessage))
                    throw $TypeError(".proto.Message.pinInChatMessage: object expected");
                m.pinInChatMessage = $root.proto.Message.PinInChatMessage.fromObject(d.pinInChatMessage, q + 1);
            }
            if (d.pollCreationMessageV3 != null) {
                if (!$util.isObject(d.pollCreationMessageV3))
                    throw $TypeError(".proto.Message.pollCreationMessageV3: object expected");
                m.pollCreationMessageV3 = $root.proto.Message.PollCreationMessage.fromObject(d.pollCreationMessageV3, q + 1);
            }
            if (d.scheduledCallEditMessage != null) {
                if (!$util.isObject(d.scheduledCallEditMessage))
                    throw $TypeError(".proto.Message.scheduledCallEditMessage: object expected");
                m.scheduledCallEditMessage = $root.proto.Message.ScheduledCallEditMessage.fromObject(d.scheduledCallEditMessage, q + 1);
            }
            if (d.ptvMessage != null) {
                if (!$util.isObject(d.ptvMessage))
                    throw $TypeError(".proto.Message.ptvMessage: object expected");
                m.ptvMessage = $root.proto.Message.VideoMessage.fromObject(d.ptvMessage, q + 1);
            }
            if (d.botInvokeMessage != null) {
                if (!$util.isObject(d.botInvokeMessage))
                    throw $TypeError(".proto.Message.botInvokeMessage: object expected");
                m.botInvokeMessage = $root.proto.Message.FutureProofMessage.fromObject(d.botInvokeMessage, q + 1);
            }
            if (d.callLogMesssage != null) {
                if (!$util.isObject(d.callLogMesssage))
                    throw $TypeError(".proto.Message.callLogMesssage: object expected");
                m.callLogMesssage = $root.proto.Message.CallLogMessage.fromObject(d.callLogMesssage, q + 1);
            }
            if (d.messageHistoryBundle != null) {
                if (!$util.isObject(d.messageHistoryBundle))
                    throw $TypeError(".proto.Message.messageHistoryBundle: object expected");
                m.messageHistoryBundle = $root.proto.Message.MessageHistoryBundle.fromObject(d.messageHistoryBundle, q + 1);
            }
            if (d.encCommentMessage != null) {
                if (!$util.isObject(d.encCommentMessage))
                    throw $TypeError(".proto.Message.encCommentMessage: object expected");
                m.encCommentMessage = $root.proto.Message.EncCommentMessage.fromObject(d.encCommentMessage, q + 1);
            }
            if (d.bcallMessage != null) {
                if (!$util.isObject(d.bcallMessage))
                    throw $TypeError(".proto.Message.bcallMessage: object expected");
                m.bcallMessage = $root.proto.Message.BCallMessage.fromObject(d.bcallMessage, q + 1);
            }
            if (d.lottieStickerMessage != null) {
                if (!$util.isObject(d.lottieStickerMessage))
                    throw $TypeError(".proto.Message.lottieStickerMessage: object expected");
                m.lottieStickerMessage = $root.proto.Message.FutureProofMessage.fromObject(d.lottieStickerMessage, q + 1);
            }
            if (d.eventMessage != null) {
                if (!$util.isObject(d.eventMessage))
                    throw $TypeError(".proto.Message.eventMessage: object expected");
                m.eventMessage = $root.proto.Message.EventMessage.fromObject(d.eventMessage, q + 1);
            }
            if (d.encEventResponseMessage != null) {
                if (!$util.isObject(d.encEventResponseMessage))
                    throw $TypeError(".proto.Message.encEventResponseMessage: object expected");
                m.encEventResponseMessage = $root.proto.Message.EncEventResponseMessage.fromObject(d.encEventResponseMessage, q + 1);
            }
            if (d.commentMessage != null) {
                if (!$util.isObject(d.commentMessage))
                    throw $TypeError(".proto.Message.commentMessage: object expected");
                m.commentMessage = $root.proto.Message.CommentMessage.fromObject(d.commentMessage, q + 1);
            }
            if (d.newsletterAdminInviteMessage != null) {
                if (!$util.isObject(d.newsletterAdminInviteMessage))
                    throw $TypeError(".proto.Message.newsletterAdminInviteMessage: object expected");
                m.newsletterAdminInviteMessage = $root.proto.Message.NewsletterAdminInviteMessage.fromObject(d.newsletterAdminInviteMessage, q + 1);
            }
            if (d.placeholderMessage != null) {
                if (!$util.isObject(d.placeholderMessage))
                    throw $TypeError(".proto.Message.placeholderMessage: object expected");
                m.placeholderMessage = $root.proto.Message.PlaceholderMessage.fromObject(d.placeholderMessage, q + 1);
            }
            if (d.secretEncryptedMessage != null) {
                if (!$util.isObject(d.secretEncryptedMessage))
                    throw $TypeError(".proto.Message.secretEncryptedMessage: object expected");
                m.secretEncryptedMessage = $root.proto.Message.SecretEncryptedMessage.fromObject(d.secretEncryptedMessage, q + 1);
            }
            if (d.albumMessage != null) {
                if (!$util.isObject(d.albumMessage))
                    throw $TypeError(".proto.Message.albumMessage: object expected");
                m.albumMessage = $root.proto.Message.AlbumMessage.fromObject(d.albumMessage, q + 1);
            }
            if (d.eventCoverImage != null) {
                if (!$util.isObject(d.eventCoverImage))
                    throw $TypeError(".proto.Message.eventCoverImage: object expected");
                m.eventCoverImage = $root.proto.Message.FutureProofMessage.fromObject(d.eventCoverImage, q + 1);
            }
            if (d.stickerPackMessage != null) {
                if (!$util.isObject(d.stickerPackMessage))
                    throw $TypeError(".proto.Message.stickerPackMessage: object expected");
                m.stickerPackMessage = $root.proto.Message.StickerPackMessage.fromObject(d.stickerPackMessage, q + 1);
            }
            if (d.statusMentionMessage != null) {
                if (!$util.isObject(d.statusMentionMessage))
                    throw $TypeError(".proto.Message.statusMentionMessage: object expected");
                m.statusMentionMessage = $root.proto.Message.FutureProofMessage.fromObject(d.statusMentionMessage, q + 1);
            }
            if (d.pollResultSnapshotMessage != null) {
                if (!$util.isObject(d.pollResultSnapshotMessage))
                    throw $TypeError(".proto.Message.pollResultSnapshotMessage: object expected");
                m.pollResultSnapshotMessage = $root.proto.Message.PollResultSnapshotMessage.fromObject(d.pollResultSnapshotMessage, q + 1);
            }
            if (d.pollCreationOptionImageMessage != null) {
                if (!$util.isObject(d.pollCreationOptionImageMessage))
                    throw $TypeError(".proto.Message.pollCreationOptionImageMessage: object expected");
                m.pollCreationOptionImageMessage = $root.proto.Message.FutureProofMessage.fromObject(d.pollCreationOptionImageMessage, q + 1);
            }
            if (d.associatedChildMessage != null) {
                if (!$util.isObject(d.associatedChildMessage))
                    throw $TypeError(".proto.Message.associatedChildMessage: object expected");
                m.associatedChildMessage = $root.proto.Message.FutureProofMessage.fromObject(d.associatedChildMessage, q + 1);
            }
            if (d.groupStatusMentionMessage != null) {
                if (!$util.isObject(d.groupStatusMentionMessage))
                    throw $TypeError(".proto.Message.groupStatusMentionMessage: object expected");
                m.groupStatusMentionMessage = $root.proto.Message.FutureProofMessage.fromObject(d.groupStatusMentionMessage, q + 1);
            }
            if (d.pollCreationMessageV4 != null) {
                if (!$util.isObject(d.pollCreationMessageV4))
                    throw $TypeError(".proto.Message.pollCreationMessageV4: object expected");
                m.pollCreationMessageV4 = $root.proto.Message.FutureProofMessage.fromObject(d.pollCreationMessageV4, q + 1);
            }
            if (d.statusAddYours != null) {
                if (!$util.isObject(d.statusAddYours))
                    throw $TypeError(".proto.Message.statusAddYours: object expected");
                m.statusAddYours = $root.proto.Message.FutureProofMessage.fromObject(d.statusAddYours, q + 1);
            }
            if (d.groupStatusMessage != null) {
                if (!$util.isObject(d.groupStatusMessage))
                    throw $TypeError(".proto.Message.groupStatusMessage: object expected");
                m.groupStatusMessage = $root.proto.Message.FutureProofMessage.fromObject(d.groupStatusMessage, q + 1);
            }
            if (d.richResponseMessage != null) {
                if (!$util.isObject(d.richResponseMessage))
                    throw $TypeError(".proto.Message.richResponseMessage: object expected");
                m.richResponseMessage = $root.proto.AIRichResponseMessage.fromObject(d.richResponseMessage, q + 1);
            }
            if (d.statusNotificationMessage != null) {
                if (!$util.isObject(d.statusNotificationMessage))
                    throw $TypeError(".proto.Message.statusNotificationMessage: object expected");
                m.statusNotificationMessage = $root.proto.Message.StatusNotificationMessage.fromObject(d.statusNotificationMessage, q + 1);
            }
            if (d.limitSharingMessage != null) {
                if (!$util.isObject(d.limitSharingMessage))
                    throw $TypeError(".proto.Message.limitSharingMessage: object expected");
                m.limitSharingMessage = $root.proto.Message.FutureProofMessage.fromObject(d.limitSharingMessage, q + 1);
            }
            if (d.botTaskMessage != null) {
                if (!$util.isObject(d.botTaskMessage))
                    throw $TypeError(".proto.Message.botTaskMessage: object expected");
                m.botTaskMessage = $root.proto.Message.FutureProofMessage.fromObject(d.botTaskMessage, q + 1);
            }
            if (d.questionMessage != null) {
                if (!$util.isObject(d.questionMessage))
                    throw $TypeError(".proto.Message.questionMessage: object expected");
                m.questionMessage = $root.proto.Message.FutureProofMessage.fromObject(d.questionMessage, q + 1);
            }
            if (d.messageHistoryNotice != null) {
                if (!$util.isObject(d.messageHistoryNotice))
                    throw $TypeError(".proto.Message.messageHistoryNotice: object expected");
                m.messageHistoryNotice = $root.proto.Message.MessageHistoryNotice.fromObject(d.messageHistoryNotice, q + 1);
            }
            if (d.groupStatusMessageV2 != null) {
                if (!$util.isObject(d.groupStatusMessageV2))
                    throw $TypeError(".proto.Message.groupStatusMessageV2: object expected");
                m.groupStatusMessageV2 = $root.proto.Message.FutureProofMessage.fromObject(d.groupStatusMessageV2, q + 1);
            }
            if (d.botForwardedMessage != null) {
                if (!$util.isObject(d.botForwardedMessage))
                    throw $TypeError(".proto.Message.botForwardedMessage: object expected");
                m.botForwardedMessage = $root.proto.Message.FutureProofMessage.fromObject(d.botForwardedMessage, q + 1);
            }
            if (d.statusQuestionAnswerMessage != null) {
                if (!$util.isObject(d.statusQuestionAnswerMessage))
                    throw $TypeError(".proto.Message.statusQuestionAnswerMessage: object expected");
                m.statusQuestionAnswerMessage = $root.proto.Message.StatusQuestionAnswerMessage.fromObject(d.statusQuestionAnswerMessage, q + 1);
            }
            if (d.questionReplyMessage != null) {
                if (!$util.isObject(d.questionReplyMessage))
                    throw $TypeError(".proto.Message.questionReplyMessage: object expected");
                m.questionReplyMessage = $root.proto.Message.FutureProofMessage.fromObject(d.questionReplyMessage, q + 1);
            }
            if (d.questionResponseMessage != null) {
                if (!$util.isObject(d.questionResponseMessage))
                    throw $TypeError(".proto.Message.questionResponseMessage: object expected");
                m.questionResponseMessage = $root.proto.Message.QuestionResponseMessage.fromObject(d.questionResponseMessage, q + 1);
            }
            if (d.statusQuotedMessage != null) {
                if (!$util.isObject(d.statusQuotedMessage))
                    throw $TypeError(".proto.Message.statusQuotedMessage: object expected");
                m.statusQuotedMessage = $root.proto.Message.StatusQuotedMessage.fromObject(d.statusQuotedMessage, q + 1);
            }
            if (d.statusStickerInteractionMessage != null) {
                if (!$util.isObject(d.statusStickerInteractionMessage))
                    throw $TypeError(".proto.Message.statusStickerInteractionMessage: object expected");
                m.statusStickerInteractionMessage = $root.proto.Message.StatusStickerInteractionMessage.fromObject(d.statusStickerInteractionMessage, q + 1);
            }
            if (d.pollCreationMessageV5 != null) {
                if (!$util.isObject(d.pollCreationMessageV5))
                    throw $TypeError(".proto.Message.pollCreationMessageV5: object expected");
                m.pollCreationMessageV5 = $root.proto.Message.PollCreationMessage.fromObject(d.pollCreationMessageV5, q + 1);
            }
            if (d.newsletterFollowerInviteMessageV2 != null) {
                if (!$util.isObject(d.newsletterFollowerInviteMessageV2))
                    throw $TypeError(".proto.Message.newsletterFollowerInviteMessageV2: object expected");
                m.newsletterFollowerInviteMessageV2 = $root.proto.Message.NewsletterFollowerInviteMessage.fromObject(d.newsletterFollowerInviteMessageV2, q + 1);
            }
            if (d.pollResultSnapshotMessageV3 != null) {
                if (!$util.isObject(d.pollResultSnapshotMessageV3))
                    throw $TypeError(".proto.Message.pollResultSnapshotMessageV3: object expected");
                m.pollResultSnapshotMessageV3 = $root.proto.Message.PollResultSnapshotMessage.fromObject(d.pollResultSnapshotMessageV3, q + 1);
            }
            if (d.newsletterAdminProfileMessage != null) {
                if (!$util.isObject(d.newsletterAdminProfileMessage))
                    throw $TypeError(".proto.Message.newsletterAdminProfileMessage: object expected");
                m.newsletterAdminProfileMessage = $root.proto.Message.FutureProofMessage.fromObject(d.newsletterAdminProfileMessage, q + 1);
            }
            if (d.spoilerMessage != null) {
                if (!$util.isObject(d.spoilerMessage))
                    throw $TypeError(".proto.Message.spoilerMessage: object expected");
                m.spoilerMessage = $root.proto.Message.FutureProofMessage.fromObject(d.spoilerMessage, q + 1);
            }
            if (d.pollCreationMessageV6 != null) {
                if (!$util.isObject(d.pollCreationMessageV6))
                    throw $TypeError(".proto.Message.pollCreationMessageV6: object expected");
                m.pollCreationMessageV6 = $root.proto.Message.PollCreationMessage.fromObject(d.pollCreationMessageV6, q + 1);
            }
            if (d.conditionalRevealMessage != null) {
                if (!$util.isObject(d.conditionalRevealMessage))
                    throw $TypeError(".proto.Message.conditionalRevealMessage: object expected");
                m.conditionalRevealMessage = $root.proto.Message.ConditionalRevealMessage.fromObject(d.conditionalRevealMessage, q + 1);
            }
            if (d.pollAddOptionMessage != null) {
                if (!$util.isObject(d.pollAddOptionMessage))
                    throw $TypeError(".proto.Message.pollAddOptionMessage: object expected");
                m.pollAddOptionMessage = $root.proto.Message.PollAddOptionMessage.fromObject(d.pollAddOptionMessage, q + 1);
            }
            if (d.eventInviteMessage != null) {
                if (!$util.isObject(d.eventInviteMessage))
                    throw $TypeError(".proto.Message.eventInviteMessage: object expected");
                m.eventInviteMessage = $root.proto.Message.EventInviteMessage.fromObject(d.eventInviteMessage, q + 1);
            }
            if (d.groupRootKeyShare != null) {
                if (!$util.isObject(d.groupRootKeyShare))
                    throw $TypeError(".proto.Message.groupRootKeyShare: object expected");
                m.groupRootKeyShare = $root.proto.GroupRootKeyShare.fromObject(d.groupRootKeyShare, q + 1);
            }
            if (d.paymentReminderMessage != null) {
                if (!$util.isObject(d.paymentReminderMessage))
                    throw $TypeError(".proto.Message.paymentReminderMessage: object expected");
                m.paymentReminderMessage = $root.proto.Message.PaymentReminderMessage.fromObject(d.paymentReminderMessage, q + 1);
            }
            if (d.splitPaymentMessage != null) {
                if (!$util.isObject(d.splitPaymentMessage))
                    throw $TypeError(".proto.Message.splitPaymentMessage: object expected");
                m.splitPaymentMessage = $root.proto.Message.SplitPaymentMessage.fromObject(d.splitPaymentMessage, q + 1);
            }
            if (d.newsletterAdminProfileStatusMessage != null) {
                if (!$util.isObject(d.newsletterAdminProfileStatusMessage))
                    throw $TypeError(".proto.Message.newsletterAdminProfileStatusMessage: object expected");
                m.newsletterAdminProfileStatusMessage = $root.proto.Message.FutureProofMessage.fromObject(d.newsletterAdminProfileStatusMessage, q + 1);
            }
            if (d.rootSecretDistributeMessage != null) {
                if (!$util.isObject(d.rootSecretDistributeMessage))
                    throw $TypeError(".proto.Message.rootSecretDistributeMessage: object expected");
                m.rootSecretDistributeMessage = $root.proto.Message.RootSecretDistributeMessage.fromObject(d.rootSecretDistributeMessage, q + 1);
            }
            if (d.splitPaymentUpdateMessage != null) {
                if (!$util.isObject(d.splitPaymentUpdateMessage))
                    throw $TypeError(".proto.Message.splitPaymentUpdateMessage: object expected");
                m.splitPaymentUpdateMessage = $root.proto.Message.SplitPaymentUpdateMessage.fromObject(d.splitPaymentUpdateMessage, q + 1);
            }
            if (d.musicMessage != null) {
                if (!$util.isObject(d.musicMessage))
                    throw $TypeError(".proto.Message.musicMessage: object expected");
                m.musicMessage = $root.proto.Message.MusicMessage.fromObject(d.musicMessage, q + 1);
            }
            if (d.statusLinkPreviewMetadata != null) {
                if (!$util.isObject(d.statusLinkPreviewMetadata))
                    throw $TypeError(".proto.Message.statusLinkPreviewMetadata: object expected");
                m.statusLinkPreviewMetadata = $root.proto.Message.StatusLinkPreviewMetadata.fromObject(d.statusLinkPreviewMetadata, q + 1);
            }
            if (d.botPlatformRegistrationSuccessMessage != null) {
                if (!$util.isObject(d.botPlatformRegistrationSuccessMessage))
                    throw $TypeError(".proto.Message.botPlatformRegistrationSuccessMessage: object expected");
                m.botPlatformRegistrationSuccessMessage = $root.proto.Message.FutureProofMessage.fromObject(d.botPlatformRegistrationSuccessMessage, q + 1);
            }
            if (d.newsletterScheduledMessage != null) {
                if (!$util.isObject(d.newsletterScheduledMessage))
                    throw $TypeError(".proto.Message.newsletterScheduledMessage: object expected");
                m.newsletterScheduledMessage = $root.proto.Message.FutureProofMessage.fromObject(d.newsletterScheduledMessage, q + 1);
            }
            return m;
        };

        Message.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.conversation != null && $Object.hasOwnProperty.call(m, "conversation")) {
                d.conversation = m.conversation;
            }
            if (m.senderKeyDistributionMessage != null && $Object.hasOwnProperty.call(m, "senderKeyDistributionMessage")) {
                d.senderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.toObject(m.senderKeyDistributionMessage, o, q + 1);
            }
            if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                d.imageMessage = $root.proto.Message.ImageMessage.toObject(m.imageMessage, o, q + 1);
            }
            if (m.contactMessage != null && $Object.hasOwnProperty.call(m, "contactMessage")) {
                d.contactMessage = $root.proto.Message.ContactMessage.toObject(m.contactMessage, o, q + 1);
            }
            if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage")) {
                d.locationMessage = $root.proto.Message.LocationMessage.toObject(m.locationMessage, o, q + 1);
            }
            if (m.extendedTextMessage != null && $Object.hasOwnProperty.call(m, "extendedTextMessage")) {
                d.extendedTextMessage = $root.proto.Message.ExtendedTextMessage.toObject(m.extendedTextMessage, o, q + 1);
            }
            if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage")) {
                d.documentMessage = $root.proto.Message.DocumentMessage.toObject(m.documentMessage, o, q + 1);
            }
            if (m.audioMessage != null && $Object.hasOwnProperty.call(m, "audioMessage")) {
                d.audioMessage = $root.proto.Message.AudioMessage.toObject(m.audioMessage, o, q + 1);
            }
            if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                d.videoMessage = $root.proto.Message.VideoMessage.toObject(m.videoMessage, o, q + 1);
            }
            if (m.call != null && $Object.hasOwnProperty.call(m, "call")) {
                d.call = $root.proto.Message.Call.toObject(m.call, o, q + 1);
            }
            if (m.chat != null && $Object.hasOwnProperty.call(m, "chat")) {
                d.chat = $root.proto.Message.Chat.toObject(m.chat, o, q + 1);
            }
            if (m.protocolMessage != null && $Object.hasOwnProperty.call(m, "protocolMessage")) {
                d.protocolMessage = $root.proto.Message.ProtocolMessage.toObject(m.protocolMessage, o, q + 1);
            }
            if (m.contactsArrayMessage != null && $Object.hasOwnProperty.call(m, "contactsArrayMessage")) {
                d.contactsArrayMessage = $root.proto.Message.ContactsArrayMessage.toObject(m.contactsArrayMessage, o, q + 1);
            }
            if (m.highlyStructuredMessage != null && $Object.hasOwnProperty.call(m, "highlyStructuredMessage")) {
                d.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.toObject(m.highlyStructuredMessage, o, q + 1);
            }
            if (m.fastRatchetKeySenderKeyDistributionMessage != null && $Object.hasOwnProperty.call(m, "fastRatchetKeySenderKeyDistributionMessage")) {
                d.fastRatchetKeySenderKeyDistributionMessage = $root.proto.Message.SenderKeyDistributionMessage.toObject(m.fastRatchetKeySenderKeyDistributionMessage, o, q + 1);
            }
            if (m.sendPaymentMessage != null && $Object.hasOwnProperty.call(m, "sendPaymentMessage")) {
                d.sendPaymentMessage = $root.proto.Message.SendPaymentMessage.toObject(m.sendPaymentMessage, o, q + 1);
            }
            if (m.liveLocationMessage != null && $Object.hasOwnProperty.call(m, "liveLocationMessage")) {
                d.liveLocationMessage = $root.proto.Message.LiveLocationMessage.toObject(m.liveLocationMessage, o, q + 1);
            }
            if (m.requestPaymentMessage != null && $Object.hasOwnProperty.call(m, "requestPaymentMessage")) {
                d.requestPaymentMessage = $root.proto.Message.RequestPaymentMessage.toObject(m.requestPaymentMessage, o, q + 1);
            }
            if (m.declinePaymentRequestMessage != null && $Object.hasOwnProperty.call(m, "declinePaymentRequestMessage")) {
                d.declinePaymentRequestMessage = $root.proto.Message.DeclinePaymentRequestMessage.toObject(m.declinePaymentRequestMessage, o, q + 1);
            }
            if (m.cancelPaymentRequestMessage != null && $Object.hasOwnProperty.call(m, "cancelPaymentRequestMessage")) {
                d.cancelPaymentRequestMessage = $root.proto.Message.CancelPaymentRequestMessage.toObject(m.cancelPaymentRequestMessage, o, q + 1);
            }
            if (m.templateMessage != null && $Object.hasOwnProperty.call(m, "templateMessage")) {
                d.templateMessage = $root.proto.Message.TemplateMessage.toObject(m.templateMessage, o, q + 1);
            }
            if (m.stickerMessage != null && $Object.hasOwnProperty.call(m, "stickerMessage")) {
                d.stickerMessage = $root.proto.Message.StickerMessage.toObject(m.stickerMessage, o, q + 1);
            }
            if (m.groupInviteMessage != null && $Object.hasOwnProperty.call(m, "groupInviteMessage")) {
                d.groupInviteMessage = $root.proto.Message.GroupInviteMessage.toObject(m.groupInviteMessage, o, q + 1);
            }
            if (m.templateButtonReplyMessage != null && $Object.hasOwnProperty.call(m, "templateButtonReplyMessage")) {
                d.templateButtonReplyMessage = $root.proto.Message.TemplateButtonReplyMessage.toObject(m.templateButtonReplyMessage, o, q + 1);
            }
            if (m.productMessage != null && $Object.hasOwnProperty.call(m, "productMessage")) {
                d.productMessage = $root.proto.Message.ProductMessage.toObject(m.productMessage, o, q + 1);
            }
            if (m.deviceSentMessage != null && $Object.hasOwnProperty.call(m, "deviceSentMessage")) {
                d.deviceSentMessage = $root.proto.Message.DeviceSentMessage.toObject(m.deviceSentMessage, o, q + 1);
            }
            if (m.messageContextInfo != null && $Object.hasOwnProperty.call(m, "messageContextInfo")) {
                d.messageContextInfo = $root.proto.MessageContextInfo.toObject(m.messageContextInfo, o, q + 1);
            }
            if (m.listMessage != null && $Object.hasOwnProperty.call(m, "listMessage")) {
                d.listMessage = $root.proto.Message.ListMessage.toObject(m.listMessage, o, q + 1);
            }
            if (m.viewOnceMessage != null && $Object.hasOwnProperty.call(m, "viewOnceMessage")) {
                d.viewOnceMessage = $root.proto.Message.FutureProofMessage.toObject(m.viewOnceMessage, o, q + 1);
            }
            if (m.orderMessage != null && $Object.hasOwnProperty.call(m, "orderMessage")) {
                d.orderMessage = $root.proto.Message.OrderMessage.toObject(m.orderMessage, o, q + 1);
            }
            if (m.listResponseMessage != null && $Object.hasOwnProperty.call(m, "listResponseMessage")) {
                d.listResponseMessage = $root.proto.Message.ListResponseMessage.toObject(m.listResponseMessage, o, q + 1);
            }
            if (m.ephemeralMessage != null && $Object.hasOwnProperty.call(m, "ephemeralMessage")) {
                d.ephemeralMessage = $root.proto.Message.FutureProofMessage.toObject(m.ephemeralMessage, o, q + 1);
            }
            if (m.invoiceMessage != null && $Object.hasOwnProperty.call(m, "invoiceMessage")) {
                d.invoiceMessage = $root.proto.Message.InvoiceMessage.toObject(m.invoiceMessage, o, q + 1);
            }
            if (m.buttonsMessage != null && $Object.hasOwnProperty.call(m, "buttonsMessage")) {
                d.buttonsMessage = $root.proto.Message.ButtonsMessage.toObject(m.buttonsMessage, o, q + 1);
            }
            if (m.buttonsResponseMessage != null && $Object.hasOwnProperty.call(m, "buttonsResponseMessage")) {
                d.buttonsResponseMessage = $root.proto.Message.ButtonsResponseMessage.toObject(m.buttonsResponseMessage, o, q + 1);
            }
            if (m.paymentInviteMessage != null && $Object.hasOwnProperty.call(m, "paymentInviteMessage")) {
                d.paymentInviteMessage = $root.proto.Message.PaymentInviteMessage.toObject(m.paymentInviteMessage, o, q + 1);
            }
            if (m.interactiveMessage != null && $Object.hasOwnProperty.call(m, "interactiveMessage")) {
                d.interactiveMessage = $root.proto.Message.InteractiveMessage.toObject(m.interactiveMessage, o, q + 1);
            }
            if (m.reactionMessage != null && $Object.hasOwnProperty.call(m, "reactionMessage")) {
                d.reactionMessage = $root.proto.Message.ReactionMessage.toObject(m.reactionMessage, o, q + 1);
            }
            if (m.stickerSyncRmrMessage != null && $Object.hasOwnProperty.call(m, "stickerSyncRmrMessage")) {
                d.stickerSyncRmrMessage = $root.proto.Message.StickerSyncRMRMessage.toObject(m.stickerSyncRmrMessage, o, q + 1);
            }
            if (m.interactiveResponseMessage != null && $Object.hasOwnProperty.call(m, "interactiveResponseMessage")) {
                d.interactiveResponseMessage = $root.proto.Message.InteractiveResponseMessage.toObject(m.interactiveResponseMessage, o, q + 1);
            }
            if (m.pollCreationMessage != null && $Object.hasOwnProperty.call(m, "pollCreationMessage")) {
                d.pollCreationMessage = $root.proto.Message.PollCreationMessage.toObject(m.pollCreationMessage, o, q + 1);
            }
            if (m.pollUpdateMessage != null && $Object.hasOwnProperty.call(m, "pollUpdateMessage")) {
                d.pollUpdateMessage = $root.proto.Message.PollUpdateMessage.toObject(m.pollUpdateMessage, o, q + 1);
            }
            if (m.keepInChatMessage != null && $Object.hasOwnProperty.call(m, "keepInChatMessage")) {
                d.keepInChatMessage = $root.proto.Message.KeepInChatMessage.toObject(m.keepInChatMessage, o, q + 1);
            }
            if (m.documentWithCaptionMessage != null && $Object.hasOwnProperty.call(m, "documentWithCaptionMessage")) {
                d.documentWithCaptionMessage = $root.proto.Message.FutureProofMessage.toObject(m.documentWithCaptionMessage, o, q + 1);
            }
            if (m.requestPhoneNumberMessage != null && $Object.hasOwnProperty.call(m, "requestPhoneNumberMessage")) {
                d.requestPhoneNumberMessage = $root.proto.Message.RequestPhoneNumberMessage.toObject(m.requestPhoneNumberMessage, o, q + 1);
            }
            if (m.viewOnceMessageV2 != null && $Object.hasOwnProperty.call(m, "viewOnceMessageV2")) {
                d.viewOnceMessageV2 = $root.proto.Message.FutureProofMessage.toObject(m.viewOnceMessageV2, o, q + 1);
            }
            if (m.encReactionMessage != null && $Object.hasOwnProperty.call(m, "encReactionMessage")) {
                d.encReactionMessage = $root.proto.Message.EncReactionMessage.toObject(m.encReactionMessage, o, q + 1);
            }
            if (m.editedMessage != null && $Object.hasOwnProperty.call(m, "editedMessage")) {
                d.editedMessage = $root.proto.Message.FutureProofMessage.toObject(m.editedMessage, o, q + 1);
            }
            if (m.viewOnceMessageV2Extension != null && $Object.hasOwnProperty.call(m, "viewOnceMessageV2Extension")) {
                d.viewOnceMessageV2Extension = $root.proto.Message.FutureProofMessage.toObject(m.viewOnceMessageV2Extension, o, q + 1);
            }
            if (m.pollCreationMessageV2 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV2")) {
                d.pollCreationMessageV2 = $root.proto.Message.PollCreationMessage.toObject(m.pollCreationMessageV2, o, q + 1);
            }
            if (m.scheduledCallCreationMessage != null && $Object.hasOwnProperty.call(m, "scheduledCallCreationMessage")) {
                d.scheduledCallCreationMessage = $root.proto.Message.ScheduledCallCreationMessage.toObject(m.scheduledCallCreationMessage, o, q + 1);
            }
            if (m.groupMentionedMessage != null && $Object.hasOwnProperty.call(m, "groupMentionedMessage")) {
                d.groupMentionedMessage = $root.proto.Message.FutureProofMessage.toObject(m.groupMentionedMessage, o, q + 1);
            }
            if (m.pinInChatMessage != null && $Object.hasOwnProperty.call(m, "pinInChatMessage")) {
                d.pinInChatMessage = $root.proto.Message.PinInChatMessage.toObject(m.pinInChatMessage, o, q + 1);
            }
            if (m.pollCreationMessageV3 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV3")) {
                d.pollCreationMessageV3 = $root.proto.Message.PollCreationMessage.toObject(m.pollCreationMessageV3, o, q + 1);
            }
            if (m.scheduledCallEditMessage != null && $Object.hasOwnProperty.call(m, "scheduledCallEditMessage")) {
                d.scheduledCallEditMessage = $root.proto.Message.ScheduledCallEditMessage.toObject(m.scheduledCallEditMessage, o, q + 1);
            }
            if (m.ptvMessage != null && $Object.hasOwnProperty.call(m, "ptvMessage")) {
                d.ptvMessage = $root.proto.Message.VideoMessage.toObject(m.ptvMessage, o, q + 1);
            }
            if (m.botInvokeMessage != null && $Object.hasOwnProperty.call(m, "botInvokeMessage")) {
                d.botInvokeMessage = $root.proto.Message.FutureProofMessage.toObject(m.botInvokeMessage, o, q + 1);
            }
            if (m.callLogMesssage != null && $Object.hasOwnProperty.call(m, "callLogMesssage")) {
                d.callLogMesssage = $root.proto.Message.CallLogMessage.toObject(m.callLogMesssage, o, q + 1);
            }
            if (m.messageHistoryBundle != null && $Object.hasOwnProperty.call(m, "messageHistoryBundle")) {
                d.messageHistoryBundle = $root.proto.Message.MessageHistoryBundle.toObject(m.messageHistoryBundle, o, q + 1);
            }
            if (m.encCommentMessage != null && $Object.hasOwnProperty.call(m, "encCommentMessage")) {
                d.encCommentMessage = $root.proto.Message.EncCommentMessage.toObject(m.encCommentMessage, o, q + 1);
            }
            if (m.bcallMessage != null && $Object.hasOwnProperty.call(m, "bcallMessage")) {
                d.bcallMessage = $root.proto.Message.BCallMessage.toObject(m.bcallMessage, o, q + 1);
            }
            if (m.lottieStickerMessage != null && $Object.hasOwnProperty.call(m, "lottieStickerMessage")) {
                d.lottieStickerMessage = $root.proto.Message.FutureProofMessage.toObject(m.lottieStickerMessage, o, q + 1);
            }
            if (m.eventMessage != null && $Object.hasOwnProperty.call(m, "eventMessage")) {
                d.eventMessage = $root.proto.Message.EventMessage.toObject(m.eventMessage, o, q + 1);
            }
            if (m.encEventResponseMessage != null && $Object.hasOwnProperty.call(m, "encEventResponseMessage")) {
                d.encEventResponseMessage = $root.proto.Message.EncEventResponseMessage.toObject(m.encEventResponseMessage, o, q + 1);
            }
            if (m.commentMessage != null && $Object.hasOwnProperty.call(m, "commentMessage")) {
                d.commentMessage = $root.proto.Message.CommentMessage.toObject(m.commentMessage, o, q + 1);
            }
            if (m.newsletterAdminInviteMessage != null && $Object.hasOwnProperty.call(m, "newsletterAdminInviteMessage")) {
                d.newsletterAdminInviteMessage = $root.proto.Message.NewsletterAdminInviteMessage.toObject(m.newsletterAdminInviteMessage, o, q + 1);
            }
            if (m.placeholderMessage != null && $Object.hasOwnProperty.call(m, "placeholderMessage")) {
                d.placeholderMessage = $root.proto.Message.PlaceholderMessage.toObject(m.placeholderMessage, o, q + 1);
            }
            if (m.secretEncryptedMessage != null && $Object.hasOwnProperty.call(m, "secretEncryptedMessage")) {
                d.secretEncryptedMessage = $root.proto.Message.SecretEncryptedMessage.toObject(m.secretEncryptedMessage, o, q + 1);
            }
            if (m.albumMessage != null && $Object.hasOwnProperty.call(m, "albumMessage")) {
                d.albumMessage = $root.proto.Message.AlbumMessage.toObject(m.albumMessage, o, q + 1);
            }
            if (m.eventCoverImage != null && $Object.hasOwnProperty.call(m, "eventCoverImage")) {
                d.eventCoverImage = $root.proto.Message.FutureProofMessage.toObject(m.eventCoverImage, o, q + 1);
            }
            if (m.stickerPackMessage != null && $Object.hasOwnProperty.call(m, "stickerPackMessage")) {
                d.stickerPackMessage = $root.proto.Message.StickerPackMessage.toObject(m.stickerPackMessage, o, q + 1);
            }
            if (m.statusMentionMessage != null && $Object.hasOwnProperty.call(m, "statusMentionMessage")) {
                d.statusMentionMessage = $root.proto.Message.FutureProofMessage.toObject(m.statusMentionMessage, o, q + 1);
            }
            if (m.pollResultSnapshotMessage != null && $Object.hasOwnProperty.call(m, "pollResultSnapshotMessage")) {
                d.pollResultSnapshotMessage = $root.proto.Message.PollResultSnapshotMessage.toObject(m.pollResultSnapshotMessage, o, q + 1);
            }
            if (m.pollCreationOptionImageMessage != null && $Object.hasOwnProperty.call(m, "pollCreationOptionImageMessage")) {
                d.pollCreationOptionImageMessage = $root.proto.Message.FutureProofMessage.toObject(m.pollCreationOptionImageMessage, o, q + 1);
            }
            if (m.associatedChildMessage != null && $Object.hasOwnProperty.call(m, "associatedChildMessage")) {
                d.associatedChildMessage = $root.proto.Message.FutureProofMessage.toObject(m.associatedChildMessage, o, q + 1);
            }
            if (m.groupStatusMentionMessage != null && $Object.hasOwnProperty.call(m, "groupStatusMentionMessage")) {
                d.groupStatusMentionMessage = $root.proto.Message.FutureProofMessage.toObject(m.groupStatusMentionMessage, o, q + 1);
            }
            if (m.pollCreationMessageV4 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV4")) {
                d.pollCreationMessageV4 = $root.proto.Message.FutureProofMessage.toObject(m.pollCreationMessageV4, o, q + 1);
            }
            if (m.statusAddYours != null && $Object.hasOwnProperty.call(m, "statusAddYours")) {
                d.statusAddYours = $root.proto.Message.FutureProofMessage.toObject(m.statusAddYours, o, q + 1);
            }
            if (m.groupStatusMessage != null && $Object.hasOwnProperty.call(m, "groupStatusMessage")) {
                d.groupStatusMessage = $root.proto.Message.FutureProofMessage.toObject(m.groupStatusMessage, o, q + 1);
            }
            if (m.richResponseMessage != null && $Object.hasOwnProperty.call(m, "richResponseMessage")) {
                d.richResponseMessage = $root.proto.AIRichResponseMessage.toObject(m.richResponseMessage, o, q + 1);
            }
            if (m.statusNotificationMessage != null && $Object.hasOwnProperty.call(m, "statusNotificationMessage")) {
                d.statusNotificationMessage = $root.proto.Message.StatusNotificationMessage.toObject(m.statusNotificationMessage, o, q + 1);
            }
            if (m.limitSharingMessage != null && $Object.hasOwnProperty.call(m, "limitSharingMessage")) {
                d.limitSharingMessage = $root.proto.Message.FutureProofMessage.toObject(m.limitSharingMessage, o, q + 1);
            }
            if (m.botTaskMessage != null && $Object.hasOwnProperty.call(m, "botTaskMessage")) {
                d.botTaskMessage = $root.proto.Message.FutureProofMessage.toObject(m.botTaskMessage, o, q + 1);
            }
            if (m.questionMessage != null && $Object.hasOwnProperty.call(m, "questionMessage")) {
                d.questionMessage = $root.proto.Message.FutureProofMessage.toObject(m.questionMessage, o, q + 1);
            }
            if (m.messageHistoryNotice != null && $Object.hasOwnProperty.call(m, "messageHistoryNotice")) {
                d.messageHistoryNotice = $root.proto.Message.MessageHistoryNotice.toObject(m.messageHistoryNotice, o, q + 1);
            }
            if (m.groupStatusMessageV2 != null && $Object.hasOwnProperty.call(m, "groupStatusMessageV2")) {
                d.groupStatusMessageV2 = $root.proto.Message.FutureProofMessage.toObject(m.groupStatusMessageV2, o, q + 1);
            }
            if (m.botForwardedMessage != null && $Object.hasOwnProperty.call(m, "botForwardedMessage")) {
                d.botForwardedMessage = $root.proto.Message.FutureProofMessage.toObject(m.botForwardedMessage, o, q + 1);
            }
            if (m.statusQuestionAnswerMessage != null && $Object.hasOwnProperty.call(m, "statusQuestionAnswerMessage")) {
                d.statusQuestionAnswerMessage = $root.proto.Message.StatusQuestionAnswerMessage.toObject(m.statusQuestionAnswerMessage, o, q + 1);
            }
            if (m.questionReplyMessage != null && $Object.hasOwnProperty.call(m, "questionReplyMessage")) {
                d.questionReplyMessage = $root.proto.Message.FutureProofMessage.toObject(m.questionReplyMessage, o, q + 1);
            }
            if (m.questionResponseMessage != null && $Object.hasOwnProperty.call(m, "questionResponseMessage")) {
                d.questionResponseMessage = $root.proto.Message.QuestionResponseMessage.toObject(m.questionResponseMessage, o, q + 1);
            }
            if (m.statusQuotedMessage != null && $Object.hasOwnProperty.call(m, "statusQuotedMessage")) {
                d.statusQuotedMessage = $root.proto.Message.StatusQuotedMessage.toObject(m.statusQuotedMessage, o, q + 1);
            }
            if (m.statusStickerInteractionMessage != null && $Object.hasOwnProperty.call(m, "statusStickerInteractionMessage")) {
                d.statusStickerInteractionMessage = $root.proto.Message.StatusStickerInteractionMessage.toObject(m.statusStickerInteractionMessage, o, q + 1);
            }
            if (m.pollCreationMessageV5 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV5")) {
                d.pollCreationMessageV5 = $root.proto.Message.PollCreationMessage.toObject(m.pollCreationMessageV5, o, q + 1);
            }
            if (m.newsletterFollowerInviteMessageV2 != null && $Object.hasOwnProperty.call(m, "newsletterFollowerInviteMessageV2")) {
                d.newsletterFollowerInviteMessageV2 = $root.proto.Message.NewsletterFollowerInviteMessage.toObject(m.newsletterFollowerInviteMessageV2, o, q + 1);
            }
            if (m.pollResultSnapshotMessageV3 != null && $Object.hasOwnProperty.call(m, "pollResultSnapshotMessageV3")) {
                d.pollResultSnapshotMessageV3 = $root.proto.Message.PollResultSnapshotMessage.toObject(m.pollResultSnapshotMessageV3, o, q + 1);
            }
            if (m.newsletterAdminProfileMessage != null && $Object.hasOwnProperty.call(m, "newsletterAdminProfileMessage")) {
                d.newsletterAdminProfileMessage = $root.proto.Message.FutureProofMessage.toObject(m.newsletterAdminProfileMessage, o, q + 1);
            }
            if (m.spoilerMessage != null && $Object.hasOwnProperty.call(m, "spoilerMessage")) {
                d.spoilerMessage = $root.proto.Message.FutureProofMessage.toObject(m.spoilerMessage, o, q + 1);
            }
            if (m.pollCreationMessageV6 != null && $Object.hasOwnProperty.call(m, "pollCreationMessageV6")) {
                d.pollCreationMessageV6 = $root.proto.Message.PollCreationMessage.toObject(m.pollCreationMessageV6, o, q + 1);
            }
            if (m.conditionalRevealMessage != null && $Object.hasOwnProperty.call(m, "conditionalRevealMessage")) {
                d.conditionalRevealMessage = $root.proto.Message.ConditionalRevealMessage.toObject(m.conditionalRevealMessage, o, q + 1);
            }
            if (m.pollAddOptionMessage != null && $Object.hasOwnProperty.call(m, "pollAddOptionMessage")) {
                d.pollAddOptionMessage = $root.proto.Message.PollAddOptionMessage.toObject(m.pollAddOptionMessage, o, q + 1);
            }
            if (m.eventInviteMessage != null && $Object.hasOwnProperty.call(m, "eventInviteMessage")) {
                d.eventInviteMessage = $root.proto.Message.EventInviteMessage.toObject(m.eventInviteMessage, o, q + 1);
            }
            if (m.groupRootKeyShare != null && $Object.hasOwnProperty.call(m, "groupRootKeyShare")) {
                d.groupRootKeyShare = $root.proto.GroupRootKeyShare.toObject(m.groupRootKeyShare, o, q + 1);
            }
            if (m.paymentReminderMessage != null && $Object.hasOwnProperty.call(m, "paymentReminderMessage")) {
                d.paymentReminderMessage = $root.proto.Message.PaymentReminderMessage.toObject(m.paymentReminderMessage, o, q + 1);
            }
            if (m.splitPaymentMessage != null && $Object.hasOwnProperty.call(m, "splitPaymentMessage")) {
                d.splitPaymentMessage = $root.proto.Message.SplitPaymentMessage.toObject(m.splitPaymentMessage, o, q + 1);
            }
            if (m.newsletterAdminProfileStatusMessage != null && $Object.hasOwnProperty.call(m, "newsletterAdminProfileStatusMessage")) {
                d.newsletterAdminProfileStatusMessage = $root.proto.Message.FutureProofMessage.toObject(m.newsletterAdminProfileStatusMessage, o, q + 1);
            }
            if (m.rootSecretDistributeMessage != null && $Object.hasOwnProperty.call(m, "rootSecretDistributeMessage")) {
                d.rootSecretDistributeMessage = $root.proto.Message.RootSecretDistributeMessage.toObject(m.rootSecretDistributeMessage, o, q + 1);
            }
            if (m.splitPaymentUpdateMessage != null && $Object.hasOwnProperty.call(m, "splitPaymentUpdateMessage")) {
                d.splitPaymentUpdateMessage = $root.proto.Message.SplitPaymentUpdateMessage.toObject(m.splitPaymentUpdateMessage, o, q + 1);
            }
            if (m.musicMessage != null && $Object.hasOwnProperty.call(m, "musicMessage")) {
                d.musicMessage = $root.proto.Message.MusicMessage.toObject(m.musicMessage, o, q + 1);
            }
            if (m.statusLinkPreviewMetadata != null && $Object.hasOwnProperty.call(m, "statusLinkPreviewMetadata")) {
                d.statusLinkPreviewMetadata = $root.proto.Message.StatusLinkPreviewMetadata.toObject(m.statusLinkPreviewMetadata, o, q + 1);
            }
            if (m.botPlatformRegistrationSuccessMessage != null && $Object.hasOwnProperty.call(m, "botPlatformRegistrationSuccessMessage")) {
                d.botPlatformRegistrationSuccessMessage = $root.proto.Message.FutureProofMessage.toObject(m.botPlatformRegistrationSuccessMessage, o, q + 1);
            }
            if (m.newsletterScheduledMessage != null && $Object.hasOwnProperty.call(m, "newsletterScheduledMessage")) {
                d.newsletterScheduledMessage = $root.proto.Message.FutureProofMessage.toObject(m.newsletterScheduledMessage, o, q + 1);
            }
            return d;
        };

        Message.prototype.toJSON = function() {
            return Message.toObject(this, $protobuf.util.toJSONOptions);
        };

        Message.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Message";
        };

        Message.AlbumMessage = (function() {

            const AlbumMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AlbumMessage.prototype.expectedImageCount = null;
            AlbumMessage.prototype.expectedVideoCount = null;
            AlbumMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AlbumMessage.prototype, "_expectedImageCount", {
                get: $util.oneOfGetter($oneOfFields = ["expectedImageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AlbumMessage.prototype, "_expectedVideoCount", {
                get: $util.oneOfGetter($oneOfFields = ["expectedVideoCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AlbumMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AlbumMessage.create = function(properties) {
                return new AlbumMessage(properties);
            };

            AlbumMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.expectedImageCount != null && $Object.hasOwnProperty.call(m, "expectedImageCount"))
                    w.uint32(16).uint32(m.expectedImageCount);
                if (m.expectedVideoCount != null && $Object.hasOwnProperty.call(m, "expectedVideoCount"))
                    w.uint32(24).uint32(m.expectedVideoCount);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AlbumMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AlbumMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 2: {
                            if (u !== 0)
                                break;
                            m.expectedImageCount = r.uint32();
                            m._expectedImageCount = "expectedImageCount";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.expectedVideoCount = r.uint32();
                            m._expectedVideoCount = "expectedVideoCount";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AlbumMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AlbumMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AlbumMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AlbumMessage();
                if (d.expectedImageCount != null) {
                    m.expectedImageCount = d.expectedImageCount >>> 0;
                }
                if (d.expectedVideoCount != null) {
                    m.expectedVideoCount = d.expectedVideoCount >>> 0;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.AlbumMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            AlbumMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.expectedImageCount != null && $Object.hasOwnProperty.call(m, "expectedImageCount")) {
                    d.expectedImageCount = m.expectedImageCount;
                }
                if (m.expectedVideoCount != null && $Object.hasOwnProperty.call(m, "expectedVideoCount")) {
                    d.expectedVideoCount = m.expectedVideoCount;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            AlbumMessage.prototype.toJSON = function() {
                return AlbumMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            AlbumMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AlbumMessage";
            };

            return AlbumMessage;
        })();

        Message.AppStateFatalExceptionNotification = (function() {

            const AppStateFatalExceptionNotification = function (p) {
                this.collectionNames = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateFatalExceptionNotification.prototype.collectionNames = $util.emptyArray;
            AppStateFatalExceptionNotification.prototype.timestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateFatalExceptionNotification.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppStateFatalExceptionNotification.create = function(properties) {
                return new AppStateFatalExceptionNotification(properties);
            };

            AppStateFatalExceptionNotification.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.collectionNames != null && m.collectionNames.length) {
                    for (var i = 0; i < m.collectionNames.length; ++i)
                        w.uint32(10).string(m.collectionNames[i]);
                }
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                    w.uint32(16).int64(m.timestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateFatalExceptionNotification.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateFatalExceptionNotification();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.collectionNames && m.collectionNames.length))
                                m.collectionNames = [];
                            m.collectionNames.push(r.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.timestamp = r.int64();
                            m._timestamp = "timestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateFatalExceptionNotification.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateFatalExceptionNotification)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateFatalExceptionNotification: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateFatalExceptionNotification();
                if (d.collectionNames) {
                    if (!$Array.isArray(d.collectionNames))
                        throw $TypeError(".proto.Message.AppStateFatalExceptionNotification.collectionNames: array expected");
                    m.collectionNames = $Array(d.collectionNames.length);
                    for (var i = 0; i < d.collectionNames.length; ++i) {
                        m.collectionNames[i] = $String(d.collectionNames[i]);
                    }
                }
                if (d.timestamp != null) {
                    if ($util.Long)
                        m.timestamp = $util.Long.fromValue(d.timestamp, false);
                    else if (typeof d.timestamp === "string")
                        m.timestamp = $parseInt(d.timestamp, 10);
                    else if (typeof d.timestamp === "number")
                        m.timestamp = d.timestamp;
                    else if (typeof d.timestamp === "object")
                        m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                }
                return m;
            };

            AppStateFatalExceptionNotification.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.collectionNames = [];
                }
                if (m.collectionNames && m.collectionNames.length) {
                    d.collectionNames = $Array(m.collectionNames.length);
                    for (var j = 0; j < m.collectionNames.length; ++j) {
                        d.collectionNames[j] = m.collectionNames[j];
                    }
                }
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.timestamp === "number")
                        d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                    else
                        d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                }
                return d;
            };

            AppStateFatalExceptionNotification.prototype.toJSON = function() {
                return AppStateFatalExceptionNotification.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateFatalExceptionNotification.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateFatalExceptionNotification";
            };

            return AppStateFatalExceptionNotification;
        })();

        Message.AppStateSyncKey = (function() {

            const AppStateSyncKey = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateSyncKey.prototype.keyId = null;
            AppStateSyncKey.prototype.keyData = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKey.prototype, "_keyId", {
                get: $util.oneOfGetter($oneOfFields = ["keyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKey.prototype, "_keyData", {
                get: $util.oneOfGetter($oneOfFields = ["keyData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppStateSyncKey.create = function(properties) {
                return new AppStateSyncKey(properties);
            };

            AppStateSyncKey.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                    $root.proto.Message.AppStateSyncKeyId.encode(m.keyId, w.uint32(10).fork(), q + 1).ldelim();
                if (m.keyData != null && $Object.hasOwnProperty.call(m, "keyData"))
                    $root.proto.Message.AppStateSyncKeyData.encode(m.keyData, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateSyncKey.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateSyncKey();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.keyId = $root.proto.Message.AppStateSyncKeyId.decode(r, r.uint32(), $undefined, q + 1, m.keyId);
                            m._keyId = "keyId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.keyData = $root.proto.Message.AppStateSyncKeyData.decode(r, r.uint32(), $undefined, q + 1, m.keyData);
                            m._keyData = "keyData";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateSyncKey.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateSyncKey)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateSyncKey: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateSyncKey();
                if (d.keyId != null) {
                    if (!$util.isObject(d.keyId))
                        throw $TypeError(".proto.Message.AppStateSyncKey.keyId: object expected");
                    m.keyId = $root.proto.Message.AppStateSyncKeyId.fromObject(d.keyId, q + 1);
                }
                if (d.keyData != null) {
                    if (!$util.isObject(d.keyData))
                        throw $TypeError(".proto.Message.AppStateSyncKey.keyData: object expected");
                    m.keyData = $root.proto.Message.AppStateSyncKeyData.fromObject(d.keyData, q + 1);
                }
                return m;
            };

            AppStateSyncKey.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                    d.keyId = $root.proto.Message.AppStateSyncKeyId.toObject(m.keyId, o, q + 1);
                }
                if (m.keyData != null && $Object.hasOwnProperty.call(m, "keyData")) {
                    d.keyData = $root.proto.Message.AppStateSyncKeyData.toObject(m.keyData, o, q + 1);
                }
                return d;
            };

            AppStateSyncKey.prototype.toJSON = function() {
                return AppStateSyncKey.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateSyncKey.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateSyncKey";
            };

            return AppStateSyncKey;
        })();

        Message.AppStateSyncKeyData = (function() {

            const AppStateSyncKeyData = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateSyncKeyData.prototype.keyData = null;
            AppStateSyncKeyData.prototype.fingerprint = null;
            AppStateSyncKeyData.prototype.timestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKeyData.prototype, "_keyData", {
                get: $util.oneOfGetter($oneOfFields = ["keyData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKeyData.prototype, "_fingerprint", {
                get: $util.oneOfGetter($oneOfFields = ["fingerprint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKeyData.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppStateSyncKeyData.create = function(properties) {
                return new AppStateSyncKeyData(properties);
            };

            AppStateSyncKeyData.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.keyData != null && $Object.hasOwnProperty.call(m, "keyData"))
                    w.uint32(10).bytes(m.keyData);
                if (m.fingerprint != null && $Object.hasOwnProperty.call(m, "fingerprint"))
                    $root.proto.Message.AppStateSyncKeyFingerprint.encode(m.fingerprint, w.uint32(18).fork(), q + 1).ldelim();
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                    w.uint32(24).int64(m.timestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateSyncKeyData.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateSyncKeyData();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.keyData = r.bytes();
                            m._keyData = "keyData";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.fingerprint = $root.proto.Message.AppStateSyncKeyFingerprint.decode(r, r.uint32(), $undefined, q + 1, m.fingerprint);
                            m._fingerprint = "fingerprint";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.timestamp = r.int64();
                            m._timestamp = "timestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateSyncKeyData.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateSyncKeyData)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateSyncKeyData: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateSyncKeyData();
                if (d.keyData != null) {
                    if (typeof d.keyData === "string")
                        $util.base64.decode(d.keyData, m.keyData = $util.newBuffer($util.base64.length(d.keyData)), 0);
                    else if (d.keyData.length >= 0)
                        m.keyData = d.keyData;
                }
                if (d.fingerprint != null) {
                    if (!$util.isObject(d.fingerprint))
                        throw $TypeError(".proto.Message.AppStateSyncKeyData.fingerprint: object expected");
                    m.fingerprint = $root.proto.Message.AppStateSyncKeyFingerprint.fromObject(d.fingerprint, q + 1);
                }
                if (d.timestamp != null) {
                    if ($util.Long)
                        m.timestamp = $util.Long.fromValue(d.timestamp, false);
                    else if (typeof d.timestamp === "string")
                        m.timestamp = $parseInt(d.timestamp, 10);
                    else if (typeof d.timestamp === "number")
                        m.timestamp = d.timestamp;
                    else if (typeof d.timestamp === "object")
                        m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                }
                return m;
            };

            AppStateSyncKeyData.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.keyData != null && $Object.hasOwnProperty.call(m, "keyData")) {
                    d.keyData = o.bytes === $String ? $util.base64.encode(m.keyData, 0, m.keyData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.keyData) : m.keyData;
                }
                if (m.fingerprint != null && $Object.hasOwnProperty.call(m, "fingerprint")) {
                    d.fingerprint = $root.proto.Message.AppStateSyncKeyFingerprint.toObject(m.fingerprint, o, q + 1);
                }
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.timestamp === "number")
                        d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                    else
                        d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                }
                return d;
            };

            AppStateSyncKeyData.prototype.toJSON = function() {
                return AppStateSyncKeyData.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateSyncKeyData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateSyncKeyData";
            };

            return AppStateSyncKeyData;
        })();

        Message.AppStateSyncKeyFingerprint = (function() {

            const AppStateSyncKeyFingerprint = function (p) {
                this.deviceIndexes = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateSyncKeyFingerprint.prototype.rawId = null;
            AppStateSyncKeyFingerprint.prototype.currentIndex = null;
            AppStateSyncKeyFingerprint.prototype.deviceIndexes = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKeyFingerprint.prototype, "_rawId", {
                get: $util.oneOfGetter($oneOfFields = ["rawId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKeyFingerprint.prototype, "_currentIndex", {
                get: $util.oneOfGetter($oneOfFields = ["currentIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppStateSyncKeyFingerprint.create = function(properties) {
                return new AppStateSyncKeyFingerprint(properties);
            };

            AppStateSyncKeyFingerprint.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.rawId != null && $Object.hasOwnProperty.call(m, "rawId"))
                    w.uint32(8).uint32(m.rawId);
                if (m.currentIndex != null && $Object.hasOwnProperty.call(m, "currentIndex"))
                    w.uint32(16).uint32(m.currentIndex);
                if (m.deviceIndexes != null && m.deviceIndexes.length) {
                    w.uint32(26).uint32s(m.deviceIndexes);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateSyncKeyFingerprint.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateSyncKeyFingerprint();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.rawId = r.uint32();
                            m._rawId = "rawId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.currentIndex = r.uint32();
                            m._currentIndex = "currentIndex";
                            continue;
                        }
                    case 3: {
                            if (u === 2) {
                                if (!(m.deviceIndexes && m.deviceIndexes.length))
                                    m.deviceIndexes = [];
                                r.uint32s(m.deviceIndexes);
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(m.deviceIndexes && m.deviceIndexes.length))
                                m.deviceIndexes = [];
                            m.deviceIndexes.push(r.uint32());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateSyncKeyFingerprint.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateSyncKeyFingerprint)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateSyncKeyFingerprint: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateSyncKeyFingerprint();
                if (d.rawId != null) {
                    m.rawId = d.rawId >>> 0;
                }
                if (d.currentIndex != null) {
                    m.currentIndex = d.currentIndex >>> 0;
                }
                if (d.deviceIndexes) {
                    if (!$Array.isArray(d.deviceIndexes))
                        throw $TypeError(".proto.Message.AppStateSyncKeyFingerprint.deviceIndexes: array expected");
                    m.deviceIndexes = $Array(d.deviceIndexes.length);
                    for (var i = 0; i < d.deviceIndexes.length; ++i) {
                        m.deviceIndexes[i] = d.deviceIndexes[i] >>> 0;
                    }
                }
                return m;
            };

            AppStateSyncKeyFingerprint.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.deviceIndexes = [];
                }
                if (m.rawId != null && $Object.hasOwnProperty.call(m, "rawId")) {
                    d.rawId = m.rawId;
                }
                if (m.currentIndex != null && $Object.hasOwnProperty.call(m, "currentIndex")) {
                    d.currentIndex = m.currentIndex;
                }
                if (m.deviceIndexes && m.deviceIndexes.length) {
                    d.deviceIndexes = $Array(m.deviceIndexes.length);
                    for (var j = 0; j < m.deviceIndexes.length; ++j) {
                        d.deviceIndexes[j] = m.deviceIndexes[j];
                    }
                }
                return d;
            };

            AppStateSyncKeyFingerprint.prototype.toJSON = function() {
                return AppStateSyncKeyFingerprint.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateSyncKeyFingerprint.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateSyncKeyFingerprint";
            };

            return AppStateSyncKeyFingerprint;
        })();

        Message.AppStateSyncKeyId = (function() {

            const AppStateSyncKeyId = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateSyncKeyId.prototype.keyId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AppStateSyncKeyId.prototype, "_keyId", {
                get: $util.oneOfGetter($oneOfFields = ["keyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AppStateSyncKeyId.create = function(properties) {
                return new AppStateSyncKeyId(properties);
            };

            AppStateSyncKeyId.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                    w.uint32(10).bytes(m.keyId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateSyncKeyId.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateSyncKeyId();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.keyId = r.bytes();
                            m._keyId = "keyId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateSyncKeyId.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateSyncKeyId)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateSyncKeyId: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateSyncKeyId();
                if (d.keyId != null) {
                    if (typeof d.keyId === "string")
                        $util.base64.decode(d.keyId, m.keyId = $util.newBuffer($util.base64.length(d.keyId)), 0);
                    else if (d.keyId.length >= 0)
                        m.keyId = d.keyId;
                }
                return m;
            };

            AppStateSyncKeyId.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                    d.keyId = o.bytes === $String ? $util.base64.encode(m.keyId, 0, m.keyId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.keyId) : m.keyId;
                }
                return d;
            };

            AppStateSyncKeyId.prototype.toJSON = function() {
                return AppStateSyncKeyId.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateSyncKeyId.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateSyncKeyId";
            };

            return AppStateSyncKeyId;
        })();

        Message.AppStateSyncKeyRequest = (function() {

            const AppStateSyncKeyRequest = function (p) {
                this.keyIds = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateSyncKeyRequest.prototype.keyIds = $util.emptyArray;

            AppStateSyncKeyRequest.create = function(properties) {
                return new AppStateSyncKeyRequest(properties);
            };

            AppStateSyncKeyRequest.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.keyIds != null && m.keyIds.length) {
                    for (var i = 0; i < m.keyIds.length; ++i)
                        $root.proto.Message.AppStateSyncKeyId.encode(m.keyIds[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateSyncKeyRequest.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateSyncKeyRequest();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.keyIds && m.keyIds.length))
                                m.keyIds = [];
                            m.keyIds.push($root.proto.Message.AppStateSyncKeyId.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateSyncKeyRequest.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateSyncKeyRequest)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateSyncKeyRequest: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateSyncKeyRequest();
                if (d.keyIds) {
                    if (!$Array.isArray(d.keyIds))
                        throw $TypeError(".proto.Message.AppStateSyncKeyRequest.keyIds: array expected");
                    m.keyIds = $Array(d.keyIds.length);
                    for (var i = 0; i < d.keyIds.length; ++i) {
                        if (!$util.isObject(d.keyIds[i]))
                            throw $TypeError(".proto.Message.AppStateSyncKeyRequest.keyIds: object expected");
                        m.keyIds[i] = $root.proto.Message.AppStateSyncKeyId.fromObject(d.keyIds[i], q + 1);
                    }
                }
                return m;
            };

            AppStateSyncKeyRequest.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.keyIds = [];
                }
                if (m.keyIds && m.keyIds.length) {
                    d.keyIds = $Array(m.keyIds.length);
                    for (var j = 0; j < m.keyIds.length; ++j) {
                        d.keyIds[j] = $root.proto.Message.AppStateSyncKeyId.toObject(m.keyIds[j], o, q + 1);
                    }
                }
                return d;
            };

            AppStateSyncKeyRequest.prototype.toJSON = function() {
                return AppStateSyncKeyRequest.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateSyncKeyRequest.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateSyncKeyRequest";
            };

            return AppStateSyncKeyRequest;
        })();

        Message.AppStateSyncKeyShare = (function() {

            const AppStateSyncKeyShare = function (p) {
                this.keys = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AppStateSyncKeyShare.prototype.keys = $util.emptyArray;

            AppStateSyncKeyShare.create = function(properties) {
                return new AppStateSyncKeyShare(properties);
            };

            AppStateSyncKeyShare.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.keys != null && m.keys.length) {
                    for (var i = 0; i < m.keys.length; ++i)
                        $root.proto.Message.AppStateSyncKey.encode(m.keys[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AppStateSyncKeyShare.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AppStateSyncKeyShare();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.keys && m.keys.length))
                                m.keys = [];
                            m.keys.push($root.proto.Message.AppStateSyncKey.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AppStateSyncKeyShare.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AppStateSyncKeyShare)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AppStateSyncKeyShare: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AppStateSyncKeyShare();
                if (d.keys) {
                    if (!$Array.isArray(d.keys))
                        throw $TypeError(".proto.Message.AppStateSyncKeyShare.keys: array expected");
                    m.keys = $Array(d.keys.length);
                    for (var i = 0; i < d.keys.length; ++i) {
                        if (!$util.isObject(d.keys[i]))
                            throw $TypeError(".proto.Message.AppStateSyncKeyShare.keys: object expected");
                        m.keys[i] = $root.proto.Message.AppStateSyncKey.fromObject(d.keys[i], q + 1);
                    }
                }
                return m;
            };

            AppStateSyncKeyShare.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.keys = [];
                }
                if (m.keys && m.keys.length) {
                    d.keys = $Array(m.keys.length);
                    for (var j = 0; j < m.keys.length; ++j) {
                        d.keys[j] = $root.proto.Message.AppStateSyncKey.toObject(m.keys[j], o, q + 1);
                    }
                }
                return d;
            };

            AppStateSyncKeyShare.prototype.toJSON = function() {
                return AppStateSyncKeyShare.toObject(this, $protobuf.util.toJSONOptions);
            };

            AppStateSyncKeyShare.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AppStateSyncKeyShare";
            };

            return AppStateSyncKeyShare;
        })();

        Message.AudioMessage = (function() {

            const AudioMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AudioMessage.prototype.url = null;
            AudioMessage.prototype.mimetype = null;
            AudioMessage.prototype.fileSha256 = null;
            AudioMessage.prototype.fileLength = null;
            AudioMessage.prototype.seconds = null;
            AudioMessage.prototype.ptt = null;
            AudioMessage.prototype.mediaKey = null;
            AudioMessage.prototype.fileEncSha256 = null;
            AudioMessage.prototype.directPath = null;
            AudioMessage.prototype.mediaKeyTimestamp = null;
            AudioMessage.prototype.contextInfo = null;
            AudioMessage.prototype.streamingSidecar = null;
            AudioMessage.prototype.waveform = null;
            AudioMessage.prototype.backgroundArgb = null;
            AudioMessage.prototype.viewOnce = null;
            AudioMessage.prototype.accessibilityLabel = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_seconds", {
                get: $util.oneOfGetter($oneOfFields = ["seconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_ptt", {
                get: $util.oneOfGetter($oneOfFields = ["ptt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_streamingSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["streamingSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_waveform", {
                get: $util.oneOfGetter($oneOfFields = ["waveform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_backgroundArgb", {
                get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AudioMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AudioMessage.create = function(properties) {
                return new AudioMessage(properties);
            };

            AudioMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(18).string(m.mimetype);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(26).bytes(m.fileSha256);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(32).uint64(m.fileLength);
                if (m.seconds != null && $Object.hasOwnProperty.call(m, "seconds"))
                    w.uint32(40).uint32(m.seconds);
                if (m.ptt != null && $Object.hasOwnProperty.call(m, "ptt"))
                    w.uint32(48).bool(m.ptt);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(58).bytes(m.mediaKey);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(66).bytes(m.fileEncSha256);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(74).string(m.directPath);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(80).int64(m.mediaKeyTimestamp);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.streamingSidecar != null && $Object.hasOwnProperty.call(m, "streamingSidecar"))
                    w.uint32(146).bytes(m.streamingSidecar);
                if (m.waveform != null && $Object.hasOwnProperty.call(m, "waveform"))
                    w.uint32(154).bytes(m.waveform);
                if (m.backgroundArgb != null && $Object.hasOwnProperty.call(m, "backgroundArgb"))
                    w.uint32(165).fixed32(m.backgroundArgb);
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce"))
                    w.uint32(168).bool(m.viewOnce);
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel"))
                    w.uint32(178).string(m.accessibilityLabel);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AudioMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.AudioMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.seconds = r.uint32();
                            m._seconds = "seconds";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.ptt = r.bool();
                            m._ptt = "ptt";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            m.streamingSidecar = r.bytes();
                            m._streamingSidecar = "streamingSidecar";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            m.waveform = r.bytes();
                            m._waveform = "waveform";
                            continue;
                        }
                    case 20: {
                            if (u !== 5)
                                break;
                            m.backgroundArgb = r.fixed32();
                            m._backgroundArgb = "backgroundArgb";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            m.viewOnce = r.bool();
                            m._viewOnce = "viewOnce";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            m.accessibilityLabel = r.stringVerify();
                            m._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AudioMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.AudioMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.AudioMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.AudioMessage();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.seconds != null) {
                    m.seconds = d.seconds >>> 0;
                }
                if (d.ptt != null) {
                    m.ptt = $Boolean(d.ptt);
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.AudioMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.streamingSidecar != null) {
                    if (typeof d.streamingSidecar === "string")
                        $util.base64.decode(d.streamingSidecar, m.streamingSidecar = $util.newBuffer($util.base64.length(d.streamingSidecar)), 0);
                    else if (d.streamingSidecar.length >= 0)
                        m.streamingSidecar = d.streamingSidecar;
                }
                if (d.waveform != null) {
                    if (typeof d.waveform === "string")
                        $util.base64.decode(d.waveform, m.waveform = $util.newBuffer($util.base64.length(d.waveform)), 0);
                    else if (d.waveform.length >= 0)
                        m.waveform = d.waveform;
                }
                if (d.backgroundArgb != null) {
                    m.backgroundArgb = d.backgroundArgb >>> 0;
                }
                if (d.viewOnce != null) {
                    m.viewOnce = $Boolean(d.viewOnce);
                }
                if (d.accessibilityLabel != null) {
                    m.accessibilityLabel = $String(d.accessibilityLabel);
                }
                return m;
            };

            AudioMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.seconds != null && $Object.hasOwnProperty.call(m, "seconds")) {
                    d.seconds = m.seconds;
                }
                if (m.ptt != null && $Object.hasOwnProperty.call(m, "ptt")) {
                    d.ptt = m.ptt;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.streamingSidecar != null && $Object.hasOwnProperty.call(m, "streamingSidecar")) {
                    d.streamingSidecar = o.bytes === $String ? $util.base64.encode(m.streamingSidecar, 0, m.streamingSidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.streamingSidecar) : m.streamingSidecar;
                }
                if (m.waveform != null && $Object.hasOwnProperty.call(m, "waveform")) {
                    d.waveform = o.bytes === $String ? $util.base64.encode(m.waveform, 0, m.waveform.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.waveform) : m.waveform;
                }
                if (m.backgroundArgb != null && $Object.hasOwnProperty.call(m, "backgroundArgb")) {
                    d.backgroundArgb = m.backgroundArgb;
                }
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce")) {
                    d.viewOnce = m.viewOnce;
                }
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel")) {
                    d.accessibilityLabel = m.accessibilityLabel;
                }
                return d;
            };

            AudioMessage.prototype.toJSON = function() {
                return AudioMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            AudioMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.AudioMessage";
            };

            return AudioMessage;
        })();

        Message.BCallMessage = (function() {

            const BCallMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BCallMessage.prototype.sessionId = null;
            BCallMessage.prototype.mediaType = null;
            BCallMessage.prototype.masterKey = null;
            BCallMessage.prototype.caption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BCallMessage.prototype, "_sessionId", {
                get: $util.oneOfGetter($oneOfFields = ["sessionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BCallMessage.prototype, "_mediaType", {
                get: $util.oneOfGetter($oneOfFields = ["mediaType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BCallMessage.prototype, "_masterKey", {
                get: $util.oneOfGetter($oneOfFields = ["masterKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BCallMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BCallMessage.create = function(properties) {
                return new BCallMessage(properties);
            };

            BCallMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId"))
                    w.uint32(10).string(m.sessionId);
                if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType"))
                    w.uint32(16).int32(m.mediaType);
                if (m.masterKey != null && $Object.hasOwnProperty.call(m, "masterKey"))
                    w.uint32(26).bytes(m.masterKey);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(34).string(m.caption);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BCallMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.BCallMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.sessionId = r.stringVerify();
                            m._sessionId = "sessionId";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.mediaType = r.int32();
                            m._mediaType = "mediaType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.masterKey = r.bytes();
                            m._masterKey = "masterKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BCallMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.BCallMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.BCallMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.BCallMessage();
                if (d.sessionId != null) {
                    m.sessionId = $String(d.sessionId);
                }
                switch (d.mediaType) {
                case "UNKNOWN":
                case 0:
                    m.mediaType = 0;
                    break;
                case "AUDIO":
                case 1:
                    m.mediaType = 1;
                    break;
                case "VIDEO":
                case 2:
                    m.mediaType = 2;
                    break;
                default:
                    if (typeof d.mediaType === "number" && (d.mediaType | 0) === d.mediaType)
                        m.mediaType = d.mediaType;
                }
                if (d.masterKey != null) {
                    if (typeof d.masterKey === "string")
                        $util.base64.decode(d.masterKey, m.masterKey = $util.newBuffer($util.base64.length(d.masterKey)), 0);
                    else if (d.masterKey.length >= 0)
                        m.masterKey = d.masterKey;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                return m;
            };

            BCallMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.sessionId != null && $Object.hasOwnProperty.call(m, "sessionId")) {
                    d.sessionId = m.sessionId;
                }
                if (m.mediaType != null && $Object.hasOwnProperty.call(m, "mediaType")) {
                    d.mediaType = o.enums === $String ? $root.proto.Message.BCallMessage.MediaType[m.mediaType] === $undefined ? m.mediaType : $root.proto.Message.BCallMessage.MediaType[m.mediaType] : m.mediaType;
                }
                if (m.masterKey != null && $Object.hasOwnProperty.call(m, "masterKey")) {
                    d.masterKey = o.bytes === $String ? $util.base64.encode(m.masterKey, 0, m.masterKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.masterKey) : m.masterKey;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                return d;
            };

            BCallMessage.prototype.toJSON = function() {
                return BCallMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            BCallMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.BCallMessage";
            };

            BCallMessage.MediaType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "AUDIO"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return BCallMessage;
        })();

        Message.BotHistoryShareSyncMetadata = (function() {

            const BotHistoryShareSyncMetadata = function (p) {
                this.historyShareMessages = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BotHistoryShareSyncMetadata.prototype.botJid = null;
            BotHistoryShareSyncMetadata.prototype.historyShareCutoffTimestamp = null;
            BotHistoryShareSyncMetadata.prototype.historyShareMessages = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotHistoryShareSyncMetadata.prototype, "_botJid", {
                get: $util.oneOfGetter($oneOfFields = ["botJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotHistoryShareSyncMetadata.prototype, "_historyShareCutoffTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["historyShareCutoffTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotHistoryShareSyncMetadata.create = function(properties) {
                return new BotHistoryShareSyncMetadata(properties);
            };

            BotHistoryShareSyncMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.botJid != null && $Object.hasOwnProperty.call(m, "botJid"))
                    w.uint32(10).string(m.botJid);
                if (m.historyShareCutoffTimestamp != null && $Object.hasOwnProperty.call(m, "historyShareCutoffTimestamp"))
                    w.uint32(16).int64(m.historyShareCutoffTimestamp);
                if (m.historyShareMessages != null && m.historyShareMessages.length) {
                    for (var i = 0; i < m.historyShareMessages.length; ++i)
                        $root.proto.Message.HistoryShareMessageEntry.encode(m.historyShareMessages[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BotHistoryShareSyncMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.BotHistoryShareSyncMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.botJid = r.stringVerify();
                            m._botJid = "botJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.historyShareCutoffTimestamp = r.int64();
                            m._historyShareCutoffTimestamp = "historyShareCutoffTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.historyShareMessages && m.historyShareMessages.length))
                                m.historyShareMessages = [];
                            m.historyShareMessages.push($root.proto.Message.HistoryShareMessageEntry.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BotHistoryShareSyncMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.BotHistoryShareSyncMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.BotHistoryShareSyncMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.BotHistoryShareSyncMetadata();
                if (d.botJid != null) {
                    m.botJid = $String(d.botJid);
                }
                if (d.historyShareCutoffTimestamp != null) {
                    if ($util.Long)
                        m.historyShareCutoffTimestamp = $util.Long.fromValue(d.historyShareCutoffTimestamp, false);
                    else if (typeof d.historyShareCutoffTimestamp === "string")
                        m.historyShareCutoffTimestamp = $parseInt(d.historyShareCutoffTimestamp, 10);
                    else if (typeof d.historyShareCutoffTimestamp === "number")
                        m.historyShareCutoffTimestamp = d.historyShareCutoffTimestamp;
                    else if (typeof d.historyShareCutoffTimestamp === "object")
                        m.historyShareCutoffTimestamp = new $util.LongBits(d.historyShareCutoffTimestamp.low >>> 0, d.historyShareCutoffTimestamp.high >>> 0).toNumber();
                }
                if (d.historyShareMessages) {
                    if (!$Array.isArray(d.historyShareMessages))
                        throw $TypeError(".proto.Message.BotHistoryShareSyncMetadata.historyShareMessages: array expected");
                    m.historyShareMessages = $Array(d.historyShareMessages.length);
                    for (var i = 0; i < d.historyShareMessages.length; ++i) {
                        if (!$util.isObject(d.historyShareMessages[i]))
                            throw $TypeError(".proto.Message.BotHistoryShareSyncMetadata.historyShareMessages: object expected");
                        m.historyShareMessages[i] = $root.proto.Message.HistoryShareMessageEntry.fromObject(d.historyShareMessages[i], q + 1);
                    }
                }
                return m;
            };

            BotHistoryShareSyncMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.historyShareMessages = [];
                }
                if (m.botJid != null && $Object.hasOwnProperty.call(m, "botJid")) {
                    d.botJid = m.botJid;
                }
                if (m.historyShareCutoffTimestamp != null && $Object.hasOwnProperty.call(m, "historyShareCutoffTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.historyShareCutoffTimestamp = typeof m.historyShareCutoffTimestamp === "number" ? $BigInt(m.historyShareCutoffTimestamp) : $util.Long.fromBits(m.historyShareCutoffTimestamp.low >>> 0, m.historyShareCutoffTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.historyShareCutoffTimestamp === "number")
                        d.historyShareCutoffTimestamp = o.longs === $String ? $String(m.historyShareCutoffTimestamp) : m.historyShareCutoffTimestamp;
                    else
                        d.historyShareCutoffTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.historyShareCutoffTimestamp) : o.longs === $Number ? new $util.LongBits(m.historyShareCutoffTimestamp.low >>> 0, m.historyShareCutoffTimestamp.high >>> 0).toNumber() : m.historyShareCutoffTimestamp;
                }
                if (m.historyShareMessages && m.historyShareMessages.length) {
                    d.historyShareMessages = $Array(m.historyShareMessages.length);
                    for (var j = 0; j < m.historyShareMessages.length; ++j) {
                        d.historyShareMessages[j] = $root.proto.Message.HistoryShareMessageEntry.toObject(m.historyShareMessages[j], o, q + 1);
                    }
                }
                return d;
            };

            BotHistoryShareSyncMetadata.prototype.toJSON = function() {
                return BotHistoryShareSyncMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotHistoryShareSyncMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.BotHistoryShareSyncMetadata";
            };

            return BotHistoryShareSyncMetadata;
        })();

        Message.ButtonsMessage = (function() {

            const ButtonsMessage = function (p) {
                this.buttons = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ButtonsMessage.prototype.contentText = null;
            ButtonsMessage.prototype.footerText = null;
            ButtonsMessage.prototype.contextInfo = null;
            ButtonsMessage.prototype.buttons = $util.emptyArray;
            ButtonsMessage.prototype.headerType = null;
            ButtonsMessage.prototype.text = null;
            ButtonsMessage.prototype.documentMessage = null;
            ButtonsMessage.prototype.imageMessage = null;
            ButtonsMessage.prototype.videoMessage = null;
            ButtonsMessage.prototype.locationMessage = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsMessage.prototype, "_contentText", {
                get: $util.oneOfGetter($oneOfFields = ["contentText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsMessage.prototype, "_footerText", {
                get: $util.oneOfGetter($oneOfFields = ["footerText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsMessage.prototype, "_headerType", {
                get: $util.oneOfGetter($oneOfFields = ["headerType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            $Object.defineProperty(ButtonsMessage.prototype, "header", {
                get: $util.oneOfGetter($oneOfFields = ["text", "documentMessage", "imageMessage", "videoMessage", "locationMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ButtonsMessage.create = function(properties) {
                return new ButtonsMessage(properties);
            };

            ButtonsMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage"))
                    $root.proto.Message.DocumentMessage.encode(m.documentMessage, w.uint32(18).fork(), q + 1).ldelim();
                if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                    $root.proto.Message.ImageMessage.encode(m.imageMessage, w.uint32(26).fork(), q + 1).ldelim();
                if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                    $root.proto.Message.VideoMessage.encode(m.videoMessage, w.uint32(34).fork(), q + 1).ldelim();
                if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage"))
                    $root.proto.Message.LocationMessage.encode(m.locationMessage, w.uint32(42).fork(), q + 1).ldelim();
                if (m.contentText != null && $Object.hasOwnProperty.call(m, "contentText"))
                    w.uint32(50).string(m.contentText);
                if (m.footerText != null && $Object.hasOwnProperty.call(m, "footerText"))
                    w.uint32(58).string(m.footerText);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(66).fork(), q + 1).ldelim();
                if (m.buttons != null && m.buttons.length) {
                    for (var i = 0; i < m.buttons.length; ++i)
                        $root.proto.Message.ButtonsMessage.Button.encode(m.buttons[i], w.uint32(74).fork(), q + 1).ldelim();
                }
                if (m.headerType != null && $Object.hasOwnProperty.call(m, "headerType"))
                    w.uint32(80).int32(m.headerType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ButtonsMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ButtonsMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 6: {
                            if (u !== 2)
                                break;
                            m.contentText = r.stringVerify();
                            m._contentText = "contentText";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.footerText = r.stringVerify();
                            m._footerText = "footerText";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            if (!(m.buttons && m.buttons.length))
                                m.buttons = [];
                            m.buttons.push($root.proto.Message.ButtonsMessage.Button.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.headerType = r.int32();
                            m._headerType = "headerType";
                            continue;
                        }
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m.header = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.documentMessage = $root.proto.Message.DocumentMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentMessage);
                            m.header = "documentMessage";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.imageMessage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                            m.header = "imageMessage";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.videoMessage = $root.proto.Message.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                            m.header = "videoMessage";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.locationMessage = $root.proto.Message.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.locationMessage);
                            m.header = "locationMessage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ButtonsMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ButtonsMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ButtonsMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ButtonsMessage();
                if (d.contentText != null) {
                    m.contentText = $String(d.contentText);
                }
                if (d.footerText != null) {
                    m.footerText = $String(d.footerText);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ButtonsMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.buttons) {
                    if (!$Array.isArray(d.buttons))
                        throw $TypeError(".proto.Message.ButtonsMessage.buttons: array expected");
                    m.buttons = $Array(d.buttons.length);
                    for (var i = 0; i < d.buttons.length; ++i) {
                        if (!$util.isObject(d.buttons[i]))
                            throw $TypeError(".proto.Message.ButtonsMessage.buttons: object expected");
                        m.buttons[i] = $root.proto.Message.ButtonsMessage.Button.fromObject(d.buttons[i], q + 1);
                    }
                }
                switch (d.headerType) {
                case "UNKNOWN":
                case 0:
                    m.headerType = 0;
                    break;
                case "EMPTY":
                case 1:
                    m.headerType = 1;
                    break;
                case "TEXT":
                case 2:
                    m.headerType = 2;
                    break;
                case "DOCUMENT":
                case 3:
                    m.headerType = 3;
                    break;
                case "IMAGE":
                case 4:
                    m.headerType = 4;
                    break;
                case "VIDEO":
                case 5:
                    m.headerType = 5;
                    break;
                case "LOCATION":
                case 6:
                    m.headerType = 6;
                    break;
                default:
                    if (typeof d.headerType === "number" && (d.headerType | 0) === d.headerType)
                        m.headerType = d.headerType;
                }
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.documentMessage != null) {
                    if (!$util.isObject(d.documentMessage))
                        throw $TypeError(".proto.Message.ButtonsMessage.documentMessage: object expected");
                    m.documentMessage = $root.proto.Message.DocumentMessage.fromObject(d.documentMessage, q + 1);
                }
                if (d.imageMessage != null) {
                    if (!$util.isObject(d.imageMessage))
                        throw $TypeError(".proto.Message.ButtonsMessage.imageMessage: object expected");
                    m.imageMessage = $root.proto.Message.ImageMessage.fromObject(d.imageMessage, q + 1);
                }
                if (d.videoMessage != null) {
                    if (!$util.isObject(d.videoMessage))
                        throw $TypeError(".proto.Message.ButtonsMessage.videoMessage: object expected");
                    m.videoMessage = $root.proto.Message.VideoMessage.fromObject(d.videoMessage, q + 1);
                }
                if (d.locationMessage != null) {
                    if (!$util.isObject(d.locationMessage))
                        throw $TypeError(".proto.Message.ButtonsMessage.locationMessage: object expected");
                    m.locationMessage = $root.proto.Message.LocationMessage.fromObject(d.locationMessage, q + 1);
                }
                return m;
            };

            ButtonsMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.buttons = [];
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                    if (o.oneofs)
                        d.header = "text";
                }
                if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage")) {
                    d.documentMessage = $root.proto.Message.DocumentMessage.toObject(m.documentMessage, o, q + 1);
                    if (o.oneofs)
                        d.header = "documentMessage";
                }
                if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                    d.imageMessage = $root.proto.Message.ImageMessage.toObject(m.imageMessage, o, q + 1);
                    if (o.oneofs)
                        d.header = "imageMessage";
                }
                if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                    d.videoMessage = $root.proto.Message.VideoMessage.toObject(m.videoMessage, o, q + 1);
                    if (o.oneofs)
                        d.header = "videoMessage";
                }
                if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage")) {
                    d.locationMessage = $root.proto.Message.LocationMessage.toObject(m.locationMessage, o, q + 1);
                    if (o.oneofs)
                        d.header = "locationMessage";
                }
                if (m.contentText != null && $Object.hasOwnProperty.call(m, "contentText")) {
                    d.contentText = m.contentText;
                }
                if (m.footerText != null && $Object.hasOwnProperty.call(m, "footerText")) {
                    d.footerText = m.footerText;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.buttons && m.buttons.length) {
                    d.buttons = $Array(m.buttons.length);
                    for (var j = 0; j < m.buttons.length; ++j) {
                        d.buttons[j] = $root.proto.Message.ButtonsMessage.Button.toObject(m.buttons[j], o, q + 1);
                    }
                }
                if (m.headerType != null && $Object.hasOwnProperty.call(m, "headerType")) {
                    d.headerType = o.enums === $String ? $root.proto.Message.ButtonsMessage.HeaderType[m.headerType] === $undefined ? m.headerType : $root.proto.Message.ButtonsMessage.HeaderType[m.headerType] : m.headerType;
                }
                return d;
            };

            ButtonsMessage.prototype.toJSON = function() {
                return ButtonsMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ButtonsMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ButtonsMessage";
            };

            ButtonsMessage.Button = (function() {

                const Button = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Button.prototype.buttonId = null;
                Button.prototype.buttonText = null;
                Button.prototype.type = null;
                Button.prototype.nativeFlowInfo = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Button.prototype, "_buttonId", {
                    get: $util.oneOfGetter($oneOfFields = ["buttonId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Button.prototype, "_buttonText", {
                    get: $util.oneOfGetter($oneOfFields = ["buttonText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Button.prototype, "_type", {
                    get: $util.oneOfGetter($oneOfFields = ["type"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Button.prototype, "_nativeFlowInfo", {
                    get: $util.oneOfGetter($oneOfFields = ["nativeFlowInfo"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Button.create = function(properties) {
                    return new Button(properties);
                };

                Button.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.buttonId != null && $Object.hasOwnProperty.call(m, "buttonId"))
                        w.uint32(10).string(m.buttonId);
                    if (m.buttonText != null && $Object.hasOwnProperty.call(m, "buttonText"))
                        $root.proto.Message.ButtonsMessage.Button.ButtonText.encode(m.buttonText, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                        w.uint32(24).int32(m.type);
                    if (m.nativeFlowInfo != null && $Object.hasOwnProperty.call(m, "nativeFlowInfo"))
                        $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.encode(m.nativeFlowInfo, w.uint32(34).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Button.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ButtonsMessage.Button(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.buttonId = r.stringVerify();
                                m._buttonId = "buttonId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.buttonText = $root.proto.Message.ButtonsMessage.Button.ButtonText.decode(r, r.uint32(), $undefined, q + 1, m.buttonText);
                                m._buttonText = "buttonText";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.type = r.int32();
                                m._type = "type";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.nativeFlowInfo = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.decode(r, r.uint32(), $undefined, q + 1, m.nativeFlowInfo);
                                m._nativeFlowInfo = "nativeFlowInfo";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Button.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ButtonsMessage.Button)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ButtonsMessage.Button: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ButtonsMessage.Button();
                    if (d.buttonId != null) {
                        m.buttonId = $String(d.buttonId);
                    }
                    if (d.buttonText != null) {
                        if (!$util.isObject(d.buttonText))
                            throw $TypeError(".proto.Message.ButtonsMessage.Button.buttonText: object expected");
                        m.buttonText = $root.proto.Message.ButtonsMessage.Button.ButtonText.fromObject(d.buttonText, q + 1);
                    }
                    switch (d.type) {
                    case "UNKNOWN":
                    case 0:
                        m.type = 0;
                        break;
                    case "RESPONSE":
                    case 1:
                        m.type = 1;
                        break;
                    case "NATIVE_FLOW":
                    case 2:
                        m.type = 2;
                        break;
                    default:
                        if (typeof d.type === "number" && (d.type | 0) === d.type)
                            m.type = d.type;
                    }
                    if (d.nativeFlowInfo != null) {
                        if (!$util.isObject(d.nativeFlowInfo))
                            throw $TypeError(".proto.Message.ButtonsMessage.Button.nativeFlowInfo: object expected");
                        m.nativeFlowInfo = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.fromObject(d.nativeFlowInfo, q + 1);
                    }
                    return m;
                };

                Button.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.buttonId != null && $Object.hasOwnProperty.call(m, "buttonId")) {
                        d.buttonId = m.buttonId;
                    }
                    if (m.buttonText != null && $Object.hasOwnProperty.call(m, "buttonText")) {
                        d.buttonText = $root.proto.Message.ButtonsMessage.Button.ButtonText.toObject(m.buttonText, o, q + 1);
                    }
                    if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                        d.type = o.enums === $String ? $root.proto.Message.ButtonsMessage.Button.Type[m.type] === $undefined ? m.type : $root.proto.Message.ButtonsMessage.Button.Type[m.type] : m.type;
                    }
                    if (m.nativeFlowInfo != null && $Object.hasOwnProperty.call(m, "nativeFlowInfo")) {
                        d.nativeFlowInfo = $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo.toObject(m.nativeFlowInfo, o, q + 1);
                    }
                    return d;
                };

                Button.prototype.toJSON = function() {
                    return Button.toObject(this, $protobuf.util.toJSONOptions);
                };

                Button.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ButtonsMessage.Button";
                };

                Button.ButtonText = (function() {

                    const ButtonText = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    ButtonText.prototype.displayText = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(ButtonText.prototype, "_displayText", {
                        get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    ButtonText.create = function(properties) {
                        return new ButtonText(properties);
                    };

                    ButtonText.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                            w.uint32(10).string(m.displayText);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    ButtonText.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ButtonsMessage.Button.ButtonText();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.displayText = r.stringVerify();
                                    m._displayText = "displayText";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    ButtonText.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.ButtonsMessage.Button.ButtonText)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.ButtonsMessage.Button.ButtonText: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.ButtonsMessage.Button.ButtonText();
                        if (d.displayText != null) {
                            m.displayText = $String(d.displayText);
                        }
                        return m;
                    };

                    ButtonText.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                            d.displayText = m.displayText;
                        }
                        return d;
                    };

                    ButtonText.prototype.toJSON = function() {
                        return ButtonText.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    ButtonText.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.ButtonsMessage.Button.ButtonText";
                    };

                    return ButtonText;
                })();

                Button.NativeFlowInfo = (function() {

                    const NativeFlowInfo = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    NativeFlowInfo.prototype.name = null;
                    NativeFlowInfo.prototype.paramsJson = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(NativeFlowInfo.prototype, "_name", {
                        get: $util.oneOfGetter($oneOfFields = ["name"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(NativeFlowInfo.prototype, "_paramsJson", {
                        get: $util.oneOfGetter($oneOfFields = ["paramsJson"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    NativeFlowInfo.create = function(properties) {
                        return new NativeFlowInfo(properties);
                    };

                    NativeFlowInfo.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                            w.uint32(10).string(m.name);
                        if (m.paramsJson != null && $Object.hasOwnProperty.call(m, "paramsJson"))
                            w.uint32(18).string(m.paramsJson);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    NativeFlowInfo.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.name = r.stringVerify();
                                    m._name = "name";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.paramsJson = r.stringVerify();
                                    m._paramsJson = "paramsJson";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    NativeFlowInfo.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.ButtonsMessage.Button.NativeFlowInfo: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.ButtonsMessage.Button.NativeFlowInfo();
                        if (d.name != null) {
                            m.name = $String(d.name);
                        }
                        if (d.paramsJson != null) {
                            m.paramsJson = $String(d.paramsJson);
                        }
                        return m;
                    };

                    NativeFlowInfo.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                            d.name = m.name;
                        }
                        if (m.paramsJson != null && $Object.hasOwnProperty.call(m, "paramsJson")) {
                            d.paramsJson = m.paramsJson;
                        }
                        return d;
                    };

                    NativeFlowInfo.prototype.toJSON = function() {
                        return NativeFlowInfo.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    NativeFlowInfo.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.ButtonsMessage.Button.NativeFlowInfo";
                    };

                    return NativeFlowInfo;
                })();

                Button.Type = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "RESPONSE"] = 1;
                    values[valuesById[2] = "NATIVE_FLOW"] = 2;
                    return values;
                })();

                return Button;
            })();

            ButtonsMessage.HeaderType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "EMPTY"] = 1;
                values[valuesById[2] = "TEXT"] = 2;
                values[valuesById[3] = "DOCUMENT"] = 3;
                values[valuesById[4] = "IMAGE"] = 4;
                values[valuesById[5] = "VIDEO"] = 5;
                values[valuesById[6] = "LOCATION"] = 6;
                return values;
            })();

            return ButtonsMessage;
        })();

        Message.ButtonsResponseMessage = (function() {

            const ButtonsResponseMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ButtonsResponseMessage.prototype.selectedButtonId = null;
            ButtonsResponseMessage.prototype.contextInfo = null;
            ButtonsResponseMessage.prototype.type = null;
            ButtonsResponseMessage.prototype.selectedDisplayText = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsResponseMessage.prototype, "_selectedButtonId", {
                get: $util.oneOfGetter($oneOfFields = ["selectedButtonId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsResponseMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ButtonsResponseMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            $Object.defineProperty(ButtonsResponseMessage.prototype, "response", {
                get: $util.oneOfGetter($oneOfFields = ["selectedDisplayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ButtonsResponseMessage.create = function(properties) {
                return new ButtonsResponseMessage(properties);
            };

            ButtonsResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.selectedButtonId != null && $Object.hasOwnProperty.call(m, "selectedButtonId"))
                    w.uint32(10).string(m.selectedButtonId);
                if (m.selectedDisplayText != null && $Object.hasOwnProperty.call(m, "selectedDisplayText"))
                    w.uint32(18).string(m.selectedDisplayText);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(26).fork(), q + 1).ldelim();
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(32).int32(m.type);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ButtonsResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ButtonsResponseMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.selectedButtonId = r.stringVerify();
                            m._selectedButtonId = "selectedButtonId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.selectedDisplayText = r.stringVerify();
                            m.response = "selectedDisplayText";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ButtonsResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ButtonsResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ButtonsResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ButtonsResponseMessage();
                if (d.selectedButtonId != null) {
                    m.selectedButtonId = $String(d.selectedButtonId);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ButtonsResponseMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                switch (d.type) {
                case "UNKNOWN":
                case 0:
                    m.type = 0;
                    break;
                case "DISPLAY_TEXT":
                case 1:
                    m.type = 1;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.selectedDisplayText != null) {
                    m.selectedDisplayText = $String(d.selectedDisplayText);
                }
                return m;
            };

            ButtonsResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.selectedButtonId != null && $Object.hasOwnProperty.call(m, "selectedButtonId")) {
                    d.selectedButtonId = m.selectedButtonId;
                }
                if (m.selectedDisplayText != null && $Object.hasOwnProperty.call(m, "selectedDisplayText")) {
                    d.selectedDisplayText = m.selectedDisplayText;
                    if (o.oneofs)
                        d.response = "selectedDisplayText";
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.ButtonsResponseMessage.Type[m.type] === $undefined ? m.type : $root.proto.Message.ButtonsResponseMessage.Type[m.type] : m.type;
                }
                return d;
            };

            ButtonsResponseMessage.prototype.toJSON = function() {
                return ButtonsResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ButtonsResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ButtonsResponseMessage";
            };

            ButtonsResponseMessage.Type = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "DISPLAY_TEXT"] = 1;
                return values;
            })();

            return ButtonsResponseMessage;
        })();

        Message.Call = (function() {

            const Call = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Call.prototype.callKey = null;
            Call.prototype.conversionSource = null;
            Call.prototype.conversionData = null;
            Call.prototype.conversionDelaySeconds = null;
            Call.prototype.ctwaSignals = null;
            Call.prototype.ctwaPayload = null;
            Call.prototype.contextInfo = null;
            Call.prototype.nativeFlowCallButtonPayload = null;
            Call.prototype.deeplinkPayload = null;
            Call.prototype.messageContextInfo = null;
            Call.prototype.callEntryPoint = null;
            Call.prototype.callReason = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_callKey", {
                get: $util.oneOfGetter($oneOfFields = ["callKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_conversionSource", {
                get: $util.oneOfGetter($oneOfFields = ["conversionSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_conversionData", {
                get: $util.oneOfGetter($oneOfFields = ["conversionData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_conversionDelaySeconds", {
                get: $util.oneOfGetter($oneOfFields = ["conversionDelaySeconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_ctwaSignals", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaSignals"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_ctwaPayload", {
                get: $util.oneOfGetter($oneOfFields = ["ctwaPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_nativeFlowCallButtonPayload", {
                get: $util.oneOfGetter($oneOfFields = ["nativeFlowCallButtonPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_deeplinkPayload", {
                get: $util.oneOfGetter($oneOfFields = ["deeplinkPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_messageContextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["messageContextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_callEntryPoint", {
                get: $util.oneOfGetter($oneOfFields = ["callEntryPoint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Call.prototype, "_callReason", {
                get: $util.oneOfGetter($oneOfFields = ["callReason"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Call.create = function(properties) {
                return new Call(properties);
            };

            Call.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.callKey != null && $Object.hasOwnProperty.call(m, "callKey"))
                    w.uint32(10).bytes(m.callKey);
                if (m.conversionSource != null && $Object.hasOwnProperty.call(m, "conversionSource"))
                    w.uint32(18).string(m.conversionSource);
                if (m.conversionData != null && $Object.hasOwnProperty.call(m, "conversionData"))
                    w.uint32(26).bytes(m.conversionData);
                if (m.conversionDelaySeconds != null && $Object.hasOwnProperty.call(m, "conversionDelaySeconds"))
                    w.uint32(32).uint32(m.conversionDelaySeconds);
                if (m.ctwaSignals != null && $Object.hasOwnProperty.call(m, "ctwaSignals"))
                    w.uint32(42).string(m.ctwaSignals);
                if (m.ctwaPayload != null && $Object.hasOwnProperty.call(m, "ctwaPayload"))
                    w.uint32(50).bytes(m.ctwaPayload);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(58).fork(), q + 1).ldelim();
                if (m.nativeFlowCallButtonPayload != null && $Object.hasOwnProperty.call(m, "nativeFlowCallButtonPayload"))
                    w.uint32(66).string(m.nativeFlowCallButtonPayload);
                if (m.deeplinkPayload != null && $Object.hasOwnProperty.call(m, "deeplinkPayload"))
                    w.uint32(74).string(m.deeplinkPayload);
                if (m.messageContextInfo != null && $Object.hasOwnProperty.call(m, "messageContextInfo"))
                    $root.proto.MessageContextInfo.encode(m.messageContextInfo, w.uint32(82).fork(), q + 1).ldelim();
                if (m.callEntryPoint != null && $Object.hasOwnProperty.call(m, "callEntryPoint"))
                    w.uint32(88).uint32(m.callEntryPoint);
                if (m.callReason != null && $Object.hasOwnProperty.call(m, "callReason"))
                    w.uint32(98).string(m.callReason);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Call.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.Call();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.callKey = r.bytes();
                            m._callKey = "callKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.conversionSource = r.stringVerify();
                            m._conversionSource = "conversionSource";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.conversionData = r.bytes();
                            m._conversionData = "conversionData";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.conversionDelaySeconds = r.uint32();
                            m._conversionDelaySeconds = "conversionDelaySeconds";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.ctwaSignals = r.stringVerify();
                            m._ctwaSignals = "ctwaSignals";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.ctwaPayload = r.bytes();
                            m._ctwaPayload = "ctwaPayload";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.nativeFlowCallButtonPayload = r.stringVerify();
                            m._nativeFlowCallButtonPayload = "nativeFlowCallButtonPayload";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.deeplinkPayload = r.stringVerify();
                            m._deeplinkPayload = "deeplinkPayload";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.messageContextInfo = $root.proto.MessageContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.messageContextInfo);
                            m._messageContextInfo = "messageContextInfo";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.callEntryPoint = r.uint32();
                            m._callEntryPoint = "callEntryPoint";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.callReason = r.stringVerify();
                            m._callReason = "callReason";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Call.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.Call)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.Call: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.Call();
                if (d.callKey != null) {
                    if (typeof d.callKey === "string")
                        $util.base64.decode(d.callKey, m.callKey = $util.newBuffer($util.base64.length(d.callKey)), 0);
                    else if (d.callKey.length >= 0)
                        m.callKey = d.callKey;
                }
                if (d.conversionSource != null) {
                    m.conversionSource = $String(d.conversionSource);
                }
                if (d.conversionData != null) {
                    if (typeof d.conversionData === "string")
                        $util.base64.decode(d.conversionData, m.conversionData = $util.newBuffer($util.base64.length(d.conversionData)), 0);
                    else if (d.conversionData.length >= 0)
                        m.conversionData = d.conversionData;
                }
                if (d.conversionDelaySeconds != null) {
                    m.conversionDelaySeconds = d.conversionDelaySeconds >>> 0;
                }
                if (d.ctwaSignals != null) {
                    m.ctwaSignals = $String(d.ctwaSignals);
                }
                if (d.ctwaPayload != null) {
                    if (typeof d.ctwaPayload === "string")
                        $util.base64.decode(d.ctwaPayload, m.ctwaPayload = $util.newBuffer($util.base64.length(d.ctwaPayload)), 0);
                    else if (d.ctwaPayload.length >= 0)
                        m.ctwaPayload = d.ctwaPayload;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.Call.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.nativeFlowCallButtonPayload != null) {
                    m.nativeFlowCallButtonPayload = $String(d.nativeFlowCallButtonPayload);
                }
                if (d.deeplinkPayload != null) {
                    m.deeplinkPayload = $String(d.deeplinkPayload);
                }
                if (d.messageContextInfo != null) {
                    if (!$util.isObject(d.messageContextInfo))
                        throw $TypeError(".proto.Message.Call.messageContextInfo: object expected");
                    m.messageContextInfo = $root.proto.MessageContextInfo.fromObject(d.messageContextInfo, q + 1);
                }
                if (d.callEntryPoint != null) {
                    m.callEntryPoint = d.callEntryPoint >>> 0;
                }
                if (d.callReason != null) {
                    m.callReason = $String(d.callReason);
                }
                return m;
            };

            Call.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.callKey != null && $Object.hasOwnProperty.call(m, "callKey")) {
                    d.callKey = o.bytes === $String ? $util.base64.encode(m.callKey, 0, m.callKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.callKey) : m.callKey;
                }
                if (m.conversionSource != null && $Object.hasOwnProperty.call(m, "conversionSource")) {
                    d.conversionSource = m.conversionSource;
                }
                if (m.conversionData != null && $Object.hasOwnProperty.call(m, "conversionData")) {
                    d.conversionData = o.bytes === $String ? $util.base64.encode(m.conversionData, 0, m.conversionData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.conversionData) : m.conversionData;
                }
                if (m.conversionDelaySeconds != null && $Object.hasOwnProperty.call(m, "conversionDelaySeconds")) {
                    d.conversionDelaySeconds = m.conversionDelaySeconds;
                }
                if (m.ctwaSignals != null && $Object.hasOwnProperty.call(m, "ctwaSignals")) {
                    d.ctwaSignals = m.ctwaSignals;
                }
                if (m.ctwaPayload != null && $Object.hasOwnProperty.call(m, "ctwaPayload")) {
                    d.ctwaPayload = o.bytes === $String ? $util.base64.encode(m.ctwaPayload, 0, m.ctwaPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ctwaPayload) : m.ctwaPayload;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.nativeFlowCallButtonPayload != null && $Object.hasOwnProperty.call(m, "nativeFlowCallButtonPayload")) {
                    d.nativeFlowCallButtonPayload = m.nativeFlowCallButtonPayload;
                }
                if (m.deeplinkPayload != null && $Object.hasOwnProperty.call(m, "deeplinkPayload")) {
                    d.deeplinkPayload = m.deeplinkPayload;
                }
                if (m.messageContextInfo != null && $Object.hasOwnProperty.call(m, "messageContextInfo")) {
                    d.messageContextInfo = $root.proto.MessageContextInfo.toObject(m.messageContextInfo, o, q + 1);
                }
                if (m.callEntryPoint != null && $Object.hasOwnProperty.call(m, "callEntryPoint")) {
                    d.callEntryPoint = m.callEntryPoint;
                }
                if (m.callReason != null && $Object.hasOwnProperty.call(m, "callReason")) {
                    d.callReason = m.callReason;
                }
                return d;
            };

            Call.prototype.toJSON = function() {
                return Call.toObject(this, $protobuf.util.toJSONOptions);
            };

            Call.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.Call";
            };

            return Call;
        })();

        Message.CallLogMessage = (function() {

            const CallLogMessage = function (p) {
                this.participants = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CallLogMessage.prototype.isVideo = null;
            CallLogMessage.prototype.callOutcome = null;
            CallLogMessage.prototype.durationSecs = null;
            CallLogMessage.prototype.callType = null;
            CallLogMessage.prototype.participants = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallLogMessage.prototype, "_isVideo", {
                get: $util.oneOfGetter($oneOfFields = ["isVideo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallLogMessage.prototype, "_callOutcome", {
                get: $util.oneOfGetter($oneOfFields = ["callOutcome"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallLogMessage.prototype, "_durationSecs", {
                get: $util.oneOfGetter($oneOfFields = ["durationSecs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallLogMessage.prototype, "_callType", {
                get: $util.oneOfGetter($oneOfFields = ["callType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CallLogMessage.create = function(properties) {
                return new CallLogMessage(properties);
            };

            CallLogMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isVideo != null && $Object.hasOwnProperty.call(m, "isVideo"))
                    w.uint32(8).bool(m.isVideo);
                if (m.callOutcome != null && $Object.hasOwnProperty.call(m, "callOutcome"))
                    w.uint32(16).int32(m.callOutcome);
                if (m.durationSecs != null && $Object.hasOwnProperty.call(m, "durationSecs"))
                    w.uint32(24).int64(m.durationSecs);
                if (m.callType != null && $Object.hasOwnProperty.call(m, "callType"))
                    w.uint32(32).int32(m.callType);
                if (m.participants != null && m.participants.length) {
                    for (var i = 0; i < m.participants.length; ++i)
                        $root.proto.Message.CallLogMessage.CallParticipant.encode(m.participants[i], w.uint32(42).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CallLogMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.CallLogMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isVideo = r.bool();
                            m._isVideo = "isVideo";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.callOutcome = r.int32();
                            m._callOutcome = "callOutcome";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.durationSecs = r.int64();
                            m._durationSecs = "durationSecs";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.callType = r.int32();
                            m._callType = "callType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(m.participants && m.participants.length))
                                m.participants = [];
                            m.participants.push($root.proto.Message.CallLogMessage.CallParticipant.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CallLogMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.CallLogMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.CallLogMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.CallLogMessage();
                if (d.isVideo != null) {
                    m.isVideo = $Boolean(d.isVideo);
                }
                switch (d.callOutcome) {
                case "CONNECTED":
                case 0:
                    m.callOutcome = 0;
                    break;
                case "MISSED":
                case 1:
                    m.callOutcome = 1;
                    break;
                case "FAILED":
                case 2:
                    m.callOutcome = 2;
                    break;
                case "REJECTED":
                case 3:
                    m.callOutcome = 3;
                    break;
                case "ACCEPTED_ELSEWHERE":
                case 4:
                    m.callOutcome = 4;
                    break;
                case "ONGOING":
                case 5:
                    m.callOutcome = 5;
                    break;
                case "SILENCED_BY_DND":
                case 6:
                    m.callOutcome = 6;
                    break;
                case "SILENCED_UNKNOWN_CALLER":
                case 7:
                    m.callOutcome = 7;
                    break;
                default:
                    if (typeof d.callOutcome === "number" && (d.callOutcome | 0) === d.callOutcome)
                        m.callOutcome = d.callOutcome;
                }
                if (d.durationSecs != null) {
                    if ($util.Long)
                        m.durationSecs = $util.Long.fromValue(d.durationSecs, false);
                    else if (typeof d.durationSecs === "string")
                        m.durationSecs = $parseInt(d.durationSecs, 10);
                    else if (typeof d.durationSecs === "number")
                        m.durationSecs = d.durationSecs;
                    else if (typeof d.durationSecs === "object")
                        m.durationSecs = new $util.LongBits(d.durationSecs.low >>> 0, d.durationSecs.high >>> 0).toNumber();
                }
                switch (d.callType) {
                case "REGULAR":
                case 0:
                    m.callType = 0;
                    break;
                case "SCHEDULED_CALL":
                case 1:
                    m.callType = 1;
                    break;
                case "VOICE_CHAT":
                case 2:
                    m.callType = 2;
                    break;
                default:
                    if (typeof d.callType === "number" && (d.callType | 0) === d.callType)
                        m.callType = d.callType;
                }
                if (d.participants) {
                    if (!$Array.isArray(d.participants))
                        throw $TypeError(".proto.Message.CallLogMessage.participants: array expected");
                    m.participants = $Array(d.participants.length);
                    for (var i = 0; i < d.participants.length; ++i) {
                        if (!$util.isObject(d.participants[i]))
                            throw $TypeError(".proto.Message.CallLogMessage.participants: object expected");
                        m.participants[i] = $root.proto.Message.CallLogMessage.CallParticipant.fromObject(d.participants[i], q + 1);
                    }
                }
                return m;
            };

            CallLogMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.participants = [];
                }
                if (m.isVideo != null && $Object.hasOwnProperty.call(m, "isVideo")) {
                    d.isVideo = m.isVideo;
                }
                if (m.callOutcome != null && $Object.hasOwnProperty.call(m, "callOutcome")) {
                    d.callOutcome = o.enums === $String ? $root.proto.Message.CallLogMessage.CallOutcome[m.callOutcome] === $undefined ? m.callOutcome : $root.proto.Message.CallLogMessage.CallOutcome[m.callOutcome] : m.callOutcome;
                }
                if (m.durationSecs != null && $Object.hasOwnProperty.call(m, "durationSecs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.durationSecs = typeof m.durationSecs === "number" ? $BigInt(m.durationSecs) : $util.Long.fromBits(m.durationSecs.low >>> 0, m.durationSecs.high >>> 0, false).toBigInt();
                    else if (typeof m.durationSecs === "number")
                        d.durationSecs = o.longs === $String ? $String(m.durationSecs) : m.durationSecs;
                    else
                        d.durationSecs = o.longs === $String ? $util.Long.prototype.toString.call(m.durationSecs) : o.longs === $Number ? new $util.LongBits(m.durationSecs.low >>> 0, m.durationSecs.high >>> 0).toNumber() : m.durationSecs;
                }
                if (m.callType != null && $Object.hasOwnProperty.call(m, "callType")) {
                    d.callType = o.enums === $String ? $root.proto.Message.CallLogMessage.CallType[m.callType] === $undefined ? m.callType : $root.proto.Message.CallLogMessage.CallType[m.callType] : m.callType;
                }
                if (m.participants && m.participants.length) {
                    d.participants = $Array(m.participants.length);
                    for (var j = 0; j < m.participants.length; ++j) {
                        d.participants[j] = $root.proto.Message.CallLogMessage.CallParticipant.toObject(m.participants[j], o, q + 1);
                    }
                }
                return d;
            };

            CallLogMessage.prototype.toJSON = function() {
                return CallLogMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            CallLogMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.CallLogMessage";
            };

            CallLogMessage.CallOutcome = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "CONNECTED"] = 0;
                values[valuesById[1] = "MISSED"] = 1;
                values[valuesById[2] = "FAILED"] = 2;
                values[valuesById[3] = "REJECTED"] = 3;
                values[valuesById[4] = "ACCEPTED_ELSEWHERE"] = 4;
                values[valuesById[5] = "ONGOING"] = 5;
                values[valuesById[6] = "SILENCED_BY_DND"] = 6;
                values[valuesById[7] = "SILENCED_UNKNOWN_CALLER"] = 7;
                return values;
            })();

            CallLogMessage.CallParticipant = (function() {

                const CallParticipant = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CallParticipant.prototype.jid = null;
                CallParticipant.prototype.callOutcome = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CallParticipant.prototype, "_jid", {
                    get: $util.oneOfGetter($oneOfFields = ["jid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CallParticipant.prototype, "_callOutcome", {
                    get: $util.oneOfGetter($oneOfFields = ["callOutcome"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CallParticipant.create = function(properties) {
                    return new CallParticipant(properties);
                };

                CallParticipant.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.jid != null && $Object.hasOwnProperty.call(m, "jid"))
                        w.uint32(10).string(m.jid);
                    if (m.callOutcome != null && $Object.hasOwnProperty.call(m, "callOutcome"))
                        w.uint32(16).int32(m.callOutcome);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CallParticipant.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.CallLogMessage.CallParticipant(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.jid = r.stringVerify();
                                m._jid = "jid";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.callOutcome = r.int32();
                                m._callOutcome = "callOutcome";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CallParticipant.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.CallLogMessage.CallParticipant)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.CallLogMessage.CallParticipant: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.CallLogMessage.CallParticipant();
                    if (d.jid != null) {
                        m.jid = $String(d.jid);
                    }
                    switch (d.callOutcome) {
                    case "CONNECTED":
                    case 0:
                        m.callOutcome = 0;
                        break;
                    case "MISSED":
                    case 1:
                        m.callOutcome = 1;
                        break;
                    case "FAILED":
                    case 2:
                        m.callOutcome = 2;
                        break;
                    case "REJECTED":
                    case 3:
                        m.callOutcome = 3;
                        break;
                    case "ACCEPTED_ELSEWHERE":
                    case 4:
                        m.callOutcome = 4;
                        break;
                    case "ONGOING":
                    case 5:
                        m.callOutcome = 5;
                        break;
                    case "SILENCED_BY_DND":
                    case 6:
                        m.callOutcome = 6;
                        break;
                    case "SILENCED_UNKNOWN_CALLER":
                    case 7:
                        m.callOutcome = 7;
                        break;
                    default:
                        if (typeof d.callOutcome === "number" && (d.callOutcome | 0) === d.callOutcome)
                            m.callOutcome = d.callOutcome;
                    }
                    return m;
                };

                CallParticipant.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.jid != null && $Object.hasOwnProperty.call(m, "jid")) {
                        d.jid = m.jid;
                    }
                    if (m.callOutcome != null && $Object.hasOwnProperty.call(m, "callOutcome")) {
                        d.callOutcome = o.enums === $String ? $root.proto.Message.CallLogMessage.CallOutcome[m.callOutcome] === $undefined ? m.callOutcome : $root.proto.Message.CallLogMessage.CallOutcome[m.callOutcome] : m.callOutcome;
                    }
                    return d;
                };

                CallParticipant.prototype.toJSON = function() {
                    return CallParticipant.toObject(this, $protobuf.util.toJSONOptions);
                };

                CallParticipant.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.CallLogMessage.CallParticipant";
                };

                return CallParticipant;
            })();

            CallLogMessage.CallType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "REGULAR"] = 0;
                values[valuesById[1] = "SCHEDULED_CALL"] = 1;
                values[valuesById[2] = "VOICE_CHAT"] = 2;
                return values;
            })();

            return CallLogMessage;
        })();

        Message.CancelPaymentRequestMessage = (function() {

            const CancelPaymentRequestMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CancelPaymentRequestMessage.prototype.key = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CancelPaymentRequestMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CancelPaymentRequestMessage.create = function(properties) {
                return new CancelPaymentRequestMessage(properties);
            };

            CancelPaymentRequestMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CancelPaymentRequestMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.CancelPaymentRequestMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CancelPaymentRequestMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.CancelPaymentRequestMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.CancelPaymentRequestMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.CancelPaymentRequestMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.CancelPaymentRequestMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                return m;
            };

            CancelPaymentRequestMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                return d;
            };

            CancelPaymentRequestMessage.prototype.toJSON = function() {
                return CancelPaymentRequestMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            CancelPaymentRequestMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.CancelPaymentRequestMessage";
            };

            return CancelPaymentRequestMessage;
        })();

        Message.Chat = (function() {

            const Chat = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Chat.prototype.displayName = null;
            Chat.prototype.id = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Chat.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Chat.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Chat.create = function(properties) {
                return new Chat(properties);
            };

            Chat.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName"))
                    w.uint32(10).string(m.displayName);
                if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                    w.uint32(18).string(m.id);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Chat.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.Chat();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayName = r.stringVerify();
                            m._displayName = "displayName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.id = r.stringVerify();
                            m._id = "id";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Chat.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.Chat)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.Chat: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.Chat();
                if (d.displayName != null) {
                    m.displayName = $String(d.displayName);
                }
                if (d.id != null) {
                    m.id = $String(d.id);
                }
                return m;
            };

            Chat.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName")) {
                    d.displayName = m.displayName;
                }
                if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                    d.id = m.id;
                }
                return d;
            };

            Chat.prototype.toJSON = function() {
                return Chat.toObject(this, $protobuf.util.toJSONOptions);
            };

            Chat.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.Chat";
            };

            return Chat;
        })();

        Message.ChatCustomImageWallpaper = (function() {

            const ChatCustomImageWallpaper = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatCustomImageWallpaper.prototype.directPath = null;
            ChatCustomImageWallpaper.prototype.mediaKey = null;
            ChatCustomImageWallpaper.prototype.fileEncSha256 = null;
            ChatCustomImageWallpaper.prototype.fileSha256 = null;
            ChatCustomImageWallpaper.prototype.dimLevel = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatCustomImageWallpaper.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatCustomImageWallpaper.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatCustomImageWallpaper.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatCustomImageWallpaper.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatCustomImageWallpaper.prototype, "_dimLevel", {
                get: $util.oneOfGetter($oneOfFields = ["dimLevel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatCustomImageWallpaper.create = function(properties) {
                return new ChatCustomImageWallpaper(properties);
            };

            ChatCustomImageWallpaper.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(10).string(m.directPath);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(18).bytes(m.mediaKey);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(26).bytes(m.fileEncSha256);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(34).bytes(m.fileSha256);
                if (m.dimLevel != null && $Object.hasOwnProperty.call(m, "dimLevel"))
                    w.uint32(45).float(m.dimLevel);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatCustomImageWallpaper.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ChatCustomImageWallpaper();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 5)
                                break;
                            m.dimLevel = r.float();
                            m._dimLevel = "dimLevel";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatCustomImageWallpaper.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ChatCustomImageWallpaper)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ChatCustomImageWallpaper: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ChatCustomImageWallpaper();
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.dimLevel != null) {
                    m.dimLevel = $Number(d.dimLevel);
                }
                return m;
            };

            ChatCustomImageWallpaper.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.dimLevel != null && $Object.hasOwnProperty.call(m, "dimLevel")) {
                    d.dimLevel = o.json && !$isFinite(m.dimLevel) ? $String(m.dimLevel) : m.dimLevel;
                }
                return d;
            };

            ChatCustomImageWallpaper.prototype.toJSON = function() {
                return ChatCustomImageWallpaper.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatCustomImageWallpaper.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ChatCustomImageWallpaper";
            };

            return ChatCustomImageWallpaper;
        })();

        Message.ChatDefaultWallpaper = (function() {

            const ChatDefaultWallpaper = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatDefaultWallpaper.prototype.isDoodleEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatDefaultWallpaper.prototype, "_isDoodleEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDoodleEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatDefaultWallpaper.create = function(properties) {
                return new ChatDefaultWallpaper(properties);
            };

            ChatDefaultWallpaper.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isDoodleEnabled != null && $Object.hasOwnProperty.call(m, "isDoodleEnabled"))
                    w.uint32(8).bool(m.isDoodleEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatDefaultWallpaper.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ChatDefaultWallpaper();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isDoodleEnabled = r.bool();
                            m._isDoodleEnabled = "isDoodleEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatDefaultWallpaper.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ChatDefaultWallpaper)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ChatDefaultWallpaper: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ChatDefaultWallpaper();
                if (d.isDoodleEnabled != null) {
                    m.isDoodleEnabled = $Boolean(d.isDoodleEnabled);
                }
                return m;
            };

            ChatDefaultWallpaper.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isDoodleEnabled != null && $Object.hasOwnProperty.call(m, "isDoodleEnabled")) {
                    d.isDoodleEnabled = m.isDoodleEnabled;
                }
                return d;
            };

            ChatDefaultWallpaper.prototype.toJSON = function() {
                return ChatDefaultWallpaper.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatDefaultWallpaper.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ChatDefaultWallpaper";
            };

            return ChatDefaultWallpaper;
        })();

        Message.ChatSolidColorWallpaper = (function() {

            const ChatSolidColorWallpaper = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatSolidColorWallpaper.prototype.colorLight = null;
            ChatSolidColorWallpaper.prototype.colorDark = null;
            ChatSolidColorWallpaper.prototype.isDoodleEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatSolidColorWallpaper.prototype, "_colorLight", {
                get: $util.oneOfGetter($oneOfFields = ["colorLight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatSolidColorWallpaper.prototype, "_colorDark", {
                get: $util.oneOfGetter($oneOfFields = ["colorDark"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatSolidColorWallpaper.prototype, "_isDoodleEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDoodleEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatSolidColorWallpaper.create = function(properties) {
                return new ChatSolidColorWallpaper(properties);
            };

            ChatSolidColorWallpaper.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.colorLight != null && $Object.hasOwnProperty.call(m, "colorLight"))
                    w.uint32(10).string(m.colorLight);
                if (m.colorDark != null && $Object.hasOwnProperty.call(m, "colorDark"))
                    w.uint32(18).string(m.colorDark);
                if (m.isDoodleEnabled != null && $Object.hasOwnProperty.call(m, "isDoodleEnabled"))
                    w.uint32(24).bool(m.isDoodleEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatSolidColorWallpaper.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ChatSolidColorWallpaper();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.colorLight = r.stringVerify();
                            m._colorLight = "colorLight";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.colorDark = r.stringVerify();
                            m._colorDark = "colorDark";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.isDoodleEnabled = r.bool();
                            m._isDoodleEnabled = "isDoodleEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatSolidColorWallpaper.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ChatSolidColorWallpaper)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ChatSolidColorWallpaper: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ChatSolidColorWallpaper();
                if (d.colorLight != null) {
                    m.colorLight = $String(d.colorLight);
                }
                if (d.colorDark != null) {
                    m.colorDark = $String(d.colorDark);
                }
                if (d.isDoodleEnabled != null) {
                    m.isDoodleEnabled = $Boolean(d.isDoodleEnabled);
                }
                return m;
            };

            ChatSolidColorWallpaper.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.colorLight != null && $Object.hasOwnProperty.call(m, "colorLight")) {
                    d.colorLight = m.colorLight;
                }
                if (m.colorDark != null && $Object.hasOwnProperty.call(m, "colorDark")) {
                    d.colorDark = m.colorDark;
                }
                if (m.isDoodleEnabled != null && $Object.hasOwnProperty.call(m, "isDoodleEnabled")) {
                    d.isDoodleEnabled = m.isDoodleEnabled;
                }
                return d;
            };

            ChatSolidColorWallpaper.prototype.toJSON = function() {
                return ChatSolidColorWallpaper.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatSolidColorWallpaper.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ChatSolidColorWallpaper";
            };

            return ChatSolidColorWallpaper;
        })();

        Message.ChatStockImageWallpaper = (function() {

            const ChatStockImageWallpaper = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatStockImageWallpaper.prototype.stockImageId = null;
            ChatStockImageWallpaper.prototype.dimLevel = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatStockImageWallpaper.prototype, "_stockImageId", {
                get: $util.oneOfGetter($oneOfFields = ["stockImageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatStockImageWallpaper.prototype, "_dimLevel", {
                get: $util.oneOfGetter($oneOfFields = ["dimLevel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatStockImageWallpaper.create = function(properties) {
                return new ChatStockImageWallpaper(properties);
            };

            ChatStockImageWallpaper.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.stockImageId != null && $Object.hasOwnProperty.call(m, "stockImageId"))
                    w.uint32(10).string(m.stockImageId);
                if (m.dimLevel != null && $Object.hasOwnProperty.call(m, "dimLevel"))
                    w.uint32(21).float(m.dimLevel);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatStockImageWallpaper.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ChatStockImageWallpaper();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.stockImageId = r.stringVerify();
                            m._stockImageId = "stockImageId";
                            continue;
                        }
                    case 2: {
                            if (u !== 5)
                                break;
                            m.dimLevel = r.float();
                            m._dimLevel = "dimLevel";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatStockImageWallpaper.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ChatStockImageWallpaper)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ChatStockImageWallpaper: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ChatStockImageWallpaper();
                if (d.stockImageId != null) {
                    m.stockImageId = $String(d.stockImageId);
                }
                if (d.dimLevel != null) {
                    m.dimLevel = $Number(d.dimLevel);
                }
                return m;
            };

            ChatStockImageWallpaper.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.stockImageId != null && $Object.hasOwnProperty.call(m, "stockImageId")) {
                    d.stockImageId = m.stockImageId;
                }
                if (m.dimLevel != null && $Object.hasOwnProperty.call(m, "dimLevel")) {
                    d.dimLevel = o.json && !$isFinite(m.dimLevel) ? $String(m.dimLevel) : m.dimLevel;
                }
                return d;
            };

            ChatStockImageWallpaper.prototype.toJSON = function() {
                return ChatStockImageWallpaper.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatStockImageWallpaper.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ChatStockImageWallpaper";
            };

            return ChatStockImageWallpaper;
        })();

        Message.ChatThemeSetting = (function() {

            const ChatThemeSetting = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatThemeSetting.prototype.settingTimestampMs = null;
            ChatThemeSetting.prototype.clearTheme = null;
            ChatThemeSetting.prototype.colorSchemeId = null;
            ChatThemeSetting.prototype.defaultWallpaper = null;
            ChatThemeSetting.prototype.solidColor = null;
            ChatThemeSetting.prototype.stockImage = null;
            ChatThemeSetting.prototype.customImage = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatThemeSetting.prototype, "_settingTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["settingTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatThemeSetting.prototype, "_clearTheme", {
                get: $util.oneOfGetter($oneOfFields = ["clearTheme"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatThemeSetting.prototype, "_colorSchemeId", {
                get: $util.oneOfGetter($oneOfFields = ["colorSchemeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            $Object.defineProperty(ChatThemeSetting.prototype, "wallpaper", {
                get: $util.oneOfGetter($oneOfFields = ["defaultWallpaper", "solidColor", "stockImage", "customImage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatThemeSetting.create = function(properties) {
                return new ChatThemeSetting(properties);
            };

            ChatThemeSetting.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.settingTimestampMs != null && $Object.hasOwnProperty.call(m, "settingTimestampMs"))
                    w.uint32(8).int64(m.settingTimestampMs);
                if (m.clearTheme != null && $Object.hasOwnProperty.call(m, "clearTheme"))
                    w.uint32(16).bool(m.clearTheme);
                if (m.colorSchemeId != null && $Object.hasOwnProperty.call(m, "colorSchemeId"))
                    w.uint32(26).string(m.colorSchemeId);
                if (m.defaultWallpaper != null && $Object.hasOwnProperty.call(m, "defaultWallpaper"))
                    $root.proto.Message.ChatDefaultWallpaper.encode(m.defaultWallpaper, w.uint32(82).fork(), q + 1).ldelim();
                if (m.solidColor != null && $Object.hasOwnProperty.call(m, "solidColor"))
                    $root.proto.Message.ChatSolidColorWallpaper.encode(m.solidColor, w.uint32(90).fork(), q + 1).ldelim();
                if (m.stockImage != null && $Object.hasOwnProperty.call(m, "stockImage"))
                    $root.proto.Message.ChatStockImageWallpaper.encode(m.stockImage, w.uint32(98).fork(), q + 1).ldelim();
                if (m.customImage != null && $Object.hasOwnProperty.call(m, "customImage"))
                    $root.proto.Message.ChatCustomImageWallpaper.encode(m.customImage, w.uint32(106).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatThemeSetting.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ChatThemeSetting();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.settingTimestampMs = r.int64();
                            m._settingTimestampMs = "settingTimestampMs";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.clearTheme = r.bool();
                            m._clearTheme = "clearTheme";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.colorSchemeId = r.stringVerify();
                            m._colorSchemeId = "colorSchemeId";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.defaultWallpaper = $root.proto.Message.ChatDefaultWallpaper.decode(r, r.uint32(), $undefined, q + 1, m.defaultWallpaper);
                            m.wallpaper = "defaultWallpaper";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.solidColor = $root.proto.Message.ChatSolidColorWallpaper.decode(r, r.uint32(), $undefined, q + 1, m.solidColor);
                            m.wallpaper = "solidColor";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.stockImage = $root.proto.Message.ChatStockImageWallpaper.decode(r, r.uint32(), $undefined, q + 1, m.stockImage);
                            m.wallpaper = "stockImage";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.customImage = $root.proto.Message.ChatCustomImageWallpaper.decode(r, r.uint32(), $undefined, q + 1, m.customImage);
                            m.wallpaper = "customImage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatThemeSetting.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ChatThemeSetting)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ChatThemeSetting: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ChatThemeSetting();
                if (d.settingTimestampMs != null) {
                    if ($util.Long)
                        m.settingTimestampMs = $util.Long.fromValue(d.settingTimestampMs, false);
                    else if (typeof d.settingTimestampMs === "string")
                        m.settingTimestampMs = $parseInt(d.settingTimestampMs, 10);
                    else if (typeof d.settingTimestampMs === "number")
                        m.settingTimestampMs = d.settingTimestampMs;
                    else if (typeof d.settingTimestampMs === "object")
                        m.settingTimestampMs = new $util.LongBits(d.settingTimestampMs.low >>> 0, d.settingTimestampMs.high >>> 0).toNumber();
                }
                if (d.clearTheme != null) {
                    m.clearTheme = $Boolean(d.clearTheme);
                }
                if (d.colorSchemeId != null) {
                    m.colorSchemeId = $String(d.colorSchemeId);
                }
                if (d.defaultWallpaper != null) {
                    if (!$util.isObject(d.defaultWallpaper))
                        throw $TypeError(".proto.Message.ChatThemeSetting.defaultWallpaper: object expected");
                    m.defaultWallpaper = $root.proto.Message.ChatDefaultWallpaper.fromObject(d.defaultWallpaper, q + 1);
                }
                if (d.solidColor != null) {
                    if (!$util.isObject(d.solidColor))
                        throw $TypeError(".proto.Message.ChatThemeSetting.solidColor: object expected");
                    m.solidColor = $root.proto.Message.ChatSolidColorWallpaper.fromObject(d.solidColor, q + 1);
                }
                if (d.stockImage != null) {
                    if (!$util.isObject(d.stockImage))
                        throw $TypeError(".proto.Message.ChatThemeSetting.stockImage: object expected");
                    m.stockImage = $root.proto.Message.ChatStockImageWallpaper.fromObject(d.stockImage, q + 1);
                }
                if (d.customImage != null) {
                    if (!$util.isObject(d.customImage))
                        throw $TypeError(".proto.Message.ChatThemeSetting.customImage: object expected");
                    m.customImage = $root.proto.Message.ChatCustomImageWallpaper.fromObject(d.customImage, q + 1);
                }
                return m;
            };

            ChatThemeSetting.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.settingTimestampMs != null && $Object.hasOwnProperty.call(m, "settingTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.settingTimestampMs = typeof m.settingTimestampMs === "number" ? $BigInt(m.settingTimestampMs) : $util.Long.fromBits(m.settingTimestampMs.low >>> 0, m.settingTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.settingTimestampMs === "number")
                        d.settingTimestampMs = o.longs === $String ? $String(m.settingTimestampMs) : m.settingTimestampMs;
                    else
                        d.settingTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.settingTimestampMs) : o.longs === $Number ? new $util.LongBits(m.settingTimestampMs.low >>> 0, m.settingTimestampMs.high >>> 0).toNumber() : m.settingTimestampMs;
                }
                if (m.clearTheme != null && $Object.hasOwnProperty.call(m, "clearTheme")) {
                    d.clearTheme = m.clearTheme;
                }
                if (m.colorSchemeId != null && $Object.hasOwnProperty.call(m, "colorSchemeId")) {
                    d.colorSchemeId = m.colorSchemeId;
                }
                if (m.defaultWallpaper != null && $Object.hasOwnProperty.call(m, "defaultWallpaper")) {
                    d.defaultWallpaper = $root.proto.Message.ChatDefaultWallpaper.toObject(m.defaultWallpaper, o, q + 1);
                    if (o.oneofs)
                        d.wallpaper = "defaultWallpaper";
                }
                if (m.solidColor != null && $Object.hasOwnProperty.call(m, "solidColor")) {
                    d.solidColor = $root.proto.Message.ChatSolidColorWallpaper.toObject(m.solidColor, o, q + 1);
                    if (o.oneofs)
                        d.wallpaper = "solidColor";
                }
                if (m.stockImage != null && $Object.hasOwnProperty.call(m, "stockImage")) {
                    d.stockImage = $root.proto.Message.ChatStockImageWallpaper.toObject(m.stockImage, o, q + 1);
                    if (o.oneofs)
                        d.wallpaper = "stockImage";
                }
                if (m.customImage != null && $Object.hasOwnProperty.call(m, "customImage")) {
                    d.customImage = $root.proto.Message.ChatCustomImageWallpaper.toObject(m.customImage, o, q + 1);
                    if (o.oneofs)
                        d.wallpaper = "customImage";
                }
                return d;
            };

            ChatThemeSetting.prototype.toJSON = function() {
                return ChatThemeSetting.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatThemeSetting.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ChatThemeSetting";
            };

            return ChatThemeSetting;
        })();

        Message.CloudAPIThreadControlNotification = (function() {

            const CloudAPIThreadControlNotification = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CloudAPIThreadControlNotification.prototype.status = null;
            CloudAPIThreadControlNotification.prototype.senderNotificationTimestampMs = null;
            CloudAPIThreadControlNotification.prototype.consumerLid = null;
            CloudAPIThreadControlNotification.prototype.consumerPhoneNumber = null;
            CloudAPIThreadControlNotification.prototype.notificationContent = null;
            CloudAPIThreadControlNotification.prototype.shouldSuppressNotification = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_senderNotificationTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderNotificationTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_consumerLid", {
                get: $util.oneOfGetter($oneOfFields = ["consumerLid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_consumerPhoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["consumerPhoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_notificationContent", {
                get: $util.oneOfGetter($oneOfFields = ["notificationContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CloudAPIThreadControlNotification.prototype, "_shouldSuppressNotification", {
                get: $util.oneOfGetter($oneOfFields = ["shouldSuppressNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CloudAPIThreadControlNotification.create = function(properties) {
                return new CloudAPIThreadControlNotification(properties);
            };

            CloudAPIThreadControlNotification.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                    w.uint32(8).int32(m.status);
                if (m.senderNotificationTimestampMs != null && $Object.hasOwnProperty.call(m, "senderNotificationTimestampMs"))
                    w.uint32(16).int64(m.senderNotificationTimestampMs);
                if (m.consumerLid != null && $Object.hasOwnProperty.call(m, "consumerLid"))
                    w.uint32(26).string(m.consumerLid);
                if (m.consumerPhoneNumber != null && $Object.hasOwnProperty.call(m, "consumerPhoneNumber"))
                    w.uint32(34).string(m.consumerPhoneNumber);
                if (m.notificationContent != null && $Object.hasOwnProperty.call(m, "notificationContent"))
                    $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.encode(m.notificationContent, w.uint32(42).fork(), q + 1).ldelim();
                if (m.shouldSuppressNotification != null && $Object.hasOwnProperty.call(m, "shouldSuppressNotification"))
                    w.uint32(48).bool(m.shouldSuppressNotification);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CloudAPIThreadControlNotification.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.CloudAPIThreadControlNotification(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            m._status = "status";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.senderNotificationTimestampMs = r.int64();
                            m._senderNotificationTimestampMs = "senderNotificationTimestampMs";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.consumerLid = r.stringVerify();
                            m._consumerLid = "consumerLid";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.consumerPhoneNumber = r.stringVerify();
                            m._consumerPhoneNumber = "consumerPhoneNumber";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.notificationContent = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.decode(r, r.uint32(), $undefined, q + 1, m.notificationContent);
                            m._notificationContent = "notificationContent";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.shouldSuppressNotification = r.bool();
                            m._shouldSuppressNotification = "shouldSuppressNotification";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CloudAPIThreadControlNotification.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.CloudAPIThreadControlNotification)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.CloudAPIThreadControlNotification: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.CloudAPIThreadControlNotification();
                switch (d.status) {
                case "UNKNOWN":
                case 0:
                    m.status = 0;
                    break;
                case "CONTROL_PASSED":
                case 1:
                    m.status = 1;
                    break;
                case "CONTROL_TAKEN":
                case 2:
                    m.status = 2;
                    break;
                case "INFO":
                case 3:
                    m.status = 3;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                if (d.senderNotificationTimestampMs != null) {
                    if ($util.Long)
                        m.senderNotificationTimestampMs = $util.Long.fromValue(d.senderNotificationTimestampMs, false);
                    else if (typeof d.senderNotificationTimestampMs === "string")
                        m.senderNotificationTimestampMs = $parseInt(d.senderNotificationTimestampMs, 10);
                    else if (typeof d.senderNotificationTimestampMs === "number")
                        m.senderNotificationTimestampMs = d.senderNotificationTimestampMs;
                    else if (typeof d.senderNotificationTimestampMs === "object")
                        m.senderNotificationTimestampMs = new $util.LongBits(d.senderNotificationTimestampMs.low >>> 0, d.senderNotificationTimestampMs.high >>> 0).toNumber();
                }
                if (d.consumerLid != null) {
                    m.consumerLid = $String(d.consumerLid);
                }
                if (d.consumerPhoneNumber != null) {
                    m.consumerPhoneNumber = $String(d.consumerPhoneNumber);
                }
                if (d.notificationContent != null) {
                    if (!$util.isObject(d.notificationContent))
                        throw $TypeError(".proto.Message.CloudAPIThreadControlNotification.notificationContent: object expected");
                    m.notificationContent = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.fromObject(d.notificationContent, q + 1);
                }
                if (d.shouldSuppressNotification != null) {
                    m.shouldSuppressNotification = $Boolean(d.shouldSuppressNotification);
                }
                return m;
            };

            CloudAPIThreadControlNotification.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl[m.status] === $undefined ? m.status : $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControl[m.status] : m.status;
                }
                if (m.senderNotificationTimestampMs != null && $Object.hasOwnProperty.call(m, "senderNotificationTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.senderNotificationTimestampMs = typeof m.senderNotificationTimestampMs === "number" ? $BigInt(m.senderNotificationTimestampMs) : $util.Long.fromBits(m.senderNotificationTimestampMs.low >>> 0, m.senderNotificationTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.senderNotificationTimestampMs === "number")
                        d.senderNotificationTimestampMs = o.longs === $String ? $String(m.senderNotificationTimestampMs) : m.senderNotificationTimestampMs;
                    else
                        d.senderNotificationTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderNotificationTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderNotificationTimestampMs.low >>> 0, m.senderNotificationTimestampMs.high >>> 0).toNumber() : m.senderNotificationTimestampMs;
                }
                if (m.consumerLid != null && $Object.hasOwnProperty.call(m, "consumerLid")) {
                    d.consumerLid = m.consumerLid;
                }
                if (m.consumerPhoneNumber != null && $Object.hasOwnProperty.call(m, "consumerPhoneNumber")) {
                    d.consumerPhoneNumber = m.consumerPhoneNumber;
                }
                if (m.notificationContent != null && $Object.hasOwnProperty.call(m, "notificationContent")) {
                    d.notificationContent = $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent.toObject(m.notificationContent, o, q + 1);
                }
                if (m.shouldSuppressNotification != null && $Object.hasOwnProperty.call(m, "shouldSuppressNotification")) {
                    d.shouldSuppressNotification = m.shouldSuppressNotification;
                }
                return d;
            };

            CloudAPIThreadControlNotification.prototype.toJSON = function() {
                return CloudAPIThreadControlNotification.toObject(this, $protobuf.util.toJSONOptions);
            };

            CloudAPIThreadControlNotification.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.CloudAPIThreadControlNotification";
            };

            CloudAPIThreadControlNotification.CloudAPIThreadControl = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CONTROL_PASSED"] = 1;
                values[valuesById[2] = "CONTROL_TAKEN"] = 2;
                values[valuesById[3] = "INFO"] = 3;
                return values;
            })();

            CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent = (function() {

                const CloudAPIThreadControlNotificationContent = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CloudAPIThreadControlNotificationContent.prototype.handoffNotificationText = null;
                CloudAPIThreadControlNotificationContent.prototype.extraJson = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CloudAPIThreadControlNotificationContent.prototype, "_handoffNotificationText", {
                    get: $util.oneOfGetter($oneOfFields = ["handoffNotificationText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CloudAPIThreadControlNotificationContent.prototype, "_extraJson", {
                    get: $util.oneOfGetter($oneOfFields = ["extraJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CloudAPIThreadControlNotificationContent.create = function(properties) {
                    return new CloudAPIThreadControlNotificationContent(properties);
                };

                CloudAPIThreadControlNotificationContent.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.handoffNotificationText != null && $Object.hasOwnProperty.call(m, "handoffNotificationText"))
                        w.uint32(10).string(m.handoffNotificationText);
                    if (m.extraJson != null && $Object.hasOwnProperty.call(m, "extraJson"))
                        w.uint32(18).string(m.extraJson);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CloudAPIThreadControlNotificationContent.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.handoffNotificationText = r.stringVerify();
                                m._handoffNotificationText = "handoffNotificationText";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.extraJson = r.stringVerify();
                                m._extraJson = "extraJson";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CloudAPIThreadControlNotificationContent.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent();
                    if (d.handoffNotificationText != null) {
                        m.handoffNotificationText = $String(d.handoffNotificationText);
                    }
                    if (d.extraJson != null) {
                        m.extraJson = $String(d.extraJson);
                    }
                    return m;
                };

                CloudAPIThreadControlNotificationContent.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.handoffNotificationText != null && $Object.hasOwnProperty.call(m, "handoffNotificationText")) {
                        d.handoffNotificationText = m.handoffNotificationText;
                    }
                    if (m.extraJson != null && $Object.hasOwnProperty.call(m, "extraJson")) {
                        d.extraJson = m.extraJson;
                    }
                    return d;
                };

                CloudAPIThreadControlNotificationContent.prototype.toJSON = function() {
                    return CloudAPIThreadControlNotificationContent.toObject(this, $protobuf.util.toJSONOptions);
                };

                CloudAPIThreadControlNotificationContent.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.CloudAPIThreadControlNotification.CloudAPIThreadControlNotificationContent";
                };

                return CloudAPIThreadControlNotificationContent;
            })();

            return CloudAPIThreadControlNotification;
        })();

        Message.CommentMessage = (function() {

            const CommentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CommentMessage.prototype.message = null;
            CommentMessage.prototype.targetMessageKey = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CommentMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CommentMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CommentMessage.create = function(properties) {
                return new CommentMessage(properties);
            };

            CommentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    $root.proto.Message.encode(m.message, w.uint32(10).fork(), q + 1).ldelim();
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey"))
                    $root.proto.MessageKey.encode(m.targetMessageKey, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CommentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.CommentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                            m._message = "message";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.targetMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.targetMessageKey);
                            m._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CommentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.CommentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.CommentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.CommentMessage();
                if (d.message != null) {
                    if (!$util.isObject(d.message))
                        throw $TypeError(".proto.Message.CommentMessage.message: object expected");
                    m.message = $root.proto.Message.fromObject(d.message, q + 1);
                }
                if (d.targetMessageKey != null) {
                    if (!$util.isObject(d.targetMessageKey))
                        throw $TypeError(".proto.Message.CommentMessage.targetMessageKey: object expected");
                    m.targetMessageKey = $root.proto.MessageKey.fromObject(d.targetMessageKey, q + 1);
                }
                return m;
            };

            CommentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = $root.proto.Message.toObject(m.message, o, q + 1);
                }
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey")) {
                    d.targetMessageKey = $root.proto.MessageKey.toObject(m.targetMessageKey, o, q + 1);
                }
                return d;
            };

            CommentMessage.prototype.toJSON = function() {
                return CommentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            CommentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.CommentMessage";
            };

            return CommentMessage;
        })();

        Message.ConditionalRevealMessage = (function() {

            const ConditionalRevealMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ConditionalRevealMessage.prototype.encPayload = null;
            ConditionalRevealMessage.prototype.encIv = null;
            ConditionalRevealMessage.prototype.conditionalRevealMessageType = null;
            ConditionalRevealMessage.prototype.revealKeyId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ConditionalRevealMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ConditionalRevealMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ConditionalRevealMessage.prototype, "_conditionalRevealMessageType", {
                get: $util.oneOfGetter($oneOfFields = ["conditionalRevealMessageType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ConditionalRevealMessage.prototype, "_revealKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["revealKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ConditionalRevealMessage.create = function(properties) {
                return new ConditionalRevealMessage(properties);
            };

            ConditionalRevealMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(10).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(18).bytes(m.encIv);
                if (m.conditionalRevealMessageType != null && $Object.hasOwnProperty.call(m, "conditionalRevealMessageType"))
                    w.uint32(24).int32(m.conditionalRevealMessageType);
                if (m.revealKeyId != null && $Object.hasOwnProperty.call(m, "revealKeyId"))
                    w.uint32(34).string(m.revealKeyId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ConditionalRevealMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ConditionalRevealMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.conditionalRevealMessageType = r.int32();
                            m._conditionalRevealMessageType = "conditionalRevealMessageType";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.revealKeyId = r.stringVerify();
                            m._revealKeyId = "revealKeyId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ConditionalRevealMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ConditionalRevealMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ConditionalRevealMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ConditionalRevealMessage();
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                switch (d.conditionalRevealMessageType) {
                case "UNKNOWN":
                case 0:
                    m.conditionalRevealMessageType = 0;
                    break;
                case "SCHEDULED_MESSAGE":
                case 1:
                    m.conditionalRevealMessageType = 1;
                    break;
                default:
                    if (typeof d.conditionalRevealMessageType === "number" && (d.conditionalRevealMessageType | 0) === d.conditionalRevealMessageType)
                        m.conditionalRevealMessageType = d.conditionalRevealMessageType;
                }
                if (d.revealKeyId != null) {
                    m.revealKeyId = $String(d.revealKeyId);
                }
                return m;
            };

            ConditionalRevealMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                if (m.conditionalRevealMessageType != null && $Object.hasOwnProperty.call(m, "conditionalRevealMessageType")) {
                    d.conditionalRevealMessageType = o.enums === $String ? $root.proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType[m.conditionalRevealMessageType] === $undefined ? m.conditionalRevealMessageType : $root.proto.Message.ConditionalRevealMessage.ConditionalRevealMessageType[m.conditionalRevealMessageType] : m.conditionalRevealMessageType;
                }
                if (m.revealKeyId != null && $Object.hasOwnProperty.call(m, "revealKeyId")) {
                    d.revealKeyId = m.revealKeyId;
                }
                return d;
            };

            ConditionalRevealMessage.prototype.toJSON = function() {
                return ConditionalRevealMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ConditionalRevealMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ConditionalRevealMessage";
            };

            ConditionalRevealMessage.ConditionalRevealMessageType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "SCHEDULED_MESSAGE"] = 1;
                return values;
            })();

            return ConditionalRevealMessage;
        })();

        Message.ContactMessage = (function() {

            const ContactMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactMessage.prototype.displayName = null;
            ContactMessage.prototype.vcard = null;
            ContactMessage.prototype.contextInfo = null;
            ContactMessage.prototype.isSelfContact = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactMessage.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactMessage.prototype, "_vcard", {
                get: $util.oneOfGetter($oneOfFields = ["vcard"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactMessage.prototype, "_isSelfContact", {
                get: $util.oneOfGetter($oneOfFields = ["isSelfContact"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactMessage.create = function(properties) {
                return new ContactMessage(properties);
            };

            ContactMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName"))
                    w.uint32(10).string(m.displayName);
                if (m.vcard != null && $Object.hasOwnProperty.call(m, "vcard"))
                    w.uint32(130).string(m.vcard);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.isSelfContact != null && $Object.hasOwnProperty.call(m, "isSelfContact"))
                    w.uint32(144).bool(m.isSelfContact);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ContactMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayName = r.stringVerify();
                            m._displayName = "displayName";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.vcard = r.stringVerify();
                            m._vcard = "vcard";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.isSelfContact = r.bool();
                            m._isSelfContact = "isSelfContact";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ContactMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ContactMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ContactMessage();
                if (d.displayName != null) {
                    m.displayName = $String(d.displayName);
                }
                if (d.vcard != null) {
                    m.vcard = $String(d.vcard);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ContactMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.isSelfContact != null) {
                    m.isSelfContact = $Boolean(d.isSelfContact);
                }
                return m;
            };

            ContactMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName")) {
                    d.displayName = m.displayName;
                }
                if (m.vcard != null && $Object.hasOwnProperty.call(m, "vcard")) {
                    d.vcard = m.vcard;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.isSelfContact != null && $Object.hasOwnProperty.call(m, "isSelfContact")) {
                    d.isSelfContact = m.isSelfContact;
                }
                return d;
            };

            ContactMessage.prototype.toJSON = function() {
                return ContactMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ContactMessage";
            };

            return ContactMessage;
        })();

        Message.ContactsArrayMessage = (function() {

            const ContactsArrayMessage = function (p) {
                this.contacts = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactsArrayMessage.prototype.displayName = null;
            ContactsArrayMessage.prototype.contacts = $util.emptyArray;
            ContactsArrayMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactsArrayMessage.prototype, "_displayName", {
                get: $util.oneOfGetter($oneOfFields = ["displayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactsArrayMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactsArrayMessage.create = function(properties) {
                return new ContactsArrayMessage(properties);
            };

            ContactsArrayMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName"))
                    w.uint32(10).string(m.displayName);
                if (m.contacts != null && m.contacts.length) {
                    for (var i = 0; i < m.contacts.length; ++i)
                        $root.proto.Message.ContactMessage.encode(m.contacts[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactsArrayMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ContactsArrayMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayName = r.stringVerify();
                            m._displayName = "displayName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.contacts && m.contacts.length))
                                m.contacts = [];
                            m.contacts.push($root.proto.Message.ContactMessage.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactsArrayMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ContactsArrayMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ContactsArrayMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ContactsArrayMessage();
                if (d.displayName != null) {
                    m.displayName = $String(d.displayName);
                }
                if (d.contacts) {
                    if (!$Array.isArray(d.contacts))
                        throw $TypeError(".proto.Message.ContactsArrayMessage.contacts: array expected");
                    m.contacts = $Array(d.contacts.length);
                    for (var i = 0; i < d.contacts.length; ++i) {
                        if (!$util.isObject(d.contacts[i]))
                            throw $TypeError(".proto.Message.ContactsArrayMessage.contacts: object expected");
                        m.contacts[i] = $root.proto.Message.ContactMessage.fromObject(d.contacts[i], q + 1);
                    }
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ContactsArrayMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            ContactsArrayMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.contacts = [];
                }
                if (m.displayName != null && $Object.hasOwnProperty.call(m, "displayName")) {
                    d.displayName = m.displayName;
                }
                if (m.contacts && m.contacts.length) {
                    d.contacts = $Array(m.contacts.length);
                    for (var j = 0; j < m.contacts.length; ++j) {
                        d.contacts[j] = $root.proto.Message.ContactMessage.toObject(m.contacts[j], o, q + 1);
                    }
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            ContactsArrayMessage.prototype.toJSON = function() {
                return ContactsArrayMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactsArrayMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ContactsArrayMessage";
            };

            return ContactsArrayMessage;
        })();

        Message.DeclinePaymentRequestMessage = (function() {

            const DeclinePaymentRequestMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DeclinePaymentRequestMessage.prototype.key = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeclinePaymentRequestMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DeclinePaymentRequestMessage.create = function(properties) {
                return new DeclinePaymentRequestMessage(properties);
            };

            DeclinePaymentRequestMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DeclinePaymentRequestMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.DeclinePaymentRequestMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DeclinePaymentRequestMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.DeclinePaymentRequestMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.DeclinePaymentRequestMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.DeclinePaymentRequestMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.DeclinePaymentRequestMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                return m;
            };

            DeclinePaymentRequestMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                return d;
            };

            DeclinePaymentRequestMessage.prototype.toJSON = function() {
                return DeclinePaymentRequestMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            DeclinePaymentRequestMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.DeclinePaymentRequestMessage";
            };

            return DeclinePaymentRequestMessage;
        })();

        Message.DeviceSentMessage = (function() {

            const DeviceSentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DeviceSentMessage.prototype.destinationJid = null;
            DeviceSentMessage.prototype.message = null;
            DeviceSentMessage.prototype.phash = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeviceSentMessage.prototype, "_destinationJid", {
                get: $util.oneOfGetter($oneOfFields = ["destinationJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeviceSentMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeviceSentMessage.prototype, "_phash", {
                get: $util.oneOfGetter($oneOfFields = ["phash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DeviceSentMessage.create = function(properties) {
                return new DeviceSentMessage(properties);
            };

            DeviceSentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.destinationJid != null && $Object.hasOwnProperty.call(m, "destinationJid"))
                    w.uint32(10).string(m.destinationJid);
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    $root.proto.Message.encode(m.message, w.uint32(18).fork(), q + 1).ldelim();
                if (m.phash != null && $Object.hasOwnProperty.call(m, "phash"))
                    w.uint32(26).string(m.phash);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DeviceSentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.DeviceSentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.destinationJid = r.stringVerify();
                            m._destinationJid = "destinationJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                            m._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.phash = r.stringVerify();
                            m._phash = "phash";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DeviceSentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.DeviceSentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.DeviceSentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.DeviceSentMessage();
                if (d.destinationJid != null) {
                    m.destinationJid = $String(d.destinationJid);
                }
                if (d.message != null) {
                    if (!$util.isObject(d.message))
                        throw $TypeError(".proto.Message.DeviceSentMessage.message: object expected");
                    m.message = $root.proto.Message.fromObject(d.message, q + 1);
                }
                if (d.phash != null) {
                    m.phash = $String(d.phash);
                }
                return m;
            };

            DeviceSentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.destinationJid != null && $Object.hasOwnProperty.call(m, "destinationJid")) {
                    d.destinationJid = m.destinationJid;
                }
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = $root.proto.Message.toObject(m.message, o, q + 1);
                }
                if (m.phash != null && $Object.hasOwnProperty.call(m, "phash")) {
                    d.phash = m.phash;
                }
                return d;
            };

            DeviceSentMessage.prototype.toJSON = function() {
                return DeviceSentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            DeviceSentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.DeviceSentMessage";
            };

            return DeviceSentMessage;
        })();

        Message.DocumentMessage = (function() {

            const DocumentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DocumentMessage.prototype.url = null;
            DocumentMessage.prototype.mimetype = null;
            DocumentMessage.prototype.title = null;
            DocumentMessage.prototype.fileSha256 = null;
            DocumentMessage.prototype.fileLength = null;
            DocumentMessage.prototype.pageCount = null;
            DocumentMessage.prototype.mediaKey = null;
            DocumentMessage.prototype.fileName = null;
            DocumentMessage.prototype.fileEncSha256 = null;
            DocumentMessage.prototype.directPath = null;
            DocumentMessage.prototype.mediaKeyTimestamp = null;
            DocumentMessage.prototype.contactVcard = null;
            DocumentMessage.prototype.thumbnailDirectPath = null;
            DocumentMessage.prototype.thumbnailSha256 = null;
            DocumentMessage.prototype.thumbnailEncSha256 = null;
            DocumentMessage.prototype.jpegThumbnail = null;
            DocumentMessage.prototype.contextInfo = null;
            DocumentMessage.prototype.thumbnailHeight = null;
            DocumentMessage.prototype.thumbnailWidth = null;
            DocumentMessage.prototype.caption = null;
            DocumentMessage.prototype.accessibilityLabel = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_pageCount", {
                get: $util.oneOfGetter($oneOfFields = ["pageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_fileName", {
                get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_contactVcard", {
                get: $util.oneOfGetter($oneOfFields = ["contactVcard"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DocumentMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DocumentMessage.create = function(properties) {
                return new DocumentMessage(properties);
            };

            DocumentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(18).string(m.mimetype);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(26).string(m.title);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(34).bytes(m.fileSha256);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(40).uint64(m.fileLength);
                if (m.pageCount != null && $Object.hasOwnProperty.call(m, "pageCount"))
                    w.uint32(48).uint32(m.pageCount);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(58).bytes(m.mediaKey);
                if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName"))
                    w.uint32(66).string(m.fileName);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(74).bytes(m.fileEncSha256);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(82).string(m.directPath);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(88).int64(m.mediaKeyTimestamp);
                if (m.contactVcard != null && $Object.hasOwnProperty.call(m, "contactVcard"))
                    w.uint32(96).bool(m.contactVcard);
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                    w.uint32(106).string(m.thumbnailDirectPath);
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256"))
                    w.uint32(114).bytes(m.thumbnailSha256);
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256"))
                    w.uint32(122).bytes(m.thumbnailEncSha256);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight"))
                    w.uint32(144).uint32(m.thumbnailHeight);
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth"))
                    w.uint32(152).uint32(m.thumbnailWidth);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(162).string(m.caption);
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel"))
                    w.uint32(170).string(m.accessibilityLabel);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DocumentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.DocumentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.pageCount = r.uint32();
                            m._pageCount = "pageCount";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.fileName = r.stringVerify();
                            m._fileName = "fileName";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.contactVcard = r.bool();
                            m._contactVcard = "contactVcard";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.thumbnailDirectPath = r.stringVerify();
                            m._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.thumbnailSha256 = r.bytes();
                            m._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            m.thumbnailEncSha256 = r.bytes();
                            m._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.thumbnailHeight = r.uint32();
                            m._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.thumbnailWidth = r.uint32();
                            m._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            m.accessibilityLabel = r.stringVerify();
                            m._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DocumentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.DocumentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.DocumentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.DocumentMessage();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.pageCount != null) {
                    m.pageCount = d.pageCount >>> 0;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.fileName != null) {
                    m.fileName = $String(d.fileName);
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.contactVcard != null) {
                    m.contactVcard = $Boolean(d.contactVcard);
                }
                if (d.thumbnailDirectPath != null) {
                    m.thumbnailDirectPath = $String(d.thumbnailDirectPath);
                }
                if (d.thumbnailSha256 != null) {
                    if (typeof d.thumbnailSha256 === "string")
                        $util.base64.decode(d.thumbnailSha256, m.thumbnailSha256 = $util.newBuffer($util.base64.length(d.thumbnailSha256)), 0);
                    else if (d.thumbnailSha256.length >= 0)
                        m.thumbnailSha256 = d.thumbnailSha256;
                }
                if (d.thumbnailEncSha256 != null) {
                    if (typeof d.thumbnailEncSha256 === "string")
                        $util.base64.decode(d.thumbnailEncSha256, m.thumbnailEncSha256 = $util.newBuffer($util.base64.length(d.thumbnailEncSha256)), 0);
                    else if (d.thumbnailEncSha256.length >= 0)
                        m.thumbnailEncSha256 = d.thumbnailEncSha256;
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.DocumentMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.thumbnailHeight != null) {
                    m.thumbnailHeight = d.thumbnailHeight >>> 0;
                }
                if (d.thumbnailWidth != null) {
                    m.thumbnailWidth = d.thumbnailWidth >>> 0;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.accessibilityLabel != null) {
                    m.accessibilityLabel = $String(d.accessibilityLabel);
                }
                return m;
            };

            DocumentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.pageCount != null && $Object.hasOwnProperty.call(m, "pageCount")) {
                    d.pageCount = m.pageCount;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName")) {
                    d.fileName = m.fileName;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.contactVcard != null && $Object.hasOwnProperty.call(m, "contactVcard")) {
                    d.contactVcard = m.contactVcard;
                }
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath")) {
                    d.thumbnailDirectPath = m.thumbnailDirectPath;
                }
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256")) {
                    d.thumbnailSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailSha256, 0, m.thumbnailSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailSha256) : m.thumbnailSha256;
                }
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256")) {
                    d.thumbnailEncSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailEncSha256, 0, m.thumbnailEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailEncSha256) : m.thumbnailEncSha256;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight")) {
                    d.thumbnailHeight = m.thumbnailHeight;
                }
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth")) {
                    d.thumbnailWidth = m.thumbnailWidth;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel")) {
                    d.accessibilityLabel = m.accessibilityLabel;
                }
                return d;
            };

            DocumentMessage.prototype.toJSON = function() {
                return DocumentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            DocumentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.DocumentMessage";
            };

            return DocumentMessage;
        })();

        Message.EncCommentMessage = (function() {

            const EncCommentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EncCommentMessage.prototype.targetMessageKey = null;
            EncCommentMessage.prototype.encPayload = null;
            EncCommentMessage.prototype.encIv = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncCommentMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncCommentMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncCommentMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EncCommentMessage.create = function(properties) {
                return new EncCommentMessage(properties);
            };

            EncCommentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey"))
                    $root.proto.MessageKey.encode(m.targetMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(18).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(26).bytes(m.encIv);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EncCommentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.EncCommentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.targetMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.targetMessageKey);
                            m._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EncCommentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.EncCommentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.EncCommentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.EncCommentMessage();
                if (d.targetMessageKey != null) {
                    if (!$util.isObject(d.targetMessageKey))
                        throw $TypeError(".proto.Message.EncCommentMessage.targetMessageKey: object expected");
                    m.targetMessageKey = $root.proto.MessageKey.fromObject(d.targetMessageKey, q + 1);
                }
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                return m;
            };

            EncCommentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey")) {
                    d.targetMessageKey = $root.proto.MessageKey.toObject(m.targetMessageKey, o, q + 1);
                }
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                return d;
            };

            EncCommentMessage.prototype.toJSON = function() {
                return EncCommentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EncCommentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.EncCommentMessage";
            };

            return EncCommentMessage;
        })();

        Message.EncEventResponseMessage = (function() {

            const EncEventResponseMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EncEventResponseMessage.prototype.eventCreationMessageKey = null;
            EncEventResponseMessage.prototype.encPayload = null;
            EncEventResponseMessage.prototype.encIv = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncEventResponseMessage.prototype, "_eventCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["eventCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncEventResponseMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncEventResponseMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EncEventResponseMessage.create = function(properties) {
                return new EncEventResponseMessage(properties);
            };

            EncEventResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.eventCreationMessageKey != null && $Object.hasOwnProperty.call(m, "eventCreationMessageKey"))
                    $root.proto.MessageKey.encode(m.eventCreationMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(18).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(26).bytes(m.encIv);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EncEventResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.EncEventResponseMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.eventCreationMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.eventCreationMessageKey);
                            m._eventCreationMessageKey = "eventCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EncEventResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.EncEventResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.EncEventResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.EncEventResponseMessage();
                if (d.eventCreationMessageKey != null) {
                    if (!$util.isObject(d.eventCreationMessageKey))
                        throw $TypeError(".proto.Message.EncEventResponseMessage.eventCreationMessageKey: object expected");
                    m.eventCreationMessageKey = $root.proto.MessageKey.fromObject(d.eventCreationMessageKey, q + 1);
                }
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                return m;
            };

            EncEventResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.eventCreationMessageKey != null && $Object.hasOwnProperty.call(m, "eventCreationMessageKey")) {
                    d.eventCreationMessageKey = $root.proto.MessageKey.toObject(m.eventCreationMessageKey, o, q + 1);
                }
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                return d;
            };

            EncEventResponseMessage.prototype.toJSON = function() {
                return EncEventResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EncEventResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.EncEventResponseMessage";
            };

            return EncEventResponseMessage;
        })();

        Message.EncReactionMessage = (function() {

            const EncReactionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EncReactionMessage.prototype.targetMessageKey = null;
            EncReactionMessage.prototype.encPayload = null;
            EncReactionMessage.prototype.encIv = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncReactionMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncReactionMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EncReactionMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EncReactionMessage.create = function(properties) {
                return new EncReactionMessage(properties);
            };

            EncReactionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey"))
                    $root.proto.MessageKey.encode(m.targetMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(18).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(26).bytes(m.encIv);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EncReactionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.EncReactionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.targetMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.targetMessageKey);
                            m._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EncReactionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.EncReactionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.EncReactionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.EncReactionMessage();
                if (d.targetMessageKey != null) {
                    if (!$util.isObject(d.targetMessageKey))
                        throw $TypeError(".proto.Message.EncReactionMessage.targetMessageKey: object expected");
                    m.targetMessageKey = $root.proto.MessageKey.fromObject(d.targetMessageKey, q + 1);
                }
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                return m;
            };

            EncReactionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey")) {
                    d.targetMessageKey = $root.proto.MessageKey.toObject(m.targetMessageKey, o, q + 1);
                }
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                return d;
            };

            EncReactionMessage.prototype.toJSON = function() {
                return EncReactionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EncReactionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.EncReactionMessage";
            };

            return EncReactionMessage;
        })();

        Message.EventInviteMessage = (function() {

            const EventInviteMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EventInviteMessage.prototype.contextInfo = null;
            EventInviteMessage.prototype.eventId = null;
            EventInviteMessage.prototype.eventTitle = null;
            EventInviteMessage.prototype.jpegThumbnail = null;
            EventInviteMessage.prototype.startTime = null;
            EventInviteMessage.prototype.caption = null;
            EventInviteMessage.prototype.isCanceled = null;
            EventInviteMessage.prototype.endTime = null;
            EventInviteMessage.prototype.callLink = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_eventId", {
                get: $util.oneOfGetter($oneOfFields = ["eventId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_eventTitle", {
                get: $util.oneOfGetter($oneOfFields = ["eventTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_startTime", {
                get: $util.oneOfGetter($oneOfFields = ["startTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_isCanceled", {
                get: $util.oneOfGetter($oneOfFields = ["isCanceled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_endTime", {
                get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventInviteMessage.prototype, "_callLink", {
                get: $util.oneOfGetter($oneOfFields = ["callLink"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EventInviteMessage.create = function(properties) {
                return new EventInviteMessage(properties);
            };

            EventInviteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(10).fork(), q + 1).ldelim();
                if (m.eventId != null && $Object.hasOwnProperty.call(m, "eventId"))
                    w.uint32(18).string(m.eventId);
                if (m.eventTitle != null && $Object.hasOwnProperty.call(m, "eventTitle"))
                    w.uint32(26).string(m.eventTitle);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(34).bytes(m.jpegThumbnail);
                if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime"))
                    w.uint32(40).int64(m.startTime);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(50).string(m.caption);
                if (m.isCanceled != null && $Object.hasOwnProperty.call(m, "isCanceled"))
                    w.uint32(56).bool(m.isCanceled);
                if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime"))
                    w.uint32(64).int64(m.endTime);
                if (m.callLink != null && $Object.hasOwnProperty.call(m, "callLink"))
                    w.uint32(74).string(m.callLink);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EventInviteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.EventInviteMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.eventId = r.stringVerify();
                            m._eventId = "eventId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.eventTitle = r.stringVerify();
                            m._eventTitle = "eventTitle";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.startTime = r.int64();
                            m._startTime = "startTime";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.isCanceled = r.bool();
                            m._isCanceled = "isCanceled";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.endTime = r.int64();
                            m._endTime = "endTime";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.callLink = r.stringVerify();
                            m._callLink = "callLink";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EventInviteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.EventInviteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.EventInviteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.EventInviteMessage();
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.EventInviteMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.eventId != null) {
                    m.eventId = $String(d.eventId);
                }
                if (d.eventTitle != null) {
                    m.eventTitle = $String(d.eventTitle);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.startTime != null) {
                    if ($util.Long)
                        m.startTime = $util.Long.fromValue(d.startTime, false);
                    else if (typeof d.startTime === "string")
                        m.startTime = $parseInt(d.startTime, 10);
                    else if (typeof d.startTime === "number")
                        m.startTime = d.startTime;
                    else if (typeof d.startTime === "object")
                        m.startTime = new $util.LongBits(d.startTime.low >>> 0, d.startTime.high >>> 0).toNumber();
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.isCanceled != null) {
                    m.isCanceled = $Boolean(d.isCanceled);
                }
                if (d.endTime != null) {
                    if ($util.Long)
                        m.endTime = $util.Long.fromValue(d.endTime, false);
                    else if (typeof d.endTime === "string")
                        m.endTime = $parseInt(d.endTime, 10);
                    else if (typeof d.endTime === "number")
                        m.endTime = d.endTime;
                    else if (typeof d.endTime === "object")
                        m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
                }
                if (d.callLink != null) {
                    m.callLink = $String(d.callLink);
                }
                return m;
            };

            EventInviteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.eventId != null && $Object.hasOwnProperty.call(m, "eventId")) {
                    d.eventId = m.eventId;
                }
                if (m.eventTitle != null && $Object.hasOwnProperty.call(m, "eventTitle")) {
                    d.eventTitle = m.eventTitle;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.startTime = typeof m.startTime === "number" ? $BigInt(m.startTime) : $util.Long.fromBits(m.startTime.low >>> 0, m.startTime.high >>> 0, false).toBigInt();
                    else if (typeof m.startTime === "number")
                        d.startTime = o.longs === $String ? $String(m.startTime) : m.startTime;
                    else
                        d.startTime = o.longs === $String ? $util.Long.prototype.toString.call(m.startTime) : o.longs === $Number ? new $util.LongBits(m.startTime.low >>> 0, m.startTime.high >>> 0).toNumber() : m.startTime;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.isCanceled != null && $Object.hasOwnProperty.call(m, "isCanceled")) {
                    d.isCanceled = m.isCanceled;
                }
                if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.endTime = typeof m.endTime === "number" ? $BigInt(m.endTime) : $util.Long.fromBits(m.endTime.low >>> 0, m.endTime.high >>> 0, false).toBigInt();
                    else if (typeof m.endTime === "number")
                        d.endTime = o.longs === $String ? $String(m.endTime) : m.endTime;
                    else
                        d.endTime = o.longs === $String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === $Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                }
                if (m.callLink != null && $Object.hasOwnProperty.call(m, "callLink")) {
                    d.callLink = m.callLink;
                }
                return d;
            };

            EventInviteMessage.prototype.toJSON = function() {
                return EventInviteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EventInviteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.EventInviteMessage";
            };

            return EventInviteMessage;
        })();

        Message.EventMessage = (function() {

            const EventMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EventMessage.prototype.contextInfo = null;
            EventMessage.prototype.isCanceled = null;
            EventMessage.prototype.name = null;
            EventMessage.prototype.description = null;
            EventMessage.prototype.location = null;
            EventMessage.prototype.joinLink = null;
            EventMessage.prototype.startTime = null;
            EventMessage.prototype.endTime = null;
            EventMessage.prototype.extraGuestsAllowed = null;
            EventMessage.prototype.isScheduleCall = null;
            EventMessage.prototype.hasReminder = null;
            EventMessage.prototype.reminderOffsetSec = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_isCanceled", {
                get: $util.oneOfGetter($oneOfFields = ["isCanceled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_location", {
                get: $util.oneOfGetter($oneOfFields = ["location"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_joinLink", {
                get: $util.oneOfGetter($oneOfFields = ["joinLink"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_startTime", {
                get: $util.oneOfGetter($oneOfFields = ["startTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_endTime", {
                get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_extraGuestsAllowed", {
                get: $util.oneOfGetter($oneOfFields = ["extraGuestsAllowed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_isScheduleCall", {
                get: $util.oneOfGetter($oneOfFields = ["isScheduleCall"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_hasReminder", {
                get: $util.oneOfGetter($oneOfFields = ["hasReminder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventMessage.prototype, "_reminderOffsetSec", {
                get: $util.oneOfGetter($oneOfFields = ["reminderOffsetSec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EventMessage.create = function(properties) {
                return new EventMessage(properties);
            };

            EventMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(10).fork(), q + 1).ldelim();
                if (m.isCanceled != null && $Object.hasOwnProperty.call(m, "isCanceled"))
                    w.uint32(16).bool(m.isCanceled);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(26).string(m.name);
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(34).string(m.description);
                if (m.location != null && $Object.hasOwnProperty.call(m, "location"))
                    $root.proto.Message.LocationMessage.encode(m.location, w.uint32(42).fork(), q + 1).ldelim();
                if (m.joinLink != null && $Object.hasOwnProperty.call(m, "joinLink"))
                    w.uint32(50).string(m.joinLink);
                if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime"))
                    w.uint32(56).int64(m.startTime);
                if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime"))
                    w.uint32(64).int64(m.endTime);
                if (m.extraGuestsAllowed != null && $Object.hasOwnProperty.call(m, "extraGuestsAllowed"))
                    w.uint32(72).bool(m.extraGuestsAllowed);
                if (m.isScheduleCall != null && $Object.hasOwnProperty.call(m, "isScheduleCall"))
                    w.uint32(80).bool(m.isScheduleCall);
                if (m.hasReminder != null && $Object.hasOwnProperty.call(m, "hasReminder"))
                    w.uint32(88).bool(m.hasReminder);
                if (m.reminderOffsetSec != null && $Object.hasOwnProperty.call(m, "reminderOffsetSec"))
                    w.uint32(96).int64(m.reminderOffsetSec);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EventMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.EventMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.isCanceled = r.bool();
                            m._isCanceled = "isCanceled";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.location = $root.proto.Message.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.location);
                            m._location = "location";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.joinLink = r.stringVerify();
                            m._joinLink = "joinLink";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.startTime = r.int64();
                            m._startTime = "startTime";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.endTime = r.int64();
                            m._endTime = "endTime";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.extraGuestsAllowed = r.bool();
                            m._extraGuestsAllowed = "extraGuestsAllowed";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.isScheduleCall = r.bool();
                            m._isScheduleCall = "isScheduleCall";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.hasReminder = r.bool();
                            m._hasReminder = "hasReminder";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.reminderOffsetSec = r.int64();
                            m._reminderOffsetSec = "reminderOffsetSec";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EventMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.EventMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.EventMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.EventMessage();
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.EventMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.isCanceled != null) {
                    m.isCanceled = $Boolean(d.isCanceled);
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                if (d.location != null) {
                    if (!$util.isObject(d.location))
                        throw $TypeError(".proto.Message.EventMessage.location: object expected");
                    m.location = $root.proto.Message.LocationMessage.fromObject(d.location, q + 1);
                }
                if (d.joinLink != null) {
                    m.joinLink = $String(d.joinLink);
                }
                if (d.startTime != null) {
                    if ($util.Long)
                        m.startTime = $util.Long.fromValue(d.startTime, false);
                    else if (typeof d.startTime === "string")
                        m.startTime = $parseInt(d.startTime, 10);
                    else if (typeof d.startTime === "number")
                        m.startTime = d.startTime;
                    else if (typeof d.startTime === "object")
                        m.startTime = new $util.LongBits(d.startTime.low >>> 0, d.startTime.high >>> 0).toNumber();
                }
                if (d.endTime != null) {
                    if ($util.Long)
                        m.endTime = $util.Long.fromValue(d.endTime, false);
                    else if (typeof d.endTime === "string")
                        m.endTime = $parseInt(d.endTime, 10);
                    else if (typeof d.endTime === "number")
                        m.endTime = d.endTime;
                    else if (typeof d.endTime === "object")
                        m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
                }
                if (d.extraGuestsAllowed != null) {
                    m.extraGuestsAllowed = $Boolean(d.extraGuestsAllowed);
                }
                if (d.isScheduleCall != null) {
                    m.isScheduleCall = $Boolean(d.isScheduleCall);
                }
                if (d.hasReminder != null) {
                    m.hasReminder = $Boolean(d.hasReminder);
                }
                if (d.reminderOffsetSec != null) {
                    if ($util.Long)
                        m.reminderOffsetSec = $util.Long.fromValue(d.reminderOffsetSec, false);
                    else if (typeof d.reminderOffsetSec === "string")
                        m.reminderOffsetSec = $parseInt(d.reminderOffsetSec, 10);
                    else if (typeof d.reminderOffsetSec === "number")
                        m.reminderOffsetSec = d.reminderOffsetSec;
                    else if (typeof d.reminderOffsetSec === "object")
                        m.reminderOffsetSec = new $util.LongBits(d.reminderOffsetSec.low >>> 0, d.reminderOffsetSec.high >>> 0).toNumber();
                }
                return m;
            };

            EventMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.isCanceled != null && $Object.hasOwnProperty.call(m, "isCanceled")) {
                    d.isCanceled = m.isCanceled;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                if (m.location != null && $Object.hasOwnProperty.call(m, "location")) {
                    d.location = $root.proto.Message.LocationMessage.toObject(m.location, o, q + 1);
                }
                if (m.joinLink != null && $Object.hasOwnProperty.call(m, "joinLink")) {
                    d.joinLink = m.joinLink;
                }
                if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.startTime = typeof m.startTime === "number" ? $BigInt(m.startTime) : $util.Long.fromBits(m.startTime.low >>> 0, m.startTime.high >>> 0, false).toBigInt();
                    else if (typeof m.startTime === "number")
                        d.startTime = o.longs === $String ? $String(m.startTime) : m.startTime;
                    else
                        d.startTime = o.longs === $String ? $util.Long.prototype.toString.call(m.startTime) : o.longs === $Number ? new $util.LongBits(m.startTime.low >>> 0, m.startTime.high >>> 0).toNumber() : m.startTime;
                }
                if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.endTime = typeof m.endTime === "number" ? $BigInt(m.endTime) : $util.Long.fromBits(m.endTime.low >>> 0, m.endTime.high >>> 0, false).toBigInt();
                    else if (typeof m.endTime === "number")
                        d.endTime = o.longs === $String ? $String(m.endTime) : m.endTime;
                    else
                        d.endTime = o.longs === $String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === $Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                }
                if (m.extraGuestsAllowed != null && $Object.hasOwnProperty.call(m, "extraGuestsAllowed")) {
                    d.extraGuestsAllowed = m.extraGuestsAllowed;
                }
                if (m.isScheduleCall != null && $Object.hasOwnProperty.call(m, "isScheduleCall")) {
                    d.isScheduleCall = m.isScheduleCall;
                }
                if (m.hasReminder != null && $Object.hasOwnProperty.call(m, "hasReminder")) {
                    d.hasReminder = m.hasReminder;
                }
                if (m.reminderOffsetSec != null && $Object.hasOwnProperty.call(m, "reminderOffsetSec")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.reminderOffsetSec = typeof m.reminderOffsetSec === "number" ? $BigInt(m.reminderOffsetSec) : $util.Long.fromBits(m.reminderOffsetSec.low >>> 0, m.reminderOffsetSec.high >>> 0, false).toBigInt();
                    else if (typeof m.reminderOffsetSec === "number")
                        d.reminderOffsetSec = o.longs === $String ? $String(m.reminderOffsetSec) : m.reminderOffsetSec;
                    else
                        d.reminderOffsetSec = o.longs === $String ? $util.Long.prototype.toString.call(m.reminderOffsetSec) : o.longs === $Number ? new $util.LongBits(m.reminderOffsetSec.low >>> 0, m.reminderOffsetSec.high >>> 0).toNumber() : m.reminderOffsetSec;
                }
                return d;
            };

            EventMessage.prototype.toJSON = function() {
                return EventMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EventMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.EventMessage";
            };

            return EventMessage;
        })();

        Message.EventResponseMessage = (function() {

            const EventResponseMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EventResponseMessage.prototype.response = null;
            EventResponseMessage.prototype.timestampMs = null;
            EventResponseMessage.prototype.extraGuestCount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventResponseMessage.prototype, "_response", {
                get: $util.oneOfGetter($oneOfFields = ["response"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventResponseMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventResponseMessage.prototype, "_extraGuestCount", {
                get: $util.oneOfGetter($oneOfFields = ["extraGuestCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EventResponseMessage.create = function(properties) {
                return new EventResponseMessage(properties);
            };

            EventResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.response != null && $Object.hasOwnProperty.call(m, "response"))
                    w.uint32(8).int32(m.response);
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                    w.uint32(16).int64(m.timestampMs);
                if (m.extraGuestCount != null && $Object.hasOwnProperty.call(m, "extraGuestCount"))
                    w.uint32(24).int32(m.extraGuestCount);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EventResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.EventResponseMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.response = r.int32();
                            m._response = "response";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.timestampMs = r.int64();
                            m._timestampMs = "timestampMs";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.extraGuestCount = r.int32();
                            m._extraGuestCount = "extraGuestCount";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EventResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.EventResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.EventResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.EventResponseMessage();
                switch (d.response) {
                case "UNKNOWN":
                case 0:
                    m.response = 0;
                    break;
                case "GOING":
                case 1:
                    m.response = 1;
                    break;
                case "NOT_GOING":
                case 2:
                    m.response = 2;
                    break;
                case "MAYBE":
                case 3:
                    m.response = 3;
                    break;
                default:
                    if (typeof d.response === "number" && (d.response | 0) === d.response)
                        m.response = d.response;
                }
                if (d.timestampMs != null) {
                    if ($util.Long)
                        m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                    else if (typeof d.timestampMs === "string")
                        m.timestampMs = $parseInt(d.timestampMs, 10);
                    else if (typeof d.timestampMs === "number")
                        m.timestampMs = d.timestampMs;
                    else if (typeof d.timestampMs === "object")
                        m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
                }
                if (d.extraGuestCount != null) {
                    m.extraGuestCount = d.extraGuestCount | 0;
                }
                return m;
            };

            EventResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.response != null && $Object.hasOwnProperty.call(m, "response")) {
                    d.response = o.enums === $String ? $root.proto.Message.EventResponseMessage.EventResponseType[m.response] === $undefined ? m.response : $root.proto.Message.EventResponseMessage.EventResponseType[m.response] : m.response;
                }
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.timestampMs === "number")
                        d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                    else
                        d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
                }
                if (m.extraGuestCount != null && $Object.hasOwnProperty.call(m, "extraGuestCount")) {
                    d.extraGuestCount = m.extraGuestCount;
                }
                return d;
            };

            EventResponseMessage.prototype.toJSON = function() {
                return EventResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            EventResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.EventResponseMessage";
            };

            EventResponseMessage.EventResponseType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "GOING"] = 1;
                values[valuesById[2] = "NOT_GOING"] = 2;
                values[valuesById[3] = "MAYBE"] = 3;
                return values;
            })();

            return EventResponseMessage;
        })();

        Message.ExtendedTextMessage = (function() {

            const ExtendedTextMessage = function (p) {
                this.endCardTiles = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ExtendedTextMessage.prototype.text = null;
            ExtendedTextMessage.prototype.matchedText = null;
            ExtendedTextMessage.prototype.description = null;
            ExtendedTextMessage.prototype.title = null;
            ExtendedTextMessage.prototype.textArgb = null;
            ExtendedTextMessage.prototype.backgroundArgb = null;
            ExtendedTextMessage.prototype.font = null;
            ExtendedTextMessage.prototype.previewType = null;
            ExtendedTextMessage.prototype.jpegThumbnail = null;
            ExtendedTextMessage.prototype.contextInfo = null;
            ExtendedTextMessage.prototype.doNotPlayInline = null;
            ExtendedTextMessage.prototype.thumbnailDirectPath = null;
            ExtendedTextMessage.prototype.thumbnailSha256 = null;
            ExtendedTextMessage.prototype.thumbnailEncSha256 = null;
            ExtendedTextMessage.prototype.mediaKey = null;
            ExtendedTextMessage.prototype.mediaKeyTimestamp = null;
            ExtendedTextMessage.prototype.thumbnailHeight = null;
            ExtendedTextMessage.prototype.thumbnailWidth = null;
            ExtendedTextMessage.prototype.inviteLinkGroupType = null;
            ExtendedTextMessage.prototype.inviteLinkParentGroupSubjectV2 = null;
            ExtendedTextMessage.prototype.inviteLinkParentGroupThumbnailV2 = null;
            ExtendedTextMessage.prototype.inviteLinkGroupTypeV2 = null;
            ExtendedTextMessage.prototype.viewOnce = null;
            ExtendedTextMessage.prototype.videoHeight = null;
            ExtendedTextMessage.prototype.videoWidth = null;
            ExtendedTextMessage.prototype.faviconMmsMetadata = null;
            ExtendedTextMessage.prototype.linkPreviewMetadata = null;
            ExtendedTextMessage.prototype.paymentLinkMetadata = null;
            ExtendedTextMessage.prototype.endCardTiles = $util.emptyArray;
            ExtendedTextMessage.prototype.videoContentUrl = null;
            ExtendedTextMessage.prototype.musicMetadata = null;
            ExtendedTextMessage.prototype.paymentExtendedMetadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_matchedText", {
                get: $util.oneOfGetter($oneOfFields = ["matchedText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_textArgb", {
                get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_backgroundArgb", {
                get: $util.oneOfGetter($oneOfFields = ["backgroundArgb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_font", {
                get: $util.oneOfGetter($oneOfFields = ["font"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_previewType", {
                get: $util.oneOfGetter($oneOfFields = ["previewType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_doNotPlayInline", {
                get: $util.oneOfGetter($oneOfFields = ["doNotPlayInline"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkGroupType", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkGroupType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkParentGroupSubjectV2", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkParentGroupSubjectV2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkParentGroupThumbnailV2", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkParentGroupThumbnailV2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_inviteLinkGroupTypeV2", {
                get: $util.oneOfGetter($oneOfFields = ["inviteLinkGroupTypeV2"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_videoHeight", {
                get: $util.oneOfGetter($oneOfFields = ["videoHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_videoWidth", {
                get: $util.oneOfGetter($oneOfFields = ["videoWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_faviconMmsMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["faviconMmsMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_linkPreviewMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["linkPreviewMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_paymentLinkMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["paymentLinkMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_videoContentUrl", {
                get: $util.oneOfGetter($oneOfFields = ["videoContentUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_musicMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["musicMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExtendedTextMessage.prototype, "_paymentExtendedMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["paymentExtendedMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExtendedTextMessage.create = function(properties) {
                return new ExtendedTextMessage(properties);
            };

            ExtendedTextMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(10).string(m.text);
                if (m.matchedText != null && $Object.hasOwnProperty.call(m, "matchedText"))
                    w.uint32(18).string(m.matchedText);
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(42).string(m.description);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(50).string(m.title);
                if (m.textArgb != null && $Object.hasOwnProperty.call(m, "textArgb"))
                    w.uint32(61).fixed32(m.textArgb);
                if (m.backgroundArgb != null && $Object.hasOwnProperty.call(m, "backgroundArgb"))
                    w.uint32(69).fixed32(m.backgroundArgb);
                if (m.font != null && $Object.hasOwnProperty.call(m, "font"))
                    w.uint32(72).int32(m.font);
                if (m.previewType != null && $Object.hasOwnProperty.call(m, "previewType"))
                    w.uint32(80).int32(m.previewType);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.doNotPlayInline != null && $Object.hasOwnProperty.call(m, "doNotPlayInline"))
                    w.uint32(144).bool(m.doNotPlayInline);
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                    w.uint32(154).string(m.thumbnailDirectPath);
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256"))
                    w.uint32(162).bytes(m.thumbnailSha256);
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256"))
                    w.uint32(170).bytes(m.thumbnailEncSha256);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(178).bytes(m.mediaKey);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(184).int64(m.mediaKeyTimestamp);
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight"))
                    w.uint32(192).uint32(m.thumbnailHeight);
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth"))
                    w.uint32(200).uint32(m.thumbnailWidth);
                if (m.inviteLinkGroupType != null && $Object.hasOwnProperty.call(m, "inviteLinkGroupType"))
                    w.uint32(208).int32(m.inviteLinkGroupType);
                if (m.inviteLinkParentGroupSubjectV2 != null && $Object.hasOwnProperty.call(m, "inviteLinkParentGroupSubjectV2"))
                    w.uint32(218).string(m.inviteLinkParentGroupSubjectV2);
                if (m.inviteLinkParentGroupThumbnailV2 != null && $Object.hasOwnProperty.call(m, "inviteLinkParentGroupThumbnailV2"))
                    w.uint32(226).bytes(m.inviteLinkParentGroupThumbnailV2);
                if (m.inviteLinkGroupTypeV2 != null && $Object.hasOwnProperty.call(m, "inviteLinkGroupTypeV2"))
                    w.uint32(232).int32(m.inviteLinkGroupTypeV2);
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce"))
                    w.uint32(240).bool(m.viewOnce);
                if (m.videoHeight != null && $Object.hasOwnProperty.call(m, "videoHeight"))
                    w.uint32(248).uint32(m.videoHeight);
                if (m.videoWidth != null && $Object.hasOwnProperty.call(m, "videoWidth"))
                    w.uint32(256).uint32(m.videoWidth);
                if (m.faviconMmsMetadata != null && $Object.hasOwnProperty.call(m, "faviconMmsMetadata"))
                    $root.proto.Message.MMSThumbnailMetadata.encode(m.faviconMmsMetadata, w.uint32(266).fork(), q + 1).ldelim();
                if (m.linkPreviewMetadata != null && $Object.hasOwnProperty.call(m, "linkPreviewMetadata"))
                    $root.proto.Message.LinkPreviewMetadata.encode(m.linkPreviewMetadata, w.uint32(274).fork(), q + 1).ldelim();
                if (m.paymentLinkMetadata != null && $Object.hasOwnProperty.call(m, "paymentLinkMetadata"))
                    $root.proto.Message.PaymentLinkMetadata.encode(m.paymentLinkMetadata, w.uint32(282).fork(), q + 1).ldelim();
                if (m.endCardTiles != null && m.endCardTiles.length) {
                    for (var i = 0; i < m.endCardTiles.length; ++i)
                        $root.proto.Message.VideoEndCard.encode(m.endCardTiles[i], w.uint32(290).fork(), q + 1).ldelim();
                }
                if (m.videoContentUrl != null && $Object.hasOwnProperty.call(m, "videoContentUrl"))
                    w.uint32(298).string(m.videoContentUrl);
                if (m.musicMetadata != null && $Object.hasOwnProperty.call(m, "musicMetadata"))
                    $root.proto.EmbeddedMusic.encode(m.musicMetadata, w.uint32(306).fork(), q + 1).ldelim();
                if (m.paymentExtendedMetadata != null && $Object.hasOwnProperty.call(m, "paymentExtendedMetadata"))
                    $root.proto.Message.PaymentExtendedMetadata.encode(m.paymentExtendedMetadata, w.uint32(314).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ExtendedTextMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ExtendedTextMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.matchedText = r.stringVerify();
                            m._matchedText = "matchedText";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 7: {
                            if (u !== 5)
                                break;
                            m.textArgb = r.fixed32();
                            m._textArgb = "textArgb";
                            continue;
                        }
                    case 8: {
                            if (u !== 5)
                                break;
                            m.backgroundArgb = r.fixed32();
                            m._backgroundArgb = "backgroundArgb";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.font = r.int32();
                            m._font = "font";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.previewType = r.int32();
                            m._previewType = "previewType";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.doNotPlayInline = r.bool();
                            m._doNotPlayInline = "doNotPlayInline";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            m.thumbnailDirectPath = r.stringVerify();
                            m._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            m.thumbnailSha256 = r.bytes();
                            m._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            m.thumbnailEncSha256 = r.bytes();
                            m._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 23: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            m.thumbnailHeight = r.uint32();
                            m._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            m.thumbnailWidth = r.uint32();
                            m._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    case 26: {
                            if (u !== 0)
                                break;
                            m.inviteLinkGroupType = r.int32();
                            m._inviteLinkGroupType = "inviteLinkGroupType";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            m.inviteLinkParentGroupSubjectV2 = r.stringVerify();
                            m._inviteLinkParentGroupSubjectV2 = "inviteLinkParentGroupSubjectV2";
                            continue;
                        }
                    case 28: {
                            if (u !== 2)
                                break;
                            m.inviteLinkParentGroupThumbnailV2 = r.bytes();
                            m._inviteLinkParentGroupThumbnailV2 = "inviteLinkParentGroupThumbnailV2";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            m.inviteLinkGroupTypeV2 = r.int32();
                            m._inviteLinkGroupTypeV2 = "inviteLinkGroupTypeV2";
                            continue;
                        }
                    case 30: {
                            if (u !== 0)
                                break;
                            m.viewOnce = r.bool();
                            m._viewOnce = "viewOnce";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            m.videoHeight = r.uint32();
                            m._videoHeight = "videoHeight";
                            continue;
                        }
                    case 32: {
                            if (u !== 0)
                                break;
                            m.videoWidth = r.uint32();
                            m._videoWidth = "videoWidth";
                            continue;
                        }
                    case 33: {
                            if (u !== 2)
                                break;
                            m.faviconMmsMetadata = $root.proto.Message.MMSThumbnailMetadata.decode(r, r.uint32(), $undefined, q + 1, m.faviconMmsMetadata);
                            m._faviconMmsMetadata = "faviconMmsMetadata";
                            continue;
                        }
                    case 34: {
                            if (u !== 2)
                                break;
                            m.linkPreviewMetadata = $root.proto.Message.LinkPreviewMetadata.decode(r, r.uint32(), $undefined, q + 1, m.linkPreviewMetadata);
                            m._linkPreviewMetadata = "linkPreviewMetadata";
                            continue;
                        }
                    case 35: {
                            if (u !== 2)
                                break;
                            m.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.decode(r, r.uint32(), $undefined, q + 1, m.paymentLinkMetadata);
                            m._paymentLinkMetadata = "paymentLinkMetadata";
                            continue;
                        }
                    case 36: {
                            if (u !== 2)
                                break;
                            if (!(m.endCardTiles && m.endCardTiles.length))
                                m.endCardTiles = [];
                            m.endCardTiles.push($root.proto.Message.VideoEndCard.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 37: {
                            if (u !== 2)
                                break;
                            m.videoContentUrl = r.stringVerify();
                            m._videoContentUrl = "videoContentUrl";
                            continue;
                        }
                    case 38: {
                            if (u !== 2)
                                break;
                            m.musicMetadata = $root.proto.EmbeddedMusic.decode(r, r.uint32(), $undefined, q + 1, m.musicMetadata);
                            m._musicMetadata = "musicMetadata";
                            continue;
                        }
                    case 39: {
                            if (u !== 2)
                                break;
                            m.paymentExtendedMetadata = $root.proto.Message.PaymentExtendedMetadata.decode(r, r.uint32(), $undefined, q + 1, m.paymentExtendedMetadata);
                            m._paymentExtendedMetadata = "paymentExtendedMetadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ExtendedTextMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ExtendedTextMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ExtendedTextMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ExtendedTextMessage();
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.matchedText != null) {
                    m.matchedText = $String(d.matchedText);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.textArgb != null) {
                    m.textArgb = d.textArgb >>> 0;
                }
                if (d.backgroundArgb != null) {
                    m.backgroundArgb = d.backgroundArgb >>> 0;
                }
                switch (d.font) {
                case "SYSTEM":
                case 0:
                    m.font = 0;
                    break;
                case "SYSTEM_TEXT":
                case 1:
                    m.font = 1;
                    break;
                case "FB_SCRIPT":
                case 2:
                    m.font = 2;
                    break;
                case "SYSTEM_BOLD":
                case 6:
                    m.font = 6;
                    break;
                case "MORNINGBREEZE_REGULAR":
                case 7:
                    m.font = 7;
                    break;
                case "CALISTOGA_REGULAR":
                case 8:
                    m.font = 8;
                    break;
                case "EXO2_EXTRABOLD":
                case 9:
                    m.font = 9;
                    break;
                case "COURIERPRIME_BOLD":
                case 10:
                    m.font = 10;
                    break;
                default:
                    if (typeof d.font === "number" && (d.font | 0) === d.font)
                        m.font = d.font;
                }
                switch (d.previewType) {
                case "NONE":
                case 0:
                    m.previewType = 0;
                    break;
                case "VIDEO":
                case 1:
                    m.previewType = 1;
                    break;
                case "PLACEHOLDER":
                case 4:
                    m.previewType = 4;
                    break;
                case "IMAGE":
                case 5:
                    m.previewType = 5;
                    break;
                case "PAYMENT_LINKS":
                case 6:
                    m.previewType = 6;
                    break;
                case "PROFILE":
                case 7:
                    m.previewType = 7;
                    break;
                default:
                    if (typeof d.previewType === "number" && (d.previewType | 0) === d.previewType)
                        m.previewType = d.previewType;
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.doNotPlayInline != null) {
                    m.doNotPlayInline = $Boolean(d.doNotPlayInline);
                }
                if (d.thumbnailDirectPath != null) {
                    m.thumbnailDirectPath = $String(d.thumbnailDirectPath);
                }
                if (d.thumbnailSha256 != null) {
                    if (typeof d.thumbnailSha256 === "string")
                        $util.base64.decode(d.thumbnailSha256, m.thumbnailSha256 = $util.newBuffer($util.base64.length(d.thumbnailSha256)), 0);
                    else if (d.thumbnailSha256.length >= 0)
                        m.thumbnailSha256 = d.thumbnailSha256;
                }
                if (d.thumbnailEncSha256 != null) {
                    if (typeof d.thumbnailEncSha256 === "string")
                        $util.base64.decode(d.thumbnailEncSha256, m.thumbnailEncSha256 = $util.newBuffer($util.base64.length(d.thumbnailEncSha256)), 0);
                    else if (d.thumbnailEncSha256.length >= 0)
                        m.thumbnailEncSha256 = d.thumbnailEncSha256;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.thumbnailHeight != null) {
                    m.thumbnailHeight = d.thumbnailHeight >>> 0;
                }
                if (d.thumbnailWidth != null) {
                    m.thumbnailWidth = d.thumbnailWidth >>> 0;
                }
                switch (d.inviteLinkGroupType) {
                case "DEFAULT":
                case 0:
                    m.inviteLinkGroupType = 0;
                    break;
                case "PARENT":
                case 1:
                    m.inviteLinkGroupType = 1;
                    break;
                case "SUB":
                case 2:
                    m.inviteLinkGroupType = 2;
                    break;
                case "DEFAULT_SUB":
                case 3:
                    m.inviteLinkGroupType = 3;
                    break;
                default:
                    if (typeof d.inviteLinkGroupType === "number" && (d.inviteLinkGroupType | 0) === d.inviteLinkGroupType)
                        m.inviteLinkGroupType = d.inviteLinkGroupType;
                }
                if (d.inviteLinkParentGroupSubjectV2 != null) {
                    m.inviteLinkParentGroupSubjectV2 = $String(d.inviteLinkParentGroupSubjectV2);
                }
                if (d.inviteLinkParentGroupThumbnailV2 != null) {
                    if (typeof d.inviteLinkParentGroupThumbnailV2 === "string")
                        $util.base64.decode(d.inviteLinkParentGroupThumbnailV2, m.inviteLinkParentGroupThumbnailV2 = $util.newBuffer($util.base64.length(d.inviteLinkParentGroupThumbnailV2)), 0);
                    else if (d.inviteLinkParentGroupThumbnailV2.length >= 0)
                        m.inviteLinkParentGroupThumbnailV2 = d.inviteLinkParentGroupThumbnailV2;
                }
                switch (d.inviteLinkGroupTypeV2) {
                case "DEFAULT":
                case 0:
                    m.inviteLinkGroupTypeV2 = 0;
                    break;
                case "PARENT":
                case 1:
                    m.inviteLinkGroupTypeV2 = 1;
                    break;
                case "SUB":
                case 2:
                    m.inviteLinkGroupTypeV2 = 2;
                    break;
                case "DEFAULT_SUB":
                case 3:
                    m.inviteLinkGroupTypeV2 = 3;
                    break;
                default:
                    if (typeof d.inviteLinkGroupTypeV2 === "number" && (d.inviteLinkGroupTypeV2 | 0) === d.inviteLinkGroupTypeV2)
                        m.inviteLinkGroupTypeV2 = d.inviteLinkGroupTypeV2;
                }
                if (d.viewOnce != null) {
                    m.viewOnce = $Boolean(d.viewOnce);
                }
                if (d.videoHeight != null) {
                    m.videoHeight = d.videoHeight >>> 0;
                }
                if (d.videoWidth != null) {
                    m.videoWidth = d.videoWidth >>> 0;
                }
                if (d.faviconMmsMetadata != null) {
                    if (!$util.isObject(d.faviconMmsMetadata))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.faviconMmsMetadata: object expected");
                    m.faviconMmsMetadata = $root.proto.Message.MMSThumbnailMetadata.fromObject(d.faviconMmsMetadata, q + 1);
                }
                if (d.linkPreviewMetadata != null) {
                    if (!$util.isObject(d.linkPreviewMetadata))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.linkPreviewMetadata: object expected");
                    m.linkPreviewMetadata = $root.proto.Message.LinkPreviewMetadata.fromObject(d.linkPreviewMetadata, q + 1);
                }
                if (d.paymentLinkMetadata != null) {
                    if (!$util.isObject(d.paymentLinkMetadata))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.paymentLinkMetadata: object expected");
                    m.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.fromObject(d.paymentLinkMetadata, q + 1);
                }
                if (d.endCardTiles) {
                    if (!$Array.isArray(d.endCardTiles))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.endCardTiles: array expected");
                    m.endCardTiles = $Array(d.endCardTiles.length);
                    for (var i = 0; i < d.endCardTiles.length; ++i) {
                        if (!$util.isObject(d.endCardTiles[i]))
                            throw $TypeError(".proto.Message.ExtendedTextMessage.endCardTiles: object expected");
                        m.endCardTiles[i] = $root.proto.Message.VideoEndCard.fromObject(d.endCardTiles[i], q + 1);
                    }
                }
                if (d.videoContentUrl != null) {
                    m.videoContentUrl = $String(d.videoContentUrl);
                }
                if (d.musicMetadata != null) {
                    if (!$util.isObject(d.musicMetadata))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.musicMetadata: object expected");
                    m.musicMetadata = $root.proto.EmbeddedMusic.fromObject(d.musicMetadata, q + 1);
                }
                if (d.paymentExtendedMetadata != null) {
                    if (!$util.isObject(d.paymentExtendedMetadata))
                        throw $TypeError(".proto.Message.ExtendedTextMessage.paymentExtendedMetadata: object expected");
                    m.paymentExtendedMetadata = $root.proto.Message.PaymentExtendedMetadata.fromObject(d.paymentExtendedMetadata, q + 1);
                }
                return m;
            };

            ExtendedTextMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.endCardTiles = [];
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                if (m.matchedText != null && $Object.hasOwnProperty.call(m, "matchedText")) {
                    d.matchedText = m.matchedText;
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.textArgb != null && $Object.hasOwnProperty.call(m, "textArgb")) {
                    d.textArgb = m.textArgb;
                }
                if (m.backgroundArgb != null && $Object.hasOwnProperty.call(m, "backgroundArgb")) {
                    d.backgroundArgb = m.backgroundArgb;
                }
                if (m.font != null && $Object.hasOwnProperty.call(m, "font")) {
                    d.font = o.enums === $String ? $root.proto.Message.ExtendedTextMessage.FontType[m.font] === $undefined ? m.font : $root.proto.Message.ExtendedTextMessage.FontType[m.font] : m.font;
                }
                if (m.previewType != null && $Object.hasOwnProperty.call(m, "previewType")) {
                    d.previewType = o.enums === $String ? $root.proto.Message.ExtendedTextMessage.PreviewType[m.previewType] === $undefined ? m.previewType : $root.proto.Message.ExtendedTextMessage.PreviewType[m.previewType] : m.previewType;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.doNotPlayInline != null && $Object.hasOwnProperty.call(m, "doNotPlayInline")) {
                    d.doNotPlayInline = m.doNotPlayInline;
                }
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath")) {
                    d.thumbnailDirectPath = m.thumbnailDirectPath;
                }
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256")) {
                    d.thumbnailSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailSha256, 0, m.thumbnailSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailSha256) : m.thumbnailSha256;
                }
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256")) {
                    d.thumbnailEncSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailEncSha256, 0, m.thumbnailEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailEncSha256) : m.thumbnailEncSha256;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight")) {
                    d.thumbnailHeight = m.thumbnailHeight;
                }
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth")) {
                    d.thumbnailWidth = m.thumbnailWidth;
                }
                if (m.inviteLinkGroupType != null && $Object.hasOwnProperty.call(m, "inviteLinkGroupType")) {
                    d.inviteLinkGroupType = o.enums === $String ? $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupType] === $undefined ? m.inviteLinkGroupType : $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupType] : m.inviteLinkGroupType;
                }
                if (m.inviteLinkParentGroupSubjectV2 != null && $Object.hasOwnProperty.call(m, "inviteLinkParentGroupSubjectV2")) {
                    d.inviteLinkParentGroupSubjectV2 = m.inviteLinkParentGroupSubjectV2;
                }
                if (m.inviteLinkParentGroupThumbnailV2 != null && $Object.hasOwnProperty.call(m, "inviteLinkParentGroupThumbnailV2")) {
                    d.inviteLinkParentGroupThumbnailV2 = o.bytes === $String ? $util.base64.encode(m.inviteLinkParentGroupThumbnailV2, 0, m.inviteLinkParentGroupThumbnailV2.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.inviteLinkParentGroupThumbnailV2) : m.inviteLinkParentGroupThumbnailV2;
                }
                if (m.inviteLinkGroupTypeV2 != null && $Object.hasOwnProperty.call(m, "inviteLinkGroupTypeV2")) {
                    d.inviteLinkGroupTypeV2 = o.enums === $String ? $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupTypeV2] === $undefined ? m.inviteLinkGroupTypeV2 : $root.proto.Message.ExtendedTextMessage.InviteLinkGroupType[m.inviteLinkGroupTypeV2] : m.inviteLinkGroupTypeV2;
                }
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce")) {
                    d.viewOnce = m.viewOnce;
                }
                if (m.videoHeight != null && $Object.hasOwnProperty.call(m, "videoHeight")) {
                    d.videoHeight = m.videoHeight;
                }
                if (m.videoWidth != null && $Object.hasOwnProperty.call(m, "videoWidth")) {
                    d.videoWidth = m.videoWidth;
                }
                if (m.faviconMmsMetadata != null && $Object.hasOwnProperty.call(m, "faviconMmsMetadata")) {
                    d.faviconMmsMetadata = $root.proto.Message.MMSThumbnailMetadata.toObject(m.faviconMmsMetadata, o, q + 1);
                }
                if (m.linkPreviewMetadata != null && $Object.hasOwnProperty.call(m, "linkPreviewMetadata")) {
                    d.linkPreviewMetadata = $root.proto.Message.LinkPreviewMetadata.toObject(m.linkPreviewMetadata, o, q + 1);
                }
                if (m.paymentLinkMetadata != null && $Object.hasOwnProperty.call(m, "paymentLinkMetadata")) {
                    d.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.toObject(m.paymentLinkMetadata, o, q + 1);
                }
                if (m.endCardTiles && m.endCardTiles.length) {
                    d.endCardTiles = $Array(m.endCardTiles.length);
                    for (var j = 0; j < m.endCardTiles.length; ++j) {
                        d.endCardTiles[j] = $root.proto.Message.VideoEndCard.toObject(m.endCardTiles[j], o, q + 1);
                    }
                }
                if (m.videoContentUrl != null && $Object.hasOwnProperty.call(m, "videoContentUrl")) {
                    d.videoContentUrl = m.videoContentUrl;
                }
                if (m.musicMetadata != null && $Object.hasOwnProperty.call(m, "musicMetadata")) {
                    d.musicMetadata = $root.proto.EmbeddedMusic.toObject(m.musicMetadata, o, q + 1);
                }
                if (m.paymentExtendedMetadata != null && $Object.hasOwnProperty.call(m, "paymentExtendedMetadata")) {
                    d.paymentExtendedMetadata = $root.proto.Message.PaymentExtendedMetadata.toObject(m.paymentExtendedMetadata, o, q + 1);
                }
                return d;
            };

            ExtendedTextMessage.prototype.toJSON = function() {
                return ExtendedTextMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExtendedTextMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ExtendedTextMessage";
            };

            ExtendedTextMessage.FontType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "SYSTEM"] = 0;
                values[valuesById[1] = "SYSTEM_TEXT"] = 1;
                values[valuesById[2] = "FB_SCRIPT"] = 2;
                values[valuesById[6] = "SYSTEM_BOLD"] = 6;
                values[valuesById[7] = "MORNINGBREEZE_REGULAR"] = 7;
                values[valuesById[8] = "CALISTOGA_REGULAR"] = 8;
                values[valuesById[9] = "EXO2_EXTRABOLD"] = 9;
                values[valuesById[10] = "COURIERPRIME_BOLD"] = 10;
                return values;
            })();

            ExtendedTextMessage.InviteLinkGroupType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "PARENT"] = 1;
                values[valuesById[2] = "SUB"] = 2;
                values[valuesById[3] = "DEFAULT_SUB"] = 3;
                return values;
            })();

            ExtendedTextMessage.PreviewType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "VIDEO"] = 1;
                values[valuesById[4] = "PLACEHOLDER"] = 4;
                values[valuesById[5] = "IMAGE"] = 5;
                values[valuesById[6] = "PAYMENT_LINKS"] = 6;
                values[valuesById[7] = "PROFILE"] = 7;
                return values;
            })();

            return ExtendedTextMessage;
        })();

        Message.FullHistorySyncOnDemandConfig = (function() {

            const FullHistorySyncOnDemandConfig = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FullHistorySyncOnDemandConfig.prototype.historyFromTimestamp = null;
            FullHistorySyncOnDemandConfig.prototype.historyDurationDays = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FullHistorySyncOnDemandConfig.prototype, "_historyFromTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["historyFromTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FullHistorySyncOnDemandConfig.prototype, "_historyDurationDays", {
                get: $util.oneOfGetter($oneOfFields = ["historyDurationDays"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FullHistorySyncOnDemandConfig.create = function(properties) {
                return new FullHistorySyncOnDemandConfig(properties);
            };

            FullHistorySyncOnDemandConfig.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.historyFromTimestamp != null && $Object.hasOwnProperty.call(m, "historyFromTimestamp"))
                    w.uint32(8).uint64(m.historyFromTimestamp);
                if (m.historyDurationDays != null && $Object.hasOwnProperty.call(m, "historyDurationDays"))
                    w.uint32(16).uint32(m.historyDurationDays);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FullHistorySyncOnDemandConfig.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.FullHistorySyncOnDemandConfig();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.historyFromTimestamp = r.uint64();
                            m._historyFromTimestamp = "historyFromTimestamp";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.historyDurationDays = r.uint32();
                            m._historyDurationDays = "historyDurationDays";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            FullHistorySyncOnDemandConfig.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.FullHistorySyncOnDemandConfig)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.FullHistorySyncOnDemandConfig: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.FullHistorySyncOnDemandConfig();
                if (d.historyFromTimestamp != null) {
                    if ($util.Long)
                        m.historyFromTimestamp = $util.Long.fromValue(d.historyFromTimestamp, true);
                    else if (typeof d.historyFromTimestamp === "string")
                        m.historyFromTimestamp = $parseInt(d.historyFromTimestamp, 10);
                    else if (typeof d.historyFromTimestamp === "number")
                        m.historyFromTimestamp = d.historyFromTimestamp;
                    else if (typeof d.historyFromTimestamp === "object")
                        m.historyFromTimestamp = new $util.LongBits(d.historyFromTimestamp.low >>> 0, d.historyFromTimestamp.high >>> 0).toNumber(true);
                }
                if (d.historyDurationDays != null) {
                    m.historyDurationDays = d.historyDurationDays >>> 0;
                }
                return m;
            };

            FullHistorySyncOnDemandConfig.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.historyFromTimestamp != null && $Object.hasOwnProperty.call(m, "historyFromTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.historyFromTimestamp = typeof m.historyFromTimestamp === "number" ? $BigInt(m.historyFromTimestamp) : $util.Long.fromBits(m.historyFromTimestamp.low >>> 0, m.historyFromTimestamp.high >>> 0, true).toBigInt();
                    else if (typeof m.historyFromTimestamp === "number")
                        d.historyFromTimestamp = o.longs === $String ? $String(m.historyFromTimestamp) : m.historyFromTimestamp;
                    else
                        d.historyFromTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.historyFromTimestamp) : o.longs === $Number ? new $util.LongBits(m.historyFromTimestamp.low >>> 0, m.historyFromTimestamp.high >>> 0).toNumber(true) : m.historyFromTimestamp;
                }
                if (m.historyDurationDays != null && $Object.hasOwnProperty.call(m, "historyDurationDays")) {
                    d.historyDurationDays = m.historyDurationDays;
                }
                return d;
            };

            FullHistorySyncOnDemandConfig.prototype.toJSON = function() {
                return FullHistorySyncOnDemandConfig.toObject(this, $protobuf.util.toJSONOptions);
            };

            FullHistorySyncOnDemandConfig.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.FullHistorySyncOnDemandConfig";
            };

            return FullHistorySyncOnDemandConfig;
        })();

        Message.FullHistorySyncOnDemandRequestMetadata = (function() {

            const FullHistorySyncOnDemandRequestMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FullHistorySyncOnDemandRequestMetadata.prototype.requestId = null;
            FullHistorySyncOnDemandRequestMetadata.prototype.businessProduct = null;
            FullHistorySyncOnDemandRequestMetadata.prototype.opaqueClientData = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_requestId", {
                get: $util.oneOfGetter($oneOfFields = ["requestId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_businessProduct", {
                get: $util.oneOfGetter($oneOfFields = ["businessProduct"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FullHistorySyncOnDemandRequestMetadata.prototype, "_opaqueClientData", {
                get: $util.oneOfGetter($oneOfFields = ["opaqueClientData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FullHistorySyncOnDemandRequestMetadata.create = function(properties) {
                return new FullHistorySyncOnDemandRequestMetadata(properties);
            };

            FullHistorySyncOnDemandRequestMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId"))
                    w.uint32(10).string(m.requestId);
                if (m.businessProduct != null && $Object.hasOwnProperty.call(m, "businessProduct"))
                    w.uint32(18).string(m.businessProduct);
                if (m.opaqueClientData != null && $Object.hasOwnProperty.call(m, "opaqueClientData"))
                    w.uint32(26).bytes(m.opaqueClientData);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FullHistorySyncOnDemandRequestMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.FullHistorySyncOnDemandRequestMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.requestId = r.stringVerify();
                            m._requestId = "requestId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.businessProduct = r.stringVerify();
                            m._businessProduct = "businessProduct";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.opaqueClientData = r.bytes();
                            m._opaqueClientData = "opaqueClientData";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            FullHistorySyncOnDemandRequestMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.FullHistorySyncOnDemandRequestMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.FullHistorySyncOnDemandRequestMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.FullHistorySyncOnDemandRequestMetadata();
                if (d.requestId != null) {
                    m.requestId = $String(d.requestId);
                }
                if (d.businessProduct != null) {
                    m.businessProduct = $String(d.businessProduct);
                }
                if (d.opaqueClientData != null) {
                    if (typeof d.opaqueClientData === "string")
                        $util.base64.decode(d.opaqueClientData, m.opaqueClientData = $util.newBuffer($util.base64.length(d.opaqueClientData)), 0);
                    else if (d.opaqueClientData.length >= 0)
                        m.opaqueClientData = d.opaqueClientData;
                }
                return m;
            };

            FullHistorySyncOnDemandRequestMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId")) {
                    d.requestId = m.requestId;
                }
                if (m.businessProduct != null && $Object.hasOwnProperty.call(m, "businessProduct")) {
                    d.businessProduct = m.businessProduct;
                }
                if (m.opaqueClientData != null && $Object.hasOwnProperty.call(m, "opaqueClientData")) {
                    d.opaqueClientData = o.bytes === $String ? $util.base64.encode(m.opaqueClientData, 0, m.opaqueClientData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.opaqueClientData) : m.opaqueClientData;
                }
                return d;
            };

            FullHistorySyncOnDemandRequestMetadata.prototype.toJSON = function() {
                return FullHistorySyncOnDemandRequestMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            FullHistorySyncOnDemandRequestMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.FullHistorySyncOnDemandRequestMetadata";
            };

            return FullHistorySyncOnDemandRequestMetadata;
        })();

        Message.FutureProofMessage = (function() {

            const FutureProofMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FutureProofMessage.prototype.message = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FutureProofMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FutureProofMessage.create = function(properties) {
                return new FutureProofMessage(properties);
            };

            FutureProofMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    $root.proto.Message.encode(m.message, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FutureProofMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.FutureProofMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                            m._message = "message";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            FutureProofMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.FutureProofMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.FutureProofMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.FutureProofMessage();
                if (d.message != null) {
                    if (!$util.isObject(d.message))
                        throw $TypeError(".proto.Message.FutureProofMessage.message: object expected");
                    m.message = $root.proto.Message.fromObject(d.message, q + 1);
                }
                return m;
            };

            FutureProofMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = $root.proto.Message.toObject(m.message, o, q + 1);
                }
                return d;
            };

            FutureProofMessage.prototype.toJSON = function() {
                return FutureProofMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            FutureProofMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.FutureProofMessage";
            };

            return FutureProofMessage;
        })();

        Message.GroupInviteMessage = (function() {

            const GroupInviteMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            GroupInviteMessage.prototype.groupJid = null;
            GroupInviteMessage.prototype.inviteCode = null;
            GroupInviteMessage.prototype.inviteExpiration = null;
            GroupInviteMessage.prototype.groupName = null;
            GroupInviteMessage.prototype.jpegThumbnail = null;
            GroupInviteMessage.prototype.caption = null;
            GroupInviteMessage.prototype.contextInfo = null;
            GroupInviteMessage.prototype.groupType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_groupJid", {
                get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_inviteCode", {
                get: $util.oneOfGetter($oneOfFields = ["inviteCode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_inviteExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_groupName", {
                get: $util.oneOfGetter($oneOfFields = ["groupName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupInviteMessage.prototype, "_groupType", {
                get: $util.oneOfGetter($oneOfFields = ["groupType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            GroupInviteMessage.create = function(properties) {
                return new GroupInviteMessage(properties);
            };

            GroupInviteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid"))
                    w.uint32(10).string(m.groupJid);
                if (m.inviteCode != null && $Object.hasOwnProperty.call(m, "inviteCode"))
                    w.uint32(18).string(m.inviteCode);
                if (m.inviteExpiration != null && $Object.hasOwnProperty.call(m, "inviteExpiration"))
                    w.uint32(24).int64(m.inviteExpiration);
                if (m.groupName != null && $Object.hasOwnProperty.call(m, "groupName"))
                    w.uint32(34).string(m.groupName);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(42).bytes(m.jpegThumbnail);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(50).string(m.caption);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(58).fork(), q + 1).ldelim();
                if (m.groupType != null && $Object.hasOwnProperty.call(m, "groupType"))
                    w.uint32(64).int32(m.groupType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            GroupInviteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.GroupInviteMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.groupJid = r.stringVerify();
                            m._groupJid = "groupJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.inviteCode = r.stringVerify();
                            m._inviteCode = "inviteCode";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.inviteExpiration = r.int64();
                            m._inviteExpiration = "inviteExpiration";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.groupName = r.stringVerify();
                            m._groupName = "groupName";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.groupType = r.int32();
                            m._groupType = "groupType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            GroupInviteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.GroupInviteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.GroupInviteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.GroupInviteMessage();
                if (d.groupJid != null) {
                    m.groupJid = $String(d.groupJid);
                }
                if (d.inviteCode != null) {
                    m.inviteCode = $String(d.inviteCode);
                }
                if (d.inviteExpiration != null) {
                    if ($util.Long)
                        m.inviteExpiration = $util.Long.fromValue(d.inviteExpiration, false);
                    else if (typeof d.inviteExpiration === "string")
                        m.inviteExpiration = $parseInt(d.inviteExpiration, 10);
                    else if (typeof d.inviteExpiration === "number")
                        m.inviteExpiration = d.inviteExpiration;
                    else if (typeof d.inviteExpiration === "object")
                        m.inviteExpiration = new $util.LongBits(d.inviteExpiration.low >>> 0, d.inviteExpiration.high >>> 0).toNumber();
                }
                if (d.groupName != null) {
                    m.groupName = $String(d.groupName);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.GroupInviteMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                switch (d.groupType) {
                case "DEFAULT":
                case 0:
                    m.groupType = 0;
                    break;
                case "PARENT":
                case 1:
                    m.groupType = 1;
                    break;
                default:
                    if (typeof d.groupType === "number" && (d.groupType | 0) === d.groupType)
                        m.groupType = d.groupType;
                }
                return m;
            };

            GroupInviteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid")) {
                    d.groupJid = m.groupJid;
                }
                if (m.inviteCode != null && $Object.hasOwnProperty.call(m, "inviteCode")) {
                    d.inviteCode = m.inviteCode;
                }
                if (m.inviteExpiration != null && $Object.hasOwnProperty.call(m, "inviteExpiration")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.inviteExpiration = typeof m.inviteExpiration === "number" ? $BigInt(m.inviteExpiration) : $util.Long.fromBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0, false).toBigInt();
                    else if (typeof m.inviteExpiration === "number")
                        d.inviteExpiration = o.longs === $String ? $String(m.inviteExpiration) : m.inviteExpiration;
                    else
                        d.inviteExpiration = o.longs === $String ? $util.Long.prototype.toString.call(m.inviteExpiration) : o.longs === $Number ? new $util.LongBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0).toNumber() : m.inviteExpiration;
                }
                if (m.groupName != null && $Object.hasOwnProperty.call(m, "groupName")) {
                    d.groupName = m.groupName;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.groupType != null && $Object.hasOwnProperty.call(m, "groupType")) {
                    d.groupType = o.enums === $String ? $root.proto.Message.GroupInviteMessage.GroupType[m.groupType] === $undefined ? m.groupType : $root.proto.Message.GroupInviteMessage.GroupType[m.groupType] : m.groupType;
                }
                return d;
            };

            GroupInviteMessage.prototype.toJSON = function() {
                return GroupInviteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            GroupInviteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.GroupInviteMessage";
            };

            GroupInviteMessage.GroupType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "PARENT"] = 1;
                return values;
            })();

            return GroupInviteMessage;
        })();

        Message.HighlyStructuredMessage = (function() {

            const HighlyStructuredMessage = function (p) {
                this.params = [];
                this.localizableParams = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HighlyStructuredMessage.prototype.namespace = null;
            HighlyStructuredMessage.prototype.elementName = null;
            HighlyStructuredMessage.prototype.params = $util.emptyArray;
            HighlyStructuredMessage.prototype.fallbackLg = null;
            HighlyStructuredMessage.prototype.fallbackLc = null;
            HighlyStructuredMessage.prototype.localizableParams = $util.emptyArray;
            HighlyStructuredMessage.prototype.deterministicLg = null;
            HighlyStructuredMessage.prototype.deterministicLc = null;
            HighlyStructuredMessage.prototype.hydratedHsm = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_namespace", {
                get: $util.oneOfGetter($oneOfFields = ["namespace"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_elementName", {
                get: $util.oneOfGetter($oneOfFields = ["elementName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_fallbackLg", {
                get: $util.oneOfGetter($oneOfFields = ["fallbackLg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_fallbackLc", {
                get: $util.oneOfGetter($oneOfFields = ["fallbackLc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_deterministicLg", {
                get: $util.oneOfGetter($oneOfFields = ["deterministicLg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_deterministicLc", {
                get: $util.oneOfGetter($oneOfFields = ["deterministicLc"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HighlyStructuredMessage.prototype, "_hydratedHsm", {
                get: $util.oneOfGetter($oneOfFields = ["hydratedHsm"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HighlyStructuredMessage.create = function(properties) {
                return new HighlyStructuredMessage(properties);
            };

            HighlyStructuredMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.namespace != null && $Object.hasOwnProperty.call(m, "namespace"))
                    w.uint32(10).string(m.namespace);
                if (m.elementName != null && $Object.hasOwnProperty.call(m, "elementName"))
                    w.uint32(18).string(m.elementName);
                if (m.params != null && m.params.length) {
                    for (var i = 0; i < m.params.length; ++i)
                        w.uint32(26).string(m.params[i]);
                }
                if (m.fallbackLg != null && $Object.hasOwnProperty.call(m, "fallbackLg"))
                    w.uint32(34).string(m.fallbackLg);
                if (m.fallbackLc != null && $Object.hasOwnProperty.call(m, "fallbackLc"))
                    w.uint32(42).string(m.fallbackLc);
                if (m.localizableParams != null && m.localizableParams.length) {
                    for (var i = 0; i < m.localizableParams.length; ++i)
                        $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.encode(m.localizableParams[i], w.uint32(50).fork(), q + 1).ldelim();
                }
                if (m.deterministicLg != null && $Object.hasOwnProperty.call(m, "deterministicLg"))
                    w.uint32(58).string(m.deterministicLg);
                if (m.deterministicLc != null && $Object.hasOwnProperty.call(m, "deterministicLc"))
                    w.uint32(66).string(m.deterministicLc);
                if (m.hydratedHsm != null && $Object.hasOwnProperty.call(m, "hydratedHsm"))
                    $root.proto.Message.TemplateMessage.encode(m.hydratedHsm, w.uint32(74).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HighlyStructuredMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HighlyStructuredMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.namespace = r.stringVerify();
                            m._namespace = "namespace";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.elementName = r.stringVerify();
                            m._elementName = "elementName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.params && m.params.length))
                                m.params = [];
                            m.params.push(r.stringVerify());
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fallbackLg = r.stringVerify();
                            m._fallbackLg = "fallbackLg";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.fallbackLc = r.stringVerify();
                            m._fallbackLc = "fallbackLc";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            if (!(m.localizableParams && m.localizableParams.length))
                                m.localizableParams = [];
                            m.localizableParams.push($root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.deterministicLg = r.stringVerify();
                            m._deterministicLg = "deterministicLg";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.deterministicLc = r.stringVerify();
                            m._deterministicLc = "deterministicLc";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.hydratedHsm = $root.proto.Message.TemplateMessage.decode(r, r.uint32(), $undefined, q + 1, m.hydratedHsm);
                            m._hydratedHsm = "hydratedHsm";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HighlyStructuredMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.HighlyStructuredMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.HighlyStructuredMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.HighlyStructuredMessage();
                if (d.namespace != null) {
                    m.namespace = $String(d.namespace);
                }
                if (d.elementName != null) {
                    m.elementName = $String(d.elementName);
                }
                if (d.params) {
                    if (!$Array.isArray(d.params))
                        throw $TypeError(".proto.Message.HighlyStructuredMessage.params: array expected");
                    m.params = $Array(d.params.length);
                    for (var i = 0; i < d.params.length; ++i) {
                        m.params[i] = $String(d.params[i]);
                    }
                }
                if (d.fallbackLg != null) {
                    m.fallbackLg = $String(d.fallbackLg);
                }
                if (d.fallbackLc != null) {
                    m.fallbackLc = $String(d.fallbackLc);
                }
                if (d.localizableParams) {
                    if (!$Array.isArray(d.localizableParams))
                        throw $TypeError(".proto.Message.HighlyStructuredMessage.localizableParams: array expected");
                    m.localizableParams = $Array(d.localizableParams.length);
                    for (var i = 0; i < d.localizableParams.length; ++i) {
                        if (!$util.isObject(d.localizableParams[i]))
                            throw $TypeError(".proto.Message.HighlyStructuredMessage.localizableParams: object expected");
                        m.localizableParams[i] = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.fromObject(d.localizableParams[i], q + 1);
                    }
                }
                if (d.deterministicLg != null) {
                    m.deterministicLg = $String(d.deterministicLg);
                }
                if (d.deterministicLc != null) {
                    m.deterministicLc = $String(d.deterministicLc);
                }
                if (d.hydratedHsm != null) {
                    if (!$util.isObject(d.hydratedHsm))
                        throw $TypeError(".proto.Message.HighlyStructuredMessage.hydratedHsm: object expected");
                    m.hydratedHsm = $root.proto.Message.TemplateMessage.fromObject(d.hydratedHsm, q + 1);
                }
                return m;
            };

            HighlyStructuredMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.params = [];
                    d.localizableParams = [];
                }
                if (m.namespace != null && $Object.hasOwnProperty.call(m, "namespace")) {
                    d.namespace = m.namespace;
                }
                if (m.elementName != null && $Object.hasOwnProperty.call(m, "elementName")) {
                    d.elementName = m.elementName;
                }
                if (m.params && m.params.length) {
                    d.params = $Array(m.params.length);
                    for (var j = 0; j < m.params.length; ++j) {
                        d.params[j] = m.params[j];
                    }
                }
                if (m.fallbackLg != null && $Object.hasOwnProperty.call(m, "fallbackLg")) {
                    d.fallbackLg = m.fallbackLg;
                }
                if (m.fallbackLc != null && $Object.hasOwnProperty.call(m, "fallbackLc")) {
                    d.fallbackLc = m.fallbackLc;
                }
                if (m.localizableParams && m.localizableParams.length) {
                    d.localizableParams = $Array(m.localizableParams.length);
                    for (var j = 0; j < m.localizableParams.length; ++j) {
                        d.localizableParams[j] = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.toObject(m.localizableParams[j], o, q + 1);
                    }
                }
                if (m.deterministicLg != null && $Object.hasOwnProperty.call(m, "deterministicLg")) {
                    d.deterministicLg = m.deterministicLg;
                }
                if (m.deterministicLc != null && $Object.hasOwnProperty.call(m, "deterministicLc")) {
                    d.deterministicLc = m.deterministicLc;
                }
                if (m.hydratedHsm != null && $Object.hasOwnProperty.call(m, "hydratedHsm")) {
                    d.hydratedHsm = $root.proto.Message.TemplateMessage.toObject(m.hydratedHsm, o, q + 1);
                }
                return d;
            };

            HighlyStructuredMessage.prototype.toJSON = function() {
                return HighlyStructuredMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            HighlyStructuredMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.HighlyStructuredMessage";
            };

            HighlyStructuredMessage.HSMLocalizableParameter = (function() {

                const HSMLocalizableParameter = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                HSMLocalizableParameter.prototype["default"] = null;
                HSMLocalizableParameter.prototype.currency = null;
                HSMLocalizableParameter.prototype.dateTime = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HSMLocalizableParameter.prototype, "_default", {
                    get: $util.oneOfGetter($oneOfFields = ["default"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                $Object.defineProperty(HSMLocalizableParameter.prototype, "paramOneof", {
                    get: $util.oneOfGetter($oneOfFields = ["currency", "dateTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HSMLocalizableParameter.create = function(properties) {
                    return new HSMLocalizableParameter(properties);
                };

                HSMLocalizableParameter.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m["default"] != null && $Object.hasOwnProperty.call(m, "default"))
                        w.uint32(10).string(m["default"]);
                    if (m.currency != null && $Object.hasOwnProperty.call(m, "currency"))
                        $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.encode(m.currency, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.dateTime != null && $Object.hasOwnProperty.call(m, "dateTime"))
                        $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.encode(m.dateTime, w.uint32(26).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                HSMLocalizableParameter.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m["default"] = r.stringVerify();
                                m._default = "default";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.currency = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.decode(r, r.uint32(), $undefined, q + 1, m.currency);
                                m.paramOneof = "currency";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.dateTime = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.decode(r, r.uint32(), $undefined, q + 1, m.dateTime);
                                m.paramOneof = "dateTime";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                HSMLocalizableParameter.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter();
                    if (d["default"] != null) {
                        m["default"] = $String(d["default"]);
                    }
                    if (d.currency != null) {
                        if (!$util.isObject(d.currency))
                            throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.currency: object expected");
                        m.currency = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.fromObject(d.currency, q + 1);
                    }
                    if (d.dateTime != null) {
                        if (!$util.isObject(d.dateTime))
                            throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.dateTime: object expected");
                        m.dateTime = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.fromObject(d.dateTime, q + 1);
                    }
                    return m;
                };

                HSMLocalizableParameter.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m["default"] != null && $Object.hasOwnProperty.call(m, "default")) {
                        d["default"] = m["default"];
                    }
                    if (m.currency != null && $Object.hasOwnProperty.call(m, "currency")) {
                        d.currency = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency.toObject(m.currency, o, q + 1);
                        if (o.oneofs)
                            d.paramOneof = "currency";
                    }
                    if (m.dateTime != null && $Object.hasOwnProperty.call(m, "dateTime")) {
                        d.dateTime = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.toObject(m.dateTime, o, q + 1);
                        if (o.oneofs)
                            d.paramOneof = "dateTime";
                    }
                    return d;
                };

                HSMLocalizableParameter.prototype.toJSON = function() {
                    return HSMLocalizableParameter.toObject(this, $protobuf.util.toJSONOptions);
                };

                HSMLocalizableParameter.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter";
                };

                HSMLocalizableParameter.HSMCurrency = (function() {

                    const HSMCurrency = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    HSMCurrency.prototype.currencyCode = null;
                    HSMCurrency.prototype.amount1000 = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HSMCurrency.prototype, "_currencyCode", {
                        get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HSMCurrency.prototype, "_amount1000", {
                        get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    HSMCurrency.create = function(properties) {
                        return new HSMCurrency(properties);
                    };

                    HSMCurrency.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.currencyCode != null && $Object.hasOwnProperty.call(m, "currencyCode"))
                            w.uint32(10).string(m.currencyCode);
                        if (m.amount1000 != null && $Object.hasOwnProperty.call(m, "amount1000"))
                            w.uint32(16).int64(m.amount1000);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    HSMCurrency.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.currencyCode = r.stringVerify();
                                    m._currencyCode = "currencyCode";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.amount1000 = r.int64();
                                    m._amount1000 = "amount1000";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    HSMCurrency.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency();
                        if (d.currencyCode != null) {
                            m.currencyCode = $String(d.currencyCode);
                        }
                        if (d.amount1000 != null) {
                            if ($util.Long)
                                m.amount1000 = $util.Long.fromValue(d.amount1000, false);
                            else if (typeof d.amount1000 === "string")
                                m.amount1000 = $parseInt(d.amount1000, 10);
                            else if (typeof d.amount1000 === "number")
                                m.amount1000 = d.amount1000;
                            else if (typeof d.amount1000 === "object")
                                m.amount1000 = new $util.LongBits(d.amount1000.low >>> 0, d.amount1000.high >>> 0).toNumber();
                        }
                        return m;
                    };

                    HSMCurrency.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.currencyCode != null && $Object.hasOwnProperty.call(m, "currencyCode")) {
                            d.currencyCode = m.currencyCode;
                        }
                        if (m.amount1000 != null && $Object.hasOwnProperty.call(m, "amount1000")) {
                            if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                d.amount1000 = typeof m.amount1000 === "number" ? $BigInt(m.amount1000) : $util.Long.fromBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0, false).toBigInt();
                            else if (typeof m.amount1000 === "number")
                                d.amount1000 = o.longs === $String ? $String(m.amount1000) : m.amount1000;
                            else
                                d.amount1000 = o.longs === $String ? $util.Long.prototype.toString.call(m.amount1000) : o.longs === $Number ? new $util.LongBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0).toNumber() : m.amount1000;
                        }
                        return d;
                    };

                    HSMCurrency.prototype.toJSON = function() {
                        return HSMCurrency.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    HSMCurrency.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMCurrency";
                    };

                    return HSMCurrency;
                })();

                HSMLocalizableParameter.HSMDateTime = (function() {

                    const HSMDateTime = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    HSMDateTime.prototype.component = null;
                    HSMDateTime.prototype.unixEpoch = null;

                    let $oneOfFields;

                    $Object.defineProperty(HSMDateTime.prototype, "datetimeOneof", {
                        get: $util.oneOfGetter($oneOfFields = ["component", "unixEpoch"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    HSMDateTime.create = function(properties) {
                        return new HSMDateTime(properties);
                    };

                    HSMDateTime.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.component != null && $Object.hasOwnProperty.call(m, "component"))
                            $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.encode(m.component, w.uint32(10).fork(), q + 1).ldelim();
                        if (m.unixEpoch != null && $Object.hasOwnProperty.call(m, "unixEpoch"))
                            $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.encode(m.unixEpoch, w.uint32(18).fork(), q + 1).ldelim();
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    HSMDateTime.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.component = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.decode(r, r.uint32(), $undefined, q + 1, m.component);
                                    m.datetimeOneof = "component";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.unixEpoch = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.decode(r, r.uint32(), $undefined, q + 1, m.unixEpoch);
                                    m.datetimeOneof = "unixEpoch";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    HSMDateTime.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime();
                        if (d.component != null) {
                            if (!$util.isObject(d.component))
                                throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.component: object expected");
                            m.component = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.fromObject(d.component, q + 1);
                        }
                        if (d.unixEpoch != null) {
                            if (!$util.isObject(d.unixEpoch))
                                throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.unixEpoch: object expected");
                            m.unixEpoch = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.fromObject(d.unixEpoch, q + 1);
                        }
                        return m;
                    };

                    HSMDateTime.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.component != null && $Object.hasOwnProperty.call(m, "component")) {
                            d.component = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.toObject(m.component, o, q + 1);
                            if (o.oneofs)
                                d.datetimeOneof = "component";
                        }
                        if (m.unixEpoch != null && $Object.hasOwnProperty.call(m, "unixEpoch")) {
                            d.unixEpoch = $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch.toObject(m.unixEpoch, o, q + 1);
                            if (o.oneofs)
                                d.datetimeOneof = "unixEpoch";
                        }
                        return d;
                    };

                    HSMDateTime.prototype.toJSON = function() {
                        return HSMDateTime.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    HSMDateTime.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime";
                    };

                    HSMDateTime.HSMDateTimeComponent = (function() {

                        const HSMDateTimeComponent = function (p) {
                            if (p)
                                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                                        this[ks[i]] = p[ks[i]];
                        };

                        HSMDateTimeComponent.prototype.dayOfWeek = null;
                        HSMDateTimeComponent.prototype.year = null;
                        HSMDateTimeComponent.prototype.month = null;
                        HSMDateTimeComponent.prototype.dayOfMonth = null;
                        HSMDateTimeComponent.prototype.hour = null;
                        HSMDateTimeComponent.prototype.minute = null;
                        HSMDateTimeComponent.prototype.calendar = null;

                        let $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_dayOfWeek", {
                            get: $util.oneOfGetter($oneOfFields = ["dayOfWeek"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_year", {
                            get: $util.oneOfGetter($oneOfFields = ["year"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_month", {
                            get: $util.oneOfGetter($oneOfFields = ["month"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_dayOfMonth", {
                            get: $util.oneOfGetter($oneOfFields = ["dayOfMonth"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_hour", {
                            get: $util.oneOfGetter($oneOfFields = ["hour"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_minute", {
                            get: $util.oneOfGetter($oneOfFields = ["minute"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeComponent.prototype, "_calendar", {
                            get: $util.oneOfGetter($oneOfFields = ["calendar"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        HSMDateTimeComponent.create = function(properties) {
                            return new HSMDateTimeComponent(properties);
                        };

                        HSMDateTimeComponent.encode = function (m, w, q) {
                            if (!w)
                                w = $Writer.create();
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (m.dayOfWeek != null && $Object.hasOwnProperty.call(m, "dayOfWeek"))
                                w.uint32(8).int32(m.dayOfWeek);
                            if (m.year != null && $Object.hasOwnProperty.call(m, "year"))
                                w.uint32(16).uint32(m.year);
                            if (m.month != null && $Object.hasOwnProperty.call(m, "month"))
                                w.uint32(24).uint32(m.month);
                            if (m.dayOfMonth != null && $Object.hasOwnProperty.call(m, "dayOfMonth"))
                                w.uint32(32).uint32(m.dayOfMonth);
                            if (m.hour != null && $Object.hasOwnProperty.call(m, "hour"))
                                w.uint32(40).uint32(m.hour);
                            if (m.minute != null && $Object.hasOwnProperty.call(m, "minute"))
                                w.uint32(48).uint32(m.minute);
                            if (m.calendar != null && $Object.hasOwnProperty.call(m, "calendar"))
                                w.uint32(56).int32(m.calendar);
                            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                                for (var i = 0; i < m.$unknowns.length; ++i)
                                    w.raw(m.$unknowns[i]);
                            return w;
                        };

                        HSMDateTimeComponent.decode = function (r, l, z, q, g) {
                            if (!(r instanceof $Reader))
                                r = $Reader.create(r);
                            if (q === $undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent(), v;
                            while (r.pos < c) {
                                var s = r.pos;
                                var t = r.tag();
                                if (t === z) {
                                    z = $undefined;
                                    break;
                                }
                                var u = t & 7;
                                switch (t >>>= 3) {
                                case 1: {
                                        if (u !== 0)
                                            break;
                                        m.dayOfWeek = r.int32();
                                        m._dayOfWeek = "dayOfWeek";
                                        continue;
                                    }
                                case 2: {
                                        if (u !== 0)
                                            break;
                                        m.year = r.uint32();
                                        m._year = "year";
                                        continue;
                                    }
                                case 3: {
                                        if (u !== 0)
                                            break;
                                        m.month = r.uint32();
                                        m._month = "month";
                                        continue;
                                    }
                                case 4: {
                                        if (u !== 0)
                                            break;
                                        m.dayOfMonth = r.uint32();
                                        m._dayOfMonth = "dayOfMonth";
                                        continue;
                                    }
                                case 5: {
                                        if (u !== 0)
                                            break;
                                        m.hour = r.uint32();
                                        m._hour = "hour";
                                        continue;
                                    }
                                case 6: {
                                        if (u !== 0)
                                            break;
                                        m.minute = r.uint32();
                                        m._minute = "minute";
                                        continue;
                                    }
                                case 7: {
                                        if (u !== 0)
                                            break;
                                        m.calendar = r.int32();
                                        m._calendar = "calendar";
                                        continue;
                                    }
                                }
                                r.skipType(u, q, t);
                                if (!r.discardUnknown) {
                                    $util.makeProp(m, "$unknowns", false);
                                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                                }
                            }
                            if (z !== $undefined)
                                throw $Error("missing end group");
                            return m;
                        };

                        HSMDateTimeComponent.fromObject = function (d, q) {
                            if (d instanceof $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent)
                                return d;
                            if (!$util.isObject(d))
                                throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent: object expected");
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var m = new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent();
                            switch (d.dayOfWeek) {
                            case "MONDAY":
                            case 1:
                                m.dayOfWeek = 1;
                                break;
                            case "TUESDAY":
                            case 2:
                                m.dayOfWeek = 2;
                                break;
                            case "WEDNESDAY":
                            case 3:
                                m.dayOfWeek = 3;
                                break;
                            case "THURSDAY":
                            case 4:
                                m.dayOfWeek = 4;
                                break;
                            case "FRIDAY":
                            case 5:
                                m.dayOfWeek = 5;
                                break;
                            case "SATURDAY":
                            case 6:
                                m.dayOfWeek = 6;
                                break;
                            case "SUNDAY":
                            case 7:
                                m.dayOfWeek = 7;
                                break;
                            default:
                                if (typeof d.dayOfWeek === "number" && (d.dayOfWeek | 0) === d.dayOfWeek)
                                    m.dayOfWeek = d.dayOfWeek;
                            }
                            if (d.year != null) {
                                m.year = d.year >>> 0;
                            }
                            if (d.month != null) {
                                m.month = d.month >>> 0;
                            }
                            if (d.dayOfMonth != null) {
                                m.dayOfMonth = d.dayOfMonth >>> 0;
                            }
                            if (d.hour != null) {
                                m.hour = d.hour >>> 0;
                            }
                            if (d.minute != null) {
                                m.minute = d.minute >>> 0;
                            }
                            switch (d.calendar) {
                            case "GREGORIAN":
                            case 1:
                                m.calendar = 1;
                                break;
                            case "SOLAR_HIJRI":
                            case 2:
                                m.calendar = 2;
                                break;
                            default:
                                if (typeof d.calendar === "number" && (d.calendar | 0) === d.calendar)
                                    m.calendar = d.calendar;
                            }
                            return m;
                        };

                        HSMDateTimeComponent.toObject = function (m, o, q) {
                            if (!o)
                                o = {};
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var d = {};
                            if (m.dayOfWeek != null && $Object.hasOwnProperty.call(m, "dayOfWeek")) {
                                d.dayOfWeek = o.enums === $String ? $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType[m.dayOfWeek] === $undefined ? m.dayOfWeek : $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.DayOfWeekType[m.dayOfWeek] : m.dayOfWeek;
                            }
                            if (m.year != null && $Object.hasOwnProperty.call(m, "year")) {
                                d.year = m.year;
                            }
                            if (m.month != null && $Object.hasOwnProperty.call(m, "month")) {
                                d.month = m.month;
                            }
                            if (m.dayOfMonth != null && $Object.hasOwnProperty.call(m, "dayOfMonth")) {
                                d.dayOfMonth = m.dayOfMonth;
                            }
                            if (m.hour != null && $Object.hasOwnProperty.call(m, "hour")) {
                                d.hour = m.hour;
                            }
                            if (m.minute != null && $Object.hasOwnProperty.call(m, "minute")) {
                                d.minute = m.minute;
                            }
                            if (m.calendar != null && $Object.hasOwnProperty.call(m, "calendar")) {
                                d.calendar = o.enums === $String ? $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType[m.calendar] === $undefined ? m.calendar : $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent.CalendarType[m.calendar] : m.calendar;
                            }
                            return d;
                        };

                        HSMDateTimeComponent.prototype.toJSON = function() {
                            return HSMDateTimeComponent.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        HSMDateTimeComponent.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeComponent";
                        };

                        HSMDateTimeComponent.CalendarType = (function() {
                            const valuesById = $Object.create(null), values = $Object.create(valuesById);
                            values[valuesById[1] = "GREGORIAN"] = 1;
                            values[valuesById[2] = "SOLAR_HIJRI"] = 2;
                            return values;
                        })();

                        HSMDateTimeComponent.DayOfWeekType = (function() {
                            const valuesById = $Object.create(null), values = $Object.create(valuesById);
                            values[valuesById[1] = "MONDAY"] = 1;
                            values[valuesById[2] = "TUESDAY"] = 2;
                            values[valuesById[3] = "WEDNESDAY"] = 3;
                            values[valuesById[4] = "THURSDAY"] = 4;
                            values[valuesById[5] = "FRIDAY"] = 5;
                            values[valuesById[6] = "SATURDAY"] = 6;
                            values[valuesById[7] = "SUNDAY"] = 7;
                            return values;
                        })();

                        return HSMDateTimeComponent;
                    })();

                    HSMDateTime.HSMDateTimeUnixEpoch = (function() {

                        const HSMDateTimeUnixEpoch = function (p) {
                            if (p)
                                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                                        this[ks[i]] = p[ks[i]];
                        };

                        HSMDateTimeUnixEpoch.prototype.timestamp = null;

                        let $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(HSMDateTimeUnixEpoch.prototype, "_timestamp", {
                            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        HSMDateTimeUnixEpoch.create = function(properties) {
                            return new HSMDateTimeUnixEpoch(properties);
                        };

                        HSMDateTimeUnixEpoch.encode = function (m, w, q) {
                            if (!w)
                                w = $Writer.create();
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                                w.uint32(8).int64(m.timestamp);
                            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                                for (var i = 0; i < m.$unknowns.length; ++i)
                                    w.raw(m.$unknowns[i]);
                            return w;
                        };

                        HSMDateTimeUnixEpoch.decode = function (r, l, z, q, g) {
                            if (!(r instanceof $Reader))
                                r = $Reader.create(r);
                            if (q === $undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch();
                            while (r.pos < c) {
                                var s = r.pos;
                                var t = r.tag();
                                if (t === z) {
                                    z = $undefined;
                                    break;
                                }
                                var u = t & 7;
                                switch (t >>>= 3) {
                                case 1: {
                                        if (u !== 0)
                                            break;
                                        m.timestamp = r.int64();
                                        m._timestamp = "timestamp";
                                        continue;
                                    }
                                }
                                r.skipType(u, q, t);
                                if (!r.discardUnknown) {
                                    $util.makeProp(m, "$unknowns", false);
                                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                                }
                            }
                            if (z !== $undefined)
                                throw $Error("missing end group");
                            return m;
                        };

                        HSMDateTimeUnixEpoch.fromObject = function (d, q) {
                            if (d instanceof $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch)
                                return d;
                            if (!$util.isObject(d))
                                throw $TypeError(".proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch: object expected");
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var m = new $root.proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch();
                            if (d.timestamp != null) {
                                if ($util.Long)
                                    m.timestamp = $util.Long.fromValue(d.timestamp, false);
                                else if (typeof d.timestamp === "string")
                                    m.timestamp = $parseInt(d.timestamp, 10);
                                else if (typeof d.timestamp === "number")
                                    m.timestamp = d.timestamp;
                                else if (typeof d.timestamp === "object")
                                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                            }
                            return m;
                        };

                        HSMDateTimeUnixEpoch.toObject = function (m, o, q) {
                            if (!o)
                                o = {};
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var d = {};
                            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                                else if (typeof m.timestamp === "number")
                                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                                else
                                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                            }
                            return d;
                        };

                        HSMDateTimeUnixEpoch.prototype.toJSON = function() {
                            return HSMDateTimeUnixEpoch.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        HSMDateTimeUnixEpoch.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/proto.Message.HighlyStructuredMessage.HSMLocalizableParameter.HSMDateTime.HSMDateTimeUnixEpoch";
                        };

                        return HSMDateTimeUnixEpoch;
                    })();

                    return HSMDateTime;
                })();

                return HSMLocalizableParameter;
            })();

            return HighlyStructuredMessage;
        })();

        Message.HistoryShareMessageEntry = (function() {

            const HistoryShareMessageEntry = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HistoryShareMessageEntry.prototype.stanzaId = null;
            HistoryShareMessageEntry.prototype.messageSecretProof = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistoryShareMessageEntry.prototype, "_stanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistoryShareMessageEntry.prototype, "_messageSecretProof", {
                get: $util.oneOfGetter($oneOfFields = ["messageSecretProof"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistoryShareMessageEntry.create = function(properties) {
                return new HistoryShareMessageEntry(properties);
            };

            HistoryShareMessageEntry.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                    w.uint32(10).string(m.stanzaId);
                if (m.messageSecretProof != null && $Object.hasOwnProperty.call(m, "messageSecretProof"))
                    w.uint32(18).bytes(m.messageSecretProof);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HistoryShareMessageEntry.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HistoryShareMessageEntry();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.stanzaId = r.stringVerify();
                            m._stanzaId = "stanzaId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.messageSecretProof = r.bytes();
                            m._messageSecretProof = "messageSecretProof";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HistoryShareMessageEntry.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.HistoryShareMessageEntry)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.HistoryShareMessageEntry: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.HistoryShareMessageEntry();
                if (d.stanzaId != null) {
                    m.stanzaId = $String(d.stanzaId);
                }
                if (d.messageSecretProof != null) {
                    if (typeof d.messageSecretProof === "string")
                        $util.base64.decode(d.messageSecretProof, m.messageSecretProof = $util.newBuffer($util.base64.length(d.messageSecretProof)), 0);
                    else if (d.messageSecretProof.length >= 0)
                        m.messageSecretProof = d.messageSecretProof;
                }
                return m;
            };

            HistoryShareMessageEntry.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                    d.stanzaId = m.stanzaId;
                }
                if (m.messageSecretProof != null && $Object.hasOwnProperty.call(m, "messageSecretProof")) {
                    d.messageSecretProof = o.bytes === $String ? $util.base64.encode(m.messageSecretProof, 0, m.messageSecretProof.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageSecretProof) : m.messageSecretProof;
                }
                return d;
            };

            HistoryShareMessageEntry.prototype.toJSON = function() {
                return HistoryShareMessageEntry.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistoryShareMessageEntry.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.HistoryShareMessageEntry";
            };

            return HistoryShareMessageEntry;
        })();

        Message.HistorySyncMessageAccessStatus = (function() {

            const HistorySyncMessageAccessStatus = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HistorySyncMessageAccessStatus.prototype.completeAccessGranted = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncMessageAccessStatus.prototype, "_completeAccessGranted", {
                get: $util.oneOfGetter($oneOfFields = ["completeAccessGranted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistorySyncMessageAccessStatus.create = function(properties) {
                return new HistorySyncMessageAccessStatus(properties);
            };

            HistorySyncMessageAccessStatus.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.completeAccessGranted != null && $Object.hasOwnProperty.call(m, "completeAccessGranted"))
                    w.uint32(8).bool(m.completeAccessGranted);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HistorySyncMessageAccessStatus.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HistorySyncMessageAccessStatus();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.completeAccessGranted = r.bool();
                            m._completeAccessGranted = "completeAccessGranted";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HistorySyncMessageAccessStatus.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.HistorySyncMessageAccessStatus)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.HistorySyncMessageAccessStatus: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.HistorySyncMessageAccessStatus();
                if (d.completeAccessGranted != null) {
                    m.completeAccessGranted = $Boolean(d.completeAccessGranted);
                }
                return m;
            };

            HistorySyncMessageAccessStatus.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.completeAccessGranted != null && $Object.hasOwnProperty.call(m, "completeAccessGranted")) {
                    d.completeAccessGranted = m.completeAccessGranted;
                }
                return d;
            };

            HistorySyncMessageAccessStatus.prototype.toJSON = function() {
                return HistorySyncMessageAccessStatus.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistorySyncMessageAccessStatus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.HistorySyncMessageAccessStatus";
            };

            return HistorySyncMessageAccessStatus;
        })();

        Message.HistorySyncNotification = (function() {

            const HistorySyncNotification = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            HistorySyncNotification.prototype.fileSha256 = null;
            HistorySyncNotification.prototype.fileLength = null;
            HistorySyncNotification.prototype.mediaKey = null;
            HistorySyncNotification.prototype.fileEncSha256 = null;
            HistorySyncNotification.prototype.directPath = null;
            HistorySyncNotification.prototype.syncType = null;
            HistorySyncNotification.prototype.chunkOrder = null;
            HistorySyncNotification.prototype.originalMessageId = null;
            HistorySyncNotification.prototype.progress = null;
            HistorySyncNotification.prototype.oldestMsgInChunkTimestampSec = null;
            HistorySyncNotification.prototype.initialHistBootstrapInlinePayload = null;
            HistorySyncNotification.prototype.peerDataRequestSessionId = null;
            HistorySyncNotification.prototype.fullHistorySyncOnDemandRequestMetadata = null;
            HistorySyncNotification.prototype.encHandle = null;
            HistorySyncNotification.prototype.messageAccessStatus = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_syncType", {
                get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_chunkOrder", {
                get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_originalMessageId", {
                get: $util.oneOfGetter($oneOfFields = ["originalMessageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_progress", {
                get: $util.oneOfGetter($oneOfFields = ["progress"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_oldestMsgInChunkTimestampSec", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMsgInChunkTimestampSec"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_initialHistBootstrapInlinePayload", {
                get: $util.oneOfGetter($oneOfFields = ["initialHistBootstrapInlinePayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_peerDataRequestSessionId", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataRequestSessionId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_fullHistorySyncOnDemandRequestMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequestMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_encHandle", {
                get: $util.oneOfGetter($oneOfFields = ["encHandle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(HistorySyncNotification.prototype, "_messageAccessStatus", {
                get: $util.oneOfGetter($oneOfFields = ["messageAccessStatus"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            HistorySyncNotification.create = function(properties) {
                return new HistorySyncNotification(properties);
            };

            HistorySyncNotification.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(10).bytes(m.fileSha256);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(16).uint64(m.fileLength);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(26).bytes(m.mediaKey);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(34).bytes(m.fileEncSha256);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(42).string(m.directPath);
                if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType"))
                    w.uint32(48).int32(m.syncType);
                if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder"))
                    w.uint32(56).uint32(m.chunkOrder);
                if (m.originalMessageId != null && $Object.hasOwnProperty.call(m, "originalMessageId"))
                    w.uint32(66).string(m.originalMessageId);
                if (m.progress != null && $Object.hasOwnProperty.call(m, "progress"))
                    w.uint32(72).uint32(m.progress);
                if (m.oldestMsgInChunkTimestampSec != null && $Object.hasOwnProperty.call(m, "oldestMsgInChunkTimestampSec"))
                    w.uint32(80).int64(m.oldestMsgInChunkTimestampSec);
                if (m.initialHistBootstrapInlinePayload != null && $Object.hasOwnProperty.call(m, "initialHistBootstrapInlinePayload"))
                    w.uint32(90).bytes(m.initialHistBootstrapInlinePayload);
                if (m.peerDataRequestSessionId != null && $Object.hasOwnProperty.call(m, "peerDataRequestSessionId"))
                    w.uint32(98).string(m.peerDataRequestSessionId);
                if (m.fullHistorySyncOnDemandRequestMetadata != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequestMetadata"))
                    $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.encode(m.fullHistorySyncOnDemandRequestMetadata, w.uint32(106).fork(), q + 1).ldelim();
                if (m.encHandle != null && $Object.hasOwnProperty.call(m, "encHandle"))
                    w.uint32(114).string(m.encHandle);
                if (m.messageAccessStatus != null && $Object.hasOwnProperty.call(m, "messageAccessStatus"))
                    $root.proto.Message.HistorySyncMessageAccessStatus.encode(m.messageAccessStatus, w.uint32(122).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            HistorySyncNotification.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.HistorySyncNotification(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.syncType = r.int32();
                            m._syncType = "syncType";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.chunkOrder = r.uint32();
                            m._chunkOrder = "chunkOrder";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.originalMessageId = r.stringVerify();
                            m._originalMessageId = "originalMessageId";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.progress = r.uint32();
                            m._progress = "progress";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.oldestMsgInChunkTimestampSec = r.int64();
                            m._oldestMsgInChunkTimestampSec = "oldestMsgInChunkTimestampSec";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.initialHistBootstrapInlinePayload = r.bytes();
                            m._initialHistBootstrapInlinePayload = "initialHistBootstrapInlinePayload";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.peerDataRequestSessionId = r.stringVerify();
                            m._peerDataRequestSessionId = "peerDataRequestSessionId";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.fullHistorySyncOnDemandRequestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.decode(r, r.uint32(), $undefined, q + 1, m.fullHistorySyncOnDemandRequestMetadata);
                            m._fullHistorySyncOnDemandRequestMetadata = "fullHistorySyncOnDemandRequestMetadata";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.encHandle = r.stringVerify();
                            m._encHandle = "encHandle";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            m.messageAccessStatus = $root.proto.Message.HistorySyncMessageAccessStatus.decode(r, r.uint32(), $undefined, q + 1, m.messageAccessStatus);
                            m._messageAccessStatus = "messageAccessStatus";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            HistorySyncNotification.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.HistorySyncNotification)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.HistorySyncNotification: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.HistorySyncNotification();
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                switch (d.syncType) {
                case "INITIAL_BOOTSTRAP":
                case 0:
                    m.syncType = 0;
                    break;
                case "INITIAL_STATUS_V3":
                case 1:
                    m.syncType = 1;
                    break;
                case "FULL":
                case 2:
                    m.syncType = 2;
                    break;
                case "RECENT":
                case 3:
                    m.syncType = 3;
                    break;
                case "PUSH_NAME":
                case 4:
                    m.syncType = 4;
                    break;
                case "NON_BLOCKING_DATA":
                case 5:
                    m.syncType = 5;
                    break;
                case "ON_DEMAND":
                case 6:
                    m.syncType = 6;
                    break;
                case "NO_HISTORY":
                case 7:
                    m.syncType = 7;
                    break;
                case "MESSAGE_ACCESS_STATUS":
                case 8:
                    m.syncType = 8;
                    break;
                default:
                    if (typeof d.syncType === "number" && (d.syncType | 0) === d.syncType)
                        m.syncType = d.syncType;
                }
                if (d.chunkOrder != null) {
                    m.chunkOrder = d.chunkOrder >>> 0;
                }
                if (d.originalMessageId != null) {
                    m.originalMessageId = $String(d.originalMessageId);
                }
                if (d.progress != null) {
                    m.progress = d.progress >>> 0;
                }
                if (d.oldestMsgInChunkTimestampSec != null) {
                    if ($util.Long)
                        m.oldestMsgInChunkTimestampSec = $util.Long.fromValue(d.oldestMsgInChunkTimestampSec, false);
                    else if (typeof d.oldestMsgInChunkTimestampSec === "string")
                        m.oldestMsgInChunkTimestampSec = $parseInt(d.oldestMsgInChunkTimestampSec, 10);
                    else if (typeof d.oldestMsgInChunkTimestampSec === "number")
                        m.oldestMsgInChunkTimestampSec = d.oldestMsgInChunkTimestampSec;
                    else if (typeof d.oldestMsgInChunkTimestampSec === "object")
                        m.oldestMsgInChunkTimestampSec = new $util.LongBits(d.oldestMsgInChunkTimestampSec.low >>> 0, d.oldestMsgInChunkTimestampSec.high >>> 0).toNumber();
                }
                if (d.initialHistBootstrapInlinePayload != null) {
                    if (typeof d.initialHistBootstrapInlinePayload === "string")
                        $util.base64.decode(d.initialHistBootstrapInlinePayload, m.initialHistBootstrapInlinePayload = $util.newBuffer($util.base64.length(d.initialHistBootstrapInlinePayload)), 0);
                    else if (d.initialHistBootstrapInlinePayload.length >= 0)
                        m.initialHistBootstrapInlinePayload = d.initialHistBootstrapInlinePayload;
                }
                if (d.peerDataRequestSessionId != null) {
                    m.peerDataRequestSessionId = $String(d.peerDataRequestSessionId);
                }
                if (d.fullHistorySyncOnDemandRequestMetadata != null) {
                    if (!$util.isObject(d.fullHistorySyncOnDemandRequestMetadata))
                        throw $TypeError(".proto.Message.HistorySyncNotification.fullHistorySyncOnDemandRequestMetadata: object expected");
                    m.fullHistorySyncOnDemandRequestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.fromObject(d.fullHistorySyncOnDemandRequestMetadata, q + 1);
                }
                if (d.encHandle != null) {
                    m.encHandle = $String(d.encHandle);
                }
                if (d.messageAccessStatus != null) {
                    if (!$util.isObject(d.messageAccessStatus))
                        throw $TypeError(".proto.Message.HistorySyncNotification.messageAccessStatus: object expected");
                    m.messageAccessStatus = $root.proto.Message.HistorySyncMessageAccessStatus.fromObject(d.messageAccessStatus, q + 1);
                }
                return m;
            };

            HistorySyncNotification.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType")) {
                    d.syncType = o.enums === $String ? $root.proto.Message.HistorySyncType[m.syncType] === $undefined ? m.syncType : $root.proto.Message.HistorySyncType[m.syncType] : m.syncType;
                }
                if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder")) {
                    d.chunkOrder = m.chunkOrder;
                }
                if (m.originalMessageId != null && $Object.hasOwnProperty.call(m, "originalMessageId")) {
                    d.originalMessageId = m.originalMessageId;
                }
                if (m.progress != null && $Object.hasOwnProperty.call(m, "progress")) {
                    d.progress = m.progress;
                }
                if (m.oldestMsgInChunkTimestampSec != null && $Object.hasOwnProperty.call(m, "oldestMsgInChunkTimestampSec")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.oldestMsgInChunkTimestampSec = typeof m.oldestMsgInChunkTimestampSec === "number" ? $BigInt(m.oldestMsgInChunkTimestampSec) : $util.Long.fromBits(m.oldestMsgInChunkTimestampSec.low >>> 0, m.oldestMsgInChunkTimestampSec.high >>> 0, false).toBigInt();
                    else if (typeof m.oldestMsgInChunkTimestampSec === "number")
                        d.oldestMsgInChunkTimestampSec = o.longs === $String ? $String(m.oldestMsgInChunkTimestampSec) : m.oldestMsgInChunkTimestampSec;
                    else
                        d.oldestMsgInChunkTimestampSec = o.longs === $String ? $util.Long.prototype.toString.call(m.oldestMsgInChunkTimestampSec) : o.longs === $Number ? new $util.LongBits(m.oldestMsgInChunkTimestampSec.low >>> 0, m.oldestMsgInChunkTimestampSec.high >>> 0).toNumber() : m.oldestMsgInChunkTimestampSec;
                }
                if (m.initialHistBootstrapInlinePayload != null && $Object.hasOwnProperty.call(m, "initialHistBootstrapInlinePayload")) {
                    d.initialHistBootstrapInlinePayload = o.bytes === $String ? $util.base64.encode(m.initialHistBootstrapInlinePayload, 0, m.initialHistBootstrapInlinePayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.initialHistBootstrapInlinePayload) : m.initialHistBootstrapInlinePayload;
                }
                if (m.peerDataRequestSessionId != null && $Object.hasOwnProperty.call(m, "peerDataRequestSessionId")) {
                    d.peerDataRequestSessionId = m.peerDataRequestSessionId;
                }
                if (m.fullHistorySyncOnDemandRequestMetadata != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequestMetadata")) {
                    d.fullHistorySyncOnDemandRequestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.toObject(m.fullHistorySyncOnDemandRequestMetadata, o, q + 1);
                }
                if (m.encHandle != null && $Object.hasOwnProperty.call(m, "encHandle")) {
                    d.encHandle = m.encHandle;
                }
                if (m.messageAccessStatus != null && $Object.hasOwnProperty.call(m, "messageAccessStatus")) {
                    d.messageAccessStatus = $root.proto.Message.HistorySyncMessageAccessStatus.toObject(m.messageAccessStatus, o, q + 1);
                }
                return d;
            };

            HistorySyncNotification.prototype.toJSON = function() {
                return HistorySyncNotification.toObject(this, $protobuf.util.toJSONOptions);
            };

            HistorySyncNotification.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.HistorySyncNotification";
            };

            return HistorySyncNotification;
        })();

        Message.HistorySyncType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "INITIAL_BOOTSTRAP"] = 0;
            values[valuesById[1] = "INITIAL_STATUS_V3"] = 1;
            values[valuesById[2] = "FULL"] = 2;
            values[valuesById[3] = "RECENT"] = 3;
            values[valuesById[4] = "PUSH_NAME"] = 4;
            values[valuesById[5] = "NON_BLOCKING_DATA"] = 5;
            values[valuesById[6] = "ON_DEMAND"] = 6;
            values[valuesById[7] = "NO_HISTORY"] = 7;
            values[valuesById[8] = "MESSAGE_ACCESS_STATUS"] = 8;
            return values;
        })();

        Message.ImageMessage = (function() {

            const ImageMessage = function (p) {
                this.interactiveAnnotations = [];
                this.scanLengths = [];
                this.annotations = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ImageMessage.prototype.url = null;
            ImageMessage.prototype.mimetype = null;
            ImageMessage.prototype.caption = null;
            ImageMessage.prototype.fileSha256 = null;
            ImageMessage.prototype.fileLength = null;
            ImageMessage.prototype.height = null;
            ImageMessage.prototype.width = null;
            ImageMessage.prototype.mediaKey = null;
            ImageMessage.prototype.fileEncSha256 = null;
            ImageMessage.prototype.interactiveAnnotations = $util.emptyArray;
            ImageMessage.prototype.directPath = null;
            ImageMessage.prototype.mediaKeyTimestamp = null;
            ImageMessage.prototype.jpegThumbnail = null;
            ImageMessage.prototype.contextInfo = null;
            ImageMessage.prototype.firstScanSidecar = null;
            ImageMessage.prototype.firstScanLength = null;
            ImageMessage.prototype.experimentGroupId = null;
            ImageMessage.prototype.scansSidecar = null;
            ImageMessage.prototype.scanLengths = $util.emptyArray;
            ImageMessage.prototype.midQualityFileSha256 = null;
            ImageMessage.prototype.midQualityFileEncSha256 = null;
            ImageMessage.prototype.viewOnce = null;
            ImageMessage.prototype.thumbnailDirectPath = null;
            ImageMessage.prototype.thumbnailSha256 = null;
            ImageMessage.prototype.thumbnailEncSha256 = null;
            ImageMessage.prototype.staticUrl = null;
            ImageMessage.prototype.annotations = $util.emptyArray;
            ImageMessage.prototype.imageSourceType = null;
            ImageMessage.prototype.accessibilityLabel = null;
            ImageMessage.prototype.qrUrl = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_firstScanSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["firstScanSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_firstScanLength", {
                get: $util.oneOfGetter($oneOfFields = ["firstScanLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_experimentGroupId", {
                get: $util.oneOfGetter($oneOfFields = ["experimentGroupId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_scansSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["scansSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_midQualityFileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["midQualityFileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_midQualityFileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["midQualityFileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_staticUrl", {
                get: $util.oneOfGetter($oneOfFields = ["staticUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_imageSourceType", {
                get: $util.oneOfGetter($oneOfFields = ["imageSourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ImageMessage.prototype, "_qrUrl", {
                get: $util.oneOfGetter($oneOfFields = ["qrUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ImageMessage.create = function(properties) {
                return new ImageMessage(properties);
            };

            ImageMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(18).string(m.mimetype);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(26).string(m.caption);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(34).bytes(m.fileSha256);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(40).uint64(m.fileLength);
                if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                    w.uint32(48).uint32(m.height);
                if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                    w.uint32(56).uint32(m.width);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(66).bytes(m.mediaKey);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(74).bytes(m.fileEncSha256);
                if (m.interactiveAnnotations != null && m.interactiveAnnotations.length) {
                    for (var i = 0; i < m.interactiveAnnotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(m.interactiveAnnotations[i], w.uint32(82).fork(), q + 1).ldelim();
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(90).string(m.directPath);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(96).int64(m.mediaKeyTimestamp);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.firstScanSidecar != null && $Object.hasOwnProperty.call(m, "firstScanSidecar"))
                    w.uint32(146).bytes(m.firstScanSidecar);
                if (m.firstScanLength != null && $Object.hasOwnProperty.call(m, "firstScanLength"))
                    w.uint32(152).uint32(m.firstScanLength);
                if (m.experimentGroupId != null && $Object.hasOwnProperty.call(m, "experimentGroupId"))
                    w.uint32(160).uint32(m.experimentGroupId);
                if (m.scansSidecar != null && $Object.hasOwnProperty.call(m, "scansSidecar"))
                    w.uint32(170).bytes(m.scansSidecar);
                if (m.scanLengths != null && m.scanLengths.length) {
                    w.uint32(178).uint32s(m.scanLengths);
                }
                if (m.midQualityFileSha256 != null && $Object.hasOwnProperty.call(m, "midQualityFileSha256"))
                    w.uint32(186).bytes(m.midQualityFileSha256);
                if (m.midQualityFileEncSha256 != null && $Object.hasOwnProperty.call(m, "midQualityFileEncSha256"))
                    w.uint32(194).bytes(m.midQualityFileEncSha256);
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce"))
                    w.uint32(200).bool(m.viewOnce);
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                    w.uint32(210).string(m.thumbnailDirectPath);
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256"))
                    w.uint32(218).bytes(m.thumbnailSha256);
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256"))
                    w.uint32(226).bytes(m.thumbnailEncSha256);
                if (m.staticUrl != null && $Object.hasOwnProperty.call(m, "staticUrl"))
                    w.uint32(234).string(m.staticUrl);
                if (m.annotations != null && m.annotations.length) {
                    for (var i = 0; i < m.annotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(m.annotations[i], w.uint32(242).fork(), q + 1).ldelim();
                }
                if (m.imageSourceType != null && $Object.hasOwnProperty.call(m, "imageSourceType"))
                    w.uint32(248).int32(m.imageSourceType);
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel"))
                    w.uint32(258).string(m.accessibilityLabel);
                if (m.qrUrl != null && $Object.hasOwnProperty.call(m, "qrUrl"))
                    w.uint32(274).string(m.qrUrl);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ImageMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ImageMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.height = r.uint32();
                            m._height = "height";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.width = r.uint32();
                            m._width = "width";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            if (!(m.interactiveAnnotations && m.interactiveAnnotations.length))
                                m.interactiveAnnotations = [];
                            m.interactiveAnnotations.push($root.proto.InteractiveAnnotation.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            m.firstScanSidecar = r.bytes();
                            m._firstScanSidecar = "firstScanSidecar";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.firstScanLength = r.uint32();
                            m._firstScanLength = "firstScanLength";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            m.experimentGroupId = r.uint32();
                            m._experimentGroupId = "experimentGroupId";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            m.scansSidecar = r.bytes();
                            m._scansSidecar = "scansSidecar";
                            continue;
                        }
                    case 22: {
                            if (u === 2) {
                                if (!(m.scanLengths && m.scanLengths.length))
                                    m.scanLengths = [];
                                r.uint32s(m.scanLengths);
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(m.scanLengths && m.scanLengths.length))
                                m.scanLengths = [];
                            m.scanLengths.push(r.uint32());
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            m.midQualityFileSha256 = r.bytes();
                            m._midQualityFileSha256 = "midQualityFileSha256";
                            continue;
                        }
                    case 24: {
                            if (u !== 2)
                                break;
                            m.midQualityFileEncSha256 = r.bytes();
                            m._midQualityFileEncSha256 = "midQualityFileEncSha256";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            m.viewOnce = r.bool();
                            m._viewOnce = "viewOnce";
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            m.thumbnailDirectPath = r.stringVerify();
                            m._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            m.thumbnailSha256 = r.bytes();
                            m._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 28: {
                            if (u !== 2)
                                break;
                            m.thumbnailEncSha256 = r.bytes();
                            m._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 29: {
                            if (u !== 2)
                                break;
                            m.staticUrl = r.stringVerify();
                            m._staticUrl = "staticUrl";
                            continue;
                        }
                    case 30: {
                            if (u !== 2)
                                break;
                            if (!(m.annotations && m.annotations.length))
                                m.annotations = [];
                            m.annotations.push($root.proto.InteractiveAnnotation.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            m.imageSourceType = r.int32();
                            m._imageSourceType = "imageSourceType";
                            continue;
                        }
                    case 32: {
                            if (u !== 2)
                                break;
                            m.accessibilityLabel = r.stringVerify();
                            m._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    case 34: {
                            if (u !== 2)
                                break;
                            m.qrUrl = r.stringVerify();
                            m._qrUrl = "qrUrl";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ImageMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ImageMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ImageMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ImageMessage();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.height != null) {
                    m.height = d.height >>> 0;
                }
                if (d.width != null) {
                    m.width = d.width >>> 0;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.interactiveAnnotations) {
                    if (!$Array.isArray(d.interactiveAnnotations))
                        throw $TypeError(".proto.Message.ImageMessage.interactiveAnnotations: array expected");
                    m.interactiveAnnotations = $Array(d.interactiveAnnotations.length);
                    for (var i = 0; i < d.interactiveAnnotations.length; ++i) {
                        if (!$util.isObject(d.interactiveAnnotations[i]))
                            throw $TypeError(".proto.Message.ImageMessage.interactiveAnnotations: object expected");
                        m.interactiveAnnotations[i] = $root.proto.InteractiveAnnotation.fromObject(d.interactiveAnnotations[i], q + 1);
                    }
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ImageMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.firstScanSidecar != null) {
                    if (typeof d.firstScanSidecar === "string")
                        $util.base64.decode(d.firstScanSidecar, m.firstScanSidecar = $util.newBuffer($util.base64.length(d.firstScanSidecar)), 0);
                    else if (d.firstScanSidecar.length >= 0)
                        m.firstScanSidecar = d.firstScanSidecar;
                }
                if (d.firstScanLength != null) {
                    m.firstScanLength = d.firstScanLength >>> 0;
                }
                if (d.experimentGroupId != null) {
                    m.experimentGroupId = d.experimentGroupId >>> 0;
                }
                if (d.scansSidecar != null) {
                    if (typeof d.scansSidecar === "string")
                        $util.base64.decode(d.scansSidecar, m.scansSidecar = $util.newBuffer($util.base64.length(d.scansSidecar)), 0);
                    else if (d.scansSidecar.length >= 0)
                        m.scansSidecar = d.scansSidecar;
                }
                if (d.scanLengths) {
                    if (!$Array.isArray(d.scanLengths))
                        throw $TypeError(".proto.Message.ImageMessage.scanLengths: array expected");
                    m.scanLengths = $Array(d.scanLengths.length);
                    for (var i = 0; i < d.scanLengths.length; ++i) {
                        m.scanLengths[i] = d.scanLengths[i] >>> 0;
                    }
                }
                if (d.midQualityFileSha256 != null) {
                    if (typeof d.midQualityFileSha256 === "string")
                        $util.base64.decode(d.midQualityFileSha256, m.midQualityFileSha256 = $util.newBuffer($util.base64.length(d.midQualityFileSha256)), 0);
                    else if (d.midQualityFileSha256.length >= 0)
                        m.midQualityFileSha256 = d.midQualityFileSha256;
                }
                if (d.midQualityFileEncSha256 != null) {
                    if (typeof d.midQualityFileEncSha256 === "string")
                        $util.base64.decode(d.midQualityFileEncSha256, m.midQualityFileEncSha256 = $util.newBuffer($util.base64.length(d.midQualityFileEncSha256)), 0);
                    else if (d.midQualityFileEncSha256.length >= 0)
                        m.midQualityFileEncSha256 = d.midQualityFileEncSha256;
                }
                if (d.viewOnce != null) {
                    m.viewOnce = $Boolean(d.viewOnce);
                }
                if (d.thumbnailDirectPath != null) {
                    m.thumbnailDirectPath = $String(d.thumbnailDirectPath);
                }
                if (d.thumbnailSha256 != null) {
                    if (typeof d.thumbnailSha256 === "string")
                        $util.base64.decode(d.thumbnailSha256, m.thumbnailSha256 = $util.newBuffer($util.base64.length(d.thumbnailSha256)), 0);
                    else if (d.thumbnailSha256.length >= 0)
                        m.thumbnailSha256 = d.thumbnailSha256;
                }
                if (d.thumbnailEncSha256 != null) {
                    if (typeof d.thumbnailEncSha256 === "string")
                        $util.base64.decode(d.thumbnailEncSha256, m.thumbnailEncSha256 = $util.newBuffer($util.base64.length(d.thumbnailEncSha256)), 0);
                    else if (d.thumbnailEncSha256.length >= 0)
                        m.thumbnailEncSha256 = d.thumbnailEncSha256;
                }
                if (d.staticUrl != null) {
                    m.staticUrl = $String(d.staticUrl);
                }
                if (d.annotations) {
                    if (!$Array.isArray(d.annotations))
                        throw $TypeError(".proto.Message.ImageMessage.annotations: array expected");
                    m.annotations = $Array(d.annotations.length);
                    for (var i = 0; i < d.annotations.length; ++i) {
                        if (!$util.isObject(d.annotations[i]))
                            throw $TypeError(".proto.Message.ImageMessage.annotations: object expected");
                        m.annotations[i] = $root.proto.InteractiveAnnotation.fromObject(d.annotations[i], q + 1);
                    }
                }
                switch (d.imageSourceType) {
                case "USER_IMAGE":
                case 0:
                    m.imageSourceType = 0;
                    break;
                case "AI_GENERATED":
                case 1:
                    m.imageSourceType = 1;
                    break;
                case "AI_MODIFIED":
                case 2:
                    m.imageSourceType = 2;
                    break;
                case "RASTERIZED_TEXT_STATUS":
                case 3:
                    m.imageSourceType = 3;
                    break;
                default:
                    if (typeof d.imageSourceType === "number" && (d.imageSourceType | 0) === d.imageSourceType)
                        m.imageSourceType = d.imageSourceType;
                }
                if (d.accessibilityLabel != null) {
                    m.accessibilityLabel = $String(d.accessibilityLabel);
                }
                if (d.qrUrl != null) {
                    m.qrUrl = $String(d.qrUrl);
                }
                return m;
            };

            ImageMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.interactiveAnnotations = [];
                    d.scanLengths = [];
                    d.annotations = [];
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                    d.height = m.height;
                }
                if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                    d.width = m.width;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.interactiveAnnotations && m.interactiveAnnotations.length) {
                    d.interactiveAnnotations = $Array(m.interactiveAnnotations.length);
                    for (var j = 0; j < m.interactiveAnnotations.length; ++j) {
                        d.interactiveAnnotations[j] = $root.proto.InteractiveAnnotation.toObject(m.interactiveAnnotations[j], o, q + 1);
                    }
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.firstScanSidecar != null && $Object.hasOwnProperty.call(m, "firstScanSidecar")) {
                    d.firstScanSidecar = o.bytes === $String ? $util.base64.encode(m.firstScanSidecar, 0, m.firstScanSidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.firstScanSidecar) : m.firstScanSidecar;
                }
                if (m.firstScanLength != null && $Object.hasOwnProperty.call(m, "firstScanLength")) {
                    d.firstScanLength = m.firstScanLength;
                }
                if (m.experimentGroupId != null && $Object.hasOwnProperty.call(m, "experimentGroupId")) {
                    d.experimentGroupId = m.experimentGroupId;
                }
                if (m.scansSidecar != null && $Object.hasOwnProperty.call(m, "scansSidecar")) {
                    d.scansSidecar = o.bytes === $String ? $util.base64.encode(m.scansSidecar, 0, m.scansSidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.scansSidecar) : m.scansSidecar;
                }
                if (m.scanLengths && m.scanLengths.length) {
                    d.scanLengths = $Array(m.scanLengths.length);
                    for (var j = 0; j < m.scanLengths.length; ++j) {
                        d.scanLengths[j] = m.scanLengths[j];
                    }
                }
                if (m.midQualityFileSha256 != null && $Object.hasOwnProperty.call(m, "midQualityFileSha256")) {
                    d.midQualityFileSha256 = o.bytes === $String ? $util.base64.encode(m.midQualityFileSha256, 0, m.midQualityFileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.midQualityFileSha256) : m.midQualityFileSha256;
                }
                if (m.midQualityFileEncSha256 != null && $Object.hasOwnProperty.call(m, "midQualityFileEncSha256")) {
                    d.midQualityFileEncSha256 = o.bytes === $String ? $util.base64.encode(m.midQualityFileEncSha256, 0, m.midQualityFileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.midQualityFileEncSha256) : m.midQualityFileEncSha256;
                }
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce")) {
                    d.viewOnce = m.viewOnce;
                }
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath")) {
                    d.thumbnailDirectPath = m.thumbnailDirectPath;
                }
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256")) {
                    d.thumbnailSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailSha256, 0, m.thumbnailSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailSha256) : m.thumbnailSha256;
                }
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256")) {
                    d.thumbnailEncSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailEncSha256, 0, m.thumbnailEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailEncSha256) : m.thumbnailEncSha256;
                }
                if (m.staticUrl != null && $Object.hasOwnProperty.call(m, "staticUrl")) {
                    d.staticUrl = m.staticUrl;
                }
                if (m.annotations && m.annotations.length) {
                    d.annotations = $Array(m.annotations.length);
                    for (var j = 0; j < m.annotations.length; ++j) {
                        d.annotations[j] = $root.proto.InteractiveAnnotation.toObject(m.annotations[j], o, q + 1);
                    }
                }
                if (m.imageSourceType != null && $Object.hasOwnProperty.call(m, "imageSourceType")) {
                    d.imageSourceType = o.enums === $String ? $root.proto.Message.ImageMessage.ImageSourceType[m.imageSourceType] === $undefined ? m.imageSourceType : $root.proto.Message.ImageMessage.ImageSourceType[m.imageSourceType] : m.imageSourceType;
                }
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel")) {
                    d.accessibilityLabel = m.accessibilityLabel;
                }
                if (m.qrUrl != null && $Object.hasOwnProperty.call(m, "qrUrl")) {
                    d.qrUrl = m.qrUrl;
                }
                return d;
            };

            ImageMessage.prototype.toJSON = function() {
                return ImageMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ImageMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ImageMessage";
            };

            ImageMessage.ImageSourceType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "USER_IMAGE"] = 0;
                values[valuesById[1] = "AI_GENERATED"] = 1;
                values[valuesById[2] = "AI_MODIFIED"] = 2;
                values[valuesById[3] = "RASTERIZED_TEXT_STATUS"] = 3;
                return values;
            })();

            return ImageMessage;
        })();

        Message.InitialSecurityNotificationSettingSync = (function() {

            const InitialSecurityNotificationSettingSync = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InitialSecurityNotificationSettingSync.prototype.securityNotificationEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InitialSecurityNotificationSettingSync.prototype, "_securityNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["securityNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InitialSecurityNotificationSettingSync.create = function(properties) {
                return new InitialSecurityNotificationSettingSync(properties);
            };

            InitialSecurityNotificationSettingSync.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.securityNotificationEnabled != null && $Object.hasOwnProperty.call(m, "securityNotificationEnabled"))
                    w.uint32(8).bool(m.securityNotificationEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InitialSecurityNotificationSettingSync.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InitialSecurityNotificationSettingSync();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.securityNotificationEnabled = r.bool();
                            m._securityNotificationEnabled = "securityNotificationEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InitialSecurityNotificationSettingSync.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.InitialSecurityNotificationSettingSync)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.InitialSecurityNotificationSettingSync: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.InitialSecurityNotificationSettingSync();
                if (d.securityNotificationEnabled != null) {
                    m.securityNotificationEnabled = $Boolean(d.securityNotificationEnabled);
                }
                return m;
            };

            InitialSecurityNotificationSettingSync.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.securityNotificationEnabled != null && $Object.hasOwnProperty.call(m, "securityNotificationEnabled")) {
                    d.securityNotificationEnabled = m.securityNotificationEnabled;
                }
                return d;
            };

            InitialSecurityNotificationSettingSync.prototype.toJSON = function() {
                return InitialSecurityNotificationSettingSync.toObject(this, $protobuf.util.toJSONOptions);
            };

            InitialSecurityNotificationSettingSync.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.InitialSecurityNotificationSettingSync";
            };

            return InitialSecurityNotificationSettingSync;
        })();

        Message.InsightDeliveryState = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "SENT"] = 0;
            values[valuesById[1] = "DELIVERED"] = 1;
            values[valuesById[2] = "READ"] = 2;
            values[valuesById[3] = "REPLIED"] = 3;
            values[valuesById[4] = "QUICK_REPLIED"] = 4;
            return values;
        })();

        Message.InteractiveMessage = (function() {

            const InteractiveMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InteractiveMessage.prototype.header = null;
            InteractiveMessage.prototype.body = null;
            InteractiveMessage.prototype.footer = null;
            InteractiveMessage.prototype.bloksWidget = null;
            InteractiveMessage.prototype.contextInfo = null;
            InteractiveMessage.prototype.urlTrackingMap = null;
            InteractiveMessage.prototype.shopStorefrontMessage = null;
            InteractiveMessage.prototype.collectionMessage = null;
            InteractiveMessage.prototype.nativeFlowMessage = null;
            InteractiveMessage.prototype.carouselMessage = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessage.prototype, "_header", {
                get: $util.oneOfGetter($oneOfFields = ["header"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessage.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessage.prototype, "_footer", {
                get: $util.oneOfGetter($oneOfFields = ["footer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessage.prototype, "_bloksWidget", {
                get: $util.oneOfGetter($oneOfFields = ["bloksWidget"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessage.prototype, "_urlTrackingMap", {
                get: $util.oneOfGetter($oneOfFields = ["urlTrackingMap"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            $Object.defineProperty(InteractiveMessage.prototype, "interactiveMessage", {
                get: $util.oneOfGetter($oneOfFields = ["shopStorefrontMessage", "collectionMessage", "nativeFlowMessage", "carouselMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InteractiveMessage.create = function(properties) {
                return new InteractiveMessage(properties);
            };

            InteractiveMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.header != null && $Object.hasOwnProperty.call(m, "header"))
                    $root.proto.Message.InteractiveMessage.Header.encode(m.header, w.uint32(10).fork(), q + 1).ldelim();
                if (m.body != null && $Object.hasOwnProperty.call(m, "body"))
                    $root.proto.Message.InteractiveMessage.Body.encode(m.body, w.uint32(18).fork(), q + 1).ldelim();
                if (m.footer != null && $Object.hasOwnProperty.call(m, "footer"))
                    $root.proto.Message.InteractiveMessage.Footer.encode(m.footer, w.uint32(26).fork(), q + 1).ldelim();
                if (m.shopStorefrontMessage != null && $Object.hasOwnProperty.call(m, "shopStorefrontMessage"))
                    $root.proto.Message.InteractiveMessage.ShopMessage.encode(m.shopStorefrontMessage, w.uint32(34).fork(), q + 1).ldelim();
                if (m.collectionMessage != null && $Object.hasOwnProperty.call(m, "collectionMessage"))
                    $root.proto.Message.InteractiveMessage.CollectionMessage.encode(m.collectionMessage, w.uint32(42).fork(), q + 1).ldelim();
                if (m.nativeFlowMessage != null && $Object.hasOwnProperty.call(m, "nativeFlowMessage"))
                    $root.proto.Message.InteractiveMessage.NativeFlowMessage.encode(m.nativeFlowMessage, w.uint32(50).fork(), q + 1).ldelim();
                if (m.carouselMessage != null && $Object.hasOwnProperty.call(m, "carouselMessage"))
                    $root.proto.Message.InteractiveMessage.CarouselMessage.encode(m.carouselMessage, w.uint32(58).fork(), q + 1).ldelim();
                if (m.bloksWidget != null && $Object.hasOwnProperty.call(m, "bloksWidget"))
                    $root.proto.Message.InteractiveMessage.BloksWidget.encode(m.bloksWidget, w.uint32(66).fork(), q + 1).ldelim();
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(122).fork(), q + 1).ldelim();
                if (m.urlTrackingMap != null && $Object.hasOwnProperty.call(m, "urlTrackingMap"))
                    $root.proto.UrlTrackingMap.encode(m.urlTrackingMap, w.uint32(130).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InteractiveMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.header = $root.proto.Message.InteractiveMessage.Header.decode(r, r.uint32(), $undefined, q + 1, m.header);
                            m._header = "header";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.body = $root.proto.Message.InteractiveMessage.Body.decode(r, r.uint32(), $undefined, q + 1, m.body);
                            m._body = "body";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.footer = $root.proto.Message.InteractiveMessage.Footer.decode(r, r.uint32(), $undefined, q + 1, m.footer);
                            m._footer = "footer";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.decode(r, r.uint32(), $undefined, q + 1, m.bloksWidget);
                            m._bloksWidget = "bloksWidget";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.urlTrackingMap = $root.proto.UrlTrackingMap.decode(r, r.uint32(), $undefined, q + 1, m.urlTrackingMap);
                            m._urlTrackingMap = "urlTrackingMap";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.shopStorefrontMessage = $root.proto.Message.InteractiveMessage.ShopMessage.decode(r, r.uint32(), $undefined, q + 1, m.shopStorefrontMessage);
                            m.interactiveMessage = "shopStorefrontMessage";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.collectionMessage = $root.proto.Message.InteractiveMessage.CollectionMessage.decode(r, r.uint32(), $undefined, q + 1, m.collectionMessage);
                            m.interactiveMessage = "collectionMessage";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.nativeFlowMessage = $root.proto.Message.InteractiveMessage.NativeFlowMessage.decode(r, r.uint32(), $undefined, q + 1, m.nativeFlowMessage);
                            m.interactiveMessage = "nativeFlowMessage";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.carouselMessage = $root.proto.Message.InteractiveMessage.CarouselMessage.decode(r, r.uint32(), $undefined, q + 1, m.carouselMessage);
                            m.interactiveMessage = "carouselMessage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InteractiveMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.InteractiveMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.InteractiveMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.InteractiveMessage();
                if (d.header != null) {
                    if (!$util.isObject(d.header))
                        throw $TypeError(".proto.Message.InteractiveMessage.header: object expected");
                    m.header = $root.proto.Message.InteractiveMessage.Header.fromObject(d.header, q + 1);
                }
                if (d.body != null) {
                    if (!$util.isObject(d.body))
                        throw $TypeError(".proto.Message.InteractiveMessage.body: object expected");
                    m.body = $root.proto.Message.InteractiveMessage.Body.fromObject(d.body, q + 1);
                }
                if (d.footer != null) {
                    if (!$util.isObject(d.footer))
                        throw $TypeError(".proto.Message.InteractiveMessage.footer: object expected");
                    m.footer = $root.proto.Message.InteractiveMessage.Footer.fromObject(d.footer, q + 1);
                }
                if (d.bloksWidget != null) {
                    if (!$util.isObject(d.bloksWidget))
                        throw $TypeError(".proto.Message.InteractiveMessage.bloksWidget: object expected");
                    m.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.fromObject(d.bloksWidget, q + 1);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.InteractiveMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.urlTrackingMap != null) {
                    if (!$util.isObject(d.urlTrackingMap))
                        throw $TypeError(".proto.Message.InteractiveMessage.urlTrackingMap: object expected");
                    m.urlTrackingMap = $root.proto.UrlTrackingMap.fromObject(d.urlTrackingMap, q + 1);
                }
                if (d.shopStorefrontMessage != null) {
                    if (!$util.isObject(d.shopStorefrontMessage))
                        throw $TypeError(".proto.Message.InteractiveMessage.shopStorefrontMessage: object expected");
                    m.shopStorefrontMessage = $root.proto.Message.InteractiveMessage.ShopMessage.fromObject(d.shopStorefrontMessage, q + 1);
                }
                if (d.collectionMessage != null) {
                    if (!$util.isObject(d.collectionMessage))
                        throw $TypeError(".proto.Message.InteractiveMessage.collectionMessage: object expected");
                    m.collectionMessage = $root.proto.Message.InteractiveMessage.CollectionMessage.fromObject(d.collectionMessage, q + 1);
                }
                if (d.nativeFlowMessage != null) {
                    if (!$util.isObject(d.nativeFlowMessage))
                        throw $TypeError(".proto.Message.InteractiveMessage.nativeFlowMessage: object expected");
                    m.nativeFlowMessage = $root.proto.Message.InteractiveMessage.NativeFlowMessage.fromObject(d.nativeFlowMessage, q + 1);
                }
                if (d.carouselMessage != null) {
                    if (!$util.isObject(d.carouselMessage))
                        throw $TypeError(".proto.Message.InteractiveMessage.carouselMessage: object expected");
                    m.carouselMessage = $root.proto.Message.InteractiveMessage.CarouselMessage.fromObject(d.carouselMessage, q + 1);
                }
                return m;
            };

            InteractiveMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.header != null && $Object.hasOwnProperty.call(m, "header")) {
                    d.header = $root.proto.Message.InteractiveMessage.Header.toObject(m.header, o, q + 1);
                }
                if (m.body != null && $Object.hasOwnProperty.call(m, "body")) {
                    d.body = $root.proto.Message.InteractiveMessage.Body.toObject(m.body, o, q + 1);
                }
                if (m.footer != null && $Object.hasOwnProperty.call(m, "footer")) {
                    d.footer = $root.proto.Message.InteractiveMessage.Footer.toObject(m.footer, o, q + 1);
                }
                if (m.shopStorefrontMessage != null && $Object.hasOwnProperty.call(m, "shopStorefrontMessage")) {
                    d.shopStorefrontMessage = $root.proto.Message.InteractiveMessage.ShopMessage.toObject(m.shopStorefrontMessage, o, q + 1);
                    if (o.oneofs)
                        d.interactiveMessage = "shopStorefrontMessage";
                }
                if (m.collectionMessage != null && $Object.hasOwnProperty.call(m, "collectionMessage")) {
                    d.collectionMessage = $root.proto.Message.InteractiveMessage.CollectionMessage.toObject(m.collectionMessage, o, q + 1);
                    if (o.oneofs)
                        d.interactiveMessage = "collectionMessage";
                }
                if (m.nativeFlowMessage != null && $Object.hasOwnProperty.call(m, "nativeFlowMessage")) {
                    d.nativeFlowMessage = $root.proto.Message.InteractiveMessage.NativeFlowMessage.toObject(m.nativeFlowMessage, o, q + 1);
                    if (o.oneofs)
                        d.interactiveMessage = "nativeFlowMessage";
                }
                if (m.carouselMessage != null && $Object.hasOwnProperty.call(m, "carouselMessage")) {
                    d.carouselMessage = $root.proto.Message.InteractiveMessage.CarouselMessage.toObject(m.carouselMessage, o, q + 1);
                    if (o.oneofs)
                        d.interactiveMessage = "carouselMessage";
                }
                if (m.bloksWidget != null && $Object.hasOwnProperty.call(m, "bloksWidget")) {
                    d.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.toObject(m.bloksWidget, o, q + 1);
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.urlTrackingMap != null && $Object.hasOwnProperty.call(m, "urlTrackingMap")) {
                    d.urlTrackingMap = $root.proto.UrlTrackingMap.toObject(m.urlTrackingMap, o, q + 1);
                }
                return d;
            };

            InteractiveMessage.prototype.toJSON = function() {
                return InteractiveMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            InteractiveMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.InteractiveMessage";
            };

            InteractiveMessage.BloksWidget = (function() {

                const BloksWidget = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                BloksWidget.prototype.uuid = null;
                BloksWidget.prototype.data = null;
                BloksWidget.prototype.type = null;
                BloksWidget.prototype.fallback = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BloksWidget.prototype, "_uuid", {
                    get: $util.oneOfGetter($oneOfFields = ["uuid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BloksWidget.prototype, "_data", {
                    get: $util.oneOfGetter($oneOfFields = ["data"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BloksWidget.prototype, "_type", {
                    get: $util.oneOfGetter($oneOfFields = ["type"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BloksWidget.prototype, "_fallback", {
                    get: $util.oneOfGetter($oneOfFields = ["fallback"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BloksWidget.create = function(properties) {
                    return new BloksWidget(properties);
                };

                BloksWidget.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.uuid != null && $Object.hasOwnProperty.call(m, "uuid"))
                        w.uint32(10).string(m.uuid);
                    if (m.data != null && $Object.hasOwnProperty.call(m, "data"))
                        w.uint32(18).string(m.data);
                    if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                        w.uint32(26).string(m.type);
                    if (m.fallback != null && $Object.hasOwnProperty.call(m, "fallback"))
                        w.uint32(34).string(m.fallback);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                BloksWidget.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.BloksWidget();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.uuid = r.stringVerify();
                                m._uuid = "uuid";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.data = r.stringVerify();
                                m._data = "data";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.type = r.stringVerify();
                                m._type = "type";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.fallback = r.stringVerify();
                                m._fallback = "fallback";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                BloksWidget.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.BloksWidget)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.BloksWidget: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.BloksWidget();
                    if (d.uuid != null) {
                        m.uuid = $String(d.uuid);
                    }
                    if (d.data != null) {
                        m.data = $String(d.data);
                    }
                    if (d.type != null) {
                        m.type = $String(d.type);
                    }
                    if (d.fallback != null) {
                        m.fallback = $String(d.fallback);
                    }
                    return m;
                };

                BloksWidget.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.uuid != null && $Object.hasOwnProperty.call(m, "uuid")) {
                        d.uuid = m.uuid;
                    }
                    if (m.data != null && $Object.hasOwnProperty.call(m, "data")) {
                        d.data = m.data;
                    }
                    if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                        d.type = m.type;
                    }
                    if (m.fallback != null && $Object.hasOwnProperty.call(m, "fallback")) {
                        d.fallback = m.fallback;
                    }
                    return d;
                };

                BloksWidget.prototype.toJSON = function() {
                    return BloksWidget.toObject(this, $protobuf.util.toJSONOptions);
                };

                BloksWidget.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.BloksWidget";
                };

                return BloksWidget;
            })();

            InteractiveMessage.Body = (function() {

                const Body = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Body.prototype.text = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Body.prototype, "_text", {
                    get: $util.oneOfGetter($oneOfFields = ["text"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Body.create = function(properties) {
                    return new Body(properties);
                };

                Body.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                        w.uint32(10).string(m.text);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Body.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.Body();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.text = r.stringVerify();
                                m._text = "text";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Body.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.Body)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.Body: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.Body();
                    if (d.text != null) {
                        m.text = $String(d.text);
                    }
                    return m;
                };

                Body.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                        d.text = m.text;
                    }
                    return d;
                };

                Body.prototype.toJSON = function() {
                    return Body.toObject(this, $protobuf.util.toJSONOptions);
                };

                Body.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.Body";
                };

                return Body;
            })();

            InteractiveMessage.CarouselMessage = (function() {

                const CarouselMessage = function (p) {
                    this.cards = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CarouselMessage.prototype.cards = $util.emptyArray;
                CarouselMessage.prototype.messageVersion = null;
                CarouselMessage.prototype.carouselCardType = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CarouselMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CarouselMessage.prototype, "_carouselCardType", {
                    get: $util.oneOfGetter($oneOfFields = ["carouselCardType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CarouselMessage.create = function(properties) {
                    return new CarouselMessage(properties);
                };

                CarouselMessage.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.cards != null && m.cards.length) {
                        for (var i = 0; i < m.cards.length; ++i)
                            $root.proto.Message.InteractiveMessage.encode(m.cards[i], w.uint32(10).fork(), q + 1).ldelim();
                    }
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion"))
                        w.uint32(16).int32(m.messageVersion);
                    if (m.carouselCardType != null && $Object.hasOwnProperty.call(m, "carouselCardType"))
                        w.uint32(24).int32(m.carouselCardType);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CarouselMessage.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.CarouselMessage(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                if (!(m.cards && m.cards.length))
                                    m.cards = [];
                                m.cards.push($root.proto.Message.InteractiveMessage.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.messageVersion = r.int32();
                                m._messageVersion = "messageVersion";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.carouselCardType = r.int32();
                                m._carouselCardType = "carouselCardType";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CarouselMessage.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.CarouselMessage)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.CarouselMessage: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.CarouselMessage();
                    if (d.cards) {
                        if (!$Array.isArray(d.cards))
                            throw $TypeError(".proto.Message.InteractiveMessage.CarouselMessage.cards: array expected");
                        m.cards = $Array(d.cards.length);
                        for (var i = 0; i < d.cards.length; ++i) {
                            if (!$util.isObject(d.cards[i]))
                                throw $TypeError(".proto.Message.InteractiveMessage.CarouselMessage.cards: object expected");
                            m.cards[i] = $root.proto.Message.InteractiveMessage.fromObject(d.cards[i], q + 1);
                        }
                    }
                    if (d.messageVersion != null) {
                        m.messageVersion = d.messageVersion | 0;
                    }
                    switch (d.carouselCardType) {
                    case "UNKNOWN":
                    case 0:
                        m.carouselCardType = 0;
                        break;
                    case "HSCROLL_CARDS":
                    case 1:
                        m.carouselCardType = 1;
                        break;
                    case "ALBUM_IMAGE":
                    case 2:
                        m.carouselCardType = 2;
                        break;
                    default:
                        if (typeof d.carouselCardType === "number" && (d.carouselCardType | 0) === d.carouselCardType)
                            m.carouselCardType = d.carouselCardType;
                    }
                    return m;
                };

                CarouselMessage.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.cards = [];
                    }
                    if (m.cards && m.cards.length) {
                        d.cards = $Array(m.cards.length);
                        for (var j = 0; j < m.cards.length; ++j) {
                            d.cards[j] = $root.proto.Message.InteractiveMessage.toObject(m.cards[j], o, q + 1);
                        }
                    }
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion")) {
                        d.messageVersion = m.messageVersion;
                    }
                    if (m.carouselCardType != null && $Object.hasOwnProperty.call(m, "carouselCardType")) {
                        d.carouselCardType = o.enums === $String ? $root.proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType[m.carouselCardType] === $undefined ? m.carouselCardType : $root.proto.Message.InteractiveMessage.CarouselMessage.CarouselCardType[m.carouselCardType] : m.carouselCardType;
                    }
                    return d;
                };

                CarouselMessage.prototype.toJSON = function() {
                    return CarouselMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                CarouselMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.CarouselMessage";
                };

                CarouselMessage.CarouselCardType = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN"] = 0;
                    values[valuesById[1] = "HSCROLL_CARDS"] = 1;
                    values[valuesById[2] = "ALBUM_IMAGE"] = 2;
                    return values;
                })();

                return CarouselMessage;
            })();

            InteractiveMessage.CollectionMessage = (function() {

                const CollectionMessage = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CollectionMessage.prototype.bizJid = null;
                CollectionMessage.prototype.id = null;
                CollectionMessage.prototype.messageVersion = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CollectionMessage.prototype, "_bizJid", {
                    get: $util.oneOfGetter($oneOfFields = ["bizJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CollectionMessage.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CollectionMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CollectionMessage.create = function(properties) {
                    return new CollectionMessage(properties);
                };

                CollectionMessage.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.bizJid != null && $Object.hasOwnProperty.call(m, "bizJid"))
                        w.uint32(10).string(m.bizJid);
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                        w.uint32(18).string(m.id);
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion"))
                        w.uint32(24).int32(m.messageVersion);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CollectionMessage.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.CollectionMessage();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.bizJid = r.stringVerify();
                                m._bizJid = "bizJid";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.id = r.stringVerify();
                                m._id = "id";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.messageVersion = r.int32();
                                m._messageVersion = "messageVersion";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CollectionMessage.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.CollectionMessage)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.CollectionMessage: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.CollectionMessage();
                    if (d.bizJid != null) {
                        m.bizJid = $String(d.bizJid);
                    }
                    if (d.id != null) {
                        m.id = $String(d.id);
                    }
                    if (d.messageVersion != null) {
                        m.messageVersion = d.messageVersion | 0;
                    }
                    return m;
                };

                CollectionMessage.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.bizJid != null && $Object.hasOwnProperty.call(m, "bizJid")) {
                        d.bizJid = m.bizJid;
                    }
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                        d.id = m.id;
                    }
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion")) {
                        d.messageVersion = m.messageVersion;
                    }
                    return d;
                };

                CollectionMessage.prototype.toJSON = function() {
                    return CollectionMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                CollectionMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.CollectionMessage";
                };

                return CollectionMessage;
            })();

            InteractiveMessage.Footer = (function() {

                const Footer = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Footer.prototype.text = null;
                Footer.prototype.hasMediaAttachment = null;
                Footer.prototype.audioMessage = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Footer.prototype, "_text", {
                    get: $util.oneOfGetter($oneOfFields = ["text"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Footer.prototype, "_hasMediaAttachment", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMediaAttachment"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                $Object.defineProperty(Footer.prototype, "media", {
                    get: $util.oneOfGetter($oneOfFields = ["audioMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Footer.create = function(properties) {
                    return new Footer(properties);
                };

                Footer.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                        w.uint32(10).string(m.text);
                    if (m.audioMessage != null && $Object.hasOwnProperty.call(m, "audioMessage"))
                        $root.proto.Message.AudioMessage.encode(m.audioMessage, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.hasMediaAttachment != null && $Object.hasOwnProperty.call(m, "hasMediaAttachment"))
                        w.uint32(24).bool(m.hasMediaAttachment);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Footer.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.Footer();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.text = r.stringVerify();
                                m._text = "text";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.hasMediaAttachment = r.bool();
                                m._hasMediaAttachment = "hasMediaAttachment";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.audioMessage = $root.proto.Message.AudioMessage.decode(r, r.uint32(), $undefined, q + 1, m.audioMessage);
                                m.media = "audioMessage";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Footer.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.Footer)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.Footer: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.Footer();
                    if (d.text != null) {
                        m.text = $String(d.text);
                    }
                    if (d.hasMediaAttachment != null) {
                        m.hasMediaAttachment = $Boolean(d.hasMediaAttachment);
                    }
                    if (d.audioMessage != null) {
                        if (!$util.isObject(d.audioMessage))
                            throw $TypeError(".proto.Message.InteractiveMessage.Footer.audioMessage: object expected");
                        m.audioMessage = $root.proto.Message.AudioMessage.fromObject(d.audioMessage, q + 1);
                    }
                    return m;
                };

                Footer.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                        d.text = m.text;
                    }
                    if (m.audioMessage != null && $Object.hasOwnProperty.call(m, "audioMessage")) {
                        d.audioMessage = $root.proto.Message.AudioMessage.toObject(m.audioMessage, o, q + 1);
                        if (o.oneofs)
                            d.media = "audioMessage";
                    }
                    if (m.hasMediaAttachment != null && $Object.hasOwnProperty.call(m, "hasMediaAttachment")) {
                        d.hasMediaAttachment = m.hasMediaAttachment;
                    }
                    return d;
                };

                Footer.prototype.toJSON = function() {
                    return Footer.toObject(this, $protobuf.util.toJSONOptions);
                };

                Footer.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.Footer";
                };

                return Footer;
            })();

            InteractiveMessage.Header = (function() {

                const Header = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Header.prototype.title = null;
                Header.prototype.subtitle = null;
                Header.prototype.hasMediaAttachment = null;
                Header.prototype.bloksWidget = null;
                Header.prototype.documentMessage = null;
                Header.prototype.imageMessage = null;
                Header.prototype.jpegThumbnail = null;
                Header.prototype.videoMessage = null;
                Header.prototype.locationMessage = null;
                Header.prototype.productMessage = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Header.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Header.prototype, "_subtitle", {
                    get: $util.oneOfGetter($oneOfFields = ["subtitle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Header.prototype, "_hasMediaAttachment", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMediaAttachment"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Header.prototype, "_bloksWidget", {
                    get: $util.oneOfGetter($oneOfFields = ["bloksWidget"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                $Object.defineProperty(Header.prototype, "media", {
                    get: $util.oneOfGetter($oneOfFields = ["documentMessage", "imageMessage", "jpegThumbnail", "videoMessage", "locationMessage", "productMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Header.create = function(properties) {
                    return new Header(properties);
                };

                Header.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(10).string(m.title);
                    if (m.subtitle != null && $Object.hasOwnProperty.call(m, "subtitle"))
                        w.uint32(18).string(m.subtitle);
                    if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage"))
                        $root.proto.Message.DocumentMessage.encode(m.documentMessage, w.uint32(26).fork(), q + 1).ldelim();
                    if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                        $root.proto.Message.ImageMessage.encode(m.imageMessage, w.uint32(34).fork(), q + 1).ldelim();
                    if (m.hasMediaAttachment != null && $Object.hasOwnProperty.call(m, "hasMediaAttachment"))
                        w.uint32(40).bool(m.hasMediaAttachment);
                    if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                        w.uint32(50).bytes(m.jpegThumbnail);
                    if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                        $root.proto.Message.VideoMessage.encode(m.videoMessage, w.uint32(58).fork(), q + 1).ldelim();
                    if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage"))
                        $root.proto.Message.LocationMessage.encode(m.locationMessage, w.uint32(66).fork(), q + 1).ldelim();
                    if (m.productMessage != null && $Object.hasOwnProperty.call(m, "productMessage"))
                        $root.proto.Message.ProductMessage.encode(m.productMessage, w.uint32(74).fork(), q + 1).ldelim();
                    if (m.bloksWidget != null && $Object.hasOwnProperty.call(m, "bloksWidget"))
                        $root.proto.Message.InteractiveMessage.BloksWidget.encode(m.bloksWidget, w.uint32(82).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Header.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.Header();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.subtitle = r.stringVerify();
                                m._subtitle = "subtitle";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.hasMediaAttachment = r.bool();
                                m._hasMediaAttachment = "hasMediaAttachment";
                                continue;
                            }
                        case 10: {
                                if (u !== 2)
                                    break;
                                m.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.decode(r, r.uint32(), $undefined, q + 1, m.bloksWidget);
                                m._bloksWidget = "bloksWidget";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.documentMessage = $root.proto.Message.DocumentMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentMessage);
                                m.media = "documentMessage";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.imageMessage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                                m.media = "imageMessage";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.jpegThumbnail = r.bytes();
                                m.media = "jpegThumbnail";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.videoMessage = $root.proto.Message.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                                m.media = "videoMessage";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                m.locationMessage = $root.proto.Message.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.locationMessage);
                                m.media = "locationMessage";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                m.productMessage = $root.proto.Message.ProductMessage.decode(r, r.uint32(), $undefined, q + 1, m.productMessage);
                                m.media = "productMessage";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Header.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.Header)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.Header: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.Header();
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.subtitle != null) {
                        m.subtitle = $String(d.subtitle);
                    }
                    if (d.hasMediaAttachment != null) {
                        m.hasMediaAttachment = $Boolean(d.hasMediaAttachment);
                    }
                    if (d.bloksWidget != null) {
                        if (!$util.isObject(d.bloksWidget))
                            throw $TypeError(".proto.Message.InteractiveMessage.Header.bloksWidget: object expected");
                        m.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.fromObject(d.bloksWidget, q + 1);
                    }
                    if (d.documentMessage != null) {
                        if (!$util.isObject(d.documentMessage))
                            throw $TypeError(".proto.Message.InteractiveMessage.Header.documentMessage: object expected");
                        m.documentMessage = $root.proto.Message.DocumentMessage.fromObject(d.documentMessage, q + 1);
                    }
                    if (d.imageMessage != null) {
                        if (!$util.isObject(d.imageMessage))
                            throw $TypeError(".proto.Message.InteractiveMessage.Header.imageMessage: object expected");
                        m.imageMessage = $root.proto.Message.ImageMessage.fromObject(d.imageMessage, q + 1);
                    }
                    if (d.jpegThumbnail != null) {
                        if (typeof d.jpegThumbnail === "string")
                            $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                        else if (d.jpegThumbnail.length >= 0)
                            m.jpegThumbnail = d.jpegThumbnail;
                    }
                    if (d.videoMessage != null) {
                        if (!$util.isObject(d.videoMessage))
                            throw $TypeError(".proto.Message.InteractiveMessage.Header.videoMessage: object expected");
                        m.videoMessage = $root.proto.Message.VideoMessage.fromObject(d.videoMessage, q + 1);
                    }
                    if (d.locationMessage != null) {
                        if (!$util.isObject(d.locationMessage))
                            throw $TypeError(".proto.Message.InteractiveMessage.Header.locationMessage: object expected");
                        m.locationMessage = $root.proto.Message.LocationMessage.fromObject(d.locationMessage, q + 1);
                    }
                    if (d.productMessage != null) {
                        if (!$util.isObject(d.productMessage))
                            throw $TypeError(".proto.Message.InteractiveMessage.Header.productMessage: object expected");
                        m.productMessage = $root.proto.Message.ProductMessage.fromObject(d.productMessage, q + 1);
                    }
                    return m;
                };

                Header.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.subtitle != null && $Object.hasOwnProperty.call(m, "subtitle")) {
                        d.subtitle = m.subtitle;
                    }
                    if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage")) {
                        d.documentMessage = $root.proto.Message.DocumentMessage.toObject(m.documentMessage, o, q + 1);
                        if (o.oneofs)
                            d.media = "documentMessage";
                    }
                    if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                        d.imageMessage = $root.proto.Message.ImageMessage.toObject(m.imageMessage, o, q + 1);
                        if (o.oneofs)
                            d.media = "imageMessage";
                    }
                    if (m.hasMediaAttachment != null && $Object.hasOwnProperty.call(m, "hasMediaAttachment")) {
                        d.hasMediaAttachment = m.hasMediaAttachment;
                    }
                    if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                        d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                        if (o.oneofs)
                            d.media = "jpegThumbnail";
                    }
                    if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                        d.videoMessage = $root.proto.Message.VideoMessage.toObject(m.videoMessage, o, q + 1);
                        if (o.oneofs)
                            d.media = "videoMessage";
                    }
                    if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage")) {
                        d.locationMessage = $root.proto.Message.LocationMessage.toObject(m.locationMessage, o, q + 1);
                        if (o.oneofs)
                            d.media = "locationMessage";
                    }
                    if (m.productMessage != null && $Object.hasOwnProperty.call(m, "productMessage")) {
                        d.productMessage = $root.proto.Message.ProductMessage.toObject(m.productMessage, o, q + 1);
                        if (o.oneofs)
                            d.media = "productMessage";
                    }
                    if (m.bloksWidget != null && $Object.hasOwnProperty.call(m, "bloksWidget")) {
                        d.bloksWidget = $root.proto.Message.InteractiveMessage.BloksWidget.toObject(m.bloksWidget, o, q + 1);
                    }
                    return d;
                };

                Header.prototype.toJSON = function() {
                    return Header.toObject(this, $protobuf.util.toJSONOptions);
                };

                Header.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.Header";
                };

                return Header;
            })();

            InteractiveMessage.NativeFlowMessage = (function() {

                const NativeFlowMessage = function (p) {
                    this.buttons = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                NativeFlowMessage.prototype.buttons = $util.emptyArray;
                NativeFlowMessage.prototype.messageParamsJson = null;
                NativeFlowMessage.prototype.messageVersion = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(NativeFlowMessage.prototype, "_messageParamsJson", {
                    get: $util.oneOfGetter($oneOfFields = ["messageParamsJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(NativeFlowMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                NativeFlowMessage.create = function(properties) {
                    return new NativeFlowMessage(properties);
                };

                NativeFlowMessage.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.buttons != null && m.buttons.length) {
                        for (var i = 0; i < m.buttons.length; ++i)
                            $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.encode(m.buttons[i], w.uint32(10).fork(), q + 1).ldelim();
                    }
                    if (m.messageParamsJson != null && $Object.hasOwnProperty.call(m, "messageParamsJson"))
                        w.uint32(18).string(m.messageParamsJson);
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion"))
                        w.uint32(24).int32(m.messageVersion);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                NativeFlowMessage.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.NativeFlowMessage();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                if (!(m.buttons && m.buttons.length))
                                    m.buttons = [];
                                m.buttons.push($root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.messageParamsJson = r.stringVerify();
                                m._messageParamsJson = "messageParamsJson";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.messageVersion = r.int32();
                                m._messageVersion = "messageVersion";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                NativeFlowMessage.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.NativeFlowMessage)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.NativeFlowMessage();
                    if (d.buttons) {
                        if (!$Array.isArray(d.buttons))
                            throw $TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage.buttons: array expected");
                        m.buttons = $Array(d.buttons.length);
                        for (var i = 0; i < d.buttons.length; ++i) {
                            if (!$util.isObject(d.buttons[i]))
                                throw $TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage.buttons: object expected");
                            m.buttons[i] = $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.fromObject(d.buttons[i], q + 1);
                        }
                    }
                    if (d.messageParamsJson != null) {
                        m.messageParamsJson = $String(d.messageParamsJson);
                    }
                    if (d.messageVersion != null) {
                        m.messageVersion = d.messageVersion | 0;
                    }
                    return m;
                };

                NativeFlowMessage.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.buttons = [];
                    }
                    if (m.buttons && m.buttons.length) {
                        d.buttons = $Array(m.buttons.length);
                        for (var j = 0; j < m.buttons.length; ++j) {
                            d.buttons[j] = $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton.toObject(m.buttons[j], o, q + 1);
                        }
                    }
                    if (m.messageParamsJson != null && $Object.hasOwnProperty.call(m, "messageParamsJson")) {
                        d.messageParamsJson = m.messageParamsJson;
                    }
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion")) {
                        d.messageVersion = m.messageVersion;
                    }
                    return d;
                };

                NativeFlowMessage.prototype.toJSON = function() {
                    return NativeFlowMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                NativeFlowMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.NativeFlowMessage";
                };

                NativeFlowMessage.NativeFlowButton = (function() {

                    const NativeFlowButton = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    NativeFlowButton.prototype.name = null;
                    NativeFlowButton.prototype.buttonParamsJson = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(NativeFlowButton.prototype, "_name", {
                        get: $util.oneOfGetter($oneOfFields = ["name"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(NativeFlowButton.prototype, "_buttonParamsJson", {
                        get: $util.oneOfGetter($oneOfFields = ["buttonParamsJson"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    NativeFlowButton.create = function(properties) {
                        return new NativeFlowButton(properties);
                    };

                    NativeFlowButton.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                            w.uint32(10).string(m.name);
                        if (m.buttonParamsJson != null && $Object.hasOwnProperty.call(m, "buttonParamsJson"))
                            w.uint32(18).string(m.buttonParamsJson);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    NativeFlowButton.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.name = r.stringVerify();
                                    m._name = "name";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.buttonParamsJson = r.stringVerify();
                                    m._buttonParamsJson = "buttonParamsJson";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    NativeFlowButton.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton();
                        if (d.name != null) {
                            m.name = $String(d.name);
                        }
                        if (d.buttonParamsJson != null) {
                            m.buttonParamsJson = $String(d.buttonParamsJson);
                        }
                        return m;
                    };

                    NativeFlowButton.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                            d.name = m.name;
                        }
                        if (m.buttonParamsJson != null && $Object.hasOwnProperty.call(m, "buttonParamsJson")) {
                            d.buttonParamsJson = m.buttonParamsJson;
                        }
                        return d;
                    };

                    NativeFlowButton.prototype.toJSON = function() {
                        return NativeFlowButton.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    NativeFlowButton.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.InteractiveMessage.NativeFlowMessage.NativeFlowButton";
                    };

                    return NativeFlowButton;
                })();

                return NativeFlowMessage;
            })();

            InteractiveMessage.ShopMessage = (function() {

                const ShopMessage = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                ShopMessage.prototype.id = null;
                ShopMessage.prototype.surface = null;
                ShopMessage.prototype.messageVersion = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ShopMessage.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ShopMessage.prototype, "_surface", {
                    get: $util.oneOfGetter($oneOfFields = ["surface"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ShopMessage.prototype, "_messageVersion", {
                    get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ShopMessage.create = function(properties) {
                    return new ShopMessage(properties);
                };

                ShopMessage.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                        w.uint32(10).string(m.id);
                    if (m.surface != null && $Object.hasOwnProperty.call(m, "surface"))
                        w.uint32(16).int32(m.surface);
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion"))
                        w.uint32(24).int32(m.messageVersion);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                ShopMessage.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveMessage.ShopMessage(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.id = r.stringVerify();
                                m._id = "id";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.surface = r.int32();
                                m._surface = "surface";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.messageVersion = r.int32();
                                m._messageVersion = "messageVersion";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                ShopMessage.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveMessage.ShopMessage)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveMessage.ShopMessage: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveMessage.ShopMessage();
                    if (d.id != null) {
                        m.id = $String(d.id);
                    }
                    switch (d.surface) {
                    case "UNKNOWN_SURFACE":
                    case 0:
                        m.surface = 0;
                        break;
                    case "FB":
                    case 1:
                        m.surface = 1;
                        break;
                    case "IG":
                    case 2:
                        m.surface = 2;
                        break;
                    case "WA":
                    case 3:
                        m.surface = 3;
                        break;
                    default:
                        if (typeof d.surface === "number" && (d.surface | 0) === d.surface)
                            m.surface = d.surface;
                    }
                    if (d.messageVersion != null) {
                        m.messageVersion = d.messageVersion | 0;
                    }
                    return m;
                };

                ShopMessage.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                        d.id = m.id;
                    }
                    if (m.surface != null && $Object.hasOwnProperty.call(m, "surface")) {
                        d.surface = o.enums === $String ? $root.proto.Message.InteractiveMessage.ShopMessage.Surface[m.surface] === $undefined ? m.surface : $root.proto.Message.InteractiveMessage.ShopMessage.Surface[m.surface] : m.surface;
                    }
                    if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion")) {
                        d.messageVersion = m.messageVersion;
                    }
                    return d;
                };

                ShopMessage.prototype.toJSON = function() {
                    return ShopMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                ShopMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveMessage.ShopMessage";
                };

                ShopMessage.Surface = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "UNKNOWN_SURFACE"] = 0;
                    values[valuesById[1] = "FB"] = 1;
                    values[valuesById[2] = "IG"] = 2;
                    values[valuesById[3] = "WA"] = 3;
                    return values;
                })();

                return ShopMessage;
            })();

            return InteractiveMessage;
        })();

        Message.InteractiveResponseMessage = (function() {

            const InteractiveResponseMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InteractiveResponseMessage.prototype.body = null;
            InteractiveResponseMessage.prototype.contextInfo = null;
            InteractiveResponseMessage.prototype.nativeFlowResponseMessage = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveResponseMessage.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveResponseMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            $Object.defineProperty(InteractiveResponseMessage.prototype, "interactiveResponseMessage", {
                get: $util.oneOfGetter($oneOfFields = ["nativeFlowResponseMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InteractiveResponseMessage.create = function(properties) {
                return new InteractiveResponseMessage(properties);
            };

            InteractiveResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.body != null && $Object.hasOwnProperty.call(m, "body"))
                    $root.proto.Message.InteractiveResponseMessage.Body.encode(m.body, w.uint32(10).fork(), q + 1).ldelim();
                if (m.nativeFlowResponseMessage != null && $Object.hasOwnProperty.call(m, "nativeFlowResponseMessage"))
                    $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.encode(m.nativeFlowResponseMessage, w.uint32(18).fork(), q + 1).ldelim();
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(122).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InteractiveResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveResponseMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.body = $root.proto.Message.InteractiveResponseMessage.Body.decode(r, r.uint32(), $undefined, q + 1, m.body);
                            m._body = "body";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.nativeFlowResponseMessage = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.nativeFlowResponseMessage);
                            m.interactiveResponseMessage = "nativeFlowResponseMessage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InteractiveResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.InteractiveResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.InteractiveResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.InteractiveResponseMessage();
                if (d.body != null) {
                    if (!$util.isObject(d.body))
                        throw $TypeError(".proto.Message.InteractiveResponseMessage.body: object expected");
                    m.body = $root.proto.Message.InteractiveResponseMessage.Body.fromObject(d.body, q + 1);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.InteractiveResponseMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.nativeFlowResponseMessage != null) {
                    if (!$util.isObject(d.nativeFlowResponseMessage))
                        throw $TypeError(".proto.Message.InteractiveResponseMessage.nativeFlowResponseMessage: object expected");
                    m.nativeFlowResponseMessage = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.fromObject(d.nativeFlowResponseMessage, q + 1);
                }
                return m;
            };

            InteractiveResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.body != null && $Object.hasOwnProperty.call(m, "body")) {
                    d.body = $root.proto.Message.InteractiveResponseMessage.Body.toObject(m.body, o, q + 1);
                }
                if (m.nativeFlowResponseMessage != null && $Object.hasOwnProperty.call(m, "nativeFlowResponseMessage")) {
                    d.nativeFlowResponseMessage = $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage.toObject(m.nativeFlowResponseMessage, o, q + 1);
                    if (o.oneofs)
                        d.interactiveResponseMessage = "nativeFlowResponseMessage";
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            InteractiveResponseMessage.prototype.toJSON = function() {
                return InteractiveResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            InteractiveResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.InteractiveResponseMessage";
            };

            InteractiveResponseMessage.Body = (function() {

                const Body = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Body.prototype.text = null;
                Body.prototype.format = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Body.prototype, "_text", {
                    get: $util.oneOfGetter($oneOfFields = ["text"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Body.prototype, "_format", {
                    get: $util.oneOfGetter($oneOfFields = ["format"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Body.create = function(properties) {
                    return new Body(properties);
                };

                Body.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                        w.uint32(10).string(m.text);
                    if (m.format != null && $Object.hasOwnProperty.call(m, "format"))
                        w.uint32(16).int32(m.format);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Body.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveResponseMessage.Body(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.text = r.stringVerify();
                                m._text = "text";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.format = r.int32();
                                m._format = "format";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Body.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveResponseMessage.Body)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveResponseMessage.Body: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveResponseMessage.Body();
                    if (d.text != null) {
                        m.text = $String(d.text);
                    }
                    switch (d.format) {
                    case "DEFAULT":
                    case 0:
                        m.format = 0;
                        break;
                    case "EXTENSIONS_1":
                    case 1:
                        m.format = 1;
                        break;
                    default:
                        if (typeof d.format === "number" && (d.format | 0) === d.format)
                            m.format = d.format;
                    }
                    return m;
                };

                Body.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                        d.text = m.text;
                    }
                    if (m.format != null && $Object.hasOwnProperty.call(m, "format")) {
                        d.format = o.enums === $String ? $root.proto.Message.InteractiveResponseMessage.Body.Format[m.format] === $undefined ? m.format : $root.proto.Message.InteractiveResponseMessage.Body.Format[m.format] : m.format;
                    }
                    return d;
                };

                Body.prototype.toJSON = function() {
                    return Body.toObject(this, $protobuf.util.toJSONOptions);
                };

                Body.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveResponseMessage.Body";
                };

                Body.Format = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "DEFAULT"] = 0;
                    values[valuesById[1] = "EXTENSIONS_1"] = 1;
                    return values;
                })();

                return Body;
            })();

            InteractiveResponseMessage.NativeFlowResponseMessage = (function() {

                const NativeFlowResponseMessage = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                NativeFlowResponseMessage.prototype.name = null;
                NativeFlowResponseMessage.prototype.paramsJson = null;
                NativeFlowResponseMessage.prototype.version = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(NativeFlowResponseMessage.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(NativeFlowResponseMessage.prototype, "_paramsJson", {
                    get: $util.oneOfGetter($oneOfFields = ["paramsJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(NativeFlowResponseMessage.prototype, "_version", {
                    get: $util.oneOfGetter($oneOfFields = ["version"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                NativeFlowResponseMessage.create = function(properties) {
                    return new NativeFlowResponseMessage(properties);
                };

                NativeFlowResponseMessage.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                        w.uint32(10).string(m.name);
                    if (m.paramsJson != null && $Object.hasOwnProperty.call(m, "paramsJson"))
                        w.uint32(18).string(m.paramsJson);
                    if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                        w.uint32(24).int32(m.version);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                NativeFlowResponseMessage.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.name = r.stringVerify();
                                m._name = "name";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.paramsJson = r.stringVerify();
                                m._paramsJson = "paramsJson";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.version = r.int32();
                                m._version = "version";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                NativeFlowResponseMessage.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage();
                    if (d.name != null) {
                        m.name = $String(d.name);
                    }
                    if (d.paramsJson != null) {
                        m.paramsJson = $String(d.paramsJson);
                    }
                    if (d.version != null) {
                        m.version = d.version | 0;
                    }
                    return m;
                };

                NativeFlowResponseMessage.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                        d.name = m.name;
                    }
                    if (m.paramsJson != null && $Object.hasOwnProperty.call(m, "paramsJson")) {
                        d.paramsJson = m.paramsJson;
                    }
                    if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                        d.version = m.version;
                    }
                    return d;
                };

                NativeFlowResponseMessage.prototype.toJSON = function() {
                    return NativeFlowResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
                };

                NativeFlowResponseMessage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.InteractiveResponseMessage.NativeFlowResponseMessage";
                };

                return NativeFlowResponseMessage;
            })();

            return InteractiveResponseMessage;
        })();

        Message.InvoiceMessage = (function() {

            const InvoiceMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InvoiceMessage.prototype.note = null;
            InvoiceMessage.prototype.token = null;
            InvoiceMessage.prototype.attachmentType = null;
            InvoiceMessage.prototype.attachmentMimetype = null;
            InvoiceMessage.prototype.attachmentMediaKey = null;
            InvoiceMessage.prototype.attachmentMediaKeyTimestamp = null;
            InvoiceMessage.prototype.attachmentFileSha256 = null;
            InvoiceMessage.prototype.attachmentFileEncSha256 = null;
            InvoiceMessage.prototype.attachmentDirectPath = null;
            InvoiceMessage.prototype.attachmentJpegThumbnail = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_note", {
                get: $util.oneOfGetter($oneOfFields = ["note"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_token", {
                get: $util.oneOfGetter($oneOfFields = ["token"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentType", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentMimetype", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentMimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentMediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentMediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentMediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentMediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentFileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentFileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentFileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentFileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InvoiceMessage.prototype, "_attachmentJpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["attachmentJpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InvoiceMessage.create = function(properties) {
                return new InvoiceMessage(properties);
            };

            InvoiceMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.note != null && $Object.hasOwnProperty.call(m, "note"))
                    w.uint32(10).string(m.note);
                if (m.token != null && $Object.hasOwnProperty.call(m, "token"))
                    w.uint32(18).string(m.token);
                if (m.attachmentType != null && $Object.hasOwnProperty.call(m, "attachmentType"))
                    w.uint32(24).int32(m.attachmentType);
                if (m.attachmentMimetype != null && $Object.hasOwnProperty.call(m, "attachmentMimetype"))
                    w.uint32(34).string(m.attachmentMimetype);
                if (m.attachmentMediaKey != null && $Object.hasOwnProperty.call(m, "attachmentMediaKey"))
                    w.uint32(42).bytes(m.attachmentMediaKey);
                if (m.attachmentMediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "attachmentMediaKeyTimestamp"))
                    w.uint32(48).int64(m.attachmentMediaKeyTimestamp);
                if (m.attachmentFileSha256 != null && $Object.hasOwnProperty.call(m, "attachmentFileSha256"))
                    w.uint32(58).bytes(m.attachmentFileSha256);
                if (m.attachmentFileEncSha256 != null && $Object.hasOwnProperty.call(m, "attachmentFileEncSha256"))
                    w.uint32(66).bytes(m.attachmentFileEncSha256);
                if (m.attachmentDirectPath != null && $Object.hasOwnProperty.call(m, "attachmentDirectPath"))
                    w.uint32(74).string(m.attachmentDirectPath);
                if (m.attachmentJpegThumbnail != null && $Object.hasOwnProperty.call(m, "attachmentJpegThumbnail"))
                    w.uint32(82).bytes(m.attachmentJpegThumbnail);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InvoiceMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.InvoiceMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.note = r.stringVerify();
                            m._note = "note";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.token = r.stringVerify();
                            m._token = "token";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.attachmentType = r.int32();
                            m._attachmentType = "attachmentType";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.attachmentMimetype = r.stringVerify();
                            m._attachmentMimetype = "attachmentMimetype";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.attachmentMediaKey = r.bytes();
                            m._attachmentMediaKey = "attachmentMediaKey";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.attachmentMediaKeyTimestamp = r.int64();
                            m._attachmentMediaKeyTimestamp = "attachmentMediaKeyTimestamp";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.attachmentFileSha256 = r.bytes();
                            m._attachmentFileSha256 = "attachmentFileSha256";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.attachmentFileEncSha256 = r.bytes();
                            m._attachmentFileEncSha256 = "attachmentFileEncSha256";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.attachmentDirectPath = r.stringVerify();
                            m._attachmentDirectPath = "attachmentDirectPath";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.attachmentJpegThumbnail = r.bytes();
                            m._attachmentJpegThumbnail = "attachmentJpegThumbnail";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            InvoiceMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.InvoiceMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.InvoiceMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.InvoiceMessage();
                if (d.note != null) {
                    m.note = $String(d.note);
                }
                if (d.token != null) {
                    m.token = $String(d.token);
                }
                switch (d.attachmentType) {
                case "IMAGE":
                case 0:
                    m.attachmentType = 0;
                    break;
                case "PDF":
                case 1:
                    m.attachmentType = 1;
                    break;
                default:
                    if (typeof d.attachmentType === "number" && (d.attachmentType | 0) === d.attachmentType)
                        m.attachmentType = d.attachmentType;
                }
                if (d.attachmentMimetype != null) {
                    m.attachmentMimetype = $String(d.attachmentMimetype);
                }
                if (d.attachmentMediaKey != null) {
                    if (typeof d.attachmentMediaKey === "string")
                        $util.base64.decode(d.attachmentMediaKey, m.attachmentMediaKey = $util.newBuffer($util.base64.length(d.attachmentMediaKey)), 0);
                    else if (d.attachmentMediaKey.length >= 0)
                        m.attachmentMediaKey = d.attachmentMediaKey;
                }
                if (d.attachmentMediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.attachmentMediaKeyTimestamp = $util.Long.fromValue(d.attachmentMediaKeyTimestamp, false);
                    else if (typeof d.attachmentMediaKeyTimestamp === "string")
                        m.attachmentMediaKeyTimestamp = $parseInt(d.attachmentMediaKeyTimestamp, 10);
                    else if (typeof d.attachmentMediaKeyTimestamp === "number")
                        m.attachmentMediaKeyTimestamp = d.attachmentMediaKeyTimestamp;
                    else if (typeof d.attachmentMediaKeyTimestamp === "object")
                        m.attachmentMediaKeyTimestamp = new $util.LongBits(d.attachmentMediaKeyTimestamp.low >>> 0, d.attachmentMediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.attachmentFileSha256 != null) {
                    if (typeof d.attachmentFileSha256 === "string")
                        $util.base64.decode(d.attachmentFileSha256, m.attachmentFileSha256 = $util.newBuffer($util.base64.length(d.attachmentFileSha256)), 0);
                    else if (d.attachmentFileSha256.length >= 0)
                        m.attachmentFileSha256 = d.attachmentFileSha256;
                }
                if (d.attachmentFileEncSha256 != null) {
                    if (typeof d.attachmentFileEncSha256 === "string")
                        $util.base64.decode(d.attachmentFileEncSha256, m.attachmentFileEncSha256 = $util.newBuffer($util.base64.length(d.attachmentFileEncSha256)), 0);
                    else if (d.attachmentFileEncSha256.length >= 0)
                        m.attachmentFileEncSha256 = d.attachmentFileEncSha256;
                }
                if (d.attachmentDirectPath != null) {
                    m.attachmentDirectPath = $String(d.attachmentDirectPath);
                }
                if (d.attachmentJpegThumbnail != null) {
                    if (typeof d.attachmentJpegThumbnail === "string")
                        $util.base64.decode(d.attachmentJpegThumbnail, m.attachmentJpegThumbnail = $util.newBuffer($util.base64.length(d.attachmentJpegThumbnail)), 0);
                    else if (d.attachmentJpegThumbnail.length >= 0)
                        m.attachmentJpegThumbnail = d.attachmentJpegThumbnail;
                }
                return m;
            };

            InvoiceMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.note != null && $Object.hasOwnProperty.call(m, "note")) {
                    d.note = m.note;
                }
                if (m.token != null && $Object.hasOwnProperty.call(m, "token")) {
                    d.token = m.token;
                }
                if (m.attachmentType != null && $Object.hasOwnProperty.call(m, "attachmentType")) {
                    d.attachmentType = o.enums === $String ? $root.proto.Message.InvoiceMessage.AttachmentType[m.attachmentType] === $undefined ? m.attachmentType : $root.proto.Message.InvoiceMessage.AttachmentType[m.attachmentType] : m.attachmentType;
                }
                if (m.attachmentMimetype != null && $Object.hasOwnProperty.call(m, "attachmentMimetype")) {
                    d.attachmentMimetype = m.attachmentMimetype;
                }
                if (m.attachmentMediaKey != null && $Object.hasOwnProperty.call(m, "attachmentMediaKey")) {
                    d.attachmentMediaKey = o.bytes === $String ? $util.base64.encode(m.attachmentMediaKey, 0, m.attachmentMediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.attachmentMediaKey) : m.attachmentMediaKey;
                }
                if (m.attachmentMediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "attachmentMediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.attachmentMediaKeyTimestamp = typeof m.attachmentMediaKeyTimestamp === "number" ? $BigInt(m.attachmentMediaKeyTimestamp) : $util.Long.fromBits(m.attachmentMediaKeyTimestamp.low >>> 0, m.attachmentMediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.attachmentMediaKeyTimestamp === "number")
                        d.attachmentMediaKeyTimestamp = o.longs === $String ? $String(m.attachmentMediaKeyTimestamp) : m.attachmentMediaKeyTimestamp;
                    else
                        d.attachmentMediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.attachmentMediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.attachmentMediaKeyTimestamp.low >>> 0, m.attachmentMediaKeyTimestamp.high >>> 0).toNumber() : m.attachmentMediaKeyTimestamp;
                }
                if (m.attachmentFileSha256 != null && $Object.hasOwnProperty.call(m, "attachmentFileSha256")) {
                    d.attachmentFileSha256 = o.bytes === $String ? $util.base64.encode(m.attachmentFileSha256, 0, m.attachmentFileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.attachmentFileSha256) : m.attachmentFileSha256;
                }
                if (m.attachmentFileEncSha256 != null && $Object.hasOwnProperty.call(m, "attachmentFileEncSha256")) {
                    d.attachmentFileEncSha256 = o.bytes === $String ? $util.base64.encode(m.attachmentFileEncSha256, 0, m.attachmentFileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.attachmentFileEncSha256) : m.attachmentFileEncSha256;
                }
                if (m.attachmentDirectPath != null && $Object.hasOwnProperty.call(m, "attachmentDirectPath")) {
                    d.attachmentDirectPath = m.attachmentDirectPath;
                }
                if (m.attachmentJpegThumbnail != null && $Object.hasOwnProperty.call(m, "attachmentJpegThumbnail")) {
                    d.attachmentJpegThumbnail = o.bytes === $String ? $util.base64.encode(m.attachmentJpegThumbnail, 0, m.attachmentJpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.attachmentJpegThumbnail) : m.attachmentJpegThumbnail;
                }
                return d;
            };

            InvoiceMessage.prototype.toJSON = function() {
                return InvoiceMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            InvoiceMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.InvoiceMessage";
            };

            InvoiceMessage.AttachmentType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "IMAGE"] = 0;
                values[valuesById[1] = "PDF"] = 1;
                return values;
            })();

            return InvoiceMessage;
        })();

        Message.KeepInChatMessage = (function() {

            const KeepInChatMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            KeepInChatMessage.prototype.key = null;
            KeepInChatMessage.prototype.keepType = null;
            KeepInChatMessage.prototype.timestampMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(KeepInChatMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(KeepInChatMessage.prototype, "_keepType", {
                get: $util.oneOfGetter($oneOfFields = ["keepType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(KeepInChatMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            KeepInChatMessage.create = function(properties) {
                return new KeepInChatMessage(properties);
            };

            KeepInChatMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.keepType != null && $Object.hasOwnProperty.call(m, "keepType"))
                    w.uint32(16).int32(m.keepType);
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                    w.uint32(24).int64(m.timestampMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            KeepInChatMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.KeepInChatMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.keepType = r.int32();
                            m._keepType = "keepType";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.timestampMs = r.int64();
                            m._timestampMs = "timestampMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            KeepInChatMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.KeepInChatMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.KeepInChatMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.KeepInChatMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.KeepInChatMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                switch (d.keepType) {
                case "UNKNOWN":
                case 0:
                    m.keepType = 0;
                    break;
                case "KEEP_FOR_ALL":
                case 1:
                    m.keepType = 1;
                    break;
                case "UNDO_KEEP_FOR_ALL":
                case 2:
                    m.keepType = 2;
                    break;
                default:
                    if (typeof d.keepType === "number" && (d.keepType | 0) === d.keepType)
                        m.keepType = d.keepType;
                }
                if (d.timestampMs != null) {
                    if ($util.Long)
                        m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                    else if (typeof d.timestampMs === "string")
                        m.timestampMs = $parseInt(d.timestampMs, 10);
                    else if (typeof d.timestampMs === "number")
                        m.timestampMs = d.timestampMs;
                    else if (typeof d.timestampMs === "object")
                        m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
                }
                return m;
            };

            KeepInChatMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.keepType != null && $Object.hasOwnProperty.call(m, "keepType")) {
                    d.keepType = o.enums === $String ? $root.proto.KeepType[m.keepType] === $undefined ? m.keepType : $root.proto.KeepType[m.keepType] : m.keepType;
                }
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.timestampMs === "number")
                        d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                    else
                        d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
                }
                return d;
            };

            KeepInChatMessage.prototype.toJSON = function() {
                return KeepInChatMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            KeepInChatMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.KeepInChatMessage";
            };

            return KeepInChatMessage;
        })();

        Message.LinkPreviewMetadata = (function() {

            const LinkPreviewMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LinkPreviewMetadata.prototype.paymentLinkMetadata = null;
            LinkPreviewMetadata.prototype.urlMetadata = null;
            LinkPreviewMetadata.prototype.fbExperimentId = null;
            LinkPreviewMetadata.prototype.linkMediaDuration = null;
            LinkPreviewMetadata.prototype.socialMediaPostType = null;
            LinkPreviewMetadata.prototype.linkInlineVideoMuted = null;
            LinkPreviewMetadata.prototype.videoContentUrl = null;
            LinkPreviewMetadata.prototype.musicMetadata = null;
            LinkPreviewMetadata.prototype.videoContentCaption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_paymentLinkMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["paymentLinkMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_urlMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["urlMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_fbExperimentId", {
                get: $util.oneOfGetter($oneOfFields = ["fbExperimentId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_linkMediaDuration", {
                get: $util.oneOfGetter($oneOfFields = ["linkMediaDuration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_socialMediaPostType", {
                get: $util.oneOfGetter($oneOfFields = ["socialMediaPostType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_linkInlineVideoMuted", {
                get: $util.oneOfGetter($oneOfFields = ["linkInlineVideoMuted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_videoContentUrl", {
                get: $util.oneOfGetter($oneOfFields = ["videoContentUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_musicMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["musicMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LinkPreviewMetadata.prototype, "_videoContentCaption", {
                get: $util.oneOfGetter($oneOfFields = ["videoContentCaption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LinkPreviewMetadata.create = function(properties) {
                return new LinkPreviewMetadata(properties);
            };

            LinkPreviewMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.paymentLinkMetadata != null && $Object.hasOwnProperty.call(m, "paymentLinkMetadata"))
                    $root.proto.Message.PaymentLinkMetadata.encode(m.paymentLinkMetadata, w.uint32(10).fork(), q + 1).ldelim();
                if (m.urlMetadata != null && $Object.hasOwnProperty.call(m, "urlMetadata"))
                    $root.proto.Message.URLMetadata.encode(m.urlMetadata, w.uint32(18).fork(), q + 1).ldelim();
                if (m.fbExperimentId != null && $Object.hasOwnProperty.call(m, "fbExperimentId"))
                    w.uint32(24).uint32(m.fbExperimentId);
                if (m.linkMediaDuration != null && $Object.hasOwnProperty.call(m, "linkMediaDuration"))
                    w.uint32(32).uint32(m.linkMediaDuration);
                if (m.socialMediaPostType != null && $Object.hasOwnProperty.call(m, "socialMediaPostType"))
                    w.uint32(40).int32(m.socialMediaPostType);
                if (m.linkInlineVideoMuted != null && $Object.hasOwnProperty.call(m, "linkInlineVideoMuted"))
                    w.uint32(48).bool(m.linkInlineVideoMuted);
                if (m.videoContentUrl != null && $Object.hasOwnProperty.call(m, "videoContentUrl"))
                    w.uint32(58).string(m.videoContentUrl);
                if (m.musicMetadata != null && $Object.hasOwnProperty.call(m, "musicMetadata"))
                    $root.proto.EmbeddedMusic.encode(m.musicMetadata, w.uint32(66).fork(), q + 1).ldelim();
                if (m.videoContentCaption != null && $Object.hasOwnProperty.call(m, "videoContentCaption"))
                    w.uint32(74).string(m.videoContentCaption);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LinkPreviewMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.LinkPreviewMetadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.decode(r, r.uint32(), $undefined, q + 1, m.paymentLinkMetadata);
                            m._paymentLinkMetadata = "paymentLinkMetadata";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.urlMetadata = $root.proto.Message.URLMetadata.decode(r, r.uint32(), $undefined, q + 1, m.urlMetadata);
                            m._urlMetadata = "urlMetadata";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.fbExperimentId = r.uint32();
                            m._fbExperimentId = "fbExperimentId";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.linkMediaDuration = r.uint32();
                            m._linkMediaDuration = "linkMediaDuration";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.socialMediaPostType = r.int32();
                            m._socialMediaPostType = "socialMediaPostType";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.linkInlineVideoMuted = r.bool();
                            m._linkInlineVideoMuted = "linkInlineVideoMuted";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.videoContentUrl = r.stringVerify();
                            m._videoContentUrl = "videoContentUrl";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.musicMetadata = $root.proto.EmbeddedMusic.decode(r, r.uint32(), $undefined, q + 1, m.musicMetadata);
                            m._musicMetadata = "musicMetadata";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.videoContentCaption = r.stringVerify();
                            m._videoContentCaption = "videoContentCaption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LinkPreviewMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.LinkPreviewMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.LinkPreviewMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.LinkPreviewMetadata();
                if (d.paymentLinkMetadata != null) {
                    if (!$util.isObject(d.paymentLinkMetadata))
                        throw $TypeError(".proto.Message.LinkPreviewMetadata.paymentLinkMetadata: object expected");
                    m.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.fromObject(d.paymentLinkMetadata, q + 1);
                }
                if (d.urlMetadata != null) {
                    if (!$util.isObject(d.urlMetadata))
                        throw $TypeError(".proto.Message.LinkPreviewMetadata.urlMetadata: object expected");
                    m.urlMetadata = $root.proto.Message.URLMetadata.fromObject(d.urlMetadata, q + 1);
                }
                if (d.fbExperimentId != null) {
                    m.fbExperimentId = d.fbExperimentId >>> 0;
                }
                if (d.linkMediaDuration != null) {
                    m.linkMediaDuration = d.linkMediaDuration >>> 0;
                }
                switch (d.socialMediaPostType) {
                case "NONE":
                case 0:
                    m.socialMediaPostType = 0;
                    break;
                case "REEL":
                case 1:
                    m.socialMediaPostType = 1;
                    break;
                case "LIVE_VIDEO":
                case 2:
                    m.socialMediaPostType = 2;
                    break;
                case "LONG_VIDEO":
                case 3:
                    m.socialMediaPostType = 3;
                    break;
                case "SINGLE_IMAGE":
                case 4:
                    m.socialMediaPostType = 4;
                    break;
                case "CAROUSEL":
                case 5:
                    m.socialMediaPostType = 5;
                    break;
                default:
                    if (typeof d.socialMediaPostType === "number" && (d.socialMediaPostType | 0) === d.socialMediaPostType)
                        m.socialMediaPostType = d.socialMediaPostType;
                }
                if (d.linkInlineVideoMuted != null) {
                    m.linkInlineVideoMuted = $Boolean(d.linkInlineVideoMuted);
                }
                if (d.videoContentUrl != null) {
                    m.videoContentUrl = $String(d.videoContentUrl);
                }
                if (d.musicMetadata != null) {
                    if (!$util.isObject(d.musicMetadata))
                        throw $TypeError(".proto.Message.LinkPreviewMetadata.musicMetadata: object expected");
                    m.musicMetadata = $root.proto.EmbeddedMusic.fromObject(d.musicMetadata, q + 1);
                }
                if (d.videoContentCaption != null) {
                    m.videoContentCaption = $String(d.videoContentCaption);
                }
                return m;
            };

            LinkPreviewMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.paymentLinkMetadata != null && $Object.hasOwnProperty.call(m, "paymentLinkMetadata")) {
                    d.paymentLinkMetadata = $root.proto.Message.PaymentLinkMetadata.toObject(m.paymentLinkMetadata, o, q + 1);
                }
                if (m.urlMetadata != null && $Object.hasOwnProperty.call(m, "urlMetadata")) {
                    d.urlMetadata = $root.proto.Message.URLMetadata.toObject(m.urlMetadata, o, q + 1);
                }
                if (m.fbExperimentId != null && $Object.hasOwnProperty.call(m, "fbExperimentId")) {
                    d.fbExperimentId = m.fbExperimentId;
                }
                if (m.linkMediaDuration != null && $Object.hasOwnProperty.call(m, "linkMediaDuration")) {
                    d.linkMediaDuration = m.linkMediaDuration;
                }
                if (m.socialMediaPostType != null && $Object.hasOwnProperty.call(m, "socialMediaPostType")) {
                    d.socialMediaPostType = o.enums === $String ? $root.proto.Message.LinkPreviewMetadata.SocialMediaPostType[m.socialMediaPostType] === $undefined ? m.socialMediaPostType : $root.proto.Message.LinkPreviewMetadata.SocialMediaPostType[m.socialMediaPostType] : m.socialMediaPostType;
                }
                if (m.linkInlineVideoMuted != null && $Object.hasOwnProperty.call(m, "linkInlineVideoMuted")) {
                    d.linkInlineVideoMuted = m.linkInlineVideoMuted;
                }
                if (m.videoContentUrl != null && $Object.hasOwnProperty.call(m, "videoContentUrl")) {
                    d.videoContentUrl = m.videoContentUrl;
                }
                if (m.musicMetadata != null && $Object.hasOwnProperty.call(m, "musicMetadata")) {
                    d.musicMetadata = $root.proto.EmbeddedMusic.toObject(m.musicMetadata, o, q + 1);
                }
                if (m.videoContentCaption != null && $Object.hasOwnProperty.call(m, "videoContentCaption")) {
                    d.videoContentCaption = m.videoContentCaption;
                }
                return d;
            };

            LinkPreviewMetadata.prototype.toJSON = function() {
                return LinkPreviewMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            LinkPreviewMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.LinkPreviewMetadata";
            };

            LinkPreviewMetadata.SocialMediaPostType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "REEL"] = 1;
                values[valuesById[2] = "LIVE_VIDEO"] = 2;
                values[valuesById[3] = "LONG_VIDEO"] = 3;
                values[valuesById[4] = "SINGLE_IMAGE"] = 4;
                values[valuesById[5] = "CAROUSEL"] = 5;
                return values;
            })();

            return LinkPreviewMetadata;
        })();

        Message.ListMessage = (function() {

            const ListMessage = function (p) {
                this.sections = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ListMessage.prototype.title = null;
            ListMessage.prototype.description = null;
            ListMessage.prototype.buttonText = null;
            ListMessage.prototype.listType = null;
            ListMessage.prototype.sections = $util.emptyArray;
            ListMessage.prototype.productListInfo = null;
            ListMessage.prototype.footerText = null;
            ListMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_buttonText", {
                get: $util.oneOfGetter($oneOfFields = ["buttonText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_listType", {
                get: $util.oneOfGetter($oneOfFields = ["listType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_productListInfo", {
                get: $util.oneOfGetter($oneOfFields = ["productListInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_footerText", {
                get: $util.oneOfGetter($oneOfFields = ["footerText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ListMessage.create = function(properties) {
                return new ListMessage(properties);
            };

            ListMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(18).string(m.description);
                if (m.buttonText != null && $Object.hasOwnProperty.call(m, "buttonText"))
                    w.uint32(26).string(m.buttonText);
                if (m.listType != null && $Object.hasOwnProperty.call(m, "listType"))
                    w.uint32(32).int32(m.listType);
                if (m.sections != null && m.sections.length) {
                    for (var i = 0; i < m.sections.length; ++i)
                        $root.proto.Message.ListMessage.Section.encode(m.sections[i], w.uint32(42).fork(), q + 1).ldelim();
                }
                if (m.productListInfo != null && $Object.hasOwnProperty.call(m, "productListInfo"))
                    $root.proto.Message.ListMessage.ProductListInfo.encode(m.productListInfo, w.uint32(50).fork(), q + 1).ldelim();
                if (m.footerText != null && $Object.hasOwnProperty.call(m, "footerText"))
                    w.uint32(58).string(m.footerText);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(66).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ListMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.buttonText = r.stringVerify();
                            m._buttonText = "buttonText";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.listType = r.int32();
                            m._listType = "listType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(m.sections && m.sections.length))
                                m.sections = [];
                            m.sections.push($root.proto.Message.ListMessage.Section.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.productListInfo = $root.proto.Message.ListMessage.ProductListInfo.decode(r, r.uint32(), $undefined, q + 1, m.productListInfo);
                            m._productListInfo = "productListInfo";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.footerText = r.stringVerify();
                            m._footerText = "footerText";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ListMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ListMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ListMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ListMessage();
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                if (d.buttonText != null) {
                    m.buttonText = $String(d.buttonText);
                }
                switch (d.listType) {
                case "UNKNOWN":
                case 0:
                    m.listType = 0;
                    break;
                case "SINGLE_SELECT":
                case 1:
                    m.listType = 1;
                    break;
                case "PRODUCT_LIST":
                case 2:
                    m.listType = 2;
                    break;
                default:
                    if (typeof d.listType === "number" && (d.listType | 0) === d.listType)
                        m.listType = d.listType;
                }
                if (d.sections) {
                    if (!$Array.isArray(d.sections))
                        throw $TypeError(".proto.Message.ListMessage.sections: array expected");
                    m.sections = $Array(d.sections.length);
                    for (var i = 0; i < d.sections.length; ++i) {
                        if (!$util.isObject(d.sections[i]))
                            throw $TypeError(".proto.Message.ListMessage.sections: object expected");
                        m.sections[i] = $root.proto.Message.ListMessage.Section.fromObject(d.sections[i], q + 1);
                    }
                }
                if (d.productListInfo != null) {
                    if (!$util.isObject(d.productListInfo))
                        throw $TypeError(".proto.Message.ListMessage.productListInfo: object expected");
                    m.productListInfo = $root.proto.Message.ListMessage.ProductListInfo.fromObject(d.productListInfo, q + 1);
                }
                if (d.footerText != null) {
                    m.footerText = $String(d.footerText);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ListMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            ListMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.sections = [];
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                if (m.buttonText != null && $Object.hasOwnProperty.call(m, "buttonText")) {
                    d.buttonText = m.buttonText;
                }
                if (m.listType != null && $Object.hasOwnProperty.call(m, "listType")) {
                    d.listType = o.enums === $String ? $root.proto.Message.ListMessage.ListType[m.listType] === $undefined ? m.listType : $root.proto.Message.ListMessage.ListType[m.listType] : m.listType;
                }
                if (m.sections && m.sections.length) {
                    d.sections = $Array(m.sections.length);
                    for (var j = 0; j < m.sections.length; ++j) {
                        d.sections[j] = $root.proto.Message.ListMessage.Section.toObject(m.sections[j], o, q + 1);
                    }
                }
                if (m.productListInfo != null && $Object.hasOwnProperty.call(m, "productListInfo")) {
                    d.productListInfo = $root.proto.Message.ListMessage.ProductListInfo.toObject(m.productListInfo, o, q + 1);
                }
                if (m.footerText != null && $Object.hasOwnProperty.call(m, "footerText")) {
                    d.footerText = m.footerText;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            ListMessage.prototype.toJSON = function() {
                return ListMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ListMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ListMessage";
            };

            ListMessage.ListType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "SINGLE_SELECT"] = 1;
                values[valuesById[2] = "PRODUCT_LIST"] = 2;
                return values;
            })();

            ListMessage.Product = (function() {

                const Product = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Product.prototype.productId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Product.prototype, "_productId", {
                    get: $util.oneOfGetter($oneOfFields = ["productId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Product.create = function(properties) {
                    return new Product(properties);
                };

                Product.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.productId != null && $Object.hasOwnProperty.call(m, "productId"))
                        w.uint32(10).string(m.productId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Product.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage.Product();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.productId = r.stringVerify();
                                m._productId = "productId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Product.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListMessage.Product)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListMessage.Product: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListMessage.Product();
                    if (d.productId != null) {
                        m.productId = $String(d.productId);
                    }
                    return m;
                };

                Product.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.productId != null && $Object.hasOwnProperty.call(m, "productId")) {
                        d.productId = m.productId;
                    }
                    return d;
                };

                Product.prototype.toJSON = function() {
                    return Product.toObject(this, $protobuf.util.toJSONOptions);
                };

                Product.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListMessage.Product";
                };

                return Product;
            })();

            ListMessage.ProductListHeaderImage = (function() {

                const ProductListHeaderImage = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                ProductListHeaderImage.prototype.productId = null;
                ProductListHeaderImage.prototype.jpegThumbnail = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductListHeaderImage.prototype, "_productId", {
                    get: $util.oneOfGetter($oneOfFields = ["productId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductListHeaderImage.prototype, "_jpegThumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ProductListHeaderImage.create = function(properties) {
                    return new ProductListHeaderImage(properties);
                };

                ProductListHeaderImage.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.productId != null && $Object.hasOwnProperty.call(m, "productId"))
                        w.uint32(10).string(m.productId);
                    if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                        w.uint32(18).bytes(m.jpegThumbnail);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                ProductListHeaderImage.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage.ProductListHeaderImage();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.productId = r.stringVerify();
                                m._productId = "productId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.jpegThumbnail = r.bytes();
                                m._jpegThumbnail = "jpegThumbnail";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                ProductListHeaderImage.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListMessage.ProductListHeaderImage)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListMessage.ProductListHeaderImage: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListMessage.ProductListHeaderImage();
                    if (d.productId != null) {
                        m.productId = $String(d.productId);
                    }
                    if (d.jpegThumbnail != null) {
                        if (typeof d.jpegThumbnail === "string")
                            $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                        else if (d.jpegThumbnail.length >= 0)
                            m.jpegThumbnail = d.jpegThumbnail;
                    }
                    return m;
                };

                ProductListHeaderImage.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.productId != null && $Object.hasOwnProperty.call(m, "productId")) {
                        d.productId = m.productId;
                    }
                    if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                        d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                    }
                    return d;
                };

                ProductListHeaderImage.prototype.toJSON = function() {
                    return ProductListHeaderImage.toObject(this, $protobuf.util.toJSONOptions);
                };

                ProductListHeaderImage.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListMessage.ProductListHeaderImage";
                };

                return ProductListHeaderImage;
            })();

            ListMessage.ProductListInfo = (function() {

                const ProductListInfo = function (p) {
                    this.productSections = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                ProductListInfo.prototype.productSections = $util.emptyArray;
                ProductListInfo.prototype.headerImage = null;
                ProductListInfo.prototype.businessOwnerJid = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductListInfo.prototype, "_headerImage", {
                    get: $util.oneOfGetter($oneOfFields = ["headerImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductListInfo.prototype, "_businessOwnerJid", {
                    get: $util.oneOfGetter($oneOfFields = ["businessOwnerJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ProductListInfo.create = function(properties) {
                    return new ProductListInfo(properties);
                };

                ProductListInfo.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.productSections != null && m.productSections.length) {
                        for (var i = 0; i < m.productSections.length; ++i)
                            $root.proto.Message.ListMessage.ProductSection.encode(m.productSections[i], w.uint32(10).fork(), q + 1).ldelim();
                    }
                    if (m.headerImage != null && $Object.hasOwnProperty.call(m, "headerImage"))
                        $root.proto.Message.ListMessage.ProductListHeaderImage.encode(m.headerImage, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.businessOwnerJid != null && $Object.hasOwnProperty.call(m, "businessOwnerJid"))
                        w.uint32(26).string(m.businessOwnerJid);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                ProductListInfo.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage.ProductListInfo();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                if (!(m.productSections && m.productSections.length))
                                    m.productSections = [];
                                m.productSections.push($root.proto.Message.ListMessage.ProductSection.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.headerImage = $root.proto.Message.ListMessage.ProductListHeaderImage.decode(r, r.uint32(), $undefined, q + 1, m.headerImage);
                                m._headerImage = "headerImage";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.businessOwnerJid = r.stringVerify();
                                m._businessOwnerJid = "businessOwnerJid";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                ProductListInfo.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListMessage.ProductListInfo)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListMessage.ProductListInfo: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListMessage.ProductListInfo();
                    if (d.productSections) {
                        if (!$Array.isArray(d.productSections))
                            throw $TypeError(".proto.Message.ListMessage.ProductListInfo.productSections: array expected");
                        m.productSections = $Array(d.productSections.length);
                        for (var i = 0; i < d.productSections.length; ++i) {
                            if (!$util.isObject(d.productSections[i]))
                                throw $TypeError(".proto.Message.ListMessage.ProductListInfo.productSections: object expected");
                            m.productSections[i] = $root.proto.Message.ListMessage.ProductSection.fromObject(d.productSections[i], q + 1);
                        }
                    }
                    if (d.headerImage != null) {
                        if (!$util.isObject(d.headerImage))
                            throw $TypeError(".proto.Message.ListMessage.ProductListInfo.headerImage: object expected");
                        m.headerImage = $root.proto.Message.ListMessage.ProductListHeaderImage.fromObject(d.headerImage, q + 1);
                    }
                    if (d.businessOwnerJid != null) {
                        m.businessOwnerJid = $String(d.businessOwnerJid);
                    }
                    return m;
                };

                ProductListInfo.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.productSections = [];
                    }
                    if (m.productSections && m.productSections.length) {
                        d.productSections = $Array(m.productSections.length);
                        for (var j = 0; j < m.productSections.length; ++j) {
                            d.productSections[j] = $root.proto.Message.ListMessage.ProductSection.toObject(m.productSections[j], o, q + 1);
                        }
                    }
                    if (m.headerImage != null && $Object.hasOwnProperty.call(m, "headerImage")) {
                        d.headerImage = $root.proto.Message.ListMessage.ProductListHeaderImage.toObject(m.headerImage, o, q + 1);
                    }
                    if (m.businessOwnerJid != null && $Object.hasOwnProperty.call(m, "businessOwnerJid")) {
                        d.businessOwnerJid = m.businessOwnerJid;
                    }
                    return d;
                };

                ProductListInfo.prototype.toJSON = function() {
                    return ProductListInfo.toObject(this, $protobuf.util.toJSONOptions);
                };

                ProductListInfo.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListMessage.ProductListInfo";
                };

                return ProductListInfo;
            })();

            ListMessage.ProductSection = (function() {

                const ProductSection = function (p) {
                    this.products = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                ProductSection.prototype.title = null;
                ProductSection.prototype.products = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSection.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ProductSection.create = function(properties) {
                    return new ProductSection(properties);
                };

                ProductSection.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(10).string(m.title);
                    if (m.products != null && m.products.length) {
                        for (var i = 0; i < m.products.length; ++i)
                            $root.proto.Message.ListMessage.Product.encode(m.products[i], w.uint32(18).fork(), q + 1).ldelim();
                    }
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                ProductSection.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage.ProductSection();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                if (!(m.products && m.products.length))
                                    m.products = [];
                                m.products.push($root.proto.Message.ListMessage.Product.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                ProductSection.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListMessage.ProductSection)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListMessage.ProductSection: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListMessage.ProductSection();
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.products) {
                        if (!$Array.isArray(d.products))
                            throw $TypeError(".proto.Message.ListMessage.ProductSection.products: array expected");
                        m.products = $Array(d.products.length);
                        for (var i = 0; i < d.products.length; ++i) {
                            if (!$util.isObject(d.products[i]))
                                throw $TypeError(".proto.Message.ListMessage.ProductSection.products: object expected");
                            m.products[i] = $root.proto.Message.ListMessage.Product.fromObject(d.products[i], q + 1);
                        }
                    }
                    return m;
                };

                ProductSection.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.products = [];
                    }
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.products && m.products.length) {
                        d.products = $Array(m.products.length);
                        for (var j = 0; j < m.products.length; ++j) {
                            d.products[j] = $root.proto.Message.ListMessage.Product.toObject(m.products[j], o, q + 1);
                        }
                    }
                    return d;
                };

                ProductSection.prototype.toJSON = function() {
                    return ProductSection.toObject(this, $protobuf.util.toJSONOptions);
                };

                ProductSection.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListMessage.ProductSection";
                };

                return ProductSection;
            })();

            ListMessage.Row = (function() {

                const Row = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Row.prototype.title = null;
                Row.prototype.description = null;
                Row.prototype.rowId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Row.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Row.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Row.prototype, "_rowId", {
                    get: $util.oneOfGetter($oneOfFields = ["rowId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Row.create = function(properties) {
                    return new Row(properties);
                };

                Row.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(10).string(m.title);
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                        w.uint32(18).string(m.description);
                    if (m.rowId != null && $Object.hasOwnProperty.call(m, "rowId"))
                        w.uint32(26).string(m.rowId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Row.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage.Row();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.description = r.stringVerify();
                                m._description = "description";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.rowId = r.stringVerify();
                                m._rowId = "rowId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Row.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListMessage.Row)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListMessage.Row: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListMessage.Row();
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.description != null) {
                        m.description = $String(d.description);
                    }
                    if (d.rowId != null) {
                        m.rowId = $String(d.rowId);
                    }
                    return m;
                };

                Row.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                        d.description = m.description;
                    }
                    if (m.rowId != null && $Object.hasOwnProperty.call(m, "rowId")) {
                        d.rowId = m.rowId;
                    }
                    return d;
                };

                Row.prototype.toJSON = function() {
                    return Row.toObject(this, $protobuf.util.toJSONOptions);
                };

                Row.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListMessage.Row";
                };

                return Row;
            })();

            ListMessage.Section = (function() {

                const Section = function (p) {
                    this.rows = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Section.prototype.title = null;
                Section.prototype.rows = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Section.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Section.create = function(properties) {
                    return new Section(properties);
                };

                Section.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(10).string(m.title);
                    if (m.rows != null && m.rows.length) {
                        for (var i = 0; i < m.rows.length; ++i)
                            $root.proto.Message.ListMessage.Row.encode(m.rows[i], w.uint32(18).fork(), q + 1).ldelim();
                    }
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Section.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListMessage.Section();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                if (!(m.rows && m.rows.length))
                                    m.rows = [];
                                m.rows.push($root.proto.Message.ListMessage.Row.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Section.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListMessage.Section)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListMessage.Section: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListMessage.Section();
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.rows) {
                        if (!$Array.isArray(d.rows))
                            throw $TypeError(".proto.Message.ListMessage.Section.rows: array expected");
                        m.rows = $Array(d.rows.length);
                        for (var i = 0; i < d.rows.length; ++i) {
                            if (!$util.isObject(d.rows[i]))
                                throw $TypeError(".proto.Message.ListMessage.Section.rows: object expected");
                            m.rows[i] = $root.proto.Message.ListMessage.Row.fromObject(d.rows[i], q + 1);
                        }
                    }
                    return m;
                };

                Section.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.rows = [];
                    }
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.rows && m.rows.length) {
                        d.rows = $Array(m.rows.length);
                        for (var j = 0; j < m.rows.length; ++j) {
                            d.rows[j] = $root.proto.Message.ListMessage.Row.toObject(m.rows[j], o, q + 1);
                        }
                    }
                    return d;
                };

                Section.prototype.toJSON = function() {
                    return Section.toObject(this, $protobuf.util.toJSONOptions);
                };

                Section.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListMessage.Section";
                };

                return Section;
            })();

            return ListMessage;
        })();

        Message.ListResponseMessage = (function() {

            const ListResponseMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ListResponseMessage.prototype.title = null;
            ListResponseMessage.prototype.listType = null;
            ListResponseMessage.prototype.singleSelectReply = null;
            ListResponseMessage.prototype.contextInfo = null;
            ListResponseMessage.prototype.description = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListResponseMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListResponseMessage.prototype, "_listType", {
                get: $util.oneOfGetter($oneOfFields = ["listType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListResponseMessage.prototype, "_singleSelectReply", {
                get: $util.oneOfGetter($oneOfFields = ["singleSelectReply"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListResponseMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ListResponseMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ListResponseMessage.create = function(properties) {
                return new ListResponseMessage(properties);
            };

            ListResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(10).string(m.title);
                if (m.listType != null && $Object.hasOwnProperty.call(m, "listType"))
                    w.uint32(16).int32(m.listType);
                if (m.singleSelectReply != null && $Object.hasOwnProperty.call(m, "singleSelectReply"))
                    $root.proto.Message.ListResponseMessage.SingleSelectReply.encode(m.singleSelectReply, w.uint32(26).fork(), q + 1).ldelim();
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(34).fork(), q + 1).ldelim();
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(42).string(m.description);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ListResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListResponseMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.listType = r.int32();
                            m._listType = "listType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.singleSelectReply = $root.proto.Message.ListResponseMessage.SingleSelectReply.decode(r, r.uint32(), $undefined, q + 1, m.singleSelectReply);
                            m._singleSelectReply = "singleSelectReply";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ListResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ListResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ListResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ListResponseMessage();
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                switch (d.listType) {
                case "UNKNOWN":
                case 0:
                    m.listType = 0;
                    break;
                case "SINGLE_SELECT":
                case 1:
                    m.listType = 1;
                    break;
                default:
                    if (typeof d.listType === "number" && (d.listType | 0) === d.listType)
                        m.listType = d.listType;
                }
                if (d.singleSelectReply != null) {
                    if (!$util.isObject(d.singleSelectReply))
                        throw $TypeError(".proto.Message.ListResponseMessage.singleSelectReply: object expected");
                    m.singleSelectReply = $root.proto.Message.ListResponseMessage.SingleSelectReply.fromObject(d.singleSelectReply, q + 1);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ListResponseMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                return m;
            };

            ListResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.listType != null && $Object.hasOwnProperty.call(m, "listType")) {
                    d.listType = o.enums === $String ? $root.proto.Message.ListResponseMessage.ListType[m.listType] === $undefined ? m.listType : $root.proto.Message.ListResponseMessage.ListType[m.listType] : m.listType;
                }
                if (m.singleSelectReply != null && $Object.hasOwnProperty.call(m, "singleSelectReply")) {
                    d.singleSelectReply = $root.proto.Message.ListResponseMessage.SingleSelectReply.toObject(m.singleSelectReply, o, q + 1);
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                return d;
            };

            ListResponseMessage.prototype.toJSON = function() {
                return ListResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ListResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ListResponseMessage";
            };

            ListResponseMessage.ListType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "SINGLE_SELECT"] = 1;
                return values;
            })();

            ListResponseMessage.SingleSelectReply = (function() {

                const SingleSelectReply = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                SingleSelectReply.prototype.selectedRowId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SingleSelectReply.prototype, "_selectedRowId", {
                    get: $util.oneOfGetter($oneOfFields = ["selectedRowId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SingleSelectReply.create = function(properties) {
                    return new SingleSelectReply(properties);
                };

                SingleSelectReply.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.selectedRowId != null && $Object.hasOwnProperty.call(m, "selectedRowId"))
                        w.uint32(10).string(m.selectedRowId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                SingleSelectReply.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ListResponseMessage.SingleSelectReply();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.selectedRowId = r.stringVerify();
                                m._selectedRowId = "selectedRowId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                SingleSelectReply.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ListResponseMessage.SingleSelectReply)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ListResponseMessage.SingleSelectReply: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ListResponseMessage.SingleSelectReply();
                    if (d.selectedRowId != null) {
                        m.selectedRowId = $String(d.selectedRowId);
                    }
                    return m;
                };

                SingleSelectReply.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.selectedRowId != null && $Object.hasOwnProperty.call(m, "selectedRowId")) {
                        d.selectedRowId = m.selectedRowId;
                    }
                    return d;
                };

                SingleSelectReply.prototype.toJSON = function() {
                    return SingleSelectReply.toObject(this, $protobuf.util.toJSONOptions);
                };

                SingleSelectReply.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ListResponseMessage.SingleSelectReply";
                };

                return SingleSelectReply;
            })();

            return ListResponseMessage;
        })();

        Message.LiveLocationMessage = (function() {

            const LiveLocationMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LiveLocationMessage.prototype.degreesLatitude = null;
            LiveLocationMessage.prototype.degreesLongitude = null;
            LiveLocationMessage.prototype.accuracyInMeters = null;
            LiveLocationMessage.prototype.speedInMps = null;
            LiveLocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;
            LiveLocationMessage.prototype.caption = null;
            LiveLocationMessage.prototype.sequenceNumber = null;
            LiveLocationMessage.prototype.timeOffset = null;
            LiveLocationMessage.prototype.jpegThumbnail = null;
            LiveLocationMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_accuracyInMeters", {
                get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_speedInMps", {
                get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
                get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_sequenceNumber", {
                get: $util.oneOfGetter($oneOfFields = ["sequenceNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_timeOffset", {
                get: $util.oneOfGetter($oneOfFields = ["timeOffset"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LiveLocationMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LiveLocationMessage.create = function(properties) {
                return new LiveLocationMessage(properties);
            };

            LiveLocationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude"))
                    w.uint32(9).double(m.degreesLatitude);
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude"))
                    w.uint32(17).double(m.degreesLongitude);
                if (m.accuracyInMeters != null && $Object.hasOwnProperty.call(m, "accuracyInMeters"))
                    w.uint32(24).uint32(m.accuracyInMeters);
                if (m.speedInMps != null && $Object.hasOwnProperty.call(m, "speedInMps"))
                    w.uint32(37).float(m.speedInMps);
                if (m.degreesClockwiseFromMagneticNorth != null && $Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth"))
                    w.uint32(40).uint32(m.degreesClockwiseFromMagneticNorth);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(50).string(m.caption);
                if (m.sequenceNumber != null && $Object.hasOwnProperty.call(m, "sequenceNumber"))
                    w.uint32(56).int64(m.sequenceNumber);
                if (m.timeOffset != null && $Object.hasOwnProperty.call(m, "timeOffset"))
                    w.uint32(64).uint32(m.timeOffset);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LiveLocationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.LiveLocationMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            m.degreesLatitude = r.double();
                            m._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            m.degreesLongitude = r.double();
                            m._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.accuracyInMeters = r.uint32();
                            m._accuracyInMeters = "accuracyInMeters";
                            continue;
                        }
                    case 4: {
                            if (u !== 5)
                                break;
                            m.speedInMps = r.float();
                            m._speedInMps = "speedInMps";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.degreesClockwiseFromMagneticNorth = r.uint32();
                            m._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.sequenceNumber = r.int64();
                            m._sequenceNumber = "sequenceNumber";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.timeOffset = r.uint32();
                            m._timeOffset = "timeOffset";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LiveLocationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.LiveLocationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.LiveLocationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.LiveLocationMessage();
                if (d.degreesLatitude != null) {
                    m.degreesLatitude = $Number(d.degreesLatitude);
                }
                if (d.degreesLongitude != null) {
                    m.degreesLongitude = $Number(d.degreesLongitude);
                }
                if (d.accuracyInMeters != null) {
                    m.accuracyInMeters = d.accuracyInMeters >>> 0;
                }
                if (d.speedInMps != null) {
                    m.speedInMps = $Number(d.speedInMps);
                }
                if (d.degreesClockwiseFromMagneticNorth != null) {
                    m.degreesClockwiseFromMagneticNorth = d.degreesClockwiseFromMagneticNorth >>> 0;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.sequenceNumber != null) {
                    if ($util.Long)
                        m.sequenceNumber = $util.Long.fromValue(d.sequenceNumber, false);
                    else if (typeof d.sequenceNumber === "string")
                        m.sequenceNumber = $parseInt(d.sequenceNumber, 10);
                    else if (typeof d.sequenceNumber === "number")
                        m.sequenceNumber = d.sequenceNumber;
                    else if (typeof d.sequenceNumber === "object")
                        m.sequenceNumber = new $util.LongBits(d.sequenceNumber.low >>> 0, d.sequenceNumber.high >>> 0).toNumber();
                }
                if (d.timeOffset != null) {
                    m.timeOffset = d.timeOffset >>> 0;
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.LiveLocationMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            LiveLocationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude")) {
                    d.degreesLatitude = o.json && !$isFinite(m.degreesLatitude) ? $String(m.degreesLatitude) : m.degreesLatitude;
                }
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude")) {
                    d.degreesLongitude = o.json && !$isFinite(m.degreesLongitude) ? $String(m.degreesLongitude) : m.degreesLongitude;
                }
                if (m.accuracyInMeters != null && $Object.hasOwnProperty.call(m, "accuracyInMeters")) {
                    d.accuracyInMeters = m.accuracyInMeters;
                }
                if (m.speedInMps != null && $Object.hasOwnProperty.call(m, "speedInMps")) {
                    d.speedInMps = o.json && !$isFinite(m.speedInMps) ? $String(m.speedInMps) : m.speedInMps;
                }
                if (m.degreesClockwiseFromMagneticNorth != null && $Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth")) {
                    d.degreesClockwiseFromMagneticNorth = m.degreesClockwiseFromMagneticNorth;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.sequenceNumber != null && $Object.hasOwnProperty.call(m, "sequenceNumber")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.sequenceNumber = typeof m.sequenceNumber === "number" ? $BigInt(m.sequenceNumber) : $util.Long.fromBits(m.sequenceNumber.low >>> 0, m.sequenceNumber.high >>> 0, false).toBigInt();
                    else if (typeof m.sequenceNumber === "number")
                        d.sequenceNumber = o.longs === $String ? $String(m.sequenceNumber) : m.sequenceNumber;
                    else
                        d.sequenceNumber = o.longs === $String ? $util.Long.prototype.toString.call(m.sequenceNumber) : o.longs === $Number ? new $util.LongBits(m.sequenceNumber.low >>> 0, m.sequenceNumber.high >>> 0).toNumber() : m.sequenceNumber;
                }
                if (m.timeOffset != null && $Object.hasOwnProperty.call(m, "timeOffset")) {
                    d.timeOffset = m.timeOffset;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            LiveLocationMessage.prototype.toJSON = function() {
                return LiveLocationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            LiveLocationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.LiveLocationMessage";
            };

            return LiveLocationMessage;
        })();

        Message.LocationMessage = (function() {

            const LocationMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LocationMessage.prototype.degreesLatitude = null;
            LocationMessage.prototype.degreesLongitude = null;
            LocationMessage.prototype.name = null;
            LocationMessage.prototype.address = null;
            LocationMessage.prototype.url = null;
            LocationMessage.prototype.isLive = null;
            LocationMessage.prototype.accuracyInMeters = null;
            LocationMessage.prototype.speedInMps = null;
            LocationMessage.prototype.degreesClockwiseFromMagneticNorth = null;
            LocationMessage.prototype.comment = null;
            LocationMessage.prototype.jpegThumbnail = null;
            LocationMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_isLive", {
                get: $util.oneOfGetter($oneOfFields = ["isLive"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_accuracyInMeters", {
                get: $util.oneOfGetter($oneOfFields = ["accuracyInMeters"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_speedInMps", {
                get: $util.oneOfGetter($oneOfFields = ["speedInMps"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_degreesClockwiseFromMagneticNorth", {
                get: $util.oneOfGetter($oneOfFields = ["degreesClockwiseFromMagneticNorth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_comment", {
                get: $util.oneOfGetter($oneOfFields = ["comment"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocationMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LocationMessage.create = function(properties) {
                return new LocationMessage(properties);
            };

            LocationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude"))
                    w.uint32(9).double(m.degreesLatitude);
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude"))
                    w.uint32(17).double(m.degreesLongitude);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(26).string(m.name);
                if (m.address != null && $Object.hasOwnProperty.call(m, "address"))
                    w.uint32(34).string(m.address);
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(42).string(m.url);
                if (m.isLive != null && $Object.hasOwnProperty.call(m, "isLive"))
                    w.uint32(48).bool(m.isLive);
                if (m.accuracyInMeters != null && $Object.hasOwnProperty.call(m, "accuracyInMeters"))
                    w.uint32(56).uint32(m.accuracyInMeters);
                if (m.speedInMps != null && $Object.hasOwnProperty.call(m, "speedInMps"))
                    w.uint32(69).float(m.speedInMps);
                if (m.degreesClockwiseFromMagneticNorth != null && $Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth"))
                    w.uint32(72).uint32(m.degreesClockwiseFromMagneticNorth);
                if (m.comment != null && $Object.hasOwnProperty.call(m, "comment"))
                    w.uint32(90).string(m.comment);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LocationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.LocationMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            m.degreesLatitude = r.double();
                            m._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            m.degreesLongitude = r.double();
                            m._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.address = r.stringVerify();
                            m._address = "address";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.isLive = r.bool();
                            m._isLive = "isLive";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.accuracyInMeters = r.uint32();
                            m._accuracyInMeters = "accuracyInMeters";
                            continue;
                        }
                    case 8: {
                            if (u !== 5)
                                break;
                            m.speedInMps = r.float();
                            m._speedInMps = "speedInMps";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.degreesClockwiseFromMagneticNorth = r.uint32();
                            m._degreesClockwiseFromMagneticNorth = "degreesClockwiseFromMagneticNorth";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.comment = r.stringVerify();
                            m._comment = "comment";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LocationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.LocationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.LocationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.LocationMessage();
                if (d.degreesLatitude != null) {
                    m.degreesLatitude = $Number(d.degreesLatitude);
                }
                if (d.degreesLongitude != null) {
                    m.degreesLongitude = $Number(d.degreesLongitude);
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.address != null) {
                    m.address = $String(d.address);
                }
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.isLive != null) {
                    m.isLive = $Boolean(d.isLive);
                }
                if (d.accuracyInMeters != null) {
                    m.accuracyInMeters = d.accuracyInMeters >>> 0;
                }
                if (d.speedInMps != null) {
                    m.speedInMps = $Number(d.speedInMps);
                }
                if (d.degreesClockwiseFromMagneticNorth != null) {
                    m.degreesClockwiseFromMagneticNorth = d.degreesClockwiseFromMagneticNorth >>> 0;
                }
                if (d.comment != null) {
                    m.comment = $String(d.comment);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.LocationMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            LocationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude")) {
                    d.degreesLatitude = o.json && !$isFinite(m.degreesLatitude) ? $String(m.degreesLatitude) : m.degreesLatitude;
                }
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude")) {
                    d.degreesLongitude = o.json && !$isFinite(m.degreesLongitude) ? $String(m.degreesLongitude) : m.degreesLongitude;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.address != null && $Object.hasOwnProperty.call(m, "address")) {
                    d.address = m.address;
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.isLive != null && $Object.hasOwnProperty.call(m, "isLive")) {
                    d.isLive = m.isLive;
                }
                if (m.accuracyInMeters != null && $Object.hasOwnProperty.call(m, "accuracyInMeters")) {
                    d.accuracyInMeters = m.accuracyInMeters;
                }
                if (m.speedInMps != null && $Object.hasOwnProperty.call(m, "speedInMps")) {
                    d.speedInMps = o.json && !$isFinite(m.speedInMps) ? $String(m.speedInMps) : m.speedInMps;
                }
                if (m.degreesClockwiseFromMagneticNorth != null && $Object.hasOwnProperty.call(m, "degreesClockwiseFromMagneticNorth")) {
                    d.degreesClockwiseFromMagneticNorth = m.degreesClockwiseFromMagneticNorth;
                }
                if (m.comment != null && $Object.hasOwnProperty.call(m, "comment")) {
                    d.comment = m.comment;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            LocationMessage.prototype.toJSON = function() {
                return LocationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            LocationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.LocationMessage";
            };

            return LocationMessage;
        })();

        Message.MMSThumbnailMetadata = (function() {

            const MMSThumbnailMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MMSThumbnailMetadata.prototype.thumbnailDirectPath = null;
            MMSThumbnailMetadata.prototype.thumbnailSha256 = null;
            MMSThumbnailMetadata.prototype.thumbnailEncSha256 = null;
            MMSThumbnailMetadata.prototype.mediaKey = null;
            MMSThumbnailMetadata.prototype.mediaKeyTimestamp = null;
            MMSThumbnailMetadata.prototype.thumbnailHeight = null;
            MMSThumbnailMetadata.prototype.thumbnailWidth = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MMSThumbnailMetadata.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MMSThumbnailMetadata.create = function(properties) {
                return new MMSThumbnailMetadata(properties);
            };

            MMSThumbnailMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                    w.uint32(10).string(m.thumbnailDirectPath);
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256"))
                    w.uint32(18).bytes(m.thumbnailSha256);
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256"))
                    w.uint32(26).bytes(m.thumbnailEncSha256);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(34).bytes(m.mediaKey);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(40).int64(m.mediaKeyTimestamp);
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight"))
                    w.uint32(48).uint32(m.thumbnailHeight);
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth"))
                    w.uint32(56).uint32(m.thumbnailWidth);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MMSThumbnailMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.MMSThumbnailMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.thumbnailDirectPath = r.stringVerify();
                            m._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.thumbnailSha256 = r.bytes();
                            m._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.thumbnailEncSha256 = r.bytes();
                            m._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.thumbnailHeight = r.uint32();
                            m._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.thumbnailWidth = r.uint32();
                            m._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MMSThumbnailMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.MMSThumbnailMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.MMSThumbnailMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.MMSThumbnailMetadata();
                if (d.thumbnailDirectPath != null) {
                    m.thumbnailDirectPath = $String(d.thumbnailDirectPath);
                }
                if (d.thumbnailSha256 != null) {
                    if (typeof d.thumbnailSha256 === "string")
                        $util.base64.decode(d.thumbnailSha256, m.thumbnailSha256 = $util.newBuffer($util.base64.length(d.thumbnailSha256)), 0);
                    else if (d.thumbnailSha256.length >= 0)
                        m.thumbnailSha256 = d.thumbnailSha256;
                }
                if (d.thumbnailEncSha256 != null) {
                    if (typeof d.thumbnailEncSha256 === "string")
                        $util.base64.decode(d.thumbnailEncSha256, m.thumbnailEncSha256 = $util.newBuffer($util.base64.length(d.thumbnailEncSha256)), 0);
                    else if (d.thumbnailEncSha256.length >= 0)
                        m.thumbnailEncSha256 = d.thumbnailEncSha256;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.thumbnailHeight != null) {
                    m.thumbnailHeight = d.thumbnailHeight >>> 0;
                }
                if (d.thumbnailWidth != null) {
                    m.thumbnailWidth = d.thumbnailWidth >>> 0;
                }
                return m;
            };

            MMSThumbnailMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath")) {
                    d.thumbnailDirectPath = m.thumbnailDirectPath;
                }
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256")) {
                    d.thumbnailSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailSha256, 0, m.thumbnailSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailSha256) : m.thumbnailSha256;
                }
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256")) {
                    d.thumbnailEncSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailEncSha256, 0, m.thumbnailEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailEncSha256) : m.thumbnailEncSha256;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight")) {
                    d.thumbnailHeight = m.thumbnailHeight;
                }
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth")) {
                    d.thumbnailWidth = m.thumbnailWidth;
                }
                return d;
            };

            MMSThumbnailMetadata.prototype.toJSON = function() {
                return MMSThumbnailMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            MMSThumbnailMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.MMSThumbnailMetadata";
            };

            return MMSThumbnailMetadata;
        })();

        Message.MarkAsVerifiedAction = (function() {

            const MarkAsVerifiedAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MarkAsVerifiedAction.prototype.userJidString = null;
            MarkAsVerifiedAction.prototype.verified = null;
            MarkAsVerifiedAction.prototype.verifiedIdentityKey = null;
            MarkAsVerifiedAction.prototype.actionSeq = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarkAsVerifiedAction.prototype, "_userJidString", {
                get: $util.oneOfGetter($oneOfFields = ["userJidString"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarkAsVerifiedAction.prototype, "_verified", {
                get: $util.oneOfGetter($oneOfFields = ["verified"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarkAsVerifiedAction.prototype, "_verifiedIdentityKey", {
                get: $util.oneOfGetter($oneOfFields = ["verifiedIdentityKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarkAsVerifiedAction.prototype, "_actionSeq", {
                get: $util.oneOfGetter($oneOfFields = ["actionSeq"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MarkAsVerifiedAction.create = function(properties) {
                return new MarkAsVerifiedAction(properties);
            };

            MarkAsVerifiedAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.userJidString != null && $Object.hasOwnProperty.call(m, "userJidString"))
                    w.uint32(10).string(m.userJidString);
                if (m.verified != null && $Object.hasOwnProperty.call(m, "verified"))
                    w.uint32(16).bool(m.verified);
                if (m.verifiedIdentityKey != null && $Object.hasOwnProperty.call(m, "verifiedIdentityKey"))
                    w.uint32(26).bytes(m.verifiedIdentityKey);
                if (m.actionSeq != null && $Object.hasOwnProperty.call(m, "actionSeq"))
                    w.uint32(32).uint64(m.actionSeq);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MarkAsVerifiedAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.MarkAsVerifiedAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.userJidString = r.stringVerify();
                            m._userJidString = "userJidString";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.verified = r.bool();
                            m._verified = "verified";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.verifiedIdentityKey = r.bytes();
                            m._verifiedIdentityKey = "verifiedIdentityKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.actionSeq = r.uint64();
                            m._actionSeq = "actionSeq";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MarkAsVerifiedAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.MarkAsVerifiedAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.MarkAsVerifiedAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.MarkAsVerifiedAction();
                if (d.userJidString != null) {
                    m.userJidString = $String(d.userJidString);
                }
                if (d.verified != null) {
                    m.verified = $Boolean(d.verified);
                }
                if (d.verifiedIdentityKey != null) {
                    if (typeof d.verifiedIdentityKey === "string")
                        $util.base64.decode(d.verifiedIdentityKey, m.verifiedIdentityKey = $util.newBuffer($util.base64.length(d.verifiedIdentityKey)), 0);
                    else if (d.verifiedIdentityKey.length >= 0)
                        m.verifiedIdentityKey = d.verifiedIdentityKey;
                }
                if (d.actionSeq != null) {
                    if ($util.Long)
                        m.actionSeq = $util.Long.fromValue(d.actionSeq, true);
                    else if (typeof d.actionSeq === "string")
                        m.actionSeq = $parseInt(d.actionSeq, 10);
                    else if (typeof d.actionSeq === "number")
                        m.actionSeq = d.actionSeq;
                    else if (typeof d.actionSeq === "object")
                        m.actionSeq = new $util.LongBits(d.actionSeq.low >>> 0, d.actionSeq.high >>> 0).toNumber(true);
                }
                return m;
            };

            MarkAsVerifiedAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.userJidString != null && $Object.hasOwnProperty.call(m, "userJidString")) {
                    d.userJidString = m.userJidString;
                }
                if (m.verified != null && $Object.hasOwnProperty.call(m, "verified")) {
                    d.verified = m.verified;
                }
                if (m.verifiedIdentityKey != null && $Object.hasOwnProperty.call(m, "verifiedIdentityKey")) {
                    d.verifiedIdentityKey = o.bytes === $String ? $util.base64.encode(m.verifiedIdentityKey, 0, m.verifiedIdentityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.verifiedIdentityKey) : m.verifiedIdentityKey;
                }
                if (m.actionSeq != null && $Object.hasOwnProperty.call(m, "actionSeq")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.actionSeq = typeof m.actionSeq === "number" ? $BigInt(m.actionSeq) : $util.Long.fromBits(m.actionSeq.low >>> 0, m.actionSeq.high >>> 0, true).toBigInt();
                    else if (typeof m.actionSeq === "number")
                        d.actionSeq = o.longs === $String ? $String(m.actionSeq) : m.actionSeq;
                    else
                        d.actionSeq = o.longs === $String ? $util.Long.prototype.toString.call(m.actionSeq) : o.longs === $Number ? new $util.LongBits(m.actionSeq.low >>> 0, m.actionSeq.high >>> 0).toNumber(true) : m.actionSeq;
                }
                return d;
            };

            MarkAsVerifiedAction.prototype.toJSON = function() {
                return MarkAsVerifiedAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MarkAsVerifiedAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.MarkAsVerifiedAction";
            };

            return MarkAsVerifiedAction;
        })();

        Message.MessageHistoryBundle = (function() {

            const MessageHistoryBundle = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MessageHistoryBundle.prototype.mimetype = null;
            MessageHistoryBundle.prototype.fileSha256 = null;
            MessageHistoryBundle.prototype.mediaKey = null;
            MessageHistoryBundle.prototype.fileEncSha256 = null;
            MessageHistoryBundle.prototype.directPath = null;
            MessageHistoryBundle.prototype.mediaKeyTimestamp = null;
            MessageHistoryBundle.prototype.contextInfo = null;
            MessageHistoryBundle.prototype.messageHistoryMetadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryBundle.prototype, "_messageHistoryMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["messageHistoryMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MessageHistoryBundle.create = function(properties) {
                return new MessageHistoryBundle(properties);
            };

            MessageHistoryBundle.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(10).string(m.mimetype);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(18).bytes(m.fileSha256);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(26).bytes(m.mediaKey);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(34).bytes(m.fileEncSha256);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(42).string(m.directPath);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(48).int64(m.mediaKeyTimestamp);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(58).fork(), q + 1).ldelim();
                if (m.messageHistoryMetadata != null && $Object.hasOwnProperty.call(m, "messageHistoryMetadata"))
                    $root.proto.Message.MessageHistoryMetadata.encode(m.messageHistoryMetadata, w.uint32(66).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MessageHistoryBundle.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.MessageHistoryBundle();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.decode(r, r.uint32(), $undefined, q + 1, m.messageHistoryMetadata);
                            m._messageHistoryMetadata = "messageHistoryMetadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MessageHistoryBundle.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.MessageHistoryBundle)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.MessageHistoryBundle: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.MessageHistoryBundle();
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.MessageHistoryBundle.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.messageHistoryMetadata != null) {
                    if (!$util.isObject(d.messageHistoryMetadata))
                        throw $TypeError(".proto.Message.MessageHistoryBundle.messageHistoryMetadata: object expected");
                    m.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.fromObject(d.messageHistoryMetadata, q + 1);
                }
                return m;
            };

            MessageHistoryBundle.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.messageHistoryMetadata != null && $Object.hasOwnProperty.call(m, "messageHistoryMetadata")) {
                    d.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.toObject(m.messageHistoryMetadata, o, q + 1);
                }
                return d;
            };

            MessageHistoryBundle.prototype.toJSON = function() {
                return MessageHistoryBundle.toObject(this, $protobuf.util.toJSONOptions);
            };

            MessageHistoryBundle.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.MessageHistoryBundle";
            };

            return MessageHistoryBundle;
        })();

        Message.MessageHistoryMetadata = (function() {

            const MessageHistoryMetadata = function (p) {
                this.historyReceivers = [];
                this.nonHistoryReceivers = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MessageHistoryMetadata.prototype.historyReceivers = $util.emptyArray;
            MessageHistoryMetadata.prototype.oldestMessageTimestampInWindow = null;
            MessageHistoryMetadata.prototype.messageCount = null;
            MessageHistoryMetadata.prototype.nonHistoryReceivers = $util.emptyArray;
            MessageHistoryMetadata.prototype.oldestMessageTimestampInBundle = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryMetadata.prototype, "_oldestMessageTimestampInWindow", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMessageTimestampInWindow"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryMetadata.prototype, "_messageCount", {
                get: $util.oneOfGetter($oneOfFields = ["messageCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryMetadata.prototype, "_oldestMessageTimestampInBundle", {
                get: $util.oneOfGetter($oneOfFields = ["oldestMessageTimestampInBundle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MessageHistoryMetadata.create = function(properties) {
                return new MessageHistoryMetadata(properties);
            };

            MessageHistoryMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.historyReceivers != null && m.historyReceivers.length) {
                    for (var i = 0; i < m.historyReceivers.length; ++i)
                        w.uint32(10).string(m.historyReceivers[i]);
                }
                if (m.oldestMessageTimestampInWindow != null && $Object.hasOwnProperty.call(m, "oldestMessageTimestampInWindow"))
                    w.uint32(16).int64(m.oldestMessageTimestampInWindow);
                if (m.messageCount != null && $Object.hasOwnProperty.call(m, "messageCount"))
                    w.uint32(24).int64(m.messageCount);
                if (m.nonHistoryReceivers != null && m.nonHistoryReceivers.length) {
                    for (var i = 0; i < m.nonHistoryReceivers.length; ++i)
                        w.uint32(34).string(m.nonHistoryReceivers[i]);
                }
                if (m.oldestMessageTimestampInBundle != null && $Object.hasOwnProperty.call(m, "oldestMessageTimestampInBundle"))
                    w.uint32(40).int64(m.oldestMessageTimestampInBundle);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MessageHistoryMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.MessageHistoryMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.historyReceivers && m.historyReceivers.length))
                                m.historyReceivers = [];
                            m.historyReceivers.push(r.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.oldestMessageTimestampInWindow = r.int64();
                            m._oldestMessageTimestampInWindow = "oldestMessageTimestampInWindow";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.messageCount = r.int64();
                            m._messageCount = "messageCount";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(m.nonHistoryReceivers && m.nonHistoryReceivers.length))
                                m.nonHistoryReceivers = [];
                            m.nonHistoryReceivers.push(r.stringVerify());
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.oldestMessageTimestampInBundle = r.int64();
                            m._oldestMessageTimestampInBundle = "oldestMessageTimestampInBundle";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MessageHistoryMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.MessageHistoryMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.MessageHistoryMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.MessageHistoryMetadata();
                if (d.historyReceivers) {
                    if (!$Array.isArray(d.historyReceivers))
                        throw $TypeError(".proto.Message.MessageHistoryMetadata.historyReceivers: array expected");
                    m.historyReceivers = $Array(d.historyReceivers.length);
                    for (var i = 0; i < d.historyReceivers.length; ++i) {
                        m.historyReceivers[i] = $String(d.historyReceivers[i]);
                    }
                }
                if (d.oldestMessageTimestampInWindow != null) {
                    if ($util.Long)
                        m.oldestMessageTimestampInWindow = $util.Long.fromValue(d.oldestMessageTimestampInWindow, false);
                    else if (typeof d.oldestMessageTimestampInWindow === "string")
                        m.oldestMessageTimestampInWindow = $parseInt(d.oldestMessageTimestampInWindow, 10);
                    else if (typeof d.oldestMessageTimestampInWindow === "number")
                        m.oldestMessageTimestampInWindow = d.oldestMessageTimestampInWindow;
                    else if (typeof d.oldestMessageTimestampInWindow === "object")
                        m.oldestMessageTimestampInWindow = new $util.LongBits(d.oldestMessageTimestampInWindow.low >>> 0, d.oldestMessageTimestampInWindow.high >>> 0).toNumber();
                }
                if (d.messageCount != null) {
                    if ($util.Long)
                        m.messageCount = $util.Long.fromValue(d.messageCount, false);
                    else if (typeof d.messageCount === "string")
                        m.messageCount = $parseInt(d.messageCount, 10);
                    else if (typeof d.messageCount === "number")
                        m.messageCount = d.messageCount;
                    else if (typeof d.messageCount === "object")
                        m.messageCount = new $util.LongBits(d.messageCount.low >>> 0, d.messageCount.high >>> 0).toNumber();
                }
                if (d.nonHistoryReceivers) {
                    if (!$Array.isArray(d.nonHistoryReceivers))
                        throw $TypeError(".proto.Message.MessageHistoryMetadata.nonHistoryReceivers: array expected");
                    m.nonHistoryReceivers = $Array(d.nonHistoryReceivers.length);
                    for (var i = 0; i < d.nonHistoryReceivers.length; ++i) {
                        m.nonHistoryReceivers[i] = $String(d.nonHistoryReceivers[i]);
                    }
                }
                if (d.oldestMessageTimestampInBundle != null) {
                    if ($util.Long)
                        m.oldestMessageTimestampInBundle = $util.Long.fromValue(d.oldestMessageTimestampInBundle, false);
                    else if (typeof d.oldestMessageTimestampInBundle === "string")
                        m.oldestMessageTimestampInBundle = $parseInt(d.oldestMessageTimestampInBundle, 10);
                    else if (typeof d.oldestMessageTimestampInBundle === "number")
                        m.oldestMessageTimestampInBundle = d.oldestMessageTimestampInBundle;
                    else if (typeof d.oldestMessageTimestampInBundle === "object")
                        m.oldestMessageTimestampInBundle = new $util.LongBits(d.oldestMessageTimestampInBundle.low >>> 0, d.oldestMessageTimestampInBundle.high >>> 0).toNumber();
                }
                return m;
            };

            MessageHistoryMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.historyReceivers = [];
                    d.nonHistoryReceivers = [];
                }
                if (m.historyReceivers && m.historyReceivers.length) {
                    d.historyReceivers = $Array(m.historyReceivers.length);
                    for (var j = 0; j < m.historyReceivers.length; ++j) {
                        d.historyReceivers[j] = m.historyReceivers[j];
                    }
                }
                if (m.oldestMessageTimestampInWindow != null && $Object.hasOwnProperty.call(m, "oldestMessageTimestampInWindow")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.oldestMessageTimestampInWindow = typeof m.oldestMessageTimestampInWindow === "number" ? $BigInt(m.oldestMessageTimestampInWindow) : $util.Long.fromBits(m.oldestMessageTimestampInWindow.low >>> 0, m.oldestMessageTimestampInWindow.high >>> 0, false).toBigInt();
                    else if (typeof m.oldestMessageTimestampInWindow === "number")
                        d.oldestMessageTimestampInWindow = o.longs === $String ? $String(m.oldestMessageTimestampInWindow) : m.oldestMessageTimestampInWindow;
                    else
                        d.oldestMessageTimestampInWindow = o.longs === $String ? $util.Long.prototype.toString.call(m.oldestMessageTimestampInWindow) : o.longs === $Number ? new $util.LongBits(m.oldestMessageTimestampInWindow.low >>> 0, m.oldestMessageTimestampInWindow.high >>> 0).toNumber() : m.oldestMessageTimestampInWindow;
                }
                if (m.messageCount != null && $Object.hasOwnProperty.call(m, "messageCount")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.messageCount = typeof m.messageCount === "number" ? $BigInt(m.messageCount) : $util.Long.fromBits(m.messageCount.low >>> 0, m.messageCount.high >>> 0, false).toBigInt();
                    else if (typeof m.messageCount === "number")
                        d.messageCount = o.longs === $String ? $String(m.messageCount) : m.messageCount;
                    else
                        d.messageCount = o.longs === $String ? $util.Long.prototype.toString.call(m.messageCount) : o.longs === $Number ? new $util.LongBits(m.messageCount.low >>> 0, m.messageCount.high >>> 0).toNumber() : m.messageCount;
                }
                if (m.nonHistoryReceivers && m.nonHistoryReceivers.length) {
                    d.nonHistoryReceivers = $Array(m.nonHistoryReceivers.length);
                    for (var j = 0; j < m.nonHistoryReceivers.length; ++j) {
                        d.nonHistoryReceivers[j] = m.nonHistoryReceivers[j];
                    }
                }
                if (m.oldestMessageTimestampInBundle != null && $Object.hasOwnProperty.call(m, "oldestMessageTimestampInBundle")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.oldestMessageTimestampInBundle = typeof m.oldestMessageTimestampInBundle === "number" ? $BigInt(m.oldestMessageTimestampInBundle) : $util.Long.fromBits(m.oldestMessageTimestampInBundle.low >>> 0, m.oldestMessageTimestampInBundle.high >>> 0, false).toBigInt();
                    else if (typeof m.oldestMessageTimestampInBundle === "number")
                        d.oldestMessageTimestampInBundle = o.longs === $String ? $String(m.oldestMessageTimestampInBundle) : m.oldestMessageTimestampInBundle;
                    else
                        d.oldestMessageTimestampInBundle = o.longs === $String ? $util.Long.prototype.toString.call(m.oldestMessageTimestampInBundle) : o.longs === $Number ? new $util.LongBits(m.oldestMessageTimestampInBundle.low >>> 0, m.oldestMessageTimestampInBundle.high >>> 0).toNumber() : m.oldestMessageTimestampInBundle;
                }
                return d;
            };

            MessageHistoryMetadata.prototype.toJSON = function() {
                return MessageHistoryMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            MessageHistoryMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.MessageHistoryMetadata";
            };

            return MessageHistoryMetadata;
        })();

        Message.MessageHistoryNotice = (function() {

            const MessageHistoryNotice = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MessageHistoryNotice.prototype.contextInfo = null;
            MessageHistoryNotice.prototype.messageHistoryMetadata = null;
            MessageHistoryNotice.prototype.botHistoryShareSyncMetadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryNotice.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryNotice.prototype, "_messageHistoryMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["messageHistoryMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MessageHistoryNotice.prototype, "_botHistoryShareSyncMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["botHistoryShareSyncMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MessageHistoryNotice.create = function(properties) {
                return new MessageHistoryNotice(properties);
            };

            MessageHistoryNotice.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(10).fork(), q + 1).ldelim();
                if (m.messageHistoryMetadata != null && $Object.hasOwnProperty.call(m, "messageHistoryMetadata"))
                    $root.proto.Message.MessageHistoryMetadata.encode(m.messageHistoryMetadata, w.uint32(18).fork(), q + 1).ldelim();
                if (m.botHistoryShareSyncMetadata != null && $Object.hasOwnProperty.call(m, "botHistoryShareSyncMetadata"))
                    $root.proto.Message.BotHistoryShareSyncMetadata.encode(m.botHistoryShareSyncMetadata, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MessageHistoryNotice.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.MessageHistoryNotice();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.decode(r, r.uint32(), $undefined, q + 1, m.messageHistoryMetadata);
                            m._messageHistoryMetadata = "messageHistoryMetadata";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.botHistoryShareSyncMetadata = $root.proto.Message.BotHistoryShareSyncMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botHistoryShareSyncMetadata);
                            m._botHistoryShareSyncMetadata = "botHistoryShareSyncMetadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MessageHistoryNotice.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.MessageHistoryNotice)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.MessageHistoryNotice: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.MessageHistoryNotice();
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.MessageHistoryNotice.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.messageHistoryMetadata != null) {
                    if (!$util.isObject(d.messageHistoryMetadata))
                        throw $TypeError(".proto.Message.MessageHistoryNotice.messageHistoryMetadata: object expected");
                    m.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.fromObject(d.messageHistoryMetadata, q + 1);
                }
                if (d.botHistoryShareSyncMetadata != null) {
                    if (!$util.isObject(d.botHistoryShareSyncMetadata))
                        throw $TypeError(".proto.Message.MessageHistoryNotice.botHistoryShareSyncMetadata: object expected");
                    m.botHistoryShareSyncMetadata = $root.proto.Message.BotHistoryShareSyncMetadata.fromObject(d.botHistoryShareSyncMetadata, q + 1);
                }
                return m;
            };

            MessageHistoryNotice.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.messageHistoryMetadata != null && $Object.hasOwnProperty.call(m, "messageHistoryMetadata")) {
                    d.messageHistoryMetadata = $root.proto.Message.MessageHistoryMetadata.toObject(m.messageHistoryMetadata, o, q + 1);
                }
                if (m.botHistoryShareSyncMetadata != null && $Object.hasOwnProperty.call(m, "botHistoryShareSyncMetadata")) {
                    d.botHistoryShareSyncMetadata = $root.proto.Message.BotHistoryShareSyncMetadata.toObject(m.botHistoryShareSyncMetadata, o, q + 1);
                }
                return d;
            };

            MessageHistoryNotice.prototype.toJSON = function() {
                return MessageHistoryNotice.toObject(this, $protobuf.util.toJSONOptions);
            };

            MessageHistoryNotice.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.MessageHistoryNotice";
            };

            return MessageHistoryNotice;
        })();

        Message.MusicMessage = (function() {

            const MusicMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MusicMessage.prototype.embeddedMusic = null;
            MusicMessage.prototype.songUri = null;
            MusicMessage.prototype.artworkUri = null;
            MusicMessage.prototype.style = null;
            MusicMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MusicMessage.prototype, "_embeddedMusic", {
                get: $util.oneOfGetter($oneOfFields = ["embeddedMusic"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MusicMessage.prototype, "_songUri", {
                get: $util.oneOfGetter($oneOfFields = ["songUri"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MusicMessage.prototype, "_artworkUri", {
                get: $util.oneOfGetter($oneOfFields = ["artworkUri"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MusicMessage.prototype, "_style", {
                get: $util.oneOfGetter($oneOfFields = ["style"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MusicMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MusicMessage.create = function(properties) {
                return new MusicMessage(properties);
            };

            MusicMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.embeddedMusic != null && $Object.hasOwnProperty.call(m, "embeddedMusic"))
                    $root.proto.EmbeddedMusic.encode(m.embeddedMusic, w.uint32(10).fork(), q + 1).ldelim();
                if (m.songUri != null && $Object.hasOwnProperty.call(m, "songUri"))
                    w.uint32(18).string(m.songUri);
                if (m.artworkUri != null && $Object.hasOwnProperty.call(m, "artworkUri"))
                    w.uint32(26).string(m.artworkUri);
                if (m.style != null && $Object.hasOwnProperty.call(m, "style"))
                    w.uint32(32).int32(m.style);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(42).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MusicMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.MusicMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.embeddedMusic = $root.proto.EmbeddedMusic.decode(r, r.uint32(), $undefined, q + 1, m.embeddedMusic);
                            m._embeddedMusic = "embeddedMusic";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.songUri = r.stringVerify();
                            m._songUri = "songUri";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.artworkUri = r.stringVerify();
                            m._artworkUri = "artworkUri";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.style = r.int32();
                            m._style = "style";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MusicMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.MusicMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.MusicMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.MusicMessage();
                if (d.embeddedMusic != null) {
                    if (!$util.isObject(d.embeddedMusic))
                        throw $TypeError(".proto.Message.MusicMessage.embeddedMusic: object expected");
                    m.embeddedMusic = $root.proto.EmbeddedMusic.fromObject(d.embeddedMusic, q + 1);
                }
                if (d.songUri != null) {
                    m.songUri = $String(d.songUri);
                }
                if (d.artworkUri != null) {
                    m.artworkUri = $String(d.artworkUri);
                }
                if (d.style != null) {
                    m.style = d.style | 0;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.MusicMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            MusicMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.embeddedMusic != null && $Object.hasOwnProperty.call(m, "embeddedMusic")) {
                    d.embeddedMusic = $root.proto.EmbeddedMusic.toObject(m.embeddedMusic, o, q + 1);
                }
                if (m.songUri != null && $Object.hasOwnProperty.call(m, "songUri")) {
                    d.songUri = m.songUri;
                }
                if (m.artworkUri != null && $Object.hasOwnProperty.call(m, "artworkUri")) {
                    d.artworkUri = m.artworkUri;
                }
                if (m.style != null && $Object.hasOwnProperty.call(m, "style")) {
                    d.style = m.style;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            MusicMessage.prototype.toJSON = function() {
                return MusicMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            MusicMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.MusicMessage";
            };

            MusicMessage.MusicMessageStyle = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "VINYL"] = 1;
                return values;
            })();

            return MusicMessage;
        })();

        Message.NewsletterAdminInviteMessage = (function() {

            const NewsletterAdminInviteMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NewsletterAdminInviteMessage.prototype.newsletterJid = null;
            NewsletterAdminInviteMessage.prototype.newsletterName = null;
            NewsletterAdminInviteMessage.prototype.jpegThumbnail = null;
            NewsletterAdminInviteMessage.prototype.caption = null;
            NewsletterAdminInviteMessage.prototype.inviteExpiration = null;
            NewsletterAdminInviteMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_newsletterJid", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_inviteExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["inviteExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterAdminInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NewsletterAdminInviteMessage.create = function(properties) {
                return new NewsletterAdminInviteMessage(properties);
            };

            NewsletterAdminInviteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.newsletterJid != null && $Object.hasOwnProperty.call(m, "newsletterJid"))
                    w.uint32(10).string(m.newsletterJid);
                if (m.newsletterName != null && $Object.hasOwnProperty.call(m, "newsletterName"))
                    w.uint32(18).string(m.newsletterName);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(26).bytes(m.jpegThumbnail);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(34).string(m.caption);
                if (m.inviteExpiration != null && $Object.hasOwnProperty.call(m, "inviteExpiration"))
                    w.uint32(40).int64(m.inviteExpiration);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(50).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NewsletterAdminInviteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.NewsletterAdminInviteMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.newsletterJid = r.stringVerify();
                            m._newsletterJid = "newsletterJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.newsletterName = r.stringVerify();
                            m._newsletterName = "newsletterName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.inviteExpiration = r.int64();
                            m._inviteExpiration = "inviteExpiration";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NewsletterAdminInviteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.NewsletterAdminInviteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.NewsletterAdminInviteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.NewsletterAdminInviteMessage();
                if (d.newsletterJid != null) {
                    m.newsletterJid = $String(d.newsletterJid);
                }
                if (d.newsletterName != null) {
                    m.newsletterName = $String(d.newsletterName);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.inviteExpiration != null) {
                    if ($util.Long)
                        m.inviteExpiration = $util.Long.fromValue(d.inviteExpiration, false);
                    else if (typeof d.inviteExpiration === "string")
                        m.inviteExpiration = $parseInt(d.inviteExpiration, 10);
                    else if (typeof d.inviteExpiration === "number")
                        m.inviteExpiration = d.inviteExpiration;
                    else if (typeof d.inviteExpiration === "object")
                        m.inviteExpiration = new $util.LongBits(d.inviteExpiration.low >>> 0, d.inviteExpiration.high >>> 0).toNumber();
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.NewsletterAdminInviteMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            NewsletterAdminInviteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.newsletterJid != null && $Object.hasOwnProperty.call(m, "newsletterJid")) {
                    d.newsletterJid = m.newsletterJid;
                }
                if (m.newsletterName != null && $Object.hasOwnProperty.call(m, "newsletterName")) {
                    d.newsletterName = m.newsletterName;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.inviteExpiration != null && $Object.hasOwnProperty.call(m, "inviteExpiration")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.inviteExpiration = typeof m.inviteExpiration === "number" ? $BigInt(m.inviteExpiration) : $util.Long.fromBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0, false).toBigInt();
                    else if (typeof m.inviteExpiration === "number")
                        d.inviteExpiration = o.longs === $String ? $String(m.inviteExpiration) : m.inviteExpiration;
                    else
                        d.inviteExpiration = o.longs === $String ? $util.Long.prototype.toString.call(m.inviteExpiration) : o.longs === $Number ? new $util.LongBits(m.inviteExpiration.low >>> 0, m.inviteExpiration.high >>> 0).toNumber() : m.inviteExpiration;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            NewsletterAdminInviteMessage.prototype.toJSON = function() {
                return NewsletterAdminInviteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            NewsletterAdminInviteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.NewsletterAdminInviteMessage";
            };

            return NewsletterAdminInviteMessage;
        })();

        Message.NewsletterFollowerInviteMessage = (function() {

            const NewsletterFollowerInviteMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NewsletterFollowerInviteMessage.prototype.newsletterJid = null;
            NewsletterFollowerInviteMessage.prototype.newsletterName = null;
            NewsletterFollowerInviteMessage.prototype.jpegThumbnail = null;
            NewsletterFollowerInviteMessage.prototype.caption = null;
            NewsletterFollowerInviteMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_newsletterJid", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_newsletterName", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterFollowerInviteMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NewsletterFollowerInviteMessage.create = function(properties) {
                return new NewsletterFollowerInviteMessage(properties);
            };

            NewsletterFollowerInviteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.newsletterJid != null && $Object.hasOwnProperty.call(m, "newsletterJid"))
                    w.uint32(10).string(m.newsletterJid);
                if (m.newsletterName != null && $Object.hasOwnProperty.call(m, "newsletterName"))
                    w.uint32(18).string(m.newsletterName);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(26).bytes(m.jpegThumbnail);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(34).string(m.caption);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(42).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NewsletterFollowerInviteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.NewsletterFollowerInviteMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.newsletterJid = r.stringVerify();
                            m._newsletterJid = "newsletterJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.newsletterName = r.stringVerify();
                            m._newsletterName = "newsletterName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NewsletterFollowerInviteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.NewsletterFollowerInviteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.NewsletterFollowerInviteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.NewsletterFollowerInviteMessage();
                if (d.newsletterJid != null) {
                    m.newsletterJid = $String(d.newsletterJid);
                }
                if (d.newsletterName != null) {
                    m.newsletterName = $String(d.newsletterName);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.NewsletterFollowerInviteMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            NewsletterFollowerInviteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.newsletterJid != null && $Object.hasOwnProperty.call(m, "newsletterJid")) {
                    d.newsletterJid = m.newsletterJid;
                }
                if (m.newsletterName != null && $Object.hasOwnProperty.call(m, "newsletterName")) {
                    d.newsletterName = m.newsletterName;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            NewsletterFollowerInviteMessage.prototype.toJSON = function() {
                return NewsletterFollowerInviteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            NewsletterFollowerInviteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.NewsletterFollowerInviteMessage";
            };

            return NewsletterFollowerInviteMessage;
        })();

        Message.OrderMessage = (function() {

            const OrderMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            OrderMessage.prototype.orderId = null;
            OrderMessage.prototype.thumbnail = null;
            OrderMessage.prototype.itemCount = null;
            OrderMessage.prototype.status = null;
            OrderMessage.prototype.surface = null;
            OrderMessage.prototype.message = null;
            OrderMessage.prototype.orderTitle = null;
            OrderMessage.prototype.sellerJid = null;
            OrderMessage.prototype.token = null;
            OrderMessage.prototype.totalAmount1000 = null;
            OrderMessage.prototype.totalCurrencyCode = null;
            OrderMessage.prototype.contextInfo = null;
            OrderMessage.prototype.messageVersion = null;
            OrderMessage.prototype.orderRequestMessageId = null;
            OrderMessage.prototype.catalogType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_orderId", {
                get: $util.oneOfGetter($oneOfFields = ["orderId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_itemCount", {
                get: $util.oneOfGetter($oneOfFields = ["itemCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_surface", {
                get: $util.oneOfGetter($oneOfFields = ["surface"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_orderTitle", {
                get: $util.oneOfGetter($oneOfFields = ["orderTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_sellerJid", {
                get: $util.oneOfGetter($oneOfFields = ["sellerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_token", {
                get: $util.oneOfGetter($oneOfFields = ["token"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_totalAmount1000", {
                get: $util.oneOfGetter($oneOfFields = ["totalAmount1000"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_totalCurrencyCode", {
                get: $util.oneOfGetter($oneOfFields = ["totalCurrencyCode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_messageVersion", {
                get: $util.oneOfGetter($oneOfFields = ["messageVersion"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_orderRequestMessageId", {
                get: $util.oneOfGetter($oneOfFields = ["orderRequestMessageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OrderMessage.prototype, "_catalogType", {
                get: $util.oneOfGetter($oneOfFields = ["catalogType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            OrderMessage.create = function(properties) {
                return new OrderMessage(properties);
            };

            OrderMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.orderId != null && $Object.hasOwnProperty.call(m, "orderId"))
                    w.uint32(10).string(m.orderId);
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail"))
                    w.uint32(18).bytes(m.thumbnail);
                if (m.itemCount != null && $Object.hasOwnProperty.call(m, "itemCount"))
                    w.uint32(24).int32(m.itemCount);
                if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                    w.uint32(32).int32(m.status);
                if (m.surface != null && $Object.hasOwnProperty.call(m, "surface"))
                    w.uint32(40).int32(m.surface);
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    w.uint32(50).string(m.message);
                if (m.orderTitle != null && $Object.hasOwnProperty.call(m, "orderTitle"))
                    w.uint32(58).string(m.orderTitle);
                if (m.sellerJid != null && $Object.hasOwnProperty.call(m, "sellerJid"))
                    w.uint32(66).string(m.sellerJid);
                if (m.token != null && $Object.hasOwnProperty.call(m, "token"))
                    w.uint32(74).string(m.token);
                if (m.totalAmount1000 != null && $Object.hasOwnProperty.call(m, "totalAmount1000"))
                    w.uint32(80).int64(m.totalAmount1000);
                if (m.totalCurrencyCode != null && $Object.hasOwnProperty.call(m, "totalCurrencyCode"))
                    w.uint32(90).string(m.totalCurrencyCode);
                if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion"))
                    w.uint32(96).int32(m.messageVersion);
                if (m.orderRequestMessageId != null && $Object.hasOwnProperty.call(m, "orderRequestMessageId"))
                    $root.proto.MessageKey.encode(m.orderRequestMessageId, w.uint32(106).fork(), q + 1).ldelim();
                if (m.catalogType != null && $Object.hasOwnProperty.call(m, "catalogType"))
                    w.uint32(122).string(m.catalogType);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            OrderMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.OrderMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.orderId = r.stringVerify();
                            m._orderId = "orderId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.thumbnail = r.bytes();
                            m._thumbnail = "thumbnail";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.itemCount = r.int32();
                            m._itemCount = "itemCount";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            m._status = "status";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.surface = r.int32();
                            m._surface = "surface";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.message = r.stringVerify();
                            m._message = "message";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.orderTitle = r.stringVerify();
                            m._orderTitle = "orderTitle";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.sellerJid = r.stringVerify();
                            m._sellerJid = "sellerJid";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.token = r.stringVerify();
                            m._token = "token";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.totalAmount1000 = r.int64();
                            m._totalAmount1000 = "totalAmount1000";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.totalCurrencyCode = r.stringVerify();
                            m._totalCurrencyCode = "totalCurrencyCode";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.messageVersion = r.int32();
                            m._messageVersion = "messageVersion";
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.orderRequestMessageId = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.orderRequestMessageId);
                            m._orderRequestMessageId = "orderRequestMessageId";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            m.catalogType = r.stringVerify();
                            m._catalogType = "catalogType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            OrderMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.OrderMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.OrderMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.OrderMessage();
                if (d.orderId != null) {
                    m.orderId = $String(d.orderId);
                }
                if (d.thumbnail != null) {
                    if (typeof d.thumbnail === "string")
                        $util.base64.decode(d.thumbnail, m.thumbnail = $util.newBuffer($util.base64.length(d.thumbnail)), 0);
                    else if (d.thumbnail.length >= 0)
                        m.thumbnail = d.thumbnail;
                }
                if (d.itemCount != null) {
                    m.itemCount = d.itemCount | 0;
                }
                switch (d.status) {
                case "INQUIRY":
                case 1:
                    m.status = 1;
                    break;
                case "ACCEPTED":
                case 2:
                    m.status = 2;
                    break;
                case "DECLINED":
                case 3:
                    m.status = 3;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                switch (d.surface) {
                case "CATALOG":
                case 1:
                    m.surface = 1;
                    break;
                default:
                    if (typeof d.surface === "number" && (d.surface | 0) === d.surface)
                        m.surface = d.surface;
                }
                if (d.message != null) {
                    m.message = $String(d.message);
                }
                if (d.orderTitle != null) {
                    m.orderTitle = $String(d.orderTitle);
                }
                if (d.sellerJid != null) {
                    m.sellerJid = $String(d.sellerJid);
                }
                if (d.token != null) {
                    m.token = $String(d.token);
                }
                if (d.totalAmount1000 != null) {
                    if ($util.Long)
                        m.totalAmount1000 = $util.Long.fromValue(d.totalAmount1000, false);
                    else if (typeof d.totalAmount1000 === "string")
                        m.totalAmount1000 = $parseInt(d.totalAmount1000, 10);
                    else if (typeof d.totalAmount1000 === "number")
                        m.totalAmount1000 = d.totalAmount1000;
                    else if (typeof d.totalAmount1000 === "object")
                        m.totalAmount1000 = new $util.LongBits(d.totalAmount1000.low >>> 0, d.totalAmount1000.high >>> 0).toNumber();
                }
                if (d.totalCurrencyCode != null) {
                    m.totalCurrencyCode = $String(d.totalCurrencyCode);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.OrderMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.messageVersion != null) {
                    m.messageVersion = d.messageVersion | 0;
                }
                if (d.orderRequestMessageId != null) {
                    if (!$util.isObject(d.orderRequestMessageId))
                        throw $TypeError(".proto.Message.OrderMessage.orderRequestMessageId: object expected");
                    m.orderRequestMessageId = $root.proto.MessageKey.fromObject(d.orderRequestMessageId, q + 1);
                }
                if (d.catalogType != null) {
                    m.catalogType = $String(d.catalogType);
                }
                return m;
            };

            OrderMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.orderId != null && $Object.hasOwnProperty.call(m, "orderId")) {
                    d.orderId = m.orderId;
                }
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail")) {
                    d.thumbnail = o.bytes === $String ? $util.base64.encode(m.thumbnail, 0, m.thumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnail) : m.thumbnail;
                }
                if (m.itemCount != null && $Object.hasOwnProperty.call(m, "itemCount")) {
                    d.itemCount = m.itemCount;
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.Message.OrderMessage.OrderStatus[m.status] === $undefined ? m.status : $root.proto.Message.OrderMessage.OrderStatus[m.status] : m.status;
                }
                if (m.surface != null && $Object.hasOwnProperty.call(m, "surface")) {
                    d.surface = o.enums === $String ? $root.proto.Message.OrderMessage.OrderSurface[m.surface] === $undefined ? m.surface : $root.proto.Message.OrderMessage.OrderSurface[m.surface] : m.surface;
                }
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = m.message;
                }
                if (m.orderTitle != null && $Object.hasOwnProperty.call(m, "orderTitle")) {
                    d.orderTitle = m.orderTitle;
                }
                if (m.sellerJid != null && $Object.hasOwnProperty.call(m, "sellerJid")) {
                    d.sellerJid = m.sellerJid;
                }
                if (m.token != null && $Object.hasOwnProperty.call(m, "token")) {
                    d.token = m.token;
                }
                if (m.totalAmount1000 != null && $Object.hasOwnProperty.call(m, "totalAmount1000")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.totalAmount1000 = typeof m.totalAmount1000 === "number" ? $BigInt(m.totalAmount1000) : $util.Long.fromBits(m.totalAmount1000.low >>> 0, m.totalAmount1000.high >>> 0, false).toBigInt();
                    else if (typeof m.totalAmount1000 === "number")
                        d.totalAmount1000 = o.longs === $String ? $String(m.totalAmount1000) : m.totalAmount1000;
                    else
                        d.totalAmount1000 = o.longs === $String ? $util.Long.prototype.toString.call(m.totalAmount1000) : o.longs === $Number ? new $util.LongBits(m.totalAmount1000.low >>> 0, m.totalAmount1000.high >>> 0).toNumber() : m.totalAmount1000;
                }
                if (m.totalCurrencyCode != null && $Object.hasOwnProperty.call(m, "totalCurrencyCode")) {
                    d.totalCurrencyCode = m.totalCurrencyCode;
                }
                if (m.messageVersion != null && $Object.hasOwnProperty.call(m, "messageVersion")) {
                    d.messageVersion = m.messageVersion;
                }
                if (m.orderRequestMessageId != null && $Object.hasOwnProperty.call(m, "orderRequestMessageId")) {
                    d.orderRequestMessageId = $root.proto.MessageKey.toObject(m.orderRequestMessageId, o, q + 1);
                }
                if (m.catalogType != null && $Object.hasOwnProperty.call(m, "catalogType")) {
                    d.catalogType = m.catalogType;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            OrderMessage.prototype.toJSON = function() {
                return OrderMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            OrderMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.OrderMessage";
            };

            OrderMessage.OrderStatus = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "INQUIRY"] = 1;
                values[valuesById[2] = "ACCEPTED"] = 2;
                values[valuesById[3] = "DECLINED"] = 3;
                return values;
            })();

            OrderMessage.OrderSurface = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "CATALOG"] = 1;
                return values;
            })();

            return OrderMessage;
        })();

        Message.PaymentExtendedMetadata = (function() {

            const PaymentExtendedMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PaymentExtendedMetadata.prototype.type = null;
            PaymentExtendedMetadata.prototype.platform = null;
            PaymentExtendedMetadata.prototype.messageParamsJson = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentExtendedMetadata.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentExtendedMetadata.prototype, "_platform", {
                get: $util.oneOfGetter($oneOfFields = ["platform"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentExtendedMetadata.prototype, "_messageParamsJson", {
                get: $util.oneOfGetter($oneOfFields = ["messageParamsJson"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentExtendedMetadata.create = function(properties) {
                return new PaymentExtendedMetadata(properties);
            };

            PaymentExtendedMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).uint32(m.type);
                if (m.platform != null && $Object.hasOwnProperty.call(m, "platform"))
                    w.uint32(18).string(m.platform);
                if (m.messageParamsJson != null && $Object.hasOwnProperty.call(m, "messageParamsJson"))
                    w.uint32(26).string(m.messageParamsJson);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PaymentExtendedMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentExtendedMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.uint32();
                            m._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.platform = r.stringVerify();
                            m._platform = "platform";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.messageParamsJson = r.stringVerify();
                            m._messageParamsJson = "messageParamsJson";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PaymentExtendedMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PaymentExtendedMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PaymentExtendedMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PaymentExtendedMetadata();
                if (d.type != null) {
                    m.type = d.type >>> 0;
                }
                if (d.platform != null) {
                    m.platform = $String(d.platform);
                }
                if (d.messageParamsJson != null) {
                    m.messageParamsJson = $String(d.messageParamsJson);
                }
                return m;
            };

            PaymentExtendedMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = m.type;
                }
                if (m.platform != null && $Object.hasOwnProperty.call(m, "platform")) {
                    d.platform = m.platform;
                }
                if (m.messageParamsJson != null && $Object.hasOwnProperty.call(m, "messageParamsJson")) {
                    d.messageParamsJson = m.messageParamsJson;
                }
                return d;
            };

            PaymentExtendedMetadata.prototype.toJSON = function() {
                return PaymentExtendedMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentExtendedMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PaymentExtendedMetadata";
            };

            return PaymentExtendedMetadata;
        })();

        Message.PaymentInviteMessage = (function() {

            const PaymentInviteMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PaymentInviteMessage.prototype.serviceType = null;
            PaymentInviteMessage.prototype.expiryTimestamp = null;
            PaymentInviteMessage.prototype.incentiveEligible = null;
            PaymentInviteMessage.prototype.referralId = null;
            PaymentInviteMessage.prototype.inviteType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentInviteMessage.prototype, "_serviceType", {
                get: $util.oneOfGetter($oneOfFields = ["serviceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentInviteMessage.prototype, "_expiryTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentInviteMessage.prototype, "_incentiveEligible", {
                get: $util.oneOfGetter($oneOfFields = ["incentiveEligible"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentInviteMessage.prototype, "_referralId", {
                get: $util.oneOfGetter($oneOfFields = ["referralId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentInviteMessage.prototype, "_inviteType", {
                get: $util.oneOfGetter($oneOfFields = ["inviteType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentInviteMessage.create = function(properties) {
                return new PaymentInviteMessage(properties);
            };

            PaymentInviteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.serviceType != null && $Object.hasOwnProperty.call(m, "serviceType"))
                    w.uint32(8).int32(m.serviceType);
                if (m.expiryTimestamp != null && $Object.hasOwnProperty.call(m, "expiryTimestamp"))
                    w.uint32(16).int64(m.expiryTimestamp);
                if (m.incentiveEligible != null && $Object.hasOwnProperty.call(m, "incentiveEligible"))
                    w.uint32(24).bool(m.incentiveEligible);
                if (m.referralId != null && $Object.hasOwnProperty.call(m, "referralId"))
                    w.uint32(34).string(m.referralId);
                if (m.inviteType != null && $Object.hasOwnProperty.call(m, "inviteType"))
                    w.uint32(40).int32(m.inviteType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PaymentInviteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentInviteMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.serviceType = r.int32();
                            m._serviceType = "serviceType";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.expiryTimestamp = r.int64();
                            m._expiryTimestamp = "expiryTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.incentiveEligible = r.bool();
                            m._incentiveEligible = "incentiveEligible";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.referralId = r.stringVerify();
                            m._referralId = "referralId";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.inviteType = r.int32();
                            m._inviteType = "inviteType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PaymentInviteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PaymentInviteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PaymentInviteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PaymentInviteMessage();
                switch (d.serviceType) {
                case "UNKNOWN":
                case 0:
                    m.serviceType = 0;
                    break;
                case "FBPAY":
                case 1:
                    m.serviceType = 1;
                    break;
                case "NOVI":
                case 2:
                    m.serviceType = 2;
                    break;
                case "UPI":
                case 3:
                    m.serviceType = 3;
                    break;
                case "PIX":
                case 4:
                    m.serviceType = 4;
                    break;
                default:
                    if (typeof d.serviceType === "number" && (d.serviceType | 0) === d.serviceType)
                        m.serviceType = d.serviceType;
                }
                if (d.expiryTimestamp != null) {
                    if ($util.Long)
                        m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp, false);
                    else if (typeof d.expiryTimestamp === "string")
                        m.expiryTimestamp = $parseInt(d.expiryTimestamp, 10);
                    else if (typeof d.expiryTimestamp === "number")
                        m.expiryTimestamp = d.expiryTimestamp;
                    else if (typeof d.expiryTimestamp === "object")
                        m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber();
                }
                if (d.incentiveEligible != null) {
                    m.incentiveEligible = $Boolean(d.incentiveEligible);
                }
                if (d.referralId != null) {
                    m.referralId = $String(d.referralId);
                }
                switch (d.inviteType) {
                case "DEFAULT":
                case 0:
                    m.inviteType = 0;
                    break;
                case "MAPPER":
                case 1:
                    m.inviteType = 1;
                    break;
                default:
                    if (typeof d.inviteType === "number" && (d.inviteType | 0) === d.inviteType)
                        m.inviteType = d.inviteType;
                }
                return m;
            };

            PaymentInviteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.serviceType != null && $Object.hasOwnProperty.call(m, "serviceType")) {
                    d.serviceType = o.enums === $String ? $root.proto.Message.PaymentInviteMessage.ServiceType[m.serviceType] === $undefined ? m.serviceType : $root.proto.Message.PaymentInviteMessage.ServiceType[m.serviceType] : m.serviceType;
                }
                if (m.expiryTimestamp != null && $Object.hasOwnProperty.call(m, "expiryTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.expiryTimestamp = typeof m.expiryTimestamp === "number" ? $BigInt(m.expiryTimestamp) : $util.Long.fromBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.expiryTimestamp === "number")
                        d.expiryTimestamp = o.longs === $String ? $String(m.expiryTimestamp) : m.expiryTimestamp;
                    else
                        d.expiryTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === $Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber() : m.expiryTimestamp;
                }
                if (m.incentiveEligible != null && $Object.hasOwnProperty.call(m, "incentiveEligible")) {
                    d.incentiveEligible = m.incentiveEligible;
                }
                if (m.referralId != null && $Object.hasOwnProperty.call(m, "referralId")) {
                    d.referralId = m.referralId;
                }
                if (m.inviteType != null && $Object.hasOwnProperty.call(m, "inviteType")) {
                    d.inviteType = o.enums === $String ? $root.proto.Message.PaymentInviteMessage.InviteType[m.inviteType] === $undefined ? m.inviteType : $root.proto.Message.PaymentInviteMessage.InviteType[m.inviteType] : m.inviteType;
                }
                return d;
            };

            PaymentInviteMessage.prototype.toJSON = function() {
                return PaymentInviteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentInviteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PaymentInviteMessage";
            };

            PaymentInviteMessage.InviteType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "DEFAULT"] = 0;
                values[valuesById[1] = "MAPPER"] = 1;
                return values;
            })();

            PaymentInviteMessage.ServiceType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "FBPAY"] = 1;
                values[valuesById[2] = "NOVI"] = 2;
                values[valuesById[3] = "UPI"] = 3;
                values[valuesById[4] = "PIX"] = 4;
                return values;
            })();

            return PaymentInviteMessage;
        })();

        Message.PaymentLinkMetadata = (function() {

            const PaymentLinkMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PaymentLinkMetadata.prototype.button = null;
            PaymentLinkMetadata.prototype.header = null;
            PaymentLinkMetadata.prototype.provider = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentLinkMetadata.prototype, "_button", {
                get: $util.oneOfGetter($oneOfFields = ["button"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentLinkMetadata.prototype, "_header", {
                get: $util.oneOfGetter($oneOfFields = ["header"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentLinkMetadata.prototype, "_provider", {
                get: $util.oneOfGetter($oneOfFields = ["provider"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentLinkMetadata.create = function(properties) {
                return new PaymentLinkMetadata(properties);
            };

            PaymentLinkMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.button != null && $Object.hasOwnProperty.call(m, "button"))
                    $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.encode(m.button, w.uint32(10).fork(), q + 1).ldelim();
                if (m.header != null && $Object.hasOwnProperty.call(m, "header"))
                    $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.encode(m.header, w.uint32(18).fork(), q + 1).ldelim();
                if (m.provider != null && $Object.hasOwnProperty.call(m, "provider"))
                    $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.encode(m.provider, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PaymentLinkMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentLinkMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.button = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.decode(r, r.uint32(), $undefined, q + 1, m.button);
                            m._button = "button";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.header = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.decode(r, r.uint32(), $undefined, q + 1, m.header);
                            m._header = "header";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.provider = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.decode(r, r.uint32(), $undefined, q + 1, m.provider);
                            m._provider = "provider";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PaymentLinkMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PaymentLinkMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PaymentLinkMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PaymentLinkMetadata();
                if (d.button != null) {
                    if (!$util.isObject(d.button))
                        throw $TypeError(".proto.Message.PaymentLinkMetadata.button: object expected");
                    m.button = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.fromObject(d.button, q + 1);
                }
                if (d.header != null) {
                    if (!$util.isObject(d.header))
                        throw $TypeError(".proto.Message.PaymentLinkMetadata.header: object expected");
                    m.header = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.fromObject(d.header, q + 1);
                }
                if (d.provider != null) {
                    if (!$util.isObject(d.provider))
                        throw $TypeError(".proto.Message.PaymentLinkMetadata.provider: object expected");
                    m.provider = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.fromObject(d.provider, q + 1);
                }
                return m;
            };

            PaymentLinkMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.button != null && $Object.hasOwnProperty.call(m, "button")) {
                    d.button = $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton.toObject(m.button, o, q + 1);
                }
                if (m.header != null && $Object.hasOwnProperty.call(m, "header")) {
                    d.header = $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.toObject(m.header, o, q + 1);
                }
                if (m.provider != null && $Object.hasOwnProperty.call(m, "provider")) {
                    d.provider = $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider.toObject(m.provider, o, q + 1);
                }
                return d;
            };

            PaymentLinkMetadata.prototype.toJSON = function() {
                return PaymentLinkMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentLinkMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PaymentLinkMetadata";
            };

            PaymentLinkMetadata.PaymentLinkButton = (function() {

                const PaymentLinkButton = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PaymentLinkButton.prototype.displayText = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaymentLinkButton.prototype, "_displayText", {
                    get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PaymentLinkButton.create = function(properties) {
                    return new PaymentLinkButton(properties);
                };

                PaymentLinkButton.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                        w.uint32(10).string(m.displayText);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PaymentLinkButton.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.displayText = r.stringVerify();
                                m._displayText = "displayText";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PaymentLinkButton.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PaymentLinkMetadata.PaymentLinkButton: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PaymentLinkMetadata.PaymentLinkButton();
                    if (d.displayText != null) {
                        m.displayText = $String(d.displayText);
                    }
                    return m;
                };

                PaymentLinkButton.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                        d.displayText = m.displayText;
                    }
                    return d;
                };

                PaymentLinkButton.prototype.toJSON = function() {
                    return PaymentLinkButton.toObject(this, $protobuf.util.toJSONOptions);
                };

                PaymentLinkButton.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PaymentLinkMetadata.PaymentLinkButton";
                };

                return PaymentLinkButton;
            })();

            PaymentLinkMetadata.PaymentLinkHeader = (function() {

                const PaymentLinkHeader = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PaymentLinkHeader.prototype.headerType = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaymentLinkHeader.prototype, "_headerType", {
                    get: $util.oneOfGetter($oneOfFields = ["headerType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PaymentLinkHeader.create = function(properties) {
                    return new PaymentLinkHeader(properties);
                };

                PaymentLinkHeader.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.headerType != null && $Object.hasOwnProperty.call(m, "headerType"))
                        w.uint32(8).int32(m.headerType);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PaymentLinkHeader.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.headerType = r.int32();
                                m._headerType = "headerType";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PaymentLinkHeader.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PaymentLinkMetadata.PaymentLinkHeader: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader();
                    switch (d.headerType) {
                    case "LINK_PREVIEW":
                    case 0:
                        m.headerType = 0;
                        break;
                    case "ORDER":
                    case 1:
                        m.headerType = 1;
                        break;
                    default:
                        if (typeof d.headerType === "number" && (d.headerType | 0) === d.headerType)
                            m.headerType = d.headerType;
                    }
                    return m;
                };

                PaymentLinkHeader.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.headerType != null && $Object.hasOwnProperty.call(m, "headerType")) {
                        d.headerType = o.enums === $String ? $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType[m.headerType] === $undefined ? m.headerType : $root.proto.Message.PaymentLinkMetadata.PaymentLinkHeader.PaymentLinkHeaderType[m.headerType] : m.headerType;
                    }
                    return d;
                };

                PaymentLinkHeader.prototype.toJSON = function() {
                    return PaymentLinkHeader.toObject(this, $protobuf.util.toJSONOptions);
                };

                PaymentLinkHeader.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PaymentLinkMetadata.PaymentLinkHeader";
                };

                PaymentLinkHeader.PaymentLinkHeaderType = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "LINK_PREVIEW"] = 0;
                    values[valuesById[1] = "ORDER"] = 1;
                    return values;
                })();

                return PaymentLinkHeader;
            })();

            PaymentLinkMetadata.PaymentLinkProvider = (function() {

                const PaymentLinkProvider = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PaymentLinkProvider.prototype.paramsJson = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaymentLinkProvider.prototype, "_paramsJson", {
                    get: $util.oneOfGetter($oneOfFields = ["paramsJson"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PaymentLinkProvider.create = function(properties) {
                    return new PaymentLinkProvider(properties);
                };

                PaymentLinkProvider.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.paramsJson != null && $Object.hasOwnProperty.call(m, "paramsJson"))
                        w.uint32(10).string(m.paramsJson);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PaymentLinkProvider.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.paramsJson = r.stringVerify();
                                m._paramsJson = "paramsJson";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PaymentLinkProvider.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PaymentLinkMetadata.PaymentLinkProvider: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PaymentLinkMetadata.PaymentLinkProvider();
                    if (d.paramsJson != null) {
                        m.paramsJson = $String(d.paramsJson);
                    }
                    return m;
                };

                PaymentLinkProvider.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.paramsJson != null && $Object.hasOwnProperty.call(m, "paramsJson")) {
                        d.paramsJson = m.paramsJson;
                    }
                    return d;
                };

                PaymentLinkProvider.prototype.toJSON = function() {
                    return PaymentLinkProvider.toObject(this, $protobuf.util.toJSONOptions);
                };

                PaymentLinkProvider.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PaymentLinkMetadata.PaymentLinkProvider";
                };

                return PaymentLinkProvider;
            })();

            return PaymentLinkMetadata;
        })();

        Message.PaymentReminderMessage = (function() {

            const PaymentReminderMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PaymentReminderMessage.prototype.reminderId = null;
            PaymentReminderMessage.prototype.instanceId = null;
            PaymentReminderMessage.prototype.description = null;
            PaymentReminderMessage.prototype.frequency = null;
            PaymentReminderMessage.prototype.status = null;
            PaymentReminderMessage.prototype.payeeVpa = null;
            PaymentReminderMessage.prototype.payeeJid = null;
            PaymentReminderMessage.prototype.payerJid = null;
            PaymentReminderMessage.prototype.amount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_reminderId", {
                get: $util.oneOfGetter($oneOfFields = ["reminderId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_instanceId", {
                get: $util.oneOfGetter($oneOfFields = ["instanceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_frequency", {
                get: $util.oneOfGetter($oneOfFields = ["frequency"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_payeeVpa", {
                get: $util.oneOfGetter($oneOfFields = ["payeeVpa"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_payeeJid", {
                get: $util.oneOfGetter($oneOfFields = ["payeeJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_payerJid", {
                get: $util.oneOfGetter($oneOfFields = ["payerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentReminderMessage.prototype, "_amount", {
                get: $util.oneOfGetter($oneOfFields = ["amount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentReminderMessage.create = function(properties) {
                return new PaymentReminderMessage(properties);
            };

            PaymentReminderMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.reminderId != null && $Object.hasOwnProperty.call(m, "reminderId"))
                    w.uint32(10).string(m.reminderId);
                if (m.instanceId != null && $Object.hasOwnProperty.call(m, "instanceId"))
                    w.uint32(18).string(m.instanceId);
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(26).string(m.description);
                if (m.frequency != null && $Object.hasOwnProperty.call(m, "frequency"))
                    w.uint32(32).int32(m.frequency);
                if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                    w.uint32(40).int32(m.status);
                if (m.payeeVpa != null && $Object.hasOwnProperty.call(m, "payeeVpa"))
                    w.uint32(50).string(m.payeeVpa);
                if (m.payeeJid != null && $Object.hasOwnProperty.call(m, "payeeJid"))
                    w.uint32(58).string(m.payeeJid);
                if (m.payerJid != null && $Object.hasOwnProperty.call(m, "payerJid"))
                    w.uint32(66).string(m.payerJid);
                if (m.amount != null && $Object.hasOwnProperty.call(m, "amount"))
                    $root.proto.Money.encode(m.amount, w.uint32(74).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PaymentReminderMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PaymentReminderMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.reminderId = r.stringVerify();
                            m._reminderId = "reminderId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.instanceId = r.stringVerify();
                            m._instanceId = "instanceId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.frequency = r.int32();
                            m._frequency = "frequency";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            m._status = "status";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.payeeVpa = r.stringVerify();
                            m._payeeVpa = "payeeVpa";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.payeeJid = r.stringVerify();
                            m._payeeJid = "payeeJid";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.payerJid = r.stringVerify();
                            m._payerJid = "payerJid";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.amount = $root.proto.Money.decode(r, r.uint32(), $undefined, q + 1, m.amount);
                            m._amount = "amount";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PaymentReminderMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PaymentReminderMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PaymentReminderMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PaymentReminderMessage();
                if (d.reminderId != null) {
                    m.reminderId = $String(d.reminderId);
                }
                if (d.instanceId != null) {
                    m.instanceId = $String(d.instanceId);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                switch (d.frequency) {
                case "REMINDER_FREQUENCY_UNKNOWN":
                case 0:
                    m.frequency = 0;
                    break;
                case "WEEKLY":
                case 1:
                    m.frequency = 1;
                    break;
                case "BI_WEEKLY":
                case 2:
                    m.frequency = 2;
                    break;
                case "MONTHLY":
                case 3:
                    m.frequency = 3;
                    break;
                case "QUARTERLY":
                case 4:
                    m.frequency = 4;
                    break;
                default:
                    if (typeof d.frequency === "number" && (d.frequency | 0) === d.frequency)
                        m.frequency = d.frequency;
                }
                switch (d.status) {
                case "REMINDER_STATUS_UNKNOWN":
                case 0:
                    m.status = 0;
                    break;
                case "ACTIVE":
                case 1:
                    m.status = 1;
                    break;
                case "CANCELLED_BY_CREATOR":
                case 2:
                    m.status = 2;
                    break;
                case "STOPPED_BY_RECEIVER":
                case 3:
                    m.status = 3;
                    break;
                case "EXPIRED":
                case 4:
                    m.status = 4;
                    break;
                case "PAID":
                case 5:
                    m.status = 5;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                if (d.payeeVpa != null) {
                    m.payeeVpa = $String(d.payeeVpa);
                }
                if (d.payeeJid != null) {
                    m.payeeJid = $String(d.payeeJid);
                }
                if (d.payerJid != null) {
                    m.payerJid = $String(d.payerJid);
                }
                if (d.amount != null) {
                    if (!$util.isObject(d.amount))
                        throw $TypeError(".proto.Message.PaymentReminderMessage.amount: object expected");
                    m.amount = $root.proto.Money.fromObject(d.amount, q + 1);
                }
                return m;
            };

            PaymentReminderMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.reminderId != null && $Object.hasOwnProperty.call(m, "reminderId")) {
                    d.reminderId = m.reminderId;
                }
                if (m.instanceId != null && $Object.hasOwnProperty.call(m, "instanceId")) {
                    d.instanceId = m.instanceId;
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                if (m.frequency != null && $Object.hasOwnProperty.call(m, "frequency")) {
                    d.frequency = o.enums === $String ? $root.proto.Message.PaymentReminderMessage.ReminderFrequency[m.frequency] === $undefined ? m.frequency : $root.proto.Message.PaymentReminderMessage.ReminderFrequency[m.frequency] : m.frequency;
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.Message.PaymentReminderMessage.ReminderStatus[m.status] === $undefined ? m.status : $root.proto.Message.PaymentReminderMessage.ReminderStatus[m.status] : m.status;
                }
                if (m.payeeVpa != null && $Object.hasOwnProperty.call(m, "payeeVpa")) {
                    d.payeeVpa = m.payeeVpa;
                }
                if (m.payeeJid != null && $Object.hasOwnProperty.call(m, "payeeJid")) {
                    d.payeeJid = m.payeeJid;
                }
                if (m.payerJid != null && $Object.hasOwnProperty.call(m, "payerJid")) {
                    d.payerJid = m.payerJid;
                }
                if (m.amount != null && $Object.hasOwnProperty.call(m, "amount")) {
                    d.amount = $root.proto.Money.toObject(m.amount, o, q + 1);
                }
                return d;
            };

            PaymentReminderMessage.prototype.toJSON = function() {
                return PaymentReminderMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentReminderMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PaymentReminderMessage";
            };

            PaymentReminderMessage.ReminderFrequency = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "REMINDER_FREQUENCY_UNKNOWN"] = 0;
                values[valuesById[1] = "WEEKLY"] = 1;
                values[valuesById[2] = "BI_WEEKLY"] = 2;
                values[valuesById[3] = "MONTHLY"] = 3;
                values[valuesById[4] = "QUARTERLY"] = 4;
                return values;
            })();

            PaymentReminderMessage.ReminderStatus = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "REMINDER_STATUS_UNKNOWN"] = 0;
                values[valuesById[1] = "ACTIVE"] = 1;
                values[valuesById[2] = "CANCELLED_BY_CREATOR"] = 2;
                values[valuesById[3] = "STOPPED_BY_RECEIVER"] = 3;
                values[valuesById[4] = "EXPIRED"] = 4;
                values[valuesById[5] = "PAID"] = 5;
                return values;
            })();

            return PaymentReminderMessage;
        })();

        Message.PeerDataOperationRequestMessage = (function() {

            const PeerDataOperationRequestMessage = function (p) {
                this.requestStickerReupload = [];
                this.requestUrlPreview = [];
                this.placeholderMessageResendRequest = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PeerDataOperationRequestMessage.prototype.peerDataOperationRequestType = null;
            PeerDataOperationRequestMessage.prototype.requestStickerReupload = $util.emptyArray;
            PeerDataOperationRequestMessage.prototype.requestUrlPreview = $util.emptyArray;
            PeerDataOperationRequestMessage.prototype.historySyncOnDemandRequest = null;
            PeerDataOperationRequestMessage.prototype.placeholderMessageResendRequest = $util.emptyArray;
            PeerDataOperationRequestMessage.prototype.fullHistorySyncOnDemandRequest = null;
            PeerDataOperationRequestMessage.prototype.syncdCollectionFatalRecoveryRequest = null;
            PeerDataOperationRequestMessage.prototype.historySyncChunkRetryRequest = null;
            PeerDataOperationRequestMessage.prototype.galaxyFlowAction = null;
            PeerDataOperationRequestMessage.prototype.companionCanonicalUserNonceFetchRequest = null;
            PeerDataOperationRequestMessage.prototype.bizBroadcastInsightsContactListRequest = null;
            PeerDataOperationRequestMessage.prototype.bizBroadcastInsightsRefreshRequest = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_peerDataOperationRequestType", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_historySyncOnDemandRequest", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncOnDemandRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_fullHistorySyncOnDemandRequest", {
                get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_syncdCollectionFatalRecoveryRequest", {
                get: $util.oneOfGetter($oneOfFields = ["syncdCollectionFatalRecoveryRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_historySyncChunkRetryRequest", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncChunkRetryRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_galaxyFlowAction", {
                get: $util.oneOfGetter($oneOfFields = ["galaxyFlowAction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_companionCanonicalUserNonceFetchRequest", {
                get: $util.oneOfGetter($oneOfFields = ["companionCanonicalUserNonceFetchRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_bizBroadcastInsightsContactListRequest", {
                get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsContactListRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestMessage.prototype, "_bizBroadcastInsightsRefreshRequest", {
                get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsRefreshRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PeerDataOperationRequestMessage.create = function(properties) {
                return new PeerDataOperationRequestMessage(properties);
            };

            PeerDataOperationRequestMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.peerDataOperationRequestType != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestType"))
                    w.uint32(8).int32(m.peerDataOperationRequestType);
                if (m.requestStickerReupload != null && m.requestStickerReupload.length) {
                    for (var i = 0; i < m.requestStickerReupload.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.encode(m.requestStickerReupload[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.requestUrlPreview != null && m.requestUrlPreview.length) {
                    for (var i = 0; i < m.requestUrlPreview.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.encode(m.requestUrlPreview[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.historySyncOnDemandRequest != null && $Object.hasOwnProperty.call(m, "historySyncOnDemandRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.encode(m.historySyncOnDemandRequest, w.uint32(34).fork(), q + 1).ldelim();
                if (m.placeholderMessageResendRequest != null && m.placeholderMessageResendRequest.length) {
                    for (var i = 0; i < m.placeholderMessageResendRequest.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.encode(m.placeholderMessageResendRequest[i], w.uint32(42).fork(), q + 1).ldelim();
                }
                if (m.fullHistorySyncOnDemandRequest != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.encode(m.fullHistorySyncOnDemandRequest, w.uint32(50).fork(), q + 1).ldelim();
                if (m.syncdCollectionFatalRecoveryRequest != null && $Object.hasOwnProperty.call(m, "syncdCollectionFatalRecoveryRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.encode(m.syncdCollectionFatalRecoveryRequest, w.uint32(58).fork(), q + 1).ldelim();
                if (m.historySyncChunkRetryRequest != null && $Object.hasOwnProperty.call(m, "historySyncChunkRetryRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.encode(m.historySyncChunkRetryRequest, w.uint32(66).fork(), q + 1).ldelim();
                if (m.galaxyFlowAction != null && $Object.hasOwnProperty.call(m, "galaxyFlowAction"))
                    $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.encode(m.galaxyFlowAction, w.uint32(74).fork(), q + 1).ldelim();
                if (m.companionCanonicalUserNonceFetchRequest != null && $Object.hasOwnProperty.call(m, "companionCanonicalUserNonceFetchRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.encode(m.companionCanonicalUserNonceFetchRequest, w.uint32(82).fork(), q + 1).ldelim();
                if (m.bizBroadcastInsightsContactListRequest != null && $Object.hasOwnProperty.call(m, "bizBroadcastInsightsContactListRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.encode(m.bizBroadcastInsightsContactListRequest, w.uint32(90).fork(), q + 1).ldelim();
                if (m.bizBroadcastInsightsRefreshRequest != null && $Object.hasOwnProperty.call(m, "bizBroadcastInsightsRefreshRequest"))
                    $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.encode(m.bizBroadcastInsightsRefreshRequest, w.uint32(98).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PeerDataOperationRequestMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.peerDataOperationRequestType = r.int32();
                            m._peerDataOperationRequestType = "peerDataOperationRequestType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.requestStickerReupload && m.requestStickerReupload.length))
                                m.requestStickerReupload = [];
                            m.requestStickerReupload.push($root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.requestUrlPreview && m.requestUrlPreview.length))
                                m.requestUrlPreview = [];
                            m.requestUrlPreview.push($root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.historySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.decode(r, r.uint32(), $undefined, q + 1, m.historySyncOnDemandRequest);
                            m._historySyncOnDemandRequest = "historySyncOnDemandRequest";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(m.placeholderMessageResendRequest && m.placeholderMessageResendRequest.length))
                                m.placeholderMessageResendRequest = [];
                            m.placeholderMessageResendRequest.push($root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.fullHistorySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.decode(r, r.uint32(), $undefined, q + 1, m.fullHistorySyncOnDemandRequest);
                            m._fullHistorySyncOnDemandRequest = "fullHistorySyncOnDemandRequest";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.syncdCollectionFatalRecoveryRequest = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.decode(r, r.uint32(), $undefined, q + 1, m.syncdCollectionFatalRecoveryRequest);
                            m._syncdCollectionFatalRecoveryRequest = "syncdCollectionFatalRecoveryRequest";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.historySyncChunkRetryRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.decode(r, r.uint32(), $undefined, q + 1, m.historySyncChunkRetryRequest);
                            m._historySyncChunkRetryRequest = "historySyncChunkRetryRequest";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.galaxyFlowAction = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.decode(r, r.uint32(), $undefined, q + 1, m.galaxyFlowAction);
                            m._galaxyFlowAction = "galaxyFlowAction";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.companionCanonicalUserNonceFetchRequest = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.decode(r, r.uint32(), $undefined, q + 1, m.companionCanonicalUserNonceFetchRequest);
                            m._companionCanonicalUserNonceFetchRequest = "companionCanonicalUserNonceFetchRequest";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.bizBroadcastInsightsContactListRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.decode(r, r.uint32(), $undefined, q + 1, m.bizBroadcastInsightsContactListRequest);
                            m._bizBroadcastInsightsContactListRequest = "bizBroadcastInsightsContactListRequest";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.bizBroadcastInsightsRefreshRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.decode(r, r.uint32(), $undefined, q + 1, m.bizBroadcastInsightsRefreshRequest);
                            m._bizBroadcastInsightsRefreshRequest = "bizBroadcastInsightsRefreshRequest";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PeerDataOperationRequestMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PeerDataOperationRequestMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PeerDataOperationRequestMessage();
                switch (d.peerDataOperationRequestType) {
                case "UPLOAD_STICKER":
                case 0:
                    m.peerDataOperationRequestType = 0;
                    break;
                case "SEND_RECENT_STICKER_BOOTSTRAP":
                case 1:
                    m.peerDataOperationRequestType = 1;
                    break;
                case "GENERATE_LINK_PREVIEW":
                case 2:
                    m.peerDataOperationRequestType = 2;
                    break;
                case "HISTORY_SYNC_ON_DEMAND":
                case 3:
                    m.peerDataOperationRequestType = 3;
                    break;
                case "PLACEHOLDER_MESSAGE_RESEND":
                case 4:
                    m.peerDataOperationRequestType = 4;
                    break;
                case "WAFFLE_LINKING_NONCE_FETCH":
                case 5:
                    m.peerDataOperationRequestType = 5;
                    break;
                case "FULL_HISTORY_SYNC_ON_DEMAND":
                case 6:
                    m.peerDataOperationRequestType = 6;
                    break;
                case "COMPANION_META_NONCE_FETCH":
                case 7:
                    m.peerDataOperationRequestType = 7;
                    break;
                case "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY":
                case 8:
                    m.peerDataOperationRequestType = 8;
                    break;
                case "COMPANION_CANONICAL_USER_NONCE_FETCH":
                case 9:
                    m.peerDataOperationRequestType = 9;
                    break;
                case "HISTORY_SYNC_CHUNK_RETRY":
                case 10:
                    m.peerDataOperationRequestType = 10;
                    break;
                case "GALAXY_FLOW_ACTION":
                case 11:
                    m.peerDataOperationRequestType = 11;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO":
                case 12:
                    m.peerDataOperationRequestType = 12;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_REFRESH":
                case 13:
                    m.peerDataOperationRequestType = 13;
                    break;
                case "CONTACT_REFRESH_REQUEST":
                case 14:
                    m.peerDataOperationRequestType = 14;
                    break;
                default:
                    if (typeof d.peerDataOperationRequestType === "number" && (d.peerDataOperationRequestType | 0) === d.peerDataOperationRequestType)
                        m.peerDataOperationRequestType = d.peerDataOperationRequestType;
                }
                if (d.requestStickerReupload) {
                    if (!$Array.isArray(d.requestStickerReupload))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.requestStickerReupload: array expected");
                    m.requestStickerReupload = $Array(d.requestStickerReupload.length);
                    for (var i = 0; i < d.requestStickerReupload.length; ++i) {
                        if (!$util.isObject(d.requestStickerReupload[i]))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.requestStickerReupload: object expected");
                        m.requestStickerReupload[i] = $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.fromObject(d.requestStickerReupload[i], q + 1);
                    }
                }
                if (d.requestUrlPreview) {
                    if (!$Array.isArray(d.requestUrlPreview))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.requestUrlPreview: array expected");
                    m.requestUrlPreview = $Array(d.requestUrlPreview.length);
                    for (var i = 0; i < d.requestUrlPreview.length; ++i) {
                        if (!$util.isObject(d.requestUrlPreview[i]))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.requestUrlPreview: object expected");
                        m.requestUrlPreview[i] = $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.fromObject(d.requestUrlPreview[i], q + 1);
                    }
                }
                if (d.historySyncOnDemandRequest != null) {
                    if (!$util.isObject(d.historySyncOnDemandRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.historySyncOnDemandRequest: object expected");
                    m.historySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.fromObject(d.historySyncOnDemandRequest, q + 1);
                }
                if (d.placeholderMessageResendRequest) {
                    if (!$Array.isArray(d.placeholderMessageResendRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.placeholderMessageResendRequest: array expected");
                    m.placeholderMessageResendRequest = $Array(d.placeholderMessageResendRequest.length);
                    for (var i = 0; i < d.placeholderMessageResendRequest.length; ++i) {
                        if (!$util.isObject(d.placeholderMessageResendRequest[i]))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.placeholderMessageResendRequest: object expected");
                        m.placeholderMessageResendRequest[i] = $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.fromObject(d.placeholderMessageResendRequest[i], q + 1);
                    }
                }
                if (d.fullHistorySyncOnDemandRequest != null) {
                    if (!$util.isObject(d.fullHistorySyncOnDemandRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.fullHistorySyncOnDemandRequest: object expected");
                    m.fullHistorySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.fromObject(d.fullHistorySyncOnDemandRequest, q + 1);
                }
                if (d.syncdCollectionFatalRecoveryRequest != null) {
                    if (!$util.isObject(d.syncdCollectionFatalRecoveryRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.syncdCollectionFatalRecoveryRequest: object expected");
                    m.syncdCollectionFatalRecoveryRequest = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.fromObject(d.syncdCollectionFatalRecoveryRequest, q + 1);
                }
                if (d.historySyncChunkRetryRequest != null) {
                    if (!$util.isObject(d.historySyncChunkRetryRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.historySyncChunkRetryRequest: object expected");
                    m.historySyncChunkRetryRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.fromObject(d.historySyncChunkRetryRequest, q + 1);
                }
                if (d.galaxyFlowAction != null) {
                    if (!$util.isObject(d.galaxyFlowAction))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.galaxyFlowAction: object expected");
                    m.galaxyFlowAction = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.fromObject(d.galaxyFlowAction, q + 1);
                }
                if (d.companionCanonicalUserNonceFetchRequest != null) {
                    if (!$util.isObject(d.companionCanonicalUserNonceFetchRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.companionCanonicalUserNonceFetchRequest: object expected");
                    m.companionCanonicalUserNonceFetchRequest = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.fromObject(d.companionCanonicalUserNonceFetchRequest, q + 1);
                }
                if (d.bizBroadcastInsightsContactListRequest != null) {
                    if (!$util.isObject(d.bizBroadcastInsightsContactListRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.bizBroadcastInsightsContactListRequest: object expected");
                    m.bizBroadcastInsightsContactListRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.fromObject(d.bizBroadcastInsightsContactListRequest, q + 1);
                }
                if (d.bizBroadcastInsightsRefreshRequest != null) {
                    if (!$util.isObject(d.bizBroadcastInsightsRefreshRequest))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.bizBroadcastInsightsRefreshRequest: object expected");
                    m.bizBroadcastInsightsRefreshRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.fromObject(d.bizBroadcastInsightsRefreshRequest, q + 1);
                }
                return m;
            };

            PeerDataOperationRequestMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.requestStickerReupload = [];
                    d.requestUrlPreview = [];
                    d.placeholderMessageResendRequest = [];
                }
                if (m.peerDataOperationRequestType != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestType")) {
                    d.peerDataOperationRequestType = o.enums === $String ? $root.proto.Message.PeerDataOperationRequestType[m.peerDataOperationRequestType] === $undefined ? m.peerDataOperationRequestType : $root.proto.Message.PeerDataOperationRequestType[m.peerDataOperationRequestType] : m.peerDataOperationRequestType;
                }
                if (m.requestStickerReupload && m.requestStickerReupload.length) {
                    d.requestStickerReupload = $Array(m.requestStickerReupload.length);
                    for (var j = 0; j < m.requestStickerReupload.length; ++j) {
                        d.requestStickerReupload[j] = $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload.toObject(m.requestStickerReupload[j], o, q + 1);
                    }
                }
                if (m.requestUrlPreview && m.requestUrlPreview.length) {
                    d.requestUrlPreview = $Array(m.requestUrlPreview.length);
                    for (var j = 0; j < m.requestUrlPreview.length; ++j) {
                        d.requestUrlPreview[j] = $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview.toObject(m.requestUrlPreview[j], o, q + 1);
                    }
                }
                if (m.historySyncOnDemandRequest != null && $Object.hasOwnProperty.call(m, "historySyncOnDemandRequest")) {
                    d.historySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest.toObject(m.historySyncOnDemandRequest, o, q + 1);
                }
                if (m.placeholderMessageResendRequest && m.placeholderMessageResendRequest.length) {
                    d.placeholderMessageResendRequest = $Array(m.placeholderMessageResendRequest.length);
                    for (var j = 0; j < m.placeholderMessageResendRequest.length; ++j) {
                        d.placeholderMessageResendRequest[j] = $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.toObject(m.placeholderMessageResendRequest[j], o, q + 1);
                    }
                }
                if (m.fullHistorySyncOnDemandRequest != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequest")) {
                    d.fullHistorySyncOnDemandRequest = $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.toObject(m.fullHistorySyncOnDemandRequest, o, q + 1);
                }
                if (m.syncdCollectionFatalRecoveryRequest != null && $Object.hasOwnProperty.call(m, "syncdCollectionFatalRecoveryRequest")) {
                    d.syncdCollectionFatalRecoveryRequest = $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest.toObject(m.syncdCollectionFatalRecoveryRequest, o, q + 1);
                }
                if (m.historySyncChunkRetryRequest != null && $Object.hasOwnProperty.call(m, "historySyncChunkRetryRequest")) {
                    d.historySyncChunkRetryRequest = $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest.toObject(m.historySyncChunkRetryRequest, o, q + 1);
                }
                if (m.galaxyFlowAction != null && $Object.hasOwnProperty.call(m, "galaxyFlowAction")) {
                    d.galaxyFlowAction = $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.toObject(m.galaxyFlowAction, o, q + 1);
                }
                if (m.companionCanonicalUserNonceFetchRequest != null && $Object.hasOwnProperty.call(m, "companionCanonicalUserNonceFetchRequest")) {
                    d.companionCanonicalUserNonceFetchRequest = $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest.toObject(m.companionCanonicalUserNonceFetchRequest, o, q + 1);
                }
                if (m.bizBroadcastInsightsContactListRequest != null && $Object.hasOwnProperty.call(m, "bizBroadcastInsightsContactListRequest")) {
                    d.bizBroadcastInsightsContactListRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest.toObject(m.bizBroadcastInsightsContactListRequest, o, q + 1);
                }
                if (m.bizBroadcastInsightsRefreshRequest != null && $Object.hasOwnProperty.call(m, "bizBroadcastInsightsRefreshRequest")) {
                    d.bizBroadcastInsightsRefreshRequest = $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest.toObject(m.bizBroadcastInsightsRefreshRequest, o, q + 1);
                }
                return d;
            };

            PeerDataOperationRequestMessage.prototype.toJSON = function() {
                return PeerDataOperationRequestMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PeerDataOperationRequestMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PeerDataOperationRequestMessage";
            };

            PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest = (function() {

                const BizBroadcastInsightsContactListRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                BizBroadcastInsightsContactListRequest.prototype.campaignId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BizBroadcastInsightsContactListRequest.prototype, "_campaignId", {
                    get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BizBroadcastInsightsContactListRequest.create = function(properties) {
                    return new BizBroadcastInsightsContactListRequest(properties);
                };

                BizBroadcastInsightsContactListRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId"))
                        w.uint32(10).string(m.campaignId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                BizBroadcastInsightsContactListRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.campaignId = r.stringVerify();
                                m._campaignId = "campaignId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                BizBroadcastInsightsContactListRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest();
                    if (d.campaignId != null) {
                        m.campaignId = $String(d.campaignId);
                    }
                    return m;
                };

                BizBroadcastInsightsContactListRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId")) {
                        d.campaignId = m.campaignId;
                    }
                    return d;
                };

                BizBroadcastInsightsContactListRequest.prototype.toJSON = function() {
                    return BizBroadcastInsightsContactListRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                BizBroadcastInsightsContactListRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsContactListRequest";
                };

                return BizBroadcastInsightsContactListRequest;
            })();

            PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest = (function() {

                const BizBroadcastInsightsRefreshRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                BizBroadcastInsightsRefreshRequest.prototype.campaignId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(BizBroadcastInsightsRefreshRequest.prototype, "_campaignId", {
                    get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                BizBroadcastInsightsRefreshRequest.create = function(properties) {
                    return new BizBroadcastInsightsRefreshRequest(properties);
                };

                BizBroadcastInsightsRefreshRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId"))
                        w.uint32(10).string(m.campaignId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                BizBroadcastInsightsRefreshRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.campaignId = r.stringVerify();
                                m._campaignId = "campaignId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                BizBroadcastInsightsRefreshRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest();
                    if (d.campaignId != null) {
                        m.campaignId = $String(d.campaignId);
                    }
                    return m;
                };

                BizBroadcastInsightsRefreshRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId")) {
                        d.campaignId = m.campaignId;
                    }
                    return d;
                };

                BizBroadcastInsightsRefreshRequest.prototype.toJSON = function() {
                    return BizBroadcastInsightsRefreshRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                BizBroadcastInsightsRefreshRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.BizBroadcastInsightsRefreshRequest";
                };

                return BizBroadcastInsightsRefreshRequest;
            })();

            PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest = (function() {

                const CompanionCanonicalUserNonceFetchRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CompanionCanonicalUserNonceFetchRequest.prototype.registrationTraceId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CompanionCanonicalUserNonceFetchRequest.prototype, "_registrationTraceId", {
                    get: $util.oneOfGetter($oneOfFields = ["registrationTraceId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CompanionCanonicalUserNonceFetchRequest.create = function(properties) {
                    return new CompanionCanonicalUserNonceFetchRequest(properties);
                };

                CompanionCanonicalUserNonceFetchRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.registrationTraceId != null && $Object.hasOwnProperty.call(m, "registrationTraceId"))
                        w.uint32(10).string(m.registrationTraceId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CompanionCanonicalUserNonceFetchRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.registrationTraceId = r.stringVerify();
                                m._registrationTraceId = "registrationTraceId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CompanionCanonicalUserNonceFetchRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest();
                    if (d.registrationTraceId != null) {
                        m.registrationTraceId = $String(d.registrationTraceId);
                    }
                    return m;
                };

                CompanionCanonicalUserNonceFetchRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.registrationTraceId != null && $Object.hasOwnProperty.call(m, "registrationTraceId")) {
                        d.registrationTraceId = m.registrationTraceId;
                    }
                    return d;
                };

                CompanionCanonicalUserNonceFetchRequest.prototype.toJSON = function() {
                    return CompanionCanonicalUserNonceFetchRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                CompanionCanonicalUserNonceFetchRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.CompanionCanonicalUserNonceFetchRequest";
                };

                return CompanionCanonicalUserNonceFetchRequest;
            })();

            PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest = (function() {

                const FullHistorySyncOnDemandRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                FullHistorySyncOnDemandRequest.prototype.requestMetadata = null;
                FullHistorySyncOnDemandRequest.prototype.historySyncConfig = null;
                FullHistorySyncOnDemandRequest.prototype.fullHistorySyncOnDemandConfig = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_requestMetadata", {
                    get: $util.oneOfGetter($oneOfFields = ["requestMetadata"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_historySyncConfig", {
                    get: $util.oneOfGetter($oneOfFields = ["historySyncConfig"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(FullHistorySyncOnDemandRequest.prototype, "_fullHistorySyncOnDemandConfig", {
                    get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandConfig"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                FullHistorySyncOnDemandRequest.create = function(properties) {
                    return new FullHistorySyncOnDemandRequest(properties);
                };

                FullHistorySyncOnDemandRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.requestMetadata != null && $Object.hasOwnProperty.call(m, "requestMetadata"))
                        $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.encode(m.requestMetadata, w.uint32(10).fork(), q + 1).ldelim();
                    if (m.historySyncConfig != null && $Object.hasOwnProperty.call(m, "historySyncConfig"))
                        $root.proto.DeviceProps.HistorySyncConfig.encode(m.historySyncConfig, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.fullHistorySyncOnDemandConfig != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandConfig"))
                        $root.proto.Message.FullHistorySyncOnDemandConfig.encode(m.fullHistorySyncOnDemandConfig, w.uint32(26).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                FullHistorySyncOnDemandRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.decode(r, r.uint32(), $undefined, q + 1, m.requestMetadata);
                                m._requestMetadata = "requestMetadata";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.decode(r, r.uint32(), $undefined, q + 1, m.historySyncConfig);
                                m._historySyncConfig = "historySyncConfig";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.fullHistorySyncOnDemandConfig = $root.proto.Message.FullHistorySyncOnDemandConfig.decode(r, r.uint32(), $undefined, q + 1, m.fullHistorySyncOnDemandConfig);
                                m._fullHistorySyncOnDemandConfig = "fullHistorySyncOnDemandConfig";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                FullHistorySyncOnDemandRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest();
                    if (d.requestMetadata != null) {
                        if (!$util.isObject(d.requestMetadata))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.requestMetadata: object expected");
                        m.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.fromObject(d.requestMetadata, q + 1);
                    }
                    if (d.historySyncConfig != null) {
                        if (!$util.isObject(d.historySyncConfig))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.historySyncConfig: object expected");
                        m.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.fromObject(d.historySyncConfig, q + 1);
                    }
                    if (d.fullHistorySyncOnDemandConfig != null) {
                        if (!$util.isObject(d.fullHistorySyncOnDemandConfig))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest.fullHistorySyncOnDemandConfig: object expected");
                        m.fullHistorySyncOnDemandConfig = $root.proto.Message.FullHistorySyncOnDemandConfig.fromObject(d.fullHistorySyncOnDemandConfig, q + 1);
                    }
                    return m;
                };

                FullHistorySyncOnDemandRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.requestMetadata != null && $Object.hasOwnProperty.call(m, "requestMetadata")) {
                        d.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.toObject(m.requestMetadata, o, q + 1);
                    }
                    if (m.historySyncConfig != null && $Object.hasOwnProperty.call(m, "historySyncConfig")) {
                        d.historySyncConfig = $root.proto.DeviceProps.HistorySyncConfig.toObject(m.historySyncConfig, o, q + 1);
                    }
                    if (m.fullHistorySyncOnDemandConfig != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandConfig")) {
                        d.fullHistorySyncOnDemandConfig = $root.proto.Message.FullHistorySyncOnDemandConfig.toObject(m.fullHistorySyncOnDemandConfig, o, q + 1);
                    }
                    return d;
                };

                FullHistorySyncOnDemandRequest.prototype.toJSON = function() {
                    return FullHistorySyncOnDemandRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                FullHistorySyncOnDemandRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.FullHistorySyncOnDemandRequest";
                };

                return FullHistorySyncOnDemandRequest;
            })();

            PeerDataOperationRequestMessage.GalaxyFlowAction = (function() {

                const GalaxyFlowAction = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                GalaxyFlowAction.prototype.type = null;
                GalaxyFlowAction.prototype.flowId = null;
                GalaxyFlowAction.prototype.stanzaId = null;
                GalaxyFlowAction.prototype.galaxyFlowDownloadRequestId = null;
                GalaxyFlowAction.prototype.agmId = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(GalaxyFlowAction.prototype, "_type", {
                    get: $util.oneOfGetter($oneOfFields = ["type"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(GalaxyFlowAction.prototype, "_flowId", {
                    get: $util.oneOfGetter($oneOfFields = ["flowId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(GalaxyFlowAction.prototype, "_stanzaId", {
                    get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(GalaxyFlowAction.prototype, "_galaxyFlowDownloadRequestId", {
                    get: $util.oneOfGetter($oneOfFields = ["galaxyFlowDownloadRequestId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(GalaxyFlowAction.prototype, "_agmId", {
                    get: $util.oneOfGetter($oneOfFields = ["agmId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                GalaxyFlowAction.create = function(properties) {
                    return new GalaxyFlowAction(properties);
                };

                GalaxyFlowAction.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                        w.uint32(8).int32(m.type);
                    if (m.flowId != null && $Object.hasOwnProperty.call(m, "flowId"))
                        w.uint32(18).string(m.flowId);
                    if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                        w.uint32(26).string(m.stanzaId);
                    if (m.galaxyFlowDownloadRequestId != null && $Object.hasOwnProperty.call(m, "galaxyFlowDownloadRequestId"))
                        w.uint32(34).string(m.galaxyFlowDownloadRequestId);
                    if (m.agmId != null && $Object.hasOwnProperty.call(m, "agmId"))
                        w.uint32(42).string(m.agmId);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                GalaxyFlowAction.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.type = r.int32();
                                m._type = "type";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.flowId = r.stringVerify();
                                m._flowId = "flowId";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.stanzaId = r.stringVerify();
                                m._stanzaId = "stanzaId";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.galaxyFlowDownloadRequestId = r.stringVerify();
                                m._galaxyFlowDownloadRequestId = "galaxyFlowDownloadRequestId";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.agmId = r.stringVerify();
                                m._agmId = "agmId";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                GalaxyFlowAction.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction();
                    switch (d.type) {
                    case "NOTIFY_LAUNCH":
                    case 1:
                        m.type = 1;
                        break;
                    case "DOWNLOAD_RESPONSES":
                    case 2:
                        m.type = 2;
                        break;
                    default:
                        if (typeof d.type === "number" && (d.type | 0) === d.type)
                            m.type = d.type;
                    }
                    if (d.flowId != null) {
                        m.flowId = $String(d.flowId);
                    }
                    if (d.stanzaId != null) {
                        m.stanzaId = $String(d.stanzaId);
                    }
                    if (d.galaxyFlowDownloadRequestId != null) {
                        m.galaxyFlowDownloadRequestId = $String(d.galaxyFlowDownloadRequestId);
                    }
                    if (d.agmId != null) {
                        m.agmId = $String(d.agmId);
                    }
                    return m;
                };

                GalaxyFlowAction.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                        d.type = o.enums === $String ? $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType[m.type] === $undefined ? m.type : $root.proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction.GalaxyFlowActionType[m.type] : m.type;
                    }
                    if (m.flowId != null && $Object.hasOwnProperty.call(m, "flowId")) {
                        d.flowId = m.flowId;
                    }
                    if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                        d.stanzaId = m.stanzaId;
                    }
                    if (m.galaxyFlowDownloadRequestId != null && $Object.hasOwnProperty.call(m, "galaxyFlowDownloadRequestId")) {
                        d.galaxyFlowDownloadRequestId = m.galaxyFlowDownloadRequestId;
                    }
                    if (m.agmId != null && $Object.hasOwnProperty.call(m, "agmId")) {
                        d.agmId = m.agmId;
                    }
                    return d;
                };

                GalaxyFlowAction.prototype.toJSON = function() {
                    return GalaxyFlowAction.toObject(this, $protobuf.util.toJSONOptions);
                };

                GalaxyFlowAction.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.GalaxyFlowAction";
                };

                GalaxyFlowAction.GalaxyFlowActionType = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[1] = "NOTIFY_LAUNCH"] = 1;
                    values[valuesById[2] = "DOWNLOAD_RESPONSES"] = 2;
                    return values;
                })();

                return GalaxyFlowAction;
            })();

            PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest = (function() {

                const HistorySyncChunkRetryRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                HistorySyncChunkRetryRequest.prototype.syncType = null;
                HistorySyncChunkRetryRequest.prototype.chunkOrder = null;
                HistorySyncChunkRetryRequest.prototype.chunkNotificationId = null;
                HistorySyncChunkRetryRequest.prototype.regenerateChunk = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_syncType", {
                    get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_chunkOrder", {
                    get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_chunkNotificationId", {
                    get: $util.oneOfGetter($oneOfFields = ["chunkNotificationId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncChunkRetryRequest.prototype, "_regenerateChunk", {
                    get: $util.oneOfGetter($oneOfFields = ["regenerateChunk"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HistorySyncChunkRetryRequest.create = function(properties) {
                    return new HistorySyncChunkRetryRequest(properties);
                };

                HistorySyncChunkRetryRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType"))
                        w.uint32(8).int32(m.syncType);
                    if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder"))
                        w.uint32(16).uint32(m.chunkOrder);
                    if (m.chunkNotificationId != null && $Object.hasOwnProperty.call(m, "chunkNotificationId"))
                        w.uint32(26).string(m.chunkNotificationId);
                    if (m.regenerateChunk != null && $Object.hasOwnProperty.call(m, "regenerateChunk"))
                        w.uint32(32).bool(m.regenerateChunk);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                HistorySyncChunkRetryRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.syncType = r.int32();
                                m._syncType = "syncType";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.chunkOrder = r.uint32();
                                m._chunkOrder = "chunkOrder";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.chunkNotificationId = r.stringVerify();
                                m._chunkNotificationId = "chunkNotificationId";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.regenerateChunk = r.bool();
                                m._regenerateChunk = "regenerateChunk";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                HistorySyncChunkRetryRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest();
                    switch (d.syncType) {
                    case "INITIAL_BOOTSTRAP":
                    case 0:
                        m.syncType = 0;
                        break;
                    case "INITIAL_STATUS_V3":
                    case 1:
                        m.syncType = 1;
                        break;
                    case "FULL":
                    case 2:
                        m.syncType = 2;
                        break;
                    case "RECENT":
                    case 3:
                        m.syncType = 3;
                        break;
                    case "PUSH_NAME":
                    case 4:
                        m.syncType = 4;
                        break;
                    case "NON_BLOCKING_DATA":
                    case 5:
                        m.syncType = 5;
                        break;
                    case "ON_DEMAND":
                    case 6:
                        m.syncType = 6;
                        break;
                    case "NO_HISTORY":
                    case 7:
                        m.syncType = 7;
                        break;
                    case "MESSAGE_ACCESS_STATUS":
                    case 8:
                        m.syncType = 8;
                        break;
                    default:
                        if (typeof d.syncType === "number" && (d.syncType | 0) === d.syncType)
                            m.syncType = d.syncType;
                    }
                    if (d.chunkOrder != null) {
                        m.chunkOrder = d.chunkOrder >>> 0;
                    }
                    if (d.chunkNotificationId != null) {
                        m.chunkNotificationId = $String(d.chunkNotificationId);
                    }
                    if (d.regenerateChunk != null) {
                        m.regenerateChunk = $Boolean(d.regenerateChunk);
                    }
                    return m;
                };

                HistorySyncChunkRetryRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType")) {
                        d.syncType = o.enums === $String ? $root.proto.Message.HistorySyncType[m.syncType] === $undefined ? m.syncType : $root.proto.Message.HistorySyncType[m.syncType] : m.syncType;
                    }
                    if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder")) {
                        d.chunkOrder = m.chunkOrder;
                    }
                    if (m.chunkNotificationId != null && $Object.hasOwnProperty.call(m, "chunkNotificationId")) {
                        d.chunkNotificationId = m.chunkNotificationId;
                    }
                    if (m.regenerateChunk != null && $Object.hasOwnProperty.call(m, "regenerateChunk")) {
                        d.regenerateChunk = m.regenerateChunk;
                    }
                    return d;
                };

                HistorySyncChunkRetryRequest.prototype.toJSON = function() {
                    return HistorySyncChunkRetryRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                HistorySyncChunkRetryRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.HistorySyncChunkRetryRequest";
                };

                return HistorySyncChunkRetryRequest;
            })();

            PeerDataOperationRequestMessage.HistorySyncOnDemandRequest = (function() {

                const HistorySyncOnDemandRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                HistorySyncOnDemandRequest.prototype.chatJid = null;
                HistorySyncOnDemandRequest.prototype.oldestMsgId = null;
                HistorySyncOnDemandRequest.prototype.oldestMsgFromMe = null;
                HistorySyncOnDemandRequest.prototype.onDemandMsgCount = null;
                HistorySyncOnDemandRequest.prototype.oldestMsgTimestampMs = null;
                HistorySyncOnDemandRequest.prototype.accountLid = null;
                HistorySyncOnDemandRequest.prototype.supportInlineResponse = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_chatJid", {
                    get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgId", {
                    get: $util.oneOfGetter($oneOfFields = ["oldestMsgId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgFromMe", {
                    get: $util.oneOfGetter($oneOfFields = ["oldestMsgFromMe"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_onDemandMsgCount", {
                    get: $util.oneOfGetter($oneOfFields = ["onDemandMsgCount"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_oldestMsgTimestampMs", {
                    get: $util.oneOfGetter($oneOfFields = ["oldestMsgTimestampMs"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_accountLid", {
                    get: $util.oneOfGetter($oneOfFields = ["accountLid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HistorySyncOnDemandRequest.prototype, "_supportInlineResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["supportInlineResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HistorySyncOnDemandRequest.create = function(properties) {
                    return new HistorySyncOnDemandRequest(properties);
                };

                HistorySyncOnDemandRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid"))
                        w.uint32(10).string(m.chatJid);
                    if (m.oldestMsgId != null && $Object.hasOwnProperty.call(m, "oldestMsgId"))
                        w.uint32(18).string(m.oldestMsgId);
                    if (m.oldestMsgFromMe != null && $Object.hasOwnProperty.call(m, "oldestMsgFromMe"))
                        w.uint32(24).bool(m.oldestMsgFromMe);
                    if (m.onDemandMsgCount != null && $Object.hasOwnProperty.call(m, "onDemandMsgCount"))
                        w.uint32(32).int32(m.onDemandMsgCount);
                    if (m.oldestMsgTimestampMs != null && $Object.hasOwnProperty.call(m, "oldestMsgTimestampMs"))
                        w.uint32(40).int64(m.oldestMsgTimestampMs);
                    if (m.accountLid != null && $Object.hasOwnProperty.call(m, "accountLid"))
                        w.uint32(50).string(m.accountLid);
                    if (m.supportInlineResponse != null && $Object.hasOwnProperty.call(m, "supportInlineResponse"))
                        w.uint32(56).bool(m.supportInlineResponse);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                HistorySyncOnDemandRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.chatJid = r.stringVerify();
                                m._chatJid = "chatJid";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.oldestMsgId = r.stringVerify();
                                m._oldestMsgId = "oldestMsgId";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.oldestMsgFromMe = r.bool();
                                m._oldestMsgFromMe = "oldestMsgFromMe";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.onDemandMsgCount = r.int32();
                                m._onDemandMsgCount = "onDemandMsgCount";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.oldestMsgTimestampMs = r.int64();
                                m._oldestMsgTimestampMs = "oldestMsgTimestampMs";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.accountLid = r.stringVerify();
                                m._accountLid = "accountLid";
                                continue;
                            }
                        case 7: {
                                if (u !== 0)
                                    break;
                                m.supportInlineResponse = r.bool();
                                m._supportInlineResponse = "supportInlineResponse";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                HistorySyncOnDemandRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest();
                    if (d.chatJid != null) {
                        m.chatJid = $String(d.chatJid);
                    }
                    if (d.oldestMsgId != null) {
                        m.oldestMsgId = $String(d.oldestMsgId);
                    }
                    if (d.oldestMsgFromMe != null) {
                        m.oldestMsgFromMe = $Boolean(d.oldestMsgFromMe);
                    }
                    if (d.onDemandMsgCount != null) {
                        m.onDemandMsgCount = d.onDemandMsgCount | 0;
                    }
                    if (d.oldestMsgTimestampMs != null) {
                        if ($util.Long)
                            m.oldestMsgTimestampMs = $util.Long.fromValue(d.oldestMsgTimestampMs, false);
                        else if (typeof d.oldestMsgTimestampMs === "string")
                            m.oldestMsgTimestampMs = $parseInt(d.oldestMsgTimestampMs, 10);
                        else if (typeof d.oldestMsgTimestampMs === "number")
                            m.oldestMsgTimestampMs = d.oldestMsgTimestampMs;
                        else if (typeof d.oldestMsgTimestampMs === "object")
                            m.oldestMsgTimestampMs = new $util.LongBits(d.oldestMsgTimestampMs.low >>> 0, d.oldestMsgTimestampMs.high >>> 0).toNumber();
                    }
                    if (d.accountLid != null) {
                        m.accountLid = $String(d.accountLid);
                    }
                    if (d.supportInlineResponse != null) {
                        m.supportInlineResponse = $Boolean(d.supportInlineResponse);
                    }
                    return m;
                };

                HistorySyncOnDemandRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid")) {
                        d.chatJid = m.chatJid;
                    }
                    if (m.oldestMsgId != null && $Object.hasOwnProperty.call(m, "oldestMsgId")) {
                        d.oldestMsgId = m.oldestMsgId;
                    }
                    if (m.oldestMsgFromMe != null && $Object.hasOwnProperty.call(m, "oldestMsgFromMe")) {
                        d.oldestMsgFromMe = m.oldestMsgFromMe;
                    }
                    if (m.onDemandMsgCount != null && $Object.hasOwnProperty.call(m, "onDemandMsgCount")) {
                        d.onDemandMsgCount = m.onDemandMsgCount;
                    }
                    if (m.oldestMsgTimestampMs != null && $Object.hasOwnProperty.call(m, "oldestMsgTimestampMs")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.oldestMsgTimestampMs = typeof m.oldestMsgTimestampMs === "number" ? $BigInt(m.oldestMsgTimestampMs) : $util.Long.fromBits(m.oldestMsgTimestampMs.low >>> 0, m.oldestMsgTimestampMs.high >>> 0, false).toBigInt();
                        else if (typeof m.oldestMsgTimestampMs === "number")
                            d.oldestMsgTimestampMs = o.longs === $String ? $String(m.oldestMsgTimestampMs) : m.oldestMsgTimestampMs;
                        else
                            d.oldestMsgTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.oldestMsgTimestampMs) : o.longs === $Number ? new $util.LongBits(m.oldestMsgTimestampMs.low >>> 0, m.oldestMsgTimestampMs.high >>> 0).toNumber() : m.oldestMsgTimestampMs;
                    }
                    if (m.accountLid != null && $Object.hasOwnProperty.call(m, "accountLid")) {
                        d.accountLid = m.accountLid;
                    }
                    if (m.supportInlineResponse != null && $Object.hasOwnProperty.call(m, "supportInlineResponse")) {
                        d.supportInlineResponse = m.supportInlineResponse;
                    }
                    return d;
                };

                HistorySyncOnDemandRequest.prototype.toJSON = function() {
                    return HistorySyncOnDemandRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                HistorySyncOnDemandRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.HistorySyncOnDemandRequest";
                };

                return HistorySyncOnDemandRequest;
            })();

            PeerDataOperationRequestMessage.PlaceholderMessageResendRequest = (function() {

                const PlaceholderMessageResendRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PlaceholderMessageResendRequest.prototype.messageKey = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PlaceholderMessageResendRequest.prototype, "_messageKey", {
                    get: $util.oneOfGetter($oneOfFields = ["messageKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PlaceholderMessageResendRequest.create = function(properties) {
                    return new PlaceholderMessageResendRequest(properties);
                };

                PlaceholderMessageResendRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey"))
                        $root.proto.MessageKey.encode(m.messageKey, w.uint32(10).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PlaceholderMessageResendRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.messageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.messageKey);
                                m._messageKey = "messageKey";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PlaceholderMessageResendRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest();
                    if (d.messageKey != null) {
                        if (!$util.isObject(d.messageKey))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest.messageKey: object expected");
                        m.messageKey = $root.proto.MessageKey.fromObject(d.messageKey, q + 1);
                    }
                    return m;
                };

                PlaceholderMessageResendRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.messageKey != null && $Object.hasOwnProperty.call(m, "messageKey")) {
                        d.messageKey = $root.proto.MessageKey.toObject(m.messageKey, o, q + 1);
                    }
                    return d;
                };

                PlaceholderMessageResendRequest.prototype.toJSON = function() {
                    return PlaceholderMessageResendRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                PlaceholderMessageResendRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.PlaceholderMessageResendRequest";
                };

                return PlaceholderMessageResendRequest;
            })();

            PeerDataOperationRequestMessage.RequestStickerReupload = (function() {

                const RequestStickerReupload = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                RequestStickerReupload.prototype.fileSha256 = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RequestStickerReupload.prototype, "_fileSha256", {
                    get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                RequestStickerReupload.create = function(properties) {
                    return new RequestStickerReupload(properties);
                };

                RequestStickerReupload.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                        w.uint32(10).string(m.fileSha256);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                RequestStickerReupload.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.fileSha256 = r.stringVerify();
                                m._fileSha256 = "fileSha256";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                RequestStickerReupload.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload();
                    if (d.fileSha256 != null) {
                        m.fileSha256 = $String(d.fileSha256);
                    }
                    return m;
                };

                RequestStickerReupload.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                        d.fileSha256 = m.fileSha256;
                    }
                    return d;
                };

                RequestStickerReupload.prototype.toJSON = function() {
                    return RequestStickerReupload.toObject(this, $protobuf.util.toJSONOptions);
                };

                RequestStickerReupload.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.RequestStickerReupload";
                };

                return RequestStickerReupload;
            })();

            PeerDataOperationRequestMessage.RequestUrlPreview = (function() {

                const RequestUrlPreview = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                RequestUrlPreview.prototype.url = null;
                RequestUrlPreview.prototype.includeHqThumbnail = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RequestUrlPreview.prototype, "_url", {
                    get: $util.oneOfGetter($oneOfFields = ["url"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RequestUrlPreview.prototype, "_includeHqThumbnail", {
                    get: $util.oneOfGetter($oneOfFields = ["includeHqThumbnail"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                RequestUrlPreview.create = function(properties) {
                    return new RequestUrlPreview(properties);
                };

                RequestUrlPreview.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                        w.uint32(10).string(m.url);
                    if (m.includeHqThumbnail != null && $Object.hasOwnProperty.call(m, "includeHqThumbnail"))
                        w.uint32(16).bool(m.includeHqThumbnail);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                RequestUrlPreview.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.url = r.stringVerify();
                                m._url = "url";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.includeHqThumbnail = r.bool();
                                m._includeHqThumbnail = "includeHqThumbnail";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                RequestUrlPreview.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview();
                    if (d.url != null) {
                        m.url = $String(d.url);
                    }
                    if (d.includeHqThumbnail != null) {
                        m.includeHqThumbnail = $Boolean(d.includeHqThumbnail);
                    }
                    return m;
                };

                RequestUrlPreview.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                        d.url = m.url;
                    }
                    if (m.includeHqThumbnail != null && $Object.hasOwnProperty.call(m, "includeHqThumbnail")) {
                        d.includeHqThumbnail = m.includeHqThumbnail;
                    }
                    return d;
                };

                RequestUrlPreview.prototype.toJSON = function() {
                    return RequestUrlPreview.toObject(this, $protobuf.util.toJSONOptions);
                };

                RequestUrlPreview.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.RequestUrlPreview";
                };

                return RequestUrlPreview;
            })();

            PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest = (function() {

                const SyncDCollectionFatalRecoveryRequest = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                SyncDCollectionFatalRecoveryRequest.prototype.collectionName = null;
                SyncDCollectionFatalRecoveryRequest.prototype.timestamp = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SyncDCollectionFatalRecoveryRequest.prototype, "_collectionName", {
                    get: $util.oneOfGetter($oneOfFields = ["collectionName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SyncDCollectionFatalRecoveryRequest.prototype, "_timestamp", {
                    get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SyncDCollectionFatalRecoveryRequest.create = function(properties) {
                    return new SyncDCollectionFatalRecoveryRequest(properties);
                };

                SyncDCollectionFatalRecoveryRequest.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.collectionName != null && $Object.hasOwnProperty.call(m, "collectionName"))
                        w.uint32(10).string(m.collectionName);
                    if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                        w.uint32(16).int64(m.timestamp);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                SyncDCollectionFatalRecoveryRequest.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.collectionName = r.stringVerify();
                                m._collectionName = "collectionName";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.timestamp = r.int64();
                                m._timestamp = "timestamp";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                SyncDCollectionFatalRecoveryRequest.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest();
                    if (d.collectionName != null) {
                        m.collectionName = $String(d.collectionName);
                    }
                    if (d.timestamp != null) {
                        if ($util.Long)
                            m.timestamp = $util.Long.fromValue(d.timestamp, false);
                        else if (typeof d.timestamp === "string")
                            m.timestamp = $parseInt(d.timestamp, 10);
                        else if (typeof d.timestamp === "number")
                            m.timestamp = d.timestamp;
                        else if (typeof d.timestamp === "object")
                            m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                    }
                    return m;
                };

                SyncDCollectionFatalRecoveryRequest.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.collectionName != null && $Object.hasOwnProperty.call(m, "collectionName")) {
                        d.collectionName = m.collectionName;
                    }
                    if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                        else if (typeof m.timestamp === "number")
                            d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                        else
                            d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                    }
                    return d;
                };

                SyncDCollectionFatalRecoveryRequest.prototype.toJSON = function() {
                    return SyncDCollectionFatalRecoveryRequest.toObject(this, $protobuf.util.toJSONOptions);
                };

                SyncDCollectionFatalRecoveryRequest.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestMessage.SyncDCollectionFatalRecoveryRequest";
                };

                return SyncDCollectionFatalRecoveryRequest;
            })();

            return PeerDataOperationRequestMessage;
        })();

        Message.PeerDataOperationRequestResponseMessage = (function() {

            const PeerDataOperationRequestResponseMessage = function (p) {
                this.peerDataOperationResult = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PeerDataOperationRequestResponseMessage.prototype.peerDataOperationRequestType = null;
            PeerDataOperationRequestResponseMessage.prototype.stanzaId = null;
            PeerDataOperationRequestResponseMessage.prototype.peerDataOperationResult = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestResponseMessage.prototype, "_peerDataOperationRequestType", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PeerDataOperationRequestResponseMessage.prototype, "_stanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PeerDataOperationRequestResponseMessage.create = function(properties) {
                return new PeerDataOperationRequestResponseMessage(properties);
            };

            PeerDataOperationRequestResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.peerDataOperationRequestType != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestType"))
                    w.uint32(8).int32(m.peerDataOperationRequestType);
                if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                    w.uint32(18).string(m.stanzaId);
                if (m.peerDataOperationResult != null && m.peerDataOperationResult.length) {
                    for (var i = 0; i < m.peerDataOperationResult.length; ++i)
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.encode(m.peerDataOperationResult[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PeerDataOperationRequestResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.peerDataOperationRequestType = r.int32();
                            m._peerDataOperationRequestType = "peerDataOperationRequestType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.stanzaId = r.stringVerify();
                            m._stanzaId = "stanzaId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.peerDataOperationResult && m.peerDataOperationResult.length))
                                m.peerDataOperationResult = [];
                            m.peerDataOperationResult.push($root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PeerDataOperationRequestResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage();
                switch (d.peerDataOperationRequestType) {
                case "UPLOAD_STICKER":
                case 0:
                    m.peerDataOperationRequestType = 0;
                    break;
                case "SEND_RECENT_STICKER_BOOTSTRAP":
                case 1:
                    m.peerDataOperationRequestType = 1;
                    break;
                case "GENERATE_LINK_PREVIEW":
                case 2:
                    m.peerDataOperationRequestType = 2;
                    break;
                case "HISTORY_SYNC_ON_DEMAND":
                case 3:
                    m.peerDataOperationRequestType = 3;
                    break;
                case "PLACEHOLDER_MESSAGE_RESEND":
                case 4:
                    m.peerDataOperationRequestType = 4;
                    break;
                case "WAFFLE_LINKING_NONCE_FETCH":
                case 5:
                    m.peerDataOperationRequestType = 5;
                    break;
                case "FULL_HISTORY_SYNC_ON_DEMAND":
                case 6:
                    m.peerDataOperationRequestType = 6;
                    break;
                case "COMPANION_META_NONCE_FETCH":
                case 7:
                    m.peerDataOperationRequestType = 7;
                    break;
                case "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY":
                case 8:
                    m.peerDataOperationRequestType = 8;
                    break;
                case "COMPANION_CANONICAL_USER_NONCE_FETCH":
                case 9:
                    m.peerDataOperationRequestType = 9;
                    break;
                case "HISTORY_SYNC_CHUNK_RETRY":
                case 10:
                    m.peerDataOperationRequestType = 10;
                    break;
                case "GALAXY_FLOW_ACTION":
                case 11:
                    m.peerDataOperationRequestType = 11;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO":
                case 12:
                    m.peerDataOperationRequestType = 12;
                    break;
                case "BUSINESS_BROADCAST_INSIGHTS_REFRESH":
                case 13:
                    m.peerDataOperationRequestType = 13;
                    break;
                case "CONTACT_REFRESH_REQUEST":
                case 14:
                    m.peerDataOperationRequestType = 14;
                    break;
                default:
                    if (typeof d.peerDataOperationRequestType === "number" && (d.peerDataOperationRequestType | 0) === d.peerDataOperationRequestType)
                        m.peerDataOperationRequestType = d.peerDataOperationRequestType;
                }
                if (d.stanzaId != null) {
                    m.stanzaId = $String(d.stanzaId);
                }
                if (d.peerDataOperationResult) {
                    if (!$Array.isArray(d.peerDataOperationResult))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.peerDataOperationResult: array expected");
                    m.peerDataOperationResult = $Array(d.peerDataOperationResult.length);
                    for (var i = 0; i < d.peerDataOperationResult.length; ++i) {
                        if (!$util.isObject(d.peerDataOperationResult[i]))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.peerDataOperationResult: object expected");
                        m.peerDataOperationResult[i] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.fromObject(d.peerDataOperationResult[i], q + 1);
                    }
                }
                return m;
            };

            PeerDataOperationRequestResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.peerDataOperationResult = [];
                }
                if (m.peerDataOperationRequestType != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestType")) {
                    d.peerDataOperationRequestType = o.enums === $String ? $root.proto.Message.PeerDataOperationRequestType[m.peerDataOperationRequestType] === $undefined ? m.peerDataOperationRequestType : $root.proto.Message.PeerDataOperationRequestType[m.peerDataOperationRequestType] : m.peerDataOperationRequestType;
                }
                if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                    d.stanzaId = m.stanzaId;
                }
                if (m.peerDataOperationResult && m.peerDataOperationResult.length) {
                    d.peerDataOperationResult = $Array(m.peerDataOperationResult.length);
                    for (var j = 0; j < m.peerDataOperationResult.length; ++j) {
                        d.peerDataOperationResult[j] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.toObject(m.peerDataOperationResult[j], o, q + 1);
                    }
                }
                return d;
            };

            PeerDataOperationRequestResponseMessage.prototype.toJSON = function() {
                return PeerDataOperationRequestResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PeerDataOperationRequestResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage";
            };

            PeerDataOperationRequestResponseMessage.PeerDataOperationResult = (function() {

                const PeerDataOperationResult = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PeerDataOperationResult.prototype.mediaUploadResult = null;
                PeerDataOperationResult.prototype.stickerMessage = null;
                PeerDataOperationResult.prototype.linkPreviewResponse = null;
                PeerDataOperationResult.prototype.placeholderMessageResendResponse = null;
                PeerDataOperationResult.prototype.waffleNonceFetchRequestResponse = null;
                PeerDataOperationResult.prototype.fullHistorySyncOnDemandRequestResponse = null;
                PeerDataOperationResult.prototype.companionMetaNonceFetchRequestResponse = null;
                PeerDataOperationResult.prototype.syncdSnapshotFatalRecoveryResponse = null;
                PeerDataOperationResult.prototype.companionCanonicalUserNonceFetchRequestResponse = null;
                PeerDataOperationResult.prototype.historySyncChunkRetryResponse = null;
                PeerDataOperationResult.prototype.flowResponsesCsvBundle = null;
                PeerDataOperationResult.prototype.bizBroadcastInsightsContactListResponse = null;
                PeerDataOperationResult.prototype.contactRefreshResponse = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_mediaUploadResult", {
                    get: $util.oneOfGetter($oneOfFields = ["mediaUploadResult"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_stickerMessage", {
                    get: $util.oneOfGetter($oneOfFields = ["stickerMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_linkPreviewResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["linkPreviewResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_placeholderMessageResendResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["placeholderMessageResendResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_waffleNonceFetchRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["waffleNonceFetchRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_fullHistorySyncOnDemandRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["fullHistorySyncOnDemandRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_companionMetaNonceFetchRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["companionMetaNonceFetchRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_syncdSnapshotFatalRecoveryResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["syncdSnapshotFatalRecoveryResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_companionCanonicalUserNonceFetchRequestResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["companionCanonicalUserNonceFetchRequestResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_historySyncChunkRetryResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["historySyncChunkRetryResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_flowResponsesCsvBundle", {
                    get: $util.oneOfGetter($oneOfFields = ["flowResponsesCsvBundle"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_bizBroadcastInsightsContactListResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["bizBroadcastInsightsContactListResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PeerDataOperationResult.prototype, "_contactRefreshResponse", {
                    get: $util.oneOfGetter($oneOfFields = ["contactRefreshResponse"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PeerDataOperationResult.create = function(properties) {
                    return new PeerDataOperationResult(properties);
                };

                PeerDataOperationResult.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.mediaUploadResult != null && $Object.hasOwnProperty.call(m, "mediaUploadResult"))
                        w.uint32(8).int32(m.mediaUploadResult);
                    if (m.stickerMessage != null && $Object.hasOwnProperty.call(m, "stickerMessage"))
                        $root.proto.Message.StickerMessage.encode(m.stickerMessage, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.linkPreviewResponse != null && $Object.hasOwnProperty.call(m, "linkPreviewResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.encode(m.linkPreviewResponse, w.uint32(26).fork(), q + 1).ldelim();
                    if (m.placeholderMessageResendResponse != null && $Object.hasOwnProperty.call(m, "placeholderMessageResendResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.encode(m.placeholderMessageResendResponse, w.uint32(34).fork(), q + 1).ldelim();
                    if (m.waffleNonceFetchRequestResponse != null && $Object.hasOwnProperty.call(m, "waffleNonceFetchRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.encode(m.waffleNonceFetchRequestResponse, w.uint32(42).fork(), q + 1).ldelim();
                    if (m.fullHistorySyncOnDemandRequestResponse != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.encode(m.fullHistorySyncOnDemandRequestResponse, w.uint32(50).fork(), q + 1).ldelim();
                    if (m.companionMetaNonceFetchRequestResponse != null && $Object.hasOwnProperty.call(m, "companionMetaNonceFetchRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.encode(m.companionMetaNonceFetchRequestResponse, w.uint32(58).fork(), q + 1).ldelim();
                    if (m.syncdSnapshotFatalRecoveryResponse != null && $Object.hasOwnProperty.call(m, "syncdSnapshotFatalRecoveryResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.encode(m.syncdSnapshotFatalRecoveryResponse, w.uint32(66).fork(), q + 1).ldelim();
                    if (m.companionCanonicalUserNonceFetchRequestResponse != null && $Object.hasOwnProperty.call(m, "companionCanonicalUserNonceFetchRequestResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.encode(m.companionCanonicalUserNonceFetchRequestResponse, w.uint32(74).fork(), q + 1).ldelim();
                    if (m.historySyncChunkRetryResponse != null && $Object.hasOwnProperty.call(m, "historySyncChunkRetryResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.encode(m.historySyncChunkRetryResponse, w.uint32(82).fork(), q + 1).ldelim();
                    if (m.flowResponsesCsvBundle != null && $Object.hasOwnProperty.call(m, "flowResponsesCsvBundle"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.encode(m.flowResponsesCsvBundle, w.uint32(90).fork(), q + 1).ldelim();
                    if (m.bizBroadcastInsightsContactListResponse != null && $Object.hasOwnProperty.call(m, "bizBroadcastInsightsContactListResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.encode(m.bizBroadcastInsightsContactListResponse, w.uint32(98).fork(), q + 1).ldelim();
                    if (m.contactRefreshResponse != null && $Object.hasOwnProperty.call(m, "contactRefreshResponse"))
                        $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse.encode(m.contactRefreshResponse, w.uint32(106).fork(), q + 1).ldelim();
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PeerDataOperationResult.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.mediaUploadResult = r.int32();
                                m._mediaUploadResult = "mediaUploadResult";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.stickerMessage = $root.proto.Message.StickerMessage.decode(r, r.uint32(), $undefined, q + 1, m.stickerMessage);
                                m._stickerMessage = "stickerMessage";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.linkPreviewResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.decode(r, r.uint32(), $undefined, q + 1, m.linkPreviewResponse);
                                m._linkPreviewResponse = "linkPreviewResponse";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.placeholderMessageResendResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.decode(r, r.uint32(), $undefined, q + 1, m.placeholderMessageResendResponse);
                                m._placeholderMessageResendResponse = "placeholderMessageResendResponse";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.waffleNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.decode(r, r.uint32(), $undefined, q + 1, m.waffleNonceFetchRequestResponse);
                                m._waffleNonceFetchRequestResponse = "waffleNonceFetchRequestResponse";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.fullHistorySyncOnDemandRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.decode(r, r.uint32(), $undefined, q + 1, m.fullHistorySyncOnDemandRequestResponse);
                                m._fullHistorySyncOnDemandRequestResponse = "fullHistorySyncOnDemandRequestResponse";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.companionMetaNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.decode(r, r.uint32(), $undefined, q + 1, m.companionMetaNonceFetchRequestResponse);
                                m._companionMetaNonceFetchRequestResponse = "companionMetaNonceFetchRequestResponse";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                m.syncdSnapshotFatalRecoveryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.decode(r, r.uint32(), $undefined, q + 1, m.syncdSnapshotFatalRecoveryResponse);
                                m._syncdSnapshotFatalRecoveryResponse = "syncdSnapshotFatalRecoveryResponse";
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                m.companionCanonicalUserNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.decode(r, r.uint32(), $undefined, q + 1, m.companionCanonicalUserNonceFetchRequestResponse);
                                m._companionCanonicalUserNonceFetchRequestResponse = "companionCanonicalUserNonceFetchRequestResponse";
                                continue;
                            }
                        case 10: {
                                if (u !== 2)
                                    break;
                                m.historySyncChunkRetryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.decode(r, r.uint32(), $undefined, q + 1, m.historySyncChunkRetryResponse);
                                m._historySyncChunkRetryResponse = "historySyncChunkRetryResponse";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                m.flowResponsesCsvBundle = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.decode(r, r.uint32(), $undefined, q + 1, m.flowResponsesCsvBundle);
                                m._flowResponsesCsvBundle = "flowResponsesCsvBundle";
                                continue;
                            }
                        case 12: {
                                if (u !== 2)
                                    break;
                                m.bizBroadcastInsightsContactListResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.decode(r, r.uint32(), $undefined, q + 1, m.bizBroadcastInsightsContactListResponse);
                                m._bizBroadcastInsightsContactListResponse = "bizBroadcastInsightsContactListResponse";
                                continue;
                            }
                        case 13: {
                                if (u !== 2)
                                    break;
                                m.contactRefreshResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse.decode(r, r.uint32(), $undefined, q + 1, m.contactRefreshResponse);
                                m._contactRefreshResponse = "contactRefreshResponse";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PeerDataOperationResult.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult();
                    switch (d.mediaUploadResult) {
                    case "GENERAL_ERROR":
                    case 0:
                        m.mediaUploadResult = 0;
                        break;
                    case "SUCCESS":
                    case 1:
                        m.mediaUploadResult = 1;
                        break;
                    case "NOT_FOUND":
                    case 2:
                        m.mediaUploadResult = 2;
                        break;
                    case "DECRYPTION_ERROR":
                    case 3:
                        m.mediaUploadResult = 3;
                        break;
                    default:
                        if (typeof d.mediaUploadResult === "number" && (d.mediaUploadResult | 0) === d.mediaUploadResult)
                            m.mediaUploadResult = d.mediaUploadResult;
                    }
                    if (d.stickerMessage != null) {
                        if (!$util.isObject(d.stickerMessage))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.stickerMessage: object expected");
                        m.stickerMessage = $root.proto.Message.StickerMessage.fromObject(d.stickerMessage, q + 1);
                    }
                    if (d.linkPreviewResponse != null) {
                        if (!$util.isObject(d.linkPreviewResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.linkPreviewResponse: object expected");
                        m.linkPreviewResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.fromObject(d.linkPreviewResponse, q + 1);
                    }
                    if (d.placeholderMessageResendResponse != null) {
                        if (!$util.isObject(d.placeholderMessageResendResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.placeholderMessageResendResponse: object expected");
                        m.placeholderMessageResendResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.fromObject(d.placeholderMessageResendResponse, q + 1);
                    }
                    if (d.waffleNonceFetchRequestResponse != null) {
                        if (!$util.isObject(d.waffleNonceFetchRequestResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.waffleNonceFetchRequestResponse: object expected");
                        m.waffleNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.fromObject(d.waffleNonceFetchRequestResponse, q + 1);
                    }
                    if (d.fullHistorySyncOnDemandRequestResponse != null) {
                        if (!$util.isObject(d.fullHistorySyncOnDemandRequestResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.fullHistorySyncOnDemandRequestResponse: object expected");
                        m.fullHistorySyncOnDemandRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.fromObject(d.fullHistorySyncOnDemandRequestResponse, q + 1);
                    }
                    if (d.companionMetaNonceFetchRequestResponse != null) {
                        if (!$util.isObject(d.companionMetaNonceFetchRequestResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.companionMetaNonceFetchRequestResponse: object expected");
                        m.companionMetaNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.fromObject(d.companionMetaNonceFetchRequestResponse, q + 1);
                    }
                    if (d.syncdSnapshotFatalRecoveryResponse != null) {
                        if (!$util.isObject(d.syncdSnapshotFatalRecoveryResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.syncdSnapshotFatalRecoveryResponse: object expected");
                        m.syncdSnapshotFatalRecoveryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.fromObject(d.syncdSnapshotFatalRecoveryResponse, q + 1);
                    }
                    if (d.companionCanonicalUserNonceFetchRequestResponse != null) {
                        if (!$util.isObject(d.companionCanonicalUserNonceFetchRequestResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.companionCanonicalUserNonceFetchRequestResponse: object expected");
                        m.companionCanonicalUserNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.fromObject(d.companionCanonicalUserNonceFetchRequestResponse, q + 1);
                    }
                    if (d.historySyncChunkRetryResponse != null) {
                        if (!$util.isObject(d.historySyncChunkRetryResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.historySyncChunkRetryResponse: object expected");
                        m.historySyncChunkRetryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.fromObject(d.historySyncChunkRetryResponse, q + 1);
                    }
                    if (d.flowResponsesCsvBundle != null) {
                        if (!$util.isObject(d.flowResponsesCsvBundle))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.flowResponsesCsvBundle: object expected");
                        m.flowResponsesCsvBundle = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.fromObject(d.flowResponsesCsvBundle, q + 1);
                    }
                    if (d.bizBroadcastInsightsContactListResponse != null) {
                        if (!$util.isObject(d.bizBroadcastInsightsContactListResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.bizBroadcastInsightsContactListResponse: object expected");
                        m.bizBroadcastInsightsContactListResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.fromObject(d.bizBroadcastInsightsContactListResponse, q + 1);
                    }
                    if (d.contactRefreshResponse != null) {
                        if (!$util.isObject(d.contactRefreshResponse))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.contactRefreshResponse: object expected");
                        m.contactRefreshResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse.fromObject(d.contactRefreshResponse, q + 1);
                    }
                    return m;
                };

                PeerDataOperationResult.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.mediaUploadResult != null && $Object.hasOwnProperty.call(m, "mediaUploadResult")) {
                        d.mediaUploadResult = o.enums === $String ? $root.proto.MediaRetryNotification.ResultType[m.mediaUploadResult] === $undefined ? m.mediaUploadResult : $root.proto.MediaRetryNotification.ResultType[m.mediaUploadResult] : m.mediaUploadResult;
                    }
                    if (m.stickerMessage != null && $Object.hasOwnProperty.call(m, "stickerMessage")) {
                        d.stickerMessage = $root.proto.Message.StickerMessage.toObject(m.stickerMessage, o, q + 1);
                    }
                    if (m.linkPreviewResponse != null && $Object.hasOwnProperty.call(m, "linkPreviewResponse")) {
                        d.linkPreviewResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.toObject(m.linkPreviewResponse, o, q + 1);
                    }
                    if (m.placeholderMessageResendResponse != null && $Object.hasOwnProperty.call(m, "placeholderMessageResendResponse")) {
                        d.placeholderMessageResendResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse.toObject(m.placeholderMessageResendResponse, o, q + 1);
                    }
                    if (m.waffleNonceFetchRequestResponse != null && $Object.hasOwnProperty.call(m, "waffleNonceFetchRequestResponse")) {
                        d.waffleNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse.toObject(m.waffleNonceFetchRequestResponse, o, q + 1);
                    }
                    if (m.fullHistorySyncOnDemandRequestResponse != null && $Object.hasOwnProperty.call(m, "fullHistorySyncOnDemandRequestResponse")) {
                        d.fullHistorySyncOnDemandRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.toObject(m.fullHistorySyncOnDemandRequestResponse, o, q + 1);
                    }
                    if (m.companionMetaNonceFetchRequestResponse != null && $Object.hasOwnProperty.call(m, "companionMetaNonceFetchRequestResponse")) {
                        d.companionMetaNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse.toObject(m.companionMetaNonceFetchRequestResponse, o, q + 1);
                    }
                    if (m.syncdSnapshotFatalRecoveryResponse != null && $Object.hasOwnProperty.call(m, "syncdSnapshotFatalRecoveryResponse")) {
                        d.syncdSnapshotFatalRecoveryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse.toObject(m.syncdSnapshotFatalRecoveryResponse, o, q + 1);
                    }
                    if (m.companionCanonicalUserNonceFetchRequestResponse != null && $Object.hasOwnProperty.call(m, "companionCanonicalUserNonceFetchRequestResponse")) {
                        d.companionCanonicalUserNonceFetchRequestResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse.toObject(m.companionCanonicalUserNonceFetchRequestResponse, o, q + 1);
                    }
                    if (m.historySyncChunkRetryResponse != null && $Object.hasOwnProperty.call(m, "historySyncChunkRetryResponse")) {
                        d.historySyncChunkRetryResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse.toObject(m.historySyncChunkRetryResponse, o, q + 1);
                    }
                    if (m.flowResponsesCsvBundle != null && $Object.hasOwnProperty.call(m, "flowResponsesCsvBundle")) {
                        d.flowResponsesCsvBundle = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle.toObject(m.flowResponsesCsvBundle, o, q + 1);
                    }
                    if (m.bizBroadcastInsightsContactListResponse != null && $Object.hasOwnProperty.call(m, "bizBroadcastInsightsContactListResponse")) {
                        d.bizBroadcastInsightsContactListResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.toObject(m.bizBroadcastInsightsContactListResponse, o, q + 1);
                    }
                    if (m.contactRefreshResponse != null && $Object.hasOwnProperty.call(m, "contactRefreshResponse")) {
                        d.contactRefreshResponse = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse.toObject(m.contactRefreshResponse, o, q + 1);
                    }
                    return d;
                };

                PeerDataOperationResult.prototype.toJSON = function() {
                    return PeerDataOperationResult.toObject(this, $protobuf.util.toJSONOptions);
                };

                PeerDataOperationResult.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult";
                };

                PeerDataOperationResult.BizBroadcastInsightsContactListResponse = (function() {

                    const BizBroadcastInsightsContactListResponse = function (p) {
                        this.contacts = [];
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    BizBroadcastInsightsContactListResponse.prototype.campaignId = null;
                    BizBroadcastInsightsContactListResponse.prototype.timestampMs = null;
                    BizBroadcastInsightsContactListResponse.prototype.contacts = $util.emptyArray;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(BizBroadcastInsightsContactListResponse.prototype, "_campaignId", {
                        get: $util.oneOfGetter($oneOfFields = ["campaignId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(BizBroadcastInsightsContactListResponse.prototype, "_timestampMs", {
                        get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    BizBroadcastInsightsContactListResponse.create = function(properties) {
                        return new BizBroadcastInsightsContactListResponse(properties);
                    };

                    BizBroadcastInsightsContactListResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId"))
                            w.uint32(10).string(m.campaignId);
                        if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                            w.uint32(16).int64(m.timestampMs);
                        if (m.contacts != null && m.contacts.length) {
                            for (var i = 0; i < m.contacts.length; ++i)
                                $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.encode(m.contacts[i], w.uint32(26).fork(), q + 1).ldelim();
                        }
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    BizBroadcastInsightsContactListResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.campaignId = r.stringVerify();
                                    m._campaignId = "campaignId";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.timestampMs = r.int64();
                                    m._timestampMs = "timestampMs";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    if (!(m.contacts && m.contacts.length))
                                        m.contacts = [];
                                    m.contacts.push($root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.decode(r, r.uint32(), $undefined, q + 1));
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    BizBroadcastInsightsContactListResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse();
                        if (d.campaignId != null) {
                            m.campaignId = $String(d.campaignId);
                        }
                        if (d.timestampMs != null) {
                            if ($util.Long)
                                m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                            else if (typeof d.timestampMs === "string")
                                m.timestampMs = $parseInt(d.timestampMs, 10);
                            else if (typeof d.timestampMs === "number")
                                m.timestampMs = d.timestampMs;
                            else if (typeof d.timestampMs === "object")
                                m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
                        }
                        if (d.contacts) {
                            if (!$Array.isArray(d.contacts))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.contacts: array expected");
                            m.contacts = $Array(d.contacts.length);
                            for (var i = 0; i < d.contacts.length; ++i) {
                                if (!$util.isObject(d.contacts[i]))
                                    throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse.contacts: object expected");
                                m.contacts[i] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.fromObject(d.contacts[i], q + 1);
                            }
                        }
                        return m;
                    };

                    BizBroadcastInsightsContactListResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (o.arrays || o.defaults) {
                            d.contacts = [];
                        }
                        if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId")) {
                            d.campaignId = m.campaignId;
                        }
                        if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                            if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                            else if (typeof m.timestampMs === "number")
                                d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                            else
                                d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
                        }
                        if (m.contacts && m.contacts.length) {
                            d.contacts = $Array(m.contacts.length);
                            for (var j = 0; j < m.contacts.length; ++j) {
                                d.contacts[j] = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState.toObject(m.contacts[j], o, q + 1);
                            }
                        }
                        return d;
                    };

                    BizBroadcastInsightsContactListResponse.prototype.toJSON = function() {
                        return BizBroadcastInsightsContactListResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    BizBroadcastInsightsContactListResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactListResponse";
                    };

                    return BizBroadcastInsightsContactListResponse;
                })();

                PeerDataOperationResult.BizBroadcastInsightsContactState = (function() {

                    const BizBroadcastInsightsContactState = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    BizBroadcastInsightsContactState.prototype.contactJid = null;
                    BizBroadcastInsightsContactState.prototype.state = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(BizBroadcastInsightsContactState.prototype, "_contactJid", {
                        get: $util.oneOfGetter($oneOfFields = ["contactJid"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(BizBroadcastInsightsContactState.prototype, "_state", {
                        get: $util.oneOfGetter($oneOfFields = ["state"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    BizBroadcastInsightsContactState.create = function(properties) {
                        return new BizBroadcastInsightsContactState(properties);
                    };

                    BizBroadcastInsightsContactState.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.contactJid != null && $Object.hasOwnProperty.call(m, "contactJid"))
                            w.uint32(10).string(m.contactJid);
                        if (m.state != null && $Object.hasOwnProperty.call(m, "state"))
                            w.uint32(16).int32(m.state);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    BizBroadcastInsightsContactState.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState(), v;
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.contactJid = r.stringVerify();
                                    m._contactJid = "contactJid";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.state = r.int32();
                                    m._state = "state";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    BizBroadcastInsightsContactState.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState();
                        if (d.contactJid != null) {
                            m.contactJid = $String(d.contactJid);
                        }
                        switch (d.state) {
                        case "SENT":
                        case 0:
                            m.state = 0;
                            break;
                        case "DELIVERED":
                        case 1:
                            m.state = 1;
                            break;
                        case "READ":
                        case 2:
                            m.state = 2;
                            break;
                        case "REPLIED":
                        case 3:
                            m.state = 3;
                            break;
                        case "QUICK_REPLIED":
                        case 4:
                            m.state = 4;
                            break;
                        default:
                            if (typeof d.state === "number" && (d.state | 0) === d.state)
                                m.state = d.state;
                        }
                        return m;
                    };

                    BizBroadcastInsightsContactState.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.contactJid != null && $Object.hasOwnProperty.call(m, "contactJid")) {
                            d.contactJid = m.contactJid;
                        }
                        if (m.state != null && $Object.hasOwnProperty.call(m, "state")) {
                            d.state = o.enums === $String ? $root.proto.Message.InsightDeliveryState[m.state] === $undefined ? m.state : $root.proto.Message.InsightDeliveryState[m.state] : m.state;
                        }
                        return d;
                    };

                    BizBroadcastInsightsContactState.prototype.toJSON = function() {
                        return BizBroadcastInsightsContactState.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    BizBroadcastInsightsContactState.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.BizBroadcastInsightsContactState";
                    };

                    return BizBroadcastInsightsContactState;
                })();

                PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse = (function() {

                    const CompanionCanonicalUserNonceFetchResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    CompanionCanonicalUserNonceFetchResponse.prototype.nonce = null;
                    CompanionCanonicalUserNonceFetchResponse.prototype.waFbid = null;
                    CompanionCanonicalUserNonceFetchResponse.prototype.forceRefresh = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_nonce", {
                        get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_waFbid", {
                        get: $util.oneOfGetter($oneOfFields = ["waFbid"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(CompanionCanonicalUserNonceFetchResponse.prototype, "_forceRefresh", {
                        get: $util.oneOfGetter($oneOfFields = ["forceRefresh"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    CompanionCanonicalUserNonceFetchResponse.create = function(properties) {
                        return new CompanionCanonicalUserNonceFetchResponse(properties);
                    };

                    CompanionCanonicalUserNonceFetchResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce"))
                            w.uint32(10).string(m.nonce);
                        if (m.waFbid != null && $Object.hasOwnProperty.call(m, "waFbid"))
                            w.uint32(18).string(m.waFbid);
                        if (m.forceRefresh != null && $Object.hasOwnProperty.call(m, "forceRefresh"))
                            w.uint32(24).bool(m.forceRefresh);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    CompanionCanonicalUserNonceFetchResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.nonce = r.stringVerify();
                                    m._nonce = "nonce";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.waFbid = r.stringVerify();
                                    m._waFbid = "waFbid";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 0)
                                        break;
                                    m.forceRefresh = r.bool();
                                    m._forceRefresh = "forceRefresh";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    CompanionCanonicalUserNonceFetchResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse();
                        if (d.nonce != null) {
                            m.nonce = $String(d.nonce);
                        }
                        if (d.waFbid != null) {
                            m.waFbid = $String(d.waFbid);
                        }
                        if (d.forceRefresh != null) {
                            m.forceRefresh = $Boolean(d.forceRefresh);
                        }
                        return m;
                    };

                    CompanionCanonicalUserNonceFetchResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce")) {
                            d.nonce = m.nonce;
                        }
                        if (m.waFbid != null && $Object.hasOwnProperty.call(m, "waFbid")) {
                            d.waFbid = m.waFbid;
                        }
                        if (m.forceRefresh != null && $Object.hasOwnProperty.call(m, "forceRefresh")) {
                            d.forceRefresh = m.forceRefresh;
                        }
                        return d;
                    };

                    CompanionCanonicalUserNonceFetchResponse.prototype.toJSON = function() {
                        return CompanionCanonicalUserNonceFetchResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    CompanionCanonicalUserNonceFetchResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionCanonicalUserNonceFetchResponse";
                    };

                    return CompanionCanonicalUserNonceFetchResponse;
                })();

                PeerDataOperationResult.CompanionMetaNonceFetchResponse = (function() {

                    const CompanionMetaNonceFetchResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    CompanionMetaNonceFetchResponse.prototype.nonce = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(CompanionMetaNonceFetchResponse.prototype, "_nonce", {
                        get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    CompanionMetaNonceFetchResponse.create = function(properties) {
                        return new CompanionMetaNonceFetchResponse(properties);
                    };

                    CompanionMetaNonceFetchResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce"))
                            w.uint32(10).string(m.nonce);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    CompanionMetaNonceFetchResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.nonce = r.stringVerify();
                                    m._nonce = "nonce";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    CompanionMetaNonceFetchResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse();
                        if (d.nonce != null) {
                            m.nonce = $String(d.nonce);
                        }
                        return m;
                    };

                    CompanionMetaNonceFetchResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce")) {
                            d.nonce = m.nonce;
                        }
                        return d;
                    };

                    CompanionMetaNonceFetchResponse.prototype.toJSON = function() {
                        return CompanionMetaNonceFetchResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    CompanionMetaNonceFetchResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.CompanionMetaNonceFetchResponse";
                    };

                    return CompanionMetaNonceFetchResponse;
                })();

                PeerDataOperationResult.ContactRefreshResponse = (function() {

                    const ContactRefreshResponse = function (p) {
                        this.coveredRequestIds = [];
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    ContactRefreshResponse.prototype.coveredRequestIds = $util.emptyArray;
                    ContactRefreshResponse.prototype.collectionVersion = null;
                    ContactRefreshResponse.prototype.primaryDurationMs = null;
                    ContactRefreshResponse.prototype.uniqueContactCount = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(ContactRefreshResponse.prototype, "_collectionVersion", {
                        get: $util.oneOfGetter($oneOfFields = ["collectionVersion"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(ContactRefreshResponse.prototype, "_primaryDurationMs", {
                        get: $util.oneOfGetter($oneOfFields = ["primaryDurationMs"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(ContactRefreshResponse.prototype, "_uniqueContactCount", {
                        get: $util.oneOfGetter($oneOfFields = ["uniqueContactCount"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    ContactRefreshResponse.create = function(properties) {
                        return new ContactRefreshResponse(properties);
                    };

                    ContactRefreshResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.coveredRequestIds != null && m.coveredRequestIds.length) {
                            for (var i = 0; i < m.coveredRequestIds.length; ++i)
                                w.uint32(10).string(m.coveredRequestIds[i]);
                        }
                        if (m.collectionVersion != null && $Object.hasOwnProperty.call(m, "collectionVersion"))
                            w.uint32(16).uint64(m.collectionVersion);
                        if (m.primaryDurationMs != null && $Object.hasOwnProperty.call(m, "primaryDurationMs"))
                            w.uint32(24).int64(m.primaryDurationMs);
                        if (m.uniqueContactCount != null && $Object.hasOwnProperty.call(m, "uniqueContactCount"))
                            w.uint32(32).uint32(m.uniqueContactCount);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    ContactRefreshResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    if (!(m.coveredRequestIds && m.coveredRequestIds.length))
                                        m.coveredRequestIds = [];
                                    m.coveredRequestIds.push(r.stringVerify());
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.collectionVersion = r.uint64();
                                    m._collectionVersion = "collectionVersion";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 0)
                                        break;
                                    m.primaryDurationMs = r.int64();
                                    m._primaryDurationMs = "primaryDurationMs";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 0)
                                        break;
                                    m.uniqueContactCount = r.uint32();
                                    m._uniqueContactCount = "uniqueContactCount";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    ContactRefreshResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse();
                        if (d.coveredRequestIds) {
                            if (!$Array.isArray(d.coveredRequestIds))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse.coveredRequestIds: array expected");
                            m.coveredRequestIds = $Array(d.coveredRequestIds.length);
                            for (var i = 0; i < d.coveredRequestIds.length; ++i) {
                                m.coveredRequestIds[i] = $String(d.coveredRequestIds[i]);
                            }
                        }
                        if (d.collectionVersion != null) {
                            if ($util.Long)
                                m.collectionVersion = $util.Long.fromValue(d.collectionVersion, true);
                            else if (typeof d.collectionVersion === "string")
                                m.collectionVersion = $parseInt(d.collectionVersion, 10);
                            else if (typeof d.collectionVersion === "number")
                                m.collectionVersion = d.collectionVersion;
                            else if (typeof d.collectionVersion === "object")
                                m.collectionVersion = new $util.LongBits(d.collectionVersion.low >>> 0, d.collectionVersion.high >>> 0).toNumber(true);
                        }
                        if (d.primaryDurationMs != null) {
                            if ($util.Long)
                                m.primaryDurationMs = $util.Long.fromValue(d.primaryDurationMs, false);
                            else if (typeof d.primaryDurationMs === "string")
                                m.primaryDurationMs = $parseInt(d.primaryDurationMs, 10);
                            else if (typeof d.primaryDurationMs === "number")
                                m.primaryDurationMs = d.primaryDurationMs;
                            else if (typeof d.primaryDurationMs === "object")
                                m.primaryDurationMs = new $util.LongBits(d.primaryDurationMs.low >>> 0, d.primaryDurationMs.high >>> 0).toNumber();
                        }
                        if (d.uniqueContactCount != null) {
                            m.uniqueContactCount = d.uniqueContactCount >>> 0;
                        }
                        return m;
                    };

                    ContactRefreshResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (o.arrays || o.defaults) {
                            d.coveredRequestIds = [];
                        }
                        if (m.coveredRequestIds && m.coveredRequestIds.length) {
                            d.coveredRequestIds = $Array(m.coveredRequestIds.length);
                            for (var j = 0; j < m.coveredRequestIds.length; ++j) {
                                d.coveredRequestIds[j] = m.coveredRequestIds[j];
                            }
                        }
                        if (m.collectionVersion != null && $Object.hasOwnProperty.call(m, "collectionVersion")) {
                            if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                d.collectionVersion = typeof m.collectionVersion === "number" ? $BigInt(m.collectionVersion) : $util.Long.fromBits(m.collectionVersion.low >>> 0, m.collectionVersion.high >>> 0, true).toBigInt();
                            else if (typeof m.collectionVersion === "number")
                                d.collectionVersion = o.longs === $String ? $String(m.collectionVersion) : m.collectionVersion;
                            else
                                d.collectionVersion = o.longs === $String ? $util.Long.prototype.toString.call(m.collectionVersion) : o.longs === $Number ? new $util.LongBits(m.collectionVersion.low >>> 0, m.collectionVersion.high >>> 0).toNumber(true) : m.collectionVersion;
                        }
                        if (m.primaryDurationMs != null && $Object.hasOwnProperty.call(m, "primaryDurationMs")) {
                            if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                d.primaryDurationMs = typeof m.primaryDurationMs === "number" ? $BigInt(m.primaryDurationMs) : $util.Long.fromBits(m.primaryDurationMs.low >>> 0, m.primaryDurationMs.high >>> 0, false).toBigInt();
                            else if (typeof m.primaryDurationMs === "number")
                                d.primaryDurationMs = o.longs === $String ? $String(m.primaryDurationMs) : m.primaryDurationMs;
                            else
                                d.primaryDurationMs = o.longs === $String ? $util.Long.prototype.toString.call(m.primaryDurationMs) : o.longs === $Number ? new $util.LongBits(m.primaryDurationMs.low >>> 0, m.primaryDurationMs.high >>> 0).toNumber() : m.primaryDurationMs;
                        }
                        if (m.uniqueContactCount != null && $Object.hasOwnProperty.call(m, "uniqueContactCount")) {
                            d.uniqueContactCount = m.uniqueContactCount;
                        }
                        return d;
                    };

                    ContactRefreshResponse.prototype.toJSON = function() {
                        return ContactRefreshResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    ContactRefreshResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.ContactRefreshResponse";
                    };

                    return ContactRefreshResponse;
                })();

                PeerDataOperationResult.FlowResponsesCsvBundle = (function() {

                    const FlowResponsesCsvBundle = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    FlowResponsesCsvBundle.prototype.flowId = null;
                    FlowResponsesCsvBundle.prototype.galaxyFlowDownloadRequestId = null;
                    FlowResponsesCsvBundle.prototype.fileName = null;
                    FlowResponsesCsvBundle.prototype.mimetype = null;
                    FlowResponsesCsvBundle.prototype.fileSha256 = null;
                    FlowResponsesCsvBundle.prototype.mediaKey = null;
                    FlowResponsesCsvBundle.prototype.fileEncSha256 = null;
                    FlowResponsesCsvBundle.prototype.directPath = null;
                    FlowResponsesCsvBundle.prototype.mediaKeyTimestamp = null;
                    FlowResponsesCsvBundle.prototype.fileLength = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_flowId", {
                        get: $util.oneOfGetter($oneOfFields = ["flowId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_galaxyFlowDownloadRequestId", {
                        get: $util.oneOfGetter($oneOfFields = ["galaxyFlowDownloadRequestId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileName", {
                        get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mimetype", {
                        get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileSha256", {
                        get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mediaKey", {
                        get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileEncSha256", {
                        get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_directPath", {
                        get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_mediaKeyTimestamp", {
                        get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FlowResponsesCsvBundle.prototype, "_fileLength", {
                        get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    FlowResponsesCsvBundle.create = function(properties) {
                        return new FlowResponsesCsvBundle(properties);
                    };

                    FlowResponsesCsvBundle.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.flowId != null && $Object.hasOwnProperty.call(m, "flowId"))
                            w.uint32(10).string(m.flowId);
                        if (m.galaxyFlowDownloadRequestId != null && $Object.hasOwnProperty.call(m, "galaxyFlowDownloadRequestId"))
                            w.uint32(18).string(m.galaxyFlowDownloadRequestId);
                        if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName"))
                            w.uint32(26).string(m.fileName);
                        if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                            w.uint32(34).string(m.mimetype);
                        if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                            w.uint32(42).bytes(m.fileSha256);
                        if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                            w.uint32(50).bytes(m.mediaKey);
                        if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                            w.uint32(58).bytes(m.fileEncSha256);
                        if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                            w.uint32(66).string(m.directPath);
                        if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                            w.uint32(72).int64(m.mediaKeyTimestamp);
                        if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                            w.uint32(80).uint64(m.fileLength);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    FlowResponsesCsvBundle.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.flowId = r.stringVerify();
                                    m._flowId = "flowId";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.galaxyFlowDownloadRequestId = r.stringVerify();
                                    m._galaxyFlowDownloadRequestId = "galaxyFlowDownloadRequestId";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    m.fileName = r.stringVerify();
                                    m._fileName = "fileName";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 2)
                                        break;
                                    m.mimetype = r.stringVerify();
                                    m._mimetype = "mimetype";
                                    continue;
                                }
                            case 5: {
                                    if (u !== 2)
                                        break;
                                    m.fileSha256 = r.bytes();
                                    m._fileSha256 = "fileSha256";
                                    continue;
                                }
                            case 6: {
                                    if (u !== 2)
                                        break;
                                    m.mediaKey = r.bytes();
                                    m._mediaKey = "mediaKey";
                                    continue;
                                }
                            case 7: {
                                    if (u !== 2)
                                        break;
                                    m.fileEncSha256 = r.bytes();
                                    m._fileEncSha256 = "fileEncSha256";
                                    continue;
                                }
                            case 8: {
                                    if (u !== 2)
                                        break;
                                    m.directPath = r.stringVerify();
                                    m._directPath = "directPath";
                                    continue;
                                }
                            case 9: {
                                    if (u !== 0)
                                        break;
                                    m.mediaKeyTimestamp = r.int64();
                                    m._mediaKeyTimestamp = "mediaKeyTimestamp";
                                    continue;
                                }
                            case 10: {
                                    if (u !== 0)
                                        break;
                                    m.fileLength = r.uint64();
                                    m._fileLength = "fileLength";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    FlowResponsesCsvBundle.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle();
                        if (d.flowId != null) {
                            m.flowId = $String(d.flowId);
                        }
                        if (d.galaxyFlowDownloadRequestId != null) {
                            m.galaxyFlowDownloadRequestId = $String(d.galaxyFlowDownloadRequestId);
                        }
                        if (d.fileName != null) {
                            m.fileName = $String(d.fileName);
                        }
                        if (d.mimetype != null) {
                            m.mimetype = $String(d.mimetype);
                        }
                        if (d.fileSha256 != null) {
                            if (typeof d.fileSha256 === "string")
                                $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                            else if (d.fileSha256.length >= 0)
                                m.fileSha256 = d.fileSha256;
                        }
                        if (d.mediaKey != null) {
                            if (typeof d.mediaKey === "string")
                                $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                            else if (d.mediaKey.length >= 0)
                                m.mediaKey = d.mediaKey;
                        }
                        if (d.fileEncSha256 != null) {
                            if (typeof d.fileEncSha256 === "string")
                                $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                            else if (d.fileEncSha256.length >= 0)
                                m.fileEncSha256 = d.fileEncSha256;
                        }
                        if (d.directPath != null) {
                            m.directPath = $String(d.directPath);
                        }
                        if (d.mediaKeyTimestamp != null) {
                            if ($util.Long)
                                m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                            else if (typeof d.mediaKeyTimestamp === "string")
                                m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                            else if (typeof d.mediaKeyTimestamp === "number")
                                m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                            else if (typeof d.mediaKeyTimestamp === "object")
                                m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                        }
                        if (d.fileLength != null) {
                            if ($util.Long)
                                m.fileLength = $util.Long.fromValue(d.fileLength, true);
                            else if (typeof d.fileLength === "string")
                                m.fileLength = $parseInt(d.fileLength, 10);
                            else if (typeof d.fileLength === "number")
                                m.fileLength = d.fileLength;
                            else if (typeof d.fileLength === "object")
                                m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                        }
                        return m;
                    };

                    FlowResponsesCsvBundle.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.flowId != null && $Object.hasOwnProperty.call(m, "flowId")) {
                            d.flowId = m.flowId;
                        }
                        if (m.galaxyFlowDownloadRequestId != null && $Object.hasOwnProperty.call(m, "galaxyFlowDownloadRequestId")) {
                            d.galaxyFlowDownloadRequestId = m.galaxyFlowDownloadRequestId;
                        }
                        if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName")) {
                            d.fileName = m.fileName;
                        }
                        if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                            d.mimetype = m.mimetype;
                        }
                        if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                            d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                        }
                        if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                            d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                        }
                        if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                            d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                        }
                        if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                            d.directPath = m.directPath;
                        }
                        if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                            if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                            else if (typeof m.mediaKeyTimestamp === "number")
                                d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                            else
                                d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                        }
                        if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                            if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                            else if (typeof m.fileLength === "number")
                                d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                            else
                                d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                        }
                        return d;
                    };

                    FlowResponsesCsvBundle.prototype.toJSON = function() {
                        return FlowResponsesCsvBundle.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    FlowResponsesCsvBundle.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FlowResponsesCsvBundle";
                    };

                    return FlowResponsesCsvBundle;
                })();

                PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse = (function() {

                    const FullHistorySyncOnDemandRequestResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    FullHistorySyncOnDemandRequestResponse.prototype.requestMetadata = null;
                    FullHistorySyncOnDemandRequestResponse.prototype.responseCode = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FullHistorySyncOnDemandRequestResponse.prototype, "_requestMetadata", {
                        get: $util.oneOfGetter($oneOfFields = ["requestMetadata"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(FullHistorySyncOnDemandRequestResponse.prototype, "_responseCode", {
                        get: $util.oneOfGetter($oneOfFields = ["responseCode"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    FullHistorySyncOnDemandRequestResponse.create = function(properties) {
                        return new FullHistorySyncOnDemandRequestResponse(properties);
                    };

                    FullHistorySyncOnDemandRequestResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.requestMetadata != null && $Object.hasOwnProperty.call(m, "requestMetadata"))
                            $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.encode(m.requestMetadata, w.uint32(10).fork(), q + 1).ldelim();
                        if (m.responseCode != null && $Object.hasOwnProperty.call(m, "responseCode"))
                            w.uint32(16).int32(m.responseCode);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    FullHistorySyncOnDemandRequestResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse(), v;
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.decode(r, r.uint32(), $undefined, q + 1, m.requestMetadata);
                                    m._requestMetadata = "requestMetadata";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.responseCode = r.int32();
                                    m._responseCode = "responseCode";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    FullHistorySyncOnDemandRequestResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse();
                        if (d.requestMetadata != null) {
                            if (!$util.isObject(d.requestMetadata))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse.requestMetadata: object expected");
                            m.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.fromObject(d.requestMetadata, q + 1);
                        }
                        switch (d.responseCode) {
                        case "REQUEST_SUCCESS":
                        case 0:
                            m.responseCode = 0;
                            break;
                        case "REQUEST_TIME_EXPIRED":
                        case 1:
                            m.responseCode = 1;
                            break;
                        case "DECLINED_SHARING_HISTORY":
                        case 2:
                            m.responseCode = 2;
                            break;
                        case "GENERIC_ERROR":
                        case 3:
                            m.responseCode = 3;
                            break;
                        case "ERROR_REQUEST_ON_NON_SMB_PRIMARY":
                        case 4:
                            m.responseCode = 4;
                            break;
                        case "ERROR_HOSTED_DEVICE_NOT_CONNECTED":
                        case 5:
                            m.responseCode = 5;
                            break;
                        case "ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET":
                        case 6:
                            m.responseCode = 6;
                            break;
                        case "ERROR_MULTI_PROVIDER_NOT_CONFIGURED":
                        case 7:
                            m.responseCode = 7;
                            break;
                        default:
                            if (typeof d.responseCode === "number" && (d.responseCode | 0) === d.responseCode)
                                m.responseCode = d.responseCode;
                        }
                        return m;
                    };

                    FullHistorySyncOnDemandRequestResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.requestMetadata != null && $Object.hasOwnProperty.call(m, "requestMetadata")) {
                            d.requestMetadata = $root.proto.Message.FullHistorySyncOnDemandRequestMetadata.toObject(m.requestMetadata, o, q + 1);
                        }
                        if (m.responseCode != null && $Object.hasOwnProperty.call(m, "responseCode")) {
                            d.responseCode = o.enums === $String ? $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode[m.responseCode] === $undefined ? m.responseCode : $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandResponseCode[m.responseCode] : m.responseCode;
                        }
                        return d;
                    };

                    FullHistorySyncOnDemandRequestResponse.prototype.toJSON = function() {
                        return FullHistorySyncOnDemandRequestResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    FullHistorySyncOnDemandRequestResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.FullHistorySyncOnDemandRequestResponse";
                    };

                    return FullHistorySyncOnDemandRequestResponse;
                })();

                PeerDataOperationResult.FullHistorySyncOnDemandResponseCode = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "REQUEST_SUCCESS"] = 0;
                    values[valuesById[1] = "REQUEST_TIME_EXPIRED"] = 1;
                    values[valuesById[2] = "DECLINED_SHARING_HISTORY"] = 2;
                    values[valuesById[3] = "GENERIC_ERROR"] = 3;
                    values[valuesById[4] = "ERROR_REQUEST_ON_NON_SMB_PRIMARY"] = 4;
                    values[valuesById[5] = "ERROR_HOSTED_DEVICE_NOT_CONNECTED"] = 5;
                    values[valuesById[6] = "ERROR_HOSTED_DEVICE_LOGIN_TIME_NOT_SET"] = 6;
                    values[valuesById[7] = "ERROR_MULTI_PROVIDER_NOT_CONFIGURED"] = 7;
                    return values;
                })();

                PeerDataOperationResult.HistorySyncChunkRetryResponse = (function() {

                    const HistorySyncChunkRetryResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    HistorySyncChunkRetryResponse.prototype.syncType = null;
                    HistorySyncChunkRetryResponse.prototype.chunkOrder = null;
                    HistorySyncChunkRetryResponse.prototype.requestId = null;
                    HistorySyncChunkRetryResponse.prototype.responseCode = null;
                    HistorySyncChunkRetryResponse.prototype.canRecover = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_syncType", {
                        get: $util.oneOfGetter($oneOfFields = ["syncType"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_chunkOrder", {
                        get: $util.oneOfGetter($oneOfFields = ["chunkOrder"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_requestId", {
                        get: $util.oneOfGetter($oneOfFields = ["requestId"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_responseCode", {
                        get: $util.oneOfGetter($oneOfFields = ["responseCode"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(HistorySyncChunkRetryResponse.prototype, "_canRecover", {
                        get: $util.oneOfGetter($oneOfFields = ["canRecover"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    HistorySyncChunkRetryResponse.create = function(properties) {
                        return new HistorySyncChunkRetryResponse(properties);
                    };

                    HistorySyncChunkRetryResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType"))
                            w.uint32(8).int32(m.syncType);
                        if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder"))
                            w.uint32(16).uint32(m.chunkOrder);
                        if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId"))
                            w.uint32(26).string(m.requestId);
                        if (m.responseCode != null && $Object.hasOwnProperty.call(m, "responseCode"))
                            w.uint32(32).int32(m.responseCode);
                        if (m.canRecover != null && $Object.hasOwnProperty.call(m, "canRecover"))
                            w.uint32(40).bool(m.canRecover);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    HistorySyncChunkRetryResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse(), v;
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 0)
                                        break;
                                    m.syncType = r.int32();
                                    m._syncType = "syncType";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.chunkOrder = r.uint32();
                                    m._chunkOrder = "chunkOrder";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    m.requestId = r.stringVerify();
                                    m._requestId = "requestId";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 0)
                                        break;
                                    m.responseCode = r.int32();
                                    m._responseCode = "responseCode";
                                    continue;
                                }
                            case 5: {
                                    if (u !== 0)
                                        break;
                                    m.canRecover = r.bool();
                                    m._canRecover = "canRecover";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    HistorySyncChunkRetryResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse();
                        switch (d.syncType) {
                        case "INITIAL_BOOTSTRAP":
                        case 0:
                            m.syncType = 0;
                            break;
                        case "INITIAL_STATUS_V3":
                        case 1:
                            m.syncType = 1;
                            break;
                        case "FULL":
                        case 2:
                            m.syncType = 2;
                            break;
                        case "RECENT":
                        case 3:
                            m.syncType = 3;
                            break;
                        case "PUSH_NAME":
                        case 4:
                            m.syncType = 4;
                            break;
                        case "NON_BLOCKING_DATA":
                        case 5:
                            m.syncType = 5;
                            break;
                        case "ON_DEMAND":
                        case 6:
                            m.syncType = 6;
                            break;
                        case "NO_HISTORY":
                        case 7:
                            m.syncType = 7;
                            break;
                        case "MESSAGE_ACCESS_STATUS":
                        case 8:
                            m.syncType = 8;
                            break;
                        default:
                            if (typeof d.syncType === "number" && (d.syncType | 0) === d.syncType)
                                m.syncType = d.syncType;
                        }
                        if (d.chunkOrder != null) {
                            m.chunkOrder = d.chunkOrder >>> 0;
                        }
                        if (d.requestId != null) {
                            m.requestId = $String(d.requestId);
                        }
                        switch (d.responseCode) {
                        case "GENERATION_ERROR":
                        case 1:
                            m.responseCode = 1;
                            break;
                        case "CHUNK_CONSUMED":
                        case 2:
                            m.responseCode = 2;
                            break;
                        case "TIMEOUT":
                        case 3:
                            m.responseCode = 3;
                            break;
                        case "SESSION_EXHAUSTED":
                        case 4:
                            m.responseCode = 4;
                            break;
                        case "CHUNK_EXHAUSTED":
                        case 5:
                            m.responseCode = 5;
                            break;
                        case "DUPLICATED_REQUEST":
                        case 6:
                            m.responseCode = 6;
                            break;
                        default:
                            if (typeof d.responseCode === "number" && (d.responseCode | 0) === d.responseCode)
                                m.responseCode = d.responseCode;
                        }
                        if (d.canRecover != null) {
                            m.canRecover = $Boolean(d.canRecover);
                        }
                        return m;
                    };

                    HistorySyncChunkRetryResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.syncType != null && $Object.hasOwnProperty.call(m, "syncType")) {
                            d.syncType = o.enums === $String ? $root.proto.Message.HistorySyncType[m.syncType] === $undefined ? m.syncType : $root.proto.Message.HistorySyncType[m.syncType] : m.syncType;
                        }
                        if (m.chunkOrder != null && $Object.hasOwnProperty.call(m, "chunkOrder")) {
                            d.chunkOrder = m.chunkOrder;
                        }
                        if (m.requestId != null && $Object.hasOwnProperty.call(m, "requestId")) {
                            d.requestId = m.requestId;
                        }
                        if (m.responseCode != null && $Object.hasOwnProperty.call(m, "responseCode")) {
                            d.responseCode = o.enums === $String ? $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode[m.responseCode] === $undefined ? m.responseCode : $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponseCode[m.responseCode] : m.responseCode;
                        }
                        if (m.canRecover != null && $Object.hasOwnProperty.call(m, "canRecover")) {
                            d.canRecover = m.canRecover;
                        }
                        return d;
                    };

                    HistorySyncChunkRetryResponse.prototype.toJSON = function() {
                        return HistorySyncChunkRetryResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    HistorySyncChunkRetryResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.HistorySyncChunkRetryResponse";
                    };

                    return HistorySyncChunkRetryResponse;
                })();

                PeerDataOperationResult.HistorySyncChunkRetryResponseCode = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[1] = "GENERATION_ERROR"] = 1;
                    values[valuesById[2] = "CHUNK_CONSUMED"] = 2;
                    values[valuesById[3] = "TIMEOUT"] = 3;
                    values[valuesById[4] = "SESSION_EXHAUSTED"] = 4;
                    values[valuesById[5] = "CHUNK_EXHAUSTED"] = 5;
                    values[valuesById[6] = "DUPLICATED_REQUEST"] = 6;
                    return values;
                })();

                PeerDataOperationResult.LinkPreviewResponse = (function() {

                    const LinkPreviewResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    LinkPreviewResponse.prototype.url = null;
                    LinkPreviewResponse.prototype.title = null;
                    LinkPreviewResponse.prototype.description = null;
                    LinkPreviewResponse.prototype.thumbData = null;
                    LinkPreviewResponse.prototype.matchText = null;
                    LinkPreviewResponse.prototype.previewType = null;
                    LinkPreviewResponse.prototype.hqThumbnail = null;
                    LinkPreviewResponse.prototype.previewMetadata = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_url", {
                        get: $util.oneOfGetter($oneOfFields = ["url"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_title", {
                        get: $util.oneOfGetter($oneOfFields = ["title"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_description", {
                        get: $util.oneOfGetter($oneOfFields = ["description"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_thumbData", {
                        get: $util.oneOfGetter($oneOfFields = ["thumbData"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_matchText", {
                        get: $util.oneOfGetter($oneOfFields = ["matchText"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_previewType", {
                        get: $util.oneOfGetter($oneOfFields = ["previewType"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_hqThumbnail", {
                        get: $util.oneOfGetter($oneOfFields = ["hqThumbnail"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(LinkPreviewResponse.prototype, "_previewMetadata", {
                        get: $util.oneOfGetter($oneOfFields = ["previewMetadata"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    LinkPreviewResponse.create = function(properties) {
                        return new LinkPreviewResponse(properties);
                    };

                    LinkPreviewResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                            w.uint32(10).string(m.url);
                        if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                            w.uint32(18).string(m.title);
                        if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                            w.uint32(26).string(m.description);
                        if (m.thumbData != null && $Object.hasOwnProperty.call(m, "thumbData"))
                            w.uint32(34).bytes(m.thumbData);
                        if (m.matchText != null && $Object.hasOwnProperty.call(m, "matchText"))
                            w.uint32(50).string(m.matchText);
                        if (m.previewType != null && $Object.hasOwnProperty.call(m, "previewType"))
                            w.uint32(58).string(m.previewType);
                        if (m.hqThumbnail != null && $Object.hasOwnProperty.call(m, "hqThumbnail"))
                            $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.encode(m.hqThumbnail, w.uint32(66).fork(), q + 1).ldelim();
                        if (m.previewMetadata != null && $Object.hasOwnProperty.call(m, "previewMetadata"))
                            $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.encode(m.previewMetadata, w.uint32(74).fork(), q + 1).ldelim();
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    LinkPreviewResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.url = r.stringVerify();
                                    m._url = "url";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.title = r.stringVerify();
                                    m._title = "title";
                                    continue;
                                }
                            case 3: {
                                    if (u !== 2)
                                        break;
                                    m.description = r.stringVerify();
                                    m._description = "description";
                                    continue;
                                }
                            case 4: {
                                    if (u !== 2)
                                        break;
                                    m.thumbData = r.bytes();
                                    m._thumbData = "thumbData";
                                    continue;
                                }
                            case 6: {
                                    if (u !== 2)
                                        break;
                                    m.matchText = r.stringVerify();
                                    m._matchText = "matchText";
                                    continue;
                                }
                            case 7: {
                                    if (u !== 2)
                                        break;
                                    m.previewType = r.stringVerify();
                                    m._previewType = "previewType";
                                    continue;
                                }
                            case 8: {
                                    if (u !== 2)
                                        break;
                                    m.hqThumbnail = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.decode(r, r.uint32(), $undefined, q + 1, m.hqThumbnail);
                                    m._hqThumbnail = "hqThumbnail";
                                    continue;
                                }
                            case 9: {
                                    if (u !== 2)
                                        break;
                                    m.previewMetadata = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.decode(r, r.uint32(), $undefined, q + 1, m.previewMetadata);
                                    m._previewMetadata = "previewMetadata";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    LinkPreviewResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse();
                        if (d.url != null) {
                            m.url = $String(d.url);
                        }
                        if (d.title != null) {
                            m.title = $String(d.title);
                        }
                        if (d.description != null) {
                            m.description = $String(d.description);
                        }
                        if (d.thumbData != null) {
                            if (typeof d.thumbData === "string")
                                $util.base64.decode(d.thumbData, m.thumbData = $util.newBuffer($util.base64.length(d.thumbData)), 0);
                            else if (d.thumbData.length >= 0)
                                m.thumbData = d.thumbData;
                        }
                        if (d.matchText != null) {
                            m.matchText = $String(d.matchText);
                        }
                        if (d.previewType != null) {
                            m.previewType = $String(d.previewType);
                        }
                        if (d.hqThumbnail != null) {
                            if (!$util.isObject(d.hqThumbnail))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.hqThumbnail: object expected");
                            m.hqThumbnail = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.fromObject(d.hqThumbnail, q + 1);
                        }
                        if (d.previewMetadata != null) {
                            if (!$util.isObject(d.previewMetadata))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.previewMetadata: object expected");
                            m.previewMetadata = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.fromObject(d.previewMetadata, q + 1);
                        }
                        return m;
                    };

                    LinkPreviewResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                            d.url = m.url;
                        }
                        if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                            d.title = m.title;
                        }
                        if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                            d.description = m.description;
                        }
                        if (m.thumbData != null && $Object.hasOwnProperty.call(m, "thumbData")) {
                            d.thumbData = o.bytes === $String ? $util.base64.encode(m.thumbData, 0, m.thumbData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbData) : m.thumbData;
                        }
                        if (m.matchText != null && $Object.hasOwnProperty.call(m, "matchText")) {
                            d.matchText = m.matchText;
                        }
                        if (m.previewType != null && $Object.hasOwnProperty.call(m, "previewType")) {
                            d.previewType = m.previewType;
                        }
                        if (m.hqThumbnail != null && $Object.hasOwnProperty.call(m, "hqThumbnail")) {
                            d.hqThumbnail = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail.toObject(m.hqThumbnail, o, q + 1);
                        }
                        if (m.previewMetadata != null && $Object.hasOwnProperty.call(m, "previewMetadata")) {
                            d.previewMetadata = $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata.toObject(m.previewMetadata, o, q + 1);
                        }
                        return d;
                    };

                    LinkPreviewResponse.prototype.toJSON = function() {
                        return LinkPreviewResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    LinkPreviewResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse";
                    };

                    LinkPreviewResponse.LinkPreviewHighQualityThumbnail = (function() {

                        const LinkPreviewHighQualityThumbnail = function (p) {
                            if (p)
                                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                                        this[ks[i]] = p[ks[i]];
                        };

                        LinkPreviewHighQualityThumbnail.prototype.directPath = null;
                        LinkPreviewHighQualityThumbnail.prototype.thumbHash = null;
                        LinkPreviewHighQualityThumbnail.prototype.encThumbHash = null;
                        LinkPreviewHighQualityThumbnail.prototype.mediaKey = null;
                        LinkPreviewHighQualityThumbnail.prototype.mediaKeyTimestampMs = null;
                        LinkPreviewHighQualityThumbnail.prototype.thumbWidth = null;
                        LinkPreviewHighQualityThumbnail.prototype.thumbHeight = null;

                        let $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_directPath", {
                            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbHash", {
                            get: $util.oneOfGetter($oneOfFields = ["thumbHash"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_encThumbHash", {
                            get: $util.oneOfGetter($oneOfFields = ["encThumbHash"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_mediaKey", {
                            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_mediaKeyTimestampMs", {
                            get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestampMs"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbWidth", {
                            get: $util.oneOfGetter($oneOfFields = ["thumbWidth"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(LinkPreviewHighQualityThumbnail.prototype, "_thumbHeight", {
                            get: $util.oneOfGetter($oneOfFields = ["thumbHeight"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        LinkPreviewHighQualityThumbnail.create = function(properties) {
                            return new LinkPreviewHighQualityThumbnail(properties);
                        };

                        LinkPreviewHighQualityThumbnail.encode = function (m, w, q) {
                            if (!w)
                                w = $Writer.create();
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                                w.uint32(10).string(m.directPath);
                            if (m.thumbHash != null && $Object.hasOwnProperty.call(m, "thumbHash"))
                                w.uint32(18).string(m.thumbHash);
                            if (m.encThumbHash != null && $Object.hasOwnProperty.call(m, "encThumbHash"))
                                w.uint32(26).string(m.encThumbHash);
                            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                                w.uint32(34).bytes(m.mediaKey);
                            if (m.mediaKeyTimestampMs != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestampMs"))
                                w.uint32(40).int64(m.mediaKeyTimestampMs);
                            if (m.thumbWidth != null && $Object.hasOwnProperty.call(m, "thumbWidth"))
                                w.uint32(48).int32(m.thumbWidth);
                            if (m.thumbHeight != null && $Object.hasOwnProperty.call(m, "thumbHeight"))
                                w.uint32(56).int32(m.thumbHeight);
                            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                                for (var i = 0; i < m.$unknowns.length; ++i)
                                    w.raw(m.$unknowns[i]);
                            return w;
                        };

                        LinkPreviewHighQualityThumbnail.decode = function (r, l, z, q, g) {
                            if (!(r instanceof $Reader))
                                r = $Reader.create(r);
                            if (q === $undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail();
                            while (r.pos < c) {
                                var s = r.pos;
                                var t = r.tag();
                                if (t === z) {
                                    z = $undefined;
                                    break;
                                }
                                var u = t & 7;
                                switch (t >>>= 3) {
                                case 1: {
                                        if (u !== 2)
                                            break;
                                        m.directPath = r.stringVerify();
                                        m._directPath = "directPath";
                                        continue;
                                    }
                                case 2: {
                                        if (u !== 2)
                                            break;
                                        m.thumbHash = r.stringVerify();
                                        m._thumbHash = "thumbHash";
                                        continue;
                                    }
                                case 3: {
                                        if (u !== 2)
                                            break;
                                        m.encThumbHash = r.stringVerify();
                                        m._encThumbHash = "encThumbHash";
                                        continue;
                                    }
                                case 4: {
                                        if (u !== 2)
                                            break;
                                        m.mediaKey = r.bytes();
                                        m._mediaKey = "mediaKey";
                                        continue;
                                    }
                                case 5: {
                                        if (u !== 0)
                                            break;
                                        m.mediaKeyTimestampMs = r.int64();
                                        m._mediaKeyTimestampMs = "mediaKeyTimestampMs";
                                        continue;
                                    }
                                case 6: {
                                        if (u !== 0)
                                            break;
                                        m.thumbWidth = r.int32();
                                        m._thumbWidth = "thumbWidth";
                                        continue;
                                    }
                                case 7: {
                                        if (u !== 0)
                                            break;
                                        m.thumbHeight = r.int32();
                                        m._thumbHeight = "thumbHeight";
                                        continue;
                                    }
                                }
                                r.skipType(u, q, t);
                                if (!r.discardUnknown) {
                                    $util.makeProp(m, "$unknowns", false);
                                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                                }
                            }
                            if (z !== $undefined)
                                throw $Error("missing end group");
                            return m;
                        };

                        LinkPreviewHighQualityThumbnail.fromObject = function (d, q) {
                            if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail)
                                return d;
                            if (!$util.isObject(d))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail: object expected");
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail();
                            if (d.directPath != null) {
                                m.directPath = $String(d.directPath);
                            }
                            if (d.thumbHash != null) {
                                m.thumbHash = $String(d.thumbHash);
                            }
                            if (d.encThumbHash != null) {
                                m.encThumbHash = $String(d.encThumbHash);
                            }
                            if (d.mediaKey != null) {
                                if (typeof d.mediaKey === "string")
                                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                                else if (d.mediaKey.length >= 0)
                                    m.mediaKey = d.mediaKey;
                            }
                            if (d.mediaKeyTimestampMs != null) {
                                if ($util.Long)
                                    m.mediaKeyTimestampMs = $util.Long.fromValue(d.mediaKeyTimestampMs, false);
                                else if (typeof d.mediaKeyTimestampMs === "string")
                                    m.mediaKeyTimestampMs = $parseInt(d.mediaKeyTimestampMs, 10);
                                else if (typeof d.mediaKeyTimestampMs === "number")
                                    m.mediaKeyTimestampMs = d.mediaKeyTimestampMs;
                                else if (typeof d.mediaKeyTimestampMs === "object")
                                    m.mediaKeyTimestampMs = new $util.LongBits(d.mediaKeyTimestampMs.low >>> 0, d.mediaKeyTimestampMs.high >>> 0).toNumber();
                            }
                            if (d.thumbWidth != null) {
                                m.thumbWidth = d.thumbWidth | 0;
                            }
                            if (d.thumbHeight != null) {
                                m.thumbHeight = d.thumbHeight | 0;
                            }
                            return m;
                        };

                        LinkPreviewHighQualityThumbnail.toObject = function (m, o, q) {
                            if (!o)
                                o = {};
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var d = {};
                            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                                d.directPath = m.directPath;
                            }
                            if (m.thumbHash != null && $Object.hasOwnProperty.call(m, "thumbHash")) {
                                d.thumbHash = m.thumbHash;
                            }
                            if (m.encThumbHash != null && $Object.hasOwnProperty.call(m, "encThumbHash")) {
                                d.encThumbHash = m.encThumbHash;
                            }
                            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                                d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                            }
                            if (m.mediaKeyTimestampMs != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestampMs")) {
                                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                                    d.mediaKeyTimestampMs = typeof m.mediaKeyTimestampMs === "number" ? $BigInt(m.mediaKeyTimestampMs) : $util.Long.fromBits(m.mediaKeyTimestampMs.low >>> 0, m.mediaKeyTimestampMs.high >>> 0, false).toBigInt();
                                else if (typeof m.mediaKeyTimestampMs === "number")
                                    d.mediaKeyTimestampMs = o.longs === $String ? $String(m.mediaKeyTimestampMs) : m.mediaKeyTimestampMs;
                                else
                                    d.mediaKeyTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestampMs) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestampMs.low >>> 0, m.mediaKeyTimestampMs.high >>> 0).toNumber() : m.mediaKeyTimestampMs;
                            }
                            if (m.thumbWidth != null && $Object.hasOwnProperty.call(m, "thumbWidth")) {
                                d.thumbWidth = m.thumbWidth;
                            }
                            if (m.thumbHeight != null && $Object.hasOwnProperty.call(m, "thumbHeight")) {
                                d.thumbHeight = m.thumbHeight;
                            }
                            return d;
                        };

                        LinkPreviewHighQualityThumbnail.prototype.toJSON = function() {
                            return LinkPreviewHighQualityThumbnail.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        LinkPreviewHighQualityThumbnail.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.LinkPreviewHighQualityThumbnail";
                        };

                        return LinkPreviewHighQualityThumbnail;
                    })();

                    LinkPreviewResponse.PaymentLinkPreviewMetadata = (function() {

                        const PaymentLinkPreviewMetadata = function (p) {
                            if (p)
                                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                                        this[ks[i]] = p[ks[i]];
                        };

                        PaymentLinkPreviewMetadata.prototype.isBusinessVerified = null;
                        PaymentLinkPreviewMetadata.prototype.providerName = null;
                        PaymentLinkPreviewMetadata.prototype.amount = null;
                        PaymentLinkPreviewMetadata.prototype.offset = null;
                        PaymentLinkPreviewMetadata.prototype.currency = null;

                        let $oneOfFields;

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_isBusinessVerified", {
                            get: $util.oneOfGetter($oneOfFields = ["isBusinessVerified"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_providerName", {
                            get: $util.oneOfGetter($oneOfFields = ["providerName"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_amount", {
                            get: $util.oneOfGetter($oneOfFields = ["amount"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_offset", {
                            get: $util.oneOfGetter($oneOfFields = ["offset"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        // Virtual OneOf for proto3 optional field
                        $Object.defineProperty(PaymentLinkPreviewMetadata.prototype, "_currency", {
                            get: $util.oneOfGetter($oneOfFields = ["currency"]),
                            set: $util.oneOfSetter($oneOfFields)
                        });

                        PaymentLinkPreviewMetadata.create = function(properties) {
                            return new PaymentLinkPreviewMetadata(properties);
                        };

                        PaymentLinkPreviewMetadata.encode = function (m, w, q) {
                            if (!w)
                                w = $Writer.create();
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            if (m.isBusinessVerified != null && $Object.hasOwnProperty.call(m, "isBusinessVerified"))
                                w.uint32(8).bool(m.isBusinessVerified);
                            if (m.providerName != null && $Object.hasOwnProperty.call(m, "providerName"))
                                w.uint32(18).string(m.providerName);
                            if (m.amount != null && $Object.hasOwnProperty.call(m, "amount"))
                                w.uint32(26).string(m.amount);
                            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset"))
                                w.uint32(34).string(m.offset);
                            if (m.currency != null && $Object.hasOwnProperty.call(m, "currency"))
                                w.uint32(42).string(m.currency);
                            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                                for (var i = 0; i < m.$unknowns.length; ++i)
                                    w.raw(m.$unknowns[i]);
                            return w;
                        };

                        PaymentLinkPreviewMetadata.decode = function (r, l, z, q, g) {
                            if (!(r instanceof $Reader))
                                r = $Reader.create(r);
                            if (q === $undefined)
                                q = 0;
                            if (q > $Reader.recursionLimit)
                                throw $Error("max depth exceeded");
                            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata();
                            while (r.pos < c) {
                                var s = r.pos;
                                var t = r.tag();
                                if (t === z) {
                                    z = $undefined;
                                    break;
                                }
                                var u = t & 7;
                                switch (t >>>= 3) {
                                case 1: {
                                        if (u !== 0)
                                            break;
                                        m.isBusinessVerified = r.bool();
                                        m._isBusinessVerified = "isBusinessVerified";
                                        continue;
                                    }
                                case 2: {
                                        if (u !== 2)
                                            break;
                                        m.providerName = r.stringVerify();
                                        m._providerName = "providerName";
                                        continue;
                                    }
                                case 3: {
                                        if (u !== 2)
                                            break;
                                        m.amount = r.stringVerify();
                                        m._amount = "amount";
                                        continue;
                                    }
                                case 4: {
                                        if (u !== 2)
                                            break;
                                        m.offset = r.stringVerify();
                                        m._offset = "offset";
                                        continue;
                                    }
                                case 5: {
                                        if (u !== 2)
                                            break;
                                        m.currency = r.stringVerify();
                                        m._currency = "currency";
                                        continue;
                                    }
                                }
                                r.skipType(u, q, t);
                                if (!r.discardUnknown) {
                                    $util.makeProp(m, "$unknowns", false);
                                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                                }
                            }
                            if (z !== $undefined)
                                throw $Error("missing end group");
                            return m;
                        };

                        PaymentLinkPreviewMetadata.fromObject = function (d, q) {
                            if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata)
                                return d;
                            if (!$util.isObject(d))
                                throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata: object expected");
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata();
                            if (d.isBusinessVerified != null) {
                                m.isBusinessVerified = $Boolean(d.isBusinessVerified);
                            }
                            if (d.providerName != null) {
                                m.providerName = $String(d.providerName);
                            }
                            if (d.amount != null) {
                                m.amount = $String(d.amount);
                            }
                            if (d.offset != null) {
                                m.offset = $String(d.offset);
                            }
                            if (d.currency != null) {
                                m.currency = $String(d.currency);
                            }
                            return m;
                        };

                        PaymentLinkPreviewMetadata.toObject = function (m, o, q) {
                            if (!o)
                                o = {};
                            if (q === $undefined)
                                q = 0;
                            if (q > $util.recursionLimit)
                                throw $Error("max depth exceeded");
                            var d = {};
                            if (m.isBusinessVerified != null && $Object.hasOwnProperty.call(m, "isBusinessVerified")) {
                                d.isBusinessVerified = m.isBusinessVerified;
                            }
                            if (m.providerName != null && $Object.hasOwnProperty.call(m, "providerName")) {
                                d.providerName = m.providerName;
                            }
                            if (m.amount != null && $Object.hasOwnProperty.call(m, "amount")) {
                                d.amount = m.amount;
                            }
                            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset")) {
                                d.offset = m.offset;
                            }
                            if (m.currency != null && $Object.hasOwnProperty.call(m, "currency")) {
                                d.currency = m.currency;
                            }
                            return d;
                        };

                        PaymentLinkPreviewMetadata.prototype.toJSON = function() {
                            return PaymentLinkPreviewMetadata.toObject(this, $protobuf.util.toJSONOptions);
                        };

                        PaymentLinkPreviewMetadata.getTypeUrl = function(prefix) {
                            if (prefix === $undefined)
                                prefix = "type.googleapis.com";
                            return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.LinkPreviewResponse.PaymentLinkPreviewMetadata";
                        };

                        return PaymentLinkPreviewMetadata;
                    })();

                    return LinkPreviewResponse;
                })();

                PeerDataOperationResult.PlaceholderMessageResendResponse = (function() {

                    const PlaceholderMessageResendResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    PlaceholderMessageResendResponse.prototype.webMessageInfoBytes = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(PlaceholderMessageResendResponse.prototype, "_webMessageInfoBytes", {
                        get: $util.oneOfGetter($oneOfFields = ["webMessageInfoBytes"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    PlaceholderMessageResendResponse.create = function(properties) {
                        return new PlaceholderMessageResendResponse(properties);
                    };

                    PlaceholderMessageResendResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.webMessageInfoBytes != null && $Object.hasOwnProperty.call(m, "webMessageInfoBytes"))
                            w.uint32(10).bytes(m.webMessageInfoBytes);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    PlaceholderMessageResendResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.webMessageInfoBytes = r.bytes();
                                    m._webMessageInfoBytes = "webMessageInfoBytes";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    PlaceholderMessageResendResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse();
                        if (d.webMessageInfoBytes != null) {
                            if (typeof d.webMessageInfoBytes === "string")
                                $util.base64.decode(d.webMessageInfoBytes, m.webMessageInfoBytes = $util.newBuffer($util.base64.length(d.webMessageInfoBytes)), 0);
                            else if (d.webMessageInfoBytes.length >= 0)
                                m.webMessageInfoBytes = d.webMessageInfoBytes;
                        }
                        return m;
                    };

                    PlaceholderMessageResendResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.webMessageInfoBytes != null && $Object.hasOwnProperty.call(m, "webMessageInfoBytes")) {
                            d.webMessageInfoBytes = o.bytes === $String ? $util.base64.encode(m.webMessageInfoBytes, 0, m.webMessageInfoBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.webMessageInfoBytes) : m.webMessageInfoBytes;
                        }
                        return d;
                    };

                    PlaceholderMessageResendResponse.prototype.toJSON = function() {
                        return PlaceholderMessageResendResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    PlaceholderMessageResendResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.PlaceholderMessageResendResponse";
                    };

                    return PlaceholderMessageResendResponse;
                })();

                PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse = (function() {

                    const SyncDSnapshotFatalRecoveryResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    SyncDSnapshotFatalRecoveryResponse.prototype.collectionSnapshot = null;
                    SyncDSnapshotFatalRecoveryResponse.prototype.isCompressed = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SyncDSnapshotFatalRecoveryResponse.prototype, "_collectionSnapshot", {
                        get: $util.oneOfGetter($oneOfFields = ["collectionSnapshot"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(SyncDSnapshotFatalRecoveryResponse.prototype, "_isCompressed", {
                        get: $util.oneOfGetter($oneOfFields = ["isCompressed"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    SyncDSnapshotFatalRecoveryResponse.create = function(properties) {
                        return new SyncDSnapshotFatalRecoveryResponse(properties);
                    };

                    SyncDSnapshotFatalRecoveryResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.collectionSnapshot != null && $Object.hasOwnProperty.call(m, "collectionSnapshot"))
                            w.uint32(10).bytes(m.collectionSnapshot);
                        if (m.isCompressed != null && $Object.hasOwnProperty.call(m, "isCompressed"))
                            w.uint32(16).bool(m.isCompressed);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    SyncDSnapshotFatalRecoveryResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.collectionSnapshot = r.bytes();
                                    m._collectionSnapshot = "collectionSnapshot";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 0)
                                        break;
                                    m.isCompressed = r.bool();
                                    m._isCompressed = "isCompressed";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    SyncDSnapshotFatalRecoveryResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse();
                        if (d.collectionSnapshot != null) {
                            if (typeof d.collectionSnapshot === "string")
                                $util.base64.decode(d.collectionSnapshot, m.collectionSnapshot = $util.newBuffer($util.base64.length(d.collectionSnapshot)), 0);
                            else if (d.collectionSnapshot.length >= 0)
                                m.collectionSnapshot = d.collectionSnapshot;
                        }
                        if (d.isCompressed != null) {
                            m.isCompressed = $Boolean(d.isCompressed);
                        }
                        return m;
                    };

                    SyncDSnapshotFatalRecoveryResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.collectionSnapshot != null && $Object.hasOwnProperty.call(m, "collectionSnapshot")) {
                            d.collectionSnapshot = o.bytes === $String ? $util.base64.encode(m.collectionSnapshot, 0, m.collectionSnapshot.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.collectionSnapshot) : m.collectionSnapshot;
                        }
                        if (m.isCompressed != null && $Object.hasOwnProperty.call(m, "isCompressed")) {
                            d.isCompressed = m.isCompressed;
                        }
                        return d;
                    };

                    SyncDSnapshotFatalRecoveryResponse.prototype.toJSON = function() {
                        return SyncDSnapshotFatalRecoveryResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    SyncDSnapshotFatalRecoveryResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.SyncDSnapshotFatalRecoveryResponse";
                    };

                    return SyncDSnapshotFatalRecoveryResponse;
                })();

                PeerDataOperationResult.WaffleNonceFetchResponse = (function() {

                    const WaffleNonceFetchResponse = function (p) {
                        if (p)
                            for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                                if (p[ks[i]] != null && ks[i] !== "__proto__")
                                    this[ks[i]] = p[ks[i]];
                    };

                    WaffleNonceFetchResponse.prototype.nonce = null;
                    WaffleNonceFetchResponse.prototype.waEntFbid = null;

                    let $oneOfFields;

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(WaffleNonceFetchResponse.prototype, "_nonce", {
                        get: $util.oneOfGetter($oneOfFields = ["nonce"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    // Virtual OneOf for proto3 optional field
                    $Object.defineProperty(WaffleNonceFetchResponse.prototype, "_waEntFbid", {
                        get: $util.oneOfGetter($oneOfFields = ["waEntFbid"]),
                        set: $util.oneOfSetter($oneOfFields)
                    });

                    WaffleNonceFetchResponse.create = function(properties) {
                        return new WaffleNonceFetchResponse(properties);
                    };

                    WaffleNonceFetchResponse.encode = function (m, w, q) {
                        if (!w)
                            w = $Writer.create();
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce"))
                            w.uint32(10).string(m.nonce);
                        if (m.waEntFbid != null && $Object.hasOwnProperty.call(m, "waEntFbid"))
                            w.uint32(18).string(m.waEntFbid);
                        if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                            for (var i = 0; i < m.$unknowns.length; ++i)
                                w.raw(m.$unknowns[i]);
                        return w;
                    };

                    WaffleNonceFetchResponse.decode = function (r, l, z, q, g) {
                        if (!(r instanceof $Reader))
                            r = $Reader.create(r);
                        if (q === $undefined)
                            q = 0;
                        if (q > $Reader.recursionLimit)
                            throw $Error("max depth exceeded");
                        var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse();
                        while (r.pos < c) {
                            var s = r.pos;
                            var t = r.tag();
                            if (t === z) {
                                z = $undefined;
                                break;
                            }
                            var u = t & 7;
                            switch (t >>>= 3) {
                            case 1: {
                                    if (u !== 2)
                                        break;
                                    m.nonce = r.stringVerify();
                                    m._nonce = "nonce";
                                    continue;
                                }
                            case 2: {
                                    if (u !== 2)
                                        break;
                                    m.waEntFbid = r.stringVerify();
                                    m._waEntFbid = "waEntFbid";
                                    continue;
                                }
                            }
                            r.skipType(u, q, t);
                            if (!r.discardUnknown) {
                                $util.makeProp(m, "$unknowns", false);
                                (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                            }
                        }
                        if (z !== $undefined)
                            throw $Error("missing end group");
                        return m;
                    };

                    WaffleNonceFetchResponse.fromObject = function (d, q) {
                        if (d instanceof $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse)
                            return d;
                        if (!$util.isObject(d))
                            throw $TypeError(".proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse: object expected");
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var m = new $root.proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse();
                        if (d.nonce != null) {
                            m.nonce = $String(d.nonce);
                        }
                        if (d.waEntFbid != null) {
                            m.waEntFbid = $String(d.waEntFbid);
                        }
                        return m;
                    };

                    WaffleNonceFetchResponse.toObject = function (m, o, q) {
                        if (!o)
                            o = {};
                        if (q === $undefined)
                            q = 0;
                        if (q > $util.recursionLimit)
                            throw $Error("max depth exceeded");
                        var d = {};
                        if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce")) {
                            d.nonce = m.nonce;
                        }
                        if (m.waEntFbid != null && $Object.hasOwnProperty.call(m, "waEntFbid")) {
                            d.waEntFbid = m.waEntFbid;
                        }
                        return d;
                    };

                    WaffleNonceFetchResponse.prototype.toJSON = function() {
                        return WaffleNonceFetchResponse.toObject(this, $protobuf.util.toJSONOptions);
                    };

                    WaffleNonceFetchResponse.getTypeUrl = function(prefix) {
                        if (prefix === $undefined)
                            prefix = "type.googleapis.com";
                        return prefix + "/proto.Message.PeerDataOperationRequestResponseMessage.PeerDataOperationResult.WaffleNonceFetchResponse";
                    };

                    return WaffleNonceFetchResponse;
                })();

                return PeerDataOperationResult;
            })();

            return PeerDataOperationRequestResponseMessage;
        })();

        Message.PeerDataOperationRequestType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UPLOAD_STICKER"] = 0;
            values[valuesById[1] = "SEND_RECENT_STICKER_BOOTSTRAP"] = 1;
            values[valuesById[2] = "GENERATE_LINK_PREVIEW"] = 2;
            values[valuesById[3] = "HISTORY_SYNC_ON_DEMAND"] = 3;
            values[valuesById[4] = "PLACEHOLDER_MESSAGE_RESEND"] = 4;
            values[valuesById[5] = "WAFFLE_LINKING_NONCE_FETCH"] = 5;
            values[valuesById[6] = "FULL_HISTORY_SYNC_ON_DEMAND"] = 6;
            values[valuesById[7] = "COMPANION_META_NONCE_FETCH"] = 7;
            values[valuesById[8] = "COMPANION_SYNCD_SNAPSHOT_FATAL_RECOVERY"] = 8;
            values[valuesById[9] = "COMPANION_CANONICAL_USER_NONCE_FETCH"] = 9;
            values[valuesById[10] = "HISTORY_SYNC_CHUNK_RETRY"] = 10;
            values[valuesById[11] = "GALAXY_FLOW_ACTION"] = 11;
            values[valuesById[12] = "BUSINESS_BROADCAST_INSIGHTS_DELIVERED_TO"] = 12;
            values[valuesById[13] = "BUSINESS_BROADCAST_INSIGHTS_REFRESH"] = 13;
            values[valuesById[14] = "CONTACT_REFRESH_REQUEST"] = 14;
            return values;
        })();

        Message.PinInChatMessage = (function() {

            const PinInChatMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PinInChatMessage.prototype.key = null;
            PinInChatMessage.prototype.type = null;
            PinInChatMessage.prototype.senderTimestampMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PinInChatMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PinInChatMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PinInChatMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PinInChatMessage.create = function(properties) {
                return new PinInChatMessage(properties);
            };

            PinInChatMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(16).int32(m.type);
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                    w.uint32(24).int64(m.senderTimestampMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PinInChatMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PinInChatMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.senderTimestampMs = r.int64();
                            m._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PinInChatMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PinInChatMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PinInChatMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PinInChatMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.PinInChatMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                switch (d.type) {
                case "UNKNOWN_TYPE":
                case 0:
                    m.type = 0;
                    break;
                case "PIN_FOR_ALL":
                case 1:
                    m.type = 1;
                    break;
                case "UNPIN_FOR_ALL":
                case 2:
                    m.type = 2;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.senderTimestampMs != null) {
                    if ($util.Long)
                        m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                    else if (typeof d.senderTimestampMs === "string")
                        m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                    else if (typeof d.senderTimestampMs === "number")
                        m.senderTimestampMs = d.senderTimestampMs;
                    else if (typeof d.senderTimestampMs === "object")
                        m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
                }
                return m;
            };

            PinInChatMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.PinInChatMessage.Type[m.type] === $undefined ? m.type : $root.proto.Message.PinInChatMessage.Type[m.type] : m.type;
                }
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.senderTimestampMs === "number")
                        d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                    else
                        d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
                }
                return d;
            };

            PinInChatMessage.prototype.toJSON = function() {
                return PinInChatMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PinInChatMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PinInChatMessage";
            };

            PinInChatMessage.Type = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
                values[valuesById[1] = "PIN_FOR_ALL"] = 1;
                values[valuesById[2] = "UNPIN_FOR_ALL"] = 2;
                return values;
            })();

            return PinInChatMessage;
        })();

        Message.PlaceholderMessage = (function() {

            const PlaceholderMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PlaceholderMessage.prototype.type = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PlaceholderMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PlaceholderMessage.create = function(properties) {
                return new PlaceholderMessage(properties);
            };

            PlaceholderMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PlaceholderMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PlaceholderMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PlaceholderMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PlaceholderMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PlaceholderMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PlaceholderMessage();
                switch (d.type) {
                case "MASK_LINKED_DEVICES":
                case 0:
                    m.type = 0;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                return m;
            };

            PlaceholderMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.PlaceholderMessage.PlaceholderType[m.type] === $undefined ? m.type : $root.proto.Message.PlaceholderMessage.PlaceholderType[m.type] : m.type;
                }
                return d;
            };

            PlaceholderMessage.prototype.toJSON = function() {
                return PlaceholderMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PlaceholderMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PlaceholderMessage";
            };

            PlaceholderMessage.PlaceholderType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "MASK_LINKED_DEVICES"] = 0;
                return values;
            })();

            return PlaceholderMessage;
        })();

        Message.PollAddOptionMessage = (function() {

            const PollAddOptionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollAddOptionMessage.prototype.pollCreationMessageKey = null;
            PollAddOptionMessage.prototype.addOption = null;
            PollAddOptionMessage.prototype.metadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollAddOptionMessage.prototype, "_pollCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollAddOptionMessage.prototype, "_addOption", {
                get: $util.oneOfGetter($oneOfFields = ["addOption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollAddOptionMessage.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollAddOptionMessage.create = function(properties) {
                return new PollAddOptionMessage(properties);
            };

            PollAddOptionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pollCreationMessageKey != null && $Object.hasOwnProperty.call(m, "pollCreationMessageKey"))
                    $root.proto.MessageKey.encode(m.pollCreationMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.addOption != null && $Object.hasOwnProperty.call(m, "addOption"))
                    $root.proto.Message.PollCreationMessage.Option.encode(m.addOption, w.uint32(18).fork(), q + 1).ldelim();
                if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata"))
                    $root.proto.Message.PollUpdateMessageMetadata.encode(m.metadata, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollAddOptionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollAddOptionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.pollCreationMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageKey);
                            m._pollCreationMessageKey = "pollCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.addOption = $root.proto.Message.PollCreationMessage.Option.decode(r, r.uint32(), $undefined, q + 1, m.addOption);
                            m._addOption = "addOption";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.metadata = $root.proto.Message.PollUpdateMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.metadata);
                            m._metadata = "metadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollAddOptionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollAddOptionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollAddOptionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollAddOptionMessage();
                if (d.pollCreationMessageKey != null) {
                    if (!$util.isObject(d.pollCreationMessageKey))
                        throw $TypeError(".proto.Message.PollAddOptionMessage.pollCreationMessageKey: object expected");
                    m.pollCreationMessageKey = $root.proto.MessageKey.fromObject(d.pollCreationMessageKey, q + 1);
                }
                if (d.addOption != null) {
                    if (!$util.isObject(d.addOption))
                        throw $TypeError(".proto.Message.PollAddOptionMessage.addOption: object expected");
                    m.addOption = $root.proto.Message.PollCreationMessage.Option.fromObject(d.addOption, q + 1);
                }
                if (d.metadata != null) {
                    if (!$util.isObject(d.metadata))
                        throw $TypeError(".proto.Message.PollAddOptionMessage.metadata: object expected");
                    m.metadata = $root.proto.Message.PollUpdateMessageMetadata.fromObject(d.metadata, q + 1);
                }
                return m;
            };

            PollAddOptionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pollCreationMessageKey != null && $Object.hasOwnProperty.call(m, "pollCreationMessageKey")) {
                    d.pollCreationMessageKey = $root.proto.MessageKey.toObject(m.pollCreationMessageKey, o, q + 1);
                }
                if (m.addOption != null && $Object.hasOwnProperty.call(m, "addOption")) {
                    d.addOption = $root.proto.Message.PollCreationMessage.Option.toObject(m.addOption, o, q + 1);
                }
                if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata")) {
                    d.metadata = $root.proto.Message.PollUpdateMessageMetadata.toObject(m.metadata, o, q + 1);
                }
                return d;
            };

            PollAddOptionMessage.prototype.toJSON = function() {
                return PollAddOptionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollAddOptionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollAddOptionMessage";
            };

            return PollAddOptionMessage;
        })();

        Message.PollContentType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "TEXT"] = 1;
            values[valuesById[2] = "IMAGE"] = 2;
            return values;
        })();

        Message.PollCreationMessage = (function() {

            const PollCreationMessage = function (p) {
                this.options = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollCreationMessage.prototype.encKey = null;
            PollCreationMessage.prototype.name = null;
            PollCreationMessage.prototype.options = $util.emptyArray;
            PollCreationMessage.prototype.selectableOptionsCount = null;
            PollCreationMessage.prototype.contextInfo = null;
            PollCreationMessage.prototype.pollContentType = null;
            PollCreationMessage.prototype.pollType = null;
            PollCreationMessage.prototype.correctAnswer = null;
            PollCreationMessage.prototype.endTime = null;
            PollCreationMessage.prototype.hideParticipantName = null;
            PollCreationMessage.prototype.allowAddOption = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_encKey", {
                get: $util.oneOfGetter($oneOfFields = ["encKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_selectableOptionsCount", {
                get: $util.oneOfGetter($oneOfFields = ["selectableOptionsCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_pollContentType", {
                get: $util.oneOfGetter($oneOfFields = ["pollContentType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_pollType", {
                get: $util.oneOfGetter($oneOfFields = ["pollType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_correctAnswer", {
                get: $util.oneOfGetter($oneOfFields = ["correctAnswer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_endTime", {
                get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_hideParticipantName", {
                get: $util.oneOfGetter($oneOfFields = ["hideParticipantName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollCreationMessage.prototype, "_allowAddOption", {
                get: $util.oneOfGetter($oneOfFields = ["allowAddOption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollCreationMessage.create = function(properties) {
                return new PollCreationMessage(properties);
            };

            PollCreationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.encKey != null && $Object.hasOwnProperty.call(m, "encKey"))
                    w.uint32(10).bytes(m.encKey);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(18).string(m.name);
                if (m.options != null && m.options.length) {
                    for (var i = 0; i < m.options.length; ++i)
                        $root.proto.Message.PollCreationMessage.Option.encode(m.options[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.selectableOptionsCount != null && $Object.hasOwnProperty.call(m, "selectableOptionsCount"))
                    w.uint32(32).uint32(m.selectableOptionsCount);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(42).fork(), q + 1).ldelim();
                if (m.pollContentType != null && $Object.hasOwnProperty.call(m, "pollContentType"))
                    w.uint32(48).int32(m.pollContentType);
                if (m.pollType != null && $Object.hasOwnProperty.call(m, "pollType"))
                    w.uint32(56).int32(m.pollType);
                if (m.correctAnswer != null && $Object.hasOwnProperty.call(m, "correctAnswer"))
                    $root.proto.Message.PollCreationMessage.Option.encode(m.correctAnswer, w.uint32(66).fork(), q + 1).ldelim();
                if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime"))
                    w.uint32(72).int64(m.endTime);
                if (m.hideParticipantName != null && $Object.hasOwnProperty.call(m, "hideParticipantName"))
                    w.uint32(80).bool(m.hideParticipantName);
                if (m.allowAddOption != null && $Object.hasOwnProperty.call(m, "allowAddOption"))
                    w.uint32(88).bool(m.allowAddOption);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollCreationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollCreationMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.encKey = r.bytes();
                            m._encKey = "encKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.options && m.options.length))
                                m.options = [];
                            m.options.push($root.proto.Message.PollCreationMessage.Option.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.selectableOptionsCount = r.uint32();
                            m._selectableOptionsCount = "selectableOptionsCount";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.pollContentType = r.int32();
                            m._pollContentType = "pollContentType";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.pollType = r.int32();
                            m._pollType = "pollType";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.correctAnswer = $root.proto.Message.PollCreationMessage.Option.decode(r, r.uint32(), $undefined, q + 1, m.correctAnswer);
                            m._correctAnswer = "correctAnswer";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.endTime = r.int64();
                            m._endTime = "endTime";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.hideParticipantName = r.bool();
                            m._hideParticipantName = "hideParticipantName";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.allowAddOption = r.bool();
                            m._allowAddOption = "allowAddOption";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollCreationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollCreationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollCreationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollCreationMessage();
                if (d.encKey != null) {
                    if (typeof d.encKey === "string")
                        $util.base64.decode(d.encKey, m.encKey = $util.newBuffer($util.base64.length(d.encKey)), 0);
                    else if (d.encKey.length >= 0)
                        m.encKey = d.encKey;
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.options) {
                    if (!$Array.isArray(d.options))
                        throw $TypeError(".proto.Message.PollCreationMessage.options: array expected");
                    m.options = $Array(d.options.length);
                    for (var i = 0; i < d.options.length; ++i) {
                        if (!$util.isObject(d.options[i]))
                            throw $TypeError(".proto.Message.PollCreationMessage.options: object expected");
                        m.options[i] = $root.proto.Message.PollCreationMessage.Option.fromObject(d.options[i], q + 1);
                    }
                }
                if (d.selectableOptionsCount != null) {
                    m.selectableOptionsCount = d.selectableOptionsCount >>> 0;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.PollCreationMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                switch (d.pollContentType) {
                case "UNKNOWN":
                case 0:
                    m.pollContentType = 0;
                    break;
                case "TEXT":
                case 1:
                    m.pollContentType = 1;
                    break;
                case "IMAGE":
                case 2:
                    m.pollContentType = 2;
                    break;
                default:
                    if (typeof d.pollContentType === "number" && (d.pollContentType | 0) === d.pollContentType)
                        m.pollContentType = d.pollContentType;
                }
                switch (d.pollType) {
                case "POLL":
                case 0:
                    m.pollType = 0;
                    break;
                case "QUIZ":
                case 1:
                    m.pollType = 1;
                    break;
                default:
                    if (typeof d.pollType === "number" && (d.pollType | 0) === d.pollType)
                        m.pollType = d.pollType;
                }
                if (d.correctAnswer != null) {
                    if (!$util.isObject(d.correctAnswer))
                        throw $TypeError(".proto.Message.PollCreationMessage.correctAnswer: object expected");
                    m.correctAnswer = $root.proto.Message.PollCreationMessage.Option.fromObject(d.correctAnswer, q + 1);
                }
                if (d.endTime != null) {
                    if ($util.Long)
                        m.endTime = $util.Long.fromValue(d.endTime, false);
                    else if (typeof d.endTime === "string")
                        m.endTime = $parseInt(d.endTime, 10);
                    else if (typeof d.endTime === "number")
                        m.endTime = d.endTime;
                    else if (typeof d.endTime === "object")
                        m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
                }
                if (d.hideParticipantName != null) {
                    m.hideParticipantName = $Boolean(d.hideParticipantName);
                }
                if (d.allowAddOption != null) {
                    m.allowAddOption = $Boolean(d.allowAddOption);
                }
                return m;
            };

            PollCreationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.options = [];
                }
                if (m.encKey != null && $Object.hasOwnProperty.call(m, "encKey")) {
                    d.encKey = o.bytes === $String ? $util.base64.encode(m.encKey, 0, m.encKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encKey) : m.encKey;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.options && m.options.length) {
                    d.options = $Array(m.options.length);
                    for (var j = 0; j < m.options.length; ++j) {
                        d.options[j] = $root.proto.Message.PollCreationMessage.Option.toObject(m.options[j], o, q + 1);
                    }
                }
                if (m.selectableOptionsCount != null && $Object.hasOwnProperty.call(m, "selectableOptionsCount")) {
                    d.selectableOptionsCount = m.selectableOptionsCount;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.pollContentType != null && $Object.hasOwnProperty.call(m, "pollContentType")) {
                    d.pollContentType = o.enums === $String ? $root.proto.Message.PollContentType[m.pollContentType] === $undefined ? m.pollContentType : $root.proto.Message.PollContentType[m.pollContentType] : m.pollContentType;
                }
                if (m.pollType != null && $Object.hasOwnProperty.call(m, "pollType")) {
                    d.pollType = o.enums === $String ? $root.proto.Message.PollType[m.pollType] === $undefined ? m.pollType : $root.proto.Message.PollType[m.pollType] : m.pollType;
                }
                if (m.correctAnswer != null && $Object.hasOwnProperty.call(m, "correctAnswer")) {
                    d.correctAnswer = $root.proto.Message.PollCreationMessage.Option.toObject(m.correctAnswer, o, q + 1);
                }
                if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.endTime = typeof m.endTime === "number" ? $BigInt(m.endTime) : $util.Long.fromBits(m.endTime.low >>> 0, m.endTime.high >>> 0, false).toBigInt();
                    else if (typeof m.endTime === "number")
                        d.endTime = o.longs === $String ? $String(m.endTime) : m.endTime;
                    else
                        d.endTime = o.longs === $String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === $Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                }
                if (m.hideParticipantName != null && $Object.hasOwnProperty.call(m, "hideParticipantName")) {
                    d.hideParticipantName = m.hideParticipantName;
                }
                if (m.allowAddOption != null && $Object.hasOwnProperty.call(m, "allowAddOption")) {
                    d.allowAddOption = m.allowAddOption;
                }
                return d;
            };

            PollCreationMessage.prototype.toJSON = function() {
                return PollCreationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollCreationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollCreationMessage";
            };

            PollCreationMessage.Option = (function() {

                const Option = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Option.prototype.optionName = null;
                Option.prototype.optionHash = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Option.prototype, "_optionName", {
                    get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Option.prototype, "_optionHash", {
                    get: $util.oneOfGetter($oneOfFields = ["optionHash"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Option.create = function(properties) {
                    return new Option(properties);
                };

                Option.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.optionName != null && $Object.hasOwnProperty.call(m, "optionName"))
                        w.uint32(10).string(m.optionName);
                    if (m.optionHash != null && $Object.hasOwnProperty.call(m, "optionHash"))
                        w.uint32(18).string(m.optionHash);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Option.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollCreationMessage.Option();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.optionName = r.stringVerify();
                                m._optionName = "optionName";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.optionHash = r.stringVerify();
                                m._optionHash = "optionHash";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Option.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PollCreationMessage.Option)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PollCreationMessage.Option: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PollCreationMessage.Option();
                    if (d.optionName != null) {
                        m.optionName = $String(d.optionName);
                    }
                    if (d.optionHash != null) {
                        m.optionHash = $String(d.optionHash);
                    }
                    return m;
                };

                Option.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.optionName != null && $Object.hasOwnProperty.call(m, "optionName")) {
                        d.optionName = m.optionName;
                    }
                    if (m.optionHash != null && $Object.hasOwnProperty.call(m, "optionHash")) {
                        d.optionHash = m.optionHash;
                    }
                    return d;
                };

                Option.prototype.toJSON = function() {
                    return Option.toObject(this, $protobuf.util.toJSONOptions);
                };

                Option.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PollCreationMessage.Option";
                };

                return Option;
            })();

            return PollCreationMessage;
        })();

        Message.PollEncValue = (function() {

            const PollEncValue = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollEncValue.prototype.encPayload = null;
            PollEncValue.prototype.encIv = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollEncValue.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollEncValue.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollEncValue.create = function(properties) {
                return new PollEncValue(properties);
            };

            PollEncValue.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(10).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(18).bytes(m.encIv);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollEncValue.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollEncValue();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollEncValue.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollEncValue)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollEncValue: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollEncValue();
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                return m;
            };

            PollEncValue.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                return d;
            };

            PollEncValue.prototype.toJSON = function() {
                return PollEncValue.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollEncValue.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollEncValue";
            };

            return PollEncValue;
        })();

        Message.PollResultSnapshotMessage = (function() {

            const PollResultSnapshotMessage = function (p) {
                this.pollVotes = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollResultSnapshotMessage.prototype.name = null;
            PollResultSnapshotMessage.prototype.pollVotes = $util.emptyArray;
            PollResultSnapshotMessage.prototype.contextInfo = null;
            PollResultSnapshotMessage.prototype.pollType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollResultSnapshotMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollResultSnapshotMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollResultSnapshotMessage.prototype, "_pollType", {
                get: $util.oneOfGetter($oneOfFields = ["pollType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollResultSnapshotMessage.create = function(properties) {
                return new PollResultSnapshotMessage(properties);
            };

            PollResultSnapshotMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.pollVotes != null && m.pollVotes.length) {
                    for (var i = 0; i < m.pollVotes.length; ++i)
                        $root.proto.Message.PollResultSnapshotMessage.PollVote.encode(m.pollVotes[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(26).fork(), q + 1).ldelim();
                if (m.pollType != null && $Object.hasOwnProperty.call(m, "pollType"))
                    w.uint32(32).int32(m.pollType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollResultSnapshotMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollResultSnapshotMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.pollVotes && m.pollVotes.length))
                                m.pollVotes = [];
                            m.pollVotes.push($root.proto.Message.PollResultSnapshotMessage.PollVote.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.pollType = r.int32();
                            m._pollType = "pollType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollResultSnapshotMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollResultSnapshotMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollResultSnapshotMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollResultSnapshotMessage();
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.pollVotes) {
                    if (!$Array.isArray(d.pollVotes))
                        throw $TypeError(".proto.Message.PollResultSnapshotMessage.pollVotes: array expected");
                    m.pollVotes = $Array(d.pollVotes.length);
                    for (var i = 0; i < d.pollVotes.length; ++i) {
                        if (!$util.isObject(d.pollVotes[i]))
                            throw $TypeError(".proto.Message.PollResultSnapshotMessage.pollVotes: object expected");
                        m.pollVotes[i] = $root.proto.Message.PollResultSnapshotMessage.PollVote.fromObject(d.pollVotes[i], q + 1);
                    }
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.PollResultSnapshotMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                switch (d.pollType) {
                case "POLL":
                case 0:
                    m.pollType = 0;
                    break;
                case "QUIZ":
                case 1:
                    m.pollType = 1;
                    break;
                default:
                    if (typeof d.pollType === "number" && (d.pollType | 0) === d.pollType)
                        m.pollType = d.pollType;
                }
                return m;
            };

            PollResultSnapshotMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.pollVotes = [];
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.pollVotes && m.pollVotes.length) {
                    d.pollVotes = $Array(m.pollVotes.length);
                    for (var j = 0; j < m.pollVotes.length; ++j) {
                        d.pollVotes[j] = $root.proto.Message.PollResultSnapshotMessage.PollVote.toObject(m.pollVotes[j], o, q + 1);
                    }
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.pollType != null && $Object.hasOwnProperty.call(m, "pollType")) {
                    d.pollType = o.enums === $String ? $root.proto.Message.PollType[m.pollType] === $undefined ? m.pollType : $root.proto.Message.PollType[m.pollType] : m.pollType;
                }
                return d;
            };

            PollResultSnapshotMessage.prototype.toJSON = function() {
                return PollResultSnapshotMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollResultSnapshotMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollResultSnapshotMessage";
            };

            PollResultSnapshotMessage.PollVote = (function() {

                const PollVote = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PollVote.prototype.optionName = null;
                PollVote.prototype.optionVoteCount = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PollVote.prototype, "_optionName", {
                    get: $util.oneOfGetter($oneOfFields = ["optionName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PollVote.prototype, "_optionVoteCount", {
                    get: $util.oneOfGetter($oneOfFields = ["optionVoteCount"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PollVote.create = function(properties) {
                    return new PollVote(properties);
                };

                PollVote.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.optionName != null && $Object.hasOwnProperty.call(m, "optionName"))
                        w.uint32(10).string(m.optionName);
                    if (m.optionVoteCount != null && $Object.hasOwnProperty.call(m, "optionVoteCount"))
                        w.uint32(16).int64(m.optionVoteCount);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PollVote.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollResultSnapshotMessage.PollVote();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.optionName = r.stringVerify();
                                m._optionName = "optionName";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.optionVoteCount = r.int64();
                                m._optionVoteCount = "optionVoteCount";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PollVote.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.PollResultSnapshotMessage.PollVote)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.PollResultSnapshotMessage.PollVote: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.PollResultSnapshotMessage.PollVote();
                    if (d.optionName != null) {
                        m.optionName = $String(d.optionName);
                    }
                    if (d.optionVoteCount != null) {
                        if ($util.Long)
                            m.optionVoteCount = $util.Long.fromValue(d.optionVoteCount, false);
                        else if (typeof d.optionVoteCount === "string")
                            m.optionVoteCount = $parseInt(d.optionVoteCount, 10);
                        else if (typeof d.optionVoteCount === "number")
                            m.optionVoteCount = d.optionVoteCount;
                        else if (typeof d.optionVoteCount === "object")
                            m.optionVoteCount = new $util.LongBits(d.optionVoteCount.low >>> 0, d.optionVoteCount.high >>> 0).toNumber();
                    }
                    return m;
                };

                PollVote.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.optionName != null && $Object.hasOwnProperty.call(m, "optionName")) {
                        d.optionName = m.optionName;
                    }
                    if (m.optionVoteCount != null && $Object.hasOwnProperty.call(m, "optionVoteCount")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.optionVoteCount = typeof m.optionVoteCount === "number" ? $BigInt(m.optionVoteCount) : $util.Long.fromBits(m.optionVoteCount.low >>> 0, m.optionVoteCount.high >>> 0, false).toBigInt();
                        else if (typeof m.optionVoteCount === "number")
                            d.optionVoteCount = o.longs === $String ? $String(m.optionVoteCount) : m.optionVoteCount;
                        else
                            d.optionVoteCount = o.longs === $String ? $util.Long.prototype.toString.call(m.optionVoteCount) : o.longs === $Number ? new $util.LongBits(m.optionVoteCount.low >>> 0, m.optionVoteCount.high >>> 0).toNumber() : m.optionVoteCount;
                    }
                    return d;
                };

                PollVote.prototype.toJSON = function() {
                    return PollVote.toObject(this, $protobuf.util.toJSONOptions);
                };

                PollVote.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.PollResultSnapshotMessage.PollVote";
                };

                return PollVote;
            })();

            return PollResultSnapshotMessage;
        })();

        Message.PollType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "POLL"] = 0;
            values[valuesById[1] = "QUIZ"] = 1;
            return values;
        })();

        Message.PollUpdateMessage = (function() {

            const PollUpdateMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollUpdateMessage.prototype.pollCreationMessageKey = null;
            PollUpdateMessage.prototype.vote = null;
            PollUpdateMessage.prototype.metadata = null;
            PollUpdateMessage.prototype.senderTimestampMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_pollCreationMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["pollCreationMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_vote", {
                get: $util.oneOfGetter($oneOfFields = ["vote"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollUpdateMessage.create = function(properties) {
                return new PollUpdateMessage(properties);
            };

            PollUpdateMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pollCreationMessageKey != null && $Object.hasOwnProperty.call(m, "pollCreationMessageKey"))
                    $root.proto.MessageKey.encode(m.pollCreationMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.vote != null && $Object.hasOwnProperty.call(m, "vote"))
                    $root.proto.Message.PollEncValue.encode(m.vote, w.uint32(18).fork(), q + 1).ldelim();
                if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata"))
                    $root.proto.Message.PollUpdateMessageMetadata.encode(m.metadata, w.uint32(26).fork(), q + 1).ldelim();
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                    w.uint32(32).int64(m.senderTimestampMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollUpdateMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollUpdateMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.pollCreationMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.pollCreationMessageKey);
                            m._pollCreationMessageKey = "pollCreationMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.vote = $root.proto.Message.PollEncValue.decode(r, r.uint32(), $undefined, q + 1, m.vote);
                            m._vote = "vote";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.metadata = $root.proto.Message.PollUpdateMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.metadata);
                            m._metadata = "metadata";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.senderTimestampMs = r.int64();
                            m._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollUpdateMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollUpdateMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollUpdateMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollUpdateMessage();
                if (d.pollCreationMessageKey != null) {
                    if (!$util.isObject(d.pollCreationMessageKey))
                        throw $TypeError(".proto.Message.PollUpdateMessage.pollCreationMessageKey: object expected");
                    m.pollCreationMessageKey = $root.proto.MessageKey.fromObject(d.pollCreationMessageKey, q + 1);
                }
                if (d.vote != null) {
                    if (!$util.isObject(d.vote))
                        throw $TypeError(".proto.Message.PollUpdateMessage.vote: object expected");
                    m.vote = $root.proto.Message.PollEncValue.fromObject(d.vote, q + 1);
                }
                if (d.metadata != null) {
                    if (!$util.isObject(d.metadata))
                        throw $TypeError(".proto.Message.PollUpdateMessage.metadata: object expected");
                    m.metadata = $root.proto.Message.PollUpdateMessageMetadata.fromObject(d.metadata, q + 1);
                }
                if (d.senderTimestampMs != null) {
                    if ($util.Long)
                        m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                    else if (typeof d.senderTimestampMs === "string")
                        m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                    else if (typeof d.senderTimestampMs === "number")
                        m.senderTimestampMs = d.senderTimestampMs;
                    else if (typeof d.senderTimestampMs === "object")
                        m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
                }
                return m;
            };

            PollUpdateMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pollCreationMessageKey != null && $Object.hasOwnProperty.call(m, "pollCreationMessageKey")) {
                    d.pollCreationMessageKey = $root.proto.MessageKey.toObject(m.pollCreationMessageKey, o, q + 1);
                }
                if (m.vote != null && $Object.hasOwnProperty.call(m, "vote")) {
                    d.vote = $root.proto.Message.PollEncValue.toObject(m.vote, o, q + 1);
                }
                if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata")) {
                    d.metadata = $root.proto.Message.PollUpdateMessageMetadata.toObject(m.metadata, o, q + 1);
                }
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.senderTimestampMs === "number")
                        d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                    else
                        d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
                }
                return d;
            };

            PollUpdateMessage.prototype.toJSON = function() {
                return PollUpdateMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollUpdateMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollUpdateMessage";
            };

            return PollUpdateMessage;
        })();

        Message.PollUpdateMessageMetadata = (function() {

            const PollUpdateMessageMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollUpdateMessageMetadata.prototype.pollNameHash = null;
            PollUpdateMessageMetadata.prototype.lastEditStanzaId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessageMetadata.prototype, "_pollNameHash", {
                get: $util.oneOfGetter($oneOfFields = ["pollNameHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollUpdateMessageMetadata.prototype, "_lastEditStanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["lastEditStanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollUpdateMessageMetadata.create = function(properties) {
                return new PollUpdateMessageMetadata(properties);
            };

            PollUpdateMessageMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pollNameHash != null && $Object.hasOwnProperty.call(m, "pollNameHash"))
                    w.uint32(10).bytes(m.pollNameHash);
                if (m.lastEditStanzaId != null && $Object.hasOwnProperty.call(m, "lastEditStanzaId"))
                    w.uint32(18).string(m.lastEditStanzaId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollUpdateMessageMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollUpdateMessageMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.pollNameHash = r.bytes();
                            m._pollNameHash = "pollNameHash";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.lastEditStanzaId = r.stringVerify();
                            m._lastEditStanzaId = "lastEditStanzaId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollUpdateMessageMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollUpdateMessageMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollUpdateMessageMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollUpdateMessageMetadata();
                if (d.pollNameHash != null) {
                    if (typeof d.pollNameHash === "string")
                        $util.base64.decode(d.pollNameHash, m.pollNameHash = $util.newBuffer($util.base64.length(d.pollNameHash)), 0);
                    else if (d.pollNameHash.length >= 0)
                        m.pollNameHash = d.pollNameHash;
                }
                if (d.lastEditStanzaId != null) {
                    m.lastEditStanzaId = $String(d.lastEditStanzaId);
                }
                return m;
            };

            PollUpdateMessageMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pollNameHash != null && $Object.hasOwnProperty.call(m, "pollNameHash")) {
                    d.pollNameHash = o.bytes === $String ? $util.base64.encode(m.pollNameHash, 0, m.pollNameHash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.pollNameHash) : m.pollNameHash;
                }
                if (m.lastEditStanzaId != null && $Object.hasOwnProperty.call(m, "lastEditStanzaId")) {
                    d.lastEditStanzaId = m.lastEditStanzaId;
                }
                return d;
            };

            PollUpdateMessageMetadata.prototype.toJSON = function() {
                return PollUpdateMessageMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollUpdateMessageMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollUpdateMessageMetadata";
            };

            return PollUpdateMessageMetadata;
        })();

        Message.PollVoteMessage = (function() {

            const PollVoteMessage = function (p) {
                this.selectedOptions = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollVoteMessage.prototype.selectedOptions = $util.emptyArray;

            PollVoteMessage.create = function(properties) {
                return new PollVoteMessage(properties);
            };

            PollVoteMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.selectedOptions != null && m.selectedOptions.length) {
                    for (var i = 0; i < m.selectedOptions.length; ++i)
                        w.uint32(10).bytes(m.selectedOptions[i]);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollVoteMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.PollVoteMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.selectedOptions && m.selectedOptions.length))
                                m.selectedOptions = [];
                            m.selectedOptions.push(r.bytes());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollVoteMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.PollVoteMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.PollVoteMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.PollVoteMessage();
                if (d.selectedOptions) {
                    if (!$Array.isArray(d.selectedOptions))
                        throw $TypeError(".proto.Message.PollVoteMessage.selectedOptions: array expected");
                    m.selectedOptions = $Array(d.selectedOptions.length);
                    for (var i = 0; i < d.selectedOptions.length; ++i) {
                        if (typeof d.selectedOptions[i] === "string")
                            $util.base64.decode(d.selectedOptions[i], m.selectedOptions[i] = $util.newBuffer($util.base64.length(d.selectedOptions[i])), 0);
                        else if (d.selectedOptions[i].length >= 0)
                            m.selectedOptions[i] = d.selectedOptions[i];
                    }
                }
                return m;
            };

            PollVoteMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.selectedOptions = [];
                }
                if (m.selectedOptions && m.selectedOptions.length) {
                    d.selectedOptions = $Array(m.selectedOptions.length);
                    for (var j = 0; j < m.selectedOptions.length; ++j) {
                        d.selectedOptions[j] = o.bytes === $String ? $util.base64.encode(m.selectedOptions[j], 0, m.selectedOptions[j].length) : o.bytes === $Array ? $Array.prototype.slice.call(m.selectedOptions[j]) : m.selectedOptions[j];
                    }
                }
                return d;
            };

            PollVoteMessage.prototype.toJSON = function() {
                return PollVoteMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollVoteMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.PollVoteMessage";
            };

            return PollVoteMessage;
        })();

        Message.ProductMessage = (function() {

            const ProductMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ProductMessage.prototype.product = null;
            ProductMessage.prototype.businessOwnerJid = null;
            ProductMessage.prototype.catalog = null;
            ProductMessage.prototype.body = null;
            ProductMessage.prototype.footer = null;
            ProductMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProductMessage.prototype, "_product", {
                get: $util.oneOfGetter($oneOfFields = ["product"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProductMessage.prototype, "_businessOwnerJid", {
                get: $util.oneOfGetter($oneOfFields = ["businessOwnerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProductMessage.prototype, "_catalog", {
                get: $util.oneOfGetter($oneOfFields = ["catalog"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProductMessage.prototype, "_body", {
                get: $util.oneOfGetter($oneOfFields = ["body"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProductMessage.prototype, "_footer", {
                get: $util.oneOfGetter($oneOfFields = ["footer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProductMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProductMessage.create = function(properties) {
                return new ProductMessage(properties);
            };

            ProductMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.product != null && $Object.hasOwnProperty.call(m, "product"))
                    $root.proto.Message.ProductMessage.ProductSnapshot.encode(m.product, w.uint32(10).fork(), q + 1).ldelim();
                if (m.businessOwnerJid != null && $Object.hasOwnProperty.call(m, "businessOwnerJid"))
                    w.uint32(18).string(m.businessOwnerJid);
                if (m.catalog != null && $Object.hasOwnProperty.call(m, "catalog"))
                    $root.proto.Message.ProductMessage.CatalogSnapshot.encode(m.catalog, w.uint32(34).fork(), q + 1).ldelim();
                if (m.body != null && $Object.hasOwnProperty.call(m, "body"))
                    w.uint32(42).string(m.body);
                if (m.footer != null && $Object.hasOwnProperty.call(m, "footer"))
                    w.uint32(50).string(m.footer);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ProductMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ProductMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.product = $root.proto.Message.ProductMessage.ProductSnapshot.decode(r, r.uint32(), $undefined, q + 1, m.product);
                            m._product = "product";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.businessOwnerJid = r.stringVerify();
                            m._businessOwnerJid = "businessOwnerJid";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.catalog = $root.proto.Message.ProductMessage.CatalogSnapshot.decode(r, r.uint32(), $undefined, q + 1, m.catalog);
                            m._catalog = "catalog";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.body = r.stringVerify();
                            m._body = "body";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.footer = r.stringVerify();
                            m._footer = "footer";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ProductMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ProductMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ProductMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ProductMessage();
                if (d.product != null) {
                    if (!$util.isObject(d.product))
                        throw $TypeError(".proto.Message.ProductMessage.product: object expected");
                    m.product = $root.proto.Message.ProductMessage.ProductSnapshot.fromObject(d.product, q + 1);
                }
                if (d.businessOwnerJid != null) {
                    m.businessOwnerJid = $String(d.businessOwnerJid);
                }
                if (d.catalog != null) {
                    if (!$util.isObject(d.catalog))
                        throw $TypeError(".proto.Message.ProductMessage.catalog: object expected");
                    m.catalog = $root.proto.Message.ProductMessage.CatalogSnapshot.fromObject(d.catalog, q + 1);
                }
                if (d.body != null) {
                    m.body = $String(d.body);
                }
                if (d.footer != null) {
                    m.footer = $String(d.footer);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.ProductMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            ProductMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.product != null && $Object.hasOwnProperty.call(m, "product")) {
                    d.product = $root.proto.Message.ProductMessage.ProductSnapshot.toObject(m.product, o, q + 1);
                }
                if (m.businessOwnerJid != null && $Object.hasOwnProperty.call(m, "businessOwnerJid")) {
                    d.businessOwnerJid = m.businessOwnerJid;
                }
                if (m.catalog != null && $Object.hasOwnProperty.call(m, "catalog")) {
                    d.catalog = $root.proto.Message.ProductMessage.CatalogSnapshot.toObject(m.catalog, o, q + 1);
                }
                if (m.body != null && $Object.hasOwnProperty.call(m, "body")) {
                    d.body = m.body;
                }
                if (m.footer != null && $Object.hasOwnProperty.call(m, "footer")) {
                    d.footer = m.footer;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            ProductMessage.prototype.toJSON = function() {
                return ProductMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProductMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ProductMessage";
            };

            ProductMessage.CatalogSnapshot = (function() {

                const CatalogSnapshot = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CatalogSnapshot.prototype.catalogImage = null;
                CatalogSnapshot.prototype.title = null;
                CatalogSnapshot.prototype.description = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CatalogSnapshot.prototype, "_catalogImage", {
                    get: $util.oneOfGetter($oneOfFields = ["catalogImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CatalogSnapshot.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CatalogSnapshot.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CatalogSnapshot.create = function(properties) {
                    return new CatalogSnapshot(properties);
                };

                CatalogSnapshot.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.catalogImage != null && $Object.hasOwnProperty.call(m, "catalogImage"))
                        $root.proto.Message.ImageMessage.encode(m.catalogImage, w.uint32(10).fork(), q + 1).ldelim();
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(18).string(m.title);
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                        w.uint32(26).string(m.description);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CatalogSnapshot.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ProductMessage.CatalogSnapshot();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.catalogImage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.catalogImage);
                                m._catalogImage = "catalogImage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.description = r.stringVerify();
                                m._description = "description";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CatalogSnapshot.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ProductMessage.CatalogSnapshot)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ProductMessage.CatalogSnapshot: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ProductMessage.CatalogSnapshot();
                    if (d.catalogImage != null) {
                        if (!$util.isObject(d.catalogImage))
                            throw $TypeError(".proto.Message.ProductMessage.CatalogSnapshot.catalogImage: object expected");
                        m.catalogImage = $root.proto.Message.ImageMessage.fromObject(d.catalogImage, q + 1);
                    }
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.description != null) {
                        m.description = $String(d.description);
                    }
                    return m;
                };

                CatalogSnapshot.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.catalogImage != null && $Object.hasOwnProperty.call(m, "catalogImage")) {
                        d.catalogImage = $root.proto.Message.ImageMessage.toObject(m.catalogImage, o, q + 1);
                    }
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                        d.description = m.description;
                    }
                    return d;
                };

                CatalogSnapshot.prototype.toJSON = function() {
                    return CatalogSnapshot.toObject(this, $protobuf.util.toJSONOptions);
                };

                CatalogSnapshot.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ProductMessage.CatalogSnapshot";
                };

                return CatalogSnapshot;
            })();

            ProductMessage.ProductSnapshot = (function() {

                const ProductSnapshot = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                ProductSnapshot.prototype.productImage = null;
                ProductSnapshot.prototype.productId = null;
                ProductSnapshot.prototype.title = null;
                ProductSnapshot.prototype.description = null;
                ProductSnapshot.prototype.currencyCode = null;
                ProductSnapshot.prototype.priceAmount1000 = null;
                ProductSnapshot.prototype.retailerId = null;
                ProductSnapshot.prototype.url = null;
                ProductSnapshot.prototype.productImageCount = null;
                ProductSnapshot.prototype.firstImageId = null;
                ProductSnapshot.prototype.salePriceAmount1000 = null;
                ProductSnapshot.prototype.signedUrl = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_productImage", {
                    get: $util.oneOfGetter($oneOfFields = ["productImage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_productId", {
                    get: $util.oneOfGetter($oneOfFields = ["productId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_title", {
                    get: $util.oneOfGetter($oneOfFields = ["title"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_description", {
                    get: $util.oneOfGetter($oneOfFields = ["description"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_currencyCode", {
                    get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_priceAmount1000", {
                    get: $util.oneOfGetter($oneOfFields = ["priceAmount1000"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_retailerId", {
                    get: $util.oneOfGetter($oneOfFields = ["retailerId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_url", {
                    get: $util.oneOfGetter($oneOfFields = ["url"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_productImageCount", {
                    get: $util.oneOfGetter($oneOfFields = ["productImageCount"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_firstImageId", {
                    get: $util.oneOfGetter($oneOfFields = ["firstImageId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_salePriceAmount1000", {
                    get: $util.oneOfGetter($oneOfFields = ["salePriceAmount1000"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ProductSnapshot.prototype, "_signedUrl", {
                    get: $util.oneOfGetter($oneOfFields = ["signedUrl"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ProductSnapshot.create = function(properties) {
                    return new ProductSnapshot(properties);
                };

                ProductSnapshot.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.productImage != null && $Object.hasOwnProperty.call(m, "productImage"))
                        $root.proto.Message.ImageMessage.encode(m.productImage, w.uint32(10).fork(), q + 1).ldelim();
                    if (m.productId != null && $Object.hasOwnProperty.call(m, "productId"))
                        w.uint32(18).string(m.productId);
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                        w.uint32(26).string(m.title);
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                        w.uint32(34).string(m.description);
                    if (m.currencyCode != null && $Object.hasOwnProperty.call(m, "currencyCode"))
                        w.uint32(42).string(m.currencyCode);
                    if (m.priceAmount1000 != null && $Object.hasOwnProperty.call(m, "priceAmount1000"))
                        w.uint32(48).int64(m.priceAmount1000);
                    if (m.retailerId != null && $Object.hasOwnProperty.call(m, "retailerId"))
                        w.uint32(58).string(m.retailerId);
                    if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                        w.uint32(66).string(m.url);
                    if (m.productImageCount != null && $Object.hasOwnProperty.call(m, "productImageCount"))
                        w.uint32(72).uint32(m.productImageCount);
                    if (m.firstImageId != null && $Object.hasOwnProperty.call(m, "firstImageId"))
                        w.uint32(90).string(m.firstImageId);
                    if (m.salePriceAmount1000 != null && $Object.hasOwnProperty.call(m, "salePriceAmount1000"))
                        w.uint32(96).int64(m.salePriceAmount1000);
                    if (m.signedUrl != null && $Object.hasOwnProperty.call(m, "signedUrl"))
                        w.uint32(106).string(m.signedUrl);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                ProductSnapshot.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ProductMessage.ProductSnapshot();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.productImage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.productImage);
                                m._productImage = "productImage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.productId = r.stringVerify();
                                m._productId = "productId";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.title = r.stringVerify();
                                m._title = "title";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.description = r.stringVerify();
                                m._description = "description";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.currencyCode = r.stringVerify();
                                m._currencyCode = "currencyCode";
                                continue;
                            }
                        case 6: {
                                if (u !== 0)
                                    break;
                                m.priceAmount1000 = r.int64();
                                m._priceAmount1000 = "priceAmount1000";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.retailerId = r.stringVerify();
                                m._retailerId = "retailerId";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                m.url = r.stringVerify();
                                m._url = "url";
                                continue;
                            }
                        case 9: {
                                if (u !== 0)
                                    break;
                                m.productImageCount = r.uint32();
                                m._productImageCount = "productImageCount";
                                continue;
                            }
                        case 11: {
                                if (u !== 2)
                                    break;
                                m.firstImageId = r.stringVerify();
                                m._firstImageId = "firstImageId";
                                continue;
                            }
                        case 12: {
                                if (u !== 0)
                                    break;
                                m.salePriceAmount1000 = r.int64();
                                m._salePriceAmount1000 = "salePriceAmount1000";
                                continue;
                            }
                        case 13: {
                                if (u !== 2)
                                    break;
                                m.signedUrl = r.stringVerify();
                                m._signedUrl = "signedUrl";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                ProductSnapshot.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.ProductMessage.ProductSnapshot)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.ProductMessage.ProductSnapshot: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.ProductMessage.ProductSnapshot();
                    if (d.productImage != null) {
                        if (!$util.isObject(d.productImage))
                            throw $TypeError(".proto.Message.ProductMessage.ProductSnapshot.productImage: object expected");
                        m.productImage = $root.proto.Message.ImageMessage.fromObject(d.productImage, q + 1);
                    }
                    if (d.productId != null) {
                        m.productId = $String(d.productId);
                    }
                    if (d.title != null) {
                        m.title = $String(d.title);
                    }
                    if (d.description != null) {
                        m.description = $String(d.description);
                    }
                    if (d.currencyCode != null) {
                        m.currencyCode = $String(d.currencyCode);
                    }
                    if (d.priceAmount1000 != null) {
                        if ($util.Long)
                            m.priceAmount1000 = $util.Long.fromValue(d.priceAmount1000, false);
                        else if (typeof d.priceAmount1000 === "string")
                            m.priceAmount1000 = $parseInt(d.priceAmount1000, 10);
                        else if (typeof d.priceAmount1000 === "number")
                            m.priceAmount1000 = d.priceAmount1000;
                        else if (typeof d.priceAmount1000 === "object")
                            m.priceAmount1000 = new $util.LongBits(d.priceAmount1000.low >>> 0, d.priceAmount1000.high >>> 0).toNumber();
                    }
                    if (d.retailerId != null) {
                        m.retailerId = $String(d.retailerId);
                    }
                    if (d.url != null) {
                        m.url = $String(d.url);
                    }
                    if (d.productImageCount != null) {
                        m.productImageCount = d.productImageCount >>> 0;
                    }
                    if (d.firstImageId != null) {
                        m.firstImageId = $String(d.firstImageId);
                    }
                    if (d.salePriceAmount1000 != null) {
                        if ($util.Long)
                            m.salePriceAmount1000 = $util.Long.fromValue(d.salePriceAmount1000, false);
                        else if (typeof d.salePriceAmount1000 === "string")
                            m.salePriceAmount1000 = $parseInt(d.salePriceAmount1000, 10);
                        else if (typeof d.salePriceAmount1000 === "number")
                            m.salePriceAmount1000 = d.salePriceAmount1000;
                        else if (typeof d.salePriceAmount1000 === "object")
                            m.salePriceAmount1000 = new $util.LongBits(d.salePriceAmount1000.low >>> 0, d.salePriceAmount1000.high >>> 0).toNumber();
                    }
                    if (d.signedUrl != null) {
                        m.signedUrl = $String(d.signedUrl);
                    }
                    return m;
                };

                ProductSnapshot.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.productImage != null && $Object.hasOwnProperty.call(m, "productImage")) {
                        d.productImage = $root.proto.Message.ImageMessage.toObject(m.productImage, o, q + 1);
                    }
                    if (m.productId != null && $Object.hasOwnProperty.call(m, "productId")) {
                        d.productId = m.productId;
                    }
                    if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                        d.title = m.title;
                    }
                    if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                        d.description = m.description;
                    }
                    if (m.currencyCode != null && $Object.hasOwnProperty.call(m, "currencyCode")) {
                        d.currencyCode = m.currencyCode;
                    }
                    if (m.priceAmount1000 != null && $Object.hasOwnProperty.call(m, "priceAmount1000")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.priceAmount1000 = typeof m.priceAmount1000 === "number" ? $BigInt(m.priceAmount1000) : $util.Long.fromBits(m.priceAmount1000.low >>> 0, m.priceAmount1000.high >>> 0, false).toBigInt();
                        else if (typeof m.priceAmount1000 === "number")
                            d.priceAmount1000 = o.longs === $String ? $String(m.priceAmount1000) : m.priceAmount1000;
                        else
                            d.priceAmount1000 = o.longs === $String ? $util.Long.prototype.toString.call(m.priceAmount1000) : o.longs === $Number ? new $util.LongBits(m.priceAmount1000.low >>> 0, m.priceAmount1000.high >>> 0).toNumber() : m.priceAmount1000;
                    }
                    if (m.retailerId != null && $Object.hasOwnProperty.call(m, "retailerId")) {
                        d.retailerId = m.retailerId;
                    }
                    if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                        d.url = m.url;
                    }
                    if (m.productImageCount != null && $Object.hasOwnProperty.call(m, "productImageCount")) {
                        d.productImageCount = m.productImageCount;
                    }
                    if (m.firstImageId != null && $Object.hasOwnProperty.call(m, "firstImageId")) {
                        d.firstImageId = m.firstImageId;
                    }
                    if (m.salePriceAmount1000 != null && $Object.hasOwnProperty.call(m, "salePriceAmount1000")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.salePriceAmount1000 = typeof m.salePriceAmount1000 === "number" ? $BigInt(m.salePriceAmount1000) : $util.Long.fromBits(m.salePriceAmount1000.low >>> 0, m.salePriceAmount1000.high >>> 0, false).toBigInt();
                        else if (typeof m.salePriceAmount1000 === "number")
                            d.salePriceAmount1000 = o.longs === $String ? $String(m.salePriceAmount1000) : m.salePriceAmount1000;
                        else
                            d.salePriceAmount1000 = o.longs === $String ? $util.Long.prototype.toString.call(m.salePriceAmount1000) : o.longs === $Number ? new $util.LongBits(m.salePriceAmount1000.low >>> 0, m.salePriceAmount1000.high >>> 0).toNumber() : m.salePriceAmount1000;
                    }
                    if (m.signedUrl != null && $Object.hasOwnProperty.call(m, "signedUrl")) {
                        d.signedUrl = m.signedUrl;
                    }
                    return d;
                };

                ProductSnapshot.prototype.toJSON = function() {
                    return ProductSnapshot.toObject(this, $protobuf.util.toJSONOptions);
                };

                ProductSnapshot.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.ProductMessage.ProductSnapshot";
                };

                return ProductSnapshot;
            })();

            return ProductMessage;
        })();

        Message.ProtocolMessage = (function() {

            const ProtocolMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ProtocolMessage.prototype.key = null;
            ProtocolMessage.prototype.type = null;
            ProtocolMessage.prototype.ephemeralExpiration = null;
            ProtocolMessage.prototype.ephemeralSettingTimestamp = null;
            ProtocolMessage.prototype.historySyncNotification = null;
            ProtocolMessage.prototype.appStateSyncKeyShare = null;
            ProtocolMessage.prototype.appStateSyncKeyRequest = null;
            ProtocolMessage.prototype.initialSecurityNotificationSettingSync = null;
            ProtocolMessage.prototype.appStateFatalExceptionNotification = null;
            ProtocolMessage.prototype.disappearingMode = null;
            ProtocolMessage.prototype.editedMessage = null;
            ProtocolMessage.prototype.timestampMs = null;
            ProtocolMessage.prototype.peerDataOperationRequestMessage = null;
            ProtocolMessage.prototype.peerDataOperationRequestResponseMessage = null;
            ProtocolMessage.prototype.botFeedbackMessage = null;
            ProtocolMessage.prototype.invokerJid = null;
            ProtocolMessage.prototype.requestWelcomeMessageMetadata = null;
            ProtocolMessage.prototype.mediaNotifyMessage = null;
            ProtocolMessage.prototype.cloudApiThreadControlNotification = null;
            ProtocolMessage.prototype.lidMigrationMappingSyncMessage = null;
            ProtocolMessage.prototype.limitSharing = null;
            ProtocolMessage.prototype.aiPsiMetadata = null;
            ProtocolMessage.prototype.aiQueryFanout = null;
            ProtocolMessage.prototype.memberLabel = null;
            ProtocolMessage.prototype.aiMediaCollectionMessage = null;
            ProtocolMessage.prototype.afterReadDuration = null;
            ProtocolMessage.prototype.chatThemeSetting = null;
            ProtocolMessage.prototype.aiMetadataOperation = null;
            ProtocolMessage.prototype.markAsVerifiedAction = null;
            ProtocolMessage.prototype.coexStateSync = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_ephemeralExpiration", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeralExpiration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_ephemeralSettingTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["ephemeralSettingTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_historySyncNotification", {
                get: $util.oneOfGetter($oneOfFields = ["historySyncNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_appStateSyncKeyShare", {
                get: $util.oneOfGetter($oneOfFields = ["appStateSyncKeyShare"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_appStateSyncKeyRequest", {
                get: $util.oneOfGetter($oneOfFields = ["appStateSyncKeyRequest"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_initialSecurityNotificationSettingSync", {
                get: $util.oneOfGetter($oneOfFields = ["initialSecurityNotificationSettingSync"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_appStateFatalExceptionNotification", {
                get: $util.oneOfGetter($oneOfFields = ["appStateFatalExceptionNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_disappearingMode", {
                get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_editedMessage", {
                get: $util.oneOfGetter($oneOfFields = ["editedMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_timestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["timestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_peerDataOperationRequestMessage", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_peerDataOperationRequestResponseMessage", {
                get: $util.oneOfGetter($oneOfFields = ["peerDataOperationRequestResponseMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_botFeedbackMessage", {
                get: $util.oneOfGetter($oneOfFields = ["botFeedbackMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_invokerJid", {
                get: $util.oneOfGetter($oneOfFields = ["invokerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_requestWelcomeMessageMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["requestWelcomeMessageMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_mediaNotifyMessage", {
                get: $util.oneOfGetter($oneOfFields = ["mediaNotifyMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_cloudApiThreadControlNotification", {
                get: $util.oneOfGetter($oneOfFields = ["cloudApiThreadControlNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_lidMigrationMappingSyncMessage", {
                get: $util.oneOfGetter($oneOfFields = ["lidMigrationMappingSyncMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_limitSharing", {
                get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_aiPsiMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["aiPsiMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_aiQueryFanout", {
                get: $util.oneOfGetter($oneOfFields = ["aiQueryFanout"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_memberLabel", {
                get: $util.oneOfGetter($oneOfFields = ["memberLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_aiMediaCollectionMessage", {
                get: $util.oneOfGetter($oneOfFields = ["aiMediaCollectionMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_afterReadDuration", {
                get: $util.oneOfGetter($oneOfFields = ["afterReadDuration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_chatThemeSetting", {
                get: $util.oneOfGetter($oneOfFields = ["chatThemeSetting"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_aiMetadataOperation", {
                get: $util.oneOfGetter($oneOfFields = ["aiMetadataOperation"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_markAsVerifiedAction", {
                get: $util.oneOfGetter($oneOfFields = ["markAsVerifiedAction"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ProtocolMessage.prototype, "_coexStateSync", {
                get: $util.oneOfGetter($oneOfFields = ["coexStateSync"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ProtocolMessage.create = function(properties) {
                return new ProtocolMessage(properties);
            };

            ProtocolMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(16).int32(m.type);
                if (m.ephemeralExpiration != null && $Object.hasOwnProperty.call(m, "ephemeralExpiration"))
                    w.uint32(32).uint32(m.ephemeralExpiration);
                if (m.ephemeralSettingTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp"))
                    w.uint32(40).int64(m.ephemeralSettingTimestamp);
                if (m.historySyncNotification != null && $Object.hasOwnProperty.call(m, "historySyncNotification"))
                    $root.proto.Message.HistorySyncNotification.encode(m.historySyncNotification, w.uint32(50).fork(), q + 1).ldelim();
                if (m.appStateSyncKeyShare != null && $Object.hasOwnProperty.call(m, "appStateSyncKeyShare"))
                    $root.proto.Message.AppStateSyncKeyShare.encode(m.appStateSyncKeyShare, w.uint32(58).fork(), q + 1).ldelim();
                if (m.appStateSyncKeyRequest != null && $Object.hasOwnProperty.call(m, "appStateSyncKeyRequest"))
                    $root.proto.Message.AppStateSyncKeyRequest.encode(m.appStateSyncKeyRequest, w.uint32(66).fork(), q + 1).ldelim();
                if (m.initialSecurityNotificationSettingSync != null && $Object.hasOwnProperty.call(m, "initialSecurityNotificationSettingSync"))
                    $root.proto.Message.InitialSecurityNotificationSettingSync.encode(m.initialSecurityNotificationSettingSync, w.uint32(74).fork(), q + 1).ldelim();
                if (m.appStateFatalExceptionNotification != null && $Object.hasOwnProperty.call(m, "appStateFatalExceptionNotification"))
                    $root.proto.Message.AppStateFatalExceptionNotification.encode(m.appStateFatalExceptionNotification, w.uint32(82).fork(), q + 1).ldelim();
                if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode"))
                    $root.proto.DisappearingMode.encode(m.disappearingMode, w.uint32(90).fork(), q + 1).ldelim();
                if (m.editedMessage != null && $Object.hasOwnProperty.call(m, "editedMessage"))
                    $root.proto.Message.encode(m.editedMessage, w.uint32(114).fork(), q + 1).ldelim();
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs"))
                    w.uint32(120).int64(m.timestampMs);
                if (m.peerDataOperationRequestMessage != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestMessage"))
                    $root.proto.Message.PeerDataOperationRequestMessage.encode(m.peerDataOperationRequestMessage, w.uint32(130).fork(), q + 1).ldelim();
                if (m.peerDataOperationRequestResponseMessage != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestResponseMessage"))
                    $root.proto.Message.PeerDataOperationRequestResponseMessage.encode(m.peerDataOperationRequestResponseMessage, w.uint32(138).fork(), q + 1).ldelim();
                if (m.botFeedbackMessage != null && $Object.hasOwnProperty.call(m, "botFeedbackMessage"))
                    $root.proto.BotFeedbackMessage.encode(m.botFeedbackMessage, w.uint32(146).fork(), q + 1).ldelim();
                if (m.invokerJid != null && $Object.hasOwnProperty.call(m, "invokerJid"))
                    w.uint32(154).string(m.invokerJid);
                if (m.requestWelcomeMessageMetadata != null && $Object.hasOwnProperty.call(m, "requestWelcomeMessageMetadata"))
                    $root.proto.Message.RequestWelcomeMessageMetadata.encode(m.requestWelcomeMessageMetadata, w.uint32(162).fork(), q + 1).ldelim();
                if (m.mediaNotifyMessage != null && $Object.hasOwnProperty.call(m, "mediaNotifyMessage"))
                    $root.proto.MediaNotifyMessage.encode(m.mediaNotifyMessage, w.uint32(170).fork(), q + 1).ldelim();
                if (m.cloudApiThreadControlNotification != null && $Object.hasOwnProperty.call(m, "cloudApiThreadControlNotification"))
                    $root.proto.Message.CloudAPIThreadControlNotification.encode(m.cloudApiThreadControlNotification, w.uint32(178).fork(), q + 1).ldelim();
                if (m.lidMigrationMappingSyncMessage != null && $Object.hasOwnProperty.call(m, "lidMigrationMappingSyncMessage"))
                    $root.proto.LIDMigrationMappingSyncMessage.encode(m.lidMigrationMappingSyncMessage, w.uint32(186).fork(), q + 1).ldelim();
                if (m.limitSharing != null && $Object.hasOwnProperty.call(m, "limitSharing"))
                    $root.proto.LimitSharing.encode(m.limitSharing, w.uint32(194).fork(), q + 1).ldelim();
                if (m.aiPsiMetadata != null && $Object.hasOwnProperty.call(m, "aiPsiMetadata"))
                    w.uint32(202).bytes(m.aiPsiMetadata);
                if (m.aiQueryFanout != null && $Object.hasOwnProperty.call(m, "aiQueryFanout"))
                    $root.proto.AIQueryFanout.encode(m.aiQueryFanout, w.uint32(210).fork(), q + 1).ldelim();
                if (m.memberLabel != null && $Object.hasOwnProperty.call(m, "memberLabel"))
                    $root.proto.MemberLabel.encode(m.memberLabel, w.uint32(218).fork(), q + 1).ldelim();
                if (m.aiMediaCollectionMessage != null && $Object.hasOwnProperty.call(m, "aiMediaCollectionMessage"))
                    $root.proto.AIMediaCollectionMessage.encode(m.aiMediaCollectionMessage, w.uint32(226).fork(), q + 1).ldelim();
                if (m.afterReadDuration != null && $Object.hasOwnProperty.call(m, "afterReadDuration"))
                    w.uint32(232).uint32(m.afterReadDuration);
                if (m.chatThemeSetting != null && $Object.hasOwnProperty.call(m, "chatThemeSetting"))
                    $root.proto.Message.ChatThemeSetting.encode(m.chatThemeSetting, w.uint32(242).fork(), q + 1).ldelim();
                if (m.aiMetadataOperation != null && $Object.hasOwnProperty.call(m, "aiMetadataOperation"))
                    $root.proto.AIMetadataOperation.encode(m.aiMetadataOperation, w.uint32(250).fork(), q + 1).ldelim();
                if (m.markAsVerifiedAction != null && $Object.hasOwnProperty.call(m, "markAsVerifiedAction"))
                    $root.proto.Message.MarkAsVerifiedAction.encode(m.markAsVerifiedAction, w.uint32(258).fork(), q + 1).ldelim();
                if (m.coexStateSync != null && $Object.hasOwnProperty.call(m, "coexStateSync"))
                    $root.proto.CoexStateSync.encode(m.coexStateSync, w.uint32(266).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ProtocolMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ProtocolMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.ephemeralExpiration = r.uint32();
                            m._ephemeralExpiration = "ephemeralExpiration";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.ephemeralSettingTimestamp = r.int64();
                            m._ephemeralSettingTimestamp = "ephemeralSettingTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.historySyncNotification = $root.proto.Message.HistorySyncNotification.decode(r, r.uint32(), $undefined, q + 1, m.historySyncNotification);
                            m._historySyncNotification = "historySyncNotification";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.appStateSyncKeyShare = $root.proto.Message.AppStateSyncKeyShare.decode(r, r.uint32(), $undefined, q + 1, m.appStateSyncKeyShare);
                            m._appStateSyncKeyShare = "appStateSyncKeyShare";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.appStateSyncKeyRequest = $root.proto.Message.AppStateSyncKeyRequest.decode(r, r.uint32(), $undefined, q + 1, m.appStateSyncKeyRequest);
                            m._appStateSyncKeyRequest = "appStateSyncKeyRequest";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.initialSecurityNotificationSettingSync = $root.proto.Message.InitialSecurityNotificationSettingSync.decode(r, r.uint32(), $undefined, q + 1, m.initialSecurityNotificationSettingSync);
                            m._initialSecurityNotificationSettingSync = "initialSecurityNotificationSettingSync";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.appStateFatalExceptionNotification = $root.proto.Message.AppStateFatalExceptionNotification.decode(r, r.uint32(), $undefined, q + 1, m.appStateFatalExceptionNotification);
                            m._appStateFatalExceptionNotification = "appStateFatalExceptionNotification";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.disappearingMode = $root.proto.DisappearingMode.decode(r, r.uint32(), $undefined, q + 1, m.disappearingMode);
                            m._disappearingMode = "disappearingMode";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.editedMessage = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.editedMessage);
                            m._editedMessage = "editedMessage";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            m.timestampMs = r.int64();
                            m._timestampMs = "timestampMs";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.peerDataOperationRequestMessage = $root.proto.Message.PeerDataOperationRequestMessage.decode(r, r.uint32(), $undefined, q + 1, m.peerDataOperationRequestMessage);
                            m._peerDataOperationRequestMessage = "peerDataOperationRequestMessage";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.peerDataOperationRequestResponseMessage = $root.proto.Message.PeerDataOperationRequestResponseMessage.decode(r, r.uint32(), $undefined, q + 1, m.peerDataOperationRequestResponseMessage);
                            m._peerDataOperationRequestResponseMessage = "peerDataOperationRequestResponseMessage";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            m.botFeedbackMessage = $root.proto.BotFeedbackMessage.decode(r, r.uint32(), $undefined, q + 1, m.botFeedbackMessage);
                            m._botFeedbackMessage = "botFeedbackMessage";
                            continue;
                        }
                    case 19: {
                            if (u !== 2)
                                break;
                            m.invokerJid = r.stringVerify();
                            m._invokerJid = "invokerJid";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            m.requestWelcomeMessageMetadata = $root.proto.Message.RequestWelcomeMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.requestWelcomeMessageMetadata);
                            m._requestWelcomeMessageMetadata = "requestWelcomeMessageMetadata";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            m.mediaNotifyMessage = $root.proto.MediaNotifyMessage.decode(r, r.uint32(), $undefined, q + 1, m.mediaNotifyMessage);
                            m._mediaNotifyMessage = "mediaNotifyMessage";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            m.cloudApiThreadControlNotification = $root.proto.Message.CloudAPIThreadControlNotification.decode(r, r.uint32(), $undefined, q + 1, m.cloudApiThreadControlNotification);
                            m._cloudApiThreadControlNotification = "cloudApiThreadControlNotification";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            m.lidMigrationMappingSyncMessage = $root.proto.LIDMigrationMappingSyncMessage.decode(r, r.uint32(), $undefined, q + 1, m.lidMigrationMappingSyncMessage);
                            m._lidMigrationMappingSyncMessage = "lidMigrationMappingSyncMessage";
                            continue;
                        }
                    case 24: {
                            if (u !== 2)
                                break;
                            m.limitSharing = $root.proto.LimitSharing.decode(r, r.uint32(), $undefined, q + 1, m.limitSharing);
                            m._limitSharing = "limitSharing";
                            continue;
                        }
                    case 25: {
                            if (u !== 2)
                                break;
                            m.aiPsiMetadata = r.bytes();
                            m._aiPsiMetadata = "aiPsiMetadata";
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            m.aiQueryFanout = $root.proto.AIQueryFanout.decode(r, r.uint32(), $undefined, q + 1, m.aiQueryFanout);
                            m._aiQueryFanout = "aiQueryFanout";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            m.memberLabel = $root.proto.MemberLabel.decode(r, r.uint32(), $undefined, q + 1, m.memberLabel);
                            m._memberLabel = "memberLabel";
                            continue;
                        }
                    case 28: {
                            if (u !== 2)
                                break;
                            m.aiMediaCollectionMessage = $root.proto.AIMediaCollectionMessage.decode(r, r.uint32(), $undefined, q + 1, m.aiMediaCollectionMessage);
                            m._aiMediaCollectionMessage = "aiMediaCollectionMessage";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            m.afterReadDuration = r.uint32();
                            m._afterReadDuration = "afterReadDuration";
                            continue;
                        }
                    case 30: {
                            if (u !== 2)
                                break;
                            m.chatThemeSetting = $root.proto.Message.ChatThemeSetting.decode(r, r.uint32(), $undefined, q + 1, m.chatThemeSetting);
                            m._chatThemeSetting = "chatThemeSetting";
                            continue;
                        }
                    case 31: {
                            if (u !== 2)
                                break;
                            m.aiMetadataOperation = $root.proto.AIMetadataOperation.decode(r, r.uint32(), $undefined, q + 1, m.aiMetadataOperation);
                            m._aiMetadataOperation = "aiMetadataOperation";
                            continue;
                        }
                    case 32: {
                            if (u !== 2)
                                break;
                            m.markAsVerifiedAction = $root.proto.Message.MarkAsVerifiedAction.decode(r, r.uint32(), $undefined, q + 1, m.markAsVerifiedAction);
                            m._markAsVerifiedAction = "markAsVerifiedAction";
                            continue;
                        }
                    case 33: {
                            if (u !== 2)
                                break;
                            m.coexStateSync = $root.proto.CoexStateSync.decode(r, r.uint32(), $undefined, q + 1, m.coexStateSync);
                            m._coexStateSync = "coexStateSync";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ProtocolMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ProtocolMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ProtocolMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ProtocolMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.ProtocolMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                switch (d.type) {
                case "REVOKE":
                case 0:
                    m.type = 0;
                    break;
                case "EPHEMERAL_SETTING":
                case 3:
                    m.type = 3;
                    break;
                case "EPHEMERAL_SYNC_RESPONSE":
                case 4:
                    m.type = 4;
                    break;
                case "HISTORY_SYNC_NOTIFICATION":
                case 5:
                    m.type = 5;
                    break;
                case "APP_STATE_SYNC_KEY_SHARE":
                case 6:
                    m.type = 6;
                    break;
                case "APP_STATE_SYNC_KEY_REQUEST":
                case 7:
                    m.type = 7;
                    break;
                case "MSG_FANOUT_BACKFILL_REQUEST":
                case 8:
                    m.type = 8;
                    break;
                case "INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC":
                case 9:
                    m.type = 9;
                    break;
                case "APP_STATE_FATAL_EXCEPTION_NOTIFICATION":
                case 10:
                    m.type = 10;
                    break;
                case "SHARE_PHONE_NUMBER":
                case 11:
                    m.type = 11;
                    break;
                case "MESSAGE_EDIT":
                case 14:
                    m.type = 14;
                    break;
                case "PEER_DATA_OPERATION_REQUEST_MESSAGE":
                case 16:
                    m.type = 16;
                    break;
                case "PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE":
                case 17:
                    m.type = 17;
                    break;
                case "REQUEST_WELCOME_MESSAGE":
                case 18:
                    m.type = 18;
                    break;
                case "BOT_FEEDBACK_MESSAGE":
                case 19:
                    m.type = 19;
                    break;
                case "MEDIA_NOTIFY_MESSAGE":
                case 20:
                    m.type = 20;
                    break;
                case "CLOUD_API_THREAD_CONTROL_NOTIFICATION":
                case 21:
                    m.type = 21;
                    break;
                case "LID_MIGRATION_MAPPING_SYNC":
                case 22:
                    m.type = 22;
                    break;
                case "REMINDER_MESSAGE":
                case 23:
                    m.type = 23;
                    break;
                case "BOT_MEMU_ONBOARDING_MESSAGE":
                case 24:
                    m.type = 24;
                    break;
                case "STATUS_MENTION_MESSAGE":
                case 25:
                    m.type = 25;
                    break;
                case "STOP_GENERATION_MESSAGE":
                case 26:
                    m.type = 26;
                    break;
                case "LIMIT_SHARING":
                case 27:
                    m.type = 27;
                    break;
                case "AI_PSI_METADATA":
                case 28:
                    m.type = 28;
                    break;
                case "AI_QUERY_FANOUT":
                case 29:
                    m.type = 29;
                    break;
                case "GROUP_MEMBER_LABEL_CHANGE":
                case 30:
                    m.type = 30;
                    break;
                case "AI_MEDIA_COLLECTION_MESSAGE":
                case 31:
                    m.type = 31;
                    break;
                case "MESSAGE_UNSCHEDULE":
                case 32:
                    m.type = 32;
                    break;
                case "CHAT_THEME_SETTING":
                case 34:
                    m.type = 34;
                    break;
                case "AI_METADATA_OPERATION":
                case 35:
                    m.type = 35;
                    break;
                case "MARK_AS_VERIFIED_ACTION":
                case 36:
                    m.type = 36;
                    break;
                case "COEX_STATE_SYNC":
                case 37:
                    m.type = 37;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.ephemeralExpiration != null) {
                    m.ephemeralExpiration = d.ephemeralExpiration >>> 0;
                }
                if (d.ephemeralSettingTimestamp != null) {
                    if ($util.Long)
                        m.ephemeralSettingTimestamp = $util.Long.fromValue(d.ephemeralSettingTimestamp, false);
                    else if (typeof d.ephemeralSettingTimestamp === "string")
                        m.ephemeralSettingTimestamp = $parseInt(d.ephemeralSettingTimestamp, 10);
                    else if (typeof d.ephemeralSettingTimestamp === "number")
                        m.ephemeralSettingTimestamp = d.ephemeralSettingTimestamp;
                    else if (typeof d.ephemeralSettingTimestamp === "object")
                        m.ephemeralSettingTimestamp = new $util.LongBits(d.ephemeralSettingTimestamp.low >>> 0, d.ephemeralSettingTimestamp.high >>> 0).toNumber();
                }
                if (d.historySyncNotification != null) {
                    if (!$util.isObject(d.historySyncNotification))
                        throw $TypeError(".proto.Message.ProtocolMessage.historySyncNotification: object expected");
                    m.historySyncNotification = $root.proto.Message.HistorySyncNotification.fromObject(d.historySyncNotification, q + 1);
                }
                if (d.appStateSyncKeyShare != null) {
                    if (!$util.isObject(d.appStateSyncKeyShare))
                        throw $TypeError(".proto.Message.ProtocolMessage.appStateSyncKeyShare: object expected");
                    m.appStateSyncKeyShare = $root.proto.Message.AppStateSyncKeyShare.fromObject(d.appStateSyncKeyShare, q + 1);
                }
                if (d.appStateSyncKeyRequest != null) {
                    if (!$util.isObject(d.appStateSyncKeyRequest))
                        throw $TypeError(".proto.Message.ProtocolMessage.appStateSyncKeyRequest: object expected");
                    m.appStateSyncKeyRequest = $root.proto.Message.AppStateSyncKeyRequest.fromObject(d.appStateSyncKeyRequest, q + 1);
                }
                if (d.initialSecurityNotificationSettingSync != null) {
                    if (!$util.isObject(d.initialSecurityNotificationSettingSync))
                        throw $TypeError(".proto.Message.ProtocolMessage.initialSecurityNotificationSettingSync: object expected");
                    m.initialSecurityNotificationSettingSync = $root.proto.Message.InitialSecurityNotificationSettingSync.fromObject(d.initialSecurityNotificationSettingSync, q + 1);
                }
                if (d.appStateFatalExceptionNotification != null) {
                    if (!$util.isObject(d.appStateFatalExceptionNotification))
                        throw $TypeError(".proto.Message.ProtocolMessage.appStateFatalExceptionNotification: object expected");
                    m.appStateFatalExceptionNotification = $root.proto.Message.AppStateFatalExceptionNotification.fromObject(d.appStateFatalExceptionNotification, q + 1);
                }
                if (d.disappearingMode != null) {
                    if (!$util.isObject(d.disappearingMode))
                        throw $TypeError(".proto.Message.ProtocolMessage.disappearingMode: object expected");
                    m.disappearingMode = $root.proto.DisappearingMode.fromObject(d.disappearingMode, q + 1);
                }
                if (d.editedMessage != null) {
                    if (!$util.isObject(d.editedMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.editedMessage: object expected");
                    m.editedMessage = $root.proto.Message.fromObject(d.editedMessage, q + 1);
                }
                if (d.timestampMs != null) {
                    if ($util.Long)
                        m.timestampMs = $util.Long.fromValue(d.timestampMs, false);
                    else if (typeof d.timestampMs === "string")
                        m.timestampMs = $parseInt(d.timestampMs, 10);
                    else if (typeof d.timestampMs === "number")
                        m.timestampMs = d.timestampMs;
                    else if (typeof d.timestampMs === "object")
                        m.timestampMs = new $util.LongBits(d.timestampMs.low >>> 0, d.timestampMs.high >>> 0).toNumber();
                }
                if (d.peerDataOperationRequestMessage != null) {
                    if (!$util.isObject(d.peerDataOperationRequestMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.peerDataOperationRequestMessage: object expected");
                    m.peerDataOperationRequestMessage = $root.proto.Message.PeerDataOperationRequestMessage.fromObject(d.peerDataOperationRequestMessage, q + 1);
                }
                if (d.peerDataOperationRequestResponseMessage != null) {
                    if (!$util.isObject(d.peerDataOperationRequestResponseMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.peerDataOperationRequestResponseMessage: object expected");
                    m.peerDataOperationRequestResponseMessage = $root.proto.Message.PeerDataOperationRequestResponseMessage.fromObject(d.peerDataOperationRequestResponseMessage, q + 1);
                }
                if (d.botFeedbackMessage != null) {
                    if (!$util.isObject(d.botFeedbackMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.botFeedbackMessage: object expected");
                    m.botFeedbackMessage = $root.proto.BotFeedbackMessage.fromObject(d.botFeedbackMessage, q + 1);
                }
                if (d.invokerJid != null) {
                    m.invokerJid = $String(d.invokerJid);
                }
                if (d.requestWelcomeMessageMetadata != null) {
                    if (!$util.isObject(d.requestWelcomeMessageMetadata))
                        throw $TypeError(".proto.Message.ProtocolMessage.requestWelcomeMessageMetadata: object expected");
                    m.requestWelcomeMessageMetadata = $root.proto.Message.RequestWelcomeMessageMetadata.fromObject(d.requestWelcomeMessageMetadata, q + 1);
                }
                if (d.mediaNotifyMessage != null) {
                    if (!$util.isObject(d.mediaNotifyMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.mediaNotifyMessage: object expected");
                    m.mediaNotifyMessage = $root.proto.MediaNotifyMessage.fromObject(d.mediaNotifyMessage, q + 1);
                }
                if (d.cloudApiThreadControlNotification != null) {
                    if (!$util.isObject(d.cloudApiThreadControlNotification))
                        throw $TypeError(".proto.Message.ProtocolMessage.cloudApiThreadControlNotification: object expected");
                    m.cloudApiThreadControlNotification = $root.proto.Message.CloudAPIThreadControlNotification.fromObject(d.cloudApiThreadControlNotification, q + 1);
                }
                if (d.lidMigrationMappingSyncMessage != null) {
                    if (!$util.isObject(d.lidMigrationMappingSyncMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.lidMigrationMappingSyncMessage: object expected");
                    m.lidMigrationMappingSyncMessage = $root.proto.LIDMigrationMappingSyncMessage.fromObject(d.lidMigrationMappingSyncMessage, q + 1);
                }
                if (d.limitSharing != null) {
                    if (!$util.isObject(d.limitSharing))
                        throw $TypeError(".proto.Message.ProtocolMessage.limitSharing: object expected");
                    m.limitSharing = $root.proto.LimitSharing.fromObject(d.limitSharing, q + 1);
                }
                if (d.aiPsiMetadata != null) {
                    if (typeof d.aiPsiMetadata === "string")
                        $util.base64.decode(d.aiPsiMetadata, m.aiPsiMetadata = $util.newBuffer($util.base64.length(d.aiPsiMetadata)), 0);
                    else if (d.aiPsiMetadata.length >= 0)
                        m.aiPsiMetadata = d.aiPsiMetadata;
                }
                if (d.aiQueryFanout != null) {
                    if (!$util.isObject(d.aiQueryFanout))
                        throw $TypeError(".proto.Message.ProtocolMessage.aiQueryFanout: object expected");
                    m.aiQueryFanout = $root.proto.AIQueryFanout.fromObject(d.aiQueryFanout, q + 1);
                }
                if (d.memberLabel != null) {
                    if (!$util.isObject(d.memberLabel))
                        throw $TypeError(".proto.Message.ProtocolMessage.memberLabel: object expected");
                    m.memberLabel = $root.proto.MemberLabel.fromObject(d.memberLabel, q + 1);
                }
                if (d.aiMediaCollectionMessage != null) {
                    if (!$util.isObject(d.aiMediaCollectionMessage))
                        throw $TypeError(".proto.Message.ProtocolMessage.aiMediaCollectionMessage: object expected");
                    m.aiMediaCollectionMessage = $root.proto.AIMediaCollectionMessage.fromObject(d.aiMediaCollectionMessage, q + 1);
                }
                if (d.afterReadDuration != null) {
                    m.afterReadDuration = d.afterReadDuration >>> 0;
                }
                if (d.chatThemeSetting != null) {
                    if (!$util.isObject(d.chatThemeSetting))
                        throw $TypeError(".proto.Message.ProtocolMessage.chatThemeSetting: object expected");
                    m.chatThemeSetting = $root.proto.Message.ChatThemeSetting.fromObject(d.chatThemeSetting, q + 1);
                }
                if (d.aiMetadataOperation != null) {
                    if (!$util.isObject(d.aiMetadataOperation))
                        throw $TypeError(".proto.Message.ProtocolMessage.aiMetadataOperation: object expected");
                    m.aiMetadataOperation = $root.proto.AIMetadataOperation.fromObject(d.aiMetadataOperation, q + 1);
                }
                if (d.markAsVerifiedAction != null) {
                    if (!$util.isObject(d.markAsVerifiedAction))
                        throw $TypeError(".proto.Message.ProtocolMessage.markAsVerifiedAction: object expected");
                    m.markAsVerifiedAction = $root.proto.Message.MarkAsVerifiedAction.fromObject(d.markAsVerifiedAction, q + 1);
                }
                if (d.coexStateSync != null) {
                    if (!$util.isObject(d.coexStateSync))
                        throw $TypeError(".proto.Message.ProtocolMessage.coexStateSync: object expected");
                    m.coexStateSync = $root.proto.CoexStateSync.fromObject(d.coexStateSync, q + 1);
                }
                return m;
            };

            ProtocolMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.ProtocolMessage.Type[m.type] === $undefined ? m.type : $root.proto.Message.ProtocolMessage.Type[m.type] : m.type;
                }
                if (m.ephemeralExpiration != null && $Object.hasOwnProperty.call(m, "ephemeralExpiration")) {
                    d.ephemeralExpiration = m.ephemeralExpiration;
                }
                if (m.ephemeralSettingTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralSettingTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.ephemeralSettingTimestamp = typeof m.ephemeralSettingTimestamp === "number" ? $BigInt(m.ephemeralSettingTimestamp) : $util.Long.fromBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.ephemeralSettingTimestamp === "number")
                        d.ephemeralSettingTimestamp = o.longs === $String ? $String(m.ephemeralSettingTimestamp) : m.ephemeralSettingTimestamp;
                    else
                        d.ephemeralSettingTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.ephemeralSettingTimestamp) : o.longs === $Number ? new $util.LongBits(m.ephemeralSettingTimestamp.low >>> 0, m.ephemeralSettingTimestamp.high >>> 0).toNumber() : m.ephemeralSettingTimestamp;
                }
                if (m.historySyncNotification != null && $Object.hasOwnProperty.call(m, "historySyncNotification")) {
                    d.historySyncNotification = $root.proto.Message.HistorySyncNotification.toObject(m.historySyncNotification, o, q + 1);
                }
                if (m.appStateSyncKeyShare != null && $Object.hasOwnProperty.call(m, "appStateSyncKeyShare")) {
                    d.appStateSyncKeyShare = $root.proto.Message.AppStateSyncKeyShare.toObject(m.appStateSyncKeyShare, o, q + 1);
                }
                if (m.appStateSyncKeyRequest != null && $Object.hasOwnProperty.call(m, "appStateSyncKeyRequest")) {
                    d.appStateSyncKeyRequest = $root.proto.Message.AppStateSyncKeyRequest.toObject(m.appStateSyncKeyRequest, o, q + 1);
                }
                if (m.initialSecurityNotificationSettingSync != null && $Object.hasOwnProperty.call(m, "initialSecurityNotificationSettingSync")) {
                    d.initialSecurityNotificationSettingSync = $root.proto.Message.InitialSecurityNotificationSettingSync.toObject(m.initialSecurityNotificationSettingSync, o, q + 1);
                }
                if (m.appStateFatalExceptionNotification != null && $Object.hasOwnProperty.call(m, "appStateFatalExceptionNotification")) {
                    d.appStateFatalExceptionNotification = $root.proto.Message.AppStateFatalExceptionNotification.toObject(m.appStateFatalExceptionNotification, o, q + 1);
                }
                if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode")) {
                    d.disappearingMode = $root.proto.DisappearingMode.toObject(m.disappearingMode, o, q + 1);
                }
                if (m.editedMessage != null && $Object.hasOwnProperty.call(m, "editedMessage")) {
                    d.editedMessage = $root.proto.Message.toObject(m.editedMessage, o, q + 1);
                }
                if (m.timestampMs != null && $Object.hasOwnProperty.call(m, "timestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestampMs = typeof m.timestampMs === "number" ? $BigInt(m.timestampMs) : $util.Long.fromBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.timestampMs === "number")
                        d.timestampMs = o.longs === $String ? $String(m.timestampMs) : m.timestampMs;
                    else
                        d.timestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.timestampMs) : o.longs === $Number ? new $util.LongBits(m.timestampMs.low >>> 0, m.timestampMs.high >>> 0).toNumber() : m.timestampMs;
                }
                if (m.peerDataOperationRequestMessage != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestMessage")) {
                    d.peerDataOperationRequestMessage = $root.proto.Message.PeerDataOperationRequestMessage.toObject(m.peerDataOperationRequestMessage, o, q + 1);
                }
                if (m.peerDataOperationRequestResponseMessage != null && $Object.hasOwnProperty.call(m, "peerDataOperationRequestResponseMessage")) {
                    d.peerDataOperationRequestResponseMessage = $root.proto.Message.PeerDataOperationRequestResponseMessage.toObject(m.peerDataOperationRequestResponseMessage, o, q + 1);
                }
                if (m.botFeedbackMessage != null && $Object.hasOwnProperty.call(m, "botFeedbackMessage")) {
                    d.botFeedbackMessage = $root.proto.BotFeedbackMessage.toObject(m.botFeedbackMessage, o, q + 1);
                }
                if (m.invokerJid != null && $Object.hasOwnProperty.call(m, "invokerJid")) {
                    d.invokerJid = m.invokerJid;
                }
                if (m.requestWelcomeMessageMetadata != null && $Object.hasOwnProperty.call(m, "requestWelcomeMessageMetadata")) {
                    d.requestWelcomeMessageMetadata = $root.proto.Message.RequestWelcomeMessageMetadata.toObject(m.requestWelcomeMessageMetadata, o, q + 1);
                }
                if (m.mediaNotifyMessage != null && $Object.hasOwnProperty.call(m, "mediaNotifyMessage")) {
                    d.mediaNotifyMessage = $root.proto.MediaNotifyMessage.toObject(m.mediaNotifyMessage, o, q + 1);
                }
                if (m.cloudApiThreadControlNotification != null && $Object.hasOwnProperty.call(m, "cloudApiThreadControlNotification")) {
                    d.cloudApiThreadControlNotification = $root.proto.Message.CloudAPIThreadControlNotification.toObject(m.cloudApiThreadControlNotification, o, q + 1);
                }
                if (m.lidMigrationMappingSyncMessage != null && $Object.hasOwnProperty.call(m, "lidMigrationMappingSyncMessage")) {
                    d.lidMigrationMappingSyncMessage = $root.proto.LIDMigrationMappingSyncMessage.toObject(m.lidMigrationMappingSyncMessage, o, q + 1);
                }
                if (m.limitSharing != null && $Object.hasOwnProperty.call(m, "limitSharing")) {
                    d.limitSharing = $root.proto.LimitSharing.toObject(m.limitSharing, o, q + 1);
                }
                if (m.aiPsiMetadata != null && $Object.hasOwnProperty.call(m, "aiPsiMetadata")) {
                    d.aiPsiMetadata = o.bytes === $String ? $util.base64.encode(m.aiPsiMetadata, 0, m.aiPsiMetadata.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.aiPsiMetadata) : m.aiPsiMetadata;
                }
                if (m.aiQueryFanout != null && $Object.hasOwnProperty.call(m, "aiQueryFanout")) {
                    d.aiQueryFanout = $root.proto.AIQueryFanout.toObject(m.aiQueryFanout, o, q + 1);
                }
                if (m.memberLabel != null && $Object.hasOwnProperty.call(m, "memberLabel")) {
                    d.memberLabel = $root.proto.MemberLabel.toObject(m.memberLabel, o, q + 1);
                }
                if (m.aiMediaCollectionMessage != null && $Object.hasOwnProperty.call(m, "aiMediaCollectionMessage")) {
                    d.aiMediaCollectionMessage = $root.proto.AIMediaCollectionMessage.toObject(m.aiMediaCollectionMessage, o, q + 1);
                }
                if (m.afterReadDuration != null && $Object.hasOwnProperty.call(m, "afterReadDuration")) {
                    d.afterReadDuration = m.afterReadDuration;
                }
                if (m.chatThemeSetting != null && $Object.hasOwnProperty.call(m, "chatThemeSetting")) {
                    d.chatThemeSetting = $root.proto.Message.ChatThemeSetting.toObject(m.chatThemeSetting, o, q + 1);
                }
                if (m.aiMetadataOperation != null && $Object.hasOwnProperty.call(m, "aiMetadataOperation")) {
                    d.aiMetadataOperation = $root.proto.AIMetadataOperation.toObject(m.aiMetadataOperation, o, q + 1);
                }
                if (m.markAsVerifiedAction != null && $Object.hasOwnProperty.call(m, "markAsVerifiedAction")) {
                    d.markAsVerifiedAction = $root.proto.Message.MarkAsVerifiedAction.toObject(m.markAsVerifiedAction, o, q + 1);
                }
                if (m.coexStateSync != null && $Object.hasOwnProperty.call(m, "coexStateSync")) {
                    d.coexStateSync = $root.proto.CoexStateSync.toObject(m.coexStateSync, o, q + 1);
                }
                return d;
            };

            ProtocolMessage.prototype.toJSON = function() {
                return ProtocolMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ProtocolMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ProtocolMessage";
            };

            ProtocolMessage.Type = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "REVOKE"] = 0;
                values[valuesById[3] = "EPHEMERAL_SETTING"] = 3;
                values[valuesById[4] = "EPHEMERAL_SYNC_RESPONSE"] = 4;
                values[valuesById[5] = "HISTORY_SYNC_NOTIFICATION"] = 5;
                values[valuesById[6] = "APP_STATE_SYNC_KEY_SHARE"] = 6;
                values[valuesById[7] = "APP_STATE_SYNC_KEY_REQUEST"] = 7;
                values[valuesById[8] = "MSG_FANOUT_BACKFILL_REQUEST"] = 8;
                values[valuesById[9] = "INITIAL_SECURITY_NOTIFICATION_SETTING_SYNC"] = 9;
                values[valuesById[10] = "APP_STATE_FATAL_EXCEPTION_NOTIFICATION"] = 10;
                values[valuesById[11] = "SHARE_PHONE_NUMBER"] = 11;
                values[valuesById[14] = "MESSAGE_EDIT"] = 14;
                values[valuesById[16] = "PEER_DATA_OPERATION_REQUEST_MESSAGE"] = 16;
                values[valuesById[17] = "PEER_DATA_OPERATION_REQUEST_RESPONSE_MESSAGE"] = 17;
                values[valuesById[18] = "REQUEST_WELCOME_MESSAGE"] = 18;
                values[valuesById[19] = "BOT_FEEDBACK_MESSAGE"] = 19;
                values[valuesById[20] = "MEDIA_NOTIFY_MESSAGE"] = 20;
                values[valuesById[21] = "CLOUD_API_THREAD_CONTROL_NOTIFICATION"] = 21;
                values[valuesById[22] = "LID_MIGRATION_MAPPING_SYNC"] = 22;
                values[valuesById[23] = "REMINDER_MESSAGE"] = 23;
                values[valuesById[24] = "BOT_MEMU_ONBOARDING_MESSAGE"] = 24;
                values[valuesById[25] = "STATUS_MENTION_MESSAGE"] = 25;
                values[valuesById[26] = "STOP_GENERATION_MESSAGE"] = 26;
                values[valuesById[27] = "LIMIT_SHARING"] = 27;
                values[valuesById[28] = "AI_PSI_METADATA"] = 28;
                values[valuesById[29] = "AI_QUERY_FANOUT"] = 29;
                values[valuesById[30] = "GROUP_MEMBER_LABEL_CHANGE"] = 30;
                values[valuesById[31] = "AI_MEDIA_COLLECTION_MESSAGE"] = 31;
                values[valuesById[32] = "MESSAGE_UNSCHEDULE"] = 32;
                values[valuesById[34] = "CHAT_THEME_SETTING"] = 34;
                values[valuesById[35] = "AI_METADATA_OPERATION"] = 35;
                values[valuesById[36] = "MARK_AS_VERIFIED_ACTION"] = 36;
                values[valuesById[37] = "COEX_STATE_SYNC"] = 37;
                return values;
            })();

            return ProtocolMessage;
        })();

        Message.QuestionResponseMessage = (function() {

            const QuestionResponseMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            QuestionResponseMessage.prototype.key = null;
            QuestionResponseMessage.prototype.text = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuestionResponseMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuestionResponseMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            QuestionResponseMessage.create = function(properties) {
                return new QuestionResponseMessage(properties);
            };

            QuestionResponseMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(18).string(m.text);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            QuestionResponseMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.QuestionResponseMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            QuestionResponseMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.QuestionResponseMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.QuestionResponseMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.QuestionResponseMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.QuestionResponseMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                return m;
            };

            QuestionResponseMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                return d;
            };

            QuestionResponseMessage.prototype.toJSON = function() {
                return QuestionResponseMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuestionResponseMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.QuestionResponseMessage";
            };

            return QuestionResponseMessage;
        })();

        Message.ReactionMessage = (function() {

            const ReactionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ReactionMessage.prototype.key = null;
            ReactionMessage.prototype.text = null;
            ReactionMessage.prototype.groupingKey = null;
            ReactionMessage.prototype.senderTimestampMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_groupingKey", {
                get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ReactionMessage.prototype, "_senderTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ReactionMessage.create = function(properties) {
                return new ReactionMessage(properties);
            };

            ReactionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(18).string(m.text);
                if (m.groupingKey != null && $Object.hasOwnProperty.call(m, "groupingKey"))
                    w.uint32(26).string(m.groupingKey);
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                    w.uint32(32).int64(m.senderTimestampMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ReactionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ReactionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.groupingKey = r.stringVerify();
                            m._groupingKey = "groupingKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.senderTimestampMs = r.int64();
                            m._senderTimestampMs = "senderTimestampMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ReactionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ReactionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ReactionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ReactionMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.ReactionMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.groupingKey != null) {
                    m.groupingKey = $String(d.groupingKey);
                }
                if (d.senderTimestampMs != null) {
                    if ($util.Long)
                        m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                    else if (typeof d.senderTimestampMs === "string")
                        m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                    else if (typeof d.senderTimestampMs === "number")
                        m.senderTimestampMs = d.senderTimestampMs;
                    else if (typeof d.senderTimestampMs === "object")
                        m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
                }
                return m;
            };

            ReactionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                if (m.groupingKey != null && $Object.hasOwnProperty.call(m, "groupingKey")) {
                    d.groupingKey = m.groupingKey;
                }
                if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.senderTimestampMs === "number")
                        d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                    else
                        d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
                }
                return d;
            };

            ReactionMessage.prototype.toJSON = function() {
                return ReactionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ReactionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ReactionMessage";
            };

            return ReactionMessage;
        })();

        Message.RequestPaymentMessage = (function() {

            const RequestPaymentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RequestPaymentMessage.prototype.noteMessage = null;
            RequestPaymentMessage.prototype.currencyCodeIso4217 = null;
            RequestPaymentMessage.prototype.amount1000 = null;
            RequestPaymentMessage.prototype.requestFrom = null;
            RequestPaymentMessage.prototype.expiryTimestamp = null;
            RequestPaymentMessage.prototype.amount = null;
            RequestPaymentMessage.prototype.background = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_noteMessage", {
                get: $util.oneOfGetter($oneOfFields = ["noteMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_currencyCodeIso4217", {
                get: $util.oneOfGetter($oneOfFields = ["currencyCodeIso4217"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_amount1000", {
                get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_requestFrom", {
                get: $util.oneOfGetter($oneOfFields = ["requestFrom"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_expiryTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_amount", {
                get: $util.oneOfGetter($oneOfFields = ["amount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPaymentMessage.prototype, "_background", {
                get: $util.oneOfGetter($oneOfFields = ["background"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RequestPaymentMessage.create = function(properties) {
                return new RequestPaymentMessage(properties);
            };

            RequestPaymentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.currencyCodeIso4217 != null && $Object.hasOwnProperty.call(m, "currencyCodeIso4217"))
                    w.uint32(10).string(m.currencyCodeIso4217);
                if (m.amount1000 != null && $Object.hasOwnProperty.call(m, "amount1000"))
                    w.uint32(16).uint64(m.amount1000);
                if (m.requestFrom != null && $Object.hasOwnProperty.call(m, "requestFrom"))
                    w.uint32(26).string(m.requestFrom);
                if (m.noteMessage != null && $Object.hasOwnProperty.call(m, "noteMessage"))
                    $root.proto.Message.encode(m.noteMessage, w.uint32(34).fork(), q + 1).ldelim();
                if (m.expiryTimestamp != null && $Object.hasOwnProperty.call(m, "expiryTimestamp"))
                    w.uint32(40).int64(m.expiryTimestamp);
                if (m.amount != null && $Object.hasOwnProperty.call(m, "amount"))
                    $root.proto.Money.encode(m.amount, w.uint32(50).fork(), q + 1).ldelim();
                if (m.background != null && $Object.hasOwnProperty.call(m, "background"))
                    $root.proto.PaymentBackground.encode(m.background, w.uint32(58).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RequestPaymentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.RequestPaymentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 4: {
                            if (u !== 2)
                                break;
                            m.noteMessage = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.noteMessage);
                            m._noteMessage = "noteMessage";
                            continue;
                        }
                    case 1: {
                            if (u !== 2)
                                break;
                            m.currencyCodeIso4217 = r.stringVerify();
                            m._currencyCodeIso4217 = "currencyCodeIso4217";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.amount1000 = r.uint64();
                            m._amount1000 = "amount1000";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.requestFrom = r.stringVerify();
                            m._requestFrom = "requestFrom";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.expiryTimestamp = r.int64();
                            m._expiryTimestamp = "expiryTimestamp";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.amount = $root.proto.Money.decode(r, r.uint32(), $undefined, q + 1, m.amount);
                            m._amount = "amount";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.background = $root.proto.PaymentBackground.decode(r, r.uint32(), $undefined, q + 1, m.background);
                            m._background = "background";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RequestPaymentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.RequestPaymentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.RequestPaymentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.RequestPaymentMessage();
                if (d.noteMessage != null) {
                    if (!$util.isObject(d.noteMessage))
                        throw $TypeError(".proto.Message.RequestPaymentMessage.noteMessage: object expected");
                    m.noteMessage = $root.proto.Message.fromObject(d.noteMessage, q + 1);
                }
                if (d.currencyCodeIso4217 != null) {
                    m.currencyCodeIso4217 = $String(d.currencyCodeIso4217);
                }
                if (d.amount1000 != null) {
                    if ($util.Long)
                        m.amount1000 = $util.Long.fromValue(d.amount1000, true);
                    else if (typeof d.amount1000 === "string")
                        m.amount1000 = $parseInt(d.amount1000, 10);
                    else if (typeof d.amount1000 === "number")
                        m.amount1000 = d.amount1000;
                    else if (typeof d.amount1000 === "object")
                        m.amount1000 = new $util.LongBits(d.amount1000.low >>> 0, d.amount1000.high >>> 0).toNumber(true);
                }
                if (d.requestFrom != null) {
                    m.requestFrom = $String(d.requestFrom);
                }
                if (d.expiryTimestamp != null) {
                    if ($util.Long)
                        m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp, false);
                    else if (typeof d.expiryTimestamp === "string")
                        m.expiryTimestamp = $parseInt(d.expiryTimestamp, 10);
                    else if (typeof d.expiryTimestamp === "number")
                        m.expiryTimestamp = d.expiryTimestamp;
                    else if (typeof d.expiryTimestamp === "object")
                        m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber();
                }
                if (d.amount != null) {
                    if (!$util.isObject(d.amount))
                        throw $TypeError(".proto.Message.RequestPaymentMessage.amount: object expected");
                    m.amount = $root.proto.Money.fromObject(d.amount, q + 1);
                }
                if (d.background != null) {
                    if (!$util.isObject(d.background))
                        throw $TypeError(".proto.Message.RequestPaymentMessage.background: object expected");
                    m.background = $root.proto.PaymentBackground.fromObject(d.background, q + 1);
                }
                return m;
            };

            RequestPaymentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.currencyCodeIso4217 != null && $Object.hasOwnProperty.call(m, "currencyCodeIso4217")) {
                    d.currencyCodeIso4217 = m.currencyCodeIso4217;
                }
                if (m.amount1000 != null && $Object.hasOwnProperty.call(m, "amount1000")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.amount1000 = typeof m.amount1000 === "number" ? $BigInt(m.amount1000) : $util.Long.fromBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0, true).toBigInt();
                    else if (typeof m.amount1000 === "number")
                        d.amount1000 = o.longs === $String ? $String(m.amount1000) : m.amount1000;
                    else
                        d.amount1000 = o.longs === $String ? $util.Long.prototype.toString.call(m.amount1000) : o.longs === $Number ? new $util.LongBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0).toNumber(true) : m.amount1000;
                }
                if (m.requestFrom != null && $Object.hasOwnProperty.call(m, "requestFrom")) {
                    d.requestFrom = m.requestFrom;
                }
                if (m.noteMessage != null && $Object.hasOwnProperty.call(m, "noteMessage")) {
                    d.noteMessage = $root.proto.Message.toObject(m.noteMessage, o, q + 1);
                }
                if (m.expiryTimestamp != null && $Object.hasOwnProperty.call(m, "expiryTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.expiryTimestamp = typeof m.expiryTimestamp === "number" ? $BigInt(m.expiryTimestamp) : $util.Long.fromBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.expiryTimestamp === "number")
                        d.expiryTimestamp = o.longs === $String ? $String(m.expiryTimestamp) : m.expiryTimestamp;
                    else
                        d.expiryTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === $Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber() : m.expiryTimestamp;
                }
                if (m.amount != null && $Object.hasOwnProperty.call(m, "amount")) {
                    d.amount = $root.proto.Money.toObject(m.amount, o, q + 1);
                }
                if (m.background != null && $Object.hasOwnProperty.call(m, "background")) {
                    d.background = $root.proto.PaymentBackground.toObject(m.background, o, q + 1);
                }
                return d;
            };

            RequestPaymentMessage.prototype.toJSON = function() {
                return RequestPaymentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            RequestPaymentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.RequestPaymentMessage";
            };

            return RequestPaymentMessage;
        })();

        Message.RequestPhoneNumberMessage = (function() {

            const RequestPhoneNumberMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RequestPhoneNumberMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestPhoneNumberMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RequestPhoneNumberMessage.create = function(properties) {
                return new RequestPhoneNumberMessage(properties);
            };

            RequestPhoneNumberMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RequestPhoneNumberMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.RequestPhoneNumberMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RequestPhoneNumberMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.RequestPhoneNumberMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.RequestPhoneNumberMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.RequestPhoneNumberMessage();
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.RequestPhoneNumberMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            RequestPhoneNumberMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            RequestPhoneNumberMessage.prototype.toJSON = function() {
                return RequestPhoneNumberMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            RequestPhoneNumberMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.RequestPhoneNumberMessage";
            };

            return RequestPhoneNumberMessage;
        })();

        Message.RequestWelcomeMessageMetadata = (function() {

            const RequestWelcomeMessageMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RequestWelcomeMessageMetadata.prototype.localChatState = null;
            RequestWelcomeMessageMetadata.prototype.welcomeTrigger = null;
            RequestWelcomeMessageMetadata.prototype.botAgentMetadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_localChatState", {
                get: $util.oneOfGetter($oneOfFields = ["localChatState"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_welcomeTrigger", {
                get: $util.oneOfGetter($oneOfFields = ["welcomeTrigger"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RequestWelcomeMessageMetadata.prototype, "_botAgentMetadata", {
                get: $util.oneOfGetter($oneOfFields = ["botAgentMetadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RequestWelcomeMessageMetadata.create = function(properties) {
                return new RequestWelcomeMessageMetadata(properties);
            };

            RequestWelcomeMessageMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.localChatState != null && $Object.hasOwnProperty.call(m, "localChatState"))
                    w.uint32(8).int32(m.localChatState);
                if (m.welcomeTrigger != null && $Object.hasOwnProperty.call(m, "welcomeTrigger"))
                    w.uint32(16).int32(m.welcomeTrigger);
                if (m.botAgentMetadata != null && $Object.hasOwnProperty.call(m, "botAgentMetadata"))
                    $root.proto.BotAgentMetadata.encode(m.botAgentMetadata, w.uint32(26).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RequestWelcomeMessageMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.RequestWelcomeMessageMetadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.localChatState = r.int32();
                            m._localChatState = "localChatState";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.welcomeTrigger = r.int32();
                            m._welcomeTrigger = "welcomeTrigger";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.botAgentMetadata = $root.proto.BotAgentMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botAgentMetadata);
                            m._botAgentMetadata = "botAgentMetadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RequestWelcomeMessageMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.RequestWelcomeMessageMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.RequestWelcomeMessageMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.RequestWelcomeMessageMetadata();
                switch (d.localChatState) {
                case "EMPTY":
                case 0:
                    m.localChatState = 0;
                    break;
                case "NON_EMPTY":
                case 1:
                    m.localChatState = 1;
                    break;
                default:
                    if (typeof d.localChatState === "number" && (d.localChatState | 0) === d.localChatState)
                        m.localChatState = d.localChatState;
                }
                switch (d.welcomeTrigger) {
                case "CHAT_OPEN":
                case 0:
                    m.welcomeTrigger = 0;
                    break;
                case "COMPANION_PAIRING":
                case 1:
                    m.welcomeTrigger = 1;
                    break;
                default:
                    if (typeof d.welcomeTrigger === "number" && (d.welcomeTrigger | 0) === d.welcomeTrigger)
                        m.welcomeTrigger = d.welcomeTrigger;
                }
                if (d.botAgentMetadata != null) {
                    if (!$util.isObject(d.botAgentMetadata))
                        throw $TypeError(".proto.Message.RequestWelcomeMessageMetadata.botAgentMetadata: object expected");
                    m.botAgentMetadata = $root.proto.BotAgentMetadata.fromObject(d.botAgentMetadata, q + 1);
                }
                return m;
            };

            RequestWelcomeMessageMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.localChatState != null && $Object.hasOwnProperty.call(m, "localChatState")) {
                    d.localChatState = o.enums === $String ? $root.proto.Message.RequestWelcomeMessageMetadata.LocalChatState[m.localChatState] === $undefined ? m.localChatState : $root.proto.Message.RequestWelcomeMessageMetadata.LocalChatState[m.localChatState] : m.localChatState;
                }
                if (m.welcomeTrigger != null && $Object.hasOwnProperty.call(m, "welcomeTrigger")) {
                    d.welcomeTrigger = o.enums === $String ? $root.proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger[m.welcomeTrigger] === $undefined ? m.welcomeTrigger : $root.proto.Message.RequestWelcomeMessageMetadata.WelcomeTrigger[m.welcomeTrigger] : m.welcomeTrigger;
                }
                if (m.botAgentMetadata != null && $Object.hasOwnProperty.call(m, "botAgentMetadata")) {
                    d.botAgentMetadata = $root.proto.BotAgentMetadata.toObject(m.botAgentMetadata, o, q + 1);
                }
                return d;
            };

            RequestWelcomeMessageMetadata.prototype.toJSON = function() {
                return RequestWelcomeMessageMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            RequestWelcomeMessageMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.RequestWelcomeMessageMetadata";
            };

            RequestWelcomeMessageMetadata.LocalChatState = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "EMPTY"] = 0;
                values[valuesById[1] = "NON_EMPTY"] = 1;
                return values;
            })();

            RequestWelcomeMessageMetadata.WelcomeTrigger = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "CHAT_OPEN"] = 0;
                values[valuesById[1] = "COMPANION_PAIRING"] = 1;
                return values;
            })();

            return RequestWelcomeMessageMetadata;
        })();

        Message.RootSecretDistributeMessage = (function() {

            const RootSecretDistributeMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RootSecretDistributeMessage.prototype.chatJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RootSecretDistributeMessage.prototype, "_chatJid", {
                get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RootSecretDistributeMessage.create = function(properties) {
                return new RootSecretDistributeMessage(properties);
            };

            RootSecretDistributeMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid"))
                    w.uint32(10).string(m.chatJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RootSecretDistributeMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.RootSecretDistributeMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.chatJid = r.stringVerify();
                            m._chatJid = "chatJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RootSecretDistributeMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.RootSecretDistributeMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.RootSecretDistributeMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.RootSecretDistributeMessage();
                if (d.chatJid != null) {
                    m.chatJid = $String(d.chatJid);
                }
                return m;
            };

            RootSecretDistributeMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid")) {
                    d.chatJid = m.chatJid;
                }
                return d;
            };

            RootSecretDistributeMessage.prototype.toJSON = function() {
                return RootSecretDistributeMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            RootSecretDistributeMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.RootSecretDistributeMessage";
            };

            return RootSecretDistributeMessage;
        })();

        Message.ScheduledCallCreationMessage = (function() {

            const ScheduledCallCreationMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ScheduledCallCreationMessage.prototype.scheduledTimestampMs = null;
            ScheduledCallCreationMessage.prototype.callType = null;
            ScheduledCallCreationMessage.prototype.title = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ScheduledCallCreationMessage.prototype, "_scheduledTimestampMs", {
                get: $util.oneOfGetter($oneOfFields = ["scheduledTimestampMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ScheduledCallCreationMessage.prototype, "_callType", {
                get: $util.oneOfGetter($oneOfFields = ["callType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ScheduledCallCreationMessage.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ScheduledCallCreationMessage.create = function(properties) {
                return new ScheduledCallCreationMessage(properties);
            };

            ScheduledCallCreationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.scheduledTimestampMs != null && $Object.hasOwnProperty.call(m, "scheduledTimestampMs"))
                    w.uint32(8).int64(m.scheduledTimestampMs);
                if (m.callType != null && $Object.hasOwnProperty.call(m, "callType"))
                    w.uint32(16).int32(m.callType);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(26).string(m.title);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ScheduledCallCreationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ScheduledCallCreationMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.scheduledTimestampMs = r.int64();
                            m._scheduledTimestampMs = "scheduledTimestampMs";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.callType = r.int32();
                            m._callType = "callType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ScheduledCallCreationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ScheduledCallCreationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ScheduledCallCreationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ScheduledCallCreationMessage();
                if (d.scheduledTimestampMs != null) {
                    if ($util.Long)
                        m.scheduledTimestampMs = $util.Long.fromValue(d.scheduledTimestampMs, false);
                    else if (typeof d.scheduledTimestampMs === "string")
                        m.scheduledTimestampMs = $parseInt(d.scheduledTimestampMs, 10);
                    else if (typeof d.scheduledTimestampMs === "number")
                        m.scheduledTimestampMs = d.scheduledTimestampMs;
                    else if (typeof d.scheduledTimestampMs === "object")
                        m.scheduledTimestampMs = new $util.LongBits(d.scheduledTimestampMs.low >>> 0, d.scheduledTimestampMs.high >>> 0).toNumber();
                }
                switch (d.callType) {
                case "UNKNOWN":
                case 0:
                    m.callType = 0;
                    break;
                case "VOICE":
                case 1:
                    m.callType = 1;
                    break;
                case "VIDEO":
                case 2:
                    m.callType = 2;
                    break;
                default:
                    if (typeof d.callType === "number" && (d.callType | 0) === d.callType)
                        m.callType = d.callType;
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                return m;
            };

            ScheduledCallCreationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.scheduledTimestampMs != null && $Object.hasOwnProperty.call(m, "scheduledTimestampMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.scheduledTimestampMs = typeof m.scheduledTimestampMs === "number" ? $BigInt(m.scheduledTimestampMs) : $util.Long.fromBits(m.scheduledTimestampMs.low >>> 0, m.scheduledTimestampMs.high >>> 0, false).toBigInt();
                    else if (typeof m.scheduledTimestampMs === "number")
                        d.scheduledTimestampMs = o.longs === $String ? $String(m.scheduledTimestampMs) : m.scheduledTimestampMs;
                    else
                        d.scheduledTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.scheduledTimestampMs) : o.longs === $Number ? new $util.LongBits(m.scheduledTimestampMs.low >>> 0, m.scheduledTimestampMs.high >>> 0).toNumber() : m.scheduledTimestampMs;
                }
                if (m.callType != null && $Object.hasOwnProperty.call(m, "callType")) {
                    d.callType = o.enums === $String ? $root.proto.Message.ScheduledCallCreationMessage.CallType[m.callType] === $undefined ? m.callType : $root.proto.Message.ScheduledCallCreationMessage.CallType[m.callType] : m.callType;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                return d;
            };

            ScheduledCallCreationMessage.prototype.toJSON = function() {
                return ScheduledCallCreationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ScheduledCallCreationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ScheduledCallCreationMessage";
            };

            ScheduledCallCreationMessage.CallType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "VOICE"] = 1;
                values[valuesById[2] = "VIDEO"] = 2;
                return values;
            })();

            return ScheduledCallCreationMessage;
        })();

        Message.ScheduledCallEditMessage = (function() {

            const ScheduledCallEditMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ScheduledCallEditMessage.prototype.key = null;
            ScheduledCallEditMessage.prototype.editType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ScheduledCallEditMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ScheduledCallEditMessage.prototype, "_editType", {
                get: $util.oneOfGetter($oneOfFields = ["editType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ScheduledCallEditMessage.create = function(properties) {
                return new ScheduledCallEditMessage(properties);
            };

            ScheduledCallEditMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.editType != null && $Object.hasOwnProperty.call(m, "editType"))
                    w.uint32(16).int32(m.editType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ScheduledCallEditMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.ScheduledCallEditMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.editType = r.int32();
                            m._editType = "editType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ScheduledCallEditMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.ScheduledCallEditMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.ScheduledCallEditMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.ScheduledCallEditMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.ScheduledCallEditMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                switch (d.editType) {
                case "UNKNOWN":
                case 0:
                    m.editType = 0;
                    break;
                case "CANCEL":
                case 1:
                    m.editType = 1;
                    break;
                default:
                    if (typeof d.editType === "number" && (d.editType | 0) === d.editType)
                        m.editType = d.editType;
                }
                return m;
            };

            ScheduledCallEditMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.editType != null && $Object.hasOwnProperty.call(m, "editType")) {
                    d.editType = o.enums === $String ? $root.proto.Message.ScheduledCallEditMessage.EditType[m.editType] === $undefined ? m.editType : $root.proto.Message.ScheduledCallEditMessage.EditType[m.editType] : m.editType;
                }
                return d;
            };

            ScheduledCallEditMessage.prototype.toJSON = function() {
                return ScheduledCallEditMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            ScheduledCallEditMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.ScheduledCallEditMessage";
            };

            ScheduledCallEditMessage.EditType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "CANCEL"] = 1;
                return values;
            })();

            return ScheduledCallEditMessage;
        })();

        Message.SecretEncryptedMessage = (function() {

            const SecretEncryptedMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SecretEncryptedMessage.prototype.targetMessageKey = null;
            SecretEncryptedMessage.prototype.encPayload = null;
            SecretEncryptedMessage.prototype.encIv = null;
            SecretEncryptedMessage.prototype.secretEncType = null;
            SecretEncryptedMessage.prototype.remoteKeyId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SecretEncryptedMessage.prototype, "_targetMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SecretEncryptedMessage.prototype, "_encPayload", {
                get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SecretEncryptedMessage.prototype, "_encIv", {
                get: $util.oneOfGetter($oneOfFields = ["encIv"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SecretEncryptedMessage.prototype, "_secretEncType", {
                get: $util.oneOfGetter($oneOfFields = ["secretEncType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SecretEncryptedMessage.prototype, "_remoteKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["remoteKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SecretEncryptedMessage.create = function(properties) {
                return new SecretEncryptedMessage(properties);
            };

            SecretEncryptedMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey"))
                    $root.proto.MessageKey.encode(m.targetMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                    w.uint32(18).bytes(m.encPayload);
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                    w.uint32(26).bytes(m.encIv);
                if (m.secretEncType != null && $Object.hasOwnProperty.call(m, "secretEncType"))
                    w.uint32(32).int32(m.secretEncType);
                if (m.remoteKeyId != null && $Object.hasOwnProperty.call(m, "remoteKeyId"))
                    w.uint32(42).string(m.remoteKeyId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SecretEncryptedMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.SecretEncryptedMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.targetMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.targetMessageKey);
                            m._targetMessageKey = "targetMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.encPayload = r.bytes();
                            m._encPayload = "encPayload";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.encIv = r.bytes();
                            m._encIv = "encIv";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.secretEncType = r.int32();
                            m._secretEncType = "secretEncType";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.remoteKeyId = r.stringVerify();
                            m._remoteKeyId = "remoteKeyId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SecretEncryptedMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.SecretEncryptedMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.SecretEncryptedMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.SecretEncryptedMessage();
                if (d.targetMessageKey != null) {
                    if (!$util.isObject(d.targetMessageKey))
                        throw $TypeError(".proto.Message.SecretEncryptedMessage.targetMessageKey: object expected");
                    m.targetMessageKey = $root.proto.MessageKey.fromObject(d.targetMessageKey, q + 1);
                }
                if (d.encPayload != null) {
                    if (typeof d.encPayload === "string")
                        $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                    else if (d.encPayload.length >= 0)
                        m.encPayload = d.encPayload;
                }
                if (d.encIv != null) {
                    if (typeof d.encIv === "string")
                        $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                    else if (d.encIv.length >= 0)
                        m.encIv = d.encIv;
                }
                switch (d.secretEncType) {
                case "UNKNOWN":
                case 0:
                    m.secretEncType = 0;
                    break;
                case "EVENT_EDIT":
                case 1:
                    m.secretEncType = 1;
                    break;
                case "MESSAGE_EDIT":
                case 2:
                    m.secretEncType = 2;
                    break;
                case "MESSAGE_SCHEDULE":
                case 3:
                    m.secretEncType = 3;
                    break;
                case "POLL_EDIT":
                case 4:
                    m.secretEncType = 4;
                    break;
                case "POLL_ADD_OPTION":
                case 5:
                    m.secretEncType = 5;
                    break;
                default:
                    if (typeof d.secretEncType === "number" && (d.secretEncType | 0) === d.secretEncType)
                        m.secretEncType = d.secretEncType;
                }
                if (d.remoteKeyId != null) {
                    m.remoteKeyId = $String(d.remoteKeyId);
                }
                return m;
            };

            SecretEncryptedMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey")) {
                    d.targetMessageKey = $root.proto.MessageKey.toObject(m.targetMessageKey, o, q + 1);
                }
                if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                    d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
                }
                if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                    d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
                }
                if (m.secretEncType != null && $Object.hasOwnProperty.call(m, "secretEncType")) {
                    d.secretEncType = o.enums === $String ? $root.proto.Message.SecretEncryptedMessage.SecretEncType[m.secretEncType] === $undefined ? m.secretEncType : $root.proto.Message.SecretEncryptedMessage.SecretEncType[m.secretEncType] : m.secretEncType;
                }
                if (m.remoteKeyId != null && $Object.hasOwnProperty.call(m, "remoteKeyId")) {
                    d.remoteKeyId = m.remoteKeyId;
                }
                return d;
            };

            SecretEncryptedMessage.prototype.toJSON = function() {
                return SecretEncryptedMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            SecretEncryptedMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.SecretEncryptedMessage";
            };

            SecretEncryptedMessage.SecretEncType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "EVENT_EDIT"] = 1;
                values[valuesById[2] = "MESSAGE_EDIT"] = 2;
                values[valuesById[3] = "MESSAGE_SCHEDULE"] = 3;
                values[valuesById[4] = "POLL_EDIT"] = 4;
                values[valuesById[5] = "POLL_ADD_OPTION"] = 5;
                return values;
            })();

            return SecretEncryptedMessage;
        })();

        Message.SendPaymentMessage = (function() {

            const SendPaymentMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SendPaymentMessage.prototype.noteMessage = null;
            SendPaymentMessage.prototype.requestMessageKey = null;
            SendPaymentMessage.prototype.background = null;
            SendPaymentMessage.prototype.transactionData = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SendPaymentMessage.prototype, "_noteMessage", {
                get: $util.oneOfGetter($oneOfFields = ["noteMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SendPaymentMessage.prototype, "_requestMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SendPaymentMessage.prototype, "_background", {
                get: $util.oneOfGetter($oneOfFields = ["background"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SendPaymentMessage.prototype, "_transactionData", {
                get: $util.oneOfGetter($oneOfFields = ["transactionData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SendPaymentMessage.create = function(properties) {
                return new SendPaymentMessage(properties);
            };

            SendPaymentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.noteMessage != null && $Object.hasOwnProperty.call(m, "noteMessage"))
                    $root.proto.Message.encode(m.noteMessage, w.uint32(18).fork(), q + 1).ldelim();
                if (m.requestMessageKey != null && $Object.hasOwnProperty.call(m, "requestMessageKey"))
                    $root.proto.MessageKey.encode(m.requestMessageKey, w.uint32(26).fork(), q + 1).ldelim();
                if (m.background != null && $Object.hasOwnProperty.call(m, "background"))
                    $root.proto.PaymentBackground.encode(m.background, w.uint32(34).fork(), q + 1).ldelim();
                if (m.transactionData != null && $Object.hasOwnProperty.call(m, "transactionData"))
                    w.uint32(42).string(m.transactionData);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SendPaymentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.SendPaymentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 2: {
                            if (u !== 2)
                                break;
                            m.noteMessage = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.noteMessage);
                            m._noteMessage = "noteMessage";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.requestMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.requestMessageKey);
                            m._requestMessageKey = "requestMessageKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.background = $root.proto.PaymentBackground.decode(r, r.uint32(), $undefined, q + 1, m.background);
                            m._background = "background";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.transactionData = r.stringVerify();
                            m._transactionData = "transactionData";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SendPaymentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.SendPaymentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.SendPaymentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.SendPaymentMessage();
                if (d.noteMessage != null) {
                    if (!$util.isObject(d.noteMessage))
                        throw $TypeError(".proto.Message.SendPaymentMessage.noteMessage: object expected");
                    m.noteMessage = $root.proto.Message.fromObject(d.noteMessage, q + 1);
                }
                if (d.requestMessageKey != null) {
                    if (!$util.isObject(d.requestMessageKey))
                        throw $TypeError(".proto.Message.SendPaymentMessage.requestMessageKey: object expected");
                    m.requestMessageKey = $root.proto.MessageKey.fromObject(d.requestMessageKey, q + 1);
                }
                if (d.background != null) {
                    if (!$util.isObject(d.background))
                        throw $TypeError(".proto.Message.SendPaymentMessage.background: object expected");
                    m.background = $root.proto.PaymentBackground.fromObject(d.background, q + 1);
                }
                if (d.transactionData != null) {
                    m.transactionData = $String(d.transactionData);
                }
                return m;
            };

            SendPaymentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.noteMessage != null && $Object.hasOwnProperty.call(m, "noteMessage")) {
                    d.noteMessage = $root.proto.Message.toObject(m.noteMessage, o, q + 1);
                }
                if (m.requestMessageKey != null && $Object.hasOwnProperty.call(m, "requestMessageKey")) {
                    d.requestMessageKey = $root.proto.MessageKey.toObject(m.requestMessageKey, o, q + 1);
                }
                if (m.background != null && $Object.hasOwnProperty.call(m, "background")) {
                    d.background = $root.proto.PaymentBackground.toObject(m.background, o, q + 1);
                }
                if (m.transactionData != null && $Object.hasOwnProperty.call(m, "transactionData")) {
                    d.transactionData = m.transactionData;
                }
                return d;
            };

            SendPaymentMessage.prototype.toJSON = function() {
                return SendPaymentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            SendPaymentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.SendPaymentMessage";
            };

            return SendPaymentMessage;
        })();

        Message.SenderKeyDistributionMessage = (function() {

            const SenderKeyDistributionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SenderKeyDistributionMessage.prototype.groupId = null;
            SenderKeyDistributionMessage.prototype.axolotlSenderKeyDistributionMessage = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderKeyDistributionMessage.prototype, "_groupId", {
                get: $util.oneOfGetter($oneOfFields = ["groupId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderKeyDistributionMessage.prototype, "_axolotlSenderKeyDistributionMessage", {
                get: $util.oneOfGetter($oneOfFields = ["axolotlSenderKeyDistributionMessage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SenderKeyDistributionMessage.create = function(properties) {
                return new SenderKeyDistributionMessage(properties);
            };

            SenderKeyDistributionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.groupId != null && $Object.hasOwnProperty.call(m, "groupId"))
                    w.uint32(10).string(m.groupId);
                if (m.axolotlSenderKeyDistributionMessage != null && $Object.hasOwnProperty.call(m, "axolotlSenderKeyDistributionMessage"))
                    w.uint32(18).bytes(m.axolotlSenderKeyDistributionMessage);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SenderKeyDistributionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.SenderKeyDistributionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.groupId = r.stringVerify();
                            m._groupId = "groupId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.axolotlSenderKeyDistributionMessage = r.bytes();
                            m._axolotlSenderKeyDistributionMessage = "axolotlSenderKeyDistributionMessage";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SenderKeyDistributionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.SenderKeyDistributionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.SenderKeyDistributionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.SenderKeyDistributionMessage();
                if (d.groupId != null) {
                    m.groupId = $String(d.groupId);
                }
                if (d.axolotlSenderKeyDistributionMessage != null) {
                    if (typeof d.axolotlSenderKeyDistributionMessage === "string")
                        $util.base64.decode(d.axolotlSenderKeyDistributionMessage, m.axolotlSenderKeyDistributionMessage = $util.newBuffer($util.base64.length(d.axolotlSenderKeyDistributionMessage)), 0);
                    else if (d.axolotlSenderKeyDistributionMessage.length >= 0)
                        m.axolotlSenderKeyDistributionMessage = d.axolotlSenderKeyDistributionMessage;
                }
                return m;
            };

            SenderKeyDistributionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.groupId != null && $Object.hasOwnProperty.call(m, "groupId")) {
                    d.groupId = m.groupId;
                }
                if (m.axolotlSenderKeyDistributionMessage != null && $Object.hasOwnProperty.call(m, "axolotlSenderKeyDistributionMessage")) {
                    d.axolotlSenderKeyDistributionMessage = o.bytes === $String ? $util.base64.encode(m.axolotlSenderKeyDistributionMessage, 0, m.axolotlSenderKeyDistributionMessage.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.axolotlSenderKeyDistributionMessage) : m.axolotlSenderKeyDistributionMessage;
                }
                return d;
            };

            SenderKeyDistributionMessage.prototype.toJSON = function() {
                return SenderKeyDistributionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            SenderKeyDistributionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.SenderKeyDistributionMessage";
            };

            return SenderKeyDistributionMessage;
        })();

        Message.SplitPaymentMessage = (function() {

            const SplitPaymentMessage = function (p) {
                this.participants = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SplitPaymentMessage.prototype.splitId = null;
            SplitPaymentMessage.prototype.totalAmount = null;
            SplitPaymentMessage.prototype.description = null;
            SplitPaymentMessage.prototype.requesterJid = null;
            SplitPaymentMessage.prototype.participants = $util.emptyArray;
            SplitPaymentMessage.prototype.createdAtMs = null;
            SplitPaymentMessage.prototype.contextInfo = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentMessage.prototype, "_splitId", {
                get: $util.oneOfGetter($oneOfFields = ["splitId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentMessage.prototype, "_totalAmount", {
                get: $util.oneOfGetter($oneOfFields = ["totalAmount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentMessage.prototype, "_description", {
                get: $util.oneOfGetter($oneOfFields = ["description"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentMessage.prototype, "_requesterJid", {
                get: $util.oneOfGetter($oneOfFields = ["requesterJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentMessage.prototype, "_createdAtMs", {
                get: $util.oneOfGetter($oneOfFields = ["createdAtMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SplitPaymentMessage.create = function(properties) {
                return new SplitPaymentMessage(properties);
            };

            SplitPaymentMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.splitId != null && $Object.hasOwnProperty.call(m, "splitId"))
                    w.uint32(10).string(m.splitId);
                if (m.totalAmount != null && $Object.hasOwnProperty.call(m, "totalAmount"))
                    $root.proto.Money.encode(m.totalAmount, w.uint32(18).fork(), q + 1).ldelim();
                if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                    w.uint32(26).string(m.description);
                if (m.requesterJid != null && $Object.hasOwnProperty.call(m, "requesterJid"))
                    w.uint32(34).string(m.requesterJid);
                if (m.participants != null && m.participants.length) {
                    for (var i = 0; i < m.participants.length; ++i)
                        $root.proto.Message.SplitPaymentParticipant.encode(m.participants[i], w.uint32(42).fork(), q + 1).ldelim();
                }
                if (m.createdAtMs != null && $Object.hasOwnProperty.call(m, "createdAtMs"))
                    w.uint32(48).int64(m.createdAtMs);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SplitPaymentMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.SplitPaymentMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.splitId = r.stringVerify();
                            m._splitId = "splitId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.totalAmount = $root.proto.Money.decode(r, r.uint32(), $undefined, q + 1, m.totalAmount);
                            m._totalAmount = "totalAmount";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.description = r.stringVerify();
                            m._description = "description";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.requesterJid = r.stringVerify();
                            m._requesterJid = "requesterJid";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(m.participants && m.participants.length))
                                m.participants = [];
                            m.participants.push($root.proto.Message.SplitPaymentParticipant.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.createdAtMs = r.int64();
                            m._createdAtMs = "createdAtMs";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SplitPaymentMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.SplitPaymentMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.SplitPaymentMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.SplitPaymentMessage();
                if (d.splitId != null) {
                    m.splitId = $String(d.splitId);
                }
                if (d.totalAmount != null) {
                    if (!$util.isObject(d.totalAmount))
                        throw $TypeError(".proto.Message.SplitPaymentMessage.totalAmount: object expected");
                    m.totalAmount = $root.proto.Money.fromObject(d.totalAmount, q + 1);
                }
                if (d.description != null) {
                    m.description = $String(d.description);
                }
                if (d.requesterJid != null) {
                    m.requesterJid = $String(d.requesterJid);
                }
                if (d.participants) {
                    if (!$Array.isArray(d.participants))
                        throw $TypeError(".proto.Message.SplitPaymentMessage.participants: array expected");
                    m.participants = $Array(d.participants.length);
                    for (var i = 0; i < d.participants.length; ++i) {
                        if (!$util.isObject(d.participants[i]))
                            throw $TypeError(".proto.Message.SplitPaymentMessage.participants: object expected");
                        m.participants[i] = $root.proto.Message.SplitPaymentParticipant.fromObject(d.participants[i], q + 1);
                    }
                }
                if (d.createdAtMs != null) {
                    if ($util.Long)
                        m.createdAtMs = $util.Long.fromValue(d.createdAtMs, false);
                    else if (typeof d.createdAtMs === "string")
                        m.createdAtMs = $parseInt(d.createdAtMs, 10);
                    else if (typeof d.createdAtMs === "number")
                        m.createdAtMs = d.createdAtMs;
                    else if (typeof d.createdAtMs === "object")
                        m.createdAtMs = new $util.LongBits(d.createdAtMs.low >>> 0, d.createdAtMs.high >>> 0).toNumber();
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.SplitPaymentMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                return m;
            };

            SplitPaymentMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.participants = [];
                }
                if (m.splitId != null && $Object.hasOwnProperty.call(m, "splitId")) {
                    d.splitId = m.splitId;
                }
                if (m.totalAmount != null && $Object.hasOwnProperty.call(m, "totalAmount")) {
                    d.totalAmount = $root.proto.Money.toObject(m.totalAmount, o, q + 1);
                }
                if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                    d.description = m.description;
                }
                if (m.requesterJid != null && $Object.hasOwnProperty.call(m, "requesterJid")) {
                    d.requesterJid = m.requesterJid;
                }
                if (m.participants && m.participants.length) {
                    d.participants = $Array(m.participants.length);
                    for (var j = 0; j < m.participants.length; ++j) {
                        d.participants[j] = $root.proto.Message.SplitPaymentParticipant.toObject(m.participants[j], o, q + 1);
                    }
                }
                if (m.createdAtMs != null && $Object.hasOwnProperty.call(m, "createdAtMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.createdAtMs = typeof m.createdAtMs === "number" ? $BigInt(m.createdAtMs) : $util.Long.fromBits(m.createdAtMs.low >>> 0, m.createdAtMs.high >>> 0, false).toBigInt();
                    else if (typeof m.createdAtMs === "number")
                        d.createdAtMs = o.longs === $String ? $String(m.createdAtMs) : m.createdAtMs;
                    else
                        d.createdAtMs = o.longs === $String ? $util.Long.prototype.toString.call(m.createdAtMs) : o.longs === $Number ? new $util.LongBits(m.createdAtMs.low >>> 0, m.createdAtMs.high >>> 0).toNumber() : m.createdAtMs;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                return d;
            };

            SplitPaymentMessage.prototype.toJSON = function() {
                return SplitPaymentMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            SplitPaymentMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.SplitPaymentMessage";
            };

            return SplitPaymentMessage;
        })();

        Message.SplitPaymentParticipant = (function() {

            const SplitPaymentParticipant = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SplitPaymentParticipant.prototype.jid = null;
            SplitPaymentParticipant.prototype.amount = null;
            SplitPaymentParticipant.prototype.status = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentParticipant.prototype, "_jid", {
                get: $util.oneOfGetter($oneOfFields = ["jid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentParticipant.prototype, "_amount", {
                get: $util.oneOfGetter($oneOfFields = ["amount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentParticipant.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SplitPaymentParticipant.create = function(properties) {
                return new SplitPaymentParticipant(properties);
            };

            SplitPaymentParticipant.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.jid != null && $Object.hasOwnProperty.call(m, "jid"))
                    w.uint32(10).string(m.jid);
                if (m.amount != null && $Object.hasOwnProperty.call(m, "amount"))
                    $root.proto.Money.encode(m.amount, w.uint32(18).fork(), q + 1).ldelim();
                if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                    w.uint32(24).int32(m.status);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SplitPaymentParticipant.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.SplitPaymentParticipant(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.jid = r.stringVerify();
                            m._jid = "jid";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.amount = $root.proto.Money.decode(r, r.uint32(), $undefined, q + 1, m.amount);
                            m._amount = "amount";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            m._status = "status";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SplitPaymentParticipant.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.SplitPaymentParticipant)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.SplitPaymentParticipant: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.SplitPaymentParticipant();
                if (d.jid != null) {
                    m.jid = $String(d.jid);
                }
                if (d.amount != null) {
                    if (!$util.isObject(d.amount))
                        throw $TypeError(".proto.Message.SplitPaymentParticipant.amount: object expected");
                    m.amount = $root.proto.Money.fromObject(d.amount, q + 1);
                }
                switch (d.status) {
                case "PENDING":
                case 0:
                    m.status = 0;
                    break;
                case "PAID":
                case 1:
                    m.status = 1;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                return m;
            };

            SplitPaymentParticipant.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.jid != null && $Object.hasOwnProperty.call(m, "jid")) {
                    d.jid = m.jid;
                }
                if (m.amount != null && $Object.hasOwnProperty.call(m, "amount")) {
                    d.amount = $root.proto.Money.toObject(m.amount, o, q + 1);
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.Message.SplitPaymentParticipant.SplitPaymentStatus[m.status] === $undefined ? m.status : $root.proto.Message.SplitPaymentParticipant.SplitPaymentStatus[m.status] : m.status;
                }
                return d;
            };

            SplitPaymentParticipant.prototype.toJSON = function() {
                return SplitPaymentParticipant.toObject(this, $protobuf.util.toJSONOptions);
            };

            SplitPaymentParticipant.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.SplitPaymentParticipant";
            };

            SplitPaymentParticipant.SplitPaymentStatus = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "PENDING"] = 0;
                values[valuesById[1] = "PAID"] = 1;
                return values;
            })();

            return SplitPaymentParticipant;
        })();

        Message.SplitPaymentUpdateMessage = (function() {

            const SplitPaymentUpdateMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SplitPaymentUpdateMessage.prototype.splitId = null;
            SplitPaymentUpdateMessage.prototype.participantJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentUpdateMessage.prototype, "_splitId", {
                get: $util.oneOfGetter($oneOfFields = ["splitId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SplitPaymentUpdateMessage.prototype, "_participantJid", {
                get: $util.oneOfGetter($oneOfFields = ["participantJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SplitPaymentUpdateMessage.create = function(properties) {
                return new SplitPaymentUpdateMessage(properties);
            };

            SplitPaymentUpdateMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.splitId != null && $Object.hasOwnProperty.call(m, "splitId"))
                    w.uint32(10).string(m.splitId);
                if (m.participantJid != null && $Object.hasOwnProperty.call(m, "participantJid"))
                    w.uint32(18).string(m.participantJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SplitPaymentUpdateMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.SplitPaymentUpdateMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.splitId = r.stringVerify();
                            m._splitId = "splitId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.participantJid = r.stringVerify();
                            m._participantJid = "participantJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SplitPaymentUpdateMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.SplitPaymentUpdateMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.SplitPaymentUpdateMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.SplitPaymentUpdateMessage();
                if (d.splitId != null) {
                    m.splitId = $String(d.splitId);
                }
                if (d.participantJid != null) {
                    m.participantJid = $String(d.participantJid);
                }
                return m;
            };

            SplitPaymentUpdateMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.splitId != null && $Object.hasOwnProperty.call(m, "splitId")) {
                    d.splitId = m.splitId;
                }
                if (m.participantJid != null && $Object.hasOwnProperty.call(m, "participantJid")) {
                    d.participantJid = m.participantJid;
                }
                return d;
            };

            SplitPaymentUpdateMessage.prototype.toJSON = function() {
                return SplitPaymentUpdateMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            SplitPaymentUpdateMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.SplitPaymentUpdateMessage";
            };

            return SplitPaymentUpdateMessage;
        })();

        Message.StatusLinkPreviewMetadata = (function() {

            const StatusLinkPreviewMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusLinkPreviewMetadata.prototype.style = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusLinkPreviewMetadata.prototype, "_style", {
                get: $util.oneOfGetter($oneOfFields = ["style"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusLinkPreviewMetadata.create = function(properties) {
                return new StatusLinkPreviewMetadata(properties);
            };

            StatusLinkPreviewMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.style != null && $Object.hasOwnProperty.call(m, "style"))
                    w.uint32(8).int32(m.style);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusLinkPreviewMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StatusLinkPreviewMetadata(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.style = r.int32();
                            m._style = "style";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusLinkPreviewMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StatusLinkPreviewMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StatusLinkPreviewMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StatusLinkPreviewMetadata();
                switch (d.style) {
                case "AUTO":
                case 0:
                    m.style = 0;
                    break;
                case "COMPACT":
                case 1:
                    m.style = 1;
                    break;
                case "FULL":
                case 2:
                    m.style = 2;
                    break;
                case "IMMERSIVE":
                case 3:
                    m.style = 3;
                    break;
                default:
                    if (typeof d.style === "number" && (d.style | 0) === d.style)
                        m.style = d.style;
                }
                return m;
            };

            StatusLinkPreviewMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.style != null && $Object.hasOwnProperty.call(m, "style")) {
                    d.style = o.enums === $String ? $root.proto.Message.StatusLinkPreviewMetadata.Style[m.style] === $undefined ? m.style : $root.proto.Message.StatusLinkPreviewMetadata.Style[m.style] : m.style;
                }
                return d;
            };

            StatusLinkPreviewMetadata.prototype.toJSON = function() {
                return StatusLinkPreviewMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusLinkPreviewMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StatusLinkPreviewMetadata";
            };

            StatusLinkPreviewMetadata.Style = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "AUTO"] = 0;
                values[valuesById[1] = "COMPACT"] = 1;
                values[valuesById[2] = "FULL"] = 2;
                values[valuesById[3] = "IMMERSIVE"] = 3;
                return values;
            })();

            return StatusLinkPreviewMetadata;
        })();

        Message.StatusNotificationMessage = (function() {

            const StatusNotificationMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusNotificationMessage.prototype.responseMessageKey = null;
            StatusNotificationMessage.prototype.originalMessageKey = null;
            StatusNotificationMessage.prototype.type = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusNotificationMessage.prototype, "_responseMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["responseMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusNotificationMessage.prototype, "_originalMessageKey", {
                get: $util.oneOfGetter($oneOfFields = ["originalMessageKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusNotificationMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusNotificationMessage.create = function(properties) {
                return new StatusNotificationMessage(properties);
            };

            StatusNotificationMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.responseMessageKey != null && $Object.hasOwnProperty.call(m, "responseMessageKey"))
                    $root.proto.MessageKey.encode(m.responseMessageKey, w.uint32(10).fork(), q + 1).ldelim();
                if (m.originalMessageKey != null && $Object.hasOwnProperty.call(m, "originalMessageKey"))
                    $root.proto.MessageKey.encode(m.originalMessageKey, w.uint32(18).fork(), q + 1).ldelim();
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(24).int32(m.type);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusNotificationMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StatusNotificationMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.responseMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.responseMessageKey);
                            m._responseMessageKey = "responseMessageKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.originalMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.originalMessageKey);
                            m._originalMessageKey = "originalMessageKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusNotificationMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StatusNotificationMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StatusNotificationMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StatusNotificationMessage();
                if (d.responseMessageKey != null) {
                    if (!$util.isObject(d.responseMessageKey))
                        throw $TypeError(".proto.Message.StatusNotificationMessage.responseMessageKey: object expected");
                    m.responseMessageKey = $root.proto.MessageKey.fromObject(d.responseMessageKey, q + 1);
                }
                if (d.originalMessageKey != null) {
                    if (!$util.isObject(d.originalMessageKey))
                        throw $TypeError(".proto.Message.StatusNotificationMessage.originalMessageKey: object expected");
                    m.originalMessageKey = $root.proto.MessageKey.fromObject(d.originalMessageKey, q + 1);
                }
                switch (d.type) {
                case "UNKNOWN":
                case 0:
                    m.type = 0;
                    break;
                case "STATUS_ADD_YOURS":
                case 1:
                    m.type = 1;
                    break;
                case "STATUS_RESHARE":
                case 2:
                    m.type = 2;
                    break;
                case "STATUS_QUESTION_ANSWER_RESHARE":
                case 3:
                    m.type = 3;
                    break;
                case "STATUS_GROUP_STATUS_REPLY":
                case 4:
                    m.type = 4;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                return m;
            };

            StatusNotificationMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.responseMessageKey != null && $Object.hasOwnProperty.call(m, "responseMessageKey")) {
                    d.responseMessageKey = $root.proto.MessageKey.toObject(m.responseMessageKey, o, q + 1);
                }
                if (m.originalMessageKey != null && $Object.hasOwnProperty.call(m, "originalMessageKey")) {
                    d.originalMessageKey = $root.proto.MessageKey.toObject(m.originalMessageKey, o, q + 1);
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.StatusNotificationMessage.StatusNotificationType[m.type] === $undefined ? m.type : $root.proto.Message.StatusNotificationMessage.StatusNotificationType[m.type] : m.type;
                }
                return d;
            };

            StatusNotificationMessage.prototype.toJSON = function() {
                return StatusNotificationMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusNotificationMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StatusNotificationMessage";
            };

            StatusNotificationMessage.StatusNotificationType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "STATUS_ADD_YOURS"] = 1;
                values[valuesById[2] = "STATUS_RESHARE"] = 2;
                values[valuesById[3] = "STATUS_QUESTION_ANSWER_RESHARE"] = 3;
                values[valuesById[4] = "STATUS_GROUP_STATUS_REPLY"] = 4;
                return values;
            })();

            return StatusNotificationMessage;
        })();

        Message.StatusQuestionAnswerMessage = (function() {

            const StatusQuestionAnswerMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusQuestionAnswerMessage.prototype.key = null;
            StatusQuestionAnswerMessage.prototype.text = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusQuestionAnswerMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusQuestionAnswerMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusQuestionAnswerMessage.create = function(properties) {
                return new StatusQuestionAnswerMessage(properties);
            };

            StatusQuestionAnswerMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(18).string(m.text);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusQuestionAnswerMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StatusQuestionAnswerMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusQuestionAnswerMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StatusQuestionAnswerMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StatusQuestionAnswerMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StatusQuestionAnswerMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.StatusQuestionAnswerMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                return m;
            };

            StatusQuestionAnswerMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                return d;
            };

            StatusQuestionAnswerMessage.prototype.toJSON = function() {
                return StatusQuestionAnswerMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusQuestionAnswerMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StatusQuestionAnswerMessage";
            };

            return StatusQuestionAnswerMessage;
        })();

        Message.StatusQuotedMessage = (function() {

            const StatusQuotedMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusQuotedMessage.prototype.type = null;
            StatusQuotedMessage.prototype.text = null;
            StatusQuotedMessage.prototype.thumbnail = null;
            StatusQuotedMessage.prototype.originalStatusId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusQuotedMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusQuotedMessage.prototype, "_text", {
                get: $util.oneOfGetter($oneOfFields = ["text"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusQuotedMessage.prototype, "_thumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusQuotedMessage.prototype, "_originalStatusId", {
                get: $util.oneOfGetter($oneOfFields = ["originalStatusId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusQuotedMessage.create = function(properties) {
                return new StatusQuotedMessage(properties);
            };

            StatusQuotedMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                    w.uint32(18).string(m.text);
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail"))
                    w.uint32(26).bytes(m.thumbnail);
                if (m.originalStatusId != null && $Object.hasOwnProperty.call(m, "originalStatusId"))
                    $root.proto.MessageKey.encode(m.originalStatusId, w.uint32(34).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusQuotedMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StatusQuotedMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.text = r.stringVerify();
                            m._text = "text";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.thumbnail = r.bytes();
                            m._thumbnail = "thumbnail";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.originalStatusId = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.originalStatusId);
                            m._originalStatusId = "originalStatusId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusQuotedMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StatusQuotedMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StatusQuotedMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StatusQuotedMessage();
                switch (d.type) {
                case "QUESTION_ANSWER":
                case 1:
                    m.type = 1;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.text != null) {
                    m.text = $String(d.text);
                }
                if (d.thumbnail != null) {
                    if (typeof d.thumbnail === "string")
                        $util.base64.decode(d.thumbnail, m.thumbnail = $util.newBuffer($util.base64.length(d.thumbnail)), 0);
                    else if (d.thumbnail.length >= 0)
                        m.thumbnail = d.thumbnail;
                }
                if (d.originalStatusId != null) {
                    if (!$util.isObject(d.originalStatusId))
                        throw $TypeError(".proto.Message.StatusQuotedMessage.originalStatusId: object expected");
                    m.originalStatusId = $root.proto.MessageKey.fromObject(d.originalStatusId, q + 1);
                }
                return m;
            };

            StatusQuotedMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.StatusQuotedMessage.StatusQuotedMessageType[m.type] === $undefined ? m.type : $root.proto.Message.StatusQuotedMessage.StatusQuotedMessageType[m.type] : m.type;
                }
                if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                    d.text = m.text;
                }
                if (m.thumbnail != null && $Object.hasOwnProperty.call(m, "thumbnail")) {
                    d.thumbnail = o.bytes === $String ? $util.base64.encode(m.thumbnail, 0, m.thumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnail) : m.thumbnail;
                }
                if (m.originalStatusId != null && $Object.hasOwnProperty.call(m, "originalStatusId")) {
                    d.originalStatusId = $root.proto.MessageKey.toObject(m.originalStatusId, o, q + 1);
                }
                return d;
            };

            StatusQuotedMessage.prototype.toJSON = function() {
                return StatusQuotedMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusQuotedMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StatusQuotedMessage";
            };

            StatusQuotedMessage.StatusQuotedMessageType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "QUESTION_ANSWER"] = 1;
                return values;
            })();

            return StatusQuotedMessage;
        })();

        Message.StatusStickerInteractionMessage = (function() {

            const StatusStickerInteractionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusStickerInteractionMessage.prototype.key = null;
            StatusStickerInteractionMessage.prototype.stickerKey = null;
            StatusStickerInteractionMessage.prototype.type = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusStickerInteractionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusStickerInteractionMessage.prototype, "_stickerKey", {
                get: $util.oneOfGetter($oneOfFields = ["stickerKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusStickerInteractionMessage.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusStickerInteractionMessage.create = function(properties) {
                return new StatusStickerInteractionMessage(properties);
            };

            StatusStickerInteractionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.stickerKey != null && $Object.hasOwnProperty.call(m, "stickerKey"))
                    w.uint32(18).string(m.stickerKey);
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(24).int32(m.type);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusStickerInteractionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StatusStickerInteractionMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.stickerKey = r.stringVerify();
                            m._stickerKey = "stickerKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusStickerInteractionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StatusStickerInteractionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StatusStickerInteractionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StatusStickerInteractionMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.Message.StatusStickerInteractionMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.stickerKey != null) {
                    m.stickerKey = $String(d.stickerKey);
                }
                switch (d.type) {
                case "UNKNOWN":
                case 0:
                    m.type = 0;
                    break;
                case "REACTION":
                case 1:
                    m.type = 1;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                return m;
            };

            StatusStickerInteractionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.stickerKey != null && $Object.hasOwnProperty.call(m, "stickerKey")) {
                    d.stickerKey = m.stickerKey;
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.Message.StatusStickerInteractionMessage.StatusStickerType[m.type] === $undefined ? m.type : $root.proto.Message.StatusStickerInteractionMessage.StatusStickerType[m.type] : m.type;
                }
                return d;
            };

            StatusStickerInteractionMessage.prototype.toJSON = function() {
                return StatusStickerInteractionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusStickerInteractionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StatusStickerInteractionMessage";
            };

            StatusStickerInteractionMessage.StatusStickerType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "REACTION"] = 1;
                return values;
            })();

            return StatusStickerInteractionMessage;
        })();

        Message.StickerMessage = (function() {

            const StickerMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StickerMessage.prototype.url = null;
            StickerMessage.prototype.fileSha256 = null;
            StickerMessage.prototype.fileEncSha256 = null;
            StickerMessage.prototype.mediaKey = null;
            StickerMessage.prototype.mimetype = null;
            StickerMessage.prototype.height = null;
            StickerMessage.prototype.width = null;
            StickerMessage.prototype.directPath = null;
            StickerMessage.prototype.fileLength = null;
            StickerMessage.prototype.mediaKeyTimestamp = null;
            StickerMessage.prototype.firstFrameLength = null;
            StickerMessage.prototype.firstFrameSidecar = null;
            StickerMessage.prototype.isAnimated = null;
            StickerMessage.prototype.pngThumbnail = null;
            StickerMessage.prototype.contextInfo = null;
            StickerMessage.prototype.stickerSentTs = null;
            StickerMessage.prototype.isAvatar = null;
            StickerMessage.prototype.isAiSticker = null;
            StickerMessage.prototype.isLottie = null;
            StickerMessage.prototype.accessibilityLabel = null;
            StickerMessage.prototype.premium = null;
            StickerMessage.prototype.emojis = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_firstFrameLength", {
                get: $util.oneOfGetter($oneOfFields = ["firstFrameLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_firstFrameSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["firstFrameSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_isAnimated", {
                get: $util.oneOfGetter($oneOfFields = ["isAnimated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_pngThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["pngThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_stickerSentTs", {
                get: $util.oneOfGetter($oneOfFields = ["stickerSentTs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_isAvatar", {
                get: $util.oneOfGetter($oneOfFields = ["isAvatar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_isAiSticker", {
                get: $util.oneOfGetter($oneOfFields = ["isAiSticker"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_isLottie", {
                get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_premium", {
                get: $util.oneOfGetter($oneOfFields = ["premium"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerMessage.prototype, "_emojis", {
                get: $util.oneOfGetter($oneOfFields = ["emojis"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StickerMessage.create = function(properties) {
                return new StickerMessage(properties);
            };

            StickerMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(18).bytes(m.fileSha256);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(26).bytes(m.fileEncSha256);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(34).bytes(m.mediaKey);
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(42).string(m.mimetype);
                if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                    w.uint32(48).uint32(m.height);
                if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                    w.uint32(56).uint32(m.width);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(66).string(m.directPath);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(72).uint64(m.fileLength);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(80).int64(m.mediaKeyTimestamp);
                if (m.firstFrameLength != null && $Object.hasOwnProperty.call(m, "firstFrameLength"))
                    w.uint32(88).uint32(m.firstFrameLength);
                if (m.firstFrameSidecar != null && $Object.hasOwnProperty.call(m, "firstFrameSidecar"))
                    w.uint32(98).bytes(m.firstFrameSidecar);
                if (m.isAnimated != null && $Object.hasOwnProperty.call(m, "isAnimated"))
                    w.uint32(104).bool(m.isAnimated);
                if (m.pngThumbnail != null && $Object.hasOwnProperty.call(m, "pngThumbnail"))
                    w.uint32(130).bytes(m.pngThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.stickerSentTs != null && $Object.hasOwnProperty.call(m, "stickerSentTs"))
                    w.uint32(144).int64(m.stickerSentTs);
                if (m.isAvatar != null && $Object.hasOwnProperty.call(m, "isAvatar"))
                    w.uint32(152).bool(m.isAvatar);
                if (m.isAiSticker != null && $Object.hasOwnProperty.call(m, "isAiSticker"))
                    w.uint32(160).bool(m.isAiSticker);
                if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie"))
                    w.uint32(168).bool(m.isLottie);
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel"))
                    w.uint32(178).string(m.accessibilityLabel);
                if (m.premium != null && $Object.hasOwnProperty.call(m, "premium"))
                    w.uint32(192).int32(m.premium);
                if (m.emojis != null && $Object.hasOwnProperty.call(m, "emojis"))
                    w.uint32(202).string(m.emojis);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StickerMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StickerMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.height = r.uint32();
                            m._height = "height";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.width = r.uint32();
                            m._width = "width";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.firstFrameLength = r.uint32();
                            m._firstFrameLength = "firstFrameLength";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.firstFrameSidecar = r.bytes();
                            m._firstFrameSidecar = "firstFrameSidecar";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            m.isAnimated = r.bool();
                            m._isAnimated = "isAnimated";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.pngThumbnail = r.bytes();
                            m._pngThumbnail = "pngThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.stickerSentTs = r.int64();
                            m._stickerSentTs = "stickerSentTs";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.isAvatar = r.bool();
                            m._isAvatar = "isAvatar";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            m.isAiSticker = r.bool();
                            m._isAiSticker = "isAiSticker";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            m.isLottie = r.bool();
                            m._isLottie = "isLottie";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            m.accessibilityLabel = r.stringVerify();
                            m._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            m.premium = r.int32();
                            m._premium = "premium";
                            continue;
                        }
                    case 25: {
                            if (u !== 2)
                                break;
                            m.emojis = r.stringVerify();
                            m._emojis = "emojis";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StickerMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StickerMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StickerMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StickerMessage();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.height != null) {
                    m.height = d.height >>> 0;
                }
                if (d.width != null) {
                    m.width = d.width >>> 0;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.firstFrameLength != null) {
                    m.firstFrameLength = d.firstFrameLength >>> 0;
                }
                if (d.firstFrameSidecar != null) {
                    if (typeof d.firstFrameSidecar === "string")
                        $util.base64.decode(d.firstFrameSidecar, m.firstFrameSidecar = $util.newBuffer($util.base64.length(d.firstFrameSidecar)), 0);
                    else if (d.firstFrameSidecar.length >= 0)
                        m.firstFrameSidecar = d.firstFrameSidecar;
                }
                if (d.isAnimated != null) {
                    m.isAnimated = $Boolean(d.isAnimated);
                }
                if (d.pngThumbnail != null) {
                    if (typeof d.pngThumbnail === "string")
                        $util.base64.decode(d.pngThumbnail, m.pngThumbnail = $util.newBuffer($util.base64.length(d.pngThumbnail)), 0);
                    else if (d.pngThumbnail.length >= 0)
                        m.pngThumbnail = d.pngThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.StickerMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.stickerSentTs != null) {
                    if ($util.Long)
                        m.stickerSentTs = $util.Long.fromValue(d.stickerSentTs, false);
                    else if (typeof d.stickerSentTs === "string")
                        m.stickerSentTs = $parseInt(d.stickerSentTs, 10);
                    else if (typeof d.stickerSentTs === "number")
                        m.stickerSentTs = d.stickerSentTs;
                    else if (typeof d.stickerSentTs === "object")
                        m.stickerSentTs = new $util.LongBits(d.stickerSentTs.low >>> 0, d.stickerSentTs.high >>> 0).toNumber();
                }
                if (d.isAvatar != null) {
                    m.isAvatar = $Boolean(d.isAvatar);
                }
                if (d.isAiSticker != null) {
                    m.isAiSticker = $Boolean(d.isAiSticker);
                }
                if (d.isLottie != null) {
                    m.isLottie = $Boolean(d.isLottie);
                }
                if (d.accessibilityLabel != null) {
                    m.accessibilityLabel = $String(d.accessibilityLabel);
                }
                if (d.premium != null) {
                    m.premium = d.premium | 0;
                }
                if (d.emojis != null) {
                    m.emojis = $String(d.emojis);
                }
                return m;
            };

            StickerMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                    d.height = m.height;
                }
                if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                    d.width = m.width;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.firstFrameLength != null && $Object.hasOwnProperty.call(m, "firstFrameLength")) {
                    d.firstFrameLength = m.firstFrameLength;
                }
                if (m.firstFrameSidecar != null && $Object.hasOwnProperty.call(m, "firstFrameSidecar")) {
                    d.firstFrameSidecar = o.bytes === $String ? $util.base64.encode(m.firstFrameSidecar, 0, m.firstFrameSidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.firstFrameSidecar) : m.firstFrameSidecar;
                }
                if (m.isAnimated != null && $Object.hasOwnProperty.call(m, "isAnimated")) {
                    d.isAnimated = m.isAnimated;
                }
                if (m.pngThumbnail != null && $Object.hasOwnProperty.call(m, "pngThumbnail")) {
                    d.pngThumbnail = o.bytes === $String ? $util.base64.encode(m.pngThumbnail, 0, m.pngThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.pngThumbnail) : m.pngThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.stickerSentTs != null && $Object.hasOwnProperty.call(m, "stickerSentTs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.stickerSentTs = typeof m.stickerSentTs === "number" ? $BigInt(m.stickerSentTs) : $util.Long.fromBits(m.stickerSentTs.low >>> 0, m.stickerSentTs.high >>> 0, false).toBigInt();
                    else if (typeof m.stickerSentTs === "number")
                        d.stickerSentTs = o.longs === $String ? $String(m.stickerSentTs) : m.stickerSentTs;
                    else
                        d.stickerSentTs = o.longs === $String ? $util.Long.prototype.toString.call(m.stickerSentTs) : o.longs === $Number ? new $util.LongBits(m.stickerSentTs.low >>> 0, m.stickerSentTs.high >>> 0).toNumber() : m.stickerSentTs;
                }
                if (m.isAvatar != null && $Object.hasOwnProperty.call(m, "isAvatar")) {
                    d.isAvatar = m.isAvatar;
                }
                if (m.isAiSticker != null && $Object.hasOwnProperty.call(m, "isAiSticker")) {
                    d.isAiSticker = m.isAiSticker;
                }
                if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie")) {
                    d.isLottie = m.isLottie;
                }
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel")) {
                    d.accessibilityLabel = m.accessibilityLabel;
                }
                if (m.premium != null && $Object.hasOwnProperty.call(m, "premium")) {
                    d.premium = m.premium;
                }
                if (m.emojis != null && $Object.hasOwnProperty.call(m, "emojis")) {
                    d.emojis = m.emojis;
                }
                return d;
            };

            StickerMessage.prototype.toJSON = function() {
                return StickerMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StickerMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StickerMessage";
            };

            return StickerMessage;
        })();

        Message.StickerPackMessage = (function() {

            const StickerPackMessage = function (p) {
                this.stickers = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StickerPackMessage.prototype.stickerPackId = null;
            StickerPackMessage.prototype.name = null;
            StickerPackMessage.prototype.publisher = null;
            StickerPackMessage.prototype.stickers = $util.emptyArray;
            StickerPackMessage.prototype.fileLength = null;
            StickerPackMessage.prototype.fileSha256 = null;
            StickerPackMessage.prototype.fileEncSha256 = null;
            StickerPackMessage.prototype.mediaKey = null;
            StickerPackMessage.prototype.directPath = null;
            StickerPackMessage.prototype.caption = null;
            StickerPackMessage.prototype.contextInfo = null;
            StickerPackMessage.prototype.packDescription = null;
            StickerPackMessage.prototype.mediaKeyTimestamp = null;
            StickerPackMessage.prototype.trayIconFileName = null;
            StickerPackMessage.prototype.thumbnailDirectPath = null;
            StickerPackMessage.prototype.thumbnailSha256 = null;
            StickerPackMessage.prototype.thumbnailEncSha256 = null;
            StickerPackMessage.prototype.thumbnailHeight = null;
            StickerPackMessage.prototype.thumbnailWidth = null;
            StickerPackMessage.prototype.imageDataHash = null;
            StickerPackMessage.prototype.stickerPackSize = null;
            StickerPackMessage.prototype.stickerPackOrigin = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_stickerPackId", {
                get: $util.oneOfGetter($oneOfFields = ["stickerPackId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_publisher", {
                get: $util.oneOfGetter($oneOfFields = ["publisher"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_packDescription", {
                get: $util.oneOfGetter($oneOfFields = ["packDescription"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_trayIconFileName", {
                get: $util.oneOfGetter($oneOfFields = ["trayIconFileName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_thumbnailHeight", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailHeight"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_thumbnailWidth", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailWidth"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_imageDataHash", {
                get: $util.oneOfGetter($oneOfFields = ["imageDataHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_stickerPackSize", {
                get: $util.oneOfGetter($oneOfFields = ["stickerPackSize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerPackMessage.prototype, "_stickerPackOrigin", {
                get: $util.oneOfGetter($oneOfFields = ["stickerPackOrigin"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StickerPackMessage.create = function(properties) {
                return new StickerPackMessage(properties);
            };

            StickerPackMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.stickerPackId != null && $Object.hasOwnProperty.call(m, "stickerPackId"))
                    w.uint32(10).string(m.stickerPackId);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(18).string(m.name);
                if (m.publisher != null && $Object.hasOwnProperty.call(m, "publisher"))
                    w.uint32(26).string(m.publisher);
                if (m.stickers != null && m.stickers.length) {
                    for (var i = 0; i < m.stickers.length; ++i)
                        $root.proto.Message.StickerPackMessage.Sticker.encode(m.stickers[i], w.uint32(34).fork(), q + 1).ldelim();
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(40).uint64(m.fileLength);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(50).bytes(m.fileSha256);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(58).bytes(m.fileEncSha256);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(66).bytes(m.mediaKey);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(74).string(m.directPath);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(82).string(m.caption);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(90).fork(), q + 1).ldelim();
                if (m.packDescription != null && $Object.hasOwnProperty.call(m, "packDescription"))
                    w.uint32(98).string(m.packDescription);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(104).int64(m.mediaKeyTimestamp);
                if (m.trayIconFileName != null && $Object.hasOwnProperty.call(m, "trayIconFileName"))
                    w.uint32(114).string(m.trayIconFileName);
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                    w.uint32(122).string(m.thumbnailDirectPath);
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256"))
                    w.uint32(130).bytes(m.thumbnailSha256);
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256"))
                    w.uint32(138).bytes(m.thumbnailEncSha256);
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight"))
                    w.uint32(144).uint32(m.thumbnailHeight);
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth"))
                    w.uint32(152).uint32(m.thumbnailWidth);
                if (m.imageDataHash != null && $Object.hasOwnProperty.call(m, "imageDataHash"))
                    w.uint32(162).string(m.imageDataHash);
                if (m.stickerPackSize != null && $Object.hasOwnProperty.call(m, "stickerPackSize"))
                    w.uint32(168).uint64(m.stickerPackSize);
                if (m.stickerPackOrigin != null && $Object.hasOwnProperty.call(m, "stickerPackOrigin"))
                    w.uint32(176).int32(m.stickerPackOrigin);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StickerPackMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StickerPackMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.stickerPackId = r.stringVerify();
                            m._stickerPackId = "stickerPackId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.publisher = r.stringVerify();
                            m._publisher = "publisher";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(m.stickers && m.stickers.length))
                                m.stickers = [];
                            m.stickers.push($root.proto.Message.StickerPackMessage.Sticker.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 10: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.packDescription = r.stringVerify();
                            m._packDescription = "packDescription";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 14: {
                            if (u !== 2)
                                break;
                            m.trayIconFileName = r.stringVerify();
                            m._trayIconFileName = "trayIconFileName";
                            continue;
                        }
                    case 15: {
                            if (u !== 2)
                                break;
                            m.thumbnailDirectPath = r.stringVerify();
                            m._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.thumbnailSha256 = r.bytes();
                            m._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.thumbnailEncSha256 = r.bytes();
                            m._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.thumbnailHeight = r.uint32();
                            m._thumbnailHeight = "thumbnailHeight";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.thumbnailWidth = r.uint32();
                            m._thumbnailWidth = "thumbnailWidth";
                            continue;
                        }
                    case 20: {
                            if (u !== 2)
                                break;
                            m.imageDataHash = r.stringVerify();
                            m._imageDataHash = "imageDataHash";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            m.stickerPackSize = r.uint64();
                            m._stickerPackSize = "stickerPackSize";
                            continue;
                        }
                    case 22: {
                            if (u !== 0)
                                break;
                            m.stickerPackOrigin = r.int32();
                            m._stickerPackOrigin = "stickerPackOrigin";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StickerPackMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StickerPackMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StickerPackMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StickerPackMessage();
                if (d.stickerPackId != null) {
                    m.stickerPackId = $String(d.stickerPackId);
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.publisher != null) {
                    m.publisher = $String(d.publisher);
                }
                if (d.stickers) {
                    if (!$Array.isArray(d.stickers))
                        throw $TypeError(".proto.Message.StickerPackMessage.stickers: array expected");
                    m.stickers = $Array(d.stickers.length);
                    for (var i = 0; i < d.stickers.length; ++i) {
                        if (!$util.isObject(d.stickers[i]))
                            throw $TypeError(".proto.Message.StickerPackMessage.stickers: object expected");
                        m.stickers[i] = $root.proto.Message.StickerPackMessage.Sticker.fromObject(d.stickers[i], q + 1);
                    }
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.StickerPackMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.packDescription != null) {
                    m.packDescription = $String(d.packDescription);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.trayIconFileName != null) {
                    m.trayIconFileName = $String(d.trayIconFileName);
                }
                if (d.thumbnailDirectPath != null) {
                    m.thumbnailDirectPath = $String(d.thumbnailDirectPath);
                }
                if (d.thumbnailSha256 != null) {
                    if (typeof d.thumbnailSha256 === "string")
                        $util.base64.decode(d.thumbnailSha256, m.thumbnailSha256 = $util.newBuffer($util.base64.length(d.thumbnailSha256)), 0);
                    else if (d.thumbnailSha256.length >= 0)
                        m.thumbnailSha256 = d.thumbnailSha256;
                }
                if (d.thumbnailEncSha256 != null) {
                    if (typeof d.thumbnailEncSha256 === "string")
                        $util.base64.decode(d.thumbnailEncSha256, m.thumbnailEncSha256 = $util.newBuffer($util.base64.length(d.thumbnailEncSha256)), 0);
                    else if (d.thumbnailEncSha256.length >= 0)
                        m.thumbnailEncSha256 = d.thumbnailEncSha256;
                }
                if (d.thumbnailHeight != null) {
                    m.thumbnailHeight = d.thumbnailHeight >>> 0;
                }
                if (d.thumbnailWidth != null) {
                    m.thumbnailWidth = d.thumbnailWidth >>> 0;
                }
                if (d.imageDataHash != null) {
                    m.imageDataHash = $String(d.imageDataHash);
                }
                if (d.stickerPackSize != null) {
                    if ($util.Long)
                        m.stickerPackSize = $util.Long.fromValue(d.stickerPackSize, true);
                    else if (typeof d.stickerPackSize === "string")
                        m.stickerPackSize = $parseInt(d.stickerPackSize, 10);
                    else if (typeof d.stickerPackSize === "number")
                        m.stickerPackSize = d.stickerPackSize;
                    else if (typeof d.stickerPackSize === "object")
                        m.stickerPackSize = new $util.LongBits(d.stickerPackSize.low >>> 0, d.stickerPackSize.high >>> 0).toNumber(true);
                }
                switch (d.stickerPackOrigin) {
                case "FIRST_PARTY":
                case 0:
                    m.stickerPackOrigin = 0;
                    break;
                case "THIRD_PARTY":
                case 1:
                    m.stickerPackOrigin = 1;
                    break;
                case "USER_CREATED":
                case 2:
                    m.stickerPackOrigin = 2;
                    break;
                default:
                    if (typeof d.stickerPackOrigin === "number" && (d.stickerPackOrigin | 0) === d.stickerPackOrigin)
                        m.stickerPackOrigin = d.stickerPackOrigin;
                }
                return m;
            };

            StickerPackMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.stickers = [];
                }
                if (m.stickerPackId != null && $Object.hasOwnProperty.call(m, "stickerPackId")) {
                    d.stickerPackId = m.stickerPackId;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.publisher != null && $Object.hasOwnProperty.call(m, "publisher")) {
                    d.publisher = m.publisher;
                }
                if (m.stickers && m.stickers.length) {
                    d.stickers = $Array(m.stickers.length);
                    for (var j = 0; j < m.stickers.length; ++j) {
                        d.stickers[j] = $root.proto.Message.StickerPackMessage.Sticker.toObject(m.stickers[j], o, q + 1);
                    }
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.packDescription != null && $Object.hasOwnProperty.call(m, "packDescription")) {
                    d.packDescription = m.packDescription;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.trayIconFileName != null && $Object.hasOwnProperty.call(m, "trayIconFileName")) {
                    d.trayIconFileName = m.trayIconFileName;
                }
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath")) {
                    d.thumbnailDirectPath = m.thumbnailDirectPath;
                }
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256")) {
                    d.thumbnailSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailSha256, 0, m.thumbnailSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailSha256) : m.thumbnailSha256;
                }
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256")) {
                    d.thumbnailEncSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailEncSha256, 0, m.thumbnailEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailEncSha256) : m.thumbnailEncSha256;
                }
                if (m.thumbnailHeight != null && $Object.hasOwnProperty.call(m, "thumbnailHeight")) {
                    d.thumbnailHeight = m.thumbnailHeight;
                }
                if (m.thumbnailWidth != null && $Object.hasOwnProperty.call(m, "thumbnailWidth")) {
                    d.thumbnailWidth = m.thumbnailWidth;
                }
                if (m.imageDataHash != null && $Object.hasOwnProperty.call(m, "imageDataHash")) {
                    d.imageDataHash = m.imageDataHash;
                }
                if (m.stickerPackSize != null && $Object.hasOwnProperty.call(m, "stickerPackSize")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.stickerPackSize = typeof m.stickerPackSize === "number" ? $BigInt(m.stickerPackSize) : $util.Long.fromBits(m.stickerPackSize.low >>> 0, m.stickerPackSize.high >>> 0, true).toBigInt();
                    else if (typeof m.stickerPackSize === "number")
                        d.stickerPackSize = o.longs === $String ? $String(m.stickerPackSize) : m.stickerPackSize;
                    else
                        d.stickerPackSize = o.longs === $String ? $util.Long.prototype.toString.call(m.stickerPackSize) : o.longs === $Number ? new $util.LongBits(m.stickerPackSize.low >>> 0, m.stickerPackSize.high >>> 0).toNumber(true) : m.stickerPackSize;
                }
                if (m.stickerPackOrigin != null && $Object.hasOwnProperty.call(m, "stickerPackOrigin")) {
                    d.stickerPackOrigin = o.enums === $String ? $root.proto.Message.StickerPackMessage.StickerPackOrigin[m.stickerPackOrigin] === $undefined ? m.stickerPackOrigin : $root.proto.Message.StickerPackMessage.StickerPackOrigin[m.stickerPackOrigin] : m.stickerPackOrigin;
                }
                return d;
            };

            StickerPackMessage.prototype.toJSON = function() {
                return StickerPackMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StickerPackMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StickerPackMessage";
            };

            StickerPackMessage.Sticker = (function() {

                const Sticker = function (p) {
                    this.emojis = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Sticker.prototype.fileName = null;
                Sticker.prototype.isAnimated = null;
                Sticker.prototype.emojis = $util.emptyArray;
                Sticker.prototype.accessibilityLabel = null;
                Sticker.prototype.isLottie = null;
                Sticker.prototype.mimetype = null;
                Sticker.prototype.premium = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Sticker.prototype, "_fileName", {
                    get: $util.oneOfGetter($oneOfFields = ["fileName"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Sticker.prototype, "_isAnimated", {
                    get: $util.oneOfGetter($oneOfFields = ["isAnimated"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Sticker.prototype, "_accessibilityLabel", {
                    get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Sticker.prototype, "_isLottie", {
                    get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Sticker.prototype, "_mimetype", {
                    get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Sticker.prototype, "_premium", {
                    get: $util.oneOfGetter($oneOfFields = ["premium"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Sticker.create = function(properties) {
                    return new Sticker(properties);
                };

                Sticker.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName"))
                        w.uint32(10).string(m.fileName);
                    if (m.isAnimated != null && $Object.hasOwnProperty.call(m, "isAnimated"))
                        w.uint32(16).bool(m.isAnimated);
                    if (m.emojis != null && m.emojis.length) {
                        for (var i = 0; i < m.emojis.length; ++i)
                            w.uint32(26).string(m.emojis[i]);
                    }
                    if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel"))
                        w.uint32(34).string(m.accessibilityLabel);
                    if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie"))
                        w.uint32(40).bool(m.isLottie);
                    if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                        w.uint32(50).string(m.mimetype);
                    if (m.premium != null && $Object.hasOwnProperty.call(m, "premium"))
                        w.uint32(56).int32(m.premium);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Sticker.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StickerPackMessage.Sticker();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.fileName = r.stringVerify();
                                m._fileName = "fileName";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.isAnimated = r.bool();
                                m._isAnimated = "isAnimated";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                if (!(m.emojis && m.emojis.length))
                                    m.emojis = [];
                                m.emojis.push(r.stringVerify());
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.accessibilityLabel = r.stringVerify();
                                m._accessibilityLabel = "accessibilityLabel";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.isLottie = r.bool();
                                m._isLottie = "isLottie";
                                continue;
                            }
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.mimetype = r.stringVerify();
                                m._mimetype = "mimetype";
                                continue;
                            }
                        case 7: {
                                if (u !== 0)
                                    break;
                                m.premium = r.int32();
                                m._premium = "premium";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Sticker.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.StickerPackMessage.Sticker)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.StickerPackMessage.Sticker: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.StickerPackMessage.Sticker();
                    if (d.fileName != null) {
                        m.fileName = $String(d.fileName);
                    }
                    if (d.isAnimated != null) {
                        m.isAnimated = $Boolean(d.isAnimated);
                    }
                    if (d.emojis) {
                        if (!$Array.isArray(d.emojis))
                            throw $TypeError(".proto.Message.StickerPackMessage.Sticker.emojis: array expected");
                        m.emojis = $Array(d.emojis.length);
                        for (var i = 0; i < d.emojis.length; ++i) {
                            m.emojis[i] = $String(d.emojis[i]);
                        }
                    }
                    if (d.accessibilityLabel != null) {
                        m.accessibilityLabel = $String(d.accessibilityLabel);
                    }
                    if (d.isLottie != null) {
                        m.isLottie = $Boolean(d.isLottie);
                    }
                    if (d.mimetype != null) {
                        m.mimetype = $String(d.mimetype);
                    }
                    if (d.premium != null) {
                        m.premium = d.premium | 0;
                    }
                    return m;
                };

                Sticker.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.emojis = [];
                    }
                    if (m.fileName != null && $Object.hasOwnProperty.call(m, "fileName")) {
                        d.fileName = m.fileName;
                    }
                    if (m.isAnimated != null && $Object.hasOwnProperty.call(m, "isAnimated")) {
                        d.isAnimated = m.isAnimated;
                    }
                    if (m.emojis && m.emojis.length) {
                        d.emojis = $Array(m.emojis.length);
                        for (var j = 0; j < m.emojis.length; ++j) {
                            d.emojis[j] = m.emojis[j];
                        }
                    }
                    if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel")) {
                        d.accessibilityLabel = m.accessibilityLabel;
                    }
                    if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie")) {
                        d.isLottie = m.isLottie;
                    }
                    if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                        d.mimetype = m.mimetype;
                    }
                    if (m.premium != null && $Object.hasOwnProperty.call(m, "premium")) {
                        d.premium = m.premium;
                    }
                    return d;
                };

                Sticker.prototype.toJSON = function() {
                    return Sticker.toObject(this, $protobuf.util.toJSONOptions);
                };

                Sticker.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.StickerPackMessage.Sticker";
                };

                return Sticker;
            })();

            StickerPackMessage.StickerPackOrigin = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "FIRST_PARTY"] = 0;
                values[valuesById[1] = "THIRD_PARTY"] = 1;
                values[valuesById[2] = "USER_CREATED"] = 2;
                return values;
            })();

            return StickerPackMessage;
        })();

        Message.StickerSyncRMRMessage = (function() {

            const StickerSyncRMRMessage = function (p) {
                this.filehash = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StickerSyncRMRMessage.prototype.filehash = $util.emptyArray;
            StickerSyncRMRMessage.prototype.rmrSource = null;
            StickerSyncRMRMessage.prototype.requestTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerSyncRMRMessage.prototype, "_rmrSource", {
                get: $util.oneOfGetter($oneOfFields = ["rmrSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerSyncRMRMessage.prototype, "_requestTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["requestTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StickerSyncRMRMessage.create = function(properties) {
                return new StickerSyncRMRMessage(properties);
            };

            StickerSyncRMRMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.filehash != null && m.filehash.length) {
                    for (var i = 0; i < m.filehash.length; ++i)
                        w.uint32(10).string(m.filehash[i]);
                }
                if (m.rmrSource != null && $Object.hasOwnProperty.call(m, "rmrSource"))
                    w.uint32(18).string(m.rmrSource);
                if (m.requestTimestamp != null && $Object.hasOwnProperty.call(m, "requestTimestamp"))
                    w.uint32(24).int64(m.requestTimestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StickerSyncRMRMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.StickerSyncRMRMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.filehash && m.filehash.length))
                                m.filehash = [];
                            m.filehash.push(r.stringVerify());
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.rmrSource = r.stringVerify();
                            m._rmrSource = "rmrSource";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.requestTimestamp = r.int64();
                            m._requestTimestamp = "requestTimestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StickerSyncRMRMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.StickerSyncRMRMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.StickerSyncRMRMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.StickerSyncRMRMessage();
                if (d.filehash) {
                    if (!$Array.isArray(d.filehash))
                        throw $TypeError(".proto.Message.StickerSyncRMRMessage.filehash: array expected");
                    m.filehash = $Array(d.filehash.length);
                    for (var i = 0; i < d.filehash.length; ++i) {
                        m.filehash[i] = $String(d.filehash[i]);
                    }
                }
                if (d.rmrSource != null) {
                    m.rmrSource = $String(d.rmrSource);
                }
                if (d.requestTimestamp != null) {
                    if ($util.Long)
                        m.requestTimestamp = $util.Long.fromValue(d.requestTimestamp, false);
                    else if (typeof d.requestTimestamp === "string")
                        m.requestTimestamp = $parseInt(d.requestTimestamp, 10);
                    else if (typeof d.requestTimestamp === "number")
                        m.requestTimestamp = d.requestTimestamp;
                    else if (typeof d.requestTimestamp === "object")
                        m.requestTimestamp = new $util.LongBits(d.requestTimestamp.low >>> 0, d.requestTimestamp.high >>> 0).toNumber();
                }
                return m;
            };

            StickerSyncRMRMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.filehash = [];
                }
                if (m.filehash && m.filehash.length) {
                    d.filehash = $Array(m.filehash.length);
                    for (var j = 0; j < m.filehash.length; ++j) {
                        d.filehash[j] = m.filehash[j];
                    }
                }
                if (m.rmrSource != null && $Object.hasOwnProperty.call(m, "rmrSource")) {
                    d.rmrSource = m.rmrSource;
                }
                if (m.requestTimestamp != null && $Object.hasOwnProperty.call(m, "requestTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.requestTimestamp = typeof m.requestTimestamp === "number" ? $BigInt(m.requestTimestamp) : $util.Long.fromBits(m.requestTimestamp.low >>> 0, m.requestTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.requestTimestamp === "number")
                        d.requestTimestamp = o.longs === $String ? $String(m.requestTimestamp) : m.requestTimestamp;
                    else
                        d.requestTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.requestTimestamp) : o.longs === $Number ? new $util.LongBits(m.requestTimestamp.low >>> 0, m.requestTimestamp.high >>> 0).toNumber() : m.requestTimestamp;
                }
                return d;
            };

            StickerSyncRMRMessage.prototype.toJSON = function() {
                return StickerSyncRMRMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            StickerSyncRMRMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.StickerSyncRMRMessage";
            };

            return StickerSyncRMRMessage;
        })();

        Message.TemplateButtonReplyMessage = (function() {

            const TemplateButtonReplyMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            TemplateButtonReplyMessage.prototype.selectedId = null;
            TemplateButtonReplyMessage.prototype.selectedDisplayText = null;
            TemplateButtonReplyMessage.prototype.contextInfo = null;
            TemplateButtonReplyMessage.prototype.selectedIndex = null;
            TemplateButtonReplyMessage.prototype.selectedCarouselCardIndex = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedId", {
                get: $util.oneOfGetter($oneOfFields = ["selectedId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedDisplayText", {
                get: $util.oneOfGetter($oneOfFields = ["selectedDisplayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateButtonReplyMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedIndex", {
                get: $util.oneOfGetter($oneOfFields = ["selectedIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateButtonReplyMessage.prototype, "_selectedCarouselCardIndex", {
                get: $util.oneOfGetter($oneOfFields = ["selectedCarouselCardIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            TemplateButtonReplyMessage.create = function(properties) {
                return new TemplateButtonReplyMessage(properties);
            };

            TemplateButtonReplyMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.selectedId != null && $Object.hasOwnProperty.call(m, "selectedId"))
                    w.uint32(10).string(m.selectedId);
                if (m.selectedDisplayText != null && $Object.hasOwnProperty.call(m, "selectedDisplayText"))
                    w.uint32(18).string(m.selectedDisplayText);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(26).fork(), q + 1).ldelim();
                if (m.selectedIndex != null && $Object.hasOwnProperty.call(m, "selectedIndex"))
                    w.uint32(32).uint32(m.selectedIndex);
                if (m.selectedCarouselCardIndex != null && $Object.hasOwnProperty.call(m, "selectedCarouselCardIndex"))
                    w.uint32(40).uint32(m.selectedCarouselCardIndex);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            TemplateButtonReplyMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.TemplateButtonReplyMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.selectedId = r.stringVerify();
                            m._selectedId = "selectedId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.selectedDisplayText = r.stringVerify();
                            m._selectedDisplayText = "selectedDisplayText";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.selectedIndex = r.uint32();
                            m._selectedIndex = "selectedIndex";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.selectedCarouselCardIndex = r.uint32();
                            m._selectedCarouselCardIndex = "selectedCarouselCardIndex";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            TemplateButtonReplyMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.TemplateButtonReplyMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.TemplateButtonReplyMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.TemplateButtonReplyMessage();
                if (d.selectedId != null) {
                    m.selectedId = $String(d.selectedId);
                }
                if (d.selectedDisplayText != null) {
                    m.selectedDisplayText = $String(d.selectedDisplayText);
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.TemplateButtonReplyMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.selectedIndex != null) {
                    m.selectedIndex = d.selectedIndex >>> 0;
                }
                if (d.selectedCarouselCardIndex != null) {
                    m.selectedCarouselCardIndex = d.selectedCarouselCardIndex >>> 0;
                }
                return m;
            };

            TemplateButtonReplyMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.selectedId != null && $Object.hasOwnProperty.call(m, "selectedId")) {
                    d.selectedId = m.selectedId;
                }
                if (m.selectedDisplayText != null && $Object.hasOwnProperty.call(m, "selectedDisplayText")) {
                    d.selectedDisplayText = m.selectedDisplayText;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.selectedIndex != null && $Object.hasOwnProperty.call(m, "selectedIndex")) {
                    d.selectedIndex = m.selectedIndex;
                }
                if (m.selectedCarouselCardIndex != null && $Object.hasOwnProperty.call(m, "selectedCarouselCardIndex")) {
                    d.selectedCarouselCardIndex = m.selectedCarouselCardIndex;
                }
                return d;
            };

            TemplateButtonReplyMessage.prototype.toJSON = function() {
                return TemplateButtonReplyMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            TemplateButtonReplyMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.TemplateButtonReplyMessage";
            };

            return TemplateButtonReplyMessage;
        })();

        Message.TemplateMessage = (function() {

            const TemplateMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            TemplateMessage.prototype.contextInfo = null;
            TemplateMessage.prototype.hydratedTemplate = null;
            TemplateMessage.prototype.templateId = null;
            TemplateMessage.prototype.fourRowTemplate = null;
            TemplateMessage.prototype.hydratedFourRowTemplate = null;
            TemplateMessage.prototype.interactiveMessageTemplate = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateMessage.prototype, "_hydratedTemplate", {
                get: $util.oneOfGetter($oneOfFields = ["hydratedTemplate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TemplateMessage.prototype, "_templateId", {
                get: $util.oneOfGetter($oneOfFields = ["templateId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            $Object.defineProperty(TemplateMessage.prototype, "format", {
                get: $util.oneOfGetter($oneOfFields = ["fourRowTemplate", "hydratedFourRowTemplate", "interactiveMessageTemplate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            TemplateMessage.create = function(properties) {
                return new TemplateMessage(properties);
            };

            TemplateMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fourRowTemplate != null && $Object.hasOwnProperty.call(m, "fourRowTemplate"))
                    $root.proto.Message.TemplateMessage.FourRowTemplate.encode(m.fourRowTemplate, w.uint32(10).fork(), q + 1).ldelim();
                if (m.hydratedFourRowTemplate != null && $Object.hasOwnProperty.call(m, "hydratedFourRowTemplate"))
                    $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.encode(m.hydratedFourRowTemplate, w.uint32(18).fork(), q + 1).ldelim();
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(26).fork(), q + 1).ldelim();
                if (m.hydratedTemplate != null && $Object.hasOwnProperty.call(m, "hydratedTemplate"))
                    $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.encode(m.hydratedTemplate, w.uint32(34).fork(), q + 1).ldelim();
                if (m.interactiveMessageTemplate != null && $Object.hasOwnProperty.call(m, "interactiveMessageTemplate"))
                    $root.proto.Message.InteractiveMessage.encode(m.interactiveMessageTemplate, w.uint32(42).fork(), q + 1).ldelim();
                if (m.templateId != null && $Object.hasOwnProperty.call(m, "templateId"))
                    w.uint32(74).string(m.templateId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            TemplateMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.TemplateMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 3: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.hydratedTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.decode(r, r.uint32(), $undefined, q + 1, m.hydratedTemplate);
                            m._hydratedTemplate = "hydratedTemplate";
                            continue;
                        }
                    case 9: {
                            if (u !== 2)
                                break;
                            m.templateId = r.stringVerify();
                            m._templateId = "templateId";
                            continue;
                        }
                    case 1: {
                            if (u !== 2)
                                break;
                            m.fourRowTemplate = $root.proto.Message.TemplateMessage.FourRowTemplate.decode(r, r.uint32(), $undefined, q + 1, m.fourRowTemplate);
                            m.format = "fourRowTemplate";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.hydratedFourRowTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.decode(r, r.uint32(), $undefined, q + 1, m.hydratedFourRowTemplate);
                            m.format = "hydratedFourRowTemplate";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.interactiveMessageTemplate = $root.proto.Message.InteractiveMessage.decode(r, r.uint32(), $undefined, q + 1, m.interactiveMessageTemplate);
                            m.format = "interactiveMessageTemplate";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            TemplateMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.TemplateMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.TemplateMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.TemplateMessage();
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.TemplateMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.hydratedTemplate != null) {
                    if (!$util.isObject(d.hydratedTemplate))
                        throw $TypeError(".proto.Message.TemplateMessage.hydratedTemplate: object expected");
                    m.hydratedTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.fromObject(d.hydratedTemplate, q + 1);
                }
                if (d.templateId != null) {
                    m.templateId = $String(d.templateId);
                }
                if (d.fourRowTemplate != null) {
                    if (!$util.isObject(d.fourRowTemplate))
                        throw $TypeError(".proto.Message.TemplateMessage.fourRowTemplate: object expected");
                    m.fourRowTemplate = $root.proto.Message.TemplateMessage.FourRowTemplate.fromObject(d.fourRowTemplate, q + 1);
                }
                if (d.hydratedFourRowTemplate != null) {
                    if (!$util.isObject(d.hydratedFourRowTemplate))
                        throw $TypeError(".proto.Message.TemplateMessage.hydratedFourRowTemplate: object expected");
                    m.hydratedFourRowTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.fromObject(d.hydratedFourRowTemplate, q + 1);
                }
                if (d.interactiveMessageTemplate != null) {
                    if (!$util.isObject(d.interactiveMessageTemplate))
                        throw $TypeError(".proto.Message.TemplateMessage.interactiveMessageTemplate: object expected");
                    m.interactiveMessageTemplate = $root.proto.Message.InteractiveMessage.fromObject(d.interactiveMessageTemplate, q + 1);
                }
                return m;
            };

            TemplateMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fourRowTemplate != null && $Object.hasOwnProperty.call(m, "fourRowTemplate")) {
                    d.fourRowTemplate = $root.proto.Message.TemplateMessage.FourRowTemplate.toObject(m.fourRowTemplate, o, q + 1);
                    if (o.oneofs)
                        d.format = "fourRowTemplate";
                }
                if (m.hydratedFourRowTemplate != null && $Object.hasOwnProperty.call(m, "hydratedFourRowTemplate")) {
                    d.hydratedFourRowTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.toObject(m.hydratedFourRowTemplate, o, q + 1);
                    if (o.oneofs)
                        d.format = "hydratedFourRowTemplate";
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.hydratedTemplate != null && $Object.hasOwnProperty.call(m, "hydratedTemplate")) {
                    d.hydratedTemplate = $root.proto.Message.TemplateMessage.HydratedFourRowTemplate.toObject(m.hydratedTemplate, o, q + 1);
                }
                if (m.interactiveMessageTemplate != null && $Object.hasOwnProperty.call(m, "interactiveMessageTemplate")) {
                    d.interactiveMessageTemplate = $root.proto.Message.InteractiveMessage.toObject(m.interactiveMessageTemplate, o, q + 1);
                    if (o.oneofs)
                        d.format = "interactiveMessageTemplate";
                }
                if (m.templateId != null && $Object.hasOwnProperty.call(m, "templateId")) {
                    d.templateId = m.templateId;
                }
                return d;
            };

            TemplateMessage.prototype.toJSON = function() {
                return TemplateMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            TemplateMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.TemplateMessage";
            };

            TemplateMessage.FourRowTemplate = (function() {

                const FourRowTemplate = function (p) {
                    this.buttons = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                FourRowTemplate.prototype.content = null;
                FourRowTemplate.prototype.footer = null;
                FourRowTemplate.prototype.buttons = $util.emptyArray;
                FourRowTemplate.prototype.documentMessage = null;
                FourRowTemplate.prototype.highlyStructuredMessage = null;
                FourRowTemplate.prototype.imageMessage = null;
                FourRowTemplate.prototype.videoMessage = null;
                FourRowTemplate.prototype.locationMessage = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(FourRowTemplate.prototype, "_content", {
                    get: $util.oneOfGetter($oneOfFields = ["content"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(FourRowTemplate.prototype, "_footer", {
                    get: $util.oneOfGetter($oneOfFields = ["footer"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                $Object.defineProperty(FourRowTemplate.prototype, "title", {
                    get: $util.oneOfGetter($oneOfFields = ["documentMessage", "highlyStructuredMessage", "imageMessage", "videoMessage", "locationMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                FourRowTemplate.create = function(properties) {
                    return new FourRowTemplate(properties);
                };

                FourRowTemplate.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage"))
                        $root.proto.Message.DocumentMessage.encode(m.documentMessage, w.uint32(10).fork(), q + 1).ldelim();
                    if (m.highlyStructuredMessage != null && $Object.hasOwnProperty.call(m, "highlyStructuredMessage"))
                        $root.proto.Message.HighlyStructuredMessage.encode(m.highlyStructuredMessage, w.uint32(18).fork(), q + 1).ldelim();
                    if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                        $root.proto.Message.ImageMessage.encode(m.imageMessage, w.uint32(26).fork(), q + 1).ldelim();
                    if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                        $root.proto.Message.VideoMessage.encode(m.videoMessage, w.uint32(34).fork(), q + 1).ldelim();
                    if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage"))
                        $root.proto.Message.LocationMessage.encode(m.locationMessage, w.uint32(42).fork(), q + 1).ldelim();
                    if (m.content != null && $Object.hasOwnProperty.call(m, "content"))
                        $root.proto.Message.HighlyStructuredMessage.encode(m.content, w.uint32(50).fork(), q + 1).ldelim();
                    if (m.footer != null && $Object.hasOwnProperty.call(m, "footer"))
                        $root.proto.Message.HighlyStructuredMessage.encode(m.footer, w.uint32(58).fork(), q + 1).ldelim();
                    if (m.buttons != null && m.buttons.length) {
                        for (var i = 0; i < m.buttons.length; ++i)
                            $root.proto.TemplateButton.encode(m.buttons[i], w.uint32(66).fork(), q + 1).ldelim();
                    }
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                FourRowTemplate.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.TemplateMessage.FourRowTemplate();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.content = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.content);
                                m._content = "content";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.footer = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.footer);
                                m._footer = "footer";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                if (!(m.buttons && m.buttons.length))
                                    m.buttons = [];
                                m.buttons.push($root.proto.TemplateButton.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.documentMessage = $root.proto.Message.DocumentMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentMessage);
                                m.title = "documentMessage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.highlyStructuredMessage);
                                m.title = "highlyStructuredMessage";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.imageMessage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                                m.title = "imageMessage";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.videoMessage = $root.proto.Message.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                                m.title = "videoMessage";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.locationMessage = $root.proto.Message.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.locationMessage);
                                m.title = "locationMessage";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                FourRowTemplate.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.TemplateMessage.FourRowTemplate)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.TemplateMessage.FourRowTemplate();
                    if (d.content != null) {
                        if (!$util.isObject(d.content))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.content: object expected");
                        m.content = $root.proto.Message.HighlyStructuredMessage.fromObject(d.content, q + 1);
                    }
                    if (d.footer != null) {
                        if (!$util.isObject(d.footer))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.footer: object expected");
                        m.footer = $root.proto.Message.HighlyStructuredMessage.fromObject(d.footer, q + 1);
                    }
                    if (d.buttons) {
                        if (!$Array.isArray(d.buttons))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.buttons: array expected");
                        m.buttons = $Array(d.buttons.length);
                        for (var i = 0; i < d.buttons.length; ++i) {
                            if (!$util.isObject(d.buttons[i]))
                                throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.buttons: object expected");
                            m.buttons[i] = $root.proto.TemplateButton.fromObject(d.buttons[i], q + 1);
                        }
                    }
                    if (d.documentMessage != null) {
                        if (!$util.isObject(d.documentMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.documentMessage: object expected");
                        m.documentMessage = $root.proto.Message.DocumentMessage.fromObject(d.documentMessage, q + 1);
                    }
                    if (d.highlyStructuredMessage != null) {
                        if (!$util.isObject(d.highlyStructuredMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.highlyStructuredMessage: object expected");
                        m.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.fromObject(d.highlyStructuredMessage, q + 1);
                    }
                    if (d.imageMessage != null) {
                        if (!$util.isObject(d.imageMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.imageMessage: object expected");
                        m.imageMessage = $root.proto.Message.ImageMessage.fromObject(d.imageMessage, q + 1);
                    }
                    if (d.videoMessage != null) {
                        if (!$util.isObject(d.videoMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.videoMessage: object expected");
                        m.videoMessage = $root.proto.Message.VideoMessage.fromObject(d.videoMessage, q + 1);
                    }
                    if (d.locationMessage != null) {
                        if (!$util.isObject(d.locationMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.FourRowTemplate.locationMessage: object expected");
                        m.locationMessage = $root.proto.Message.LocationMessage.fromObject(d.locationMessage, q + 1);
                    }
                    return m;
                };

                FourRowTemplate.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.buttons = [];
                    }
                    if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage")) {
                        d.documentMessage = $root.proto.Message.DocumentMessage.toObject(m.documentMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "documentMessage";
                    }
                    if (m.highlyStructuredMessage != null && $Object.hasOwnProperty.call(m, "highlyStructuredMessage")) {
                        d.highlyStructuredMessage = $root.proto.Message.HighlyStructuredMessage.toObject(m.highlyStructuredMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "highlyStructuredMessage";
                    }
                    if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                        d.imageMessage = $root.proto.Message.ImageMessage.toObject(m.imageMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "imageMessage";
                    }
                    if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                        d.videoMessage = $root.proto.Message.VideoMessage.toObject(m.videoMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "videoMessage";
                    }
                    if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage")) {
                        d.locationMessage = $root.proto.Message.LocationMessage.toObject(m.locationMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "locationMessage";
                    }
                    if (m.content != null && $Object.hasOwnProperty.call(m, "content")) {
                        d.content = $root.proto.Message.HighlyStructuredMessage.toObject(m.content, o, q + 1);
                    }
                    if (m.footer != null && $Object.hasOwnProperty.call(m, "footer")) {
                        d.footer = $root.proto.Message.HighlyStructuredMessage.toObject(m.footer, o, q + 1);
                    }
                    if (m.buttons && m.buttons.length) {
                        d.buttons = $Array(m.buttons.length);
                        for (var j = 0; j < m.buttons.length; ++j) {
                            d.buttons[j] = $root.proto.TemplateButton.toObject(m.buttons[j], o, q + 1);
                        }
                    }
                    return d;
                };

                FourRowTemplate.prototype.toJSON = function() {
                    return FourRowTemplate.toObject(this, $protobuf.util.toJSONOptions);
                };

                FourRowTemplate.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.TemplateMessage.FourRowTemplate";
                };

                return FourRowTemplate;
            })();

            TemplateMessage.HydratedFourRowTemplate = (function() {

                const HydratedFourRowTemplate = function (p) {
                    this.hydratedButtons = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                HydratedFourRowTemplate.prototype.hydratedContentText = null;
                HydratedFourRowTemplate.prototype.hydratedFooterText = null;
                HydratedFourRowTemplate.prototype.hydratedButtons = $util.emptyArray;
                HydratedFourRowTemplate.prototype.templateId = null;
                HydratedFourRowTemplate.prototype.maskLinkedDevices = null;
                HydratedFourRowTemplate.prototype.documentMessage = null;
                HydratedFourRowTemplate.prototype.hydratedTitleText = null;
                HydratedFourRowTemplate.prototype.imageMessage = null;
                HydratedFourRowTemplate.prototype.videoMessage = null;
                HydratedFourRowTemplate.prototype.locationMessage = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HydratedFourRowTemplate.prototype, "_hydratedContentText", {
                    get: $util.oneOfGetter($oneOfFields = ["hydratedContentText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HydratedFourRowTemplate.prototype, "_hydratedFooterText", {
                    get: $util.oneOfGetter($oneOfFields = ["hydratedFooterText"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HydratedFourRowTemplate.prototype, "_templateId", {
                    get: $util.oneOfGetter($oneOfFields = ["templateId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(HydratedFourRowTemplate.prototype, "_maskLinkedDevices", {
                    get: $util.oneOfGetter($oneOfFields = ["maskLinkedDevices"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                $Object.defineProperty(HydratedFourRowTemplate.prototype, "title", {
                    get: $util.oneOfGetter($oneOfFields = ["documentMessage", "hydratedTitleText", "imageMessage", "videoMessage", "locationMessage"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                HydratedFourRowTemplate.create = function(properties) {
                    return new HydratedFourRowTemplate(properties);
                };

                HydratedFourRowTemplate.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage"))
                        $root.proto.Message.DocumentMessage.encode(m.documentMessage, w.uint32(10).fork(), q + 1).ldelim();
                    if (m.hydratedTitleText != null && $Object.hasOwnProperty.call(m, "hydratedTitleText"))
                        w.uint32(18).string(m.hydratedTitleText);
                    if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage"))
                        $root.proto.Message.ImageMessage.encode(m.imageMessage, w.uint32(26).fork(), q + 1).ldelim();
                    if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage"))
                        $root.proto.Message.VideoMessage.encode(m.videoMessage, w.uint32(34).fork(), q + 1).ldelim();
                    if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage"))
                        $root.proto.Message.LocationMessage.encode(m.locationMessage, w.uint32(42).fork(), q + 1).ldelim();
                    if (m.hydratedContentText != null && $Object.hasOwnProperty.call(m, "hydratedContentText"))
                        w.uint32(50).string(m.hydratedContentText);
                    if (m.hydratedFooterText != null && $Object.hasOwnProperty.call(m, "hydratedFooterText"))
                        w.uint32(58).string(m.hydratedFooterText);
                    if (m.hydratedButtons != null && m.hydratedButtons.length) {
                        for (var i = 0; i < m.hydratedButtons.length; ++i)
                            $root.proto.HydratedTemplateButton.encode(m.hydratedButtons[i], w.uint32(66).fork(), q + 1).ldelim();
                    }
                    if (m.templateId != null && $Object.hasOwnProperty.call(m, "templateId"))
                        w.uint32(74).string(m.templateId);
                    if (m.maskLinkedDevices != null && $Object.hasOwnProperty.call(m, "maskLinkedDevices"))
                        w.uint32(80).bool(m.maskLinkedDevices);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                HydratedFourRowTemplate.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.TemplateMessage.HydratedFourRowTemplate();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 6: {
                                if (u !== 2)
                                    break;
                                m.hydratedContentText = r.stringVerify();
                                m._hydratedContentText = "hydratedContentText";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.hydratedFooterText = r.stringVerify();
                                m._hydratedFooterText = "hydratedFooterText";
                                continue;
                            }
                        case 8: {
                                if (u !== 2)
                                    break;
                                if (!(m.hydratedButtons && m.hydratedButtons.length))
                                    m.hydratedButtons = [];
                                m.hydratedButtons.push($root.proto.HydratedTemplateButton.decode(r, r.uint32(), $undefined, q + 1));
                                continue;
                            }
                        case 9: {
                                if (u !== 2)
                                    break;
                                m.templateId = r.stringVerify();
                                m._templateId = "templateId";
                                continue;
                            }
                        case 10: {
                                if (u !== 0)
                                    break;
                                m.maskLinkedDevices = r.bool();
                                m._maskLinkedDevices = "maskLinkedDevices";
                                continue;
                            }
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.documentMessage = $root.proto.Message.DocumentMessage.decode(r, r.uint32(), $undefined, q + 1, m.documentMessage);
                                m.title = "documentMessage";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.hydratedTitleText = r.stringVerify();
                                m.title = "hydratedTitleText";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.imageMessage = $root.proto.Message.ImageMessage.decode(r, r.uint32(), $undefined, q + 1, m.imageMessage);
                                m.title = "imageMessage";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.videoMessage = $root.proto.Message.VideoMessage.decode(r, r.uint32(), $undefined, q + 1, m.videoMessage);
                                m.title = "videoMessage";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                m.locationMessage = $root.proto.Message.LocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.locationMessage);
                                m.title = "locationMessage";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                HydratedFourRowTemplate.fromObject = function (d, q) {
                    if (d instanceof $root.proto.Message.TemplateMessage.HydratedFourRowTemplate)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.Message.TemplateMessage.HydratedFourRowTemplate();
                    if (d.hydratedContentText != null) {
                        m.hydratedContentText = $String(d.hydratedContentText);
                    }
                    if (d.hydratedFooterText != null) {
                        m.hydratedFooterText = $String(d.hydratedFooterText);
                    }
                    if (d.hydratedButtons) {
                        if (!$Array.isArray(d.hydratedButtons))
                            throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.hydratedButtons: array expected");
                        m.hydratedButtons = $Array(d.hydratedButtons.length);
                        for (var i = 0; i < d.hydratedButtons.length; ++i) {
                            if (!$util.isObject(d.hydratedButtons[i]))
                                throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.hydratedButtons: object expected");
                            m.hydratedButtons[i] = $root.proto.HydratedTemplateButton.fromObject(d.hydratedButtons[i], q + 1);
                        }
                    }
                    if (d.templateId != null) {
                        m.templateId = $String(d.templateId);
                    }
                    if (d.maskLinkedDevices != null) {
                        m.maskLinkedDevices = $Boolean(d.maskLinkedDevices);
                    }
                    if (d.documentMessage != null) {
                        if (!$util.isObject(d.documentMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.documentMessage: object expected");
                        m.documentMessage = $root.proto.Message.DocumentMessage.fromObject(d.documentMessage, q + 1);
                    }
                    if (d.hydratedTitleText != null) {
                        m.hydratedTitleText = $String(d.hydratedTitleText);
                    }
                    if (d.imageMessage != null) {
                        if (!$util.isObject(d.imageMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.imageMessage: object expected");
                        m.imageMessage = $root.proto.Message.ImageMessage.fromObject(d.imageMessage, q + 1);
                    }
                    if (d.videoMessage != null) {
                        if (!$util.isObject(d.videoMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.videoMessage: object expected");
                        m.videoMessage = $root.proto.Message.VideoMessage.fromObject(d.videoMessage, q + 1);
                    }
                    if (d.locationMessage != null) {
                        if (!$util.isObject(d.locationMessage))
                            throw $TypeError(".proto.Message.TemplateMessage.HydratedFourRowTemplate.locationMessage: object expected");
                        m.locationMessage = $root.proto.Message.LocationMessage.fromObject(d.locationMessage, q + 1);
                    }
                    return m;
                };

                HydratedFourRowTemplate.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.hydratedButtons = [];
                    }
                    if (m.documentMessage != null && $Object.hasOwnProperty.call(m, "documentMessage")) {
                        d.documentMessage = $root.proto.Message.DocumentMessage.toObject(m.documentMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "documentMessage";
                    }
                    if (m.hydratedTitleText != null && $Object.hasOwnProperty.call(m, "hydratedTitleText")) {
                        d.hydratedTitleText = m.hydratedTitleText;
                        if (o.oneofs)
                            d.title = "hydratedTitleText";
                    }
                    if (m.imageMessage != null && $Object.hasOwnProperty.call(m, "imageMessage")) {
                        d.imageMessage = $root.proto.Message.ImageMessage.toObject(m.imageMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "imageMessage";
                    }
                    if (m.videoMessage != null && $Object.hasOwnProperty.call(m, "videoMessage")) {
                        d.videoMessage = $root.proto.Message.VideoMessage.toObject(m.videoMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "videoMessage";
                    }
                    if (m.locationMessage != null && $Object.hasOwnProperty.call(m, "locationMessage")) {
                        d.locationMessage = $root.proto.Message.LocationMessage.toObject(m.locationMessage, o, q + 1);
                        if (o.oneofs)
                            d.title = "locationMessage";
                    }
                    if (m.hydratedContentText != null && $Object.hasOwnProperty.call(m, "hydratedContentText")) {
                        d.hydratedContentText = m.hydratedContentText;
                    }
                    if (m.hydratedFooterText != null && $Object.hasOwnProperty.call(m, "hydratedFooterText")) {
                        d.hydratedFooterText = m.hydratedFooterText;
                    }
                    if (m.hydratedButtons && m.hydratedButtons.length) {
                        d.hydratedButtons = $Array(m.hydratedButtons.length);
                        for (var j = 0; j < m.hydratedButtons.length; ++j) {
                            d.hydratedButtons[j] = $root.proto.HydratedTemplateButton.toObject(m.hydratedButtons[j], o, q + 1);
                        }
                    }
                    if (m.templateId != null && $Object.hasOwnProperty.call(m, "templateId")) {
                        d.templateId = m.templateId;
                    }
                    if (m.maskLinkedDevices != null && $Object.hasOwnProperty.call(m, "maskLinkedDevices")) {
                        d.maskLinkedDevices = m.maskLinkedDevices;
                    }
                    return d;
                };

                HydratedFourRowTemplate.prototype.toJSON = function() {
                    return HydratedFourRowTemplate.toObject(this, $protobuf.util.toJSONOptions);
                };

                HydratedFourRowTemplate.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.Message.TemplateMessage.HydratedFourRowTemplate";
                };

                return HydratedFourRowTemplate;
            })();

            return TemplateMessage;
        })();

        Message.URLMetadata = (function() {

            const URLMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            URLMetadata.prototype.fbExperimentId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(URLMetadata.prototype, "_fbExperimentId", {
                get: $util.oneOfGetter($oneOfFields = ["fbExperimentId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            URLMetadata.create = function(properties) {
                return new URLMetadata(properties);
            };

            URLMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fbExperimentId != null && $Object.hasOwnProperty.call(m, "fbExperimentId"))
                    w.uint32(8).uint32(m.fbExperimentId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            URLMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.URLMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.fbExperimentId = r.uint32();
                            m._fbExperimentId = "fbExperimentId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            URLMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.URLMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.URLMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.URLMetadata();
                if (d.fbExperimentId != null) {
                    m.fbExperimentId = d.fbExperimentId >>> 0;
                }
                return m;
            };

            URLMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fbExperimentId != null && $Object.hasOwnProperty.call(m, "fbExperimentId")) {
                    d.fbExperimentId = m.fbExperimentId;
                }
                return d;
            };

            URLMetadata.prototype.toJSON = function() {
                return URLMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            URLMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.URLMetadata";
            };

            return URLMetadata;
        })();

        Message.VideoEndCard = (function() {

            const VideoEndCard = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            VideoEndCard.prototype.username = "";
            VideoEndCard.prototype.caption = "";
            VideoEndCard.prototype.thumbnailImageUrl = "";
            VideoEndCard.prototype.profilePictureUrl = "";

            VideoEndCard.create = function(properties) {
                return new VideoEndCard(properties);
            };

            VideoEndCard.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(10).string(m.username);
                w.uint32(18).string(m.caption);
                w.uint32(26).string(m.thumbnailImageUrl);
                w.uint32(34).string(m.profilePictureUrl);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            VideoEndCard.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.VideoEndCard();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.username = r.stringVerify();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.thumbnailImageUrl = r.stringVerify();
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.profilePictureUrl = r.stringVerify();
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "username"))
                    throw $util.ProtocolError("missing required 'username'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "caption"))
                    throw $util.ProtocolError("missing required 'caption'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "thumbnailImageUrl"))
                    throw $util.ProtocolError("missing required 'thumbnailImageUrl'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "profilePictureUrl"))
                    throw $util.ProtocolError("missing required 'profilePictureUrl'", { instance: m });
                return m;
            };

            VideoEndCard.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.VideoEndCard)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.VideoEndCard: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.VideoEndCard();
                if (d.username != null) {
                    m.username = $String(d.username);
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.thumbnailImageUrl != null) {
                    m.thumbnailImageUrl = $String(d.thumbnailImageUrl);
                }
                if (d.profilePictureUrl != null) {
                    m.profilePictureUrl = $String(d.profilePictureUrl);
                }
                return m;
            };

            VideoEndCard.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.defaults) {
                    d.username = "";
                    d.caption = "";
                    d.thumbnailImageUrl = "";
                    d.profilePictureUrl = "";
                }
                if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                    d.username = m.username;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.thumbnailImageUrl != null && $Object.hasOwnProperty.call(m, "thumbnailImageUrl")) {
                    d.thumbnailImageUrl = m.thumbnailImageUrl;
                }
                if (m.profilePictureUrl != null && $Object.hasOwnProperty.call(m, "profilePictureUrl")) {
                    d.profilePictureUrl = m.profilePictureUrl;
                }
                return d;
            };

            VideoEndCard.prototype.toJSON = function() {
                return VideoEndCard.toObject(this, $protobuf.util.toJSONOptions);
            };

            VideoEndCard.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.VideoEndCard";
            };

            return VideoEndCard;
        })();

        Message.VideoMessage = (function() {

            const VideoMessage = function (p) {
                this.interactiveAnnotations = [];
                this.annotations = [];
                this.processedVideos = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            VideoMessage.prototype.url = null;
            VideoMessage.prototype.mimetype = null;
            VideoMessage.prototype.fileSha256 = null;
            VideoMessage.prototype.fileLength = null;
            VideoMessage.prototype.seconds = null;
            VideoMessage.prototype.mediaKey = null;
            VideoMessage.prototype.caption = null;
            VideoMessage.prototype.gifPlayback = null;
            VideoMessage.prototype.height = null;
            VideoMessage.prototype.width = null;
            VideoMessage.prototype.fileEncSha256 = null;
            VideoMessage.prototype.interactiveAnnotations = $util.emptyArray;
            VideoMessage.prototype.directPath = null;
            VideoMessage.prototype.mediaKeyTimestamp = null;
            VideoMessage.prototype.jpegThumbnail = null;
            VideoMessage.prototype.contextInfo = null;
            VideoMessage.prototype.streamingSidecar = null;
            VideoMessage.prototype.gifAttribution = null;
            VideoMessage.prototype.viewOnce = null;
            VideoMessage.prototype.thumbnailDirectPath = null;
            VideoMessage.prototype.thumbnailSha256 = null;
            VideoMessage.prototype.thumbnailEncSha256 = null;
            VideoMessage.prototype.staticUrl = null;
            VideoMessage.prototype.annotations = $util.emptyArray;
            VideoMessage.prototype.accessibilityLabel = null;
            VideoMessage.prototype.processedVideos = $util.emptyArray;
            VideoMessage.prototype.externalShareFullVideoDurationInSeconds = null;
            VideoMessage.prototype.motionPhotoPresentationOffsetMs = null;
            VideoMessage.prototype.metadataUrl = null;
            VideoMessage.prototype.videoSourceType = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_seconds", {
                get: $util.oneOfGetter($oneOfFields = ["seconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_caption", {
                get: $util.oneOfGetter($oneOfFields = ["caption"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_gifPlayback", {
                get: $util.oneOfGetter($oneOfFields = ["gifPlayback"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_contextInfo", {
                get: $util.oneOfGetter($oneOfFields = ["contextInfo"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_streamingSidecar", {
                get: $util.oneOfGetter($oneOfFields = ["streamingSidecar"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_gifAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["gifAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_viewOnce", {
                get: $util.oneOfGetter($oneOfFields = ["viewOnce"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_thumbnailDirectPath", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailDirectPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_thumbnailSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_thumbnailEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["thumbnailEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_staticUrl", {
                get: $util.oneOfGetter($oneOfFields = ["staticUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_accessibilityLabel", {
                get: $util.oneOfGetter($oneOfFields = ["accessibilityLabel"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_externalShareFullVideoDurationInSeconds", {
                get: $util.oneOfGetter($oneOfFields = ["externalShareFullVideoDurationInSeconds"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_motionPhotoPresentationOffsetMs", {
                get: $util.oneOfGetter($oneOfFields = ["motionPhotoPresentationOffsetMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_metadataUrl", {
                get: $util.oneOfGetter($oneOfFields = ["metadataUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(VideoMessage.prototype, "_videoSourceType", {
                get: $util.oneOfGetter($oneOfFields = ["videoSourceType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            VideoMessage.create = function(properties) {
                return new VideoMessage(properties);
            };

            VideoMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(18).string(m.mimetype);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(26).bytes(m.fileSha256);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(32).uint64(m.fileLength);
                if (m.seconds != null && $Object.hasOwnProperty.call(m, "seconds"))
                    w.uint32(40).uint32(m.seconds);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(50).bytes(m.mediaKey);
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                    w.uint32(58).string(m.caption);
                if (m.gifPlayback != null && $Object.hasOwnProperty.call(m, "gifPlayback"))
                    w.uint32(64).bool(m.gifPlayback);
                if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                    w.uint32(72).uint32(m.height);
                if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                    w.uint32(80).uint32(m.width);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(90).bytes(m.fileEncSha256);
                if (m.interactiveAnnotations != null && m.interactiveAnnotations.length) {
                    for (var i = 0; i < m.interactiveAnnotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(m.interactiveAnnotations[i], w.uint32(98).fork(), q + 1).ldelim();
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(106).string(m.directPath);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(112).int64(m.mediaKeyTimestamp);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(130).bytes(m.jpegThumbnail);
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo"))
                    $root.proto.ContextInfo.encode(m.contextInfo, w.uint32(138).fork(), q + 1).ldelim();
                if (m.streamingSidecar != null && $Object.hasOwnProperty.call(m, "streamingSidecar"))
                    w.uint32(146).bytes(m.streamingSidecar);
                if (m.gifAttribution != null && $Object.hasOwnProperty.call(m, "gifAttribution"))
                    w.uint32(152).int32(m.gifAttribution);
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce"))
                    w.uint32(160).bool(m.viewOnce);
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath"))
                    w.uint32(170).string(m.thumbnailDirectPath);
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256"))
                    w.uint32(178).bytes(m.thumbnailSha256);
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256"))
                    w.uint32(186).bytes(m.thumbnailEncSha256);
                if (m.staticUrl != null && $Object.hasOwnProperty.call(m, "staticUrl"))
                    w.uint32(194).string(m.staticUrl);
                if (m.annotations != null && m.annotations.length) {
                    for (var i = 0; i < m.annotations.length; ++i)
                        $root.proto.InteractiveAnnotation.encode(m.annotations[i], w.uint32(202).fork(), q + 1).ldelim();
                }
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel"))
                    w.uint32(210).string(m.accessibilityLabel);
                if (m.processedVideos != null && m.processedVideos.length) {
                    for (var i = 0; i < m.processedVideos.length; ++i)
                        $root.proto.ProcessedVideo.encode(m.processedVideos[i], w.uint32(218).fork(), q + 1).ldelim();
                }
                if (m.externalShareFullVideoDurationInSeconds != null && $Object.hasOwnProperty.call(m, "externalShareFullVideoDurationInSeconds"))
                    w.uint32(224).uint32(m.externalShareFullVideoDurationInSeconds);
                if (m.motionPhotoPresentationOffsetMs != null && $Object.hasOwnProperty.call(m, "motionPhotoPresentationOffsetMs"))
                    w.uint32(232).uint64(m.motionPhotoPresentationOffsetMs);
                if (m.metadataUrl != null && $Object.hasOwnProperty.call(m, "metadataUrl"))
                    w.uint32(242).string(m.metadataUrl);
                if (m.videoSourceType != null && $Object.hasOwnProperty.call(m, "videoSourceType"))
                    w.uint32(248).int32(m.videoSourceType);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            VideoMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Message.VideoMessage(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.seconds = r.uint32();
                            m._seconds = "seconds";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.caption = r.stringVerify();
                            m._caption = "caption";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.gifPlayback = r.bool();
                            m._gifPlayback = "gifPlayback";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.height = r.uint32();
                            m._height = "height";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.width = r.uint32();
                            m._width = "width";
                            continue;
                        }
                    case 11: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            if (!(m.interactiveAnnotations && m.interactiveAnnotations.length))
                                m.interactiveAnnotations = [];
                            m.interactiveAnnotations.push($root.proto.InteractiveAnnotation.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 13: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 14: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 16: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    case 17: {
                            if (u !== 2)
                                break;
                            m.contextInfo = $root.proto.ContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.contextInfo);
                            m._contextInfo = "contextInfo";
                            continue;
                        }
                    case 18: {
                            if (u !== 2)
                                break;
                            m.streamingSidecar = r.bytes();
                            m._streamingSidecar = "streamingSidecar";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.gifAttribution = r.int32();
                            m._gifAttribution = "gifAttribution";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            m.viewOnce = r.bool();
                            m._viewOnce = "viewOnce";
                            continue;
                        }
                    case 21: {
                            if (u !== 2)
                                break;
                            m.thumbnailDirectPath = r.stringVerify();
                            m._thumbnailDirectPath = "thumbnailDirectPath";
                            continue;
                        }
                    case 22: {
                            if (u !== 2)
                                break;
                            m.thumbnailSha256 = r.bytes();
                            m._thumbnailSha256 = "thumbnailSha256";
                            continue;
                        }
                    case 23: {
                            if (u !== 2)
                                break;
                            m.thumbnailEncSha256 = r.bytes();
                            m._thumbnailEncSha256 = "thumbnailEncSha256";
                            continue;
                        }
                    case 24: {
                            if (u !== 2)
                                break;
                            m.staticUrl = r.stringVerify();
                            m._staticUrl = "staticUrl";
                            continue;
                        }
                    case 25: {
                            if (u !== 2)
                                break;
                            if (!(m.annotations && m.annotations.length))
                                m.annotations = [];
                            m.annotations.push($root.proto.InteractiveAnnotation.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 26: {
                            if (u !== 2)
                                break;
                            m.accessibilityLabel = r.stringVerify();
                            m._accessibilityLabel = "accessibilityLabel";
                            continue;
                        }
                    case 27: {
                            if (u !== 2)
                                break;
                            if (!(m.processedVideos && m.processedVideos.length))
                                m.processedVideos = [];
                            m.processedVideos.push($root.proto.ProcessedVideo.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 28: {
                            if (u !== 0)
                                break;
                            m.externalShareFullVideoDurationInSeconds = r.uint32();
                            m._externalShareFullVideoDurationInSeconds = "externalShareFullVideoDurationInSeconds";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            m.motionPhotoPresentationOffsetMs = r.uint64();
                            m._motionPhotoPresentationOffsetMs = "motionPhotoPresentationOffsetMs";
                            continue;
                        }
                    case 30: {
                            if (u !== 2)
                                break;
                            m.metadataUrl = r.stringVerify();
                            m._metadataUrl = "metadataUrl";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            m.videoSourceType = r.int32();
                            m._videoSourceType = "videoSourceType";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            VideoMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.Message.VideoMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.Message.VideoMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.Message.VideoMessage();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.seconds != null) {
                    m.seconds = d.seconds >>> 0;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.caption != null) {
                    m.caption = $String(d.caption);
                }
                if (d.gifPlayback != null) {
                    m.gifPlayback = $Boolean(d.gifPlayback);
                }
                if (d.height != null) {
                    m.height = d.height >>> 0;
                }
                if (d.width != null) {
                    m.width = d.width >>> 0;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.interactiveAnnotations) {
                    if (!$Array.isArray(d.interactiveAnnotations))
                        throw $TypeError(".proto.Message.VideoMessage.interactiveAnnotations: array expected");
                    m.interactiveAnnotations = $Array(d.interactiveAnnotations.length);
                    for (var i = 0; i < d.interactiveAnnotations.length; ++i) {
                        if (!$util.isObject(d.interactiveAnnotations[i]))
                            throw $TypeError(".proto.Message.VideoMessage.interactiveAnnotations: object expected");
                        m.interactiveAnnotations[i] = $root.proto.InteractiveAnnotation.fromObject(d.interactiveAnnotations[i], q + 1);
                    }
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                if (d.contextInfo != null) {
                    if (!$util.isObject(d.contextInfo))
                        throw $TypeError(".proto.Message.VideoMessage.contextInfo: object expected");
                    m.contextInfo = $root.proto.ContextInfo.fromObject(d.contextInfo, q + 1);
                }
                if (d.streamingSidecar != null) {
                    if (typeof d.streamingSidecar === "string")
                        $util.base64.decode(d.streamingSidecar, m.streamingSidecar = $util.newBuffer($util.base64.length(d.streamingSidecar)), 0);
                    else if (d.streamingSidecar.length >= 0)
                        m.streamingSidecar = d.streamingSidecar;
                }
                switch (d.gifAttribution) {
                case "NONE":
                case 0:
                    m.gifAttribution = 0;
                    break;
                case "GIPHY":
                case 1:
                    m.gifAttribution = 1;
                    break;
                case "TENOR":
                case 2:
                    m.gifAttribution = 2;
                    break;
                case "KLIPY":
                case 3:
                    m.gifAttribution = 3;
                    break;
                default:
                    if (typeof d.gifAttribution === "number" && (d.gifAttribution | 0) === d.gifAttribution)
                        m.gifAttribution = d.gifAttribution;
                }
                if (d.viewOnce != null) {
                    m.viewOnce = $Boolean(d.viewOnce);
                }
                if (d.thumbnailDirectPath != null) {
                    m.thumbnailDirectPath = $String(d.thumbnailDirectPath);
                }
                if (d.thumbnailSha256 != null) {
                    if (typeof d.thumbnailSha256 === "string")
                        $util.base64.decode(d.thumbnailSha256, m.thumbnailSha256 = $util.newBuffer($util.base64.length(d.thumbnailSha256)), 0);
                    else if (d.thumbnailSha256.length >= 0)
                        m.thumbnailSha256 = d.thumbnailSha256;
                }
                if (d.thumbnailEncSha256 != null) {
                    if (typeof d.thumbnailEncSha256 === "string")
                        $util.base64.decode(d.thumbnailEncSha256, m.thumbnailEncSha256 = $util.newBuffer($util.base64.length(d.thumbnailEncSha256)), 0);
                    else if (d.thumbnailEncSha256.length >= 0)
                        m.thumbnailEncSha256 = d.thumbnailEncSha256;
                }
                if (d.staticUrl != null) {
                    m.staticUrl = $String(d.staticUrl);
                }
                if (d.annotations) {
                    if (!$Array.isArray(d.annotations))
                        throw $TypeError(".proto.Message.VideoMessage.annotations: array expected");
                    m.annotations = $Array(d.annotations.length);
                    for (var i = 0; i < d.annotations.length; ++i) {
                        if (!$util.isObject(d.annotations[i]))
                            throw $TypeError(".proto.Message.VideoMessage.annotations: object expected");
                        m.annotations[i] = $root.proto.InteractiveAnnotation.fromObject(d.annotations[i], q + 1);
                    }
                }
                if (d.accessibilityLabel != null) {
                    m.accessibilityLabel = $String(d.accessibilityLabel);
                }
                if (d.processedVideos) {
                    if (!$Array.isArray(d.processedVideos))
                        throw $TypeError(".proto.Message.VideoMessage.processedVideos: array expected");
                    m.processedVideos = $Array(d.processedVideos.length);
                    for (var i = 0; i < d.processedVideos.length; ++i) {
                        if (!$util.isObject(d.processedVideos[i]))
                            throw $TypeError(".proto.Message.VideoMessage.processedVideos: object expected");
                        m.processedVideos[i] = $root.proto.ProcessedVideo.fromObject(d.processedVideos[i], q + 1);
                    }
                }
                if (d.externalShareFullVideoDurationInSeconds != null) {
                    m.externalShareFullVideoDurationInSeconds = d.externalShareFullVideoDurationInSeconds >>> 0;
                }
                if (d.motionPhotoPresentationOffsetMs != null) {
                    if ($util.Long)
                        m.motionPhotoPresentationOffsetMs = $util.Long.fromValue(d.motionPhotoPresentationOffsetMs, true);
                    else if (typeof d.motionPhotoPresentationOffsetMs === "string")
                        m.motionPhotoPresentationOffsetMs = $parseInt(d.motionPhotoPresentationOffsetMs, 10);
                    else if (typeof d.motionPhotoPresentationOffsetMs === "number")
                        m.motionPhotoPresentationOffsetMs = d.motionPhotoPresentationOffsetMs;
                    else if (typeof d.motionPhotoPresentationOffsetMs === "object")
                        m.motionPhotoPresentationOffsetMs = new $util.LongBits(d.motionPhotoPresentationOffsetMs.low >>> 0, d.motionPhotoPresentationOffsetMs.high >>> 0).toNumber(true);
                }
                if (d.metadataUrl != null) {
                    m.metadataUrl = $String(d.metadataUrl);
                }
                switch (d.videoSourceType) {
                case "USER_VIDEO":
                case 0:
                    m.videoSourceType = 0;
                    break;
                case "AI_GENERATED":
                case 1:
                    m.videoSourceType = 1;
                    break;
                default:
                    if (typeof d.videoSourceType === "number" && (d.videoSourceType | 0) === d.videoSourceType)
                        m.videoSourceType = d.videoSourceType;
                }
                return m;
            };

            VideoMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.interactiveAnnotations = [];
                    d.annotations = [];
                    d.processedVideos = [];
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.seconds != null && $Object.hasOwnProperty.call(m, "seconds")) {
                    d.seconds = m.seconds;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                    d.caption = m.caption;
                }
                if (m.gifPlayback != null && $Object.hasOwnProperty.call(m, "gifPlayback")) {
                    d.gifPlayback = m.gifPlayback;
                }
                if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                    d.height = m.height;
                }
                if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                    d.width = m.width;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.interactiveAnnotations && m.interactiveAnnotations.length) {
                    d.interactiveAnnotations = $Array(m.interactiveAnnotations.length);
                    for (var j = 0; j < m.interactiveAnnotations.length; ++j) {
                        d.interactiveAnnotations[j] = $root.proto.InteractiveAnnotation.toObject(m.interactiveAnnotations[j], o, q + 1);
                    }
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                if (m.contextInfo != null && $Object.hasOwnProperty.call(m, "contextInfo")) {
                    d.contextInfo = $root.proto.ContextInfo.toObject(m.contextInfo, o, q + 1);
                }
                if (m.streamingSidecar != null && $Object.hasOwnProperty.call(m, "streamingSidecar")) {
                    d.streamingSidecar = o.bytes === $String ? $util.base64.encode(m.streamingSidecar, 0, m.streamingSidecar.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.streamingSidecar) : m.streamingSidecar;
                }
                if (m.gifAttribution != null && $Object.hasOwnProperty.call(m, "gifAttribution")) {
                    d.gifAttribution = o.enums === $String ? $root.proto.Message.VideoMessage.Attribution[m.gifAttribution] === $undefined ? m.gifAttribution : $root.proto.Message.VideoMessage.Attribution[m.gifAttribution] : m.gifAttribution;
                }
                if (m.viewOnce != null && $Object.hasOwnProperty.call(m, "viewOnce")) {
                    d.viewOnce = m.viewOnce;
                }
                if (m.thumbnailDirectPath != null && $Object.hasOwnProperty.call(m, "thumbnailDirectPath")) {
                    d.thumbnailDirectPath = m.thumbnailDirectPath;
                }
                if (m.thumbnailSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailSha256")) {
                    d.thumbnailSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailSha256, 0, m.thumbnailSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailSha256) : m.thumbnailSha256;
                }
                if (m.thumbnailEncSha256 != null && $Object.hasOwnProperty.call(m, "thumbnailEncSha256")) {
                    d.thumbnailEncSha256 = o.bytes === $String ? $util.base64.encode(m.thumbnailEncSha256, 0, m.thumbnailEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.thumbnailEncSha256) : m.thumbnailEncSha256;
                }
                if (m.staticUrl != null && $Object.hasOwnProperty.call(m, "staticUrl")) {
                    d.staticUrl = m.staticUrl;
                }
                if (m.annotations && m.annotations.length) {
                    d.annotations = $Array(m.annotations.length);
                    for (var j = 0; j < m.annotations.length; ++j) {
                        d.annotations[j] = $root.proto.InteractiveAnnotation.toObject(m.annotations[j], o, q + 1);
                    }
                }
                if (m.accessibilityLabel != null && $Object.hasOwnProperty.call(m, "accessibilityLabel")) {
                    d.accessibilityLabel = m.accessibilityLabel;
                }
                if (m.processedVideos && m.processedVideos.length) {
                    d.processedVideos = $Array(m.processedVideos.length);
                    for (var j = 0; j < m.processedVideos.length; ++j) {
                        d.processedVideos[j] = $root.proto.ProcessedVideo.toObject(m.processedVideos[j], o, q + 1);
                    }
                }
                if (m.externalShareFullVideoDurationInSeconds != null && $Object.hasOwnProperty.call(m, "externalShareFullVideoDurationInSeconds")) {
                    d.externalShareFullVideoDurationInSeconds = m.externalShareFullVideoDurationInSeconds;
                }
                if (m.motionPhotoPresentationOffsetMs != null && $Object.hasOwnProperty.call(m, "motionPhotoPresentationOffsetMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.motionPhotoPresentationOffsetMs = typeof m.motionPhotoPresentationOffsetMs === "number" ? $BigInt(m.motionPhotoPresentationOffsetMs) : $util.Long.fromBits(m.motionPhotoPresentationOffsetMs.low >>> 0, m.motionPhotoPresentationOffsetMs.high >>> 0, true).toBigInt();
                    else if (typeof m.motionPhotoPresentationOffsetMs === "number")
                        d.motionPhotoPresentationOffsetMs = o.longs === $String ? $String(m.motionPhotoPresentationOffsetMs) : m.motionPhotoPresentationOffsetMs;
                    else
                        d.motionPhotoPresentationOffsetMs = o.longs === $String ? $util.Long.prototype.toString.call(m.motionPhotoPresentationOffsetMs) : o.longs === $Number ? new $util.LongBits(m.motionPhotoPresentationOffsetMs.low >>> 0, m.motionPhotoPresentationOffsetMs.high >>> 0).toNumber(true) : m.motionPhotoPresentationOffsetMs;
                }
                if (m.metadataUrl != null && $Object.hasOwnProperty.call(m, "metadataUrl")) {
                    d.metadataUrl = m.metadataUrl;
                }
                if (m.videoSourceType != null && $Object.hasOwnProperty.call(m, "videoSourceType")) {
                    d.videoSourceType = o.enums === $String ? $root.proto.Message.VideoMessage.VideoSourceType[m.videoSourceType] === $undefined ? m.videoSourceType : $root.proto.Message.VideoMessage.VideoSourceType[m.videoSourceType] : m.videoSourceType;
                }
                return d;
            };

            VideoMessage.prototype.toJSON = function() {
                return VideoMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            VideoMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.Message.VideoMessage";
            };

            VideoMessage.Attribution = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "GIPHY"] = 1;
                values[valuesById[2] = "TENOR"] = 2;
                values[valuesById[3] = "KLIPY"] = 3;
                return values;
            })();

            VideoMessage.VideoSourceType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "USER_VIDEO"] = 0;
                values[valuesById[1] = "AI_GENERATED"] = 1;
                return values;
            })();

            return VideoMessage;
        })();

        return Message;
    })();

    proto.MessageAddOn = (function() {

        const MessageAddOn = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageAddOn.prototype.messageAddOnType = null;
        MessageAddOn.prototype.messageAddOn = null;
        MessageAddOn.prototype.senderTimestampMs = null;
        MessageAddOn.prototype.serverTimestampMs = null;
        MessageAddOn.prototype.status = null;
        MessageAddOn.prototype.addOnContextInfo = null;
        MessageAddOn.prototype.messageAddOnKey = null;
        MessageAddOn.prototype.legacyMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_messageAddOnType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_messageAddOn", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_addOnContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["addOnContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_messageAddOnKey", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOn.prototype, "_legacyMessage", {
            get: $util.oneOfGetter($oneOfFields = ["legacyMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageAddOn.create = function(properties) {
            return new MessageAddOn(properties);
        };

        MessageAddOn.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageAddOnType != null && $Object.hasOwnProperty.call(m, "messageAddOnType"))
                w.uint32(8).int32(m.messageAddOnType);
            if (m.messageAddOn != null && $Object.hasOwnProperty.call(m, "messageAddOn"))
                $root.proto.Message.encode(m.messageAddOn, w.uint32(18).fork(), q + 1).ldelim();
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                w.uint32(24).int64(m.senderTimestampMs);
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs"))
                w.uint32(32).int64(m.serverTimestampMs);
            if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                w.uint32(40).int32(m.status);
            if (m.addOnContextInfo != null && $Object.hasOwnProperty.call(m, "addOnContextInfo"))
                $root.proto.MessageAddOnContextInfo.encode(m.addOnContextInfo, w.uint32(50).fork(), q + 1).ldelim();
            if (m.messageAddOnKey != null && $Object.hasOwnProperty.call(m, "messageAddOnKey"))
                $root.proto.MessageKey.encode(m.messageAddOnKey, w.uint32(58).fork(), q + 1).ldelim();
            if (m.legacyMessage != null && $Object.hasOwnProperty.call(m, "legacyMessage"))
                $root.proto.LegacyMessage.encode(m.legacyMessage, w.uint32(66).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageAddOn.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageAddOn(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.messageAddOnType = r.int32();
                        m._messageAddOnType = "messageAddOnType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.messageAddOn = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.messageAddOn);
                        m._messageAddOn = "messageAddOn";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.senderTimestampMs = r.int64();
                        m._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.serverTimestampMs = r.int64();
                        m._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.status = r.int32();
                        m._status = "status";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.addOnContextInfo = $root.proto.MessageAddOnContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.addOnContextInfo);
                        m._addOnContextInfo = "addOnContextInfo";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.messageAddOnKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.messageAddOnKey);
                        m._messageAddOnKey = "messageAddOnKey";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.legacyMessage = $root.proto.LegacyMessage.decode(r, r.uint32(), $undefined, q + 1, m.legacyMessage);
                        m._legacyMessage = "legacyMessage";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageAddOn.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageAddOn)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageAddOn: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageAddOn();
            switch (d.messageAddOnType) {
            case "UNDEFINED":
            case 0:
                m.messageAddOnType = 0;
                break;
            case "REACTION":
            case 1:
                m.messageAddOnType = 1;
                break;
            case "EVENT_RESPONSE":
            case 2:
                m.messageAddOnType = 2;
                break;
            case "POLL_UPDATE":
            case 3:
                m.messageAddOnType = 3;
                break;
            case "PIN_IN_CHAT":
            case 4:
                m.messageAddOnType = 4;
                break;
            default:
                if (typeof d.messageAddOnType === "number" && (d.messageAddOnType | 0) === d.messageAddOnType)
                    m.messageAddOnType = d.messageAddOnType;
            }
            if (d.messageAddOn != null) {
                if (!$util.isObject(d.messageAddOn))
                    throw $TypeError(".proto.MessageAddOn.messageAddOn: object expected");
                m.messageAddOn = $root.proto.Message.fromObject(d.messageAddOn, q + 1);
            }
            if (d.senderTimestampMs != null) {
                if ($util.Long)
                    m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                else if (typeof d.senderTimestampMs === "string")
                    m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                else if (typeof d.senderTimestampMs === "number")
                    m.senderTimestampMs = d.senderTimestampMs;
                else if (typeof d.senderTimestampMs === "object")
                    m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
            }
            if (d.serverTimestampMs != null) {
                if ($util.Long)
                    m.serverTimestampMs = $util.Long.fromValue(d.serverTimestampMs, false);
                else if (typeof d.serverTimestampMs === "string")
                    m.serverTimestampMs = $parseInt(d.serverTimestampMs, 10);
                else if (typeof d.serverTimestampMs === "number")
                    m.serverTimestampMs = d.serverTimestampMs;
                else if (typeof d.serverTimestampMs === "object")
                    m.serverTimestampMs = new $util.LongBits(d.serverTimestampMs.low >>> 0, d.serverTimestampMs.high >>> 0).toNumber();
            }
            switch (d.status) {
            case "ERROR":
            case 0:
                m.status = 0;
                break;
            case "PENDING":
            case 1:
                m.status = 1;
                break;
            case "SERVER_ACK":
            case 2:
                m.status = 2;
                break;
            case "DELIVERY_ACK":
            case 3:
                m.status = 3;
                break;
            case "READ":
            case 4:
                m.status = 4;
                break;
            case "PLAYED":
            case 5:
                m.status = 5;
                break;
            default:
                if (typeof d.status === "number" && (d.status | 0) === d.status)
                    m.status = d.status;
            }
            if (d.addOnContextInfo != null) {
                if (!$util.isObject(d.addOnContextInfo))
                    throw $TypeError(".proto.MessageAddOn.addOnContextInfo: object expected");
                m.addOnContextInfo = $root.proto.MessageAddOnContextInfo.fromObject(d.addOnContextInfo, q + 1);
            }
            if (d.messageAddOnKey != null) {
                if (!$util.isObject(d.messageAddOnKey))
                    throw $TypeError(".proto.MessageAddOn.messageAddOnKey: object expected");
                m.messageAddOnKey = $root.proto.MessageKey.fromObject(d.messageAddOnKey, q + 1);
            }
            if (d.legacyMessage != null) {
                if (!$util.isObject(d.legacyMessage))
                    throw $TypeError(".proto.MessageAddOn.legacyMessage: object expected");
                m.legacyMessage = $root.proto.LegacyMessage.fromObject(d.legacyMessage, q + 1);
            }
            return m;
        };

        MessageAddOn.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageAddOnType != null && $Object.hasOwnProperty.call(m, "messageAddOnType")) {
                d.messageAddOnType = o.enums === $String ? $root.proto.MessageAddOn.MessageAddOnType[m.messageAddOnType] === $undefined ? m.messageAddOnType : $root.proto.MessageAddOn.MessageAddOnType[m.messageAddOnType] : m.messageAddOnType;
            }
            if (m.messageAddOn != null && $Object.hasOwnProperty.call(m, "messageAddOn")) {
                d.messageAddOn = $root.proto.Message.toObject(m.messageAddOn, o, q + 1);
            }
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.senderTimestampMs === "number")
                    d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                else
                    d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
            }
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.serverTimestampMs = typeof m.serverTimestampMs === "number" ? $BigInt(m.serverTimestampMs) : $util.Long.fromBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.serverTimestampMs === "number")
                    d.serverTimestampMs = o.longs === $String ? $String(m.serverTimestampMs) : m.serverTimestampMs;
                else
                    d.serverTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.serverTimestampMs) : o.longs === $Number ? new $util.LongBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0).toNumber() : m.serverTimestampMs;
            }
            if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                d.status = o.enums === $String ? $root.proto.WebMessageInfo.Status[m.status] === $undefined ? m.status : $root.proto.WebMessageInfo.Status[m.status] : m.status;
            }
            if (m.addOnContextInfo != null && $Object.hasOwnProperty.call(m, "addOnContextInfo")) {
                d.addOnContextInfo = $root.proto.MessageAddOnContextInfo.toObject(m.addOnContextInfo, o, q + 1);
            }
            if (m.messageAddOnKey != null && $Object.hasOwnProperty.call(m, "messageAddOnKey")) {
                d.messageAddOnKey = $root.proto.MessageKey.toObject(m.messageAddOnKey, o, q + 1);
            }
            if (m.legacyMessage != null && $Object.hasOwnProperty.call(m, "legacyMessage")) {
                d.legacyMessage = $root.proto.LegacyMessage.toObject(m.legacyMessage, o, q + 1);
            }
            return d;
        };

        MessageAddOn.prototype.toJSON = function() {
            return MessageAddOn.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageAddOn.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageAddOn";
        };

        MessageAddOn.MessageAddOnType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNDEFINED"] = 0;
            values[valuesById[1] = "REACTION"] = 1;
            values[valuesById[2] = "EVENT_RESPONSE"] = 2;
            values[valuesById[3] = "POLL_UPDATE"] = 3;
            values[valuesById[4] = "PIN_IN_CHAT"] = 4;
            return values;
        })();

        return MessageAddOn;
    })();

    proto.MessageAddOnContextInfo = (function() {

        const MessageAddOnContextInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageAddOnContextInfo.prototype.messageAddOnDurationInSecs = null;
        MessageAddOnContextInfo.prototype.messageAddOnExpiryType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOnContextInfo.prototype, "_messageAddOnDurationInSecs", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnDurationInSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAddOnContextInfo.prototype, "_messageAddOnExpiryType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnExpiryType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageAddOnContextInfo.create = function(properties) {
            return new MessageAddOnContextInfo(properties);
        };

        MessageAddOnContextInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageAddOnDurationInSecs != null && $Object.hasOwnProperty.call(m, "messageAddOnDurationInSecs"))
                w.uint32(8).uint32(m.messageAddOnDurationInSecs);
            if (m.messageAddOnExpiryType != null && $Object.hasOwnProperty.call(m, "messageAddOnExpiryType"))
                w.uint32(16).int32(m.messageAddOnExpiryType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageAddOnContextInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageAddOnContextInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.messageAddOnDurationInSecs = r.uint32();
                        m._messageAddOnDurationInSecs = "messageAddOnDurationInSecs";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.messageAddOnExpiryType = r.int32();
                        m._messageAddOnExpiryType = "messageAddOnExpiryType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageAddOnContextInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageAddOnContextInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageAddOnContextInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageAddOnContextInfo();
            if (d.messageAddOnDurationInSecs != null) {
                m.messageAddOnDurationInSecs = d.messageAddOnDurationInSecs >>> 0;
            }
            switch (d.messageAddOnExpiryType) {
            case "STATIC":
            case 1:
                m.messageAddOnExpiryType = 1;
                break;
            case "DEPENDENT_ON_PARENT":
            case 2:
                m.messageAddOnExpiryType = 2;
                break;
            default:
                if (typeof d.messageAddOnExpiryType === "number" && (d.messageAddOnExpiryType | 0) === d.messageAddOnExpiryType)
                    m.messageAddOnExpiryType = d.messageAddOnExpiryType;
            }
            return m;
        };

        MessageAddOnContextInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageAddOnDurationInSecs != null && $Object.hasOwnProperty.call(m, "messageAddOnDurationInSecs")) {
                d.messageAddOnDurationInSecs = m.messageAddOnDurationInSecs;
            }
            if (m.messageAddOnExpiryType != null && $Object.hasOwnProperty.call(m, "messageAddOnExpiryType")) {
                d.messageAddOnExpiryType = o.enums === $String ? $root.proto.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] === $undefined ? m.messageAddOnExpiryType : $root.proto.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] : m.messageAddOnExpiryType;
            }
            return d;
        };

        MessageAddOnContextInfo.prototype.toJSON = function() {
            return MessageAddOnContextInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageAddOnContextInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageAddOnContextInfo";
        };

        return MessageAddOnContextInfo;
    })();

    proto.MessageAssociation = (function() {

        const MessageAssociation = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageAssociation.prototype.associationType = null;
        MessageAssociation.prototype.parentMessageKey = null;
        MessageAssociation.prototype.messageIndex = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAssociation.prototype, "_associationType", {
            get: $util.oneOfGetter($oneOfFields = ["associationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAssociation.prototype, "_parentMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["parentMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageAssociation.prototype, "_messageIndex", {
            get: $util.oneOfGetter($oneOfFields = ["messageIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageAssociation.create = function(properties) {
            return new MessageAssociation(properties);
        };

        MessageAssociation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.associationType != null && $Object.hasOwnProperty.call(m, "associationType"))
                w.uint32(8).int32(m.associationType);
            if (m.parentMessageKey != null && $Object.hasOwnProperty.call(m, "parentMessageKey"))
                $root.proto.MessageKey.encode(m.parentMessageKey, w.uint32(18).fork(), q + 1).ldelim();
            if (m.messageIndex != null && $Object.hasOwnProperty.call(m, "messageIndex"))
                w.uint32(24).int32(m.messageIndex);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageAssociation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageAssociation(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.associationType = r.int32();
                        m._associationType = "associationType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.parentMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.parentMessageKey);
                        m._parentMessageKey = "parentMessageKey";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.messageIndex = r.int32();
                        m._messageIndex = "messageIndex";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageAssociation.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageAssociation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageAssociation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageAssociation();
            switch (d.associationType) {
            case "UNKNOWN":
            case 0:
                m.associationType = 0;
                break;
            case "MEDIA_ALBUM":
            case 1:
                m.associationType = 1;
                break;
            case "BOT_PLUGIN":
            case 2:
                m.associationType = 2;
                break;
            case "EVENT_COVER_IMAGE":
            case 3:
                m.associationType = 3;
                break;
            case "STATUS_POLL":
            case 4:
                m.associationType = 4;
                break;
            case "HD_VIDEO_DUAL_UPLOAD":
            case 5:
                m.associationType = 5;
                break;
            case "STATUS_EXTERNAL_RESHARE":
            case 6:
                m.associationType = 6;
                break;
            case "MEDIA_POLL":
            case 7:
                m.associationType = 7;
                break;
            case "STATUS_ADD_YOURS":
            case 8:
                m.associationType = 8;
                break;
            case "STATUS_NOTIFICATION":
            case 9:
                m.associationType = 9;
                break;
            case "HD_IMAGE_DUAL_UPLOAD":
            case 10:
                m.associationType = 10;
                break;
            case "STICKER_ANNOTATION":
            case 11:
                m.associationType = 11;
                break;
            case "MOTION_PHOTO":
            case 12:
                m.associationType = 12;
                break;
            case "STATUS_LINK_ACTION":
            case 13:
                m.associationType = 13;
                break;
            case "VIEW_ALL_REPLIES":
            case 14:
                m.associationType = 14;
                break;
            case "STATUS_ADD_YOURS_AI_IMAGINE":
            case 15:
                m.associationType = 15;
                break;
            case "STATUS_QUESTION":
            case 16:
                m.associationType = 16;
                break;
            case "STATUS_ADD_YOURS_DIWALI":
            case 17:
                m.associationType = 17;
                break;
            case "STATUS_REACTION":
            case 18:
                m.associationType = 18;
                break;
            case "HEVC_VIDEO_DUAL_UPLOAD":
            case 19:
                m.associationType = 19;
                break;
            case "POLL_ADD_OPTION":
            case 20:
                m.associationType = 20;
                break;
            default:
                if (typeof d.associationType === "number" && (d.associationType | 0) === d.associationType)
                    m.associationType = d.associationType;
            }
            if (d.parentMessageKey != null) {
                if (!$util.isObject(d.parentMessageKey))
                    throw $TypeError(".proto.MessageAssociation.parentMessageKey: object expected");
                m.parentMessageKey = $root.proto.MessageKey.fromObject(d.parentMessageKey, q + 1);
            }
            if (d.messageIndex != null) {
                m.messageIndex = d.messageIndex | 0;
            }
            return m;
        };

        MessageAssociation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.associationType != null && $Object.hasOwnProperty.call(m, "associationType")) {
                d.associationType = o.enums === $String ? $root.proto.MessageAssociation.AssociationType[m.associationType] === $undefined ? m.associationType : $root.proto.MessageAssociation.AssociationType[m.associationType] : m.associationType;
            }
            if (m.parentMessageKey != null && $Object.hasOwnProperty.call(m, "parentMessageKey")) {
                d.parentMessageKey = $root.proto.MessageKey.toObject(m.parentMessageKey, o, q + 1);
            }
            if (m.messageIndex != null && $Object.hasOwnProperty.call(m, "messageIndex")) {
                d.messageIndex = m.messageIndex;
            }
            return d;
        };

        MessageAssociation.prototype.toJSON = function() {
            return MessageAssociation.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageAssociation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageAssociation";
        };

        MessageAssociation.AssociationType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "MEDIA_ALBUM"] = 1;
            values[valuesById[2] = "BOT_PLUGIN"] = 2;
            values[valuesById[3] = "EVENT_COVER_IMAGE"] = 3;
            values[valuesById[4] = "STATUS_POLL"] = 4;
            values[valuesById[5] = "HD_VIDEO_DUAL_UPLOAD"] = 5;
            values[valuesById[6] = "STATUS_EXTERNAL_RESHARE"] = 6;
            values[valuesById[7] = "MEDIA_POLL"] = 7;
            values[valuesById[8] = "STATUS_ADD_YOURS"] = 8;
            values[valuesById[9] = "STATUS_NOTIFICATION"] = 9;
            values[valuesById[10] = "HD_IMAGE_DUAL_UPLOAD"] = 10;
            values[valuesById[11] = "STICKER_ANNOTATION"] = 11;
            values[valuesById[12] = "MOTION_PHOTO"] = 12;
            values[valuesById[13] = "STATUS_LINK_ACTION"] = 13;
            values[valuesById[14] = "VIEW_ALL_REPLIES"] = 14;
            values[valuesById[15] = "STATUS_ADD_YOURS_AI_IMAGINE"] = 15;
            values[valuesById[16] = "STATUS_QUESTION"] = 16;
            values[valuesById[17] = "STATUS_ADD_YOURS_DIWALI"] = 17;
            values[valuesById[18] = "STATUS_REACTION"] = 18;
            values[valuesById[19] = "HEVC_VIDEO_DUAL_UPLOAD"] = 19;
            values[valuesById[20] = "POLL_ADD_OPTION"] = 20;
            return values;
        })();

        return MessageAssociation;
    })();

    proto.MessageContextInfo = (function() {

        const MessageContextInfo = function (p) {
            this.threadId = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageContextInfo.prototype.deviceListMetadata = null;
        MessageContextInfo.prototype.deviceListMetadataVersion = null;
        MessageContextInfo.prototype.messageSecret = null;
        MessageContextInfo.prototype.paddingBytes = null;
        MessageContextInfo.prototype.messageAddOnDurationInSecs = null;
        MessageContextInfo.prototype.botMessageSecret = null;
        MessageContextInfo.prototype.botMetadata = null;
        MessageContextInfo.prototype.reportingTokenVersion = null;
        MessageContextInfo.prototype.messageAddOnExpiryType = null;
        MessageContextInfo.prototype.messageAssociation = null;
        MessageContextInfo.prototype.capiCreatedGroup = null;
        MessageContextInfo.prototype.supportPayload = null;
        MessageContextInfo.prototype.limitSharing = null;
        MessageContextInfo.prototype.limitSharingV2 = null;
        MessageContextInfo.prototype.threadId = $util.emptyArray;
        MessageContextInfo.prototype.weblinkRenderConfig = null;
        MessageContextInfo.prototype.teeBotMetadata = null;
        MessageContextInfo.prototype.accountEncryptionAttestation = null;
        MessageContextInfo.prototype.associatedPrimaryIdentityKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_deviceListMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["deviceListMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_deviceListMetadataVersion", {
            get: $util.oneOfGetter($oneOfFields = ["deviceListMetadataVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_paddingBytes", {
            get: $util.oneOfGetter($oneOfFields = ["paddingBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_messageAddOnDurationInSecs", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnDurationInSecs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_botMessageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_botMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["botMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_reportingTokenVersion", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTokenVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_messageAddOnExpiryType", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnExpiryType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_messageAssociation", {
            get: $util.oneOfGetter($oneOfFields = ["messageAssociation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_capiCreatedGroup", {
            get: $util.oneOfGetter($oneOfFields = ["capiCreatedGroup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_supportPayload", {
            get: $util.oneOfGetter($oneOfFields = ["supportPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_limitSharing", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_limitSharingV2", {
            get: $util.oneOfGetter($oneOfFields = ["limitSharingV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_weblinkRenderConfig", {
            get: $util.oneOfGetter($oneOfFields = ["weblinkRenderConfig"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_teeBotMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["teeBotMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_accountEncryptionAttestation", {
            get: $util.oneOfGetter($oneOfFields = ["accountEncryptionAttestation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageContextInfo.prototype, "_associatedPrimaryIdentityKey", {
            get: $util.oneOfGetter($oneOfFields = ["associatedPrimaryIdentityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageContextInfo.create = function(properties) {
            return new MessageContextInfo(properties);
        };

        MessageContextInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.deviceListMetadata != null && $Object.hasOwnProperty.call(m, "deviceListMetadata"))
                $root.proto.DeviceListMetadata.encode(m.deviceListMetadata, w.uint32(10).fork(), q + 1).ldelim();
            if (m.deviceListMetadataVersion != null && $Object.hasOwnProperty.call(m, "deviceListMetadataVersion"))
                w.uint32(16).int32(m.deviceListMetadataVersion);
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(26).bytes(m.messageSecret);
            if (m.paddingBytes != null && $Object.hasOwnProperty.call(m, "paddingBytes"))
                w.uint32(34).bytes(m.paddingBytes);
            if (m.messageAddOnDurationInSecs != null && $Object.hasOwnProperty.call(m, "messageAddOnDurationInSecs"))
                w.uint32(40).uint32(m.messageAddOnDurationInSecs);
            if (m.botMessageSecret != null && $Object.hasOwnProperty.call(m, "botMessageSecret"))
                w.uint32(50).bytes(m.botMessageSecret);
            if (m.botMetadata != null && $Object.hasOwnProperty.call(m, "botMetadata"))
                $root.proto.BotMetadata.encode(m.botMetadata, w.uint32(58).fork(), q + 1).ldelim();
            if (m.reportingTokenVersion != null && $Object.hasOwnProperty.call(m, "reportingTokenVersion"))
                w.uint32(64).int32(m.reportingTokenVersion);
            if (m.messageAddOnExpiryType != null && $Object.hasOwnProperty.call(m, "messageAddOnExpiryType"))
                w.uint32(72).int32(m.messageAddOnExpiryType);
            if (m.messageAssociation != null && $Object.hasOwnProperty.call(m, "messageAssociation"))
                $root.proto.MessageAssociation.encode(m.messageAssociation, w.uint32(82).fork(), q + 1).ldelim();
            if (m.capiCreatedGroup != null && $Object.hasOwnProperty.call(m, "capiCreatedGroup"))
                w.uint32(88).bool(m.capiCreatedGroup);
            if (m.supportPayload != null && $Object.hasOwnProperty.call(m, "supportPayload"))
                w.uint32(98).string(m.supportPayload);
            if (m.limitSharing != null && $Object.hasOwnProperty.call(m, "limitSharing"))
                $root.proto.LimitSharing.encode(m.limitSharing, w.uint32(106).fork(), q + 1).ldelim();
            if (m.limitSharingV2 != null && $Object.hasOwnProperty.call(m, "limitSharingV2"))
                $root.proto.LimitSharing.encode(m.limitSharingV2, w.uint32(114).fork(), q + 1).ldelim();
            if (m.threadId != null && m.threadId.length) {
                for (var i = 0; i < m.threadId.length; ++i)
                    $root.proto.ThreadID.encode(m.threadId[i], w.uint32(122).fork(), q + 1).ldelim();
            }
            if (m.weblinkRenderConfig != null && $Object.hasOwnProperty.call(m, "weblinkRenderConfig"))
                w.uint32(128).int32(m.weblinkRenderConfig);
            if (m.teeBotMetadata != null && $Object.hasOwnProperty.call(m, "teeBotMetadata"))
                w.uint32(138).bytes(m.teeBotMetadata);
            if (m.accountEncryptionAttestation != null && $Object.hasOwnProperty.call(m, "accountEncryptionAttestation"))
                $root.proto.NonE2EEAttestation.encode(m.accountEncryptionAttestation, w.uint32(146).fork(), q + 1).ldelim();
            if (m.associatedPrimaryIdentityKey != null && $Object.hasOwnProperty.call(m, "associatedPrimaryIdentityKey"))
                w.uint32(154).bytes(m.associatedPrimaryIdentityKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageContextInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageContextInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.deviceListMetadata = $root.proto.DeviceListMetadata.decode(r, r.uint32(), $undefined, q + 1, m.deviceListMetadata);
                        m._deviceListMetadata = "deviceListMetadata";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.deviceListMetadataVersion = r.int32();
                        m._deviceListMetadataVersion = "deviceListMetadataVersion";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.messageSecret = r.bytes();
                        m._messageSecret = "messageSecret";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.paddingBytes = r.bytes();
                        m._paddingBytes = "paddingBytes";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.messageAddOnDurationInSecs = r.uint32();
                        m._messageAddOnDurationInSecs = "messageAddOnDurationInSecs";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.botMessageSecret = r.bytes();
                        m._botMessageSecret = "botMessageSecret";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.botMetadata = $root.proto.BotMetadata.decode(r, r.uint32(), $undefined, q + 1, m.botMetadata);
                        m._botMetadata = "botMetadata";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.reportingTokenVersion = r.int32();
                        m._reportingTokenVersion = "reportingTokenVersion";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.messageAddOnExpiryType = r.int32();
                        m._messageAddOnExpiryType = "messageAddOnExpiryType";
                        continue;
                    }
                case 10: {
                        if (u !== 2)
                            break;
                        m.messageAssociation = $root.proto.MessageAssociation.decode(r, r.uint32(), $undefined, q + 1, m.messageAssociation);
                        m._messageAssociation = "messageAssociation";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.capiCreatedGroup = r.bool();
                        m._capiCreatedGroup = "capiCreatedGroup";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.supportPayload = r.stringVerify();
                        m._supportPayload = "supportPayload";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.limitSharing = $root.proto.LimitSharing.decode(r, r.uint32(), $undefined, q + 1, m.limitSharing);
                        m._limitSharing = "limitSharing";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.limitSharingV2 = $root.proto.LimitSharing.decode(r, r.uint32(), $undefined, q + 1, m.limitSharingV2);
                        m._limitSharingV2 = "limitSharingV2";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        if (!(m.threadId && m.threadId.length))
                            m.threadId = [];
                        m.threadId.push($root.proto.ThreadID.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        m.weblinkRenderConfig = r.int32();
                        m._weblinkRenderConfig = "weblinkRenderConfig";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.teeBotMetadata = r.bytes();
                        m._teeBotMetadata = "teeBotMetadata";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        m.accountEncryptionAttestation = $root.proto.NonE2EEAttestation.decode(r, r.uint32(), $undefined, q + 1, m.accountEncryptionAttestation);
                        m._accountEncryptionAttestation = "accountEncryptionAttestation";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.associatedPrimaryIdentityKey = r.bytes();
                        m._associatedPrimaryIdentityKey = "associatedPrimaryIdentityKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageContextInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageContextInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageContextInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageContextInfo();
            if (d.deviceListMetadata != null) {
                if (!$util.isObject(d.deviceListMetadata))
                    throw $TypeError(".proto.MessageContextInfo.deviceListMetadata: object expected");
                m.deviceListMetadata = $root.proto.DeviceListMetadata.fromObject(d.deviceListMetadata, q + 1);
            }
            if (d.deviceListMetadataVersion != null) {
                m.deviceListMetadataVersion = d.deviceListMetadataVersion | 0;
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            if (d.paddingBytes != null) {
                if (typeof d.paddingBytes === "string")
                    $util.base64.decode(d.paddingBytes, m.paddingBytes = $util.newBuffer($util.base64.length(d.paddingBytes)), 0);
                else if (d.paddingBytes.length >= 0)
                    m.paddingBytes = d.paddingBytes;
            }
            if (d.messageAddOnDurationInSecs != null) {
                m.messageAddOnDurationInSecs = d.messageAddOnDurationInSecs >>> 0;
            }
            if (d.botMessageSecret != null) {
                if (typeof d.botMessageSecret === "string")
                    $util.base64.decode(d.botMessageSecret, m.botMessageSecret = $util.newBuffer($util.base64.length(d.botMessageSecret)), 0);
                else if (d.botMessageSecret.length >= 0)
                    m.botMessageSecret = d.botMessageSecret;
            }
            if (d.botMetadata != null) {
                if (!$util.isObject(d.botMetadata))
                    throw $TypeError(".proto.MessageContextInfo.botMetadata: object expected");
                m.botMetadata = $root.proto.BotMetadata.fromObject(d.botMetadata, q + 1);
            }
            if (d.reportingTokenVersion != null) {
                m.reportingTokenVersion = d.reportingTokenVersion | 0;
            }
            switch (d.messageAddOnExpiryType) {
            case "STATIC":
            case 1:
                m.messageAddOnExpiryType = 1;
                break;
            case "DEPENDENT_ON_PARENT":
            case 2:
                m.messageAddOnExpiryType = 2;
                break;
            default:
                if (typeof d.messageAddOnExpiryType === "number" && (d.messageAddOnExpiryType | 0) === d.messageAddOnExpiryType)
                    m.messageAddOnExpiryType = d.messageAddOnExpiryType;
            }
            if (d.messageAssociation != null) {
                if (!$util.isObject(d.messageAssociation))
                    throw $TypeError(".proto.MessageContextInfo.messageAssociation: object expected");
                m.messageAssociation = $root.proto.MessageAssociation.fromObject(d.messageAssociation, q + 1);
            }
            if (d.capiCreatedGroup != null) {
                m.capiCreatedGroup = $Boolean(d.capiCreatedGroup);
            }
            if (d.supportPayload != null) {
                m.supportPayload = $String(d.supportPayload);
            }
            if (d.limitSharing != null) {
                if (!$util.isObject(d.limitSharing))
                    throw $TypeError(".proto.MessageContextInfo.limitSharing: object expected");
                m.limitSharing = $root.proto.LimitSharing.fromObject(d.limitSharing, q + 1);
            }
            if (d.limitSharingV2 != null) {
                if (!$util.isObject(d.limitSharingV2))
                    throw $TypeError(".proto.MessageContextInfo.limitSharingV2: object expected");
                m.limitSharingV2 = $root.proto.LimitSharing.fromObject(d.limitSharingV2, q + 1);
            }
            if (d.threadId) {
                if (!$Array.isArray(d.threadId))
                    throw $TypeError(".proto.MessageContextInfo.threadId: array expected");
                m.threadId = $Array(d.threadId.length);
                for (var i = 0; i < d.threadId.length; ++i) {
                    if (!$util.isObject(d.threadId[i]))
                        throw $TypeError(".proto.MessageContextInfo.threadId: object expected");
                    m.threadId[i] = $root.proto.ThreadID.fromObject(d.threadId[i], q + 1);
                }
            }
            switch (d.weblinkRenderConfig) {
            case "WEBVIEW":
            case 0:
                m.weblinkRenderConfig = 0;
                break;
            case "SYSTEM":
            case 1:
                m.weblinkRenderConfig = 1;
                break;
            default:
                if (typeof d.weblinkRenderConfig === "number" && (d.weblinkRenderConfig | 0) === d.weblinkRenderConfig)
                    m.weblinkRenderConfig = d.weblinkRenderConfig;
            }
            if (d.teeBotMetadata != null) {
                if (typeof d.teeBotMetadata === "string")
                    $util.base64.decode(d.teeBotMetadata, m.teeBotMetadata = $util.newBuffer($util.base64.length(d.teeBotMetadata)), 0);
                else if (d.teeBotMetadata.length >= 0)
                    m.teeBotMetadata = d.teeBotMetadata;
            }
            if (d.accountEncryptionAttestation != null) {
                if (!$util.isObject(d.accountEncryptionAttestation))
                    throw $TypeError(".proto.MessageContextInfo.accountEncryptionAttestation: object expected");
                m.accountEncryptionAttestation = $root.proto.NonE2EEAttestation.fromObject(d.accountEncryptionAttestation, q + 1);
            }
            if (d.associatedPrimaryIdentityKey != null) {
                if (typeof d.associatedPrimaryIdentityKey === "string")
                    $util.base64.decode(d.associatedPrimaryIdentityKey, m.associatedPrimaryIdentityKey = $util.newBuffer($util.base64.length(d.associatedPrimaryIdentityKey)), 0);
                else if (d.associatedPrimaryIdentityKey.length >= 0)
                    m.associatedPrimaryIdentityKey = d.associatedPrimaryIdentityKey;
            }
            return m;
        };

        MessageContextInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.threadId = [];
            }
            if (m.deviceListMetadata != null && $Object.hasOwnProperty.call(m, "deviceListMetadata")) {
                d.deviceListMetadata = $root.proto.DeviceListMetadata.toObject(m.deviceListMetadata, o, q + 1);
            }
            if (m.deviceListMetadataVersion != null && $Object.hasOwnProperty.call(m, "deviceListMetadataVersion")) {
                d.deviceListMetadataVersion = m.deviceListMetadataVersion;
            }
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret")) {
                d.messageSecret = o.bytes === $String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
            }
            if (m.paddingBytes != null && $Object.hasOwnProperty.call(m, "paddingBytes")) {
                d.paddingBytes = o.bytes === $String ? $util.base64.encode(m.paddingBytes, 0, m.paddingBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.paddingBytes) : m.paddingBytes;
            }
            if (m.messageAddOnDurationInSecs != null && $Object.hasOwnProperty.call(m, "messageAddOnDurationInSecs")) {
                d.messageAddOnDurationInSecs = m.messageAddOnDurationInSecs;
            }
            if (m.botMessageSecret != null && $Object.hasOwnProperty.call(m, "botMessageSecret")) {
                d.botMessageSecret = o.bytes === $String ? $util.base64.encode(m.botMessageSecret, 0, m.botMessageSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.botMessageSecret) : m.botMessageSecret;
            }
            if (m.botMetadata != null && $Object.hasOwnProperty.call(m, "botMetadata")) {
                d.botMetadata = $root.proto.BotMetadata.toObject(m.botMetadata, o, q + 1);
            }
            if (m.reportingTokenVersion != null && $Object.hasOwnProperty.call(m, "reportingTokenVersion")) {
                d.reportingTokenVersion = m.reportingTokenVersion;
            }
            if (m.messageAddOnExpiryType != null && $Object.hasOwnProperty.call(m, "messageAddOnExpiryType")) {
                d.messageAddOnExpiryType = o.enums === $String ? $root.proto.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] === $undefined ? m.messageAddOnExpiryType : $root.proto.MessageContextInfo.MessageAddonExpiryType[m.messageAddOnExpiryType] : m.messageAddOnExpiryType;
            }
            if (m.messageAssociation != null && $Object.hasOwnProperty.call(m, "messageAssociation")) {
                d.messageAssociation = $root.proto.MessageAssociation.toObject(m.messageAssociation, o, q + 1);
            }
            if (m.capiCreatedGroup != null && $Object.hasOwnProperty.call(m, "capiCreatedGroup")) {
                d.capiCreatedGroup = m.capiCreatedGroup;
            }
            if (m.supportPayload != null && $Object.hasOwnProperty.call(m, "supportPayload")) {
                d.supportPayload = m.supportPayload;
            }
            if (m.limitSharing != null && $Object.hasOwnProperty.call(m, "limitSharing")) {
                d.limitSharing = $root.proto.LimitSharing.toObject(m.limitSharing, o, q + 1);
            }
            if (m.limitSharingV2 != null && $Object.hasOwnProperty.call(m, "limitSharingV2")) {
                d.limitSharingV2 = $root.proto.LimitSharing.toObject(m.limitSharingV2, o, q + 1);
            }
            if (m.threadId && m.threadId.length) {
                d.threadId = $Array(m.threadId.length);
                for (var j = 0; j < m.threadId.length; ++j) {
                    d.threadId[j] = $root.proto.ThreadID.toObject(m.threadId[j], o, q + 1);
                }
            }
            if (m.weblinkRenderConfig != null && $Object.hasOwnProperty.call(m, "weblinkRenderConfig")) {
                d.weblinkRenderConfig = o.enums === $String ? $root.proto.WebLinkRenderConfig[m.weblinkRenderConfig] === $undefined ? m.weblinkRenderConfig : $root.proto.WebLinkRenderConfig[m.weblinkRenderConfig] : m.weblinkRenderConfig;
            }
            if (m.teeBotMetadata != null && $Object.hasOwnProperty.call(m, "teeBotMetadata")) {
                d.teeBotMetadata = o.bytes === $String ? $util.base64.encode(m.teeBotMetadata, 0, m.teeBotMetadata.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.teeBotMetadata) : m.teeBotMetadata;
            }
            if (m.accountEncryptionAttestation != null && $Object.hasOwnProperty.call(m, "accountEncryptionAttestation")) {
                d.accountEncryptionAttestation = $root.proto.NonE2EEAttestation.toObject(m.accountEncryptionAttestation, o, q + 1);
            }
            if (m.associatedPrimaryIdentityKey != null && $Object.hasOwnProperty.call(m, "associatedPrimaryIdentityKey")) {
                d.associatedPrimaryIdentityKey = o.bytes === $String ? $util.base64.encode(m.associatedPrimaryIdentityKey, 0, m.associatedPrimaryIdentityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.associatedPrimaryIdentityKey) : m.associatedPrimaryIdentityKey;
            }
            return d;
        };

        MessageContextInfo.prototype.toJSON = function() {
            return MessageContextInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageContextInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageContextInfo";
        };

        MessageContextInfo.MessageAddonExpiryType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "STATIC"] = 1;
            values[valuesById[2] = "DEPENDENT_ON_PARENT"] = 2;
            return values;
        })();

        return MessageContextInfo;
    })();

    proto.MessageKey = (function() {

        const MessageKey = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageKey.prototype.remoteJid = null;
        MessageKey.prototype.fromMe = null;
        MessageKey.prototype.id = null;
        MessageKey.prototype.participant = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageKey.prototype, "_remoteJid", {
            get: $util.oneOfGetter($oneOfFields = ["remoteJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageKey.prototype, "_fromMe", {
            get: $util.oneOfGetter($oneOfFields = ["fromMe"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageKey.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageKey.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageKey.create = function(properties) {
            return new MessageKey(properties);
        };

        MessageKey.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.remoteJid != null && $Object.hasOwnProperty.call(m, "remoteJid"))
                w.uint32(10).string(m.remoteJid);
            if (m.fromMe != null && $Object.hasOwnProperty.call(m, "fromMe"))
                w.uint32(16).bool(m.fromMe);
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(26).string(m.id);
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant"))
                w.uint32(34).string(m.participant);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageKey.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageKey();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.remoteJid = r.stringVerify();
                        m._remoteJid = "remoteJid";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.fromMe = r.bool();
                        m._fromMe = "fromMe";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.id = r.stringVerify();
                        m._id = "id";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.participant = r.stringVerify();
                        m._participant = "participant";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageKey.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageKey)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageKey: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageKey();
            if (d.remoteJid != null) {
                m.remoteJid = $String(d.remoteJid);
            }
            if (d.fromMe != null) {
                m.fromMe = $Boolean(d.fromMe);
            }
            if (d.id != null) {
                m.id = $String(d.id);
            }
            if (d.participant != null) {
                m.participant = $String(d.participant);
            }
            return m;
        };

        MessageKey.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.remoteJid != null && $Object.hasOwnProperty.call(m, "remoteJid")) {
                d.remoteJid = m.remoteJid;
            }
            if (m.fromMe != null && $Object.hasOwnProperty.call(m, "fromMe")) {
                d.fromMe = m.fromMe;
            }
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant")) {
                d.participant = m.participant;
            }
            return d;
        };

        MessageKey.prototype.toJSON = function() {
            return MessageKey.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageKey.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageKey";
        };

        return MessageKey;
    })();

    proto.MessageSecretMessage = (function() {

        const MessageSecretMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageSecretMessage.prototype.version = null;
        MessageSecretMessage.prototype.encIv = null;
        MessageSecretMessage.prototype.encPayload = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageSecretMessage.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageSecretMessage.prototype, "_encIv", {
            get: $util.oneOfGetter($oneOfFields = ["encIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageSecretMessage.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageSecretMessage.create = function(properties) {
            return new MessageSecretMessage(properties);
        };

        MessageSecretMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(13).sfixed32(m.version);
            if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                w.uint32(18).bytes(m.encIv);
            if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(26).bytes(m.encPayload);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageSecretMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageSecretMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 5)
                            break;
                        m.version = r.sfixed32();
                        m._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.encIv = r.bytes();
                        m._encIv = "encIv";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.encPayload = r.bytes();
                        m._encPayload = "encPayload";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageSecretMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageSecretMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageSecretMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageSecretMessage();
            if (d.version != null) {
                m.version = d.version | 0;
            }
            if (d.encIv != null) {
                if (typeof d.encIv === "string")
                    $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                else if (d.encIv.length >= 0)
                    m.encIv = d.encIv;
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            return m;
        };

        MessageSecretMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = m.version;
            }
            if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
            }
            if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
            }
            return d;
        };

        MessageSecretMessage.prototype.toJSON = function() {
            return MessageSecretMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageSecretMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageSecretMessage";
        };

        return MessageSecretMessage;
    })();

    proto.MessageText = (function() {

        const MessageText = function (p) {
            this.mentionedJid = [];
            this.commands = [];
            this.mentions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MessageText.prototype.text = null;
        MessageText.prototype.mentionedJid = $util.emptyArray;
        MessageText.prototype.commands = $util.emptyArray;
        MessageText.prototype.mentions = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MessageText.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MessageText.create = function(properties) {
            return new MessageText(properties);
        };

        MessageText.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                w.uint32(10).string(m.text);
            if (m.mentionedJid != null && m.mentionedJid.length) {
                for (var i = 0; i < m.mentionedJid.length; ++i)
                    w.uint32(18).string(m.mentionedJid[i]);
            }
            if (m.commands != null && m.commands.length) {
                for (var i = 0; i < m.commands.length; ++i)
                    $root.proto.Command.encode(m.commands[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.mentions != null && m.mentions.length) {
                for (var i = 0; i < m.mentions.length; ++i)
                    $root.proto.Mention.encode(m.mentions[i], w.uint32(34).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MessageText.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MessageText();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.text = r.stringVerify();
                        m._text = "text";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.mentionedJid && m.mentionedJid.length))
                            m.mentionedJid = [];
                        m.mentionedJid.push(r.stringVerify());
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.commands && m.commands.length))
                            m.commands = [];
                        m.commands.push($root.proto.Command.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(m.mentions && m.mentions.length))
                            m.mentions = [];
                        m.mentions.push($root.proto.Mention.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MessageText.fromObject = function (d, q) {
            if (d instanceof $root.proto.MessageText)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MessageText: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MessageText();
            if (d.text != null) {
                m.text = $String(d.text);
            }
            if (d.mentionedJid) {
                if (!$Array.isArray(d.mentionedJid))
                    throw $TypeError(".proto.MessageText.mentionedJid: array expected");
                m.mentionedJid = $Array(d.mentionedJid.length);
                for (var i = 0; i < d.mentionedJid.length; ++i) {
                    m.mentionedJid[i] = $String(d.mentionedJid[i]);
                }
            }
            if (d.commands) {
                if (!$Array.isArray(d.commands))
                    throw $TypeError(".proto.MessageText.commands: array expected");
                m.commands = $Array(d.commands.length);
                for (var i = 0; i < d.commands.length; ++i) {
                    if (!$util.isObject(d.commands[i]))
                        throw $TypeError(".proto.MessageText.commands: object expected");
                    m.commands[i] = $root.proto.Command.fromObject(d.commands[i], q + 1);
                }
            }
            if (d.mentions) {
                if (!$Array.isArray(d.mentions))
                    throw $TypeError(".proto.MessageText.mentions: array expected");
                m.mentions = $Array(d.mentions.length);
                for (var i = 0; i < d.mentions.length; ++i) {
                    if (!$util.isObject(d.mentions[i]))
                        throw $TypeError(".proto.MessageText.mentions: object expected");
                    m.mentions[i] = $root.proto.Mention.fromObject(d.mentions[i], q + 1);
                }
            }
            return m;
        };

        MessageText.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mentionedJid = [];
                d.commands = [];
                d.mentions = [];
            }
            if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                d.text = m.text;
            }
            if (m.mentionedJid && m.mentionedJid.length) {
                d.mentionedJid = $Array(m.mentionedJid.length);
                for (var j = 0; j < m.mentionedJid.length; ++j) {
                    d.mentionedJid[j] = m.mentionedJid[j];
                }
            }
            if (m.commands && m.commands.length) {
                d.commands = $Array(m.commands.length);
                for (var j = 0; j < m.commands.length; ++j) {
                    d.commands[j] = $root.proto.Command.toObject(m.commands[j], o, q + 1);
                }
            }
            if (m.mentions && m.mentions.length) {
                d.mentions = $Array(m.mentions.length);
                for (var j = 0; j < m.mentions.length; ++j) {
                    d.mentions[j] = $root.proto.Mention.toObject(m.mentions[j], o, q + 1);
                }
            }
            return d;
        };

        MessageText.prototype.toJSON = function() {
            return MessageText.toObject(this, $protobuf.util.toJSONOptions);
        };

        MessageText.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MessageText";
        };

        return MessageText;
    })();

    proto.Money = (function() {

        const Money = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Money.prototype.value = null;
        Money.prototype.offset = null;
        Money.prototype.currencyCode = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Money.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Money.prototype, "_offset", {
            get: $util.oneOfGetter($oneOfFields = ["offset"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Money.prototype, "_currencyCode", {
            get: $util.oneOfGetter($oneOfFields = ["currencyCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Money.create = function(properties) {
            return new Money(properties);
        };

        Money.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                w.uint32(8).int64(m.value);
            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset"))
                w.uint32(16).uint32(m.offset);
            if (m.currencyCode != null && $Object.hasOwnProperty.call(m, "currencyCode"))
                w.uint32(26).string(m.currencyCode);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Money.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Money();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.value = r.int64();
                        m._value = "value";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.offset = r.uint32();
                        m._offset = "offset";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.currencyCode = r.stringVerify();
                        m._currencyCode = "currencyCode";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Money.fromObject = function (d, q) {
            if (d instanceof $root.proto.Money)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Money: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Money();
            if (d.value != null) {
                if ($util.Long)
                    m.value = $util.Long.fromValue(d.value, false);
                else if (typeof d.value === "string")
                    m.value = $parseInt(d.value, 10);
                else if (typeof d.value === "number")
                    m.value = d.value;
                else if (typeof d.value === "object")
                    m.value = new $util.LongBits(d.value.low >>> 0, d.value.high >>> 0).toNumber();
            }
            if (d.offset != null) {
                m.offset = d.offset >>> 0;
            }
            if (d.currencyCode != null) {
                m.currencyCode = $String(d.currencyCode);
            }
            return m;
        };

        Money.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.value = typeof m.value === "number" ? $BigInt(m.value) : $util.Long.fromBits(m.value.low >>> 0, m.value.high >>> 0, false).toBigInt();
                else if (typeof m.value === "number")
                    d.value = o.longs === $String ? $String(m.value) : m.value;
                else
                    d.value = o.longs === $String ? $util.Long.prototype.toString.call(m.value) : o.longs === $Number ? new $util.LongBits(m.value.low >>> 0, m.value.high >>> 0).toNumber() : m.value;
            }
            if (m.offset != null && $Object.hasOwnProperty.call(m, "offset")) {
                d.offset = m.offset;
            }
            if (m.currencyCode != null && $Object.hasOwnProperty.call(m, "currencyCode")) {
                d.currencyCode = m.currencyCode;
            }
            return d;
        };

        Money.prototype.toJSON = function() {
            return Money.toObject(this, $protobuf.util.toJSONOptions);
        };

        Money.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Money";
        };

        return Money;
    })();

    proto.MsgOpaqueData = (function() {

        const MsgOpaqueData = function (p) {
            this.pollOptions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MsgOpaqueData.prototype.body = null;
        MsgOpaqueData.prototype.caption = null;
        MsgOpaqueData.prototype.lng = null;
        MsgOpaqueData.prototype.isLive = null;
        MsgOpaqueData.prototype.lat = null;
        MsgOpaqueData.prototype.paymentAmount1000 = null;
        MsgOpaqueData.prototype.paymentNoteMsgBody = null;
        MsgOpaqueData.prototype.matchedText = null;
        MsgOpaqueData.prototype.title = null;
        MsgOpaqueData.prototype.description = null;
        MsgOpaqueData.prototype.futureproofBuffer = null;
        MsgOpaqueData.prototype.clientUrl = null;
        MsgOpaqueData.prototype.loc = null;
        MsgOpaqueData.prototype.pollName = null;
        MsgOpaqueData.prototype.pollOptions = $util.emptyArray;
        MsgOpaqueData.prototype.pollSelectableOptionsCount = null;
        MsgOpaqueData.prototype.messageSecret = null;
        MsgOpaqueData.prototype.originalSelfAuthor = null;
        MsgOpaqueData.prototype.senderTimestampMs = null;
        MsgOpaqueData.prototype.pollUpdateParentKey = null;
        MsgOpaqueData.prototype.encPollVote = null;
        MsgOpaqueData.prototype.isSentCagPollCreation = null;
        MsgOpaqueData.prototype.pollContentType = null;
        MsgOpaqueData.prototype.pollType = null;
        MsgOpaqueData.prototype.correctOptionIndex = null;
        MsgOpaqueData.prototype.pollVotesSnapshot = null;
        MsgOpaqueData.prototype.encReactionTargetMessageKey = null;
        MsgOpaqueData.prototype.encReactionEncPayload = null;
        MsgOpaqueData.prototype.encReactionEncIv = null;
        MsgOpaqueData.prototype.botMessageSecret = null;
        MsgOpaqueData.prototype.targetMessageKey = null;
        MsgOpaqueData.prototype.encPayload = null;
        MsgOpaqueData.prototype.encIv = null;
        MsgOpaqueData.prototype.eventName = null;
        MsgOpaqueData.prototype.isEventCanceled = null;
        MsgOpaqueData.prototype.eventDescription = null;
        MsgOpaqueData.prototype.eventJoinLink = null;
        MsgOpaqueData.prototype.eventStartTime = null;
        MsgOpaqueData.prototype.eventLocation = null;
        MsgOpaqueData.prototype.eventEndTime = null;
        MsgOpaqueData.prototype.eventIsScheduledCall = null;
        MsgOpaqueData.prototype.eventExtraGuestsAllowed = null;
        MsgOpaqueData.prototype.plainProtobufBytes = null;
        MsgOpaqueData.prototype.quarantineExtractedText = null;
        MsgOpaqueData.prototype.pollEndTime = null;
        MsgOpaqueData.prototype.pollHideVoterNames = null;
        MsgOpaqueData.prototype.pollAllowAddOption = null;
        MsgOpaqueData.prototype.sharableEventInviteId = null;
        MsgOpaqueData.prototype.sharableEventInviteTitle = null;
        MsgOpaqueData.prototype.sharableEventInviteStartTime = null;
        MsgOpaqueData.prototype.sharableEventInviteEndTime = null;
        MsgOpaqueData.prototype.sharableEventInviteCaption = null;
        MsgOpaqueData.prototype.sharableEventInviteIsCanceled = null;
        MsgOpaqueData.prototype.sharableEventInviteJpegThumbnail = null;
        MsgOpaqueData.prototype.sharableEventInviteCallLink = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_body", {
            get: $util.oneOfGetter($oneOfFields = ["body"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_caption", {
            get: $util.oneOfGetter($oneOfFields = ["caption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_lng", {
            get: $util.oneOfGetter($oneOfFields = ["lng"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_isLive", {
            get: $util.oneOfGetter($oneOfFields = ["isLive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_lat", {
            get: $util.oneOfGetter($oneOfFields = ["lat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_paymentAmount1000", {
            get: $util.oneOfGetter($oneOfFields = ["paymentAmount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_paymentNoteMsgBody", {
            get: $util.oneOfGetter($oneOfFields = ["paymentNoteMsgBody"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_matchedText", {
            get: $util.oneOfGetter($oneOfFields = ["matchedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_description", {
            get: $util.oneOfGetter($oneOfFields = ["description"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_futureproofBuffer", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofBuffer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_clientUrl", {
            get: $util.oneOfGetter($oneOfFields = ["clientUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_loc", {
            get: $util.oneOfGetter($oneOfFields = ["loc"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollName", {
            get: $util.oneOfGetter($oneOfFields = ["pollName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollSelectableOptionsCount", {
            get: $util.oneOfGetter($oneOfFields = ["pollSelectableOptionsCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_originalSelfAuthor", {
            get: $util.oneOfGetter($oneOfFields = ["originalSelfAuthor"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollUpdateParentKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateParentKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_encPollVote", {
            get: $util.oneOfGetter($oneOfFields = ["encPollVote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_isSentCagPollCreation", {
            get: $util.oneOfGetter($oneOfFields = ["isSentCagPollCreation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollContentType", {
            get: $util.oneOfGetter($oneOfFields = ["pollContentType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollType", {
            get: $util.oneOfGetter($oneOfFields = ["pollType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_correctOptionIndex", {
            get: $util.oneOfGetter($oneOfFields = ["correctOptionIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollVotesSnapshot", {
            get: $util.oneOfGetter($oneOfFields = ["pollVotesSnapshot"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_encReactionTargetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionTargetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_encReactionEncPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionEncPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_encReactionEncIv", {
            get: $util.oneOfGetter($oneOfFields = ["encReactionEncIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_botMessageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_targetMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_encIv", {
            get: $util.oneOfGetter($oneOfFields = ["encIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventName", {
            get: $util.oneOfGetter($oneOfFields = ["eventName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_isEventCanceled", {
            get: $util.oneOfGetter($oneOfFields = ["isEventCanceled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventDescription", {
            get: $util.oneOfGetter($oneOfFields = ["eventDescription"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventJoinLink", {
            get: $util.oneOfGetter($oneOfFields = ["eventJoinLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventStartTime", {
            get: $util.oneOfGetter($oneOfFields = ["eventStartTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventLocation", {
            get: $util.oneOfGetter($oneOfFields = ["eventLocation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["eventEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventIsScheduledCall", {
            get: $util.oneOfGetter($oneOfFields = ["eventIsScheduledCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_eventExtraGuestsAllowed", {
            get: $util.oneOfGetter($oneOfFields = ["eventExtraGuestsAllowed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_plainProtobufBytes", {
            get: $util.oneOfGetter($oneOfFields = ["plainProtobufBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_quarantineExtractedText", {
            get: $util.oneOfGetter($oneOfFields = ["quarantineExtractedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["pollEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollHideVoterNames", {
            get: $util.oneOfGetter($oneOfFields = ["pollHideVoterNames"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_pollAllowAddOption", {
            get: $util.oneOfGetter($oneOfFields = ["pollAllowAddOption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteId", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteTitle", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteTitle"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteStartTime", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteStartTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteEndTime", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteEndTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteCaption", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteCaption"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteIsCanceled", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteIsCanceled"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteJpegThumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteJpegThumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgOpaqueData.prototype, "_sharableEventInviteCallLink", {
            get: $util.oneOfGetter($oneOfFields = ["sharableEventInviteCallLink"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MsgOpaqueData.create = function(properties) {
            return new MsgOpaqueData(properties);
        };

        MsgOpaqueData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.body != null && $Object.hasOwnProperty.call(m, "body"))
                w.uint32(10).string(m.body);
            if (m.caption != null && $Object.hasOwnProperty.call(m, "caption"))
                w.uint32(26).string(m.caption);
            if (m.lng != null && $Object.hasOwnProperty.call(m, "lng"))
                w.uint32(41).double(m.lng);
            if (m.isLive != null && $Object.hasOwnProperty.call(m, "isLive"))
                w.uint32(48).bool(m.isLive);
            if (m.lat != null && $Object.hasOwnProperty.call(m, "lat"))
                w.uint32(57).double(m.lat);
            if (m.paymentAmount1000 != null && $Object.hasOwnProperty.call(m, "paymentAmount1000"))
                w.uint32(64).int32(m.paymentAmount1000);
            if (m.paymentNoteMsgBody != null && $Object.hasOwnProperty.call(m, "paymentNoteMsgBody"))
                w.uint32(74).string(m.paymentNoteMsgBody);
            if (m.matchedText != null && $Object.hasOwnProperty.call(m, "matchedText"))
                w.uint32(90).string(m.matchedText);
            if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                w.uint32(98).string(m.title);
            if (m.description != null && $Object.hasOwnProperty.call(m, "description"))
                w.uint32(106).string(m.description);
            if (m.futureproofBuffer != null && $Object.hasOwnProperty.call(m, "futureproofBuffer"))
                w.uint32(114).bytes(m.futureproofBuffer);
            if (m.clientUrl != null && $Object.hasOwnProperty.call(m, "clientUrl"))
                w.uint32(122).string(m.clientUrl);
            if (m.loc != null && $Object.hasOwnProperty.call(m, "loc"))
                w.uint32(130).string(m.loc);
            if (m.pollName != null && $Object.hasOwnProperty.call(m, "pollName"))
                w.uint32(138).string(m.pollName);
            if (m.pollOptions != null && m.pollOptions.length) {
                for (var i = 0; i < m.pollOptions.length; ++i)
                    $root.proto.MsgOpaqueData.PollOption.encode(m.pollOptions[i], w.uint32(146).fork(), q + 1).ldelim();
            }
            if (m.pollSelectableOptionsCount != null && $Object.hasOwnProperty.call(m, "pollSelectableOptionsCount"))
                w.uint32(160).uint32(m.pollSelectableOptionsCount);
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(170).bytes(m.messageSecret);
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                w.uint32(176).int64(m.senderTimestampMs);
            if (m.pollUpdateParentKey != null && $Object.hasOwnProperty.call(m, "pollUpdateParentKey"))
                w.uint32(186).string(m.pollUpdateParentKey);
            if (m.encPollVote != null && $Object.hasOwnProperty.call(m, "encPollVote"))
                $root.proto.PollEncValue.encode(m.encPollVote, w.uint32(194).fork(), q + 1).ldelim();
            if (m.encReactionTargetMessageKey != null && $Object.hasOwnProperty.call(m, "encReactionTargetMessageKey"))
                w.uint32(202).string(m.encReactionTargetMessageKey);
            if (m.encReactionEncPayload != null && $Object.hasOwnProperty.call(m, "encReactionEncPayload"))
                w.uint32(210).bytes(m.encReactionEncPayload);
            if (m.encReactionEncIv != null && $Object.hasOwnProperty.call(m, "encReactionEncIv"))
                w.uint32(218).bytes(m.encReactionEncIv);
            if (m.isSentCagPollCreation != null && $Object.hasOwnProperty.call(m, "isSentCagPollCreation"))
                w.uint32(224).bool(m.isSentCagPollCreation);
            if (m.botMessageSecret != null && $Object.hasOwnProperty.call(m, "botMessageSecret"))
                w.uint32(234).bytes(m.botMessageSecret);
            if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey"))
                w.uint32(242).string(m.targetMessageKey);
            if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(250).bytes(m.encPayload);
            if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                w.uint32(258).bytes(m.encIv);
            if (m.eventName != null && $Object.hasOwnProperty.call(m, "eventName"))
                w.uint32(266).string(m.eventName);
            if (m.isEventCanceled != null && $Object.hasOwnProperty.call(m, "isEventCanceled"))
                w.uint32(272).bool(m.isEventCanceled);
            if (m.eventDescription != null && $Object.hasOwnProperty.call(m, "eventDescription"))
                w.uint32(282).string(m.eventDescription);
            if (m.eventJoinLink != null && $Object.hasOwnProperty.call(m, "eventJoinLink"))
                w.uint32(290).string(m.eventJoinLink);
            if (m.eventStartTime != null && $Object.hasOwnProperty.call(m, "eventStartTime"))
                w.uint32(296).int64(m.eventStartTime);
            if (m.eventLocation != null && $Object.hasOwnProperty.call(m, "eventLocation"))
                $root.proto.MsgOpaqueData.EventLocation.encode(m.eventLocation, w.uint32(306).fork(), q + 1).ldelim();
            if (m.eventEndTime != null && $Object.hasOwnProperty.call(m, "eventEndTime"))
                w.uint32(320).int64(m.eventEndTime);
            if (m.pollVotesSnapshot != null && $Object.hasOwnProperty.call(m, "pollVotesSnapshot"))
                $root.proto.MsgOpaqueData.PollVotesSnapshot.encode(m.pollVotesSnapshot, w.uint32(330).fork(), q + 1).ldelim();
            if (m.pollContentType != null && $Object.hasOwnProperty.call(m, "pollContentType"))
                w.uint32(336).int32(m.pollContentType);
            if (m.plainProtobufBytes != null && $Object.hasOwnProperty.call(m, "plainProtobufBytes"))
                w.uint32(346).bytes(m.plainProtobufBytes);
            if (m.eventIsScheduledCall != null && $Object.hasOwnProperty.call(m, "eventIsScheduledCall"))
                w.uint32(352).bool(m.eventIsScheduledCall);
            if (m.eventExtraGuestsAllowed != null && $Object.hasOwnProperty.call(m, "eventExtraGuestsAllowed"))
                w.uint32(360).bool(m.eventExtraGuestsAllowed);
            if (m.pollType != null && $Object.hasOwnProperty.call(m, "pollType"))
                w.uint32(368).int32(m.pollType);
            if (m.correctOptionIndex != null && $Object.hasOwnProperty.call(m, "correctOptionIndex"))
                w.uint32(376).int32(m.correctOptionIndex);
            if (m.quarantineExtractedText != null && $Object.hasOwnProperty.call(m, "quarantineExtractedText"))
                w.uint32(386).string(m.quarantineExtractedText);
            if (m.pollEndTime != null && $Object.hasOwnProperty.call(m, "pollEndTime"))
                w.uint32(392).int64(m.pollEndTime);
            if (m.pollHideVoterNames != null && $Object.hasOwnProperty.call(m, "pollHideVoterNames"))
                w.uint32(400).bool(m.pollHideVoterNames);
            if (m.originalSelfAuthor != null && $Object.hasOwnProperty.call(m, "originalSelfAuthor"))
                w.uint32(410).string(m.originalSelfAuthor);
            if (m.pollAllowAddOption != null && $Object.hasOwnProperty.call(m, "pollAllowAddOption"))
                w.uint32(416).bool(m.pollAllowAddOption);
            if (m.sharableEventInviteId != null && $Object.hasOwnProperty.call(m, "sharableEventInviteId"))
                w.uint32(426).string(m.sharableEventInviteId);
            if (m.sharableEventInviteTitle != null && $Object.hasOwnProperty.call(m, "sharableEventInviteTitle"))
                w.uint32(434).string(m.sharableEventInviteTitle);
            if (m.sharableEventInviteStartTime != null && $Object.hasOwnProperty.call(m, "sharableEventInviteStartTime"))
                w.uint32(440).int64(m.sharableEventInviteStartTime);
            if (m.sharableEventInviteEndTime != null && $Object.hasOwnProperty.call(m, "sharableEventInviteEndTime"))
                w.uint32(448).int64(m.sharableEventInviteEndTime);
            if (m.sharableEventInviteCaption != null && $Object.hasOwnProperty.call(m, "sharableEventInviteCaption"))
                w.uint32(458).string(m.sharableEventInviteCaption);
            if (m.sharableEventInviteIsCanceled != null && $Object.hasOwnProperty.call(m, "sharableEventInviteIsCanceled"))
                w.uint32(464).bool(m.sharableEventInviteIsCanceled);
            if (m.sharableEventInviteJpegThumbnail != null && $Object.hasOwnProperty.call(m, "sharableEventInviteJpegThumbnail"))
                w.uint32(474).bytes(m.sharableEventInviteJpegThumbnail);
            if (m.sharableEventInviteCallLink != null && $Object.hasOwnProperty.call(m, "sharableEventInviteCallLink"))
                w.uint32(482).string(m.sharableEventInviteCallLink);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MsgOpaqueData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MsgOpaqueData(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.body = r.stringVerify();
                        m._body = "body";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.caption = r.stringVerify();
                        m._caption = "caption";
                        continue;
                    }
                case 5: {
                        if (u !== 1)
                            break;
                        m.lng = r.double();
                        m._lng = "lng";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.isLive = r.bool();
                        m._isLive = "isLive";
                        continue;
                    }
                case 7: {
                        if (u !== 1)
                            break;
                        m.lat = r.double();
                        m._lat = "lat";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.paymentAmount1000 = r.int32();
                        m._paymentAmount1000 = "paymentAmount1000";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.paymentNoteMsgBody = r.stringVerify();
                        m._paymentNoteMsgBody = "paymentNoteMsgBody";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.matchedText = r.stringVerify();
                        m._matchedText = "matchedText";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.title = r.stringVerify();
                        m._title = "title";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.description = r.stringVerify();
                        m._description = "description";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.futureproofBuffer = r.bytes();
                        m._futureproofBuffer = "futureproofBuffer";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.clientUrl = r.stringVerify();
                        m._clientUrl = "clientUrl";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        m.loc = r.stringVerify();
                        m._loc = "loc";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.pollName = r.stringVerify();
                        m._pollName = "pollName";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        if (!(m.pollOptions && m.pollOptions.length))
                            m.pollOptions = [];
                        m.pollOptions.push($root.proto.MsgOpaqueData.PollOption.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        m.pollSelectableOptionsCount = r.uint32();
                        m._pollSelectableOptionsCount = "pollSelectableOptionsCount";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        m.messageSecret = r.bytes();
                        m._messageSecret = "messageSecret";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        m.originalSelfAuthor = r.stringVerify();
                        m._originalSelfAuthor = "originalSelfAuthor";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        m.senderTimestampMs = r.int64();
                        m._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        m.pollUpdateParentKey = r.stringVerify();
                        m._pollUpdateParentKey = "pollUpdateParentKey";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        m.encPollVote = $root.proto.PollEncValue.decode(r, r.uint32(), $undefined, q + 1, m.encPollVote);
                        m._encPollVote = "encPollVote";
                        continue;
                    }
                case 28: {
                        if (u !== 0)
                            break;
                        m.isSentCagPollCreation = r.bool();
                        m._isSentCagPollCreation = "isSentCagPollCreation";
                        continue;
                    }
                case 42: {
                        if (u !== 0)
                            break;
                        m.pollContentType = r.int32();
                        m._pollContentType = "pollContentType";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        m.pollType = r.int32();
                        m._pollType = "pollType";
                        continue;
                    }
                case 47: {
                        if (u !== 0)
                            break;
                        m.correctOptionIndex = r.int32();
                        m._correctOptionIndex = "correctOptionIndex";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        m.pollVotesSnapshot = $root.proto.MsgOpaqueData.PollVotesSnapshot.decode(r, r.uint32(), $undefined, q + 1, m.pollVotesSnapshot);
                        m._pollVotesSnapshot = "pollVotesSnapshot";
                        continue;
                    }
                case 25: {
                        if (u !== 2)
                            break;
                        m.encReactionTargetMessageKey = r.stringVerify();
                        m._encReactionTargetMessageKey = "encReactionTargetMessageKey";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        m.encReactionEncPayload = r.bytes();
                        m._encReactionEncPayload = "encReactionEncPayload";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        m.encReactionEncIv = r.bytes();
                        m._encReactionEncIv = "encReactionEncIv";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        m.botMessageSecret = r.bytes();
                        m._botMessageSecret = "botMessageSecret";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        m.targetMessageKey = r.stringVerify();
                        m._targetMessageKey = "targetMessageKey";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        m.encPayload = r.bytes();
                        m._encPayload = "encPayload";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        m.encIv = r.bytes();
                        m._encIv = "encIv";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        m.eventName = r.stringVerify();
                        m._eventName = "eventName";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        m.isEventCanceled = r.bool();
                        m._isEventCanceled = "isEventCanceled";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        m.eventDescription = r.stringVerify();
                        m._eventDescription = "eventDescription";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        m.eventJoinLink = r.stringVerify();
                        m._eventJoinLink = "eventJoinLink";
                        continue;
                    }
                case 37: {
                        if (u !== 0)
                            break;
                        m.eventStartTime = r.int64();
                        m._eventStartTime = "eventStartTime";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.eventLocation = $root.proto.MsgOpaqueData.EventLocation.decode(r, r.uint32(), $undefined, q + 1, m.eventLocation);
                        m._eventLocation = "eventLocation";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        m.eventEndTime = r.int64();
                        m._eventEndTime = "eventEndTime";
                        continue;
                    }
                case 44: {
                        if (u !== 0)
                            break;
                        m.eventIsScheduledCall = r.bool();
                        m._eventIsScheduledCall = "eventIsScheduledCall";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        m.eventExtraGuestsAllowed = r.bool();
                        m._eventExtraGuestsAllowed = "eventExtraGuestsAllowed";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.plainProtobufBytes = r.bytes();
                        m._plainProtobufBytes = "plainProtobufBytes";
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        m.quarantineExtractedText = r.stringVerify();
                        m._quarantineExtractedText = "quarantineExtractedText";
                        continue;
                    }
                case 49: {
                        if (u !== 0)
                            break;
                        m.pollEndTime = r.int64();
                        m._pollEndTime = "pollEndTime";
                        continue;
                    }
                case 50: {
                        if (u !== 0)
                            break;
                        m.pollHideVoterNames = r.bool();
                        m._pollHideVoterNames = "pollHideVoterNames";
                        continue;
                    }
                case 52: {
                        if (u !== 0)
                            break;
                        m.pollAllowAddOption = r.bool();
                        m._pollAllowAddOption = "pollAllowAddOption";
                        continue;
                    }
                case 53: {
                        if (u !== 2)
                            break;
                        m.sharableEventInviteId = r.stringVerify();
                        m._sharableEventInviteId = "sharableEventInviteId";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        m.sharableEventInviteTitle = r.stringVerify();
                        m._sharableEventInviteTitle = "sharableEventInviteTitle";
                        continue;
                    }
                case 55: {
                        if (u !== 0)
                            break;
                        m.sharableEventInviteStartTime = r.int64();
                        m._sharableEventInviteStartTime = "sharableEventInviteStartTime";
                        continue;
                    }
                case 56: {
                        if (u !== 0)
                            break;
                        m.sharableEventInviteEndTime = r.int64();
                        m._sharableEventInviteEndTime = "sharableEventInviteEndTime";
                        continue;
                    }
                case 57: {
                        if (u !== 2)
                            break;
                        m.sharableEventInviteCaption = r.stringVerify();
                        m._sharableEventInviteCaption = "sharableEventInviteCaption";
                        continue;
                    }
                case 58: {
                        if (u !== 0)
                            break;
                        m.sharableEventInviteIsCanceled = r.bool();
                        m._sharableEventInviteIsCanceled = "sharableEventInviteIsCanceled";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        m.sharableEventInviteJpegThumbnail = r.bytes();
                        m._sharableEventInviteJpegThumbnail = "sharableEventInviteJpegThumbnail";
                        continue;
                    }
                case 60: {
                        if (u !== 2)
                            break;
                        m.sharableEventInviteCallLink = r.stringVerify();
                        m._sharableEventInviteCallLink = "sharableEventInviteCallLink";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MsgOpaqueData.fromObject = function (d, q) {
            if (d instanceof $root.proto.MsgOpaqueData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MsgOpaqueData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MsgOpaqueData();
            if (d.body != null) {
                m.body = $String(d.body);
            }
            if (d.caption != null) {
                m.caption = $String(d.caption);
            }
            if (d.lng != null) {
                m.lng = $Number(d.lng);
            }
            if (d.isLive != null) {
                m.isLive = $Boolean(d.isLive);
            }
            if (d.lat != null) {
                m.lat = $Number(d.lat);
            }
            if (d.paymentAmount1000 != null) {
                m.paymentAmount1000 = d.paymentAmount1000 | 0;
            }
            if (d.paymentNoteMsgBody != null) {
                m.paymentNoteMsgBody = $String(d.paymentNoteMsgBody);
            }
            if (d.matchedText != null) {
                m.matchedText = $String(d.matchedText);
            }
            if (d.title != null) {
                m.title = $String(d.title);
            }
            if (d.description != null) {
                m.description = $String(d.description);
            }
            if (d.futureproofBuffer != null) {
                if (typeof d.futureproofBuffer === "string")
                    $util.base64.decode(d.futureproofBuffer, m.futureproofBuffer = $util.newBuffer($util.base64.length(d.futureproofBuffer)), 0);
                else if (d.futureproofBuffer.length >= 0)
                    m.futureproofBuffer = d.futureproofBuffer;
            }
            if (d.clientUrl != null) {
                m.clientUrl = $String(d.clientUrl);
            }
            if (d.loc != null) {
                m.loc = $String(d.loc);
            }
            if (d.pollName != null) {
                m.pollName = $String(d.pollName);
            }
            if (d.pollOptions) {
                if (!$Array.isArray(d.pollOptions))
                    throw $TypeError(".proto.MsgOpaqueData.pollOptions: array expected");
                m.pollOptions = $Array(d.pollOptions.length);
                for (var i = 0; i < d.pollOptions.length; ++i) {
                    if (!$util.isObject(d.pollOptions[i]))
                        throw $TypeError(".proto.MsgOpaqueData.pollOptions: object expected");
                    m.pollOptions[i] = $root.proto.MsgOpaqueData.PollOption.fromObject(d.pollOptions[i], q + 1);
                }
            }
            if (d.pollSelectableOptionsCount != null) {
                m.pollSelectableOptionsCount = d.pollSelectableOptionsCount >>> 0;
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            if (d.originalSelfAuthor != null) {
                m.originalSelfAuthor = $String(d.originalSelfAuthor);
            }
            if (d.senderTimestampMs != null) {
                if ($util.Long)
                    m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                else if (typeof d.senderTimestampMs === "string")
                    m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                else if (typeof d.senderTimestampMs === "number")
                    m.senderTimestampMs = d.senderTimestampMs;
                else if (typeof d.senderTimestampMs === "object")
                    m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
            }
            if (d.pollUpdateParentKey != null) {
                m.pollUpdateParentKey = $String(d.pollUpdateParentKey);
            }
            if (d.encPollVote != null) {
                if (!$util.isObject(d.encPollVote))
                    throw $TypeError(".proto.MsgOpaqueData.encPollVote: object expected");
                m.encPollVote = $root.proto.PollEncValue.fromObject(d.encPollVote, q + 1);
            }
            if (d.isSentCagPollCreation != null) {
                m.isSentCagPollCreation = $Boolean(d.isSentCagPollCreation);
            }
            switch (d.pollContentType) {
            case "UNKNOWN":
            case 0:
                m.pollContentType = 0;
                break;
            case "TEXT":
            case 1:
                m.pollContentType = 1;
                break;
            case "IMAGE":
            case 2:
                m.pollContentType = 2;
                break;
            default:
                if (typeof d.pollContentType === "number" && (d.pollContentType | 0) === d.pollContentType)
                    m.pollContentType = d.pollContentType;
            }
            switch (d.pollType) {
            case "POLL":
            case 0:
                m.pollType = 0;
                break;
            case "QUIZ":
            case 1:
                m.pollType = 1;
                break;
            default:
                if (typeof d.pollType === "number" && (d.pollType | 0) === d.pollType)
                    m.pollType = d.pollType;
            }
            if (d.correctOptionIndex != null) {
                m.correctOptionIndex = d.correctOptionIndex | 0;
            }
            if (d.pollVotesSnapshot != null) {
                if (!$util.isObject(d.pollVotesSnapshot))
                    throw $TypeError(".proto.MsgOpaqueData.pollVotesSnapshot: object expected");
                m.pollVotesSnapshot = $root.proto.MsgOpaqueData.PollVotesSnapshot.fromObject(d.pollVotesSnapshot, q + 1);
            }
            if (d.encReactionTargetMessageKey != null) {
                m.encReactionTargetMessageKey = $String(d.encReactionTargetMessageKey);
            }
            if (d.encReactionEncPayload != null) {
                if (typeof d.encReactionEncPayload === "string")
                    $util.base64.decode(d.encReactionEncPayload, m.encReactionEncPayload = $util.newBuffer($util.base64.length(d.encReactionEncPayload)), 0);
                else if (d.encReactionEncPayload.length >= 0)
                    m.encReactionEncPayload = d.encReactionEncPayload;
            }
            if (d.encReactionEncIv != null) {
                if (typeof d.encReactionEncIv === "string")
                    $util.base64.decode(d.encReactionEncIv, m.encReactionEncIv = $util.newBuffer($util.base64.length(d.encReactionEncIv)), 0);
                else if (d.encReactionEncIv.length >= 0)
                    m.encReactionEncIv = d.encReactionEncIv;
            }
            if (d.botMessageSecret != null) {
                if (typeof d.botMessageSecret === "string")
                    $util.base64.decode(d.botMessageSecret, m.botMessageSecret = $util.newBuffer($util.base64.length(d.botMessageSecret)), 0);
                else if (d.botMessageSecret.length >= 0)
                    m.botMessageSecret = d.botMessageSecret;
            }
            if (d.targetMessageKey != null) {
                m.targetMessageKey = $String(d.targetMessageKey);
            }
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIv != null) {
                if (typeof d.encIv === "string")
                    $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                else if (d.encIv.length >= 0)
                    m.encIv = d.encIv;
            }
            if (d.eventName != null) {
                m.eventName = $String(d.eventName);
            }
            if (d.isEventCanceled != null) {
                m.isEventCanceled = $Boolean(d.isEventCanceled);
            }
            if (d.eventDescription != null) {
                m.eventDescription = $String(d.eventDescription);
            }
            if (d.eventJoinLink != null) {
                m.eventJoinLink = $String(d.eventJoinLink);
            }
            if (d.eventStartTime != null) {
                if ($util.Long)
                    m.eventStartTime = $util.Long.fromValue(d.eventStartTime, false);
                else if (typeof d.eventStartTime === "string")
                    m.eventStartTime = $parseInt(d.eventStartTime, 10);
                else if (typeof d.eventStartTime === "number")
                    m.eventStartTime = d.eventStartTime;
                else if (typeof d.eventStartTime === "object")
                    m.eventStartTime = new $util.LongBits(d.eventStartTime.low >>> 0, d.eventStartTime.high >>> 0).toNumber();
            }
            if (d.eventLocation != null) {
                if (!$util.isObject(d.eventLocation))
                    throw $TypeError(".proto.MsgOpaqueData.eventLocation: object expected");
                m.eventLocation = $root.proto.MsgOpaqueData.EventLocation.fromObject(d.eventLocation, q + 1);
            }
            if (d.eventEndTime != null) {
                if ($util.Long)
                    m.eventEndTime = $util.Long.fromValue(d.eventEndTime, false);
                else if (typeof d.eventEndTime === "string")
                    m.eventEndTime = $parseInt(d.eventEndTime, 10);
                else if (typeof d.eventEndTime === "number")
                    m.eventEndTime = d.eventEndTime;
                else if (typeof d.eventEndTime === "object")
                    m.eventEndTime = new $util.LongBits(d.eventEndTime.low >>> 0, d.eventEndTime.high >>> 0).toNumber();
            }
            if (d.eventIsScheduledCall != null) {
                m.eventIsScheduledCall = $Boolean(d.eventIsScheduledCall);
            }
            if (d.eventExtraGuestsAllowed != null) {
                m.eventExtraGuestsAllowed = $Boolean(d.eventExtraGuestsAllowed);
            }
            if (d.plainProtobufBytes != null) {
                if (typeof d.plainProtobufBytes === "string")
                    $util.base64.decode(d.plainProtobufBytes, m.plainProtobufBytes = $util.newBuffer($util.base64.length(d.plainProtobufBytes)), 0);
                else if (d.plainProtobufBytes.length >= 0)
                    m.plainProtobufBytes = d.plainProtobufBytes;
            }
            if (d.quarantineExtractedText != null) {
                m.quarantineExtractedText = $String(d.quarantineExtractedText);
            }
            if (d.pollEndTime != null) {
                if ($util.Long)
                    m.pollEndTime = $util.Long.fromValue(d.pollEndTime, false);
                else if (typeof d.pollEndTime === "string")
                    m.pollEndTime = $parseInt(d.pollEndTime, 10);
                else if (typeof d.pollEndTime === "number")
                    m.pollEndTime = d.pollEndTime;
                else if (typeof d.pollEndTime === "object")
                    m.pollEndTime = new $util.LongBits(d.pollEndTime.low >>> 0, d.pollEndTime.high >>> 0).toNumber();
            }
            if (d.pollHideVoterNames != null) {
                m.pollHideVoterNames = $Boolean(d.pollHideVoterNames);
            }
            if (d.pollAllowAddOption != null) {
                m.pollAllowAddOption = $Boolean(d.pollAllowAddOption);
            }
            if (d.sharableEventInviteId != null) {
                m.sharableEventInviteId = $String(d.sharableEventInviteId);
            }
            if (d.sharableEventInviteTitle != null) {
                m.sharableEventInviteTitle = $String(d.sharableEventInviteTitle);
            }
            if (d.sharableEventInviteStartTime != null) {
                if ($util.Long)
                    m.sharableEventInviteStartTime = $util.Long.fromValue(d.sharableEventInviteStartTime, false);
                else if (typeof d.sharableEventInviteStartTime === "string")
                    m.sharableEventInviteStartTime = $parseInt(d.sharableEventInviteStartTime, 10);
                else if (typeof d.sharableEventInviteStartTime === "number")
                    m.sharableEventInviteStartTime = d.sharableEventInviteStartTime;
                else if (typeof d.sharableEventInviteStartTime === "object")
                    m.sharableEventInviteStartTime = new $util.LongBits(d.sharableEventInviteStartTime.low >>> 0, d.sharableEventInviteStartTime.high >>> 0).toNumber();
            }
            if (d.sharableEventInviteEndTime != null) {
                if ($util.Long)
                    m.sharableEventInviteEndTime = $util.Long.fromValue(d.sharableEventInviteEndTime, false);
                else if (typeof d.sharableEventInviteEndTime === "string")
                    m.sharableEventInviteEndTime = $parseInt(d.sharableEventInviteEndTime, 10);
                else if (typeof d.sharableEventInviteEndTime === "number")
                    m.sharableEventInviteEndTime = d.sharableEventInviteEndTime;
                else if (typeof d.sharableEventInviteEndTime === "object")
                    m.sharableEventInviteEndTime = new $util.LongBits(d.sharableEventInviteEndTime.low >>> 0, d.sharableEventInviteEndTime.high >>> 0).toNumber();
            }
            if (d.sharableEventInviteCaption != null) {
                m.sharableEventInviteCaption = $String(d.sharableEventInviteCaption);
            }
            if (d.sharableEventInviteIsCanceled != null) {
                m.sharableEventInviteIsCanceled = $Boolean(d.sharableEventInviteIsCanceled);
            }
            if (d.sharableEventInviteJpegThumbnail != null) {
                if (typeof d.sharableEventInviteJpegThumbnail === "string")
                    $util.base64.decode(d.sharableEventInviteJpegThumbnail, m.sharableEventInviteJpegThumbnail = $util.newBuffer($util.base64.length(d.sharableEventInviteJpegThumbnail)), 0);
                else if (d.sharableEventInviteJpegThumbnail.length >= 0)
                    m.sharableEventInviteJpegThumbnail = d.sharableEventInviteJpegThumbnail;
            }
            if (d.sharableEventInviteCallLink != null) {
                m.sharableEventInviteCallLink = $String(d.sharableEventInviteCallLink);
            }
            return m;
        };

        MsgOpaqueData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.pollOptions = [];
            }
            if (m.body != null && $Object.hasOwnProperty.call(m, "body")) {
                d.body = m.body;
            }
            if (m.caption != null && $Object.hasOwnProperty.call(m, "caption")) {
                d.caption = m.caption;
            }
            if (m.lng != null && $Object.hasOwnProperty.call(m, "lng")) {
                d.lng = o.json && !$isFinite(m.lng) ? $String(m.lng) : m.lng;
            }
            if (m.isLive != null && $Object.hasOwnProperty.call(m, "isLive")) {
                d.isLive = m.isLive;
            }
            if (m.lat != null && $Object.hasOwnProperty.call(m, "lat")) {
                d.lat = o.json && !$isFinite(m.lat) ? $String(m.lat) : m.lat;
            }
            if (m.paymentAmount1000 != null && $Object.hasOwnProperty.call(m, "paymentAmount1000")) {
                d.paymentAmount1000 = m.paymentAmount1000;
            }
            if (m.paymentNoteMsgBody != null && $Object.hasOwnProperty.call(m, "paymentNoteMsgBody")) {
                d.paymentNoteMsgBody = m.paymentNoteMsgBody;
            }
            if (m.matchedText != null && $Object.hasOwnProperty.call(m, "matchedText")) {
                d.matchedText = m.matchedText;
            }
            if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                d.title = m.title;
            }
            if (m.description != null && $Object.hasOwnProperty.call(m, "description")) {
                d.description = m.description;
            }
            if (m.futureproofBuffer != null && $Object.hasOwnProperty.call(m, "futureproofBuffer")) {
                d.futureproofBuffer = o.bytes === $String ? $util.base64.encode(m.futureproofBuffer, 0, m.futureproofBuffer.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.futureproofBuffer) : m.futureproofBuffer;
            }
            if (m.clientUrl != null && $Object.hasOwnProperty.call(m, "clientUrl")) {
                d.clientUrl = m.clientUrl;
            }
            if (m.loc != null && $Object.hasOwnProperty.call(m, "loc")) {
                d.loc = m.loc;
            }
            if (m.pollName != null && $Object.hasOwnProperty.call(m, "pollName")) {
                d.pollName = m.pollName;
            }
            if (m.pollOptions && m.pollOptions.length) {
                d.pollOptions = $Array(m.pollOptions.length);
                for (var j = 0; j < m.pollOptions.length; ++j) {
                    d.pollOptions[j] = $root.proto.MsgOpaqueData.PollOption.toObject(m.pollOptions[j], o, q + 1);
                }
            }
            if (m.pollSelectableOptionsCount != null && $Object.hasOwnProperty.call(m, "pollSelectableOptionsCount")) {
                d.pollSelectableOptionsCount = m.pollSelectableOptionsCount;
            }
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret")) {
                d.messageSecret = o.bytes === $String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
            }
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.senderTimestampMs === "number")
                    d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                else
                    d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
            }
            if (m.pollUpdateParentKey != null && $Object.hasOwnProperty.call(m, "pollUpdateParentKey")) {
                d.pollUpdateParentKey = m.pollUpdateParentKey;
            }
            if (m.encPollVote != null && $Object.hasOwnProperty.call(m, "encPollVote")) {
                d.encPollVote = $root.proto.PollEncValue.toObject(m.encPollVote, o, q + 1);
            }
            if (m.encReactionTargetMessageKey != null && $Object.hasOwnProperty.call(m, "encReactionTargetMessageKey")) {
                d.encReactionTargetMessageKey = m.encReactionTargetMessageKey;
            }
            if (m.encReactionEncPayload != null && $Object.hasOwnProperty.call(m, "encReactionEncPayload")) {
                d.encReactionEncPayload = o.bytes === $String ? $util.base64.encode(m.encReactionEncPayload, 0, m.encReactionEncPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encReactionEncPayload) : m.encReactionEncPayload;
            }
            if (m.encReactionEncIv != null && $Object.hasOwnProperty.call(m, "encReactionEncIv")) {
                d.encReactionEncIv = o.bytes === $String ? $util.base64.encode(m.encReactionEncIv, 0, m.encReactionEncIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encReactionEncIv) : m.encReactionEncIv;
            }
            if (m.isSentCagPollCreation != null && $Object.hasOwnProperty.call(m, "isSentCagPollCreation")) {
                d.isSentCagPollCreation = m.isSentCagPollCreation;
            }
            if (m.botMessageSecret != null && $Object.hasOwnProperty.call(m, "botMessageSecret")) {
                d.botMessageSecret = o.bytes === $String ? $util.base64.encode(m.botMessageSecret, 0, m.botMessageSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.botMessageSecret) : m.botMessageSecret;
            }
            if (m.targetMessageKey != null && $Object.hasOwnProperty.call(m, "targetMessageKey")) {
                d.targetMessageKey = m.targetMessageKey;
            }
            if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
            }
            if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
            }
            if (m.eventName != null && $Object.hasOwnProperty.call(m, "eventName")) {
                d.eventName = m.eventName;
            }
            if (m.isEventCanceled != null && $Object.hasOwnProperty.call(m, "isEventCanceled")) {
                d.isEventCanceled = m.isEventCanceled;
            }
            if (m.eventDescription != null && $Object.hasOwnProperty.call(m, "eventDescription")) {
                d.eventDescription = m.eventDescription;
            }
            if (m.eventJoinLink != null && $Object.hasOwnProperty.call(m, "eventJoinLink")) {
                d.eventJoinLink = m.eventJoinLink;
            }
            if (m.eventStartTime != null && $Object.hasOwnProperty.call(m, "eventStartTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.eventStartTime = typeof m.eventStartTime === "number" ? $BigInt(m.eventStartTime) : $util.Long.fromBits(m.eventStartTime.low >>> 0, m.eventStartTime.high >>> 0, false).toBigInt();
                else if (typeof m.eventStartTime === "number")
                    d.eventStartTime = o.longs === $String ? $String(m.eventStartTime) : m.eventStartTime;
                else
                    d.eventStartTime = o.longs === $String ? $util.Long.prototype.toString.call(m.eventStartTime) : o.longs === $Number ? new $util.LongBits(m.eventStartTime.low >>> 0, m.eventStartTime.high >>> 0).toNumber() : m.eventStartTime;
            }
            if (m.eventLocation != null && $Object.hasOwnProperty.call(m, "eventLocation")) {
                d.eventLocation = $root.proto.MsgOpaqueData.EventLocation.toObject(m.eventLocation, o, q + 1);
            }
            if (m.eventEndTime != null && $Object.hasOwnProperty.call(m, "eventEndTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.eventEndTime = typeof m.eventEndTime === "number" ? $BigInt(m.eventEndTime) : $util.Long.fromBits(m.eventEndTime.low >>> 0, m.eventEndTime.high >>> 0, false).toBigInt();
                else if (typeof m.eventEndTime === "number")
                    d.eventEndTime = o.longs === $String ? $String(m.eventEndTime) : m.eventEndTime;
                else
                    d.eventEndTime = o.longs === $String ? $util.Long.prototype.toString.call(m.eventEndTime) : o.longs === $Number ? new $util.LongBits(m.eventEndTime.low >>> 0, m.eventEndTime.high >>> 0).toNumber() : m.eventEndTime;
            }
            if (m.pollVotesSnapshot != null && $Object.hasOwnProperty.call(m, "pollVotesSnapshot")) {
                d.pollVotesSnapshot = $root.proto.MsgOpaqueData.PollVotesSnapshot.toObject(m.pollVotesSnapshot, o, q + 1);
            }
            if (m.pollContentType != null && $Object.hasOwnProperty.call(m, "pollContentType")) {
                d.pollContentType = o.enums === $String ? $root.proto.MsgOpaqueData.PollContentType[m.pollContentType] === $undefined ? m.pollContentType : $root.proto.MsgOpaqueData.PollContentType[m.pollContentType] : m.pollContentType;
            }
            if (m.plainProtobufBytes != null && $Object.hasOwnProperty.call(m, "plainProtobufBytes")) {
                d.plainProtobufBytes = o.bytes === $String ? $util.base64.encode(m.plainProtobufBytes, 0, m.plainProtobufBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.plainProtobufBytes) : m.plainProtobufBytes;
            }
            if (m.eventIsScheduledCall != null && $Object.hasOwnProperty.call(m, "eventIsScheduledCall")) {
                d.eventIsScheduledCall = m.eventIsScheduledCall;
            }
            if (m.eventExtraGuestsAllowed != null && $Object.hasOwnProperty.call(m, "eventExtraGuestsAllowed")) {
                d.eventExtraGuestsAllowed = m.eventExtraGuestsAllowed;
            }
            if (m.pollType != null && $Object.hasOwnProperty.call(m, "pollType")) {
                d.pollType = o.enums === $String ? $root.proto.MsgOpaqueData.PollType[m.pollType] === $undefined ? m.pollType : $root.proto.MsgOpaqueData.PollType[m.pollType] : m.pollType;
            }
            if (m.correctOptionIndex != null && $Object.hasOwnProperty.call(m, "correctOptionIndex")) {
                d.correctOptionIndex = m.correctOptionIndex;
            }
            if (m.quarantineExtractedText != null && $Object.hasOwnProperty.call(m, "quarantineExtractedText")) {
                d.quarantineExtractedText = m.quarantineExtractedText;
            }
            if (m.pollEndTime != null && $Object.hasOwnProperty.call(m, "pollEndTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.pollEndTime = typeof m.pollEndTime === "number" ? $BigInt(m.pollEndTime) : $util.Long.fromBits(m.pollEndTime.low >>> 0, m.pollEndTime.high >>> 0, false).toBigInt();
                else if (typeof m.pollEndTime === "number")
                    d.pollEndTime = o.longs === $String ? $String(m.pollEndTime) : m.pollEndTime;
                else
                    d.pollEndTime = o.longs === $String ? $util.Long.prototype.toString.call(m.pollEndTime) : o.longs === $Number ? new $util.LongBits(m.pollEndTime.low >>> 0, m.pollEndTime.high >>> 0).toNumber() : m.pollEndTime;
            }
            if (m.pollHideVoterNames != null && $Object.hasOwnProperty.call(m, "pollHideVoterNames")) {
                d.pollHideVoterNames = m.pollHideVoterNames;
            }
            if (m.originalSelfAuthor != null && $Object.hasOwnProperty.call(m, "originalSelfAuthor")) {
                d.originalSelfAuthor = m.originalSelfAuthor;
            }
            if (m.pollAllowAddOption != null && $Object.hasOwnProperty.call(m, "pollAllowAddOption")) {
                d.pollAllowAddOption = m.pollAllowAddOption;
            }
            if (m.sharableEventInviteId != null && $Object.hasOwnProperty.call(m, "sharableEventInviteId")) {
                d.sharableEventInviteId = m.sharableEventInviteId;
            }
            if (m.sharableEventInviteTitle != null && $Object.hasOwnProperty.call(m, "sharableEventInviteTitle")) {
                d.sharableEventInviteTitle = m.sharableEventInviteTitle;
            }
            if (m.sharableEventInviteStartTime != null && $Object.hasOwnProperty.call(m, "sharableEventInviteStartTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.sharableEventInviteStartTime = typeof m.sharableEventInviteStartTime === "number" ? $BigInt(m.sharableEventInviteStartTime) : $util.Long.fromBits(m.sharableEventInviteStartTime.low >>> 0, m.sharableEventInviteStartTime.high >>> 0, false).toBigInt();
                else if (typeof m.sharableEventInviteStartTime === "number")
                    d.sharableEventInviteStartTime = o.longs === $String ? $String(m.sharableEventInviteStartTime) : m.sharableEventInviteStartTime;
                else
                    d.sharableEventInviteStartTime = o.longs === $String ? $util.Long.prototype.toString.call(m.sharableEventInviteStartTime) : o.longs === $Number ? new $util.LongBits(m.sharableEventInviteStartTime.low >>> 0, m.sharableEventInviteStartTime.high >>> 0).toNumber() : m.sharableEventInviteStartTime;
            }
            if (m.sharableEventInviteEndTime != null && $Object.hasOwnProperty.call(m, "sharableEventInviteEndTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.sharableEventInviteEndTime = typeof m.sharableEventInviteEndTime === "number" ? $BigInt(m.sharableEventInviteEndTime) : $util.Long.fromBits(m.sharableEventInviteEndTime.low >>> 0, m.sharableEventInviteEndTime.high >>> 0, false).toBigInt();
                else if (typeof m.sharableEventInviteEndTime === "number")
                    d.sharableEventInviteEndTime = o.longs === $String ? $String(m.sharableEventInviteEndTime) : m.sharableEventInviteEndTime;
                else
                    d.sharableEventInviteEndTime = o.longs === $String ? $util.Long.prototype.toString.call(m.sharableEventInviteEndTime) : o.longs === $Number ? new $util.LongBits(m.sharableEventInviteEndTime.low >>> 0, m.sharableEventInviteEndTime.high >>> 0).toNumber() : m.sharableEventInviteEndTime;
            }
            if (m.sharableEventInviteCaption != null && $Object.hasOwnProperty.call(m, "sharableEventInviteCaption")) {
                d.sharableEventInviteCaption = m.sharableEventInviteCaption;
            }
            if (m.sharableEventInviteIsCanceled != null && $Object.hasOwnProperty.call(m, "sharableEventInviteIsCanceled")) {
                d.sharableEventInviteIsCanceled = m.sharableEventInviteIsCanceled;
            }
            if (m.sharableEventInviteJpegThumbnail != null && $Object.hasOwnProperty.call(m, "sharableEventInviteJpegThumbnail")) {
                d.sharableEventInviteJpegThumbnail = o.bytes === $String ? $util.base64.encode(m.sharableEventInviteJpegThumbnail, 0, m.sharableEventInviteJpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.sharableEventInviteJpegThumbnail) : m.sharableEventInviteJpegThumbnail;
            }
            if (m.sharableEventInviteCallLink != null && $Object.hasOwnProperty.call(m, "sharableEventInviteCallLink")) {
                d.sharableEventInviteCallLink = m.sharableEventInviteCallLink;
            }
            return d;
        };

        MsgOpaqueData.prototype.toJSON = function() {
            return MsgOpaqueData.toObject(this, $protobuf.util.toJSONOptions);
        };

        MsgOpaqueData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MsgOpaqueData";
        };

        MsgOpaqueData.EventLocation = (function() {

            const EventLocation = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            EventLocation.prototype.degreesLatitude = null;
            EventLocation.prototype.degreesLongitude = null;
            EventLocation.prototype.name = null;
            EventLocation.prototype.address = null;
            EventLocation.prototype.url = null;
            EventLocation.prototype.jpegThumbnail = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventLocation.prototype, "_degreesLatitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLatitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventLocation.prototype, "_degreesLongitude", {
                get: $util.oneOfGetter($oneOfFields = ["degreesLongitude"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventLocation.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventLocation.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventLocation.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(EventLocation.prototype, "_jpegThumbnail", {
                get: $util.oneOfGetter($oneOfFields = ["jpegThumbnail"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            EventLocation.create = function(properties) {
                return new EventLocation(properties);
            };

            EventLocation.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude"))
                    w.uint32(9).double(m.degreesLatitude);
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude"))
                    w.uint32(17).double(m.degreesLongitude);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(26).string(m.name);
                if (m.address != null && $Object.hasOwnProperty.call(m, "address"))
                    w.uint32(34).string(m.address);
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(42).string(m.url);
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail"))
                    w.uint32(50).bytes(m.jpegThumbnail);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            EventLocation.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MsgOpaqueData.EventLocation();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 1)
                                break;
                            m.degreesLatitude = r.double();
                            m._degreesLatitude = "degreesLatitude";
                            continue;
                        }
                    case 2: {
                            if (u !== 1)
                                break;
                            m.degreesLongitude = r.double();
                            m._degreesLongitude = "degreesLongitude";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.address = r.stringVerify();
                            m._address = "address";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.jpegThumbnail = r.bytes();
                            m._jpegThumbnail = "jpegThumbnail";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            EventLocation.fromObject = function (d, q) {
                if (d instanceof $root.proto.MsgOpaqueData.EventLocation)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.MsgOpaqueData.EventLocation: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.MsgOpaqueData.EventLocation();
                if (d.degreesLatitude != null) {
                    m.degreesLatitude = $Number(d.degreesLatitude);
                }
                if (d.degreesLongitude != null) {
                    m.degreesLongitude = $Number(d.degreesLongitude);
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.address != null) {
                    m.address = $String(d.address);
                }
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.jpegThumbnail != null) {
                    if (typeof d.jpegThumbnail === "string")
                        $util.base64.decode(d.jpegThumbnail, m.jpegThumbnail = $util.newBuffer($util.base64.length(d.jpegThumbnail)), 0);
                    else if (d.jpegThumbnail.length >= 0)
                        m.jpegThumbnail = d.jpegThumbnail;
                }
                return m;
            };

            EventLocation.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.degreesLatitude != null && $Object.hasOwnProperty.call(m, "degreesLatitude")) {
                    d.degreesLatitude = o.json && !$isFinite(m.degreesLatitude) ? $String(m.degreesLatitude) : m.degreesLatitude;
                }
                if (m.degreesLongitude != null && $Object.hasOwnProperty.call(m, "degreesLongitude")) {
                    d.degreesLongitude = o.json && !$isFinite(m.degreesLongitude) ? $String(m.degreesLongitude) : m.degreesLongitude;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.address != null && $Object.hasOwnProperty.call(m, "address")) {
                    d.address = m.address;
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.jpegThumbnail != null && $Object.hasOwnProperty.call(m, "jpegThumbnail")) {
                    d.jpegThumbnail = o.bytes === $String ? $util.base64.encode(m.jpegThumbnail, 0, m.jpegThumbnail.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.jpegThumbnail) : m.jpegThumbnail;
                }
                return d;
            };

            EventLocation.prototype.toJSON = function() {
                return EventLocation.toObject(this, $protobuf.util.toJSONOptions);
            };

            EventLocation.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.MsgOpaqueData.EventLocation";
            };

            return EventLocation;
        })();

        MsgOpaqueData.PollContentType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "TEXT"] = 1;
            values[valuesById[2] = "IMAGE"] = 2;
            return values;
        })();

        MsgOpaqueData.PollOption = (function() {

            const PollOption = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollOption.prototype.name = null;
            PollOption.prototype.hash = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollOption.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollOption.prototype, "_hash", {
                get: $util.oneOfGetter($oneOfFields = ["hash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollOption.create = function(properties) {
                return new PollOption(properties);
            };

            PollOption.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.hash != null && $Object.hasOwnProperty.call(m, "hash"))
                    w.uint32(18).string(m.hash);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollOption.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MsgOpaqueData.PollOption();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.hash = r.stringVerify();
                            m._hash = "hash";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollOption.fromObject = function (d, q) {
                if (d instanceof $root.proto.MsgOpaqueData.PollOption)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.MsgOpaqueData.PollOption: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.MsgOpaqueData.PollOption();
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.hash != null) {
                    m.hash = $String(d.hash);
                }
                return m;
            };

            PollOption.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.hash != null && $Object.hasOwnProperty.call(m, "hash")) {
                    d.hash = m.hash;
                }
                return d;
            };

            PollOption.prototype.toJSON = function() {
                return PollOption.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollOption.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.MsgOpaqueData.PollOption";
            };

            return PollOption;
        })();

        MsgOpaqueData.PollType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "POLL"] = 0;
            values[valuesById[1] = "QUIZ"] = 1;
            return values;
        })();

        MsgOpaqueData.PollVoteSnapshot = (function() {

            const PollVoteSnapshot = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollVoteSnapshot.prototype.option = null;
            PollVoteSnapshot.prototype.optionVoteCount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollVoteSnapshot.prototype, "_option", {
                get: $util.oneOfGetter($oneOfFields = ["option"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollVoteSnapshot.prototype, "_optionVoteCount", {
                get: $util.oneOfGetter($oneOfFields = ["optionVoteCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollVoteSnapshot.create = function(properties) {
                return new PollVoteSnapshot(properties);
            };

            PollVoteSnapshot.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.option != null && $Object.hasOwnProperty.call(m, "option"))
                    $root.proto.MsgOpaqueData.PollOption.encode(m.option, w.uint32(10).fork(), q + 1).ldelim();
                if (m.optionVoteCount != null && $Object.hasOwnProperty.call(m, "optionVoteCount"))
                    w.uint32(16).int32(m.optionVoteCount);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollVoteSnapshot.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MsgOpaqueData.PollVoteSnapshot();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.option = $root.proto.MsgOpaqueData.PollOption.decode(r, r.uint32(), $undefined, q + 1, m.option);
                            m._option = "option";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.optionVoteCount = r.int32();
                            m._optionVoteCount = "optionVoteCount";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollVoteSnapshot.fromObject = function (d, q) {
                if (d instanceof $root.proto.MsgOpaqueData.PollVoteSnapshot)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.MsgOpaqueData.PollVoteSnapshot: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.MsgOpaqueData.PollVoteSnapshot();
                if (d.option != null) {
                    if (!$util.isObject(d.option))
                        throw $TypeError(".proto.MsgOpaqueData.PollVoteSnapshot.option: object expected");
                    m.option = $root.proto.MsgOpaqueData.PollOption.fromObject(d.option, q + 1);
                }
                if (d.optionVoteCount != null) {
                    m.optionVoteCount = d.optionVoteCount | 0;
                }
                return m;
            };

            PollVoteSnapshot.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.option != null && $Object.hasOwnProperty.call(m, "option")) {
                    d.option = $root.proto.MsgOpaqueData.PollOption.toObject(m.option, o, q + 1);
                }
                if (m.optionVoteCount != null && $Object.hasOwnProperty.call(m, "optionVoteCount")) {
                    d.optionVoteCount = m.optionVoteCount;
                }
                return d;
            };

            PollVoteSnapshot.prototype.toJSON = function() {
                return PollVoteSnapshot.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollVoteSnapshot.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.MsgOpaqueData.PollVoteSnapshot";
            };

            return PollVoteSnapshot;
        })();

        MsgOpaqueData.PollVotesSnapshot = (function() {

            const PollVotesSnapshot = function (p) {
                this.pollVotes = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollVotesSnapshot.prototype.pollVotes = $util.emptyArray;

            PollVotesSnapshot.create = function(properties) {
                return new PollVotesSnapshot(properties);
            };

            PollVotesSnapshot.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pollVotes != null && m.pollVotes.length) {
                    for (var i = 0; i < m.pollVotes.length; ++i)
                        $root.proto.MsgOpaqueData.PollVoteSnapshot.encode(m.pollVotes[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollVotesSnapshot.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MsgOpaqueData.PollVotesSnapshot();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.pollVotes && m.pollVotes.length))
                                m.pollVotes = [];
                            m.pollVotes.push($root.proto.MsgOpaqueData.PollVoteSnapshot.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollVotesSnapshot.fromObject = function (d, q) {
                if (d instanceof $root.proto.MsgOpaqueData.PollVotesSnapshot)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.MsgOpaqueData.PollVotesSnapshot: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.MsgOpaqueData.PollVotesSnapshot();
                if (d.pollVotes) {
                    if (!$Array.isArray(d.pollVotes))
                        throw $TypeError(".proto.MsgOpaqueData.PollVotesSnapshot.pollVotes: array expected");
                    m.pollVotes = $Array(d.pollVotes.length);
                    for (var i = 0; i < d.pollVotes.length; ++i) {
                        if (!$util.isObject(d.pollVotes[i]))
                            throw $TypeError(".proto.MsgOpaqueData.PollVotesSnapshot.pollVotes: object expected");
                        m.pollVotes[i] = $root.proto.MsgOpaqueData.PollVoteSnapshot.fromObject(d.pollVotes[i], q + 1);
                    }
                }
                return m;
            };

            PollVotesSnapshot.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.pollVotes = [];
                }
                if (m.pollVotes && m.pollVotes.length) {
                    d.pollVotes = $Array(m.pollVotes.length);
                    for (var j = 0; j < m.pollVotes.length; ++j) {
                        d.pollVotes[j] = $root.proto.MsgOpaqueData.PollVoteSnapshot.toObject(m.pollVotes[j], o, q + 1);
                    }
                }
                return d;
            };

            PollVotesSnapshot.prototype.toJSON = function() {
                return PollVotesSnapshot.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollVotesSnapshot.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.MsgOpaqueData.PollVotesSnapshot";
            };

            return PollVotesSnapshot;
        })();

        return MsgOpaqueData;
    })();

    proto.MsgRowOpaqueData = (function() {

        const MsgRowOpaqueData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        MsgRowOpaqueData.prototype.currentMsg = null;
        MsgRowOpaqueData.prototype.quotedMsg = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgRowOpaqueData.prototype, "_currentMsg", {
            get: $util.oneOfGetter($oneOfFields = ["currentMsg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(MsgRowOpaqueData.prototype, "_quotedMsg", {
            get: $util.oneOfGetter($oneOfFields = ["quotedMsg"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        MsgRowOpaqueData.create = function(properties) {
            return new MsgRowOpaqueData(properties);
        };

        MsgRowOpaqueData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.currentMsg != null && $Object.hasOwnProperty.call(m, "currentMsg"))
                $root.proto.MsgOpaqueData.encode(m.currentMsg, w.uint32(10).fork(), q + 1).ldelim();
            if (m.quotedMsg != null && $Object.hasOwnProperty.call(m, "quotedMsg"))
                $root.proto.MsgOpaqueData.encode(m.quotedMsg, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        MsgRowOpaqueData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.MsgRowOpaqueData();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.currentMsg = $root.proto.MsgOpaqueData.decode(r, r.uint32(), $undefined, q + 1, m.currentMsg);
                        m._currentMsg = "currentMsg";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.quotedMsg = $root.proto.MsgOpaqueData.decode(r, r.uint32(), $undefined, q + 1, m.quotedMsg);
                        m._quotedMsg = "quotedMsg";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        MsgRowOpaqueData.fromObject = function (d, q) {
            if (d instanceof $root.proto.MsgRowOpaqueData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.MsgRowOpaqueData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.MsgRowOpaqueData();
            if (d.currentMsg != null) {
                if (!$util.isObject(d.currentMsg))
                    throw $TypeError(".proto.MsgRowOpaqueData.currentMsg: object expected");
                m.currentMsg = $root.proto.MsgOpaqueData.fromObject(d.currentMsg, q + 1);
            }
            if (d.quotedMsg != null) {
                if (!$util.isObject(d.quotedMsg))
                    throw $TypeError(".proto.MsgRowOpaqueData.quotedMsg: object expected");
                m.quotedMsg = $root.proto.MsgOpaqueData.fromObject(d.quotedMsg, q + 1);
            }
            return m;
        };

        MsgRowOpaqueData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.currentMsg != null && $Object.hasOwnProperty.call(m, "currentMsg")) {
                d.currentMsg = $root.proto.MsgOpaqueData.toObject(m.currentMsg, o, q + 1);
            }
            if (m.quotedMsg != null && $Object.hasOwnProperty.call(m, "quotedMsg")) {
                d.quotedMsg = $root.proto.MsgOpaqueData.toObject(m.quotedMsg, o, q + 1);
            }
            return d;
        };

        MsgRowOpaqueData.prototype.toJSON = function() {
            return MsgRowOpaqueData.toObject(this, $protobuf.util.toJSONOptions);
        };

        MsgRowOpaqueData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.MsgRowOpaqueData";
        };

        return MsgRowOpaqueData;
    })();

    proto.MutationProps = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[2] = "STAR_ACTION"] = 2;
        values[valuesById[3] = "CONTACT_ACTION"] = 3;
        values[valuesById[4] = "MUTE_ACTION"] = 4;
        values[valuesById[5] = "PIN_ACTION"] = 5;
        values[valuesById[6] = "SECURITY_NOTIFICATION_SETTING"] = 6;
        values[valuesById[7] = "PUSH_NAME_SETTING"] = 7;
        values[valuesById[8] = "QUICK_REPLY_ACTION"] = 8;
        values[valuesById[11] = "RECENT_EMOJI_WEIGHTS_ACTION"] = 11;
        values[valuesById[13] = "LABEL_MESSAGE_ACTION"] = 13;
        values[valuesById[14] = "LABEL_EDIT_ACTION"] = 14;
        values[valuesById[15] = "LABEL_ASSOCIATION_ACTION"] = 15;
        values[valuesById[16] = "LOCALE_SETTING"] = 16;
        values[valuesById[17] = "ARCHIVE_CHAT_ACTION"] = 17;
        values[valuesById[18] = "DELETE_MESSAGE_FOR_ME_ACTION"] = 18;
        values[valuesById[19] = "KEY_EXPIRATION"] = 19;
        values[valuesById[20] = "MARK_CHAT_AS_READ_ACTION"] = 20;
        values[valuesById[21] = "CLEAR_CHAT_ACTION"] = 21;
        values[valuesById[22] = "DELETE_CHAT_ACTION"] = 22;
        values[valuesById[23] = "UNARCHIVE_CHATS_SETTING"] = 23;
        values[valuesById[24] = "PRIMARY_FEATURE"] = 24;
        values[valuesById[26] = "ANDROID_UNSUPPORTED_ACTIONS"] = 26;
        values[valuesById[27] = "AGENT_ACTION"] = 27;
        values[valuesById[28] = "SUBSCRIPTION_ACTION"] = 28;
        values[valuesById[29] = "USER_STATUS_MUTE_ACTION"] = 29;
        values[valuesById[30] = "TIME_FORMAT_ACTION"] = 30;
        values[valuesById[31] = "NUX_ACTION"] = 31;
        values[valuesById[32] = "PRIMARY_VERSION_ACTION"] = 32;
        values[valuesById[33] = "STICKER_ACTION"] = 33;
        values[valuesById[34] = "REMOVE_RECENT_STICKER_ACTION"] = 34;
        values[valuesById[35] = "CHAT_ASSIGNMENT"] = 35;
        values[valuesById[36] = "CHAT_ASSIGNMENT_OPENED_STATUS"] = 36;
        values[valuesById[37] = "PN_FOR_LID_CHAT_ACTION"] = 37;
        values[valuesById[38] = "MARKETING_MESSAGE_ACTION"] = 38;
        values[valuesById[39] = "MARKETING_MESSAGE_BROADCAST_ACTION"] = 39;
        values[valuesById[40] = "EXTERNAL_WEB_BETA_ACTION"] = 40;
        values[valuesById[41] = "PRIVACY_SETTING_RELAY_ALL_CALLS"] = 41;
        values[valuesById[42] = "CALL_LOG_ACTION"] = 42;
        values[valuesById[43] = "UGC_BOT"] = 43;
        values[valuesById[44] = "STATUS_PRIVACY"] = 44;
        values[valuesById[45] = "BOT_WELCOME_REQUEST_ACTION"] = 45;
        values[valuesById[46] = "DELETE_INDIVIDUAL_CALL_LOG"] = 46;
        values[valuesById[47] = "LABEL_REORDERING_ACTION"] = 47;
        values[valuesById[48] = "PAYMENT_INFO_ACTION"] = 48;
        values[valuesById[49] = "CUSTOM_PAYMENT_METHODS_ACTION"] = 49;
        values[valuesById[50] = "LOCK_CHAT_ACTION"] = 50;
        values[valuesById[51] = "CHAT_LOCK_SETTINGS"] = 51;
        values[valuesById[52] = "WAMO_USER_IDENTIFIER_ACTION"] = 52;
        values[valuesById[53] = "PRIVACY_SETTING_DISABLE_LINK_PREVIEWS_ACTION"] = 53;
        values[valuesById[54] = "DEVICE_CAPABILITIES"] = 54;
        values[valuesById[55] = "NOTE_EDIT_ACTION"] = 55;
        values[valuesById[56] = "FAVORITES_ACTION"] = 56;
        values[valuesById[57] = "MERCHANT_PAYMENT_PARTNER_ACTION"] = 57;
        values[valuesById[58] = "WAFFLE_ACCOUNT_LINK_STATE_ACTION"] = 58;
        values[valuesById[59] = "USERNAME_CHAT_START_MODE"] = 59;
        values[valuesById[60] = "NOTIFICATION_ACTIVITY_SETTING_ACTION"] = 60;
        values[valuesById[61] = "LID_CONTACT_ACTION"] = 61;
        values[valuesById[62] = "CTWA_PER_CUSTOMER_DATA_SHARING_ACTION"] = 62;
        values[valuesById[63] = "PAYMENT_TOS_ACTION"] = 63;
        values[valuesById[64] = "PRIVACY_SETTING_CHANNELS_PERSONALISED_RECOMMENDATION_ACTION"] = 64;
        values[valuesById[65] = "BUSINESS_BROADCAST_ASSOCIATION_ACTION"] = 65;
        values[valuesById[66] = "DETECTED_OUTCOMES_STATUS_ACTION"] = 66;
        values[valuesById[68] = "MAIBA_AI_FEATURES_CONTROL_ACTION"] = 68;
        values[valuesById[69] = "BUSINESS_BROADCAST_LIST_ACTION"] = 69;
        values[valuesById[70] = "MUSIC_USER_ID_ACTION"] = 70;
        values[valuesById[71] = "STATUS_POST_OPT_IN_NOTIFICATION_PREFERENCES_ACTION"] = 71;
        values[valuesById[72] = "AVATAR_UPDATED_ACTION"] = 72;
        values[valuesById[73] = "GALAXY_FLOW_ACTION"] = 73;
        values[valuesById[74] = "PRIVATE_PROCESSING_SETTING_ACTION"] = 74;
        values[valuesById[75] = "NEWSLETTER_SAVED_INTERESTS_ACTION"] = 75;
        values[valuesById[76] = "AI_THREAD_RENAME_ACTION"] = 76;
        values[valuesById[77] = "INTERACTIVE_MESSAGE_ACTION"] = 77;
        values[valuesById[78] = "SETTINGS_SYNC_ACTION"] = 78;
        values[valuesById[79] = "OUT_CONTACT_ACTION"] = 79;
        values[valuesById[80] = "NCT_SALT_SYNC_ACTION"] = 80;
        values[valuesById[81] = "BUSINESS_BROADCAST_CAMPAIGN_ACTION"] = 81;
        values[valuesById[82] = "BUSINESS_BROADCAST_INSIGHTS_ACTION"] = 82;
        values[valuesById[83] = "CUSTOMER_DATA_ACTION"] = 83;
        values[valuesById[84] = "SUBSCRIPTIONS_SYNC_V2_ACTION"] = 84;
        values[valuesById[85] = "THREAD_PIN_ACTION"] = 85;
        values[valuesById[86] = "AUTO_ORGANIZE_BUSINESS_CHAT_SETTING"] = 86;
        values[valuesById[87] = "BIZ_AI_SETTINGS_NUDGE_ACTION"] = 87;
        values[valuesById[88] = "COEX_V2_VERSION_ACTION"] = 88;
        values[valuesById[89] = "WASA_ROOT_SECRET_ACTION"] = 89;
        values[valuesById[90] = "BUBBLE_LOCK_MESSAGE_ACTION"] = 90;
        values[valuesById[91] = "LABEL_SUBLIST_ACTION"] = 91;
        values[valuesById[92] = "DEVICE_CAPABILITIES_V2"] = 92;
        values[valuesById[93] = "CTWA_MESSAGE_RECEIVED_ACTION"] = 93;
        values[valuesById[94] = "SHARED_DEVICE_ALLOWLIST_ACTION"] = 94;
        values[valuesById[95] = "CONTACT_MANAGER_METADATA_ACTION"] = 95;
        values[valuesById[96] = "BUSINESS_FOLDER_ACTIVATION_ACTION"] = 96;
        values[valuesById[10001] = "SHARE_OWN_PN"] = 10001;
        values[valuesById[10002] = "BUSINESS_BROADCAST_ACTION"] = 10002;
        values[valuesById[10003] = "AI_THREAD_DELETE_ACTION"] = 10003;
        return values;
    })();

    proto.NoiseCertificate = (function() {

        const NoiseCertificate = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        NoiseCertificate.prototype.details = null;
        NoiseCertificate.prototype.signature = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NoiseCertificate.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NoiseCertificate.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NoiseCertificate.create = function(properties) {
            return new NoiseCertificate(properties);
        };

        NoiseCertificate.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.details != null && $Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature"))
                w.uint32(18).bytes(m.signature);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        NoiseCertificate.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.NoiseCertificate();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.details = r.bytes();
                        m._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.signature = r.bytes();
                        m._signature = "signature";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        NoiseCertificate.fromObject = function (d, q) {
            if (d instanceof $root.proto.NoiseCertificate)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.NoiseCertificate: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.NoiseCertificate();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.signature != null) {
                if (typeof d.signature === "string")
                    $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                else if (d.signature.length >= 0)
                    m.signature = d.signature;
            }
            return m;
        };

        NoiseCertificate.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.details != null && $Object.hasOwnProperty.call(m, "details")) {
                d.details = o.bytes === $String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.details) : m.details;
            }
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature")) {
                d.signature = o.bytes === $String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signature) : m.signature;
            }
            return d;
        };

        NoiseCertificate.prototype.toJSON = function() {
            return NoiseCertificate.toObject(this, $protobuf.util.toJSONOptions);
        };

        NoiseCertificate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NoiseCertificate";
        };

        NoiseCertificate.Details = (function() {

            const Details = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Details.prototype.serial = null;
            Details.prototype.issuer = null;
            Details.prototype.expires = null;
            Details.prototype.subject = null;
            Details.prototype.key = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_serial", {
                get: $util.oneOfGetter($oneOfFields = ["serial"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_issuer", {
                get: $util.oneOfGetter($oneOfFields = ["issuer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_expires", {
                get: $util.oneOfGetter($oneOfFields = ["expires"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_subject", {
                get: $util.oneOfGetter($oneOfFields = ["subject"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Details.create = function(properties) {
                return new Details(properties);
            };

            Details.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.serial != null && $Object.hasOwnProperty.call(m, "serial"))
                    w.uint32(8).uint32(m.serial);
                if (m.issuer != null && $Object.hasOwnProperty.call(m, "issuer"))
                    w.uint32(18).string(m.issuer);
                if (m.expires != null && $Object.hasOwnProperty.call(m, "expires"))
                    w.uint32(24).uint64(m.expires);
                if (m.subject != null && $Object.hasOwnProperty.call(m, "subject"))
                    w.uint32(34).string(m.subject);
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    w.uint32(42).bytes(m.key);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Details.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.NoiseCertificate.Details();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.serial = r.uint32();
                            m._serial = "serial";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.issuer = r.stringVerify();
                            m._issuer = "issuer";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.expires = r.uint64();
                            m._expires = "expires";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.subject = r.stringVerify();
                            m._subject = "subject";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.key = r.bytes();
                            m._key = "key";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Details.fromObject = function (d, q) {
                if (d instanceof $root.proto.NoiseCertificate.Details)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.NoiseCertificate.Details: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.NoiseCertificate.Details();
                if (d.serial != null) {
                    m.serial = d.serial >>> 0;
                }
                if (d.issuer != null) {
                    m.issuer = $String(d.issuer);
                }
                if (d.expires != null) {
                    if ($util.Long)
                        m.expires = $util.Long.fromValue(d.expires, true);
                    else if (typeof d.expires === "string")
                        m.expires = $parseInt(d.expires, 10);
                    else if (typeof d.expires === "number")
                        m.expires = d.expires;
                    else if (typeof d.expires === "object")
                        m.expires = new $util.LongBits(d.expires.low >>> 0, d.expires.high >>> 0).toNumber(true);
                }
                if (d.subject != null) {
                    m.subject = $String(d.subject);
                }
                if (d.key != null) {
                    if (typeof d.key === "string")
                        $util.base64.decode(d.key, m.key = $util.newBuffer($util.base64.length(d.key)), 0);
                    else if (d.key.length >= 0)
                        m.key = d.key;
                }
                return m;
            };

            Details.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.serial != null && $Object.hasOwnProperty.call(m, "serial")) {
                    d.serial = m.serial;
                }
                if (m.issuer != null && $Object.hasOwnProperty.call(m, "issuer")) {
                    d.issuer = m.issuer;
                }
                if (m.expires != null && $Object.hasOwnProperty.call(m, "expires")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.expires = typeof m.expires === "number" ? $BigInt(m.expires) : $util.Long.fromBits(m.expires.low >>> 0, m.expires.high >>> 0, true).toBigInt();
                    else if (typeof m.expires === "number")
                        d.expires = o.longs === $String ? $String(m.expires) : m.expires;
                    else
                        d.expires = o.longs === $String ? $util.Long.prototype.toString.call(m.expires) : o.longs === $Number ? new $util.LongBits(m.expires.low >>> 0, m.expires.high >>> 0).toNumber(true) : m.expires;
                }
                if (m.subject != null && $Object.hasOwnProperty.call(m, "subject")) {
                    d.subject = m.subject;
                }
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = o.bytes === $String ? $util.base64.encode(m.key, 0, m.key.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.key) : m.key;
                }
                return d;
            };

            Details.prototype.toJSON = function() {
                return Details.toObject(this, $protobuf.util.toJSONOptions);
            };

            Details.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.NoiseCertificate.Details";
            };

            return Details;
        })();

        return NoiseCertificate;
    })();

    proto.NonE2EEAttestation = (function() {

        const NonE2EEAttestation = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        NonE2EEAttestation.prototype.accountType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NonE2EEAttestation.prototype, "_accountType", {
            get: $util.oneOfGetter($oneOfFields = ["accountType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NonE2EEAttestation.create = function(properties) {
            return new NonE2EEAttestation(properties);
        };

        NonE2EEAttestation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType"))
                w.uint32(8).int32(m.accountType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        NonE2EEAttestation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.NonE2EEAttestation(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.accountType = r.int32();
                        m._accountType = "accountType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        NonE2EEAttestation.fromObject = function (d, q) {
            if (d instanceof $root.proto.NonE2EEAttestation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.NonE2EEAttestation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.NonE2EEAttestation();
            switch (d.accountType) {
            case "E2EE":
            case 0:
                m.accountType = 0;
                break;
            case "HYBRID_E2EE":
            case 1:
                m.accountType = 1;
                break;
            case "NON_E2EE":
            case 2:
                m.accountType = 2;
                break;
            default:
                if (typeof d.accountType === "number" && (d.accountType | 0) === d.accountType)
                    m.accountType = d.accountType;
            }
            return m;
        };

        NonE2EEAttestation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.accountType != null && $Object.hasOwnProperty.call(m, "accountType")) {
                d.accountType = o.enums === $String ? $root.proto.NonE2EEAttestation.AccountType[m.accountType] === $undefined ? m.accountType : $root.proto.NonE2EEAttestation.AccountType[m.accountType] : m.accountType;
            }
            return d;
        };

        NonE2EEAttestation.prototype.toJSON = function() {
            return NonE2EEAttestation.toObject(this, $protobuf.util.toJSONOptions);
        };

        NonE2EEAttestation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NonE2EEAttestation";
        };

        NonE2EEAttestation.AccountType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "E2EE"] = 0;
            values[valuesById[1] = "HYBRID_E2EE"] = 1;
            values[valuesById[2] = "NON_E2EE"] = 2;
            return values;
        })();

        return NonE2EEAttestation;
    })();

    proto.NotificationMessageInfo = (function() {

        const NotificationMessageInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        NotificationMessageInfo.prototype.key = null;
        NotificationMessageInfo.prototype.message = null;
        NotificationMessageInfo.prototype.messageTimestamp = null;
        NotificationMessageInfo.prototype.participant = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationMessageInfo.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationMessageInfo.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationMessageInfo.prototype, "_messageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationMessageInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NotificationMessageInfo.create = function(properties) {
            return new NotificationMessageInfo(properties);
        };

        NotificationMessageInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
            if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                $root.proto.Message.encode(m.message, w.uint32(18).fork(), q + 1).ldelim();
            if (m.messageTimestamp != null && $Object.hasOwnProperty.call(m, "messageTimestamp"))
                w.uint32(24).uint64(m.messageTimestamp);
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant"))
                w.uint32(34).string(m.participant);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        NotificationMessageInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.NotificationMessageInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                        m._key = "key";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                        m._message = "message";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.messageTimestamp = r.uint64();
                        m._messageTimestamp = "messageTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.participant = r.stringVerify();
                        m._participant = "participant";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        NotificationMessageInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.NotificationMessageInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.NotificationMessageInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.NotificationMessageInfo();
            if (d.key != null) {
                if (!$util.isObject(d.key))
                    throw $TypeError(".proto.NotificationMessageInfo.key: object expected");
                m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
            }
            if (d.message != null) {
                if (!$util.isObject(d.message))
                    throw $TypeError(".proto.NotificationMessageInfo.message: object expected");
                m.message = $root.proto.Message.fromObject(d.message, q + 1);
            }
            if (d.messageTimestamp != null) {
                if ($util.Long)
                    m.messageTimestamp = $util.Long.fromValue(d.messageTimestamp, true);
                else if (typeof d.messageTimestamp === "string")
                    m.messageTimestamp = $parseInt(d.messageTimestamp, 10);
                else if (typeof d.messageTimestamp === "number")
                    m.messageTimestamp = d.messageTimestamp;
                else if (typeof d.messageTimestamp === "object")
                    m.messageTimestamp = new $util.LongBits(d.messageTimestamp.low >>> 0, d.messageTimestamp.high >>> 0).toNumber(true);
            }
            if (d.participant != null) {
                m.participant = $String(d.participant);
            }
            return m;
        };

        NotificationMessageInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
            }
            if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                d.message = $root.proto.Message.toObject(m.message, o, q + 1);
            }
            if (m.messageTimestamp != null && $Object.hasOwnProperty.call(m, "messageTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.messageTimestamp = typeof m.messageTimestamp === "number" ? $BigInt(m.messageTimestamp) : $util.Long.fromBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.messageTimestamp === "number")
                    d.messageTimestamp = o.longs === $String ? $String(m.messageTimestamp) : m.messageTimestamp;
                else
                    d.messageTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.messageTimestamp) : o.longs === $Number ? new $util.LongBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0).toNumber(true) : m.messageTimestamp;
            }
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant")) {
                d.participant = m.participant;
            }
            return d;
        };

        NotificationMessageInfo.prototype.toJSON = function() {
            return NotificationMessageInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotificationMessageInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NotificationMessageInfo";
        };

        return NotificationMessageInfo;
    })();

    proto.NotificationSettings = (function() {

        const NotificationSettings = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        NotificationSettings.prototype.messageVibrate = null;
        NotificationSettings.prototype.messagePopup = null;
        NotificationSettings.prototype.messageLight = null;
        NotificationSettings.prototype.lowPriorityNotifications = null;
        NotificationSettings.prototype.reactionsMuted = null;
        NotificationSettings.prototype.callVibrate = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationSettings.prototype, "_messageVibrate", {
            get: $util.oneOfGetter($oneOfFields = ["messageVibrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationSettings.prototype, "_messagePopup", {
            get: $util.oneOfGetter($oneOfFields = ["messagePopup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationSettings.prototype, "_messageLight", {
            get: $util.oneOfGetter($oneOfFields = ["messageLight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationSettings.prototype, "_lowPriorityNotifications", {
            get: $util.oneOfGetter($oneOfFields = ["lowPriorityNotifications"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationSettings.prototype, "_reactionsMuted", {
            get: $util.oneOfGetter($oneOfFields = ["reactionsMuted"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(NotificationSettings.prototype, "_callVibrate", {
            get: $util.oneOfGetter($oneOfFields = ["callVibrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        NotificationSettings.create = function(properties) {
            return new NotificationSettings(properties);
        };

        NotificationSettings.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messageVibrate != null && $Object.hasOwnProperty.call(m, "messageVibrate"))
                w.uint32(10).string(m.messageVibrate);
            if (m.messagePopup != null && $Object.hasOwnProperty.call(m, "messagePopup"))
                w.uint32(18).string(m.messagePopup);
            if (m.messageLight != null && $Object.hasOwnProperty.call(m, "messageLight"))
                w.uint32(26).string(m.messageLight);
            if (m.lowPriorityNotifications != null && $Object.hasOwnProperty.call(m, "lowPriorityNotifications"))
                w.uint32(32).bool(m.lowPriorityNotifications);
            if (m.reactionsMuted != null && $Object.hasOwnProperty.call(m, "reactionsMuted"))
                w.uint32(40).bool(m.reactionsMuted);
            if (m.callVibrate != null && $Object.hasOwnProperty.call(m, "callVibrate"))
                w.uint32(50).string(m.callVibrate);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        NotificationSettings.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.NotificationSettings();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.messageVibrate = r.stringVerify();
                        m._messageVibrate = "messageVibrate";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.messagePopup = r.stringVerify();
                        m._messagePopup = "messagePopup";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.messageLight = r.stringVerify();
                        m._messageLight = "messageLight";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.lowPriorityNotifications = r.bool();
                        m._lowPriorityNotifications = "lowPriorityNotifications";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.reactionsMuted = r.bool();
                        m._reactionsMuted = "reactionsMuted";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.callVibrate = r.stringVerify();
                        m._callVibrate = "callVibrate";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        NotificationSettings.fromObject = function (d, q) {
            if (d instanceof $root.proto.NotificationSettings)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.NotificationSettings: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.NotificationSettings();
            if (d.messageVibrate != null) {
                m.messageVibrate = $String(d.messageVibrate);
            }
            if (d.messagePopup != null) {
                m.messagePopup = $String(d.messagePopup);
            }
            if (d.messageLight != null) {
                m.messageLight = $String(d.messageLight);
            }
            if (d.lowPriorityNotifications != null) {
                m.lowPriorityNotifications = $Boolean(d.lowPriorityNotifications);
            }
            if (d.reactionsMuted != null) {
                m.reactionsMuted = $Boolean(d.reactionsMuted);
            }
            if (d.callVibrate != null) {
                m.callVibrate = $String(d.callVibrate);
            }
            return m;
        };

        NotificationSettings.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.messageVibrate != null && $Object.hasOwnProperty.call(m, "messageVibrate")) {
                d.messageVibrate = m.messageVibrate;
            }
            if (m.messagePopup != null && $Object.hasOwnProperty.call(m, "messagePopup")) {
                d.messagePopup = m.messagePopup;
            }
            if (m.messageLight != null && $Object.hasOwnProperty.call(m, "messageLight")) {
                d.messageLight = m.messageLight;
            }
            if (m.lowPriorityNotifications != null && $Object.hasOwnProperty.call(m, "lowPriorityNotifications")) {
                d.lowPriorityNotifications = m.lowPriorityNotifications;
            }
            if (m.reactionsMuted != null && $Object.hasOwnProperty.call(m, "reactionsMuted")) {
                d.reactionsMuted = m.reactionsMuted;
            }
            if (m.callVibrate != null && $Object.hasOwnProperty.call(m, "callVibrate")) {
                d.callVibrate = m.callVibrate;
            }
            return d;
        };

        NotificationSettings.prototype.toJSON = function() {
            return NotificationSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        NotificationSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.NotificationSettings";
        };

        return NotificationSettings;
    })();

    proto.OrfThreadIdInput = (function() {

        const OrfThreadIdInput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        OrfThreadIdInput.prototype.orfClientState = $util.newBuffer([]);
        OrfThreadIdInput.prototype.threadId = "";

        OrfThreadIdInput.create = function(properties) {
            return new OrfThreadIdInput(properties);
        };

        OrfThreadIdInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.orfClientState);
            w.uint32(18).string(m.threadId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        OrfThreadIdInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.OrfThreadIdInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.orfClientState = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.threadId = r.stringVerify();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "orfClientState"))
                throw $util.ProtocolError("missing required 'orfClientState'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "threadId"))
                throw $util.ProtocolError("missing required 'threadId'", { instance: m });
            return m;
        };

        OrfThreadIdInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.OrfThreadIdInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.OrfThreadIdInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.OrfThreadIdInput();
            if (d.orfClientState != null) {
                if (typeof d.orfClientState === "string")
                    $util.base64.decode(d.orfClientState, m.orfClientState = $util.newBuffer($util.base64.length(d.orfClientState)), 0);
                else if (d.orfClientState.length >= 0)
                    m.orfClientState = d.orfClientState;
            }
            if (d.threadId != null) {
                m.threadId = $String(d.threadId);
            }
            return m;
        };

        OrfThreadIdInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if (o.bytes === $String)
                    d.orfClientState = "";
                else {
                    d.orfClientState = [];
                    if (o.bytes !== $Array)
                        d.orfClientState = $util.newBuffer(d.orfClientState);
                }
                d.threadId = "";
            }
            if (m.orfClientState != null && $Object.hasOwnProperty.call(m, "orfClientState")) {
                d.orfClientState = o.bytes === $String ? $util.base64.encode(m.orfClientState, 0, m.orfClientState.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.orfClientState) : m.orfClientState;
            }
            if (m.threadId != null && $Object.hasOwnProperty.call(m, "threadId")) {
                d.threadId = m.threadId;
            }
            return d;
        };

        OrfThreadIdInput.prototype.toJSON = function() {
            return OrfThreadIdInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        OrfThreadIdInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.OrfThreadIdInput";
        };

        return OrfThreadIdInput;
    })();

    proto.OrfThreadIdOutput = (function() {

        const OrfThreadIdOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        OrfThreadIdOutput.prototype.orfThreadId = null;
        OrfThreadIdOutput.prototype.error = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(OrfThreadIdOutput.prototype, "_orfThreadId", {
            get: $util.oneOfGetter($oneOfFields = ["orfThreadId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(OrfThreadIdOutput.prototype, "_error", {
            get: $util.oneOfGetter($oneOfFields = ["error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        OrfThreadIdOutput.create = function(properties) {
            return new OrfThreadIdOutput(properties);
        };

        OrfThreadIdOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.orfThreadId != null && $Object.hasOwnProperty.call(m, "orfThreadId"))
                w.uint32(10).bytes(m.orfThreadId);
            if (m.error != null && $Object.hasOwnProperty.call(m, "error"))
                w.uint32(18).string(m.error);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        OrfThreadIdOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.OrfThreadIdOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.orfThreadId = r.bytes();
                        m._orfThreadId = "orfThreadId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.error = r.stringVerify();
                        m._error = "error";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        OrfThreadIdOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.OrfThreadIdOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.OrfThreadIdOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.OrfThreadIdOutput();
            if (d.orfThreadId != null) {
                if (typeof d.orfThreadId === "string")
                    $util.base64.decode(d.orfThreadId, m.orfThreadId = $util.newBuffer($util.base64.length(d.orfThreadId)), 0);
                else if (d.orfThreadId.length >= 0)
                    m.orfThreadId = d.orfThreadId;
            }
            if (d.error != null) {
                m.error = $String(d.error);
            }
            return m;
        };

        OrfThreadIdOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.orfThreadId != null && $Object.hasOwnProperty.call(m, "orfThreadId")) {
                d.orfThreadId = o.bytes === $String ? $util.base64.encode(m.orfThreadId, 0, m.orfThreadId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.orfThreadId) : m.orfThreadId;
            }
            if (m.error != null && $Object.hasOwnProperty.call(m, "error")) {
                d.error = m.error;
            }
            return d;
        };

        OrfThreadIdOutput.prototype.toJSON = function() {
            return OrfThreadIdOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        OrfThreadIdOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.OrfThreadIdOutput";
        };

        return OrfThreadIdOutput;
    })();

    proto.PairingRequest = (function() {

        const PairingRequest = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PairingRequest.prototype.companionPublicKey = null;
        PairingRequest.prototype.companionIdentityKey = null;
        PairingRequest.prototype.advSecret = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PairingRequest.prototype, "_companionPublicKey", {
            get: $util.oneOfGetter($oneOfFields = ["companionPublicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PairingRequest.prototype, "_companionIdentityKey", {
            get: $util.oneOfGetter($oneOfFields = ["companionIdentityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PairingRequest.prototype, "_advSecret", {
            get: $util.oneOfGetter($oneOfFields = ["advSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PairingRequest.create = function(properties) {
            return new PairingRequest(properties);
        };

        PairingRequest.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.companionPublicKey != null && $Object.hasOwnProperty.call(m, "companionPublicKey"))
                w.uint32(10).bytes(m.companionPublicKey);
            if (m.companionIdentityKey != null && $Object.hasOwnProperty.call(m, "companionIdentityKey"))
                w.uint32(18).bytes(m.companionIdentityKey);
            if (m.advSecret != null && $Object.hasOwnProperty.call(m, "advSecret"))
                w.uint32(26).bytes(m.advSecret);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PairingRequest.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PairingRequest();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.companionPublicKey = r.bytes();
                        m._companionPublicKey = "companionPublicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.companionIdentityKey = r.bytes();
                        m._companionIdentityKey = "companionIdentityKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.advSecret = r.bytes();
                        m._advSecret = "advSecret";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PairingRequest.fromObject = function (d, q) {
            if (d instanceof $root.proto.PairingRequest)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PairingRequest: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PairingRequest();
            if (d.companionPublicKey != null) {
                if (typeof d.companionPublicKey === "string")
                    $util.base64.decode(d.companionPublicKey, m.companionPublicKey = $util.newBuffer($util.base64.length(d.companionPublicKey)), 0);
                else if (d.companionPublicKey.length >= 0)
                    m.companionPublicKey = d.companionPublicKey;
            }
            if (d.companionIdentityKey != null) {
                if (typeof d.companionIdentityKey === "string")
                    $util.base64.decode(d.companionIdentityKey, m.companionIdentityKey = $util.newBuffer($util.base64.length(d.companionIdentityKey)), 0);
                else if (d.companionIdentityKey.length >= 0)
                    m.companionIdentityKey = d.companionIdentityKey;
            }
            if (d.advSecret != null) {
                if (typeof d.advSecret === "string")
                    $util.base64.decode(d.advSecret, m.advSecret = $util.newBuffer($util.base64.length(d.advSecret)), 0);
                else if (d.advSecret.length >= 0)
                    m.advSecret = d.advSecret;
            }
            return m;
        };

        PairingRequest.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.companionPublicKey != null && $Object.hasOwnProperty.call(m, "companionPublicKey")) {
                d.companionPublicKey = o.bytes === $String ? $util.base64.encode(m.companionPublicKey, 0, m.companionPublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.companionPublicKey) : m.companionPublicKey;
            }
            if (m.companionIdentityKey != null && $Object.hasOwnProperty.call(m, "companionIdentityKey")) {
                d.companionIdentityKey = o.bytes === $String ? $util.base64.encode(m.companionIdentityKey, 0, m.companionIdentityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.companionIdentityKey) : m.companionIdentityKey;
            }
            if (m.advSecret != null && $Object.hasOwnProperty.call(m, "advSecret")) {
                d.advSecret = o.bytes === $String ? $util.base64.encode(m.advSecret, 0, m.advSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.advSecret) : m.advSecret;
            }
            return d;
        };

        PairingRequest.prototype.toJSON = function() {
            return PairingRequest.toObject(this, $protobuf.util.toJSONOptions);
        };

        PairingRequest.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PairingRequest";
        };

        return PairingRequest;
    })();

    proto.PastParticipant = (function() {

        const PastParticipant = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PastParticipant.prototype.userJid = null;
        PastParticipant.prototype.leaveReason = null;
        PastParticipant.prototype.leaveTs = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PastParticipant.prototype, "_userJid", {
            get: $util.oneOfGetter($oneOfFields = ["userJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PastParticipant.prototype, "_leaveReason", {
            get: $util.oneOfGetter($oneOfFields = ["leaveReason"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PastParticipant.prototype, "_leaveTs", {
            get: $util.oneOfGetter($oneOfFields = ["leaveTs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PastParticipant.create = function(properties) {
            return new PastParticipant(properties);
        };

        PastParticipant.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.userJid != null && $Object.hasOwnProperty.call(m, "userJid"))
                w.uint32(10).string(m.userJid);
            if (m.leaveReason != null && $Object.hasOwnProperty.call(m, "leaveReason"))
                w.uint32(16).int32(m.leaveReason);
            if (m.leaveTs != null && $Object.hasOwnProperty.call(m, "leaveTs"))
                w.uint32(24).uint64(m.leaveTs);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PastParticipant.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PastParticipant(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.userJid = r.stringVerify();
                        m._userJid = "userJid";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.leaveReason = r.int32();
                        m._leaveReason = "leaveReason";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.leaveTs = r.uint64();
                        m._leaveTs = "leaveTs";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PastParticipant.fromObject = function (d, q) {
            if (d instanceof $root.proto.PastParticipant)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PastParticipant: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PastParticipant();
            if (d.userJid != null) {
                m.userJid = $String(d.userJid);
            }
            switch (d.leaveReason) {
            case "LEFT":
            case 0:
                m.leaveReason = 0;
                break;
            case "REMOVED":
            case 1:
                m.leaveReason = 1;
                break;
            default:
                if (typeof d.leaveReason === "number" && (d.leaveReason | 0) === d.leaveReason)
                    m.leaveReason = d.leaveReason;
            }
            if (d.leaveTs != null) {
                if ($util.Long)
                    m.leaveTs = $util.Long.fromValue(d.leaveTs, true);
                else if (typeof d.leaveTs === "string")
                    m.leaveTs = $parseInt(d.leaveTs, 10);
                else if (typeof d.leaveTs === "number")
                    m.leaveTs = d.leaveTs;
                else if (typeof d.leaveTs === "object")
                    m.leaveTs = new $util.LongBits(d.leaveTs.low >>> 0, d.leaveTs.high >>> 0).toNumber(true);
            }
            return m;
        };

        PastParticipant.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.userJid != null && $Object.hasOwnProperty.call(m, "userJid")) {
                d.userJid = m.userJid;
            }
            if (m.leaveReason != null && $Object.hasOwnProperty.call(m, "leaveReason")) {
                d.leaveReason = o.enums === $String ? $root.proto.PastParticipant.LeaveReason[m.leaveReason] === $undefined ? m.leaveReason : $root.proto.PastParticipant.LeaveReason[m.leaveReason] : m.leaveReason;
            }
            if (m.leaveTs != null && $Object.hasOwnProperty.call(m, "leaveTs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.leaveTs = typeof m.leaveTs === "number" ? $BigInt(m.leaveTs) : $util.Long.fromBits(m.leaveTs.low >>> 0, m.leaveTs.high >>> 0, true).toBigInt();
                else if (typeof m.leaveTs === "number")
                    d.leaveTs = o.longs === $String ? $String(m.leaveTs) : m.leaveTs;
                else
                    d.leaveTs = o.longs === $String ? $util.Long.prototype.toString.call(m.leaveTs) : o.longs === $Number ? new $util.LongBits(m.leaveTs.low >>> 0, m.leaveTs.high >>> 0).toNumber(true) : m.leaveTs;
            }
            return d;
        };

        PastParticipant.prototype.toJSON = function() {
            return PastParticipant.toObject(this, $protobuf.util.toJSONOptions);
        };

        PastParticipant.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PastParticipant";
        };

        PastParticipant.LeaveReason = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "LEFT"] = 0;
            values[valuesById[1] = "REMOVED"] = 1;
            return values;
        })();

        return PastParticipant;
    })();

    proto.PastParticipants = (function() {

        const PastParticipants = function (p) {
            this.pastParticipants = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PastParticipants.prototype.groupJid = null;
        PastParticipants.prototype.pastParticipants = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PastParticipants.prototype, "_groupJid", {
            get: $util.oneOfGetter($oneOfFields = ["groupJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PastParticipants.create = function(properties) {
            return new PastParticipants(properties);
        };

        PastParticipants.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid"))
                w.uint32(10).string(m.groupJid);
            if (m.pastParticipants != null && m.pastParticipants.length) {
                for (var i = 0; i < m.pastParticipants.length; ++i)
                    $root.proto.PastParticipant.encode(m.pastParticipants[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PastParticipants.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PastParticipants();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.groupJid = r.stringVerify();
                        m._groupJid = "groupJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.pastParticipants && m.pastParticipants.length))
                            m.pastParticipants = [];
                        m.pastParticipants.push($root.proto.PastParticipant.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PastParticipants.fromObject = function (d, q) {
            if (d instanceof $root.proto.PastParticipants)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PastParticipants: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PastParticipants();
            if (d.groupJid != null) {
                m.groupJid = $String(d.groupJid);
            }
            if (d.pastParticipants) {
                if (!$Array.isArray(d.pastParticipants))
                    throw $TypeError(".proto.PastParticipants.pastParticipants: array expected");
                m.pastParticipants = $Array(d.pastParticipants.length);
                for (var i = 0; i < d.pastParticipants.length; ++i) {
                    if (!$util.isObject(d.pastParticipants[i]))
                        throw $TypeError(".proto.PastParticipants.pastParticipants: object expected");
                    m.pastParticipants[i] = $root.proto.PastParticipant.fromObject(d.pastParticipants[i], q + 1);
                }
            }
            return m;
        };

        PastParticipants.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.pastParticipants = [];
            }
            if (m.groupJid != null && $Object.hasOwnProperty.call(m, "groupJid")) {
                d.groupJid = m.groupJid;
            }
            if (m.pastParticipants && m.pastParticipants.length) {
                d.pastParticipants = $Array(m.pastParticipants.length);
                for (var j = 0; j < m.pastParticipants.length; ++j) {
                    d.pastParticipants[j] = $root.proto.PastParticipant.toObject(m.pastParticipants[j], o, q + 1);
                }
            }
            return d;
        };

        PastParticipants.prototype.toJSON = function() {
            return PastParticipants.toObject(this, $protobuf.util.toJSONOptions);
        };

        PastParticipants.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PastParticipants";
        };

        return PastParticipants;
    })();

    proto.PatchDebugData = (function() {

        const PatchDebugData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PatchDebugData.prototype.currentLthash = null;
        PatchDebugData.prototype.newLthash = null;
        PatchDebugData.prototype.patchVersion = null;
        PatchDebugData.prototype.collectionName = null;
        PatchDebugData.prototype.firstFourBytesFromAHashOfSnapshotMacKey = null;
        PatchDebugData.prototype.newLthashSubtract = null;
        PatchDebugData.prototype.numberAdd = null;
        PatchDebugData.prototype.numberRemove = null;
        PatchDebugData.prototype.numberOverride = null;
        PatchDebugData.prototype.senderPlatform = null;
        PatchDebugData.prototype.isSenderPrimary = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_currentLthash", {
            get: $util.oneOfGetter($oneOfFields = ["currentLthash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_newLthash", {
            get: $util.oneOfGetter($oneOfFields = ["newLthash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_patchVersion", {
            get: $util.oneOfGetter($oneOfFields = ["patchVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_collectionName", {
            get: $util.oneOfGetter($oneOfFields = ["collectionName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_firstFourBytesFromAHashOfSnapshotMacKey", {
            get: $util.oneOfGetter($oneOfFields = ["firstFourBytesFromAHashOfSnapshotMacKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_newLthashSubtract", {
            get: $util.oneOfGetter($oneOfFields = ["newLthashSubtract"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_numberAdd", {
            get: $util.oneOfGetter($oneOfFields = ["numberAdd"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_numberRemove", {
            get: $util.oneOfGetter($oneOfFields = ["numberRemove"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_numberOverride", {
            get: $util.oneOfGetter($oneOfFields = ["numberOverride"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_senderPlatform", {
            get: $util.oneOfGetter($oneOfFields = ["senderPlatform"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PatchDebugData.prototype, "_isSenderPrimary", {
            get: $util.oneOfGetter($oneOfFields = ["isSenderPrimary"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PatchDebugData.create = function(properties) {
            return new PatchDebugData(properties);
        };

        PatchDebugData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.currentLthash != null && $Object.hasOwnProperty.call(m, "currentLthash"))
                w.uint32(10).bytes(m.currentLthash);
            if (m.newLthash != null && $Object.hasOwnProperty.call(m, "newLthash"))
                w.uint32(18).bytes(m.newLthash);
            if (m.patchVersion != null && $Object.hasOwnProperty.call(m, "patchVersion"))
                w.uint32(26).bytes(m.patchVersion);
            if (m.collectionName != null && $Object.hasOwnProperty.call(m, "collectionName"))
                w.uint32(34).bytes(m.collectionName);
            if (m.firstFourBytesFromAHashOfSnapshotMacKey != null && $Object.hasOwnProperty.call(m, "firstFourBytesFromAHashOfSnapshotMacKey"))
                w.uint32(42).bytes(m.firstFourBytesFromAHashOfSnapshotMacKey);
            if (m.newLthashSubtract != null && $Object.hasOwnProperty.call(m, "newLthashSubtract"))
                w.uint32(50).bytes(m.newLthashSubtract);
            if (m.numberAdd != null && $Object.hasOwnProperty.call(m, "numberAdd"))
                w.uint32(56).int32(m.numberAdd);
            if (m.numberRemove != null && $Object.hasOwnProperty.call(m, "numberRemove"))
                w.uint32(64).int32(m.numberRemove);
            if (m.numberOverride != null && $Object.hasOwnProperty.call(m, "numberOverride"))
                w.uint32(72).int32(m.numberOverride);
            if (m.senderPlatform != null && $Object.hasOwnProperty.call(m, "senderPlatform"))
                w.uint32(80).int32(m.senderPlatform);
            if (m.isSenderPrimary != null && $Object.hasOwnProperty.call(m, "isSenderPrimary"))
                w.uint32(88).bool(m.isSenderPrimary);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PatchDebugData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PatchDebugData(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.currentLthash = r.bytes();
                        m._currentLthash = "currentLthash";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.newLthash = r.bytes();
                        m._newLthash = "newLthash";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.patchVersion = r.bytes();
                        m._patchVersion = "patchVersion";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.collectionName = r.bytes();
                        m._collectionName = "collectionName";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.firstFourBytesFromAHashOfSnapshotMacKey = r.bytes();
                        m._firstFourBytesFromAHashOfSnapshotMacKey = "firstFourBytesFromAHashOfSnapshotMacKey";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.newLthashSubtract = r.bytes();
                        m._newLthashSubtract = "newLthashSubtract";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.numberAdd = r.int32();
                        m._numberAdd = "numberAdd";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.numberRemove = r.int32();
                        m._numberRemove = "numberRemove";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.numberOverride = r.int32();
                        m._numberOverride = "numberOverride";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.senderPlatform = r.int32();
                        m._senderPlatform = "senderPlatform";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.isSenderPrimary = r.bool();
                        m._isSenderPrimary = "isSenderPrimary";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PatchDebugData.fromObject = function (d, q) {
            if (d instanceof $root.proto.PatchDebugData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PatchDebugData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PatchDebugData();
            if (d.currentLthash != null) {
                if (typeof d.currentLthash === "string")
                    $util.base64.decode(d.currentLthash, m.currentLthash = $util.newBuffer($util.base64.length(d.currentLthash)), 0);
                else if (d.currentLthash.length >= 0)
                    m.currentLthash = d.currentLthash;
            }
            if (d.newLthash != null) {
                if (typeof d.newLthash === "string")
                    $util.base64.decode(d.newLthash, m.newLthash = $util.newBuffer($util.base64.length(d.newLthash)), 0);
                else if (d.newLthash.length >= 0)
                    m.newLthash = d.newLthash;
            }
            if (d.patchVersion != null) {
                if (typeof d.patchVersion === "string")
                    $util.base64.decode(d.patchVersion, m.patchVersion = $util.newBuffer($util.base64.length(d.patchVersion)), 0);
                else if (d.patchVersion.length >= 0)
                    m.patchVersion = d.patchVersion;
            }
            if (d.collectionName != null) {
                if (typeof d.collectionName === "string")
                    $util.base64.decode(d.collectionName, m.collectionName = $util.newBuffer($util.base64.length(d.collectionName)), 0);
                else if (d.collectionName.length >= 0)
                    m.collectionName = d.collectionName;
            }
            if (d.firstFourBytesFromAHashOfSnapshotMacKey != null) {
                if (typeof d.firstFourBytesFromAHashOfSnapshotMacKey === "string")
                    $util.base64.decode(d.firstFourBytesFromAHashOfSnapshotMacKey, m.firstFourBytesFromAHashOfSnapshotMacKey = $util.newBuffer($util.base64.length(d.firstFourBytesFromAHashOfSnapshotMacKey)), 0);
                else if (d.firstFourBytesFromAHashOfSnapshotMacKey.length >= 0)
                    m.firstFourBytesFromAHashOfSnapshotMacKey = d.firstFourBytesFromAHashOfSnapshotMacKey;
            }
            if (d.newLthashSubtract != null) {
                if (typeof d.newLthashSubtract === "string")
                    $util.base64.decode(d.newLthashSubtract, m.newLthashSubtract = $util.newBuffer($util.base64.length(d.newLthashSubtract)), 0);
                else if (d.newLthashSubtract.length >= 0)
                    m.newLthashSubtract = d.newLthashSubtract;
            }
            if (d.numberAdd != null) {
                m.numberAdd = d.numberAdd | 0;
            }
            if (d.numberRemove != null) {
                m.numberRemove = d.numberRemove | 0;
            }
            if (d.numberOverride != null) {
                m.numberOverride = d.numberOverride | 0;
            }
            switch (d.senderPlatform) {
            case "ANDROID":
            case 0:
                m.senderPlatform = 0;
                break;
            case "SMBA":
            case 1:
                m.senderPlatform = 1;
                break;
            case "IPHONE":
            case 2:
                m.senderPlatform = 2;
                break;
            case "SMBI":
            case 3:
                m.senderPlatform = 3;
                break;
            case "WEB":
            case 4:
                m.senderPlatform = 4;
                break;
            case "UWP":
            case 5:
                m.senderPlatform = 5;
                break;
            case "DARWIN":
            case 6:
                m.senderPlatform = 6;
                break;
            case "IPAD":
            case 7:
                m.senderPlatform = 7;
                break;
            case "WEAROS":
            case 8:
                m.senderPlatform = 8;
                break;
            case "WASG":
            case 9:
                m.senderPlatform = 9;
                break;
            case "WEARM":
            case 10:
                m.senderPlatform = 10;
                break;
            case "CAPI":
            case 11:
                m.senderPlatform = 11;
                break;
            default:
                if (typeof d.senderPlatform === "number" && (d.senderPlatform | 0) === d.senderPlatform)
                    m.senderPlatform = d.senderPlatform;
            }
            if (d.isSenderPrimary != null) {
                m.isSenderPrimary = $Boolean(d.isSenderPrimary);
            }
            return m;
        };

        PatchDebugData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.currentLthash != null && $Object.hasOwnProperty.call(m, "currentLthash")) {
                d.currentLthash = o.bytes === $String ? $util.base64.encode(m.currentLthash, 0, m.currentLthash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.currentLthash) : m.currentLthash;
            }
            if (m.newLthash != null && $Object.hasOwnProperty.call(m, "newLthash")) {
                d.newLthash = o.bytes === $String ? $util.base64.encode(m.newLthash, 0, m.newLthash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.newLthash) : m.newLthash;
            }
            if (m.patchVersion != null && $Object.hasOwnProperty.call(m, "patchVersion")) {
                d.patchVersion = o.bytes === $String ? $util.base64.encode(m.patchVersion, 0, m.patchVersion.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.patchVersion) : m.patchVersion;
            }
            if (m.collectionName != null && $Object.hasOwnProperty.call(m, "collectionName")) {
                d.collectionName = o.bytes === $String ? $util.base64.encode(m.collectionName, 0, m.collectionName.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.collectionName) : m.collectionName;
            }
            if (m.firstFourBytesFromAHashOfSnapshotMacKey != null && $Object.hasOwnProperty.call(m, "firstFourBytesFromAHashOfSnapshotMacKey")) {
                d.firstFourBytesFromAHashOfSnapshotMacKey = o.bytes === $String ? $util.base64.encode(m.firstFourBytesFromAHashOfSnapshotMacKey, 0, m.firstFourBytesFromAHashOfSnapshotMacKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.firstFourBytesFromAHashOfSnapshotMacKey) : m.firstFourBytesFromAHashOfSnapshotMacKey;
            }
            if (m.newLthashSubtract != null && $Object.hasOwnProperty.call(m, "newLthashSubtract")) {
                d.newLthashSubtract = o.bytes === $String ? $util.base64.encode(m.newLthashSubtract, 0, m.newLthashSubtract.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.newLthashSubtract) : m.newLthashSubtract;
            }
            if (m.numberAdd != null && $Object.hasOwnProperty.call(m, "numberAdd")) {
                d.numberAdd = m.numberAdd;
            }
            if (m.numberRemove != null && $Object.hasOwnProperty.call(m, "numberRemove")) {
                d.numberRemove = m.numberRemove;
            }
            if (m.numberOverride != null && $Object.hasOwnProperty.call(m, "numberOverride")) {
                d.numberOverride = m.numberOverride;
            }
            if (m.senderPlatform != null && $Object.hasOwnProperty.call(m, "senderPlatform")) {
                d.senderPlatform = o.enums === $String ? $root.proto.PatchDebugData.Platform[m.senderPlatform] === $undefined ? m.senderPlatform : $root.proto.PatchDebugData.Platform[m.senderPlatform] : m.senderPlatform;
            }
            if (m.isSenderPrimary != null && $Object.hasOwnProperty.call(m, "isSenderPrimary")) {
                d.isSenderPrimary = m.isSenderPrimary;
            }
            return d;
        };

        PatchDebugData.prototype.toJSON = function() {
            return PatchDebugData.toObject(this, $protobuf.util.toJSONOptions);
        };

        PatchDebugData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PatchDebugData";
        };

        PatchDebugData.Platform = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ANDROID"] = 0;
            values[valuesById[1] = "SMBA"] = 1;
            values[valuesById[2] = "IPHONE"] = 2;
            values[valuesById[3] = "SMBI"] = 3;
            values[valuesById[4] = "WEB"] = 4;
            values[valuesById[5] = "UWP"] = 5;
            values[valuesById[6] = "DARWIN"] = 6;
            values[valuesById[7] = "IPAD"] = 7;
            values[valuesById[8] = "WEAROS"] = 8;
            values[valuesById[9] = "WASG"] = 9;
            values[valuesById[10] = "WEARM"] = 10;
            values[valuesById[11] = "CAPI"] = 11;
            return values;
        })();

        return PatchDebugData;
    })();

    proto.PaymentBackground = (function() {

        const PaymentBackground = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PaymentBackground.prototype.id = null;
        PaymentBackground.prototype.fileLength = null;
        PaymentBackground.prototype.width = null;
        PaymentBackground.prototype.height = null;
        PaymentBackground.prototype.mimetype = null;
        PaymentBackground.prototype.placeholderArgb = null;
        PaymentBackground.prototype.textArgb = null;
        PaymentBackground.prototype.subtextArgb = null;
        PaymentBackground.prototype.mediaData = null;
        PaymentBackground.prototype.type = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_placeholderArgb", {
            get: $util.oneOfGetter($oneOfFields = ["placeholderArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_textArgb", {
            get: $util.oneOfGetter($oneOfFields = ["textArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_subtextArgb", {
            get: $util.oneOfGetter($oneOfFields = ["subtextArgb"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_mediaData", {
            get: $util.oneOfGetter($oneOfFields = ["mediaData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentBackground.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentBackground.create = function(properties) {
            return new PaymentBackground(properties);
        };

        PaymentBackground.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(10).string(m.id);
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(16).uint64(m.fileLength);
            if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                w.uint32(24).uint32(m.width);
            if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                w.uint32(32).uint32(m.height);
            if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(42).string(m.mimetype);
            if (m.placeholderArgb != null && $Object.hasOwnProperty.call(m, "placeholderArgb"))
                w.uint32(53).fixed32(m.placeholderArgb);
            if (m.textArgb != null && $Object.hasOwnProperty.call(m, "textArgb"))
                w.uint32(61).fixed32(m.textArgb);
            if (m.subtextArgb != null && $Object.hasOwnProperty.call(m, "subtextArgb"))
                w.uint32(69).fixed32(m.subtextArgb);
            if (m.mediaData != null && $Object.hasOwnProperty.call(m, "mediaData"))
                $root.proto.PaymentBackground.MediaData.encode(m.mediaData, w.uint32(74).fork(), q + 1).ldelim();
            if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                w.uint32(80).int32(m.type);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PaymentBackground.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PaymentBackground(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.id = r.stringVerify();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.fileLength = r.uint64();
                        m._fileLength = "fileLength";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.width = r.uint32();
                        m._width = "width";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.height = r.uint32();
                        m._height = "height";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.mimetype = r.stringVerify();
                        m._mimetype = "mimetype";
                        continue;
                    }
                case 6: {
                        if (u !== 5)
                            break;
                        m.placeholderArgb = r.fixed32();
                        m._placeholderArgb = "placeholderArgb";
                        continue;
                    }
                case 7: {
                        if (u !== 5)
                            break;
                        m.textArgb = r.fixed32();
                        m._textArgb = "textArgb";
                        continue;
                    }
                case 8: {
                        if (u !== 5)
                            break;
                        m.subtextArgb = r.fixed32();
                        m._subtextArgb = "subtextArgb";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.mediaData = $root.proto.PaymentBackground.MediaData.decode(r, r.uint32(), $undefined, q + 1, m.mediaData);
                        m._mediaData = "mediaData";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.type = r.int32();
                        m._type = "type";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PaymentBackground.fromObject = function (d, q) {
            if (d instanceof $root.proto.PaymentBackground)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PaymentBackground: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PaymentBackground();
            if (d.id != null) {
                m.id = $String(d.id);
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    m.fileLength = $util.Long.fromValue(d.fileLength, true);
                else if (typeof d.fileLength === "string")
                    m.fileLength = $parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.mimetype != null) {
                m.mimetype = $String(d.mimetype);
            }
            if (d.placeholderArgb != null) {
                m.placeholderArgb = d.placeholderArgb >>> 0;
            }
            if (d.textArgb != null) {
                m.textArgb = d.textArgb >>> 0;
            }
            if (d.subtextArgb != null) {
                m.subtextArgb = d.subtextArgb >>> 0;
            }
            if (d.mediaData != null) {
                if (!$util.isObject(d.mediaData))
                    throw $TypeError(".proto.PaymentBackground.mediaData: object expected");
                m.mediaData = $root.proto.PaymentBackground.MediaData.fromObject(d.mediaData, q + 1);
            }
            switch (d.type) {
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "DEFAULT":
            case 1:
                m.type = 1;
                break;
            default:
                if (typeof d.type === "number" && (d.type | 0) === d.type)
                    m.type = d.type;
            }
            return m;
        };

        PaymentBackground.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                else if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
            }
            if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                d.width = m.width;
            }
            if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                d.height = m.height;
            }
            if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                d.mimetype = m.mimetype;
            }
            if (m.placeholderArgb != null && $Object.hasOwnProperty.call(m, "placeholderArgb")) {
                d.placeholderArgb = m.placeholderArgb;
            }
            if (m.textArgb != null && $Object.hasOwnProperty.call(m, "textArgb")) {
                d.textArgb = m.textArgb;
            }
            if (m.subtextArgb != null && $Object.hasOwnProperty.call(m, "subtextArgb")) {
                d.subtextArgb = m.subtextArgb;
            }
            if (m.mediaData != null && $Object.hasOwnProperty.call(m, "mediaData")) {
                d.mediaData = $root.proto.PaymentBackground.MediaData.toObject(m.mediaData, o, q + 1);
            }
            if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                d.type = o.enums === $String ? $root.proto.PaymentBackground.Type[m.type] === $undefined ? m.type : $root.proto.PaymentBackground.Type[m.type] : m.type;
            }
            return d;
        };

        PaymentBackground.prototype.toJSON = function() {
            return PaymentBackground.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentBackground.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PaymentBackground";
        };

        PaymentBackground.MediaData = (function() {

            const MediaData = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MediaData.prototype.mediaKey = null;
            MediaData.prototype.mediaKeyTimestamp = null;
            MediaData.prototype.fileSha256 = null;
            MediaData.prototype.fileEncSha256 = null;
            MediaData.prototype.directPath = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaData.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaData.prototype, "_mediaKeyTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKeyTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaData.prototype, "_fileSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaData.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MediaData.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MediaData.create = function(properties) {
                return new MediaData(properties);
            };

            MediaData.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(10).bytes(m.mediaKey);
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp"))
                    w.uint32(16).int64(m.mediaKeyTimestamp);
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                    w.uint32(26).bytes(m.fileSha256);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(34).bytes(m.fileEncSha256);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(42).string(m.directPath);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MediaData.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PaymentBackground.MediaData();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.mediaKeyTimestamp = r.int64();
                            m._mediaKeyTimestamp = "mediaKeyTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.fileSha256 = r.bytes();
                            m._fileSha256 = "fileSha256";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MediaData.fromObject = function (d, q) {
                if (d instanceof $root.proto.PaymentBackground.MediaData)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.PaymentBackground.MediaData: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.PaymentBackground.MediaData();
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mediaKeyTimestamp != null) {
                    if ($util.Long)
                        m.mediaKeyTimestamp = $util.Long.fromValue(d.mediaKeyTimestamp, false);
                    else if (typeof d.mediaKeyTimestamp === "string")
                        m.mediaKeyTimestamp = $parseInt(d.mediaKeyTimestamp, 10);
                    else if (typeof d.mediaKeyTimestamp === "number")
                        m.mediaKeyTimestamp = d.mediaKeyTimestamp;
                    else if (typeof d.mediaKeyTimestamp === "object")
                        m.mediaKeyTimestamp = new $util.LongBits(d.mediaKeyTimestamp.low >>> 0, d.mediaKeyTimestamp.high >>> 0).toNumber();
                }
                if (d.fileSha256 != null) {
                    if (typeof d.fileSha256 === "string")
                        $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                    else if (d.fileSha256.length >= 0)
                        m.fileSha256 = d.fileSha256;
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                return m;
            };

            MediaData.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.mediaKeyTimestamp != null && $Object.hasOwnProperty.call(m, "mediaKeyTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.mediaKeyTimestamp = typeof m.mediaKeyTimestamp === "number" ? $BigInt(m.mediaKeyTimestamp) : $util.Long.fromBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.mediaKeyTimestamp === "number")
                        d.mediaKeyTimestamp = o.longs === $String ? $String(m.mediaKeyTimestamp) : m.mediaKeyTimestamp;
                    else
                        d.mediaKeyTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.mediaKeyTimestamp) : o.longs === $Number ? new $util.LongBits(m.mediaKeyTimestamp.low >>> 0, m.mediaKeyTimestamp.high >>> 0).toNumber() : m.mediaKeyTimestamp;
                }
                if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                    d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                return d;
            };

            MediaData.prototype.toJSON = function() {
                return MediaData.toObject(this, $protobuf.util.toJSONOptions);
            };

            MediaData.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.PaymentBackground.MediaData";
            };

            return MediaData;
        })();

        PaymentBackground.Type = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "DEFAULT"] = 1;
            return values;
        })();

        return PaymentBackground;
    })();

    proto.PaymentInfo = (function() {

        const PaymentInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PaymentInfo.prototype.currencyDeprecated = null;
        PaymentInfo.prototype.amount1000 = null;
        PaymentInfo.prototype.receiverJid = null;
        PaymentInfo.prototype.status = null;
        PaymentInfo.prototype.transactionTimestamp = null;
        PaymentInfo.prototype.requestMessageKey = null;
        PaymentInfo.prototype.expiryTimestamp = null;
        PaymentInfo.prototype.futureproofed = null;
        PaymentInfo.prototype.currency = null;
        PaymentInfo.prototype.txnStatus = null;
        PaymentInfo.prototype.useNoviFiatFormat = null;
        PaymentInfo.prototype.primaryAmount = null;
        PaymentInfo.prototype.exchangeAmount = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_currencyDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["currencyDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_amount1000", {
            get: $util.oneOfGetter($oneOfFields = ["amount1000"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_receiverJid", {
            get: $util.oneOfGetter($oneOfFields = ["receiverJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_transactionTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["transactionTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_requestMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["requestMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_expiryTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["expiryTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_futureproofed", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_currency", {
            get: $util.oneOfGetter($oneOfFields = ["currency"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_txnStatus", {
            get: $util.oneOfGetter($oneOfFields = ["txnStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_useNoviFiatFormat", {
            get: $util.oneOfGetter($oneOfFields = ["useNoviFiatFormat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_primaryAmount", {
            get: $util.oneOfGetter($oneOfFields = ["primaryAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PaymentInfo.prototype, "_exchangeAmount", {
            get: $util.oneOfGetter($oneOfFields = ["exchangeAmount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PaymentInfo.create = function(properties) {
            return new PaymentInfo(properties);
        };

        PaymentInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.currencyDeprecated != null && $Object.hasOwnProperty.call(m, "currencyDeprecated"))
                w.uint32(8).int32(m.currencyDeprecated);
            if (m.amount1000 != null && $Object.hasOwnProperty.call(m, "amount1000"))
                w.uint32(16).uint64(m.amount1000);
            if (m.receiverJid != null && $Object.hasOwnProperty.call(m, "receiverJid"))
                w.uint32(26).string(m.receiverJid);
            if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                w.uint32(32).int32(m.status);
            if (m.transactionTimestamp != null && $Object.hasOwnProperty.call(m, "transactionTimestamp"))
                w.uint32(40).uint64(m.transactionTimestamp);
            if (m.requestMessageKey != null && $Object.hasOwnProperty.call(m, "requestMessageKey"))
                $root.proto.MessageKey.encode(m.requestMessageKey, w.uint32(50).fork(), q + 1).ldelim();
            if (m.expiryTimestamp != null && $Object.hasOwnProperty.call(m, "expiryTimestamp"))
                w.uint32(56).uint64(m.expiryTimestamp);
            if (m.futureproofed != null && $Object.hasOwnProperty.call(m, "futureproofed"))
                w.uint32(64).bool(m.futureproofed);
            if (m.currency != null && $Object.hasOwnProperty.call(m, "currency"))
                w.uint32(74).string(m.currency);
            if (m.txnStatus != null && $Object.hasOwnProperty.call(m, "txnStatus"))
                w.uint32(80).int32(m.txnStatus);
            if (m.useNoviFiatFormat != null && $Object.hasOwnProperty.call(m, "useNoviFiatFormat"))
                w.uint32(88).bool(m.useNoviFiatFormat);
            if (m.primaryAmount != null && $Object.hasOwnProperty.call(m, "primaryAmount"))
                $root.proto.Money.encode(m.primaryAmount, w.uint32(98).fork(), q + 1).ldelim();
            if (m.exchangeAmount != null && $Object.hasOwnProperty.call(m, "exchangeAmount"))
                $root.proto.Money.encode(m.exchangeAmount, w.uint32(106).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PaymentInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PaymentInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.currencyDeprecated = r.int32();
                        m._currencyDeprecated = "currencyDeprecated";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.amount1000 = r.uint64();
                        m._amount1000 = "amount1000";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.receiverJid = r.stringVerify();
                        m._receiverJid = "receiverJid";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.status = r.int32();
                        m._status = "status";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.transactionTimestamp = r.uint64();
                        m._transactionTimestamp = "transactionTimestamp";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.requestMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.requestMessageKey);
                        m._requestMessageKey = "requestMessageKey";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.expiryTimestamp = r.uint64();
                        m._expiryTimestamp = "expiryTimestamp";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.futureproofed = r.bool();
                        m._futureproofed = "futureproofed";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.currency = r.stringVerify();
                        m._currency = "currency";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.txnStatus = r.int32();
                        m._txnStatus = "txnStatus";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.useNoviFiatFormat = r.bool();
                        m._useNoviFiatFormat = "useNoviFiatFormat";
                        continue;
                    }
                case 12: {
                        if (u !== 2)
                            break;
                        m.primaryAmount = $root.proto.Money.decode(r, r.uint32(), $undefined, q + 1, m.primaryAmount);
                        m._primaryAmount = "primaryAmount";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.exchangeAmount = $root.proto.Money.decode(r, r.uint32(), $undefined, q + 1, m.exchangeAmount);
                        m._exchangeAmount = "exchangeAmount";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PaymentInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.PaymentInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PaymentInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PaymentInfo();
            switch (d.currencyDeprecated) {
            case "UNKNOWN_CURRENCY":
            case 0:
                m.currencyDeprecated = 0;
                break;
            case "INR":
            case 1:
                m.currencyDeprecated = 1;
                break;
            default:
                if (typeof d.currencyDeprecated === "number" && (d.currencyDeprecated | 0) === d.currencyDeprecated)
                    m.currencyDeprecated = d.currencyDeprecated;
            }
            if (d.amount1000 != null) {
                if ($util.Long)
                    m.amount1000 = $util.Long.fromValue(d.amount1000, true);
                else if (typeof d.amount1000 === "string")
                    m.amount1000 = $parseInt(d.amount1000, 10);
                else if (typeof d.amount1000 === "number")
                    m.amount1000 = d.amount1000;
                else if (typeof d.amount1000 === "object")
                    m.amount1000 = new $util.LongBits(d.amount1000.low >>> 0, d.amount1000.high >>> 0).toNumber(true);
            }
            if (d.receiverJid != null) {
                m.receiverJid = $String(d.receiverJid);
            }
            switch (d.status) {
            case "UNKNOWN_STATUS":
            case 0:
                m.status = 0;
                break;
            case "PROCESSING":
            case 1:
                m.status = 1;
                break;
            case "SENT":
            case 2:
                m.status = 2;
                break;
            case "NEED_TO_ACCEPT":
            case 3:
                m.status = 3;
                break;
            case "COMPLETE":
            case 4:
                m.status = 4;
                break;
            case "COULD_NOT_COMPLETE":
            case 5:
                m.status = 5;
                break;
            case "REFUNDED":
            case 6:
                m.status = 6;
                break;
            case "EXPIRED":
            case 7:
                m.status = 7;
                break;
            case "REJECTED":
            case 8:
                m.status = 8;
                break;
            case "CANCELLED":
            case 9:
                m.status = 9;
                break;
            case "WAITING_FOR_PAYER":
            case 10:
                m.status = 10;
                break;
            case "WAITING":
            case 11:
                m.status = 11;
                break;
            default:
                if (typeof d.status === "number" && (d.status | 0) === d.status)
                    m.status = d.status;
            }
            if (d.transactionTimestamp != null) {
                if ($util.Long)
                    m.transactionTimestamp = $util.Long.fromValue(d.transactionTimestamp, true);
                else if (typeof d.transactionTimestamp === "string")
                    m.transactionTimestamp = $parseInt(d.transactionTimestamp, 10);
                else if (typeof d.transactionTimestamp === "number")
                    m.transactionTimestamp = d.transactionTimestamp;
                else if (typeof d.transactionTimestamp === "object")
                    m.transactionTimestamp = new $util.LongBits(d.transactionTimestamp.low >>> 0, d.transactionTimestamp.high >>> 0).toNumber(true);
            }
            if (d.requestMessageKey != null) {
                if (!$util.isObject(d.requestMessageKey))
                    throw $TypeError(".proto.PaymentInfo.requestMessageKey: object expected");
                m.requestMessageKey = $root.proto.MessageKey.fromObject(d.requestMessageKey, q + 1);
            }
            if (d.expiryTimestamp != null) {
                if ($util.Long)
                    m.expiryTimestamp = $util.Long.fromValue(d.expiryTimestamp, true);
                else if (typeof d.expiryTimestamp === "string")
                    m.expiryTimestamp = $parseInt(d.expiryTimestamp, 10);
                else if (typeof d.expiryTimestamp === "number")
                    m.expiryTimestamp = d.expiryTimestamp;
                else if (typeof d.expiryTimestamp === "object")
                    m.expiryTimestamp = new $util.LongBits(d.expiryTimestamp.low >>> 0, d.expiryTimestamp.high >>> 0).toNumber(true);
            }
            if (d.futureproofed != null) {
                m.futureproofed = $Boolean(d.futureproofed);
            }
            if (d.currency != null) {
                m.currency = $String(d.currency);
            }
            switch (d.txnStatus) {
            case "UNKNOWN":
            case 0:
                m.txnStatus = 0;
                break;
            case "PENDING_SETUP":
            case 1:
                m.txnStatus = 1;
                break;
            case "PENDING_RECEIVER_SETUP":
            case 2:
                m.txnStatus = 2;
                break;
            case "INIT":
            case 3:
                m.txnStatus = 3;
                break;
            case "SUCCESS":
            case 4:
                m.txnStatus = 4;
                break;
            case "COMPLETED":
            case 5:
                m.txnStatus = 5;
                break;
            case "FAILED":
            case 6:
                m.txnStatus = 6;
                break;
            case "FAILED_RISK":
            case 7:
                m.txnStatus = 7;
                break;
            case "FAILED_PROCESSING":
            case 8:
                m.txnStatus = 8;
                break;
            case "FAILED_RECEIVER_PROCESSING":
            case 9:
                m.txnStatus = 9;
                break;
            case "FAILED_DA":
            case 10:
                m.txnStatus = 10;
                break;
            case "FAILED_DA_FINAL":
            case 11:
                m.txnStatus = 11;
                break;
            case "REFUNDED_TXN":
            case 12:
                m.txnStatus = 12;
                break;
            case "REFUND_FAILED":
            case 13:
                m.txnStatus = 13;
                break;
            case "REFUND_FAILED_PROCESSING":
            case 14:
                m.txnStatus = 14;
                break;
            case "REFUND_FAILED_DA":
            case 15:
                m.txnStatus = 15;
                break;
            case "EXPIRED_TXN":
            case 16:
                m.txnStatus = 16;
                break;
            case "AUTH_CANCELED":
            case 17:
                m.txnStatus = 17;
                break;
            case "AUTH_CANCEL_FAILED_PROCESSING":
            case 18:
                m.txnStatus = 18;
                break;
            case "AUTH_CANCEL_FAILED":
            case 19:
                m.txnStatus = 19;
                break;
            case "COLLECT_INIT":
            case 20:
                m.txnStatus = 20;
                break;
            case "COLLECT_SUCCESS":
            case 21:
                m.txnStatus = 21;
                break;
            case "COLLECT_FAILED":
            case 22:
                m.txnStatus = 22;
                break;
            case "COLLECT_FAILED_RISK":
            case 23:
                m.txnStatus = 23;
                break;
            case "COLLECT_REJECTED":
            case 24:
                m.txnStatus = 24;
                break;
            case "COLLECT_EXPIRED":
            case 25:
                m.txnStatus = 25;
                break;
            case "COLLECT_CANCELED":
            case 26:
                m.txnStatus = 26;
                break;
            case "COLLECT_CANCELLING":
            case 27:
                m.txnStatus = 27;
                break;
            case "IN_REVIEW":
            case 28:
                m.txnStatus = 28;
                break;
            case "REVERSAL_SUCCESS":
            case 29:
                m.txnStatus = 29;
                break;
            case "REVERSAL_PENDING":
            case 30:
                m.txnStatus = 30;
                break;
            case "REFUND_PENDING":
            case 31:
                m.txnStatus = 31;
                break;
            default:
                if (typeof d.txnStatus === "number" && (d.txnStatus | 0) === d.txnStatus)
                    m.txnStatus = d.txnStatus;
            }
            if (d.useNoviFiatFormat != null) {
                m.useNoviFiatFormat = $Boolean(d.useNoviFiatFormat);
            }
            if (d.primaryAmount != null) {
                if (!$util.isObject(d.primaryAmount))
                    throw $TypeError(".proto.PaymentInfo.primaryAmount: object expected");
                m.primaryAmount = $root.proto.Money.fromObject(d.primaryAmount, q + 1);
            }
            if (d.exchangeAmount != null) {
                if (!$util.isObject(d.exchangeAmount))
                    throw $TypeError(".proto.PaymentInfo.exchangeAmount: object expected");
                m.exchangeAmount = $root.proto.Money.fromObject(d.exchangeAmount, q + 1);
            }
            return m;
        };

        PaymentInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.currencyDeprecated != null && $Object.hasOwnProperty.call(m, "currencyDeprecated")) {
                d.currencyDeprecated = o.enums === $String ? $root.proto.PaymentInfo.Currency[m.currencyDeprecated] === $undefined ? m.currencyDeprecated : $root.proto.PaymentInfo.Currency[m.currencyDeprecated] : m.currencyDeprecated;
            }
            if (m.amount1000 != null && $Object.hasOwnProperty.call(m, "amount1000")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.amount1000 = typeof m.amount1000 === "number" ? $BigInt(m.amount1000) : $util.Long.fromBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0, true).toBigInt();
                else if (typeof m.amount1000 === "number")
                    d.amount1000 = o.longs === $String ? $String(m.amount1000) : m.amount1000;
                else
                    d.amount1000 = o.longs === $String ? $util.Long.prototype.toString.call(m.amount1000) : o.longs === $Number ? new $util.LongBits(m.amount1000.low >>> 0, m.amount1000.high >>> 0).toNumber(true) : m.amount1000;
            }
            if (m.receiverJid != null && $Object.hasOwnProperty.call(m, "receiverJid")) {
                d.receiverJid = m.receiverJid;
            }
            if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                d.status = o.enums === $String ? $root.proto.PaymentInfo.Status[m.status] === $undefined ? m.status : $root.proto.PaymentInfo.Status[m.status] : m.status;
            }
            if (m.transactionTimestamp != null && $Object.hasOwnProperty.call(m, "transactionTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.transactionTimestamp = typeof m.transactionTimestamp === "number" ? $BigInt(m.transactionTimestamp) : $util.Long.fromBits(m.transactionTimestamp.low >>> 0, m.transactionTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.transactionTimestamp === "number")
                    d.transactionTimestamp = o.longs === $String ? $String(m.transactionTimestamp) : m.transactionTimestamp;
                else
                    d.transactionTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.transactionTimestamp) : o.longs === $Number ? new $util.LongBits(m.transactionTimestamp.low >>> 0, m.transactionTimestamp.high >>> 0).toNumber(true) : m.transactionTimestamp;
            }
            if (m.requestMessageKey != null && $Object.hasOwnProperty.call(m, "requestMessageKey")) {
                d.requestMessageKey = $root.proto.MessageKey.toObject(m.requestMessageKey, o, q + 1);
            }
            if (m.expiryTimestamp != null && $Object.hasOwnProperty.call(m, "expiryTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.expiryTimestamp = typeof m.expiryTimestamp === "number" ? $BigInt(m.expiryTimestamp) : $util.Long.fromBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.expiryTimestamp === "number")
                    d.expiryTimestamp = o.longs === $String ? $String(m.expiryTimestamp) : m.expiryTimestamp;
                else
                    d.expiryTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.expiryTimestamp) : o.longs === $Number ? new $util.LongBits(m.expiryTimestamp.low >>> 0, m.expiryTimestamp.high >>> 0).toNumber(true) : m.expiryTimestamp;
            }
            if (m.futureproofed != null && $Object.hasOwnProperty.call(m, "futureproofed")) {
                d.futureproofed = m.futureproofed;
            }
            if (m.currency != null && $Object.hasOwnProperty.call(m, "currency")) {
                d.currency = m.currency;
            }
            if (m.txnStatus != null && $Object.hasOwnProperty.call(m, "txnStatus")) {
                d.txnStatus = o.enums === $String ? $root.proto.PaymentInfo.TxnStatus[m.txnStatus] === $undefined ? m.txnStatus : $root.proto.PaymentInfo.TxnStatus[m.txnStatus] : m.txnStatus;
            }
            if (m.useNoviFiatFormat != null && $Object.hasOwnProperty.call(m, "useNoviFiatFormat")) {
                d.useNoviFiatFormat = m.useNoviFiatFormat;
            }
            if (m.primaryAmount != null && $Object.hasOwnProperty.call(m, "primaryAmount")) {
                d.primaryAmount = $root.proto.Money.toObject(m.primaryAmount, o, q + 1);
            }
            if (m.exchangeAmount != null && $Object.hasOwnProperty.call(m, "exchangeAmount")) {
                d.exchangeAmount = $root.proto.Money.toObject(m.exchangeAmount, o, q + 1);
            }
            return d;
        };

        PaymentInfo.prototype.toJSON = function() {
            return PaymentInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        PaymentInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PaymentInfo";
        };

        PaymentInfo.Currency = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_CURRENCY"] = 0;
            values[valuesById[1] = "INR"] = 1;
            return values;
        })();

        PaymentInfo.Status = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_STATUS"] = 0;
            values[valuesById[1] = "PROCESSING"] = 1;
            values[valuesById[2] = "SENT"] = 2;
            values[valuesById[3] = "NEED_TO_ACCEPT"] = 3;
            values[valuesById[4] = "COMPLETE"] = 4;
            values[valuesById[5] = "COULD_NOT_COMPLETE"] = 5;
            values[valuesById[6] = "REFUNDED"] = 6;
            values[valuesById[7] = "EXPIRED"] = 7;
            values[valuesById[8] = "REJECTED"] = 8;
            values[valuesById[9] = "CANCELLED"] = 9;
            values[valuesById[10] = "WAITING_FOR_PAYER"] = 10;
            values[valuesById[11] = "WAITING"] = 11;
            return values;
        })();

        PaymentInfo.TxnStatus = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "PENDING_SETUP"] = 1;
            values[valuesById[2] = "PENDING_RECEIVER_SETUP"] = 2;
            values[valuesById[3] = "INIT"] = 3;
            values[valuesById[4] = "SUCCESS"] = 4;
            values[valuesById[5] = "COMPLETED"] = 5;
            values[valuesById[6] = "FAILED"] = 6;
            values[valuesById[7] = "FAILED_RISK"] = 7;
            values[valuesById[8] = "FAILED_PROCESSING"] = 8;
            values[valuesById[9] = "FAILED_RECEIVER_PROCESSING"] = 9;
            values[valuesById[10] = "FAILED_DA"] = 10;
            values[valuesById[11] = "FAILED_DA_FINAL"] = 11;
            values[valuesById[12] = "REFUNDED_TXN"] = 12;
            values[valuesById[13] = "REFUND_FAILED"] = 13;
            values[valuesById[14] = "REFUND_FAILED_PROCESSING"] = 14;
            values[valuesById[15] = "REFUND_FAILED_DA"] = 15;
            values[valuesById[16] = "EXPIRED_TXN"] = 16;
            values[valuesById[17] = "AUTH_CANCELED"] = 17;
            values[valuesById[18] = "AUTH_CANCEL_FAILED_PROCESSING"] = 18;
            values[valuesById[19] = "AUTH_CANCEL_FAILED"] = 19;
            values[valuesById[20] = "COLLECT_INIT"] = 20;
            values[valuesById[21] = "COLLECT_SUCCESS"] = 21;
            values[valuesById[22] = "COLLECT_FAILED"] = 22;
            values[valuesById[23] = "COLLECT_FAILED_RISK"] = 23;
            values[valuesById[24] = "COLLECT_REJECTED"] = 24;
            values[valuesById[25] = "COLLECT_EXPIRED"] = 25;
            values[valuesById[26] = "COLLECT_CANCELED"] = 26;
            values[valuesById[27] = "COLLECT_CANCELLING"] = 27;
            values[valuesById[28] = "IN_REVIEW"] = 28;
            values[valuesById[29] = "REVERSAL_SUCCESS"] = 29;
            values[valuesById[30] = "REVERSAL_PENDING"] = 30;
            values[valuesById[31] = "REFUND_PENDING"] = 31;
            return values;
        })();

        return PaymentInfo;
    })();

    proto.PhoneNumberToLIDMapping = (function() {

        const PhoneNumberToLIDMapping = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PhoneNumberToLIDMapping.prototype.pnJid = null;
        PhoneNumberToLIDMapping.prototype.lidJid = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PhoneNumberToLIDMapping.prototype, "_pnJid", {
            get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PhoneNumberToLIDMapping.prototype, "_lidJid", {
            get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PhoneNumberToLIDMapping.create = function(properties) {
            return new PhoneNumberToLIDMapping(properties);
        };

        PhoneNumberToLIDMapping.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid"))
                w.uint32(10).string(m.pnJid);
            if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid"))
                w.uint32(18).string(m.lidJid);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PhoneNumberToLIDMapping.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PhoneNumberToLIDMapping();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.pnJid = r.stringVerify();
                        m._pnJid = "pnJid";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.lidJid = r.stringVerify();
                        m._lidJid = "lidJid";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PhoneNumberToLIDMapping.fromObject = function (d, q) {
            if (d instanceof $root.proto.PhoneNumberToLIDMapping)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PhoneNumberToLIDMapping: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PhoneNumberToLIDMapping();
            if (d.pnJid != null) {
                m.pnJid = $String(d.pnJid);
            }
            if (d.lidJid != null) {
                m.lidJid = $String(d.lidJid);
            }
            return m;
        };

        PhoneNumberToLIDMapping.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid")) {
                d.pnJid = m.pnJid;
            }
            if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid")) {
                d.lidJid = m.lidJid;
            }
            return d;
        };

        PhoneNumberToLIDMapping.prototype.toJSON = function() {
            return PhoneNumberToLIDMapping.toObject(this, $protobuf.util.toJSONOptions);
        };

        PhoneNumberToLIDMapping.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PhoneNumberToLIDMapping";
        };

        return PhoneNumberToLIDMapping;
    })();

    proto.PhotoChange = (function() {

        const PhotoChange = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PhotoChange.prototype.oldPhoto = null;
        PhotoChange.prototype.newPhoto = null;
        PhotoChange.prototype.newPhotoId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PhotoChange.prototype, "_oldPhoto", {
            get: $util.oneOfGetter($oneOfFields = ["oldPhoto"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PhotoChange.prototype, "_newPhoto", {
            get: $util.oneOfGetter($oneOfFields = ["newPhoto"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PhotoChange.prototype, "_newPhotoId", {
            get: $util.oneOfGetter($oneOfFields = ["newPhotoId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PhotoChange.create = function(properties) {
            return new PhotoChange(properties);
        };

        PhotoChange.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.oldPhoto != null && $Object.hasOwnProperty.call(m, "oldPhoto"))
                w.uint32(10).bytes(m.oldPhoto);
            if (m.newPhoto != null && $Object.hasOwnProperty.call(m, "newPhoto"))
                w.uint32(18).bytes(m.newPhoto);
            if (m.newPhotoId != null && $Object.hasOwnProperty.call(m, "newPhotoId"))
                w.uint32(24).uint32(m.newPhotoId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PhotoChange.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PhotoChange();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.oldPhoto = r.bytes();
                        m._oldPhoto = "oldPhoto";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.newPhoto = r.bytes();
                        m._newPhoto = "newPhoto";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.newPhotoId = r.uint32();
                        m._newPhotoId = "newPhotoId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PhotoChange.fromObject = function (d, q) {
            if (d instanceof $root.proto.PhotoChange)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PhotoChange: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PhotoChange();
            if (d.oldPhoto != null) {
                if (typeof d.oldPhoto === "string")
                    $util.base64.decode(d.oldPhoto, m.oldPhoto = $util.newBuffer($util.base64.length(d.oldPhoto)), 0);
                else if (d.oldPhoto.length >= 0)
                    m.oldPhoto = d.oldPhoto;
            }
            if (d.newPhoto != null) {
                if (typeof d.newPhoto === "string")
                    $util.base64.decode(d.newPhoto, m.newPhoto = $util.newBuffer($util.base64.length(d.newPhoto)), 0);
                else if (d.newPhoto.length >= 0)
                    m.newPhoto = d.newPhoto;
            }
            if (d.newPhotoId != null) {
                m.newPhotoId = d.newPhotoId >>> 0;
            }
            return m;
        };

        PhotoChange.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.oldPhoto != null && $Object.hasOwnProperty.call(m, "oldPhoto")) {
                d.oldPhoto = o.bytes === $String ? $util.base64.encode(m.oldPhoto, 0, m.oldPhoto.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.oldPhoto) : m.oldPhoto;
            }
            if (m.newPhoto != null && $Object.hasOwnProperty.call(m, "newPhoto")) {
                d.newPhoto = o.bytes === $String ? $util.base64.encode(m.newPhoto, 0, m.newPhoto.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.newPhoto) : m.newPhoto;
            }
            if (m.newPhotoId != null && $Object.hasOwnProperty.call(m, "newPhotoId")) {
                d.newPhotoId = m.newPhotoId;
            }
            return d;
        };

        PhotoChange.prototype.toJSON = function() {
            return PhotoChange.toObject(this, $protobuf.util.toJSONOptions);
        };

        PhotoChange.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PhotoChange";
        };

        return PhotoChange;
    })();

    proto.PinInChat = (function() {

        const PinInChat = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PinInChat.prototype.type = null;
        PinInChat.prototype.key = null;
        PinInChat.prototype.senderTimestampMs = null;
        PinInChat.prototype.serverTimestampMs = null;
        PinInChat.prototype.messageAddOnContextInfo = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PinInChat.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PinInChat.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PinInChat.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PinInChat.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PinInChat.prototype, "_messageAddOnContextInfo", {
            get: $util.oneOfGetter($oneOfFields = ["messageAddOnContextInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PinInChat.create = function(properties) {
            return new PinInChat(properties);
        };

        PinInChat.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                $root.proto.MessageKey.encode(m.key, w.uint32(18).fork(), q + 1).ldelim();
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                w.uint32(24).int64(m.senderTimestampMs);
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs"))
                w.uint32(32).int64(m.serverTimestampMs);
            if (m.messageAddOnContextInfo != null && $Object.hasOwnProperty.call(m, "messageAddOnContextInfo"))
                $root.proto.MessageAddOnContextInfo.encode(m.messageAddOnContextInfo, w.uint32(42).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PinInChat.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PinInChat(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.type = r.int32();
                        m._type = "type";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                        m._key = "key";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.senderTimestampMs = r.int64();
                        m._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.serverTimestampMs = r.int64();
                        m._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.messageAddOnContextInfo = $root.proto.MessageAddOnContextInfo.decode(r, r.uint32(), $undefined, q + 1, m.messageAddOnContextInfo);
                        m._messageAddOnContextInfo = "messageAddOnContextInfo";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PinInChat.fromObject = function (d, q) {
            if (d instanceof $root.proto.PinInChat)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PinInChat: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PinInChat();
            switch (d.type) {
            case "UNKNOWN_TYPE":
            case 0:
                m.type = 0;
                break;
            case "PIN_FOR_ALL":
            case 1:
                m.type = 1;
                break;
            case "UNPIN_FOR_ALL":
            case 2:
                m.type = 2;
                break;
            default:
                if (typeof d.type === "number" && (d.type | 0) === d.type)
                    m.type = d.type;
            }
            if (d.key != null) {
                if (!$util.isObject(d.key))
                    throw $TypeError(".proto.PinInChat.key: object expected");
                m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
            }
            if (d.senderTimestampMs != null) {
                if ($util.Long)
                    m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                else if (typeof d.senderTimestampMs === "string")
                    m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                else if (typeof d.senderTimestampMs === "number")
                    m.senderTimestampMs = d.senderTimestampMs;
                else if (typeof d.senderTimestampMs === "object")
                    m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
            }
            if (d.serverTimestampMs != null) {
                if ($util.Long)
                    m.serverTimestampMs = $util.Long.fromValue(d.serverTimestampMs, false);
                else if (typeof d.serverTimestampMs === "string")
                    m.serverTimestampMs = $parseInt(d.serverTimestampMs, 10);
                else if (typeof d.serverTimestampMs === "number")
                    m.serverTimestampMs = d.serverTimestampMs;
                else if (typeof d.serverTimestampMs === "object")
                    m.serverTimestampMs = new $util.LongBits(d.serverTimestampMs.low >>> 0, d.serverTimestampMs.high >>> 0).toNumber();
            }
            if (d.messageAddOnContextInfo != null) {
                if (!$util.isObject(d.messageAddOnContextInfo))
                    throw $TypeError(".proto.PinInChat.messageAddOnContextInfo: object expected");
                m.messageAddOnContextInfo = $root.proto.MessageAddOnContextInfo.fromObject(d.messageAddOnContextInfo, q + 1);
            }
            return m;
        };

        PinInChat.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                d.type = o.enums === $String ? $root.proto.PinInChat.Type[m.type] === $undefined ? m.type : $root.proto.PinInChat.Type[m.type] : m.type;
            }
            if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
            }
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.senderTimestampMs === "number")
                    d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                else
                    d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
            }
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.serverTimestampMs = typeof m.serverTimestampMs === "number" ? $BigInt(m.serverTimestampMs) : $util.Long.fromBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.serverTimestampMs === "number")
                    d.serverTimestampMs = o.longs === $String ? $String(m.serverTimestampMs) : m.serverTimestampMs;
                else
                    d.serverTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.serverTimestampMs) : o.longs === $Number ? new $util.LongBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0).toNumber() : m.serverTimestampMs;
            }
            if (m.messageAddOnContextInfo != null && $Object.hasOwnProperty.call(m, "messageAddOnContextInfo")) {
                d.messageAddOnContextInfo = $root.proto.MessageAddOnContextInfo.toObject(m.messageAddOnContextInfo, o, q + 1);
            }
            return d;
        };

        PinInChat.prototype.toJSON = function() {
            return PinInChat.toObject(this, $protobuf.util.toJSONOptions);
        };

        PinInChat.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PinInChat";
        };

        PinInChat.Type = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
            values[valuesById[1] = "PIN_FOR_ALL"] = 1;
            values[valuesById[2] = "UNPIN_FOR_ALL"] = 2;
            return values;
        })();

        return PinInChat;
    })();

    proto.Point = (function() {

        const Point = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Point.prototype.xDeprecated = null;
        Point.prototype.yDeprecated = null;
        Point.prototype.x = null;
        Point.prototype.y = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Point.prototype, "_xDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["xDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Point.prototype, "_yDeprecated", {
            get: $util.oneOfGetter($oneOfFields = ["yDeprecated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Point.prototype, "_x", {
            get: $util.oneOfGetter($oneOfFields = ["x"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Point.prototype, "_y", {
            get: $util.oneOfGetter($oneOfFields = ["y"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Point.create = function(properties) {
            return new Point(properties);
        };

        Point.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.xDeprecated != null && $Object.hasOwnProperty.call(m, "xDeprecated"))
                w.uint32(8).int32(m.xDeprecated);
            if (m.yDeprecated != null && $Object.hasOwnProperty.call(m, "yDeprecated"))
                w.uint32(16).int32(m.yDeprecated);
            if (m.x != null && $Object.hasOwnProperty.call(m, "x"))
                w.uint32(25).double(m.x);
            if (m.y != null && $Object.hasOwnProperty.call(m, "y"))
                w.uint32(33).double(m.y);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Point.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Point();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.xDeprecated = r.int32();
                        m._xDeprecated = "xDeprecated";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.yDeprecated = r.int32();
                        m._yDeprecated = "yDeprecated";
                        continue;
                    }
                case 3: {
                        if (u !== 1)
                            break;
                        m.x = r.double();
                        m._x = "x";
                        continue;
                    }
                case 4: {
                        if (u !== 1)
                            break;
                        m.y = r.double();
                        m._y = "y";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Point.fromObject = function (d, q) {
            if (d instanceof $root.proto.Point)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Point: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Point();
            if (d.xDeprecated != null) {
                m.xDeprecated = d.xDeprecated | 0;
            }
            if (d.yDeprecated != null) {
                m.yDeprecated = d.yDeprecated | 0;
            }
            if (d.x != null) {
                m.x = $Number(d.x);
            }
            if (d.y != null) {
                m.y = $Number(d.y);
            }
            return m;
        };

        Point.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.xDeprecated != null && $Object.hasOwnProperty.call(m, "xDeprecated")) {
                d.xDeprecated = m.xDeprecated;
            }
            if (m.yDeprecated != null && $Object.hasOwnProperty.call(m, "yDeprecated")) {
                d.yDeprecated = m.yDeprecated;
            }
            if (m.x != null && $Object.hasOwnProperty.call(m, "x")) {
                d.x = o.json && !$isFinite(m.x) ? $String(m.x) : m.x;
            }
            if (m.y != null && $Object.hasOwnProperty.call(m, "y")) {
                d.y = o.json && !$isFinite(m.y) ? $String(m.y) : m.y;
            }
            return d;
        };

        Point.prototype.toJSON = function() {
            return Point.toObject(this, $protobuf.util.toJSONOptions);
        };

        Point.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Point";
        };

        return Point;
    })();

    proto.PollAdditionalMetadata = (function() {

        const PollAdditionalMetadata = function (p) {
            this.pollNameHashHistory = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PollAdditionalMetadata.prototype.pollInvalidated = null;
        PollAdditionalMetadata.prototype.pollNameHashHistory = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollAdditionalMetadata.prototype, "_pollInvalidated", {
            get: $util.oneOfGetter($oneOfFields = ["pollInvalidated"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollAdditionalMetadata.create = function(properties) {
            return new PollAdditionalMetadata(properties);
        };

        PollAdditionalMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.pollInvalidated != null && $Object.hasOwnProperty.call(m, "pollInvalidated"))
                w.uint32(8).bool(m.pollInvalidated);
            if (m.pollNameHashHistory != null && m.pollNameHashHistory.length) {
                for (var i = 0; i < m.pollNameHashHistory.length; ++i)
                    $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.encode(m.pollNameHashHistory[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PollAdditionalMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PollAdditionalMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.pollInvalidated = r.bool();
                        m._pollInvalidated = "pollInvalidated";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.pollNameHashHistory && m.pollNameHashHistory.length))
                            m.pollNameHashHistory = [];
                        m.pollNameHashHistory.push($root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PollAdditionalMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.PollAdditionalMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PollAdditionalMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PollAdditionalMetadata();
            if (d.pollInvalidated != null) {
                m.pollInvalidated = $Boolean(d.pollInvalidated);
            }
            if (d.pollNameHashHistory) {
                if (!$Array.isArray(d.pollNameHashHistory))
                    throw $TypeError(".proto.PollAdditionalMetadata.pollNameHashHistory: array expected");
                m.pollNameHashHistory = $Array(d.pollNameHashHistory.length);
                for (var i = 0; i < d.pollNameHashHistory.length; ++i) {
                    if (!$util.isObject(d.pollNameHashHistory[i]))
                        throw $TypeError(".proto.PollAdditionalMetadata.pollNameHashHistory: object expected");
                    m.pollNameHashHistory[i] = $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.fromObject(d.pollNameHashHistory[i], q + 1);
                }
            }
            return m;
        };

        PollAdditionalMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.pollNameHashHistory = [];
            }
            if (m.pollInvalidated != null && $Object.hasOwnProperty.call(m, "pollInvalidated")) {
                d.pollInvalidated = m.pollInvalidated;
            }
            if (m.pollNameHashHistory && m.pollNameHashHistory.length) {
                d.pollNameHashHistory = $Array(m.pollNameHashHistory.length);
                for (var j = 0; j < m.pollNameHashHistory.length; ++j) {
                    d.pollNameHashHistory[j] = $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry.toObject(m.pollNameHashHistory[j], o, q + 1);
                }
            }
            return d;
        };

        PollAdditionalMetadata.prototype.toJSON = function() {
            return PollAdditionalMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollAdditionalMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PollAdditionalMetadata";
        };

        PollAdditionalMetadata.PollNameHashHistoryEntry = (function() {

            const PollNameHashHistoryEntry = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PollNameHashHistoryEntry.prototype.editStanzaId = null;
            PollNameHashHistoryEntry.prototype.pollNameHash = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollNameHashHistoryEntry.prototype, "_editStanzaId", {
                get: $util.oneOfGetter($oneOfFields = ["editStanzaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PollNameHashHistoryEntry.prototype, "_pollNameHash", {
                get: $util.oneOfGetter($oneOfFields = ["pollNameHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PollNameHashHistoryEntry.create = function(properties) {
                return new PollNameHashHistoryEntry(properties);
            };

            PollNameHashHistoryEntry.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.editStanzaId != null && $Object.hasOwnProperty.call(m, "editStanzaId"))
                    w.uint32(10).string(m.editStanzaId);
                if (m.pollNameHash != null && $Object.hasOwnProperty.call(m, "pollNameHash"))
                    w.uint32(18).bytes(m.pollNameHash);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PollNameHashHistoryEntry.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.editStanzaId = r.stringVerify();
                            m._editStanzaId = "editStanzaId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.pollNameHash = r.bytes();
                            m._pollNameHash = "pollNameHash";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PollNameHashHistoryEntry.fromObject = function (d, q) {
                if (d instanceof $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.PollAdditionalMetadata.PollNameHashHistoryEntry: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.PollAdditionalMetadata.PollNameHashHistoryEntry();
                if (d.editStanzaId != null) {
                    m.editStanzaId = $String(d.editStanzaId);
                }
                if (d.pollNameHash != null) {
                    if (typeof d.pollNameHash === "string")
                        $util.base64.decode(d.pollNameHash, m.pollNameHash = $util.newBuffer($util.base64.length(d.pollNameHash)), 0);
                    else if (d.pollNameHash.length >= 0)
                        m.pollNameHash = d.pollNameHash;
                }
                return m;
            };

            PollNameHashHistoryEntry.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.editStanzaId != null && $Object.hasOwnProperty.call(m, "editStanzaId")) {
                    d.editStanzaId = m.editStanzaId;
                }
                if (m.pollNameHash != null && $Object.hasOwnProperty.call(m, "pollNameHash")) {
                    d.pollNameHash = o.bytes === $String ? $util.base64.encode(m.pollNameHash, 0, m.pollNameHash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.pollNameHash) : m.pollNameHash;
                }
                return d;
            };

            PollNameHashHistoryEntry.prototype.toJSON = function() {
                return PollNameHashHistoryEntry.toObject(this, $protobuf.util.toJSONOptions);
            };

            PollNameHashHistoryEntry.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.PollAdditionalMetadata.PollNameHashHistoryEntry";
            };

            return PollNameHashHistoryEntry;
        })();

        return PollAdditionalMetadata;
    })();

    proto.PollEncValue = (function() {

        const PollEncValue = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PollEncValue.prototype.encPayload = null;
        PollEncValue.prototype.encIv = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollEncValue.prototype, "_encPayload", {
            get: $util.oneOfGetter($oneOfFields = ["encPayload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollEncValue.prototype, "_encIv", {
            get: $util.oneOfGetter($oneOfFields = ["encIv"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollEncValue.create = function(properties) {
            return new PollEncValue(properties);
        };

        PollEncValue.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload"))
                w.uint32(10).bytes(m.encPayload);
            if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv"))
                w.uint32(18).bytes(m.encIv);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PollEncValue.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PollEncValue();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.encPayload = r.bytes();
                        m._encPayload = "encPayload";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.encIv = r.bytes();
                        m._encIv = "encIv";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PollEncValue.fromObject = function (d, q) {
            if (d instanceof $root.proto.PollEncValue)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PollEncValue: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PollEncValue();
            if (d.encPayload != null) {
                if (typeof d.encPayload === "string")
                    $util.base64.decode(d.encPayload, m.encPayload = $util.newBuffer($util.base64.length(d.encPayload)), 0);
                else if (d.encPayload.length >= 0)
                    m.encPayload = d.encPayload;
            }
            if (d.encIv != null) {
                if (typeof d.encIv === "string")
                    $util.base64.decode(d.encIv, m.encIv = $util.newBuffer($util.base64.length(d.encIv)), 0);
                else if (d.encIv.length >= 0)
                    m.encIv = d.encIv;
            }
            return m;
        };

        PollEncValue.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.encPayload != null && $Object.hasOwnProperty.call(m, "encPayload")) {
                d.encPayload = o.bytes === $String ? $util.base64.encode(m.encPayload, 0, m.encPayload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encPayload) : m.encPayload;
            }
            if (m.encIv != null && $Object.hasOwnProperty.call(m, "encIv")) {
                d.encIv = o.bytes === $String ? $util.base64.encode(m.encIv, 0, m.encIv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encIv) : m.encIv;
            }
            return d;
        };

        PollEncValue.prototype.toJSON = function() {
            return PollEncValue.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollEncValue.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PollEncValue";
        };

        return PollEncValue;
    })();

    proto.PollUpdate = (function() {

        const PollUpdate = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PollUpdate.prototype.pollUpdateMessageKey = null;
        PollUpdate.prototype.vote = null;
        PollUpdate.prototype.senderTimestampMs = null;
        PollUpdate.prototype.serverTimestampMs = null;
        PollUpdate.prototype.unread = null;
        PollUpdate.prototype.metadata = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollUpdate.prototype, "_pollUpdateMessageKey", {
            get: $util.oneOfGetter($oneOfFields = ["pollUpdateMessageKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollUpdate.prototype, "_vote", {
            get: $util.oneOfGetter($oneOfFields = ["vote"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollUpdate.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollUpdate.prototype, "_serverTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["serverTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollUpdate.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PollUpdate.prototype, "_metadata", {
            get: $util.oneOfGetter($oneOfFields = ["metadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PollUpdate.create = function(properties) {
            return new PollUpdate(properties);
        };

        PollUpdate.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.pollUpdateMessageKey != null && $Object.hasOwnProperty.call(m, "pollUpdateMessageKey"))
                $root.proto.MessageKey.encode(m.pollUpdateMessageKey, w.uint32(10).fork(), q + 1).ldelim();
            if (m.vote != null && $Object.hasOwnProperty.call(m, "vote"))
                $root.proto.Message.PollVoteMessage.encode(m.vote, w.uint32(18).fork(), q + 1).ldelim();
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                w.uint32(24).int64(m.senderTimestampMs);
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs"))
                w.uint32(32).int64(m.serverTimestampMs);
            if (m.unread != null && $Object.hasOwnProperty.call(m, "unread"))
                w.uint32(40).bool(m.unread);
            if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata"))
                $root.proto.Message.PollUpdateMessageMetadata.encode(m.metadata, w.uint32(50).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PollUpdate.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PollUpdate();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.pollUpdateMessageKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.pollUpdateMessageKey);
                        m._pollUpdateMessageKey = "pollUpdateMessageKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.vote = $root.proto.Message.PollVoteMessage.decode(r, r.uint32(), $undefined, q + 1, m.vote);
                        m._vote = "vote";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.senderTimestampMs = r.int64();
                        m._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.serverTimestampMs = r.int64();
                        m._serverTimestampMs = "serverTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.unread = r.bool();
                        m._unread = "unread";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.metadata = $root.proto.Message.PollUpdateMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.metadata);
                        m._metadata = "metadata";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PollUpdate.fromObject = function (d, q) {
            if (d instanceof $root.proto.PollUpdate)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PollUpdate: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PollUpdate();
            if (d.pollUpdateMessageKey != null) {
                if (!$util.isObject(d.pollUpdateMessageKey))
                    throw $TypeError(".proto.PollUpdate.pollUpdateMessageKey: object expected");
                m.pollUpdateMessageKey = $root.proto.MessageKey.fromObject(d.pollUpdateMessageKey, q + 1);
            }
            if (d.vote != null) {
                if (!$util.isObject(d.vote))
                    throw $TypeError(".proto.PollUpdate.vote: object expected");
                m.vote = $root.proto.Message.PollVoteMessage.fromObject(d.vote, q + 1);
            }
            if (d.senderTimestampMs != null) {
                if ($util.Long)
                    m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                else if (typeof d.senderTimestampMs === "string")
                    m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                else if (typeof d.senderTimestampMs === "number")
                    m.senderTimestampMs = d.senderTimestampMs;
                else if (typeof d.senderTimestampMs === "object")
                    m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
            }
            if (d.serverTimestampMs != null) {
                if ($util.Long)
                    m.serverTimestampMs = $util.Long.fromValue(d.serverTimestampMs, false);
                else if (typeof d.serverTimestampMs === "string")
                    m.serverTimestampMs = $parseInt(d.serverTimestampMs, 10);
                else if (typeof d.serverTimestampMs === "number")
                    m.serverTimestampMs = d.serverTimestampMs;
                else if (typeof d.serverTimestampMs === "object")
                    m.serverTimestampMs = new $util.LongBits(d.serverTimestampMs.low >>> 0, d.serverTimestampMs.high >>> 0).toNumber();
            }
            if (d.unread != null) {
                m.unread = $Boolean(d.unread);
            }
            if (d.metadata != null) {
                if (!$util.isObject(d.metadata))
                    throw $TypeError(".proto.PollUpdate.metadata: object expected");
                m.metadata = $root.proto.Message.PollUpdateMessageMetadata.fromObject(d.metadata, q + 1);
            }
            return m;
        };

        PollUpdate.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.pollUpdateMessageKey != null && $Object.hasOwnProperty.call(m, "pollUpdateMessageKey")) {
                d.pollUpdateMessageKey = $root.proto.MessageKey.toObject(m.pollUpdateMessageKey, o, q + 1);
            }
            if (m.vote != null && $Object.hasOwnProperty.call(m, "vote")) {
                d.vote = $root.proto.Message.PollVoteMessage.toObject(m.vote, o, q + 1);
            }
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.senderTimestampMs === "number")
                    d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                else
                    d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
            }
            if (m.serverTimestampMs != null && $Object.hasOwnProperty.call(m, "serverTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.serverTimestampMs = typeof m.serverTimestampMs === "number" ? $BigInt(m.serverTimestampMs) : $util.Long.fromBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.serverTimestampMs === "number")
                    d.serverTimestampMs = o.longs === $String ? $String(m.serverTimestampMs) : m.serverTimestampMs;
                else
                    d.serverTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.serverTimestampMs) : o.longs === $Number ? new $util.LongBits(m.serverTimestampMs.low >>> 0, m.serverTimestampMs.high >>> 0).toNumber() : m.serverTimestampMs;
            }
            if (m.unread != null && $Object.hasOwnProperty.call(m, "unread")) {
                d.unread = m.unread;
            }
            if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata")) {
                d.metadata = $root.proto.Message.PollUpdateMessageMetadata.toObject(m.metadata, o, q + 1);
            }
            return d;
        };

        PollUpdate.prototype.toJSON = function() {
            return PollUpdate.toObject(this, $protobuf.util.toJSONOptions);
        };

        PollUpdate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PollUpdate";
        };

        return PollUpdate;
    })();

    proto.PreKeyRecordStructure = (function() {

        const PreKeyRecordStructure = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PreKeyRecordStructure.prototype.id = null;
        PreKeyRecordStructure.prototype.publicKey = null;
        PreKeyRecordStructure.prototype.privateKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeyRecordStructure.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeyRecordStructure.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeyRecordStructure.prototype, "_privateKey", {
            get: $util.oneOfGetter($oneOfFields = ["privateKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PreKeyRecordStructure.create = function(properties) {
            return new PreKeyRecordStructure(properties);
        };

        PreKeyRecordStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(8).uint32(m.id);
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(18).bytes(m.publicKey);
            if (m.privateKey != null && $Object.hasOwnProperty.call(m, "privateKey"))
                w.uint32(26).bytes(m.privateKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PreKeyRecordStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PreKeyRecordStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.id = r.uint32();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        m._publicKey = "publicKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.privateKey = r.bytes();
                        m._privateKey = "privateKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PreKeyRecordStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.PreKeyRecordStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PreKeyRecordStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PreKeyRecordStructure();
            if (d.id != null) {
                m.id = d.id >>> 0;
            }
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.privateKey != null) {
                if (typeof d.privateKey === "string")
                    $util.base64.decode(d.privateKey, m.privateKey = $util.newBuffer($util.base64.length(d.privateKey)), 0);
                else if (d.privateKey.length >= 0)
                    m.privateKey = d.privateKey;
            }
            return m;
        };

        PreKeyRecordStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.privateKey != null && $Object.hasOwnProperty.call(m, "privateKey")) {
                d.privateKey = o.bytes === $String ? $util.base64.encode(m.privateKey, 0, m.privateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.privateKey) : m.privateKey;
            }
            return d;
        };

        PreKeyRecordStructure.prototype.toJSON = function() {
            return PreKeyRecordStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        PreKeyRecordStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PreKeyRecordStructure";
        };

        return PreKeyRecordStructure;
    })();

    proto.PreKeySignalMessage = (function() {

        const PreKeySignalMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PreKeySignalMessage.prototype.registrationId = null;
        PreKeySignalMessage.prototype.preKeyId = null;
        PreKeySignalMessage.prototype.signedPreKeyId = null;
        PreKeySignalMessage.prototype.baseKey = null;
        PreKeySignalMessage.prototype.identityKey = null;
        PreKeySignalMessage.prototype.message = null;
        PreKeySignalMessage.prototype.kyberPreKeyId = null;
        PreKeySignalMessage.prototype.kyberCiphertext = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_registrationId", {
            get: $util.oneOfGetter($oneOfFields = ["registrationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_preKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["preKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_signedPreKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["signedPreKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_baseKey", {
            get: $util.oneOfGetter($oneOfFields = ["baseKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_identityKey", {
            get: $util.oneOfGetter($oneOfFields = ["identityKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_kyberPreKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["kyberPreKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PreKeySignalMessage.prototype, "_kyberCiphertext", {
            get: $util.oneOfGetter($oneOfFields = ["kyberCiphertext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PreKeySignalMessage.create = function(properties) {
            return new PreKeySignalMessage(properties);
        };

        PreKeySignalMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.preKeyId != null && $Object.hasOwnProperty.call(m, "preKeyId"))
                w.uint32(8).uint32(m.preKeyId);
            if (m.baseKey != null && $Object.hasOwnProperty.call(m, "baseKey"))
                w.uint32(18).bytes(m.baseKey);
            if (m.identityKey != null && $Object.hasOwnProperty.call(m, "identityKey"))
                w.uint32(26).bytes(m.identityKey);
            if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                w.uint32(34).bytes(m.message);
            if (m.registrationId != null && $Object.hasOwnProperty.call(m, "registrationId"))
                w.uint32(40).uint32(m.registrationId);
            if (m.signedPreKeyId != null && $Object.hasOwnProperty.call(m, "signedPreKeyId"))
                w.uint32(48).uint32(m.signedPreKeyId);
            if (m.kyberPreKeyId != null && $Object.hasOwnProperty.call(m, "kyberPreKeyId"))
                w.uint32(56).uint32(m.kyberPreKeyId);
            if (m.kyberCiphertext != null && $Object.hasOwnProperty.call(m, "kyberCiphertext"))
                w.uint32(66).bytes(m.kyberCiphertext);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PreKeySignalMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PreKeySignalMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 5: {
                        if (u !== 0)
                            break;
                        m.registrationId = r.uint32();
                        m._registrationId = "registrationId";
                        continue;
                    }
                case 1: {
                        if (u !== 0)
                            break;
                        m.preKeyId = r.uint32();
                        m._preKeyId = "preKeyId";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.signedPreKeyId = r.uint32();
                        m._signedPreKeyId = "signedPreKeyId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.baseKey = r.bytes();
                        m._baseKey = "baseKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.identityKey = r.bytes();
                        m._identityKey = "identityKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.message = r.bytes();
                        m._message = "message";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.kyberPreKeyId = r.uint32();
                        m._kyberPreKeyId = "kyberPreKeyId";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.kyberCiphertext = r.bytes();
                        m._kyberCiphertext = "kyberCiphertext";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PreKeySignalMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.PreKeySignalMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PreKeySignalMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PreKeySignalMessage();
            if (d.registrationId != null) {
                m.registrationId = d.registrationId >>> 0;
            }
            if (d.preKeyId != null) {
                m.preKeyId = d.preKeyId >>> 0;
            }
            if (d.signedPreKeyId != null) {
                m.signedPreKeyId = d.signedPreKeyId >>> 0;
            }
            if (d.baseKey != null) {
                if (typeof d.baseKey === "string")
                    $util.base64.decode(d.baseKey, m.baseKey = $util.newBuffer($util.base64.length(d.baseKey)), 0);
                else if (d.baseKey.length >= 0)
                    m.baseKey = d.baseKey;
            }
            if (d.identityKey != null) {
                if (typeof d.identityKey === "string")
                    $util.base64.decode(d.identityKey, m.identityKey = $util.newBuffer($util.base64.length(d.identityKey)), 0);
                else if (d.identityKey.length >= 0)
                    m.identityKey = d.identityKey;
            }
            if (d.message != null) {
                if (typeof d.message === "string")
                    $util.base64.decode(d.message, m.message = $util.newBuffer($util.base64.length(d.message)), 0);
                else if (d.message.length >= 0)
                    m.message = d.message;
            }
            if (d.kyberPreKeyId != null) {
                m.kyberPreKeyId = d.kyberPreKeyId >>> 0;
            }
            if (d.kyberCiphertext != null) {
                if (typeof d.kyberCiphertext === "string")
                    $util.base64.decode(d.kyberCiphertext, m.kyberCiphertext = $util.newBuffer($util.base64.length(d.kyberCiphertext)), 0);
                else if (d.kyberCiphertext.length >= 0)
                    m.kyberCiphertext = d.kyberCiphertext;
            }
            return m;
        };

        PreKeySignalMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.preKeyId != null && $Object.hasOwnProperty.call(m, "preKeyId")) {
                d.preKeyId = m.preKeyId;
            }
            if (m.baseKey != null && $Object.hasOwnProperty.call(m, "baseKey")) {
                d.baseKey = o.bytes === $String ? $util.base64.encode(m.baseKey, 0, m.baseKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.baseKey) : m.baseKey;
            }
            if (m.identityKey != null && $Object.hasOwnProperty.call(m, "identityKey")) {
                d.identityKey = o.bytes === $String ? $util.base64.encode(m.identityKey, 0, m.identityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.identityKey) : m.identityKey;
            }
            if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                d.message = o.bytes === $String ? $util.base64.encode(m.message, 0, m.message.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.message) : m.message;
            }
            if (m.registrationId != null && $Object.hasOwnProperty.call(m, "registrationId")) {
                d.registrationId = m.registrationId;
            }
            if (m.signedPreKeyId != null && $Object.hasOwnProperty.call(m, "signedPreKeyId")) {
                d.signedPreKeyId = m.signedPreKeyId;
            }
            if (m.kyberPreKeyId != null && $Object.hasOwnProperty.call(m, "kyberPreKeyId")) {
                d.kyberPreKeyId = m.kyberPreKeyId;
            }
            if (m.kyberCiphertext != null && $Object.hasOwnProperty.call(m, "kyberCiphertext")) {
                d.kyberCiphertext = o.bytes === $String ? $util.base64.encode(m.kyberCiphertext, 0, m.kyberCiphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.kyberCiphertext) : m.kyberCiphertext;
            }
            return d;
        };

        PreKeySignalMessage.prototype.toJSON = function() {
            return PreKeySignalMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        PreKeySignalMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PreKeySignalMessage";
        };

        return PreKeySignalMessage;
    })();

    proto.PremiumMessageInfo = (function() {

        const PremiumMessageInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PremiumMessageInfo.prototype.serverCampaignId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PremiumMessageInfo.prototype, "_serverCampaignId", {
            get: $util.oneOfGetter($oneOfFields = ["serverCampaignId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PremiumMessageInfo.create = function(properties) {
            return new PremiumMessageInfo(properties);
        };

        PremiumMessageInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.serverCampaignId != null && $Object.hasOwnProperty.call(m, "serverCampaignId"))
                w.uint32(10).string(m.serverCampaignId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PremiumMessageInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PremiumMessageInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.serverCampaignId = r.stringVerify();
                        m._serverCampaignId = "serverCampaignId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PremiumMessageInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.PremiumMessageInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PremiumMessageInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PremiumMessageInfo();
            if (d.serverCampaignId != null) {
                m.serverCampaignId = $String(d.serverCampaignId);
            }
            return m;
        };

        PremiumMessageInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.serverCampaignId != null && $Object.hasOwnProperty.call(m, "serverCampaignId")) {
                d.serverCampaignId = m.serverCampaignId;
            }
            return d;
        };

        PremiumMessageInfo.prototype.toJSON = function() {
            return PremiumMessageInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        PremiumMessageInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PremiumMessageInfo";
        };

        return PremiumMessageInfo;
    })();

    proto.PrimaryEphemeralIdentity = (function() {

        const PrimaryEphemeralIdentity = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        PrimaryEphemeralIdentity.prototype.publicKey = null;
        PrimaryEphemeralIdentity.prototype.nonce = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PrimaryEphemeralIdentity.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(PrimaryEphemeralIdentity.prototype, "_nonce", {
            get: $util.oneOfGetter($oneOfFields = ["nonce"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        PrimaryEphemeralIdentity.create = function(properties) {
            return new PrimaryEphemeralIdentity(properties);
        };

        PrimaryEphemeralIdentity.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(10).bytes(m.publicKey);
            if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce"))
                w.uint32(18).bytes(m.nonce);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        PrimaryEphemeralIdentity.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.PrimaryEphemeralIdentity();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        m._publicKey = "publicKey";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.nonce = r.bytes();
                        m._nonce = "nonce";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        PrimaryEphemeralIdentity.fromObject = function (d, q) {
            if (d instanceof $root.proto.PrimaryEphemeralIdentity)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.PrimaryEphemeralIdentity: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.PrimaryEphemeralIdentity();
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.nonce != null) {
                if (typeof d.nonce === "string")
                    $util.base64.decode(d.nonce, m.nonce = $util.newBuffer($util.base64.length(d.nonce)), 0);
                else if (d.nonce.length >= 0)
                    m.nonce = d.nonce;
            }
            return m;
        };

        PrimaryEphemeralIdentity.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.nonce != null && $Object.hasOwnProperty.call(m, "nonce")) {
                d.nonce = o.bytes === $String ? $util.base64.encode(m.nonce, 0, m.nonce.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.nonce) : m.nonce;
            }
            return d;
        };

        PrimaryEphemeralIdentity.prototype.toJSON = function() {
            return PrimaryEphemeralIdentity.toObject(this, $protobuf.util.toJSONOptions);
        };

        PrimaryEphemeralIdentity.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.PrimaryEphemeralIdentity";
        };

        return PrimaryEphemeralIdentity;
    })();

    proto.PrivacySystemMessage = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[1] = "E2EE_MSG"] = 1;
        values[valuesById[2] = "NE2EE_SELF"] = 2;
        values[valuesById[3] = "NE2EE_OTHER"] = 3;
        return values;
    })();

    proto.ProcessedVideo = (function() {

        const ProcessedVideo = function (p) {
            this.capabilities = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ProcessedVideo.prototype.directPath = null;
        ProcessedVideo.prototype.fileSha256 = null;
        ProcessedVideo.prototype.height = null;
        ProcessedVideo.prototype.width = null;
        ProcessedVideo.prototype.fileLength = null;
        ProcessedVideo.prototype.bitrate = null;
        ProcessedVideo.prototype.quality = null;
        ProcessedVideo.prototype.capabilities = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_bitrate", {
            get: $util.oneOfGetter($oneOfFields = ["bitrate"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProcessedVideo.prototype, "_quality", {
            get: $util.oneOfGetter($oneOfFields = ["quality"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ProcessedVideo.create = function(properties) {
            return new ProcessedVideo(properties);
        };

        ProcessedVideo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(10).string(m.directPath);
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                w.uint32(18).bytes(m.fileSha256);
            if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                w.uint32(24).uint32(m.height);
            if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                w.uint32(32).uint32(m.width);
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(40).uint64(m.fileLength);
            if (m.bitrate != null && $Object.hasOwnProperty.call(m, "bitrate"))
                w.uint32(48).uint32(m.bitrate);
            if (m.quality != null && $Object.hasOwnProperty.call(m, "quality"))
                w.uint32(56).int32(m.quality);
            if (m.capabilities != null && m.capabilities.length) {
                for (var i = 0; i < m.capabilities.length; ++i)
                    w.uint32(66).string(m.capabilities[i]);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ProcessedVideo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ProcessedVideo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.directPath = r.stringVerify();
                        m._directPath = "directPath";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.fileSha256 = r.bytes();
                        m._fileSha256 = "fileSha256";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.height = r.uint32();
                        m._height = "height";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.width = r.uint32();
                        m._width = "width";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.fileLength = r.uint64();
                        m._fileLength = "fileLength";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.bitrate = r.uint32();
                        m._bitrate = "bitrate";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.quality = r.int32();
                        m._quality = "quality";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        if (!(m.capabilities && m.capabilities.length))
                            m.capabilities = [];
                        m.capabilities.push(r.stringVerify());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ProcessedVideo.fromObject = function (d, q) {
            if (d instanceof $root.proto.ProcessedVideo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ProcessedVideo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ProcessedVideo();
            if (d.directPath != null) {
                m.directPath = $String(d.directPath);
            }
            if (d.fileSha256 != null) {
                if (typeof d.fileSha256 === "string")
                    $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                else if (d.fileSha256.length >= 0)
                    m.fileSha256 = d.fileSha256;
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    m.fileLength = $util.Long.fromValue(d.fileLength, true);
                else if (typeof d.fileLength === "string")
                    m.fileLength = $parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.bitrate != null) {
                m.bitrate = d.bitrate >>> 0;
            }
            switch (d.quality) {
            case "UNDEFINED":
            case 0:
                m.quality = 0;
                break;
            case "LOW":
            case 1:
                m.quality = 1;
                break;
            case "MID":
            case 2:
                m.quality = 2;
                break;
            case "HIGH":
            case 3:
                m.quality = 3;
                break;
            default:
                if (typeof d.quality === "number" && (d.quality | 0) === d.quality)
                    m.quality = d.quality;
            }
            if (d.capabilities) {
                if (!$Array.isArray(d.capabilities))
                    throw $TypeError(".proto.ProcessedVideo.capabilities: array expected");
                m.capabilities = $Array(d.capabilities.length);
                for (var i = 0; i < d.capabilities.length; ++i) {
                    m.capabilities[i] = $String(d.capabilities[i]);
                }
            }
            return m;
        };

        ProcessedVideo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.capabilities = [];
            }
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                d.directPath = m.directPath;
            }
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
            }
            if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                d.height = m.height;
            }
            if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                d.width = m.width;
            }
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                else if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
            }
            if (m.bitrate != null && $Object.hasOwnProperty.call(m, "bitrate")) {
                d.bitrate = m.bitrate;
            }
            if (m.quality != null && $Object.hasOwnProperty.call(m, "quality")) {
                d.quality = o.enums === $String ? $root.proto.ProcessedVideo.VideoQuality[m.quality] === $undefined ? m.quality : $root.proto.ProcessedVideo.VideoQuality[m.quality] : m.quality;
            }
            if (m.capabilities && m.capabilities.length) {
                d.capabilities = $Array(m.capabilities.length);
                for (var j = 0; j < m.capabilities.length; ++j) {
                    d.capabilities[j] = m.capabilities[j];
                }
            }
            return d;
        };

        ProcessedVideo.prototype.toJSON = function() {
            return ProcessedVideo.toObject(this, $protobuf.util.toJSONOptions);
        };

        ProcessedVideo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProcessedVideo";
        };

        ProcessedVideo.VideoQuality = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNDEFINED"] = 0;
            values[valuesById[1] = "LOW"] = 1;
            values[valuesById[2] = "MID"] = 2;
            values[valuesById[3] = "HIGH"] = 3;
            return values;
        })();

        return ProcessedVideo;
    })();

    proto.ProloguePayload = (function() {

        const ProloguePayload = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ProloguePayload.prototype.companionEphemeralIdentity = null;
        ProloguePayload.prototype.commitment = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProloguePayload.prototype, "_companionEphemeralIdentity", {
            get: $util.oneOfGetter($oneOfFields = ["companionEphemeralIdentity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ProloguePayload.prototype, "_commitment", {
            get: $util.oneOfGetter($oneOfFields = ["commitment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ProloguePayload.create = function(properties) {
            return new ProloguePayload(properties);
        };

        ProloguePayload.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.companionEphemeralIdentity != null && $Object.hasOwnProperty.call(m, "companionEphemeralIdentity"))
                w.uint32(10).bytes(m.companionEphemeralIdentity);
            if (m.commitment != null && $Object.hasOwnProperty.call(m, "commitment"))
                $root.proto.CompanionCommitment.encode(m.commitment, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ProloguePayload.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ProloguePayload();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.companionEphemeralIdentity = r.bytes();
                        m._companionEphemeralIdentity = "companionEphemeralIdentity";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.commitment = $root.proto.CompanionCommitment.decode(r, r.uint32(), $undefined, q + 1, m.commitment);
                        m._commitment = "commitment";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ProloguePayload.fromObject = function (d, q) {
            if (d instanceof $root.proto.ProloguePayload)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ProloguePayload: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ProloguePayload();
            if (d.companionEphemeralIdentity != null) {
                if (typeof d.companionEphemeralIdentity === "string")
                    $util.base64.decode(d.companionEphemeralIdentity, m.companionEphemeralIdentity = $util.newBuffer($util.base64.length(d.companionEphemeralIdentity)), 0);
                else if (d.companionEphemeralIdentity.length >= 0)
                    m.companionEphemeralIdentity = d.companionEphemeralIdentity;
            }
            if (d.commitment != null) {
                if (!$util.isObject(d.commitment))
                    throw $TypeError(".proto.ProloguePayload.commitment: object expected");
                m.commitment = $root.proto.CompanionCommitment.fromObject(d.commitment, q + 1);
            }
            return m;
        };

        ProloguePayload.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.companionEphemeralIdentity != null && $Object.hasOwnProperty.call(m, "companionEphemeralIdentity")) {
                d.companionEphemeralIdentity = o.bytes === $String ? $util.base64.encode(m.companionEphemeralIdentity, 0, m.companionEphemeralIdentity.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.companionEphemeralIdentity) : m.companionEphemeralIdentity;
            }
            if (m.commitment != null && $Object.hasOwnProperty.call(m, "commitment")) {
                d.commitment = $root.proto.CompanionCommitment.toObject(m.commitment, o, q + 1);
            }
            return d;
        };

        ProloguePayload.prototype.toJSON = function() {
            return ProloguePayload.toObject(this, $protobuf.util.toJSONOptions);
        };

        ProloguePayload.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ProloguePayload";
        };

        return ProloguePayload;
    })();

    proto.Pushname = (function() {

        const Pushname = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Pushname.prototype.id = null;
        Pushname.prototype.pushname = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Pushname.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Pushname.prototype, "_pushname", {
            get: $util.oneOfGetter($oneOfFields = ["pushname"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Pushname.create = function(properties) {
            return new Pushname(properties);
        };

        Pushname.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(10).string(m.id);
            if (m.pushname != null && $Object.hasOwnProperty.call(m, "pushname"))
                w.uint32(18).string(m.pushname);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Pushname.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Pushname();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.id = r.stringVerify();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.pushname = r.stringVerify();
                        m._pushname = "pushname";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Pushname.fromObject = function (d, q) {
            if (d instanceof $root.proto.Pushname)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Pushname: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Pushname();
            if (d.id != null) {
                m.id = $String(d.id);
            }
            if (d.pushname != null) {
                m.pushname = $String(d.pushname);
            }
            return m;
        };

        Pushname.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.pushname != null && $Object.hasOwnProperty.call(m, "pushname")) {
                d.pushname = m.pushname;
            }
            return d;
        };

        Pushname.prototype.toJSON = function() {
            return Pushname.toObject(this, $protobuf.util.toJSONOptions);
        };

        Pushname.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Pushname";
        };

        return Pushname;
    })();

    proto.QP = (function() {

        const QP = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        QP.create = function(properties) {
            return new QP(properties);
        };

        QP.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        QP.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.QP();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                r.skipType(t & 7, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        QP.fromObject = function (d, q) {
            if (d instanceof $root.proto.QP)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.QP: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            return new $root.proto.QP();
        };

        QP.toObject = function () {
            return {};
        };

        QP.prototype.toJSON = function() {
            return QP.toObject(this, $protobuf.util.toJSONOptions);
        };

        QP.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.QP";
        };

        QP.ClauseType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "AND"] = 1;
            values[valuesById[2] = "OR"] = 2;
            values[valuesById[3] = "NOR"] = 3;
            return values;
        })();

        QP.Filter = (function() {

            const Filter = function (p) {
                this.parameters = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Filter.prototype.filterName = "";
            Filter.prototype.parameters = $util.emptyArray;
            Filter.prototype.filterResult = null;
            Filter.prototype.clientNotSupportedConfig = 1;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Filter.prototype, "_filterResult", {
                get: $util.oneOfGetter($oneOfFields = ["filterResult"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Filter.create = function(properties) {
                return new Filter(properties);
            };

            Filter.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(10).string(m.filterName);
                if (m.parameters != null && m.parameters.length) {
                    for (var i = 0; i < m.parameters.length; ++i)
                        $root.proto.QP.FilterParameters.encode(m.parameters[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.filterResult != null && $Object.hasOwnProperty.call(m, "filterResult"))
                    w.uint32(24).int32(m.filterResult);
                w.uint32(32).int32(m.clientNotSupportedConfig);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Filter.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.QP.Filter(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.filterName = r.stringVerify();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.parameters && m.parameters.length))
                                m.parameters = [];
                            m.parameters.push($root.proto.QP.FilterParameters.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.filterResult = r.int32();
                            m._filterResult = "filterResult";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.clientNotSupportedConfig = r.int32();
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "filterName"))
                    throw $util.ProtocolError("missing required 'filterName'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "clientNotSupportedConfig"))
                    throw $util.ProtocolError("missing required 'clientNotSupportedConfig'", { instance: m });
                return m;
            };

            Filter.fromObject = function (d, q) {
                if (d instanceof $root.proto.QP.Filter)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.QP.Filter: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.QP.Filter();
                if (d.filterName != null) {
                    m.filterName = $String(d.filterName);
                }
                if (d.parameters) {
                    if (!$Array.isArray(d.parameters))
                        throw $TypeError(".proto.QP.Filter.parameters: array expected");
                    m.parameters = $Array(d.parameters.length);
                    for (var i = 0; i < d.parameters.length; ++i) {
                        if (!$util.isObject(d.parameters[i]))
                            throw $TypeError(".proto.QP.Filter.parameters: object expected");
                        m.parameters[i] = $root.proto.QP.FilterParameters.fromObject(d.parameters[i], q + 1);
                    }
                }
                switch (d.filterResult) {
                case "TRUE":
                case 1:
                    m.filterResult = 1;
                    break;
                case "FALSE":
                case 2:
                    m.filterResult = 2;
                    break;
                case "UNKNOWN":
                case 3:
                    m.filterResult = 3;
                    break;
                default:
                    if (typeof d.filterResult === "number" && (d.filterResult | 0) === d.filterResult)
                        m.filterResult = d.filterResult;
                }
                switch (d.clientNotSupportedConfig) {
                case "PASS_BY_DEFAULT":
                case 1:
                    m.clientNotSupportedConfig = 1;
                    break;
                case "FAIL_BY_DEFAULT":
                case 2:
                    m.clientNotSupportedConfig = 2;
                    break;
                default:
                    if (typeof d.clientNotSupportedConfig === "number" && (d.clientNotSupportedConfig | 0) === d.clientNotSupportedConfig)
                        m.clientNotSupportedConfig = d.clientNotSupportedConfig;
                }
                return m;
            };

            Filter.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.parameters = [];
                }
                if (o.defaults) {
                    d.filterName = "";
                    d.clientNotSupportedConfig = o.enums === $String ? "PASS_BY_DEFAULT" : 1;
                }
                if (m.filterName != null && $Object.hasOwnProperty.call(m, "filterName")) {
                    d.filterName = m.filterName;
                }
                if (m.parameters && m.parameters.length) {
                    d.parameters = $Array(m.parameters.length);
                    for (var j = 0; j < m.parameters.length; ++j) {
                        d.parameters[j] = $root.proto.QP.FilterParameters.toObject(m.parameters[j], o, q + 1);
                    }
                }
                if (m.filterResult != null && $Object.hasOwnProperty.call(m, "filterResult")) {
                    d.filterResult = o.enums === $String ? $root.proto.QP.FilterResult[m.filterResult] === $undefined ? m.filterResult : $root.proto.QP.FilterResult[m.filterResult] : m.filterResult;
                }
                if (m.clientNotSupportedConfig != null && $Object.hasOwnProperty.call(m, "clientNotSupportedConfig")) {
                    d.clientNotSupportedConfig = o.enums === $String ? $root.proto.QP.FilterClientNotSupportedConfig[m.clientNotSupportedConfig] === $undefined ? m.clientNotSupportedConfig : $root.proto.QP.FilterClientNotSupportedConfig[m.clientNotSupportedConfig] : m.clientNotSupportedConfig;
                }
                return d;
            };

            Filter.prototype.toJSON = function() {
                return Filter.toObject(this, $protobuf.util.toJSONOptions);
            };

            Filter.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.QP.Filter";
            };

            return Filter;
        })();

        QP.FilterClause = (function() {

            const FilterClause = function (p) {
                this.clauses = [];
                this.filters = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FilterClause.prototype.clauseType = 1;
            FilterClause.prototype.clauses = $util.emptyArray;
            FilterClause.prototype.filters = $util.emptyArray;

            FilterClause.create = function(properties) {
                return new FilterClause(properties);
            };

            FilterClause.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(8).int32(m.clauseType);
                if (m.clauses != null && m.clauses.length) {
                    for (var i = 0; i < m.clauses.length; ++i)
                        $root.proto.QP.FilterClause.encode(m.clauses[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.filters != null && m.filters.length) {
                    for (var i = 0; i < m.filters.length; ++i)
                        $root.proto.QP.Filter.encode(m.filters[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FilterClause.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.QP.FilterClause(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.clauseType = r.int32();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.clauses && m.clauses.length))
                                m.clauses = [];
                            m.clauses.push($root.proto.QP.FilterClause.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.filters && m.filters.length))
                                m.filters = [];
                            m.filters.push($root.proto.QP.Filter.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "clauseType"))
                    throw $util.ProtocolError("missing required 'clauseType'", { instance: m });
                return m;
            };

            FilterClause.fromObject = function (d, q) {
                if (d instanceof $root.proto.QP.FilterClause)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.QP.FilterClause: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.QP.FilterClause();
                switch (d.clauseType) {
                case "AND":
                case 1:
                    m.clauseType = 1;
                    break;
                case "OR":
                case 2:
                    m.clauseType = 2;
                    break;
                case "NOR":
                case 3:
                    m.clauseType = 3;
                    break;
                default:
                    if (typeof d.clauseType === "number" && (d.clauseType | 0) === d.clauseType)
                        m.clauseType = d.clauseType;
                }
                if (d.clauses) {
                    if (!$Array.isArray(d.clauses))
                        throw $TypeError(".proto.QP.FilterClause.clauses: array expected");
                    m.clauses = $Array(d.clauses.length);
                    for (var i = 0; i < d.clauses.length; ++i) {
                        if (!$util.isObject(d.clauses[i]))
                            throw $TypeError(".proto.QP.FilterClause.clauses: object expected");
                        m.clauses[i] = $root.proto.QP.FilterClause.fromObject(d.clauses[i], q + 1);
                    }
                }
                if (d.filters) {
                    if (!$Array.isArray(d.filters))
                        throw $TypeError(".proto.QP.FilterClause.filters: array expected");
                    m.filters = $Array(d.filters.length);
                    for (var i = 0; i < d.filters.length; ++i) {
                        if (!$util.isObject(d.filters[i]))
                            throw $TypeError(".proto.QP.FilterClause.filters: object expected");
                        m.filters[i] = $root.proto.QP.Filter.fromObject(d.filters[i], q + 1);
                    }
                }
                return m;
            };

            FilterClause.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.clauses = [];
                    d.filters = [];
                }
                if (o.defaults) {
                    d.clauseType = o.enums === $String ? "AND" : 1;
                }
                if (m.clauseType != null && $Object.hasOwnProperty.call(m, "clauseType")) {
                    d.clauseType = o.enums === $String ? $root.proto.QP.ClauseType[m.clauseType] === $undefined ? m.clauseType : $root.proto.QP.ClauseType[m.clauseType] : m.clauseType;
                }
                if (m.clauses && m.clauses.length) {
                    d.clauses = $Array(m.clauses.length);
                    for (var j = 0; j < m.clauses.length; ++j) {
                        d.clauses[j] = $root.proto.QP.FilterClause.toObject(m.clauses[j], o, q + 1);
                    }
                }
                if (m.filters && m.filters.length) {
                    d.filters = $Array(m.filters.length);
                    for (var j = 0; j < m.filters.length; ++j) {
                        d.filters[j] = $root.proto.QP.Filter.toObject(m.filters[j], o, q + 1);
                    }
                }
                return d;
            };

            FilterClause.prototype.toJSON = function() {
                return FilterClause.toObject(this, $protobuf.util.toJSONOptions);
            };

            FilterClause.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.QP.FilterClause";
            };

            return FilterClause;
        })();

        QP.FilterClientNotSupportedConfig = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "PASS_BY_DEFAULT"] = 1;
            values[valuesById[2] = "FAIL_BY_DEFAULT"] = 2;
            return values;
        })();

        QP.FilterParameters = (function() {

            const FilterParameters = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FilterParameters.prototype.key = null;
            FilterParameters.prototype.value = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FilterParameters.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(FilterParameters.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            FilterParameters.create = function(properties) {
                return new FilterParameters(properties);
            };

            FilterParameters.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    w.uint32(10).string(m.key);
                if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                    w.uint32(18).string(m.value);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FilterParameters.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.QP.FilterParameters();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = r.stringVerify();
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.value = r.stringVerify();
                            m._value = "value";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            FilterParameters.fromObject = function (d, q) {
                if (d instanceof $root.proto.QP.FilterParameters)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.QP.FilterParameters: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.QP.FilterParameters();
                if (d.key != null) {
                    m.key = $String(d.key);
                }
                if (d.value != null) {
                    m.value = $String(d.value);
                }
                return m;
            };

            FilterParameters.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = m.key;
                }
                if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                    d.value = m.value;
                }
                return d;
            };

            FilterParameters.prototype.toJSON = function() {
                return FilterParameters.toObject(this, $protobuf.util.toJSONOptions);
            };

            FilterParameters.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.QP.FilterParameters";
            };

            return FilterParameters;
        })();

        QP.FilterResult = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "TRUE"] = 1;
            values[valuesById[2] = "FALSE"] = 2;
            values[valuesById[3] = "UNKNOWN"] = 3;
            return values;
        })();

        return QP;
    })();

    proto.QuarantinedMessage = (function() {

        const QuarantinedMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        QuarantinedMessage.prototype.originalData = null;
        QuarantinedMessage.prototype.extractedText = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(QuarantinedMessage.prototype, "_originalData", {
            get: $util.oneOfGetter($oneOfFields = ["originalData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(QuarantinedMessage.prototype, "_extractedText", {
            get: $util.oneOfGetter($oneOfFields = ["extractedText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        QuarantinedMessage.create = function(properties) {
            return new QuarantinedMessage(properties);
        };

        QuarantinedMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.originalData != null && $Object.hasOwnProperty.call(m, "originalData"))
                w.uint32(10).bytes(m.originalData);
            if (m.extractedText != null && $Object.hasOwnProperty.call(m, "extractedText"))
                w.uint32(18).string(m.extractedText);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        QuarantinedMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.QuarantinedMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.originalData = r.bytes();
                        m._originalData = "originalData";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.extractedText = r.stringVerify();
                        m._extractedText = "extractedText";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        QuarantinedMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.QuarantinedMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.QuarantinedMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.QuarantinedMessage();
            if (d.originalData != null) {
                if (typeof d.originalData === "string")
                    $util.base64.decode(d.originalData, m.originalData = $util.newBuffer($util.base64.length(d.originalData)), 0);
                else if (d.originalData.length >= 0)
                    m.originalData = d.originalData;
            }
            if (d.extractedText != null) {
                m.extractedText = $String(d.extractedText);
            }
            return m;
        };

        QuarantinedMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.originalData != null && $Object.hasOwnProperty.call(m, "originalData")) {
                d.originalData = o.bytes === $String ? $util.base64.encode(m.originalData, 0, m.originalData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.originalData) : m.originalData;
            }
            if (m.extractedText != null && $Object.hasOwnProperty.call(m, "extractedText")) {
                d.extractedText = m.extractedText;
            }
            return d;
        };

        QuarantinedMessage.prototype.toJSON = function() {
            return QuarantinedMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        QuarantinedMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.QuarantinedMessage";
        };

        return QuarantinedMessage;
    })();

    proto.Reaction = (function() {

        const Reaction = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Reaction.prototype.key = null;
        Reaction.prototype.text = null;
        Reaction.prototype.groupingKey = null;
        Reaction.prototype.senderTimestampMs = null;
        Reaction.prototype.unread = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reaction.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reaction.prototype, "_text", {
            get: $util.oneOfGetter($oneOfFields = ["text"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reaction.prototype, "_groupingKey", {
            get: $util.oneOfGetter($oneOfFields = ["groupingKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reaction.prototype, "_senderTimestampMs", {
            get: $util.oneOfGetter($oneOfFields = ["senderTimestampMs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reaction.prototype, "_unread", {
            get: $util.oneOfGetter($oneOfFields = ["unread"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Reaction.create = function(properties) {
            return new Reaction(properties);
        };

        Reaction.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
            if (m.text != null && $Object.hasOwnProperty.call(m, "text"))
                w.uint32(18).string(m.text);
            if (m.groupingKey != null && $Object.hasOwnProperty.call(m, "groupingKey"))
                w.uint32(26).string(m.groupingKey);
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs"))
                w.uint32(32).int64(m.senderTimestampMs);
            if (m.unread != null && $Object.hasOwnProperty.call(m, "unread"))
                w.uint32(40).bool(m.unread);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Reaction.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Reaction();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                        m._key = "key";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.text = r.stringVerify();
                        m._text = "text";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.groupingKey = r.stringVerify();
                        m._groupingKey = "groupingKey";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.senderTimestampMs = r.int64();
                        m._senderTimestampMs = "senderTimestampMs";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.unread = r.bool();
                        m._unread = "unread";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Reaction.fromObject = function (d, q) {
            if (d instanceof $root.proto.Reaction)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Reaction: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Reaction();
            if (d.key != null) {
                if (!$util.isObject(d.key))
                    throw $TypeError(".proto.Reaction.key: object expected");
                m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
            }
            if (d.text != null) {
                m.text = $String(d.text);
            }
            if (d.groupingKey != null) {
                m.groupingKey = $String(d.groupingKey);
            }
            if (d.senderTimestampMs != null) {
                if ($util.Long)
                    m.senderTimestampMs = $util.Long.fromValue(d.senderTimestampMs, false);
                else if (typeof d.senderTimestampMs === "string")
                    m.senderTimestampMs = $parseInt(d.senderTimestampMs, 10);
                else if (typeof d.senderTimestampMs === "number")
                    m.senderTimestampMs = d.senderTimestampMs;
                else if (typeof d.senderTimestampMs === "object")
                    m.senderTimestampMs = new $util.LongBits(d.senderTimestampMs.low >>> 0, d.senderTimestampMs.high >>> 0).toNumber();
            }
            if (d.unread != null) {
                m.unread = $Boolean(d.unread);
            }
            return m;
        };

        Reaction.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
            }
            if (m.text != null && $Object.hasOwnProperty.call(m, "text")) {
                d.text = m.text;
            }
            if (m.groupingKey != null && $Object.hasOwnProperty.call(m, "groupingKey")) {
                d.groupingKey = m.groupingKey;
            }
            if (m.senderTimestampMs != null && $Object.hasOwnProperty.call(m, "senderTimestampMs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.senderTimestampMs = typeof m.senderTimestampMs === "number" ? $BigInt(m.senderTimestampMs) : $util.Long.fromBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0, false).toBigInt();
                else if (typeof m.senderTimestampMs === "number")
                    d.senderTimestampMs = o.longs === $String ? $String(m.senderTimestampMs) : m.senderTimestampMs;
                else
                    d.senderTimestampMs = o.longs === $String ? $util.Long.prototype.toString.call(m.senderTimestampMs) : o.longs === $Number ? new $util.LongBits(m.senderTimestampMs.low >>> 0, m.senderTimestampMs.high >>> 0).toNumber() : m.senderTimestampMs;
            }
            if (m.unread != null && $Object.hasOwnProperty.call(m, "unread")) {
                d.unread = m.unread;
            }
            return d;
        };

        Reaction.prototype.toJSON = function() {
            return Reaction.toObject(this, $protobuf.util.toJSONOptions);
        };

        Reaction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Reaction";
        };

        return Reaction;
    })();

    proto.RecentEmojiWeight = (function() {

        const RecentEmojiWeight = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RecentEmojiWeight.prototype.emoji = null;
        RecentEmojiWeight.prototype.weight = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RecentEmojiWeight.prototype, "_emoji", {
            get: $util.oneOfGetter($oneOfFields = ["emoji"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RecentEmojiWeight.prototype, "_weight", {
            get: $util.oneOfGetter($oneOfFields = ["weight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RecentEmojiWeight.create = function(properties) {
            return new RecentEmojiWeight(properties);
        };

        RecentEmojiWeight.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.emoji != null && $Object.hasOwnProperty.call(m, "emoji"))
                w.uint32(10).string(m.emoji);
            if (m.weight != null && $Object.hasOwnProperty.call(m, "weight"))
                w.uint32(21).float(m.weight);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RecentEmojiWeight.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RecentEmojiWeight();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.emoji = r.stringVerify();
                        m._emoji = "emoji";
                        continue;
                    }
                case 2: {
                        if (u !== 5)
                            break;
                        m.weight = r.float();
                        m._weight = "weight";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        RecentEmojiWeight.fromObject = function (d, q) {
            if (d instanceof $root.proto.RecentEmojiWeight)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RecentEmojiWeight: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RecentEmojiWeight();
            if (d.emoji != null) {
                m.emoji = $String(d.emoji);
            }
            if (d.weight != null) {
                m.weight = $Number(d.weight);
            }
            return m;
        };

        RecentEmojiWeight.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.emoji != null && $Object.hasOwnProperty.call(m, "emoji")) {
                d.emoji = m.emoji;
            }
            if (m.weight != null && $Object.hasOwnProperty.call(m, "weight")) {
                d.weight = o.json && !$isFinite(m.weight) ? $String(m.weight) : m.weight;
            }
            return d;
        };

        RecentEmojiWeight.prototype.toJSON = function() {
            return RecentEmojiWeight.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecentEmojiWeight.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RecentEmojiWeight";
        };

        return RecentEmojiWeight;
    })();

    proto.RecordStructure = (function() {

        const RecordStructure = function (p) {
            this.previousSessions = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RecordStructure.prototype.currentSession = null;
        RecordStructure.prototype.previousSessions = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RecordStructure.prototype, "_currentSession", {
            get: $util.oneOfGetter($oneOfFields = ["currentSession"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RecordStructure.create = function(properties) {
            return new RecordStructure(properties);
        };

        RecordStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.currentSession != null && $Object.hasOwnProperty.call(m, "currentSession"))
                $root.proto.SessionStructure.encode(m.currentSession, w.uint32(10).fork(), q + 1).ldelim();
            if (m.previousSessions != null && m.previousSessions.length) {
                for (var i = 0; i < m.previousSessions.length; ++i)
                    $root.proto.SessionStructure.encode(m.previousSessions[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RecordStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RecordStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.currentSession = $root.proto.SessionStructure.decode(r, r.uint32(), $undefined, q + 1, m.currentSession);
                        m._currentSession = "currentSession";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.previousSessions && m.previousSessions.length))
                            m.previousSessions = [];
                        m.previousSessions.push($root.proto.SessionStructure.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        RecordStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.RecordStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RecordStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RecordStructure();
            if (d.currentSession != null) {
                if (!$util.isObject(d.currentSession))
                    throw $TypeError(".proto.RecordStructure.currentSession: object expected");
                m.currentSession = $root.proto.SessionStructure.fromObject(d.currentSession, q + 1);
            }
            if (d.previousSessions) {
                if (!$Array.isArray(d.previousSessions))
                    throw $TypeError(".proto.RecordStructure.previousSessions: array expected");
                m.previousSessions = $Array(d.previousSessions.length);
                for (var i = 0; i < d.previousSessions.length; ++i) {
                    if (!$util.isObject(d.previousSessions[i]))
                        throw $TypeError(".proto.RecordStructure.previousSessions: object expected");
                    m.previousSessions[i] = $root.proto.SessionStructure.fromObject(d.previousSessions[i], q + 1);
                }
            }
            return m;
        };

        RecordStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.previousSessions = [];
            }
            if (m.currentSession != null && $Object.hasOwnProperty.call(m, "currentSession")) {
                d.currentSession = $root.proto.SessionStructure.toObject(m.currentSession, o, q + 1);
            }
            if (m.previousSessions && m.previousSessions.length) {
                d.previousSessions = $Array(m.previousSessions.length);
                for (var j = 0; j < m.previousSessions.length; ++j) {
                    d.previousSessions[j] = $root.proto.SessionStructure.toObject(m.previousSessions[j], o, q + 1);
                }
            }
            return d;
        };

        RecordStructure.prototype.toJSON = function() {
            return RecordStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        RecordStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RecordStructure";
        };

        return RecordStructure;
    })();

    proto.Reportable = (function() {

        const Reportable = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        Reportable.prototype.minVersion = null;
        Reportable.prototype.maxVersion = null;
        Reportable.prototype.notReportableMinVersion = null;
        Reportable.prototype.never = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reportable.prototype, "_minVersion", {
            get: $util.oneOfGetter($oneOfFields = ["minVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reportable.prototype, "_maxVersion", {
            get: $util.oneOfGetter($oneOfFields = ["maxVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reportable.prototype, "_notReportableMinVersion", {
            get: $util.oneOfGetter($oneOfFields = ["notReportableMinVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(Reportable.prototype, "_never", {
            get: $util.oneOfGetter($oneOfFields = ["never"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        Reportable.create = function(properties) {
            return new Reportable(properties);
        };

        Reportable.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.minVersion != null && $Object.hasOwnProperty.call(m, "minVersion"))
                w.uint32(8).uint32(m.minVersion);
            if (m.maxVersion != null && $Object.hasOwnProperty.call(m, "maxVersion"))
                w.uint32(16).uint32(m.maxVersion);
            if (m.notReportableMinVersion != null && $Object.hasOwnProperty.call(m, "notReportableMinVersion"))
                w.uint32(24).uint32(m.notReportableMinVersion);
            if (m.never != null && $Object.hasOwnProperty.call(m, "never"))
                w.uint32(32).bool(m.never);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        Reportable.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.Reportable();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.minVersion = r.uint32();
                        m._minVersion = "minVersion";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.maxVersion = r.uint32();
                        m._maxVersion = "maxVersion";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.notReportableMinVersion = r.uint32();
                        m._notReportableMinVersion = "notReportableMinVersion";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.never = r.bool();
                        m._never = "never";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        Reportable.fromObject = function (d, q) {
            if (d instanceof $root.proto.Reportable)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.Reportable: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.Reportable();
            if (d.minVersion != null) {
                m.minVersion = d.minVersion >>> 0;
            }
            if (d.maxVersion != null) {
                m.maxVersion = d.maxVersion >>> 0;
            }
            if (d.notReportableMinVersion != null) {
                m.notReportableMinVersion = d.notReportableMinVersion >>> 0;
            }
            if (d.never != null) {
                m.never = $Boolean(d.never);
            }
            return m;
        };

        Reportable.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.minVersion != null && $Object.hasOwnProperty.call(m, "minVersion")) {
                d.minVersion = m.minVersion;
            }
            if (m.maxVersion != null && $Object.hasOwnProperty.call(m, "maxVersion")) {
                d.maxVersion = m.maxVersion;
            }
            if (m.notReportableMinVersion != null && $Object.hasOwnProperty.call(m, "notReportableMinVersion")) {
                d.notReportableMinVersion = m.notReportableMinVersion;
            }
            if (m.never != null && $Object.hasOwnProperty.call(m, "never")) {
                d.never = m.never;
            }
            return d;
        };

        Reportable.prototype.toJSON = function() {
            return Reportable.toObject(this, $protobuf.util.toJSONOptions);
        };

        Reportable.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.Reportable";
        };

        return Reportable;
    })();

    proto.ReportingTokenInfo = (function() {

        const ReportingTokenInfo = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ReportingTokenInfo.prototype.reportingTag = null;
        ReportingTokenInfo.prototype.reportingTagTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ReportingTokenInfo.prototype, "_reportingTag", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTag"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ReportingTokenInfo.prototype, "_reportingTagTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTagTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ReportingTokenInfo.create = function(properties) {
            return new ReportingTokenInfo(properties);
        };

        ReportingTokenInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.reportingTag != null && $Object.hasOwnProperty.call(m, "reportingTag"))
                w.uint32(10).bytes(m.reportingTag);
            if (m.reportingTagTimestamp != null && $Object.hasOwnProperty.call(m, "reportingTagTimestamp"))
                w.uint32(16).uint64(m.reportingTagTimestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ReportingTokenInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ReportingTokenInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.reportingTag = r.bytes();
                        m._reportingTag = "reportingTag";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.reportingTagTimestamp = r.uint64();
                        m._reportingTagTimestamp = "reportingTagTimestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ReportingTokenInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.ReportingTokenInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ReportingTokenInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ReportingTokenInfo();
            if (d.reportingTag != null) {
                if (typeof d.reportingTag === "string")
                    $util.base64.decode(d.reportingTag, m.reportingTag = $util.newBuffer($util.base64.length(d.reportingTag)), 0);
                else if (d.reportingTag.length >= 0)
                    m.reportingTag = d.reportingTag;
            }
            if (d.reportingTagTimestamp != null) {
                if ($util.Long)
                    m.reportingTagTimestamp = $util.Long.fromValue(d.reportingTagTimestamp, true);
                else if (typeof d.reportingTagTimestamp === "string")
                    m.reportingTagTimestamp = $parseInt(d.reportingTagTimestamp, 10);
                else if (typeof d.reportingTagTimestamp === "number")
                    m.reportingTagTimestamp = d.reportingTagTimestamp;
                else if (typeof d.reportingTagTimestamp === "object")
                    m.reportingTagTimestamp = new $util.LongBits(d.reportingTagTimestamp.low >>> 0, d.reportingTagTimestamp.high >>> 0).toNumber(true);
            }
            return m;
        };

        ReportingTokenInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.reportingTag != null && $Object.hasOwnProperty.call(m, "reportingTag")) {
                d.reportingTag = o.bytes === $String ? $util.base64.encode(m.reportingTag, 0, m.reportingTag.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.reportingTag) : m.reportingTag;
            }
            if (m.reportingTagTimestamp != null && $Object.hasOwnProperty.call(m, "reportingTagTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.reportingTagTimestamp = typeof m.reportingTagTimestamp === "number" ? $BigInt(m.reportingTagTimestamp) : $util.Long.fromBits(m.reportingTagTimestamp.low >>> 0, m.reportingTagTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.reportingTagTimestamp === "number")
                    d.reportingTagTimestamp = o.longs === $String ? $String(m.reportingTagTimestamp) : m.reportingTagTimestamp;
                else
                    d.reportingTagTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.reportingTagTimestamp) : o.longs === $Number ? new $util.LongBits(m.reportingTagTimestamp.low >>> 0, m.reportingTagTimestamp.high >>> 0).toNumber(true) : m.reportingTagTimestamp;
            }
            return d;
        };

        ReportingTokenInfo.prototype.toJSON = function() {
            return ReportingTokenInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        ReportingTokenInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ReportingTokenInfo";
        };

        return ReportingTokenInfo;
    })();

    proto.RotateEpochInput = (function() {

        const RotateEpochInput = function (p) {
            this.members = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RotateEpochInput.prototype.currentEpochRootKey = $util.newBuffer([]);
        RotateEpochInput.prototype.currentEpochAnonId = $util.newBuffer([]);
        RotateEpochInput.prototype.currentEpochFbid = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        RotateEpochInput.prototype.epochStoragePrivateKey = $util.newBuffer([]);
        RotateEpochInput.prototype.members = $util.emptyArray;

        RotateEpochInput.create = function(properties) {
            return new RotateEpochInput(properties);
        };

        RotateEpochInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.currentEpochRootKey);
            w.uint32(18).bytes(m.currentEpochAnonId);
            w.uint32(24).uint64(m.currentEpochFbid);
            w.uint32(34).bytes(m.epochStoragePrivateKey);
            if (m.members != null && m.members.length) {
                for (var i = 0; i < m.members.length; ++i)
                    $root.proto.RotateEpochMemberInput.encode(m.members[i], w.uint32(42).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RotateEpochInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RotateEpochInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.currentEpochRootKey = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.currentEpochAnonId = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.currentEpochFbid = r.uint64();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.epochStoragePrivateKey = r.bytes();
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(m.members && m.members.length))
                            m.members = [];
                        m.members.push($root.proto.RotateEpochMemberInput.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "currentEpochRootKey"))
                throw $util.ProtocolError("missing required 'currentEpochRootKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "currentEpochAnonId"))
                throw $util.ProtocolError("missing required 'currentEpochAnonId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "currentEpochFbid"))
                throw $util.ProtocolError("missing required 'currentEpochFbid'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochStoragePrivateKey"))
                throw $util.ProtocolError("missing required 'epochStoragePrivateKey'", { instance: m });
            return m;
        };

        RotateEpochInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.RotateEpochInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RotateEpochInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RotateEpochInput();
            if (d.currentEpochRootKey != null) {
                if (typeof d.currentEpochRootKey === "string")
                    $util.base64.decode(d.currentEpochRootKey, m.currentEpochRootKey = $util.newBuffer($util.base64.length(d.currentEpochRootKey)), 0);
                else if (d.currentEpochRootKey.length >= 0)
                    m.currentEpochRootKey = d.currentEpochRootKey;
            }
            if (d.currentEpochAnonId != null) {
                if (typeof d.currentEpochAnonId === "string")
                    $util.base64.decode(d.currentEpochAnonId, m.currentEpochAnonId = $util.newBuffer($util.base64.length(d.currentEpochAnonId)), 0);
                else if (d.currentEpochAnonId.length >= 0)
                    m.currentEpochAnonId = d.currentEpochAnonId;
            }
            if (d.currentEpochFbid != null) {
                if ($util.Long)
                    m.currentEpochFbid = $util.Long.fromValue(d.currentEpochFbid, true);
                else if (typeof d.currentEpochFbid === "string")
                    m.currentEpochFbid = $parseInt(d.currentEpochFbid, 10);
                else if (typeof d.currentEpochFbid === "number")
                    m.currentEpochFbid = d.currentEpochFbid;
                else if (typeof d.currentEpochFbid === "object")
                    m.currentEpochFbid = new $util.LongBits(d.currentEpochFbid.low >>> 0, d.currentEpochFbid.high >>> 0).toNumber(true);
            }
            if (d.epochStoragePrivateKey != null) {
                if (typeof d.epochStoragePrivateKey === "string")
                    $util.base64.decode(d.epochStoragePrivateKey, m.epochStoragePrivateKey = $util.newBuffer($util.base64.length(d.epochStoragePrivateKey)), 0);
                else if (d.epochStoragePrivateKey.length >= 0)
                    m.epochStoragePrivateKey = d.epochStoragePrivateKey;
            }
            if (d.members) {
                if (!$Array.isArray(d.members))
                    throw $TypeError(".proto.RotateEpochInput.members: array expected");
                m.members = $Array(d.members.length);
                for (var i = 0; i < d.members.length; ++i) {
                    if (!$util.isObject(d.members[i]))
                        throw $TypeError(".proto.RotateEpochInput.members: object expected");
                    m.members[i] = $root.proto.RotateEpochMemberInput.fromObject(d.members[i], q + 1);
                }
            }
            return m;
        };

        RotateEpochInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.members = [];
            }
            if (o.defaults) {
                if (o.bytes === $String)
                    d.currentEpochRootKey = "";
                else {
                    d.currentEpochRootKey = [];
                    if (o.bytes !== $Array)
                        d.currentEpochRootKey = $util.newBuffer(d.currentEpochRootKey);
                }
                if (o.bytes === $String)
                    d.currentEpochAnonId = "";
                else {
                    d.currentEpochAnonId = [];
                    if (o.bytes !== $Array)
                        d.currentEpochAnonId = $util.newBuffer(d.currentEpochAnonId);
                }
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.currentEpochFbid = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.currentEpochFbid = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                if (o.bytes === $String)
                    d.epochStoragePrivateKey = "";
                else {
                    d.epochStoragePrivateKey = [];
                    if (o.bytes !== $Array)
                        d.epochStoragePrivateKey = $util.newBuffer(d.epochStoragePrivateKey);
                }
            }
            if (m.currentEpochRootKey != null && $Object.hasOwnProperty.call(m, "currentEpochRootKey")) {
                d.currentEpochRootKey = o.bytes === $String ? $util.base64.encode(m.currentEpochRootKey, 0, m.currentEpochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.currentEpochRootKey) : m.currentEpochRootKey;
            }
            if (m.currentEpochAnonId != null && $Object.hasOwnProperty.call(m, "currentEpochAnonId")) {
                d.currentEpochAnonId = o.bytes === $String ? $util.base64.encode(m.currentEpochAnonId, 0, m.currentEpochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.currentEpochAnonId) : m.currentEpochAnonId;
            }
            if (m.currentEpochFbid != null && $Object.hasOwnProperty.call(m, "currentEpochFbid")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.currentEpochFbid = typeof m.currentEpochFbid === "number" ? $BigInt(m.currentEpochFbid) : $util.Long.fromBits(m.currentEpochFbid.low >>> 0, m.currentEpochFbid.high >>> 0, true).toBigInt();
                else if (typeof m.currentEpochFbid === "number")
                    d.currentEpochFbid = o.longs === $String ? $String(m.currentEpochFbid) : m.currentEpochFbid;
                else
                    d.currentEpochFbid = o.longs === $String ? $util.Long.prototype.toString.call(m.currentEpochFbid) : o.longs === $Number ? new $util.LongBits(m.currentEpochFbid.low >>> 0, m.currentEpochFbid.high >>> 0).toNumber(true) : m.currentEpochFbid;
            }
            if (m.epochStoragePrivateKey != null && $Object.hasOwnProperty.call(m, "epochStoragePrivateKey")) {
                d.epochStoragePrivateKey = o.bytes === $String ? $util.base64.encode(m.epochStoragePrivateKey, 0, m.epochStoragePrivateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochStoragePrivateKey) : m.epochStoragePrivateKey;
            }
            if (m.members && m.members.length) {
                d.members = $Array(m.members.length);
                for (var j = 0; j < m.members.length; ++j) {
                    d.members[j] = $root.proto.RotateEpochMemberInput.toObject(m.members[j], o, q + 1);
                }
            }
            return d;
        };

        RotateEpochInput.prototype.toJSON = function() {
            return RotateEpochInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        RotateEpochInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RotateEpochInput";
        };

        return RotateEpochInput;
    })();

    proto.RotateEpochMemberEdge = (function() {

        const RotateEpochMemberEdge = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RotateEpochMemberEdge.prototype.deviceId = null;
        RotateEpochMemberEdge.prototype.encryptedEpochKey = null;
        RotateEpochMemberEdge.prototype.deviceEpochHmac = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochMemberEdge.prototype, "_deviceId", {
            get: $util.oneOfGetter($oneOfFields = ["deviceId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochMemberEdge.prototype, "_encryptedEpochKey", {
            get: $util.oneOfGetter($oneOfFields = ["encryptedEpochKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochMemberEdge.prototype, "_deviceEpochHmac", {
            get: $util.oneOfGetter($oneOfFields = ["deviceEpochHmac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RotateEpochMemberEdge.create = function(properties) {
            return new RotateEpochMemberEdge(properties);
        };

        RotateEpochMemberEdge.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId"))
                w.uint32(8).uint64(m.deviceId);
            if (m.encryptedEpochKey != null && $Object.hasOwnProperty.call(m, "encryptedEpochKey"))
                w.uint32(18).bytes(m.encryptedEpochKey);
            if (m.deviceEpochHmac != null && $Object.hasOwnProperty.call(m, "deviceEpochHmac"))
                w.uint32(26).bytes(m.deviceEpochHmac);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RotateEpochMemberEdge.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RotateEpochMemberEdge();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.deviceId = r.uint64();
                        m._deviceId = "deviceId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.encryptedEpochKey = r.bytes();
                        m._encryptedEpochKey = "encryptedEpochKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.deviceEpochHmac = r.bytes();
                        m._deviceEpochHmac = "deviceEpochHmac";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        RotateEpochMemberEdge.fromObject = function (d, q) {
            if (d instanceof $root.proto.RotateEpochMemberEdge)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RotateEpochMemberEdge: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RotateEpochMemberEdge();
            if (d.deviceId != null) {
                if ($util.Long)
                    m.deviceId = $util.Long.fromValue(d.deviceId, true);
                else if (typeof d.deviceId === "string")
                    m.deviceId = $parseInt(d.deviceId, 10);
                else if (typeof d.deviceId === "number")
                    m.deviceId = d.deviceId;
                else if (typeof d.deviceId === "object")
                    m.deviceId = new $util.LongBits(d.deviceId.low >>> 0, d.deviceId.high >>> 0).toNumber(true);
            }
            if (d.encryptedEpochKey != null) {
                if (typeof d.encryptedEpochKey === "string")
                    $util.base64.decode(d.encryptedEpochKey, m.encryptedEpochKey = $util.newBuffer($util.base64.length(d.encryptedEpochKey)), 0);
                else if (d.encryptedEpochKey.length >= 0)
                    m.encryptedEpochKey = d.encryptedEpochKey;
            }
            if (d.deviceEpochHmac != null) {
                if (typeof d.deviceEpochHmac === "string")
                    $util.base64.decode(d.deviceEpochHmac, m.deviceEpochHmac = $util.newBuffer($util.base64.length(d.deviceEpochHmac)), 0);
                else if (d.deviceEpochHmac.length >= 0)
                    m.deviceEpochHmac = d.deviceEpochHmac;
            }
            return m;
        };

        RotateEpochMemberEdge.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.deviceId = typeof m.deviceId === "number" ? $BigInt(m.deviceId) : $util.Long.fromBits(m.deviceId.low >>> 0, m.deviceId.high >>> 0, true).toBigInt();
                else if (typeof m.deviceId === "number")
                    d.deviceId = o.longs === $String ? $String(m.deviceId) : m.deviceId;
                else
                    d.deviceId = o.longs === $String ? $util.Long.prototype.toString.call(m.deviceId) : o.longs === $Number ? new $util.LongBits(m.deviceId.low >>> 0, m.deviceId.high >>> 0).toNumber(true) : m.deviceId;
            }
            if (m.encryptedEpochKey != null && $Object.hasOwnProperty.call(m, "encryptedEpochKey")) {
                d.encryptedEpochKey = o.bytes === $String ? $util.base64.encode(m.encryptedEpochKey, 0, m.encryptedEpochKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.encryptedEpochKey) : m.encryptedEpochKey;
            }
            if (m.deviceEpochHmac != null && $Object.hasOwnProperty.call(m, "deviceEpochHmac")) {
                d.deviceEpochHmac = o.bytes === $String ? $util.base64.encode(m.deviceEpochHmac, 0, m.deviceEpochHmac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.deviceEpochHmac) : m.deviceEpochHmac;
            }
            return d;
        };

        RotateEpochMemberEdge.prototype.toJSON = function() {
            return RotateEpochMemberEdge.toObject(this, $protobuf.util.toJSONOptions);
        };

        RotateEpochMemberEdge.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RotateEpochMemberEdge";
        };

        return RotateEpochMemberEdge;
    })();

    proto.RotateEpochMemberInput = (function() {

        const RotateEpochMemberInput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RotateEpochMemberInput.prototype.deviceId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        RotateEpochMemberInput.prototype.epochStoragePublicKey = $util.newBuffer([]);
        RotateEpochMemberInput.prototype.devicePublicKey = $util.newBuffer([]);

        RotateEpochMemberInput.create = function(properties) {
            return new RotateEpochMemberInput(properties);
        };

        RotateEpochMemberInput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(8).uint64(m.deviceId);
            w.uint32(18).bytes(m.epochStoragePublicKey);
            w.uint32(26).bytes(m.devicePublicKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RotateEpochMemberInput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RotateEpochMemberInput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.deviceId = r.uint64();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.epochStoragePublicKey = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.devicePublicKey = r.bytes();
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "deviceId"))
                throw $util.ProtocolError("missing required 'deviceId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "epochStoragePublicKey"))
                throw $util.ProtocolError("missing required 'epochStoragePublicKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "devicePublicKey"))
                throw $util.ProtocolError("missing required 'devicePublicKey'", { instance: m });
            return m;
        };

        RotateEpochMemberInput.fromObject = function (d, q) {
            if (d instanceof $root.proto.RotateEpochMemberInput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RotateEpochMemberInput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RotateEpochMemberInput();
            if (d.deviceId != null) {
                if ($util.Long)
                    m.deviceId = $util.Long.fromValue(d.deviceId, true);
                else if (typeof d.deviceId === "string")
                    m.deviceId = $parseInt(d.deviceId, 10);
                else if (typeof d.deviceId === "number")
                    m.deviceId = d.deviceId;
                else if (typeof d.deviceId === "object")
                    m.deviceId = new $util.LongBits(d.deviceId.low >>> 0, d.deviceId.high >>> 0).toNumber(true);
            }
            if (d.epochStoragePublicKey != null) {
                if (typeof d.epochStoragePublicKey === "string")
                    $util.base64.decode(d.epochStoragePublicKey, m.epochStoragePublicKey = $util.newBuffer($util.base64.length(d.epochStoragePublicKey)), 0);
                else if (d.epochStoragePublicKey.length >= 0)
                    m.epochStoragePublicKey = d.epochStoragePublicKey;
            }
            if (d.devicePublicKey != null) {
                if (typeof d.devicePublicKey === "string")
                    $util.base64.decode(d.devicePublicKey, m.devicePublicKey = $util.newBuffer($util.base64.length(d.devicePublicKey)), 0);
                else if (d.devicePublicKey.length >= 0)
                    m.devicePublicKey = d.devicePublicKey;
            }
            return m;
        };

        RotateEpochMemberInput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.deviceId = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.deviceId = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
                if (o.bytes === $String)
                    d.epochStoragePublicKey = "";
                else {
                    d.epochStoragePublicKey = [];
                    if (o.bytes !== $Array)
                        d.epochStoragePublicKey = $util.newBuffer(d.epochStoragePublicKey);
                }
                if (o.bytes === $String)
                    d.devicePublicKey = "";
                else {
                    d.devicePublicKey = [];
                    if (o.bytes !== $Array)
                        d.devicePublicKey = $util.newBuffer(d.devicePublicKey);
                }
            }
            if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.deviceId = typeof m.deviceId === "number" ? $BigInt(m.deviceId) : $util.Long.fromBits(m.deviceId.low >>> 0, m.deviceId.high >>> 0, true).toBigInt();
                else if (typeof m.deviceId === "number")
                    d.deviceId = o.longs === $String ? $String(m.deviceId) : m.deviceId;
                else
                    d.deviceId = o.longs === $String ? $util.Long.prototype.toString.call(m.deviceId) : o.longs === $Number ? new $util.LongBits(m.deviceId.low >>> 0, m.deviceId.high >>> 0).toNumber(true) : m.deviceId;
            }
            if (m.epochStoragePublicKey != null && $Object.hasOwnProperty.call(m, "epochStoragePublicKey")) {
                d.epochStoragePublicKey = o.bytes === $String ? $util.base64.encode(m.epochStoragePublicKey, 0, m.epochStoragePublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochStoragePublicKey) : m.epochStoragePublicKey;
            }
            if (m.devicePublicKey != null && $Object.hasOwnProperty.call(m, "devicePublicKey")) {
                d.devicePublicKey = o.bytes === $String ? $util.base64.encode(m.devicePublicKey, 0, m.devicePublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.devicePublicKey) : m.devicePublicKey;
            }
            return d;
        };

        RotateEpochMemberInput.prototype.toJSON = function() {
            return RotateEpochMemberInput.toObject(this, $protobuf.util.toJSONOptions);
        };

        RotateEpochMemberInput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RotateEpochMemberInput";
        };

        return RotateEpochMemberInput;
    })();

    proto.RotateEpochOutput = (function() {

        const RotateEpochOutput = function (p) {
            this.memberEdges = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RotateEpochOutput.prototype.newEpochRootKey = null;
        RotateEpochOutput.prototype.newEpochAnonId = null;
        RotateEpochOutput.prototype.newEpochFbid = null;
        RotateEpochOutput.prototype.epochAnonId = null;
        RotateEpochOutput.prototype.backwardEdge = null;
        RotateEpochOutput.prototype.memberEdges = $util.emptyArray;
        RotateEpochOutput.prototype.epochRootKeyFingerprint = null;
        RotateEpochOutput.prototype.error = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_newEpochRootKey", {
            get: $util.oneOfGetter($oneOfFields = ["newEpochRootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_newEpochAnonId", {
            get: $util.oneOfGetter($oneOfFields = ["newEpochAnonId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_newEpochFbid", {
            get: $util.oneOfGetter($oneOfFields = ["newEpochFbid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_epochAnonId", {
            get: $util.oneOfGetter($oneOfFields = ["epochAnonId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_backwardEdge", {
            get: $util.oneOfGetter($oneOfFields = ["backwardEdge"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_epochRootKeyFingerprint", {
            get: $util.oneOfGetter($oneOfFields = ["epochRootKeyFingerprint"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RotateEpochOutput.prototype, "_error", {
            get: $util.oneOfGetter($oneOfFields = ["error"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RotateEpochOutput.create = function(properties) {
            return new RotateEpochOutput(properties);
        };

        RotateEpochOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.newEpochRootKey != null && $Object.hasOwnProperty.call(m, "newEpochRootKey"))
                w.uint32(10).bytes(m.newEpochRootKey);
            if (m.newEpochAnonId != null && $Object.hasOwnProperty.call(m, "newEpochAnonId"))
                w.uint32(16).uint64(m.newEpochAnonId);
            if (m.epochAnonId != null && $Object.hasOwnProperty.call(m, "epochAnonId"))
                w.uint32(26).bytes(m.epochAnonId);
            if (m.backwardEdge != null && $Object.hasOwnProperty.call(m, "backwardEdge"))
                $root.proto.BackwardEdge.encode(m.backwardEdge, w.uint32(34).fork(), q + 1).ldelim();
            if (m.memberEdges != null && m.memberEdges.length) {
                for (var i = 0; i < m.memberEdges.length; ++i)
                    $root.proto.RotateEpochMemberEdge.encode(m.memberEdges[i], w.uint32(42).fork(), q + 1).ldelim();
            }
            if (m.epochRootKeyFingerprint != null && $Object.hasOwnProperty.call(m, "epochRootKeyFingerprint"))
                w.uint32(50).bytes(m.epochRootKeyFingerprint);
            if (m.error != null && $Object.hasOwnProperty.call(m, "error"))
                w.uint32(58).string(m.error);
            if (m.newEpochFbid != null && $Object.hasOwnProperty.call(m, "newEpochFbid"))
                w.uint32(64).uint64(m.newEpochFbid);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RotateEpochOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RotateEpochOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.newEpochRootKey = r.bytes();
                        m._newEpochRootKey = "newEpochRootKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.newEpochAnonId = r.uint64();
                        m._newEpochAnonId = "newEpochAnonId";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.newEpochFbid = r.uint64();
                        m._newEpochFbid = "newEpochFbid";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.epochAnonId = r.bytes();
                        m._epochAnonId = "epochAnonId";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.backwardEdge = $root.proto.BackwardEdge.decode(r, r.uint32(), $undefined, q + 1, m.backwardEdge);
                        m._backwardEdge = "backwardEdge";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(m.memberEdges && m.memberEdges.length))
                            m.memberEdges = [];
                        m.memberEdges.push($root.proto.RotateEpochMemberEdge.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.epochRootKeyFingerprint = r.bytes();
                        m._epochRootKeyFingerprint = "epochRootKeyFingerprint";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.error = r.stringVerify();
                        m._error = "error";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        RotateEpochOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.RotateEpochOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RotateEpochOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RotateEpochOutput();
            if (d.newEpochRootKey != null) {
                if (typeof d.newEpochRootKey === "string")
                    $util.base64.decode(d.newEpochRootKey, m.newEpochRootKey = $util.newBuffer($util.base64.length(d.newEpochRootKey)), 0);
                else if (d.newEpochRootKey.length >= 0)
                    m.newEpochRootKey = d.newEpochRootKey;
            }
            if (d.newEpochAnonId != null) {
                if ($util.Long)
                    m.newEpochAnonId = $util.Long.fromValue(d.newEpochAnonId, true);
                else if (typeof d.newEpochAnonId === "string")
                    m.newEpochAnonId = $parseInt(d.newEpochAnonId, 10);
                else if (typeof d.newEpochAnonId === "number")
                    m.newEpochAnonId = d.newEpochAnonId;
                else if (typeof d.newEpochAnonId === "object")
                    m.newEpochAnonId = new $util.LongBits(d.newEpochAnonId.low >>> 0, d.newEpochAnonId.high >>> 0).toNumber(true);
            }
            if (d.newEpochFbid != null) {
                if ($util.Long)
                    m.newEpochFbid = $util.Long.fromValue(d.newEpochFbid, true);
                else if (typeof d.newEpochFbid === "string")
                    m.newEpochFbid = $parseInt(d.newEpochFbid, 10);
                else if (typeof d.newEpochFbid === "number")
                    m.newEpochFbid = d.newEpochFbid;
                else if (typeof d.newEpochFbid === "object")
                    m.newEpochFbid = new $util.LongBits(d.newEpochFbid.low >>> 0, d.newEpochFbid.high >>> 0).toNumber(true);
            }
            if (d.epochAnonId != null) {
                if (typeof d.epochAnonId === "string")
                    $util.base64.decode(d.epochAnonId, m.epochAnonId = $util.newBuffer($util.base64.length(d.epochAnonId)), 0);
                else if (d.epochAnonId.length >= 0)
                    m.epochAnonId = d.epochAnonId;
            }
            if (d.backwardEdge != null) {
                if (!$util.isObject(d.backwardEdge))
                    throw $TypeError(".proto.RotateEpochOutput.backwardEdge: object expected");
                m.backwardEdge = $root.proto.BackwardEdge.fromObject(d.backwardEdge, q + 1);
            }
            if (d.memberEdges) {
                if (!$Array.isArray(d.memberEdges))
                    throw $TypeError(".proto.RotateEpochOutput.memberEdges: array expected");
                m.memberEdges = $Array(d.memberEdges.length);
                for (var i = 0; i < d.memberEdges.length; ++i) {
                    if (!$util.isObject(d.memberEdges[i]))
                        throw $TypeError(".proto.RotateEpochOutput.memberEdges: object expected");
                    m.memberEdges[i] = $root.proto.RotateEpochMemberEdge.fromObject(d.memberEdges[i], q + 1);
                }
            }
            if (d.epochRootKeyFingerprint != null) {
                if (typeof d.epochRootKeyFingerprint === "string")
                    $util.base64.decode(d.epochRootKeyFingerprint, m.epochRootKeyFingerprint = $util.newBuffer($util.base64.length(d.epochRootKeyFingerprint)), 0);
                else if (d.epochRootKeyFingerprint.length >= 0)
                    m.epochRootKeyFingerprint = d.epochRootKeyFingerprint;
            }
            if (d.error != null) {
                m.error = $String(d.error);
            }
            return m;
        };

        RotateEpochOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.memberEdges = [];
            }
            if (m.newEpochRootKey != null && $Object.hasOwnProperty.call(m, "newEpochRootKey")) {
                d.newEpochRootKey = o.bytes === $String ? $util.base64.encode(m.newEpochRootKey, 0, m.newEpochRootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.newEpochRootKey) : m.newEpochRootKey;
            }
            if (m.newEpochAnonId != null && $Object.hasOwnProperty.call(m, "newEpochAnonId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.newEpochAnonId = typeof m.newEpochAnonId === "number" ? $BigInt(m.newEpochAnonId) : $util.Long.fromBits(m.newEpochAnonId.low >>> 0, m.newEpochAnonId.high >>> 0, true).toBigInt();
                else if (typeof m.newEpochAnonId === "number")
                    d.newEpochAnonId = o.longs === $String ? $String(m.newEpochAnonId) : m.newEpochAnonId;
                else
                    d.newEpochAnonId = o.longs === $String ? $util.Long.prototype.toString.call(m.newEpochAnonId) : o.longs === $Number ? new $util.LongBits(m.newEpochAnonId.low >>> 0, m.newEpochAnonId.high >>> 0).toNumber(true) : m.newEpochAnonId;
            }
            if (m.epochAnonId != null && $Object.hasOwnProperty.call(m, "epochAnonId")) {
                d.epochAnonId = o.bytes === $String ? $util.base64.encode(m.epochAnonId, 0, m.epochAnonId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochAnonId) : m.epochAnonId;
            }
            if (m.backwardEdge != null && $Object.hasOwnProperty.call(m, "backwardEdge")) {
                d.backwardEdge = $root.proto.BackwardEdge.toObject(m.backwardEdge, o, q + 1);
            }
            if (m.memberEdges && m.memberEdges.length) {
                d.memberEdges = $Array(m.memberEdges.length);
                for (var j = 0; j < m.memberEdges.length; ++j) {
                    d.memberEdges[j] = $root.proto.RotateEpochMemberEdge.toObject(m.memberEdges[j], o, q + 1);
                }
            }
            if (m.epochRootKeyFingerprint != null && $Object.hasOwnProperty.call(m, "epochRootKeyFingerprint")) {
                d.epochRootKeyFingerprint = o.bytes === $String ? $util.base64.encode(m.epochRootKeyFingerprint, 0, m.epochRootKeyFingerprint.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.epochRootKeyFingerprint) : m.epochRootKeyFingerprint;
            }
            if (m.error != null && $Object.hasOwnProperty.call(m, "error")) {
                d.error = m.error;
            }
            if (m.newEpochFbid != null && $Object.hasOwnProperty.call(m, "newEpochFbid")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.newEpochFbid = typeof m.newEpochFbid === "number" ? $BigInt(m.newEpochFbid) : $util.Long.fromBits(m.newEpochFbid.low >>> 0, m.newEpochFbid.high >>> 0, true).toBigInt();
                else if (typeof m.newEpochFbid === "number")
                    d.newEpochFbid = o.longs === $String ? $String(m.newEpochFbid) : m.newEpochFbid;
                else
                    d.newEpochFbid = o.longs === $String ? $util.Long.prototype.toString.call(m.newEpochFbid) : o.longs === $Number ? new $util.LongBits(m.newEpochFbid.low >>> 0, m.newEpochFbid.high >>> 0).toNumber(true) : m.newEpochFbid;
            }
            return d;
        };

        RotateEpochOutput.prototype.toJSON = function() {
            return RotateEpochOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        RotateEpochOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RotateEpochOutput";
        };

        return RotateEpochOutput;
    })();

    proto.RoutingInfo = (function() {

        const RoutingInfo = function (p) {
            this.regionId = [];
            this.clusterId = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        RoutingInfo.prototype.regionId = $util.emptyArray;
        RoutingInfo.prototype.clusterId = $util.emptyArray;
        RoutingInfo.prototype.taskId = null;
        RoutingInfo.prototype.debug = null;
        RoutingInfo.prototype.tcpBbr = null;
        RoutingInfo.prototype.tcpKeepalive = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RoutingInfo.prototype, "_taskId", {
            get: $util.oneOfGetter($oneOfFields = ["taskId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RoutingInfo.prototype, "_debug", {
            get: $util.oneOfGetter($oneOfFields = ["debug"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RoutingInfo.prototype, "_tcpBbr", {
            get: $util.oneOfGetter($oneOfFields = ["tcpBbr"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(RoutingInfo.prototype, "_tcpKeepalive", {
            get: $util.oneOfGetter($oneOfFields = ["tcpKeepalive"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        RoutingInfo.create = function(properties) {
            return new RoutingInfo(properties);
        };

        RoutingInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.regionId != null && m.regionId.length) {
                w.uint32(10).int32s(m.regionId);
            }
            if (m.clusterId != null && m.clusterId.length) {
                w.uint32(18).int32s(m.clusterId);
            }
            if (m.taskId != null && $Object.hasOwnProperty.call(m, "taskId"))
                w.uint32(24).int32(m.taskId);
            if (m.debug != null && $Object.hasOwnProperty.call(m, "debug"))
                w.uint32(32).bool(m.debug);
            if (m.tcpBbr != null && $Object.hasOwnProperty.call(m, "tcpBbr"))
                w.uint32(40).bool(m.tcpBbr);
            if (m.tcpKeepalive != null && $Object.hasOwnProperty.call(m, "tcpKeepalive"))
                w.uint32(48).bool(m.tcpKeepalive);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        RoutingInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.RoutingInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u === 2) {
                            if (!(m.regionId && m.regionId.length))
                                m.regionId = [];
                            r.int32s(m.regionId);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.regionId && m.regionId.length))
                            m.regionId = [];
                        m.regionId.push(r.int32());
                        continue;
                    }
                case 2: {
                        if (u === 2) {
                            if (!(m.clusterId && m.clusterId.length))
                                m.clusterId = [];
                            r.int32s(m.clusterId);
                            continue;
                        }
                        if (u !== 0)
                            break;
                        if (!(m.clusterId && m.clusterId.length))
                            m.clusterId = [];
                        m.clusterId.push(r.int32());
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.taskId = r.int32();
                        m._taskId = "taskId";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.debug = r.bool();
                        m._debug = "debug";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.tcpBbr = r.bool();
                        m._tcpBbr = "tcpBbr";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.tcpKeepalive = r.bool();
                        m._tcpKeepalive = "tcpKeepalive";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        RoutingInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.RoutingInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.RoutingInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.RoutingInfo();
            if (d.regionId) {
                if (!$Array.isArray(d.regionId))
                    throw $TypeError(".proto.RoutingInfo.regionId: array expected");
                m.regionId = $Array(d.regionId.length);
                for (var i = 0; i < d.regionId.length; ++i) {
                    m.regionId[i] = d.regionId[i] | 0;
                }
            }
            if (d.clusterId) {
                if (!$Array.isArray(d.clusterId))
                    throw $TypeError(".proto.RoutingInfo.clusterId: array expected");
                m.clusterId = $Array(d.clusterId.length);
                for (var i = 0; i < d.clusterId.length; ++i) {
                    m.clusterId[i] = d.clusterId[i] | 0;
                }
            }
            if (d.taskId != null) {
                m.taskId = d.taskId | 0;
            }
            if (d.debug != null) {
                m.debug = $Boolean(d.debug);
            }
            if (d.tcpBbr != null) {
                m.tcpBbr = $Boolean(d.tcpBbr);
            }
            if (d.tcpKeepalive != null) {
                m.tcpKeepalive = $Boolean(d.tcpKeepalive);
            }
            return m;
        };

        RoutingInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.regionId = [];
                d.clusterId = [];
            }
            if (m.regionId && m.regionId.length) {
                d.regionId = $Array(m.regionId.length);
                for (var j = 0; j < m.regionId.length; ++j) {
                    d.regionId[j] = m.regionId[j];
                }
            }
            if (m.clusterId && m.clusterId.length) {
                d.clusterId = $Array(m.clusterId.length);
                for (var j = 0; j < m.clusterId.length; ++j) {
                    d.clusterId[j] = m.clusterId[j];
                }
            }
            if (m.taskId != null && $Object.hasOwnProperty.call(m, "taskId")) {
                d.taskId = m.taskId;
            }
            if (m.debug != null && $Object.hasOwnProperty.call(m, "debug")) {
                d.debug = m.debug;
            }
            if (m.tcpBbr != null && $Object.hasOwnProperty.call(m, "tcpBbr")) {
                d.tcpBbr = m.tcpBbr;
            }
            if (m.tcpKeepalive != null && $Object.hasOwnProperty.call(m, "tcpKeepalive")) {
                d.tcpKeepalive = m.tcpKeepalive;
            }
            return d;
        };

        RoutingInfo.prototype.toJSON = function() {
            return RoutingInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        RoutingInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.RoutingInfo";
        };

        return RoutingInfo;
    })();

    proto.ScheduledMessageMetadata = (function() {

        const ScheduledMessageMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ScheduledMessageMetadata.prototype.revealKeyId = null;
        ScheduledMessageMetadata.prototype.revealKey = null;
        ScheduledMessageMetadata.prototype.scheduledTime = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ScheduledMessageMetadata.prototype, "_revealKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["revealKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ScheduledMessageMetadata.prototype, "_revealKey", {
            get: $util.oneOfGetter($oneOfFields = ["revealKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ScheduledMessageMetadata.prototype, "_scheduledTime", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledTime"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ScheduledMessageMetadata.create = function(properties) {
            return new ScheduledMessageMetadata(properties);
        };

        ScheduledMessageMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.revealKeyId != null && $Object.hasOwnProperty.call(m, "revealKeyId"))
                w.uint32(10).string(m.revealKeyId);
            if (m.revealKey != null && $Object.hasOwnProperty.call(m, "revealKey"))
                w.uint32(18).bytes(m.revealKey);
            if (m.scheduledTime != null && $Object.hasOwnProperty.call(m, "scheduledTime"))
                w.uint32(24).uint64(m.scheduledTime);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ScheduledMessageMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ScheduledMessageMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.revealKeyId = r.stringVerify();
                        m._revealKeyId = "revealKeyId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.revealKey = r.bytes();
                        m._revealKey = "revealKey";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.scheduledTime = r.uint64();
                        m._scheduledTime = "scheduledTime";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ScheduledMessageMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.ScheduledMessageMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ScheduledMessageMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ScheduledMessageMetadata();
            if (d.revealKeyId != null) {
                m.revealKeyId = $String(d.revealKeyId);
            }
            if (d.revealKey != null) {
                if (typeof d.revealKey === "string")
                    $util.base64.decode(d.revealKey, m.revealKey = $util.newBuffer($util.base64.length(d.revealKey)), 0);
                else if (d.revealKey.length >= 0)
                    m.revealKey = d.revealKey;
            }
            if (d.scheduledTime != null) {
                if ($util.Long)
                    m.scheduledTime = $util.Long.fromValue(d.scheduledTime, true);
                else if (typeof d.scheduledTime === "string")
                    m.scheduledTime = $parseInt(d.scheduledTime, 10);
                else if (typeof d.scheduledTime === "number")
                    m.scheduledTime = d.scheduledTime;
                else if (typeof d.scheduledTime === "object")
                    m.scheduledTime = new $util.LongBits(d.scheduledTime.low >>> 0, d.scheduledTime.high >>> 0).toNumber(true);
            }
            return m;
        };

        ScheduledMessageMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.revealKeyId != null && $Object.hasOwnProperty.call(m, "revealKeyId")) {
                d.revealKeyId = m.revealKeyId;
            }
            if (m.revealKey != null && $Object.hasOwnProperty.call(m, "revealKey")) {
                d.revealKey = o.bytes === $String ? $util.base64.encode(m.revealKey, 0, m.revealKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.revealKey) : m.revealKey;
            }
            if (m.scheduledTime != null && $Object.hasOwnProperty.call(m, "scheduledTime")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.scheduledTime = typeof m.scheduledTime === "number" ? $BigInt(m.scheduledTime) : $util.Long.fromBits(m.scheduledTime.low >>> 0, m.scheduledTime.high >>> 0, true).toBigInt();
                else if (typeof m.scheduledTime === "number")
                    d.scheduledTime = o.longs === $String ? $String(m.scheduledTime) : m.scheduledTime;
                else
                    d.scheduledTime = o.longs === $String ? $util.Long.prototype.toString.call(m.scheduledTime) : o.longs === $Number ? new $util.LongBits(m.scheduledTime.low >>> 0, m.scheduledTime.high >>> 0).toNumber(true) : m.scheduledTime;
            }
            return d;
        };

        ScheduledMessageMetadata.prototype.toJSON = function() {
            return ScheduledMessageMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        ScheduledMessageMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ScheduledMessageMetadata";
        };

        return ScheduledMessageMetadata;
    })();

    proto.SenderKeyDistributionMessage = (function() {

        const SenderKeyDistributionMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SenderKeyDistributionMessage.prototype.id = null;
        SenderKeyDistributionMessage.prototype.iteration = null;
        SenderKeyDistributionMessage.prototype.chainKey = null;
        SenderKeyDistributionMessage.prototype.signingKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyDistributionMessage.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyDistributionMessage.prototype, "_iteration", {
            get: $util.oneOfGetter($oneOfFields = ["iteration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyDistributionMessage.prototype, "_chainKey", {
            get: $util.oneOfGetter($oneOfFields = ["chainKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyDistributionMessage.prototype, "_signingKey", {
            get: $util.oneOfGetter($oneOfFields = ["signingKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SenderKeyDistributionMessage.create = function(properties) {
            return new SenderKeyDistributionMessage(properties);
        };

        SenderKeyDistributionMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(8).uint32(m.id);
            if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration"))
                w.uint32(16).uint32(m.iteration);
            if (m.chainKey != null && $Object.hasOwnProperty.call(m, "chainKey"))
                w.uint32(26).bytes(m.chainKey);
            if (m.signingKey != null && $Object.hasOwnProperty.call(m, "signingKey"))
                w.uint32(34).bytes(m.signingKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SenderKeyDistributionMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyDistributionMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.id = r.uint32();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.iteration = r.uint32();
                        m._iteration = "iteration";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.chainKey = r.bytes();
                        m._chainKey = "chainKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.signingKey = r.bytes();
                        m._signingKey = "signingKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SenderKeyDistributionMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.SenderKeyDistributionMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SenderKeyDistributionMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SenderKeyDistributionMessage();
            if (d.id != null) {
                m.id = d.id >>> 0;
            }
            if (d.iteration != null) {
                m.iteration = d.iteration >>> 0;
            }
            if (d.chainKey != null) {
                if (typeof d.chainKey === "string")
                    $util.base64.decode(d.chainKey, m.chainKey = $util.newBuffer($util.base64.length(d.chainKey)), 0);
                else if (d.chainKey.length >= 0)
                    m.chainKey = d.chainKey;
            }
            if (d.signingKey != null) {
                if (typeof d.signingKey === "string")
                    $util.base64.decode(d.signingKey, m.signingKey = $util.newBuffer($util.base64.length(d.signingKey)), 0);
                else if (d.signingKey.length >= 0)
                    m.signingKey = d.signingKey;
            }
            return m;
        };

        SenderKeyDistributionMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration")) {
                d.iteration = m.iteration;
            }
            if (m.chainKey != null && $Object.hasOwnProperty.call(m, "chainKey")) {
                d.chainKey = o.bytes === $String ? $util.base64.encode(m.chainKey, 0, m.chainKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.chainKey) : m.chainKey;
            }
            if (m.signingKey != null && $Object.hasOwnProperty.call(m, "signingKey")) {
                d.signingKey = o.bytes === $String ? $util.base64.encode(m.signingKey, 0, m.signingKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signingKey) : m.signingKey;
            }
            return d;
        };

        SenderKeyDistributionMessage.prototype.toJSON = function() {
            return SenderKeyDistributionMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        SenderKeyDistributionMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SenderKeyDistributionMessage";
        };

        return SenderKeyDistributionMessage;
    })();

    proto.SenderKeyMessage = (function() {

        const SenderKeyMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SenderKeyMessage.prototype.id = null;
        SenderKeyMessage.prototype.iteration = null;
        SenderKeyMessage.prototype.ciphertext = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyMessage.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyMessage.prototype, "_iteration", {
            get: $util.oneOfGetter($oneOfFields = ["iteration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyMessage.prototype, "_ciphertext", {
            get: $util.oneOfGetter($oneOfFields = ["ciphertext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SenderKeyMessage.create = function(properties) {
            return new SenderKeyMessage(properties);
        };

        SenderKeyMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(8).uint32(m.id);
            if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration"))
                w.uint32(16).uint32(m.iteration);
            if (m.ciphertext != null && $Object.hasOwnProperty.call(m, "ciphertext"))
                w.uint32(26).bytes(m.ciphertext);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SenderKeyMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.id = r.uint32();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.iteration = r.uint32();
                        m._iteration = "iteration";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.ciphertext = r.bytes();
                        m._ciphertext = "ciphertext";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SenderKeyMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.SenderKeyMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SenderKeyMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SenderKeyMessage();
            if (d.id != null) {
                m.id = d.id >>> 0;
            }
            if (d.iteration != null) {
                m.iteration = d.iteration >>> 0;
            }
            if (d.ciphertext != null) {
                if (typeof d.ciphertext === "string")
                    $util.base64.decode(d.ciphertext, m.ciphertext = $util.newBuffer($util.base64.length(d.ciphertext)), 0);
                else if (d.ciphertext.length >= 0)
                    m.ciphertext = d.ciphertext;
            }
            return m;
        };

        SenderKeyMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration")) {
                d.iteration = m.iteration;
            }
            if (m.ciphertext != null && $Object.hasOwnProperty.call(m, "ciphertext")) {
                d.ciphertext = o.bytes === $String ? $util.base64.encode(m.ciphertext, 0, m.ciphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ciphertext) : m.ciphertext;
            }
            return d;
        };

        SenderKeyMessage.prototype.toJSON = function() {
            return SenderKeyMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        SenderKeyMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SenderKeyMessage";
        };

        return SenderKeyMessage;
    })();

    proto.SenderKeyRecordStructure = (function() {

        const SenderKeyRecordStructure = function (p) {
            this.senderKeyStates = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SenderKeyRecordStructure.prototype.senderKeyStates = $util.emptyArray;

        SenderKeyRecordStructure.create = function(properties) {
            return new SenderKeyRecordStructure(properties);
        };

        SenderKeyRecordStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.senderKeyStates != null && m.senderKeyStates.length) {
                for (var i = 0; i < m.senderKeyStates.length; ++i)
                    $root.proto.SenderKeyStateStructure.encode(m.senderKeyStates[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SenderKeyRecordStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyRecordStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.senderKeyStates && m.senderKeyStates.length))
                            m.senderKeyStates = [];
                        m.senderKeyStates.push($root.proto.SenderKeyStateStructure.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SenderKeyRecordStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.SenderKeyRecordStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SenderKeyRecordStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SenderKeyRecordStructure();
            if (d.senderKeyStates) {
                if (!$Array.isArray(d.senderKeyStates))
                    throw $TypeError(".proto.SenderKeyRecordStructure.senderKeyStates: array expected");
                m.senderKeyStates = $Array(d.senderKeyStates.length);
                for (var i = 0; i < d.senderKeyStates.length; ++i) {
                    if (!$util.isObject(d.senderKeyStates[i]))
                        throw $TypeError(".proto.SenderKeyRecordStructure.senderKeyStates: object expected");
                    m.senderKeyStates[i] = $root.proto.SenderKeyStateStructure.fromObject(d.senderKeyStates[i], q + 1);
                }
            }
            return m;
        };

        SenderKeyRecordStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.senderKeyStates = [];
            }
            if (m.senderKeyStates && m.senderKeyStates.length) {
                d.senderKeyStates = $Array(m.senderKeyStates.length);
                for (var j = 0; j < m.senderKeyStates.length; ++j) {
                    d.senderKeyStates[j] = $root.proto.SenderKeyStateStructure.toObject(m.senderKeyStates[j], o, q + 1);
                }
            }
            return d;
        };

        SenderKeyRecordStructure.prototype.toJSON = function() {
            return SenderKeyRecordStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        SenderKeyRecordStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SenderKeyRecordStructure";
        };

        return SenderKeyRecordStructure;
    })();

    proto.SenderKeyStateStructure = (function() {

        const SenderKeyStateStructure = function (p) {
            this.senderMessageKeys = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SenderKeyStateStructure.prototype.senderKeyId = null;
        SenderKeyStateStructure.prototype.senderChainKey = null;
        SenderKeyStateStructure.prototype.senderSigningKey = null;
        SenderKeyStateStructure.prototype.senderMessageKeys = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyStateStructure.prototype, "_senderKeyId", {
            get: $util.oneOfGetter($oneOfFields = ["senderKeyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyStateStructure.prototype, "_senderChainKey", {
            get: $util.oneOfGetter($oneOfFields = ["senderChainKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SenderKeyStateStructure.prototype, "_senderSigningKey", {
            get: $util.oneOfGetter($oneOfFields = ["senderSigningKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SenderKeyStateStructure.create = function(properties) {
            return new SenderKeyStateStructure(properties);
        };

        SenderKeyStateStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.senderKeyId != null && $Object.hasOwnProperty.call(m, "senderKeyId"))
                w.uint32(8).uint32(m.senderKeyId);
            if (m.senderChainKey != null && $Object.hasOwnProperty.call(m, "senderChainKey"))
                $root.proto.SenderKeyStateStructure.SenderChainKey.encode(m.senderChainKey, w.uint32(18).fork(), q + 1).ldelim();
            if (m.senderSigningKey != null && $Object.hasOwnProperty.call(m, "senderSigningKey"))
                $root.proto.SenderKeyStateStructure.SenderSigningKey.encode(m.senderSigningKey, w.uint32(26).fork(), q + 1).ldelim();
            if (m.senderMessageKeys != null && m.senderMessageKeys.length) {
                for (var i = 0; i < m.senderMessageKeys.length; ++i)
                    $root.proto.SenderKeyStateStructure.SenderMessageKey.encode(m.senderMessageKeys[i], w.uint32(34).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SenderKeyStateStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyStateStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.senderKeyId = r.uint32();
                        m._senderKeyId = "senderKeyId";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.senderChainKey = $root.proto.SenderKeyStateStructure.SenderChainKey.decode(r, r.uint32(), $undefined, q + 1, m.senderChainKey);
                        m._senderChainKey = "senderChainKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.senderSigningKey = $root.proto.SenderKeyStateStructure.SenderSigningKey.decode(r, r.uint32(), $undefined, q + 1, m.senderSigningKey);
                        m._senderSigningKey = "senderSigningKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        if (!(m.senderMessageKeys && m.senderMessageKeys.length))
                            m.senderMessageKeys = [];
                        m.senderMessageKeys.push($root.proto.SenderKeyStateStructure.SenderMessageKey.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SenderKeyStateStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.SenderKeyStateStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SenderKeyStateStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SenderKeyStateStructure();
            if (d.senderKeyId != null) {
                m.senderKeyId = d.senderKeyId >>> 0;
            }
            if (d.senderChainKey != null) {
                if (!$util.isObject(d.senderChainKey))
                    throw $TypeError(".proto.SenderKeyStateStructure.senderChainKey: object expected");
                m.senderChainKey = $root.proto.SenderKeyStateStructure.SenderChainKey.fromObject(d.senderChainKey, q + 1);
            }
            if (d.senderSigningKey != null) {
                if (!$util.isObject(d.senderSigningKey))
                    throw $TypeError(".proto.SenderKeyStateStructure.senderSigningKey: object expected");
                m.senderSigningKey = $root.proto.SenderKeyStateStructure.SenderSigningKey.fromObject(d.senderSigningKey, q + 1);
            }
            if (d.senderMessageKeys) {
                if (!$Array.isArray(d.senderMessageKeys))
                    throw $TypeError(".proto.SenderKeyStateStructure.senderMessageKeys: array expected");
                m.senderMessageKeys = $Array(d.senderMessageKeys.length);
                for (var i = 0; i < d.senderMessageKeys.length; ++i) {
                    if (!$util.isObject(d.senderMessageKeys[i]))
                        throw $TypeError(".proto.SenderKeyStateStructure.senderMessageKeys: object expected");
                    m.senderMessageKeys[i] = $root.proto.SenderKeyStateStructure.SenderMessageKey.fromObject(d.senderMessageKeys[i], q + 1);
                }
            }
            return m;
        };

        SenderKeyStateStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.senderMessageKeys = [];
            }
            if (m.senderKeyId != null && $Object.hasOwnProperty.call(m, "senderKeyId")) {
                d.senderKeyId = m.senderKeyId;
            }
            if (m.senderChainKey != null && $Object.hasOwnProperty.call(m, "senderChainKey")) {
                d.senderChainKey = $root.proto.SenderKeyStateStructure.SenderChainKey.toObject(m.senderChainKey, o, q + 1);
            }
            if (m.senderSigningKey != null && $Object.hasOwnProperty.call(m, "senderSigningKey")) {
                d.senderSigningKey = $root.proto.SenderKeyStateStructure.SenderSigningKey.toObject(m.senderSigningKey, o, q + 1);
            }
            if (m.senderMessageKeys && m.senderMessageKeys.length) {
                d.senderMessageKeys = $Array(m.senderMessageKeys.length);
                for (var j = 0; j < m.senderMessageKeys.length; ++j) {
                    d.senderMessageKeys[j] = $root.proto.SenderKeyStateStructure.SenderMessageKey.toObject(m.senderMessageKeys[j], o, q + 1);
                }
            }
            return d;
        };

        SenderKeyStateStructure.prototype.toJSON = function() {
            return SenderKeyStateStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        SenderKeyStateStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SenderKeyStateStructure";
        };

        SenderKeyStateStructure.SenderChainKey = (function() {

            const SenderChainKey = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SenderChainKey.prototype.iteration = null;
            SenderChainKey.prototype.seed = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderChainKey.prototype, "_iteration", {
                get: $util.oneOfGetter($oneOfFields = ["iteration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderChainKey.prototype, "_seed", {
                get: $util.oneOfGetter($oneOfFields = ["seed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SenderChainKey.create = function(properties) {
                return new SenderChainKey(properties);
            };

            SenderChainKey.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration"))
                    w.uint32(8).uint32(m.iteration);
                if (m.seed != null && $Object.hasOwnProperty.call(m, "seed"))
                    w.uint32(18).bytes(m.seed);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SenderChainKey.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyStateStructure.SenderChainKey();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.iteration = r.uint32();
                            m._iteration = "iteration";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.seed = r.bytes();
                            m._seed = "seed";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SenderChainKey.fromObject = function (d, q) {
                if (d instanceof $root.proto.SenderKeyStateStructure.SenderChainKey)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SenderKeyStateStructure.SenderChainKey: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SenderKeyStateStructure.SenderChainKey();
                if (d.iteration != null) {
                    m.iteration = d.iteration >>> 0;
                }
                if (d.seed != null) {
                    if (typeof d.seed === "string")
                        $util.base64.decode(d.seed, m.seed = $util.newBuffer($util.base64.length(d.seed)), 0);
                    else if (d.seed.length >= 0)
                        m.seed = d.seed;
                }
                return m;
            };

            SenderChainKey.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration")) {
                    d.iteration = m.iteration;
                }
                if (m.seed != null && $Object.hasOwnProperty.call(m, "seed")) {
                    d.seed = o.bytes === $String ? $util.base64.encode(m.seed, 0, m.seed.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.seed) : m.seed;
                }
                return d;
            };

            SenderChainKey.prototype.toJSON = function() {
                return SenderChainKey.toObject(this, $protobuf.util.toJSONOptions);
            };

            SenderChainKey.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SenderKeyStateStructure.SenderChainKey";
            };

            return SenderChainKey;
        })();

        SenderKeyStateStructure.SenderMessageKey = (function() {

            const SenderMessageKey = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SenderMessageKey.prototype.iteration = null;
            SenderMessageKey.prototype.seed = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderMessageKey.prototype, "_iteration", {
                get: $util.oneOfGetter($oneOfFields = ["iteration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderMessageKey.prototype, "_seed", {
                get: $util.oneOfGetter($oneOfFields = ["seed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SenderMessageKey.create = function(properties) {
                return new SenderMessageKey(properties);
            };

            SenderMessageKey.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration"))
                    w.uint32(8).uint32(m.iteration);
                if (m.seed != null && $Object.hasOwnProperty.call(m, "seed"))
                    w.uint32(18).bytes(m.seed);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SenderMessageKey.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyStateStructure.SenderMessageKey();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.iteration = r.uint32();
                            m._iteration = "iteration";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.seed = r.bytes();
                            m._seed = "seed";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SenderMessageKey.fromObject = function (d, q) {
                if (d instanceof $root.proto.SenderKeyStateStructure.SenderMessageKey)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SenderKeyStateStructure.SenderMessageKey: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SenderKeyStateStructure.SenderMessageKey();
                if (d.iteration != null) {
                    m.iteration = d.iteration >>> 0;
                }
                if (d.seed != null) {
                    if (typeof d.seed === "string")
                        $util.base64.decode(d.seed, m.seed = $util.newBuffer($util.base64.length(d.seed)), 0);
                    else if (d.seed.length >= 0)
                        m.seed = d.seed;
                }
                return m;
            };

            SenderMessageKey.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.iteration != null && $Object.hasOwnProperty.call(m, "iteration")) {
                    d.iteration = m.iteration;
                }
                if (m.seed != null && $Object.hasOwnProperty.call(m, "seed")) {
                    d.seed = o.bytes === $String ? $util.base64.encode(m.seed, 0, m.seed.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.seed) : m.seed;
                }
                return d;
            };

            SenderMessageKey.prototype.toJSON = function() {
                return SenderMessageKey.toObject(this, $protobuf.util.toJSONOptions);
            };

            SenderMessageKey.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SenderKeyStateStructure.SenderMessageKey";
            };

            return SenderMessageKey;
        })();

        SenderKeyStateStructure.SenderSigningKey = (function() {

            const SenderSigningKey = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SenderSigningKey.prototype["public"] = null;
            SenderSigningKey.prototype["private"] = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderSigningKey.prototype, "_public", {
                get: $util.oneOfGetter($oneOfFields = ["public"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SenderSigningKey.prototype, "_private", {
                get: $util.oneOfGetter($oneOfFields = ["private"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SenderSigningKey.create = function(properties) {
                return new SenderSigningKey(properties);
            };

            SenderSigningKey.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m["public"] != null && $Object.hasOwnProperty.call(m, "public"))
                    w.uint32(10).bytes(m["public"]);
                if (m["private"] != null && $Object.hasOwnProperty.call(m, "private"))
                    w.uint32(18).bytes(m["private"]);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SenderSigningKey.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SenderKeyStateStructure.SenderSigningKey();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m["public"] = r.bytes();
                            m._public = "public";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m["private"] = r.bytes();
                            m._private = "private";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SenderSigningKey.fromObject = function (d, q) {
                if (d instanceof $root.proto.SenderKeyStateStructure.SenderSigningKey)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SenderKeyStateStructure.SenderSigningKey: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SenderKeyStateStructure.SenderSigningKey();
                if (d["public"] != null) {
                    if (typeof d["public"] === "string")
                        $util.base64.decode(d["public"], m["public"] = $util.newBuffer($util.base64.length(d["public"])), 0);
                    else if (d["public"].length >= 0)
                        m["public"] = d["public"];
                }
                if (d["private"] != null) {
                    if (typeof d["private"] === "string")
                        $util.base64.decode(d["private"], m["private"] = $util.newBuffer($util.base64.length(d["private"])), 0);
                    else if (d["private"].length >= 0)
                        m["private"] = d["private"];
                }
                return m;
            };

            SenderSigningKey.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m["public"] != null && $Object.hasOwnProperty.call(m, "public")) {
                    d["public"] = o.bytes === $String ? $util.base64.encode(m["public"], 0, m["public"].length) : o.bytes === $Array ? $Array.prototype.slice.call(m["public"]) : m["public"];
                }
                if (m["private"] != null && $Object.hasOwnProperty.call(m, "private")) {
                    d["private"] = o.bytes === $String ? $util.base64.encode(m["private"], 0, m["private"].length) : o.bytes === $Array ? $Array.prototype.slice.call(m["private"]) : m["private"];
                }
                return d;
            };

            SenderSigningKey.prototype.toJSON = function() {
                return SenderSigningKey.toObject(this, $protobuf.util.toJSONOptions);
            };

            SenderSigningKey.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SenderKeyStateStructure.SenderSigningKey";
            };

            return SenderSigningKey;
        })();

        return SenderKeyStateStructure;
    })();

    proto.ServerErrorReceipt = (function() {

        const ServerErrorReceipt = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ServerErrorReceipt.prototype.stanzaId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ServerErrorReceipt.prototype, "_stanzaId", {
            get: $util.oneOfGetter($oneOfFields = ["stanzaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ServerErrorReceipt.create = function(properties) {
            return new ServerErrorReceipt(properties);
        };

        ServerErrorReceipt.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId"))
                w.uint32(10).string(m.stanzaId);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ServerErrorReceipt.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ServerErrorReceipt();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.stanzaId = r.stringVerify();
                        m._stanzaId = "stanzaId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ServerErrorReceipt.fromObject = function (d, q) {
            if (d instanceof $root.proto.ServerErrorReceipt)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ServerErrorReceipt: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ServerErrorReceipt();
            if (d.stanzaId != null) {
                m.stanzaId = $String(d.stanzaId);
            }
            return m;
        };

        ServerErrorReceipt.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.stanzaId != null && $Object.hasOwnProperty.call(m, "stanzaId")) {
                d.stanzaId = m.stanzaId;
            }
            return d;
        };

        ServerErrorReceipt.prototype.toJSON = function() {
            return ServerErrorReceipt.toObject(this, $protobuf.util.toJSONOptions);
        };

        ServerErrorReceipt.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ServerErrorReceipt";
        };

        return ServerErrorReceipt;
    })();

    proto.SessionStructure = (function() {

        const SessionStructure = function (p) {
            this.receiverChains = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SessionStructure.prototype.sessionVersion = null;
        SessionStructure.prototype.localIdentityPublic = null;
        SessionStructure.prototype.remoteIdentityPublic = null;
        SessionStructure.prototype.rootKey = null;
        SessionStructure.prototype.previousCounter = null;
        SessionStructure.prototype.senderChain = null;
        SessionStructure.prototype.receiverChains = $util.emptyArray;
        SessionStructure.prototype.pendingKeyExchange = null;
        SessionStructure.prototype.pendingPreKey = null;
        SessionStructure.prototype.remoteRegistrationId = null;
        SessionStructure.prototype.localRegistrationId = null;
        SessionStructure.prototype.needsRefresh = null;
        SessionStructure.prototype.aliceBaseKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_sessionVersion", {
            get: $util.oneOfGetter($oneOfFields = ["sessionVersion"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_localIdentityPublic", {
            get: $util.oneOfGetter($oneOfFields = ["localIdentityPublic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_remoteIdentityPublic", {
            get: $util.oneOfGetter($oneOfFields = ["remoteIdentityPublic"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_rootKey", {
            get: $util.oneOfGetter($oneOfFields = ["rootKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_previousCounter", {
            get: $util.oneOfGetter($oneOfFields = ["previousCounter"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_senderChain", {
            get: $util.oneOfGetter($oneOfFields = ["senderChain"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_pendingKeyExchange", {
            get: $util.oneOfGetter($oneOfFields = ["pendingKeyExchange"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_pendingPreKey", {
            get: $util.oneOfGetter($oneOfFields = ["pendingPreKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_remoteRegistrationId", {
            get: $util.oneOfGetter($oneOfFields = ["remoteRegistrationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_localRegistrationId", {
            get: $util.oneOfGetter($oneOfFields = ["localRegistrationId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_needsRefresh", {
            get: $util.oneOfGetter($oneOfFields = ["needsRefresh"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionStructure.prototype, "_aliceBaseKey", {
            get: $util.oneOfGetter($oneOfFields = ["aliceBaseKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SessionStructure.create = function(properties) {
            return new SessionStructure(properties);
        };

        SessionStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.sessionVersion != null && $Object.hasOwnProperty.call(m, "sessionVersion"))
                w.uint32(8).uint32(m.sessionVersion);
            if (m.localIdentityPublic != null && $Object.hasOwnProperty.call(m, "localIdentityPublic"))
                w.uint32(18).bytes(m.localIdentityPublic);
            if (m.remoteIdentityPublic != null && $Object.hasOwnProperty.call(m, "remoteIdentityPublic"))
                w.uint32(26).bytes(m.remoteIdentityPublic);
            if (m.rootKey != null && $Object.hasOwnProperty.call(m, "rootKey"))
                w.uint32(34).bytes(m.rootKey);
            if (m.previousCounter != null && $Object.hasOwnProperty.call(m, "previousCounter"))
                w.uint32(40).uint32(m.previousCounter);
            if (m.senderChain != null && $Object.hasOwnProperty.call(m, "senderChain"))
                $root.proto.SessionStructure.Chain.encode(m.senderChain, w.uint32(50).fork(), q + 1).ldelim();
            if (m.receiverChains != null && m.receiverChains.length) {
                for (var i = 0; i < m.receiverChains.length; ++i)
                    $root.proto.SessionStructure.Chain.encode(m.receiverChains[i], w.uint32(58).fork(), q + 1).ldelim();
            }
            if (m.pendingKeyExchange != null && $Object.hasOwnProperty.call(m, "pendingKeyExchange"))
                $root.proto.SessionStructure.PendingKeyExchange.encode(m.pendingKeyExchange, w.uint32(66).fork(), q + 1).ldelim();
            if (m.pendingPreKey != null && $Object.hasOwnProperty.call(m, "pendingPreKey"))
                $root.proto.SessionStructure.PendingPreKey.encode(m.pendingPreKey, w.uint32(74).fork(), q + 1).ldelim();
            if (m.remoteRegistrationId != null && $Object.hasOwnProperty.call(m, "remoteRegistrationId"))
                w.uint32(80).uint32(m.remoteRegistrationId);
            if (m.localRegistrationId != null && $Object.hasOwnProperty.call(m, "localRegistrationId"))
                w.uint32(88).uint32(m.localRegistrationId);
            if (m.needsRefresh != null && $Object.hasOwnProperty.call(m, "needsRefresh"))
                w.uint32(96).bool(m.needsRefresh);
            if (m.aliceBaseKey != null && $Object.hasOwnProperty.call(m, "aliceBaseKey"))
                w.uint32(106).bytes(m.aliceBaseKey);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SessionStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.sessionVersion = r.uint32();
                        m._sessionVersion = "sessionVersion";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.localIdentityPublic = r.bytes();
                        m._localIdentityPublic = "localIdentityPublic";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.remoteIdentityPublic = r.bytes();
                        m._remoteIdentityPublic = "remoteIdentityPublic";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.rootKey = r.bytes();
                        m._rootKey = "rootKey";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.previousCounter = r.uint32();
                        m._previousCounter = "previousCounter";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.senderChain = $root.proto.SessionStructure.Chain.decode(r, r.uint32(), $undefined, q + 1, m.senderChain);
                        m._senderChain = "senderChain";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        if (!(m.receiverChains && m.receiverChains.length))
                            m.receiverChains = [];
                        m.receiverChains.push($root.proto.SessionStructure.Chain.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.pendingKeyExchange = $root.proto.SessionStructure.PendingKeyExchange.decode(r, r.uint32(), $undefined, q + 1, m.pendingKeyExchange);
                        m._pendingKeyExchange = "pendingKeyExchange";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.pendingPreKey = $root.proto.SessionStructure.PendingPreKey.decode(r, r.uint32(), $undefined, q + 1, m.pendingPreKey);
                        m._pendingPreKey = "pendingPreKey";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.remoteRegistrationId = r.uint32();
                        m._remoteRegistrationId = "remoteRegistrationId";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.localRegistrationId = r.uint32();
                        m._localRegistrationId = "localRegistrationId";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.needsRefresh = r.bool();
                        m._needsRefresh = "needsRefresh";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.aliceBaseKey = r.bytes();
                        m._aliceBaseKey = "aliceBaseKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SessionStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.SessionStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SessionStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SessionStructure();
            if (d.sessionVersion != null) {
                m.sessionVersion = d.sessionVersion >>> 0;
            }
            if (d.localIdentityPublic != null) {
                if (typeof d.localIdentityPublic === "string")
                    $util.base64.decode(d.localIdentityPublic, m.localIdentityPublic = $util.newBuffer($util.base64.length(d.localIdentityPublic)), 0);
                else if (d.localIdentityPublic.length >= 0)
                    m.localIdentityPublic = d.localIdentityPublic;
            }
            if (d.remoteIdentityPublic != null) {
                if (typeof d.remoteIdentityPublic === "string")
                    $util.base64.decode(d.remoteIdentityPublic, m.remoteIdentityPublic = $util.newBuffer($util.base64.length(d.remoteIdentityPublic)), 0);
                else if (d.remoteIdentityPublic.length >= 0)
                    m.remoteIdentityPublic = d.remoteIdentityPublic;
            }
            if (d.rootKey != null) {
                if (typeof d.rootKey === "string")
                    $util.base64.decode(d.rootKey, m.rootKey = $util.newBuffer($util.base64.length(d.rootKey)), 0);
                else if (d.rootKey.length >= 0)
                    m.rootKey = d.rootKey;
            }
            if (d.previousCounter != null) {
                m.previousCounter = d.previousCounter >>> 0;
            }
            if (d.senderChain != null) {
                if (!$util.isObject(d.senderChain))
                    throw $TypeError(".proto.SessionStructure.senderChain: object expected");
                m.senderChain = $root.proto.SessionStructure.Chain.fromObject(d.senderChain, q + 1);
            }
            if (d.receiverChains) {
                if (!$Array.isArray(d.receiverChains))
                    throw $TypeError(".proto.SessionStructure.receiverChains: array expected");
                m.receiverChains = $Array(d.receiverChains.length);
                for (var i = 0; i < d.receiverChains.length; ++i) {
                    if (!$util.isObject(d.receiverChains[i]))
                        throw $TypeError(".proto.SessionStructure.receiverChains: object expected");
                    m.receiverChains[i] = $root.proto.SessionStructure.Chain.fromObject(d.receiverChains[i], q + 1);
                }
            }
            if (d.pendingKeyExchange != null) {
                if (!$util.isObject(d.pendingKeyExchange))
                    throw $TypeError(".proto.SessionStructure.pendingKeyExchange: object expected");
                m.pendingKeyExchange = $root.proto.SessionStructure.PendingKeyExchange.fromObject(d.pendingKeyExchange, q + 1);
            }
            if (d.pendingPreKey != null) {
                if (!$util.isObject(d.pendingPreKey))
                    throw $TypeError(".proto.SessionStructure.pendingPreKey: object expected");
                m.pendingPreKey = $root.proto.SessionStructure.PendingPreKey.fromObject(d.pendingPreKey, q + 1);
            }
            if (d.remoteRegistrationId != null) {
                m.remoteRegistrationId = d.remoteRegistrationId >>> 0;
            }
            if (d.localRegistrationId != null) {
                m.localRegistrationId = d.localRegistrationId >>> 0;
            }
            if (d.needsRefresh != null) {
                m.needsRefresh = $Boolean(d.needsRefresh);
            }
            if (d.aliceBaseKey != null) {
                if (typeof d.aliceBaseKey === "string")
                    $util.base64.decode(d.aliceBaseKey, m.aliceBaseKey = $util.newBuffer($util.base64.length(d.aliceBaseKey)), 0);
                else if (d.aliceBaseKey.length >= 0)
                    m.aliceBaseKey = d.aliceBaseKey;
            }
            return m;
        };

        SessionStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.receiverChains = [];
            }
            if (m.sessionVersion != null && $Object.hasOwnProperty.call(m, "sessionVersion")) {
                d.sessionVersion = m.sessionVersion;
            }
            if (m.localIdentityPublic != null && $Object.hasOwnProperty.call(m, "localIdentityPublic")) {
                d.localIdentityPublic = o.bytes === $String ? $util.base64.encode(m.localIdentityPublic, 0, m.localIdentityPublic.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localIdentityPublic) : m.localIdentityPublic;
            }
            if (m.remoteIdentityPublic != null && $Object.hasOwnProperty.call(m, "remoteIdentityPublic")) {
                d.remoteIdentityPublic = o.bytes === $String ? $util.base64.encode(m.remoteIdentityPublic, 0, m.remoteIdentityPublic.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.remoteIdentityPublic) : m.remoteIdentityPublic;
            }
            if (m.rootKey != null && $Object.hasOwnProperty.call(m, "rootKey")) {
                d.rootKey = o.bytes === $String ? $util.base64.encode(m.rootKey, 0, m.rootKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.rootKey) : m.rootKey;
            }
            if (m.previousCounter != null && $Object.hasOwnProperty.call(m, "previousCounter")) {
                d.previousCounter = m.previousCounter;
            }
            if (m.senderChain != null && $Object.hasOwnProperty.call(m, "senderChain")) {
                d.senderChain = $root.proto.SessionStructure.Chain.toObject(m.senderChain, o, q + 1);
            }
            if (m.receiverChains && m.receiverChains.length) {
                d.receiverChains = $Array(m.receiverChains.length);
                for (var j = 0; j < m.receiverChains.length; ++j) {
                    d.receiverChains[j] = $root.proto.SessionStructure.Chain.toObject(m.receiverChains[j], o, q + 1);
                }
            }
            if (m.pendingKeyExchange != null && $Object.hasOwnProperty.call(m, "pendingKeyExchange")) {
                d.pendingKeyExchange = $root.proto.SessionStructure.PendingKeyExchange.toObject(m.pendingKeyExchange, o, q + 1);
            }
            if (m.pendingPreKey != null && $Object.hasOwnProperty.call(m, "pendingPreKey")) {
                d.pendingPreKey = $root.proto.SessionStructure.PendingPreKey.toObject(m.pendingPreKey, o, q + 1);
            }
            if (m.remoteRegistrationId != null && $Object.hasOwnProperty.call(m, "remoteRegistrationId")) {
                d.remoteRegistrationId = m.remoteRegistrationId;
            }
            if (m.localRegistrationId != null && $Object.hasOwnProperty.call(m, "localRegistrationId")) {
                d.localRegistrationId = m.localRegistrationId;
            }
            if (m.needsRefresh != null && $Object.hasOwnProperty.call(m, "needsRefresh")) {
                d.needsRefresh = m.needsRefresh;
            }
            if (m.aliceBaseKey != null && $Object.hasOwnProperty.call(m, "aliceBaseKey")) {
                d.aliceBaseKey = o.bytes === $String ? $util.base64.encode(m.aliceBaseKey, 0, m.aliceBaseKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.aliceBaseKey) : m.aliceBaseKey;
            }
            return d;
        };

        SessionStructure.prototype.toJSON = function() {
            return SessionStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        SessionStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SessionStructure";
        };

        SessionStructure.Chain = (function() {

            const Chain = function (p) {
                this.messageKeys = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Chain.prototype.senderRatchetKey = null;
            Chain.prototype.senderRatchetKeyPrivate = null;
            Chain.prototype.chainKey = null;
            Chain.prototype.messageKeys = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Chain.prototype, "_senderRatchetKey", {
                get: $util.oneOfGetter($oneOfFields = ["senderRatchetKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Chain.prototype, "_senderRatchetKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["senderRatchetKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Chain.prototype, "_chainKey", {
                get: $util.oneOfGetter($oneOfFields = ["chainKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Chain.create = function(properties) {
                return new Chain(properties);
            };

            Chain.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.senderRatchetKey != null && $Object.hasOwnProperty.call(m, "senderRatchetKey"))
                    w.uint32(10).bytes(m.senderRatchetKey);
                if (m.senderRatchetKeyPrivate != null && $Object.hasOwnProperty.call(m, "senderRatchetKeyPrivate"))
                    w.uint32(18).bytes(m.senderRatchetKeyPrivate);
                if (m.chainKey != null && $Object.hasOwnProperty.call(m, "chainKey"))
                    $root.proto.SessionStructure.Chain.ChainKey.encode(m.chainKey, w.uint32(26).fork(), q + 1).ldelim();
                if (m.messageKeys != null && m.messageKeys.length) {
                    for (var i = 0; i < m.messageKeys.length; ++i)
                        $root.proto.SessionStructure.Chain.MessageKey.encode(m.messageKeys[i], w.uint32(34).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Chain.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionStructure.Chain();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.senderRatchetKey = r.bytes();
                            m._senderRatchetKey = "senderRatchetKey";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.senderRatchetKeyPrivate = r.bytes();
                            m._senderRatchetKeyPrivate = "senderRatchetKeyPrivate";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.chainKey = $root.proto.SessionStructure.Chain.ChainKey.decode(r, r.uint32(), $undefined, q + 1, m.chainKey);
                            m._chainKey = "chainKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(m.messageKeys && m.messageKeys.length))
                                m.messageKeys = [];
                            m.messageKeys.push($root.proto.SessionStructure.Chain.MessageKey.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Chain.fromObject = function (d, q) {
                if (d instanceof $root.proto.SessionStructure.Chain)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SessionStructure.Chain: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SessionStructure.Chain();
                if (d.senderRatchetKey != null) {
                    if (typeof d.senderRatchetKey === "string")
                        $util.base64.decode(d.senderRatchetKey, m.senderRatchetKey = $util.newBuffer($util.base64.length(d.senderRatchetKey)), 0);
                    else if (d.senderRatchetKey.length >= 0)
                        m.senderRatchetKey = d.senderRatchetKey;
                }
                if (d.senderRatchetKeyPrivate != null) {
                    if (typeof d.senderRatchetKeyPrivate === "string")
                        $util.base64.decode(d.senderRatchetKeyPrivate, m.senderRatchetKeyPrivate = $util.newBuffer($util.base64.length(d.senderRatchetKeyPrivate)), 0);
                    else if (d.senderRatchetKeyPrivate.length >= 0)
                        m.senderRatchetKeyPrivate = d.senderRatchetKeyPrivate;
                }
                if (d.chainKey != null) {
                    if (!$util.isObject(d.chainKey))
                        throw $TypeError(".proto.SessionStructure.Chain.chainKey: object expected");
                    m.chainKey = $root.proto.SessionStructure.Chain.ChainKey.fromObject(d.chainKey, q + 1);
                }
                if (d.messageKeys) {
                    if (!$Array.isArray(d.messageKeys))
                        throw $TypeError(".proto.SessionStructure.Chain.messageKeys: array expected");
                    m.messageKeys = $Array(d.messageKeys.length);
                    for (var i = 0; i < d.messageKeys.length; ++i) {
                        if (!$util.isObject(d.messageKeys[i]))
                            throw $TypeError(".proto.SessionStructure.Chain.messageKeys: object expected");
                        m.messageKeys[i] = $root.proto.SessionStructure.Chain.MessageKey.fromObject(d.messageKeys[i], q + 1);
                    }
                }
                return m;
            };

            Chain.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.messageKeys = [];
                }
                if (m.senderRatchetKey != null && $Object.hasOwnProperty.call(m, "senderRatchetKey")) {
                    d.senderRatchetKey = o.bytes === $String ? $util.base64.encode(m.senderRatchetKey, 0, m.senderRatchetKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.senderRatchetKey) : m.senderRatchetKey;
                }
                if (m.senderRatchetKeyPrivate != null && $Object.hasOwnProperty.call(m, "senderRatchetKeyPrivate")) {
                    d.senderRatchetKeyPrivate = o.bytes === $String ? $util.base64.encode(m.senderRatchetKeyPrivate, 0, m.senderRatchetKeyPrivate.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.senderRatchetKeyPrivate) : m.senderRatchetKeyPrivate;
                }
                if (m.chainKey != null && $Object.hasOwnProperty.call(m, "chainKey")) {
                    d.chainKey = $root.proto.SessionStructure.Chain.ChainKey.toObject(m.chainKey, o, q + 1);
                }
                if (m.messageKeys && m.messageKeys.length) {
                    d.messageKeys = $Array(m.messageKeys.length);
                    for (var j = 0; j < m.messageKeys.length; ++j) {
                        d.messageKeys[j] = $root.proto.SessionStructure.Chain.MessageKey.toObject(m.messageKeys[j], o, q + 1);
                    }
                }
                return d;
            };

            Chain.prototype.toJSON = function() {
                return Chain.toObject(this, $protobuf.util.toJSONOptions);
            };

            Chain.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SessionStructure.Chain";
            };

            Chain.ChainKey = (function() {

                const ChainKey = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                ChainKey.prototype.index = null;
                ChainKey.prototype.key = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ChainKey.prototype, "_index", {
                    get: $util.oneOfGetter($oneOfFields = ["index"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(ChainKey.prototype, "_key", {
                    get: $util.oneOfGetter($oneOfFields = ["key"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                ChainKey.create = function(properties) {
                    return new ChainKey(properties);
                };

                ChainKey.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                        w.uint32(8).uint32(m.index);
                    if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                        w.uint32(18).bytes(m.key);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                ChainKey.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionStructure.Chain.ChainKey();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.index = r.uint32();
                                m._index = "index";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.key = r.bytes();
                                m._key = "key";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                ChainKey.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SessionStructure.Chain.ChainKey)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SessionStructure.Chain.ChainKey: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SessionStructure.Chain.ChainKey();
                    if (d.index != null) {
                        m.index = d.index >>> 0;
                    }
                    if (d.key != null) {
                        if (typeof d.key === "string")
                            $util.base64.decode(d.key, m.key = $util.newBuffer($util.base64.length(d.key)), 0);
                        else if (d.key.length >= 0)
                            m.key = d.key;
                    }
                    return m;
                };

                ChainKey.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                        d.index = m.index;
                    }
                    if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                        d.key = o.bytes === $String ? $util.base64.encode(m.key, 0, m.key.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.key) : m.key;
                    }
                    return d;
                };

                ChainKey.prototype.toJSON = function() {
                    return ChainKey.toObject(this, $protobuf.util.toJSONOptions);
                };

                ChainKey.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SessionStructure.Chain.ChainKey";
                };

                return ChainKey;
            })();

            Chain.MessageKey = (function() {

                const MessageKey = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                MessageKey.prototype.index = null;
                MessageKey.prototype.cipherKey = null;
                MessageKey.prototype.macKey = null;
                MessageKey.prototype.iv = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(MessageKey.prototype, "_index", {
                    get: $util.oneOfGetter($oneOfFields = ["index"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(MessageKey.prototype, "_cipherKey", {
                    get: $util.oneOfGetter($oneOfFields = ["cipherKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(MessageKey.prototype, "_macKey", {
                    get: $util.oneOfGetter($oneOfFields = ["macKey"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(MessageKey.prototype, "_iv", {
                    get: $util.oneOfGetter($oneOfFields = ["iv"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                MessageKey.create = function(properties) {
                    return new MessageKey(properties);
                };

                MessageKey.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                        w.uint32(8).uint32(m.index);
                    if (m.cipherKey != null && $Object.hasOwnProperty.call(m, "cipherKey"))
                        w.uint32(18).bytes(m.cipherKey);
                    if (m.macKey != null && $Object.hasOwnProperty.call(m, "macKey"))
                        w.uint32(26).bytes(m.macKey);
                    if (m.iv != null && $Object.hasOwnProperty.call(m, "iv"))
                        w.uint32(34).bytes(m.iv);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                MessageKey.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionStructure.Chain.MessageKey();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.index = r.uint32();
                                m._index = "index";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.cipherKey = r.bytes();
                                m._cipherKey = "cipherKey";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.macKey = r.bytes();
                                m._macKey = "macKey";
                                continue;
                            }
                        case 4: {
                                if (u !== 2)
                                    break;
                                m.iv = r.bytes();
                                m._iv = "iv";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                MessageKey.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SessionStructure.Chain.MessageKey)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SessionStructure.Chain.MessageKey: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SessionStructure.Chain.MessageKey();
                    if (d.index != null) {
                        m.index = d.index >>> 0;
                    }
                    if (d.cipherKey != null) {
                        if (typeof d.cipherKey === "string")
                            $util.base64.decode(d.cipherKey, m.cipherKey = $util.newBuffer($util.base64.length(d.cipherKey)), 0);
                        else if (d.cipherKey.length >= 0)
                            m.cipherKey = d.cipherKey;
                    }
                    if (d.macKey != null) {
                        if (typeof d.macKey === "string")
                            $util.base64.decode(d.macKey, m.macKey = $util.newBuffer($util.base64.length(d.macKey)), 0);
                        else if (d.macKey.length >= 0)
                            m.macKey = d.macKey;
                    }
                    if (d.iv != null) {
                        if (typeof d.iv === "string")
                            $util.base64.decode(d.iv, m.iv = $util.newBuffer($util.base64.length(d.iv)), 0);
                        else if (d.iv.length >= 0)
                            m.iv = d.iv;
                    }
                    return m;
                };

                MessageKey.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                        d.index = m.index;
                    }
                    if (m.cipherKey != null && $Object.hasOwnProperty.call(m, "cipherKey")) {
                        d.cipherKey = o.bytes === $String ? $util.base64.encode(m.cipherKey, 0, m.cipherKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.cipherKey) : m.cipherKey;
                    }
                    if (m.macKey != null && $Object.hasOwnProperty.call(m, "macKey")) {
                        d.macKey = o.bytes === $String ? $util.base64.encode(m.macKey, 0, m.macKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.macKey) : m.macKey;
                    }
                    if (m.iv != null && $Object.hasOwnProperty.call(m, "iv")) {
                        d.iv = o.bytes === $String ? $util.base64.encode(m.iv, 0, m.iv.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.iv) : m.iv;
                    }
                    return d;
                };

                MessageKey.prototype.toJSON = function() {
                    return MessageKey.toObject(this, $protobuf.util.toJSONOptions);
                };

                MessageKey.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SessionStructure.Chain.MessageKey";
                };

                return MessageKey;
            })();

            return Chain;
        })();

        SessionStructure.PendingKeyExchange = (function() {

            const PendingKeyExchange = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PendingKeyExchange.prototype.sequence = null;
            PendingKeyExchange.prototype.localBaseKey = null;
            PendingKeyExchange.prototype.localBaseKeyPrivate = null;
            PendingKeyExchange.prototype.localRatchetKey = null;
            PendingKeyExchange.prototype.localRatchetKeyPrivate = null;
            PendingKeyExchange.prototype.localIdentityKey = null;
            PendingKeyExchange.prototype.localIdentityKeyPrivate = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_sequence", {
                get: $util.oneOfGetter($oneOfFields = ["sequence"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_localBaseKey", {
                get: $util.oneOfGetter($oneOfFields = ["localBaseKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_localBaseKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["localBaseKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_localRatchetKey", {
                get: $util.oneOfGetter($oneOfFields = ["localRatchetKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_localRatchetKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["localRatchetKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_localIdentityKey", {
                get: $util.oneOfGetter($oneOfFields = ["localIdentityKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingKeyExchange.prototype, "_localIdentityKeyPrivate", {
                get: $util.oneOfGetter($oneOfFields = ["localIdentityKeyPrivate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PendingKeyExchange.create = function(properties) {
                return new PendingKeyExchange(properties);
            };

            PendingKeyExchange.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.sequence != null && $Object.hasOwnProperty.call(m, "sequence"))
                    w.uint32(8).uint32(m.sequence);
                if (m.localBaseKey != null && $Object.hasOwnProperty.call(m, "localBaseKey"))
                    w.uint32(18).bytes(m.localBaseKey);
                if (m.localBaseKeyPrivate != null && $Object.hasOwnProperty.call(m, "localBaseKeyPrivate"))
                    w.uint32(26).bytes(m.localBaseKeyPrivate);
                if (m.localRatchetKey != null && $Object.hasOwnProperty.call(m, "localRatchetKey"))
                    w.uint32(34).bytes(m.localRatchetKey);
                if (m.localRatchetKeyPrivate != null && $Object.hasOwnProperty.call(m, "localRatchetKeyPrivate"))
                    w.uint32(42).bytes(m.localRatchetKeyPrivate);
                if (m.localIdentityKey != null && $Object.hasOwnProperty.call(m, "localIdentityKey"))
                    w.uint32(58).bytes(m.localIdentityKey);
                if (m.localIdentityKeyPrivate != null && $Object.hasOwnProperty.call(m, "localIdentityKeyPrivate"))
                    w.uint32(66).bytes(m.localIdentityKeyPrivate);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PendingKeyExchange.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionStructure.PendingKeyExchange();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.sequence = r.uint32();
                            m._sequence = "sequence";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.localBaseKey = r.bytes();
                            m._localBaseKey = "localBaseKey";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.localBaseKeyPrivate = r.bytes();
                            m._localBaseKeyPrivate = "localBaseKeyPrivate";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.localRatchetKey = r.bytes();
                            m._localRatchetKey = "localRatchetKey";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.localRatchetKeyPrivate = r.bytes();
                            m._localRatchetKeyPrivate = "localRatchetKeyPrivate";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.localIdentityKey = r.bytes();
                            m._localIdentityKey = "localIdentityKey";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            m.localIdentityKeyPrivate = r.bytes();
                            m._localIdentityKeyPrivate = "localIdentityKeyPrivate";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PendingKeyExchange.fromObject = function (d, q) {
                if (d instanceof $root.proto.SessionStructure.PendingKeyExchange)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SessionStructure.PendingKeyExchange: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SessionStructure.PendingKeyExchange();
                if (d.sequence != null) {
                    m.sequence = d.sequence >>> 0;
                }
                if (d.localBaseKey != null) {
                    if (typeof d.localBaseKey === "string")
                        $util.base64.decode(d.localBaseKey, m.localBaseKey = $util.newBuffer($util.base64.length(d.localBaseKey)), 0);
                    else if (d.localBaseKey.length >= 0)
                        m.localBaseKey = d.localBaseKey;
                }
                if (d.localBaseKeyPrivate != null) {
                    if (typeof d.localBaseKeyPrivate === "string")
                        $util.base64.decode(d.localBaseKeyPrivate, m.localBaseKeyPrivate = $util.newBuffer($util.base64.length(d.localBaseKeyPrivate)), 0);
                    else if (d.localBaseKeyPrivate.length >= 0)
                        m.localBaseKeyPrivate = d.localBaseKeyPrivate;
                }
                if (d.localRatchetKey != null) {
                    if (typeof d.localRatchetKey === "string")
                        $util.base64.decode(d.localRatchetKey, m.localRatchetKey = $util.newBuffer($util.base64.length(d.localRatchetKey)), 0);
                    else if (d.localRatchetKey.length >= 0)
                        m.localRatchetKey = d.localRatchetKey;
                }
                if (d.localRatchetKeyPrivate != null) {
                    if (typeof d.localRatchetKeyPrivate === "string")
                        $util.base64.decode(d.localRatchetKeyPrivate, m.localRatchetKeyPrivate = $util.newBuffer($util.base64.length(d.localRatchetKeyPrivate)), 0);
                    else if (d.localRatchetKeyPrivate.length >= 0)
                        m.localRatchetKeyPrivate = d.localRatchetKeyPrivate;
                }
                if (d.localIdentityKey != null) {
                    if (typeof d.localIdentityKey === "string")
                        $util.base64.decode(d.localIdentityKey, m.localIdentityKey = $util.newBuffer($util.base64.length(d.localIdentityKey)), 0);
                    else if (d.localIdentityKey.length >= 0)
                        m.localIdentityKey = d.localIdentityKey;
                }
                if (d.localIdentityKeyPrivate != null) {
                    if (typeof d.localIdentityKeyPrivate === "string")
                        $util.base64.decode(d.localIdentityKeyPrivate, m.localIdentityKeyPrivate = $util.newBuffer($util.base64.length(d.localIdentityKeyPrivate)), 0);
                    else if (d.localIdentityKeyPrivate.length >= 0)
                        m.localIdentityKeyPrivate = d.localIdentityKeyPrivate;
                }
                return m;
            };

            PendingKeyExchange.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.sequence != null && $Object.hasOwnProperty.call(m, "sequence")) {
                    d.sequence = m.sequence;
                }
                if (m.localBaseKey != null && $Object.hasOwnProperty.call(m, "localBaseKey")) {
                    d.localBaseKey = o.bytes === $String ? $util.base64.encode(m.localBaseKey, 0, m.localBaseKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localBaseKey) : m.localBaseKey;
                }
                if (m.localBaseKeyPrivate != null && $Object.hasOwnProperty.call(m, "localBaseKeyPrivate")) {
                    d.localBaseKeyPrivate = o.bytes === $String ? $util.base64.encode(m.localBaseKeyPrivate, 0, m.localBaseKeyPrivate.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localBaseKeyPrivate) : m.localBaseKeyPrivate;
                }
                if (m.localRatchetKey != null && $Object.hasOwnProperty.call(m, "localRatchetKey")) {
                    d.localRatchetKey = o.bytes === $String ? $util.base64.encode(m.localRatchetKey, 0, m.localRatchetKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localRatchetKey) : m.localRatchetKey;
                }
                if (m.localRatchetKeyPrivate != null && $Object.hasOwnProperty.call(m, "localRatchetKeyPrivate")) {
                    d.localRatchetKeyPrivate = o.bytes === $String ? $util.base64.encode(m.localRatchetKeyPrivate, 0, m.localRatchetKeyPrivate.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localRatchetKeyPrivate) : m.localRatchetKeyPrivate;
                }
                if (m.localIdentityKey != null && $Object.hasOwnProperty.call(m, "localIdentityKey")) {
                    d.localIdentityKey = o.bytes === $String ? $util.base64.encode(m.localIdentityKey, 0, m.localIdentityKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localIdentityKey) : m.localIdentityKey;
                }
                if (m.localIdentityKeyPrivate != null && $Object.hasOwnProperty.call(m, "localIdentityKeyPrivate")) {
                    d.localIdentityKeyPrivate = o.bytes === $String ? $util.base64.encode(m.localIdentityKeyPrivate, 0, m.localIdentityKeyPrivate.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.localIdentityKeyPrivate) : m.localIdentityKeyPrivate;
                }
                return d;
            };

            PendingKeyExchange.prototype.toJSON = function() {
                return PendingKeyExchange.toObject(this, $protobuf.util.toJSONOptions);
            };

            PendingKeyExchange.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SessionStructure.PendingKeyExchange";
            };

            return PendingKeyExchange;
        })();

        SessionStructure.PendingPreKey = (function() {

            const PendingPreKey = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PendingPreKey.prototype.preKeyId = null;
            PendingPreKey.prototype.signedPreKeyId = null;
            PendingPreKey.prototype.baseKey = null;
            PendingPreKey.prototype.kyberPreKeyId = null;
            PendingPreKey.prototype.kyberCiphertext = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingPreKey.prototype, "_preKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["preKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingPreKey.prototype, "_signedPreKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["signedPreKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingPreKey.prototype, "_baseKey", {
                get: $util.oneOfGetter($oneOfFields = ["baseKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingPreKey.prototype, "_kyberPreKeyId", {
                get: $util.oneOfGetter($oneOfFields = ["kyberPreKeyId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PendingPreKey.prototype, "_kyberCiphertext", {
                get: $util.oneOfGetter($oneOfFields = ["kyberCiphertext"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PendingPreKey.create = function(properties) {
                return new PendingPreKey(properties);
            };

            PendingPreKey.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.preKeyId != null && $Object.hasOwnProperty.call(m, "preKeyId"))
                    w.uint32(8).uint32(m.preKeyId);
                if (m.baseKey != null && $Object.hasOwnProperty.call(m, "baseKey"))
                    w.uint32(18).bytes(m.baseKey);
                if (m.signedPreKeyId != null && $Object.hasOwnProperty.call(m, "signedPreKeyId"))
                    w.uint32(24).int32(m.signedPreKeyId);
                if (m.kyberPreKeyId != null && $Object.hasOwnProperty.call(m, "kyberPreKeyId"))
                    w.uint32(32).uint32(m.kyberPreKeyId);
                if (m.kyberCiphertext != null && $Object.hasOwnProperty.call(m, "kyberCiphertext"))
                    w.uint32(42).bytes(m.kyberCiphertext);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PendingPreKey.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionStructure.PendingPreKey();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.preKeyId = r.uint32();
                            m._preKeyId = "preKeyId";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.signedPreKeyId = r.int32();
                            m._signedPreKeyId = "signedPreKeyId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.baseKey = r.bytes();
                            m._baseKey = "baseKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.kyberPreKeyId = r.uint32();
                            m._kyberPreKeyId = "kyberPreKeyId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.kyberCiphertext = r.bytes();
                            m._kyberCiphertext = "kyberCiphertext";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PendingPreKey.fromObject = function (d, q) {
                if (d instanceof $root.proto.SessionStructure.PendingPreKey)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SessionStructure.PendingPreKey: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SessionStructure.PendingPreKey();
                if (d.preKeyId != null) {
                    m.preKeyId = d.preKeyId >>> 0;
                }
                if (d.signedPreKeyId != null) {
                    m.signedPreKeyId = d.signedPreKeyId | 0;
                }
                if (d.baseKey != null) {
                    if (typeof d.baseKey === "string")
                        $util.base64.decode(d.baseKey, m.baseKey = $util.newBuffer($util.base64.length(d.baseKey)), 0);
                    else if (d.baseKey.length >= 0)
                        m.baseKey = d.baseKey;
                }
                if (d.kyberPreKeyId != null) {
                    m.kyberPreKeyId = d.kyberPreKeyId >>> 0;
                }
                if (d.kyberCiphertext != null) {
                    if (typeof d.kyberCiphertext === "string")
                        $util.base64.decode(d.kyberCiphertext, m.kyberCiphertext = $util.newBuffer($util.base64.length(d.kyberCiphertext)), 0);
                    else if (d.kyberCiphertext.length >= 0)
                        m.kyberCiphertext = d.kyberCiphertext;
                }
                return m;
            };

            PendingPreKey.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.preKeyId != null && $Object.hasOwnProperty.call(m, "preKeyId")) {
                    d.preKeyId = m.preKeyId;
                }
                if (m.baseKey != null && $Object.hasOwnProperty.call(m, "baseKey")) {
                    d.baseKey = o.bytes === $String ? $util.base64.encode(m.baseKey, 0, m.baseKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.baseKey) : m.baseKey;
                }
                if (m.signedPreKeyId != null && $Object.hasOwnProperty.call(m, "signedPreKeyId")) {
                    d.signedPreKeyId = m.signedPreKeyId;
                }
                if (m.kyberPreKeyId != null && $Object.hasOwnProperty.call(m, "kyberPreKeyId")) {
                    d.kyberPreKeyId = m.kyberPreKeyId;
                }
                if (m.kyberCiphertext != null && $Object.hasOwnProperty.call(m, "kyberCiphertext")) {
                    d.kyberCiphertext = o.bytes === $String ? $util.base64.encode(m.kyberCiphertext, 0, m.kyberCiphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.kyberCiphertext) : m.kyberCiphertext;
                }
                return d;
            };

            PendingPreKey.prototype.toJSON = function() {
                return PendingPreKey.toObject(this, $protobuf.util.toJSONOptions);
            };

            PendingPreKey.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SessionStructure.PendingPreKey";
            };

            return PendingPreKey;
        })();

        return SessionStructure;
    })();

    proto.SessionTransparencyMetadata = (function() {

        const SessionTransparencyMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SessionTransparencyMetadata.prototype.disclaimerText = null;
        SessionTransparencyMetadata.prototype.hcaId = null;
        SessionTransparencyMetadata.prototype.sessionTransparencyType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionTransparencyMetadata.prototype, "_disclaimerText", {
            get: $util.oneOfGetter($oneOfFields = ["disclaimerText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionTransparencyMetadata.prototype, "_hcaId", {
            get: $util.oneOfGetter($oneOfFields = ["hcaId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SessionTransparencyMetadata.prototype, "_sessionTransparencyType", {
            get: $util.oneOfGetter($oneOfFields = ["sessionTransparencyType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SessionTransparencyMetadata.create = function(properties) {
            return new SessionTransparencyMetadata(properties);
        };

        SessionTransparencyMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.disclaimerText != null && $Object.hasOwnProperty.call(m, "disclaimerText"))
                w.uint32(10).string(m.disclaimerText);
            if (m.hcaId != null && $Object.hasOwnProperty.call(m, "hcaId"))
                w.uint32(18).string(m.hcaId);
            if (m.sessionTransparencyType != null && $Object.hasOwnProperty.call(m, "sessionTransparencyType"))
                w.uint32(24).int32(m.sessionTransparencyType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SessionTransparencyMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SessionTransparencyMetadata(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.disclaimerText = r.stringVerify();
                        m._disclaimerText = "disclaimerText";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.hcaId = r.stringVerify();
                        m._hcaId = "hcaId";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.sessionTransparencyType = r.int32();
                        m._sessionTransparencyType = "sessionTransparencyType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SessionTransparencyMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.SessionTransparencyMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SessionTransparencyMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SessionTransparencyMetadata();
            if (d.disclaimerText != null) {
                m.disclaimerText = $String(d.disclaimerText);
            }
            if (d.hcaId != null) {
                m.hcaId = $String(d.hcaId);
            }
            switch (d.sessionTransparencyType) {
            case "UNKNOWN_TYPE":
            case 0:
                m.sessionTransparencyType = 0;
                break;
            case "NY_AI_SAFETY_DISCLAIMER":
            case 1:
                m.sessionTransparencyType = 1;
                break;
            default:
                if (typeof d.sessionTransparencyType === "number" && (d.sessionTransparencyType | 0) === d.sessionTransparencyType)
                    m.sessionTransparencyType = d.sessionTransparencyType;
            }
            return m;
        };

        SessionTransparencyMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.disclaimerText != null && $Object.hasOwnProperty.call(m, "disclaimerText")) {
                d.disclaimerText = m.disclaimerText;
            }
            if (m.hcaId != null && $Object.hasOwnProperty.call(m, "hcaId")) {
                d.hcaId = m.hcaId;
            }
            if (m.sessionTransparencyType != null && $Object.hasOwnProperty.call(m, "sessionTransparencyType")) {
                d.sessionTransparencyType = o.enums === $String ? $root.proto.SessionTransparencyType[m.sessionTransparencyType] === $undefined ? m.sessionTransparencyType : $root.proto.SessionTransparencyType[m.sessionTransparencyType] : m.sessionTransparencyType;
            }
            return d;
        };

        SessionTransparencyMetadata.prototype.toJSON = function() {
            return SessionTransparencyMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        SessionTransparencyMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SessionTransparencyMetadata";
        };

        return SessionTransparencyMetadata;
    })();

    proto.SessionTransparencyType = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "UNKNOWN_TYPE"] = 0;
        values[valuesById[1] = "NY_AI_SAFETY_DISCLAIMER"] = 1;
        return values;
    })();

    proto.SignalMessage = (function() {

        const SignalMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SignalMessage.prototype.ratchetKey = null;
        SignalMessage.prototype.counter = null;
        SignalMessage.prototype.previousCounter = null;
        SignalMessage.prototype.ciphertext = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignalMessage.prototype, "_ratchetKey", {
            get: $util.oneOfGetter($oneOfFields = ["ratchetKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignalMessage.prototype, "_counter", {
            get: $util.oneOfGetter($oneOfFields = ["counter"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignalMessage.prototype, "_previousCounter", {
            get: $util.oneOfGetter($oneOfFields = ["previousCounter"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignalMessage.prototype, "_ciphertext", {
            get: $util.oneOfGetter($oneOfFields = ["ciphertext"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SignalMessage.create = function(properties) {
            return new SignalMessage(properties);
        };

        SignalMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.ratchetKey != null && $Object.hasOwnProperty.call(m, "ratchetKey"))
                w.uint32(10).bytes(m.ratchetKey);
            if (m.counter != null && $Object.hasOwnProperty.call(m, "counter"))
                w.uint32(16).uint32(m.counter);
            if (m.previousCounter != null && $Object.hasOwnProperty.call(m, "previousCounter"))
                w.uint32(24).uint32(m.previousCounter);
            if (m.ciphertext != null && $Object.hasOwnProperty.call(m, "ciphertext"))
                w.uint32(34).bytes(m.ciphertext);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SignalMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SignalMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.ratchetKey = r.bytes();
                        m._ratchetKey = "ratchetKey";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.counter = r.uint32();
                        m._counter = "counter";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.previousCounter = r.uint32();
                        m._previousCounter = "previousCounter";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.ciphertext = r.bytes();
                        m._ciphertext = "ciphertext";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SignalMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.SignalMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SignalMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SignalMessage();
            if (d.ratchetKey != null) {
                if (typeof d.ratchetKey === "string")
                    $util.base64.decode(d.ratchetKey, m.ratchetKey = $util.newBuffer($util.base64.length(d.ratchetKey)), 0);
                else if (d.ratchetKey.length >= 0)
                    m.ratchetKey = d.ratchetKey;
            }
            if (d.counter != null) {
                m.counter = d.counter >>> 0;
            }
            if (d.previousCounter != null) {
                m.previousCounter = d.previousCounter >>> 0;
            }
            if (d.ciphertext != null) {
                if (typeof d.ciphertext === "string")
                    $util.base64.decode(d.ciphertext, m.ciphertext = $util.newBuffer($util.base64.length(d.ciphertext)), 0);
                else if (d.ciphertext.length >= 0)
                    m.ciphertext = d.ciphertext;
            }
            return m;
        };

        SignalMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.ratchetKey != null && $Object.hasOwnProperty.call(m, "ratchetKey")) {
                d.ratchetKey = o.bytes === $String ? $util.base64.encode(m.ratchetKey, 0, m.ratchetKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ratchetKey) : m.ratchetKey;
            }
            if (m.counter != null && $Object.hasOwnProperty.call(m, "counter")) {
                d.counter = m.counter;
            }
            if (m.previousCounter != null && $Object.hasOwnProperty.call(m, "previousCounter")) {
                d.previousCounter = m.previousCounter;
            }
            if (m.ciphertext != null && $Object.hasOwnProperty.call(m, "ciphertext")) {
                d.ciphertext = o.bytes === $String ? $util.base64.encode(m.ciphertext, 0, m.ciphertext.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ciphertext) : m.ciphertext;
            }
            return d;
        };

        SignalMessage.prototype.toJSON = function() {
            return SignalMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        SignalMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignalMessage";
        };

        return SignalMessage;
    })();

    proto.SignedPreKeyRecordStructure = (function() {

        const SignedPreKeyRecordStructure = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SignedPreKeyRecordStructure.prototype.id = null;
        SignedPreKeyRecordStructure.prototype.publicKey = null;
        SignedPreKeyRecordStructure.prototype.privateKey = null;
        SignedPreKeyRecordStructure.prototype.signature = null;
        SignedPreKeyRecordStructure.prototype.timestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_id", {
            get: $util.oneOfGetter($oneOfFields = ["id"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_publicKey", {
            get: $util.oneOfGetter($oneOfFields = ["publicKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_privateKey", {
            get: $util.oneOfGetter($oneOfFields = ["privateKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SignedPreKeyRecordStructure.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SignedPreKeyRecordStructure.create = function(properties) {
            return new SignedPreKeyRecordStructure(properties);
        };

        SignedPreKeyRecordStructure.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                w.uint32(8).uint32(m.id);
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey"))
                w.uint32(18).bytes(m.publicKey);
            if (m.privateKey != null && $Object.hasOwnProperty.call(m, "privateKey"))
                w.uint32(26).bytes(m.privateKey);
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature"))
                w.uint32(34).bytes(m.signature);
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(41).fixed64(m.timestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SignedPreKeyRecordStructure.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SignedPreKeyRecordStructure();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.id = r.uint32();
                        m._id = "id";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.publicKey = r.bytes();
                        m._publicKey = "publicKey";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.privateKey = r.bytes();
                        m._privateKey = "privateKey";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.signature = r.bytes();
                        m._signature = "signature";
                        continue;
                    }
                case 5: {
                        if (u !== 1)
                            break;
                        m.timestamp = r.fixed64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SignedPreKeyRecordStructure.fromObject = function (d, q) {
            if (d instanceof $root.proto.SignedPreKeyRecordStructure)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SignedPreKeyRecordStructure: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SignedPreKeyRecordStructure();
            if (d.id != null) {
                m.id = d.id >>> 0;
            }
            if (d.publicKey != null) {
                if (typeof d.publicKey === "string")
                    $util.base64.decode(d.publicKey, m.publicKey = $util.newBuffer($util.base64.length(d.publicKey)), 0);
                else if (d.publicKey.length >= 0)
                    m.publicKey = d.publicKey;
            }
            if (d.privateKey != null) {
                if (typeof d.privateKey === "string")
                    $util.base64.decode(d.privateKey, m.privateKey = $util.newBuffer($util.base64.length(d.privateKey)), 0);
                else if (d.privateKey.length >= 0)
                    m.privateKey = d.privateKey;
            }
            if (d.signature != null) {
                if (typeof d.signature === "string")
                    $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                else if (d.signature.length >= 0)
                    m.signature = d.signature;
            }
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, true);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            return m;
        };

        SignedPreKeyRecordStructure.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                d.id = m.id;
            }
            if (m.publicKey != null && $Object.hasOwnProperty.call(m, "publicKey")) {
                d.publicKey = o.bytes === $String ? $util.base64.encode(m.publicKey, 0, m.publicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.publicKey) : m.publicKey;
            }
            if (m.privateKey != null && $Object.hasOwnProperty.call(m, "privateKey")) {
                d.privateKey = o.bytes === $String ? $util.base64.encode(m.privateKey, 0, m.privateKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.privateKey) : m.privateKey;
            }
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature")) {
                d.signature = o.bytes === $String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signature) : m.signature;
            }
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, true).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
            }
            return d;
        };

        SignedPreKeyRecordStructure.prototype.toJSON = function() {
            return SignedPreKeyRecordStructure.toObject(this, $protobuf.util.toJSONOptions);
        };

        SignedPreKeyRecordStructure.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SignedPreKeyRecordStructure";
        };

        return SignedPreKeyRecordStructure;
    })();

    proto.StatusAttribution = (function() {

        const StatusAttribution = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        StatusAttribution.prototype.type = null;
        StatusAttribution.prototype.actionUrl = null;
        StatusAttribution.prototype.statusReshare = null;
        StatusAttribution.prototype.externalShare = null;
        StatusAttribution.prototype.music = null;
        StatusAttribution.prototype.groupStatus = null;
        StatusAttribution.prototype.rlAttribution = null;
        StatusAttribution.prototype.aiCreatedAttribution = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StatusAttribution.prototype, "_type", {
            get: $util.oneOfGetter($oneOfFields = ["type"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StatusAttribution.prototype, "_actionUrl", {
            get: $util.oneOfGetter($oneOfFields = ["actionUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        $Object.defineProperty(StatusAttribution.prototype, "attributionData", {
            get: $util.oneOfGetter($oneOfFields = ["statusReshare", "externalShare", "music", "groupStatus", "rlAttribution", "aiCreatedAttribution"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusAttribution.create = function(properties) {
            return new StatusAttribution(properties);
        };

        StatusAttribution.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                w.uint32(8).int32(m.type);
            if (m.actionUrl != null && $Object.hasOwnProperty.call(m, "actionUrl"))
                w.uint32(18).string(m.actionUrl);
            if (m.statusReshare != null && $Object.hasOwnProperty.call(m, "statusReshare"))
                $root.proto.StatusAttribution.StatusReshare.encode(m.statusReshare, w.uint32(26).fork(), q + 1).ldelim();
            if (m.externalShare != null && $Object.hasOwnProperty.call(m, "externalShare"))
                $root.proto.StatusAttribution.ExternalShare.encode(m.externalShare, w.uint32(34).fork(), q + 1).ldelim();
            if (m.music != null && $Object.hasOwnProperty.call(m, "music"))
                $root.proto.StatusAttribution.Music.encode(m.music, w.uint32(42).fork(), q + 1).ldelim();
            if (m.groupStatus != null && $Object.hasOwnProperty.call(m, "groupStatus"))
                $root.proto.StatusAttribution.GroupStatus.encode(m.groupStatus, w.uint32(50).fork(), q + 1).ldelim();
            if (m.rlAttribution != null && $Object.hasOwnProperty.call(m, "rlAttribution"))
                $root.proto.StatusAttribution.RLAttribution.encode(m.rlAttribution, w.uint32(58).fork(), q + 1).ldelim();
            if (m.aiCreatedAttribution != null && $Object.hasOwnProperty.call(m, "aiCreatedAttribution"))
                $root.proto.StatusAttribution.AiCreatedAttribution.encode(m.aiCreatedAttribution, w.uint32(66).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        StatusAttribution.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.type = r.int32();
                        m._type = "type";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.actionUrl = r.stringVerify();
                        m._actionUrl = "actionUrl";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.statusReshare = $root.proto.StatusAttribution.StatusReshare.decode(r, r.uint32(), $undefined, q + 1, m.statusReshare);
                        m.attributionData = "statusReshare";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.externalShare = $root.proto.StatusAttribution.ExternalShare.decode(r, r.uint32(), $undefined, q + 1, m.externalShare);
                        m.attributionData = "externalShare";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.music = $root.proto.StatusAttribution.Music.decode(r, r.uint32(), $undefined, q + 1, m.music);
                        m.attributionData = "music";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.groupStatus = $root.proto.StatusAttribution.GroupStatus.decode(r, r.uint32(), $undefined, q + 1, m.groupStatus);
                        m.attributionData = "groupStatus";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.rlAttribution = $root.proto.StatusAttribution.RLAttribution.decode(r, r.uint32(), $undefined, q + 1, m.rlAttribution);
                        m.attributionData = "rlAttribution";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.aiCreatedAttribution = $root.proto.StatusAttribution.AiCreatedAttribution.decode(r, r.uint32(), $undefined, q + 1, m.aiCreatedAttribution);
                        m.attributionData = "aiCreatedAttribution";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        StatusAttribution.fromObject = function (d, q) {
            if (d instanceof $root.proto.StatusAttribution)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.StatusAttribution: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.StatusAttribution();
            switch (d.type) {
            case "UNKNOWN":
            case 0:
                m.type = 0;
                break;
            case "RESHARE":
            case 1:
                m.type = 1;
                break;
            case "EXTERNAL_SHARE":
            case 2:
                m.type = 2;
                break;
            case "MUSIC":
            case 3:
                m.type = 3;
                break;
            case "STATUS_MENTION":
            case 4:
                m.type = 4;
                break;
            case "GROUP_STATUS":
            case 5:
                m.type = 5;
                break;
            case "RL_ATTRIBUTION":
            case 6:
                m.type = 6;
                break;
            case "AI_CREATED":
            case 7:
                m.type = 7;
                break;
            case "LAYOUTS":
            case 8:
                m.type = 8;
                break;
            case "NEWSLETTER_STATUS":
            case 9:
                m.type = 9;
                break;
            case "STATUS_CLOSE_SHARING":
            case 10:
                m.type = 10;
                break;
            case "PAID_PARTNERSHIP":
            case 11:
                m.type = 11;
                break;
            case "USERNAME_STATUS":
            case 12:
                m.type = 12;
                break;
            default:
                if (typeof d.type === "number" && (d.type | 0) === d.type)
                    m.type = d.type;
            }
            if (d.actionUrl != null) {
                m.actionUrl = $String(d.actionUrl);
            }
            if (d.statusReshare != null) {
                if (!$util.isObject(d.statusReshare))
                    throw $TypeError(".proto.StatusAttribution.statusReshare: object expected");
                m.statusReshare = $root.proto.StatusAttribution.StatusReshare.fromObject(d.statusReshare, q + 1);
            }
            if (d.externalShare != null) {
                if (!$util.isObject(d.externalShare))
                    throw $TypeError(".proto.StatusAttribution.externalShare: object expected");
                m.externalShare = $root.proto.StatusAttribution.ExternalShare.fromObject(d.externalShare, q + 1);
            }
            if (d.music != null) {
                if (!$util.isObject(d.music))
                    throw $TypeError(".proto.StatusAttribution.music: object expected");
                m.music = $root.proto.StatusAttribution.Music.fromObject(d.music, q + 1);
            }
            if (d.groupStatus != null) {
                if (!$util.isObject(d.groupStatus))
                    throw $TypeError(".proto.StatusAttribution.groupStatus: object expected");
                m.groupStatus = $root.proto.StatusAttribution.GroupStatus.fromObject(d.groupStatus, q + 1);
            }
            if (d.rlAttribution != null) {
                if (!$util.isObject(d.rlAttribution))
                    throw $TypeError(".proto.StatusAttribution.rlAttribution: object expected");
                m.rlAttribution = $root.proto.StatusAttribution.RLAttribution.fromObject(d.rlAttribution, q + 1);
            }
            if (d.aiCreatedAttribution != null) {
                if (!$util.isObject(d.aiCreatedAttribution))
                    throw $TypeError(".proto.StatusAttribution.aiCreatedAttribution: object expected");
                m.aiCreatedAttribution = $root.proto.StatusAttribution.AiCreatedAttribution.fromObject(d.aiCreatedAttribution, q + 1);
            }
            return m;
        };

        StatusAttribution.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                d.type = o.enums === $String ? $root.proto.StatusAttribution.Type[m.type] === $undefined ? m.type : $root.proto.StatusAttribution.Type[m.type] : m.type;
            }
            if (m.actionUrl != null && $Object.hasOwnProperty.call(m, "actionUrl")) {
                d.actionUrl = m.actionUrl;
            }
            if (m.statusReshare != null && $Object.hasOwnProperty.call(m, "statusReshare")) {
                d.statusReshare = $root.proto.StatusAttribution.StatusReshare.toObject(m.statusReshare, o, q + 1);
                if (o.oneofs)
                    d.attributionData = "statusReshare";
            }
            if (m.externalShare != null && $Object.hasOwnProperty.call(m, "externalShare")) {
                d.externalShare = $root.proto.StatusAttribution.ExternalShare.toObject(m.externalShare, o, q + 1);
                if (o.oneofs)
                    d.attributionData = "externalShare";
            }
            if (m.music != null && $Object.hasOwnProperty.call(m, "music")) {
                d.music = $root.proto.StatusAttribution.Music.toObject(m.music, o, q + 1);
                if (o.oneofs)
                    d.attributionData = "music";
            }
            if (m.groupStatus != null && $Object.hasOwnProperty.call(m, "groupStatus")) {
                d.groupStatus = $root.proto.StatusAttribution.GroupStatus.toObject(m.groupStatus, o, q + 1);
                if (o.oneofs)
                    d.attributionData = "groupStatus";
            }
            if (m.rlAttribution != null && $Object.hasOwnProperty.call(m, "rlAttribution")) {
                d.rlAttribution = $root.proto.StatusAttribution.RLAttribution.toObject(m.rlAttribution, o, q + 1);
                if (o.oneofs)
                    d.attributionData = "rlAttribution";
            }
            if (m.aiCreatedAttribution != null && $Object.hasOwnProperty.call(m, "aiCreatedAttribution")) {
                d.aiCreatedAttribution = $root.proto.StatusAttribution.AiCreatedAttribution.toObject(m.aiCreatedAttribution, o, q + 1);
                if (o.oneofs)
                    d.attributionData = "aiCreatedAttribution";
            }
            return d;
        };

        StatusAttribution.prototype.toJSON = function() {
            return StatusAttribution.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusAttribution.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StatusAttribution";
        };

        StatusAttribution.AiCreatedAttribution = (function() {

            const AiCreatedAttribution = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AiCreatedAttribution.prototype.source = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AiCreatedAttribution.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AiCreatedAttribution.create = function(properties) {
                return new AiCreatedAttribution(properties);
            };

            AiCreatedAttribution.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.source != null && $Object.hasOwnProperty.call(m, "source"))
                    w.uint32(8).int32(m.source);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AiCreatedAttribution.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.AiCreatedAttribution(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.source = r.int32();
                            m._source = "source";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AiCreatedAttribution.fromObject = function (d, q) {
                if (d instanceof $root.proto.StatusAttribution.AiCreatedAttribution)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.StatusAttribution.AiCreatedAttribution: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.StatusAttribution.AiCreatedAttribution();
                switch (d.source) {
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "STATUS_MIMICRY":
                case 1:
                    m.source = 1;
                    break;
                default:
                    if (typeof d.source === "number" && (d.source | 0) === d.source)
                        m.source = d.source;
                }
                return m;
            };

            AiCreatedAttribution.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.source != null && $Object.hasOwnProperty.call(m, "source")) {
                    d.source = o.enums === $String ? $root.proto.StatusAttribution.AiCreatedAttribution.Source[m.source] === $undefined ? m.source : $root.proto.StatusAttribution.AiCreatedAttribution.Source[m.source] : m.source;
                }
                return d;
            };

            AiCreatedAttribution.prototype.toJSON = function() {
                return AiCreatedAttribution.toObject(this, $protobuf.util.toJSONOptions);
            };

            AiCreatedAttribution.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.StatusAttribution.AiCreatedAttribution";
            };

            AiCreatedAttribution.Source = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "STATUS_MIMICRY"] = 1;
                return values;
            })();

            return AiCreatedAttribution;
        })();

        StatusAttribution.ExternalShare = (function() {

            const ExternalShare = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ExternalShare.prototype.actionUrl = null;
            ExternalShare.prototype.source = null;
            ExternalShare.prototype.duration = null;
            ExternalShare.prototype.actionFallbackUrl = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalShare.prototype, "_actionUrl", {
                get: $util.oneOfGetter($oneOfFields = ["actionUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalShare.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalShare.prototype, "_duration", {
                get: $util.oneOfGetter($oneOfFields = ["duration"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalShare.prototype, "_actionFallbackUrl", {
                get: $util.oneOfGetter($oneOfFields = ["actionFallbackUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExternalShare.create = function(properties) {
                return new ExternalShare(properties);
            };

            ExternalShare.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.actionUrl != null && $Object.hasOwnProperty.call(m, "actionUrl"))
                    w.uint32(10).string(m.actionUrl);
                if (m.source != null && $Object.hasOwnProperty.call(m, "source"))
                    w.uint32(16).int32(m.source);
                if (m.duration != null && $Object.hasOwnProperty.call(m, "duration"))
                    w.uint32(24).int32(m.duration);
                if (m.actionFallbackUrl != null && $Object.hasOwnProperty.call(m, "actionFallbackUrl"))
                    w.uint32(34).string(m.actionFallbackUrl);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ExternalShare.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.ExternalShare(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.actionUrl = r.stringVerify();
                            m._actionUrl = "actionUrl";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.source = r.int32();
                            m._source = "source";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.duration = r.int32();
                            m._duration = "duration";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.actionFallbackUrl = r.stringVerify();
                            m._actionFallbackUrl = "actionFallbackUrl";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ExternalShare.fromObject = function (d, q) {
                if (d instanceof $root.proto.StatusAttribution.ExternalShare)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.StatusAttribution.ExternalShare: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.StatusAttribution.ExternalShare();
                if (d.actionUrl != null) {
                    m.actionUrl = $String(d.actionUrl);
                }
                switch (d.source) {
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "INSTAGRAM":
                case 1:
                    m.source = 1;
                    break;
                case "FACEBOOK":
                case 2:
                    m.source = 2;
                    break;
                case "MESSENGER":
                case 3:
                    m.source = 3;
                    break;
                case "SPOTIFY":
                case 4:
                    m.source = 4;
                    break;
                case "YOUTUBE":
                case 5:
                    m.source = 5;
                    break;
                case "PINTEREST":
                case 6:
                    m.source = 6;
                    break;
                case "THREADS":
                case 7:
                    m.source = 7;
                    break;
                case "APPLE_MUSIC":
                case 8:
                    m.source = 8;
                    break;
                case "SHARECHAT":
                case 9:
                    m.source = 9;
                    break;
                case "GOOGLE_PHOTOS":
                case 10:
                    m.source = 10;
                    break;
                case "SOUNDCLOUD":
                case 11:
                    m.source = 11;
                    break;
                case "SHAZAM":
                case 12:
                    m.source = 12;
                    break;
                case "PICSART":
                case 13:
                    m.source = 13;
                    break;
                default:
                    if (typeof d.source === "number" && (d.source | 0) === d.source)
                        m.source = d.source;
                }
                if (d.duration != null) {
                    m.duration = d.duration | 0;
                }
                if (d.actionFallbackUrl != null) {
                    m.actionFallbackUrl = $String(d.actionFallbackUrl);
                }
                return m;
            };

            ExternalShare.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.actionUrl != null && $Object.hasOwnProperty.call(m, "actionUrl")) {
                    d.actionUrl = m.actionUrl;
                }
                if (m.source != null && $Object.hasOwnProperty.call(m, "source")) {
                    d.source = o.enums === $String ? $root.proto.StatusAttribution.ExternalShare.Source[m.source] === $undefined ? m.source : $root.proto.StatusAttribution.ExternalShare.Source[m.source] : m.source;
                }
                if (m.duration != null && $Object.hasOwnProperty.call(m, "duration")) {
                    d.duration = m.duration;
                }
                if (m.actionFallbackUrl != null && $Object.hasOwnProperty.call(m, "actionFallbackUrl")) {
                    d.actionFallbackUrl = m.actionFallbackUrl;
                }
                return d;
            };

            ExternalShare.prototype.toJSON = function() {
                return ExternalShare.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExternalShare.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.StatusAttribution.ExternalShare";
            };

            ExternalShare.Source = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INSTAGRAM"] = 1;
                values[valuesById[2] = "FACEBOOK"] = 2;
                values[valuesById[3] = "MESSENGER"] = 3;
                values[valuesById[4] = "SPOTIFY"] = 4;
                values[valuesById[5] = "YOUTUBE"] = 5;
                values[valuesById[6] = "PINTEREST"] = 6;
                values[valuesById[7] = "THREADS"] = 7;
                values[valuesById[8] = "APPLE_MUSIC"] = 8;
                values[valuesById[9] = "SHARECHAT"] = 9;
                values[valuesById[10] = "GOOGLE_PHOTOS"] = 10;
                values[valuesById[11] = "SOUNDCLOUD"] = 11;
                values[valuesById[12] = "SHAZAM"] = 12;
                values[valuesById[13] = "PICSART"] = 13;
                return values;
            })();

            return ExternalShare;
        })();

        StatusAttribution.GroupStatus = (function() {

            const GroupStatus = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            GroupStatus.prototype.authorJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(GroupStatus.prototype, "_authorJid", {
                get: $util.oneOfGetter($oneOfFields = ["authorJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            GroupStatus.create = function(properties) {
                return new GroupStatus(properties);
            };

            GroupStatus.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.authorJid != null && $Object.hasOwnProperty.call(m, "authorJid"))
                    w.uint32(10).string(m.authorJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            GroupStatus.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.GroupStatus();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.authorJid = r.stringVerify();
                            m._authorJid = "authorJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            GroupStatus.fromObject = function (d, q) {
                if (d instanceof $root.proto.StatusAttribution.GroupStatus)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.StatusAttribution.GroupStatus: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.StatusAttribution.GroupStatus();
                if (d.authorJid != null) {
                    m.authorJid = $String(d.authorJid);
                }
                return m;
            };

            GroupStatus.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.authorJid != null && $Object.hasOwnProperty.call(m, "authorJid")) {
                    d.authorJid = m.authorJid;
                }
                return d;
            };

            GroupStatus.prototype.toJSON = function() {
                return GroupStatus.toObject(this, $protobuf.util.toJSONOptions);
            };

            GroupStatus.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.StatusAttribution.GroupStatus";
            };

            return GroupStatus;
        })();

        StatusAttribution.Music = (function() {

            const Music = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Music.prototype.authorName = null;
            Music.prototype.songId = null;
            Music.prototype.title = null;
            Music.prototype.author = null;
            Music.prototype.artistAttribution = null;
            Music.prototype.isExplicit = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Music.prototype, "_authorName", {
                get: $util.oneOfGetter($oneOfFields = ["authorName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Music.prototype, "_songId", {
                get: $util.oneOfGetter($oneOfFields = ["songId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Music.prototype, "_title", {
                get: $util.oneOfGetter($oneOfFields = ["title"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Music.prototype, "_author", {
                get: $util.oneOfGetter($oneOfFields = ["author"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Music.prototype, "_artistAttribution", {
                get: $util.oneOfGetter($oneOfFields = ["artistAttribution"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Music.prototype, "_isExplicit", {
                get: $util.oneOfGetter($oneOfFields = ["isExplicit"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Music.create = function(properties) {
                return new Music(properties);
            };

            Music.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.authorName != null && $Object.hasOwnProperty.call(m, "authorName"))
                    w.uint32(10).string(m.authorName);
                if (m.songId != null && $Object.hasOwnProperty.call(m, "songId"))
                    w.uint32(18).string(m.songId);
                if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                    w.uint32(26).string(m.title);
                if (m.author != null && $Object.hasOwnProperty.call(m, "author"))
                    w.uint32(34).string(m.author);
                if (m.artistAttribution != null && $Object.hasOwnProperty.call(m, "artistAttribution"))
                    w.uint32(42).string(m.artistAttribution);
                if (m.isExplicit != null && $Object.hasOwnProperty.call(m, "isExplicit"))
                    w.uint32(48).bool(m.isExplicit);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Music.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.Music();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.authorName = r.stringVerify();
                            m._authorName = "authorName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.songId = r.stringVerify();
                            m._songId = "songId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.title = r.stringVerify();
                            m._title = "title";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.author = r.stringVerify();
                            m._author = "author";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.artistAttribution = r.stringVerify();
                            m._artistAttribution = "artistAttribution";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.isExplicit = r.bool();
                            m._isExplicit = "isExplicit";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Music.fromObject = function (d, q) {
                if (d instanceof $root.proto.StatusAttribution.Music)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.StatusAttribution.Music: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.StatusAttribution.Music();
                if (d.authorName != null) {
                    m.authorName = $String(d.authorName);
                }
                if (d.songId != null) {
                    m.songId = $String(d.songId);
                }
                if (d.title != null) {
                    m.title = $String(d.title);
                }
                if (d.author != null) {
                    m.author = $String(d.author);
                }
                if (d.artistAttribution != null) {
                    m.artistAttribution = $String(d.artistAttribution);
                }
                if (d.isExplicit != null) {
                    m.isExplicit = $Boolean(d.isExplicit);
                }
                return m;
            };

            Music.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.authorName != null && $Object.hasOwnProperty.call(m, "authorName")) {
                    d.authorName = m.authorName;
                }
                if (m.songId != null && $Object.hasOwnProperty.call(m, "songId")) {
                    d.songId = m.songId;
                }
                if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                    d.title = m.title;
                }
                if (m.author != null && $Object.hasOwnProperty.call(m, "author")) {
                    d.author = m.author;
                }
                if (m.artistAttribution != null && $Object.hasOwnProperty.call(m, "artistAttribution")) {
                    d.artistAttribution = m.artistAttribution;
                }
                if (m.isExplicit != null && $Object.hasOwnProperty.call(m, "isExplicit")) {
                    d.isExplicit = m.isExplicit;
                }
                return d;
            };

            Music.prototype.toJSON = function() {
                return Music.toObject(this, $protobuf.util.toJSONOptions);
            };

            Music.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.StatusAttribution.Music";
            };

            return Music;
        })();

        StatusAttribution.RLAttribution = (function() {

            const RLAttribution = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RLAttribution.prototype.source = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RLAttribution.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RLAttribution.create = function(properties) {
                return new RLAttribution(properties);
            };

            RLAttribution.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.source != null && $Object.hasOwnProperty.call(m, "source"))
                    w.uint32(8).int32(m.source);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RLAttribution.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.RLAttribution(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.source = r.int32();
                            m._source = "source";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RLAttribution.fromObject = function (d, q) {
                if (d instanceof $root.proto.StatusAttribution.RLAttribution)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.StatusAttribution.RLAttribution: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.StatusAttribution.RLAttribution();
                switch (d.source) {
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "RAY_BAN_META_GLASSES":
                case 1:
                    m.source = 1;
                    break;
                case "OAKLEY_META_GLASSES":
                case 2:
                    m.source = 2;
                    break;
                case "HYPERNOVA_GLASSES":
                case 3:
                    m.source = 3;
                    break;
                default:
                    if (typeof d.source === "number" && (d.source | 0) === d.source)
                        m.source = d.source;
                }
                return m;
            };

            RLAttribution.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.source != null && $Object.hasOwnProperty.call(m, "source")) {
                    d.source = o.enums === $String ? $root.proto.StatusAttribution.RLAttribution.Source[m.source] === $undefined ? m.source : $root.proto.StatusAttribution.RLAttribution.Source[m.source] : m.source;
                }
                return d;
            };

            RLAttribution.prototype.toJSON = function() {
                return RLAttribution.toObject(this, $protobuf.util.toJSONOptions);
            };

            RLAttribution.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.StatusAttribution.RLAttribution";
            };

            RLAttribution.Source = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "RAY_BAN_META_GLASSES"] = 1;
                values[valuesById[2] = "OAKLEY_META_GLASSES"] = 2;
                values[valuesById[3] = "HYPERNOVA_GLASSES"] = 3;
                return values;
            })();

            return RLAttribution;
        })();

        StatusAttribution.StatusReshare = (function() {

            const StatusReshare = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusReshare.prototype.source = null;
            StatusReshare.prototype.metadata = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusReshare.prototype, "_source", {
                get: $util.oneOfGetter($oneOfFields = ["source"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusReshare.prototype, "_metadata", {
                get: $util.oneOfGetter($oneOfFields = ["metadata"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusReshare.create = function(properties) {
                return new StatusReshare(properties);
            };

            StatusReshare.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.source != null && $Object.hasOwnProperty.call(m, "source"))
                    w.uint32(8).int32(m.source);
                if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata"))
                    $root.proto.StatusAttribution.StatusReshare.Metadata.encode(m.metadata, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusReshare.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.StatusReshare(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.source = r.int32();
                            m._source = "source";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.metadata = $root.proto.StatusAttribution.StatusReshare.Metadata.decode(r, r.uint32(), $undefined, q + 1, m.metadata);
                            m._metadata = "metadata";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusReshare.fromObject = function (d, q) {
                if (d instanceof $root.proto.StatusAttribution.StatusReshare)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.StatusAttribution.StatusReshare: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.StatusAttribution.StatusReshare();
                switch (d.source) {
                case "UNKNOWN":
                case 0:
                    m.source = 0;
                    break;
                case "INTERNAL_RESHARE":
                case 1:
                    m.source = 1;
                    break;
                case "MENTION_RESHARE":
                case 2:
                    m.source = 2;
                    break;
                case "CHANNEL_RESHARE":
                case 3:
                    m.source = 3;
                    break;
                case "FORWARD":
                case 4:
                    m.source = 4;
                    break;
                default:
                    if (typeof d.source === "number" && (d.source | 0) === d.source)
                        m.source = d.source;
                }
                if (d.metadata != null) {
                    if (!$util.isObject(d.metadata))
                        throw $TypeError(".proto.StatusAttribution.StatusReshare.metadata: object expected");
                    m.metadata = $root.proto.StatusAttribution.StatusReshare.Metadata.fromObject(d.metadata, q + 1);
                }
                return m;
            };

            StatusReshare.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.source != null && $Object.hasOwnProperty.call(m, "source")) {
                    d.source = o.enums === $String ? $root.proto.StatusAttribution.StatusReshare.Source[m.source] === $undefined ? m.source : $root.proto.StatusAttribution.StatusReshare.Source[m.source] : m.source;
                }
                if (m.metadata != null && $Object.hasOwnProperty.call(m, "metadata")) {
                    d.metadata = $root.proto.StatusAttribution.StatusReshare.Metadata.toObject(m.metadata, o, q + 1);
                }
                return d;
            };

            StatusReshare.prototype.toJSON = function() {
                return StatusReshare.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusReshare.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.StatusAttribution.StatusReshare";
            };

            StatusReshare.Metadata = (function() {

                const Metadata = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Metadata.prototype.duration = null;
                Metadata.prototype.channelJid = null;
                Metadata.prototype.channelMessageId = null;
                Metadata.prototype.hasMultipleReshares = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Metadata.prototype, "_duration", {
                    get: $util.oneOfGetter($oneOfFields = ["duration"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Metadata.prototype, "_channelJid", {
                    get: $util.oneOfGetter($oneOfFields = ["channelJid"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Metadata.prototype, "_channelMessageId", {
                    get: $util.oneOfGetter($oneOfFields = ["channelMessageId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Metadata.prototype, "_hasMultipleReshares", {
                    get: $util.oneOfGetter($oneOfFields = ["hasMultipleReshares"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Metadata.create = function(properties) {
                    return new Metadata(properties);
                };

                Metadata.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.duration != null && $Object.hasOwnProperty.call(m, "duration"))
                        w.uint32(8).int32(m.duration);
                    if (m.channelJid != null && $Object.hasOwnProperty.call(m, "channelJid"))
                        w.uint32(18).string(m.channelJid);
                    if (m.channelMessageId != null && $Object.hasOwnProperty.call(m, "channelMessageId"))
                        w.uint32(24).int32(m.channelMessageId);
                    if (m.hasMultipleReshares != null && $Object.hasOwnProperty.call(m, "hasMultipleReshares"))
                        w.uint32(32).bool(m.hasMultipleReshares);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Metadata.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusAttribution.StatusReshare.Metadata();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 0)
                                    break;
                                m.duration = r.int32();
                                m._duration = "duration";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.channelJid = r.stringVerify();
                                m._channelJid = "channelJid";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.channelMessageId = r.int32();
                                m._channelMessageId = "channelMessageId";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.hasMultipleReshares = r.bool();
                                m._hasMultipleReshares = "hasMultipleReshares";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Metadata.fromObject = function (d, q) {
                    if (d instanceof $root.proto.StatusAttribution.StatusReshare.Metadata)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.StatusAttribution.StatusReshare.Metadata: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.StatusAttribution.StatusReshare.Metadata();
                    if (d.duration != null) {
                        m.duration = d.duration | 0;
                    }
                    if (d.channelJid != null) {
                        m.channelJid = $String(d.channelJid);
                    }
                    if (d.channelMessageId != null) {
                        m.channelMessageId = d.channelMessageId | 0;
                    }
                    if (d.hasMultipleReshares != null) {
                        m.hasMultipleReshares = $Boolean(d.hasMultipleReshares);
                    }
                    return m;
                };

                Metadata.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.duration != null && $Object.hasOwnProperty.call(m, "duration")) {
                        d.duration = m.duration;
                    }
                    if (m.channelJid != null && $Object.hasOwnProperty.call(m, "channelJid")) {
                        d.channelJid = m.channelJid;
                    }
                    if (m.channelMessageId != null && $Object.hasOwnProperty.call(m, "channelMessageId")) {
                        d.channelMessageId = m.channelMessageId;
                    }
                    if (m.hasMultipleReshares != null && $Object.hasOwnProperty.call(m, "hasMultipleReshares")) {
                        d.hasMultipleReshares = m.hasMultipleReshares;
                    }
                    return d;
                };

                Metadata.prototype.toJSON = function() {
                    return Metadata.toObject(this, $protobuf.util.toJSONOptions);
                };

                Metadata.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.StatusAttribution.StatusReshare.Metadata";
                };

                return Metadata;
            })();

            StatusReshare.Source = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INTERNAL_RESHARE"] = 1;
                values[valuesById[2] = "MENTION_RESHARE"] = 2;
                values[valuesById[3] = "CHANNEL_RESHARE"] = 3;
                values[valuesById[4] = "FORWARD"] = 4;
                return values;
            })();

            return StatusReshare;
        })();

        StatusAttribution.Type = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "RESHARE"] = 1;
            values[valuesById[2] = "EXTERNAL_SHARE"] = 2;
            values[valuesById[3] = "MUSIC"] = 3;
            values[valuesById[4] = "STATUS_MENTION"] = 4;
            values[valuesById[5] = "GROUP_STATUS"] = 5;
            values[valuesById[6] = "RL_ATTRIBUTION"] = 6;
            values[valuesById[7] = "AI_CREATED"] = 7;
            values[valuesById[8] = "LAYOUTS"] = 8;
            values[valuesById[9] = "NEWSLETTER_STATUS"] = 9;
            values[valuesById[10] = "STATUS_CLOSE_SHARING"] = 10;
            values[valuesById[11] = "PAID_PARTNERSHIP"] = 11;
            values[valuesById[12] = "USERNAME_STATUS"] = 12;
            return values;
        })();

        return StatusAttribution;
    })();

    proto.StatusMentionMessage = (function() {

        const StatusMentionMessage = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        StatusMentionMessage.prototype.quotedStatus = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StatusMentionMessage.prototype, "_quotedStatus", {
            get: $util.oneOfGetter($oneOfFields = ["quotedStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusMentionMessage.create = function(properties) {
            return new StatusMentionMessage(properties);
        };

        StatusMentionMessage.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.quotedStatus != null && $Object.hasOwnProperty.call(m, "quotedStatus"))
                $root.proto.Message.encode(m.quotedStatus, w.uint32(10).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        StatusMentionMessage.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusMentionMessage();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.quotedStatus = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.quotedStatus);
                        m._quotedStatus = "quotedStatus";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        StatusMentionMessage.fromObject = function (d, q) {
            if (d instanceof $root.proto.StatusMentionMessage)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.StatusMentionMessage: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.StatusMentionMessage();
            if (d.quotedStatus != null) {
                if (!$util.isObject(d.quotedStatus))
                    throw $TypeError(".proto.StatusMentionMessage.quotedStatus: object expected");
                m.quotedStatus = $root.proto.Message.fromObject(d.quotedStatus, q + 1);
            }
            return m;
        };

        StatusMentionMessage.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.quotedStatus != null && $Object.hasOwnProperty.call(m, "quotedStatus")) {
                d.quotedStatus = $root.proto.Message.toObject(m.quotedStatus, o, q + 1);
            }
            return d;
        };

        StatusMentionMessage.prototype.toJSON = function() {
            return StatusMentionMessage.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusMentionMessage.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StatusMentionMessage";
        };

        return StatusMentionMessage;
    })();

    proto.StatusPSA = (function() {

        const StatusPSA = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        StatusPSA.prototype.campaignId = $util.Long ? $util.Long.fromBits(0,0,true) : 0;
        StatusPSA.prototype.campaignExpirationTimestamp = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StatusPSA.prototype, "_campaignExpirationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["campaignExpirationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StatusPSA.create = function(properties) {
            return new StatusPSA(properties);
        };

        StatusPSA.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(352).uint64(m.campaignId);
            if (m.campaignExpirationTimestamp != null && $Object.hasOwnProperty.call(m, "campaignExpirationTimestamp"))
                w.uint32(360).uint64(m.campaignExpirationTimestamp);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        StatusPSA.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StatusPSA();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 44: {
                        if (u !== 0)
                            break;
                        m.campaignId = r.uint64();
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        m.campaignExpirationTimestamp = r.uint64();
                        m._campaignExpirationTimestamp = "campaignExpirationTimestamp";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "campaignId"))
                throw $util.ProtocolError("missing required 'campaignId'", { instance: m });
            return m;
        };

        StatusPSA.fromObject = function (d, q) {
            if (d instanceof $root.proto.StatusPSA)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.StatusPSA: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.StatusPSA();
            if (d.campaignId != null) {
                if ($util.Long)
                    m.campaignId = $util.Long.fromValue(d.campaignId, true);
                else if (typeof d.campaignId === "string")
                    m.campaignId = $parseInt(d.campaignId, 10);
                else if (typeof d.campaignId === "number")
                    m.campaignId = d.campaignId;
                else if (typeof d.campaignId === "object")
                    m.campaignId = new $util.LongBits(d.campaignId.low >>> 0, d.campaignId.high >>> 0).toNumber(true);
            }
            if (d.campaignExpirationTimestamp != null) {
                if ($util.Long)
                    m.campaignExpirationTimestamp = $util.Long.fromValue(d.campaignExpirationTimestamp, true);
                else if (typeof d.campaignExpirationTimestamp === "string")
                    m.campaignExpirationTimestamp = $parseInt(d.campaignExpirationTimestamp, 10);
                else if (typeof d.campaignExpirationTimestamp === "number")
                    m.campaignExpirationTimestamp = d.campaignExpirationTimestamp;
                else if (typeof d.campaignExpirationTimestamp === "object")
                    m.campaignExpirationTimestamp = new $util.LongBits(d.campaignExpirationTimestamp.low >>> 0, d.campaignExpirationTimestamp.high >>> 0).toNumber(true);
            }
            return m;
        };

        StatusPSA.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if ($util.Long) {
                    var n = new $util.Long(0, 0, true);
                    d.campaignId = o.longs === $String ? n.toString() : o.longs === $Number ? n.toNumber() : typeof $BigInt !== "undefined" && o.longs === $BigInt ? n.toBigInt() : n;
                } else
                    d.campaignId = o.longs === $String ? "0" : typeof $BigInt !== "undefined" && o.longs === $BigInt ? $BigInt("0") : 0;
            }
            if (m.campaignId != null && $Object.hasOwnProperty.call(m, "campaignId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.campaignId = typeof m.campaignId === "number" ? $BigInt(m.campaignId) : $util.Long.fromBits(m.campaignId.low >>> 0, m.campaignId.high >>> 0, true).toBigInt();
                else if (typeof m.campaignId === "number")
                    d.campaignId = o.longs === $String ? $String(m.campaignId) : m.campaignId;
                else
                    d.campaignId = o.longs === $String ? $util.Long.prototype.toString.call(m.campaignId) : o.longs === $Number ? new $util.LongBits(m.campaignId.low >>> 0, m.campaignId.high >>> 0).toNumber(true) : m.campaignId;
            }
            if (m.campaignExpirationTimestamp != null && $Object.hasOwnProperty.call(m, "campaignExpirationTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.campaignExpirationTimestamp = typeof m.campaignExpirationTimestamp === "number" ? $BigInt(m.campaignExpirationTimestamp) : $util.Long.fromBits(m.campaignExpirationTimestamp.low >>> 0, m.campaignExpirationTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.campaignExpirationTimestamp === "number")
                    d.campaignExpirationTimestamp = o.longs === $String ? $String(m.campaignExpirationTimestamp) : m.campaignExpirationTimestamp;
                else
                    d.campaignExpirationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.campaignExpirationTimestamp) : o.longs === $Number ? new $util.LongBits(m.campaignExpirationTimestamp.low >>> 0, m.campaignExpirationTimestamp.high >>> 0).toNumber(true) : m.campaignExpirationTimestamp;
            }
            return d;
        };

        StatusPSA.prototype.toJSON = function() {
            return StatusPSA.toObject(this, $protobuf.util.toJSONOptions);
        };

        StatusPSA.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StatusPSA";
        };

        return StatusPSA;
    })();

    proto.StickerMetadata = (function() {

        const StickerMetadata = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        StickerMetadata.prototype.url = null;
        StickerMetadata.prototype.fileSha256 = null;
        StickerMetadata.prototype.fileEncSha256 = null;
        StickerMetadata.prototype.mediaKey = null;
        StickerMetadata.prototype.mimetype = null;
        StickerMetadata.prototype.height = null;
        StickerMetadata.prototype.width = null;
        StickerMetadata.prototype.directPath = null;
        StickerMetadata.prototype.fileLength = null;
        StickerMetadata.prototype.weight = null;
        StickerMetadata.prototype.lastStickerSentTs = null;
        StickerMetadata.prototype.isLottie = null;
        StickerMetadata.prototype.imageHash = null;
        StickerMetadata.prototype.isAvatarSticker = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_url", {
            get: $util.oneOfGetter($oneOfFields = ["url"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_fileSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_fileEncSha256", {
            get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_mediaKey", {
            get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_mimetype", {
            get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_height", {
            get: $util.oneOfGetter($oneOfFields = ["height"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_width", {
            get: $util.oneOfGetter($oneOfFields = ["width"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_directPath", {
            get: $util.oneOfGetter($oneOfFields = ["directPath"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_fileLength", {
            get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_weight", {
            get: $util.oneOfGetter($oneOfFields = ["weight"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_lastStickerSentTs", {
            get: $util.oneOfGetter($oneOfFields = ["lastStickerSentTs"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_isLottie", {
            get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_imageHash", {
            get: $util.oneOfGetter($oneOfFields = ["imageHash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(StickerMetadata.prototype, "_isAvatarSticker", {
            get: $util.oneOfGetter($oneOfFields = ["isAvatarSticker"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        StickerMetadata.create = function(properties) {
            return new StickerMetadata(properties);
        };

        StickerMetadata.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                w.uint32(10).string(m.url);
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256"))
                w.uint32(18).bytes(m.fileSha256);
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                w.uint32(26).bytes(m.fileEncSha256);
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                w.uint32(34).bytes(m.mediaKey);
            if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                w.uint32(42).string(m.mimetype);
            if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                w.uint32(48).uint32(m.height);
            if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                w.uint32(56).uint32(m.width);
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                w.uint32(66).string(m.directPath);
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                w.uint32(72).uint64(m.fileLength);
            if (m.weight != null && $Object.hasOwnProperty.call(m, "weight"))
                w.uint32(85).float(m.weight);
            if (m.lastStickerSentTs != null && $Object.hasOwnProperty.call(m, "lastStickerSentTs"))
                w.uint32(88).int64(m.lastStickerSentTs);
            if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie"))
                w.uint32(96).bool(m.isLottie);
            if (m.imageHash != null && $Object.hasOwnProperty.call(m, "imageHash"))
                w.uint32(106).string(m.imageHash);
            if (m.isAvatarSticker != null && $Object.hasOwnProperty.call(m, "isAvatarSticker"))
                w.uint32(112).bool(m.isAvatarSticker);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        StickerMetadata.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.StickerMetadata();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.url = r.stringVerify();
                        m._url = "url";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.fileSha256 = r.bytes();
                        m._fileSha256 = "fileSha256";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.fileEncSha256 = r.bytes();
                        m._fileEncSha256 = "fileEncSha256";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.mediaKey = r.bytes();
                        m._mediaKey = "mediaKey";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.mimetype = r.stringVerify();
                        m._mimetype = "mimetype";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.height = r.uint32();
                        m._height = "height";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.width = r.uint32();
                        m._width = "width";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.directPath = r.stringVerify();
                        m._directPath = "directPath";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.fileLength = r.uint64();
                        m._fileLength = "fileLength";
                        continue;
                    }
                case 10: {
                        if (u !== 5)
                            break;
                        m.weight = r.float();
                        m._weight = "weight";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.lastStickerSentTs = r.int64();
                        m._lastStickerSentTs = "lastStickerSentTs";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.isLottie = r.bool();
                        m._isLottie = "isLottie";
                        continue;
                    }
                case 13: {
                        if (u !== 2)
                            break;
                        m.imageHash = r.stringVerify();
                        m._imageHash = "imageHash";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        m.isAvatarSticker = r.bool();
                        m._isAvatarSticker = "isAvatarSticker";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        StickerMetadata.fromObject = function (d, q) {
            if (d instanceof $root.proto.StickerMetadata)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.StickerMetadata: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.StickerMetadata();
            if (d.url != null) {
                m.url = $String(d.url);
            }
            if (d.fileSha256 != null) {
                if (typeof d.fileSha256 === "string")
                    $util.base64.decode(d.fileSha256, m.fileSha256 = $util.newBuffer($util.base64.length(d.fileSha256)), 0);
                else if (d.fileSha256.length >= 0)
                    m.fileSha256 = d.fileSha256;
            }
            if (d.fileEncSha256 != null) {
                if (typeof d.fileEncSha256 === "string")
                    $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                else if (d.fileEncSha256.length >= 0)
                    m.fileEncSha256 = d.fileEncSha256;
            }
            if (d.mediaKey != null) {
                if (typeof d.mediaKey === "string")
                    $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                else if (d.mediaKey.length >= 0)
                    m.mediaKey = d.mediaKey;
            }
            if (d.mimetype != null) {
                m.mimetype = $String(d.mimetype);
            }
            if (d.height != null) {
                m.height = d.height >>> 0;
            }
            if (d.width != null) {
                m.width = d.width >>> 0;
            }
            if (d.directPath != null) {
                m.directPath = $String(d.directPath);
            }
            if (d.fileLength != null) {
                if ($util.Long)
                    m.fileLength = $util.Long.fromValue(d.fileLength, true);
                else if (typeof d.fileLength === "string")
                    m.fileLength = $parseInt(d.fileLength, 10);
                else if (typeof d.fileLength === "number")
                    m.fileLength = d.fileLength;
                else if (typeof d.fileLength === "object")
                    m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
            }
            if (d.weight != null) {
                m.weight = $Number(d.weight);
            }
            if (d.lastStickerSentTs != null) {
                if ($util.Long)
                    m.lastStickerSentTs = $util.Long.fromValue(d.lastStickerSentTs, false);
                else if (typeof d.lastStickerSentTs === "string")
                    m.lastStickerSentTs = $parseInt(d.lastStickerSentTs, 10);
                else if (typeof d.lastStickerSentTs === "number")
                    m.lastStickerSentTs = d.lastStickerSentTs;
                else if (typeof d.lastStickerSentTs === "object")
                    m.lastStickerSentTs = new $util.LongBits(d.lastStickerSentTs.low >>> 0, d.lastStickerSentTs.high >>> 0).toNumber();
            }
            if (d.isLottie != null) {
                m.isLottie = $Boolean(d.isLottie);
            }
            if (d.imageHash != null) {
                m.imageHash = $String(d.imageHash);
            }
            if (d.isAvatarSticker != null) {
                m.isAvatarSticker = $Boolean(d.isAvatarSticker);
            }
            return m;
        };

        StickerMetadata.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                d.url = m.url;
            }
            if (m.fileSha256 != null && $Object.hasOwnProperty.call(m, "fileSha256")) {
                d.fileSha256 = o.bytes === $String ? $util.base64.encode(m.fileSha256, 0, m.fileSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileSha256) : m.fileSha256;
            }
            if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
            }
            if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
            }
            if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                d.mimetype = m.mimetype;
            }
            if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                d.height = m.height;
            }
            if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                d.width = m.width;
            }
            if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                d.directPath = m.directPath;
            }
            if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                else if (typeof m.fileLength === "number")
                    d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                else
                    d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
            }
            if (m.weight != null && $Object.hasOwnProperty.call(m, "weight")) {
                d.weight = o.json && !$isFinite(m.weight) ? $String(m.weight) : m.weight;
            }
            if (m.lastStickerSentTs != null && $Object.hasOwnProperty.call(m, "lastStickerSentTs")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.lastStickerSentTs = typeof m.lastStickerSentTs === "number" ? $BigInt(m.lastStickerSentTs) : $util.Long.fromBits(m.lastStickerSentTs.low >>> 0, m.lastStickerSentTs.high >>> 0, false).toBigInt();
                else if (typeof m.lastStickerSentTs === "number")
                    d.lastStickerSentTs = o.longs === $String ? $String(m.lastStickerSentTs) : m.lastStickerSentTs;
                else
                    d.lastStickerSentTs = o.longs === $String ? $util.Long.prototype.toString.call(m.lastStickerSentTs) : o.longs === $Number ? new $util.LongBits(m.lastStickerSentTs.low >>> 0, m.lastStickerSentTs.high >>> 0).toNumber() : m.lastStickerSentTs;
            }
            if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie")) {
                d.isLottie = m.isLottie;
            }
            if (m.imageHash != null && $Object.hasOwnProperty.call(m, "imageHash")) {
                d.imageHash = m.imageHash;
            }
            if (m.isAvatarSticker != null && $Object.hasOwnProperty.call(m, "isAvatarSticker")) {
                d.isAvatarSticker = m.isAvatarSticker;
            }
            return d;
        };

        StickerMetadata.prototype.toJSON = function() {
            return StickerMetadata.toObject(this, $protobuf.util.toJSONOptions);
        };

        StickerMetadata.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.StickerMetadata";
        };

        return StickerMetadata;
    })();

    proto.SubProtocol = (function() {

        const SubProtocol = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SubProtocol.prototype.payload = null;
        SubProtocol.prototype.version = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SubProtocol.prototype, "_payload", {
            get: $util.oneOfGetter($oneOfFields = ["payload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SubProtocol.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SubProtocol.create = function(properties) {
            return new SubProtocol(properties);
        };

        SubProtocol.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.payload != null && $Object.hasOwnProperty.call(m, "payload"))
                w.uint32(10).bytes(m.payload);
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(16).int32(m.version);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SubProtocol.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SubProtocol();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.payload = r.bytes();
                        m._payload = "payload";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.version = r.int32();
                        m._version = "version";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SubProtocol.fromObject = function (d, q) {
            if (d instanceof $root.proto.SubProtocol)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SubProtocol: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SubProtocol();
            if (d.payload != null) {
                if (typeof d.payload === "string")
                    $util.base64.decode(d.payload, m.payload = $util.newBuffer($util.base64.length(d.payload)), 0);
                else if (d.payload.length >= 0)
                    m.payload = d.payload;
            }
            if (d.version != null) {
                m.version = d.version | 0;
            }
            return m;
        };

        SubProtocol.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.payload != null && $Object.hasOwnProperty.call(m, "payload")) {
                d.payload = o.bytes === $String ? $util.base64.encode(m.payload, 0, m.payload.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.payload) : m.payload;
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = m.version;
            }
            return d;
        };

        SubProtocol.prototype.toJSON = function() {
            return SubProtocol.toObject(this, $protobuf.util.toJSONOptions);
        };

        SubProtocol.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SubProtocol";
        };

        return SubProtocol;
    })();

    proto.SyncActionData = (function() {

        const SyncActionData = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncActionData.prototype.index = null;
        SyncActionData.prototype.value = null;
        SyncActionData.prototype.padding = null;
        SyncActionData.prototype.version = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionData.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionData.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionData.prototype, "_padding", {
            get: $util.oneOfGetter($oneOfFields = ["padding"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionData.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncActionData.create = function(properties) {
            return new SyncActionData(properties);
        };

        SyncActionData.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                w.uint32(10).bytes(m.index);
            if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                $root.proto.SyncActionValue.encode(m.value, w.uint32(18).fork(), q + 1).ldelim();
            if (m.padding != null && $Object.hasOwnProperty.call(m, "padding"))
                w.uint32(26).bytes(m.padding);
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(32).int32(m.version);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncActionData.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionData();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.index = r.bytes();
                        m._index = "index";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.value = $root.proto.SyncActionValue.decode(r, r.uint32(), $undefined, q + 1, m.value);
                        m._value = "value";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.padding = r.bytes();
                        m._padding = "padding";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.version = r.int32();
                        m._version = "version";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncActionData.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncActionData)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncActionData: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncActionData();
            if (d.index != null) {
                if (typeof d.index === "string")
                    $util.base64.decode(d.index, m.index = $util.newBuffer($util.base64.length(d.index)), 0);
                else if (d.index.length >= 0)
                    m.index = d.index;
            }
            if (d.value != null) {
                if (!$util.isObject(d.value))
                    throw $TypeError(".proto.SyncActionData.value: object expected");
                m.value = $root.proto.SyncActionValue.fromObject(d.value, q + 1);
            }
            if (d.padding != null) {
                if (typeof d.padding === "string")
                    $util.base64.decode(d.padding, m.padding = $util.newBuffer($util.base64.length(d.padding)), 0);
                else if (d.padding.length >= 0)
                    m.padding = d.padding;
            }
            if (d.version != null) {
                m.version = d.version | 0;
            }
            return m;
        };

        SyncActionData.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                d.index = o.bytes === $String ? $util.base64.encode(m.index, 0, m.index.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.index) : m.index;
            }
            if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                d.value = $root.proto.SyncActionValue.toObject(m.value, o, q + 1);
            }
            if (m.padding != null && $Object.hasOwnProperty.call(m, "padding")) {
                d.padding = o.bytes === $String ? $util.base64.encode(m.padding, 0, m.padding.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.padding) : m.padding;
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = m.version;
            }
            return d;
        };

        SyncActionData.prototype.toJSON = function() {
            return SyncActionData.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncActionData.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncActionData";
        };

        return SyncActionData;
    })();

    proto.SyncActionValue = (function() {

        const SyncActionValue = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncActionValue.prototype.timestamp = null;
        SyncActionValue.prototype.starAction = null;
        SyncActionValue.prototype.contactAction = null;
        SyncActionValue.prototype.muteAction = null;
        SyncActionValue.prototype.pinAction = null;
        SyncActionValue.prototype.pushNameSetting = null;
        SyncActionValue.prototype.quickReplyAction = null;
        SyncActionValue.prototype.recentEmojiWeightsAction = null;
        SyncActionValue.prototype.labelEditAction = null;
        SyncActionValue.prototype.labelAssociationAction = null;
        SyncActionValue.prototype.localeSetting = null;
        SyncActionValue.prototype.archiveChatAction = null;
        SyncActionValue.prototype.deleteMessageForMeAction = null;
        SyncActionValue.prototype.keyExpiration = null;
        SyncActionValue.prototype.markChatAsReadAction = null;
        SyncActionValue.prototype.clearChatAction = null;
        SyncActionValue.prototype.deleteChatAction = null;
        SyncActionValue.prototype.unarchiveChatsSetting = null;
        SyncActionValue.prototype.primaryFeature = null;
        SyncActionValue.prototype.androidUnsupportedActions = null;
        SyncActionValue.prototype.agentAction = null;
        SyncActionValue.prototype.subscriptionAction = null;
        SyncActionValue.prototype.userStatusMuteAction = null;
        SyncActionValue.prototype.timeFormatAction = null;
        SyncActionValue.prototype.nuxAction = null;
        SyncActionValue.prototype.primaryVersionAction = null;
        SyncActionValue.prototype.stickerAction = null;
        SyncActionValue.prototype.removeRecentStickerAction = null;
        SyncActionValue.prototype.chatAssignment = null;
        SyncActionValue.prototype.chatAssignmentOpenedStatus = null;
        SyncActionValue.prototype.pnForLidChatAction = null;
        SyncActionValue.prototype.marketingMessageAction = null;
        SyncActionValue.prototype.marketingMessageBroadcastAction = null;
        SyncActionValue.prototype.externalWebBetaAction = null;
        SyncActionValue.prototype.privacySettingRelayAllCalls = null;
        SyncActionValue.prototype.callLogAction = null;
        SyncActionValue.prototype.ugcBot = null;
        SyncActionValue.prototype.statusPrivacy = null;
        SyncActionValue.prototype.botWelcomeRequestAction = null;
        SyncActionValue.prototype.deleteIndividualCallLog = null;
        SyncActionValue.prototype.labelReorderingAction = null;
        SyncActionValue.prototype.paymentInfoAction = null;
        SyncActionValue.prototype.customPaymentMethodsAction = null;
        SyncActionValue.prototype.lockChatAction = null;
        SyncActionValue.prototype.chatLockSettings = null;
        SyncActionValue.prototype.wamoUserIdentifierAction = null;
        SyncActionValue.prototype.privacySettingDisableLinkPreviewsAction = null;
        SyncActionValue.prototype.deviceCapabilities = null;
        SyncActionValue.prototype.noteEditAction = null;
        SyncActionValue.prototype.favoritesAction = null;
        SyncActionValue.prototype.merchantPaymentPartnerAction = null;
        SyncActionValue.prototype.waffleAccountLinkStateAction = null;
        SyncActionValue.prototype.usernameChatStartMode = null;
        SyncActionValue.prototype.notificationActivitySettingAction = null;
        SyncActionValue.prototype.lidContactAction = null;
        SyncActionValue.prototype.ctwaPerCustomerDataSharingAction = null;
        SyncActionValue.prototype.paymentTosAction = null;
        SyncActionValue.prototype.privacySettingChannelsPersonalisedRecommendationAction = null;
        SyncActionValue.prototype.detectedOutcomesStatusAction = null;
        SyncActionValue.prototype.maibaAiFeaturesControlAction = null;
        SyncActionValue.prototype.businessBroadcastListAction = null;
        SyncActionValue.prototype.musicUserIdAction = null;
        SyncActionValue.prototype.statusPostOptInNotificationPreferencesAction = null;
        SyncActionValue.prototype.avatarUpdatedAction = null;
        SyncActionValue.prototype.privateProcessingSettingAction = null;
        SyncActionValue.prototype.newsletterSavedInterestsAction = null;
        SyncActionValue.prototype.aiThreadRenameAction = null;
        SyncActionValue.prototype.interactiveMessageAction = null;
        SyncActionValue.prototype.settingsSyncAction = null;
        SyncActionValue.prototype.outContactAction = null;
        SyncActionValue.prototype.nctSaltSyncAction = null;
        SyncActionValue.prototype.businessBroadcastCampaignAction = null;
        SyncActionValue.prototype.businessBroadcastInsightsAction = null;
        SyncActionValue.prototype.customerDataAction = null;
        SyncActionValue.prototype.subscriptionsSyncV2Action = null;
        SyncActionValue.prototype.threadPinAction = null;
        SyncActionValue.prototype.autoOrganizeBusinessChatSetting = null;
        SyncActionValue.prototype.bizAiSettingsNudgeAction = null;
        SyncActionValue.prototype.coexV2VersionAction = null;
        SyncActionValue.prototype.wasaRootSecretAction = null;
        SyncActionValue.prototype.bubbleLockMessageAction = null;
        SyncActionValue.prototype.labelSublistAction = null;
        SyncActionValue.prototype.deviceCapabilitiesV2 = null;
        SyncActionValue.prototype.ctwaMessageReceivedAction = null;
        SyncActionValue.prototype.sharedDeviceAllowlistAction = null;
        SyncActionValue.prototype.contactManagerMetadataAction = null;
        SyncActionValue.prototype.businessFolderActivationAction = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_starAction", {
            get: $util.oneOfGetter($oneOfFields = ["starAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_contactAction", {
            get: $util.oneOfGetter($oneOfFields = ["contactAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_muteAction", {
            get: $util.oneOfGetter($oneOfFields = ["muteAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_pinAction", {
            get: $util.oneOfGetter($oneOfFields = ["pinAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_pushNameSetting", {
            get: $util.oneOfGetter($oneOfFields = ["pushNameSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_quickReplyAction", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_recentEmojiWeightsAction", {
            get: $util.oneOfGetter($oneOfFields = ["recentEmojiWeightsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_labelEditAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelEditAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_labelAssociationAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelAssociationAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_localeSetting", {
            get: $util.oneOfGetter($oneOfFields = ["localeSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_archiveChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["archiveChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_deleteMessageForMeAction", {
            get: $util.oneOfGetter($oneOfFields = ["deleteMessageForMeAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_keyExpiration", {
            get: $util.oneOfGetter($oneOfFields = ["keyExpiration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_markChatAsReadAction", {
            get: $util.oneOfGetter($oneOfFields = ["markChatAsReadAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_clearChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["clearChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_deleteChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["deleteChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_unarchiveChatsSetting", {
            get: $util.oneOfGetter($oneOfFields = ["unarchiveChatsSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_primaryFeature", {
            get: $util.oneOfGetter($oneOfFields = ["primaryFeature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_androidUnsupportedActions", {
            get: $util.oneOfGetter($oneOfFields = ["androidUnsupportedActions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_agentAction", {
            get: $util.oneOfGetter($oneOfFields = ["agentAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_subscriptionAction", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_userStatusMuteAction", {
            get: $util.oneOfGetter($oneOfFields = ["userStatusMuteAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_timeFormatAction", {
            get: $util.oneOfGetter($oneOfFields = ["timeFormatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_nuxAction", {
            get: $util.oneOfGetter($oneOfFields = ["nuxAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_primaryVersionAction", {
            get: $util.oneOfGetter($oneOfFields = ["primaryVersionAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_stickerAction", {
            get: $util.oneOfGetter($oneOfFields = ["stickerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_removeRecentStickerAction", {
            get: $util.oneOfGetter($oneOfFields = ["removeRecentStickerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_chatAssignment", {
            get: $util.oneOfGetter($oneOfFields = ["chatAssignment"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_chatAssignmentOpenedStatus", {
            get: $util.oneOfGetter($oneOfFields = ["chatAssignmentOpenedStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_pnForLidChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["pnForLidChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_marketingMessageAction", {
            get: $util.oneOfGetter($oneOfFields = ["marketingMessageAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_marketingMessageBroadcastAction", {
            get: $util.oneOfGetter($oneOfFields = ["marketingMessageBroadcastAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_externalWebBetaAction", {
            get: $util.oneOfGetter($oneOfFields = ["externalWebBetaAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_privacySettingRelayAllCalls", {
            get: $util.oneOfGetter($oneOfFields = ["privacySettingRelayAllCalls"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_callLogAction", {
            get: $util.oneOfGetter($oneOfFields = ["callLogAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_ugcBot", {
            get: $util.oneOfGetter($oneOfFields = ["ugcBot"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_statusPrivacy", {
            get: $util.oneOfGetter($oneOfFields = ["statusPrivacy"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_botWelcomeRequestAction", {
            get: $util.oneOfGetter($oneOfFields = ["botWelcomeRequestAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_deleteIndividualCallLog", {
            get: $util.oneOfGetter($oneOfFields = ["deleteIndividualCallLog"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_labelReorderingAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelReorderingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_paymentInfoAction", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInfoAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_customPaymentMethodsAction", {
            get: $util.oneOfGetter($oneOfFields = ["customPaymentMethodsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_lockChatAction", {
            get: $util.oneOfGetter($oneOfFields = ["lockChatAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_chatLockSettings", {
            get: $util.oneOfGetter($oneOfFields = ["chatLockSettings"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_wamoUserIdentifierAction", {
            get: $util.oneOfGetter($oneOfFields = ["wamoUserIdentifierAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_privacySettingDisableLinkPreviewsAction", {
            get: $util.oneOfGetter($oneOfFields = ["privacySettingDisableLinkPreviewsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_deviceCapabilities", {
            get: $util.oneOfGetter($oneOfFields = ["deviceCapabilities"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_noteEditAction", {
            get: $util.oneOfGetter($oneOfFields = ["noteEditAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_favoritesAction", {
            get: $util.oneOfGetter($oneOfFields = ["favoritesAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_merchantPaymentPartnerAction", {
            get: $util.oneOfGetter($oneOfFields = ["merchantPaymentPartnerAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_waffleAccountLinkStateAction", {
            get: $util.oneOfGetter($oneOfFields = ["waffleAccountLinkStateAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_usernameChatStartMode", {
            get: $util.oneOfGetter($oneOfFields = ["usernameChatStartMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_notificationActivitySettingAction", {
            get: $util.oneOfGetter($oneOfFields = ["notificationActivitySettingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_lidContactAction", {
            get: $util.oneOfGetter($oneOfFields = ["lidContactAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_ctwaPerCustomerDataSharingAction", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaPerCustomerDataSharingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_paymentTosAction", {
            get: $util.oneOfGetter($oneOfFields = ["paymentTosAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_privacySettingChannelsPersonalisedRecommendationAction", {
            get: $util.oneOfGetter($oneOfFields = ["privacySettingChannelsPersonalisedRecommendationAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_detectedOutcomesStatusAction", {
            get: $util.oneOfGetter($oneOfFields = ["detectedOutcomesStatusAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_maibaAiFeaturesControlAction", {
            get: $util.oneOfGetter($oneOfFields = ["maibaAiFeaturesControlAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_businessBroadcastListAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcastListAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_musicUserIdAction", {
            get: $util.oneOfGetter($oneOfFields = ["musicUserIdAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_statusPostOptInNotificationPreferencesAction", {
            get: $util.oneOfGetter($oneOfFields = ["statusPostOptInNotificationPreferencesAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_avatarUpdatedAction", {
            get: $util.oneOfGetter($oneOfFields = ["avatarUpdatedAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_privateProcessingSettingAction", {
            get: $util.oneOfGetter($oneOfFields = ["privateProcessingSettingAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_newsletterSavedInterestsAction", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterSavedInterestsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_aiThreadRenameAction", {
            get: $util.oneOfGetter($oneOfFields = ["aiThreadRenameAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_interactiveMessageAction", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessageAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_settingsSyncAction", {
            get: $util.oneOfGetter($oneOfFields = ["settingsSyncAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_outContactAction", {
            get: $util.oneOfGetter($oneOfFields = ["outContactAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_nctSaltSyncAction", {
            get: $util.oneOfGetter($oneOfFields = ["nctSaltSyncAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_businessBroadcastCampaignAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcastCampaignAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_businessBroadcastInsightsAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessBroadcastInsightsAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_customerDataAction", {
            get: $util.oneOfGetter($oneOfFields = ["customerDataAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_subscriptionsSyncV2Action", {
            get: $util.oneOfGetter($oneOfFields = ["subscriptionsSyncV2Action"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_threadPinAction", {
            get: $util.oneOfGetter($oneOfFields = ["threadPinAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_autoOrganizeBusinessChatSetting", {
            get: $util.oneOfGetter($oneOfFields = ["autoOrganizeBusinessChatSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_bizAiSettingsNudgeAction", {
            get: $util.oneOfGetter($oneOfFields = ["bizAiSettingsNudgeAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_coexV2VersionAction", {
            get: $util.oneOfGetter($oneOfFields = ["coexV2VersionAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_wasaRootSecretAction", {
            get: $util.oneOfGetter($oneOfFields = ["wasaRootSecretAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_bubbleLockMessageAction", {
            get: $util.oneOfGetter($oneOfFields = ["bubbleLockMessageAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_labelSublistAction", {
            get: $util.oneOfGetter($oneOfFields = ["labelSublistAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_deviceCapabilitiesV2", {
            get: $util.oneOfGetter($oneOfFields = ["deviceCapabilitiesV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_ctwaMessageReceivedAction", {
            get: $util.oneOfGetter($oneOfFields = ["ctwaMessageReceivedAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_sharedDeviceAllowlistAction", {
            get: $util.oneOfGetter($oneOfFields = ["sharedDeviceAllowlistAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_contactManagerMetadataAction", {
            get: $util.oneOfGetter($oneOfFields = ["contactManagerMetadataAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncActionValue.prototype, "_businessFolderActivationAction", {
            get: $util.oneOfGetter($oneOfFields = ["businessFolderActivationAction"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncActionValue.create = function(properties) {
            return new SyncActionValue(properties);
        };

        SyncActionValue.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(8).int64(m.timestamp);
            if (m.starAction != null && $Object.hasOwnProperty.call(m, "starAction"))
                $root.proto.SyncActionValue.StarAction.encode(m.starAction, w.uint32(18).fork(), q + 1).ldelim();
            if (m.contactAction != null && $Object.hasOwnProperty.call(m, "contactAction"))
                $root.proto.SyncActionValue.ContactAction.encode(m.contactAction, w.uint32(26).fork(), q + 1).ldelim();
            if (m.muteAction != null && $Object.hasOwnProperty.call(m, "muteAction"))
                $root.proto.SyncActionValue.MuteAction.encode(m.muteAction, w.uint32(34).fork(), q + 1).ldelim();
            if (m.pinAction != null && $Object.hasOwnProperty.call(m, "pinAction"))
                $root.proto.SyncActionValue.PinAction.encode(m.pinAction, w.uint32(42).fork(), q + 1).ldelim();
            if (m.pushNameSetting != null && $Object.hasOwnProperty.call(m, "pushNameSetting"))
                $root.proto.SyncActionValue.PushNameSetting.encode(m.pushNameSetting, w.uint32(58).fork(), q + 1).ldelim();
            if (m.quickReplyAction != null && $Object.hasOwnProperty.call(m, "quickReplyAction"))
                $root.proto.SyncActionValue.QuickReplyAction.encode(m.quickReplyAction, w.uint32(66).fork(), q + 1).ldelim();
            if (m.recentEmojiWeightsAction != null && $Object.hasOwnProperty.call(m, "recentEmojiWeightsAction"))
                $root.proto.SyncActionValue.RecentEmojiWeightsAction.encode(m.recentEmojiWeightsAction, w.uint32(90).fork(), q + 1).ldelim();
            if (m.labelEditAction != null && $Object.hasOwnProperty.call(m, "labelEditAction"))
                $root.proto.SyncActionValue.LabelEditAction.encode(m.labelEditAction, w.uint32(114).fork(), q + 1).ldelim();
            if (m.labelAssociationAction != null && $Object.hasOwnProperty.call(m, "labelAssociationAction"))
                $root.proto.SyncActionValue.LabelAssociationAction.encode(m.labelAssociationAction, w.uint32(122).fork(), q + 1).ldelim();
            if (m.localeSetting != null && $Object.hasOwnProperty.call(m, "localeSetting"))
                $root.proto.SyncActionValue.LocaleSetting.encode(m.localeSetting, w.uint32(130).fork(), q + 1).ldelim();
            if (m.archiveChatAction != null && $Object.hasOwnProperty.call(m, "archiveChatAction"))
                $root.proto.SyncActionValue.ArchiveChatAction.encode(m.archiveChatAction, w.uint32(138).fork(), q + 1).ldelim();
            if (m.deleteMessageForMeAction != null && $Object.hasOwnProperty.call(m, "deleteMessageForMeAction"))
                $root.proto.SyncActionValue.DeleteMessageForMeAction.encode(m.deleteMessageForMeAction, w.uint32(146).fork(), q + 1).ldelim();
            if (m.keyExpiration != null && $Object.hasOwnProperty.call(m, "keyExpiration"))
                $root.proto.SyncActionValue.KeyExpiration.encode(m.keyExpiration, w.uint32(154).fork(), q + 1).ldelim();
            if (m.markChatAsReadAction != null && $Object.hasOwnProperty.call(m, "markChatAsReadAction"))
                $root.proto.SyncActionValue.MarkChatAsReadAction.encode(m.markChatAsReadAction, w.uint32(162).fork(), q + 1).ldelim();
            if (m.clearChatAction != null && $Object.hasOwnProperty.call(m, "clearChatAction"))
                $root.proto.SyncActionValue.ClearChatAction.encode(m.clearChatAction, w.uint32(170).fork(), q + 1).ldelim();
            if (m.deleteChatAction != null && $Object.hasOwnProperty.call(m, "deleteChatAction"))
                $root.proto.SyncActionValue.DeleteChatAction.encode(m.deleteChatAction, w.uint32(178).fork(), q + 1).ldelim();
            if (m.unarchiveChatsSetting != null && $Object.hasOwnProperty.call(m, "unarchiveChatsSetting"))
                $root.proto.SyncActionValue.UnarchiveChatsSetting.encode(m.unarchiveChatsSetting, w.uint32(186).fork(), q + 1).ldelim();
            if (m.primaryFeature != null && $Object.hasOwnProperty.call(m, "primaryFeature"))
                $root.proto.SyncActionValue.PrimaryFeature.encode(m.primaryFeature, w.uint32(194).fork(), q + 1).ldelim();
            if (m.androidUnsupportedActions != null && $Object.hasOwnProperty.call(m, "androidUnsupportedActions"))
                $root.proto.SyncActionValue.AndroidUnsupportedActions.encode(m.androidUnsupportedActions, w.uint32(210).fork(), q + 1).ldelim();
            if (m.agentAction != null && $Object.hasOwnProperty.call(m, "agentAction"))
                $root.proto.SyncActionValue.AgentAction.encode(m.agentAction, w.uint32(218).fork(), q + 1).ldelim();
            if (m.subscriptionAction != null && $Object.hasOwnProperty.call(m, "subscriptionAction"))
                $root.proto.SyncActionValue.SubscriptionAction.encode(m.subscriptionAction, w.uint32(226).fork(), q + 1).ldelim();
            if (m.userStatusMuteAction != null && $Object.hasOwnProperty.call(m, "userStatusMuteAction"))
                $root.proto.SyncActionValue.UserStatusMuteAction.encode(m.userStatusMuteAction, w.uint32(234).fork(), q + 1).ldelim();
            if (m.timeFormatAction != null && $Object.hasOwnProperty.call(m, "timeFormatAction"))
                $root.proto.SyncActionValue.TimeFormatAction.encode(m.timeFormatAction, w.uint32(242).fork(), q + 1).ldelim();
            if (m.nuxAction != null && $Object.hasOwnProperty.call(m, "nuxAction"))
                $root.proto.SyncActionValue.NuxAction.encode(m.nuxAction, w.uint32(250).fork(), q + 1).ldelim();
            if (m.primaryVersionAction != null && $Object.hasOwnProperty.call(m, "primaryVersionAction"))
                $root.proto.SyncActionValue.PrimaryVersionAction.encode(m.primaryVersionAction, w.uint32(258).fork(), q + 1).ldelim();
            if (m.stickerAction != null && $Object.hasOwnProperty.call(m, "stickerAction"))
                $root.proto.SyncActionValue.StickerAction.encode(m.stickerAction, w.uint32(266).fork(), q + 1).ldelim();
            if (m.removeRecentStickerAction != null && $Object.hasOwnProperty.call(m, "removeRecentStickerAction"))
                $root.proto.SyncActionValue.RemoveRecentStickerAction.encode(m.removeRecentStickerAction, w.uint32(274).fork(), q + 1).ldelim();
            if (m.chatAssignment != null && $Object.hasOwnProperty.call(m, "chatAssignment"))
                $root.proto.SyncActionValue.ChatAssignmentAction.encode(m.chatAssignment, w.uint32(282).fork(), q + 1).ldelim();
            if (m.chatAssignmentOpenedStatus != null && $Object.hasOwnProperty.call(m, "chatAssignmentOpenedStatus"))
                $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.encode(m.chatAssignmentOpenedStatus, w.uint32(290).fork(), q + 1).ldelim();
            if (m.pnForLidChatAction != null && $Object.hasOwnProperty.call(m, "pnForLidChatAction"))
                $root.proto.SyncActionValue.PnForLidChatAction.encode(m.pnForLidChatAction, w.uint32(298).fork(), q + 1).ldelim();
            if (m.marketingMessageAction != null && $Object.hasOwnProperty.call(m, "marketingMessageAction"))
                $root.proto.SyncActionValue.MarketingMessageAction.encode(m.marketingMessageAction, w.uint32(306).fork(), q + 1).ldelim();
            if (m.marketingMessageBroadcastAction != null && $Object.hasOwnProperty.call(m, "marketingMessageBroadcastAction"))
                $root.proto.SyncActionValue.MarketingMessageBroadcastAction.encode(m.marketingMessageBroadcastAction, w.uint32(314).fork(), q + 1).ldelim();
            if (m.externalWebBetaAction != null && $Object.hasOwnProperty.call(m, "externalWebBetaAction"))
                $root.proto.SyncActionValue.ExternalWebBetaAction.encode(m.externalWebBetaAction, w.uint32(322).fork(), q + 1).ldelim();
            if (m.privacySettingRelayAllCalls != null && $Object.hasOwnProperty.call(m, "privacySettingRelayAllCalls"))
                $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.encode(m.privacySettingRelayAllCalls, w.uint32(330).fork(), q + 1).ldelim();
            if (m.callLogAction != null && $Object.hasOwnProperty.call(m, "callLogAction"))
                $root.proto.SyncActionValue.CallLogAction.encode(m.callLogAction, w.uint32(338).fork(), q + 1).ldelim();
            if (m.ugcBot != null && $Object.hasOwnProperty.call(m, "ugcBot"))
                $root.proto.SyncActionValue.UGCBot.encode(m.ugcBot, w.uint32(346).fork(), q + 1).ldelim();
            if (m.statusPrivacy != null && $Object.hasOwnProperty.call(m, "statusPrivacy"))
                $root.proto.SyncActionValue.StatusPrivacyAction.encode(m.statusPrivacy, w.uint32(354).fork(), q + 1).ldelim();
            if (m.botWelcomeRequestAction != null && $Object.hasOwnProperty.call(m, "botWelcomeRequestAction"))
                $root.proto.SyncActionValue.BotWelcomeRequestAction.encode(m.botWelcomeRequestAction, w.uint32(362).fork(), q + 1).ldelim();
            if (m.deleteIndividualCallLog != null && $Object.hasOwnProperty.call(m, "deleteIndividualCallLog"))
                $root.proto.SyncActionValue.DeleteIndividualCallLogAction.encode(m.deleteIndividualCallLog, w.uint32(370).fork(), q + 1).ldelim();
            if (m.labelReorderingAction != null && $Object.hasOwnProperty.call(m, "labelReorderingAction"))
                $root.proto.SyncActionValue.LabelReorderingAction.encode(m.labelReorderingAction, w.uint32(378).fork(), q + 1).ldelim();
            if (m.paymentInfoAction != null && $Object.hasOwnProperty.call(m, "paymentInfoAction"))
                $root.proto.SyncActionValue.PaymentInfoAction.encode(m.paymentInfoAction, w.uint32(386).fork(), q + 1).ldelim();
            if (m.customPaymentMethodsAction != null && $Object.hasOwnProperty.call(m, "customPaymentMethodsAction"))
                $root.proto.SyncActionValue.CustomPaymentMethodsAction.encode(m.customPaymentMethodsAction, w.uint32(394).fork(), q + 1).ldelim();
            if (m.lockChatAction != null && $Object.hasOwnProperty.call(m, "lockChatAction"))
                $root.proto.SyncActionValue.LockChatAction.encode(m.lockChatAction, w.uint32(402).fork(), q + 1).ldelim();
            if (m.chatLockSettings != null && $Object.hasOwnProperty.call(m, "chatLockSettings"))
                $root.proto.ChatLockSettings.encode(m.chatLockSettings, w.uint32(410).fork(), q + 1).ldelim();
            if (m.wamoUserIdentifierAction != null && $Object.hasOwnProperty.call(m, "wamoUserIdentifierAction"))
                $root.proto.SyncActionValue.WamoUserIdentifierAction.encode(m.wamoUserIdentifierAction, w.uint32(418).fork(), q + 1).ldelim();
            if (m.privacySettingDisableLinkPreviewsAction != null && $Object.hasOwnProperty.call(m, "privacySettingDisableLinkPreviewsAction"))
                $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.encode(m.privacySettingDisableLinkPreviewsAction, w.uint32(426).fork(), q + 1).ldelim();
            if (m.deviceCapabilities != null && $Object.hasOwnProperty.call(m, "deviceCapabilities"))
                $root.proto.DeviceCapabilities.encode(m.deviceCapabilities, w.uint32(434).fork(), q + 1).ldelim();
            if (m.noteEditAction != null && $Object.hasOwnProperty.call(m, "noteEditAction"))
                $root.proto.SyncActionValue.NoteEditAction.encode(m.noteEditAction, w.uint32(442).fork(), q + 1).ldelim();
            if (m.favoritesAction != null && $Object.hasOwnProperty.call(m, "favoritesAction"))
                $root.proto.SyncActionValue.FavoritesAction.encode(m.favoritesAction, w.uint32(450).fork(), q + 1).ldelim();
            if (m.merchantPaymentPartnerAction != null && $Object.hasOwnProperty.call(m, "merchantPaymentPartnerAction"))
                $root.proto.SyncActionValue.MerchantPaymentPartnerAction.encode(m.merchantPaymentPartnerAction, w.uint32(458).fork(), q + 1).ldelim();
            if (m.waffleAccountLinkStateAction != null && $Object.hasOwnProperty.call(m, "waffleAccountLinkStateAction"))
                $root.proto.SyncActionValue.WaffleAccountLinkStateAction.encode(m.waffleAccountLinkStateAction, w.uint32(466).fork(), q + 1).ldelim();
            if (m.usernameChatStartMode != null && $Object.hasOwnProperty.call(m, "usernameChatStartMode"))
                $root.proto.SyncActionValue.UsernameChatStartModeAction.encode(m.usernameChatStartMode, w.uint32(474).fork(), q + 1).ldelim();
            if (m.notificationActivitySettingAction != null && $Object.hasOwnProperty.call(m, "notificationActivitySettingAction"))
                $root.proto.SyncActionValue.NotificationActivitySettingAction.encode(m.notificationActivitySettingAction, w.uint32(482).fork(), q + 1).ldelim();
            if (m.lidContactAction != null && $Object.hasOwnProperty.call(m, "lidContactAction"))
                $root.proto.SyncActionValue.LidContactAction.encode(m.lidContactAction, w.uint32(490).fork(), q + 1).ldelim();
            if (m.ctwaPerCustomerDataSharingAction != null && $Object.hasOwnProperty.call(m, "ctwaPerCustomerDataSharingAction"))
                $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.encode(m.ctwaPerCustomerDataSharingAction, w.uint32(498).fork(), q + 1).ldelim();
            if (m.paymentTosAction != null && $Object.hasOwnProperty.call(m, "paymentTosAction"))
                $root.proto.SyncActionValue.PaymentTosAction.encode(m.paymentTosAction, w.uint32(506).fork(), q + 1).ldelim();
            if (m.privacySettingChannelsPersonalisedRecommendationAction != null && $Object.hasOwnProperty.call(m, "privacySettingChannelsPersonalisedRecommendationAction"))
                $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.encode(m.privacySettingChannelsPersonalisedRecommendationAction, w.uint32(514).fork(), q + 1).ldelim();
            if (m.detectedOutcomesStatusAction != null && $Object.hasOwnProperty.call(m, "detectedOutcomesStatusAction"))
                $root.proto.SyncActionValue.DetectedOutcomesStatusAction.encode(m.detectedOutcomesStatusAction, w.uint32(530).fork(), q + 1).ldelim();
            if (m.maibaAiFeaturesControlAction != null && $Object.hasOwnProperty.call(m, "maibaAiFeaturesControlAction"))
                $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.encode(m.maibaAiFeaturesControlAction, w.uint32(546).fork(), q + 1).ldelim();
            if (m.businessBroadcastListAction != null && $Object.hasOwnProperty.call(m, "businessBroadcastListAction"))
                $root.proto.SyncActionValue.BusinessBroadcastListAction.encode(m.businessBroadcastListAction, w.uint32(554).fork(), q + 1).ldelim();
            if (m.musicUserIdAction != null && $Object.hasOwnProperty.call(m, "musicUserIdAction"))
                $root.proto.SyncActionValue.MusicUserIdAction.encode(m.musicUserIdAction, w.uint32(562).fork(), q + 1).ldelim();
            if (m.statusPostOptInNotificationPreferencesAction != null && $Object.hasOwnProperty.call(m, "statusPostOptInNotificationPreferencesAction"))
                $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.encode(m.statusPostOptInNotificationPreferencesAction, w.uint32(570).fork(), q + 1).ldelim();
            if (m.avatarUpdatedAction != null && $Object.hasOwnProperty.call(m, "avatarUpdatedAction"))
                $root.proto.SyncActionValue.AvatarUpdatedAction.encode(m.avatarUpdatedAction, w.uint32(578).fork(), q + 1).ldelim();
            if (m.privateProcessingSettingAction != null && $Object.hasOwnProperty.call(m, "privateProcessingSettingAction"))
                $root.proto.SyncActionValue.PrivateProcessingSettingAction.encode(m.privateProcessingSettingAction, w.uint32(594).fork(), q + 1).ldelim();
            if (m.newsletterSavedInterestsAction != null && $Object.hasOwnProperty.call(m, "newsletterSavedInterestsAction"))
                $root.proto.SyncActionValue.NewsletterSavedInterestsAction.encode(m.newsletterSavedInterestsAction, w.uint32(602).fork(), q + 1).ldelim();
            if (m.aiThreadRenameAction != null && $Object.hasOwnProperty.call(m, "aiThreadRenameAction"))
                $root.proto.SyncActionValue.AiThreadRenameAction.encode(m.aiThreadRenameAction, w.uint32(610).fork(), q + 1).ldelim();
            if (m.interactiveMessageAction != null && $Object.hasOwnProperty.call(m, "interactiveMessageAction"))
                $root.proto.SyncActionValue.InteractiveMessageAction.encode(m.interactiveMessageAction, w.uint32(618).fork(), q + 1).ldelim();
            if (m.settingsSyncAction != null && $Object.hasOwnProperty.call(m, "settingsSyncAction"))
                $root.proto.SyncActionValue.SettingsSyncAction.encode(m.settingsSyncAction, w.uint32(626).fork(), q + 1).ldelim();
            if (m.outContactAction != null && $Object.hasOwnProperty.call(m, "outContactAction"))
                $root.proto.SyncActionValue.OutContactAction.encode(m.outContactAction, w.uint32(634).fork(), q + 1).ldelim();
            if (m.nctSaltSyncAction != null && $Object.hasOwnProperty.call(m, "nctSaltSyncAction"))
                $root.proto.SyncActionValue.NctSaltSyncAction.encode(m.nctSaltSyncAction, w.uint32(642).fork(), q + 1).ldelim();
            if (m.businessBroadcastCampaignAction != null && $Object.hasOwnProperty.call(m, "businessBroadcastCampaignAction"))
                $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.encode(m.businessBroadcastCampaignAction, w.uint32(650).fork(), q + 1).ldelim();
            if (m.businessBroadcastInsightsAction != null && $Object.hasOwnProperty.call(m, "businessBroadcastInsightsAction"))
                $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.encode(m.businessBroadcastInsightsAction, w.uint32(658).fork(), q + 1).ldelim();
            if (m.customerDataAction != null && $Object.hasOwnProperty.call(m, "customerDataAction"))
                $root.proto.SyncActionValue.CustomerDataAction.encode(m.customerDataAction, w.uint32(666).fork(), q + 1).ldelim();
            if (m.subscriptionsSyncV2Action != null && $Object.hasOwnProperty.call(m, "subscriptionsSyncV2Action"))
                $root.proto.SyncActionValue.SubscriptionsSyncV2Action.encode(m.subscriptionsSyncV2Action, w.uint32(674).fork(), q + 1).ldelim();
            if (m.threadPinAction != null && $Object.hasOwnProperty.call(m, "threadPinAction"))
                $root.proto.SyncActionValue.ThreadPinAction.encode(m.threadPinAction, w.uint32(682).fork(), q + 1).ldelim();
            if (m.autoOrganizeBusinessChatSetting != null && $Object.hasOwnProperty.call(m, "autoOrganizeBusinessChatSetting"))
                $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.encode(m.autoOrganizeBusinessChatSetting, w.uint32(690).fork(), q + 1).ldelim();
            if (m.bizAiSettingsNudgeAction != null && $Object.hasOwnProperty.call(m, "bizAiSettingsNudgeAction"))
                $root.proto.SyncActionValue.BizAISettingsNudgeAction.encode(m.bizAiSettingsNudgeAction, w.uint32(698).fork(), q + 1).ldelim();
            if (m.coexV2VersionAction != null && $Object.hasOwnProperty.call(m, "coexV2VersionAction"))
                $root.proto.SyncActionValue.CoexV2VersionAction.encode(m.coexV2VersionAction, w.uint32(706).fork(), q + 1).ldelim();
            if (m.wasaRootSecretAction != null && $Object.hasOwnProperty.call(m, "wasaRootSecretAction"))
                $root.proto.SyncActionValue.WASARootSecretAction.encode(m.wasaRootSecretAction, w.uint32(714).fork(), q + 1).ldelim();
            if (m.bubbleLockMessageAction != null && $Object.hasOwnProperty.call(m, "bubbleLockMessageAction"))
                $root.proto.SyncActionValue.BubbleLockMessageAction.encode(m.bubbleLockMessageAction, w.uint32(722).fork(), q + 1).ldelim();
            if (m.labelSublistAction != null && $Object.hasOwnProperty.call(m, "labelSublistAction"))
                $root.proto.SyncActionValue.LabelSublistAction.encode(m.labelSublistAction, w.uint32(730).fork(), q + 1).ldelim();
            if (m.deviceCapabilitiesV2 != null && $Object.hasOwnProperty.call(m, "deviceCapabilitiesV2"))
                $root.proto.DeviceCapabilities.encode(m.deviceCapabilitiesV2, w.uint32(738).fork(), q + 1).ldelim();
            if (m.ctwaMessageReceivedAction != null && $Object.hasOwnProperty.call(m, "ctwaMessageReceivedAction"))
                $root.proto.SyncActionValue.CtwaMessageReceivedAction.encode(m.ctwaMessageReceivedAction, w.uint32(746).fork(), q + 1).ldelim();
            if (m.sharedDeviceAllowlistAction != null && $Object.hasOwnProperty.call(m, "sharedDeviceAllowlistAction"))
                $root.proto.SyncActionValue.SharedDeviceAllowlistAction.encode(m.sharedDeviceAllowlistAction, w.uint32(754).fork(), q + 1).ldelim();
            if (m.contactManagerMetadataAction != null && $Object.hasOwnProperty.call(m, "contactManagerMetadataAction"))
                $root.proto.SyncActionValue.ContactManagerMetadataAction.encode(m.contactManagerMetadataAction, w.uint32(762).fork(), q + 1).ldelim();
            if (m.businessFolderActivationAction != null && $Object.hasOwnProperty.call(m, "businessFolderActivationAction"))
                $root.proto.SyncActionValue.BusinessFolderActivationAction.encode(m.businessFolderActivationAction, w.uint32(770).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncActionValue.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.timestamp = r.int64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.starAction = $root.proto.SyncActionValue.StarAction.decode(r, r.uint32(), $undefined, q + 1, m.starAction);
                        m._starAction = "starAction";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.contactAction = $root.proto.SyncActionValue.ContactAction.decode(r, r.uint32(), $undefined, q + 1, m.contactAction);
                        m._contactAction = "contactAction";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.muteAction = $root.proto.SyncActionValue.MuteAction.decode(r, r.uint32(), $undefined, q + 1, m.muteAction);
                        m._muteAction = "muteAction";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.pinAction = $root.proto.SyncActionValue.PinAction.decode(r, r.uint32(), $undefined, q + 1, m.pinAction);
                        m._pinAction = "pinAction";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.pushNameSetting = $root.proto.SyncActionValue.PushNameSetting.decode(r, r.uint32(), $undefined, q + 1, m.pushNameSetting);
                        m._pushNameSetting = "pushNameSetting";
                        continue;
                    }
                case 8: {
                        if (u !== 2)
                            break;
                        m.quickReplyAction = $root.proto.SyncActionValue.QuickReplyAction.decode(r, r.uint32(), $undefined, q + 1, m.quickReplyAction);
                        m._quickReplyAction = "quickReplyAction";
                        continue;
                    }
                case 11: {
                        if (u !== 2)
                            break;
                        m.recentEmojiWeightsAction = $root.proto.SyncActionValue.RecentEmojiWeightsAction.decode(r, r.uint32(), $undefined, q + 1, m.recentEmojiWeightsAction);
                        m._recentEmojiWeightsAction = "recentEmojiWeightsAction";
                        continue;
                    }
                case 14: {
                        if (u !== 2)
                            break;
                        m.labelEditAction = $root.proto.SyncActionValue.LabelEditAction.decode(r, r.uint32(), $undefined, q + 1, m.labelEditAction);
                        m._labelEditAction = "labelEditAction";
                        continue;
                    }
                case 15: {
                        if (u !== 2)
                            break;
                        m.labelAssociationAction = $root.proto.SyncActionValue.LabelAssociationAction.decode(r, r.uint32(), $undefined, q + 1, m.labelAssociationAction);
                        m._labelAssociationAction = "labelAssociationAction";
                        continue;
                    }
                case 16: {
                        if (u !== 2)
                            break;
                        m.localeSetting = $root.proto.SyncActionValue.LocaleSetting.decode(r, r.uint32(), $undefined, q + 1, m.localeSetting);
                        m._localeSetting = "localeSetting";
                        continue;
                    }
                case 17: {
                        if (u !== 2)
                            break;
                        m.archiveChatAction = $root.proto.SyncActionValue.ArchiveChatAction.decode(r, r.uint32(), $undefined, q + 1, m.archiveChatAction);
                        m._archiveChatAction = "archiveChatAction";
                        continue;
                    }
                case 18: {
                        if (u !== 2)
                            break;
                        m.deleteMessageForMeAction = $root.proto.SyncActionValue.DeleteMessageForMeAction.decode(r, r.uint32(), $undefined, q + 1, m.deleteMessageForMeAction);
                        m._deleteMessageForMeAction = "deleteMessageForMeAction";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.keyExpiration = $root.proto.SyncActionValue.KeyExpiration.decode(r, r.uint32(), $undefined, q + 1, m.keyExpiration);
                        m._keyExpiration = "keyExpiration";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        m.markChatAsReadAction = $root.proto.SyncActionValue.MarkChatAsReadAction.decode(r, r.uint32(), $undefined, q + 1, m.markChatAsReadAction);
                        m._markChatAsReadAction = "markChatAsReadAction";
                        continue;
                    }
                case 21: {
                        if (u !== 2)
                            break;
                        m.clearChatAction = $root.proto.SyncActionValue.ClearChatAction.decode(r, r.uint32(), $undefined, q + 1, m.clearChatAction);
                        m._clearChatAction = "clearChatAction";
                        continue;
                    }
                case 22: {
                        if (u !== 2)
                            break;
                        m.deleteChatAction = $root.proto.SyncActionValue.DeleteChatAction.decode(r, r.uint32(), $undefined, q + 1, m.deleteChatAction);
                        m._deleteChatAction = "deleteChatAction";
                        continue;
                    }
                case 23: {
                        if (u !== 2)
                            break;
                        m.unarchiveChatsSetting = $root.proto.SyncActionValue.UnarchiveChatsSetting.decode(r, r.uint32(), $undefined, q + 1, m.unarchiveChatsSetting);
                        m._unarchiveChatsSetting = "unarchiveChatsSetting";
                        continue;
                    }
                case 24: {
                        if (u !== 2)
                            break;
                        m.primaryFeature = $root.proto.SyncActionValue.PrimaryFeature.decode(r, r.uint32(), $undefined, q + 1, m.primaryFeature);
                        m._primaryFeature = "primaryFeature";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        m.androidUnsupportedActions = $root.proto.SyncActionValue.AndroidUnsupportedActions.decode(r, r.uint32(), $undefined, q + 1, m.androidUnsupportedActions);
                        m._androidUnsupportedActions = "androidUnsupportedActions";
                        continue;
                    }
                case 27: {
                        if (u !== 2)
                            break;
                        m.agentAction = $root.proto.SyncActionValue.AgentAction.decode(r, r.uint32(), $undefined, q + 1, m.agentAction);
                        m._agentAction = "agentAction";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        m.subscriptionAction = $root.proto.SyncActionValue.SubscriptionAction.decode(r, r.uint32(), $undefined, q + 1, m.subscriptionAction);
                        m._subscriptionAction = "subscriptionAction";
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        m.userStatusMuteAction = $root.proto.SyncActionValue.UserStatusMuteAction.decode(r, r.uint32(), $undefined, q + 1, m.userStatusMuteAction);
                        m._userStatusMuteAction = "userStatusMuteAction";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        m.timeFormatAction = $root.proto.SyncActionValue.TimeFormatAction.decode(r, r.uint32(), $undefined, q + 1, m.timeFormatAction);
                        m._timeFormatAction = "timeFormatAction";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        m.nuxAction = $root.proto.SyncActionValue.NuxAction.decode(r, r.uint32(), $undefined, q + 1, m.nuxAction);
                        m._nuxAction = "nuxAction";
                        continue;
                    }
                case 32: {
                        if (u !== 2)
                            break;
                        m.primaryVersionAction = $root.proto.SyncActionValue.PrimaryVersionAction.decode(r, r.uint32(), $undefined, q + 1, m.primaryVersionAction);
                        m._primaryVersionAction = "primaryVersionAction";
                        continue;
                    }
                case 33: {
                        if (u !== 2)
                            break;
                        m.stickerAction = $root.proto.SyncActionValue.StickerAction.decode(r, r.uint32(), $undefined, q + 1, m.stickerAction);
                        m._stickerAction = "stickerAction";
                        continue;
                    }
                case 34: {
                        if (u !== 2)
                            break;
                        m.removeRecentStickerAction = $root.proto.SyncActionValue.RemoveRecentStickerAction.decode(r, r.uint32(), $undefined, q + 1, m.removeRecentStickerAction);
                        m._removeRecentStickerAction = "removeRecentStickerAction";
                        continue;
                    }
                case 35: {
                        if (u !== 2)
                            break;
                        m.chatAssignment = $root.proto.SyncActionValue.ChatAssignmentAction.decode(r, r.uint32(), $undefined, q + 1, m.chatAssignment);
                        m._chatAssignment = "chatAssignment";
                        continue;
                    }
                case 36: {
                        if (u !== 2)
                            break;
                        m.chatAssignmentOpenedStatus = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.decode(r, r.uint32(), $undefined, q + 1, m.chatAssignmentOpenedStatus);
                        m._chatAssignmentOpenedStatus = "chatAssignmentOpenedStatus";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        m.pnForLidChatAction = $root.proto.SyncActionValue.PnForLidChatAction.decode(r, r.uint32(), $undefined, q + 1, m.pnForLidChatAction);
                        m._pnForLidChatAction = "pnForLidChatAction";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.marketingMessageAction = $root.proto.SyncActionValue.MarketingMessageAction.decode(r, r.uint32(), $undefined, q + 1, m.marketingMessageAction);
                        m._marketingMessageAction = "marketingMessageAction";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        m.marketingMessageBroadcastAction = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.decode(r, r.uint32(), $undefined, q + 1, m.marketingMessageBroadcastAction);
                        m._marketingMessageBroadcastAction = "marketingMessageBroadcastAction";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        m.externalWebBetaAction = $root.proto.SyncActionValue.ExternalWebBetaAction.decode(r, r.uint32(), $undefined, q + 1, m.externalWebBetaAction);
                        m._externalWebBetaAction = "externalWebBetaAction";
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        m.privacySettingRelayAllCalls = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.decode(r, r.uint32(), $undefined, q + 1, m.privacySettingRelayAllCalls);
                        m._privacySettingRelayAllCalls = "privacySettingRelayAllCalls";
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        m.callLogAction = $root.proto.SyncActionValue.CallLogAction.decode(r, r.uint32(), $undefined, q + 1, m.callLogAction);
                        m._callLogAction = "callLogAction";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.ugcBot = $root.proto.SyncActionValue.UGCBot.decode(r, r.uint32(), $undefined, q + 1, m.ugcBot);
                        m._ugcBot = "ugcBot";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        m.statusPrivacy = $root.proto.SyncActionValue.StatusPrivacyAction.decode(r, r.uint32(), $undefined, q + 1, m.statusPrivacy);
                        m._statusPrivacy = "statusPrivacy";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        m.botWelcomeRequestAction = $root.proto.SyncActionValue.BotWelcomeRequestAction.decode(r, r.uint32(), $undefined, q + 1, m.botWelcomeRequestAction);
                        m._botWelcomeRequestAction = "botWelcomeRequestAction";
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        m.deleteIndividualCallLog = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.decode(r, r.uint32(), $undefined, q + 1, m.deleteIndividualCallLog);
                        m._deleteIndividualCallLog = "deleteIndividualCallLog";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        m.labelReorderingAction = $root.proto.SyncActionValue.LabelReorderingAction.decode(r, r.uint32(), $undefined, q + 1, m.labelReorderingAction);
                        m._labelReorderingAction = "labelReorderingAction";
                        continue;
                    }
                case 48: {
                        if (u !== 2)
                            break;
                        m.paymentInfoAction = $root.proto.SyncActionValue.PaymentInfoAction.decode(r, r.uint32(), $undefined, q + 1, m.paymentInfoAction);
                        m._paymentInfoAction = "paymentInfoAction";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        m.customPaymentMethodsAction = $root.proto.SyncActionValue.CustomPaymentMethodsAction.decode(r, r.uint32(), $undefined, q + 1, m.customPaymentMethodsAction);
                        m._customPaymentMethodsAction = "customPaymentMethodsAction";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        m.lockChatAction = $root.proto.SyncActionValue.LockChatAction.decode(r, r.uint32(), $undefined, q + 1, m.lockChatAction);
                        m._lockChatAction = "lockChatAction";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        m.chatLockSettings = $root.proto.ChatLockSettings.decode(r, r.uint32(), $undefined, q + 1, m.chatLockSettings);
                        m._chatLockSettings = "chatLockSettings";
                        continue;
                    }
                case 52: {
                        if (u !== 2)
                            break;
                        m.wamoUserIdentifierAction = $root.proto.SyncActionValue.WamoUserIdentifierAction.decode(r, r.uint32(), $undefined, q + 1, m.wamoUserIdentifierAction);
                        m._wamoUserIdentifierAction = "wamoUserIdentifierAction";
                        continue;
                    }
                case 53: {
                        if (u !== 2)
                            break;
                        m.privacySettingDisableLinkPreviewsAction = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.decode(r, r.uint32(), $undefined, q + 1, m.privacySettingDisableLinkPreviewsAction);
                        m._privacySettingDisableLinkPreviewsAction = "privacySettingDisableLinkPreviewsAction";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        m.deviceCapabilities = $root.proto.DeviceCapabilities.decode(r, r.uint32(), $undefined, q + 1, m.deviceCapabilities);
                        m._deviceCapabilities = "deviceCapabilities";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        m.noteEditAction = $root.proto.SyncActionValue.NoteEditAction.decode(r, r.uint32(), $undefined, q + 1, m.noteEditAction);
                        m._noteEditAction = "noteEditAction";
                        continue;
                    }
                case 56: {
                        if (u !== 2)
                            break;
                        m.favoritesAction = $root.proto.SyncActionValue.FavoritesAction.decode(r, r.uint32(), $undefined, q + 1, m.favoritesAction);
                        m._favoritesAction = "favoritesAction";
                        continue;
                    }
                case 57: {
                        if (u !== 2)
                            break;
                        m.merchantPaymentPartnerAction = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.decode(r, r.uint32(), $undefined, q + 1, m.merchantPaymentPartnerAction);
                        m._merchantPaymentPartnerAction = "merchantPaymentPartnerAction";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        m.waffleAccountLinkStateAction = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.decode(r, r.uint32(), $undefined, q + 1, m.waffleAccountLinkStateAction);
                        m._waffleAccountLinkStateAction = "waffleAccountLinkStateAction";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        m.usernameChatStartMode = $root.proto.SyncActionValue.UsernameChatStartModeAction.decode(r, r.uint32(), $undefined, q + 1, m.usernameChatStartMode);
                        m._usernameChatStartMode = "usernameChatStartMode";
                        continue;
                    }
                case 60: {
                        if (u !== 2)
                            break;
                        m.notificationActivitySettingAction = $root.proto.SyncActionValue.NotificationActivitySettingAction.decode(r, r.uint32(), $undefined, q + 1, m.notificationActivitySettingAction);
                        m._notificationActivitySettingAction = "notificationActivitySettingAction";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        m.lidContactAction = $root.proto.SyncActionValue.LidContactAction.decode(r, r.uint32(), $undefined, q + 1, m.lidContactAction);
                        m._lidContactAction = "lidContactAction";
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        m.ctwaPerCustomerDataSharingAction = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.decode(r, r.uint32(), $undefined, q + 1, m.ctwaPerCustomerDataSharingAction);
                        m._ctwaPerCustomerDataSharingAction = "ctwaPerCustomerDataSharingAction";
                        continue;
                    }
                case 63: {
                        if (u !== 2)
                            break;
                        m.paymentTosAction = $root.proto.SyncActionValue.PaymentTosAction.decode(r, r.uint32(), $undefined, q + 1, m.paymentTosAction);
                        m._paymentTosAction = "paymentTosAction";
                        continue;
                    }
                case 64: {
                        if (u !== 2)
                            break;
                        m.privacySettingChannelsPersonalisedRecommendationAction = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.decode(r, r.uint32(), $undefined, q + 1, m.privacySettingChannelsPersonalisedRecommendationAction);
                        m._privacySettingChannelsPersonalisedRecommendationAction = "privacySettingChannelsPersonalisedRecommendationAction";
                        continue;
                    }
                case 66: {
                        if (u !== 2)
                            break;
                        m.detectedOutcomesStatusAction = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.decode(r, r.uint32(), $undefined, q + 1, m.detectedOutcomesStatusAction);
                        m._detectedOutcomesStatusAction = "detectedOutcomesStatusAction";
                        continue;
                    }
                case 68: {
                        if (u !== 2)
                            break;
                        m.maibaAiFeaturesControlAction = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.decode(r, r.uint32(), $undefined, q + 1, m.maibaAiFeaturesControlAction);
                        m._maibaAiFeaturesControlAction = "maibaAiFeaturesControlAction";
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        m.businessBroadcastListAction = $root.proto.SyncActionValue.BusinessBroadcastListAction.decode(r, r.uint32(), $undefined, q + 1, m.businessBroadcastListAction);
                        m._businessBroadcastListAction = "businessBroadcastListAction";
                        continue;
                    }
                case 70: {
                        if (u !== 2)
                            break;
                        m.musicUserIdAction = $root.proto.SyncActionValue.MusicUserIdAction.decode(r, r.uint32(), $undefined, q + 1, m.musicUserIdAction);
                        m._musicUserIdAction = "musicUserIdAction";
                        continue;
                    }
                case 71: {
                        if (u !== 2)
                            break;
                        m.statusPostOptInNotificationPreferencesAction = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.decode(r, r.uint32(), $undefined, q + 1, m.statusPostOptInNotificationPreferencesAction);
                        m._statusPostOptInNotificationPreferencesAction = "statusPostOptInNotificationPreferencesAction";
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        m.avatarUpdatedAction = $root.proto.SyncActionValue.AvatarUpdatedAction.decode(r, r.uint32(), $undefined, q + 1, m.avatarUpdatedAction);
                        m._avatarUpdatedAction = "avatarUpdatedAction";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        m.privateProcessingSettingAction = $root.proto.SyncActionValue.PrivateProcessingSettingAction.decode(r, r.uint32(), $undefined, q + 1, m.privateProcessingSettingAction);
                        m._privateProcessingSettingAction = "privateProcessingSettingAction";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        m.newsletterSavedInterestsAction = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.decode(r, r.uint32(), $undefined, q + 1, m.newsletterSavedInterestsAction);
                        m._newsletterSavedInterestsAction = "newsletterSavedInterestsAction";
                        continue;
                    }
                case 76: {
                        if (u !== 2)
                            break;
                        m.aiThreadRenameAction = $root.proto.SyncActionValue.AiThreadRenameAction.decode(r, r.uint32(), $undefined, q + 1, m.aiThreadRenameAction);
                        m._aiThreadRenameAction = "aiThreadRenameAction";
                        continue;
                    }
                case 77: {
                        if (u !== 2)
                            break;
                        m.interactiveMessageAction = $root.proto.SyncActionValue.InteractiveMessageAction.decode(r, r.uint32(), $undefined, q + 1, m.interactiveMessageAction);
                        m._interactiveMessageAction = "interactiveMessageAction";
                        continue;
                    }
                case 78: {
                        if (u !== 2)
                            break;
                        m.settingsSyncAction = $root.proto.SyncActionValue.SettingsSyncAction.decode(r, r.uint32(), $undefined, q + 1, m.settingsSyncAction);
                        m._settingsSyncAction = "settingsSyncAction";
                        continue;
                    }
                case 79: {
                        if (u !== 2)
                            break;
                        m.outContactAction = $root.proto.SyncActionValue.OutContactAction.decode(r, r.uint32(), $undefined, q + 1, m.outContactAction);
                        m._outContactAction = "outContactAction";
                        continue;
                    }
                case 80: {
                        if (u !== 2)
                            break;
                        m.nctSaltSyncAction = $root.proto.SyncActionValue.NctSaltSyncAction.decode(r, r.uint32(), $undefined, q + 1, m.nctSaltSyncAction);
                        m._nctSaltSyncAction = "nctSaltSyncAction";
                        continue;
                    }
                case 81: {
                        if (u !== 2)
                            break;
                        m.businessBroadcastCampaignAction = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.decode(r, r.uint32(), $undefined, q + 1, m.businessBroadcastCampaignAction);
                        m._businessBroadcastCampaignAction = "businessBroadcastCampaignAction";
                        continue;
                    }
                case 82: {
                        if (u !== 2)
                            break;
                        m.businessBroadcastInsightsAction = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.decode(r, r.uint32(), $undefined, q + 1, m.businessBroadcastInsightsAction);
                        m._businessBroadcastInsightsAction = "businessBroadcastInsightsAction";
                        continue;
                    }
                case 83: {
                        if (u !== 2)
                            break;
                        m.customerDataAction = $root.proto.SyncActionValue.CustomerDataAction.decode(r, r.uint32(), $undefined, q + 1, m.customerDataAction);
                        m._customerDataAction = "customerDataAction";
                        continue;
                    }
                case 84: {
                        if (u !== 2)
                            break;
                        m.subscriptionsSyncV2Action = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.decode(r, r.uint32(), $undefined, q + 1, m.subscriptionsSyncV2Action);
                        m._subscriptionsSyncV2Action = "subscriptionsSyncV2Action";
                        continue;
                    }
                case 85: {
                        if (u !== 2)
                            break;
                        m.threadPinAction = $root.proto.SyncActionValue.ThreadPinAction.decode(r, r.uint32(), $undefined, q + 1, m.threadPinAction);
                        m._threadPinAction = "threadPinAction";
                        continue;
                    }
                case 86: {
                        if (u !== 2)
                            break;
                        m.autoOrganizeBusinessChatSetting = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.decode(r, r.uint32(), $undefined, q + 1, m.autoOrganizeBusinessChatSetting);
                        m._autoOrganizeBusinessChatSetting = "autoOrganizeBusinessChatSetting";
                        continue;
                    }
                case 87: {
                        if (u !== 2)
                            break;
                        m.bizAiSettingsNudgeAction = $root.proto.SyncActionValue.BizAISettingsNudgeAction.decode(r, r.uint32(), $undefined, q + 1, m.bizAiSettingsNudgeAction);
                        m._bizAiSettingsNudgeAction = "bizAiSettingsNudgeAction";
                        continue;
                    }
                case 88: {
                        if (u !== 2)
                            break;
                        m.coexV2VersionAction = $root.proto.SyncActionValue.CoexV2VersionAction.decode(r, r.uint32(), $undefined, q + 1, m.coexV2VersionAction);
                        m._coexV2VersionAction = "coexV2VersionAction";
                        continue;
                    }
                case 89: {
                        if (u !== 2)
                            break;
                        m.wasaRootSecretAction = $root.proto.SyncActionValue.WASARootSecretAction.decode(r, r.uint32(), $undefined, q + 1, m.wasaRootSecretAction);
                        m._wasaRootSecretAction = "wasaRootSecretAction";
                        continue;
                    }
                case 90: {
                        if (u !== 2)
                            break;
                        m.bubbleLockMessageAction = $root.proto.SyncActionValue.BubbleLockMessageAction.decode(r, r.uint32(), $undefined, q + 1, m.bubbleLockMessageAction);
                        m._bubbleLockMessageAction = "bubbleLockMessageAction";
                        continue;
                    }
                case 91: {
                        if (u !== 2)
                            break;
                        m.labelSublistAction = $root.proto.SyncActionValue.LabelSublistAction.decode(r, r.uint32(), $undefined, q + 1, m.labelSublistAction);
                        m._labelSublistAction = "labelSublistAction";
                        continue;
                    }
                case 92: {
                        if (u !== 2)
                            break;
                        m.deviceCapabilitiesV2 = $root.proto.DeviceCapabilities.decode(r, r.uint32(), $undefined, q + 1, m.deviceCapabilitiesV2);
                        m._deviceCapabilitiesV2 = "deviceCapabilitiesV2";
                        continue;
                    }
                case 93: {
                        if (u !== 2)
                            break;
                        m.ctwaMessageReceivedAction = $root.proto.SyncActionValue.CtwaMessageReceivedAction.decode(r, r.uint32(), $undefined, q + 1, m.ctwaMessageReceivedAction);
                        m._ctwaMessageReceivedAction = "ctwaMessageReceivedAction";
                        continue;
                    }
                case 94: {
                        if (u !== 2)
                            break;
                        m.sharedDeviceAllowlistAction = $root.proto.SyncActionValue.SharedDeviceAllowlistAction.decode(r, r.uint32(), $undefined, q + 1, m.sharedDeviceAllowlistAction);
                        m._sharedDeviceAllowlistAction = "sharedDeviceAllowlistAction";
                        continue;
                    }
                case 95: {
                        if (u !== 2)
                            break;
                        m.contactManagerMetadataAction = $root.proto.SyncActionValue.ContactManagerMetadataAction.decode(r, r.uint32(), $undefined, q + 1, m.contactManagerMetadataAction);
                        m._contactManagerMetadataAction = "contactManagerMetadataAction";
                        continue;
                    }
                case 96: {
                        if (u !== 2)
                            break;
                        m.businessFolderActivationAction = $root.proto.SyncActionValue.BusinessFolderActivationAction.decode(r, r.uint32(), $undefined, q + 1, m.businessFolderActivationAction);
                        m._businessFolderActivationAction = "businessFolderActivationAction";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncActionValue.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncActionValue)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncActionValue: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncActionValue();
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, false);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
            }
            if (d.starAction != null) {
                if (!$util.isObject(d.starAction))
                    throw $TypeError(".proto.SyncActionValue.starAction: object expected");
                m.starAction = $root.proto.SyncActionValue.StarAction.fromObject(d.starAction, q + 1);
            }
            if (d.contactAction != null) {
                if (!$util.isObject(d.contactAction))
                    throw $TypeError(".proto.SyncActionValue.contactAction: object expected");
                m.contactAction = $root.proto.SyncActionValue.ContactAction.fromObject(d.contactAction, q + 1);
            }
            if (d.muteAction != null) {
                if (!$util.isObject(d.muteAction))
                    throw $TypeError(".proto.SyncActionValue.muteAction: object expected");
                m.muteAction = $root.proto.SyncActionValue.MuteAction.fromObject(d.muteAction, q + 1);
            }
            if (d.pinAction != null) {
                if (!$util.isObject(d.pinAction))
                    throw $TypeError(".proto.SyncActionValue.pinAction: object expected");
                m.pinAction = $root.proto.SyncActionValue.PinAction.fromObject(d.pinAction, q + 1);
            }
            if (d.pushNameSetting != null) {
                if (!$util.isObject(d.pushNameSetting))
                    throw $TypeError(".proto.SyncActionValue.pushNameSetting: object expected");
                m.pushNameSetting = $root.proto.SyncActionValue.PushNameSetting.fromObject(d.pushNameSetting, q + 1);
            }
            if (d.quickReplyAction != null) {
                if (!$util.isObject(d.quickReplyAction))
                    throw $TypeError(".proto.SyncActionValue.quickReplyAction: object expected");
                m.quickReplyAction = $root.proto.SyncActionValue.QuickReplyAction.fromObject(d.quickReplyAction, q + 1);
            }
            if (d.recentEmojiWeightsAction != null) {
                if (!$util.isObject(d.recentEmojiWeightsAction))
                    throw $TypeError(".proto.SyncActionValue.recentEmojiWeightsAction: object expected");
                m.recentEmojiWeightsAction = $root.proto.SyncActionValue.RecentEmojiWeightsAction.fromObject(d.recentEmojiWeightsAction, q + 1);
            }
            if (d.labelEditAction != null) {
                if (!$util.isObject(d.labelEditAction))
                    throw $TypeError(".proto.SyncActionValue.labelEditAction: object expected");
                m.labelEditAction = $root.proto.SyncActionValue.LabelEditAction.fromObject(d.labelEditAction, q + 1);
            }
            if (d.labelAssociationAction != null) {
                if (!$util.isObject(d.labelAssociationAction))
                    throw $TypeError(".proto.SyncActionValue.labelAssociationAction: object expected");
                m.labelAssociationAction = $root.proto.SyncActionValue.LabelAssociationAction.fromObject(d.labelAssociationAction, q + 1);
            }
            if (d.localeSetting != null) {
                if (!$util.isObject(d.localeSetting))
                    throw $TypeError(".proto.SyncActionValue.localeSetting: object expected");
                m.localeSetting = $root.proto.SyncActionValue.LocaleSetting.fromObject(d.localeSetting, q + 1);
            }
            if (d.archiveChatAction != null) {
                if (!$util.isObject(d.archiveChatAction))
                    throw $TypeError(".proto.SyncActionValue.archiveChatAction: object expected");
                m.archiveChatAction = $root.proto.SyncActionValue.ArchiveChatAction.fromObject(d.archiveChatAction, q + 1);
            }
            if (d.deleteMessageForMeAction != null) {
                if (!$util.isObject(d.deleteMessageForMeAction))
                    throw $TypeError(".proto.SyncActionValue.deleteMessageForMeAction: object expected");
                m.deleteMessageForMeAction = $root.proto.SyncActionValue.DeleteMessageForMeAction.fromObject(d.deleteMessageForMeAction, q + 1);
            }
            if (d.keyExpiration != null) {
                if (!$util.isObject(d.keyExpiration))
                    throw $TypeError(".proto.SyncActionValue.keyExpiration: object expected");
                m.keyExpiration = $root.proto.SyncActionValue.KeyExpiration.fromObject(d.keyExpiration, q + 1);
            }
            if (d.markChatAsReadAction != null) {
                if (!$util.isObject(d.markChatAsReadAction))
                    throw $TypeError(".proto.SyncActionValue.markChatAsReadAction: object expected");
                m.markChatAsReadAction = $root.proto.SyncActionValue.MarkChatAsReadAction.fromObject(d.markChatAsReadAction, q + 1);
            }
            if (d.clearChatAction != null) {
                if (!$util.isObject(d.clearChatAction))
                    throw $TypeError(".proto.SyncActionValue.clearChatAction: object expected");
                m.clearChatAction = $root.proto.SyncActionValue.ClearChatAction.fromObject(d.clearChatAction, q + 1);
            }
            if (d.deleteChatAction != null) {
                if (!$util.isObject(d.deleteChatAction))
                    throw $TypeError(".proto.SyncActionValue.deleteChatAction: object expected");
                m.deleteChatAction = $root.proto.SyncActionValue.DeleteChatAction.fromObject(d.deleteChatAction, q + 1);
            }
            if (d.unarchiveChatsSetting != null) {
                if (!$util.isObject(d.unarchiveChatsSetting))
                    throw $TypeError(".proto.SyncActionValue.unarchiveChatsSetting: object expected");
                m.unarchiveChatsSetting = $root.proto.SyncActionValue.UnarchiveChatsSetting.fromObject(d.unarchiveChatsSetting, q + 1);
            }
            if (d.primaryFeature != null) {
                if (!$util.isObject(d.primaryFeature))
                    throw $TypeError(".proto.SyncActionValue.primaryFeature: object expected");
                m.primaryFeature = $root.proto.SyncActionValue.PrimaryFeature.fromObject(d.primaryFeature, q + 1);
            }
            if (d.androidUnsupportedActions != null) {
                if (!$util.isObject(d.androidUnsupportedActions))
                    throw $TypeError(".proto.SyncActionValue.androidUnsupportedActions: object expected");
                m.androidUnsupportedActions = $root.proto.SyncActionValue.AndroidUnsupportedActions.fromObject(d.androidUnsupportedActions, q + 1);
            }
            if (d.agentAction != null) {
                if (!$util.isObject(d.agentAction))
                    throw $TypeError(".proto.SyncActionValue.agentAction: object expected");
                m.agentAction = $root.proto.SyncActionValue.AgentAction.fromObject(d.agentAction, q + 1);
            }
            if (d.subscriptionAction != null) {
                if (!$util.isObject(d.subscriptionAction))
                    throw $TypeError(".proto.SyncActionValue.subscriptionAction: object expected");
                m.subscriptionAction = $root.proto.SyncActionValue.SubscriptionAction.fromObject(d.subscriptionAction, q + 1);
            }
            if (d.userStatusMuteAction != null) {
                if (!$util.isObject(d.userStatusMuteAction))
                    throw $TypeError(".proto.SyncActionValue.userStatusMuteAction: object expected");
                m.userStatusMuteAction = $root.proto.SyncActionValue.UserStatusMuteAction.fromObject(d.userStatusMuteAction, q + 1);
            }
            if (d.timeFormatAction != null) {
                if (!$util.isObject(d.timeFormatAction))
                    throw $TypeError(".proto.SyncActionValue.timeFormatAction: object expected");
                m.timeFormatAction = $root.proto.SyncActionValue.TimeFormatAction.fromObject(d.timeFormatAction, q + 1);
            }
            if (d.nuxAction != null) {
                if (!$util.isObject(d.nuxAction))
                    throw $TypeError(".proto.SyncActionValue.nuxAction: object expected");
                m.nuxAction = $root.proto.SyncActionValue.NuxAction.fromObject(d.nuxAction, q + 1);
            }
            if (d.primaryVersionAction != null) {
                if (!$util.isObject(d.primaryVersionAction))
                    throw $TypeError(".proto.SyncActionValue.primaryVersionAction: object expected");
                m.primaryVersionAction = $root.proto.SyncActionValue.PrimaryVersionAction.fromObject(d.primaryVersionAction, q + 1);
            }
            if (d.stickerAction != null) {
                if (!$util.isObject(d.stickerAction))
                    throw $TypeError(".proto.SyncActionValue.stickerAction: object expected");
                m.stickerAction = $root.proto.SyncActionValue.StickerAction.fromObject(d.stickerAction, q + 1);
            }
            if (d.removeRecentStickerAction != null) {
                if (!$util.isObject(d.removeRecentStickerAction))
                    throw $TypeError(".proto.SyncActionValue.removeRecentStickerAction: object expected");
                m.removeRecentStickerAction = $root.proto.SyncActionValue.RemoveRecentStickerAction.fromObject(d.removeRecentStickerAction, q + 1);
            }
            if (d.chatAssignment != null) {
                if (!$util.isObject(d.chatAssignment))
                    throw $TypeError(".proto.SyncActionValue.chatAssignment: object expected");
                m.chatAssignment = $root.proto.SyncActionValue.ChatAssignmentAction.fromObject(d.chatAssignment, q + 1);
            }
            if (d.chatAssignmentOpenedStatus != null) {
                if (!$util.isObject(d.chatAssignmentOpenedStatus))
                    throw $TypeError(".proto.SyncActionValue.chatAssignmentOpenedStatus: object expected");
                m.chatAssignmentOpenedStatus = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.fromObject(d.chatAssignmentOpenedStatus, q + 1);
            }
            if (d.pnForLidChatAction != null) {
                if (!$util.isObject(d.pnForLidChatAction))
                    throw $TypeError(".proto.SyncActionValue.pnForLidChatAction: object expected");
                m.pnForLidChatAction = $root.proto.SyncActionValue.PnForLidChatAction.fromObject(d.pnForLidChatAction, q + 1);
            }
            if (d.marketingMessageAction != null) {
                if (!$util.isObject(d.marketingMessageAction))
                    throw $TypeError(".proto.SyncActionValue.marketingMessageAction: object expected");
                m.marketingMessageAction = $root.proto.SyncActionValue.MarketingMessageAction.fromObject(d.marketingMessageAction, q + 1);
            }
            if (d.marketingMessageBroadcastAction != null) {
                if (!$util.isObject(d.marketingMessageBroadcastAction))
                    throw $TypeError(".proto.SyncActionValue.marketingMessageBroadcastAction: object expected");
                m.marketingMessageBroadcastAction = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.fromObject(d.marketingMessageBroadcastAction, q + 1);
            }
            if (d.externalWebBetaAction != null) {
                if (!$util.isObject(d.externalWebBetaAction))
                    throw $TypeError(".proto.SyncActionValue.externalWebBetaAction: object expected");
                m.externalWebBetaAction = $root.proto.SyncActionValue.ExternalWebBetaAction.fromObject(d.externalWebBetaAction, q + 1);
            }
            if (d.privacySettingRelayAllCalls != null) {
                if (!$util.isObject(d.privacySettingRelayAllCalls))
                    throw $TypeError(".proto.SyncActionValue.privacySettingRelayAllCalls: object expected");
                m.privacySettingRelayAllCalls = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.fromObject(d.privacySettingRelayAllCalls, q + 1);
            }
            if (d.callLogAction != null) {
                if (!$util.isObject(d.callLogAction))
                    throw $TypeError(".proto.SyncActionValue.callLogAction: object expected");
                m.callLogAction = $root.proto.SyncActionValue.CallLogAction.fromObject(d.callLogAction, q + 1);
            }
            if (d.ugcBot != null) {
                if (!$util.isObject(d.ugcBot))
                    throw $TypeError(".proto.SyncActionValue.ugcBot: object expected");
                m.ugcBot = $root.proto.SyncActionValue.UGCBot.fromObject(d.ugcBot, q + 1);
            }
            if (d.statusPrivacy != null) {
                if (!$util.isObject(d.statusPrivacy))
                    throw $TypeError(".proto.SyncActionValue.statusPrivacy: object expected");
                m.statusPrivacy = $root.proto.SyncActionValue.StatusPrivacyAction.fromObject(d.statusPrivacy, q + 1);
            }
            if (d.botWelcomeRequestAction != null) {
                if (!$util.isObject(d.botWelcomeRequestAction))
                    throw $TypeError(".proto.SyncActionValue.botWelcomeRequestAction: object expected");
                m.botWelcomeRequestAction = $root.proto.SyncActionValue.BotWelcomeRequestAction.fromObject(d.botWelcomeRequestAction, q + 1);
            }
            if (d.deleteIndividualCallLog != null) {
                if (!$util.isObject(d.deleteIndividualCallLog))
                    throw $TypeError(".proto.SyncActionValue.deleteIndividualCallLog: object expected");
                m.deleteIndividualCallLog = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.fromObject(d.deleteIndividualCallLog, q + 1);
            }
            if (d.labelReorderingAction != null) {
                if (!$util.isObject(d.labelReorderingAction))
                    throw $TypeError(".proto.SyncActionValue.labelReorderingAction: object expected");
                m.labelReorderingAction = $root.proto.SyncActionValue.LabelReorderingAction.fromObject(d.labelReorderingAction, q + 1);
            }
            if (d.paymentInfoAction != null) {
                if (!$util.isObject(d.paymentInfoAction))
                    throw $TypeError(".proto.SyncActionValue.paymentInfoAction: object expected");
                m.paymentInfoAction = $root.proto.SyncActionValue.PaymentInfoAction.fromObject(d.paymentInfoAction, q + 1);
            }
            if (d.customPaymentMethodsAction != null) {
                if (!$util.isObject(d.customPaymentMethodsAction))
                    throw $TypeError(".proto.SyncActionValue.customPaymentMethodsAction: object expected");
                m.customPaymentMethodsAction = $root.proto.SyncActionValue.CustomPaymentMethodsAction.fromObject(d.customPaymentMethodsAction, q + 1);
            }
            if (d.lockChatAction != null) {
                if (!$util.isObject(d.lockChatAction))
                    throw $TypeError(".proto.SyncActionValue.lockChatAction: object expected");
                m.lockChatAction = $root.proto.SyncActionValue.LockChatAction.fromObject(d.lockChatAction, q + 1);
            }
            if (d.chatLockSettings != null) {
                if (!$util.isObject(d.chatLockSettings))
                    throw $TypeError(".proto.SyncActionValue.chatLockSettings: object expected");
                m.chatLockSettings = $root.proto.ChatLockSettings.fromObject(d.chatLockSettings, q + 1);
            }
            if (d.wamoUserIdentifierAction != null) {
                if (!$util.isObject(d.wamoUserIdentifierAction))
                    throw $TypeError(".proto.SyncActionValue.wamoUserIdentifierAction: object expected");
                m.wamoUserIdentifierAction = $root.proto.SyncActionValue.WamoUserIdentifierAction.fromObject(d.wamoUserIdentifierAction, q + 1);
            }
            if (d.privacySettingDisableLinkPreviewsAction != null) {
                if (!$util.isObject(d.privacySettingDisableLinkPreviewsAction))
                    throw $TypeError(".proto.SyncActionValue.privacySettingDisableLinkPreviewsAction: object expected");
                m.privacySettingDisableLinkPreviewsAction = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.fromObject(d.privacySettingDisableLinkPreviewsAction, q + 1);
            }
            if (d.deviceCapabilities != null) {
                if (!$util.isObject(d.deviceCapabilities))
                    throw $TypeError(".proto.SyncActionValue.deviceCapabilities: object expected");
                m.deviceCapabilities = $root.proto.DeviceCapabilities.fromObject(d.deviceCapabilities, q + 1);
            }
            if (d.noteEditAction != null) {
                if (!$util.isObject(d.noteEditAction))
                    throw $TypeError(".proto.SyncActionValue.noteEditAction: object expected");
                m.noteEditAction = $root.proto.SyncActionValue.NoteEditAction.fromObject(d.noteEditAction, q + 1);
            }
            if (d.favoritesAction != null) {
                if (!$util.isObject(d.favoritesAction))
                    throw $TypeError(".proto.SyncActionValue.favoritesAction: object expected");
                m.favoritesAction = $root.proto.SyncActionValue.FavoritesAction.fromObject(d.favoritesAction, q + 1);
            }
            if (d.merchantPaymentPartnerAction != null) {
                if (!$util.isObject(d.merchantPaymentPartnerAction))
                    throw $TypeError(".proto.SyncActionValue.merchantPaymentPartnerAction: object expected");
                m.merchantPaymentPartnerAction = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.fromObject(d.merchantPaymentPartnerAction, q + 1);
            }
            if (d.waffleAccountLinkStateAction != null) {
                if (!$util.isObject(d.waffleAccountLinkStateAction))
                    throw $TypeError(".proto.SyncActionValue.waffleAccountLinkStateAction: object expected");
                m.waffleAccountLinkStateAction = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.fromObject(d.waffleAccountLinkStateAction, q + 1);
            }
            if (d.usernameChatStartMode != null) {
                if (!$util.isObject(d.usernameChatStartMode))
                    throw $TypeError(".proto.SyncActionValue.usernameChatStartMode: object expected");
                m.usernameChatStartMode = $root.proto.SyncActionValue.UsernameChatStartModeAction.fromObject(d.usernameChatStartMode, q + 1);
            }
            if (d.notificationActivitySettingAction != null) {
                if (!$util.isObject(d.notificationActivitySettingAction))
                    throw $TypeError(".proto.SyncActionValue.notificationActivitySettingAction: object expected");
                m.notificationActivitySettingAction = $root.proto.SyncActionValue.NotificationActivitySettingAction.fromObject(d.notificationActivitySettingAction, q + 1);
            }
            if (d.lidContactAction != null) {
                if (!$util.isObject(d.lidContactAction))
                    throw $TypeError(".proto.SyncActionValue.lidContactAction: object expected");
                m.lidContactAction = $root.proto.SyncActionValue.LidContactAction.fromObject(d.lidContactAction, q + 1);
            }
            if (d.ctwaPerCustomerDataSharingAction != null) {
                if (!$util.isObject(d.ctwaPerCustomerDataSharingAction))
                    throw $TypeError(".proto.SyncActionValue.ctwaPerCustomerDataSharingAction: object expected");
                m.ctwaPerCustomerDataSharingAction = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.fromObject(d.ctwaPerCustomerDataSharingAction, q + 1);
            }
            if (d.paymentTosAction != null) {
                if (!$util.isObject(d.paymentTosAction))
                    throw $TypeError(".proto.SyncActionValue.paymentTosAction: object expected");
                m.paymentTosAction = $root.proto.SyncActionValue.PaymentTosAction.fromObject(d.paymentTosAction, q + 1);
            }
            if (d.privacySettingChannelsPersonalisedRecommendationAction != null) {
                if (!$util.isObject(d.privacySettingChannelsPersonalisedRecommendationAction))
                    throw $TypeError(".proto.SyncActionValue.privacySettingChannelsPersonalisedRecommendationAction: object expected");
                m.privacySettingChannelsPersonalisedRecommendationAction = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.fromObject(d.privacySettingChannelsPersonalisedRecommendationAction, q + 1);
            }
            if (d.detectedOutcomesStatusAction != null) {
                if (!$util.isObject(d.detectedOutcomesStatusAction))
                    throw $TypeError(".proto.SyncActionValue.detectedOutcomesStatusAction: object expected");
                m.detectedOutcomesStatusAction = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.fromObject(d.detectedOutcomesStatusAction, q + 1);
            }
            if (d.maibaAiFeaturesControlAction != null) {
                if (!$util.isObject(d.maibaAiFeaturesControlAction))
                    throw $TypeError(".proto.SyncActionValue.maibaAiFeaturesControlAction: object expected");
                m.maibaAiFeaturesControlAction = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.fromObject(d.maibaAiFeaturesControlAction, q + 1);
            }
            if (d.businessBroadcastListAction != null) {
                if (!$util.isObject(d.businessBroadcastListAction))
                    throw $TypeError(".proto.SyncActionValue.businessBroadcastListAction: object expected");
                m.businessBroadcastListAction = $root.proto.SyncActionValue.BusinessBroadcastListAction.fromObject(d.businessBroadcastListAction, q + 1);
            }
            if (d.musicUserIdAction != null) {
                if (!$util.isObject(d.musicUserIdAction))
                    throw $TypeError(".proto.SyncActionValue.musicUserIdAction: object expected");
                m.musicUserIdAction = $root.proto.SyncActionValue.MusicUserIdAction.fromObject(d.musicUserIdAction, q + 1);
            }
            if (d.statusPostOptInNotificationPreferencesAction != null) {
                if (!$util.isObject(d.statusPostOptInNotificationPreferencesAction))
                    throw $TypeError(".proto.SyncActionValue.statusPostOptInNotificationPreferencesAction: object expected");
                m.statusPostOptInNotificationPreferencesAction = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.fromObject(d.statusPostOptInNotificationPreferencesAction, q + 1);
            }
            if (d.avatarUpdatedAction != null) {
                if (!$util.isObject(d.avatarUpdatedAction))
                    throw $TypeError(".proto.SyncActionValue.avatarUpdatedAction: object expected");
                m.avatarUpdatedAction = $root.proto.SyncActionValue.AvatarUpdatedAction.fromObject(d.avatarUpdatedAction, q + 1);
            }
            if (d.privateProcessingSettingAction != null) {
                if (!$util.isObject(d.privateProcessingSettingAction))
                    throw $TypeError(".proto.SyncActionValue.privateProcessingSettingAction: object expected");
                m.privateProcessingSettingAction = $root.proto.SyncActionValue.PrivateProcessingSettingAction.fromObject(d.privateProcessingSettingAction, q + 1);
            }
            if (d.newsletterSavedInterestsAction != null) {
                if (!$util.isObject(d.newsletterSavedInterestsAction))
                    throw $TypeError(".proto.SyncActionValue.newsletterSavedInterestsAction: object expected");
                m.newsletterSavedInterestsAction = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.fromObject(d.newsletterSavedInterestsAction, q + 1);
            }
            if (d.aiThreadRenameAction != null) {
                if (!$util.isObject(d.aiThreadRenameAction))
                    throw $TypeError(".proto.SyncActionValue.aiThreadRenameAction: object expected");
                m.aiThreadRenameAction = $root.proto.SyncActionValue.AiThreadRenameAction.fromObject(d.aiThreadRenameAction, q + 1);
            }
            if (d.interactiveMessageAction != null) {
                if (!$util.isObject(d.interactiveMessageAction))
                    throw $TypeError(".proto.SyncActionValue.interactiveMessageAction: object expected");
                m.interactiveMessageAction = $root.proto.SyncActionValue.InteractiveMessageAction.fromObject(d.interactiveMessageAction, q + 1);
            }
            if (d.settingsSyncAction != null) {
                if (!$util.isObject(d.settingsSyncAction))
                    throw $TypeError(".proto.SyncActionValue.settingsSyncAction: object expected");
                m.settingsSyncAction = $root.proto.SyncActionValue.SettingsSyncAction.fromObject(d.settingsSyncAction, q + 1);
            }
            if (d.outContactAction != null) {
                if (!$util.isObject(d.outContactAction))
                    throw $TypeError(".proto.SyncActionValue.outContactAction: object expected");
                m.outContactAction = $root.proto.SyncActionValue.OutContactAction.fromObject(d.outContactAction, q + 1);
            }
            if (d.nctSaltSyncAction != null) {
                if (!$util.isObject(d.nctSaltSyncAction))
                    throw $TypeError(".proto.SyncActionValue.nctSaltSyncAction: object expected");
                m.nctSaltSyncAction = $root.proto.SyncActionValue.NctSaltSyncAction.fromObject(d.nctSaltSyncAction, q + 1);
            }
            if (d.businessBroadcastCampaignAction != null) {
                if (!$util.isObject(d.businessBroadcastCampaignAction))
                    throw $TypeError(".proto.SyncActionValue.businessBroadcastCampaignAction: object expected");
                m.businessBroadcastCampaignAction = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.fromObject(d.businessBroadcastCampaignAction, q + 1);
            }
            if (d.businessBroadcastInsightsAction != null) {
                if (!$util.isObject(d.businessBroadcastInsightsAction))
                    throw $TypeError(".proto.SyncActionValue.businessBroadcastInsightsAction: object expected");
                m.businessBroadcastInsightsAction = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.fromObject(d.businessBroadcastInsightsAction, q + 1);
            }
            if (d.customerDataAction != null) {
                if (!$util.isObject(d.customerDataAction))
                    throw $TypeError(".proto.SyncActionValue.customerDataAction: object expected");
                m.customerDataAction = $root.proto.SyncActionValue.CustomerDataAction.fromObject(d.customerDataAction, q + 1);
            }
            if (d.subscriptionsSyncV2Action != null) {
                if (!$util.isObject(d.subscriptionsSyncV2Action))
                    throw $TypeError(".proto.SyncActionValue.subscriptionsSyncV2Action: object expected");
                m.subscriptionsSyncV2Action = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.fromObject(d.subscriptionsSyncV2Action, q + 1);
            }
            if (d.threadPinAction != null) {
                if (!$util.isObject(d.threadPinAction))
                    throw $TypeError(".proto.SyncActionValue.threadPinAction: object expected");
                m.threadPinAction = $root.proto.SyncActionValue.ThreadPinAction.fromObject(d.threadPinAction, q + 1);
            }
            if (d.autoOrganizeBusinessChatSetting != null) {
                if (!$util.isObject(d.autoOrganizeBusinessChatSetting))
                    throw $TypeError(".proto.SyncActionValue.autoOrganizeBusinessChatSetting: object expected");
                m.autoOrganizeBusinessChatSetting = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.fromObject(d.autoOrganizeBusinessChatSetting, q + 1);
            }
            if (d.bizAiSettingsNudgeAction != null) {
                if (!$util.isObject(d.bizAiSettingsNudgeAction))
                    throw $TypeError(".proto.SyncActionValue.bizAiSettingsNudgeAction: object expected");
                m.bizAiSettingsNudgeAction = $root.proto.SyncActionValue.BizAISettingsNudgeAction.fromObject(d.bizAiSettingsNudgeAction, q + 1);
            }
            if (d.coexV2VersionAction != null) {
                if (!$util.isObject(d.coexV2VersionAction))
                    throw $TypeError(".proto.SyncActionValue.coexV2VersionAction: object expected");
                m.coexV2VersionAction = $root.proto.SyncActionValue.CoexV2VersionAction.fromObject(d.coexV2VersionAction, q + 1);
            }
            if (d.wasaRootSecretAction != null) {
                if (!$util.isObject(d.wasaRootSecretAction))
                    throw $TypeError(".proto.SyncActionValue.wasaRootSecretAction: object expected");
                m.wasaRootSecretAction = $root.proto.SyncActionValue.WASARootSecretAction.fromObject(d.wasaRootSecretAction, q + 1);
            }
            if (d.bubbleLockMessageAction != null) {
                if (!$util.isObject(d.bubbleLockMessageAction))
                    throw $TypeError(".proto.SyncActionValue.bubbleLockMessageAction: object expected");
                m.bubbleLockMessageAction = $root.proto.SyncActionValue.BubbleLockMessageAction.fromObject(d.bubbleLockMessageAction, q + 1);
            }
            if (d.labelSublistAction != null) {
                if (!$util.isObject(d.labelSublistAction))
                    throw $TypeError(".proto.SyncActionValue.labelSublistAction: object expected");
                m.labelSublistAction = $root.proto.SyncActionValue.LabelSublistAction.fromObject(d.labelSublistAction, q + 1);
            }
            if (d.deviceCapabilitiesV2 != null) {
                if (!$util.isObject(d.deviceCapabilitiesV2))
                    throw $TypeError(".proto.SyncActionValue.deviceCapabilitiesV2: object expected");
                m.deviceCapabilitiesV2 = $root.proto.DeviceCapabilities.fromObject(d.deviceCapabilitiesV2, q + 1);
            }
            if (d.ctwaMessageReceivedAction != null) {
                if (!$util.isObject(d.ctwaMessageReceivedAction))
                    throw $TypeError(".proto.SyncActionValue.ctwaMessageReceivedAction: object expected");
                m.ctwaMessageReceivedAction = $root.proto.SyncActionValue.CtwaMessageReceivedAction.fromObject(d.ctwaMessageReceivedAction, q + 1);
            }
            if (d.sharedDeviceAllowlistAction != null) {
                if (!$util.isObject(d.sharedDeviceAllowlistAction))
                    throw $TypeError(".proto.SyncActionValue.sharedDeviceAllowlistAction: object expected");
                m.sharedDeviceAllowlistAction = $root.proto.SyncActionValue.SharedDeviceAllowlistAction.fromObject(d.sharedDeviceAllowlistAction, q + 1);
            }
            if (d.contactManagerMetadataAction != null) {
                if (!$util.isObject(d.contactManagerMetadataAction))
                    throw $TypeError(".proto.SyncActionValue.contactManagerMetadataAction: object expected");
                m.contactManagerMetadataAction = $root.proto.SyncActionValue.ContactManagerMetadataAction.fromObject(d.contactManagerMetadataAction, q + 1);
            }
            if (d.businessFolderActivationAction != null) {
                if (!$util.isObject(d.businessFolderActivationAction))
                    throw $TypeError(".proto.SyncActionValue.businessFolderActivationAction: object expected");
                m.businessFolderActivationAction = $root.proto.SyncActionValue.BusinessFolderActivationAction.fromObject(d.businessFolderActivationAction, q + 1);
            }
            return m;
        };

        SyncActionValue.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
            }
            if (m.starAction != null && $Object.hasOwnProperty.call(m, "starAction")) {
                d.starAction = $root.proto.SyncActionValue.StarAction.toObject(m.starAction, o, q + 1);
            }
            if (m.contactAction != null && $Object.hasOwnProperty.call(m, "contactAction")) {
                d.contactAction = $root.proto.SyncActionValue.ContactAction.toObject(m.contactAction, o, q + 1);
            }
            if (m.muteAction != null && $Object.hasOwnProperty.call(m, "muteAction")) {
                d.muteAction = $root.proto.SyncActionValue.MuteAction.toObject(m.muteAction, o, q + 1);
            }
            if (m.pinAction != null && $Object.hasOwnProperty.call(m, "pinAction")) {
                d.pinAction = $root.proto.SyncActionValue.PinAction.toObject(m.pinAction, o, q + 1);
            }
            if (m.pushNameSetting != null && $Object.hasOwnProperty.call(m, "pushNameSetting")) {
                d.pushNameSetting = $root.proto.SyncActionValue.PushNameSetting.toObject(m.pushNameSetting, o, q + 1);
            }
            if (m.quickReplyAction != null && $Object.hasOwnProperty.call(m, "quickReplyAction")) {
                d.quickReplyAction = $root.proto.SyncActionValue.QuickReplyAction.toObject(m.quickReplyAction, o, q + 1);
            }
            if (m.recentEmojiWeightsAction != null && $Object.hasOwnProperty.call(m, "recentEmojiWeightsAction")) {
                d.recentEmojiWeightsAction = $root.proto.SyncActionValue.RecentEmojiWeightsAction.toObject(m.recentEmojiWeightsAction, o, q + 1);
            }
            if (m.labelEditAction != null && $Object.hasOwnProperty.call(m, "labelEditAction")) {
                d.labelEditAction = $root.proto.SyncActionValue.LabelEditAction.toObject(m.labelEditAction, o, q + 1);
            }
            if (m.labelAssociationAction != null && $Object.hasOwnProperty.call(m, "labelAssociationAction")) {
                d.labelAssociationAction = $root.proto.SyncActionValue.LabelAssociationAction.toObject(m.labelAssociationAction, o, q + 1);
            }
            if (m.localeSetting != null && $Object.hasOwnProperty.call(m, "localeSetting")) {
                d.localeSetting = $root.proto.SyncActionValue.LocaleSetting.toObject(m.localeSetting, o, q + 1);
            }
            if (m.archiveChatAction != null && $Object.hasOwnProperty.call(m, "archiveChatAction")) {
                d.archiveChatAction = $root.proto.SyncActionValue.ArchiveChatAction.toObject(m.archiveChatAction, o, q + 1);
            }
            if (m.deleteMessageForMeAction != null && $Object.hasOwnProperty.call(m, "deleteMessageForMeAction")) {
                d.deleteMessageForMeAction = $root.proto.SyncActionValue.DeleteMessageForMeAction.toObject(m.deleteMessageForMeAction, o, q + 1);
            }
            if (m.keyExpiration != null && $Object.hasOwnProperty.call(m, "keyExpiration")) {
                d.keyExpiration = $root.proto.SyncActionValue.KeyExpiration.toObject(m.keyExpiration, o, q + 1);
            }
            if (m.markChatAsReadAction != null && $Object.hasOwnProperty.call(m, "markChatAsReadAction")) {
                d.markChatAsReadAction = $root.proto.SyncActionValue.MarkChatAsReadAction.toObject(m.markChatAsReadAction, o, q + 1);
            }
            if (m.clearChatAction != null && $Object.hasOwnProperty.call(m, "clearChatAction")) {
                d.clearChatAction = $root.proto.SyncActionValue.ClearChatAction.toObject(m.clearChatAction, o, q + 1);
            }
            if (m.deleteChatAction != null && $Object.hasOwnProperty.call(m, "deleteChatAction")) {
                d.deleteChatAction = $root.proto.SyncActionValue.DeleteChatAction.toObject(m.deleteChatAction, o, q + 1);
            }
            if (m.unarchiveChatsSetting != null && $Object.hasOwnProperty.call(m, "unarchiveChatsSetting")) {
                d.unarchiveChatsSetting = $root.proto.SyncActionValue.UnarchiveChatsSetting.toObject(m.unarchiveChatsSetting, o, q + 1);
            }
            if (m.primaryFeature != null && $Object.hasOwnProperty.call(m, "primaryFeature")) {
                d.primaryFeature = $root.proto.SyncActionValue.PrimaryFeature.toObject(m.primaryFeature, o, q + 1);
            }
            if (m.androidUnsupportedActions != null && $Object.hasOwnProperty.call(m, "androidUnsupportedActions")) {
                d.androidUnsupportedActions = $root.proto.SyncActionValue.AndroidUnsupportedActions.toObject(m.androidUnsupportedActions, o, q + 1);
            }
            if (m.agentAction != null && $Object.hasOwnProperty.call(m, "agentAction")) {
                d.agentAction = $root.proto.SyncActionValue.AgentAction.toObject(m.agentAction, o, q + 1);
            }
            if (m.subscriptionAction != null && $Object.hasOwnProperty.call(m, "subscriptionAction")) {
                d.subscriptionAction = $root.proto.SyncActionValue.SubscriptionAction.toObject(m.subscriptionAction, o, q + 1);
            }
            if (m.userStatusMuteAction != null && $Object.hasOwnProperty.call(m, "userStatusMuteAction")) {
                d.userStatusMuteAction = $root.proto.SyncActionValue.UserStatusMuteAction.toObject(m.userStatusMuteAction, o, q + 1);
            }
            if (m.timeFormatAction != null && $Object.hasOwnProperty.call(m, "timeFormatAction")) {
                d.timeFormatAction = $root.proto.SyncActionValue.TimeFormatAction.toObject(m.timeFormatAction, o, q + 1);
            }
            if (m.nuxAction != null && $Object.hasOwnProperty.call(m, "nuxAction")) {
                d.nuxAction = $root.proto.SyncActionValue.NuxAction.toObject(m.nuxAction, o, q + 1);
            }
            if (m.primaryVersionAction != null && $Object.hasOwnProperty.call(m, "primaryVersionAction")) {
                d.primaryVersionAction = $root.proto.SyncActionValue.PrimaryVersionAction.toObject(m.primaryVersionAction, o, q + 1);
            }
            if (m.stickerAction != null && $Object.hasOwnProperty.call(m, "stickerAction")) {
                d.stickerAction = $root.proto.SyncActionValue.StickerAction.toObject(m.stickerAction, o, q + 1);
            }
            if (m.removeRecentStickerAction != null && $Object.hasOwnProperty.call(m, "removeRecentStickerAction")) {
                d.removeRecentStickerAction = $root.proto.SyncActionValue.RemoveRecentStickerAction.toObject(m.removeRecentStickerAction, o, q + 1);
            }
            if (m.chatAssignment != null && $Object.hasOwnProperty.call(m, "chatAssignment")) {
                d.chatAssignment = $root.proto.SyncActionValue.ChatAssignmentAction.toObject(m.chatAssignment, o, q + 1);
            }
            if (m.chatAssignmentOpenedStatus != null && $Object.hasOwnProperty.call(m, "chatAssignmentOpenedStatus")) {
                d.chatAssignmentOpenedStatus = $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction.toObject(m.chatAssignmentOpenedStatus, o, q + 1);
            }
            if (m.pnForLidChatAction != null && $Object.hasOwnProperty.call(m, "pnForLidChatAction")) {
                d.pnForLidChatAction = $root.proto.SyncActionValue.PnForLidChatAction.toObject(m.pnForLidChatAction, o, q + 1);
            }
            if (m.marketingMessageAction != null && $Object.hasOwnProperty.call(m, "marketingMessageAction")) {
                d.marketingMessageAction = $root.proto.SyncActionValue.MarketingMessageAction.toObject(m.marketingMessageAction, o, q + 1);
            }
            if (m.marketingMessageBroadcastAction != null && $Object.hasOwnProperty.call(m, "marketingMessageBroadcastAction")) {
                d.marketingMessageBroadcastAction = $root.proto.SyncActionValue.MarketingMessageBroadcastAction.toObject(m.marketingMessageBroadcastAction, o, q + 1);
            }
            if (m.externalWebBetaAction != null && $Object.hasOwnProperty.call(m, "externalWebBetaAction")) {
                d.externalWebBetaAction = $root.proto.SyncActionValue.ExternalWebBetaAction.toObject(m.externalWebBetaAction, o, q + 1);
            }
            if (m.privacySettingRelayAllCalls != null && $Object.hasOwnProperty.call(m, "privacySettingRelayAllCalls")) {
                d.privacySettingRelayAllCalls = $root.proto.SyncActionValue.PrivacySettingRelayAllCalls.toObject(m.privacySettingRelayAllCalls, o, q + 1);
            }
            if (m.callLogAction != null && $Object.hasOwnProperty.call(m, "callLogAction")) {
                d.callLogAction = $root.proto.SyncActionValue.CallLogAction.toObject(m.callLogAction, o, q + 1);
            }
            if (m.ugcBot != null && $Object.hasOwnProperty.call(m, "ugcBot")) {
                d.ugcBot = $root.proto.SyncActionValue.UGCBot.toObject(m.ugcBot, o, q + 1);
            }
            if (m.statusPrivacy != null && $Object.hasOwnProperty.call(m, "statusPrivacy")) {
                d.statusPrivacy = $root.proto.SyncActionValue.StatusPrivacyAction.toObject(m.statusPrivacy, o, q + 1);
            }
            if (m.botWelcomeRequestAction != null && $Object.hasOwnProperty.call(m, "botWelcomeRequestAction")) {
                d.botWelcomeRequestAction = $root.proto.SyncActionValue.BotWelcomeRequestAction.toObject(m.botWelcomeRequestAction, o, q + 1);
            }
            if (m.deleteIndividualCallLog != null && $Object.hasOwnProperty.call(m, "deleteIndividualCallLog")) {
                d.deleteIndividualCallLog = $root.proto.SyncActionValue.DeleteIndividualCallLogAction.toObject(m.deleteIndividualCallLog, o, q + 1);
            }
            if (m.labelReorderingAction != null && $Object.hasOwnProperty.call(m, "labelReorderingAction")) {
                d.labelReorderingAction = $root.proto.SyncActionValue.LabelReorderingAction.toObject(m.labelReorderingAction, o, q + 1);
            }
            if (m.paymentInfoAction != null && $Object.hasOwnProperty.call(m, "paymentInfoAction")) {
                d.paymentInfoAction = $root.proto.SyncActionValue.PaymentInfoAction.toObject(m.paymentInfoAction, o, q + 1);
            }
            if (m.customPaymentMethodsAction != null && $Object.hasOwnProperty.call(m, "customPaymentMethodsAction")) {
                d.customPaymentMethodsAction = $root.proto.SyncActionValue.CustomPaymentMethodsAction.toObject(m.customPaymentMethodsAction, o, q + 1);
            }
            if (m.lockChatAction != null && $Object.hasOwnProperty.call(m, "lockChatAction")) {
                d.lockChatAction = $root.proto.SyncActionValue.LockChatAction.toObject(m.lockChatAction, o, q + 1);
            }
            if (m.chatLockSettings != null && $Object.hasOwnProperty.call(m, "chatLockSettings")) {
                d.chatLockSettings = $root.proto.ChatLockSettings.toObject(m.chatLockSettings, o, q + 1);
            }
            if (m.wamoUserIdentifierAction != null && $Object.hasOwnProperty.call(m, "wamoUserIdentifierAction")) {
                d.wamoUserIdentifierAction = $root.proto.SyncActionValue.WamoUserIdentifierAction.toObject(m.wamoUserIdentifierAction, o, q + 1);
            }
            if (m.privacySettingDisableLinkPreviewsAction != null && $Object.hasOwnProperty.call(m, "privacySettingDisableLinkPreviewsAction")) {
                d.privacySettingDisableLinkPreviewsAction = $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction.toObject(m.privacySettingDisableLinkPreviewsAction, o, q + 1);
            }
            if (m.deviceCapabilities != null && $Object.hasOwnProperty.call(m, "deviceCapabilities")) {
                d.deviceCapabilities = $root.proto.DeviceCapabilities.toObject(m.deviceCapabilities, o, q + 1);
            }
            if (m.noteEditAction != null && $Object.hasOwnProperty.call(m, "noteEditAction")) {
                d.noteEditAction = $root.proto.SyncActionValue.NoteEditAction.toObject(m.noteEditAction, o, q + 1);
            }
            if (m.favoritesAction != null && $Object.hasOwnProperty.call(m, "favoritesAction")) {
                d.favoritesAction = $root.proto.SyncActionValue.FavoritesAction.toObject(m.favoritesAction, o, q + 1);
            }
            if (m.merchantPaymentPartnerAction != null && $Object.hasOwnProperty.call(m, "merchantPaymentPartnerAction")) {
                d.merchantPaymentPartnerAction = $root.proto.SyncActionValue.MerchantPaymentPartnerAction.toObject(m.merchantPaymentPartnerAction, o, q + 1);
            }
            if (m.waffleAccountLinkStateAction != null && $Object.hasOwnProperty.call(m, "waffleAccountLinkStateAction")) {
                d.waffleAccountLinkStateAction = $root.proto.SyncActionValue.WaffleAccountLinkStateAction.toObject(m.waffleAccountLinkStateAction, o, q + 1);
            }
            if (m.usernameChatStartMode != null && $Object.hasOwnProperty.call(m, "usernameChatStartMode")) {
                d.usernameChatStartMode = $root.proto.SyncActionValue.UsernameChatStartModeAction.toObject(m.usernameChatStartMode, o, q + 1);
            }
            if (m.notificationActivitySettingAction != null && $Object.hasOwnProperty.call(m, "notificationActivitySettingAction")) {
                d.notificationActivitySettingAction = $root.proto.SyncActionValue.NotificationActivitySettingAction.toObject(m.notificationActivitySettingAction, o, q + 1);
            }
            if (m.lidContactAction != null && $Object.hasOwnProperty.call(m, "lidContactAction")) {
                d.lidContactAction = $root.proto.SyncActionValue.LidContactAction.toObject(m.lidContactAction, o, q + 1);
            }
            if (m.ctwaPerCustomerDataSharingAction != null && $Object.hasOwnProperty.call(m, "ctwaPerCustomerDataSharingAction")) {
                d.ctwaPerCustomerDataSharingAction = $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction.toObject(m.ctwaPerCustomerDataSharingAction, o, q + 1);
            }
            if (m.paymentTosAction != null && $Object.hasOwnProperty.call(m, "paymentTosAction")) {
                d.paymentTosAction = $root.proto.SyncActionValue.PaymentTosAction.toObject(m.paymentTosAction, o, q + 1);
            }
            if (m.privacySettingChannelsPersonalisedRecommendationAction != null && $Object.hasOwnProperty.call(m, "privacySettingChannelsPersonalisedRecommendationAction")) {
                d.privacySettingChannelsPersonalisedRecommendationAction = $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction.toObject(m.privacySettingChannelsPersonalisedRecommendationAction, o, q + 1);
            }
            if (m.detectedOutcomesStatusAction != null && $Object.hasOwnProperty.call(m, "detectedOutcomesStatusAction")) {
                d.detectedOutcomesStatusAction = $root.proto.SyncActionValue.DetectedOutcomesStatusAction.toObject(m.detectedOutcomesStatusAction, o, q + 1);
            }
            if (m.maibaAiFeaturesControlAction != null && $Object.hasOwnProperty.call(m, "maibaAiFeaturesControlAction")) {
                d.maibaAiFeaturesControlAction = $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.toObject(m.maibaAiFeaturesControlAction, o, q + 1);
            }
            if (m.businessBroadcastListAction != null && $Object.hasOwnProperty.call(m, "businessBroadcastListAction")) {
                d.businessBroadcastListAction = $root.proto.SyncActionValue.BusinessBroadcastListAction.toObject(m.businessBroadcastListAction, o, q + 1);
            }
            if (m.musicUserIdAction != null && $Object.hasOwnProperty.call(m, "musicUserIdAction")) {
                d.musicUserIdAction = $root.proto.SyncActionValue.MusicUserIdAction.toObject(m.musicUserIdAction, o, q + 1);
            }
            if (m.statusPostOptInNotificationPreferencesAction != null && $Object.hasOwnProperty.call(m, "statusPostOptInNotificationPreferencesAction")) {
                d.statusPostOptInNotificationPreferencesAction = $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction.toObject(m.statusPostOptInNotificationPreferencesAction, o, q + 1);
            }
            if (m.avatarUpdatedAction != null && $Object.hasOwnProperty.call(m, "avatarUpdatedAction")) {
                d.avatarUpdatedAction = $root.proto.SyncActionValue.AvatarUpdatedAction.toObject(m.avatarUpdatedAction, o, q + 1);
            }
            if (m.privateProcessingSettingAction != null && $Object.hasOwnProperty.call(m, "privateProcessingSettingAction")) {
                d.privateProcessingSettingAction = $root.proto.SyncActionValue.PrivateProcessingSettingAction.toObject(m.privateProcessingSettingAction, o, q + 1);
            }
            if (m.newsletterSavedInterestsAction != null && $Object.hasOwnProperty.call(m, "newsletterSavedInterestsAction")) {
                d.newsletterSavedInterestsAction = $root.proto.SyncActionValue.NewsletterSavedInterestsAction.toObject(m.newsletterSavedInterestsAction, o, q + 1);
            }
            if (m.aiThreadRenameAction != null && $Object.hasOwnProperty.call(m, "aiThreadRenameAction")) {
                d.aiThreadRenameAction = $root.proto.SyncActionValue.AiThreadRenameAction.toObject(m.aiThreadRenameAction, o, q + 1);
            }
            if (m.interactiveMessageAction != null && $Object.hasOwnProperty.call(m, "interactiveMessageAction")) {
                d.interactiveMessageAction = $root.proto.SyncActionValue.InteractiveMessageAction.toObject(m.interactiveMessageAction, o, q + 1);
            }
            if (m.settingsSyncAction != null && $Object.hasOwnProperty.call(m, "settingsSyncAction")) {
                d.settingsSyncAction = $root.proto.SyncActionValue.SettingsSyncAction.toObject(m.settingsSyncAction, o, q + 1);
            }
            if (m.outContactAction != null && $Object.hasOwnProperty.call(m, "outContactAction")) {
                d.outContactAction = $root.proto.SyncActionValue.OutContactAction.toObject(m.outContactAction, o, q + 1);
            }
            if (m.nctSaltSyncAction != null && $Object.hasOwnProperty.call(m, "nctSaltSyncAction")) {
                d.nctSaltSyncAction = $root.proto.SyncActionValue.NctSaltSyncAction.toObject(m.nctSaltSyncAction, o, q + 1);
            }
            if (m.businessBroadcastCampaignAction != null && $Object.hasOwnProperty.call(m, "businessBroadcastCampaignAction")) {
                d.businessBroadcastCampaignAction = $root.proto.SyncActionValue.BusinessBroadcastCampaignAction.toObject(m.businessBroadcastCampaignAction, o, q + 1);
            }
            if (m.businessBroadcastInsightsAction != null && $Object.hasOwnProperty.call(m, "businessBroadcastInsightsAction")) {
                d.businessBroadcastInsightsAction = $root.proto.SyncActionValue.BusinessBroadcastInsightsAction.toObject(m.businessBroadcastInsightsAction, o, q + 1);
            }
            if (m.customerDataAction != null && $Object.hasOwnProperty.call(m, "customerDataAction")) {
                d.customerDataAction = $root.proto.SyncActionValue.CustomerDataAction.toObject(m.customerDataAction, o, q + 1);
            }
            if (m.subscriptionsSyncV2Action != null && $Object.hasOwnProperty.call(m, "subscriptionsSyncV2Action")) {
                d.subscriptionsSyncV2Action = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.toObject(m.subscriptionsSyncV2Action, o, q + 1);
            }
            if (m.threadPinAction != null && $Object.hasOwnProperty.call(m, "threadPinAction")) {
                d.threadPinAction = $root.proto.SyncActionValue.ThreadPinAction.toObject(m.threadPinAction, o, q + 1);
            }
            if (m.autoOrganizeBusinessChatSetting != null && $Object.hasOwnProperty.call(m, "autoOrganizeBusinessChatSetting")) {
                d.autoOrganizeBusinessChatSetting = $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting.toObject(m.autoOrganizeBusinessChatSetting, o, q + 1);
            }
            if (m.bizAiSettingsNudgeAction != null && $Object.hasOwnProperty.call(m, "bizAiSettingsNudgeAction")) {
                d.bizAiSettingsNudgeAction = $root.proto.SyncActionValue.BizAISettingsNudgeAction.toObject(m.bizAiSettingsNudgeAction, o, q + 1);
            }
            if (m.coexV2VersionAction != null && $Object.hasOwnProperty.call(m, "coexV2VersionAction")) {
                d.coexV2VersionAction = $root.proto.SyncActionValue.CoexV2VersionAction.toObject(m.coexV2VersionAction, o, q + 1);
            }
            if (m.wasaRootSecretAction != null && $Object.hasOwnProperty.call(m, "wasaRootSecretAction")) {
                d.wasaRootSecretAction = $root.proto.SyncActionValue.WASARootSecretAction.toObject(m.wasaRootSecretAction, o, q + 1);
            }
            if (m.bubbleLockMessageAction != null && $Object.hasOwnProperty.call(m, "bubbleLockMessageAction")) {
                d.bubbleLockMessageAction = $root.proto.SyncActionValue.BubbleLockMessageAction.toObject(m.bubbleLockMessageAction, o, q + 1);
            }
            if (m.labelSublistAction != null && $Object.hasOwnProperty.call(m, "labelSublistAction")) {
                d.labelSublistAction = $root.proto.SyncActionValue.LabelSublistAction.toObject(m.labelSublistAction, o, q + 1);
            }
            if (m.deviceCapabilitiesV2 != null && $Object.hasOwnProperty.call(m, "deviceCapabilitiesV2")) {
                d.deviceCapabilitiesV2 = $root.proto.DeviceCapabilities.toObject(m.deviceCapabilitiesV2, o, q + 1);
            }
            if (m.ctwaMessageReceivedAction != null && $Object.hasOwnProperty.call(m, "ctwaMessageReceivedAction")) {
                d.ctwaMessageReceivedAction = $root.proto.SyncActionValue.CtwaMessageReceivedAction.toObject(m.ctwaMessageReceivedAction, o, q + 1);
            }
            if (m.sharedDeviceAllowlistAction != null && $Object.hasOwnProperty.call(m, "sharedDeviceAllowlistAction")) {
                d.sharedDeviceAllowlistAction = $root.proto.SyncActionValue.SharedDeviceAllowlistAction.toObject(m.sharedDeviceAllowlistAction, o, q + 1);
            }
            if (m.contactManagerMetadataAction != null && $Object.hasOwnProperty.call(m, "contactManagerMetadataAction")) {
                d.contactManagerMetadataAction = $root.proto.SyncActionValue.ContactManagerMetadataAction.toObject(m.contactManagerMetadataAction, o, q + 1);
            }
            if (m.businessFolderActivationAction != null && $Object.hasOwnProperty.call(m, "businessFolderActivationAction")) {
                d.businessFolderActivationAction = $root.proto.SyncActionValue.BusinessFolderActivationAction.toObject(m.businessFolderActivationAction, o, q + 1);
            }
            return d;
        };

        SyncActionValue.prototype.toJSON = function() {
            return SyncActionValue.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncActionValue.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncActionValue";
        };

        SyncActionValue.AgentAction = (function() {

            const AgentAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AgentAction.prototype.name = null;
            AgentAction.prototype.deviceId = null;
            AgentAction.prototype.isDeleted = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AgentAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AgentAction.prototype, "_deviceId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AgentAction.prototype, "_isDeleted", {
                get: $util.oneOfGetter($oneOfFields = ["isDeleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AgentAction.create = function(properties) {
                return new AgentAction(properties);
            };

            AgentAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId"))
                    w.uint32(16).int32(m.deviceId);
                if (m.isDeleted != null && $Object.hasOwnProperty.call(m, "isDeleted"))
                    w.uint32(24).bool(m.isDeleted);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AgentAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.AgentAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.deviceId = r.int32();
                            m._deviceId = "deviceId";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.isDeleted = r.bool();
                            m._isDeleted = "isDeleted";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AgentAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.AgentAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.AgentAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.AgentAction();
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.deviceId != null) {
                    m.deviceId = d.deviceId | 0;
                }
                if (d.isDeleted != null) {
                    m.isDeleted = $Boolean(d.isDeleted);
                }
                return m;
            };

            AgentAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId")) {
                    d.deviceId = m.deviceId;
                }
                if (m.isDeleted != null && $Object.hasOwnProperty.call(m, "isDeleted")) {
                    d.isDeleted = m.isDeleted;
                }
                return d;
            };

            AgentAction.prototype.toJSON = function() {
                return AgentAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            AgentAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.AgentAction";
            };

            return AgentAction;
        })();

        SyncActionValue.AiThreadRenameAction = (function() {

            const AiThreadRenameAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AiThreadRenameAction.prototype.newTitle = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AiThreadRenameAction.prototype, "_newTitle", {
                get: $util.oneOfGetter($oneOfFields = ["newTitle"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AiThreadRenameAction.create = function(properties) {
                return new AiThreadRenameAction(properties);
            };

            AiThreadRenameAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.newTitle != null && $Object.hasOwnProperty.call(m, "newTitle"))
                    w.uint32(10).string(m.newTitle);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AiThreadRenameAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.AiThreadRenameAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.newTitle = r.stringVerify();
                            m._newTitle = "newTitle";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AiThreadRenameAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.AiThreadRenameAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.AiThreadRenameAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.AiThreadRenameAction();
                if (d.newTitle != null) {
                    m.newTitle = $String(d.newTitle);
                }
                return m;
            };

            AiThreadRenameAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.newTitle != null && $Object.hasOwnProperty.call(m, "newTitle")) {
                    d.newTitle = m.newTitle;
                }
                return d;
            };

            AiThreadRenameAction.prototype.toJSON = function() {
                return AiThreadRenameAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            AiThreadRenameAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.AiThreadRenameAction";
            };

            return AiThreadRenameAction;
        })();

        SyncActionValue.AndroidUnsupportedActions = (function() {

            const AndroidUnsupportedActions = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AndroidUnsupportedActions.prototype.allowed = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AndroidUnsupportedActions.prototype, "_allowed", {
                get: $util.oneOfGetter($oneOfFields = ["allowed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AndroidUnsupportedActions.create = function(properties) {
                return new AndroidUnsupportedActions(properties);
            };

            AndroidUnsupportedActions.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.allowed != null && $Object.hasOwnProperty.call(m, "allowed"))
                    w.uint32(8).bool(m.allowed);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AndroidUnsupportedActions.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.AndroidUnsupportedActions();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.allowed = r.bool();
                            m._allowed = "allowed";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AndroidUnsupportedActions.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.AndroidUnsupportedActions)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.AndroidUnsupportedActions: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.AndroidUnsupportedActions();
                if (d.allowed != null) {
                    m.allowed = $Boolean(d.allowed);
                }
                return m;
            };

            AndroidUnsupportedActions.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.allowed != null && $Object.hasOwnProperty.call(m, "allowed")) {
                    d.allowed = m.allowed;
                }
                return d;
            };

            AndroidUnsupportedActions.prototype.toJSON = function() {
                return AndroidUnsupportedActions.toObject(this, $protobuf.util.toJSONOptions);
            };

            AndroidUnsupportedActions.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.AndroidUnsupportedActions";
            };

            return AndroidUnsupportedActions;
        })();

        SyncActionValue.ArchiveChatAction = (function() {

            const ArchiveChatAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ArchiveChatAction.prototype.archived = null;
            ArchiveChatAction.prototype.messageRange = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ArchiveChatAction.prototype, "_archived", {
                get: $util.oneOfGetter($oneOfFields = ["archived"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ArchiveChatAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ArchiveChatAction.create = function(properties) {
                return new ArchiveChatAction(properties);
            };

            ArchiveChatAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.archived != null && $Object.hasOwnProperty.call(m, "archived"))
                    w.uint32(8).bool(m.archived);
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(m.messageRange, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ArchiveChatAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ArchiveChatAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.archived = r.bool();
                            m._archived = "archived";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(r, r.uint32(), $undefined, q + 1, m.messageRange);
                            m._messageRange = "messageRange";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ArchiveChatAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ArchiveChatAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ArchiveChatAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ArchiveChatAction();
                if (d.archived != null) {
                    m.archived = $Boolean(d.archived);
                }
                if (d.messageRange != null) {
                    if (!$util.isObject(d.messageRange))
                        throw $TypeError(".proto.SyncActionValue.ArchiveChatAction.messageRange: object expected");
                    m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(d.messageRange, q + 1);
                }
                return m;
            };

            ArchiveChatAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.archived != null && $Object.hasOwnProperty.call(m, "archived")) {
                    d.archived = m.archived;
                }
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange")) {
                    d.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(m.messageRange, o, q + 1);
                }
                return d;
            };

            ArchiveChatAction.prototype.toJSON = function() {
                return ArchiveChatAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ArchiveChatAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ArchiveChatAction";
            };

            return ArchiveChatAction;
        })();

        SyncActionValue.AutoOrganizeBusinessChatSetting = (function() {

            const AutoOrganizeBusinessChatSetting = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AutoOrganizeBusinessChatSetting.prototype.autoOrganize = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AutoOrganizeBusinessChatSetting.prototype, "_autoOrganize", {
                get: $util.oneOfGetter($oneOfFields = ["autoOrganize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AutoOrganizeBusinessChatSetting.create = function(properties) {
                return new AutoOrganizeBusinessChatSetting(properties);
            };

            AutoOrganizeBusinessChatSetting.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.autoOrganize != null && $Object.hasOwnProperty.call(m, "autoOrganize"))
                    w.uint32(8).bool(m.autoOrganize);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AutoOrganizeBusinessChatSetting.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.autoOrganize = r.bool();
                            m._autoOrganize = "autoOrganize";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AutoOrganizeBusinessChatSetting.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.AutoOrganizeBusinessChatSetting: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.AutoOrganizeBusinessChatSetting();
                if (d.autoOrganize != null) {
                    m.autoOrganize = $Boolean(d.autoOrganize);
                }
                return m;
            };

            AutoOrganizeBusinessChatSetting.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.autoOrganize != null && $Object.hasOwnProperty.call(m, "autoOrganize")) {
                    d.autoOrganize = m.autoOrganize;
                }
                return d;
            };

            AutoOrganizeBusinessChatSetting.prototype.toJSON = function() {
                return AutoOrganizeBusinessChatSetting.toObject(this, $protobuf.util.toJSONOptions);
            };

            AutoOrganizeBusinessChatSetting.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.AutoOrganizeBusinessChatSetting";
            };

            return AutoOrganizeBusinessChatSetting;
        })();

        SyncActionValue.AvatarUpdatedAction = (function() {

            const AvatarUpdatedAction = function (p) {
                this.recentAvatarStickers = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            AvatarUpdatedAction.prototype.eventType = null;
            AvatarUpdatedAction.prototype.recentAvatarStickers = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(AvatarUpdatedAction.prototype, "_eventType", {
                get: $util.oneOfGetter($oneOfFields = ["eventType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            AvatarUpdatedAction.create = function(properties) {
                return new AvatarUpdatedAction(properties);
            };

            AvatarUpdatedAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.eventType != null && $Object.hasOwnProperty.call(m, "eventType"))
                    w.uint32(8).int32(m.eventType);
                if (m.recentAvatarStickers != null && m.recentAvatarStickers.length) {
                    for (var i = 0; i < m.recentAvatarStickers.length; ++i)
                        $root.proto.SyncActionValue.StickerAction.encode(m.recentAvatarStickers[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            AvatarUpdatedAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.AvatarUpdatedAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.eventType = r.int32();
                            m._eventType = "eventType";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.recentAvatarStickers && m.recentAvatarStickers.length))
                                m.recentAvatarStickers = [];
                            m.recentAvatarStickers.push($root.proto.SyncActionValue.StickerAction.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            AvatarUpdatedAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.AvatarUpdatedAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.AvatarUpdatedAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.AvatarUpdatedAction();
                switch (d.eventType) {
                case "UPDATED":
                case 0:
                    m.eventType = 0;
                    break;
                case "CREATED":
                case 1:
                    m.eventType = 1;
                    break;
                case "DELETED":
                case 2:
                    m.eventType = 2;
                    break;
                default:
                    if (typeof d.eventType === "number" && (d.eventType | 0) === d.eventType)
                        m.eventType = d.eventType;
                }
                if (d.recentAvatarStickers) {
                    if (!$Array.isArray(d.recentAvatarStickers))
                        throw $TypeError(".proto.SyncActionValue.AvatarUpdatedAction.recentAvatarStickers: array expected");
                    m.recentAvatarStickers = $Array(d.recentAvatarStickers.length);
                    for (var i = 0; i < d.recentAvatarStickers.length; ++i) {
                        if (!$util.isObject(d.recentAvatarStickers[i]))
                            throw $TypeError(".proto.SyncActionValue.AvatarUpdatedAction.recentAvatarStickers: object expected");
                        m.recentAvatarStickers[i] = $root.proto.SyncActionValue.StickerAction.fromObject(d.recentAvatarStickers[i], q + 1);
                    }
                }
                return m;
            };

            AvatarUpdatedAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.recentAvatarStickers = [];
                }
                if (m.eventType != null && $Object.hasOwnProperty.call(m, "eventType")) {
                    d.eventType = o.enums === $String ? $root.proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType[m.eventType] === $undefined ? m.eventType : $root.proto.SyncActionValue.AvatarUpdatedAction.AvatarEventType[m.eventType] : m.eventType;
                }
                if (m.recentAvatarStickers && m.recentAvatarStickers.length) {
                    d.recentAvatarStickers = $Array(m.recentAvatarStickers.length);
                    for (var j = 0; j < m.recentAvatarStickers.length; ++j) {
                        d.recentAvatarStickers[j] = $root.proto.SyncActionValue.StickerAction.toObject(m.recentAvatarStickers[j], o, q + 1);
                    }
                }
                return d;
            };

            AvatarUpdatedAction.prototype.toJSON = function() {
                return AvatarUpdatedAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            AvatarUpdatedAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.AvatarUpdatedAction";
            };

            AvatarUpdatedAction.AvatarEventType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UPDATED"] = 0;
                values[valuesById[1] = "CREATED"] = 1;
                values[valuesById[2] = "DELETED"] = 2;
                return values;
            })();

            return AvatarUpdatedAction;
        })();

        SyncActionValue.BizAISettingsNudgeAction = (function() {

            const BizAISettingsNudgeAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BizAISettingsNudgeAction.prototype.category = null;
            BizAISettingsNudgeAction.prototype.version = null;
            BizAISettingsNudgeAction.prototype.updatedAtMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BizAISettingsNudgeAction.prototype, "_category", {
                get: $util.oneOfGetter($oneOfFields = ["category"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BizAISettingsNudgeAction.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BizAISettingsNudgeAction.prototype, "_updatedAtMs", {
                get: $util.oneOfGetter($oneOfFields = ["updatedAtMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BizAISettingsNudgeAction.create = function(properties) {
                return new BizAISettingsNudgeAction(properties);
            };

            BizAISettingsNudgeAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.category != null && $Object.hasOwnProperty.call(m, "category"))
                    w.uint32(8).int32(m.category);
                if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                    w.uint32(16).int64(m.version);
                if (m.updatedAtMs != null && $Object.hasOwnProperty.call(m, "updatedAtMs"))
                    w.uint32(24).int64(m.updatedAtMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BizAISettingsNudgeAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BizAISettingsNudgeAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.category = r.int32();
                            m._category = "category";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.version = r.int64();
                            m._version = "version";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.updatedAtMs = r.int64();
                            m._updatedAtMs = "updatedAtMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BizAISettingsNudgeAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BizAISettingsNudgeAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BizAISettingsNudgeAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BizAISettingsNudgeAction();
                switch (d.category) {
                case "UNKNOWN":
                case 0:
                    m.category = 0;
                    break;
                case "INSTRUCTIONS":
                case 1:
                    m.category = 1;
                    break;
                case "RESPONSE_SETTINGS":
                case 2:
                    m.category = 2;
                    break;
                case "EXAMPLE_RESPONSES":
                case 3:
                    m.category = 3;
                    break;
                case "KNOWLEDGE":
                case 4:
                    m.category = 4;
                    break;
                case "LEAD_GEN":
                case 5:
                    m.category = 5;
                    break;
                case "HANDOFF_REMOVAL_TIMING":
                case 6:
                    m.category = 6;
                    break;
                default:
                    if (typeof d.category === "number" && (d.category | 0) === d.category)
                        m.category = d.category;
                }
                if (d.version != null) {
                    if ($util.Long)
                        m.version = $util.Long.fromValue(d.version, false);
                    else if (typeof d.version === "string")
                        m.version = $parseInt(d.version, 10);
                    else if (typeof d.version === "number")
                        m.version = d.version;
                    else if (typeof d.version === "object")
                        m.version = new $util.LongBits(d.version.low >>> 0, d.version.high >>> 0).toNumber();
                }
                if (d.updatedAtMs != null) {
                    if ($util.Long)
                        m.updatedAtMs = $util.Long.fromValue(d.updatedAtMs, false);
                    else if (typeof d.updatedAtMs === "string")
                        m.updatedAtMs = $parseInt(d.updatedAtMs, 10);
                    else if (typeof d.updatedAtMs === "number")
                        m.updatedAtMs = d.updatedAtMs;
                    else if (typeof d.updatedAtMs === "object")
                        m.updatedAtMs = new $util.LongBits(d.updatedAtMs.low >>> 0, d.updatedAtMs.high >>> 0).toNumber();
                }
                return m;
            };

            BizAISettingsNudgeAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.category != null && $Object.hasOwnProperty.call(m, "category")) {
                    d.category = o.enums === $String ? $root.proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory[m.category] === $undefined ? m.category : $root.proto.SyncActionValue.BizAISettingsNudgeAction.BizAISettingsCategory[m.category] : m.category;
                }
                if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.version = typeof m.version === "number" ? $BigInt(m.version) : $util.Long.fromBits(m.version.low >>> 0, m.version.high >>> 0, false).toBigInt();
                    else if (typeof m.version === "number")
                        d.version = o.longs === $String ? $String(m.version) : m.version;
                    else
                        d.version = o.longs === $String ? $util.Long.prototype.toString.call(m.version) : o.longs === $Number ? new $util.LongBits(m.version.low >>> 0, m.version.high >>> 0).toNumber() : m.version;
                }
                if (m.updatedAtMs != null && $Object.hasOwnProperty.call(m, "updatedAtMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.updatedAtMs = typeof m.updatedAtMs === "number" ? $BigInt(m.updatedAtMs) : $util.Long.fromBits(m.updatedAtMs.low >>> 0, m.updatedAtMs.high >>> 0, false).toBigInt();
                    else if (typeof m.updatedAtMs === "number")
                        d.updatedAtMs = o.longs === $String ? $String(m.updatedAtMs) : m.updatedAtMs;
                    else
                        d.updatedAtMs = o.longs === $String ? $util.Long.prototype.toString.call(m.updatedAtMs) : o.longs === $Number ? new $util.LongBits(m.updatedAtMs.low >>> 0, m.updatedAtMs.high >>> 0).toNumber() : m.updatedAtMs;
                }
                return d;
            };

            BizAISettingsNudgeAction.prototype.toJSON = function() {
                return BizAISettingsNudgeAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BizAISettingsNudgeAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BizAISettingsNudgeAction";
            };

            BizAISettingsNudgeAction.BizAISettingsCategory = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNKNOWN"] = 0;
                values[valuesById[1] = "INSTRUCTIONS"] = 1;
                values[valuesById[2] = "RESPONSE_SETTINGS"] = 2;
                values[valuesById[3] = "EXAMPLE_RESPONSES"] = 3;
                values[valuesById[4] = "KNOWLEDGE"] = 4;
                values[valuesById[5] = "LEAD_GEN"] = 5;
                values[valuesById[6] = "HANDOFF_REMOVAL_TIMING"] = 6;
                return values;
            })();

            return BizAISettingsNudgeAction;
        })();

        SyncActionValue.BotWelcomeRequestAction = (function() {

            const BotWelcomeRequestAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BotWelcomeRequestAction.prototype.isSent = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BotWelcomeRequestAction.prototype, "_isSent", {
                get: $util.oneOfGetter($oneOfFields = ["isSent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BotWelcomeRequestAction.create = function(properties) {
                return new BotWelcomeRequestAction(properties);
            };

            BotWelcomeRequestAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isSent != null && $Object.hasOwnProperty.call(m, "isSent"))
                    w.uint32(8).bool(m.isSent);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BotWelcomeRequestAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BotWelcomeRequestAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isSent = r.bool();
                            m._isSent = "isSent";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BotWelcomeRequestAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BotWelcomeRequestAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BotWelcomeRequestAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BotWelcomeRequestAction();
                if (d.isSent != null) {
                    m.isSent = $Boolean(d.isSent);
                }
                return m;
            };

            BotWelcomeRequestAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isSent != null && $Object.hasOwnProperty.call(m, "isSent")) {
                    d.isSent = m.isSent;
                }
                return d;
            };

            BotWelcomeRequestAction.prototype.toJSON = function() {
                return BotWelcomeRequestAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BotWelcomeRequestAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BotWelcomeRequestAction";
            };

            return BotWelcomeRequestAction;
        })();

        SyncActionValue.BroadcastListParticipant = (function() {

            const BroadcastListParticipant = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BroadcastListParticipant.prototype.lidJid = "";
            BroadcastListParticipant.prototype.pnJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BroadcastListParticipant.prototype, "_pnJid", {
                get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BroadcastListParticipant.create = function(properties) {
                return new BroadcastListParticipant(properties);
            };

            BroadcastListParticipant.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(10).string(m.lidJid);
                if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid"))
                    w.uint32(18).string(m.pnJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BroadcastListParticipant.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BroadcastListParticipant();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.lidJid = r.stringVerify();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.pnJid = r.stringVerify();
                            m._pnJid = "pnJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "lidJid"))
                    throw $util.ProtocolError("missing required 'lidJid'", { instance: m });
                return m;
            };

            BroadcastListParticipant.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BroadcastListParticipant)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BroadcastListParticipant: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BroadcastListParticipant();
                if (d.lidJid != null) {
                    m.lidJid = $String(d.lidJid);
                }
                if (d.pnJid != null) {
                    m.pnJid = $String(d.pnJid);
                }
                return m;
            };

            BroadcastListParticipant.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.defaults) {
                    d.lidJid = "";
                }
                if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid")) {
                    d.lidJid = m.lidJid;
                }
                if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid")) {
                    d.pnJid = m.pnJid;
                }
                return d;
            };

            BroadcastListParticipant.prototype.toJSON = function() {
                return BroadcastListParticipant.toObject(this, $protobuf.util.toJSONOptions);
            };

            BroadcastListParticipant.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BroadcastListParticipant";
            };

            return BroadcastListParticipant;
        })();

        SyncActionValue.BubbleLockMessageAction = (function() {

            const BubbleLockMessageAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BubbleLockMessageAction.prototype.locked = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BubbleLockMessageAction.prototype, "_locked", {
                get: $util.oneOfGetter($oneOfFields = ["locked"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BubbleLockMessageAction.create = function(properties) {
                return new BubbleLockMessageAction(properties);
            };

            BubbleLockMessageAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.locked != null && $Object.hasOwnProperty.call(m, "locked"))
                    w.uint32(8).bool(m.locked);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BubbleLockMessageAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BubbleLockMessageAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.locked = r.bool();
                            m._locked = "locked";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BubbleLockMessageAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BubbleLockMessageAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BubbleLockMessageAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BubbleLockMessageAction();
                if (d.locked != null) {
                    m.locked = $Boolean(d.locked);
                }
                return m;
            };

            BubbleLockMessageAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.locked != null && $Object.hasOwnProperty.call(m, "locked")) {
                    d.locked = m.locked;
                }
                return d;
            };

            BubbleLockMessageAction.prototype.toJSON = function() {
                return BubbleLockMessageAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BubbleLockMessageAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BubbleLockMessageAction";
            };

            return BubbleLockMessageAction;
        })();

        SyncActionValue.BusinessBroadcastAssociationAction = (function() {

            const BusinessBroadcastAssociationAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessBroadcastAssociationAction.prototype.deleted = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastAssociationAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessBroadcastAssociationAction.create = function(properties) {
                return new BusinessBroadcastAssociationAction(properties);
            };

            BusinessBroadcastAssociationAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted"))
                    w.uint32(8).bool(m.deleted);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessBroadcastAssociationAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BusinessBroadcastAssociationAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.deleted = r.bool();
                            m._deleted = "deleted";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessBroadcastAssociationAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BusinessBroadcastAssociationAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BusinessBroadcastAssociationAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BusinessBroadcastAssociationAction();
                if (d.deleted != null) {
                    m.deleted = $Boolean(d.deleted);
                }
                return m;
            };

            BusinessBroadcastAssociationAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted")) {
                    d.deleted = m.deleted;
                }
                return d;
            };

            BusinessBroadcastAssociationAction.prototype.toJSON = function() {
                return BusinessBroadcastAssociationAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessBroadcastAssociationAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BusinessBroadcastAssociationAction";
            };

            return BusinessBroadcastAssociationAction;
        })();

        SyncActionValue.BusinessBroadcastCampaignAction = (function() {

            const BusinessBroadcastCampaignAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessBroadcastCampaignAction.prototype.deviceId = null;
            BusinessBroadcastCampaignAction.prototype.adId = null;
            BusinessBroadcastCampaignAction.prototype.name = null;
            BusinessBroadcastCampaignAction.prototype.msgId = null;
            BusinessBroadcastCampaignAction.prototype.broadcastJid = null;
            BusinessBroadcastCampaignAction.prototype.reservedQuota = null;
            BusinessBroadcastCampaignAction.prototype.scheduledTimestamp = null;
            BusinessBroadcastCampaignAction.prototype.createTimestamp = null;
            BusinessBroadcastCampaignAction.prototype.status = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_deviceId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_adId", {
                get: $util.oneOfGetter($oneOfFields = ["adId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_msgId", {
                get: $util.oneOfGetter($oneOfFields = ["msgId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_broadcastJid", {
                get: $util.oneOfGetter($oneOfFields = ["broadcastJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_reservedQuota", {
                get: $util.oneOfGetter($oneOfFields = ["reservedQuota"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_scheduledTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["scheduledTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_createTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["createTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastCampaignAction.prototype, "_status", {
                get: $util.oneOfGetter($oneOfFields = ["status"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessBroadcastCampaignAction.create = function(properties) {
                return new BusinessBroadcastCampaignAction(properties);
            };

            BusinessBroadcastCampaignAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId"))
                    w.uint32(8).int32(m.deviceId);
                if (m.adId != null && $Object.hasOwnProperty.call(m, "adId"))
                    w.uint32(18).string(m.adId);
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(26).string(m.name);
                if (m.msgId != null && $Object.hasOwnProperty.call(m, "msgId"))
                    w.uint32(34).string(m.msgId);
                if (m.broadcastJid != null && $Object.hasOwnProperty.call(m, "broadcastJid"))
                    w.uint32(42).string(m.broadcastJid);
                if (m.reservedQuota != null && $Object.hasOwnProperty.call(m, "reservedQuota"))
                    w.uint32(48).int32(m.reservedQuota);
                if (m.scheduledTimestamp != null && $Object.hasOwnProperty.call(m, "scheduledTimestamp"))
                    w.uint32(56).int64(m.scheduledTimestamp);
                if (m.createTimestamp != null && $Object.hasOwnProperty.call(m, "createTimestamp"))
                    w.uint32(64).int64(m.createTimestamp);
                if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                    w.uint32(72).int32(m.status);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessBroadcastCampaignAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BusinessBroadcastCampaignAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.deviceId = r.int32();
                            m._deviceId = "deviceId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.adId = r.stringVerify();
                            m._adId = "adId";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.msgId = r.stringVerify();
                            m._msgId = "msgId";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.broadcastJid = r.stringVerify();
                            m._broadcastJid = "broadcastJid";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.reservedQuota = r.int32();
                            m._reservedQuota = "reservedQuota";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.scheduledTimestamp = r.int64();
                            m._scheduledTimestamp = "scheduledTimestamp";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.createTimestamp = r.int64();
                            m._createTimestamp = "createTimestamp";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            m._status = "status";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessBroadcastCampaignAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BusinessBroadcastCampaignAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BusinessBroadcastCampaignAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BusinessBroadcastCampaignAction();
                if (d.deviceId != null) {
                    m.deviceId = d.deviceId | 0;
                }
                if (d.adId != null) {
                    m.adId = $String(d.adId);
                }
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.msgId != null) {
                    m.msgId = $String(d.msgId);
                }
                if (d.broadcastJid != null) {
                    m.broadcastJid = $String(d.broadcastJid);
                }
                if (d.reservedQuota != null) {
                    m.reservedQuota = d.reservedQuota | 0;
                }
                if (d.scheduledTimestamp != null) {
                    if ($util.Long)
                        m.scheduledTimestamp = $util.Long.fromValue(d.scheduledTimestamp, false);
                    else if (typeof d.scheduledTimestamp === "string")
                        m.scheduledTimestamp = $parseInt(d.scheduledTimestamp, 10);
                    else if (typeof d.scheduledTimestamp === "number")
                        m.scheduledTimestamp = d.scheduledTimestamp;
                    else if (typeof d.scheduledTimestamp === "object")
                        m.scheduledTimestamp = new $util.LongBits(d.scheduledTimestamp.low >>> 0, d.scheduledTimestamp.high >>> 0).toNumber();
                }
                if (d.createTimestamp != null) {
                    if ($util.Long)
                        m.createTimestamp = $util.Long.fromValue(d.createTimestamp, false);
                    else if (typeof d.createTimestamp === "string")
                        m.createTimestamp = $parseInt(d.createTimestamp, 10);
                    else if (typeof d.createTimestamp === "number")
                        m.createTimestamp = d.createTimestamp;
                    else if (typeof d.createTimestamp === "object")
                        m.createTimestamp = new $util.LongBits(d.createTimestamp.low >>> 0, d.createTimestamp.high >>> 0).toNumber();
                }
                switch (d.status) {
                case "DRAFT":
                case 1:
                    m.status = 1;
                    break;
                case "SCHEDULED":
                case 2:
                    m.status = 2;
                    break;
                case "PROCESSING":
                case 3:
                    m.status = 3;
                    break;
                case "FAILED":
                case 4:
                    m.status = 4;
                    break;
                case "SENT":
                case 5:
                    m.status = 5;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                return m;
            };

            BusinessBroadcastCampaignAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.deviceId != null && $Object.hasOwnProperty.call(m, "deviceId")) {
                    d.deviceId = m.deviceId;
                }
                if (m.adId != null && $Object.hasOwnProperty.call(m, "adId")) {
                    d.adId = m.adId;
                }
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.msgId != null && $Object.hasOwnProperty.call(m, "msgId")) {
                    d.msgId = m.msgId;
                }
                if (m.broadcastJid != null && $Object.hasOwnProperty.call(m, "broadcastJid")) {
                    d.broadcastJid = m.broadcastJid;
                }
                if (m.reservedQuota != null && $Object.hasOwnProperty.call(m, "reservedQuota")) {
                    d.reservedQuota = m.reservedQuota;
                }
                if (m.scheduledTimestamp != null && $Object.hasOwnProperty.call(m, "scheduledTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.scheduledTimestamp = typeof m.scheduledTimestamp === "number" ? $BigInt(m.scheduledTimestamp) : $util.Long.fromBits(m.scheduledTimestamp.low >>> 0, m.scheduledTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.scheduledTimestamp === "number")
                        d.scheduledTimestamp = o.longs === $String ? $String(m.scheduledTimestamp) : m.scheduledTimestamp;
                    else
                        d.scheduledTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.scheduledTimestamp) : o.longs === $Number ? new $util.LongBits(m.scheduledTimestamp.low >>> 0, m.scheduledTimestamp.high >>> 0).toNumber() : m.scheduledTimestamp;
                }
                if (m.createTimestamp != null && $Object.hasOwnProperty.call(m, "createTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.createTimestamp = typeof m.createTimestamp === "number" ? $BigInt(m.createTimestamp) : $util.Long.fromBits(m.createTimestamp.low >>> 0, m.createTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.createTimestamp === "number")
                        d.createTimestamp = o.longs === $String ? $String(m.createTimestamp) : m.createTimestamp;
                    else
                        d.createTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.createTimestamp) : o.longs === $Number ? new $util.LongBits(m.createTimestamp.low >>> 0, m.createTimestamp.high >>> 0).toNumber() : m.createTimestamp;
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.SyncActionValue.BusinessBroadcastCampaignStatus[m.status] === $undefined ? m.status : $root.proto.SyncActionValue.BusinessBroadcastCampaignStatus[m.status] : m.status;
                }
                return d;
            };

            BusinessBroadcastCampaignAction.prototype.toJSON = function() {
                return BusinessBroadcastCampaignAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessBroadcastCampaignAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BusinessBroadcastCampaignAction";
            };

            return BusinessBroadcastCampaignAction;
        })();

        SyncActionValue.BusinessBroadcastCampaignStatus = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[1] = "DRAFT"] = 1;
            values[valuesById[2] = "SCHEDULED"] = 2;
            values[valuesById[3] = "PROCESSING"] = 3;
            values[valuesById[4] = "FAILED"] = 4;
            values[valuesById[5] = "SENT"] = 5;
            return values;
        })();

        SyncActionValue.BusinessBroadcastInsightsAction = (function() {

            const BusinessBroadcastInsightsAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessBroadcastInsightsAction.prototype.recipientCount = null;
            BusinessBroadcastInsightsAction.prototype.deliveredCount = null;
            BusinessBroadcastInsightsAction.prototype.readCount = null;
            BusinessBroadcastInsightsAction.prototype.repliedCount = null;
            BusinessBroadcastInsightsAction.prototype.quickReplyCount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_recipientCount", {
                get: $util.oneOfGetter($oneOfFields = ["recipientCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_deliveredCount", {
                get: $util.oneOfGetter($oneOfFields = ["deliveredCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_readCount", {
                get: $util.oneOfGetter($oneOfFields = ["readCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_repliedCount", {
                get: $util.oneOfGetter($oneOfFields = ["repliedCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastInsightsAction.prototype, "_quickReplyCount", {
                get: $util.oneOfGetter($oneOfFields = ["quickReplyCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessBroadcastInsightsAction.create = function(properties) {
                return new BusinessBroadcastInsightsAction(properties);
            };

            BusinessBroadcastInsightsAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.recipientCount != null && $Object.hasOwnProperty.call(m, "recipientCount"))
                    w.uint32(8).int32(m.recipientCount);
                if (m.deliveredCount != null && $Object.hasOwnProperty.call(m, "deliveredCount"))
                    w.uint32(16).int32(m.deliveredCount);
                if (m.readCount != null && $Object.hasOwnProperty.call(m, "readCount"))
                    w.uint32(24).int32(m.readCount);
                if (m.repliedCount != null && $Object.hasOwnProperty.call(m, "repliedCount"))
                    w.uint32(32).int32(m.repliedCount);
                if (m.quickReplyCount != null && $Object.hasOwnProperty.call(m, "quickReplyCount"))
                    w.uint32(40).int32(m.quickReplyCount);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessBroadcastInsightsAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BusinessBroadcastInsightsAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.recipientCount = r.int32();
                            m._recipientCount = "recipientCount";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.deliveredCount = r.int32();
                            m._deliveredCount = "deliveredCount";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.readCount = r.int32();
                            m._readCount = "readCount";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.repliedCount = r.int32();
                            m._repliedCount = "repliedCount";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.quickReplyCount = r.int32();
                            m._quickReplyCount = "quickReplyCount";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessBroadcastInsightsAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BusinessBroadcastInsightsAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BusinessBroadcastInsightsAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BusinessBroadcastInsightsAction();
                if (d.recipientCount != null) {
                    m.recipientCount = d.recipientCount | 0;
                }
                if (d.deliveredCount != null) {
                    m.deliveredCount = d.deliveredCount | 0;
                }
                if (d.readCount != null) {
                    m.readCount = d.readCount | 0;
                }
                if (d.repliedCount != null) {
                    m.repliedCount = d.repliedCount | 0;
                }
                if (d.quickReplyCount != null) {
                    m.quickReplyCount = d.quickReplyCount | 0;
                }
                return m;
            };

            BusinessBroadcastInsightsAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.recipientCount != null && $Object.hasOwnProperty.call(m, "recipientCount")) {
                    d.recipientCount = m.recipientCount;
                }
                if (m.deliveredCount != null && $Object.hasOwnProperty.call(m, "deliveredCount")) {
                    d.deliveredCount = m.deliveredCount;
                }
                if (m.readCount != null && $Object.hasOwnProperty.call(m, "readCount")) {
                    d.readCount = m.readCount;
                }
                if (m.repliedCount != null && $Object.hasOwnProperty.call(m, "repliedCount")) {
                    d.repliedCount = m.repliedCount;
                }
                if (m.quickReplyCount != null && $Object.hasOwnProperty.call(m, "quickReplyCount")) {
                    d.quickReplyCount = m.quickReplyCount;
                }
                return d;
            };

            BusinessBroadcastInsightsAction.prototype.toJSON = function() {
                return BusinessBroadcastInsightsAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessBroadcastInsightsAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BusinessBroadcastInsightsAction";
            };

            return BusinessBroadcastInsightsAction;
        })();

        SyncActionValue.BusinessBroadcastListAction = (function() {

            const BusinessBroadcastListAction = function (p) {
                this.participants = [];
                this.labelIds = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessBroadcastListAction.prototype.deleted = null;
            BusinessBroadcastListAction.prototype.participants = $util.emptyArray;
            BusinessBroadcastListAction.prototype.listName = null;
            BusinessBroadcastListAction.prototype.labelIds = $util.emptyArray;
            BusinessBroadcastListAction.prototype.audienceExpression = null;
            BusinessBroadcastListAction.prototype.customAudienceFbid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastListAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastListAction.prototype, "_listName", {
                get: $util.oneOfGetter($oneOfFields = ["listName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastListAction.prototype, "_audienceExpression", {
                get: $util.oneOfGetter($oneOfFields = ["audienceExpression"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessBroadcastListAction.prototype, "_customAudienceFbid", {
                get: $util.oneOfGetter($oneOfFields = ["customAudienceFbid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessBroadcastListAction.create = function(properties) {
                return new BusinessBroadcastListAction(properties);
            };

            BusinessBroadcastListAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted"))
                    w.uint32(8).bool(m.deleted);
                if (m.participants != null && m.participants.length) {
                    for (var i = 0; i < m.participants.length; ++i)
                        $root.proto.SyncActionValue.BroadcastListParticipant.encode(m.participants[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.listName != null && $Object.hasOwnProperty.call(m, "listName"))
                    w.uint32(26).string(m.listName);
                if (m.labelIds != null && m.labelIds.length) {
                    for (var i = 0; i < m.labelIds.length; ++i)
                        w.uint32(34).string(m.labelIds[i]);
                }
                if (m.audienceExpression != null && $Object.hasOwnProperty.call(m, "audienceExpression"))
                    w.uint32(42).string(m.audienceExpression);
                if (m.customAudienceFbid != null && $Object.hasOwnProperty.call(m, "customAudienceFbid"))
                    w.uint32(50).string(m.customAudienceFbid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessBroadcastListAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BusinessBroadcastListAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.deleted = r.bool();
                            m._deleted = "deleted";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.participants && m.participants.length))
                                m.participants = [];
                            m.participants.push($root.proto.SyncActionValue.BroadcastListParticipant.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.listName = r.stringVerify();
                            m._listName = "listName";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(m.labelIds && m.labelIds.length))
                                m.labelIds = [];
                            m.labelIds.push(r.stringVerify());
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.audienceExpression = r.stringVerify();
                            m._audienceExpression = "audienceExpression";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.customAudienceFbid = r.stringVerify();
                            m._customAudienceFbid = "customAudienceFbid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessBroadcastListAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BusinessBroadcastListAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BusinessBroadcastListAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BusinessBroadcastListAction();
                if (d.deleted != null) {
                    m.deleted = $Boolean(d.deleted);
                }
                if (d.participants) {
                    if (!$Array.isArray(d.participants))
                        throw $TypeError(".proto.SyncActionValue.BusinessBroadcastListAction.participants: array expected");
                    m.participants = $Array(d.participants.length);
                    for (var i = 0; i < d.participants.length; ++i) {
                        if (!$util.isObject(d.participants[i]))
                            throw $TypeError(".proto.SyncActionValue.BusinessBroadcastListAction.participants: object expected");
                        m.participants[i] = $root.proto.SyncActionValue.BroadcastListParticipant.fromObject(d.participants[i], q + 1);
                    }
                }
                if (d.listName != null) {
                    m.listName = $String(d.listName);
                }
                if (d.labelIds) {
                    if (!$Array.isArray(d.labelIds))
                        throw $TypeError(".proto.SyncActionValue.BusinessBroadcastListAction.labelIds: array expected");
                    m.labelIds = $Array(d.labelIds.length);
                    for (var i = 0; i < d.labelIds.length; ++i) {
                        m.labelIds[i] = $String(d.labelIds[i]);
                    }
                }
                if (d.audienceExpression != null) {
                    m.audienceExpression = $String(d.audienceExpression);
                }
                if (d.customAudienceFbid != null) {
                    m.customAudienceFbid = $String(d.customAudienceFbid);
                }
                return m;
            };

            BusinessBroadcastListAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.participants = [];
                    d.labelIds = [];
                }
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted")) {
                    d.deleted = m.deleted;
                }
                if (m.participants && m.participants.length) {
                    d.participants = $Array(m.participants.length);
                    for (var j = 0; j < m.participants.length; ++j) {
                        d.participants[j] = $root.proto.SyncActionValue.BroadcastListParticipant.toObject(m.participants[j], o, q + 1);
                    }
                }
                if (m.listName != null && $Object.hasOwnProperty.call(m, "listName")) {
                    d.listName = m.listName;
                }
                if (m.labelIds && m.labelIds.length) {
                    d.labelIds = $Array(m.labelIds.length);
                    for (var j = 0; j < m.labelIds.length; ++j) {
                        d.labelIds[j] = m.labelIds[j];
                    }
                }
                if (m.audienceExpression != null && $Object.hasOwnProperty.call(m, "audienceExpression")) {
                    d.audienceExpression = m.audienceExpression;
                }
                if (m.customAudienceFbid != null && $Object.hasOwnProperty.call(m, "customAudienceFbid")) {
                    d.customAudienceFbid = m.customAudienceFbid;
                }
                return d;
            };

            BusinessBroadcastListAction.prototype.toJSON = function() {
                return BusinessBroadcastListAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessBroadcastListAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BusinessBroadcastListAction";
            };

            return BusinessBroadcastListAction;
        })();

        SyncActionValue.BusinessFolderActivationAction = (function() {

            const BusinessFolderActivationAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            BusinessFolderActivationAction.prototype.activated = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(BusinessFolderActivationAction.prototype, "_activated", {
                get: $util.oneOfGetter($oneOfFields = ["activated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            BusinessFolderActivationAction.create = function(properties) {
                return new BusinessFolderActivationAction(properties);
            };

            BusinessFolderActivationAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.activated != null && $Object.hasOwnProperty.call(m, "activated"))
                    w.uint32(8).bool(m.activated);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            BusinessFolderActivationAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.BusinessFolderActivationAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.activated = r.bool();
                            m._activated = "activated";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            BusinessFolderActivationAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.BusinessFolderActivationAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.BusinessFolderActivationAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.BusinessFolderActivationAction();
                if (d.activated != null) {
                    m.activated = $Boolean(d.activated);
                }
                return m;
            };

            BusinessFolderActivationAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.activated != null && $Object.hasOwnProperty.call(m, "activated")) {
                    d.activated = m.activated;
                }
                return d;
            };

            BusinessFolderActivationAction.prototype.toJSON = function() {
                return BusinessFolderActivationAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            BusinessFolderActivationAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.BusinessFolderActivationAction";
            };

            return BusinessFolderActivationAction;
        })();

        SyncActionValue.CallLogAction = (function() {

            const CallLogAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CallLogAction.prototype.callLogRecord = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallLogAction.prototype, "_callLogRecord", {
                get: $util.oneOfGetter($oneOfFields = ["callLogRecord"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CallLogAction.create = function(properties) {
                return new CallLogAction(properties);
            };

            CallLogAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.callLogRecord != null && $Object.hasOwnProperty.call(m, "callLogRecord"))
                    $root.proto.CallLogRecord.encode(m.callLogRecord, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CallLogAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CallLogAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.callLogRecord = $root.proto.CallLogRecord.decode(r, r.uint32(), $undefined, q + 1, m.callLogRecord);
                            m._callLogRecord = "callLogRecord";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CallLogAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CallLogAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CallLogAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CallLogAction();
                if (d.callLogRecord != null) {
                    if (!$util.isObject(d.callLogRecord))
                        throw $TypeError(".proto.SyncActionValue.CallLogAction.callLogRecord: object expected");
                    m.callLogRecord = $root.proto.CallLogRecord.fromObject(d.callLogRecord, q + 1);
                }
                return m;
            };

            CallLogAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.callLogRecord != null && $Object.hasOwnProperty.call(m, "callLogRecord")) {
                    d.callLogRecord = $root.proto.CallLogRecord.toObject(m.callLogRecord, o, q + 1);
                }
                return d;
            };

            CallLogAction.prototype.toJSON = function() {
                return CallLogAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            CallLogAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CallLogAction";
            };

            return CallLogAction;
        })();

        SyncActionValue.ChatAssignmentAction = (function() {

            const ChatAssignmentAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatAssignmentAction.prototype.deviceAgentId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatAssignmentAction.prototype, "_deviceAgentId", {
                get: $util.oneOfGetter($oneOfFields = ["deviceAgentId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatAssignmentAction.create = function(properties) {
                return new ChatAssignmentAction(properties);
            };

            ChatAssignmentAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.deviceAgentId != null && $Object.hasOwnProperty.call(m, "deviceAgentId"))
                    w.uint32(10).string(m.deviceAgentId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatAssignmentAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ChatAssignmentAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.deviceAgentId = r.stringVerify();
                            m._deviceAgentId = "deviceAgentId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatAssignmentAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ChatAssignmentAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ChatAssignmentAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ChatAssignmentAction();
                if (d.deviceAgentId != null) {
                    m.deviceAgentId = $String(d.deviceAgentId);
                }
                return m;
            };

            ChatAssignmentAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.deviceAgentId != null && $Object.hasOwnProperty.call(m, "deviceAgentId")) {
                    d.deviceAgentId = m.deviceAgentId;
                }
                return d;
            };

            ChatAssignmentAction.prototype.toJSON = function() {
                return ChatAssignmentAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatAssignmentAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ChatAssignmentAction";
            };

            return ChatAssignmentAction;
        })();

        SyncActionValue.ChatAssignmentOpenedStatusAction = (function() {

            const ChatAssignmentOpenedStatusAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ChatAssignmentOpenedStatusAction.prototype.chatOpened = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ChatAssignmentOpenedStatusAction.prototype, "_chatOpened", {
                get: $util.oneOfGetter($oneOfFields = ["chatOpened"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ChatAssignmentOpenedStatusAction.create = function(properties) {
                return new ChatAssignmentOpenedStatusAction(properties);
            };

            ChatAssignmentOpenedStatusAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.chatOpened != null && $Object.hasOwnProperty.call(m, "chatOpened"))
                    w.uint32(8).bool(m.chatOpened);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ChatAssignmentOpenedStatusAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.chatOpened = r.bool();
                            m._chatOpened = "chatOpened";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ChatAssignmentOpenedStatusAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ChatAssignmentOpenedStatusAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ChatAssignmentOpenedStatusAction();
                if (d.chatOpened != null) {
                    m.chatOpened = $Boolean(d.chatOpened);
                }
                return m;
            };

            ChatAssignmentOpenedStatusAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.chatOpened != null && $Object.hasOwnProperty.call(m, "chatOpened")) {
                    d.chatOpened = m.chatOpened;
                }
                return d;
            };

            ChatAssignmentOpenedStatusAction.prototype.toJSON = function() {
                return ChatAssignmentOpenedStatusAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ChatAssignmentOpenedStatusAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ChatAssignmentOpenedStatusAction";
            };

            return ChatAssignmentOpenedStatusAction;
        })();

        SyncActionValue.ClearChatAction = (function() {

            const ClearChatAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ClearChatAction.prototype.messageRange = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ClearChatAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ClearChatAction.create = function(properties) {
                return new ClearChatAction(properties);
            };

            ClearChatAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(m.messageRange, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ClearChatAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ClearChatAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(r, r.uint32(), $undefined, q + 1, m.messageRange);
                            m._messageRange = "messageRange";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ClearChatAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ClearChatAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ClearChatAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ClearChatAction();
                if (d.messageRange != null) {
                    if (!$util.isObject(d.messageRange))
                        throw $TypeError(".proto.SyncActionValue.ClearChatAction.messageRange: object expected");
                    m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(d.messageRange, q + 1);
                }
                return m;
            };

            ClearChatAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange")) {
                    d.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(m.messageRange, o, q + 1);
                }
                return d;
            };

            ClearChatAction.prototype.toJSON = function() {
                return ClearChatAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ClearChatAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ClearChatAction";
            };

            return ClearChatAction;
        })();

        SyncActionValue.CoexV2VersionAction = (function() {

            const CoexV2VersionAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CoexV2VersionAction.prototype.version = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CoexV2VersionAction.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CoexV2VersionAction.create = function(properties) {
                return new CoexV2VersionAction(properties);
            };

            CoexV2VersionAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                    w.uint32(8).uint64(m.version);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CoexV2VersionAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CoexV2VersionAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.version = r.uint64();
                            m._version = "version";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CoexV2VersionAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CoexV2VersionAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CoexV2VersionAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CoexV2VersionAction();
                if (d.version != null) {
                    if ($util.Long)
                        m.version = $util.Long.fromValue(d.version, true);
                    else if (typeof d.version === "string")
                        m.version = $parseInt(d.version, 10);
                    else if (typeof d.version === "number")
                        m.version = d.version;
                    else if (typeof d.version === "object")
                        m.version = new $util.LongBits(d.version.low >>> 0, d.version.high >>> 0).toNumber(true);
                }
                return m;
            };

            CoexV2VersionAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.version = typeof m.version === "number" ? $BigInt(m.version) : $util.Long.fromBits(m.version.low >>> 0, m.version.high >>> 0, true).toBigInt();
                    else if (typeof m.version === "number")
                        d.version = o.longs === $String ? $String(m.version) : m.version;
                    else
                        d.version = o.longs === $String ? $util.Long.prototype.toString.call(m.version) : o.longs === $Number ? new $util.LongBits(m.version.low >>> 0, m.version.high >>> 0).toNumber(true) : m.version;
                }
                return d;
            };

            CoexV2VersionAction.prototype.toJSON = function() {
                return CoexV2VersionAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            CoexV2VersionAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CoexV2VersionAction";
            };

            return CoexV2VersionAction;
        })();

        SyncActionValue.ContactAction = (function() {

            const ContactAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactAction.prototype.fullName = null;
            ContactAction.prototype.firstName = null;
            ContactAction.prototype.lidJid = null;
            ContactAction.prototype.saveOnPrimaryAddressbook = null;
            ContactAction.prototype.pnJid = null;
            ContactAction.prototype.username = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactAction.prototype, "_fullName", {
                get: $util.oneOfGetter($oneOfFields = ["fullName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactAction.prototype, "_firstName", {
                get: $util.oneOfGetter($oneOfFields = ["firstName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactAction.prototype, "_lidJid", {
                get: $util.oneOfGetter($oneOfFields = ["lidJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactAction.prototype, "_saveOnPrimaryAddressbook", {
                get: $util.oneOfGetter($oneOfFields = ["saveOnPrimaryAddressbook"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactAction.prototype, "_pnJid", {
                get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactAction.prototype, "_username", {
                get: $util.oneOfGetter($oneOfFields = ["username"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactAction.create = function(properties) {
                return new ContactAction(properties);
            };

            ContactAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName"))
                    w.uint32(10).string(m.fullName);
                if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName"))
                    w.uint32(18).string(m.firstName);
                if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid"))
                    w.uint32(26).string(m.lidJid);
                if (m.saveOnPrimaryAddressbook != null && $Object.hasOwnProperty.call(m, "saveOnPrimaryAddressbook"))
                    w.uint32(32).bool(m.saveOnPrimaryAddressbook);
                if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid"))
                    w.uint32(42).string(m.pnJid);
                if (m.username != null && $Object.hasOwnProperty.call(m, "username"))
                    w.uint32(50).string(m.username);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ContactAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.fullName = r.stringVerify();
                            m._fullName = "fullName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.firstName = r.stringVerify();
                            m._firstName = "firstName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.lidJid = r.stringVerify();
                            m._lidJid = "lidJid";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.saveOnPrimaryAddressbook = r.bool();
                            m._saveOnPrimaryAddressbook = "saveOnPrimaryAddressbook";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.pnJid = r.stringVerify();
                            m._pnJid = "pnJid";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.username = r.stringVerify();
                            m._username = "username";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ContactAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ContactAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ContactAction();
                if (d.fullName != null) {
                    m.fullName = $String(d.fullName);
                }
                if (d.firstName != null) {
                    m.firstName = $String(d.firstName);
                }
                if (d.lidJid != null) {
                    m.lidJid = $String(d.lidJid);
                }
                if (d.saveOnPrimaryAddressbook != null) {
                    m.saveOnPrimaryAddressbook = $Boolean(d.saveOnPrimaryAddressbook);
                }
                if (d.pnJid != null) {
                    m.pnJid = $String(d.pnJid);
                }
                if (d.username != null) {
                    m.username = $String(d.username);
                }
                return m;
            };

            ContactAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName")) {
                    d.fullName = m.fullName;
                }
                if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName")) {
                    d.firstName = m.firstName;
                }
                if (m.lidJid != null && $Object.hasOwnProperty.call(m, "lidJid")) {
                    d.lidJid = m.lidJid;
                }
                if (m.saveOnPrimaryAddressbook != null && $Object.hasOwnProperty.call(m, "saveOnPrimaryAddressbook")) {
                    d.saveOnPrimaryAddressbook = m.saveOnPrimaryAddressbook;
                }
                if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid")) {
                    d.pnJid = m.pnJid;
                }
                if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                    d.username = m.username;
                }
                return d;
            };

            ContactAction.prototype.toJSON = function() {
                return ContactAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ContactAction";
            };

            return ContactAction;
        })();

        SyncActionValue.ContactManagerMetadataAction = (function() {

            const ContactManagerMetadataAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ContactManagerMetadataAction.prototype.isHidden = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ContactManagerMetadataAction.prototype, "_isHidden", {
                get: $util.oneOfGetter($oneOfFields = ["isHidden"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ContactManagerMetadataAction.create = function(properties) {
                return new ContactManagerMetadataAction(properties);
            };

            ContactManagerMetadataAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isHidden != null && $Object.hasOwnProperty.call(m, "isHidden"))
                    w.uint32(8).bool(m.isHidden);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ContactManagerMetadataAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ContactManagerMetadataAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isHidden = r.bool();
                            m._isHidden = "isHidden";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ContactManagerMetadataAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ContactManagerMetadataAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ContactManagerMetadataAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ContactManagerMetadataAction();
                if (d.isHidden != null) {
                    m.isHidden = $Boolean(d.isHidden);
                }
                return m;
            };

            ContactManagerMetadataAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isHidden != null && $Object.hasOwnProperty.call(m, "isHidden")) {
                    d.isHidden = m.isHidden;
                }
                return d;
            };

            ContactManagerMetadataAction.prototype.toJSON = function() {
                return ContactManagerMetadataAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ContactManagerMetadataAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ContactManagerMetadataAction";
            };

            return ContactManagerMetadataAction;
        })();

        SyncActionValue.CtwaMessageReceivedAction = (function() {

            const CtwaMessageReceivedAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CtwaMessageReceivedAction.prototype.isCtwaMessageReceived = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CtwaMessageReceivedAction.prototype, "_isCtwaMessageReceived", {
                get: $util.oneOfGetter($oneOfFields = ["isCtwaMessageReceived"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CtwaMessageReceivedAction.create = function(properties) {
                return new CtwaMessageReceivedAction(properties);
            };

            CtwaMessageReceivedAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isCtwaMessageReceived != null && $Object.hasOwnProperty.call(m, "isCtwaMessageReceived"))
                    w.uint32(8).bool(m.isCtwaMessageReceived);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CtwaMessageReceivedAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CtwaMessageReceivedAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isCtwaMessageReceived = r.bool();
                            m._isCtwaMessageReceived = "isCtwaMessageReceived";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CtwaMessageReceivedAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CtwaMessageReceivedAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CtwaMessageReceivedAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CtwaMessageReceivedAction();
                if (d.isCtwaMessageReceived != null) {
                    m.isCtwaMessageReceived = $Boolean(d.isCtwaMessageReceived);
                }
                return m;
            };

            CtwaMessageReceivedAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isCtwaMessageReceived != null && $Object.hasOwnProperty.call(m, "isCtwaMessageReceived")) {
                    d.isCtwaMessageReceived = m.isCtwaMessageReceived;
                }
                return d;
            };

            CtwaMessageReceivedAction.prototype.toJSON = function() {
                return CtwaMessageReceivedAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            CtwaMessageReceivedAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CtwaMessageReceivedAction";
            };

            return CtwaMessageReceivedAction;
        })();

        SyncActionValue.CtwaPerCustomerDataSharingAction = (function() {

            const CtwaPerCustomerDataSharingAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CtwaPerCustomerDataSharingAction.prototype.isCtwaPerCustomerDataSharingEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CtwaPerCustomerDataSharingAction.prototype, "_isCtwaPerCustomerDataSharingEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isCtwaPerCustomerDataSharingEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CtwaPerCustomerDataSharingAction.create = function(properties) {
                return new CtwaPerCustomerDataSharingAction(properties);
            };

            CtwaPerCustomerDataSharingAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isCtwaPerCustomerDataSharingEnabled != null && $Object.hasOwnProperty.call(m, "isCtwaPerCustomerDataSharingEnabled"))
                    w.uint32(8).bool(m.isCtwaPerCustomerDataSharingEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CtwaPerCustomerDataSharingAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isCtwaPerCustomerDataSharingEnabled = r.bool();
                            m._isCtwaPerCustomerDataSharingEnabled = "isCtwaPerCustomerDataSharingEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CtwaPerCustomerDataSharingAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CtwaPerCustomerDataSharingAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CtwaPerCustomerDataSharingAction();
                if (d.isCtwaPerCustomerDataSharingEnabled != null) {
                    m.isCtwaPerCustomerDataSharingEnabled = $Boolean(d.isCtwaPerCustomerDataSharingEnabled);
                }
                return m;
            };

            CtwaPerCustomerDataSharingAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isCtwaPerCustomerDataSharingEnabled != null && $Object.hasOwnProperty.call(m, "isCtwaPerCustomerDataSharingEnabled")) {
                    d.isCtwaPerCustomerDataSharingEnabled = m.isCtwaPerCustomerDataSharingEnabled;
                }
                return d;
            };

            CtwaPerCustomerDataSharingAction.prototype.toJSON = function() {
                return CtwaPerCustomerDataSharingAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            CtwaPerCustomerDataSharingAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CtwaPerCustomerDataSharingAction";
            };

            return CtwaPerCustomerDataSharingAction;
        })();

        SyncActionValue.CustomPaymentMethod = (function() {

            const CustomPaymentMethod = function (p) {
                this.metadata = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CustomPaymentMethod.prototype.credentialId = "";
            CustomPaymentMethod.prototype.country = "";
            CustomPaymentMethod.prototype.type = "";
            CustomPaymentMethod.prototype.metadata = $util.emptyArray;

            CustomPaymentMethod.create = function(properties) {
                return new CustomPaymentMethod(properties);
            };

            CustomPaymentMethod.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(10).string(m.credentialId);
                w.uint32(18).string(m.country);
                w.uint32(26).string(m.type);
                if (m.metadata != null && m.metadata.length) {
                    for (var i = 0; i < m.metadata.length; ++i)
                        $root.proto.SyncActionValue.CustomPaymentMethodMetadata.encode(m.metadata[i], w.uint32(34).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CustomPaymentMethod.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CustomPaymentMethod();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.credentialId = r.stringVerify();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.country = r.stringVerify();
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.type = r.stringVerify();
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            if (!(m.metadata && m.metadata.length))
                                m.metadata = [];
                            m.metadata.push($root.proto.SyncActionValue.CustomPaymentMethodMetadata.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "credentialId"))
                    throw $util.ProtocolError("missing required 'credentialId'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "country"))
                    throw $util.ProtocolError("missing required 'country'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "type"))
                    throw $util.ProtocolError("missing required 'type'", { instance: m });
                return m;
            };

            CustomPaymentMethod.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CustomPaymentMethod)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CustomPaymentMethod: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CustomPaymentMethod();
                if (d.credentialId != null) {
                    m.credentialId = $String(d.credentialId);
                }
                if (d.country != null) {
                    m.country = $String(d.country);
                }
                if (d.type != null) {
                    m.type = $String(d.type);
                }
                if (d.metadata) {
                    if (!$Array.isArray(d.metadata))
                        throw $TypeError(".proto.SyncActionValue.CustomPaymentMethod.metadata: array expected");
                    m.metadata = $Array(d.metadata.length);
                    for (var i = 0; i < d.metadata.length; ++i) {
                        if (!$util.isObject(d.metadata[i]))
                            throw $TypeError(".proto.SyncActionValue.CustomPaymentMethod.metadata: object expected");
                        m.metadata[i] = $root.proto.SyncActionValue.CustomPaymentMethodMetadata.fromObject(d.metadata[i], q + 1);
                    }
                }
                return m;
            };

            CustomPaymentMethod.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.metadata = [];
                }
                if (o.defaults) {
                    d.credentialId = "";
                    d.country = "";
                    d.type = "";
                }
                if (m.credentialId != null && $Object.hasOwnProperty.call(m, "credentialId")) {
                    d.credentialId = m.credentialId;
                }
                if (m.country != null && $Object.hasOwnProperty.call(m, "country")) {
                    d.country = m.country;
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = m.type;
                }
                if (m.metadata && m.metadata.length) {
                    d.metadata = $Array(m.metadata.length);
                    for (var j = 0; j < m.metadata.length; ++j) {
                        d.metadata[j] = $root.proto.SyncActionValue.CustomPaymentMethodMetadata.toObject(m.metadata[j], o, q + 1);
                    }
                }
                return d;
            };

            CustomPaymentMethod.prototype.toJSON = function() {
                return CustomPaymentMethod.toObject(this, $protobuf.util.toJSONOptions);
            };

            CustomPaymentMethod.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CustomPaymentMethod";
            };

            return CustomPaymentMethod;
        })();

        SyncActionValue.CustomPaymentMethodMetadata = (function() {

            const CustomPaymentMethodMetadata = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CustomPaymentMethodMetadata.prototype.key = "";
            CustomPaymentMethodMetadata.prototype.value = "";

            CustomPaymentMethodMetadata.create = function(properties) {
                return new CustomPaymentMethodMetadata(properties);
            };

            CustomPaymentMethodMetadata.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(10).string(m.key);
                w.uint32(18).string(m.value);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CustomPaymentMethodMetadata.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CustomPaymentMethodMetadata();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = r.stringVerify();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.value = r.stringVerify();
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "key"))
                    throw $util.ProtocolError("missing required 'key'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "value"))
                    throw $util.ProtocolError("missing required 'value'", { instance: m });
                return m;
            };

            CustomPaymentMethodMetadata.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CustomPaymentMethodMetadata)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CustomPaymentMethodMetadata: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CustomPaymentMethodMetadata();
                if (d.key != null) {
                    m.key = $String(d.key);
                }
                if (d.value != null) {
                    m.value = $String(d.value);
                }
                return m;
            };

            CustomPaymentMethodMetadata.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.defaults) {
                    d.key = "";
                    d.value = "";
                }
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = m.key;
                }
                if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                    d.value = m.value;
                }
                return d;
            };

            CustomPaymentMethodMetadata.prototype.toJSON = function() {
                return CustomPaymentMethodMetadata.toObject(this, $protobuf.util.toJSONOptions);
            };

            CustomPaymentMethodMetadata.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CustomPaymentMethodMetadata";
            };

            return CustomPaymentMethodMetadata;
        })();

        SyncActionValue.CustomPaymentMethodsAction = (function() {

            const CustomPaymentMethodsAction = function (p) {
                this.customPaymentMethods = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CustomPaymentMethodsAction.prototype.customPaymentMethods = $util.emptyArray;

            CustomPaymentMethodsAction.create = function(properties) {
                return new CustomPaymentMethodsAction(properties);
            };

            CustomPaymentMethodsAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.customPaymentMethods != null && m.customPaymentMethods.length) {
                    for (var i = 0; i < m.customPaymentMethods.length; ++i)
                        $root.proto.SyncActionValue.CustomPaymentMethod.encode(m.customPaymentMethods[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CustomPaymentMethodsAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CustomPaymentMethodsAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.customPaymentMethods && m.customPaymentMethods.length))
                                m.customPaymentMethods = [];
                            m.customPaymentMethods.push($root.proto.SyncActionValue.CustomPaymentMethod.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CustomPaymentMethodsAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CustomPaymentMethodsAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CustomPaymentMethodsAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CustomPaymentMethodsAction();
                if (d.customPaymentMethods) {
                    if (!$Array.isArray(d.customPaymentMethods))
                        throw $TypeError(".proto.SyncActionValue.CustomPaymentMethodsAction.customPaymentMethods: array expected");
                    m.customPaymentMethods = $Array(d.customPaymentMethods.length);
                    for (var i = 0; i < d.customPaymentMethods.length; ++i) {
                        if (!$util.isObject(d.customPaymentMethods[i]))
                            throw $TypeError(".proto.SyncActionValue.CustomPaymentMethodsAction.customPaymentMethods: object expected");
                        m.customPaymentMethods[i] = $root.proto.SyncActionValue.CustomPaymentMethod.fromObject(d.customPaymentMethods[i], q + 1);
                    }
                }
                return m;
            };

            CustomPaymentMethodsAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.customPaymentMethods = [];
                }
                if (m.customPaymentMethods && m.customPaymentMethods.length) {
                    d.customPaymentMethods = $Array(m.customPaymentMethods.length);
                    for (var j = 0; j < m.customPaymentMethods.length; ++j) {
                        d.customPaymentMethods[j] = $root.proto.SyncActionValue.CustomPaymentMethod.toObject(m.customPaymentMethods[j], o, q + 1);
                    }
                }
                return d;
            };

            CustomPaymentMethodsAction.prototype.toJSON = function() {
                return CustomPaymentMethodsAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            CustomPaymentMethodsAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CustomPaymentMethodsAction";
            };

            return CustomPaymentMethodsAction;
        })();

        SyncActionValue.CustomerDataAction = (function() {

            const CustomerDataAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CustomerDataAction.prototype.chatJid = null;
            CustomerDataAction.prototype.contactType = null;
            CustomerDataAction.prototype.email = null;
            CustomerDataAction.prototype.altPhoneNumbers = null;
            CustomerDataAction.prototype.birthday = null;
            CustomerDataAction.prototype.address = null;
            CustomerDataAction.prototype.acquisitionSource = null;
            CustomerDataAction.prototype.leadStage = null;
            CustomerDataAction.prototype.lastOrder = null;
            CustomerDataAction.prototype.createdAt = null;
            CustomerDataAction.prototype.modifiedAt = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_chatJid", {
                get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_contactType", {
                get: $util.oneOfGetter($oneOfFields = ["contactType"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_email", {
                get: $util.oneOfGetter($oneOfFields = ["email"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_altPhoneNumbers", {
                get: $util.oneOfGetter($oneOfFields = ["altPhoneNumbers"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_birthday", {
                get: $util.oneOfGetter($oneOfFields = ["birthday"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_address", {
                get: $util.oneOfGetter($oneOfFields = ["address"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_acquisitionSource", {
                get: $util.oneOfGetter($oneOfFields = ["acquisitionSource"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_leadStage", {
                get: $util.oneOfGetter($oneOfFields = ["leadStage"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_lastOrder", {
                get: $util.oneOfGetter($oneOfFields = ["lastOrder"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_createdAt", {
                get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CustomerDataAction.prototype, "_modifiedAt", {
                get: $util.oneOfGetter($oneOfFields = ["modifiedAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CustomerDataAction.create = function(properties) {
                return new CustomerDataAction(properties);
            };

            CustomerDataAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid"))
                    w.uint32(10).string(m.chatJid);
                if (m.contactType != null && $Object.hasOwnProperty.call(m, "contactType"))
                    w.uint32(16).int32(m.contactType);
                if (m.email != null && $Object.hasOwnProperty.call(m, "email"))
                    w.uint32(26).string(m.email);
                if (m.altPhoneNumbers != null && $Object.hasOwnProperty.call(m, "altPhoneNumbers"))
                    w.uint32(34).string(m.altPhoneNumbers);
                if (m.birthday != null && $Object.hasOwnProperty.call(m, "birthday"))
                    w.uint32(40).int64(m.birthday);
                if (m.address != null && $Object.hasOwnProperty.call(m, "address"))
                    w.uint32(50).string(m.address);
                if (m.acquisitionSource != null && $Object.hasOwnProperty.call(m, "acquisitionSource"))
                    w.uint32(56).int32(m.acquisitionSource);
                if (m.leadStage != null && $Object.hasOwnProperty.call(m, "leadStage"))
                    w.uint32(64).int32(m.leadStage);
                if (m.lastOrder != null && $Object.hasOwnProperty.call(m, "lastOrder"))
                    w.uint32(72).int64(m.lastOrder);
                if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt"))
                    w.uint32(80).int64(m.createdAt);
                if (m.modifiedAt != null && $Object.hasOwnProperty.call(m, "modifiedAt"))
                    w.uint32(88).int64(m.modifiedAt);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CustomerDataAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.CustomerDataAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.chatJid = r.stringVerify();
                            m._chatJid = "chatJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.contactType = r.int32();
                            m._contactType = "contactType";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.email = r.stringVerify();
                            m._email = "email";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.altPhoneNumbers = r.stringVerify();
                            m._altPhoneNumbers = "altPhoneNumbers";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.birthday = r.int64();
                            m._birthday = "birthday";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            m.address = r.stringVerify();
                            m._address = "address";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.acquisitionSource = r.int32();
                            m._acquisitionSource = "acquisitionSource";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.leadStage = r.int32();
                            m._leadStage = "leadStage";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.lastOrder = r.int64();
                            m._lastOrder = "lastOrder";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.createdAt = r.int64();
                            m._createdAt = "createdAt";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.modifiedAt = r.int64();
                            m._modifiedAt = "modifiedAt";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CustomerDataAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.CustomerDataAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.CustomerDataAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.CustomerDataAction();
                if (d.chatJid != null) {
                    m.chatJid = $String(d.chatJid);
                }
                if (d.contactType != null) {
                    m.contactType = d.contactType | 0;
                }
                if (d.email != null) {
                    m.email = $String(d.email);
                }
                if (d.altPhoneNumbers != null) {
                    m.altPhoneNumbers = $String(d.altPhoneNumbers);
                }
                if (d.birthday != null) {
                    if ($util.Long)
                        m.birthday = $util.Long.fromValue(d.birthday, false);
                    else if (typeof d.birthday === "string")
                        m.birthday = $parseInt(d.birthday, 10);
                    else if (typeof d.birthday === "number")
                        m.birthday = d.birthday;
                    else if (typeof d.birthday === "object")
                        m.birthday = new $util.LongBits(d.birthday.low >>> 0, d.birthday.high >>> 0).toNumber();
                }
                if (d.address != null) {
                    m.address = $String(d.address);
                }
                if (d.acquisitionSource != null) {
                    m.acquisitionSource = d.acquisitionSource | 0;
                }
                if (d.leadStage != null) {
                    m.leadStage = d.leadStage | 0;
                }
                if (d.lastOrder != null) {
                    if ($util.Long)
                        m.lastOrder = $util.Long.fromValue(d.lastOrder, false);
                    else if (typeof d.lastOrder === "string")
                        m.lastOrder = $parseInt(d.lastOrder, 10);
                    else if (typeof d.lastOrder === "number")
                        m.lastOrder = d.lastOrder;
                    else if (typeof d.lastOrder === "object")
                        m.lastOrder = new $util.LongBits(d.lastOrder.low >>> 0, d.lastOrder.high >>> 0).toNumber();
                }
                if (d.createdAt != null) {
                    if ($util.Long)
                        m.createdAt = $util.Long.fromValue(d.createdAt, false);
                    else if (typeof d.createdAt === "string")
                        m.createdAt = $parseInt(d.createdAt, 10);
                    else if (typeof d.createdAt === "number")
                        m.createdAt = d.createdAt;
                    else if (typeof d.createdAt === "object")
                        m.createdAt = new $util.LongBits(d.createdAt.low >>> 0, d.createdAt.high >>> 0).toNumber();
                }
                if (d.modifiedAt != null) {
                    if ($util.Long)
                        m.modifiedAt = $util.Long.fromValue(d.modifiedAt, false);
                    else if (typeof d.modifiedAt === "string")
                        m.modifiedAt = $parseInt(d.modifiedAt, 10);
                    else if (typeof d.modifiedAt === "number")
                        m.modifiedAt = d.modifiedAt;
                    else if (typeof d.modifiedAt === "object")
                        m.modifiedAt = new $util.LongBits(d.modifiedAt.low >>> 0, d.modifiedAt.high >>> 0).toNumber();
                }
                return m;
            };

            CustomerDataAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid")) {
                    d.chatJid = m.chatJid;
                }
                if (m.contactType != null && $Object.hasOwnProperty.call(m, "contactType")) {
                    d.contactType = m.contactType;
                }
                if (m.email != null && $Object.hasOwnProperty.call(m, "email")) {
                    d.email = m.email;
                }
                if (m.altPhoneNumbers != null && $Object.hasOwnProperty.call(m, "altPhoneNumbers")) {
                    d.altPhoneNumbers = m.altPhoneNumbers;
                }
                if (m.birthday != null && $Object.hasOwnProperty.call(m, "birthday")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.birthday = typeof m.birthday === "number" ? $BigInt(m.birthday) : $util.Long.fromBits(m.birthday.low >>> 0, m.birthday.high >>> 0, false).toBigInt();
                    else if (typeof m.birthday === "number")
                        d.birthday = o.longs === $String ? $String(m.birthday) : m.birthday;
                    else
                        d.birthday = o.longs === $String ? $util.Long.prototype.toString.call(m.birthday) : o.longs === $Number ? new $util.LongBits(m.birthday.low >>> 0, m.birthday.high >>> 0).toNumber() : m.birthday;
                }
                if (m.address != null && $Object.hasOwnProperty.call(m, "address")) {
                    d.address = m.address;
                }
                if (m.acquisitionSource != null && $Object.hasOwnProperty.call(m, "acquisitionSource")) {
                    d.acquisitionSource = m.acquisitionSource;
                }
                if (m.leadStage != null && $Object.hasOwnProperty.call(m, "leadStage")) {
                    d.leadStage = m.leadStage;
                }
                if (m.lastOrder != null && $Object.hasOwnProperty.call(m, "lastOrder")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.lastOrder = typeof m.lastOrder === "number" ? $BigInt(m.lastOrder) : $util.Long.fromBits(m.lastOrder.low >>> 0, m.lastOrder.high >>> 0, false).toBigInt();
                    else if (typeof m.lastOrder === "number")
                        d.lastOrder = o.longs === $String ? $String(m.lastOrder) : m.lastOrder;
                    else
                        d.lastOrder = o.longs === $String ? $util.Long.prototype.toString.call(m.lastOrder) : o.longs === $Number ? new $util.LongBits(m.lastOrder.low >>> 0, m.lastOrder.high >>> 0).toNumber() : m.lastOrder;
                }
                if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.createdAt = typeof m.createdAt === "number" ? $BigInt(m.createdAt) : $util.Long.fromBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof m.createdAt === "number")
                        d.createdAt = o.longs === $String ? $String(m.createdAt) : m.createdAt;
                    else
                        d.createdAt = o.longs === $String ? $util.Long.prototype.toString.call(m.createdAt) : o.longs === $Number ? new $util.LongBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0).toNumber() : m.createdAt;
                }
                if (m.modifiedAt != null && $Object.hasOwnProperty.call(m, "modifiedAt")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.modifiedAt = typeof m.modifiedAt === "number" ? $BigInt(m.modifiedAt) : $util.Long.fromBits(m.modifiedAt.low >>> 0, m.modifiedAt.high >>> 0, false).toBigInt();
                    else if (typeof m.modifiedAt === "number")
                        d.modifiedAt = o.longs === $String ? $String(m.modifiedAt) : m.modifiedAt;
                    else
                        d.modifiedAt = o.longs === $String ? $util.Long.prototype.toString.call(m.modifiedAt) : o.longs === $Number ? new $util.LongBits(m.modifiedAt.low >>> 0, m.modifiedAt.high >>> 0).toNumber() : m.modifiedAt;
                }
                return d;
            };

            CustomerDataAction.prototype.toJSON = function() {
                return CustomerDataAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            CustomerDataAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.CustomerDataAction";
            };

            return CustomerDataAction;
        })();

        SyncActionValue.DeleteChatAction = (function() {

            const DeleteChatAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DeleteChatAction.prototype.messageRange = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeleteChatAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DeleteChatAction.create = function(properties) {
                return new DeleteChatAction(properties);
            };

            DeleteChatAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(m.messageRange, w.uint32(10).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DeleteChatAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.DeleteChatAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(r, r.uint32(), $undefined, q + 1, m.messageRange);
                            m._messageRange = "messageRange";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DeleteChatAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.DeleteChatAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.DeleteChatAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.DeleteChatAction();
                if (d.messageRange != null) {
                    if (!$util.isObject(d.messageRange))
                        throw $TypeError(".proto.SyncActionValue.DeleteChatAction.messageRange: object expected");
                    m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(d.messageRange, q + 1);
                }
                return m;
            };

            DeleteChatAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange")) {
                    d.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(m.messageRange, o, q + 1);
                }
                return d;
            };

            DeleteChatAction.prototype.toJSON = function() {
                return DeleteChatAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            DeleteChatAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.DeleteChatAction";
            };

            return DeleteChatAction;
        })();

        SyncActionValue.DeleteIndividualCallLogAction = (function() {

            const DeleteIndividualCallLogAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DeleteIndividualCallLogAction.prototype.peerJid = null;
            DeleteIndividualCallLogAction.prototype.isIncoming = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeleteIndividualCallLogAction.prototype, "_peerJid", {
                get: $util.oneOfGetter($oneOfFields = ["peerJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeleteIndividualCallLogAction.prototype, "_isIncoming", {
                get: $util.oneOfGetter($oneOfFields = ["isIncoming"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DeleteIndividualCallLogAction.create = function(properties) {
                return new DeleteIndividualCallLogAction(properties);
            };

            DeleteIndividualCallLogAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.peerJid != null && $Object.hasOwnProperty.call(m, "peerJid"))
                    w.uint32(10).string(m.peerJid);
                if (m.isIncoming != null && $Object.hasOwnProperty.call(m, "isIncoming"))
                    w.uint32(16).bool(m.isIncoming);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DeleteIndividualCallLogAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.DeleteIndividualCallLogAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.peerJid = r.stringVerify();
                            m._peerJid = "peerJid";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.isIncoming = r.bool();
                            m._isIncoming = "isIncoming";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DeleteIndividualCallLogAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.DeleteIndividualCallLogAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.DeleteIndividualCallLogAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.DeleteIndividualCallLogAction();
                if (d.peerJid != null) {
                    m.peerJid = $String(d.peerJid);
                }
                if (d.isIncoming != null) {
                    m.isIncoming = $Boolean(d.isIncoming);
                }
                return m;
            };

            DeleteIndividualCallLogAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.peerJid != null && $Object.hasOwnProperty.call(m, "peerJid")) {
                    d.peerJid = m.peerJid;
                }
                if (m.isIncoming != null && $Object.hasOwnProperty.call(m, "isIncoming")) {
                    d.isIncoming = m.isIncoming;
                }
                return d;
            };

            DeleteIndividualCallLogAction.prototype.toJSON = function() {
                return DeleteIndividualCallLogAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            DeleteIndividualCallLogAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.DeleteIndividualCallLogAction";
            };

            return DeleteIndividualCallLogAction;
        })();

        SyncActionValue.DeleteMessageForMeAction = (function() {

            const DeleteMessageForMeAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DeleteMessageForMeAction.prototype.deleteMedia = null;
            DeleteMessageForMeAction.prototype.messageTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeleteMessageForMeAction.prototype, "_deleteMedia", {
                get: $util.oneOfGetter($oneOfFields = ["deleteMedia"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DeleteMessageForMeAction.prototype, "_messageTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DeleteMessageForMeAction.create = function(properties) {
                return new DeleteMessageForMeAction(properties);
            };

            DeleteMessageForMeAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.deleteMedia != null && $Object.hasOwnProperty.call(m, "deleteMedia"))
                    w.uint32(8).bool(m.deleteMedia);
                if (m.messageTimestamp != null && $Object.hasOwnProperty.call(m, "messageTimestamp"))
                    w.uint32(16).int64(m.messageTimestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DeleteMessageForMeAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.DeleteMessageForMeAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.deleteMedia = r.bool();
                            m._deleteMedia = "deleteMedia";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.messageTimestamp = r.int64();
                            m._messageTimestamp = "messageTimestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DeleteMessageForMeAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.DeleteMessageForMeAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.DeleteMessageForMeAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.DeleteMessageForMeAction();
                if (d.deleteMedia != null) {
                    m.deleteMedia = $Boolean(d.deleteMedia);
                }
                if (d.messageTimestamp != null) {
                    if ($util.Long)
                        m.messageTimestamp = $util.Long.fromValue(d.messageTimestamp, false);
                    else if (typeof d.messageTimestamp === "string")
                        m.messageTimestamp = $parseInt(d.messageTimestamp, 10);
                    else if (typeof d.messageTimestamp === "number")
                        m.messageTimestamp = d.messageTimestamp;
                    else if (typeof d.messageTimestamp === "object")
                        m.messageTimestamp = new $util.LongBits(d.messageTimestamp.low >>> 0, d.messageTimestamp.high >>> 0).toNumber();
                }
                return m;
            };

            DeleteMessageForMeAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.deleteMedia != null && $Object.hasOwnProperty.call(m, "deleteMedia")) {
                    d.deleteMedia = m.deleteMedia;
                }
                if (m.messageTimestamp != null && $Object.hasOwnProperty.call(m, "messageTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.messageTimestamp = typeof m.messageTimestamp === "number" ? $BigInt(m.messageTimestamp) : $util.Long.fromBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.messageTimestamp === "number")
                        d.messageTimestamp = o.longs === $String ? $String(m.messageTimestamp) : m.messageTimestamp;
                    else
                        d.messageTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.messageTimestamp) : o.longs === $Number ? new $util.LongBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0).toNumber() : m.messageTimestamp;
                }
                return d;
            };

            DeleteMessageForMeAction.prototype.toJSON = function() {
                return DeleteMessageForMeAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            DeleteMessageForMeAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.DeleteMessageForMeAction";
            };

            return DeleteMessageForMeAction;
        })();

        SyncActionValue.DetectedOutcomesStatusAction = (function() {

            const DetectedOutcomesStatusAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            DetectedOutcomesStatusAction.prototype.isEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(DetectedOutcomesStatusAction.prototype, "_isEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            DetectedOutcomesStatusAction.create = function(properties) {
                return new DetectedOutcomesStatusAction(properties);
            };

            DetectedOutcomesStatusAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isEnabled != null && $Object.hasOwnProperty.call(m, "isEnabled"))
                    w.uint32(8).bool(m.isEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            DetectedOutcomesStatusAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.DetectedOutcomesStatusAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isEnabled = r.bool();
                            m._isEnabled = "isEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            DetectedOutcomesStatusAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.DetectedOutcomesStatusAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.DetectedOutcomesStatusAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.DetectedOutcomesStatusAction();
                if (d.isEnabled != null) {
                    m.isEnabled = $Boolean(d.isEnabled);
                }
                return m;
            };

            DetectedOutcomesStatusAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isEnabled != null && $Object.hasOwnProperty.call(m, "isEnabled")) {
                    d.isEnabled = m.isEnabled;
                }
                return d;
            };

            DetectedOutcomesStatusAction.prototype.toJSON = function() {
                return DetectedOutcomesStatusAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            DetectedOutcomesStatusAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.DetectedOutcomesStatusAction";
            };

            return DetectedOutcomesStatusAction;
        })();

        SyncActionValue.ExternalWebBetaAction = (function() {

            const ExternalWebBetaAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ExternalWebBetaAction.prototype.isOptIn = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ExternalWebBetaAction.prototype, "_isOptIn", {
                get: $util.oneOfGetter($oneOfFields = ["isOptIn"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ExternalWebBetaAction.create = function(properties) {
                return new ExternalWebBetaAction(properties);
            };

            ExternalWebBetaAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isOptIn != null && $Object.hasOwnProperty.call(m, "isOptIn"))
                    w.uint32(8).bool(m.isOptIn);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ExternalWebBetaAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ExternalWebBetaAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isOptIn = r.bool();
                            m._isOptIn = "isOptIn";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ExternalWebBetaAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ExternalWebBetaAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ExternalWebBetaAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ExternalWebBetaAction();
                if (d.isOptIn != null) {
                    m.isOptIn = $Boolean(d.isOptIn);
                }
                return m;
            };

            ExternalWebBetaAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isOptIn != null && $Object.hasOwnProperty.call(m, "isOptIn")) {
                    d.isOptIn = m.isOptIn;
                }
                return d;
            };

            ExternalWebBetaAction.prototype.toJSON = function() {
                return ExternalWebBetaAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ExternalWebBetaAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ExternalWebBetaAction";
            };

            return ExternalWebBetaAction;
        })();

        SyncActionValue.FavoritesAction = (function() {

            const FavoritesAction = function (p) {
                this.favorites = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            FavoritesAction.prototype.favorites = $util.emptyArray;

            FavoritesAction.create = function(properties) {
                return new FavoritesAction(properties);
            };

            FavoritesAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.favorites != null && m.favorites.length) {
                    for (var i = 0; i < m.favorites.length; ++i)
                        $root.proto.SyncActionValue.FavoritesAction.Favorite.encode(m.favorites[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            FavoritesAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.FavoritesAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.favorites && m.favorites.length))
                                m.favorites = [];
                            m.favorites.push($root.proto.SyncActionValue.FavoritesAction.Favorite.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            FavoritesAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.FavoritesAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.FavoritesAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.FavoritesAction();
                if (d.favorites) {
                    if (!$Array.isArray(d.favorites))
                        throw $TypeError(".proto.SyncActionValue.FavoritesAction.favorites: array expected");
                    m.favorites = $Array(d.favorites.length);
                    for (var i = 0; i < d.favorites.length; ++i) {
                        if (!$util.isObject(d.favorites[i]))
                            throw $TypeError(".proto.SyncActionValue.FavoritesAction.favorites: object expected");
                        m.favorites[i] = $root.proto.SyncActionValue.FavoritesAction.Favorite.fromObject(d.favorites[i], q + 1);
                    }
                }
                return m;
            };

            FavoritesAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.favorites = [];
                }
                if (m.favorites && m.favorites.length) {
                    d.favorites = $Array(m.favorites.length);
                    for (var j = 0; j < m.favorites.length; ++j) {
                        d.favorites[j] = $root.proto.SyncActionValue.FavoritesAction.Favorite.toObject(m.favorites[j], o, q + 1);
                    }
                }
                return d;
            };

            FavoritesAction.prototype.toJSON = function() {
                return FavoritesAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            FavoritesAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.FavoritesAction";
            };

            FavoritesAction.Favorite = (function() {

                const Favorite = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Favorite.prototype.id = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(Favorite.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Favorite.create = function(properties) {
                    return new Favorite(properties);
                };

                Favorite.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                        w.uint32(10).string(m.id);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Favorite.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.FavoritesAction.Favorite();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.id = r.stringVerify();
                                m._id = "id";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Favorite.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SyncActionValue.FavoritesAction.Favorite)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SyncActionValue.FavoritesAction.Favorite: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SyncActionValue.FavoritesAction.Favorite();
                    if (d.id != null) {
                        m.id = $String(d.id);
                    }
                    return m;
                };

                Favorite.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                        d.id = m.id;
                    }
                    return d;
                };

                Favorite.prototype.toJSON = function() {
                    return Favorite.toObject(this, $protobuf.util.toJSONOptions);
                };

                Favorite.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SyncActionValue.FavoritesAction.Favorite";
                };

                return Favorite;
            })();

            return FavoritesAction;
        })();

        SyncActionValue.InteractiveMessageAction = (function() {

            const InteractiveMessageAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            InteractiveMessageAction.prototype.type = 1;
            InteractiveMessageAction.prototype.agmId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(InteractiveMessageAction.prototype, "_agmId", {
                get: $util.oneOfGetter($oneOfFields = ["agmId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            InteractiveMessageAction.create = function(properties) {
                return new InteractiveMessageAction(properties);
            };

            InteractiveMessageAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(8).int32(m.type);
                if (m.agmId != null && $Object.hasOwnProperty.call(m, "agmId"))
                    w.uint32(18).string(m.agmId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            InteractiveMessageAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.InteractiveMessageAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.agmId = r.stringVerify();
                            m._agmId = "agmId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "type"))
                    throw $util.ProtocolError("missing required 'type'", { instance: m });
                return m;
            };

            InteractiveMessageAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.InteractiveMessageAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.InteractiveMessageAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.InteractiveMessageAction();
                switch (d.type) {
                case "DISABLE_CTA":
                case 1:
                    m.type = 1;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.agmId != null) {
                    m.agmId = $String(d.agmId);
                }
                return m;
            };

            InteractiveMessageAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.defaults) {
                    d.type = o.enums === $String ? "DISABLE_CTA" : 1;
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode[m.type] === $undefined ? m.type : $root.proto.SyncActionValue.InteractiveMessageAction.InteractiveMessageActionMode[m.type] : m.type;
                }
                if (m.agmId != null && $Object.hasOwnProperty.call(m, "agmId")) {
                    d.agmId = m.agmId;
                }
                return d;
            };

            InteractiveMessageAction.prototype.toJSON = function() {
                return InteractiveMessageAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            InteractiveMessageAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.InteractiveMessageAction";
            };

            InteractiveMessageAction.InteractiveMessageActionMode = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "DISABLE_CTA"] = 1;
                return values;
            })();

            return InteractiveMessageAction;
        })();

        SyncActionValue.KeyExpiration = (function() {

            const KeyExpiration = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            KeyExpiration.prototype.expiredKeyEpoch = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(KeyExpiration.prototype, "_expiredKeyEpoch", {
                get: $util.oneOfGetter($oneOfFields = ["expiredKeyEpoch"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            KeyExpiration.create = function(properties) {
                return new KeyExpiration(properties);
            };

            KeyExpiration.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.expiredKeyEpoch != null && $Object.hasOwnProperty.call(m, "expiredKeyEpoch"))
                    w.uint32(8).int32(m.expiredKeyEpoch);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            KeyExpiration.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.KeyExpiration();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.expiredKeyEpoch = r.int32();
                            m._expiredKeyEpoch = "expiredKeyEpoch";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            KeyExpiration.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.KeyExpiration)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.KeyExpiration: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.KeyExpiration();
                if (d.expiredKeyEpoch != null) {
                    m.expiredKeyEpoch = d.expiredKeyEpoch | 0;
                }
                return m;
            };

            KeyExpiration.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.expiredKeyEpoch != null && $Object.hasOwnProperty.call(m, "expiredKeyEpoch")) {
                    d.expiredKeyEpoch = m.expiredKeyEpoch;
                }
                return d;
            };

            KeyExpiration.prototype.toJSON = function() {
                return KeyExpiration.toObject(this, $protobuf.util.toJSONOptions);
            };

            KeyExpiration.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.KeyExpiration";
            };

            return KeyExpiration;
        })();

        SyncActionValue.LabelAssociationAction = (function() {

            const LabelAssociationAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LabelAssociationAction.prototype.labeled = null;
            LabelAssociationAction.prototype.modelMetaData = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelAssociationAction.prototype, "_labeled", {
                get: $util.oneOfGetter($oneOfFields = ["labeled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelAssociationAction.prototype, "_modelMetaData", {
                get: $util.oneOfGetter($oneOfFields = ["modelMetaData"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LabelAssociationAction.create = function(properties) {
                return new LabelAssociationAction(properties);
            };

            LabelAssociationAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.labeled != null && $Object.hasOwnProperty.call(m, "labeled"))
                    w.uint32(8).bool(m.labeled);
                if (m.modelMetaData != null && $Object.hasOwnProperty.call(m, "modelMetaData"))
                    w.uint32(18).string(m.modelMetaData);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LabelAssociationAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LabelAssociationAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.labeled = r.bool();
                            m._labeled = "labeled";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.modelMetaData = r.stringVerify();
                            m._modelMetaData = "modelMetaData";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LabelAssociationAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LabelAssociationAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LabelAssociationAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LabelAssociationAction();
                if (d.labeled != null) {
                    m.labeled = $Boolean(d.labeled);
                }
                if (d.modelMetaData != null) {
                    m.modelMetaData = $String(d.modelMetaData);
                }
                return m;
            };

            LabelAssociationAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.labeled != null && $Object.hasOwnProperty.call(m, "labeled")) {
                    d.labeled = m.labeled;
                }
                if (m.modelMetaData != null && $Object.hasOwnProperty.call(m, "modelMetaData")) {
                    d.modelMetaData = m.modelMetaData;
                }
                return d;
            };

            LabelAssociationAction.prototype.toJSON = function() {
                return LabelAssociationAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            LabelAssociationAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LabelAssociationAction";
            };

            return LabelAssociationAction;
        })();

        SyncActionValue.LabelEditAction = (function() {

            const LabelEditAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LabelEditAction.prototype.name = null;
            LabelEditAction.prototype.color = null;
            LabelEditAction.prototype.predefinedId = null;
            LabelEditAction.prototype.deleted = null;
            LabelEditAction.prototype.orderIndex = null;
            LabelEditAction.prototype.isActive = null;
            LabelEditAction.prototype.type = null;
            LabelEditAction.prototype.isImmutable = null;
            LabelEditAction.prototype.muteEndTimeMs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_color", {
                get: $util.oneOfGetter($oneOfFields = ["color"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_predefinedId", {
                get: $util.oneOfGetter($oneOfFields = ["predefinedId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_orderIndex", {
                get: $util.oneOfGetter($oneOfFields = ["orderIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_isActive", {
                get: $util.oneOfGetter($oneOfFields = ["isActive"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_isImmutable", {
                get: $util.oneOfGetter($oneOfFields = ["isImmutable"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelEditAction.prototype, "_muteEndTimeMs", {
                get: $util.oneOfGetter($oneOfFields = ["muteEndTimeMs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LabelEditAction.create = function(properties) {
                return new LabelEditAction(properties);
            };

            LabelEditAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.color != null && $Object.hasOwnProperty.call(m, "color"))
                    w.uint32(16).int32(m.color);
                if (m.predefinedId != null && $Object.hasOwnProperty.call(m, "predefinedId"))
                    w.uint32(24).int32(m.predefinedId);
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted"))
                    w.uint32(32).bool(m.deleted);
                if (m.orderIndex != null && $Object.hasOwnProperty.call(m, "orderIndex"))
                    w.uint32(40).int32(m.orderIndex);
                if (m.isActive != null && $Object.hasOwnProperty.call(m, "isActive"))
                    w.uint32(48).bool(m.isActive);
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(56).int32(m.type);
                if (m.isImmutable != null && $Object.hasOwnProperty.call(m, "isImmutable"))
                    w.uint32(64).bool(m.isImmutable);
                if (m.muteEndTimeMs != null && $Object.hasOwnProperty.call(m, "muteEndTimeMs"))
                    w.uint32(72).int64(m.muteEndTimeMs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LabelEditAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LabelEditAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.color = r.int32();
                            m._color = "color";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.predefinedId = r.int32();
                            m._predefinedId = "predefinedId";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.deleted = r.bool();
                            m._deleted = "deleted";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.orderIndex = r.int32();
                            m._orderIndex = "orderIndex";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.isActive = r.bool();
                            m._isActive = "isActive";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.isImmutable = r.bool();
                            m._isImmutable = "isImmutable";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.muteEndTimeMs = r.int64();
                            m._muteEndTimeMs = "muteEndTimeMs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LabelEditAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LabelEditAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LabelEditAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LabelEditAction();
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.color != null) {
                    m.color = d.color | 0;
                }
                if (d.predefinedId != null) {
                    m.predefinedId = d.predefinedId | 0;
                }
                if (d.deleted != null) {
                    m.deleted = $Boolean(d.deleted);
                }
                if (d.orderIndex != null) {
                    m.orderIndex = d.orderIndex | 0;
                }
                if (d.isActive != null) {
                    m.isActive = $Boolean(d.isActive);
                }
                switch (d.type) {
                case "NONE":
                case 0:
                    m.type = 0;
                    break;
                case "UNREAD":
                case 1:
                    m.type = 1;
                    break;
                case "GROUPS":
                case 2:
                    m.type = 2;
                    break;
                case "FAVORITES":
                case 3:
                    m.type = 3;
                    break;
                case "PREDEFINED":
                case 4:
                    m.type = 4;
                    break;
                case "CUSTOM":
                case 5:
                    m.type = 5;
                    break;
                case "COMMUNITY":
                case 6:
                    m.type = 6;
                    break;
                case "SERVER_ASSIGNED":
                case 7:
                    m.type = 7;
                    break;
                case "DRAFTED":
                case 8:
                    m.type = 8;
                    break;
                case "AI_HANDOFF":
                case 9:
                    m.type = 9;
                    break;
                case "CHANNELS":
                case 10:
                    m.type = 10;
                    break;
                case "AI_RESPONDING":
                case 11:
                    m.type = 11;
                    break;
                case "ARCHIVED":
                case 12:
                    m.type = 12;
                    break;
                case "LOCKED":
                case 13:
                    m.type = 13;
                    break;
                case "INVITES":
                case 14:
                    m.type = 14;
                    break;
                case "THIRD_PARTY":
                case 15:
                    m.type = 15;
                    break;
                case "LEAD":
                case 16:
                    m.type = 16;
                    break;
                case "MENTIONS_AND_REPLIES":
                case 17:
                    m.type = 17;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.isImmutable != null) {
                    m.isImmutable = $Boolean(d.isImmutable);
                }
                if (d.muteEndTimeMs != null) {
                    if ($util.Long)
                        m.muteEndTimeMs = $util.Long.fromValue(d.muteEndTimeMs, false);
                    else if (typeof d.muteEndTimeMs === "string")
                        m.muteEndTimeMs = $parseInt(d.muteEndTimeMs, 10);
                    else if (typeof d.muteEndTimeMs === "number")
                        m.muteEndTimeMs = d.muteEndTimeMs;
                    else if (typeof d.muteEndTimeMs === "object")
                        m.muteEndTimeMs = new $util.LongBits(d.muteEndTimeMs.low >>> 0, d.muteEndTimeMs.high >>> 0).toNumber();
                }
                return m;
            };

            LabelEditAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.color != null && $Object.hasOwnProperty.call(m, "color")) {
                    d.color = m.color;
                }
                if (m.predefinedId != null && $Object.hasOwnProperty.call(m, "predefinedId")) {
                    d.predefinedId = m.predefinedId;
                }
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted")) {
                    d.deleted = m.deleted;
                }
                if (m.orderIndex != null && $Object.hasOwnProperty.call(m, "orderIndex")) {
                    d.orderIndex = m.orderIndex;
                }
                if (m.isActive != null && $Object.hasOwnProperty.call(m, "isActive")) {
                    d.isActive = m.isActive;
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.SyncActionValue.LabelEditAction.ListType[m.type] === $undefined ? m.type : $root.proto.SyncActionValue.LabelEditAction.ListType[m.type] : m.type;
                }
                if (m.isImmutable != null && $Object.hasOwnProperty.call(m, "isImmutable")) {
                    d.isImmutable = m.isImmutable;
                }
                if (m.muteEndTimeMs != null && $Object.hasOwnProperty.call(m, "muteEndTimeMs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.muteEndTimeMs = typeof m.muteEndTimeMs === "number" ? $BigInt(m.muteEndTimeMs) : $util.Long.fromBits(m.muteEndTimeMs.low >>> 0, m.muteEndTimeMs.high >>> 0, false).toBigInt();
                    else if (typeof m.muteEndTimeMs === "number")
                        d.muteEndTimeMs = o.longs === $String ? $String(m.muteEndTimeMs) : m.muteEndTimeMs;
                    else
                        d.muteEndTimeMs = o.longs === $String ? $util.Long.prototype.toString.call(m.muteEndTimeMs) : o.longs === $Number ? new $util.LongBits(m.muteEndTimeMs.low >>> 0, m.muteEndTimeMs.high >>> 0).toNumber() : m.muteEndTimeMs;
                }
                return d;
            };

            LabelEditAction.prototype.toJSON = function() {
                return LabelEditAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            LabelEditAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LabelEditAction";
            };

            LabelEditAction.ListType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "NONE"] = 0;
                values[valuesById[1] = "UNREAD"] = 1;
                values[valuesById[2] = "GROUPS"] = 2;
                values[valuesById[3] = "FAVORITES"] = 3;
                values[valuesById[4] = "PREDEFINED"] = 4;
                values[valuesById[5] = "CUSTOM"] = 5;
                values[valuesById[6] = "COMMUNITY"] = 6;
                values[valuesById[7] = "SERVER_ASSIGNED"] = 7;
                values[valuesById[8] = "DRAFTED"] = 8;
                values[valuesById[9] = "AI_HANDOFF"] = 9;
                values[valuesById[10] = "CHANNELS"] = 10;
                values[valuesById[11] = "AI_RESPONDING"] = 11;
                values[valuesById[12] = "ARCHIVED"] = 12;
                values[valuesById[13] = "LOCKED"] = 13;
                values[valuesById[14] = "INVITES"] = 14;
                values[valuesById[15] = "THIRD_PARTY"] = 15;
                values[valuesById[16] = "LEAD"] = 16;
                values[valuesById[17] = "MENTIONS_AND_REPLIES"] = 17;
                return values;
            })();

            return LabelEditAction;
        })();

        SyncActionValue.LabelReorderingAction = (function() {

            const LabelReorderingAction = function (p) {
                this.sortedLabelIds = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LabelReorderingAction.prototype.sortedLabelIds = $util.emptyArray;

            LabelReorderingAction.create = function(properties) {
                return new LabelReorderingAction(properties);
            };

            LabelReorderingAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.sortedLabelIds != null && m.sortedLabelIds.length) {
                    w.uint32(10).int32s(m.sortedLabelIds);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LabelReorderingAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LabelReorderingAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u === 2) {
                                if (!(m.sortedLabelIds && m.sortedLabelIds.length))
                                    m.sortedLabelIds = [];
                                r.int32s(m.sortedLabelIds);
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(m.sortedLabelIds && m.sortedLabelIds.length))
                                m.sortedLabelIds = [];
                            m.sortedLabelIds.push(r.int32());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LabelReorderingAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LabelReorderingAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LabelReorderingAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LabelReorderingAction();
                if (d.sortedLabelIds) {
                    if (!$Array.isArray(d.sortedLabelIds))
                        throw $TypeError(".proto.SyncActionValue.LabelReorderingAction.sortedLabelIds: array expected");
                    m.sortedLabelIds = $Array(d.sortedLabelIds.length);
                    for (var i = 0; i < d.sortedLabelIds.length; ++i) {
                        m.sortedLabelIds[i] = d.sortedLabelIds[i] | 0;
                    }
                }
                return m;
            };

            LabelReorderingAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.sortedLabelIds = [];
                }
                if (m.sortedLabelIds && m.sortedLabelIds.length) {
                    d.sortedLabelIds = $Array(m.sortedLabelIds.length);
                    for (var j = 0; j < m.sortedLabelIds.length; ++j) {
                        d.sortedLabelIds[j] = m.sortedLabelIds[j];
                    }
                }
                return d;
            };

            LabelReorderingAction.prototype.toJSON = function() {
                return LabelReorderingAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            LabelReorderingAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LabelReorderingAction";
            };

            return LabelReorderingAction;
        })();

        SyncActionValue.LabelSublistAction = (function() {

            const LabelSublistAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LabelSublistAction.prototype.subListId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LabelSublistAction.prototype, "_subListId", {
                get: $util.oneOfGetter($oneOfFields = ["subListId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LabelSublistAction.create = function(properties) {
                return new LabelSublistAction(properties);
            };

            LabelSublistAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.subListId != null && $Object.hasOwnProperty.call(m, "subListId"))
                    w.uint32(8).int32(m.subListId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LabelSublistAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LabelSublistAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.subListId = r.int32();
                            m._subListId = "subListId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LabelSublistAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LabelSublistAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LabelSublistAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LabelSublistAction();
                if (d.subListId != null) {
                    m.subListId = d.subListId | 0;
                }
                return m;
            };

            LabelSublistAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.subListId != null && $Object.hasOwnProperty.call(m, "subListId")) {
                    d.subListId = m.subListId;
                }
                return d;
            };

            LabelSublistAction.prototype.toJSON = function() {
                return LabelSublistAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            LabelSublistAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LabelSublistAction";
            };

            return LabelSublistAction;
        })();

        SyncActionValue.LidContactAction = (function() {

            const LidContactAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LidContactAction.prototype.fullName = null;
            LidContactAction.prototype.firstName = null;
            LidContactAction.prototype.username = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LidContactAction.prototype, "_fullName", {
                get: $util.oneOfGetter($oneOfFields = ["fullName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LidContactAction.prototype, "_firstName", {
                get: $util.oneOfGetter($oneOfFields = ["firstName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LidContactAction.prototype, "_username", {
                get: $util.oneOfGetter($oneOfFields = ["username"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LidContactAction.create = function(properties) {
                return new LidContactAction(properties);
            };

            LidContactAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName"))
                    w.uint32(10).string(m.fullName);
                if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName"))
                    w.uint32(18).string(m.firstName);
                if (m.username != null && $Object.hasOwnProperty.call(m, "username"))
                    w.uint32(26).string(m.username);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LidContactAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LidContactAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.fullName = r.stringVerify();
                            m._fullName = "fullName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.firstName = r.stringVerify();
                            m._firstName = "firstName";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.username = r.stringVerify();
                            m._username = "username";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LidContactAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LidContactAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LidContactAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LidContactAction();
                if (d.fullName != null) {
                    m.fullName = $String(d.fullName);
                }
                if (d.firstName != null) {
                    m.firstName = $String(d.firstName);
                }
                if (d.username != null) {
                    m.username = $String(d.username);
                }
                return m;
            };

            LidContactAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName")) {
                    d.fullName = m.fullName;
                }
                if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName")) {
                    d.firstName = m.firstName;
                }
                if (m.username != null && $Object.hasOwnProperty.call(m, "username")) {
                    d.username = m.username;
                }
                return d;
            };

            LidContactAction.prototype.toJSON = function() {
                return LidContactAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            LidContactAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LidContactAction";
            };

            return LidContactAction;
        })();

        SyncActionValue.LocaleSetting = (function() {

            const LocaleSetting = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LocaleSetting.prototype.locale = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LocaleSetting.prototype, "_locale", {
                get: $util.oneOfGetter($oneOfFields = ["locale"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LocaleSetting.create = function(properties) {
                return new LocaleSetting(properties);
            };

            LocaleSetting.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.locale != null && $Object.hasOwnProperty.call(m, "locale"))
                    w.uint32(10).string(m.locale);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LocaleSetting.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LocaleSetting();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.locale = r.stringVerify();
                            m._locale = "locale";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LocaleSetting.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LocaleSetting)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LocaleSetting: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LocaleSetting();
                if (d.locale != null) {
                    m.locale = $String(d.locale);
                }
                return m;
            };

            LocaleSetting.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.locale != null && $Object.hasOwnProperty.call(m, "locale")) {
                    d.locale = m.locale;
                }
                return d;
            };

            LocaleSetting.prototype.toJSON = function() {
                return LocaleSetting.toObject(this, $protobuf.util.toJSONOptions);
            };

            LocaleSetting.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LocaleSetting";
            };

            return LocaleSetting;
        })();

        SyncActionValue.LockChatAction = (function() {

            const LockChatAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            LockChatAction.prototype.locked = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(LockChatAction.prototype, "_locked", {
                get: $util.oneOfGetter($oneOfFields = ["locked"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            LockChatAction.create = function(properties) {
                return new LockChatAction(properties);
            };

            LockChatAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.locked != null && $Object.hasOwnProperty.call(m, "locked"))
                    w.uint32(8).bool(m.locked);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            LockChatAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.LockChatAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.locked = r.bool();
                            m._locked = "locked";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            LockChatAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.LockChatAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.LockChatAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.LockChatAction();
                if (d.locked != null) {
                    m.locked = $Boolean(d.locked);
                }
                return m;
            };

            LockChatAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.locked != null && $Object.hasOwnProperty.call(m, "locked")) {
                    d.locked = m.locked;
                }
                return d;
            };

            LockChatAction.prototype.toJSON = function() {
                return LockChatAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            LockChatAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.LockChatAction";
            };

            return LockChatAction;
        })();

        SyncActionValue.MaibaAIFeaturesControlAction = (function() {

            const MaibaAIFeaturesControlAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MaibaAIFeaturesControlAction.prototype.aiFeatureStatus = null;
            MaibaAIFeaturesControlAction.prototype.aiReplyMode = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MaibaAIFeaturesControlAction.prototype, "_aiFeatureStatus", {
                get: $util.oneOfGetter($oneOfFields = ["aiFeatureStatus"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MaibaAIFeaturesControlAction.prototype, "_aiReplyMode", {
                get: $util.oneOfGetter($oneOfFields = ["aiReplyMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MaibaAIFeaturesControlAction.create = function(properties) {
                return new MaibaAIFeaturesControlAction(properties);
            };

            MaibaAIFeaturesControlAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.aiFeatureStatus != null && $Object.hasOwnProperty.call(m, "aiFeatureStatus"))
                    w.uint32(8).int32(m.aiFeatureStatus);
                if (m.aiReplyMode != null && $Object.hasOwnProperty.call(m, "aiReplyMode"))
                    w.uint32(16).int32(m.aiReplyMode);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MaibaAIFeaturesControlAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MaibaAIFeaturesControlAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.aiFeatureStatus = r.int32();
                            m._aiFeatureStatus = "aiFeatureStatus";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.aiReplyMode = r.int32();
                            m._aiReplyMode = "aiReplyMode";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MaibaAIFeaturesControlAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MaibaAIFeaturesControlAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MaibaAIFeaturesControlAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MaibaAIFeaturesControlAction();
                switch (d.aiFeatureStatus) {
                case "ENABLED":
                case 0:
                    m.aiFeatureStatus = 0;
                    break;
                case "ENABLED_HAS_LEARNING":
                case 1:
                    m.aiFeatureStatus = 1;
                    break;
                case "DISABLED":
                case 2:
                    m.aiFeatureStatus = 2;
                    break;
                default:
                    if (typeof d.aiFeatureStatus === "number" && (d.aiFeatureStatus | 0) === d.aiFeatureStatus)
                        m.aiFeatureStatus = d.aiFeatureStatus;
                }
                switch (d.aiReplyMode) {
                case "MUTED":
                case 0:
                    m.aiReplyMode = 0;
                    break;
                case "AI_AGENT":
                case 1:
                    m.aiReplyMode = 1;
                    break;
                case "SUGGESTIONS":
                case 2:
                    m.aiReplyMode = 2;
                    break;
                default:
                    if (typeof d.aiReplyMode === "number" && (d.aiReplyMode | 0) === d.aiReplyMode)
                        m.aiReplyMode = d.aiReplyMode;
                }
                return m;
            };

            MaibaAIFeaturesControlAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.aiFeatureStatus != null && $Object.hasOwnProperty.call(m, "aiFeatureStatus")) {
                    d.aiFeatureStatus = o.enums === $String ? $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus[m.aiFeatureStatus] === $undefined ? m.aiFeatureStatus : $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIFeatureStatus[m.aiFeatureStatus] : m.aiFeatureStatus;
                }
                if (m.aiReplyMode != null && $Object.hasOwnProperty.call(m, "aiReplyMode")) {
                    d.aiReplyMode = o.enums === $String ? $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode[m.aiReplyMode] === $undefined ? m.aiReplyMode : $root.proto.SyncActionValue.MaibaAIFeaturesControlAction.MaibaAIReplyMode[m.aiReplyMode] : m.aiReplyMode;
                }
                return d;
            };

            MaibaAIFeaturesControlAction.prototype.toJSON = function() {
                return MaibaAIFeaturesControlAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MaibaAIFeaturesControlAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MaibaAIFeaturesControlAction";
            };

            MaibaAIFeaturesControlAction.MaibaAIFeatureStatus = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "ENABLED"] = 0;
                values[valuesById[1] = "ENABLED_HAS_LEARNING"] = 1;
                values[valuesById[2] = "DISABLED"] = 2;
                return values;
            })();

            MaibaAIFeaturesControlAction.MaibaAIReplyMode = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "MUTED"] = 0;
                values[valuesById[1] = "AI_AGENT"] = 1;
                values[valuesById[2] = "SUGGESTIONS"] = 2;
                return values;
            })();

            return MaibaAIFeaturesControlAction;
        })();

        SyncActionValue.MarkChatAsReadAction = (function() {

            const MarkChatAsReadAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MarkChatAsReadAction.prototype.read = null;
            MarkChatAsReadAction.prototype.messageRange = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarkChatAsReadAction.prototype, "_read", {
                get: $util.oneOfGetter($oneOfFields = ["read"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarkChatAsReadAction.prototype, "_messageRange", {
                get: $util.oneOfGetter($oneOfFields = ["messageRange"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MarkChatAsReadAction.create = function(properties) {
                return new MarkChatAsReadAction(properties);
            };

            MarkChatAsReadAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.read != null && $Object.hasOwnProperty.call(m, "read"))
                    w.uint32(8).bool(m.read);
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange"))
                    $root.proto.SyncActionValue.SyncActionMessageRange.encode(m.messageRange, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MarkChatAsReadAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MarkChatAsReadAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.read = r.bool();
                            m._read = "read";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.decode(r, r.uint32(), $undefined, q + 1, m.messageRange);
                            m._messageRange = "messageRange";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MarkChatAsReadAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MarkChatAsReadAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MarkChatAsReadAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MarkChatAsReadAction();
                if (d.read != null) {
                    m.read = $Boolean(d.read);
                }
                if (d.messageRange != null) {
                    if (!$util.isObject(d.messageRange))
                        throw $TypeError(".proto.SyncActionValue.MarkChatAsReadAction.messageRange: object expected");
                    m.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.fromObject(d.messageRange, q + 1);
                }
                return m;
            };

            MarkChatAsReadAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.read != null && $Object.hasOwnProperty.call(m, "read")) {
                    d.read = m.read;
                }
                if (m.messageRange != null && $Object.hasOwnProperty.call(m, "messageRange")) {
                    d.messageRange = $root.proto.SyncActionValue.SyncActionMessageRange.toObject(m.messageRange, o, q + 1);
                }
                return d;
            };

            MarkChatAsReadAction.prototype.toJSON = function() {
                return MarkChatAsReadAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MarkChatAsReadAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MarkChatAsReadAction";
            };

            return MarkChatAsReadAction;
        })();

        SyncActionValue.MarketingMessageAction = (function() {

            const MarketingMessageAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MarketingMessageAction.prototype.name = null;
            MarketingMessageAction.prototype.message = null;
            MarketingMessageAction.prototype.type = null;
            MarketingMessageAction.prototype.createdAt = null;
            MarketingMessageAction.prototype.lastSentAt = null;
            MarketingMessageAction.prototype.isDeleted = null;
            MarketingMessageAction.prototype.mediaId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_createdAt", {
                get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_lastSentAt", {
                get: $util.oneOfGetter($oneOfFields = ["lastSentAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_isDeleted", {
                get: $util.oneOfGetter($oneOfFields = ["isDeleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageAction.prototype, "_mediaId", {
                get: $util.oneOfGetter($oneOfFields = ["mediaId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MarketingMessageAction.create = function(properties) {
                return new MarketingMessageAction(properties);
            };

            MarketingMessageAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    w.uint32(18).string(m.message);
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(24).int32(m.type);
                if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt"))
                    w.uint32(32).int64(m.createdAt);
                if (m.lastSentAt != null && $Object.hasOwnProperty.call(m, "lastSentAt"))
                    w.uint32(40).int64(m.lastSentAt);
                if (m.isDeleted != null && $Object.hasOwnProperty.call(m, "isDeleted"))
                    w.uint32(48).bool(m.isDeleted);
                if (m.mediaId != null && $Object.hasOwnProperty.call(m, "mediaId"))
                    w.uint32(58).string(m.mediaId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MarketingMessageAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MarketingMessageAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.message = r.stringVerify();
                            m._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.createdAt = r.int64();
                            m._createdAt = "createdAt";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.lastSentAt = r.int64();
                            m._lastSentAt = "lastSentAt";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.isDeleted = r.bool();
                            m._isDeleted = "isDeleted";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.mediaId = r.stringVerify();
                            m._mediaId = "mediaId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MarketingMessageAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MarketingMessageAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MarketingMessageAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MarketingMessageAction();
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                if (d.message != null) {
                    m.message = $String(d.message);
                }
                switch (d.type) {
                case "PERSONALIZED":
                case 0:
                    m.type = 0;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.createdAt != null) {
                    if ($util.Long)
                        m.createdAt = $util.Long.fromValue(d.createdAt, false);
                    else if (typeof d.createdAt === "string")
                        m.createdAt = $parseInt(d.createdAt, 10);
                    else if (typeof d.createdAt === "number")
                        m.createdAt = d.createdAt;
                    else if (typeof d.createdAt === "object")
                        m.createdAt = new $util.LongBits(d.createdAt.low >>> 0, d.createdAt.high >>> 0).toNumber();
                }
                if (d.lastSentAt != null) {
                    if ($util.Long)
                        m.lastSentAt = $util.Long.fromValue(d.lastSentAt, false);
                    else if (typeof d.lastSentAt === "string")
                        m.lastSentAt = $parseInt(d.lastSentAt, 10);
                    else if (typeof d.lastSentAt === "number")
                        m.lastSentAt = d.lastSentAt;
                    else if (typeof d.lastSentAt === "object")
                        m.lastSentAt = new $util.LongBits(d.lastSentAt.low >>> 0, d.lastSentAt.high >>> 0).toNumber();
                }
                if (d.isDeleted != null) {
                    m.isDeleted = $Boolean(d.isDeleted);
                }
                if (d.mediaId != null) {
                    m.mediaId = $String(d.mediaId);
                }
                return m;
            };

            MarketingMessageAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = m.message;
                }
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType[m.type] === $undefined ? m.type : $root.proto.SyncActionValue.MarketingMessageAction.MarketingMessagePrototypeType[m.type] : m.type;
                }
                if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.createdAt = typeof m.createdAt === "number" ? $BigInt(m.createdAt) : $util.Long.fromBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof m.createdAt === "number")
                        d.createdAt = o.longs === $String ? $String(m.createdAt) : m.createdAt;
                    else
                        d.createdAt = o.longs === $String ? $util.Long.prototype.toString.call(m.createdAt) : o.longs === $Number ? new $util.LongBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0).toNumber() : m.createdAt;
                }
                if (m.lastSentAt != null && $Object.hasOwnProperty.call(m, "lastSentAt")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.lastSentAt = typeof m.lastSentAt === "number" ? $BigInt(m.lastSentAt) : $util.Long.fromBits(m.lastSentAt.low >>> 0, m.lastSentAt.high >>> 0, false).toBigInt();
                    else if (typeof m.lastSentAt === "number")
                        d.lastSentAt = o.longs === $String ? $String(m.lastSentAt) : m.lastSentAt;
                    else
                        d.lastSentAt = o.longs === $String ? $util.Long.prototype.toString.call(m.lastSentAt) : o.longs === $Number ? new $util.LongBits(m.lastSentAt.low >>> 0, m.lastSentAt.high >>> 0).toNumber() : m.lastSentAt;
                }
                if (m.isDeleted != null && $Object.hasOwnProperty.call(m, "isDeleted")) {
                    d.isDeleted = m.isDeleted;
                }
                if (m.mediaId != null && $Object.hasOwnProperty.call(m, "mediaId")) {
                    d.mediaId = m.mediaId;
                }
                return d;
            };

            MarketingMessageAction.prototype.toJSON = function() {
                return MarketingMessageAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MarketingMessageAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MarketingMessageAction";
            };

            MarketingMessageAction.MarketingMessagePrototypeType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "PERSONALIZED"] = 0;
                return values;
            })();

            return MarketingMessageAction;
        })();

        SyncActionValue.MarketingMessageBroadcastAction = (function() {

            const MarketingMessageBroadcastAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MarketingMessageBroadcastAction.prototype.repliedCount = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MarketingMessageBroadcastAction.prototype, "_repliedCount", {
                get: $util.oneOfGetter($oneOfFields = ["repliedCount"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MarketingMessageBroadcastAction.create = function(properties) {
                return new MarketingMessageBroadcastAction(properties);
            };

            MarketingMessageBroadcastAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.repliedCount != null && $Object.hasOwnProperty.call(m, "repliedCount"))
                    w.uint32(8).int32(m.repliedCount);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MarketingMessageBroadcastAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MarketingMessageBroadcastAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.repliedCount = r.int32();
                            m._repliedCount = "repliedCount";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MarketingMessageBroadcastAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MarketingMessageBroadcastAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MarketingMessageBroadcastAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MarketingMessageBroadcastAction();
                if (d.repliedCount != null) {
                    m.repliedCount = d.repliedCount | 0;
                }
                return m;
            };

            MarketingMessageBroadcastAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.repliedCount != null && $Object.hasOwnProperty.call(m, "repliedCount")) {
                    d.repliedCount = m.repliedCount;
                }
                return d;
            };

            MarketingMessageBroadcastAction.prototype.toJSON = function() {
                return MarketingMessageBroadcastAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MarketingMessageBroadcastAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MarketingMessageBroadcastAction";
            };

            return MarketingMessageBroadcastAction;
        })();

        SyncActionValue.MerchantPaymentPartnerAction = (function() {

            const MerchantPaymentPartnerAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MerchantPaymentPartnerAction.prototype.status = 0;
            MerchantPaymentPartnerAction.prototype.country = "";
            MerchantPaymentPartnerAction.prototype.gatewayName = null;
            MerchantPaymentPartnerAction.prototype.credentialId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MerchantPaymentPartnerAction.prototype, "_gatewayName", {
                get: $util.oneOfGetter($oneOfFields = ["gatewayName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MerchantPaymentPartnerAction.prototype, "_credentialId", {
                get: $util.oneOfGetter($oneOfFields = ["credentialId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MerchantPaymentPartnerAction.create = function(properties) {
                return new MerchantPaymentPartnerAction(properties);
            };

            MerchantPaymentPartnerAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(8).int32(m.status);
                w.uint32(18).string(m.country);
                if (m.gatewayName != null && $Object.hasOwnProperty.call(m, "gatewayName"))
                    w.uint32(26).string(m.gatewayName);
                if (m.credentialId != null && $Object.hasOwnProperty.call(m, "credentialId"))
                    w.uint32(34).string(m.credentialId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MerchantPaymentPartnerAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MerchantPaymentPartnerAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.status = r.int32();
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.country = r.stringVerify();
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.gatewayName = r.stringVerify();
                            m._gatewayName = "gatewayName";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.credentialId = r.stringVerify();
                            m._credentialId = "credentialId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "status"))
                    throw $util.ProtocolError("missing required 'status'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "country"))
                    throw $util.ProtocolError("missing required 'country'", { instance: m });
                return m;
            };

            MerchantPaymentPartnerAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MerchantPaymentPartnerAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MerchantPaymentPartnerAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MerchantPaymentPartnerAction();
                switch (d.status) {
                case "ACTIVE":
                case 0:
                    m.status = 0;
                    break;
                case "INACTIVE":
                case 1:
                    m.status = 1;
                    break;
                default:
                    if (typeof d.status === "number" && (d.status | 0) === d.status)
                        m.status = d.status;
                }
                if (d.country != null) {
                    m.country = $String(d.country);
                }
                if (d.gatewayName != null) {
                    m.gatewayName = $String(d.gatewayName);
                }
                if (d.credentialId != null) {
                    m.credentialId = $String(d.credentialId);
                }
                return m;
            };

            MerchantPaymentPartnerAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.defaults) {
                    d.status = o.enums === $String ? "ACTIVE" : 0;
                    d.country = "";
                }
                if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                    d.status = o.enums === $String ? $root.proto.SyncActionValue.MerchantPaymentPartnerAction.Status[m.status] === $undefined ? m.status : $root.proto.SyncActionValue.MerchantPaymentPartnerAction.Status[m.status] : m.status;
                }
                if (m.country != null && $Object.hasOwnProperty.call(m, "country")) {
                    d.country = m.country;
                }
                if (m.gatewayName != null && $Object.hasOwnProperty.call(m, "gatewayName")) {
                    d.gatewayName = m.gatewayName;
                }
                if (m.credentialId != null && $Object.hasOwnProperty.call(m, "credentialId")) {
                    d.credentialId = m.credentialId;
                }
                return d;
            };

            MerchantPaymentPartnerAction.prototype.toJSON = function() {
                return MerchantPaymentPartnerAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MerchantPaymentPartnerAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MerchantPaymentPartnerAction";
            };

            MerchantPaymentPartnerAction.Status = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "ACTIVE"] = 0;
                values[valuesById[1] = "INACTIVE"] = 1;
                return values;
            })();

            return MerchantPaymentPartnerAction;
        })();

        SyncActionValue.MusicUserIdAction = (function() {

            const MusicUserIdAction = function (p) {
                this.musicUserIdMap = {};
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MusicUserIdAction.prototype.musicUserId = null;
            MusicUserIdAction.prototype.musicUserIdMap = $util.emptyObject;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MusicUserIdAction.prototype, "_musicUserId", {
                get: $util.oneOfGetter($oneOfFields = ["musicUserId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MusicUserIdAction.create = function(properties) {
                return new MusicUserIdAction(properties);
            };

            MusicUserIdAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.musicUserId != null && $Object.hasOwnProperty.call(m, "musicUserId"))
                    w.uint32(10).string(m.musicUserId);
                if (m.musicUserIdMap != null && $Object.hasOwnProperty.call(m, "musicUserIdMap")) {
                    for (var ks = $Object.keys(m.musicUserIdMap), i = 0; i < ks.length; ++i) {
                        w.uint32(18).fork().uint32(10).string(ks[i]).uint32(18).string(m.musicUserIdMap[ks[i]]).ldelim();
                    }
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MusicUserIdAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MusicUserIdAction(), k, v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.musicUserId = r.stringVerify();
                            m._musicUserId = "musicUserId";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (m.musicUserIdMap === $util.emptyObject)
                                m.musicUserIdMap = {};
                            var c2 = r.uint32() + r.pos;
                            k = "";
                            v = "";
                            while (r.pos < c2) {
                                var t2 = r.tag();
                                u = t2 & 7;
                                switch (t2 >>>= 3) {
                                case 1:
                                    if (u !== 2)
                                        break;
                                    k = r.stringVerify();
                                    continue;
                                case 2:
                                    if (u !== 2)
                                        break;
                                    v = r.stringVerify();
                                    continue;
                                }
                                r.skipType(u, q, t2);
                            }
                            if (k === "__proto__")
                                $util.makeProp(m.musicUserIdMap, k);
                            m.musicUserIdMap[k] = v;
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MusicUserIdAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MusicUserIdAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MusicUserIdAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MusicUserIdAction();
                if (d.musicUserId != null) {
                    m.musicUserId = $String(d.musicUserId);
                }
                if (d.musicUserIdMap) {
                    if (!$util.isObject(d.musicUserIdMap))
                        throw $TypeError(".proto.SyncActionValue.MusicUserIdAction.musicUserIdMap: object expected");
                    m.musicUserIdMap = {};
                    for (var ks = $Object.keys(d.musicUserIdMap), i = 0; i < ks.length; ++i) {
                        if (ks[i] === "__proto__")
                            $util.makeProp(m.musicUserIdMap, ks[i]);
                        m.musicUserIdMap[ks[i]] = $String(d.musicUserIdMap[ks[i]]);
                    }
                }
                return m;
            };

            MusicUserIdAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.objects || o.defaults) {
                    d.musicUserIdMap = {};
                }
                if (m.musicUserId != null && $Object.hasOwnProperty.call(m, "musicUserId")) {
                    d.musicUserId = m.musicUserId;
                }
                var ks2;
                if (m.musicUserIdMap && (ks2 = $Object.keys(m.musicUserIdMap)).length) {
                    d.musicUserIdMap = {};
                    for (var j = 0; j < ks2.length; ++j) {
                        if (ks2[j] === "__proto__")
                            $util.makeProp(d.musicUserIdMap, ks2[j]);
                        d.musicUserIdMap[ks2[j]] = m.musicUserIdMap[ks2[j]];
                    }
                }
                return d;
            };

            MusicUserIdAction.prototype.toJSON = function() {
                return MusicUserIdAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MusicUserIdAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MusicUserIdAction";
            };

            return MusicUserIdAction;
        })();

        SyncActionValue.MuteAction = (function() {

            const MuteAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            MuteAction.prototype.muted = null;
            MuteAction.prototype.muteEndTimestamp = null;
            MuteAction.prototype.autoMuted = null;
            MuteAction.prototype.muteEveryoneMentionEndTimestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MuteAction.prototype, "_muted", {
                get: $util.oneOfGetter($oneOfFields = ["muted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MuteAction.prototype, "_muteEndTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["muteEndTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MuteAction.prototype, "_autoMuted", {
                get: $util.oneOfGetter($oneOfFields = ["autoMuted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(MuteAction.prototype, "_muteEveryoneMentionEndTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["muteEveryoneMentionEndTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            MuteAction.create = function(properties) {
                return new MuteAction(properties);
            };

            MuteAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.muted != null && $Object.hasOwnProperty.call(m, "muted"))
                    w.uint32(8).bool(m.muted);
                if (m.muteEndTimestamp != null && $Object.hasOwnProperty.call(m, "muteEndTimestamp"))
                    w.uint32(16).int64(m.muteEndTimestamp);
                if (m.autoMuted != null && $Object.hasOwnProperty.call(m, "autoMuted"))
                    w.uint32(24).bool(m.autoMuted);
                if (m.muteEveryoneMentionEndTimestamp != null && $Object.hasOwnProperty.call(m, "muteEveryoneMentionEndTimestamp"))
                    w.uint32(32).int64(m.muteEveryoneMentionEndTimestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            MuteAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.MuteAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.muted = r.bool();
                            m._muted = "muted";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.muteEndTimestamp = r.int64();
                            m._muteEndTimestamp = "muteEndTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.autoMuted = r.bool();
                            m._autoMuted = "autoMuted";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.muteEveryoneMentionEndTimestamp = r.int64();
                            m._muteEveryoneMentionEndTimestamp = "muteEveryoneMentionEndTimestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            MuteAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.MuteAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.MuteAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.MuteAction();
                if (d.muted != null) {
                    m.muted = $Boolean(d.muted);
                }
                if (d.muteEndTimestamp != null) {
                    if ($util.Long)
                        m.muteEndTimestamp = $util.Long.fromValue(d.muteEndTimestamp, false);
                    else if (typeof d.muteEndTimestamp === "string")
                        m.muteEndTimestamp = $parseInt(d.muteEndTimestamp, 10);
                    else if (typeof d.muteEndTimestamp === "number")
                        m.muteEndTimestamp = d.muteEndTimestamp;
                    else if (typeof d.muteEndTimestamp === "object")
                        m.muteEndTimestamp = new $util.LongBits(d.muteEndTimestamp.low >>> 0, d.muteEndTimestamp.high >>> 0).toNumber();
                }
                if (d.autoMuted != null) {
                    m.autoMuted = $Boolean(d.autoMuted);
                }
                if (d.muteEveryoneMentionEndTimestamp != null) {
                    if ($util.Long)
                        m.muteEveryoneMentionEndTimestamp = $util.Long.fromValue(d.muteEveryoneMentionEndTimestamp, false);
                    else if (typeof d.muteEveryoneMentionEndTimestamp === "string")
                        m.muteEveryoneMentionEndTimestamp = $parseInt(d.muteEveryoneMentionEndTimestamp, 10);
                    else if (typeof d.muteEveryoneMentionEndTimestamp === "number")
                        m.muteEveryoneMentionEndTimestamp = d.muteEveryoneMentionEndTimestamp;
                    else if (typeof d.muteEveryoneMentionEndTimestamp === "object")
                        m.muteEveryoneMentionEndTimestamp = new $util.LongBits(d.muteEveryoneMentionEndTimestamp.low >>> 0, d.muteEveryoneMentionEndTimestamp.high >>> 0).toNumber();
                }
                return m;
            };

            MuteAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.muted != null && $Object.hasOwnProperty.call(m, "muted")) {
                    d.muted = m.muted;
                }
                if (m.muteEndTimestamp != null && $Object.hasOwnProperty.call(m, "muteEndTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.muteEndTimestamp = typeof m.muteEndTimestamp === "number" ? $BigInt(m.muteEndTimestamp) : $util.Long.fromBits(m.muteEndTimestamp.low >>> 0, m.muteEndTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.muteEndTimestamp === "number")
                        d.muteEndTimestamp = o.longs === $String ? $String(m.muteEndTimestamp) : m.muteEndTimestamp;
                    else
                        d.muteEndTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.muteEndTimestamp) : o.longs === $Number ? new $util.LongBits(m.muteEndTimestamp.low >>> 0, m.muteEndTimestamp.high >>> 0).toNumber() : m.muteEndTimestamp;
                }
                if (m.autoMuted != null && $Object.hasOwnProperty.call(m, "autoMuted")) {
                    d.autoMuted = m.autoMuted;
                }
                if (m.muteEveryoneMentionEndTimestamp != null && $Object.hasOwnProperty.call(m, "muteEveryoneMentionEndTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.muteEveryoneMentionEndTimestamp = typeof m.muteEveryoneMentionEndTimestamp === "number" ? $BigInt(m.muteEveryoneMentionEndTimestamp) : $util.Long.fromBits(m.muteEveryoneMentionEndTimestamp.low >>> 0, m.muteEveryoneMentionEndTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.muteEveryoneMentionEndTimestamp === "number")
                        d.muteEveryoneMentionEndTimestamp = o.longs === $String ? $String(m.muteEveryoneMentionEndTimestamp) : m.muteEveryoneMentionEndTimestamp;
                    else
                        d.muteEveryoneMentionEndTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.muteEveryoneMentionEndTimestamp) : o.longs === $Number ? new $util.LongBits(m.muteEveryoneMentionEndTimestamp.low >>> 0, m.muteEveryoneMentionEndTimestamp.high >>> 0).toNumber() : m.muteEveryoneMentionEndTimestamp;
                }
                return d;
            };

            MuteAction.prototype.toJSON = function() {
                return MuteAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            MuteAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.MuteAction";
            };

            return MuteAction;
        })();

        SyncActionValue.NctSaltSyncAction = (function() {

            const NctSaltSyncAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NctSaltSyncAction.prototype.salt = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NctSaltSyncAction.prototype, "_salt", {
                get: $util.oneOfGetter($oneOfFields = ["salt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NctSaltSyncAction.create = function(properties) {
                return new NctSaltSyncAction(properties);
            };

            NctSaltSyncAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.salt != null && $Object.hasOwnProperty.call(m, "salt"))
                    w.uint32(10).bytes(m.salt);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NctSaltSyncAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.NctSaltSyncAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.salt = r.bytes();
                            m._salt = "salt";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NctSaltSyncAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.NctSaltSyncAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.NctSaltSyncAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.NctSaltSyncAction();
                if (d.salt != null) {
                    if (typeof d.salt === "string")
                        $util.base64.decode(d.salt, m.salt = $util.newBuffer($util.base64.length(d.salt)), 0);
                    else if (d.salt.length >= 0)
                        m.salt = d.salt;
                }
                return m;
            };

            NctSaltSyncAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.salt != null && $Object.hasOwnProperty.call(m, "salt")) {
                    d.salt = o.bytes === $String ? $util.base64.encode(m.salt, 0, m.salt.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.salt) : m.salt;
                }
                return d;
            };

            NctSaltSyncAction.prototype.toJSON = function() {
                return NctSaltSyncAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            NctSaltSyncAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.NctSaltSyncAction";
            };

            return NctSaltSyncAction;
        })();

        SyncActionValue.NewsletterSavedInterestsAction = (function() {

            const NewsletterSavedInterestsAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NewsletterSavedInterestsAction.prototype.newsletterSavedInterests = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NewsletterSavedInterestsAction.prototype, "_newsletterSavedInterests", {
                get: $util.oneOfGetter($oneOfFields = ["newsletterSavedInterests"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NewsletterSavedInterestsAction.create = function(properties) {
                return new NewsletterSavedInterestsAction(properties);
            };

            NewsletterSavedInterestsAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.newsletterSavedInterests != null && $Object.hasOwnProperty.call(m, "newsletterSavedInterests"))
                    w.uint32(10).string(m.newsletterSavedInterests);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NewsletterSavedInterestsAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.NewsletterSavedInterestsAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.newsletterSavedInterests = r.stringVerify();
                            m._newsletterSavedInterests = "newsletterSavedInterests";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NewsletterSavedInterestsAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.NewsletterSavedInterestsAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.NewsletterSavedInterestsAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.NewsletterSavedInterestsAction();
                if (d.newsletterSavedInterests != null) {
                    m.newsletterSavedInterests = $String(d.newsletterSavedInterests);
                }
                return m;
            };

            NewsletterSavedInterestsAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.newsletterSavedInterests != null && $Object.hasOwnProperty.call(m, "newsletterSavedInterests")) {
                    d.newsletterSavedInterests = m.newsletterSavedInterests;
                }
                return d;
            };

            NewsletterSavedInterestsAction.prototype.toJSON = function() {
                return NewsletterSavedInterestsAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            NewsletterSavedInterestsAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.NewsletterSavedInterestsAction";
            };

            return NewsletterSavedInterestsAction;
        })();

        SyncActionValue.NoteEditAction = (function() {

            const NoteEditAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NoteEditAction.prototype.type = null;
            NoteEditAction.prototype.chatJid = null;
            NoteEditAction.prototype.createdAt = null;
            NoteEditAction.prototype.deleted = null;
            NoteEditAction.prototype.unstructuredContent = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoteEditAction.prototype, "_type", {
                get: $util.oneOfGetter($oneOfFields = ["type"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoteEditAction.prototype, "_chatJid", {
                get: $util.oneOfGetter($oneOfFields = ["chatJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoteEditAction.prototype, "_createdAt", {
                get: $util.oneOfGetter($oneOfFields = ["createdAt"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoteEditAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NoteEditAction.prototype, "_unstructuredContent", {
                get: $util.oneOfGetter($oneOfFields = ["unstructuredContent"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NoteEditAction.create = function(properties) {
                return new NoteEditAction(properties);
            };

            NoteEditAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.type != null && $Object.hasOwnProperty.call(m, "type"))
                    w.uint32(8).int32(m.type);
                if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid"))
                    w.uint32(18).string(m.chatJid);
                if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt"))
                    w.uint32(24).int64(m.createdAt);
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted"))
                    w.uint32(32).bool(m.deleted);
                if (m.unstructuredContent != null && $Object.hasOwnProperty.call(m, "unstructuredContent"))
                    w.uint32(42).string(m.unstructuredContent);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NoteEditAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.NoteEditAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.type = r.int32();
                            m._type = "type";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.chatJid = r.stringVerify();
                            m._chatJid = "chatJid";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.createdAt = r.int64();
                            m._createdAt = "createdAt";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.deleted = r.bool();
                            m._deleted = "deleted";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            m.unstructuredContent = r.stringVerify();
                            m._unstructuredContent = "unstructuredContent";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NoteEditAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.NoteEditAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.NoteEditAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.NoteEditAction();
                switch (d.type) {
                case "UNSTRUCTURED":
                case 1:
                    m.type = 1;
                    break;
                case "STRUCTURED":
                case 2:
                    m.type = 2;
                    break;
                default:
                    if (typeof d.type === "number" && (d.type | 0) === d.type)
                        m.type = d.type;
                }
                if (d.chatJid != null) {
                    m.chatJid = $String(d.chatJid);
                }
                if (d.createdAt != null) {
                    if ($util.Long)
                        m.createdAt = $util.Long.fromValue(d.createdAt, false);
                    else if (typeof d.createdAt === "string")
                        m.createdAt = $parseInt(d.createdAt, 10);
                    else if (typeof d.createdAt === "number")
                        m.createdAt = d.createdAt;
                    else if (typeof d.createdAt === "object")
                        m.createdAt = new $util.LongBits(d.createdAt.low >>> 0, d.createdAt.high >>> 0).toNumber();
                }
                if (d.deleted != null) {
                    m.deleted = $Boolean(d.deleted);
                }
                if (d.unstructuredContent != null) {
                    m.unstructuredContent = $String(d.unstructuredContent);
                }
                return m;
            };

            NoteEditAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.type != null && $Object.hasOwnProperty.call(m, "type")) {
                    d.type = o.enums === $String ? $root.proto.SyncActionValue.NoteEditAction.NoteType[m.type] === $undefined ? m.type : $root.proto.SyncActionValue.NoteEditAction.NoteType[m.type] : m.type;
                }
                if (m.chatJid != null && $Object.hasOwnProperty.call(m, "chatJid")) {
                    d.chatJid = m.chatJid;
                }
                if (m.createdAt != null && $Object.hasOwnProperty.call(m, "createdAt")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.createdAt = typeof m.createdAt === "number" ? $BigInt(m.createdAt) : $util.Long.fromBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0, false).toBigInt();
                    else if (typeof m.createdAt === "number")
                        d.createdAt = o.longs === $String ? $String(m.createdAt) : m.createdAt;
                    else
                        d.createdAt = o.longs === $String ? $util.Long.prototype.toString.call(m.createdAt) : o.longs === $Number ? new $util.LongBits(m.createdAt.low >>> 0, m.createdAt.high >>> 0).toNumber() : m.createdAt;
                }
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted")) {
                    d.deleted = m.deleted;
                }
                if (m.unstructuredContent != null && $Object.hasOwnProperty.call(m, "unstructuredContent")) {
                    d.unstructuredContent = m.unstructuredContent;
                }
                return d;
            };

            NoteEditAction.prototype.toJSON = function() {
                return NoteEditAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            NoteEditAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.NoteEditAction";
            };

            NoteEditAction.NoteType = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "UNSTRUCTURED"] = 1;
                values[valuesById[2] = "STRUCTURED"] = 2;
                return values;
            })();

            return NoteEditAction;
        })();

        SyncActionValue.NotificationActivitySettingAction = (function() {

            const NotificationActivitySettingAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NotificationActivitySettingAction.prototype.notificationActivitySetting = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NotificationActivitySettingAction.prototype, "_notificationActivitySetting", {
                get: $util.oneOfGetter($oneOfFields = ["notificationActivitySetting"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NotificationActivitySettingAction.create = function(properties) {
                return new NotificationActivitySettingAction(properties);
            };

            NotificationActivitySettingAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.notificationActivitySetting != null && $Object.hasOwnProperty.call(m, "notificationActivitySetting"))
                    w.uint32(8).int32(m.notificationActivitySetting);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NotificationActivitySettingAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.NotificationActivitySettingAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.notificationActivitySetting = r.int32();
                            m._notificationActivitySetting = "notificationActivitySetting";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NotificationActivitySettingAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.NotificationActivitySettingAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.NotificationActivitySettingAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.NotificationActivitySettingAction();
                switch (d.notificationActivitySetting) {
                case "DEFAULT_ALL_MESSAGES":
                case 0:
                    m.notificationActivitySetting = 0;
                    break;
                case "ALL_MESSAGES":
                case 1:
                    m.notificationActivitySetting = 1;
                    break;
                case "HIGHLIGHTS":
                case 2:
                    m.notificationActivitySetting = 2;
                    break;
                case "DEFAULT_HIGHLIGHTS":
                case 3:
                    m.notificationActivitySetting = 3;
                    break;
                default:
                    if (typeof d.notificationActivitySetting === "number" && (d.notificationActivitySetting | 0) === d.notificationActivitySetting)
                        m.notificationActivitySetting = d.notificationActivitySetting;
                }
                return m;
            };

            NotificationActivitySettingAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.notificationActivitySetting != null && $Object.hasOwnProperty.call(m, "notificationActivitySetting")) {
                    d.notificationActivitySetting = o.enums === $String ? $root.proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting[m.notificationActivitySetting] === $undefined ? m.notificationActivitySetting : $root.proto.SyncActionValue.NotificationActivitySettingAction.NotificationActivitySetting[m.notificationActivitySetting] : m.notificationActivitySetting;
                }
                return d;
            };

            NotificationActivitySettingAction.prototype.toJSON = function() {
                return NotificationActivitySettingAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            NotificationActivitySettingAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.NotificationActivitySettingAction";
            };

            NotificationActivitySettingAction.NotificationActivitySetting = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "DEFAULT_ALL_MESSAGES"] = 0;
                values[valuesById[1] = "ALL_MESSAGES"] = 1;
                values[valuesById[2] = "HIGHLIGHTS"] = 2;
                values[valuesById[3] = "DEFAULT_HIGHLIGHTS"] = 3;
                return values;
            })();

            return NotificationActivitySettingAction;
        })();

        SyncActionValue.NuxAction = (function() {

            const NuxAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            NuxAction.prototype.acknowledged = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(NuxAction.prototype, "_acknowledged", {
                get: $util.oneOfGetter($oneOfFields = ["acknowledged"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            NuxAction.create = function(properties) {
                return new NuxAction(properties);
            };

            NuxAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.acknowledged != null && $Object.hasOwnProperty.call(m, "acknowledged"))
                    w.uint32(8).bool(m.acknowledged);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            NuxAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.NuxAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.acknowledged = r.bool();
                            m._acknowledged = "acknowledged";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            NuxAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.NuxAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.NuxAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.NuxAction();
                if (d.acknowledged != null) {
                    m.acknowledged = $Boolean(d.acknowledged);
                }
                return m;
            };

            NuxAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.acknowledged != null && $Object.hasOwnProperty.call(m, "acknowledged")) {
                    d.acknowledged = m.acknowledged;
                }
                return d;
            };

            NuxAction.prototype.toJSON = function() {
                return NuxAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            NuxAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.NuxAction";
            };

            return NuxAction;
        })();

        SyncActionValue.OutContactAction = (function() {

            const OutContactAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            OutContactAction.prototype.fullName = null;
            OutContactAction.prototype.firstName = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OutContactAction.prototype, "_fullName", {
                get: $util.oneOfGetter($oneOfFields = ["fullName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(OutContactAction.prototype, "_firstName", {
                get: $util.oneOfGetter($oneOfFields = ["firstName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            OutContactAction.create = function(properties) {
                return new OutContactAction(properties);
            };

            OutContactAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName"))
                    w.uint32(10).string(m.fullName);
                if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName"))
                    w.uint32(18).string(m.firstName);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            OutContactAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.OutContactAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.fullName = r.stringVerify();
                            m._fullName = "fullName";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.firstName = r.stringVerify();
                            m._firstName = "firstName";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            OutContactAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.OutContactAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.OutContactAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.OutContactAction();
                if (d.fullName != null) {
                    m.fullName = $String(d.fullName);
                }
                if (d.firstName != null) {
                    m.firstName = $String(d.firstName);
                }
                return m;
            };

            OutContactAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.fullName != null && $Object.hasOwnProperty.call(m, "fullName")) {
                    d.fullName = m.fullName;
                }
                if (m.firstName != null && $Object.hasOwnProperty.call(m, "firstName")) {
                    d.firstName = m.firstName;
                }
                return d;
            };

            OutContactAction.prototype.toJSON = function() {
                return OutContactAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            OutContactAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.OutContactAction";
            };

            return OutContactAction;
        })();

        SyncActionValue.PaymentInfoAction = (function() {

            const PaymentInfoAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PaymentInfoAction.prototype.cpi = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PaymentInfoAction.prototype, "_cpi", {
                get: $util.oneOfGetter($oneOfFields = ["cpi"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PaymentInfoAction.create = function(properties) {
                return new PaymentInfoAction(properties);
            };

            PaymentInfoAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.cpi != null && $Object.hasOwnProperty.call(m, "cpi"))
                    w.uint32(10).string(m.cpi);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PaymentInfoAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PaymentInfoAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.cpi = r.stringVerify();
                            m._cpi = "cpi";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PaymentInfoAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PaymentInfoAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PaymentInfoAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PaymentInfoAction();
                if (d.cpi != null) {
                    m.cpi = $String(d.cpi);
                }
                return m;
            };

            PaymentInfoAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.cpi != null && $Object.hasOwnProperty.call(m, "cpi")) {
                    d.cpi = m.cpi;
                }
                return d;
            };

            PaymentInfoAction.prototype.toJSON = function() {
                return PaymentInfoAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentInfoAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PaymentInfoAction";
            };

            return PaymentInfoAction;
        })();

        SyncActionValue.PaymentTosAction = (function() {

            const PaymentTosAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PaymentTosAction.prototype.paymentNotice = 0;
            PaymentTosAction.prototype.accepted = false;

            PaymentTosAction.create = function(properties) {
                return new PaymentTosAction(properties);
            };

            PaymentTosAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                w.uint32(8).int32(m.paymentNotice);
                w.uint32(16).bool(m.accepted);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PaymentTosAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PaymentTosAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.paymentNotice = r.int32();
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.accepted = r.bool();
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                if (!$Object.hasOwnProperty.call(m, "paymentNotice"))
                    throw $util.ProtocolError("missing required 'paymentNotice'", { instance: m });
                if (!$Object.hasOwnProperty.call(m, "accepted"))
                    throw $util.ProtocolError("missing required 'accepted'", { instance: m });
                return m;
            };

            PaymentTosAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PaymentTosAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PaymentTosAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PaymentTosAction();
                switch (d.paymentNotice) {
                case "BR_PAY_PRIVACY_POLICY":
                case 0:
                    m.paymentNotice = 0;
                    break;
                default:
                    if (typeof d.paymentNotice === "number" && (d.paymentNotice | 0) === d.paymentNotice)
                        m.paymentNotice = d.paymentNotice;
                }
                if (d.accepted != null) {
                    m.accepted = $Boolean(d.accepted);
                }
                return m;
            };

            PaymentTosAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.defaults) {
                    d.paymentNotice = o.enums === $String ? "BR_PAY_PRIVACY_POLICY" : 0;
                    d.accepted = false;
                }
                if (m.paymentNotice != null && $Object.hasOwnProperty.call(m, "paymentNotice")) {
                    d.paymentNotice = o.enums === $String ? $root.proto.SyncActionValue.PaymentTosAction.PaymentNotice[m.paymentNotice] === $undefined ? m.paymentNotice : $root.proto.SyncActionValue.PaymentTosAction.PaymentNotice[m.paymentNotice] : m.paymentNotice;
                }
                if (m.accepted != null && $Object.hasOwnProperty.call(m, "accepted")) {
                    d.accepted = m.accepted;
                }
                return d;
            };

            PaymentTosAction.prototype.toJSON = function() {
                return PaymentTosAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PaymentTosAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PaymentTosAction";
            };

            PaymentTosAction.PaymentNotice = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "BR_PAY_PRIVACY_POLICY"] = 0;
                return values;
            })();

            return PaymentTosAction;
        })();

        SyncActionValue.PinAction = (function() {

            const PinAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PinAction.prototype.pinned = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PinAction.prototype, "_pinned", {
                get: $util.oneOfGetter($oneOfFields = ["pinned"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PinAction.create = function(properties) {
                return new PinAction(properties);
            };

            PinAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pinned != null && $Object.hasOwnProperty.call(m, "pinned"))
                    w.uint32(8).bool(m.pinned);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PinAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PinAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.pinned = r.bool();
                            m._pinned = "pinned";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PinAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PinAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PinAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PinAction();
                if (d.pinned != null) {
                    m.pinned = $Boolean(d.pinned);
                }
                return m;
            };

            PinAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pinned != null && $Object.hasOwnProperty.call(m, "pinned")) {
                    d.pinned = m.pinned;
                }
                return d;
            };

            PinAction.prototype.toJSON = function() {
                return PinAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PinAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PinAction";
            };

            return PinAction;
        })();

        SyncActionValue.PnForLidChatAction = (function() {

            const PnForLidChatAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PnForLidChatAction.prototype.pnJid = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PnForLidChatAction.prototype, "_pnJid", {
                get: $util.oneOfGetter($oneOfFields = ["pnJid"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PnForLidChatAction.create = function(properties) {
                return new PnForLidChatAction(properties);
            };

            PnForLidChatAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid"))
                    w.uint32(10).string(m.pnJid);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PnForLidChatAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PnForLidChatAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.pnJid = r.stringVerify();
                            m._pnJid = "pnJid";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PnForLidChatAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PnForLidChatAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PnForLidChatAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PnForLidChatAction();
                if (d.pnJid != null) {
                    m.pnJid = $String(d.pnJid);
                }
                return m;
            };

            PnForLidChatAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pnJid != null && $Object.hasOwnProperty.call(m, "pnJid")) {
                    d.pnJid = m.pnJid;
                }
                return d;
            };

            PnForLidChatAction.prototype.toJSON = function() {
                return PnForLidChatAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PnForLidChatAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PnForLidChatAction";
            };

            return PnForLidChatAction;
        })();

        SyncActionValue.PrimaryFeature = (function() {

            const PrimaryFeature = function (p) {
                this.flags = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PrimaryFeature.prototype.flags = $util.emptyArray;

            PrimaryFeature.create = function(properties) {
                return new PrimaryFeature(properties);
            };

            PrimaryFeature.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.flags != null && m.flags.length) {
                    for (var i = 0; i < m.flags.length; ++i)
                        w.uint32(10).string(m.flags[i]);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PrimaryFeature.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PrimaryFeature();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.flags && m.flags.length))
                                m.flags = [];
                            m.flags.push(r.stringVerify());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PrimaryFeature.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PrimaryFeature)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PrimaryFeature: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PrimaryFeature();
                if (d.flags) {
                    if (!$Array.isArray(d.flags))
                        throw $TypeError(".proto.SyncActionValue.PrimaryFeature.flags: array expected");
                    m.flags = $Array(d.flags.length);
                    for (var i = 0; i < d.flags.length; ++i) {
                        m.flags[i] = $String(d.flags[i]);
                    }
                }
                return m;
            };

            PrimaryFeature.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.flags = [];
                }
                if (m.flags && m.flags.length) {
                    d.flags = $Array(m.flags.length);
                    for (var j = 0; j < m.flags.length; ++j) {
                        d.flags[j] = m.flags[j];
                    }
                }
                return d;
            };

            PrimaryFeature.prototype.toJSON = function() {
                return PrimaryFeature.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrimaryFeature.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PrimaryFeature";
            };

            return PrimaryFeature;
        })();

        SyncActionValue.PrimaryVersionAction = (function() {

            const PrimaryVersionAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PrimaryVersionAction.prototype.version = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PrimaryVersionAction.prototype, "_version", {
                get: $util.oneOfGetter($oneOfFields = ["version"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PrimaryVersionAction.create = function(properties) {
                return new PrimaryVersionAction(properties);
            };

            PrimaryVersionAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                    w.uint32(10).string(m.version);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PrimaryVersionAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PrimaryVersionAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.version = r.stringVerify();
                            m._version = "version";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PrimaryVersionAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PrimaryVersionAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PrimaryVersionAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PrimaryVersionAction();
                if (d.version != null) {
                    m.version = $String(d.version);
                }
                return m;
            };

            PrimaryVersionAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                    d.version = m.version;
                }
                return d;
            };

            PrimaryVersionAction.prototype.toJSON = function() {
                return PrimaryVersionAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrimaryVersionAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PrimaryVersionAction";
            };

            return PrimaryVersionAction;
        })();

        SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction = (function() {

            const PrivacySettingChannelsPersonalisedRecommendationAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.prototype.isUserOptedOut = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PrivacySettingChannelsPersonalisedRecommendationAction.prototype, "_isUserOptedOut", {
                get: $util.oneOfGetter($oneOfFields = ["isUserOptedOut"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PrivacySettingChannelsPersonalisedRecommendationAction.create = function(properties) {
                return new PrivacySettingChannelsPersonalisedRecommendationAction(properties);
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isUserOptedOut != null && $Object.hasOwnProperty.call(m, "isUserOptedOut"))
                    w.uint32(8).bool(m.isUserOptedOut);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isUserOptedOut = r.bool();
                            m._isUserOptedOut = "isUserOptedOut";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction();
                if (d.isUserOptedOut != null) {
                    m.isUserOptedOut = $Boolean(d.isUserOptedOut);
                }
                return m;
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isUserOptedOut != null && $Object.hasOwnProperty.call(m, "isUserOptedOut")) {
                    d.isUserOptedOut = m.isUserOptedOut;
                }
                return d;
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.prototype.toJSON = function() {
                return PrivacySettingChannelsPersonalisedRecommendationAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrivacySettingChannelsPersonalisedRecommendationAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PrivacySettingChannelsPersonalisedRecommendationAction";
            };

            return PrivacySettingChannelsPersonalisedRecommendationAction;
        })();

        SyncActionValue.PrivacySettingDisableLinkPreviewsAction = (function() {

            const PrivacySettingDisableLinkPreviewsAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PrivacySettingDisableLinkPreviewsAction.prototype.isPreviewsDisabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PrivacySettingDisableLinkPreviewsAction.prototype, "_isPreviewsDisabled", {
                get: $util.oneOfGetter($oneOfFields = ["isPreviewsDisabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PrivacySettingDisableLinkPreviewsAction.create = function(properties) {
                return new PrivacySettingDisableLinkPreviewsAction(properties);
            };

            PrivacySettingDisableLinkPreviewsAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isPreviewsDisabled != null && $Object.hasOwnProperty.call(m, "isPreviewsDisabled"))
                    w.uint32(8).bool(m.isPreviewsDisabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PrivacySettingDisableLinkPreviewsAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isPreviewsDisabled = r.bool();
                            m._isPreviewsDisabled = "isPreviewsDisabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PrivacySettingDisableLinkPreviewsAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction();
                if (d.isPreviewsDisabled != null) {
                    m.isPreviewsDisabled = $Boolean(d.isPreviewsDisabled);
                }
                return m;
            };

            PrivacySettingDisableLinkPreviewsAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isPreviewsDisabled != null && $Object.hasOwnProperty.call(m, "isPreviewsDisabled")) {
                    d.isPreviewsDisabled = m.isPreviewsDisabled;
                }
                return d;
            };

            PrivacySettingDisableLinkPreviewsAction.prototype.toJSON = function() {
                return PrivacySettingDisableLinkPreviewsAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrivacySettingDisableLinkPreviewsAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PrivacySettingDisableLinkPreviewsAction";
            };

            return PrivacySettingDisableLinkPreviewsAction;
        })();

        SyncActionValue.PrivacySettingRelayAllCalls = (function() {

            const PrivacySettingRelayAllCalls = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PrivacySettingRelayAllCalls.prototype.isEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PrivacySettingRelayAllCalls.prototype, "_isEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PrivacySettingRelayAllCalls.create = function(properties) {
                return new PrivacySettingRelayAllCalls(properties);
            };

            PrivacySettingRelayAllCalls.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isEnabled != null && $Object.hasOwnProperty.call(m, "isEnabled"))
                    w.uint32(8).bool(m.isEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PrivacySettingRelayAllCalls.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PrivacySettingRelayAllCalls();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isEnabled = r.bool();
                            m._isEnabled = "isEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PrivacySettingRelayAllCalls.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PrivacySettingRelayAllCalls)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PrivacySettingRelayAllCalls: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PrivacySettingRelayAllCalls();
                if (d.isEnabled != null) {
                    m.isEnabled = $Boolean(d.isEnabled);
                }
                return m;
            };

            PrivacySettingRelayAllCalls.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isEnabled != null && $Object.hasOwnProperty.call(m, "isEnabled")) {
                    d.isEnabled = m.isEnabled;
                }
                return d;
            };

            PrivacySettingRelayAllCalls.prototype.toJSON = function() {
                return PrivacySettingRelayAllCalls.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrivacySettingRelayAllCalls.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PrivacySettingRelayAllCalls";
            };

            return PrivacySettingRelayAllCalls;
        })();

        SyncActionValue.PrivateProcessingSettingAction = (function() {

            const PrivateProcessingSettingAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PrivateProcessingSettingAction.prototype.privateProcessingStatus = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PrivateProcessingSettingAction.prototype, "_privateProcessingStatus", {
                get: $util.oneOfGetter($oneOfFields = ["privateProcessingStatus"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PrivateProcessingSettingAction.create = function(properties) {
                return new PrivateProcessingSettingAction(properties);
            };

            PrivateProcessingSettingAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.privateProcessingStatus != null && $Object.hasOwnProperty.call(m, "privateProcessingStatus"))
                    w.uint32(8).int32(m.privateProcessingStatus);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PrivateProcessingSettingAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PrivateProcessingSettingAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.privateProcessingStatus = r.int32();
                            m._privateProcessingStatus = "privateProcessingStatus";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PrivateProcessingSettingAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PrivateProcessingSettingAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PrivateProcessingSettingAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PrivateProcessingSettingAction();
                switch (d.privateProcessingStatus) {
                case "UNDEFINED":
                case 0:
                    m.privateProcessingStatus = 0;
                    break;
                case "ENABLED":
                case 1:
                    m.privateProcessingStatus = 1;
                    break;
                case "DISABLED":
                case 2:
                    m.privateProcessingStatus = 2;
                    break;
                default:
                    if (typeof d.privateProcessingStatus === "number" && (d.privateProcessingStatus | 0) === d.privateProcessingStatus)
                        m.privateProcessingStatus = d.privateProcessingStatus;
                }
                return m;
            };

            PrivateProcessingSettingAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.privateProcessingStatus != null && $Object.hasOwnProperty.call(m, "privateProcessingStatus")) {
                    d.privateProcessingStatus = o.enums === $String ? $root.proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus[m.privateProcessingStatus] === $undefined ? m.privateProcessingStatus : $root.proto.SyncActionValue.PrivateProcessingSettingAction.PrivateProcessingStatus[m.privateProcessingStatus] : m.privateProcessingStatus;
                }
                return d;
            };

            PrivateProcessingSettingAction.prototype.toJSON = function() {
                return PrivateProcessingSettingAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            PrivateProcessingSettingAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PrivateProcessingSettingAction";
            };

            PrivateProcessingSettingAction.PrivateProcessingStatus = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "UNDEFINED"] = 0;
                values[valuesById[1] = "ENABLED"] = 1;
                values[valuesById[2] = "DISABLED"] = 2;
                return values;
            })();

            return PrivateProcessingSettingAction;
        })();

        SyncActionValue.PushNameSetting = (function() {

            const PushNameSetting = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            PushNameSetting.prototype.name = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(PushNameSetting.prototype, "_name", {
                get: $util.oneOfGetter($oneOfFields = ["name"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            PushNameSetting.create = function(properties) {
                return new PushNameSetting(properties);
            };

            PushNameSetting.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                    w.uint32(10).string(m.name);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            PushNameSetting.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.PushNameSetting();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.name = r.stringVerify();
                            m._name = "name";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            PushNameSetting.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.PushNameSetting)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.PushNameSetting: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.PushNameSetting();
                if (d.name != null) {
                    m.name = $String(d.name);
                }
                return m;
            };

            PushNameSetting.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                    d.name = m.name;
                }
                return d;
            };

            PushNameSetting.prototype.toJSON = function() {
                return PushNameSetting.toObject(this, $protobuf.util.toJSONOptions);
            };

            PushNameSetting.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.PushNameSetting";
            };

            return PushNameSetting;
        })();

        SyncActionValue.QuickReplyAction = (function() {

            const QuickReplyAction = function (p) {
                this.keywords = [];
                this.associatedLabelIds = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            QuickReplyAction.prototype.shortcut = null;
            QuickReplyAction.prototype.message = null;
            QuickReplyAction.prototype.keywords = $util.emptyArray;
            QuickReplyAction.prototype.count = null;
            QuickReplyAction.prototype.deleted = null;
            QuickReplyAction.prototype.associatedLabelIds = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuickReplyAction.prototype, "_shortcut", {
                get: $util.oneOfGetter($oneOfFields = ["shortcut"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuickReplyAction.prototype, "_message", {
                get: $util.oneOfGetter($oneOfFields = ["message"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuickReplyAction.prototype, "_count", {
                get: $util.oneOfGetter($oneOfFields = ["count"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuickReplyAction.prototype, "_deleted", {
                get: $util.oneOfGetter($oneOfFields = ["deleted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            QuickReplyAction.create = function(properties) {
                return new QuickReplyAction(properties);
            };

            QuickReplyAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.shortcut != null && $Object.hasOwnProperty.call(m, "shortcut"))
                    w.uint32(10).string(m.shortcut);
                if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                    w.uint32(18).string(m.message);
                if (m.keywords != null && m.keywords.length) {
                    for (var i = 0; i < m.keywords.length; ++i)
                        w.uint32(26).string(m.keywords[i]);
                }
                if (m.count != null && $Object.hasOwnProperty.call(m, "count"))
                    w.uint32(32).int32(m.count);
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted"))
                    w.uint32(40).bool(m.deleted);
                if (m.associatedLabelIds != null && m.associatedLabelIds.length) {
                    for (var i = 0; i < m.associatedLabelIds.length; ++i)
                        w.uint32(50).string(m.associatedLabelIds[i]);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            QuickReplyAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.QuickReplyAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.shortcut = r.stringVerify();
                            m._shortcut = "shortcut";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.message = r.stringVerify();
                            m._message = "message";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.keywords && m.keywords.length))
                                m.keywords = [];
                            m.keywords.push(r.stringVerify());
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.count = r.int32();
                            m._count = "count";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.deleted = r.bool();
                            m._deleted = "deleted";
                            continue;
                        }
                    case 6: {
                            if (u !== 2)
                                break;
                            if (!(m.associatedLabelIds && m.associatedLabelIds.length))
                                m.associatedLabelIds = [];
                            m.associatedLabelIds.push(r.stringVerify());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            QuickReplyAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.QuickReplyAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.QuickReplyAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.QuickReplyAction();
                if (d.shortcut != null) {
                    m.shortcut = $String(d.shortcut);
                }
                if (d.message != null) {
                    m.message = $String(d.message);
                }
                if (d.keywords) {
                    if (!$Array.isArray(d.keywords))
                        throw $TypeError(".proto.SyncActionValue.QuickReplyAction.keywords: array expected");
                    m.keywords = $Array(d.keywords.length);
                    for (var i = 0; i < d.keywords.length; ++i) {
                        m.keywords[i] = $String(d.keywords[i]);
                    }
                }
                if (d.count != null) {
                    m.count = d.count | 0;
                }
                if (d.deleted != null) {
                    m.deleted = $Boolean(d.deleted);
                }
                if (d.associatedLabelIds) {
                    if (!$Array.isArray(d.associatedLabelIds))
                        throw $TypeError(".proto.SyncActionValue.QuickReplyAction.associatedLabelIds: array expected");
                    m.associatedLabelIds = $Array(d.associatedLabelIds.length);
                    for (var i = 0; i < d.associatedLabelIds.length; ++i) {
                        m.associatedLabelIds[i] = $String(d.associatedLabelIds[i]);
                    }
                }
                return m;
            };

            QuickReplyAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.keywords = [];
                    d.associatedLabelIds = [];
                }
                if (m.shortcut != null && $Object.hasOwnProperty.call(m, "shortcut")) {
                    d.shortcut = m.shortcut;
                }
                if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                    d.message = m.message;
                }
                if (m.keywords && m.keywords.length) {
                    d.keywords = $Array(m.keywords.length);
                    for (var j = 0; j < m.keywords.length; ++j) {
                        d.keywords[j] = m.keywords[j];
                    }
                }
                if (m.count != null && $Object.hasOwnProperty.call(m, "count")) {
                    d.count = m.count;
                }
                if (m.deleted != null && $Object.hasOwnProperty.call(m, "deleted")) {
                    d.deleted = m.deleted;
                }
                if (m.associatedLabelIds && m.associatedLabelIds.length) {
                    d.associatedLabelIds = $Array(m.associatedLabelIds.length);
                    for (var j = 0; j < m.associatedLabelIds.length; ++j) {
                        d.associatedLabelIds[j] = m.associatedLabelIds[j];
                    }
                }
                return d;
            };

            QuickReplyAction.prototype.toJSON = function() {
                return QuickReplyAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuickReplyAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.QuickReplyAction";
            };

            return QuickReplyAction;
        })();

        SyncActionValue.RecentEmojiWeightsAction = (function() {

            const RecentEmojiWeightsAction = function (p) {
                this.weights = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RecentEmojiWeightsAction.prototype.weights = $util.emptyArray;

            RecentEmojiWeightsAction.create = function(properties) {
                return new RecentEmojiWeightsAction(properties);
            };

            RecentEmojiWeightsAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.weights != null && m.weights.length) {
                    for (var i = 0; i < m.weights.length; ++i)
                        $root.proto.RecentEmojiWeight.encode(m.weights[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RecentEmojiWeightsAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.RecentEmojiWeightsAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.weights && m.weights.length))
                                m.weights = [];
                            m.weights.push($root.proto.RecentEmojiWeight.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RecentEmojiWeightsAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.RecentEmojiWeightsAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.RecentEmojiWeightsAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.RecentEmojiWeightsAction();
                if (d.weights) {
                    if (!$Array.isArray(d.weights))
                        throw $TypeError(".proto.SyncActionValue.RecentEmojiWeightsAction.weights: array expected");
                    m.weights = $Array(d.weights.length);
                    for (var i = 0; i < d.weights.length; ++i) {
                        if (!$util.isObject(d.weights[i]))
                            throw $TypeError(".proto.SyncActionValue.RecentEmojiWeightsAction.weights: object expected");
                        m.weights[i] = $root.proto.RecentEmojiWeight.fromObject(d.weights[i], q + 1);
                    }
                }
                return m;
            };

            RecentEmojiWeightsAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.weights = [];
                }
                if (m.weights && m.weights.length) {
                    d.weights = $Array(m.weights.length);
                    for (var j = 0; j < m.weights.length; ++j) {
                        d.weights[j] = $root.proto.RecentEmojiWeight.toObject(m.weights[j], o, q + 1);
                    }
                }
                return d;
            };

            RecentEmojiWeightsAction.prototype.toJSON = function() {
                return RecentEmojiWeightsAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            RecentEmojiWeightsAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.RecentEmojiWeightsAction";
            };

            return RecentEmojiWeightsAction;
        })();

        SyncActionValue.RemoveRecentStickerAction = (function() {

            const RemoveRecentStickerAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            RemoveRecentStickerAction.prototype.lastStickerSentTs = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(RemoveRecentStickerAction.prototype, "_lastStickerSentTs", {
                get: $util.oneOfGetter($oneOfFields = ["lastStickerSentTs"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            RemoveRecentStickerAction.create = function(properties) {
                return new RemoveRecentStickerAction(properties);
            };

            RemoveRecentStickerAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.lastStickerSentTs != null && $Object.hasOwnProperty.call(m, "lastStickerSentTs"))
                    w.uint32(8).int64(m.lastStickerSentTs);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            RemoveRecentStickerAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.RemoveRecentStickerAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.lastStickerSentTs = r.int64();
                            m._lastStickerSentTs = "lastStickerSentTs";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            RemoveRecentStickerAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.RemoveRecentStickerAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.RemoveRecentStickerAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.RemoveRecentStickerAction();
                if (d.lastStickerSentTs != null) {
                    if ($util.Long)
                        m.lastStickerSentTs = $util.Long.fromValue(d.lastStickerSentTs, false);
                    else if (typeof d.lastStickerSentTs === "string")
                        m.lastStickerSentTs = $parseInt(d.lastStickerSentTs, 10);
                    else if (typeof d.lastStickerSentTs === "number")
                        m.lastStickerSentTs = d.lastStickerSentTs;
                    else if (typeof d.lastStickerSentTs === "object")
                        m.lastStickerSentTs = new $util.LongBits(d.lastStickerSentTs.low >>> 0, d.lastStickerSentTs.high >>> 0).toNumber();
                }
                return m;
            };

            RemoveRecentStickerAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.lastStickerSentTs != null && $Object.hasOwnProperty.call(m, "lastStickerSentTs")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.lastStickerSentTs = typeof m.lastStickerSentTs === "number" ? $BigInt(m.lastStickerSentTs) : $util.Long.fromBits(m.lastStickerSentTs.low >>> 0, m.lastStickerSentTs.high >>> 0, false).toBigInt();
                    else if (typeof m.lastStickerSentTs === "number")
                        d.lastStickerSentTs = o.longs === $String ? $String(m.lastStickerSentTs) : m.lastStickerSentTs;
                    else
                        d.lastStickerSentTs = o.longs === $String ? $util.Long.prototype.toString.call(m.lastStickerSentTs) : o.longs === $Number ? new $util.LongBits(m.lastStickerSentTs.low >>> 0, m.lastStickerSentTs.high >>> 0).toNumber() : m.lastStickerSentTs;
                }
                return d;
            };

            RemoveRecentStickerAction.prototype.toJSON = function() {
                return RemoveRecentStickerAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            RemoveRecentStickerAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.RemoveRecentStickerAction";
            };

            return RemoveRecentStickerAction;
        })();

        SyncActionValue.SettingsSyncAction = (function() {

            const SettingsSyncAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SettingsSyncAction.prototype.startAtLogin = null;
            SettingsSyncAction.prototype.minimizeToTray = null;
            SettingsSyncAction.prototype.language = null;
            SettingsSyncAction.prototype.replaceTextWithEmoji = null;
            SettingsSyncAction.prototype.bannerNotificationDisplayMode = null;
            SettingsSyncAction.prototype.unreadCounterBadgeDisplayMode = null;
            SettingsSyncAction.prototype.isMessagesNotificationEnabled = null;
            SettingsSyncAction.prototype.isCallsNotificationEnabled = null;
            SettingsSyncAction.prototype.isReactionsNotificationEnabled = null;
            SettingsSyncAction.prototype.isStatusReactionsNotificationEnabled = null;
            SettingsSyncAction.prototype.isTextPreviewForNotificationEnabled = null;
            SettingsSyncAction.prototype.defaultNotificationToneId = null;
            SettingsSyncAction.prototype.groupDefaultNotificationToneId = null;
            SettingsSyncAction.prototype.appTheme = null;
            SettingsSyncAction.prototype.wallpaperId = null;
            SettingsSyncAction.prototype.isDoodleWallpaperEnabled = null;
            SettingsSyncAction.prototype.fontSize = null;
            SettingsSyncAction.prototype.isPhotosAutodownloadEnabled = null;
            SettingsSyncAction.prototype.isAudiosAutodownloadEnabled = null;
            SettingsSyncAction.prototype.isVideosAutodownloadEnabled = null;
            SettingsSyncAction.prototype.isDocumentsAutodownloadEnabled = null;
            SettingsSyncAction.prototype.disableLinkPreviews = null;
            SettingsSyncAction.prototype.notificationToneId = null;
            SettingsSyncAction.prototype.mediaUploadQuality = null;
            SettingsSyncAction.prototype.isSpellCheckEnabled = null;
            SettingsSyncAction.prototype.isEnterToSendEnabled = null;
            SettingsSyncAction.prototype.isGroupMessageNotificationEnabled = null;
            SettingsSyncAction.prototype.isGroupReactionsNotificationEnabled = null;
            SettingsSyncAction.prototype.isStatusNotificationEnabled = null;
            SettingsSyncAction.prototype.statusNotificationToneId = null;
            SettingsSyncAction.prototype.shouldPlaySoundForCallNotification = null;
            SettingsSyncAction.prototype.chatThemeId = null;
            SettingsSyncAction.prototype.colorSchemeId = null;
            SettingsSyncAction.prototype.stockWallpaperImageId = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_startAtLogin", {
                get: $util.oneOfGetter($oneOfFields = ["startAtLogin"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_minimizeToTray", {
                get: $util.oneOfGetter($oneOfFields = ["minimizeToTray"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_language", {
                get: $util.oneOfGetter($oneOfFields = ["language"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_replaceTextWithEmoji", {
                get: $util.oneOfGetter($oneOfFields = ["replaceTextWithEmoji"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_bannerNotificationDisplayMode", {
                get: $util.oneOfGetter($oneOfFields = ["bannerNotificationDisplayMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_unreadCounterBadgeDisplayMode", {
                get: $util.oneOfGetter($oneOfFields = ["unreadCounterBadgeDisplayMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isMessagesNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isMessagesNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isCallsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isCallsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isReactionsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isReactionsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isStatusReactionsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isStatusReactionsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isTextPreviewForNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isTextPreviewForNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_defaultNotificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["defaultNotificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_groupDefaultNotificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["groupDefaultNotificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_appTheme", {
                get: $util.oneOfGetter($oneOfFields = ["appTheme"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_wallpaperId", {
                get: $util.oneOfGetter($oneOfFields = ["wallpaperId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isDoodleWallpaperEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDoodleWallpaperEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_fontSize", {
                get: $util.oneOfGetter($oneOfFields = ["fontSize"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isPhotosAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isPhotosAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isAudiosAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isAudiosAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isVideosAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isVideosAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isDocumentsAutodownloadEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isDocumentsAutodownloadEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_disableLinkPreviews", {
                get: $util.oneOfGetter($oneOfFields = ["disableLinkPreviews"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_notificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["notificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_mediaUploadQuality", {
                get: $util.oneOfGetter($oneOfFields = ["mediaUploadQuality"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isSpellCheckEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isSpellCheckEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isEnterToSendEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isEnterToSendEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isGroupMessageNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isGroupMessageNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isGroupReactionsNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isGroupReactionsNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_isStatusNotificationEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isStatusNotificationEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_statusNotificationToneId", {
                get: $util.oneOfGetter($oneOfFields = ["statusNotificationToneId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_shouldPlaySoundForCallNotification", {
                get: $util.oneOfGetter($oneOfFields = ["shouldPlaySoundForCallNotification"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_chatThemeId", {
                get: $util.oneOfGetter($oneOfFields = ["chatThemeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_colorSchemeId", {
                get: $util.oneOfGetter($oneOfFields = ["colorSchemeId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SettingsSyncAction.prototype, "_stockWallpaperImageId", {
                get: $util.oneOfGetter($oneOfFields = ["stockWallpaperImageId"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SettingsSyncAction.create = function(properties) {
                return new SettingsSyncAction(properties);
            };

            SettingsSyncAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.startAtLogin != null && $Object.hasOwnProperty.call(m, "startAtLogin"))
                    w.uint32(8).bool(m.startAtLogin);
                if (m.minimizeToTray != null && $Object.hasOwnProperty.call(m, "minimizeToTray"))
                    w.uint32(16).bool(m.minimizeToTray);
                if (m.language != null && $Object.hasOwnProperty.call(m, "language"))
                    w.uint32(26).string(m.language);
                if (m.replaceTextWithEmoji != null && $Object.hasOwnProperty.call(m, "replaceTextWithEmoji"))
                    w.uint32(32).bool(m.replaceTextWithEmoji);
                if (m.bannerNotificationDisplayMode != null && $Object.hasOwnProperty.call(m, "bannerNotificationDisplayMode"))
                    w.uint32(40).int32(m.bannerNotificationDisplayMode);
                if (m.unreadCounterBadgeDisplayMode != null && $Object.hasOwnProperty.call(m, "unreadCounterBadgeDisplayMode"))
                    w.uint32(48).int32(m.unreadCounterBadgeDisplayMode);
                if (m.isMessagesNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isMessagesNotificationEnabled"))
                    w.uint32(56).bool(m.isMessagesNotificationEnabled);
                if (m.isCallsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isCallsNotificationEnabled"))
                    w.uint32(64).bool(m.isCallsNotificationEnabled);
                if (m.isReactionsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isReactionsNotificationEnabled"))
                    w.uint32(72).bool(m.isReactionsNotificationEnabled);
                if (m.isStatusReactionsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isStatusReactionsNotificationEnabled"))
                    w.uint32(80).bool(m.isStatusReactionsNotificationEnabled);
                if (m.isTextPreviewForNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isTextPreviewForNotificationEnabled"))
                    w.uint32(88).bool(m.isTextPreviewForNotificationEnabled);
                if (m.defaultNotificationToneId != null && $Object.hasOwnProperty.call(m, "defaultNotificationToneId"))
                    w.uint32(96).int32(m.defaultNotificationToneId);
                if (m.groupDefaultNotificationToneId != null && $Object.hasOwnProperty.call(m, "groupDefaultNotificationToneId"))
                    w.uint32(104).int32(m.groupDefaultNotificationToneId);
                if (m.appTheme != null && $Object.hasOwnProperty.call(m, "appTheme"))
                    w.uint32(112).int32(m.appTheme);
                if (m.wallpaperId != null && $Object.hasOwnProperty.call(m, "wallpaperId"))
                    w.uint32(120).int32(m.wallpaperId);
                if (m.isDoodleWallpaperEnabled != null && $Object.hasOwnProperty.call(m, "isDoodleWallpaperEnabled"))
                    w.uint32(128).bool(m.isDoodleWallpaperEnabled);
                if (m.fontSize != null && $Object.hasOwnProperty.call(m, "fontSize"))
                    w.uint32(136).int32(m.fontSize);
                if (m.isPhotosAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isPhotosAutodownloadEnabled"))
                    w.uint32(144).bool(m.isPhotosAutodownloadEnabled);
                if (m.isAudiosAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isAudiosAutodownloadEnabled"))
                    w.uint32(152).bool(m.isAudiosAutodownloadEnabled);
                if (m.isVideosAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isVideosAutodownloadEnabled"))
                    w.uint32(160).bool(m.isVideosAutodownloadEnabled);
                if (m.isDocumentsAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isDocumentsAutodownloadEnabled"))
                    w.uint32(168).bool(m.isDocumentsAutodownloadEnabled);
                if (m.disableLinkPreviews != null && $Object.hasOwnProperty.call(m, "disableLinkPreviews"))
                    w.uint32(176).bool(m.disableLinkPreviews);
                if (m.notificationToneId != null && $Object.hasOwnProperty.call(m, "notificationToneId"))
                    w.uint32(184).int32(m.notificationToneId);
                if (m.mediaUploadQuality != null && $Object.hasOwnProperty.call(m, "mediaUploadQuality"))
                    w.uint32(192).int32(m.mediaUploadQuality);
                if (m.isSpellCheckEnabled != null && $Object.hasOwnProperty.call(m, "isSpellCheckEnabled"))
                    w.uint32(200).bool(m.isSpellCheckEnabled);
                if (m.isEnterToSendEnabled != null && $Object.hasOwnProperty.call(m, "isEnterToSendEnabled"))
                    w.uint32(208).bool(m.isEnterToSendEnabled);
                if (m.isGroupMessageNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isGroupMessageNotificationEnabled"))
                    w.uint32(216).bool(m.isGroupMessageNotificationEnabled);
                if (m.isGroupReactionsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isGroupReactionsNotificationEnabled"))
                    w.uint32(224).bool(m.isGroupReactionsNotificationEnabled);
                if (m.isStatusNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isStatusNotificationEnabled"))
                    w.uint32(232).bool(m.isStatusNotificationEnabled);
                if (m.statusNotificationToneId != null && $Object.hasOwnProperty.call(m, "statusNotificationToneId"))
                    w.uint32(240).int32(m.statusNotificationToneId);
                if (m.shouldPlaySoundForCallNotification != null && $Object.hasOwnProperty.call(m, "shouldPlaySoundForCallNotification"))
                    w.uint32(248).bool(m.shouldPlaySoundForCallNotification);
                if (m.chatThemeId != null && $Object.hasOwnProperty.call(m, "chatThemeId"))
                    w.uint32(258).string(m.chatThemeId);
                if (m.colorSchemeId != null && $Object.hasOwnProperty.call(m, "colorSchemeId"))
                    w.uint32(266).string(m.colorSchemeId);
                if (m.stockWallpaperImageId != null && $Object.hasOwnProperty.call(m, "stockWallpaperImageId"))
                    w.uint32(274).string(m.stockWallpaperImageId);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SettingsSyncAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SettingsSyncAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.startAtLogin = r.bool();
                            m._startAtLogin = "startAtLogin";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.minimizeToTray = r.bool();
                            m._minimizeToTray = "minimizeToTray";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.language = r.stringVerify();
                            m._language = "language";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.replaceTextWithEmoji = r.bool();
                            m._replaceTextWithEmoji = "replaceTextWithEmoji";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.bannerNotificationDisplayMode = r.int32();
                            m._bannerNotificationDisplayMode = "bannerNotificationDisplayMode";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.unreadCounterBadgeDisplayMode = r.int32();
                            m._unreadCounterBadgeDisplayMode = "unreadCounterBadgeDisplayMode";
                            continue;
                        }
                    case 7: {
                            if (u !== 0)
                                break;
                            m.isMessagesNotificationEnabled = r.bool();
                            m._isMessagesNotificationEnabled = "isMessagesNotificationEnabled";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.isCallsNotificationEnabled = r.bool();
                            m._isCallsNotificationEnabled = "isCallsNotificationEnabled";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.isReactionsNotificationEnabled = r.bool();
                            m._isReactionsNotificationEnabled = "isReactionsNotificationEnabled";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.isStatusReactionsNotificationEnabled = r.bool();
                            m._isStatusReactionsNotificationEnabled = "isStatusReactionsNotificationEnabled";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.isTextPreviewForNotificationEnabled = r.bool();
                            m._isTextPreviewForNotificationEnabled = "isTextPreviewForNotificationEnabled";
                            continue;
                        }
                    case 12: {
                            if (u !== 0)
                                break;
                            m.defaultNotificationToneId = r.int32();
                            m._defaultNotificationToneId = "defaultNotificationToneId";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            m.groupDefaultNotificationToneId = r.int32();
                            m._groupDefaultNotificationToneId = "groupDefaultNotificationToneId";
                            continue;
                        }
                    case 14: {
                            if (u !== 0)
                                break;
                            m.appTheme = r.int32();
                            m._appTheme = "appTheme";
                            continue;
                        }
                    case 15: {
                            if (u !== 0)
                                break;
                            m.wallpaperId = r.int32();
                            m._wallpaperId = "wallpaperId";
                            continue;
                        }
                    case 16: {
                            if (u !== 0)
                                break;
                            m.isDoodleWallpaperEnabled = r.bool();
                            m._isDoodleWallpaperEnabled = "isDoodleWallpaperEnabled";
                            continue;
                        }
                    case 17: {
                            if (u !== 0)
                                break;
                            m.fontSize = r.int32();
                            m._fontSize = "fontSize";
                            continue;
                        }
                    case 18: {
                            if (u !== 0)
                                break;
                            m.isPhotosAutodownloadEnabled = r.bool();
                            m._isPhotosAutodownloadEnabled = "isPhotosAutodownloadEnabled";
                            continue;
                        }
                    case 19: {
                            if (u !== 0)
                                break;
                            m.isAudiosAutodownloadEnabled = r.bool();
                            m._isAudiosAutodownloadEnabled = "isAudiosAutodownloadEnabled";
                            continue;
                        }
                    case 20: {
                            if (u !== 0)
                                break;
                            m.isVideosAutodownloadEnabled = r.bool();
                            m._isVideosAutodownloadEnabled = "isVideosAutodownloadEnabled";
                            continue;
                        }
                    case 21: {
                            if (u !== 0)
                                break;
                            m.isDocumentsAutodownloadEnabled = r.bool();
                            m._isDocumentsAutodownloadEnabled = "isDocumentsAutodownloadEnabled";
                            continue;
                        }
                    case 22: {
                            if (u !== 0)
                                break;
                            m.disableLinkPreviews = r.bool();
                            m._disableLinkPreviews = "disableLinkPreviews";
                            continue;
                        }
                    case 23: {
                            if (u !== 0)
                                break;
                            m.notificationToneId = r.int32();
                            m._notificationToneId = "notificationToneId";
                            continue;
                        }
                    case 24: {
                            if (u !== 0)
                                break;
                            m.mediaUploadQuality = r.int32();
                            m._mediaUploadQuality = "mediaUploadQuality";
                            continue;
                        }
                    case 25: {
                            if (u !== 0)
                                break;
                            m.isSpellCheckEnabled = r.bool();
                            m._isSpellCheckEnabled = "isSpellCheckEnabled";
                            continue;
                        }
                    case 26: {
                            if (u !== 0)
                                break;
                            m.isEnterToSendEnabled = r.bool();
                            m._isEnterToSendEnabled = "isEnterToSendEnabled";
                            continue;
                        }
                    case 27: {
                            if (u !== 0)
                                break;
                            m.isGroupMessageNotificationEnabled = r.bool();
                            m._isGroupMessageNotificationEnabled = "isGroupMessageNotificationEnabled";
                            continue;
                        }
                    case 28: {
                            if (u !== 0)
                                break;
                            m.isGroupReactionsNotificationEnabled = r.bool();
                            m._isGroupReactionsNotificationEnabled = "isGroupReactionsNotificationEnabled";
                            continue;
                        }
                    case 29: {
                            if (u !== 0)
                                break;
                            m.isStatusNotificationEnabled = r.bool();
                            m._isStatusNotificationEnabled = "isStatusNotificationEnabled";
                            continue;
                        }
                    case 30: {
                            if (u !== 0)
                                break;
                            m.statusNotificationToneId = r.int32();
                            m._statusNotificationToneId = "statusNotificationToneId";
                            continue;
                        }
                    case 31: {
                            if (u !== 0)
                                break;
                            m.shouldPlaySoundForCallNotification = r.bool();
                            m._shouldPlaySoundForCallNotification = "shouldPlaySoundForCallNotification";
                            continue;
                        }
                    case 32: {
                            if (u !== 2)
                                break;
                            m.chatThemeId = r.stringVerify();
                            m._chatThemeId = "chatThemeId";
                            continue;
                        }
                    case 33: {
                            if (u !== 2)
                                break;
                            m.colorSchemeId = r.stringVerify();
                            m._colorSchemeId = "colorSchemeId";
                            continue;
                        }
                    case 34: {
                            if (u !== 2)
                                break;
                            m.stockWallpaperImageId = r.stringVerify();
                            m._stockWallpaperImageId = "stockWallpaperImageId";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SettingsSyncAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.SettingsSyncAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.SettingsSyncAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.SettingsSyncAction();
                if (d.startAtLogin != null) {
                    m.startAtLogin = $Boolean(d.startAtLogin);
                }
                if (d.minimizeToTray != null) {
                    m.minimizeToTray = $Boolean(d.minimizeToTray);
                }
                if (d.language != null) {
                    m.language = $String(d.language);
                }
                if (d.replaceTextWithEmoji != null) {
                    m.replaceTextWithEmoji = $Boolean(d.replaceTextWithEmoji);
                }
                switch (d.bannerNotificationDisplayMode) {
                case "DISPLAY_MODE_UNKNOWN":
                case 0:
                    m.bannerNotificationDisplayMode = 0;
                    break;
                case "ALWAYS":
                case 1:
                    m.bannerNotificationDisplayMode = 1;
                    break;
                case "NEVER":
                case 2:
                    m.bannerNotificationDisplayMode = 2;
                    break;
                case "ONLY_WHEN_APP_IS_OPEN":
                case 3:
                    m.bannerNotificationDisplayMode = 3;
                    break;
                default:
                    if (typeof d.bannerNotificationDisplayMode === "number" && (d.bannerNotificationDisplayMode | 0) === d.bannerNotificationDisplayMode)
                        m.bannerNotificationDisplayMode = d.bannerNotificationDisplayMode;
                }
                switch (d.unreadCounterBadgeDisplayMode) {
                case "DISPLAY_MODE_UNKNOWN":
                case 0:
                    m.unreadCounterBadgeDisplayMode = 0;
                    break;
                case "ALWAYS":
                case 1:
                    m.unreadCounterBadgeDisplayMode = 1;
                    break;
                case "NEVER":
                case 2:
                    m.unreadCounterBadgeDisplayMode = 2;
                    break;
                case "ONLY_WHEN_APP_IS_OPEN":
                case 3:
                    m.unreadCounterBadgeDisplayMode = 3;
                    break;
                default:
                    if (typeof d.unreadCounterBadgeDisplayMode === "number" && (d.unreadCounterBadgeDisplayMode | 0) === d.unreadCounterBadgeDisplayMode)
                        m.unreadCounterBadgeDisplayMode = d.unreadCounterBadgeDisplayMode;
                }
                if (d.isMessagesNotificationEnabled != null) {
                    m.isMessagesNotificationEnabled = $Boolean(d.isMessagesNotificationEnabled);
                }
                if (d.isCallsNotificationEnabled != null) {
                    m.isCallsNotificationEnabled = $Boolean(d.isCallsNotificationEnabled);
                }
                if (d.isReactionsNotificationEnabled != null) {
                    m.isReactionsNotificationEnabled = $Boolean(d.isReactionsNotificationEnabled);
                }
                if (d.isStatusReactionsNotificationEnabled != null) {
                    m.isStatusReactionsNotificationEnabled = $Boolean(d.isStatusReactionsNotificationEnabled);
                }
                if (d.isTextPreviewForNotificationEnabled != null) {
                    m.isTextPreviewForNotificationEnabled = $Boolean(d.isTextPreviewForNotificationEnabled);
                }
                if (d.defaultNotificationToneId != null) {
                    m.defaultNotificationToneId = d.defaultNotificationToneId | 0;
                }
                if (d.groupDefaultNotificationToneId != null) {
                    m.groupDefaultNotificationToneId = d.groupDefaultNotificationToneId | 0;
                }
                if (d.appTheme != null) {
                    m.appTheme = d.appTheme | 0;
                }
                if (d.wallpaperId != null) {
                    m.wallpaperId = d.wallpaperId | 0;
                }
                if (d.isDoodleWallpaperEnabled != null) {
                    m.isDoodleWallpaperEnabled = $Boolean(d.isDoodleWallpaperEnabled);
                }
                if (d.fontSize != null) {
                    m.fontSize = d.fontSize | 0;
                }
                if (d.isPhotosAutodownloadEnabled != null) {
                    m.isPhotosAutodownloadEnabled = $Boolean(d.isPhotosAutodownloadEnabled);
                }
                if (d.isAudiosAutodownloadEnabled != null) {
                    m.isAudiosAutodownloadEnabled = $Boolean(d.isAudiosAutodownloadEnabled);
                }
                if (d.isVideosAutodownloadEnabled != null) {
                    m.isVideosAutodownloadEnabled = $Boolean(d.isVideosAutodownloadEnabled);
                }
                if (d.isDocumentsAutodownloadEnabled != null) {
                    m.isDocumentsAutodownloadEnabled = $Boolean(d.isDocumentsAutodownloadEnabled);
                }
                if (d.disableLinkPreviews != null) {
                    m.disableLinkPreviews = $Boolean(d.disableLinkPreviews);
                }
                if (d.notificationToneId != null) {
                    m.notificationToneId = d.notificationToneId | 0;
                }
                switch (d.mediaUploadQuality) {
                case "MEDIA_QUALITY_UNKNOWN":
                case 0:
                    m.mediaUploadQuality = 0;
                    break;
                case "STANDARD":
                case 1:
                    m.mediaUploadQuality = 1;
                    break;
                case "HD":
                case 2:
                    m.mediaUploadQuality = 2;
                    break;
                default:
                    if (typeof d.mediaUploadQuality === "number" && (d.mediaUploadQuality | 0) === d.mediaUploadQuality)
                        m.mediaUploadQuality = d.mediaUploadQuality;
                }
                if (d.isSpellCheckEnabled != null) {
                    m.isSpellCheckEnabled = $Boolean(d.isSpellCheckEnabled);
                }
                if (d.isEnterToSendEnabled != null) {
                    m.isEnterToSendEnabled = $Boolean(d.isEnterToSendEnabled);
                }
                if (d.isGroupMessageNotificationEnabled != null) {
                    m.isGroupMessageNotificationEnabled = $Boolean(d.isGroupMessageNotificationEnabled);
                }
                if (d.isGroupReactionsNotificationEnabled != null) {
                    m.isGroupReactionsNotificationEnabled = $Boolean(d.isGroupReactionsNotificationEnabled);
                }
                if (d.isStatusNotificationEnabled != null) {
                    m.isStatusNotificationEnabled = $Boolean(d.isStatusNotificationEnabled);
                }
                if (d.statusNotificationToneId != null) {
                    m.statusNotificationToneId = d.statusNotificationToneId | 0;
                }
                if (d.shouldPlaySoundForCallNotification != null) {
                    m.shouldPlaySoundForCallNotification = $Boolean(d.shouldPlaySoundForCallNotification);
                }
                if (d.chatThemeId != null) {
                    m.chatThemeId = $String(d.chatThemeId);
                }
                if (d.colorSchemeId != null) {
                    m.colorSchemeId = $String(d.colorSchemeId);
                }
                if (d.stockWallpaperImageId != null) {
                    m.stockWallpaperImageId = $String(d.stockWallpaperImageId);
                }
                return m;
            };

            SettingsSyncAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.startAtLogin != null && $Object.hasOwnProperty.call(m, "startAtLogin")) {
                    d.startAtLogin = m.startAtLogin;
                }
                if (m.minimizeToTray != null && $Object.hasOwnProperty.call(m, "minimizeToTray")) {
                    d.minimizeToTray = m.minimizeToTray;
                }
                if (m.language != null && $Object.hasOwnProperty.call(m, "language")) {
                    d.language = m.language;
                }
                if (m.replaceTextWithEmoji != null && $Object.hasOwnProperty.call(m, "replaceTextWithEmoji")) {
                    d.replaceTextWithEmoji = m.replaceTextWithEmoji;
                }
                if (m.bannerNotificationDisplayMode != null && $Object.hasOwnProperty.call(m, "bannerNotificationDisplayMode")) {
                    d.bannerNotificationDisplayMode = o.enums === $String ? $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[m.bannerNotificationDisplayMode] === $undefined ? m.bannerNotificationDisplayMode : $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[m.bannerNotificationDisplayMode] : m.bannerNotificationDisplayMode;
                }
                if (m.unreadCounterBadgeDisplayMode != null && $Object.hasOwnProperty.call(m, "unreadCounterBadgeDisplayMode")) {
                    d.unreadCounterBadgeDisplayMode = o.enums === $String ? $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[m.unreadCounterBadgeDisplayMode] === $undefined ? m.unreadCounterBadgeDisplayMode : $root.proto.SyncActionValue.SettingsSyncAction.DisplayMode[m.unreadCounterBadgeDisplayMode] : m.unreadCounterBadgeDisplayMode;
                }
                if (m.isMessagesNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isMessagesNotificationEnabled")) {
                    d.isMessagesNotificationEnabled = m.isMessagesNotificationEnabled;
                }
                if (m.isCallsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isCallsNotificationEnabled")) {
                    d.isCallsNotificationEnabled = m.isCallsNotificationEnabled;
                }
                if (m.isReactionsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isReactionsNotificationEnabled")) {
                    d.isReactionsNotificationEnabled = m.isReactionsNotificationEnabled;
                }
                if (m.isStatusReactionsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isStatusReactionsNotificationEnabled")) {
                    d.isStatusReactionsNotificationEnabled = m.isStatusReactionsNotificationEnabled;
                }
                if (m.isTextPreviewForNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isTextPreviewForNotificationEnabled")) {
                    d.isTextPreviewForNotificationEnabled = m.isTextPreviewForNotificationEnabled;
                }
                if (m.defaultNotificationToneId != null && $Object.hasOwnProperty.call(m, "defaultNotificationToneId")) {
                    d.defaultNotificationToneId = m.defaultNotificationToneId;
                }
                if (m.groupDefaultNotificationToneId != null && $Object.hasOwnProperty.call(m, "groupDefaultNotificationToneId")) {
                    d.groupDefaultNotificationToneId = m.groupDefaultNotificationToneId;
                }
                if (m.appTheme != null && $Object.hasOwnProperty.call(m, "appTheme")) {
                    d.appTheme = m.appTheme;
                }
                if (m.wallpaperId != null && $Object.hasOwnProperty.call(m, "wallpaperId")) {
                    d.wallpaperId = m.wallpaperId;
                }
                if (m.isDoodleWallpaperEnabled != null && $Object.hasOwnProperty.call(m, "isDoodleWallpaperEnabled")) {
                    d.isDoodleWallpaperEnabled = m.isDoodleWallpaperEnabled;
                }
                if (m.fontSize != null && $Object.hasOwnProperty.call(m, "fontSize")) {
                    d.fontSize = m.fontSize;
                }
                if (m.isPhotosAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isPhotosAutodownloadEnabled")) {
                    d.isPhotosAutodownloadEnabled = m.isPhotosAutodownloadEnabled;
                }
                if (m.isAudiosAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isAudiosAutodownloadEnabled")) {
                    d.isAudiosAutodownloadEnabled = m.isAudiosAutodownloadEnabled;
                }
                if (m.isVideosAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isVideosAutodownloadEnabled")) {
                    d.isVideosAutodownloadEnabled = m.isVideosAutodownloadEnabled;
                }
                if (m.isDocumentsAutodownloadEnabled != null && $Object.hasOwnProperty.call(m, "isDocumentsAutodownloadEnabled")) {
                    d.isDocumentsAutodownloadEnabled = m.isDocumentsAutodownloadEnabled;
                }
                if (m.disableLinkPreviews != null && $Object.hasOwnProperty.call(m, "disableLinkPreviews")) {
                    d.disableLinkPreviews = m.disableLinkPreviews;
                }
                if (m.notificationToneId != null && $Object.hasOwnProperty.call(m, "notificationToneId")) {
                    d.notificationToneId = m.notificationToneId;
                }
                if (m.mediaUploadQuality != null && $Object.hasOwnProperty.call(m, "mediaUploadQuality")) {
                    d.mediaUploadQuality = o.enums === $String ? $root.proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting[m.mediaUploadQuality] === $undefined ? m.mediaUploadQuality : $root.proto.SyncActionValue.SettingsSyncAction.MediaQualitySetting[m.mediaUploadQuality] : m.mediaUploadQuality;
                }
                if (m.isSpellCheckEnabled != null && $Object.hasOwnProperty.call(m, "isSpellCheckEnabled")) {
                    d.isSpellCheckEnabled = m.isSpellCheckEnabled;
                }
                if (m.isEnterToSendEnabled != null && $Object.hasOwnProperty.call(m, "isEnterToSendEnabled")) {
                    d.isEnterToSendEnabled = m.isEnterToSendEnabled;
                }
                if (m.isGroupMessageNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isGroupMessageNotificationEnabled")) {
                    d.isGroupMessageNotificationEnabled = m.isGroupMessageNotificationEnabled;
                }
                if (m.isGroupReactionsNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isGroupReactionsNotificationEnabled")) {
                    d.isGroupReactionsNotificationEnabled = m.isGroupReactionsNotificationEnabled;
                }
                if (m.isStatusNotificationEnabled != null && $Object.hasOwnProperty.call(m, "isStatusNotificationEnabled")) {
                    d.isStatusNotificationEnabled = m.isStatusNotificationEnabled;
                }
                if (m.statusNotificationToneId != null && $Object.hasOwnProperty.call(m, "statusNotificationToneId")) {
                    d.statusNotificationToneId = m.statusNotificationToneId;
                }
                if (m.shouldPlaySoundForCallNotification != null && $Object.hasOwnProperty.call(m, "shouldPlaySoundForCallNotification")) {
                    d.shouldPlaySoundForCallNotification = m.shouldPlaySoundForCallNotification;
                }
                if (m.chatThemeId != null && $Object.hasOwnProperty.call(m, "chatThemeId")) {
                    d.chatThemeId = m.chatThemeId;
                }
                if (m.colorSchemeId != null && $Object.hasOwnProperty.call(m, "colorSchemeId")) {
                    d.colorSchemeId = m.colorSchemeId;
                }
                if (m.stockWallpaperImageId != null && $Object.hasOwnProperty.call(m, "stockWallpaperImageId")) {
                    d.stockWallpaperImageId = m.stockWallpaperImageId;
                }
                return d;
            };

            SettingsSyncAction.prototype.toJSON = function() {
                return SettingsSyncAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            SettingsSyncAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.SettingsSyncAction";
            };

            SettingsSyncAction.DisplayMode = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "DISPLAY_MODE_UNKNOWN"] = 0;
                values[valuesById[1] = "ALWAYS"] = 1;
                values[valuesById[2] = "NEVER"] = 2;
                values[valuesById[3] = "ONLY_WHEN_APP_IS_OPEN"] = 3;
                return values;
            })();

            SettingsSyncAction.MediaQualitySetting = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "MEDIA_QUALITY_UNKNOWN"] = 0;
                values[valuesById[1] = "STANDARD"] = 1;
                values[valuesById[2] = "HD"] = 2;
                return values;
            })();

            SettingsSyncAction.SettingKey = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "SETTING_KEY_UNKNOWN"] = 0;
                values[valuesById[1] = "START_AT_LOGIN"] = 1;
                values[valuesById[2] = "MINIMIZE_TO_TRAY"] = 2;
                values[valuesById[3] = "LANGUAGE"] = 3;
                values[valuesById[4] = "REPLACE_TEXT_WITH_EMOJI"] = 4;
                values[valuesById[5] = "BANNER_NOTIFICATION_DISPLAY_MODE"] = 5;
                values[valuesById[6] = "UNREAD_COUNTER_BADGE_DISPLAY_MODE"] = 6;
                values[valuesById[7] = "IS_MESSAGES_NOTIFICATION_ENABLED"] = 7;
                values[valuesById[8] = "IS_CALLS_NOTIFICATION_ENABLED"] = 8;
                values[valuesById[9] = "IS_REACTIONS_NOTIFICATION_ENABLED"] = 9;
                values[valuesById[10] = "IS_STATUS_REACTIONS_NOTIFICATION_ENABLED"] = 10;
                values[valuesById[11] = "IS_TEXT_PREVIEW_FOR_NOTIFICATION_ENABLED"] = 11;
                values[valuesById[12] = "DEFAULT_NOTIFICATION_TONE_ID"] = 12;
                values[valuesById[13] = "GROUP_DEFAULT_NOTIFICATION_TONE_ID"] = 13;
                values[valuesById[14] = "APP_THEME"] = 14;
                values[valuesById[15] = "WALLPAPER_ID"] = 15;
                values[valuesById[16] = "IS_DOODLE_WALLPAPER_ENABLED"] = 16;
                values[valuesById[17] = "FONT_SIZE"] = 17;
                values[valuesById[18] = "IS_PHOTOS_AUTODOWNLOAD_ENABLED"] = 18;
                values[valuesById[19] = "IS_AUDIOS_AUTODOWNLOAD_ENABLED"] = 19;
                values[valuesById[20] = "IS_VIDEOS_AUTODOWNLOAD_ENABLED"] = 20;
                values[valuesById[21] = "IS_DOCUMENTS_AUTODOWNLOAD_ENABLED"] = 21;
                values[valuesById[22] = "DISABLE_LINK_PREVIEWS"] = 22;
                values[valuesById[23] = "NOTIFICATION_TONE_ID"] = 23;
                values[valuesById[24] = "MEDIA_UPLOAD_QUALITY"] = 24;
                values[valuesById[25] = "IS_SPELL_CHECK_ENABLED"] = 25;
                values[valuesById[26] = "IS_ENTER_TO_SEND_ENABLED"] = 26;
                values[valuesById[27] = "IS_GROUP_MESSAGE_NOTIFICATION_ENABLED"] = 27;
                values[valuesById[28] = "IS_GROUP_REACTIONS_NOTIFICATION_ENABLED"] = 28;
                values[valuesById[29] = "IS_STATUS_NOTIFICATION_ENABLED"] = 29;
                values[valuesById[30] = "STATUS_NOTIFICATION_TONE_ID"] = 30;
                values[valuesById[31] = "SHOULD_PLAY_SOUND_FOR_CALL_NOTIFICATION"] = 31;
                values[valuesById[32] = "CHAT_THEME_ID"] = 32;
                values[valuesById[33] = "COLOR_SCHEME_ID"] = 33;
                values[valuesById[34] = "STOCK_WALLPAPER_IMAGE_ID"] = 34;
                return values;
            })();

            SettingsSyncAction.SettingPlatform = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "PLATFORM_UNKNOWN"] = 0;
                values[valuesById[1] = "WEB"] = 1;
                values[valuesById[2] = "HYBRID"] = 2;
                values[valuesById[3] = "WINDOWS"] = 3;
                values[valuesById[4] = "MAC"] = 4;
                return values;
            })();

            return SettingsSyncAction;
        })();

        SyncActionValue.SharedDeviceAllowlistAction = (function() {

            const SharedDeviceAllowlistAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SharedDeviceAllowlistAction.prototype.allowed = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SharedDeviceAllowlistAction.prototype, "_allowed", {
                get: $util.oneOfGetter($oneOfFields = ["allowed"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SharedDeviceAllowlistAction.create = function(properties) {
                return new SharedDeviceAllowlistAction(properties);
            };

            SharedDeviceAllowlistAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.allowed != null && $Object.hasOwnProperty.call(m, "allowed"))
                    w.uint32(8).bool(m.allowed);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SharedDeviceAllowlistAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SharedDeviceAllowlistAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.allowed = r.bool();
                            m._allowed = "allowed";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SharedDeviceAllowlistAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.SharedDeviceAllowlistAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.SharedDeviceAllowlistAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.SharedDeviceAllowlistAction();
                if (d.allowed != null) {
                    m.allowed = $Boolean(d.allowed);
                }
                return m;
            };

            SharedDeviceAllowlistAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.allowed != null && $Object.hasOwnProperty.call(m, "allowed")) {
                    d.allowed = m.allowed;
                }
                return d;
            };

            SharedDeviceAllowlistAction.prototype.toJSON = function() {
                return SharedDeviceAllowlistAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            SharedDeviceAllowlistAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.SharedDeviceAllowlistAction";
            };

            return SharedDeviceAllowlistAction;
        })();

        SyncActionValue.StarAction = (function() {

            const StarAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StarAction.prototype.starred = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StarAction.prototype, "_starred", {
                get: $util.oneOfGetter($oneOfFields = ["starred"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StarAction.create = function(properties) {
                return new StarAction(properties);
            };

            StarAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.starred != null && $Object.hasOwnProperty.call(m, "starred"))
                    w.uint32(8).bool(m.starred);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StarAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.StarAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.starred = r.bool();
                            m._starred = "starred";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StarAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.StarAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.StarAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.StarAction();
                if (d.starred != null) {
                    m.starred = $Boolean(d.starred);
                }
                return m;
            };

            StarAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.starred != null && $Object.hasOwnProperty.call(m, "starred")) {
                    d.starred = m.starred;
                }
                return d;
            };

            StarAction.prototype.toJSON = function() {
                return StarAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            StarAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.StarAction";
            };

            return StarAction;
        })();

        SyncActionValue.StatusPostOptInNotificationPreferencesAction = (function() {

            const StatusPostOptInNotificationPreferencesAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusPostOptInNotificationPreferencesAction.prototype.enabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusPostOptInNotificationPreferencesAction.prototype, "_enabled", {
                get: $util.oneOfGetter($oneOfFields = ["enabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusPostOptInNotificationPreferencesAction.create = function(properties) {
                return new StatusPostOptInNotificationPreferencesAction(properties);
            };

            StatusPostOptInNotificationPreferencesAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.enabled != null && $Object.hasOwnProperty.call(m, "enabled"))
                    w.uint32(8).bool(m.enabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusPostOptInNotificationPreferencesAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.enabled = r.bool();
                            m._enabled = "enabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusPostOptInNotificationPreferencesAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction();
                if (d.enabled != null) {
                    m.enabled = $Boolean(d.enabled);
                }
                return m;
            };

            StatusPostOptInNotificationPreferencesAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.enabled != null && $Object.hasOwnProperty.call(m, "enabled")) {
                    d.enabled = m.enabled;
                }
                return d;
            };

            StatusPostOptInNotificationPreferencesAction.prototype.toJSON = function() {
                return StatusPostOptInNotificationPreferencesAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusPostOptInNotificationPreferencesAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.StatusPostOptInNotificationPreferencesAction";
            };

            return StatusPostOptInNotificationPreferencesAction;
        })();

        SyncActionValue.StatusPrivacyAction = (function() {

            const StatusPrivacyAction = function (p) {
                this.userJid = [];
                this.customLists = [];
                this.modes = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StatusPrivacyAction.prototype.mode = null;
            StatusPrivacyAction.prototype.userJid = $util.emptyArray;
            StatusPrivacyAction.prototype.shareToFb = null;
            StatusPrivacyAction.prototype.shareToIg = null;
            StatusPrivacyAction.prototype.customLists = $util.emptyArray;
            StatusPrivacyAction.prototype.modes = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusPrivacyAction.prototype, "_mode", {
                get: $util.oneOfGetter($oneOfFields = ["mode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusPrivacyAction.prototype, "_shareToFb", {
                get: $util.oneOfGetter($oneOfFields = ["shareToFb"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StatusPrivacyAction.prototype, "_shareToIg", {
                get: $util.oneOfGetter($oneOfFields = ["shareToIg"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StatusPrivacyAction.create = function(properties) {
                return new StatusPrivacyAction(properties);
            };

            StatusPrivacyAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.mode != null && $Object.hasOwnProperty.call(m, "mode"))
                    w.uint32(8).int32(m.mode);
                if (m.userJid != null && m.userJid.length) {
                    for (var i = 0; i < m.userJid.length; ++i)
                        w.uint32(18).string(m.userJid[i]);
                }
                if (m.shareToFb != null && $Object.hasOwnProperty.call(m, "shareToFb"))
                    w.uint32(24).bool(m.shareToFb);
                if (m.shareToIg != null && $Object.hasOwnProperty.call(m, "shareToIg"))
                    w.uint32(32).bool(m.shareToIg);
                if (m.customLists != null && m.customLists.length) {
                    for (var i = 0; i < m.customLists.length; ++i)
                        $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.encode(m.customLists[i], w.uint32(42).fork(), q + 1).ldelim();
                }
                if (m.modes != null && m.modes.length) {
                    w.uint32(50).int32s(m.modes);
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StatusPrivacyAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.StatusPrivacyAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.mode = r.int32();
                            m._mode = "mode";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.userJid && m.userJid.length))
                                m.userJid = [];
                            m.userJid.push(r.stringVerify());
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.shareToFb = r.bool();
                            m._shareToFb = "shareToFb";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.shareToIg = r.bool();
                            m._shareToIg = "shareToIg";
                            continue;
                        }
                    case 5: {
                            if (u !== 2)
                                break;
                            if (!(m.customLists && m.customLists.length))
                                m.customLists = [];
                            m.customLists.push($root.proto.SyncActionValue.StatusPrivacyAction.CustomList.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 6: {
                            if (u === 2) {
                                if (!(m.modes && m.modes.length))
                                    m.modes = [];
                                r.int32s(m.modes);
                                continue;
                            }
                            if (u !== 0)
                                break;
                            if (!(m.modes && m.modes.length))
                                m.modes = [];
                            m.modes.push(r.int32());
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StatusPrivacyAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.StatusPrivacyAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.StatusPrivacyAction();
                switch (d.mode) {
                case "ALLOW_LIST":
                case 0:
                    m.mode = 0;
                    break;
                case "DENY_LIST":
                case 1:
                    m.mode = 1;
                    break;
                case "CONTACTS":
                case 2:
                    m.mode = 2;
                    break;
                case "CLOSE_FRIENDS":
                case 3:
                    m.mode = 3;
                    break;
                case "CUSTOM_LIST":
                case 4:
                    m.mode = 4;
                    break;
                default:
                    if (typeof d.mode === "number" && (d.mode | 0) === d.mode)
                        m.mode = d.mode;
                }
                if (d.userJid) {
                    if (!$Array.isArray(d.userJid))
                        throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction.userJid: array expected");
                    m.userJid = $Array(d.userJid.length);
                    for (var i = 0; i < d.userJid.length; ++i) {
                        m.userJid[i] = $String(d.userJid[i]);
                    }
                }
                if (d.shareToFb != null) {
                    m.shareToFb = $Boolean(d.shareToFb);
                }
                if (d.shareToIg != null) {
                    m.shareToIg = $Boolean(d.shareToIg);
                }
                if (d.customLists) {
                    if (!$Array.isArray(d.customLists))
                        throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction.customLists: array expected");
                    m.customLists = $Array(d.customLists.length);
                    for (var i = 0; i < d.customLists.length; ++i) {
                        if (!$util.isObject(d.customLists[i]))
                            throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction.customLists: object expected");
                        m.customLists[i] = $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.fromObject(d.customLists[i], q + 1);
                    }
                }
                if (d.modes) {
                    if (!$Array.isArray(d.modes))
                        throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction.modes: array expected");
                    m.modes = [];
                    for (var i = 0; i < d.modes.length; ++i) {
                        switch (d.modes[i]) {
                        case "ALLOW_LIST":
                        case 0:
                            m.modes[m.modes.length] = 0;
                            break;
                        case "DENY_LIST":
                        case 1:
                            m.modes[m.modes.length] = 1;
                            break;
                        case "CONTACTS":
                        case 2:
                            m.modes[m.modes.length] = 2;
                            break;
                        case "CLOSE_FRIENDS":
                        case 3:
                            m.modes[m.modes.length] = 3;
                            break;
                        case "CUSTOM_LIST":
                        case 4:
                            m.modes[m.modes.length] = 4;
                            break;
                        default:
                            if (typeof d.modes[i] === "number" && (d.modes[i] | 0) === d.modes[i])
                                m.modes[m.modes.length] = d.modes[i];
                        }
                    }
                }
                return m;
            };

            StatusPrivacyAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.userJid = [];
                    d.customLists = [];
                    d.modes = [];
                }
                if (m.mode != null && $Object.hasOwnProperty.call(m, "mode")) {
                    d.mode = o.enums === $String ? $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[m.mode] === $undefined ? m.mode : $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[m.mode] : m.mode;
                }
                if (m.userJid && m.userJid.length) {
                    d.userJid = $Array(m.userJid.length);
                    for (var j = 0; j < m.userJid.length; ++j) {
                        d.userJid[j] = m.userJid[j];
                    }
                }
                if (m.shareToFb != null && $Object.hasOwnProperty.call(m, "shareToFb")) {
                    d.shareToFb = m.shareToFb;
                }
                if (m.shareToIg != null && $Object.hasOwnProperty.call(m, "shareToIg")) {
                    d.shareToIg = m.shareToIg;
                }
                if (m.customLists && m.customLists.length) {
                    d.customLists = $Array(m.customLists.length);
                    for (var j = 0; j < m.customLists.length; ++j) {
                        d.customLists[j] = $root.proto.SyncActionValue.StatusPrivacyAction.CustomList.toObject(m.customLists[j], o, q + 1);
                    }
                }
                if (m.modes && m.modes.length) {
                    d.modes = $Array(m.modes.length);
                    for (var j = 0; j < m.modes.length; ++j) {
                        d.modes[j] = o.enums === $String ? $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[m.modes[j]] === $undefined ? m.modes[j] : $root.proto.SyncActionValue.StatusPrivacyAction.StatusDistributionMode[m.modes[j]] : m.modes[j];
                    }
                }
                return d;
            };

            StatusPrivacyAction.prototype.toJSON = function() {
                return StatusPrivacyAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            StatusPrivacyAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.StatusPrivacyAction";
            };

            StatusPrivacyAction.CustomList = (function() {

                const CustomList = function (p) {
                    this.userJid = [];
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                CustomList.prototype.listId = null;
                CustomList.prototype.name = null;
                CustomList.prototype.emoji = null;
                CustomList.prototype.isSelected = null;
                CustomList.prototype.userJid = $util.emptyArray;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CustomList.prototype, "_listId", {
                    get: $util.oneOfGetter($oneOfFields = ["listId"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CustomList.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CustomList.prototype, "_emoji", {
                    get: $util.oneOfGetter($oneOfFields = ["emoji"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(CustomList.prototype, "_isSelected", {
                    get: $util.oneOfGetter($oneOfFields = ["isSelected"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                CustomList.create = function(properties) {
                    return new CustomList(properties);
                };

                CustomList.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.listId != null && $Object.hasOwnProperty.call(m, "listId"))
                        w.uint32(10).string(m.listId);
                    if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                        w.uint32(18).string(m.name);
                    if (m.emoji != null && $Object.hasOwnProperty.call(m, "emoji"))
                        w.uint32(26).string(m.emoji);
                    if (m.isSelected != null && $Object.hasOwnProperty.call(m, "isSelected"))
                        w.uint32(32).bool(m.isSelected);
                    if (m.userJid != null && m.userJid.length) {
                        for (var i = 0; i < m.userJid.length; ++i)
                            w.uint32(42).string(m.userJid[i]);
                    }
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                CustomList.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.StatusPrivacyAction.CustomList();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.listId = r.stringVerify();
                                m._listId = "listId";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.name = r.stringVerify();
                                m._name = "name";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.emoji = r.stringVerify();
                                m._emoji = "emoji";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.isSelected = r.bool();
                                m._isSelected = "isSelected";
                                continue;
                            }
                        case 5: {
                                if (u !== 2)
                                    break;
                                if (!(m.userJid && m.userJid.length))
                                    m.userJid = [];
                                m.userJid.push(r.stringVerify());
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                CustomList.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SyncActionValue.StatusPrivacyAction.CustomList)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction.CustomList: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SyncActionValue.StatusPrivacyAction.CustomList();
                    if (d.listId != null) {
                        m.listId = $String(d.listId);
                    }
                    if (d.name != null) {
                        m.name = $String(d.name);
                    }
                    if (d.emoji != null) {
                        m.emoji = $String(d.emoji);
                    }
                    if (d.isSelected != null) {
                        m.isSelected = $Boolean(d.isSelected);
                    }
                    if (d.userJid) {
                        if (!$Array.isArray(d.userJid))
                            throw $TypeError(".proto.SyncActionValue.StatusPrivacyAction.CustomList.userJid: array expected");
                        m.userJid = $Array(d.userJid.length);
                        for (var i = 0; i < d.userJid.length; ++i) {
                            m.userJid[i] = $String(d.userJid[i]);
                        }
                    }
                    return m;
                };

                CustomList.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (o.arrays || o.defaults) {
                        d.userJid = [];
                    }
                    if (m.listId != null && $Object.hasOwnProperty.call(m, "listId")) {
                        d.listId = m.listId;
                    }
                    if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                        d.name = m.name;
                    }
                    if (m.emoji != null && $Object.hasOwnProperty.call(m, "emoji")) {
                        d.emoji = m.emoji;
                    }
                    if (m.isSelected != null && $Object.hasOwnProperty.call(m, "isSelected")) {
                        d.isSelected = m.isSelected;
                    }
                    if (m.userJid && m.userJid.length) {
                        d.userJid = $Array(m.userJid.length);
                        for (var j = 0; j < m.userJid.length; ++j) {
                            d.userJid[j] = m.userJid[j];
                        }
                    }
                    return d;
                };

                CustomList.prototype.toJSON = function() {
                    return CustomList.toObject(this, $protobuf.util.toJSONOptions);
                };

                CustomList.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SyncActionValue.StatusPrivacyAction.CustomList";
                };

                return CustomList;
            })();

            StatusPrivacyAction.StatusDistributionMode = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "ALLOW_LIST"] = 0;
                values[valuesById[1] = "DENY_LIST"] = 1;
                values[valuesById[2] = "CONTACTS"] = 2;
                values[valuesById[3] = "CLOSE_FRIENDS"] = 3;
                values[valuesById[4] = "CUSTOM_LIST"] = 4;
                return values;
            })();

            return StatusPrivacyAction;
        })();

        SyncActionValue.StickerAction = (function() {

            const StickerAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            StickerAction.prototype.url = null;
            StickerAction.prototype.fileEncSha256 = null;
            StickerAction.prototype.mediaKey = null;
            StickerAction.prototype.mimetype = null;
            StickerAction.prototype.height = null;
            StickerAction.prototype.width = null;
            StickerAction.prototype.directPath = null;
            StickerAction.prototype.fileLength = null;
            StickerAction.prototype.isFavorite = null;
            StickerAction.prototype.deviceIdHint = null;
            StickerAction.prototype.isLottie = null;
            StickerAction.prototype.imageHash = null;
            StickerAction.prototype.isAvatarSticker = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_fileEncSha256", {
                get: $util.oneOfGetter($oneOfFields = ["fileEncSha256"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_mediaKey", {
                get: $util.oneOfGetter($oneOfFields = ["mediaKey"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_mimetype", {
                get: $util.oneOfGetter($oneOfFields = ["mimetype"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_height", {
                get: $util.oneOfGetter($oneOfFields = ["height"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_width", {
                get: $util.oneOfGetter($oneOfFields = ["width"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_directPath", {
                get: $util.oneOfGetter($oneOfFields = ["directPath"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_fileLength", {
                get: $util.oneOfGetter($oneOfFields = ["fileLength"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_isFavorite", {
                get: $util.oneOfGetter($oneOfFields = ["isFavorite"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_deviceIdHint", {
                get: $util.oneOfGetter($oneOfFields = ["deviceIdHint"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_isLottie", {
                get: $util.oneOfGetter($oneOfFields = ["isLottie"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_imageHash", {
                get: $util.oneOfGetter($oneOfFields = ["imageHash"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(StickerAction.prototype, "_isAvatarSticker", {
                get: $util.oneOfGetter($oneOfFields = ["isAvatarSticker"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            StickerAction.create = function(properties) {
                return new StickerAction(properties);
            };

            StickerAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    w.uint32(10).string(m.url);
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256"))
                    w.uint32(18).bytes(m.fileEncSha256);
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey"))
                    w.uint32(26).bytes(m.mediaKey);
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype"))
                    w.uint32(34).string(m.mimetype);
                if (m.height != null && $Object.hasOwnProperty.call(m, "height"))
                    w.uint32(40).uint32(m.height);
                if (m.width != null && $Object.hasOwnProperty.call(m, "width"))
                    w.uint32(48).uint32(m.width);
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath"))
                    w.uint32(58).string(m.directPath);
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength"))
                    w.uint32(64).uint64(m.fileLength);
                if (m.isFavorite != null && $Object.hasOwnProperty.call(m, "isFavorite"))
                    w.uint32(72).bool(m.isFavorite);
                if (m.deviceIdHint != null && $Object.hasOwnProperty.call(m, "deviceIdHint"))
                    w.uint32(80).uint32(m.deviceIdHint);
                if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie"))
                    w.uint32(88).bool(m.isLottie);
                if (m.imageHash != null && $Object.hasOwnProperty.call(m, "imageHash"))
                    w.uint32(98).string(m.imageHash);
                if (m.isAvatarSticker != null && $Object.hasOwnProperty.call(m, "isAvatarSticker"))
                    w.uint32(104).bool(m.isAvatarSticker);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            StickerAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.StickerAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.url = r.stringVerify();
                            m._url = "url";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.fileEncSha256 = r.bytes();
                            m._fileEncSha256 = "fileEncSha256";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.mediaKey = r.bytes();
                            m._mediaKey = "mediaKey";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.mimetype = r.stringVerify();
                            m._mimetype = "mimetype";
                            continue;
                        }
                    case 5: {
                            if (u !== 0)
                                break;
                            m.height = r.uint32();
                            m._height = "height";
                            continue;
                        }
                    case 6: {
                            if (u !== 0)
                                break;
                            m.width = r.uint32();
                            m._width = "width";
                            continue;
                        }
                    case 7: {
                            if (u !== 2)
                                break;
                            m.directPath = r.stringVerify();
                            m._directPath = "directPath";
                            continue;
                        }
                    case 8: {
                            if (u !== 0)
                                break;
                            m.fileLength = r.uint64();
                            m._fileLength = "fileLength";
                            continue;
                        }
                    case 9: {
                            if (u !== 0)
                                break;
                            m.isFavorite = r.bool();
                            m._isFavorite = "isFavorite";
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.deviceIdHint = r.uint32();
                            m._deviceIdHint = "deviceIdHint";
                            continue;
                        }
                    case 11: {
                            if (u !== 0)
                                break;
                            m.isLottie = r.bool();
                            m._isLottie = "isLottie";
                            continue;
                        }
                    case 12: {
                            if (u !== 2)
                                break;
                            m.imageHash = r.stringVerify();
                            m._imageHash = "imageHash";
                            continue;
                        }
                    case 13: {
                            if (u !== 0)
                                break;
                            m.isAvatarSticker = r.bool();
                            m._isAvatarSticker = "isAvatarSticker";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            StickerAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.StickerAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.StickerAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.StickerAction();
                if (d.url != null) {
                    m.url = $String(d.url);
                }
                if (d.fileEncSha256 != null) {
                    if (typeof d.fileEncSha256 === "string")
                        $util.base64.decode(d.fileEncSha256, m.fileEncSha256 = $util.newBuffer($util.base64.length(d.fileEncSha256)), 0);
                    else if (d.fileEncSha256.length >= 0)
                        m.fileEncSha256 = d.fileEncSha256;
                }
                if (d.mediaKey != null) {
                    if (typeof d.mediaKey === "string")
                        $util.base64.decode(d.mediaKey, m.mediaKey = $util.newBuffer($util.base64.length(d.mediaKey)), 0);
                    else if (d.mediaKey.length >= 0)
                        m.mediaKey = d.mediaKey;
                }
                if (d.mimetype != null) {
                    m.mimetype = $String(d.mimetype);
                }
                if (d.height != null) {
                    m.height = d.height >>> 0;
                }
                if (d.width != null) {
                    m.width = d.width >>> 0;
                }
                if (d.directPath != null) {
                    m.directPath = $String(d.directPath);
                }
                if (d.fileLength != null) {
                    if ($util.Long)
                        m.fileLength = $util.Long.fromValue(d.fileLength, true);
                    else if (typeof d.fileLength === "string")
                        m.fileLength = $parseInt(d.fileLength, 10);
                    else if (typeof d.fileLength === "number")
                        m.fileLength = d.fileLength;
                    else if (typeof d.fileLength === "object")
                        m.fileLength = new $util.LongBits(d.fileLength.low >>> 0, d.fileLength.high >>> 0).toNumber(true);
                }
                if (d.isFavorite != null) {
                    m.isFavorite = $Boolean(d.isFavorite);
                }
                if (d.deviceIdHint != null) {
                    m.deviceIdHint = d.deviceIdHint >>> 0;
                }
                if (d.isLottie != null) {
                    m.isLottie = $Boolean(d.isLottie);
                }
                if (d.imageHash != null) {
                    m.imageHash = $String(d.imageHash);
                }
                if (d.isAvatarSticker != null) {
                    m.isAvatarSticker = $Boolean(d.isAvatarSticker);
                }
                return m;
            };

            StickerAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = m.url;
                }
                if (m.fileEncSha256 != null && $Object.hasOwnProperty.call(m, "fileEncSha256")) {
                    d.fileEncSha256 = o.bytes === $String ? $util.base64.encode(m.fileEncSha256, 0, m.fileEncSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.fileEncSha256) : m.fileEncSha256;
                }
                if (m.mediaKey != null && $Object.hasOwnProperty.call(m, "mediaKey")) {
                    d.mediaKey = o.bytes === $String ? $util.base64.encode(m.mediaKey, 0, m.mediaKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaKey) : m.mediaKey;
                }
                if (m.mimetype != null && $Object.hasOwnProperty.call(m, "mimetype")) {
                    d.mimetype = m.mimetype;
                }
                if (m.height != null && $Object.hasOwnProperty.call(m, "height")) {
                    d.height = m.height;
                }
                if (m.width != null && $Object.hasOwnProperty.call(m, "width")) {
                    d.width = m.width;
                }
                if (m.directPath != null && $Object.hasOwnProperty.call(m, "directPath")) {
                    d.directPath = m.directPath;
                }
                if (m.fileLength != null && $Object.hasOwnProperty.call(m, "fileLength")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.fileLength = typeof m.fileLength === "number" ? $BigInt(m.fileLength) : $util.Long.fromBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0, true).toBigInt();
                    else if (typeof m.fileLength === "number")
                        d.fileLength = o.longs === $String ? $String(m.fileLength) : m.fileLength;
                    else
                        d.fileLength = o.longs === $String ? $util.Long.prototype.toString.call(m.fileLength) : o.longs === $Number ? new $util.LongBits(m.fileLength.low >>> 0, m.fileLength.high >>> 0).toNumber(true) : m.fileLength;
                }
                if (m.isFavorite != null && $Object.hasOwnProperty.call(m, "isFavorite")) {
                    d.isFavorite = m.isFavorite;
                }
                if (m.deviceIdHint != null && $Object.hasOwnProperty.call(m, "deviceIdHint")) {
                    d.deviceIdHint = m.deviceIdHint;
                }
                if (m.isLottie != null && $Object.hasOwnProperty.call(m, "isLottie")) {
                    d.isLottie = m.isLottie;
                }
                if (m.imageHash != null && $Object.hasOwnProperty.call(m, "imageHash")) {
                    d.imageHash = m.imageHash;
                }
                if (m.isAvatarSticker != null && $Object.hasOwnProperty.call(m, "isAvatarSticker")) {
                    d.isAvatarSticker = m.isAvatarSticker;
                }
                return d;
            };

            StickerAction.prototype.toJSON = function() {
                return StickerAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            StickerAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.StickerAction";
            };

            return StickerAction;
        })();

        SyncActionValue.SubscriptionAction = (function() {

            const SubscriptionAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SubscriptionAction.prototype.isDeactivated = null;
            SubscriptionAction.prototype.isAutoRenewing = null;
            SubscriptionAction.prototype.expirationDate = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SubscriptionAction.prototype, "_isDeactivated", {
                get: $util.oneOfGetter($oneOfFields = ["isDeactivated"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SubscriptionAction.prototype, "_isAutoRenewing", {
                get: $util.oneOfGetter($oneOfFields = ["isAutoRenewing"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SubscriptionAction.prototype, "_expirationDate", {
                get: $util.oneOfGetter($oneOfFields = ["expirationDate"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SubscriptionAction.create = function(properties) {
                return new SubscriptionAction(properties);
            };

            SubscriptionAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isDeactivated != null && $Object.hasOwnProperty.call(m, "isDeactivated"))
                    w.uint32(8).bool(m.isDeactivated);
                if (m.isAutoRenewing != null && $Object.hasOwnProperty.call(m, "isAutoRenewing"))
                    w.uint32(16).bool(m.isAutoRenewing);
                if (m.expirationDate != null && $Object.hasOwnProperty.call(m, "expirationDate"))
                    w.uint32(24).int64(m.expirationDate);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SubscriptionAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SubscriptionAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isDeactivated = r.bool();
                            m._isDeactivated = "isDeactivated";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.isAutoRenewing = r.bool();
                            m._isAutoRenewing = "isAutoRenewing";
                            continue;
                        }
                    case 3: {
                            if (u !== 0)
                                break;
                            m.expirationDate = r.int64();
                            m._expirationDate = "expirationDate";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SubscriptionAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.SubscriptionAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.SubscriptionAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.SubscriptionAction();
                if (d.isDeactivated != null) {
                    m.isDeactivated = $Boolean(d.isDeactivated);
                }
                if (d.isAutoRenewing != null) {
                    m.isAutoRenewing = $Boolean(d.isAutoRenewing);
                }
                if (d.expirationDate != null) {
                    if ($util.Long)
                        m.expirationDate = $util.Long.fromValue(d.expirationDate, false);
                    else if (typeof d.expirationDate === "string")
                        m.expirationDate = $parseInt(d.expirationDate, 10);
                    else if (typeof d.expirationDate === "number")
                        m.expirationDate = d.expirationDate;
                    else if (typeof d.expirationDate === "object")
                        m.expirationDate = new $util.LongBits(d.expirationDate.low >>> 0, d.expirationDate.high >>> 0).toNumber();
                }
                return m;
            };

            SubscriptionAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isDeactivated != null && $Object.hasOwnProperty.call(m, "isDeactivated")) {
                    d.isDeactivated = m.isDeactivated;
                }
                if (m.isAutoRenewing != null && $Object.hasOwnProperty.call(m, "isAutoRenewing")) {
                    d.isAutoRenewing = m.isAutoRenewing;
                }
                if (m.expirationDate != null && $Object.hasOwnProperty.call(m, "expirationDate")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.expirationDate = typeof m.expirationDate === "number" ? $BigInt(m.expirationDate) : $util.Long.fromBits(m.expirationDate.low >>> 0, m.expirationDate.high >>> 0, false).toBigInt();
                    else if (typeof m.expirationDate === "number")
                        d.expirationDate = o.longs === $String ? $String(m.expirationDate) : m.expirationDate;
                    else
                        d.expirationDate = o.longs === $String ? $util.Long.prototype.toString.call(m.expirationDate) : o.longs === $Number ? new $util.LongBits(m.expirationDate.low >>> 0, m.expirationDate.high >>> 0).toNumber() : m.expirationDate;
                }
                return d;
            };

            SubscriptionAction.prototype.toJSON = function() {
                return SubscriptionAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            SubscriptionAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.SubscriptionAction";
            };

            return SubscriptionAction;
        })();

        SyncActionValue.SubscriptionsSyncV2Action = (function() {

            const SubscriptionsSyncV2Action = function (p) {
                this.subscriptions = [];
                this.paidFeature = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SubscriptionsSyncV2Action.prototype.subscriptions = $util.emptyArray;
            SubscriptionsSyncV2Action.prototype.paidFeature = $util.emptyArray;

            SubscriptionsSyncV2Action.create = function(properties) {
                return new SubscriptionsSyncV2Action(properties);
            };

            SubscriptionsSyncV2Action.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.subscriptions != null && m.subscriptions.length) {
                    for (var i = 0; i < m.subscriptions.length; ++i)
                        $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.encode(m.subscriptions[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.paidFeature != null && m.paidFeature.length) {
                    for (var i = 0; i < m.paidFeature.length; ++i)
                        $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.encode(m.paidFeature[i], w.uint32(18).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SubscriptionsSyncV2Action.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SubscriptionsSyncV2Action();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.subscriptions && m.subscriptions.length))
                                m.subscriptions = [];
                            m.subscriptions.push($root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            if (!(m.paidFeature && m.paidFeature.length))
                                m.paidFeature = [];
                            m.paidFeature.push($root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SubscriptionsSyncV2Action.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.SubscriptionsSyncV2Action)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.SubscriptionsSyncV2Action();
                if (d.subscriptions) {
                    if (!$Array.isArray(d.subscriptions))
                        throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.subscriptions: array expected");
                    m.subscriptions = $Array(d.subscriptions.length);
                    for (var i = 0; i < d.subscriptions.length; ++i) {
                        if (!$util.isObject(d.subscriptions[i]))
                            throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.subscriptions: object expected");
                        m.subscriptions[i] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.fromObject(d.subscriptions[i], q + 1);
                    }
                }
                if (d.paidFeature) {
                    if (!$Array.isArray(d.paidFeature))
                        throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.paidFeature: array expected");
                    m.paidFeature = $Array(d.paidFeature.length);
                    for (var i = 0; i < d.paidFeature.length; ++i) {
                        if (!$util.isObject(d.paidFeature[i]))
                            throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.paidFeature: object expected");
                        m.paidFeature[i] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.fromObject(d.paidFeature[i], q + 1);
                    }
                }
                return m;
            };

            SubscriptionsSyncV2Action.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.subscriptions = [];
                    d.paidFeature = [];
                }
                if (m.subscriptions && m.subscriptions.length) {
                    d.subscriptions = $Array(m.subscriptions.length);
                    for (var j = 0; j < m.subscriptions.length; ++j) {
                        d.subscriptions[j] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo.toObject(m.subscriptions[j], o, q + 1);
                    }
                }
                if (m.paidFeature && m.paidFeature.length) {
                    d.paidFeature = $Array(m.paidFeature.length);
                    for (var j = 0; j < m.paidFeature.length; ++j) {
                        d.paidFeature[j] = $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature.toObject(m.paidFeature[j], o, q + 1);
                    }
                }
                return d;
            };

            SubscriptionsSyncV2Action.prototype.toJSON = function() {
                return SubscriptionsSyncV2Action.toObject(this, $protobuf.util.toJSONOptions);
            };

            SubscriptionsSyncV2Action.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.SubscriptionsSyncV2Action";
            };

            SubscriptionsSyncV2Action.PaidFeature = (function() {

                const PaidFeature = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                PaidFeature.prototype.name = null;
                PaidFeature.prototype.enabled = null;
                PaidFeature.prototype.limit = null;
                PaidFeature.prototype.expirationTime = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaidFeature.prototype, "_name", {
                    get: $util.oneOfGetter($oneOfFields = ["name"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaidFeature.prototype, "_enabled", {
                    get: $util.oneOfGetter($oneOfFields = ["enabled"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaidFeature.prototype, "_limit", {
                    get: $util.oneOfGetter($oneOfFields = ["limit"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(PaidFeature.prototype, "_expirationTime", {
                    get: $util.oneOfGetter($oneOfFields = ["expirationTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                PaidFeature.create = function(properties) {
                    return new PaidFeature(properties);
                };

                PaidFeature.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.name != null && $Object.hasOwnProperty.call(m, "name"))
                        w.uint32(10).string(m.name);
                    if (m.enabled != null && $Object.hasOwnProperty.call(m, "enabled"))
                        w.uint32(16).bool(m.enabled);
                    if (m.limit != null && $Object.hasOwnProperty.call(m, "limit"))
                        w.uint32(24).int32(m.limit);
                    if (m.expirationTime != null && $Object.hasOwnProperty.call(m, "expirationTime"))
                        w.uint32(32).int64(m.expirationTime);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                PaidFeature.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.name = r.stringVerify();
                                m._name = "name";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.enabled = r.bool();
                                m._enabled = "enabled";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.limit = r.int32();
                                m._limit = "limit";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.expirationTime = r.int64();
                                m._expirationTime = "expirationTime";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                PaidFeature.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature();
                    if (d.name != null) {
                        m.name = $String(d.name);
                    }
                    if (d.enabled != null) {
                        m.enabled = $Boolean(d.enabled);
                    }
                    if (d.limit != null) {
                        m.limit = d.limit | 0;
                    }
                    if (d.expirationTime != null) {
                        if ($util.Long)
                            m.expirationTime = $util.Long.fromValue(d.expirationTime, false);
                        else if (typeof d.expirationTime === "string")
                            m.expirationTime = $parseInt(d.expirationTime, 10);
                        else if (typeof d.expirationTime === "number")
                            m.expirationTime = d.expirationTime;
                        else if (typeof d.expirationTime === "object")
                            m.expirationTime = new $util.LongBits(d.expirationTime.low >>> 0, d.expirationTime.high >>> 0).toNumber();
                    }
                    return m;
                };

                PaidFeature.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.name != null && $Object.hasOwnProperty.call(m, "name")) {
                        d.name = m.name;
                    }
                    if (m.enabled != null && $Object.hasOwnProperty.call(m, "enabled")) {
                        d.enabled = m.enabled;
                    }
                    if (m.limit != null && $Object.hasOwnProperty.call(m, "limit")) {
                        d.limit = m.limit;
                    }
                    if (m.expirationTime != null && $Object.hasOwnProperty.call(m, "expirationTime")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.expirationTime = typeof m.expirationTime === "number" ? $BigInt(m.expirationTime) : $util.Long.fromBits(m.expirationTime.low >>> 0, m.expirationTime.high >>> 0, false).toBigInt();
                        else if (typeof m.expirationTime === "number")
                            d.expirationTime = o.longs === $String ? $String(m.expirationTime) : m.expirationTime;
                        else
                            d.expirationTime = o.longs === $String ? $util.Long.prototype.toString.call(m.expirationTime) : o.longs === $Number ? new $util.LongBits(m.expirationTime.low >>> 0, m.expirationTime.high >>> 0).toNumber() : m.expirationTime;
                    }
                    return d;
                };

                PaidFeature.prototype.toJSON = function() {
                    return PaidFeature.toObject(this, $protobuf.util.toJSONOptions);
                };

                PaidFeature.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SyncActionValue.SubscriptionsSyncV2Action.PaidFeature";
                };

                return PaidFeature;
            })();

            SubscriptionsSyncV2Action.SubscriptionInfo = (function() {

                const SubscriptionInfo = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                SubscriptionInfo.prototype.id = null;
                SubscriptionInfo.prototype.tier = null;
                SubscriptionInfo.prototype.status = null;
                SubscriptionInfo.prototype.startTime = null;
                SubscriptionInfo.prototype.endTime = null;
                SubscriptionInfo.prototype.isPlatformChanged = null;
                SubscriptionInfo.prototype.source = null;
                SubscriptionInfo.prototype.creationTime = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_tier", {
                    get: $util.oneOfGetter($oneOfFields = ["tier"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_status", {
                    get: $util.oneOfGetter($oneOfFields = ["status"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_startTime", {
                    get: $util.oneOfGetter($oneOfFields = ["startTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_endTime", {
                    get: $util.oneOfGetter($oneOfFields = ["endTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_isPlatformChanged", {
                    get: $util.oneOfGetter($oneOfFields = ["isPlatformChanged"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_source", {
                    get: $util.oneOfGetter($oneOfFields = ["source"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(SubscriptionInfo.prototype, "_creationTime", {
                    get: $util.oneOfGetter($oneOfFields = ["creationTime"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                SubscriptionInfo.create = function(properties) {
                    return new SubscriptionInfo(properties);
                };

                SubscriptionInfo.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                        w.uint32(10).string(m.id);
                    if (m.tier != null && $Object.hasOwnProperty.call(m, "tier"))
                        w.uint32(16).int32(m.tier);
                    if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                        w.uint32(26).string(m.status);
                    if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime"))
                        w.uint32(32).int64(m.startTime);
                    if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime"))
                        w.uint32(40).int64(m.endTime);
                    if (m.isPlatformChanged != null && $Object.hasOwnProperty.call(m, "isPlatformChanged"))
                        w.uint32(48).bool(m.isPlatformChanged);
                    if (m.source != null && $Object.hasOwnProperty.call(m, "source"))
                        w.uint32(58).string(m.source);
                    if (m.creationTime != null && $Object.hasOwnProperty.call(m, "creationTime"))
                        w.uint32(64).int64(m.creationTime);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                SubscriptionInfo.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.id = r.stringVerify();
                                m._id = "id";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.tier = r.int32();
                                m._tier = "tier";
                                continue;
                            }
                        case 3: {
                                if (u !== 2)
                                    break;
                                m.status = r.stringVerify();
                                m._status = "status";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.startTime = r.int64();
                                m._startTime = "startTime";
                                continue;
                            }
                        case 5: {
                                if (u !== 0)
                                    break;
                                m.endTime = r.int64();
                                m._endTime = "endTime";
                                continue;
                            }
                        case 6: {
                                if (u !== 0)
                                    break;
                                m.isPlatformChanged = r.bool();
                                m._isPlatformChanged = "isPlatformChanged";
                                continue;
                            }
                        case 7: {
                                if (u !== 2)
                                    break;
                                m.source = r.stringVerify();
                                m._source = "source";
                                continue;
                            }
                        case 8: {
                                if (u !== 0)
                                    break;
                                m.creationTime = r.int64();
                                m._creationTime = "creationTime";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                SubscriptionInfo.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo();
                    if (d.id != null) {
                        m.id = $String(d.id);
                    }
                    if (d.tier != null) {
                        m.tier = d.tier | 0;
                    }
                    if (d.status != null) {
                        m.status = $String(d.status);
                    }
                    if (d.startTime != null) {
                        if ($util.Long)
                            m.startTime = $util.Long.fromValue(d.startTime, false);
                        else if (typeof d.startTime === "string")
                            m.startTime = $parseInt(d.startTime, 10);
                        else if (typeof d.startTime === "number")
                            m.startTime = d.startTime;
                        else if (typeof d.startTime === "object")
                            m.startTime = new $util.LongBits(d.startTime.low >>> 0, d.startTime.high >>> 0).toNumber();
                    }
                    if (d.endTime != null) {
                        if ($util.Long)
                            m.endTime = $util.Long.fromValue(d.endTime, false);
                        else if (typeof d.endTime === "string")
                            m.endTime = $parseInt(d.endTime, 10);
                        else if (typeof d.endTime === "number")
                            m.endTime = d.endTime;
                        else if (typeof d.endTime === "object")
                            m.endTime = new $util.LongBits(d.endTime.low >>> 0, d.endTime.high >>> 0).toNumber();
                    }
                    if (d.isPlatformChanged != null) {
                        m.isPlatformChanged = $Boolean(d.isPlatformChanged);
                    }
                    if (d.source != null) {
                        m.source = $String(d.source);
                    }
                    if (d.creationTime != null) {
                        if ($util.Long)
                            m.creationTime = $util.Long.fromValue(d.creationTime, false);
                        else if (typeof d.creationTime === "string")
                            m.creationTime = $parseInt(d.creationTime, 10);
                        else if (typeof d.creationTime === "number")
                            m.creationTime = d.creationTime;
                        else if (typeof d.creationTime === "object")
                            m.creationTime = new $util.LongBits(d.creationTime.low >>> 0, d.creationTime.high >>> 0).toNumber();
                    }
                    return m;
                };

                SubscriptionInfo.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                        d.id = m.id;
                    }
                    if (m.tier != null && $Object.hasOwnProperty.call(m, "tier")) {
                        d.tier = m.tier;
                    }
                    if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                        d.status = m.status;
                    }
                    if (m.startTime != null && $Object.hasOwnProperty.call(m, "startTime")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.startTime = typeof m.startTime === "number" ? $BigInt(m.startTime) : $util.Long.fromBits(m.startTime.low >>> 0, m.startTime.high >>> 0, false).toBigInt();
                        else if (typeof m.startTime === "number")
                            d.startTime = o.longs === $String ? $String(m.startTime) : m.startTime;
                        else
                            d.startTime = o.longs === $String ? $util.Long.prototype.toString.call(m.startTime) : o.longs === $Number ? new $util.LongBits(m.startTime.low >>> 0, m.startTime.high >>> 0).toNumber() : m.startTime;
                    }
                    if (m.endTime != null && $Object.hasOwnProperty.call(m, "endTime")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.endTime = typeof m.endTime === "number" ? $BigInt(m.endTime) : $util.Long.fromBits(m.endTime.low >>> 0, m.endTime.high >>> 0, false).toBigInt();
                        else if (typeof m.endTime === "number")
                            d.endTime = o.longs === $String ? $String(m.endTime) : m.endTime;
                        else
                            d.endTime = o.longs === $String ? $util.Long.prototype.toString.call(m.endTime) : o.longs === $Number ? new $util.LongBits(m.endTime.low >>> 0, m.endTime.high >>> 0).toNumber() : m.endTime;
                    }
                    if (m.isPlatformChanged != null && $Object.hasOwnProperty.call(m, "isPlatformChanged")) {
                        d.isPlatformChanged = m.isPlatformChanged;
                    }
                    if (m.source != null && $Object.hasOwnProperty.call(m, "source")) {
                        d.source = m.source;
                    }
                    if (m.creationTime != null && $Object.hasOwnProperty.call(m, "creationTime")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.creationTime = typeof m.creationTime === "number" ? $BigInt(m.creationTime) : $util.Long.fromBits(m.creationTime.low >>> 0, m.creationTime.high >>> 0, false).toBigInt();
                        else if (typeof m.creationTime === "number")
                            d.creationTime = o.longs === $String ? $String(m.creationTime) : m.creationTime;
                        else
                            d.creationTime = o.longs === $String ? $util.Long.prototype.toString.call(m.creationTime) : o.longs === $Number ? new $util.LongBits(m.creationTime.low >>> 0, m.creationTime.high >>> 0).toNumber() : m.creationTime;
                    }
                    return d;
                };

                SubscriptionInfo.prototype.toJSON = function() {
                    return SubscriptionInfo.toObject(this, $protobuf.util.toJSONOptions);
                };

                SubscriptionInfo.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SyncActionValue.SubscriptionsSyncV2Action.SubscriptionInfo";
                };

                return SubscriptionInfo;
            })();

            return SubscriptionsSyncV2Action;
        })();

        SyncActionValue.SyncActionMessage = (function() {

            const SyncActionMessage = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SyncActionMessage.prototype.key = null;
            SyncActionMessage.prototype.timestamp = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SyncActionMessage.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SyncActionMessage.prototype, "_timestamp", {
                get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SyncActionMessage.create = function(properties) {
                return new SyncActionMessage(properties);
            };

            SyncActionMessage.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                    w.uint32(16).int64(m.timestamp);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SyncActionMessage.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SyncActionMessage();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.timestamp = r.int64();
                            m._timestamp = "timestamp";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SyncActionMessage.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.SyncActionMessage)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.SyncActionMessage: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.SyncActionMessage();
                if (d.key != null) {
                    if (!$util.isObject(d.key))
                        throw $TypeError(".proto.SyncActionValue.SyncActionMessage.key: object expected");
                    m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
                }
                if (d.timestamp != null) {
                    if ($util.Long)
                        m.timestamp = $util.Long.fromValue(d.timestamp, false);
                    else if (typeof d.timestamp === "string")
                        m.timestamp = $parseInt(d.timestamp, 10);
                    else if (typeof d.timestamp === "number")
                        m.timestamp = d.timestamp;
                    else if (typeof d.timestamp === "object")
                        m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber();
                }
                return m;
            };

            SyncActionMessage.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
                }
                if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.timestamp === "number")
                        d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                    else
                        d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber() : m.timestamp;
                }
                return d;
            };

            SyncActionMessage.prototype.toJSON = function() {
                return SyncActionMessage.toObject(this, $protobuf.util.toJSONOptions);
            };

            SyncActionMessage.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.SyncActionMessage";
            };

            return SyncActionMessage;
        })();

        SyncActionValue.SyncActionMessageRange = (function() {

            const SyncActionMessageRange = function (p) {
                this.messages = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            SyncActionMessageRange.prototype.lastMessageTimestamp = null;
            SyncActionMessageRange.prototype.lastSystemMessageTimestamp = null;
            SyncActionMessageRange.prototype.messages = $util.emptyArray;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SyncActionMessageRange.prototype, "_lastMessageTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["lastMessageTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(SyncActionMessageRange.prototype, "_lastSystemMessageTimestamp", {
                get: $util.oneOfGetter($oneOfFields = ["lastSystemMessageTimestamp"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            SyncActionMessageRange.create = function(properties) {
                return new SyncActionMessageRange(properties);
            };

            SyncActionMessageRange.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.lastMessageTimestamp != null && $Object.hasOwnProperty.call(m, "lastMessageTimestamp"))
                    w.uint32(8).int64(m.lastMessageTimestamp);
                if (m.lastSystemMessageTimestamp != null && $Object.hasOwnProperty.call(m, "lastSystemMessageTimestamp"))
                    w.uint32(16).int64(m.lastSystemMessageTimestamp);
                if (m.messages != null && m.messages.length) {
                    for (var i = 0; i < m.messages.length; ++i)
                        $root.proto.SyncActionValue.SyncActionMessage.encode(m.messages[i], w.uint32(26).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            SyncActionMessageRange.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.SyncActionMessageRange();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.lastMessageTimestamp = r.int64();
                            m._lastMessageTimestamp = "lastMessageTimestamp";
                            continue;
                        }
                    case 2: {
                            if (u !== 0)
                                break;
                            m.lastSystemMessageTimestamp = r.int64();
                            m._lastSystemMessageTimestamp = "lastSystemMessageTimestamp";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            if (!(m.messages && m.messages.length))
                                m.messages = [];
                            m.messages.push($root.proto.SyncActionValue.SyncActionMessage.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            SyncActionMessageRange.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.SyncActionMessageRange)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.SyncActionMessageRange: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.SyncActionMessageRange();
                if (d.lastMessageTimestamp != null) {
                    if ($util.Long)
                        m.lastMessageTimestamp = $util.Long.fromValue(d.lastMessageTimestamp, false);
                    else if (typeof d.lastMessageTimestamp === "string")
                        m.lastMessageTimestamp = $parseInt(d.lastMessageTimestamp, 10);
                    else if (typeof d.lastMessageTimestamp === "number")
                        m.lastMessageTimestamp = d.lastMessageTimestamp;
                    else if (typeof d.lastMessageTimestamp === "object")
                        m.lastMessageTimestamp = new $util.LongBits(d.lastMessageTimestamp.low >>> 0, d.lastMessageTimestamp.high >>> 0).toNumber();
                }
                if (d.lastSystemMessageTimestamp != null) {
                    if ($util.Long)
                        m.lastSystemMessageTimestamp = $util.Long.fromValue(d.lastSystemMessageTimestamp, false);
                    else if (typeof d.lastSystemMessageTimestamp === "string")
                        m.lastSystemMessageTimestamp = $parseInt(d.lastSystemMessageTimestamp, 10);
                    else if (typeof d.lastSystemMessageTimestamp === "number")
                        m.lastSystemMessageTimestamp = d.lastSystemMessageTimestamp;
                    else if (typeof d.lastSystemMessageTimestamp === "object")
                        m.lastSystemMessageTimestamp = new $util.LongBits(d.lastSystemMessageTimestamp.low >>> 0, d.lastSystemMessageTimestamp.high >>> 0).toNumber();
                }
                if (d.messages) {
                    if (!$Array.isArray(d.messages))
                        throw $TypeError(".proto.SyncActionValue.SyncActionMessageRange.messages: array expected");
                    m.messages = $Array(d.messages.length);
                    for (var i = 0; i < d.messages.length; ++i) {
                        if (!$util.isObject(d.messages[i]))
                            throw $TypeError(".proto.SyncActionValue.SyncActionMessageRange.messages: object expected");
                        m.messages[i] = $root.proto.SyncActionValue.SyncActionMessage.fromObject(d.messages[i], q + 1);
                    }
                }
                return m;
            };

            SyncActionMessageRange.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.messages = [];
                }
                if (m.lastMessageTimestamp != null && $Object.hasOwnProperty.call(m, "lastMessageTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.lastMessageTimestamp = typeof m.lastMessageTimestamp === "number" ? $BigInt(m.lastMessageTimestamp) : $util.Long.fromBits(m.lastMessageTimestamp.low >>> 0, m.lastMessageTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.lastMessageTimestamp === "number")
                        d.lastMessageTimestamp = o.longs === $String ? $String(m.lastMessageTimestamp) : m.lastMessageTimestamp;
                    else
                        d.lastMessageTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.lastMessageTimestamp) : o.longs === $Number ? new $util.LongBits(m.lastMessageTimestamp.low >>> 0, m.lastMessageTimestamp.high >>> 0).toNumber() : m.lastMessageTimestamp;
                }
                if (m.lastSystemMessageTimestamp != null && $Object.hasOwnProperty.call(m, "lastSystemMessageTimestamp")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.lastSystemMessageTimestamp = typeof m.lastSystemMessageTimestamp === "number" ? $BigInt(m.lastSystemMessageTimestamp) : $util.Long.fromBits(m.lastSystemMessageTimestamp.low >>> 0, m.lastSystemMessageTimestamp.high >>> 0, false).toBigInt();
                    else if (typeof m.lastSystemMessageTimestamp === "number")
                        d.lastSystemMessageTimestamp = o.longs === $String ? $String(m.lastSystemMessageTimestamp) : m.lastSystemMessageTimestamp;
                    else
                        d.lastSystemMessageTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.lastSystemMessageTimestamp) : o.longs === $Number ? new $util.LongBits(m.lastSystemMessageTimestamp.low >>> 0, m.lastSystemMessageTimestamp.high >>> 0).toNumber() : m.lastSystemMessageTimestamp;
                }
                if (m.messages && m.messages.length) {
                    d.messages = $Array(m.messages.length);
                    for (var j = 0; j < m.messages.length; ++j) {
                        d.messages[j] = $root.proto.SyncActionValue.SyncActionMessage.toObject(m.messages[j], o, q + 1);
                    }
                }
                return d;
            };

            SyncActionMessageRange.prototype.toJSON = function() {
                return SyncActionMessageRange.toObject(this, $protobuf.util.toJSONOptions);
            };

            SyncActionMessageRange.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.SyncActionMessageRange";
            };

            return SyncActionMessageRange;
        })();

        SyncActionValue.ThreadPinAction = (function() {

            const ThreadPinAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            ThreadPinAction.prototype.pinned = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(ThreadPinAction.prototype, "_pinned", {
                get: $util.oneOfGetter($oneOfFields = ["pinned"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            ThreadPinAction.create = function(properties) {
                return new ThreadPinAction(properties);
            };

            ThreadPinAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.pinned != null && $Object.hasOwnProperty.call(m, "pinned"))
                    w.uint32(8).bool(m.pinned);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            ThreadPinAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.ThreadPinAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.pinned = r.bool();
                            m._pinned = "pinned";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            ThreadPinAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.ThreadPinAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.ThreadPinAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.ThreadPinAction();
                if (d.pinned != null) {
                    m.pinned = $Boolean(d.pinned);
                }
                return m;
            };

            ThreadPinAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.pinned != null && $Object.hasOwnProperty.call(m, "pinned")) {
                    d.pinned = m.pinned;
                }
                return d;
            };

            ThreadPinAction.prototype.toJSON = function() {
                return ThreadPinAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            ThreadPinAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.ThreadPinAction";
            };

            return ThreadPinAction;
        })();

        SyncActionValue.TimeFormatAction = (function() {

            const TimeFormatAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            TimeFormatAction.prototype.isTwentyFourHourFormatEnabled = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TimeFormatAction.prototype, "_isTwentyFourHourFormatEnabled", {
                get: $util.oneOfGetter($oneOfFields = ["isTwentyFourHourFormatEnabled"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            TimeFormatAction.create = function(properties) {
                return new TimeFormatAction(properties);
            };

            TimeFormatAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.isTwentyFourHourFormatEnabled != null && $Object.hasOwnProperty.call(m, "isTwentyFourHourFormatEnabled"))
                    w.uint32(8).bool(m.isTwentyFourHourFormatEnabled);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            TimeFormatAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.TimeFormatAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.isTwentyFourHourFormatEnabled = r.bool();
                            m._isTwentyFourHourFormatEnabled = "isTwentyFourHourFormatEnabled";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            TimeFormatAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.TimeFormatAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.TimeFormatAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.TimeFormatAction();
                if (d.isTwentyFourHourFormatEnabled != null) {
                    m.isTwentyFourHourFormatEnabled = $Boolean(d.isTwentyFourHourFormatEnabled);
                }
                return m;
            };

            TimeFormatAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.isTwentyFourHourFormatEnabled != null && $Object.hasOwnProperty.call(m, "isTwentyFourHourFormatEnabled")) {
                    d.isTwentyFourHourFormatEnabled = m.isTwentyFourHourFormatEnabled;
                }
                return d;
            };

            TimeFormatAction.prototype.toJSON = function() {
                return TimeFormatAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            TimeFormatAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.TimeFormatAction";
            };

            return TimeFormatAction;
        })();

        SyncActionValue.UGCBot = (function() {

            const UGCBot = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UGCBot.prototype.definition = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UGCBot.prototype, "_definition", {
                get: $util.oneOfGetter($oneOfFields = ["definition"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UGCBot.create = function(properties) {
                return new UGCBot(properties);
            };

            UGCBot.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.definition != null && $Object.hasOwnProperty.call(m, "definition"))
                    w.uint32(10).bytes(m.definition);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UGCBot.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.UGCBot();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.definition = r.bytes();
                            m._definition = "definition";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UGCBot.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.UGCBot)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.UGCBot: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.UGCBot();
                if (d.definition != null) {
                    if (typeof d.definition === "string")
                        $util.base64.decode(d.definition, m.definition = $util.newBuffer($util.base64.length(d.definition)), 0);
                    else if (d.definition.length >= 0)
                        m.definition = d.definition;
                }
                return m;
            };

            UGCBot.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.definition != null && $Object.hasOwnProperty.call(m, "definition")) {
                    d.definition = o.bytes === $String ? $util.base64.encode(m.definition, 0, m.definition.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.definition) : m.definition;
                }
                return d;
            };

            UGCBot.prototype.toJSON = function() {
                return UGCBot.toObject(this, $protobuf.util.toJSONOptions);
            };

            UGCBot.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.UGCBot";
            };

            return UGCBot;
        })();

        SyncActionValue.UnarchiveChatsSetting = (function() {

            const UnarchiveChatsSetting = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UnarchiveChatsSetting.prototype.unarchiveChats = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UnarchiveChatsSetting.prototype, "_unarchiveChats", {
                get: $util.oneOfGetter($oneOfFields = ["unarchiveChats"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UnarchiveChatsSetting.create = function(properties) {
                return new UnarchiveChatsSetting(properties);
            };

            UnarchiveChatsSetting.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.unarchiveChats != null && $Object.hasOwnProperty.call(m, "unarchiveChats"))
                    w.uint32(8).bool(m.unarchiveChats);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UnarchiveChatsSetting.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.UnarchiveChatsSetting();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.unarchiveChats = r.bool();
                            m._unarchiveChats = "unarchiveChats";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UnarchiveChatsSetting.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.UnarchiveChatsSetting)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.UnarchiveChatsSetting: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.UnarchiveChatsSetting();
                if (d.unarchiveChats != null) {
                    m.unarchiveChats = $Boolean(d.unarchiveChats);
                }
                return m;
            };

            UnarchiveChatsSetting.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.unarchiveChats != null && $Object.hasOwnProperty.call(m, "unarchiveChats")) {
                    d.unarchiveChats = m.unarchiveChats;
                }
                return d;
            };

            UnarchiveChatsSetting.prototype.toJSON = function() {
                return UnarchiveChatsSetting.toObject(this, $protobuf.util.toJSONOptions);
            };

            UnarchiveChatsSetting.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.UnarchiveChatsSetting";
            };

            return UnarchiveChatsSetting;
        })();

        SyncActionValue.UserStatusMuteAction = (function() {

            const UserStatusMuteAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UserStatusMuteAction.prototype.muted = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UserStatusMuteAction.prototype, "_muted", {
                get: $util.oneOfGetter($oneOfFields = ["muted"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UserStatusMuteAction.create = function(properties) {
                return new UserStatusMuteAction(properties);
            };

            UserStatusMuteAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.muted != null && $Object.hasOwnProperty.call(m, "muted"))
                    w.uint32(8).bool(m.muted);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UserStatusMuteAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.UserStatusMuteAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.muted = r.bool();
                            m._muted = "muted";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UserStatusMuteAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.UserStatusMuteAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.UserStatusMuteAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.UserStatusMuteAction();
                if (d.muted != null) {
                    m.muted = $Boolean(d.muted);
                }
                return m;
            };

            UserStatusMuteAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.muted != null && $Object.hasOwnProperty.call(m, "muted")) {
                    d.muted = m.muted;
                }
                return d;
            };

            UserStatusMuteAction.prototype.toJSON = function() {
                return UserStatusMuteAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            UserStatusMuteAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.UserStatusMuteAction";
            };

            return UserStatusMuteAction;
        })();

        SyncActionValue.UsernameChatStartModeAction = (function() {

            const UsernameChatStartModeAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UsernameChatStartModeAction.prototype.chatStartMode = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UsernameChatStartModeAction.prototype, "_chatStartMode", {
                get: $util.oneOfGetter($oneOfFields = ["chatStartMode"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UsernameChatStartModeAction.create = function(properties) {
                return new UsernameChatStartModeAction(properties);
            };

            UsernameChatStartModeAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.chatStartMode != null && $Object.hasOwnProperty.call(m, "chatStartMode"))
                    w.uint32(8).int32(m.chatStartMode);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UsernameChatStartModeAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.UsernameChatStartModeAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.chatStartMode = r.int32();
                            m._chatStartMode = "chatStartMode";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UsernameChatStartModeAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.UsernameChatStartModeAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.UsernameChatStartModeAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.UsernameChatStartModeAction();
                switch (d.chatStartMode) {
                case "LID":
                case 1:
                    m.chatStartMode = 1;
                    break;
                case "PN":
                case 2:
                    m.chatStartMode = 2;
                    break;
                default:
                    if (typeof d.chatStartMode === "number" && (d.chatStartMode | 0) === d.chatStartMode)
                        m.chatStartMode = d.chatStartMode;
                }
                return m;
            };

            UsernameChatStartModeAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.chatStartMode != null && $Object.hasOwnProperty.call(m, "chatStartMode")) {
                    d.chatStartMode = o.enums === $String ? $root.proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode[m.chatStartMode] === $undefined ? m.chatStartMode : $root.proto.SyncActionValue.UsernameChatStartModeAction.ChatStartMode[m.chatStartMode] : m.chatStartMode;
                }
                return d;
            };

            UsernameChatStartModeAction.prototype.toJSON = function() {
                return UsernameChatStartModeAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            UsernameChatStartModeAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.UsernameChatStartModeAction";
            };

            UsernameChatStartModeAction.ChatStartMode = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[1] = "LID"] = 1;
                values[valuesById[2] = "PN"] = 2;
                return values;
            })();

            return UsernameChatStartModeAction;
        })();

        SyncActionValue.WASARootSecretAction = (function() {

            const WASARootSecretAction = function (p) {
                this.secrets = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            WASARootSecretAction.prototype.secrets = $util.emptyArray;

            WASARootSecretAction.create = function(properties) {
                return new WASARootSecretAction(properties);
            };

            WASARootSecretAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.secrets != null && m.secrets.length) {
                    for (var i = 0; i < m.secrets.length; ++i)
                        $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.encode(m.secrets[i], w.uint32(10).fork(), q + 1).ldelim();
                }
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            WASARootSecretAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.WASARootSecretAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            if (!(m.secrets && m.secrets.length))
                                m.secrets = [];
                            m.secrets.push($root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            WASARootSecretAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.WASARootSecretAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.WASARootSecretAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.WASARootSecretAction();
                if (d.secrets) {
                    if (!$Array.isArray(d.secrets))
                        throw $TypeError(".proto.SyncActionValue.WASARootSecretAction.secrets: array expected");
                    m.secrets = $Array(d.secrets.length);
                    for (var i = 0; i < d.secrets.length; ++i) {
                        if (!$util.isObject(d.secrets[i]))
                            throw $TypeError(".proto.SyncActionValue.WASARootSecretAction.secrets: object expected");
                        m.secrets[i] = $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.fromObject(d.secrets[i], q + 1);
                    }
                }
                return m;
            };

            WASARootSecretAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.secrets = [];
                }
                if (m.secrets && m.secrets.length) {
                    d.secrets = $Array(m.secrets.length);
                    for (var j = 0; j < m.secrets.length; ++j) {
                        d.secrets[j] = $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.toObject(m.secrets[j], o, q + 1);
                    }
                }
                return d;
            };

            WASARootSecretAction.prototype.toJSON = function() {
                return WASARootSecretAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            WASARootSecretAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.WASARootSecretAction";
            };

            WASARootSecretAction.RootSecretEntry = (function() {

                const RootSecretEntry = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                RootSecretEntry.prototype.id = null;
                RootSecretEntry.prototype.rootSecret = null;
                RootSecretEntry.prototype.epoch = null;
                RootSecretEntry.prototype.status = null;

                let $oneOfFields;

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RootSecretEntry.prototype, "_id", {
                    get: $util.oneOfGetter($oneOfFields = ["id"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RootSecretEntry.prototype, "_rootSecret", {
                    get: $util.oneOfGetter($oneOfFields = ["rootSecret"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RootSecretEntry.prototype, "_epoch", {
                    get: $util.oneOfGetter($oneOfFields = ["epoch"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                // Virtual OneOf for proto3 optional field
                $Object.defineProperty(RootSecretEntry.prototype, "_status", {
                    get: $util.oneOfGetter($oneOfFields = ["status"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                RootSecretEntry.create = function(properties) {
                    return new RootSecretEntry(properties);
                };

                RootSecretEntry.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                        w.uint32(10).string(m.id);
                    if (m.rootSecret != null && $Object.hasOwnProperty.call(m, "rootSecret"))
                        w.uint32(18).bytes(m.rootSecret);
                    if (m.epoch != null && $Object.hasOwnProperty.call(m, "epoch"))
                        w.uint32(24).int64(m.epoch);
                    if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                        w.uint32(32).int32(m.status);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                RootSecretEntry.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry(), v;
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.id = r.stringVerify();
                                m._id = "id";
                                continue;
                            }
                        case 2: {
                                if (u !== 2)
                                    break;
                                m.rootSecret = r.bytes();
                                m._rootSecret = "rootSecret";
                                continue;
                            }
                        case 3: {
                                if (u !== 0)
                                    break;
                                m.epoch = r.int64();
                                m._epoch = "epoch";
                                continue;
                            }
                        case 4: {
                                if (u !== 0)
                                    break;
                                m.status = r.int32();
                                m._status = "status";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                RootSecretEntry.fromObject = function (d, q) {
                    if (d instanceof $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.SyncActionValue.WASARootSecretAction.RootSecretEntry: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry();
                    if (d.id != null) {
                        m.id = $String(d.id);
                    }
                    if (d.rootSecret != null) {
                        if (typeof d.rootSecret === "string")
                            $util.base64.decode(d.rootSecret, m.rootSecret = $util.newBuffer($util.base64.length(d.rootSecret)), 0);
                        else if (d.rootSecret.length >= 0)
                            m.rootSecret = d.rootSecret;
                    }
                    if (d.epoch != null) {
                        if ($util.Long)
                            m.epoch = $util.Long.fromValue(d.epoch, false);
                        else if (typeof d.epoch === "string")
                            m.epoch = $parseInt(d.epoch, 10);
                        else if (typeof d.epoch === "number")
                            m.epoch = d.epoch;
                        else if (typeof d.epoch === "object")
                            m.epoch = new $util.LongBits(d.epoch.low >>> 0, d.epoch.high >>> 0).toNumber();
                    }
                    switch (d.status) {
                    case "INACTIVE":
                    case 0:
                        m.status = 0;
                        break;
                    case "ACTIVE":
                    case 1:
                        m.status = 1;
                        break;
                    default:
                        if (typeof d.status === "number" && (d.status | 0) === d.status)
                            m.status = d.status;
                    }
                    return m;
                };

                RootSecretEntry.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                        d.id = m.id;
                    }
                    if (m.rootSecret != null && $Object.hasOwnProperty.call(m, "rootSecret")) {
                        d.rootSecret = o.bytes === $String ? $util.base64.encode(m.rootSecret, 0, m.rootSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.rootSecret) : m.rootSecret;
                    }
                    if (m.epoch != null && $Object.hasOwnProperty.call(m, "epoch")) {
                        if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                            d.epoch = typeof m.epoch === "number" ? $BigInt(m.epoch) : $util.Long.fromBits(m.epoch.low >>> 0, m.epoch.high >>> 0, false).toBigInt();
                        else if (typeof m.epoch === "number")
                            d.epoch = o.longs === $String ? $String(m.epoch) : m.epoch;
                        else
                            d.epoch = o.longs === $String ? $util.Long.prototype.toString.call(m.epoch) : o.longs === $Number ? new $util.LongBits(m.epoch.low >>> 0, m.epoch.high >>> 0).toNumber() : m.epoch;
                    }
                    if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                        d.status = o.enums === $String ? $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.Status[m.status] === $undefined ? m.status : $root.proto.SyncActionValue.WASARootSecretAction.RootSecretEntry.Status[m.status] : m.status;
                    }
                    return d;
                };

                RootSecretEntry.prototype.toJSON = function() {
                    return RootSecretEntry.toObject(this, $protobuf.util.toJSONOptions);
                };

                RootSecretEntry.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.SyncActionValue.WASARootSecretAction.RootSecretEntry";
                };

                RootSecretEntry.Status = (function() {
                    const valuesById = $Object.create(null), values = $Object.create(valuesById);
                    values[valuesById[0] = "INACTIVE"] = 0;
                    values[valuesById[1] = "ACTIVE"] = 1;
                    return values;
                })();

                return RootSecretEntry;
            })();

            return WASARootSecretAction;
        })();

        SyncActionValue.WaffleAccountLinkStateAction = (function() {

            const WaffleAccountLinkStateAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            WaffleAccountLinkStateAction.prototype.linkState = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WaffleAccountLinkStateAction.prototype, "_linkState", {
                get: $util.oneOfGetter($oneOfFields = ["linkState"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            WaffleAccountLinkStateAction.create = function(properties) {
                return new WaffleAccountLinkStateAction(properties);
            };

            WaffleAccountLinkStateAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.linkState != null && $Object.hasOwnProperty.call(m, "linkState"))
                    w.uint32(16).int32(m.linkState);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            WaffleAccountLinkStateAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.WaffleAccountLinkStateAction(), v;
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 2: {
                            if (u !== 0)
                                break;
                            m.linkState = r.int32();
                            m._linkState = "linkState";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            WaffleAccountLinkStateAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.WaffleAccountLinkStateAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.WaffleAccountLinkStateAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.WaffleAccountLinkStateAction();
                switch (d.linkState) {
                case "ACTIVE":
                case 0:
                    m.linkState = 0;
                    break;
                case "PAUSED":
                case 1:
                    m.linkState = 1;
                    break;
                case "UNLINKED":
                case 2:
                    m.linkState = 2;
                    break;
                default:
                    if (typeof d.linkState === "number" && (d.linkState | 0) === d.linkState)
                        m.linkState = d.linkState;
                }
                return m;
            };

            WaffleAccountLinkStateAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.linkState != null && $Object.hasOwnProperty.call(m, "linkState")) {
                    d.linkState = o.enums === $String ? $root.proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState[m.linkState] === $undefined ? m.linkState : $root.proto.SyncActionValue.WaffleAccountLinkStateAction.AccountLinkState[m.linkState] : m.linkState;
                }
                return d;
            };

            WaffleAccountLinkStateAction.prototype.toJSON = function() {
                return WaffleAccountLinkStateAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            WaffleAccountLinkStateAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.WaffleAccountLinkStateAction";
            };

            WaffleAccountLinkStateAction.AccountLinkState = (function() {
                const valuesById = $Object.create(null), values = $Object.create(valuesById);
                values[valuesById[0] = "ACTIVE"] = 0;
                values[valuesById[1] = "PAUSED"] = 1;
                values[valuesById[2] = "UNLINKED"] = 2;
                return values;
            })();

            return WaffleAccountLinkStateAction;
        })();

        SyncActionValue.WamoUserIdentifierAction = (function() {

            const WamoUserIdentifierAction = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            WamoUserIdentifierAction.prototype.identifier = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(WamoUserIdentifierAction.prototype, "_identifier", {
                get: $util.oneOfGetter($oneOfFields = ["identifier"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            WamoUserIdentifierAction.create = function(properties) {
                return new WamoUserIdentifierAction(properties);
            };

            WamoUserIdentifierAction.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.identifier != null && $Object.hasOwnProperty.call(m, "identifier"))
                    w.uint32(10).string(m.identifier);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            WamoUserIdentifierAction.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncActionValue.WamoUserIdentifierAction();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.identifier = r.stringVerify();
                            m._identifier = "identifier";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            WamoUserIdentifierAction.fromObject = function (d, q) {
                if (d instanceof $root.proto.SyncActionValue.WamoUserIdentifierAction)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.SyncActionValue.WamoUserIdentifierAction: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.SyncActionValue.WamoUserIdentifierAction();
                if (d.identifier != null) {
                    m.identifier = $String(d.identifier);
                }
                return m;
            };

            WamoUserIdentifierAction.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.identifier != null && $Object.hasOwnProperty.call(m, "identifier")) {
                    d.identifier = m.identifier;
                }
                return d;
            };

            WamoUserIdentifierAction.prototype.toJSON = function() {
                return WamoUserIdentifierAction.toObject(this, $protobuf.util.toJSONOptions);
            };

            WamoUserIdentifierAction.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.SyncActionValue.WamoUserIdentifierAction";
            };

            return WamoUserIdentifierAction;
        })();

        return SyncActionValue;
    })();

    proto.SyncdIndex = (function() {

        const SyncdIndex = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdIndex.prototype.blob = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdIndex.prototype, "_blob", {
            get: $util.oneOfGetter($oneOfFields = ["blob"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdIndex.create = function(properties) {
            return new SyncdIndex(properties);
        };

        SyncdIndex.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.blob != null && $Object.hasOwnProperty.call(m, "blob"))
                w.uint32(10).bytes(m.blob);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdIndex.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdIndex();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.blob = r.bytes();
                        m._blob = "blob";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdIndex.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdIndex)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdIndex: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdIndex();
            if (d.blob != null) {
                if (typeof d.blob === "string")
                    $util.base64.decode(d.blob, m.blob = $util.newBuffer($util.base64.length(d.blob)), 0);
                else if (d.blob.length >= 0)
                    m.blob = d.blob;
            }
            return m;
        };

        SyncdIndex.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.blob != null && $Object.hasOwnProperty.call(m, "blob")) {
                d.blob = o.bytes === $String ? $util.base64.encode(m.blob, 0, m.blob.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.blob) : m.blob;
            }
            return d;
        };

        SyncdIndex.prototype.toJSON = function() {
            return SyncdIndex.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdIndex.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdIndex";
        };

        return SyncdIndex;
    })();

    proto.SyncdMutation = (function() {

        const SyncdMutation = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdMutation.prototype.operation = null;
        SyncdMutation.prototype.record = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdMutation.prototype, "_operation", {
            get: $util.oneOfGetter($oneOfFields = ["operation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdMutation.prototype, "_record", {
            get: $util.oneOfGetter($oneOfFields = ["record"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdMutation.create = function(properties) {
            return new SyncdMutation(properties);
        };

        SyncdMutation.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.operation != null && $Object.hasOwnProperty.call(m, "operation"))
                w.uint32(8).int32(m.operation);
            if (m.record != null && $Object.hasOwnProperty.call(m, "record"))
                $root.proto.SyncdRecord.encode(m.record, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdMutation.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdMutation(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.operation = r.int32();
                        m._operation = "operation";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.record = $root.proto.SyncdRecord.decode(r, r.uint32(), $undefined, q + 1, m.record);
                        m._record = "record";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdMutation.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdMutation)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdMutation: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdMutation();
            switch (d.operation) {
            case "SET":
            case 0:
                m.operation = 0;
                break;
            case "REMOVE":
            case 1:
                m.operation = 1;
                break;
            default:
                if (typeof d.operation === "number" && (d.operation | 0) === d.operation)
                    m.operation = d.operation;
            }
            if (d.record != null) {
                if (!$util.isObject(d.record))
                    throw $TypeError(".proto.SyncdMutation.record: object expected");
                m.record = $root.proto.SyncdRecord.fromObject(d.record, q + 1);
            }
            return m;
        };

        SyncdMutation.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.operation != null && $Object.hasOwnProperty.call(m, "operation")) {
                d.operation = o.enums === $String ? $root.proto.SyncdMutation.SyncdOperation[m.operation] === $undefined ? m.operation : $root.proto.SyncdMutation.SyncdOperation[m.operation] : m.operation;
            }
            if (m.record != null && $Object.hasOwnProperty.call(m, "record")) {
                d.record = $root.proto.SyncdRecord.toObject(m.record, o, q + 1);
            }
            return d;
        };

        SyncdMutation.prototype.toJSON = function() {
            return SyncdMutation.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdMutation.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdMutation";
        };

        SyncdMutation.SyncdOperation = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "SET"] = 0;
            values[valuesById[1] = "REMOVE"] = 1;
            return values;
        })();

        return SyncdMutation;
    })();

    proto.SyncdMutations = (function() {

        const SyncdMutations = function (p) {
            this.mutations = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdMutations.prototype.mutations = $util.emptyArray;

        SyncdMutations.create = function(properties) {
            return new SyncdMutations(properties);
        };

        SyncdMutations.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.mutations != null && m.mutations.length) {
                for (var i = 0; i < m.mutations.length; ++i)
                    $root.proto.SyncdMutation.encode(m.mutations[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdMutations.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdMutations();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.mutations && m.mutations.length))
                            m.mutations = [];
                        m.mutations.push($root.proto.SyncdMutation.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdMutations.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdMutations)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdMutations: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdMutations();
            if (d.mutations) {
                if (!$Array.isArray(d.mutations))
                    throw $TypeError(".proto.SyncdMutations.mutations: array expected");
                m.mutations = $Array(d.mutations.length);
                for (var i = 0; i < d.mutations.length; ++i) {
                    if (!$util.isObject(d.mutations[i]))
                        throw $TypeError(".proto.SyncdMutations.mutations: object expected");
                    m.mutations[i] = $root.proto.SyncdMutation.fromObject(d.mutations[i], q + 1);
                }
            }
            return m;
        };

        SyncdMutations.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mutations = [];
            }
            if (m.mutations && m.mutations.length) {
                d.mutations = $Array(m.mutations.length);
                for (var j = 0; j < m.mutations.length; ++j) {
                    d.mutations[j] = $root.proto.SyncdMutation.toObject(m.mutations[j], o, q + 1);
                }
            }
            return d;
        };

        SyncdMutations.prototype.toJSON = function() {
            return SyncdMutations.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdMutations.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdMutations";
        };

        return SyncdMutations;
    })();

    proto.SyncdPatch = (function() {

        const SyncdPatch = function (p) {
            this.mutations = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdPatch.prototype.version = null;
        SyncdPatch.prototype.mutations = $util.emptyArray;
        SyncdPatch.prototype.externalMutations = null;
        SyncdPatch.prototype.snapshotMac = null;
        SyncdPatch.prototype.patchMac = null;
        SyncdPatch.prototype.keyId = null;
        SyncdPatch.prototype.exitCode = null;
        SyncdPatch.prototype.deviceIndex = null;
        SyncdPatch.prototype.clientDebugData = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_externalMutations", {
            get: $util.oneOfGetter($oneOfFields = ["externalMutations"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_snapshotMac", {
            get: $util.oneOfGetter($oneOfFields = ["snapshotMac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_patchMac", {
            get: $util.oneOfGetter($oneOfFields = ["patchMac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_exitCode", {
            get: $util.oneOfGetter($oneOfFields = ["exitCode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_deviceIndex", {
            get: $util.oneOfGetter($oneOfFields = ["deviceIndex"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPatch.prototype, "_clientDebugData", {
            get: $util.oneOfGetter($oneOfFields = ["clientDebugData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdPatch.create = function(properties) {
            return new SyncdPatch(properties);
        };

        SyncdPatch.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                $root.proto.SyncdVersion.encode(m.version, w.uint32(10).fork(), q + 1).ldelim();
            if (m.mutations != null && m.mutations.length) {
                for (var i = 0; i < m.mutations.length; ++i)
                    $root.proto.SyncdMutation.encode(m.mutations[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.externalMutations != null && $Object.hasOwnProperty.call(m, "externalMutations"))
                $root.proto.ExternalBlobReference.encode(m.externalMutations, w.uint32(26).fork(), q + 1).ldelim();
            if (m.snapshotMac != null && $Object.hasOwnProperty.call(m, "snapshotMac"))
                w.uint32(34).bytes(m.snapshotMac);
            if (m.patchMac != null && $Object.hasOwnProperty.call(m, "patchMac"))
                w.uint32(42).bytes(m.patchMac);
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                $root.proto.KeyId.encode(m.keyId, w.uint32(50).fork(), q + 1).ldelim();
            if (m.exitCode != null && $Object.hasOwnProperty.call(m, "exitCode"))
                $root.proto.ExitCode.encode(m.exitCode, w.uint32(58).fork(), q + 1).ldelim();
            if (m.deviceIndex != null && $Object.hasOwnProperty.call(m, "deviceIndex"))
                w.uint32(64).uint32(m.deviceIndex);
            if (m.clientDebugData != null && $Object.hasOwnProperty.call(m, "clientDebugData"))
                w.uint32(74).bytes(m.clientDebugData);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdPatch.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdPatch();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.version = $root.proto.SyncdVersion.decode(r, r.uint32(), $undefined, q + 1, m.version);
                        m._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.mutations && m.mutations.length))
                            m.mutations = [];
                        m.mutations.push($root.proto.SyncdMutation.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.externalMutations = $root.proto.ExternalBlobReference.decode(r, r.uint32(), $undefined, q + 1, m.externalMutations);
                        m._externalMutations = "externalMutations";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.snapshotMac = r.bytes();
                        m._snapshotMac = "snapshotMac";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.patchMac = r.bytes();
                        m._patchMac = "patchMac";
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.keyId = $root.proto.KeyId.decode(r, r.uint32(), $undefined, q + 1, m.keyId);
                        m._keyId = "keyId";
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.exitCode = $root.proto.ExitCode.decode(r, r.uint32(), $undefined, q + 1, m.exitCode);
                        m._exitCode = "exitCode";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.deviceIndex = r.uint32();
                        m._deviceIndex = "deviceIndex";
                        continue;
                    }
                case 9: {
                        if (u !== 2)
                            break;
                        m.clientDebugData = r.bytes();
                        m._clientDebugData = "clientDebugData";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdPatch.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdPatch)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdPatch: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdPatch();
            if (d.version != null) {
                if (!$util.isObject(d.version))
                    throw $TypeError(".proto.SyncdPatch.version: object expected");
                m.version = $root.proto.SyncdVersion.fromObject(d.version, q + 1);
            }
            if (d.mutations) {
                if (!$Array.isArray(d.mutations))
                    throw $TypeError(".proto.SyncdPatch.mutations: array expected");
                m.mutations = $Array(d.mutations.length);
                for (var i = 0; i < d.mutations.length; ++i) {
                    if (!$util.isObject(d.mutations[i]))
                        throw $TypeError(".proto.SyncdPatch.mutations: object expected");
                    m.mutations[i] = $root.proto.SyncdMutation.fromObject(d.mutations[i], q + 1);
                }
            }
            if (d.externalMutations != null) {
                if (!$util.isObject(d.externalMutations))
                    throw $TypeError(".proto.SyncdPatch.externalMutations: object expected");
                m.externalMutations = $root.proto.ExternalBlobReference.fromObject(d.externalMutations, q + 1);
            }
            if (d.snapshotMac != null) {
                if (typeof d.snapshotMac === "string")
                    $util.base64.decode(d.snapshotMac, m.snapshotMac = $util.newBuffer($util.base64.length(d.snapshotMac)), 0);
                else if (d.snapshotMac.length >= 0)
                    m.snapshotMac = d.snapshotMac;
            }
            if (d.patchMac != null) {
                if (typeof d.patchMac === "string")
                    $util.base64.decode(d.patchMac, m.patchMac = $util.newBuffer($util.base64.length(d.patchMac)), 0);
                else if (d.patchMac.length >= 0)
                    m.patchMac = d.patchMac;
            }
            if (d.keyId != null) {
                if (!$util.isObject(d.keyId))
                    throw $TypeError(".proto.SyncdPatch.keyId: object expected");
                m.keyId = $root.proto.KeyId.fromObject(d.keyId, q + 1);
            }
            if (d.exitCode != null) {
                if (!$util.isObject(d.exitCode))
                    throw $TypeError(".proto.SyncdPatch.exitCode: object expected");
                m.exitCode = $root.proto.ExitCode.fromObject(d.exitCode, q + 1);
            }
            if (d.deviceIndex != null) {
                m.deviceIndex = d.deviceIndex >>> 0;
            }
            if (d.clientDebugData != null) {
                if (typeof d.clientDebugData === "string")
                    $util.base64.decode(d.clientDebugData, m.clientDebugData = $util.newBuffer($util.base64.length(d.clientDebugData)), 0);
                else if (d.clientDebugData.length >= 0)
                    m.clientDebugData = d.clientDebugData;
            }
            return m;
        };

        SyncdPatch.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mutations = [];
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = $root.proto.SyncdVersion.toObject(m.version, o, q + 1);
            }
            if (m.mutations && m.mutations.length) {
                d.mutations = $Array(m.mutations.length);
                for (var j = 0; j < m.mutations.length; ++j) {
                    d.mutations[j] = $root.proto.SyncdMutation.toObject(m.mutations[j], o, q + 1);
                }
            }
            if (m.externalMutations != null && $Object.hasOwnProperty.call(m, "externalMutations")) {
                d.externalMutations = $root.proto.ExternalBlobReference.toObject(m.externalMutations, o, q + 1);
            }
            if (m.snapshotMac != null && $Object.hasOwnProperty.call(m, "snapshotMac")) {
                d.snapshotMac = o.bytes === $String ? $util.base64.encode(m.snapshotMac, 0, m.snapshotMac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.snapshotMac) : m.snapshotMac;
            }
            if (m.patchMac != null && $Object.hasOwnProperty.call(m, "patchMac")) {
                d.patchMac = o.bytes === $String ? $util.base64.encode(m.patchMac, 0, m.patchMac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.patchMac) : m.patchMac;
            }
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                d.keyId = $root.proto.KeyId.toObject(m.keyId, o, q + 1);
            }
            if (m.exitCode != null && $Object.hasOwnProperty.call(m, "exitCode")) {
                d.exitCode = $root.proto.ExitCode.toObject(m.exitCode, o, q + 1);
            }
            if (m.deviceIndex != null && $Object.hasOwnProperty.call(m, "deviceIndex")) {
                d.deviceIndex = m.deviceIndex;
            }
            if (m.clientDebugData != null && $Object.hasOwnProperty.call(m, "clientDebugData")) {
                d.clientDebugData = o.bytes === $String ? $util.base64.encode(m.clientDebugData, 0, m.clientDebugData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.clientDebugData) : m.clientDebugData;
            }
            return d;
        };

        SyncdPatch.prototype.toJSON = function() {
            return SyncdPatch.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdPatch.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdPatch";
        };

        return SyncdPatch;
    })();

    proto.SyncdPlainTextRecord = (function() {

        const SyncdPlainTextRecord = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdPlainTextRecord.prototype.value = null;
        SyncdPlainTextRecord.prototype.keyId = null;
        SyncdPlainTextRecord.prototype.mac = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPlainTextRecord.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPlainTextRecord.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdPlainTextRecord.prototype, "_mac", {
            get: $util.oneOfGetter($oneOfFields = ["mac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdPlainTextRecord.create = function(properties) {
            return new SyncdPlainTextRecord(properties);
        };

        SyncdPlainTextRecord.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                $root.proto.SyncActionData.encode(m.value, w.uint32(10).fork(), q + 1).ldelim();
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                w.uint32(18).bytes(m.keyId);
            if (m.mac != null && $Object.hasOwnProperty.call(m, "mac"))
                w.uint32(26).bytes(m.mac);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdPlainTextRecord.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdPlainTextRecord();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.value = $root.proto.SyncActionData.decode(r, r.uint32(), $undefined, q + 1, m.value);
                        m._value = "value";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.keyId = r.bytes();
                        m._keyId = "keyId";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.mac = r.bytes();
                        m._mac = "mac";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdPlainTextRecord.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdPlainTextRecord)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdPlainTextRecord: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdPlainTextRecord();
            if (d.value != null) {
                if (!$util.isObject(d.value))
                    throw $TypeError(".proto.SyncdPlainTextRecord.value: object expected");
                m.value = $root.proto.SyncActionData.fromObject(d.value, q + 1);
            }
            if (d.keyId != null) {
                if (typeof d.keyId === "string")
                    $util.base64.decode(d.keyId, m.keyId = $util.newBuffer($util.base64.length(d.keyId)), 0);
                else if (d.keyId.length >= 0)
                    m.keyId = d.keyId;
            }
            if (d.mac != null) {
                if (typeof d.mac === "string")
                    $util.base64.decode(d.mac, m.mac = $util.newBuffer($util.base64.length(d.mac)), 0);
                else if (d.mac.length >= 0)
                    m.mac = d.mac;
            }
            return m;
        };

        SyncdPlainTextRecord.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                d.value = $root.proto.SyncActionData.toObject(m.value, o, q + 1);
            }
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                d.keyId = o.bytes === $String ? $util.base64.encode(m.keyId, 0, m.keyId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.keyId) : m.keyId;
            }
            if (m.mac != null && $Object.hasOwnProperty.call(m, "mac")) {
                d.mac = o.bytes === $String ? $util.base64.encode(m.mac, 0, m.mac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mac) : m.mac;
            }
            return d;
        };

        SyncdPlainTextRecord.prototype.toJSON = function() {
            return SyncdPlainTextRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdPlainTextRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdPlainTextRecord";
        };

        return SyncdPlainTextRecord;
    })();

    proto.SyncdRecord = (function() {

        const SyncdRecord = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdRecord.prototype.index = null;
        SyncdRecord.prototype.value = null;
        SyncdRecord.prototype.keyId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdRecord.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdRecord.prototype, "_value", {
            get: $util.oneOfGetter($oneOfFields = ["value"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdRecord.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdRecord.create = function(properties) {
            return new SyncdRecord(properties);
        };

        SyncdRecord.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                $root.proto.SyncdIndex.encode(m.index, w.uint32(10).fork(), q + 1).ldelim();
            if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                $root.proto.SyncdValue.encode(m.value, w.uint32(18).fork(), q + 1).ldelim();
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                $root.proto.KeyId.encode(m.keyId, w.uint32(26).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdRecord.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdRecord();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.index = $root.proto.SyncdIndex.decode(r, r.uint32(), $undefined, q + 1, m.index);
                        m._index = "index";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.value = $root.proto.SyncdValue.decode(r, r.uint32(), $undefined, q + 1, m.value);
                        m._value = "value";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.keyId = $root.proto.KeyId.decode(r, r.uint32(), $undefined, q + 1, m.keyId);
                        m._keyId = "keyId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdRecord.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdRecord)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdRecord: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdRecord();
            if (d.index != null) {
                if (!$util.isObject(d.index))
                    throw $TypeError(".proto.SyncdRecord.index: object expected");
                m.index = $root.proto.SyncdIndex.fromObject(d.index, q + 1);
            }
            if (d.value != null) {
                if (!$util.isObject(d.value))
                    throw $TypeError(".proto.SyncdRecord.value: object expected");
                m.value = $root.proto.SyncdValue.fromObject(d.value, q + 1);
            }
            if (d.keyId != null) {
                if (!$util.isObject(d.keyId))
                    throw $TypeError(".proto.SyncdRecord.keyId: object expected");
                m.keyId = $root.proto.KeyId.fromObject(d.keyId, q + 1);
            }
            return m;
        };

        SyncdRecord.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                d.index = $root.proto.SyncdIndex.toObject(m.index, o, q + 1);
            }
            if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                d.value = $root.proto.SyncdValue.toObject(m.value, o, q + 1);
            }
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                d.keyId = $root.proto.KeyId.toObject(m.keyId, o, q + 1);
            }
            return d;
        };

        SyncdRecord.prototype.toJSON = function() {
            return SyncdRecord.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdRecord.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdRecord";
        };

        return SyncdRecord;
    })();

    proto.SyncdSnapshot = (function() {

        const SyncdSnapshot = function (p) {
            this.records = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdSnapshot.prototype.version = null;
        SyncdSnapshot.prototype.records = $util.emptyArray;
        SyncdSnapshot.prototype.mac = null;
        SyncdSnapshot.prototype.keyId = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdSnapshot.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdSnapshot.prototype, "_mac", {
            get: $util.oneOfGetter($oneOfFields = ["mac"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdSnapshot.prototype, "_keyId", {
            get: $util.oneOfGetter($oneOfFields = ["keyId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdSnapshot.create = function(properties) {
            return new SyncdSnapshot(properties);
        };

        SyncdSnapshot.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                $root.proto.SyncdVersion.encode(m.version, w.uint32(10).fork(), q + 1).ldelim();
            if (m.records != null && m.records.length) {
                for (var i = 0; i < m.records.length; ++i)
                    $root.proto.SyncdRecord.encode(m.records[i], w.uint32(18).fork(), q + 1).ldelim();
            }
            if (m.mac != null && $Object.hasOwnProperty.call(m, "mac"))
                w.uint32(26).bytes(m.mac);
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId"))
                $root.proto.KeyId.encode(m.keyId, w.uint32(34).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdSnapshot.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdSnapshot();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.version = $root.proto.SyncdVersion.decode(r, r.uint32(), $undefined, q + 1, m.version);
                        m._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        if (!(m.records && m.records.length))
                            m.records = [];
                        m.records.push($root.proto.SyncdRecord.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.mac = r.bytes();
                        m._mac = "mac";
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.keyId = $root.proto.KeyId.decode(r, r.uint32(), $undefined, q + 1, m.keyId);
                        m._keyId = "keyId";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdSnapshot.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdSnapshot)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdSnapshot: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdSnapshot();
            if (d.version != null) {
                if (!$util.isObject(d.version))
                    throw $TypeError(".proto.SyncdSnapshot.version: object expected");
                m.version = $root.proto.SyncdVersion.fromObject(d.version, q + 1);
            }
            if (d.records) {
                if (!$Array.isArray(d.records))
                    throw $TypeError(".proto.SyncdSnapshot.records: array expected");
                m.records = $Array(d.records.length);
                for (var i = 0; i < d.records.length; ++i) {
                    if (!$util.isObject(d.records[i]))
                        throw $TypeError(".proto.SyncdSnapshot.records: object expected");
                    m.records[i] = $root.proto.SyncdRecord.fromObject(d.records[i], q + 1);
                }
            }
            if (d.mac != null) {
                if (typeof d.mac === "string")
                    $util.base64.decode(d.mac, m.mac = $util.newBuffer($util.base64.length(d.mac)), 0);
                else if (d.mac.length >= 0)
                    m.mac = d.mac;
            }
            if (d.keyId != null) {
                if (!$util.isObject(d.keyId))
                    throw $TypeError(".proto.SyncdSnapshot.keyId: object expected");
                m.keyId = $root.proto.KeyId.fromObject(d.keyId, q + 1);
            }
            return m;
        };

        SyncdSnapshot.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.records = [];
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = $root.proto.SyncdVersion.toObject(m.version, o, q + 1);
            }
            if (m.records && m.records.length) {
                d.records = $Array(m.records.length);
                for (var j = 0; j < m.records.length; ++j) {
                    d.records[j] = $root.proto.SyncdRecord.toObject(m.records[j], o, q + 1);
                }
            }
            if (m.mac != null && $Object.hasOwnProperty.call(m, "mac")) {
                d.mac = o.bytes === $String ? $util.base64.encode(m.mac, 0, m.mac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mac) : m.mac;
            }
            if (m.keyId != null && $Object.hasOwnProperty.call(m, "keyId")) {
                d.keyId = $root.proto.KeyId.toObject(m.keyId, o, q + 1);
            }
            return d;
        };

        SyncdSnapshot.prototype.toJSON = function() {
            return SyncdSnapshot.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdSnapshot.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdSnapshot";
        };

        return SyncdSnapshot;
    })();

    proto.SyncdSnapshotRecovery = (function() {

        const SyncdSnapshotRecovery = function (p) {
            this.mutationRecords = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdSnapshotRecovery.prototype.version = null;
        SyncdSnapshotRecovery.prototype.collectionName = null;
        SyncdSnapshotRecovery.prototype.mutationRecords = $util.emptyArray;
        SyncdSnapshotRecovery.prototype.collectionLthash = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdSnapshotRecovery.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdSnapshotRecovery.prototype, "_collectionName", {
            get: $util.oneOfGetter($oneOfFields = ["collectionName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdSnapshotRecovery.prototype, "_collectionLthash", {
            get: $util.oneOfGetter($oneOfFields = ["collectionLthash"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdSnapshotRecovery.create = function(properties) {
            return new SyncdSnapshotRecovery(properties);
        };

        SyncdSnapshotRecovery.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                $root.proto.SyncdVersion.encode(m.version, w.uint32(10).fork(), q + 1).ldelim();
            if (m.collectionName != null && $Object.hasOwnProperty.call(m, "collectionName"))
                w.uint32(18).string(m.collectionName);
            if (m.mutationRecords != null && m.mutationRecords.length) {
                for (var i = 0; i < m.mutationRecords.length; ++i)
                    $root.proto.SyncdPlainTextRecord.encode(m.mutationRecords[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.collectionLthash != null && $Object.hasOwnProperty.call(m, "collectionLthash"))
                w.uint32(34).bytes(m.collectionLthash);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdSnapshotRecovery.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdSnapshotRecovery();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.version = $root.proto.SyncdVersion.decode(r, r.uint32(), $undefined, q + 1, m.version);
                        m._version = "version";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.collectionName = r.stringVerify();
                        m._collectionName = "collectionName";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.mutationRecords && m.mutationRecords.length))
                            m.mutationRecords = [];
                        m.mutationRecords.push($root.proto.SyncdPlainTextRecord.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.collectionLthash = r.bytes();
                        m._collectionLthash = "collectionLthash";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdSnapshotRecovery.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdSnapshotRecovery)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdSnapshotRecovery: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdSnapshotRecovery();
            if (d.version != null) {
                if (!$util.isObject(d.version))
                    throw $TypeError(".proto.SyncdSnapshotRecovery.version: object expected");
                m.version = $root.proto.SyncdVersion.fromObject(d.version, q + 1);
            }
            if (d.collectionName != null) {
                m.collectionName = $String(d.collectionName);
            }
            if (d.mutationRecords) {
                if (!$Array.isArray(d.mutationRecords))
                    throw $TypeError(".proto.SyncdSnapshotRecovery.mutationRecords: array expected");
                m.mutationRecords = $Array(d.mutationRecords.length);
                for (var i = 0; i < d.mutationRecords.length; ++i) {
                    if (!$util.isObject(d.mutationRecords[i]))
                        throw $TypeError(".proto.SyncdSnapshotRecovery.mutationRecords: object expected");
                    m.mutationRecords[i] = $root.proto.SyncdPlainTextRecord.fromObject(d.mutationRecords[i], q + 1);
                }
            }
            if (d.collectionLthash != null) {
                if (typeof d.collectionLthash === "string")
                    $util.base64.decode(d.collectionLthash, m.collectionLthash = $util.newBuffer($util.base64.length(d.collectionLthash)), 0);
                else if (d.collectionLthash.length >= 0)
                    m.collectionLthash = d.collectionLthash;
            }
            return m;
        };

        SyncdSnapshotRecovery.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.mutationRecords = [];
            }
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                d.version = $root.proto.SyncdVersion.toObject(m.version, o, q + 1);
            }
            if (m.collectionName != null && $Object.hasOwnProperty.call(m, "collectionName")) {
                d.collectionName = m.collectionName;
            }
            if (m.mutationRecords && m.mutationRecords.length) {
                d.mutationRecords = $Array(m.mutationRecords.length);
                for (var j = 0; j < m.mutationRecords.length; ++j) {
                    d.mutationRecords[j] = $root.proto.SyncdPlainTextRecord.toObject(m.mutationRecords[j], o, q + 1);
                }
            }
            if (m.collectionLthash != null && $Object.hasOwnProperty.call(m, "collectionLthash")) {
                d.collectionLthash = o.bytes === $String ? $util.base64.encode(m.collectionLthash, 0, m.collectionLthash.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.collectionLthash) : m.collectionLthash;
            }
            return d;
        };

        SyncdSnapshotRecovery.prototype.toJSON = function() {
            return SyncdSnapshotRecovery.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdSnapshotRecovery.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdSnapshotRecovery";
        };

        return SyncdSnapshotRecovery;
    })();

    proto.SyncdValue = (function() {

        const SyncdValue = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdValue.prototype.blob = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdValue.prototype, "_blob", {
            get: $util.oneOfGetter($oneOfFields = ["blob"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdValue.create = function(properties) {
            return new SyncdValue(properties);
        };

        SyncdValue.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.blob != null && $Object.hasOwnProperty.call(m, "blob"))
                w.uint32(10).bytes(m.blob);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdValue.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdValue();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.blob = r.bytes();
                        m._blob = "blob";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdValue.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdValue)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdValue: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdValue();
            if (d.blob != null) {
                if (typeof d.blob === "string")
                    $util.base64.decode(d.blob, m.blob = $util.newBuffer($util.base64.length(d.blob)), 0);
                else if (d.blob.length >= 0)
                    m.blob = d.blob;
            }
            return m;
        };

        SyncdValue.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.blob != null && $Object.hasOwnProperty.call(m, "blob")) {
                d.blob = o.bytes === $String ? $util.base64.encode(m.blob, 0, m.blob.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.blob) : m.blob;
            }
            return d;
        };

        SyncdValue.prototype.toJSON = function() {
            return SyncdValue.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdValue.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdValue";
        };

        return SyncdValue;
    })();

    proto.SyncdVersion = (function() {

        const SyncdVersion = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        SyncdVersion.prototype.version = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(SyncdVersion.prototype, "_version", {
            get: $util.oneOfGetter($oneOfFields = ["version"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        SyncdVersion.create = function(properties) {
            return new SyncdVersion(properties);
        };

        SyncdVersion.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.version != null && $Object.hasOwnProperty.call(m, "version"))
                w.uint32(8).uint64(m.version);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        SyncdVersion.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.SyncdVersion();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.version = r.uint64();
                        m._version = "version";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        SyncdVersion.fromObject = function (d, q) {
            if (d instanceof $root.proto.SyncdVersion)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.SyncdVersion: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.SyncdVersion();
            if (d.version != null) {
                if ($util.Long)
                    m.version = $util.Long.fromValue(d.version, true);
                else if (typeof d.version === "string")
                    m.version = $parseInt(d.version, 10);
                else if (typeof d.version === "number")
                    m.version = d.version;
                else if (typeof d.version === "object")
                    m.version = new $util.LongBits(d.version.low >>> 0, d.version.high >>> 0).toNumber(true);
            }
            return m;
        };

        SyncdVersion.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.version != null && $Object.hasOwnProperty.call(m, "version")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.version = typeof m.version === "number" ? $BigInt(m.version) : $util.Long.fromBits(m.version.low >>> 0, m.version.high >>> 0, true).toBigInt();
                else if (typeof m.version === "number")
                    d.version = o.longs === $String ? $String(m.version) : m.version;
                else
                    d.version = o.longs === $String ? $util.Long.prototype.toString.call(m.version) : o.longs === $Number ? new $util.LongBits(m.version.low >>> 0, m.version.high >>> 0).toNumber(true) : m.version;
            }
            return d;
        };

        SyncdVersion.prototype.toJSON = function() {
            return SyncdVersion.toObject(this, $protobuf.util.toJSONOptions);
        };

        SyncdVersion.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.SyncdVersion";
        };

        return SyncdVersion;
    })();

    proto.TapLinkAction = (function() {

        const TapLinkAction = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        TapLinkAction.prototype.title = null;
        TapLinkAction.prototype.tapUrl = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(TapLinkAction.prototype, "_title", {
            get: $util.oneOfGetter($oneOfFields = ["title"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(TapLinkAction.prototype, "_tapUrl", {
            get: $util.oneOfGetter($oneOfFields = ["tapUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TapLinkAction.create = function(properties) {
            return new TapLinkAction(properties);
        };

        TapLinkAction.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.title != null && $Object.hasOwnProperty.call(m, "title"))
                w.uint32(10).string(m.title);
            if (m.tapUrl != null && $Object.hasOwnProperty.call(m, "tapUrl"))
                w.uint32(18).string(m.tapUrl);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        TapLinkAction.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.TapLinkAction();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.title = r.stringVerify();
                        m._title = "title";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.tapUrl = r.stringVerify();
                        m._tapUrl = "tapUrl";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        TapLinkAction.fromObject = function (d, q) {
            if (d instanceof $root.proto.TapLinkAction)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.TapLinkAction: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.TapLinkAction();
            if (d.title != null) {
                m.title = $String(d.title);
            }
            if (d.tapUrl != null) {
                m.tapUrl = $String(d.tapUrl);
            }
            return m;
        };

        TapLinkAction.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.title != null && $Object.hasOwnProperty.call(m, "title")) {
                d.title = m.title;
            }
            if (m.tapUrl != null && $Object.hasOwnProperty.call(m, "tapUrl")) {
                d.tapUrl = m.tapUrl;
            }
            return d;
        };

        TapLinkAction.prototype.toJSON = function() {
            return TapLinkAction.toObject(this, $protobuf.util.toJSONOptions);
        };

        TapLinkAction.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TapLinkAction";
        };

        return TapLinkAction;
    })();

    proto.TemplateButton = (function() {

        const TemplateButton = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        TemplateButton.prototype.index = null;
        TemplateButton.prototype.quickReplyButton = null;
        TemplateButton.prototype.urlButton = null;
        TemplateButton.prototype.callButton = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(TemplateButton.prototype, "_index", {
            get: $util.oneOfGetter($oneOfFields = ["index"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        $Object.defineProperty(TemplateButton.prototype, "button", {
            get: $util.oneOfGetter($oneOfFields = ["quickReplyButton", "urlButton", "callButton"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        TemplateButton.create = function(properties) {
            return new TemplateButton(properties);
        };

        TemplateButton.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.quickReplyButton != null && $Object.hasOwnProperty.call(m, "quickReplyButton"))
                $root.proto.TemplateButton.QuickReplyButton.encode(m.quickReplyButton, w.uint32(10).fork(), q + 1).ldelim();
            if (m.urlButton != null && $Object.hasOwnProperty.call(m, "urlButton"))
                $root.proto.TemplateButton.URLButton.encode(m.urlButton, w.uint32(18).fork(), q + 1).ldelim();
            if (m.callButton != null && $Object.hasOwnProperty.call(m, "callButton"))
                $root.proto.TemplateButton.CallButton.encode(m.callButton, w.uint32(26).fork(), q + 1).ldelim();
            if (m.index != null && $Object.hasOwnProperty.call(m, "index"))
                w.uint32(32).uint32(m.index);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        TemplateButton.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.TemplateButton();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 4: {
                        if (u !== 0)
                            break;
                        m.index = r.uint32();
                        m._index = "index";
                        continue;
                    }
                case 1: {
                        if (u !== 2)
                            break;
                        m.quickReplyButton = $root.proto.TemplateButton.QuickReplyButton.decode(r, r.uint32(), $undefined, q + 1, m.quickReplyButton);
                        m.button = "quickReplyButton";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.urlButton = $root.proto.TemplateButton.URLButton.decode(r, r.uint32(), $undefined, q + 1, m.urlButton);
                        m.button = "urlButton";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.callButton = $root.proto.TemplateButton.CallButton.decode(r, r.uint32(), $undefined, q + 1, m.callButton);
                        m.button = "callButton";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        TemplateButton.fromObject = function (d, q) {
            if (d instanceof $root.proto.TemplateButton)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.TemplateButton: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.TemplateButton();
            if (d.index != null) {
                m.index = d.index >>> 0;
            }
            if (d.quickReplyButton != null) {
                if (!$util.isObject(d.quickReplyButton))
                    throw $TypeError(".proto.TemplateButton.quickReplyButton: object expected");
                m.quickReplyButton = $root.proto.TemplateButton.QuickReplyButton.fromObject(d.quickReplyButton, q + 1);
            }
            if (d.urlButton != null) {
                if (!$util.isObject(d.urlButton))
                    throw $TypeError(".proto.TemplateButton.urlButton: object expected");
                m.urlButton = $root.proto.TemplateButton.URLButton.fromObject(d.urlButton, q + 1);
            }
            if (d.callButton != null) {
                if (!$util.isObject(d.callButton))
                    throw $TypeError(".proto.TemplateButton.callButton: object expected");
                m.callButton = $root.proto.TemplateButton.CallButton.fromObject(d.callButton, q + 1);
            }
            return m;
        };

        TemplateButton.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.quickReplyButton != null && $Object.hasOwnProperty.call(m, "quickReplyButton")) {
                d.quickReplyButton = $root.proto.TemplateButton.QuickReplyButton.toObject(m.quickReplyButton, o, q + 1);
                if (o.oneofs)
                    d.button = "quickReplyButton";
            }
            if (m.urlButton != null && $Object.hasOwnProperty.call(m, "urlButton")) {
                d.urlButton = $root.proto.TemplateButton.URLButton.toObject(m.urlButton, o, q + 1);
                if (o.oneofs)
                    d.button = "urlButton";
            }
            if (m.callButton != null && $Object.hasOwnProperty.call(m, "callButton")) {
                d.callButton = $root.proto.TemplateButton.CallButton.toObject(m.callButton, o, q + 1);
                if (o.oneofs)
                    d.button = "callButton";
            }
            if (m.index != null && $Object.hasOwnProperty.call(m, "index")) {
                d.index = m.index;
            }
            return d;
        };

        TemplateButton.prototype.toJSON = function() {
            return TemplateButton.toObject(this, $protobuf.util.toJSONOptions);
        };

        TemplateButton.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.TemplateButton";
        };

        TemplateButton.CallButton = (function() {

            const CallButton = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            CallButton.prototype.displayText = null;
            CallButton.prototype.phoneNumber = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(CallButton.prototype, "_phoneNumber", {
                get: $util.oneOfGetter($oneOfFields = ["phoneNumber"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            CallButton.create = function(properties) {
                return new CallButton(properties);
            };

            CallButton.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                    $root.proto.Message.HighlyStructuredMessage.encode(m.displayText, w.uint32(10).fork(), q + 1).ldelim();
                if (m.phoneNumber != null && $Object.hasOwnProperty.call(m, "phoneNumber"))
                    $root.proto.Message.HighlyStructuredMessage.encode(m.phoneNumber, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            CallButton.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.TemplateButton.CallButton();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayText = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.displayText);
                            m._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.phoneNumber = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.phoneNumber);
                            m._phoneNumber = "phoneNumber";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            CallButton.fromObject = function (d, q) {
                if (d instanceof $root.proto.TemplateButton.CallButton)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.TemplateButton.CallButton: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.TemplateButton.CallButton();
                if (d.displayText != null) {
                    if (!$util.isObject(d.displayText))
                        throw $TypeError(".proto.TemplateButton.CallButton.displayText: object expected");
                    m.displayText = $root.proto.Message.HighlyStructuredMessage.fromObject(d.displayText, q + 1);
                }
                if (d.phoneNumber != null) {
                    if (!$util.isObject(d.phoneNumber))
                        throw $TypeError(".proto.TemplateButton.CallButton.phoneNumber: object expected");
                    m.phoneNumber = $root.proto.Message.HighlyStructuredMessage.fromObject(d.phoneNumber, q + 1);
                }
                return m;
            };

            CallButton.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                    d.displayText = $root.proto.Message.HighlyStructuredMessage.toObject(m.displayText, o, q + 1);
                }
                if (m.phoneNumber != null && $Object.hasOwnProperty.call(m, "phoneNumber")) {
                    d.phoneNumber = $root.proto.Message.HighlyStructuredMessage.toObject(m.phoneNumber, o, q + 1);
                }
                return d;
            };

            CallButton.prototype.toJSON = function() {
                return CallButton.toObject(this, $protobuf.util.toJSONOptions);
            };

            CallButton.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.TemplateButton.CallButton";
            };

            return CallButton;
        })();

        TemplateButton.QuickReplyButton = (function() {

            const QuickReplyButton = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            QuickReplyButton.prototype.displayText = null;
            QuickReplyButton.prototype.id = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuickReplyButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(QuickReplyButton.prototype, "_id", {
                get: $util.oneOfGetter($oneOfFields = ["id"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            QuickReplyButton.create = function(properties) {
                return new QuickReplyButton(properties);
            };

            QuickReplyButton.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                    $root.proto.Message.HighlyStructuredMessage.encode(m.displayText, w.uint32(10).fork(), q + 1).ldelim();
                if (m.id != null && $Object.hasOwnProperty.call(m, "id"))
                    w.uint32(18).string(m.id);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            QuickReplyButton.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.TemplateButton.QuickReplyButton();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayText = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.displayText);
                            m._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.id = r.stringVerify();
                            m._id = "id";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            QuickReplyButton.fromObject = function (d, q) {
                if (d instanceof $root.proto.TemplateButton.QuickReplyButton)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.TemplateButton.QuickReplyButton: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.TemplateButton.QuickReplyButton();
                if (d.displayText != null) {
                    if (!$util.isObject(d.displayText))
                        throw $TypeError(".proto.TemplateButton.QuickReplyButton.displayText: object expected");
                    m.displayText = $root.proto.Message.HighlyStructuredMessage.fromObject(d.displayText, q + 1);
                }
                if (d.id != null) {
                    m.id = $String(d.id);
                }
                return m;
            };

            QuickReplyButton.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                    d.displayText = $root.proto.Message.HighlyStructuredMessage.toObject(m.displayText, o, q + 1);
                }
                if (m.id != null && $Object.hasOwnProperty.call(m, "id")) {
                    d.id = m.id;
                }
                return d;
            };

            QuickReplyButton.prototype.toJSON = function() {
                return QuickReplyButton.toObject(this, $protobuf.util.toJSONOptions);
            };

            QuickReplyButton.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.TemplateButton.QuickReplyButton";
            };

            return QuickReplyButton;
        })();

        TemplateButton.URLButton = (function() {

            const URLButton = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            URLButton.prototype.displayText = null;
            URLButton.prototype.url = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(URLButton.prototype, "_displayText", {
                get: $util.oneOfGetter($oneOfFields = ["displayText"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(URLButton.prototype, "_url", {
                get: $util.oneOfGetter($oneOfFields = ["url"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            URLButton.create = function(properties) {
                return new URLButton(properties);
            };

            URLButton.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText"))
                    $root.proto.Message.HighlyStructuredMessage.encode(m.displayText, w.uint32(10).fork(), q + 1).ldelim();
                if (m.url != null && $Object.hasOwnProperty.call(m, "url"))
                    $root.proto.Message.HighlyStructuredMessage.encode(m.url, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            URLButton.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.TemplateButton.URLButton();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.displayText = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.displayText);
                            m._displayText = "displayText";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.url = $root.proto.Message.HighlyStructuredMessage.decode(r, r.uint32(), $undefined, q + 1, m.url);
                            m._url = "url";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            URLButton.fromObject = function (d, q) {
                if (d instanceof $root.proto.TemplateButton.URLButton)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.TemplateButton.URLButton: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.TemplateButton.URLButton();
                if (d.displayText != null) {
                    if (!$util.isObject(d.displayText))
                        throw $TypeError(".proto.TemplateButton.URLButton.displayText: object expected");
                    m.displayText = $root.proto.Message.HighlyStructuredMessage.fromObject(d.displayText, q + 1);
                }
                if (d.url != null) {
                    if (!$util.isObject(d.url))
                        throw $TypeError(".proto.TemplateButton.URLButton.url: object expected");
                    m.url = $root.proto.Message.HighlyStructuredMessage.fromObject(d.url, q + 1);
                }
                return m;
            };

            URLButton.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.displayText != null && $Object.hasOwnProperty.call(m, "displayText")) {
                    d.displayText = $root.proto.Message.HighlyStructuredMessage.toObject(m.displayText, o, q + 1);
                }
                if (m.url != null && $Object.hasOwnProperty.call(m, "url")) {
                    d.url = $root.proto.Message.HighlyStructuredMessage.toObject(m.url, o, q + 1);
                }
                return d;
            };

            URLButton.prototype.toJSON = function() {
                return URLButton.toObject(this, $protobuf.util.toJSONOptions);
            };

            URLButton.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.TemplateButton.URLButton";
            };

            return URLButton;
        })();

        return TemplateButton;
    })();

    proto.ThreadID = (function() {

        const ThreadID = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        ThreadID.prototype.threadType = null;
        ThreadID.prototype.threadKey = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ThreadID.prototype, "_threadType", {
            get: $util.oneOfGetter($oneOfFields = ["threadType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(ThreadID.prototype, "_threadKey", {
            get: $util.oneOfGetter($oneOfFields = ["threadKey"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        ThreadID.create = function(properties) {
            return new ThreadID(properties);
        };

        ThreadID.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.threadType != null && $Object.hasOwnProperty.call(m, "threadType"))
                w.uint32(8).int32(m.threadType);
            if (m.threadKey != null && $Object.hasOwnProperty.call(m, "threadKey"))
                $root.proto.MessageKey.encode(m.threadKey, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        ThreadID.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.ThreadID(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.threadType = r.int32();
                        m._threadType = "threadType";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.threadKey = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.threadKey);
                        m._threadKey = "threadKey";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        ThreadID.fromObject = function (d, q) {
            if (d instanceof $root.proto.ThreadID)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.ThreadID: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.ThreadID();
            switch (d.threadType) {
            case "UNKNOWN":
            case 0:
                m.threadType = 0;
                break;
            case "VIEW_REPLIES":
            case 1:
                m.threadType = 1;
                break;
            case "AI_THREAD":
            case 2:
                m.threadType = 2;
                break;
            default:
                if (typeof d.threadType === "number" && (d.threadType | 0) === d.threadType)
                    m.threadType = d.threadType;
            }
            if (d.threadKey != null) {
                if (!$util.isObject(d.threadKey))
                    throw $TypeError(".proto.ThreadID.threadKey: object expected");
                m.threadKey = $root.proto.MessageKey.fromObject(d.threadKey, q + 1);
            }
            return m;
        };

        ThreadID.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.threadType != null && $Object.hasOwnProperty.call(m, "threadType")) {
                d.threadType = o.enums === $String ? $root.proto.ThreadID.ThreadType[m.threadType] === $undefined ? m.threadType : $root.proto.ThreadID.ThreadType[m.threadType] : m.threadType;
            }
            if (m.threadKey != null && $Object.hasOwnProperty.call(m, "threadKey")) {
                d.threadKey = $root.proto.MessageKey.toObject(m.threadKey, o, q + 1);
            }
            return d;
        };

        ThreadID.prototype.toJSON = function() {
            return ThreadID.toObject(this, $protobuf.util.toJSONOptions);
        };

        ThreadID.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.ThreadID";
        };

        ThreadID.ThreadType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "VIEW_REPLIES"] = 1;
            values[valuesById[2] = "AI_THREAD"] = 2;
            return values;
        })();

        return ThreadID;
    })();

    proto.UnCountedAssociatedMessageList = (function() {

        const UnCountedAssociatedMessageList = function (p) {
            this.messages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        UnCountedAssociatedMessageList.prototype.messages = $util.emptyArray;
        UnCountedAssociatedMessageList.prototype.parentMessage = null;
        UnCountedAssociatedMessageList.prototype.associationType = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UnCountedAssociatedMessageList.prototype, "_parentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["parentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UnCountedAssociatedMessageList.prototype, "_associationType", {
            get: $util.oneOfGetter($oneOfFields = ["associationType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        UnCountedAssociatedMessageList.create = function(properties) {
            return new UnCountedAssociatedMessageList(properties);
        };

        UnCountedAssociatedMessageList.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messages != null && m.messages.length) {
                for (var i = 0; i < m.messages.length; ++i)
                    $root.proto.WebMessageInfo.encode(m.messages[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.parentMessage != null && $Object.hasOwnProperty.call(m, "parentMessage"))
                $root.proto.MessageKey.encode(m.parentMessage, w.uint32(18).fork(), q + 1).ldelim();
            if (m.associationType != null && $Object.hasOwnProperty.call(m, "associationType"))
                w.uint32(24).int32(m.associationType);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        UnCountedAssociatedMessageList.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UnCountedAssociatedMessageList(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.messages && m.messages.length))
                            m.messages = [];
                        m.messages.push($root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.parentMessage = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.parentMessage);
                        m._parentMessage = "parentMessage";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.associationType = r.int32();
                        m._associationType = "associationType";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        UnCountedAssociatedMessageList.fromObject = function (d, q) {
            if (d instanceof $root.proto.UnCountedAssociatedMessageList)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.UnCountedAssociatedMessageList: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.UnCountedAssociatedMessageList();
            if (d.messages) {
                if (!$Array.isArray(d.messages))
                    throw $TypeError(".proto.UnCountedAssociatedMessageList.messages: array expected");
                m.messages = $Array(d.messages.length);
                for (var i = 0; i < d.messages.length; ++i) {
                    if (!$util.isObject(d.messages[i]))
                        throw $TypeError(".proto.UnCountedAssociatedMessageList.messages: object expected");
                    m.messages[i] = $root.proto.WebMessageInfo.fromObject(d.messages[i], q + 1);
                }
            }
            if (d.parentMessage != null) {
                if (!$util.isObject(d.parentMessage))
                    throw $TypeError(".proto.UnCountedAssociatedMessageList.parentMessage: object expected");
                m.parentMessage = $root.proto.MessageKey.fromObject(d.parentMessage, q + 1);
            }
            switch (d.associationType) {
            case "UNKNOWN":
            case 0:
                m.associationType = 0;
                break;
            case "MEDIA_ALBUM":
            case 1:
                m.associationType = 1;
                break;
            case "BOT_PLUGIN":
            case 2:
                m.associationType = 2;
                break;
            case "EVENT_COVER_IMAGE":
            case 3:
                m.associationType = 3;
                break;
            case "STATUS_POLL":
            case 4:
                m.associationType = 4;
                break;
            case "HD_VIDEO_DUAL_UPLOAD":
            case 5:
                m.associationType = 5;
                break;
            case "STATUS_EXTERNAL_RESHARE":
            case 6:
                m.associationType = 6;
                break;
            case "MEDIA_POLL":
            case 7:
                m.associationType = 7;
                break;
            case "STATUS_ADD_YOURS":
            case 8:
                m.associationType = 8;
                break;
            case "STATUS_NOTIFICATION":
            case 9:
                m.associationType = 9;
                break;
            case "HD_IMAGE_DUAL_UPLOAD":
            case 10:
                m.associationType = 10;
                break;
            case "STICKER_ANNOTATION":
            case 11:
                m.associationType = 11;
                break;
            case "MOTION_PHOTO":
            case 12:
                m.associationType = 12;
                break;
            case "STATUS_LINK_ACTION":
            case 13:
                m.associationType = 13;
                break;
            case "VIEW_ALL_REPLIES":
            case 14:
                m.associationType = 14;
                break;
            case "STATUS_ADD_YOURS_AI_IMAGINE":
            case 15:
                m.associationType = 15;
                break;
            case "STATUS_QUESTION":
            case 16:
                m.associationType = 16;
                break;
            case "STATUS_ADD_YOURS_DIWALI":
            case 17:
                m.associationType = 17;
                break;
            case "STATUS_REACTION":
            case 18:
                m.associationType = 18;
                break;
            case "HEVC_VIDEO_DUAL_UPLOAD":
            case 19:
                m.associationType = 19;
                break;
            case "POLL_ADD_OPTION":
            case 20:
                m.associationType = 20;
                break;
            default:
                if (typeof d.associationType === "number" && (d.associationType | 0) === d.associationType)
                    m.associationType = d.associationType;
            }
            return m;
        };

        UnCountedAssociatedMessageList.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.messages = [];
            }
            if (m.messages && m.messages.length) {
                d.messages = $Array(m.messages.length);
                for (var j = 0; j < m.messages.length; ++j) {
                    d.messages[j] = $root.proto.WebMessageInfo.toObject(m.messages[j], o, q + 1);
                }
            }
            if (m.parentMessage != null && $Object.hasOwnProperty.call(m, "parentMessage")) {
                d.parentMessage = $root.proto.MessageKey.toObject(m.parentMessage, o, q + 1);
            }
            if (m.associationType != null && $Object.hasOwnProperty.call(m, "associationType")) {
                d.associationType = o.enums === $String ? $root.proto.MessageAssociation.AssociationType[m.associationType] === $undefined ? m.associationType : $root.proto.MessageAssociation.AssociationType[m.associationType] : m.associationType;
            }
            return d;
        };

        UnCountedAssociatedMessageList.prototype.toJSON = function() {
            return UnCountedAssociatedMessageList.toObject(this, $protobuf.util.toJSONOptions);
        };

        UnCountedAssociatedMessageList.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UnCountedAssociatedMessageList";
        };

        return UnCountedAssociatedMessageList;
    })();

    proto.UnCountedAssociatedMessageListWithMessageBytes = (function() {

        const UnCountedAssociatedMessageListWithMessageBytes = function (p) {
            this.messages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        UnCountedAssociatedMessageListWithMessageBytes.prototype.messages = $util.emptyArray;
        UnCountedAssociatedMessageListWithMessageBytes.prototype.parentMessage = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UnCountedAssociatedMessageListWithMessageBytes.prototype, "_parentMessage", {
            get: $util.oneOfGetter($oneOfFields = ["parentMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        UnCountedAssociatedMessageListWithMessageBytes.create = function(properties) {
            return new UnCountedAssociatedMessageListWithMessageBytes(properties);
        };

        UnCountedAssociatedMessageListWithMessageBytes.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.messages != null && m.messages.length) {
                for (var i = 0; i < m.messages.length; ++i)
                    $root.proto.WebMessageInfoWithMessageBytes.encode(m.messages[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.parentMessage != null && $Object.hasOwnProperty.call(m, "parentMessage"))
                $root.proto.MessageKey.encode(m.parentMessage, w.uint32(18).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        UnCountedAssociatedMessageListWithMessageBytes.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UnCountedAssociatedMessageListWithMessageBytes();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.messages && m.messages.length))
                            m.messages = [];
                        m.messages.push($root.proto.WebMessageInfoWithMessageBytes.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.parentMessage = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.parentMessage);
                        m._parentMessage = "parentMessage";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        UnCountedAssociatedMessageListWithMessageBytes.fromObject = function (d, q) {
            if (d instanceof $root.proto.UnCountedAssociatedMessageListWithMessageBytes)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.UnCountedAssociatedMessageListWithMessageBytes: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.UnCountedAssociatedMessageListWithMessageBytes();
            if (d.messages) {
                if (!$Array.isArray(d.messages))
                    throw $TypeError(".proto.UnCountedAssociatedMessageListWithMessageBytes.messages: array expected");
                m.messages = $Array(d.messages.length);
                for (var i = 0; i < d.messages.length; ++i) {
                    if (!$util.isObject(d.messages[i]))
                        throw $TypeError(".proto.UnCountedAssociatedMessageListWithMessageBytes.messages: object expected");
                    m.messages[i] = $root.proto.WebMessageInfoWithMessageBytes.fromObject(d.messages[i], q + 1);
                }
            }
            if (d.parentMessage != null) {
                if (!$util.isObject(d.parentMessage))
                    throw $TypeError(".proto.UnCountedAssociatedMessageListWithMessageBytes.parentMessage: object expected");
                m.parentMessage = $root.proto.MessageKey.fromObject(d.parentMessage, q + 1);
            }
            return m;
        };

        UnCountedAssociatedMessageListWithMessageBytes.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.messages = [];
            }
            if (m.messages && m.messages.length) {
                d.messages = $Array(m.messages.length);
                for (var j = 0; j < m.messages.length; ++j) {
                    d.messages[j] = $root.proto.WebMessageInfoWithMessageBytes.toObject(m.messages[j], o, q + 1);
                }
            }
            if (m.parentMessage != null && $Object.hasOwnProperty.call(m, "parentMessage")) {
                d.parentMessage = $root.proto.MessageKey.toObject(m.parentMessage, o, q + 1);
            }
            return d;
        };

        UnCountedAssociatedMessageListWithMessageBytes.prototype.toJSON = function() {
            return UnCountedAssociatedMessageListWithMessageBytes.toObject(this, $protobuf.util.toJSONOptions);
        };

        UnCountedAssociatedMessageListWithMessageBytes.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UnCountedAssociatedMessageListWithMessageBytes";
        };

        return UnCountedAssociatedMessageListWithMessageBytes;
    })();

    proto.UrlTrackingMap = (function() {

        const UrlTrackingMap = function (p) {
            this.urlTrackingMapElements = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        UrlTrackingMap.prototype.urlTrackingMapElements = $util.emptyArray;

        UrlTrackingMap.create = function(properties) {
            return new UrlTrackingMap(properties);
        };

        UrlTrackingMap.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.urlTrackingMapElements != null && m.urlTrackingMapElements.length) {
                for (var i = 0; i < m.urlTrackingMapElements.length; ++i)
                    $root.proto.UrlTrackingMap.UrlTrackingMapElement.encode(m.urlTrackingMapElements[i], w.uint32(10).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        UrlTrackingMap.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UrlTrackingMap();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        if (!(m.urlTrackingMapElements && m.urlTrackingMapElements.length))
                            m.urlTrackingMapElements = [];
                        m.urlTrackingMapElements.push($root.proto.UrlTrackingMap.UrlTrackingMapElement.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        UrlTrackingMap.fromObject = function (d, q) {
            if (d instanceof $root.proto.UrlTrackingMap)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.UrlTrackingMap: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.UrlTrackingMap();
            if (d.urlTrackingMapElements) {
                if (!$Array.isArray(d.urlTrackingMapElements))
                    throw $TypeError(".proto.UrlTrackingMap.urlTrackingMapElements: array expected");
                m.urlTrackingMapElements = $Array(d.urlTrackingMapElements.length);
                for (var i = 0; i < d.urlTrackingMapElements.length; ++i) {
                    if (!$util.isObject(d.urlTrackingMapElements[i]))
                        throw $TypeError(".proto.UrlTrackingMap.urlTrackingMapElements: object expected");
                    m.urlTrackingMapElements[i] = $root.proto.UrlTrackingMap.UrlTrackingMapElement.fromObject(d.urlTrackingMapElements[i], q + 1);
                }
            }
            return m;
        };

        UrlTrackingMap.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.urlTrackingMapElements = [];
            }
            if (m.urlTrackingMapElements && m.urlTrackingMapElements.length) {
                d.urlTrackingMapElements = $Array(m.urlTrackingMapElements.length);
                for (var j = 0; j < m.urlTrackingMapElements.length; ++j) {
                    d.urlTrackingMapElements[j] = $root.proto.UrlTrackingMap.UrlTrackingMapElement.toObject(m.urlTrackingMapElements[j], o, q + 1);
                }
            }
            return d;
        };

        UrlTrackingMap.prototype.toJSON = function() {
            return UrlTrackingMap.toObject(this, $protobuf.util.toJSONOptions);
        };

        UrlTrackingMap.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UrlTrackingMap";
        };

        UrlTrackingMap.UrlTrackingMapElement = (function() {

            const UrlTrackingMapElement = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            UrlTrackingMapElement.prototype.originalUrl = null;
            UrlTrackingMapElement.prototype.unconsentedUsersUrl = null;
            UrlTrackingMapElement.prototype.consentedUsersUrl = null;
            UrlTrackingMapElement.prototype.cardIndex = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UrlTrackingMapElement.prototype, "_originalUrl", {
                get: $util.oneOfGetter($oneOfFields = ["originalUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UrlTrackingMapElement.prototype, "_unconsentedUsersUrl", {
                get: $util.oneOfGetter($oneOfFields = ["unconsentedUsersUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UrlTrackingMapElement.prototype, "_consentedUsersUrl", {
                get: $util.oneOfGetter($oneOfFields = ["consentedUsersUrl"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(UrlTrackingMapElement.prototype, "_cardIndex", {
                get: $util.oneOfGetter($oneOfFields = ["cardIndex"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            UrlTrackingMapElement.create = function(properties) {
                return new UrlTrackingMapElement(properties);
            };

            UrlTrackingMapElement.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.originalUrl != null && $Object.hasOwnProperty.call(m, "originalUrl"))
                    w.uint32(10).string(m.originalUrl);
                if (m.unconsentedUsersUrl != null && $Object.hasOwnProperty.call(m, "unconsentedUsersUrl"))
                    w.uint32(18).string(m.unconsentedUsersUrl);
                if (m.consentedUsersUrl != null && $Object.hasOwnProperty.call(m, "consentedUsersUrl"))
                    w.uint32(26).string(m.consentedUsersUrl);
                if (m.cardIndex != null && $Object.hasOwnProperty.call(m, "cardIndex"))
                    w.uint32(32).uint32(m.cardIndex);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            UrlTrackingMapElement.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UrlTrackingMap.UrlTrackingMapElement();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.originalUrl = r.stringVerify();
                            m._originalUrl = "originalUrl";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.unconsentedUsersUrl = r.stringVerify();
                            m._unconsentedUsersUrl = "unconsentedUsersUrl";
                            continue;
                        }
                    case 3: {
                            if (u !== 2)
                                break;
                            m.consentedUsersUrl = r.stringVerify();
                            m._consentedUsersUrl = "consentedUsersUrl";
                            continue;
                        }
                    case 4: {
                            if (u !== 0)
                                break;
                            m.cardIndex = r.uint32();
                            m._cardIndex = "cardIndex";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            UrlTrackingMapElement.fromObject = function (d, q) {
                if (d instanceof $root.proto.UrlTrackingMap.UrlTrackingMapElement)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.UrlTrackingMap.UrlTrackingMapElement: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.UrlTrackingMap.UrlTrackingMapElement();
                if (d.originalUrl != null) {
                    m.originalUrl = $String(d.originalUrl);
                }
                if (d.unconsentedUsersUrl != null) {
                    m.unconsentedUsersUrl = $String(d.unconsentedUsersUrl);
                }
                if (d.consentedUsersUrl != null) {
                    m.consentedUsersUrl = $String(d.consentedUsersUrl);
                }
                if (d.cardIndex != null) {
                    m.cardIndex = d.cardIndex >>> 0;
                }
                return m;
            };

            UrlTrackingMapElement.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.originalUrl != null && $Object.hasOwnProperty.call(m, "originalUrl")) {
                    d.originalUrl = m.originalUrl;
                }
                if (m.unconsentedUsersUrl != null && $Object.hasOwnProperty.call(m, "unconsentedUsersUrl")) {
                    d.unconsentedUsersUrl = m.unconsentedUsersUrl;
                }
                if (m.consentedUsersUrl != null && $Object.hasOwnProperty.call(m, "consentedUsersUrl")) {
                    d.consentedUsersUrl = m.consentedUsersUrl;
                }
                if (m.cardIndex != null && $Object.hasOwnProperty.call(m, "cardIndex")) {
                    d.cardIndex = m.cardIndex;
                }
                return d;
            };

            UrlTrackingMapElement.prototype.toJSON = function() {
                return UrlTrackingMapElement.toObject(this, $protobuf.util.toJSONOptions);
            };

            UrlTrackingMapElement.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.UrlTrackingMap.UrlTrackingMapElement";
            };

            return UrlTrackingMapElement;
        })();

        return UrlTrackingMap;
    })();

    proto.UserPassword = (function() {

        const UserPassword = function (p) {
            this.transformerArg = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        UserPassword.prototype.encoding = null;
        UserPassword.prototype.transformer = null;
        UserPassword.prototype.transformerArg = $util.emptyArray;
        UserPassword.prototype.transformedData = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UserPassword.prototype, "_encoding", {
            get: $util.oneOfGetter($oneOfFields = ["encoding"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UserPassword.prototype, "_transformer", {
            get: $util.oneOfGetter($oneOfFields = ["transformer"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UserPassword.prototype, "_transformedData", {
            get: $util.oneOfGetter($oneOfFields = ["transformedData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        UserPassword.create = function(properties) {
            return new UserPassword(properties);
        };

        UserPassword.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.encoding != null && $Object.hasOwnProperty.call(m, "encoding"))
                w.uint32(8).int32(m.encoding);
            if (m.transformer != null && $Object.hasOwnProperty.call(m, "transformer"))
                w.uint32(16).int32(m.transformer);
            if (m.transformerArg != null && m.transformerArg.length) {
                for (var i = 0; i < m.transformerArg.length; ++i)
                    $root.proto.UserPassword.TransformerArg.encode(m.transformerArg[i], w.uint32(26).fork(), q + 1).ldelim();
            }
            if (m.transformedData != null && $Object.hasOwnProperty.call(m, "transformedData"))
                w.uint32(34).bytes(m.transformedData);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        UserPassword.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UserPassword(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.encoding = r.int32();
                        m._encoding = "encoding";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.transformer = r.int32();
                        m._transformer = "transformer";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        if (!(m.transformerArg && m.transformerArg.length))
                            m.transformerArg = [];
                        m.transformerArg.push($root.proto.UserPassword.TransformerArg.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.transformedData = r.bytes();
                        m._transformedData = "transformedData";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        UserPassword.fromObject = function (d, q) {
            if (d instanceof $root.proto.UserPassword)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.UserPassword: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.UserPassword();
            switch (d.encoding) {
            case "UTF8":
            case 0:
                m.encoding = 0;
                break;
            case "UTF8_BROKEN":
            case 1:
                m.encoding = 1;
                break;
            default:
                if (typeof d.encoding === "number" && (d.encoding | 0) === d.encoding)
                    m.encoding = d.encoding;
            }
            switch (d.transformer) {
            case "NONE":
            case 0:
                m.transformer = 0;
                break;
            case "PBKDF2_HMAC_SHA512":
            case 1:
                m.transformer = 1;
                break;
            case "PBKDF2_HMAC_SHA384":
            case 2:
                m.transformer = 2;
                break;
            default:
                if (typeof d.transformer === "number" && (d.transformer | 0) === d.transformer)
                    m.transformer = d.transformer;
            }
            if (d.transformerArg) {
                if (!$Array.isArray(d.transformerArg))
                    throw $TypeError(".proto.UserPassword.transformerArg: array expected");
                m.transformerArg = $Array(d.transformerArg.length);
                for (var i = 0; i < d.transformerArg.length; ++i) {
                    if (!$util.isObject(d.transformerArg[i]))
                        throw $TypeError(".proto.UserPassword.transformerArg: object expected");
                    m.transformerArg[i] = $root.proto.UserPassword.TransformerArg.fromObject(d.transformerArg[i], q + 1);
                }
            }
            if (d.transformedData != null) {
                if (typeof d.transformedData === "string")
                    $util.base64.decode(d.transformedData, m.transformedData = $util.newBuffer($util.base64.length(d.transformedData)), 0);
                else if (d.transformedData.length >= 0)
                    m.transformedData = d.transformedData;
            }
            return m;
        };

        UserPassword.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.transformerArg = [];
            }
            if (m.encoding != null && $Object.hasOwnProperty.call(m, "encoding")) {
                d.encoding = o.enums === $String ? $root.proto.UserPassword.Encoding[m.encoding] === $undefined ? m.encoding : $root.proto.UserPassword.Encoding[m.encoding] : m.encoding;
            }
            if (m.transformer != null && $Object.hasOwnProperty.call(m, "transformer")) {
                d.transformer = o.enums === $String ? $root.proto.UserPassword.Transformer[m.transformer] === $undefined ? m.transformer : $root.proto.UserPassword.Transformer[m.transformer] : m.transformer;
            }
            if (m.transformerArg && m.transformerArg.length) {
                d.transformerArg = $Array(m.transformerArg.length);
                for (var j = 0; j < m.transformerArg.length; ++j) {
                    d.transformerArg[j] = $root.proto.UserPassword.TransformerArg.toObject(m.transformerArg[j], o, q + 1);
                }
            }
            if (m.transformedData != null && $Object.hasOwnProperty.call(m, "transformedData")) {
                d.transformedData = o.bytes === $String ? $util.base64.encode(m.transformedData, 0, m.transformedData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.transformedData) : m.transformedData;
            }
            return d;
        };

        UserPassword.prototype.toJSON = function() {
            return UserPassword.toObject(this, $protobuf.util.toJSONOptions);
        };

        UserPassword.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UserPassword";
        };

        UserPassword.Encoding = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UTF8"] = 0;
            values[valuesById[1] = "UTF8_BROKEN"] = 1;
            return values;
        })();

        UserPassword.Transformer = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NONE"] = 0;
            values[valuesById[1] = "PBKDF2_HMAC_SHA512"] = 1;
            values[valuesById[2] = "PBKDF2_HMAC_SHA384"] = 2;
            return values;
        })();

        UserPassword.TransformerArg = (function() {

            const TransformerArg = function (p) {
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            TransformerArg.prototype.key = null;
            TransformerArg.prototype.value = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TransformerArg.prototype, "_key", {
                get: $util.oneOfGetter($oneOfFields = ["key"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(TransformerArg.prototype, "_value", {
                get: $util.oneOfGetter($oneOfFields = ["value"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            TransformerArg.create = function(properties) {
                return new TransformerArg(properties);
            };

            TransformerArg.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                    w.uint32(10).string(m.key);
                if (m.value != null && $Object.hasOwnProperty.call(m, "value"))
                    $root.proto.UserPassword.TransformerArg.Value.encode(m.value, w.uint32(18).fork(), q + 1).ldelim();
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            TransformerArg.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UserPassword.TransformerArg();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 2)
                                break;
                            m.key = r.stringVerify();
                            m._key = "key";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.value = $root.proto.UserPassword.TransformerArg.Value.decode(r, r.uint32(), $undefined, q + 1, m.value);
                            m._value = "value";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            TransformerArg.fromObject = function (d, q) {
                if (d instanceof $root.proto.UserPassword.TransformerArg)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.UserPassword.TransformerArg: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.UserPassword.TransformerArg();
                if (d.key != null) {
                    m.key = $String(d.key);
                }
                if (d.value != null) {
                    if (!$util.isObject(d.value))
                        throw $TypeError(".proto.UserPassword.TransformerArg.value: object expected");
                    m.value = $root.proto.UserPassword.TransformerArg.Value.fromObject(d.value, q + 1);
                }
                return m;
            };

            TransformerArg.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                    d.key = m.key;
                }
                if (m.value != null && $Object.hasOwnProperty.call(m, "value")) {
                    d.value = $root.proto.UserPassword.TransformerArg.Value.toObject(m.value, o, q + 1);
                }
                return d;
            };

            TransformerArg.prototype.toJSON = function() {
                return TransformerArg.toObject(this, $protobuf.util.toJSONOptions);
            };

            TransformerArg.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.UserPassword.TransformerArg";
            };

            TransformerArg.Value = (function() {

                const Value = function (p) {
                    if (p)
                        for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                            if (p[ks[i]] != null && ks[i] !== "__proto__")
                                this[ks[i]] = p[ks[i]];
                };

                Value.prototype.asBlob = null;
                Value.prototype.asUnsignedInteger = null;

                let $oneOfFields;

                $Object.defineProperty(Value.prototype, "value", {
                    get: $util.oneOfGetter($oneOfFields = ["asBlob", "asUnsignedInteger"]),
                    set: $util.oneOfSetter($oneOfFields)
                });

                Value.create = function(properties) {
                    return new Value(properties);
                };

                Value.encode = function (m, w, q) {
                    if (!w)
                        w = $Writer.create();
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    if (m.asBlob != null && $Object.hasOwnProperty.call(m, "asBlob"))
                        w.uint32(10).bytes(m.asBlob);
                    if (m.asUnsignedInteger != null && $Object.hasOwnProperty.call(m, "asUnsignedInteger"))
                        w.uint32(16).uint32(m.asUnsignedInteger);
                    if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                        for (var i = 0; i < m.$unknowns.length; ++i)
                            w.raw(m.$unknowns[i]);
                    return w;
                };

                Value.decode = function (r, l, z, q, g) {
                    if (!(r instanceof $Reader))
                        r = $Reader.create(r);
                    if (q === $undefined)
                        q = 0;
                    if (q > $Reader.recursionLimit)
                        throw $Error("max depth exceeded");
                    var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UserPassword.TransformerArg.Value();
                    while (r.pos < c) {
                        var s = r.pos;
                        var t = r.tag();
                        if (t === z) {
                            z = $undefined;
                            break;
                        }
                        var u = t & 7;
                        switch (t >>>= 3) {
                        case 1: {
                                if (u !== 2)
                                    break;
                                m.asBlob = r.bytes();
                                m.value = "asBlob";
                                continue;
                            }
                        case 2: {
                                if (u !== 0)
                                    break;
                                m.asUnsignedInteger = r.uint32();
                                m.value = "asUnsignedInteger";
                                continue;
                            }
                        }
                        r.skipType(u, q, t);
                        if (!r.discardUnknown) {
                            $util.makeProp(m, "$unknowns", false);
                            (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                        }
                    }
                    if (z !== $undefined)
                        throw $Error("missing end group");
                    return m;
                };

                Value.fromObject = function (d, q) {
                    if (d instanceof $root.proto.UserPassword.TransformerArg.Value)
                        return d;
                    if (!$util.isObject(d))
                        throw $TypeError(".proto.UserPassword.TransformerArg.Value: object expected");
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var m = new $root.proto.UserPassword.TransformerArg.Value();
                    if (d.asBlob != null) {
                        if (typeof d.asBlob === "string")
                            $util.base64.decode(d.asBlob, m.asBlob = $util.newBuffer($util.base64.length(d.asBlob)), 0);
                        else if (d.asBlob.length >= 0)
                            m.asBlob = d.asBlob;
                    }
                    if (d.asUnsignedInteger != null) {
                        m.asUnsignedInteger = d.asUnsignedInteger >>> 0;
                    }
                    return m;
                };

                Value.toObject = function (m, o, q) {
                    if (!o)
                        o = {};
                    if (q === $undefined)
                        q = 0;
                    if (q > $util.recursionLimit)
                        throw $Error("max depth exceeded");
                    var d = {};
                    if (m.asBlob != null && $Object.hasOwnProperty.call(m, "asBlob")) {
                        d.asBlob = o.bytes === $String ? $util.base64.encode(m.asBlob, 0, m.asBlob.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.asBlob) : m.asBlob;
                        if (o.oneofs)
                            d.value = "asBlob";
                    }
                    if (m.asUnsignedInteger != null && $Object.hasOwnProperty.call(m, "asUnsignedInteger")) {
                        d.asUnsignedInteger = m.asUnsignedInteger;
                        if (o.oneofs)
                            d.value = "asUnsignedInteger";
                    }
                    return d;
                };

                Value.prototype.toJSON = function() {
                    return Value.toObject(this, $protobuf.util.toJSONOptions);
                };

                Value.getTypeUrl = function(prefix) {
                    if (prefix === $undefined)
                        prefix = "type.googleapis.com";
                    return prefix + "/proto.UserPassword.TransformerArg.Value";
                };

                return Value;
            })();

            return TransformerArg;
        })();

        return UserPassword;
    })();

    proto.UserReceipt = (function() {

        const UserReceipt = function (p) {
            this.pendingDeviceJid = [];
            this.deliveredDeviceJid = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        UserReceipt.prototype.userJid = "";
        UserReceipt.prototype.receiptTimestamp = null;
        UserReceipt.prototype.readTimestamp = null;
        UserReceipt.prototype.playedTimestamp = null;
        UserReceipt.prototype.pendingDeviceJid = $util.emptyArray;
        UserReceipt.prototype.deliveredDeviceJid = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UserReceipt.prototype, "_receiptTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["receiptTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UserReceipt.prototype, "_readTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["readTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(UserReceipt.prototype, "_playedTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["playedTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        UserReceipt.create = function(properties) {
            return new UserReceipt(properties);
        };

        UserReceipt.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).string(m.userJid);
            if (m.receiptTimestamp != null && $Object.hasOwnProperty.call(m, "receiptTimestamp"))
                w.uint32(16).int64(m.receiptTimestamp);
            if (m.readTimestamp != null && $Object.hasOwnProperty.call(m, "readTimestamp"))
                w.uint32(24).int64(m.readTimestamp);
            if (m.playedTimestamp != null && $Object.hasOwnProperty.call(m, "playedTimestamp"))
                w.uint32(32).int64(m.playedTimestamp);
            if (m.pendingDeviceJid != null && m.pendingDeviceJid.length) {
                for (var i = 0; i < m.pendingDeviceJid.length; ++i)
                    w.uint32(42).string(m.pendingDeviceJid[i]);
            }
            if (m.deliveredDeviceJid != null && m.deliveredDeviceJid.length) {
                for (var i = 0; i < m.deliveredDeviceJid.length; ++i)
                    w.uint32(50).string(m.deliveredDeviceJid[i]);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        UserReceipt.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.UserReceipt();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.userJid = r.stringVerify();
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.receiptTimestamp = r.int64();
                        m._receiptTimestamp = "receiptTimestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.readTimestamp = r.int64();
                        m._readTimestamp = "readTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.playedTimestamp = r.int64();
                        m._playedTimestamp = "playedTimestamp";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(m.pendingDeviceJid && m.pendingDeviceJid.length))
                            m.pendingDeviceJid = [];
                        m.pendingDeviceJid.push(r.stringVerify());
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        if (!(m.deliveredDeviceJid && m.deliveredDeviceJid.length))
                            m.deliveredDeviceJid = [];
                        m.deliveredDeviceJid.push(r.stringVerify());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "userJid"))
                throw $util.ProtocolError("missing required 'userJid'", { instance: m });
            return m;
        };

        UserReceipt.fromObject = function (d, q) {
            if (d instanceof $root.proto.UserReceipt)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.UserReceipt: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.UserReceipt();
            if (d.userJid != null) {
                m.userJid = $String(d.userJid);
            }
            if (d.receiptTimestamp != null) {
                if ($util.Long)
                    m.receiptTimestamp = $util.Long.fromValue(d.receiptTimestamp, false);
                else if (typeof d.receiptTimestamp === "string")
                    m.receiptTimestamp = $parseInt(d.receiptTimestamp, 10);
                else if (typeof d.receiptTimestamp === "number")
                    m.receiptTimestamp = d.receiptTimestamp;
                else if (typeof d.receiptTimestamp === "object")
                    m.receiptTimestamp = new $util.LongBits(d.receiptTimestamp.low >>> 0, d.receiptTimestamp.high >>> 0).toNumber();
            }
            if (d.readTimestamp != null) {
                if ($util.Long)
                    m.readTimestamp = $util.Long.fromValue(d.readTimestamp, false);
                else if (typeof d.readTimestamp === "string")
                    m.readTimestamp = $parseInt(d.readTimestamp, 10);
                else if (typeof d.readTimestamp === "number")
                    m.readTimestamp = d.readTimestamp;
                else if (typeof d.readTimestamp === "object")
                    m.readTimestamp = new $util.LongBits(d.readTimestamp.low >>> 0, d.readTimestamp.high >>> 0).toNumber();
            }
            if (d.playedTimestamp != null) {
                if ($util.Long)
                    m.playedTimestamp = $util.Long.fromValue(d.playedTimestamp, false);
                else if (typeof d.playedTimestamp === "string")
                    m.playedTimestamp = $parseInt(d.playedTimestamp, 10);
                else if (typeof d.playedTimestamp === "number")
                    m.playedTimestamp = d.playedTimestamp;
                else if (typeof d.playedTimestamp === "object")
                    m.playedTimestamp = new $util.LongBits(d.playedTimestamp.low >>> 0, d.playedTimestamp.high >>> 0).toNumber();
            }
            if (d.pendingDeviceJid) {
                if (!$Array.isArray(d.pendingDeviceJid))
                    throw $TypeError(".proto.UserReceipt.pendingDeviceJid: array expected");
                m.pendingDeviceJid = $Array(d.pendingDeviceJid.length);
                for (var i = 0; i < d.pendingDeviceJid.length; ++i) {
                    m.pendingDeviceJid[i] = $String(d.pendingDeviceJid[i]);
                }
            }
            if (d.deliveredDeviceJid) {
                if (!$Array.isArray(d.deliveredDeviceJid))
                    throw $TypeError(".proto.UserReceipt.deliveredDeviceJid: array expected");
                m.deliveredDeviceJid = $Array(d.deliveredDeviceJid.length);
                for (var i = 0; i < d.deliveredDeviceJid.length; ++i) {
                    m.deliveredDeviceJid[i] = $String(d.deliveredDeviceJid[i]);
                }
            }
            return m;
        };

        UserReceipt.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.pendingDeviceJid = [];
                d.deliveredDeviceJid = [];
            }
            if (o.defaults) {
                d.userJid = "";
            }
            if (m.userJid != null && $Object.hasOwnProperty.call(m, "userJid")) {
                d.userJid = m.userJid;
            }
            if (m.receiptTimestamp != null && $Object.hasOwnProperty.call(m, "receiptTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.receiptTimestamp = typeof m.receiptTimestamp === "number" ? $BigInt(m.receiptTimestamp) : $util.Long.fromBits(m.receiptTimestamp.low >>> 0, m.receiptTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.receiptTimestamp === "number")
                    d.receiptTimestamp = o.longs === $String ? $String(m.receiptTimestamp) : m.receiptTimestamp;
                else
                    d.receiptTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.receiptTimestamp) : o.longs === $Number ? new $util.LongBits(m.receiptTimestamp.low >>> 0, m.receiptTimestamp.high >>> 0).toNumber() : m.receiptTimestamp;
            }
            if (m.readTimestamp != null && $Object.hasOwnProperty.call(m, "readTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.readTimestamp = typeof m.readTimestamp === "number" ? $BigInt(m.readTimestamp) : $util.Long.fromBits(m.readTimestamp.low >>> 0, m.readTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.readTimestamp === "number")
                    d.readTimestamp = o.longs === $String ? $String(m.readTimestamp) : m.readTimestamp;
                else
                    d.readTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.readTimestamp) : o.longs === $Number ? new $util.LongBits(m.readTimestamp.low >>> 0, m.readTimestamp.high >>> 0).toNumber() : m.readTimestamp;
            }
            if (m.playedTimestamp != null && $Object.hasOwnProperty.call(m, "playedTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.playedTimestamp = typeof m.playedTimestamp === "number" ? $BigInt(m.playedTimestamp) : $util.Long.fromBits(m.playedTimestamp.low >>> 0, m.playedTimestamp.high >>> 0, false).toBigInt();
                else if (typeof m.playedTimestamp === "number")
                    d.playedTimestamp = o.longs === $String ? $String(m.playedTimestamp) : m.playedTimestamp;
                else
                    d.playedTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.playedTimestamp) : o.longs === $Number ? new $util.LongBits(m.playedTimestamp.low >>> 0, m.playedTimestamp.high >>> 0).toNumber() : m.playedTimestamp;
            }
            if (m.pendingDeviceJid && m.pendingDeviceJid.length) {
                d.pendingDeviceJid = $Array(m.pendingDeviceJid.length);
                for (var j = 0; j < m.pendingDeviceJid.length; ++j) {
                    d.pendingDeviceJid[j] = m.pendingDeviceJid[j];
                }
            }
            if (m.deliveredDeviceJid && m.deliveredDeviceJid.length) {
                d.deliveredDeviceJid = $Array(m.deliveredDeviceJid.length);
                for (var j = 0; j < m.deliveredDeviceJid.length; ++j) {
                    d.deliveredDeviceJid[j] = m.deliveredDeviceJid[j];
                }
            }
            return d;
        };

        UserReceipt.prototype.toJSON = function() {
            return UserReceipt.toObject(this, $protobuf.util.toJSONOptions);
        };

        UserReceipt.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.UserReceipt";
        };

        return UserReceipt;
    })();

    proto.VerifiedNameCertificate = (function() {

        const VerifiedNameCertificate = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        VerifiedNameCertificate.prototype.details = null;
        VerifiedNameCertificate.prototype.signature = null;
        VerifiedNameCertificate.prototype.serverSignature = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(VerifiedNameCertificate.prototype, "_details", {
            get: $util.oneOfGetter($oneOfFields = ["details"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(VerifiedNameCertificate.prototype, "_signature", {
            get: $util.oneOfGetter($oneOfFields = ["signature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(VerifiedNameCertificate.prototype, "_serverSignature", {
            get: $util.oneOfGetter($oneOfFields = ["serverSignature"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        VerifiedNameCertificate.create = function(properties) {
            return new VerifiedNameCertificate(properties);
        };

        VerifiedNameCertificate.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.details != null && $Object.hasOwnProperty.call(m, "details"))
                w.uint32(10).bytes(m.details);
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature"))
                w.uint32(18).bytes(m.signature);
            if (m.serverSignature != null && $Object.hasOwnProperty.call(m, "serverSignature"))
                w.uint32(26).bytes(m.serverSignature);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        VerifiedNameCertificate.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.VerifiedNameCertificate();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.details = r.bytes();
                        m._details = "details";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.signature = r.bytes();
                        m._signature = "signature";
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.serverSignature = r.bytes();
                        m._serverSignature = "serverSignature";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        VerifiedNameCertificate.fromObject = function (d, q) {
            if (d instanceof $root.proto.VerifiedNameCertificate)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.VerifiedNameCertificate: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.VerifiedNameCertificate();
            if (d.details != null) {
                if (typeof d.details === "string")
                    $util.base64.decode(d.details, m.details = $util.newBuffer($util.base64.length(d.details)), 0);
                else if (d.details.length >= 0)
                    m.details = d.details;
            }
            if (d.signature != null) {
                if (typeof d.signature === "string")
                    $util.base64.decode(d.signature, m.signature = $util.newBuffer($util.base64.length(d.signature)), 0);
                else if (d.signature.length >= 0)
                    m.signature = d.signature;
            }
            if (d.serverSignature != null) {
                if (typeof d.serverSignature === "string")
                    $util.base64.decode(d.serverSignature, m.serverSignature = $util.newBuffer($util.base64.length(d.serverSignature)), 0);
                else if (d.serverSignature.length >= 0)
                    m.serverSignature = d.serverSignature;
            }
            return m;
        };

        VerifiedNameCertificate.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.details != null && $Object.hasOwnProperty.call(m, "details")) {
                d.details = o.bytes === $String ? $util.base64.encode(m.details, 0, m.details.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.details) : m.details;
            }
            if (m.signature != null && $Object.hasOwnProperty.call(m, "signature")) {
                d.signature = o.bytes === $String ? $util.base64.encode(m.signature, 0, m.signature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.signature) : m.signature;
            }
            if (m.serverSignature != null && $Object.hasOwnProperty.call(m, "serverSignature")) {
                d.serverSignature = o.bytes === $String ? $util.base64.encode(m.serverSignature, 0, m.serverSignature.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.serverSignature) : m.serverSignature;
            }
            return d;
        };

        VerifiedNameCertificate.prototype.toJSON = function() {
            return VerifiedNameCertificate.toObject(this, $protobuf.util.toJSONOptions);
        };

        VerifiedNameCertificate.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.VerifiedNameCertificate";
        };

        VerifiedNameCertificate.Details = (function() {

            const Details = function (p) {
                this.localizedNames = [];
                if (p)
                    for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                        if (p[ks[i]] != null && ks[i] !== "__proto__")
                            this[ks[i]] = p[ks[i]];
            };

            Details.prototype.serial = null;
            Details.prototype.issuer = null;
            Details.prototype.verifiedName = null;
            Details.prototype.localizedNames = $util.emptyArray;
            Details.prototype.issueTime = null;

            let $oneOfFields;

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_serial", {
                get: $util.oneOfGetter($oneOfFields = ["serial"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_issuer", {
                get: $util.oneOfGetter($oneOfFields = ["issuer"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_verifiedName", {
                get: $util.oneOfGetter($oneOfFields = ["verifiedName"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            // Virtual OneOf for proto3 optional field
            $Object.defineProperty(Details.prototype, "_issueTime", {
                get: $util.oneOfGetter($oneOfFields = ["issueTime"]),
                set: $util.oneOfSetter($oneOfFields)
            });

            Details.create = function(properties) {
                return new Details(properties);
            };

            Details.encode = function (m, w, q) {
                if (!w)
                    w = $Writer.create();
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                if (m.serial != null && $Object.hasOwnProperty.call(m, "serial"))
                    w.uint32(8).uint64(m.serial);
                if (m.issuer != null && $Object.hasOwnProperty.call(m, "issuer"))
                    w.uint32(18).string(m.issuer);
                if (m.verifiedName != null && $Object.hasOwnProperty.call(m, "verifiedName"))
                    w.uint32(34).string(m.verifiedName);
                if (m.localizedNames != null && m.localizedNames.length) {
                    for (var i = 0; i < m.localizedNames.length; ++i)
                        $root.proto.LocalizedName.encode(m.localizedNames[i], w.uint32(66).fork(), q + 1).ldelim();
                }
                if (m.issueTime != null && $Object.hasOwnProperty.call(m, "issueTime"))
                    w.uint32(80).uint64(m.issueTime);
                if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                    for (var i = 0; i < m.$unknowns.length; ++i)
                        w.raw(m.$unknowns[i]);
                return w;
            };

            Details.decode = function (r, l, z, q, g) {
                if (!(r instanceof $Reader))
                    r = $Reader.create(r);
                if (q === $undefined)
                    q = 0;
                if (q > $Reader.recursionLimit)
                    throw $Error("max depth exceeded");
                var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.VerifiedNameCertificate.Details();
                while (r.pos < c) {
                    var s = r.pos;
                    var t = r.tag();
                    if (t === z) {
                        z = $undefined;
                        break;
                    }
                    var u = t & 7;
                    switch (t >>>= 3) {
                    case 1: {
                            if (u !== 0)
                                break;
                            m.serial = r.uint64();
                            m._serial = "serial";
                            continue;
                        }
                    case 2: {
                            if (u !== 2)
                                break;
                            m.issuer = r.stringVerify();
                            m._issuer = "issuer";
                            continue;
                        }
                    case 4: {
                            if (u !== 2)
                                break;
                            m.verifiedName = r.stringVerify();
                            m._verifiedName = "verifiedName";
                            continue;
                        }
                    case 8: {
                            if (u !== 2)
                                break;
                            if (!(m.localizedNames && m.localizedNames.length))
                                m.localizedNames = [];
                            m.localizedNames.push($root.proto.LocalizedName.decode(r, r.uint32(), $undefined, q + 1));
                            continue;
                        }
                    case 10: {
                            if (u !== 0)
                                break;
                            m.issueTime = r.uint64();
                            m._issueTime = "issueTime";
                            continue;
                        }
                    }
                    r.skipType(u, q, t);
                    if (!r.discardUnknown) {
                        $util.makeProp(m, "$unknowns", false);
                        (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                    }
                }
                if (z !== $undefined)
                    throw $Error("missing end group");
                return m;
            };

            Details.fromObject = function (d, q) {
                if (d instanceof $root.proto.VerifiedNameCertificate.Details)
                    return d;
                if (!$util.isObject(d))
                    throw $TypeError(".proto.VerifiedNameCertificate.Details: object expected");
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var m = new $root.proto.VerifiedNameCertificate.Details();
                if (d.serial != null) {
                    if ($util.Long)
                        m.serial = $util.Long.fromValue(d.serial, true);
                    else if (typeof d.serial === "string")
                        m.serial = $parseInt(d.serial, 10);
                    else if (typeof d.serial === "number")
                        m.serial = d.serial;
                    else if (typeof d.serial === "object")
                        m.serial = new $util.LongBits(d.serial.low >>> 0, d.serial.high >>> 0).toNumber(true);
                }
                if (d.issuer != null) {
                    m.issuer = $String(d.issuer);
                }
                if (d.verifiedName != null) {
                    m.verifiedName = $String(d.verifiedName);
                }
                if (d.localizedNames) {
                    if (!$Array.isArray(d.localizedNames))
                        throw $TypeError(".proto.VerifiedNameCertificate.Details.localizedNames: array expected");
                    m.localizedNames = $Array(d.localizedNames.length);
                    for (var i = 0; i < d.localizedNames.length; ++i) {
                        if (!$util.isObject(d.localizedNames[i]))
                            throw $TypeError(".proto.VerifiedNameCertificate.Details.localizedNames: object expected");
                        m.localizedNames[i] = $root.proto.LocalizedName.fromObject(d.localizedNames[i], q + 1);
                    }
                }
                if (d.issueTime != null) {
                    if ($util.Long)
                        m.issueTime = $util.Long.fromValue(d.issueTime, true);
                    else if (typeof d.issueTime === "string")
                        m.issueTime = $parseInt(d.issueTime, 10);
                    else if (typeof d.issueTime === "number")
                        m.issueTime = d.issueTime;
                    else if (typeof d.issueTime === "object")
                        m.issueTime = new $util.LongBits(d.issueTime.low >>> 0, d.issueTime.high >>> 0).toNumber(true);
                }
                return m;
            };

            Details.toObject = function (m, o, q) {
                if (!o)
                    o = {};
                if (q === $undefined)
                    q = 0;
                if (q > $util.recursionLimit)
                    throw $Error("max depth exceeded");
                var d = {};
                if (o.arrays || o.defaults) {
                    d.localizedNames = [];
                }
                if (m.serial != null && $Object.hasOwnProperty.call(m, "serial")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.serial = typeof m.serial === "number" ? $BigInt(m.serial) : $util.Long.fromBits(m.serial.low >>> 0, m.serial.high >>> 0, true).toBigInt();
                    else if (typeof m.serial === "number")
                        d.serial = o.longs === $String ? $String(m.serial) : m.serial;
                    else
                        d.serial = o.longs === $String ? $util.Long.prototype.toString.call(m.serial) : o.longs === $Number ? new $util.LongBits(m.serial.low >>> 0, m.serial.high >>> 0).toNumber(true) : m.serial;
                }
                if (m.issuer != null && $Object.hasOwnProperty.call(m, "issuer")) {
                    d.issuer = m.issuer;
                }
                if (m.verifiedName != null && $Object.hasOwnProperty.call(m, "verifiedName")) {
                    d.verifiedName = m.verifiedName;
                }
                if (m.localizedNames && m.localizedNames.length) {
                    d.localizedNames = $Array(m.localizedNames.length);
                    for (var j = 0; j < m.localizedNames.length; ++j) {
                        d.localizedNames[j] = $root.proto.LocalizedName.toObject(m.localizedNames[j], o, q + 1);
                    }
                }
                if (m.issueTime != null && $Object.hasOwnProperty.call(m, "issueTime")) {
                    if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                        d.issueTime = typeof m.issueTime === "number" ? $BigInt(m.issueTime) : $util.Long.fromBits(m.issueTime.low >>> 0, m.issueTime.high >>> 0, true).toBigInt();
                    else if (typeof m.issueTime === "number")
                        d.issueTime = o.longs === $String ? $String(m.issueTime) : m.issueTime;
                    else
                        d.issueTime = o.longs === $String ? $util.Long.prototype.toString.call(m.issueTime) : o.longs === $Number ? new $util.LongBits(m.issueTime.low >>> 0, m.issueTime.high >>> 0).toNumber(true) : m.issueTime;
                }
                return d;
            };

            Details.prototype.toJSON = function() {
                return Details.toObject(this, $protobuf.util.toJSONOptions);
            };

            Details.getTypeUrl = function(prefix) {
                if (prefix === $undefined)
                    prefix = "type.googleapis.com";
                return prefix + "/proto.VerifiedNameCertificate.Details";
            };

            return Details;
        })();

        return VerifiedNameCertificate;
    })();

    proto.VirtualDeviceOutput = (function() {

        const VirtualDeviceOutput = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        VirtualDeviceOutput.prototype.vdId = $util.newBuffer([]);
        VirtualDeviceOutput.prototype.vdPublicKey = $util.newBuffer([]);
        VirtualDeviceOutput.prototype.vdEpochStoragePublicKey = $util.newBuffer([]);
        VirtualDeviceOutput.prototype.vdEpochStoragePublicKeySig = $util.newBuffer([]);
        VirtualDeviceOutput.prototype.ocmfRotationToken = $util.newBuffer([]);
        VirtualDeviceOutput.prototype.deviceEpochHmac = $util.newBuffer([]);
        VirtualDeviceOutput.prototype.encryptedSecretValues = null;

        VirtualDeviceOutput.create = function(properties) {
            return new VirtualDeviceOutput(properties);
        };

        VirtualDeviceOutput.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            w.uint32(10).bytes(m.vdId);
            w.uint32(18).bytes(m.vdPublicKey);
            w.uint32(26).bytes(m.vdEpochStoragePublicKey);
            w.uint32(34).bytes(m.vdEpochStoragePublicKeySig);
            w.uint32(42).bytes(m.ocmfRotationToken);
            w.uint32(50).bytes(m.deviceEpochHmac);
            $root.proto.EncryptedSecretValuesOutput.encode(m.encryptedSecretValues, w.uint32(58).fork(), q + 1).ldelim();
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        VirtualDeviceOutput.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.VirtualDeviceOutput();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.vdId = r.bytes();
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.vdPublicKey = r.bytes();
                        continue;
                    }
                case 3: {
                        if (u !== 2)
                            break;
                        m.vdEpochStoragePublicKey = r.bytes();
                        continue;
                    }
                case 4: {
                        if (u !== 2)
                            break;
                        m.vdEpochStoragePublicKeySig = r.bytes();
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.ocmfRotationToken = r.bytes();
                        continue;
                    }
                case 6: {
                        if (u !== 2)
                            break;
                        m.deviceEpochHmac = r.bytes();
                        continue;
                    }
                case 7: {
                        if (u !== 2)
                            break;
                        m.encryptedSecretValues = $root.proto.EncryptedSecretValuesOutput.decode(r, r.uint32(), $undefined, q + 1, m.encryptedSecretValues);
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "vdId"))
                throw $util.ProtocolError("missing required 'vdId'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "vdPublicKey"))
                throw $util.ProtocolError("missing required 'vdPublicKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "vdEpochStoragePublicKey"))
                throw $util.ProtocolError("missing required 'vdEpochStoragePublicKey'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "vdEpochStoragePublicKeySig"))
                throw $util.ProtocolError("missing required 'vdEpochStoragePublicKeySig'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "ocmfRotationToken"))
                throw $util.ProtocolError("missing required 'ocmfRotationToken'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "deviceEpochHmac"))
                throw $util.ProtocolError("missing required 'deviceEpochHmac'", { instance: m });
            if (!$Object.hasOwnProperty.call(m, "encryptedSecretValues"))
                throw $util.ProtocolError("missing required 'encryptedSecretValues'", { instance: m });
            return m;
        };

        VirtualDeviceOutput.fromObject = function (d, q) {
            if (d instanceof $root.proto.VirtualDeviceOutput)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.VirtualDeviceOutput: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.VirtualDeviceOutput();
            if (d.vdId != null) {
                if (typeof d.vdId === "string")
                    $util.base64.decode(d.vdId, m.vdId = $util.newBuffer($util.base64.length(d.vdId)), 0);
                else if (d.vdId.length >= 0)
                    m.vdId = d.vdId;
            }
            if (d.vdPublicKey != null) {
                if (typeof d.vdPublicKey === "string")
                    $util.base64.decode(d.vdPublicKey, m.vdPublicKey = $util.newBuffer($util.base64.length(d.vdPublicKey)), 0);
                else if (d.vdPublicKey.length >= 0)
                    m.vdPublicKey = d.vdPublicKey;
            }
            if (d.vdEpochStoragePublicKey != null) {
                if (typeof d.vdEpochStoragePublicKey === "string")
                    $util.base64.decode(d.vdEpochStoragePublicKey, m.vdEpochStoragePublicKey = $util.newBuffer($util.base64.length(d.vdEpochStoragePublicKey)), 0);
                else if (d.vdEpochStoragePublicKey.length >= 0)
                    m.vdEpochStoragePublicKey = d.vdEpochStoragePublicKey;
            }
            if (d.vdEpochStoragePublicKeySig != null) {
                if (typeof d.vdEpochStoragePublicKeySig === "string")
                    $util.base64.decode(d.vdEpochStoragePublicKeySig, m.vdEpochStoragePublicKeySig = $util.newBuffer($util.base64.length(d.vdEpochStoragePublicKeySig)), 0);
                else if (d.vdEpochStoragePublicKeySig.length >= 0)
                    m.vdEpochStoragePublicKeySig = d.vdEpochStoragePublicKeySig;
            }
            if (d.ocmfRotationToken != null) {
                if (typeof d.ocmfRotationToken === "string")
                    $util.base64.decode(d.ocmfRotationToken, m.ocmfRotationToken = $util.newBuffer($util.base64.length(d.ocmfRotationToken)), 0);
                else if (d.ocmfRotationToken.length >= 0)
                    m.ocmfRotationToken = d.ocmfRotationToken;
            }
            if (d.deviceEpochHmac != null) {
                if (typeof d.deviceEpochHmac === "string")
                    $util.base64.decode(d.deviceEpochHmac, m.deviceEpochHmac = $util.newBuffer($util.base64.length(d.deviceEpochHmac)), 0);
                else if (d.deviceEpochHmac.length >= 0)
                    m.deviceEpochHmac = d.deviceEpochHmac;
            }
            if (d.encryptedSecretValues != null) {
                if (!$util.isObject(d.encryptedSecretValues))
                    throw $TypeError(".proto.VirtualDeviceOutput.encryptedSecretValues: object expected");
                m.encryptedSecretValues = $root.proto.EncryptedSecretValuesOutput.fromObject(d.encryptedSecretValues, q + 1);
            }
            return m;
        };

        VirtualDeviceOutput.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.defaults) {
                if (o.bytes === $String)
                    d.vdId = "";
                else {
                    d.vdId = [];
                    if (o.bytes !== $Array)
                        d.vdId = $util.newBuffer(d.vdId);
                }
                if (o.bytes === $String)
                    d.vdPublicKey = "";
                else {
                    d.vdPublicKey = [];
                    if (o.bytes !== $Array)
                        d.vdPublicKey = $util.newBuffer(d.vdPublicKey);
                }
                if (o.bytes === $String)
                    d.vdEpochStoragePublicKey = "";
                else {
                    d.vdEpochStoragePublicKey = [];
                    if (o.bytes !== $Array)
                        d.vdEpochStoragePublicKey = $util.newBuffer(d.vdEpochStoragePublicKey);
                }
                if (o.bytes === $String)
                    d.vdEpochStoragePublicKeySig = "";
                else {
                    d.vdEpochStoragePublicKeySig = [];
                    if (o.bytes !== $Array)
                        d.vdEpochStoragePublicKeySig = $util.newBuffer(d.vdEpochStoragePublicKeySig);
                }
                if (o.bytes === $String)
                    d.ocmfRotationToken = "";
                else {
                    d.ocmfRotationToken = [];
                    if (o.bytes !== $Array)
                        d.ocmfRotationToken = $util.newBuffer(d.ocmfRotationToken);
                }
                if (o.bytes === $String)
                    d.deviceEpochHmac = "";
                else {
                    d.deviceEpochHmac = [];
                    if (o.bytes !== $Array)
                        d.deviceEpochHmac = $util.newBuffer(d.deviceEpochHmac);
                }
                d.encryptedSecretValues = null;
            }
            if (m.vdId != null && $Object.hasOwnProperty.call(m, "vdId")) {
                d.vdId = o.bytes === $String ? $util.base64.encode(m.vdId, 0, m.vdId.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.vdId) : m.vdId;
            }
            if (m.vdPublicKey != null && $Object.hasOwnProperty.call(m, "vdPublicKey")) {
                d.vdPublicKey = o.bytes === $String ? $util.base64.encode(m.vdPublicKey, 0, m.vdPublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.vdPublicKey) : m.vdPublicKey;
            }
            if (m.vdEpochStoragePublicKey != null && $Object.hasOwnProperty.call(m, "vdEpochStoragePublicKey")) {
                d.vdEpochStoragePublicKey = o.bytes === $String ? $util.base64.encode(m.vdEpochStoragePublicKey, 0, m.vdEpochStoragePublicKey.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.vdEpochStoragePublicKey) : m.vdEpochStoragePublicKey;
            }
            if (m.vdEpochStoragePublicKeySig != null && $Object.hasOwnProperty.call(m, "vdEpochStoragePublicKeySig")) {
                d.vdEpochStoragePublicKeySig = o.bytes === $String ? $util.base64.encode(m.vdEpochStoragePublicKeySig, 0, m.vdEpochStoragePublicKeySig.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.vdEpochStoragePublicKeySig) : m.vdEpochStoragePublicKeySig;
            }
            if (m.ocmfRotationToken != null && $Object.hasOwnProperty.call(m, "ocmfRotationToken")) {
                d.ocmfRotationToken = o.bytes === $String ? $util.base64.encode(m.ocmfRotationToken, 0, m.ocmfRotationToken.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.ocmfRotationToken) : m.ocmfRotationToken;
            }
            if (m.deviceEpochHmac != null && $Object.hasOwnProperty.call(m, "deviceEpochHmac")) {
                d.deviceEpochHmac = o.bytes === $String ? $util.base64.encode(m.deviceEpochHmac, 0, m.deviceEpochHmac.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.deviceEpochHmac) : m.deviceEpochHmac;
            }
            if (m.encryptedSecretValues != null && $Object.hasOwnProperty.call(m, "encryptedSecretValues")) {
                d.encryptedSecretValues = $root.proto.EncryptedSecretValuesOutput.toObject(m.encryptedSecretValues, o, q + 1);
            }
            return d;
        };

        VirtualDeviceOutput.prototype.toJSON = function() {
            return VirtualDeviceOutput.toObject(this, $protobuf.util.toJSONOptions);
        };

        VirtualDeviceOutput.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.VirtualDeviceOutput";
        };

        return VirtualDeviceOutput;
    })();

    proto.WallpaperSettings = (function() {

        const WallpaperSettings = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        WallpaperSettings.prototype.filename = null;
        WallpaperSettings.prototype.opacity = null;
        WallpaperSettings.prototype.isGenAi = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WallpaperSettings.prototype, "_filename", {
            get: $util.oneOfGetter($oneOfFields = ["filename"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WallpaperSettings.prototype, "_opacity", {
            get: $util.oneOfGetter($oneOfFields = ["opacity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WallpaperSettings.prototype, "_isGenAi", {
            get: $util.oneOfGetter($oneOfFields = ["isGenAi"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WallpaperSettings.create = function(properties) {
            return new WallpaperSettings(properties);
        };

        WallpaperSettings.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.filename != null && $Object.hasOwnProperty.call(m, "filename"))
                w.uint32(10).string(m.filename);
            if (m.opacity != null && $Object.hasOwnProperty.call(m, "opacity"))
                w.uint32(16).uint32(m.opacity);
            if (m.isGenAi != null && $Object.hasOwnProperty.call(m, "isGenAi"))
                w.uint32(24).bool(m.isGenAi);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        WallpaperSettings.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.WallpaperSettings();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.filename = r.stringVerify();
                        m._filename = "filename";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.opacity = r.uint32();
                        m._opacity = "opacity";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.isGenAi = r.bool();
                        m._isGenAi = "isGenAi";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        WallpaperSettings.fromObject = function (d, q) {
            if (d instanceof $root.proto.WallpaperSettings)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.WallpaperSettings: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.WallpaperSettings();
            if (d.filename != null) {
                m.filename = $String(d.filename);
            }
            if (d.opacity != null) {
                m.opacity = d.opacity >>> 0;
            }
            if (d.isGenAi != null) {
                m.isGenAi = $Boolean(d.isGenAi);
            }
            return m;
        };

        WallpaperSettings.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.filename != null && $Object.hasOwnProperty.call(m, "filename")) {
                d.filename = m.filename;
            }
            if (m.opacity != null && $Object.hasOwnProperty.call(m, "opacity")) {
                d.opacity = m.opacity;
            }
            if (m.isGenAi != null && $Object.hasOwnProperty.call(m, "isGenAi")) {
                d.isGenAi = m.isGenAi;
            }
            return d;
        };

        WallpaperSettings.prototype.toJSON = function() {
            return WallpaperSettings.toObject(this, $protobuf.util.toJSONOptions);
        };

        WallpaperSettings.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.WallpaperSettings";
        };

        return WallpaperSettings;
    })();

    proto.WebFeatures = (function() {

        const WebFeatures = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        WebFeatures.prototype.labelsDisplay = null;
        WebFeatures.prototype.voipIndividualOutgoing = null;
        WebFeatures.prototype.groupsV3 = null;
        WebFeatures.prototype.groupsV3Create = null;
        WebFeatures.prototype.changeNumberV2 = null;
        WebFeatures.prototype.queryStatusV3Thumbnail = null;
        WebFeatures.prototype.liveLocations = null;
        WebFeatures.prototype.queryVname = null;
        WebFeatures.prototype.voipIndividualIncoming = null;
        WebFeatures.prototype.quickRepliesQuery = null;
        WebFeatures.prototype.payments = null;
        WebFeatures.prototype.stickerPackQuery = null;
        WebFeatures.prototype.liveLocationsFinal = null;
        WebFeatures.prototype.labelsEdit = null;
        WebFeatures.prototype.mediaUpload = null;
        WebFeatures.prototype.mediaUploadRichQuickReplies = null;
        WebFeatures.prototype.vnameV2 = null;
        WebFeatures.prototype.videoPlaybackUrl = null;
        WebFeatures.prototype.statusRanking = null;
        WebFeatures.prototype.voipIndividualVideo = null;
        WebFeatures.prototype.thirdPartyStickers = null;
        WebFeatures.prototype.frequentlyForwardedSetting = null;
        WebFeatures.prototype.groupsV4JoinPermission = null;
        WebFeatures.prototype.recentStickers = null;
        WebFeatures.prototype.catalog = null;
        WebFeatures.prototype.starredStickers = null;
        WebFeatures.prototype.voipGroupCall = null;
        WebFeatures.prototype.templateMessage = null;
        WebFeatures.prototype.templateMessageInteractivity = null;
        WebFeatures.prototype.ephemeralMessages = null;
        WebFeatures.prototype.e2ENotificationSync = null;
        WebFeatures.prototype.recentStickersV2 = null;
        WebFeatures.prototype.recentStickersV3 = null;
        WebFeatures.prototype.userNotice = null;
        WebFeatures.prototype.support = null;
        WebFeatures.prototype.groupUiiCleanup = null;
        WebFeatures.prototype.groupDogfoodingInternalOnly = null;
        WebFeatures.prototype.settingsSync = null;
        WebFeatures.prototype.archiveV2 = null;
        WebFeatures.prototype.ephemeralAllowGroupMembers = null;
        WebFeatures.prototype.ephemeral24HDuration = null;
        WebFeatures.prototype.mdForceUpgrade = null;
        WebFeatures.prototype.disappearingMode = null;
        WebFeatures.prototype.externalMdOptInAvailable = null;
        WebFeatures.prototype.noDeleteMessageTimeLimit = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_labelsDisplay", {
            get: $util.oneOfGetter($oneOfFields = ["labelsDisplay"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_voipIndividualOutgoing", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualOutgoing"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_groupsV3", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_groupsV3Create", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV3Create"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_changeNumberV2", {
            get: $util.oneOfGetter($oneOfFields = ["changeNumberV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_queryStatusV3Thumbnail", {
            get: $util.oneOfGetter($oneOfFields = ["queryStatusV3Thumbnail"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_liveLocations", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocations"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_queryVname", {
            get: $util.oneOfGetter($oneOfFields = ["queryVname"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_voipIndividualIncoming", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualIncoming"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_quickRepliesQuery", {
            get: $util.oneOfGetter($oneOfFields = ["quickRepliesQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_payments", {
            get: $util.oneOfGetter($oneOfFields = ["payments"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_stickerPackQuery", {
            get: $util.oneOfGetter($oneOfFields = ["stickerPackQuery"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_liveLocationsFinal", {
            get: $util.oneOfGetter($oneOfFields = ["liveLocationsFinal"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_labelsEdit", {
            get: $util.oneOfGetter($oneOfFields = ["labelsEdit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_mediaUpload", {
            get: $util.oneOfGetter($oneOfFields = ["mediaUpload"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_mediaUploadRichQuickReplies", {
            get: $util.oneOfGetter($oneOfFields = ["mediaUploadRichQuickReplies"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_vnameV2", {
            get: $util.oneOfGetter($oneOfFields = ["vnameV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_videoPlaybackUrl", {
            get: $util.oneOfGetter($oneOfFields = ["videoPlaybackUrl"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_statusRanking", {
            get: $util.oneOfGetter($oneOfFields = ["statusRanking"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_voipIndividualVideo", {
            get: $util.oneOfGetter($oneOfFields = ["voipIndividualVideo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_thirdPartyStickers", {
            get: $util.oneOfGetter($oneOfFields = ["thirdPartyStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_frequentlyForwardedSetting", {
            get: $util.oneOfGetter($oneOfFields = ["frequentlyForwardedSetting"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_groupsV4JoinPermission", {
            get: $util.oneOfGetter($oneOfFields = ["groupsV4JoinPermission"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_recentStickers", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_catalog", {
            get: $util.oneOfGetter($oneOfFields = ["catalog"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_starredStickers", {
            get: $util.oneOfGetter($oneOfFields = ["starredStickers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_voipGroupCall", {
            get: $util.oneOfGetter($oneOfFields = ["voipGroupCall"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_templateMessage", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_templateMessageInteractivity", {
            get: $util.oneOfGetter($oneOfFields = ["templateMessageInteractivity"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_ephemeralMessages", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralMessages"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_e2ENotificationSync", {
            get: $util.oneOfGetter($oneOfFields = ["e2ENotificationSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_recentStickersV2", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickersV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_recentStickersV3", {
            get: $util.oneOfGetter($oneOfFields = ["recentStickersV3"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_userNotice", {
            get: $util.oneOfGetter($oneOfFields = ["userNotice"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_support", {
            get: $util.oneOfGetter($oneOfFields = ["support"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_groupUiiCleanup", {
            get: $util.oneOfGetter($oneOfFields = ["groupUiiCleanup"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_groupDogfoodingInternalOnly", {
            get: $util.oneOfGetter($oneOfFields = ["groupDogfoodingInternalOnly"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_settingsSync", {
            get: $util.oneOfGetter($oneOfFields = ["settingsSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_archiveV2", {
            get: $util.oneOfGetter($oneOfFields = ["archiveV2"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_ephemeralAllowGroupMembers", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralAllowGroupMembers"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_ephemeral24HDuration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeral24HDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_mdForceUpgrade", {
            get: $util.oneOfGetter($oneOfFields = ["mdForceUpgrade"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_disappearingMode", {
            get: $util.oneOfGetter($oneOfFields = ["disappearingMode"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_externalMdOptInAvailable", {
            get: $util.oneOfGetter($oneOfFields = ["externalMdOptInAvailable"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebFeatures.prototype, "_noDeleteMessageTimeLimit", {
            get: $util.oneOfGetter($oneOfFields = ["noDeleteMessageTimeLimit"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebFeatures.create = function(properties) {
            return new WebFeatures(properties);
        };

        WebFeatures.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.labelsDisplay != null && $Object.hasOwnProperty.call(m, "labelsDisplay"))
                w.uint32(8).int32(m.labelsDisplay);
            if (m.voipIndividualOutgoing != null && $Object.hasOwnProperty.call(m, "voipIndividualOutgoing"))
                w.uint32(16).int32(m.voipIndividualOutgoing);
            if (m.groupsV3 != null && $Object.hasOwnProperty.call(m, "groupsV3"))
                w.uint32(24).int32(m.groupsV3);
            if (m.groupsV3Create != null && $Object.hasOwnProperty.call(m, "groupsV3Create"))
                w.uint32(32).int32(m.groupsV3Create);
            if (m.changeNumberV2 != null && $Object.hasOwnProperty.call(m, "changeNumberV2"))
                w.uint32(40).int32(m.changeNumberV2);
            if (m.queryStatusV3Thumbnail != null && $Object.hasOwnProperty.call(m, "queryStatusV3Thumbnail"))
                w.uint32(48).int32(m.queryStatusV3Thumbnail);
            if (m.liveLocations != null && $Object.hasOwnProperty.call(m, "liveLocations"))
                w.uint32(56).int32(m.liveLocations);
            if (m.queryVname != null && $Object.hasOwnProperty.call(m, "queryVname"))
                w.uint32(64).int32(m.queryVname);
            if (m.voipIndividualIncoming != null && $Object.hasOwnProperty.call(m, "voipIndividualIncoming"))
                w.uint32(72).int32(m.voipIndividualIncoming);
            if (m.quickRepliesQuery != null && $Object.hasOwnProperty.call(m, "quickRepliesQuery"))
                w.uint32(80).int32(m.quickRepliesQuery);
            if (m.payments != null && $Object.hasOwnProperty.call(m, "payments"))
                w.uint32(88).int32(m.payments);
            if (m.stickerPackQuery != null && $Object.hasOwnProperty.call(m, "stickerPackQuery"))
                w.uint32(96).int32(m.stickerPackQuery);
            if (m.liveLocationsFinal != null && $Object.hasOwnProperty.call(m, "liveLocationsFinal"))
                w.uint32(104).int32(m.liveLocationsFinal);
            if (m.labelsEdit != null && $Object.hasOwnProperty.call(m, "labelsEdit"))
                w.uint32(112).int32(m.labelsEdit);
            if (m.mediaUpload != null && $Object.hasOwnProperty.call(m, "mediaUpload"))
                w.uint32(120).int32(m.mediaUpload);
            if (m.mediaUploadRichQuickReplies != null && $Object.hasOwnProperty.call(m, "mediaUploadRichQuickReplies"))
                w.uint32(144).int32(m.mediaUploadRichQuickReplies);
            if (m.vnameV2 != null && $Object.hasOwnProperty.call(m, "vnameV2"))
                w.uint32(152).int32(m.vnameV2);
            if (m.videoPlaybackUrl != null && $Object.hasOwnProperty.call(m, "videoPlaybackUrl"))
                w.uint32(160).int32(m.videoPlaybackUrl);
            if (m.statusRanking != null && $Object.hasOwnProperty.call(m, "statusRanking"))
                w.uint32(168).int32(m.statusRanking);
            if (m.voipIndividualVideo != null && $Object.hasOwnProperty.call(m, "voipIndividualVideo"))
                w.uint32(176).int32(m.voipIndividualVideo);
            if (m.thirdPartyStickers != null && $Object.hasOwnProperty.call(m, "thirdPartyStickers"))
                w.uint32(184).int32(m.thirdPartyStickers);
            if (m.frequentlyForwardedSetting != null && $Object.hasOwnProperty.call(m, "frequentlyForwardedSetting"))
                w.uint32(192).int32(m.frequentlyForwardedSetting);
            if (m.groupsV4JoinPermission != null && $Object.hasOwnProperty.call(m, "groupsV4JoinPermission"))
                w.uint32(200).int32(m.groupsV4JoinPermission);
            if (m.recentStickers != null && $Object.hasOwnProperty.call(m, "recentStickers"))
                w.uint32(208).int32(m.recentStickers);
            if (m.catalog != null && $Object.hasOwnProperty.call(m, "catalog"))
                w.uint32(216).int32(m.catalog);
            if (m.starredStickers != null && $Object.hasOwnProperty.call(m, "starredStickers"))
                w.uint32(224).int32(m.starredStickers);
            if (m.voipGroupCall != null && $Object.hasOwnProperty.call(m, "voipGroupCall"))
                w.uint32(232).int32(m.voipGroupCall);
            if (m.templateMessage != null && $Object.hasOwnProperty.call(m, "templateMessage"))
                w.uint32(240).int32(m.templateMessage);
            if (m.templateMessageInteractivity != null && $Object.hasOwnProperty.call(m, "templateMessageInteractivity"))
                w.uint32(248).int32(m.templateMessageInteractivity);
            if (m.ephemeralMessages != null && $Object.hasOwnProperty.call(m, "ephemeralMessages"))
                w.uint32(256).int32(m.ephemeralMessages);
            if (m.e2ENotificationSync != null && $Object.hasOwnProperty.call(m, "e2ENotificationSync"))
                w.uint32(264).int32(m.e2ENotificationSync);
            if (m.recentStickersV2 != null && $Object.hasOwnProperty.call(m, "recentStickersV2"))
                w.uint32(272).int32(m.recentStickersV2);
            if (m.recentStickersV3 != null && $Object.hasOwnProperty.call(m, "recentStickersV3"))
                w.uint32(288).int32(m.recentStickersV3);
            if (m.userNotice != null && $Object.hasOwnProperty.call(m, "userNotice"))
                w.uint32(296).int32(m.userNotice);
            if (m.support != null && $Object.hasOwnProperty.call(m, "support"))
                w.uint32(312).int32(m.support);
            if (m.groupUiiCleanup != null && $Object.hasOwnProperty.call(m, "groupUiiCleanup"))
                w.uint32(320).int32(m.groupUiiCleanup);
            if (m.groupDogfoodingInternalOnly != null && $Object.hasOwnProperty.call(m, "groupDogfoodingInternalOnly"))
                w.uint32(328).int32(m.groupDogfoodingInternalOnly);
            if (m.settingsSync != null && $Object.hasOwnProperty.call(m, "settingsSync"))
                w.uint32(336).int32(m.settingsSync);
            if (m.archiveV2 != null && $Object.hasOwnProperty.call(m, "archiveV2"))
                w.uint32(344).int32(m.archiveV2);
            if (m.ephemeralAllowGroupMembers != null && $Object.hasOwnProperty.call(m, "ephemeralAllowGroupMembers"))
                w.uint32(352).int32(m.ephemeralAllowGroupMembers);
            if (m.ephemeral24HDuration != null && $Object.hasOwnProperty.call(m, "ephemeral24HDuration"))
                w.uint32(360).int32(m.ephemeral24HDuration);
            if (m.mdForceUpgrade != null && $Object.hasOwnProperty.call(m, "mdForceUpgrade"))
                w.uint32(368).int32(m.mdForceUpgrade);
            if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode"))
                w.uint32(376).int32(m.disappearingMode);
            if (m.externalMdOptInAvailable != null && $Object.hasOwnProperty.call(m, "externalMdOptInAvailable"))
                w.uint32(384).int32(m.externalMdOptInAvailable);
            if (m.noDeleteMessageTimeLimit != null && $Object.hasOwnProperty.call(m, "noDeleteMessageTimeLimit"))
                w.uint32(392).int32(m.noDeleteMessageTimeLimit);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        WebFeatures.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.WebFeatures(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 0)
                            break;
                        m.labelsDisplay = r.int32();
                        m._labelsDisplay = "labelsDisplay";
                        continue;
                    }
                case 2: {
                        if (u !== 0)
                            break;
                        m.voipIndividualOutgoing = r.int32();
                        m._voipIndividualOutgoing = "voipIndividualOutgoing";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.groupsV3 = r.int32();
                        m._groupsV3 = "groupsV3";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.groupsV3Create = r.int32();
                        m._groupsV3Create = "groupsV3Create";
                        continue;
                    }
                case 5: {
                        if (u !== 0)
                            break;
                        m.changeNumberV2 = r.int32();
                        m._changeNumberV2 = "changeNumberV2";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.queryStatusV3Thumbnail = r.int32();
                        m._queryStatusV3Thumbnail = "queryStatusV3Thumbnail";
                        continue;
                    }
                case 7: {
                        if (u !== 0)
                            break;
                        m.liveLocations = r.int32();
                        m._liveLocations = "liveLocations";
                        continue;
                    }
                case 8: {
                        if (u !== 0)
                            break;
                        m.queryVname = r.int32();
                        m._queryVname = "queryVname";
                        continue;
                    }
                case 9: {
                        if (u !== 0)
                            break;
                        m.voipIndividualIncoming = r.int32();
                        m._voipIndividualIncoming = "voipIndividualIncoming";
                        continue;
                    }
                case 10: {
                        if (u !== 0)
                            break;
                        m.quickRepliesQuery = r.int32();
                        m._quickRepliesQuery = "quickRepliesQuery";
                        continue;
                    }
                case 11: {
                        if (u !== 0)
                            break;
                        m.payments = r.int32();
                        m._payments = "payments";
                        continue;
                    }
                case 12: {
                        if (u !== 0)
                            break;
                        m.stickerPackQuery = r.int32();
                        m._stickerPackQuery = "stickerPackQuery";
                        continue;
                    }
                case 13: {
                        if (u !== 0)
                            break;
                        m.liveLocationsFinal = r.int32();
                        m._liveLocationsFinal = "liveLocationsFinal";
                        continue;
                    }
                case 14: {
                        if (u !== 0)
                            break;
                        m.labelsEdit = r.int32();
                        m._labelsEdit = "labelsEdit";
                        continue;
                    }
                case 15: {
                        if (u !== 0)
                            break;
                        m.mediaUpload = r.int32();
                        m._mediaUpload = "mediaUpload";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        m.mediaUploadRichQuickReplies = r.int32();
                        m._mediaUploadRichQuickReplies = "mediaUploadRichQuickReplies";
                        continue;
                    }
                case 19: {
                        if (u !== 0)
                            break;
                        m.vnameV2 = r.int32();
                        m._vnameV2 = "vnameV2";
                        continue;
                    }
                case 20: {
                        if (u !== 0)
                            break;
                        m.videoPlaybackUrl = r.int32();
                        m._videoPlaybackUrl = "videoPlaybackUrl";
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        m.statusRanking = r.int32();
                        m._statusRanking = "statusRanking";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        m.voipIndividualVideo = r.int32();
                        m._voipIndividualVideo = "voipIndividualVideo";
                        continue;
                    }
                case 23: {
                        if (u !== 0)
                            break;
                        m.thirdPartyStickers = r.int32();
                        m._thirdPartyStickers = "thirdPartyStickers";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        m.frequentlyForwardedSetting = r.int32();
                        m._frequentlyForwardedSetting = "frequentlyForwardedSetting";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        m.groupsV4JoinPermission = r.int32();
                        m._groupsV4JoinPermission = "groupsV4JoinPermission";
                        continue;
                    }
                case 26: {
                        if (u !== 0)
                            break;
                        m.recentStickers = r.int32();
                        m._recentStickers = "recentStickers";
                        continue;
                    }
                case 27: {
                        if (u !== 0)
                            break;
                        m.catalog = r.int32();
                        m._catalog = "catalog";
                        continue;
                    }
                case 28: {
                        if (u !== 0)
                            break;
                        m.starredStickers = r.int32();
                        m._starredStickers = "starredStickers";
                        continue;
                    }
                case 29: {
                        if (u !== 0)
                            break;
                        m.voipGroupCall = r.int32();
                        m._voipGroupCall = "voipGroupCall";
                        continue;
                    }
                case 30: {
                        if (u !== 0)
                            break;
                        m.templateMessage = r.int32();
                        m._templateMessage = "templateMessage";
                        continue;
                    }
                case 31: {
                        if (u !== 0)
                            break;
                        m.templateMessageInteractivity = r.int32();
                        m._templateMessageInteractivity = "templateMessageInteractivity";
                        continue;
                    }
                case 32: {
                        if (u !== 0)
                            break;
                        m.ephemeralMessages = r.int32();
                        m._ephemeralMessages = "ephemeralMessages";
                        continue;
                    }
                case 33: {
                        if (u !== 0)
                            break;
                        m.e2ENotificationSync = r.int32();
                        m._e2ENotificationSync = "e2ENotificationSync";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        m.recentStickersV2 = r.int32();
                        m._recentStickersV2 = "recentStickersV2";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        m.recentStickersV3 = r.int32();
                        m._recentStickersV3 = "recentStickersV3";
                        continue;
                    }
                case 37: {
                        if (u !== 0)
                            break;
                        m.userNotice = r.int32();
                        m._userNotice = "userNotice";
                        continue;
                    }
                case 39: {
                        if (u !== 0)
                            break;
                        m.support = r.int32();
                        m._support = "support";
                        continue;
                    }
                case 40: {
                        if (u !== 0)
                            break;
                        m.groupUiiCleanup = r.int32();
                        m._groupUiiCleanup = "groupUiiCleanup";
                        continue;
                    }
                case 41: {
                        if (u !== 0)
                            break;
                        m.groupDogfoodingInternalOnly = r.int32();
                        m._groupDogfoodingInternalOnly = "groupDogfoodingInternalOnly";
                        continue;
                    }
                case 42: {
                        if (u !== 0)
                            break;
                        m.settingsSync = r.int32();
                        m._settingsSync = "settingsSync";
                        continue;
                    }
                case 43: {
                        if (u !== 0)
                            break;
                        m.archiveV2 = r.int32();
                        m._archiveV2 = "archiveV2";
                        continue;
                    }
                case 44: {
                        if (u !== 0)
                            break;
                        m.ephemeralAllowGroupMembers = r.int32();
                        m._ephemeralAllowGroupMembers = "ephemeralAllowGroupMembers";
                        continue;
                    }
                case 45: {
                        if (u !== 0)
                            break;
                        m.ephemeral24HDuration = r.int32();
                        m._ephemeral24HDuration = "ephemeral24HDuration";
                        continue;
                    }
                case 46: {
                        if (u !== 0)
                            break;
                        m.mdForceUpgrade = r.int32();
                        m._mdForceUpgrade = "mdForceUpgrade";
                        continue;
                    }
                case 47: {
                        if (u !== 0)
                            break;
                        m.disappearingMode = r.int32();
                        m._disappearingMode = "disappearingMode";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        m.externalMdOptInAvailable = r.int32();
                        m._externalMdOptInAvailable = "externalMdOptInAvailable";
                        continue;
                    }
                case 49: {
                        if (u !== 0)
                            break;
                        m.noDeleteMessageTimeLimit = r.int32();
                        m._noDeleteMessageTimeLimit = "noDeleteMessageTimeLimit";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        WebFeatures.fromObject = function (d, q) {
            if (d instanceof $root.proto.WebFeatures)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.WebFeatures: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.WebFeatures();
            switch (d.labelsDisplay) {
            case "NOT_STARTED":
            case 0:
                m.labelsDisplay = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.labelsDisplay = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.labelsDisplay = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.labelsDisplay = 3;
                break;
            default:
                if (typeof d.labelsDisplay === "number" && (d.labelsDisplay | 0) === d.labelsDisplay)
                    m.labelsDisplay = d.labelsDisplay;
            }
            switch (d.voipIndividualOutgoing) {
            case "NOT_STARTED":
            case 0:
                m.voipIndividualOutgoing = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipIndividualOutgoing = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipIndividualOutgoing = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipIndividualOutgoing = 3;
                break;
            default:
                if (typeof d.voipIndividualOutgoing === "number" && (d.voipIndividualOutgoing | 0) === d.voipIndividualOutgoing)
                    m.voipIndividualOutgoing = d.voipIndividualOutgoing;
            }
            switch (d.groupsV3) {
            case "NOT_STARTED":
            case 0:
                m.groupsV3 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupsV3 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupsV3 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupsV3 = 3;
                break;
            default:
                if (typeof d.groupsV3 === "number" && (d.groupsV3 | 0) === d.groupsV3)
                    m.groupsV3 = d.groupsV3;
            }
            switch (d.groupsV3Create) {
            case "NOT_STARTED":
            case 0:
                m.groupsV3Create = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupsV3Create = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupsV3Create = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupsV3Create = 3;
                break;
            default:
                if (typeof d.groupsV3Create === "number" && (d.groupsV3Create | 0) === d.groupsV3Create)
                    m.groupsV3Create = d.groupsV3Create;
            }
            switch (d.changeNumberV2) {
            case "NOT_STARTED":
            case 0:
                m.changeNumberV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.changeNumberV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.changeNumberV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.changeNumberV2 = 3;
                break;
            default:
                if (typeof d.changeNumberV2 === "number" && (d.changeNumberV2 | 0) === d.changeNumberV2)
                    m.changeNumberV2 = d.changeNumberV2;
            }
            switch (d.queryStatusV3Thumbnail) {
            case "NOT_STARTED":
            case 0:
                m.queryStatusV3Thumbnail = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.queryStatusV3Thumbnail = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.queryStatusV3Thumbnail = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.queryStatusV3Thumbnail = 3;
                break;
            default:
                if (typeof d.queryStatusV3Thumbnail === "number" && (d.queryStatusV3Thumbnail | 0) === d.queryStatusV3Thumbnail)
                    m.queryStatusV3Thumbnail = d.queryStatusV3Thumbnail;
            }
            switch (d.liveLocations) {
            case "NOT_STARTED":
            case 0:
                m.liveLocations = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.liveLocations = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.liveLocations = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.liveLocations = 3;
                break;
            default:
                if (typeof d.liveLocations === "number" && (d.liveLocations | 0) === d.liveLocations)
                    m.liveLocations = d.liveLocations;
            }
            switch (d.queryVname) {
            case "NOT_STARTED":
            case 0:
                m.queryVname = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.queryVname = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.queryVname = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.queryVname = 3;
                break;
            default:
                if (typeof d.queryVname === "number" && (d.queryVname | 0) === d.queryVname)
                    m.queryVname = d.queryVname;
            }
            switch (d.voipIndividualIncoming) {
            case "NOT_STARTED":
            case 0:
                m.voipIndividualIncoming = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipIndividualIncoming = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipIndividualIncoming = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipIndividualIncoming = 3;
                break;
            default:
                if (typeof d.voipIndividualIncoming === "number" && (d.voipIndividualIncoming | 0) === d.voipIndividualIncoming)
                    m.voipIndividualIncoming = d.voipIndividualIncoming;
            }
            switch (d.quickRepliesQuery) {
            case "NOT_STARTED":
            case 0:
                m.quickRepliesQuery = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.quickRepliesQuery = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.quickRepliesQuery = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.quickRepliesQuery = 3;
                break;
            default:
                if (typeof d.quickRepliesQuery === "number" && (d.quickRepliesQuery | 0) === d.quickRepliesQuery)
                    m.quickRepliesQuery = d.quickRepliesQuery;
            }
            switch (d.payments) {
            case "NOT_STARTED":
            case 0:
                m.payments = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.payments = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.payments = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.payments = 3;
                break;
            default:
                if (typeof d.payments === "number" && (d.payments | 0) === d.payments)
                    m.payments = d.payments;
            }
            switch (d.stickerPackQuery) {
            case "NOT_STARTED":
            case 0:
                m.stickerPackQuery = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.stickerPackQuery = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.stickerPackQuery = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.stickerPackQuery = 3;
                break;
            default:
                if (typeof d.stickerPackQuery === "number" && (d.stickerPackQuery | 0) === d.stickerPackQuery)
                    m.stickerPackQuery = d.stickerPackQuery;
            }
            switch (d.liveLocationsFinal) {
            case "NOT_STARTED":
            case 0:
                m.liveLocationsFinal = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.liveLocationsFinal = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.liveLocationsFinal = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.liveLocationsFinal = 3;
                break;
            default:
                if (typeof d.liveLocationsFinal === "number" && (d.liveLocationsFinal | 0) === d.liveLocationsFinal)
                    m.liveLocationsFinal = d.liveLocationsFinal;
            }
            switch (d.labelsEdit) {
            case "NOT_STARTED":
            case 0:
                m.labelsEdit = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.labelsEdit = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.labelsEdit = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.labelsEdit = 3;
                break;
            default:
                if (typeof d.labelsEdit === "number" && (d.labelsEdit | 0) === d.labelsEdit)
                    m.labelsEdit = d.labelsEdit;
            }
            switch (d.mediaUpload) {
            case "NOT_STARTED":
            case 0:
                m.mediaUpload = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.mediaUpload = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.mediaUpload = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.mediaUpload = 3;
                break;
            default:
                if (typeof d.mediaUpload === "number" && (d.mediaUpload | 0) === d.mediaUpload)
                    m.mediaUpload = d.mediaUpload;
            }
            switch (d.mediaUploadRichQuickReplies) {
            case "NOT_STARTED":
            case 0:
                m.mediaUploadRichQuickReplies = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.mediaUploadRichQuickReplies = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.mediaUploadRichQuickReplies = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.mediaUploadRichQuickReplies = 3;
                break;
            default:
                if (typeof d.mediaUploadRichQuickReplies === "number" && (d.mediaUploadRichQuickReplies | 0) === d.mediaUploadRichQuickReplies)
                    m.mediaUploadRichQuickReplies = d.mediaUploadRichQuickReplies;
            }
            switch (d.vnameV2) {
            case "NOT_STARTED":
            case 0:
                m.vnameV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.vnameV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.vnameV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.vnameV2 = 3;
                break;
            default:
                if (typeof d.vnameV2 === "number" && (d.vnameV2 | 0) === d.vnameV2)
                    m.vnameV2 = d.vnameV2;
            }
            switch (d.videoPlaybackUrl) {
            case "NOT_STARTED":
            case 0:
                m.videoPlaybackUrl = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.videoPlaybackUrl = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.videoPlaybackUrl = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.videoPlaybackUrl = 3;
                break;
            default:
                if (typeof d.videoPlaybackUrl === "number" && (d.videoPlaybackUrl | 0) === d.videoPlaybackUrl)
                    m.videoPlaybackUrl = d.videoPlaybackUrl;
            }
            switch (d.statusRanking) {
            case "NOT_STARTED":
            case 0:
                m.statusRanking = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.statusRanking = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.statusRanking = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.statusRanking = 3;
                break;
            default:
                if (typeof d.statusRanking === "number" && (d.statusRanking | 0) === d.statusRanking)
                    m.statusRanking = d.statusRanking;
            }
            switch (d.voipIndividualVideo) {
            case "NOT_STARTED":
            case 0:
                m.voipIndividualVideo = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipIndividualVideo = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipIndividualVideo = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipIndividualVideo = 3;
                break;
            default:
                if (typeof d.voipIndividualVideo === "number" && (d.voipIndividualVideo | 0) === d.voipIndividualVideo)
                    m.voipIndividualVideo = d.voipIndividualVideo;
            }
            switch (d.thirdPartyStickers) {
            case "NOT_STARTED":
            case 0:
                m.thirdPartyStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.thirdPartyStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.thirdPartyStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.thirdPartyStickers = 3;
                break;
            default:
                if (typeof d.thirdPartyStickers === "number" && (d.thirdPartyStickers | 0) === d.thirdPartyStickers)
                    m.thirdPartyStickers = d.thirdPartyStickers;
            }
            switch (d.frequentlyForwardedSetting) {
            case "NOT_STARTED":
            case 0:
                m.frequentlyForwardedSetting = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.frequentlyForwardedSetting = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.frequentlyForwardedSetting = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.frequentlyForwardedSetting = 3;
                break;
            default:
                if (typeof d.frequentlyForwardedSetting === "number" && (d.frequentlyForwardedSetting | 0) === d.frequentlyForwardedSetting)
                    m.frequentlyForwardedSetting = d.frequentlyForwardedSetting;
            }
            switch (d.groupsV4JoinPermission) {
            case "NOT_STARTED":
            case 0:
                m.groupsV4JoinPermission = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupsV4JoinPermission = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupsV4JoinPermission = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupsV4JoinPermission = 3;
                break;
            default:
                if (typeof d.groupsV4JoinPermission === "number" && (d.groupsV4JoinPermission | 0) === d.groupsV4JoinPermission)
                    m.groupsV4JoinPermission = d.groupsV4JoinPermission;
            }
            switch (d.recentStickers) {
            case "NOT_STARTED":
            case 0:
                m.recentStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.recentStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.recentStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.recentStickers = 3;
                break;
            default:
                if (typeof d.recentStickers === "number" && (d.recentStickers | 0) === d.recentStickers)
                    m.recentStickers = d.recentStickers;
            }
            switch (d.catalog) {
            case "NOT_STARTED":
            case 0:
                m.catalog = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.catalog = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.catalog = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.catalog = 3;
                break;
            default:
                if (typeof d.catalog === "number" && (d.catalog | 0) === d.catalog)
                    m.catalog = d.catalog;
            }
            switch (d.starredStickers) {
            case "NOT_STARTED":
            case 0:
                m.starredStickers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.starredStickers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.starredStickers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.starredStickers = 3;
                break;
            default:
                if (typeof d.starredStickers === "number" && (d.starredStickers | 0) === d.starredStickers)
                    m.starredStickers = d.starredStickers;
            }
            switch (d.voipGroupCall) {
            case "NOT_STARTED":
            case 0:
                m.voipGroupCall = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.voipGroupCall = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.voipGroupCall = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.voipGroupCall = 3;
                break;
            default:
                if (typeof d.voipGroupCall === "number" && (d.voipGroupCall | 0) === d.voipGroupCall)
                    m.voipGroupCall = d.voipGroupCall;
            }
            switch (d.templateMessage) {
            case "NOT_STARTED":
            case 0:
                m.templateMessage = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.templateMessage = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.templateMessage = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.templateMessage = 3;
                break;
            default:
                if (typeof d.templateMessage === "number" && (d.templateMessage | 0) === d.templateMessage)
                    m.templateMessage = d.templateMessage;
            }
            switch (d.templateMessageInteractivity) {
            case "NOT_STARTED":
            case 0:
                m.templateMessageInteractivity = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.templateMessageInteractivity = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.templateMessageInteractivity = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.templateMessageInteractivity = 3;
                break;
            default:
                if (typeof d.templateMessageInteractivity === "number" && (d.templateMessageInteractivity | 0) === d.templateMessageInteractivity)
                    m.templateMessageInteractivity = d.templateMessageInteractivity;
            }
            switch (d.ephemeralMessages) {
            case "NOT_STARTED":
            case 0:
                m.ephemeralMessages = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.ephemeralMessages = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.ephemeralMessages = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.ephemeralMessages = 3;
                break;
            default:
                if (typeof d.ephemeralMessages === "number" && (d.ephemeralMessages | 0) === d.ephemeralMessages)
                    m.ephemeralMessages = d.ephemeralMessages;
            }
            switch (d.e2ENotificationSync) {
            case "NOT_STARTED":
            case 0:
                m.e2ENotificationSync = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.e2ENotificationSync = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.e2ENotificationSync = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.e2ENotificationSync = 3;
                break;
            default:
                if (typeof d.e2ENotificationSync === "number" && (d.e2ENotificationSync | 0) === d.e2ENotificationSync)
                    m.e2ENotificationSync = d.e2ENotificationSync;
            }
            switch (d.recentStickersV2) {
            case "NOT_STARTED":
            case 0:
                m.recentStickersV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.recentStickersV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.recentStickersV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.recentStickersV2 = 3;
                break;
            default:
                if (typeof d.recentStickersV2 === "number" && (d.recentStickersV2 | 0) === d.recentStickersV2)
                    m.recentStickersV2 = d.recentStickersV2;
            }
            switch (d.recentStickersV3) {
            case "NOT_STARTED":
            case 0:
                m.recentStickersV3 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.recentStickersV3 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.recentStickersV3 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.recentStickersV3 = 3;
                break;
            default:
                if (typeof d.recentStickersV3 === "number" && (d.recentStickersV3 | 0) === d.recentStickersV3)
                    m.recentStickersV3 = d.recentStickersV3;
            }
            switch (d.userNotice) {
            case "NOT_STARTED":
            case 0:
                m.userNotice = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.userNotice = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.userNotice = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.userNotice = 3;
                break;
            default:
                if (typeof d.userNotice === "number" && (d.userNotice | 0) === d.userNotice)
                    m.userNotice = d.userNotice;
            }
            switch (d.support) {
            case "NOT_STARTED":
            case 0:
                m.support = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.support = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.support = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.support = 3;
                break;
            default:
                if (typeof d.support === "number" && (d.support | 0) === d.support)
                    m.support = d.support;
            }
            switch (d.groupUiiCleanup) {
            case "NOT_STARTED":
            case 0:
                m.groupUiiCleanup = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupUiiCleanup = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupUiiCleanup = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupUiiCleanup = 3;
                break;
            default:
                if (typeof d.groupUiiCleanup === "number" && (d.groupUiiCleanup | 0) === d.groupUiiCleanup)
                    m.groupUiiCleanup = d.groupUiiCleanup;
            }
            switch (d.groupDogfoodingInternalOnly) {
            case "NOT_STARTED":
            case 0:
                m.groupDogfoodingInternalOnly = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.groupDogfoodingInternalOnly = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.groupDogfoodingInternalOnly = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.groupDogfoodingInternalOnly = 3;
                break;
            default:
                if (typeof d.groupDogfoodingInternalOnly === "number" && (d.groupDogfoodingInternalOnly | 0) === d.groupDogfoodingInternalOnly)
                    m.groupDogfoodingInternalOnly = d.groupDogfoodingInternalOnly;
            }
            switch (d.settingsSync) {
            case "NOT_STARTED":
            case 0:
                m.settingsSync = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.settingsSync = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.settingsSync = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.settingsSync = 3;
                break;
            default:
                if (typeof d.settingsSync === "number" && (d.settingsSync | 0) === d.settingsSync)
                    m.settingsSync = d.settingsSync;
            }
            switch (d.archiveV2) {
            case "NOT_STARTED":
            case 0:
                m.archiveV2 = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.archiveV2 = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.archiveV2 = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.archiveV2 = 3;
                break;
            default:
                if (typeof d.archiveV2 === "number" && (d.archiveV2 | 0) === d.archiveV2)
                    m.archiveV2 = d.archiveV2;
            }
            switch (d.ephemeralAllowGroupMembers) {
            case "NOT_STARTED":
            case 0:
                m.ephemeralAllowGroupMembers = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.ephemeralAllowGroupMembers = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.ephemeralAllowGroupMembers = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.ephemeralAllowGroupMembers = 3;
                break;
            default:
                if (typeof d.ephemeralAllowGroupMembers === "number" && (d.ephemeralAllowGroupMembers | 0) === d.ephemeralAllowGroupMembers)
                    m.ephemeralAllowGroupMembers = d.ephemeralAllowGroupMembers;
            }
            switch (d.ephemeral24HDuration) {
            case "NOT_STARTED":
            case 0:
                m.ephemeral24HDuration = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.ephemeral24HDuration = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.ephemeral24HDuration = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.ephemeral24HDuration = 3;
                break;
            default:
                if (typeof d.ephemeral24HDuration === "number" && (d.ephemeral24HDuration | 0) === d.ephemeral24HDuration)
                    m.ephemeral24HDuration = d.ephemeral24HDuration;
            }
            switch (d.mdForceUpgrade) {
            case "NOT_STARTED":
            case 0:
                m.mdForceUpgrade = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.mdForceUpgrade = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.mdForceUpgrade = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.mdForceUpgrade = 3;
                break;
            default:
                if (typeof d.mdForceUpgrade === "number" && (d.mdForceUpgrade | 0) === d.mdForceUpgrade)
                    m.mdForceUpgrade = d.mdForceUpgrade;
            }
            switch (d.disappearingMode) {
            case "NOT_STARTED":
            case 0:
                m.disappearingMode = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.disappearingMode = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.disappearingMode = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.disappearingMode = 3;
                break;
            default:
                if (typeof d.disappearingMode === "number" && (d.disappearingMode | 0) === d.disappearingMode)
                    m.disappearingMode = d.disappearingMode;
            }
            switch (d.externalMdOptInAvailable) {
            case "NOT_STARTED":
            case 0:
                m.externalMdOptInAvailable = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.externalMdOptInAvailable = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.externalMdOptInAvailable = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.externalMdOptInAvailable = 3;
                break;
            default:
                if (typeof d.externalMdOptInAvailable === "number" && (d.externalMdOptInAvailable | 0) === d.externalMdOptInAvailable)
                    m.externalMdOptInAvailable = d.externalMdOptInAvailable;
            }
            switch (d.noDeleteMessageTimeLimit) {
            case "NOT_STARTED":
            case 0:
                m.noDeleteMessageTimeLimit = 0;
                break;
            case "FORCE_UPGRADE":
            case 1:
                m.noDeleteMessageTimeLimit = 1;
                break;
            case "DEVELOPMENT":
            case 2:
                m.noDeleteMessageTimeLimit = 2;
                break;
            case "PRODUCTION":
            case 3:
                m.noDeleteMessageTimeLimit = 3;
                break;
            default:
                if (typeof d.noDeleteMessageTimeLimit === "number" && (d.noDeleteMessageTimeLimit | 0) === d.noDeleteMessageTimeLimit)
                    m.noDeleteMessageTimeLimit = d.noDeleteMessageTimeLimit;
            }
            return m;
        };

        WebFeatures.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.labelsDisplay != null && $Object.hasOwnProperty.call(m, "labelsDisplay")) {
                d.labelsDisplay = o.enums === $String ? $root.proto.WebFeatures.Flag[m.labelsDisplay] === $undefined ? m.labelsDisplay : $root.proto.WebFeatures.Flag[m.labelsDisplay] : m.labelsDisplay;
            }
            if (m.voipIndividualOutgoing != null && $Object.hasOwnProperty.call(m, "voipIndividualOutgoing")) {
                d.voipIndividualOutgoing = o.enums === $String ? $root.proto.WebFeatures.Flag[m.voipIndividualOutgoing] === $undefined ? m.voipIndividualOutgoing : $root.proto.WebFeatures.Flag[m.voipIndividualOutgoing] : m.voipIndividualOutgoing;
            }
            if (m.groupsV3 != null && $Object.hasOwnProperty.call(m, "groupsV3")) {
                d.groupsV3 = o.enums === $String ? $root.proto.WebFeatures.Flag[m.groupsV3] === $undefined ? m.groupsV3 : $root.proto.WebFeatures.Flag[m.groupsV3] : m.groupsV3;
            }
            if (m.groupsV3Create != null && $Object.hasOwnProperty.call(m, "groupsV3Create")) {
                d.groupsV3Create = o.enums === $String ? $root.proto.WebFeatures.Flag[m.groupsV3Create] === $undefined ? m.groupsV3Create : $root.proto.WebFeatures.Flag[m.groupsV3Create] : m.groupsV3Create;
            }
            if (m.changeNumberV2 != null && $Object.hasOwnProperty.call(m, "changeNumberV2")) {
                d.changeNumberV2 = o.enums === $String ? $root.proto.WebFeatures.Flag[m.changeNumberV2] === $undefined ? m.changeNumberV2 : $root.proto.WebFeatures.Flag[m.changeNumberV2] : m.changeNumberV2;
            }
            if (m.queryStatusV3Thumbnail != null && $Object.hasOwnProperty.call(m, "queryStatusV3Thumbnail")) {
                d.queryStatusV3Thumbnail = o.enums === $String ? $root.proto.WebFeatures.Flag[m.queryStatusV3Thumbnail] === $undefined ? m.queryStatusV3Thumbnail : $root.proto.WebFeatures.Flag[m.queryStatusV3Thumbnail] : m.queryStatusV3Thumbnail;
            }
            if (m.liveLocations != null && $Object.hasOwnProperty.call(m, "liveLocations")) {
                d.liveLocations = o.enums === $String ? $root.proto.WebFeatures.Flag[m.liveLocations] === $undefined ? m.liveLocations : $root.proto.WebFeatures.Flag[m.liveLocations] : m.liveLocations;
            }
            if (m.queryVname != null && $Object.hasOwnProperty.call(m, "queryVname")) {
                d.queryVname = o.enums === $String ? $root.proto.WebFeatures.Flag[m.queryVname] === $undefined ? m.queryVname : $root.proto.WebFeatures.Flag[m.queryVname] : m.queryVname;
            }
            if (m.voipIndividualIncoming != null && $Object.hasOwnProperty.call(m, "voipIndividualIncoming")) {
                d.voipIndividualIncoming = o.enums === $String ? $root.proto.WebFeatures.Flag[m.voipIndividualIncoming] === $undefined ? m.voipIndividualIncoming : $root.proto.WebFeatures.Flag[m.voipIndividualIncoming] : m.voipIndividualIncoming;
            }
            if (m.quickRepliesQuery != null && $Object.hasOwnProperty.call(m, "quickRepliesQuery")) {
                d.quickRepliesQuery = o.enums === $String ? $root.proto.WebFeatures.Flag[m.quickRepliesQuery] === $undefined ? m.quickRepliesQuery : $root.proto.WebFeatures.Flag[m.quickRepliesQuery] : m.quickRepliesQuery;
            }
            if (m.payments != null && $Object.hasOwnProperty.call(m, "payments")) {
                d.payments = o.enums === $String ? $root.proto.WebFeatures.Flag[m.payments] === $undefined ? m.payments : $root.proto.WebFeatures.Flag[m.payments] : m.payments;
            }
            if (m.stickerPackQuery != null && $Object.hasOwnProperty.call(m, "stickerPackQuery")) {
                d.stickerPackQuery = o.enums === $String ? $root.proto.WebFeatures.Flag[m.stickerPackQuery] === $undefined ? m.stickerPackQuery : $root.proto.WebFeatures.Flag[m.stickerPackQuery] : m.stickerPackQuery;
            }
            if (m.liveLocationsFinal != null && $Object.hasOwnProperty.call(m, "liveLocationsFinal")) {
                d.liveLocationsFinal = o.enums === $String ? $root.proto.WebFeatures.Flag[m.liveLocationsFinal] === $undefined ? m.liveLocationsFinal : $root.proto.WebFeatures.Flag[m.liveLocationsFinal] : m.liveLocationsFinal;
            }
            if (m.labelsEdit != null && $Object.hasOwnProperty.call(m, "labelsEdit")) {
                d.labelsEdit = o.enums === $String ? $root.proto.WebFeatures.Flag[m.labelsEdit] === $undefined ? m.labelsEdit : $root.proto.WebFeatures.Flag[m.labelsEdit] : m.labelsEdit;
            }
            if (m.mediaUpload != null && $Object.hasOwnProperty.call(m, "mediaUpload")) {
                d.mediaUpload = o.enums === $String ? $root.proto.WebFeatures.Flag[m.mediaUpload] === $undefined ? m.mediaUpload : $root.proto.WebFeatures.Flag[m.mediaUpload] : m.mediaUpload;
            }
            if (m.mediaUploadRichQuickReplies != null && $Object.hasOwnProperty.call(m, "mediaUploadRichQuickReplies")) {
                d.mediaUploadRichQuickReplies = o.enums === $String ? $root.proto.WebFeatures.Flag[m.mediaUploadRichQuickReplies] === $undefined ? m.mediaUploadRichQuickReplies : $root.proto.WebFeatures.Flag[m.mediaUploadRichQuickReplies] : m.mediaUploadRichQuickReplies;
            }
            if (m.vnameV2 != null && $Object.hasOwnProperty.call(m, "vnameV2")) {
                d.vnameV2 = o.enums === $String ? $root.proto.WebFeatures.Flag[m.vnameV2] === $undefined ? m.vnameV2 : $root.proto.WebFeatures.Flag[m.vnameV2] : m.vnameV2;
            }
            if (m.videoPlaybackUrl != null && $Object.hasOwnProperty.call(m, "videoPlaybackUrl")) {
                d.videoPlaybackUrl = o.enums === $String ? $root.proto.WebFeatures.Flag[m.videoPlaybackUrl] === $undefined ? m.videoPlaybackUrl : $root.proto.WebFeatures.Flag[m.videoPlaybackUrl] : m.videoPlaybackUrl;
            }
            if (m.statusRanking != null && $Object.hasOwnProperty.call(m, "statusRanking")) {
                d.statusRanking = o.enums === $String ? $root.proto.WebFeatures.Flag[m.statusRanking] === $undefined ? m.statusRanking : $root.proto.WebFeatures.Flag[m.statusRanking] : m.statusRanking;
            }
            if (m.voipIndividualVideo != null && $Object.hasOwnProperty.call(m, "voipIndividualVideo")) {
                d.voipIndividualVideo = o.enums === $String ? $root.proto.WebFeatures.Flag[m.voipIndividualVideo] === $undefined ? m.voipIndividualVideo : $root.proto.WebFeatures.Flag[m.voipIndividualVideo] : m.voipIndividualVideo;
            }
            if (m.thirdPartyStickers != null && $Object.hasOwnProperty.call(m, "thirdPartyStickers")) {
                d.thirdPartyStickers = o.enums === $String ? $root.proto.WebFeatures.Flag[m.thirdPartyStickers] === $undefined ? m.thirdPartyStickers : $root.proto.WebFeatures.Flag[m.thirdPartyStickers] : m.thirdPartyStickers;
            }
            if (m.frequentlyForwardedSetting != null && $Object.hasOwnProperty.call(m, "frequentlyForwardedSetting")) {
                d.frequentlyForwardedSetting = o.enums === $String ? $root.proto.WebFeatures.Flag[m.frequentlyForwardedSetting] === $undefined ? m.frequentlyForwardedSetting : $root.proto.WebFeatures.Flag[m.frequentlyForwardedSetting] : m.frequentlyForwardedSetting;
            }
            if (m.groupsV4JoinPermission != null && $Object.hasOwnProperty.call(m, "groupsV4JoinPermission")) {
                d.groupsV4JoinPermission = o.enums === $String ? $root.proto.WebFeatures.Flag[m.groupsV4JoinPermission] === $undefined ? m.groupsV4JoinPermission : $root.proto.WebFeatures.Flag[m.groupsV4JoinPermission] : m.groupsV4JoinPermission;
            }
            if (m.recentStickers != null && $Object.hasOwnProperty.call(m, "recentStickers")) {
                d.recentStickers = o.enums === $String ? $root.proto.WebFeatures.Flag[m.recentStickers] === $undefined ? m.recentStickers : $root.proto.WebFeatures.Flag[m.recentStickers] : m.recentStickers;
            }
            if (m.catalog != null && $Object.hasOwnProperty.call(m, "catalog")) {
                d.catalog = o.enums === $String ? $root.proto.WebFeatures.Flag[m.catalog] === $undefined ? m.catalog : $root.proto.WebFeatures.Flag[m.catalog] : m.catalog;
            }
            if (m.starredStickers != null && $Object.hasOwnProperty.call(m, "starredStickers")) {
                d.starredStickers = o.enums === $String ? $root.proto.WebFeatures.Flag[m.starredStickers] === $undefined ? m.starredStickers : $root.proto.WebFeatures.Flag[m.starredStickers] : m.starredStickers;
            }
            if (m.voipGroupCall != null && $Object.hasOwnProperty.call(m, "voipGroupCall")) {
                d.voipGroupCall = o.enums === $String ? $root.proto.WebFeatures.Flag[m.voipGroupCall] === $undefined ? m.voipGroupCall : $root.proto.WebFeatures.Flag[m.voipGroupCall] : m.voipGroupCall;
            }
            if (m.templateMessage != null && $Object.hasOwnProperty.call(m, "templateMessage")) {
                d.templateMessage = o.enums === $String ? $root.proto.WebFeatures.Flag[m.templateMessage] === $undefined ? m.templateMessage : $root.proto.WebFeatures.Flag[m.templateMessage] : m.templateMessage;
            }
            if (m.templateMessageInteractivity != null && $Object.hasOwnProperty.call(m, "templateMessageInteractivity")) {
                d.templateMessageInteractivity = o.enums === $String ? $root.proto.WebFeatures.Flag[m.templateMessageInteractivity] === $undefined ? m.templateMessageInteractivity : $root.proto.WebFeatures.Flag[m.templateMessageInteractivity] : m.templateMessageInteractivity;
            }
            if (m.ephemeralMessages != null && $Object.hasOwnProperty.call(m, "ephemeralMessages")) {
                d.ephemeralMessages = o.enums === $String ? $root.proto.WebFeatures.Flag[m.ephemeralMessages] === $undefined ? m.ephemeralMessages : $root.proto.WebFeatures.Flag[m.ephemeralMessages] : m.ephemeralMessages;
            }
            if (m.e2ENotificationSync != null && $Object.hasOwnProperty.call(m, "e2ENotificationSync")) {
                d.e2ENotificationSync = o.enums === $String ? $root.proto.WebFeatures.Flag[m.e2ENotificationSync] === $undefined ? m.e2ENotificationSync : $root.proto.WebFeatures.Flag[m.e2ENotificationSync] : m.e2ENotificationSync;
            }
            if (m.recentStickersV2 != null && $Object.hasOwnProperty.call(m, "recentStickersV2")) {
                d.recentStickersV2 = o.enums === $String ? $root.proto.WebFeatures.Flag[m.recentStickersV2] === $undefined ? m.recentStickersV2 : $root.proto.WebFeatures.Flag[m.recentStickersV2] : m.recentStickersV2;
            }
            if (m.recentStickersV3 != null && $Object.hasOwnProperty.call(m, "recentStickersV3")) {
                d.recentStickersV3 = o.enums === $String ? $root.proto.WebFeatures.Flag[m.recentStickersV3] === $undefined ? m.recentStickersV3 : $root.proto.WebFeatures.Flag[m.recentStickersV3] : m.recentStickersV3;
            }
            if (m.userNotice != null && $Object.hasOwnProperty.call(m, "userNotice")) {
                d.userNotice = o.enums === $String ? $root.proto.WebFeatures.Flag[m.userNotice] === $undefined ? m.userNotice : $root.proto.WebFeatures.Flag[m.userNotice] : m.userNotice;
            }
            if (m.support != null && $Object.hasOwnProperty.call(m, "support")) {
                d.support = o.enums === $String ? $root.proto.WebFeatures.Flag[m.support] === $undefined ? m.support : $root.proto.WebFeatures.Flag[m.support] : m.support;
            }
            if (m.groupUiiCleanup != null && $Object.hasOwnProperty.call(m, "groupUiiCleanup")) {
                d.groupUiiCleanup = o.enums === $String ? $root.proto.WebFeatures.Flag[m.groupUiiCleanup] === $undefined ? m.groupUiiCleanup : $root.proto.WebFeatures.Flag[m.groupUiiCleanup] : m.groupUiiCleanup;
            }
            if (m.groupDogfoodingInternalOnly != null && $Object.hasOwnProperty.call(m, "groupDogfoodingInternalOnly")) {
                d.groupDogfoodingInternalOnly = o.enums === $String ? $root.proto.WebFeatures.Flag[m.groupDogfoodingInternalOnly] === $undefined ? m.groupDogfoodingInternalOnly : $root.proto.WebFeatures.Flag[m.groupDogfoodingInternalOnly] : m.groupDogfoodingInternalOnly;
            }
            if (m.settingsSync != null && $Object.hasOwnProperty.call(m, "settingsSync")) {
                d.settingsSync = o.enums === $String ? $root.proto.WebFeatures.Flag[m.settingsSync] === $undefined ? m.settingsSync : $root.proto.WebFeatures.Flag[m.settingsSync] : m.settingsSync;
            }
            if (m.archiveV2 != null && $Object.hasOwnProperty.call(m, "archiveV2")) {
                d.archiveV2 = o.enums === $String ? $root.proto.WebFeatures.Flag[m.archiveV2] === $undefined ? m.archiveV2 : $root.proto.WebFeatures.Flag[m.archiveV2] : m.archiveV2;
            }
            if (m.ephemeralAllowGroupMembers != null && $Object.hasOwnProperty.call(m, "ephemeralAllowGroupMembers")) {
                d.ephemeralAllowGroupMembers = o.enums === $String ? $root.proto.WebFeatures.Flag[m.ephemeralAllowGroupMembers] === $undefined ? m.ephemeralAllowGroupMembers : $root.proto.WebFeatures.Flag[m.ephemeralAllowGroupMembers] : m.ephemeralAllowGroupMembers;
            }
            if (m.ephemeral24HDuration != null && $Object.hasOwnProperty.call(m, "ephemeral24HDuration")) {
                d.ephemeral24HDuration = o.enums === $String ? $root.proto.WebFeatures.Flag[m.ephemeral24HDuration] === $undefined ? m.ephemeral24HDuration : $root.proto.WebFeatures.Flag[m.ephemeral24HDuration] : m.ephemeral24HDuration;
            }
            if (m.mdForceUpgrade != null && $Object.hasOwnProperty.call(m, "mdForceUpgrade")) {
                d.mdForceUpgrade = o.enums === $String ? $root.proto.WebFeatures.Flag[m.mdForceUpgrade] === $undefined ? m.mdForceUpgrade : $root.proto.WebFeatures.Flag[m.mdForceUpgrade] : m.mdForceUpgrade;
            }
            if (m.disappearingMode != null && $Object.hasOwnProperty.call(m, "disappearingMode")) {
                d.disappearingMode = o.enums === $String ? $root.proto.WebFeatures.Flag[m.disappearingMode] === $undefined ? m.disappearingMode : $root.proto.WebFeatures.Flag[m.disappearingMode] : m.disappearingMode;
            }
            if (m.externalMdOptInAvailable != null && $Object.hasOwnProperty.call(m, "externalMdOptInAvailable")) {
                d.externalMdOptInAvailable = o.enums === $String ? $root.proto.WebFeatures.Flag[m.externalMdOptInAvailable] === $undefined ? m.externalMdOptInAvailable : $root.proto.WebFeatures.Flag[m.externalMdOptInAvailable] : m.externalMdOptInAvailable;
            }
            if (m.noDeleteMessageTimeLimit != null && $Object.hasOwnProperty.call(m, "noDeleteMessageTimeLimit")) {
                d.noDeleteMessageTimeLimit = o.enums === $String ? $root.proto.WebFeatures.Flag[m.noDeleteMessageTimeLimit] === $undefined ? m.noDeleteMessageTimeLimit : $root.proto.WebFeatures.Flag[m.noDeleteMessageTimeLimit] : m.noDeleteMessageTimeLimit;
            }
            return d;
        };

        WebFeatures.prototype.toJSON = function() {
            return WebFeatures.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebFeatures.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.WebFeatures";
        };

        WebFeatures.Flag = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "NOT_STARTED"] = 0;
            values[valuesById[1] = "FORCE_UPGRADE"] = 1;
            values[valuesById[2] = "DEVELOPMENT"] = 2;
            values[valuesById[3] = "PRODUCTION"] = 3;
            return values;
        })();

        return WebFeatures;
    })();

    proto.WebLinkRenderConfig = (function() {
        const valuesById = $Object.create(null), values = $Object.create(valuesById);
        values[valuesById[0] = "WEBVIEW"] = 0;
        values[valuesById[1] = "SYSTEM"] = 1;
        return values;
    })();

    proto.WebMessageInfo = (function() {

        const WebMessageInfo = function (p) {
            this.messageStubParameters = [];
            this.labels = [];
            this.userReceipt = [];
            this.reactions = [];
            this.pollUpdates = [];
            this.eventResponses = [];
            this.statusMentions = [];
            this.messageAddOns = [];
            this.statusMentionSources = [];
            this.supportAiCitations = [];
            this.decisionSources = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        WebMessageInfo.prototype.key = null;
        WebMessageInfo.prototype.message = null;
        WebMessageInfo.prototype.messageTimestamp = null;
        WebMessageInfo.prototype.status = null;
        WebMessageInfo.prototype.participant = null;
        WebMessageInfo.prototype.messageC2STimestamp = null;
        WebMessageInfo.prototype.ignore = null;
        WebMessageInfo.prototype.starred = null;
        WebMessageInfo.prototype.broadcast = null;
        WebMessageInfo.prototype.pushName = null;
        WebMessageInfo.prototype.mediaCiphertextSha256 = null;
        WebMessageInfo.prototype.multicast = null;
        WebMessageInfo.prototype.urlText = null;
        WebMessageInfo.prototype.urlNumber = null;
        WebMessageInfo.prototype.messageStubType = null;
        WebMessageInfo.prototype.clearMedia = null;
        WebMessageInfo.prototype.messageStubParameters = $util.emptyArray;
        WebMessageInfo.prototype.duration = null;
        WebMessageInfo.prototype.labels = $util.emptyArray;
        WebMessageInfo.prototype.paymentInfo = null;
        WebMessageInfo.prototype.finalLiveLocation = null;
        WebMessageInfo.prototype.quotedPaymentInfo = null;
        WebMessageInfo.prototype.ephemeralStartTimestamp = null;
        WebMessageInfo.prototype.ephemeralDuration = null;
        WebMessageInfo.prototype.ephemeralOffToOn = null;
        WebMessageInfo.prototype.ephemeralOutOfSync = null;
        WebMessageInfo.prototype.bizPrivacyStatus = null;
        WebMessageInfo.prototype.verifiedBizName = null;
        WebMessageInfo.prototype.mediaData = null;
        WebMessageInfo.prototype.photoChange = null;
        WebMessageInfo.prototype.userReceipt = $util.emptyArray;
        WebMessageInfo.prototype.reactions = $util.emptyArray;
        WebMessageInfo.prototype.quotedStickerData = null;
        WebMessageInfo.prototype.futureproofData = null;
        WebMessageInfo.prototype.statusPsa = null;
        WebMessageInfo.prototype.pollUpdates = $util.emptyArray;
        WebMessageInfo.prototype.pollAdditionalMetadata = null;
        WebMessageInfo.prototype.agentId = null;
        WebMessageInfo.prototype.statusAlreadyViewed = null;
        WebMessageInfo.prototype.messageSecret = null;
        WebMessageInfo.prototype.keepInChat = null;
        WebMessageInfo.prototype.originalSelfAuthorUserJidString = null;
        WebMessageInfo.prototype.revokeMessageTimestamp = null;
        WebMessageInfo.prototype.pinInChat = null;
        WebMessageInfo.prototype.premiumMessageInfo = null;
        WebMessageInfo.prototype.is1PBizBotMessage = null;
        WebMessageInfo.prototype.isGroupHistoryMessage = null;
        WebMessageInfo.prototype.botMessageInvokerJid = null;
        WebMessageInfo.prototype.commentMetadata = null;
        WebMessageInfo.prototype.eventResponses = $util.emptyArray;
        WebMessageInfo.prototype.reportingTokenInfo = null;
        WebMessageInfo.prototype.newsletterServerId = null;
        WebMessageInfo.prototype.eventAdditionalMetadata = null;
        WebMessageInfo.prototype.isMentionedInStatus = null;
        WebMessageInfo.prototype.statusMentions = $util.emptyArray;
        WebMessageInfo.prototype.targetMessageId = null;
        WebMessageInfo.prototype.messageAddOns = $util.emptyArray;
        WebMessageInfo.prototype.statusMentionMessageInfo = null;
        WebMessageInfo.prototype.isSupportAiMessage = null;
        WebMessageInfo.prototype.statusMentionSources = $util.emptyArray;
        WebMessageInfo.prototype.supportAiCitations = $util.emptyArray;
        WebMessageInfo.prototype.botTargetId = null;
        WebMessageInfo.prototype.groupHistoryIndividualMessageInfo = null;
        WebMessageInfo.prototype.groupHistoryBundleInfo = null;
        WebMessageInfo.prototype.interactiveMessageAdditionalMetadata = null;
        WebMessageInfo.prototype.quarantinedMessage = null;
        WebMessageInfo.prototype.nonJidMentions = null;
        WebMessageInfo.prototype.hsmTag = null;
        WebMessageInfo.prototype.ephemeralExpirationTimestamp = null;
        WebMessageInfo.prototype.scheduledMessageMetadata = null;
        WebMessageInfo.prototype.decisionId = null;
        WebMessageInfo.prototype.decisionSources = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_message", {
            get: $util.oneOfGetter($oneOfFields = ["message"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_messageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_status", {
            get: $util.oneOfGetter($oneOfFields = ["status"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_participant", {
            get: $util.oneOfGetter($oneOfFields = ["participant"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_messageC2STimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["messageC2STimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_ignore", {
            get: $util.oneOfGetter($oneOfFields = ["ignore"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_starred", {
            get: $util.oneOfGetter($oneOfFields = ["starred"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_broadcast", {
            get: $util.oneOfGetter($oneOfFields = ["broadcast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_pushName", {
            get: $util.oneOfGetter($oneOfFields = ["pushName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_mediaCiphertextSha256", {
            get: $util.oneOfGetter($oneOfFields = ["mediaCiphertextSha256"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_multicast", {
            get: $util.oneOfGetter($oneOfFields = ["multicast"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_urlText", {
            get: $util.oneOfGetter($oneOfFields = ["urlText"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_urlNumber", {
            get: $util.oneOfGetter($oneOfFields = ["urlNumber"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_messageStubType", {
            get: $util.oneOfGetter($oneOfFields = ["messageStubType"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_clearMedia", {
            get: $util.oneOfGetter($oneOfFields = ["clearMedia"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_duration", {
            get: $util.oneOfGetter($oneOfFields = ["duration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_paymentInfo", {
            get: $util.oneOfGetter($oneOfFields = ["paymentInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_finalLiveLocation", {
            get: $util.oneOfGetter($oneOfFields = ["finalLiveLocation"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_quotedPaymentInfo", {
            get: $util.oneOfGetter($oneOfFields = ["quotedPaymentInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_ephemeralStartTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralStartTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_ephemeralDuration", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralDuration"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_ephemeralOffToOn", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralOffToOn"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_ephemeralOutOfSync", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralOutOfSync"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_bizPrivacyStatus", {
            get: $util.oneOfGetter($oneOfFields = ["bizPrivacyStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_verifiedBizName", {
            get: $util.oneOfGetter($oneOfFields = ["verifiedBizName"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_mediaData", {
            get: $util.oneOfGetter($oneOfFields = ["mediaData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_photoChange", {
            get: $util.oneOfGetter($oneOfFields = ["photoChange"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_quotedStickerData", {
            get: $util.oneOfGetter($oneOfFields = ["quotedStickerData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_futureproofData", {
            get: $util.oneOfGetter($oneOfFields = ["futureproofData"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_statusPsa", {
            get: $util.oneOfGetter($oneOfFields = ["statusPsa"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_pollAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["pollAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_agentId", {
            get: $util.oneOfGetter($oneOfFields = ["agentId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_statusAlreadyViewed", {
            get: $util.oneOfGetter($oneOfFields = ["statusAlreadyViewed"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_messageSecret", {
            get: $util.oneOfGetter($oneOfFields = ["messageSecret"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_keepInChat", {
            get: $util.oneOfGetter($oneOfFields = ["keepInChat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_originalSelfAuthorUserJidString", {
            get: $util.oneOfGetter($oneOfFields = ["originalSelfAuthorUserJidString"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_revokeMessageTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["revokeMessageTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_pinInChat", {
            get: $util.oneOfGetter($oneOfFields = ["pinInChat"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_premiumMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["premiumMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_is1PBizBotMessage", {
            get: $util.oneOfGetter($oneOfFields = ["is1PBizBotMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_isGroupHistoryMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isGroupHistoryMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_botMessageInvokerJid", {
            get: $util.oneOfGetter($oneOfFields = ["botMessageInvokerJid"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_commentMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["commentMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_reportingTokenInfo", {
            get: $util.oneOfGetter($oneOfFields = ["reportingTokenInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_newsletterServerId", {
            get: $util.oneOfGetter($oneOfFields = ["newsletterServerId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_eventAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["eventAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_isMentionedInStatus", {
            get: $util.oneOfGetter($oneOfFields = ["isMentionedInStatus"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_targetMessageId", {
            get: $util.oneOfGetter($oneOfFields = ["targetMessageId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_statusMentionMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["statusMentionMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_isSupportAiMessage", {
            get: $util.oneOfGetter($oneOfFields = ["isSupportAiMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_botTargetId", {
            get: $util.oneOfGetter($oneOfFields = ["botTargetId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_groupHistoryIndividualMessageInfo", {
            get: $util.oneOfGetter($oneOfFields = ["groupHistoryIndividualMessageInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_groupHistoryBundleInfo", {
            get: $util.oneOfGetter($oneOfFields = ["groupHistoryBundleInfo"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_interactiveMessageAdditionalMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["interactiveMessageAdditionalMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_quarantinedMessage", {
            get: $util.oneOfGetter($oneOfFields = ["quarantinedMessage"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_nonJidMentions", {
            get: $util.oneOfGetter($oneOfFields = ["nonJidMentions"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_hsmTag", {
            get: $util.oneOfGetter($oneOfFields = ["hsmTag"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_ephemeralExpirationTimestamp", {
            get: $util.oneOfGetter($oneOfFields = ["ephemeralExpirationTimestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_scheduledMessageMetadata", {
            get: $util.oneOfGetter($oneOfFields = ["scheduledMessageMetadata"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfo.prototype, "_decisionId", {
            get: $util.oneOfGetter($oneOfFields = ["decisionId"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebMessageInfo.create = function(properties) {
            return new WebMessageInfo(properties);
        };

        WebMessageInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
            if (m.message != null && $Object.hasOwnProperty.call(m, "message"))
                $root.proto.Message.encode(m.message, w.uint32(18).fork(), q + 1).ldelim();
            if (m.messageTimestamp != null && $Object.hasOwnProperty.call(m, "messageTimestamp"))
                w.uint32(24).uint64(m.messageTimestamp);
            if (m.status != null && $Object.hasOwnProperty.call(m, "status"))
                w.uint32(32).int32(m.status);
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant"))
                w.uint32(42).string(m.participant);
            if (m.messageC2STimestamp != null && $Object.hasOwnProperty.call(m, "messageC2STimestamp"))
                w.uint32(48).uint64(m.messageC2STimestamp);
            if (m.ignore != null && $Object.hasOwnProperty.call(m, "ignore"))
                w.uint32(128).bool(m.ignore);
            if (m.starred != null && $Object.hasOwnProperty.call(m, "starred"))
                w.uint32(136).bool(m.starred);
            if (m.broadcast != null && $Object.hasOwnProperty.call(m, "broadcast"))
                w.uint32(144).bool(m.broadcast);
            if (m.pushName != null && $Object.hasOwnProperty.call(m, "pushName"))
                w.uint32(154).string(m.pushName);
            if (m.mediaCiphertextSha256 != null && $Object.hasOwnProperty.call(m, "mediaCiphertextSha256"))
                w.uint32(162).bytes(m.mediaCiphertextSha256);
            if (m.multicast != null && $Object.hasOwnProperty.call(m, "multicast"))
                w.uint32(168).bool(m.multicast);
            if (m.urlText != null && $Object.hasOwnProperty.call(m, "urlText"))
                w.uint32(176).bool(m.urlText);
            if (m.urlNumber != null && $Object.hasOwnProperty.call(m, "urlNumber"))
                w.uint32(184).bool(m.urlNumber);
            if (m.messageStubType != null && $Object.hasOwnProperty.call(m, "messageStubType"))
                w.uint32(192).int32(m.messageStubType);
            if (m.clearMedia != null && $Object.hasOwnProperty.call(m, "clearMedia"))
                w.uint32(200).bool(m.clearMedia);
            if (m.messageStubParameters != null && m.messageStubParameters.length) {
                for (var i = 0; i < m.messageStubParameters.length; ++i)
                    w.uint32(210).string(m.messageStubParameters[i]);
            }
            if (m.duration != null && $Object.hasOwnProperty.call(m, "duration"))
                w.uint32(216).uint32(m.duration);
            if (m.labels != null && m.labels.length) {
                for (var i = 0; i < m.labels.length; ++i)
                    w.uint32(226).string(m.labels[i]);
            }
            if (m.paymentInfo != null && $Object.hasOwnProperty.call(m, "paymentInfo"))
                $root.proto.PaymentInfo.encode(m.paymentInfo, w.uint32(234).fork(), q + 1).ldelim();
            if (m.finalLiveLocation != null && $Object.hasOwnProperty.call(m, "finalLiveLocation"))
                $root.proto.Message.LiveLocationMessage.encode(m.finalLiveLocation, w.uint32(242).fork(), q + 1).ldelim();
            if (m.quotedPaymentInfo != null && $Object.hasOwnProperty.call(m, "quotedPaymentInfo"))
                $root.proto.PaymentInfo.encode(m.quotedPaymentInfo, w.uint32(250).fork(), q + 1).ldelim();
            if (m.ephemeralStartTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralStartTimestamp"))
                w.uint32(256).uint64(m.ephemeralStartTimestamp);
            if (m.ephemeralDuration != null && $Object.hasOwnProperty.call(m, "ephemeralDuration"))
                w.uint32(264).uint32(m.ephemeralDuration);
            if (m.ephemeralOffToOn != null && $Object.hasOwnProperty.call(m, "ephemeralOffToOn"))
                w.uint32(272).bool(m.ephemeralOffToOn);
            if (m.ephemeralOutOfSync != null && $Object.hasOwnProperty.call(m, "ephemeralOutOfSync"))
                w.uint32(280).bool(m.ephemeralOutOfSync);
            if (m.bizPrivacyStatus != null && $Object.hasOwnProperty.call(m, "bizPrivacyStatus"))
                w.uint32(288).int32(m.bizPrivacyStatus);
            if (m.verifiedBizName != null && $Object.hasOwnProperty.call(m, "verifiedBizName"))
                w.uint32(298).string(m.verifiedBizName);
            if (m.mediaData != null && $Object.hasOwnProperty.call(m, "mediaData"))
                $root.proto.MediaData.encode(m.mediaData, w.uint32(306).fork(), q + 1).ldelim();
            if (m.photoChange != null && $Object.hasOwnProperty.call(m, "photoChange"))
                $root.proto.PhotoChange.encode(m.photoChange, w.uint32(314).fork(), q + 1).ldelim();
            if (m.userReceipt != null && m.userReceipt.length) {
                for (var i = 0; i < m.userReceipt.length; ++i)
                    $root.proto.UserReceipt.encode(m.userReceipt[i], w.uint32(322).fork(), q + 1).ldelim();
            }
            if (m.reactions != null && m.reactions.length) {
                for (var i = 0; i < m.reactions.length; ++i)
                    $root.proto.Reaction.encode(m.reactions[i], w.uint32(330).fork(), q + 1).ldelim();
            }
            if (m.quotedStickerData != null && $Object.hasOwnProperty.call(m, "quotedStickerData"))
                $root.proto.MediaData.encode(m.quotedStickerData, w.uint32(338).fork(), q + 1).ldelim();
            if (m.futureproofData != null && $Object.hasOwnProperty.call(m, "futureproofData"))
                w.uint32(346).bytes(m.futureproofData);
            if (m.statusPsa != null && $Object.hasOwnProperty.call(m, "statusPsa"))
                $root.proto.StatusPSA.encode(m.statusPsa, w.uint32(354).fork(), q + 1).ldelim();
            if (m.pollUpdates != null && m.pollUpdates.length) {
                for (var i = 0; i < m.pollUpdates.length; ++i)
                    $root.proto.PollUpdate.encode(m.pollUpdates[i], w.uint32(362).fork(), q + 1).ldelim();
            }
            if (m.pollAdditionalMetadata != null && $Object.hasOwnProperty.call(m, "pollAdditionalMetadata"))
                $root.proto.PollAdditionalMetadata.encode(m.pollAdditionalMetadata, w.uint32(370).fork(), q + 1).ldelim();
            if (m.agentId != null && $Object.hasOwnProperty.call(m, "agentId"))
                w.uint32(378).string(m.agentId);
            if (m.statusAlreadyViewed != null && $Object.hasOwnProperty.call(m, "statusAlreadyViewed"))
                w.uint32(384).bool(m.statusAlreadyViewed);
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret"))
                w.uint32(394).bytes(m.messageSecret);
            if (m.keepInChat != null && $Object.hasOwnProperty.call(m, "keepInChat"))
                $root.proto.KeepInChat.encode(m.keepInChat, w.uint32(402).fork(), q + 1).ldelim();
            if (m.originalSelfAuthorUserJidString != null && $Object.hasOwnProperty.call(m, "originalSelfAuthorUserJidString"))
                w.uint32(410).string(m.originalSelfAuthorUserJidString);
            if (m.revokeMessageTimestamp != null && $Object.hasOwnProperty.call(m, "revokeMessageTimestamp"))
                w.uint32(416).uint64(m.revokeMessageTimestamp);
            if (m.pinInChat != null && $Object.hasOwnProperty.call(m, "pinInChat"))
                $root.proto.PinInChat.encode(m.pinInChat, w.uint32(434).fork(), q + 1).ldelim();
            if (m.premiumMessageInfo != null && $Object.hasOwnProperty.call(m, "premiumMessageInfo"))
                $root.proto.PremiumMessageInfo.encode(m.premiumMessageInfo, w.uint32(442).fork(), q + 1).ldelim();
            if (m.is1PBizBotMessage != null && $Object.hasOwnProperty.call(m, "is1PBizBotMessage"))
                w.uint32(448).bool(m.is1PBizBotMessage);
            if (m.isGroupHistoryMessage != null && $Object.hasOwnProperty.call(m, "isGroupHistoryMessage"))
                w.uint32(456).bool(m.isGroupHistoryMessage);
            if (m.botMessageInvokerJid != null && $Object.hasOwnProperty.call(m, "botMessageInvokerJid"))
                w.uint32(466).string(m.botMessageInvokerJid);
            if (m.commentMetadata != null && $Object.hasOwnProperty.call(m, "commentMetadata"))
                $root.proto.CommentMetadata.encode(m.commentMetadata, w.uint32(474).fork(), q + 1).ldelim();
            if (m.eventResponses != null && m.eventResponses.length) {
                for (var i = 0; i < m.eventResponses.length; ++i)
                    $root.proto.EventResponse.encode(m.eventResponses[i], w.uint32(490).fork(), q + 1).ldelim();
            }
            if (m.reportingTokenInfo != null && $Object.hasOwnProperty.call(m, "reportingTokenInfo"))
                $root.proto.ReportingTokenInfo.encode(m.reportingTokenInfo, w.uint32(498).fork(), q + 1).ldelim();
            if (m.newsletterServerId != null && $Object.hasOwnProperty.call(m, "newsletterServerId"))
                w.uint32(504).uint64(m.newsletterServerId);
            if (m.eventAdditionalMetadata != null && $Object.hasOwnProperty.call(m, "eventAdditionalMetadata"))
                $root.proto.EventAdditionalMetadata.encode(m.eventAdditionalMetadata, w.uint32(514).fork(), q + 1).ldelim();
            if (m.isMentionedInStatus != null && $Object.hasOwnProperty.call(m, "isMentionedInStatus"))
                w.uint32(520).bool(m.isMentionedInStatus);
            if (m.statusMentions != null && m.statusMentions.length) {
                for (var i = 0; i < m.statusMentions.length; ++i)
                    w.uint32(530).string(m.statusMentions[i]);
            }
            if (m.targetMessageId != null && $Object.hasOwnProperty.call(m, "targetMessageId"))
                $root.proto.MessageKey.encode(m.targetMessageId, w.uint32(538).fork(), q + 1).ldelim();
            if (m.messageAddOns != null && m.messageAddOns.length) {
                for (var i = 0; i < m.messageAddOns.length; ++i)
                    $root.proto.MessageAddOn.encode(m.messageAddOns[i], w.uint32(546).fork(), q + 1).ldelim();
            }
            if (m.statusMentionMessageInfo != null && $Object.hasOwnProperty.call(m, "statusMentionMessageInfo"))
                $root.proto.StatusMentionMessage.encode(m.statusMentionMessageInfo, w.uint32(554).fork(), q + 1).ldelim();
            if (m.isSupportAiMessage != null && $Object.hasOwnProperty.call(m, "isSupportAiMessage"))
                w.uint32(560).bool(m.isSupportAiMessage);
            if (m.statusMentionSources != null && m.statusMentionSources.length) {
                for (var i = 0; i < m.statusMentionSources.length; ++i)
                    w.uint32(570).string(m.statusMentionSources[i]);
            }
            if (m.supportAiCitations != null && m.supportAiCitations.length) {
                for (var i = 0; i < m.supportAiCitations.length; ++i)
                    $root.proto.Citation.encode(m.supportAiCitations[i], w.uint32(578).fork(), q + 1).ldelim();
            }
            if (m.botTargetId != null && $Object.hasOwnProperty.call(m, "botTargetId"))
                w.uint32(586).string(m.botTargetId);
            if (m.groupHistoryIndividualMessageInfo != null && $Object.hasOwnProperty.call(m, "groupHistoryIndividualMessageInfo"))
                $root.proto.GroupHistoryIndividualMessageInfo.encode(m.groupHistoryIndividualMessageInfo, w.uint32(594).fork(), q + 1).ldelim();
            if (m.groupHistoryBundleInfo != null && $Object.hasOwnProperty.call(m, "groupHistoryBundleInfo"))
                $root.proto.GroupHistoryBundleInfo.encode(m.groupHistoryBundleInfo, w.uint32(602).fork(), q + 1).ldelim();
            if (m.interactiveMessageAdditionalMetadata != null && $Object.hasOwnProperty.call(m, "interactiveMessageAdditionalMetadata"))
                $root.proto.InteractiveMessageAdditionalMetadata.encode(m.interactiveMessageAdditionalMetadata, w.uint32(610).fork(), q + 1).ldelim();
            if (m.quarantinedMessage != null && $Object.hasOwnProperty.call(m, "quarantinedMessage"))
                $root.proto.QuarantinedMessage.encode(m.quarantinedMessage, w.uint32(618).fork(), q + 1).ldelim();
            if (m.nonJidMentions != null && $Object.hasOwnProperty.call(m, "nonJidMentions"))
                w.uint32(624).uint32(m.nonJidMentions);
            if (m.hsmTag != null && $Object.hasOwnProperty.call(m, "hsmTag"))
                w.uint32(634).string(m.hsmTag);
            if (m.ephemeralExpirationTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralExpirationTimestamp"))
                w.uint32(640).uint64(m.ephemeralExpirationTimestamp);
            if (m.scheduledMessageMetadata != null && $Object.hasOwnProperty.call(m, "scheduledMessageMetadata"))
                $root.proto.ScheduledMessageMetadata.encode(m.scheduledMessageMetadata, w.uint32(650).fork(), q + 1).ldelim();
            if (m.decisionId != null && $Object.hasOwnProperty.call(m, "decisionId"))
                w.uint32(658).string(m.decisionId);
            if (m.decisionSources != null && m.decisionSources.length) {
                for (var i = 0; i < m.decisionSources.length; ++i)
                    w.uint32(666).string(m.decisionSources[i]);
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        WebMessageInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.WebMessageInfo(), v;
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.message = $root.proto.Message.decode(r, r.uint32(), $undefined, q + 1, m.message);
                        m._message = "message";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.messageTimestamp = r.uint64();
                        m._messageTimestamp = "messageTimestamp";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.status = r.int32();
                        m._status = "status";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        m.participant = r.stringVerify();
                        m._participant = "participant";
                        continue;
                    }
                case 6: {
                        if (u !== 0)
                            break;
                        m.messageC2STimestamp = r.uint64();
                        m._messageC2STimestamp = "messageC2STimestamp";
                        continue;
                    }
                case 16: {
                        if (u !== 0)
                            break;
                        m.ignore = r.bool();
                        m._ignore = "ignore";
                        continue;
                    }
                case 17: {
                        if (u !== 0)
                            break;
                        m.starred = r.bool();
                        m._starred = "starred";
                        continue;
                    }
                case 18: {
                        if (u !== 0)
                            break;
                        m.broadcast = r.bool();
                        m._broadcast = "broadcast";
                        continue;
                    }
                case 19: {
                        if (u !== 2)
                            break;
                        m.pushName = r.stringVerify();
                        m._pushName = "pushName";
                        continue;
                    }
                case 20: {
                        if (u !== 2)
                            break;
                        m.mediaCiphertextSha256 = r.bytes();
                        m._mediaCiphertextSha256 = "mediaCiphertextSha256";
                        continue;
                    }
                case 21: {
                        if (u !== 0)
                            break;
                        m.multicast = r.bool();
                        m._multicast = "multicast";
                        continue;
                    }
                case 22: {
                        if (u !== 0)
                            break;
                        m.urlText = r.bool();
                        m._urlText = "urlText";
                        continue;
                    }
                case 23: {
                        if (u !== 0)
                            break;
                        m.urlNumber = r.bool();
                        m._urlNumber = "urlNumber";
                        continue;
                    }
                case 24: {
                        if (u !== 0)
                            break;
                        m.messageStubType = r.int32();
                        m._messageStubType = "messageStubType";
                        continue;
                    }
                case 25: {
                        if (u !== 0)
                            break;
                        m.clearMedia = r.bool();
                        m._clearMedia = "clearMedia";
                        continue;
                    }
                case 26: {
                        if (u !== 2)
                            break;
                        if (!(m.messageStubParameters && m.messageStubParameters.length))
                            m.messageStubParameters = [];
                        m.messageStubParameters.push(r.stringVerify());
                        continue;
                    }
                case 27: {
                        if (u !== 0)
                            break;
                        m.duration = r.uint32();
                        m._duration = "duration";
                        continue;
                    }
                case 28: {
                        if (u !== 2)
                            break;
                        if (!(m.labels && m.labels.length))
                            m.labels = [];
                        m.labels.push(r.stringVerify());
                        continue;
                    }
                case 29: {
                        if (u !== 2)
                            break;
                        m.paymentInfo = $root.proto.PaymentInfo.decode(r, r.uint32(), $undefined, q + 1, m.paymentInfo);
                        m._paymentInfo = "paymentInfo";
                        continue;
                    }
                case 30: {
                        if (u !== 2)
                            break;
                        m.finalLiveLocation = $root.proto.Message.LiveLocationMessage.decode(r, r.uint32(), $undefined, q + 1, m.finalLiveLocation);
                        m._finalLiveLocation = "finalLiveLocation";
                        continue;
                    }
                case 31: {
                        if (u !== 2)
                            break;
                        m.quotedPaymentInfo = $root.proto.PaymentInfo.decode(r, r.uint32(), $undefined, q + 1, m.quotedPaymentInfo);
                        m._quotedPaymentInfo = "quotedPaymentInfo";
                        continue;
                    }
                case 32: {
                        if (u !== 0)
                            break;
                        m.ephemeralStartTimestamp = r.uint64();
                        m._ephemeralStartTimestamp = "ephemeralStartTimestamp";
                        continue;
                    }
                case 33: {
                        if (u !== 0)
                            break;
                        m.ephemeralDuration = r.uint32();
                        m._ephemeralDuration = "ephemeralDuration";
                        continue;
                    }
                case 34: {
                        if (u !== 0)
                            break;
                        m.ephemeralOffToOn = r.bool();
                        m._ephemeralOffToOn = "ephemeralOffToOn";
                        continue;
                    }
                case 35: {
                        if (u !== 0)
                            break;
                        m.ephemeralOutOfSync = r.bool();
                        m._ephemeralOutOfSync = "ephemeralOutOfSync";
                        continue;
                    }
                case 36: {
                        if (u !== 0)
                            break;
                        m.bizPrivacyStatus = r.int32();
                        m._bizPrivacyStatus = "bizPrivacyStatus";
                        continue;
                    }
                case 37: {
                        if (u !== 2)
                            break;
                        m.verifiedBizName = r.stringVerify();
                        m._verifiedBizName = "verifiedBizName";
                        continue;
                    }
                case 38: {
                        if (u !== 2)
                            break;
                        m.mediaData = $root.proto.MediaData.decode(r, r.uint32(), $undefined, q + 1, m.mediaData);
                        m._mediaData = "mediaData";
                        continue;
                    }
                case 39: {
                        if (u !== 2)
                            break;
                        m.photoChange = $root.proto.PhotoChange.decode(r, r.uint32(), $undefined, q + 1, m.photoChange);
                        m._photoChange = "photoChange";
                        continue;
                    }
                case 40: {
                        if (u !== 2)
                            break;
                        if (!(m.userReceipt && m.userReceipt.length))
                            m.userReceipt = [];
                        m.userReceipt.push($root.proto.UserReceipt.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 41: {
                        if (u !== 2)
                            break;
                        if (!(m.reactions && m.reactions.length))
                            m.reactions = [];
                        m.reactions.push($root.proto.Reaction.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 42: {
                        if (u !== 2)
                            break;
                        m.quotedStickerData = $root.proto.MediaData.decode(r, r.uint32(), $undefined, q + 1, m.quotedStickerData);
                        m._quotedStickerData = "quotedStickerData";
                        continue;
                    }
                case 43: {
                        if (u !== 2)
                            break;
                        m.futureproofData = r.bytes();
                        m._futureproofData = "futureproofData";
                        continue;
                    }
                case 44: {
                        if (u !== 2)
                            break;
                        m.statusPsa = $root.proto.StatusPSA.decode(r, r.uint32(), $undefined, q + 1, m.statusPsa);
                        m._statusPsa = "statusPsa";
                        continue;
                    }
                case 45: {
                        if (u !== 2)
                            break;
                        if (!(m.pollUpdates && m.pollUpdates.length))
                            m.pollUpdates = [];
                        m.pollUpdates.push($root.proto.PollUpdate.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 46: {
                        if (u !== 2)
                            break;
                        m.pollAdditionalMetadata = $root.proto.PollAdditionalMetadata.decode(r, r.uint32(), $undefined, q + 1, m.pollAdditionalMetadata);
                        m._pollAdditionalMetadata = "pollAdditionalMetadata";
                        continue;
                    }
                case 47: {
                        if (u !== 2)
                            break;
                        m.agentId = r.stringVerify();
                        m._agentId = "agentId";
                        continue;
                    }
                case 48: {
                        if (u !== 0)
                            break;
                        m.statusAlreadyViewed = r.bool();
                        m._statusAlreadyViewed = "statusAlreadyViewed";
                        continue;
                    }
                case 49: {
                        if (u !== 2)
                            break;
                        m.messageSecret = r.bytes();
                        m._messageSecret = "messageSecret";
                        continue;
                    }
                case 50: {
                        if (u !== 2)
                            break;
                        m.keepInChat = $root.proto.KeepInChat.decode(r, r.uint32(), $undefined, q + 1, m.keepInChat);
                        m._keepInChat = "keepInChat";
                        continue;
                    }
                case 51: {
                        if (u !== 2)
                            break;
                        m.originalSelfAuthorUserJidString = r.stringVerify();
                        m._originalSelfAuthorUserJidString = "originalSelfAuthorUserJidString";
                        continue;
                    }
                case 52: {
                        if (u !== 0)
                            break;
                        m.revokeMessageTimestamp = r.uint64();
                        m._revokeMessageTimestamp = "revokeMessageTimestamp";
                        continue;
                    }
                case 54: {
                        if (u !== 2)
                            break;
                        m.pinInChat = $root.proto.PinInChat.decode(r, r.uint32(), $undefined, q + 1, m.pinInChat);
                        m._pinInChat = "pinInChat";
                        continue;
                    }
                case 55: {
                        if (u !== 2)
                            break;
                        m.premiumMessageInfo = $root.proto.PremiumMessageInfo.decode(r, r.uint32(), $undefined, q + 1, m.premiumMessageInfo);
                        m._premiumMessageInfo = "premiumMessageInfo";
                        continue;
                    }
                case 56: {
                        if (u !== 0)
                            break;
                        m.is1PBizBotMessage = r.bool();
                        m._is1PBizBotMessage = "is1PBizBotMessage";
                        continue;
                    }
                case 57: {
                        if (u !== 0)
                            break;
                        m.isGroupHistoryMessage = r.bool();
                        m._isGroupHistoryMessage = "isGroupHistoryMessage";
                        continue;
                    }
                case 58: {
                        if (u !== 2)
                            break;
                        m.botMessageInvokerJid = r.stringVerify();
                        m._botMessageInvokerJid = "botMessageInvokerJid";
                        continue;
                    }
                case 59: {
                        if (u !== 2)
                            break;
                        m.commentMetadata = $root.proto.CommentMetadata.decode(r, r.uint32(), $undefined, q + 1, m.commentMetadata);
                        m._commentMetadata = "commentMetadata";
                        continue;
                    }
                case 61: {
                        if (u !== 2)
                            break;
                        if (!(m.eventResponses && m.eventResponses.length))
                            m.eventResponses = [];
                        m.eventResponses.push($root.proto.EventResponse.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 62: {
                        if (u !== 2)
                            break;
                        m.reportingTokenInfo = $root.proto.ReportingTokenInfo.decode(r, r.uint32(), $undefined, q + 1, m.reportingTokenInfo);
                        m._reportingTokenInfo = "reportingTokenInfo";
                        continue;
                    }
                case 63: {
                        if (u !== 0)
                            break;
                        m.newsletterServerId = r.uint64();
                        m._newsletterServerId = "newsletterServerId";
                        continue;
                    }
                case 64: {
                        if (u !== 2)
                            break;
                        m.eventAdditionalMetadata = $root.proto.EventAdditionalMetadata.decode(r, r.uint32(), $undefined, q + 1, m.eventAdditionalMetadata);
                        m._eventAdditionalMetadata = "eventAdditionalMetadata";
                        continue;
                    }
                case 65: {
                        if (u !== 0)
                            break;
                        m.isMentionedInStatus = r.bool();
                        m._isMentionedInStatus = "isMentionedInStatus";
                        continue;
                    }
                case 66: {
                        if (u !== 2)
                            break;
                        if (!(m.statusMentions && m.statusMentions.length))
                            m.statusMentions = [];
                        m.statusMentions.push(r.stringVerify());
                        continue;
                    }
                case 67: {
                        if (u !== 2)
                            break;
                        m.targetMessageId = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.targetMessageId);
                        m._targetMessageId = "targetMessageId";
                        continue;
                    }
                case 68: {
                        if (u !== 2)
                            break;
                        if (!(m.messageAddOns && m.messageAddOns.length))
                            m.messageAddOns = [];
                        m.messageAddOns.push($root.proto.MessageAddOn.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 69: {
                        if (u !== 2)
                            break;
                        m.statusMentionMessageInfo = $root.proto.StatusMentionMessage.decode(r, r.uint32(), $undefined, q + 1, m.statusMentionMessageInfo);
                        m._statusMentionMessageInfo = "statusMentionMessageInfo";
                        continue;
                    }
                case 70: {
                        if (u !== 0)
                            break;
                        m.isSupportAiMessage = r.bool();
                        m._isSupportAiMessage = "isSupportAiMessage";
                        continue;
                    }
                case 71: {
                        if (u !== 2)
                            break;
                        if (!(m.statusMentionSources && m.statusMentionSources.length))
                            m.statusMentionSources = [];
                        m.statusMentionSources.push(r.stringVerify());
                        continue;
                    }
                case 72: {
                        if (u !== 2)
                            break;
                        if (!(m.supportAiCitations && m.supportAiCitations.length))
                            m.supportAiCitations = [];
                        m.supportAiCitations.push($root.proto.Citation.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                case 73: {
                        if (u !== 2)
                            break;
                        m.botTargetId = r.stringVerify();
                        m._botTargetId = "botTargetId";
                        continue;
                    }
                case 74: {
                        if (u !== 2)
                            break;
                        m.groupHistoryIndividualMessageInfo = $root.proto.GroupHistoryIndividualMessageInfo.decode(r, r.uint32(), $undefined, q + 1, m.groupHistoryIndividualMessageInfo);
                        m._groupHistoryIndividualMessageInfo = "groupHistoryIndividualMessageInfo";
                        continue;
                    }
                case 75: {
                        if (u !== 2)
                            break;
                        m.groupHistoryBundleInfo = $root.proto.GroupHistoryBundleInfo.decode(r, r.uint32(), $undefined, q + 1, m.groupHistoryBundleInfo);
                        m._groupHistoryBundleInfo = "groupHistoryBundleInfo";
                        continue;
                    }
                case 76: {
                        if (u !== 2)
                            break;
                        m.interactiveMessageAdditionalMetadata = $root.proto.InteractiveMessageAdditionalMetadata.decode(r, r.uint32(), $undefined, q + 1, m.interactiveMessageAdditionalMetadata);
                        m._interactiveMessageAdditionalMetadata = "interactiveMessageAdditionalMetadata";
                        continue;
                    }
                case 77: {
                        if (u !== 2)
                            break;
                        m.quarantinedMessage = $root.proto.QuarantinedMessage.decode(r, r.uint32(), $undefined, q + 1, m.quarantinedMessage);
                        m._quarantinedMessage = "quarantinedMessage";
                        continue;
                    }
                case 78: {
                        if (u !== 0)
                            break;
                        m.nonJidMentions = r.uint32();
                        m._nonJidMentions = "nonJidMentions";
                        continue;
                    }
                case 79: {
                        if (u !== 2)
                            break;
                        m.hsmTag = r.stringVerify();
                        m._hsmTag = "hsmTag";
                        continue;
                    }
                case 80: {
                        if (u !== 0)
                            break;
                        m.ephemeralExpirationTimestamp = r.uint64();
                        m._ephemeralExpirationTimestamp = "ephemeralExpirationTimestamp";
                        continue;
                    }
                case 81: {
                        if (u !== 2)
                            break;
                        m.scheduledMessageMetadata = $root.proto.ScheduledMessageMetadata.decode(r, r.uint32(), $undefined, q + 1, m.scheduledMessageMetadata);
                        m._scheduledMessageMetadata = "scheduledMessageMetadata";
                        continue;
                    }
                case 82: {
                        if (u !== 2)
                            break;
                        m.decisionId = r.stringVerify();
                        m._decisionId = "decisionId";
                        continue;
                    }
                case 83: {
                        if (u !== 2)
                            break;
                        if (!(m.decisionSources && m.decisionSources.length))
                            m.decisionSources = [];
                        m.decisionSources.push(r.stringVerify());
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            if (!$Object.hasOwnProperty.call(m, "key"))
                throw $util.ProtocolError("missing required 'key'", { instance: m });
            return m;
        };

        WebMessageInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.WebMessageInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.WebMessageInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.WebMessageInfo();
            if (d.key != null) {
                if (!$util.isObject(d.key))
                    throw $TypeError(".proto.WebMessageInfo.key: object expected");
                m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
            }
            if (d.message != null) {
                if (!$util.isObject(d.message))
                    throw $TypeError(".proto.WebMessageInfo.message: object expected");
                m.message = $root.proto.Message.fromObject(d.message, q + 1);
            }
            if (d.messageTimestamp != null) {
                if ($util.Long)
                    m.messageTimestamp = $util.Long.fromValue(d.messageTimestamp, true);
                else if (typeof d.messageTimestamp === "string")
                    m.messageTimestamp = $parseInt(d.messageTimestamp, 10);
                else if (typeof d.messageTimestamp === "number")
                    m.messageTimestamp = d.messageTimestamp;
                else if (typeof d.messageTimestamp === "object")
                    m.messageTimestamp = new $util.LongBits(d.messageTimestamp.low >>> 0, d.messageTimestamp.high >>> 0).toNumber(true);
            }
            switch (d.status) {
            case "ERROR":
            case 0:
                m.status = 0;
                break;
            case "PENDING":
            case 1:
                m.status = 1;
                break;
            case "SERVER_ACK":
            case 2:
                m.status = 2;
                break;
            case "DELIVERY_ACK":
            case 3:
                m.status = 3;
                break;
            case "READ":
            case 4:
                m.status = 4;
                break;
            case "PLAYED":
            case 5:
                m.status = 5;
                break;
            default:
                if (typeof d.status === "number" && (d.status | 0) === d.status)
                    m.status = d.status;
            }
            if (d.participant != null) {
                m.participant = $String(d.participant);
            }
            if (d.messageC2STimestamp != null) {
                if ($util.Long)
                    m.messageC2STimestamp = $util.Long.fromValue(d.messageC2STimestamp, true);
                else if (typeof d.messageC2STimestamp === "string")
                    m.messageC2STimestamp = $parseInt(d.messageC2STimestamp, 10);
                else if (typeof d.messageC2STimestamp === "number")
                    m.messageC2STimestamp = d.messageC2STimestamp;
                else if (typeof d.messageC2STimestamp === "object")
                    m.messageC2STimestamp = new $util.LongBits(d.messageC2STimestamp.low >>> 0, d.messageC2STimestamp.high >>> 0).toNumber(true);
            }
            if (d.ignore != null) {
                m.ignore = $Boolean(d.ignore);
            }
            if (d.starred != null) {
                m.starred = $Boolean(d.starred);
            }
            if (d.broadcast != null) {
                m.broadcast = $Boolean(d.broadcast);
            }
            if (d.pushName != null) {
                m.pushName = $String(d.pushName);
            }
            if (d.mediaCiphertextSha256 != null) {
                if (typeof d.mediaCiphertextSha256 === "string")
                    $util.base64.decode(d.mediaCiphertextSha256, m.mediaCiphertextSha256 = $util.newBuffer($util.base64.length(d.mediaCiphertextSha256)), 0);
                else if (d.mediaCiphertextSha256.length >= 0)
                    m.mediaCiphertextSha256 = d.mediaCiphertextSha256;
            }
            if (d.multicast != null) {
                m.multicast = $Boolean(d.multicast);
            }
            if (d.urlText != null) {
                m.urlText = $Boolean(d.urlText);
            }
            if (d.urlNumber != null) {
                m.urlNumber = $Boolean(d.urlNumber);
            }
            switch (d.messageStubType) {
            case "UNKNOWN":
            case 0:
                m.messageStubType = 0;
                break;
            case "REVOKE":
            case 1:
                m.messageStubType = 1;
                break;
            case "CIPHERTEXT":
            case 2:
                m.messageStubType = 2;
                break;
            case "FUTUREPROOF":
            case 3:
                m.messageStubType = 3;
                break;
            case "NON_VERIFIED_TRANSITION":
            case 4:
                m.messageStubType = 4;
                break;
            case "UNVERIFIED_TRANSITION":
            case 5:
                m.messageStubType = 5;
                break;
            case "VERIFIED_TRANSITION":
            case 6:
                m.messageStubType = 6;
                break;
            case "VERIFIED_LOW_UNKNOWN":
            case 7:
                m.messageStubType = 7;
                break;
            case "VERIFIED_HIGH":
            case 8:
                m.messageStubType = 8;
                break;
            case "VERIFIED_INITIAL_UNKNOWN":
            case 9:
                m.messageStubType = 9;
                break;
            case "VERIFIED_INITIAL_LOW":
            case 10:
                m.messageStubType = 10;
                break;
            case "VERIFIED_INITIAL_HIGH":
            case 11:
                m.messageStubType = 11;
                break;
            case "VERIFIED_TRANSITION_ANY_TO_NONE":
            case 12:
                m.messageStubType = 12;
                break;
            case "VERIFIED_TRANSITION_ANY_TO_HIGH":
            case 13:
                m.messageStubType = 13;
                break;
            case "VERIFIED_TRANSITION_HIGH_TO_LOW":
            case 14:
                m.messageStubType = 14;
                break;
            case "VERIFIED_TRANSITION_HIGH_TO_UNKNOWN":
            case 15:
                m.messageStubType = 15;
                break;
            case "VERIFIED_TRANSITION_UNKNOWN_TO_LOW":
            case 16:
                m.messageStubType = 16;
                break;
            case "VERIFIED_TRANSITION_LOW_TO_UNKNOWN":
            case 17:
                m.messageStubType = 17;
                break;
            case "VERIFIED_TRANSITION_NONE_TO_LOW":
            case 18:
                m.messageStubType = 18;
                break;
            case "VERIFIED_TRANSITION_NONE_TO_UNKNOWN":
            case 19:
                m.messageStubType = 19;
                break;
            case "GROUP_CREATE":
            case 20:
                m.messageStubType = 20;
                break;
            case "GROUP_CHANGE_SUBJECT":
            case 21:
                m.messageStubType = 21;
                break;
            case "GROUP_CHANGE_ICON":
            case 22:
                m.messageStubType = 22;
                break;
            case "GROUP_CHANGE_INVITE_LINK":
            case 23:
                m.messageStubType = 23;
                break;
            case "GROUP_CHANGE_DESCRIPTION":
            case 24:
                m.messageStubType = 24;
                break;
            case "GROUP_CHANGE_RESTRICT":
            case 25:
                m.messageStubType = 25;
                break;
            case "GROUP_CHANGE_ANNOUNCE":
            case 26:
                m.messageStubType = 26;
                break;
            case "GROUP_PARTICIPANT_ADD":
            case 27:
                m.messageStubType = 27;
                break;
            case "GROUP_PARTICIPANT_REMOVE":
            case 28:
                m.messageStubType = 28;
                break;
            case "GROUP_PARTICIPANT_PROMOTE":
            case 29:
                m.messageStubType = 29;
                break;
            case "GROUP_PARTICIPANT_DEMOTE":
            case 30:
                m.messageStubType = 30;
                break;
            case "GROUP_PARTICIPANT_INVITE":
            case 31:
                m.messageStubType = 31;
                break;
            case "GROUP_PARTICIPANT_LEAVE":
            case 32:
                m.messageStubType = 32;
                break;
            case "GROUP_PARTICIPANT_CHANGE_NUMBER":
            case 33:
                m.messageStubType = 33;
                break;
            case "BROADCAST_CREATE":
            case 34:
                m.messageStubType = 34;
                break;
            case "BROADCAST_ADD":
            case 35:
                m.messageStubType = 35;
                break;
            case "BROADCAST_REMOVE":
            case 36:
                m.messageStubType = 36;
                break;
            case "GENERIC_NOTIFICATION":
            case 37:
                m.messageStubType = 37;
                break;
            case "E2E_IDENTITY_CHANGED":
            case 38:
                m.messageStubType = 38;
                break;
            case "E2E_ENCRYPTED":
            case 39:
                m.messageStubType = 39;
                break;
            case "CALL_MISSED_VOICE":
            case 40:
                m.messageStubType = 40;
                break;
            case "CALL_MISSED_VIDEO":
            case 41:
                m.messageStubType = 41;
                break;
            case "INDIVIDUAL_CHANGE_NUMBER":
            case 42:
                m.messageStubType = 42;
                break;
            case "GROUP_DELETE":
            case 43:
                m.messageStubType = 43;
                break;
            case "GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE":
            case 44:
                m.messageStubType = 44;
                break;
            case "CALL_MISSED_GROUP_VOICE":
            case 45:
                m.messageStubType = 45;
                break;
            case "CALL_MISSED_GROUP_VIDEO":
            case 46:
                m.messageStubType = 46;
                break;
            case "PAYMENT_CIPHERTEXT":
            case 47:
                m.messageStubType = 47;
                break;
            case "PAYMENT_FUTUREPROOF":
            case 48:
                m.messageStubType = 48;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED":
            case 49:
                m.messageStubType = 49;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED":
            case 50:
                m.messageStubType = 50;
                break;
            case "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED":
            case 51:
                m.messageStubType = 51;
                break;
            case "PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP":
            case 52:
                m.messageStubType = 52;
                break;
            case "PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP":
            case 53:
                m.messageStubType = 53;
                break;
            case "PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER":
            case 54:
                m.messageStubType = 54;
                break;
            case "PAYMENT_ACTION_SEND_PAYMENT_REMINDER":
            case 55:
                m.messageStubType = 55;
                break;
            case "PAYMENT_ACTION_SEND_PAYMENT_INVITATION":
            case 56:
                m.messageStubType = 56;
                break;
            case "PAYMENT_ACTION_REQUEST_DECLINED":
            case 57:
                m.messageStubType = 57;
                break;
            case "PAYMENT_ACTION_REQUEST_EXPIRED":
            case 58:
                m.messageStubType = 58;
                break;
            case "PAYMENT_ACTION_REQUEST_CANCELLED":
            case 59:
                m.messageStubType = 59;
                break;
            case "BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM":
            case 60:
                m.messageStubType = 60;
                break;
            case "BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP":
            case 61:
                m.messageStubType = 61;
                break;
            case "BIZ_INTRO_TOP":
            case 62:
                m.messageStubType = 62;
                break;
            case "BIZ_INTRO_BOTTOM":
            case 63:
                m.messageStubType = 63;
                break;
            case "BIZ_NAME_CHANGE":
            case 64:
                m.messageStubType = 64;
                break;
            case "BIZ_MOVE_TO_CONSUMER_APP":
            case 65:
                m.messageStubType = 65;
                break;
            case "BIZ_TWO_TIER_MIGRATION_TOP":
            case 66:
                m.messageStubType = 66;
                break;
            case "BIZ_TWO_TIER_MIGRATION_BOTTOM":
            case 67:
                m.messageStubType = 67;
                break;
            case "OVERSIZED":
            case 68:
                m.messageStubType = 68;
                break;
            case "GROUP_CHANGE_NO_FREQUENTLY_FORWARDED":
            case 69:
                m.messageStubType = 69;
                break;
            case "GROUP_V4_ADD_INVITE_SENT":
            case 70:
                m.messageStubType = 70;
                break;
            case "GROUP_PARTICIPANT_ADD_REQUEST_JOIN":
            case 71:
                m.messageStubType = 71;
                break;
            case "CHANGE_EPHEMERAL_SETTING":
            case 72:
                m.messageStubType = 72;
                break;
            case "E2E_DEVICE_CHANGED":
            case 73:
                m.messageStubType = 73;
                break;
            case "VIEWED_ONCE":
            case 74:
                m.messageStubType = 74;
                break;
            case "E2E_ENCRYPTED_NOW":
            case 75:
                m.messageStubType = 75;
                break;
            case "BLUE_MSG_BSP_FB_TO_BSP_PREMISE":
            case 76:
                m.messageStubType = 76;
                break;
            case "BLUE_MSG_BSP_FB_TO_SELF_FB":
            case 77:
                m.messageStubType = 77;
                break;
            case "BLUE_MSG_BSP_FB_TO_SELF_PREMISE":
            case 78:
                m.messageStubType = 78;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED":
            case 79:
                m.messageStubType = 79;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 80:
                m.messageStubType = 80;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED":
            case 81:
                m.messageStubType = 81;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 82:
                m.messageStubType = 82;
                break;
            case "BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE":
            case 83:
                m.messageStubType = 83;
                break;
            case "BLUE_MSG_BSP_PREMISE_UNVERIFIED":
            case 84:
                m.messageStubType = 84;
                break;
            case "BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 85:
                m.messageStubType = 85;
                break;
            case "BLUE_MSG_BSP_PREMISE_VERIFIED":
            case 86:
                m.messageStubType = 86;
                break;
            case "BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 87:
                m.messageStubType = 87;
                break;
            case "BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED":
            case 88:
                m.messageStubType = 88;
                break;
            case "BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED":
            case 89:
                m.messageStubType = 89;
                break;
            case "BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED":
            case 90:
                m.messageStubType = 90;
                break;
            case "BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED":
            case 91:
                m.messageStubType = 91;
                break;
            case "BLUE_MSG_SELF_FB_TO_BSP_PREMISE":
            case 92:
                m.messageStubType = 92;
                break;
            case "BLUE_MSG_SELF_FB_TO_SELF_PREMISE":
            case 93:
                m.messageStubType = 93;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED":
            case 94:
                m.messageStubType = 94;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED":
            case 95:
                m.messageStubType = 95;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED":
            case 96:
                m.messageStubType = 96;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED":
            case 97:
                m.messageStubType = 97;
                break;
            case "BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE":
            case 98:
                m.messageStubType = 98;
                break;
            case "BLUE_MSG_SELF_PREMISE_UNVERIFIED":
            case 99:
                m.messageStubType = 99;
                break;
            case "BLUE_MSG_SELF_PREMISE_VERIFIED":
            case 100:
                m.messageStubType = 100;
                break;
            case "BLUE_MSG_TO_BSP_FB":
            case 101:
                m.messageStubType = 101;
                break;
            case "BLUE_MSG_TO_CONSUMER":
            case 102:
                m.messageStubType = 102;
                break;
            case "BLUE_MSG_TO_SELF_FB":
            case 103:
                m.messageStubType = 103;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED":
            case 104:
                m.messageStubType = 104;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 105:
                m.messageStubType = 105;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED":
            case 106:
                m.messageStubType = 106;
                break;
            case "BLUE_MSG_UNVERIFIED_TO_VERIFIED":
            case 107:
                m.messageStubType = 107;
                break;
            case "BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED":
            case 108:
                m.messageStubType = 108;
                break;
            case "BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 109:
                m.messageStubType = 109;
                break;
            case "BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED":
            case 110:
                m.messageStubType = 110;
                break;
            case "BLUE_MSG_VERIFIED_TO_UNVERIFIED":
            case 111:
                m.messageStubType = 111;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 112:
                m.messageStubType = 112;
                break;
            case "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED":
            case 113:
                m.messageStubType = 113;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 114:
                m.messageStubType = 114;
                break;
            case "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED":
            case 115:
                m.messageStubType = 115;
                break;
            case "BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED":
            case 116:
                m.messageStubType = 116;
                break;
            case "BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED":
            case 117:
                m.messageStubType = 117;
                break;
            case "E2E_IDENTITY_UNAVAILABLE":
            case 118:
                m.messageStubType = 118;
                break;
            case "GROUP_CREATING":
            case 119:
                m.messageStubType = 119;
                break;
            case "GROUP_CREATE_FAILED":
            case 120:
                m.messageStubType = 120;
                break;
            case "GROUP_BOUNCED":
            case 121:
                m.messageStubType = 121;
                break;
            case "BLOCK_CONTACT":
            case 122:
                m.messageStubType = 122;
                break;
            case "EPHEMERAL_SETTING_NOT_APPLIED":
            case 123:
                m.messageStubType = 123;
                break;
            case "SYNC_FAILED":
            case 124:
                m.messageStubType = 124;
                break;
            case "SYNCING":
            case 125:
                m.messageStubType = 125;
                break;
            case "BIZ_PRIVACY_MODE_INIT_FB":
            case 126:
                m.messageStubType = 126;
                break;
            case "BIZ_PRIVACY_MODE_INIT_BSP":
            case 127:
                m.messageStubType = 127;
                break;
            case "BIZ_PRIVACY_MODE_TO_FB":
            case 128:
                m.messageStubType = 128;
                break;
            case "BIZ_PRIVACY_MODE_TO_BSP":
            case 129:
                m.messageStubType = 129;
                break;
            case "DISAPPEARING_MODE":
            case 130:
                m.messageStubType = 130;
                break;
            case "E2E_DEVICE_FETCH_FAILED":
            case 131:
                m.messageStubType = 131;
                break;
            case "ADMIN_REVOKE":
            case 132:
                m.messageStubType = 132;
                break;
            case "GROUP_INVITE_LINK_GROWTH_LOCKED":
            case 133:
                m.messageStubType = 133;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP":
            case 134:
                m.messageStubType = 134;
                break;
            case "COMMUNITY_LINK_SIBLING_GROUP":
            case 135:
                m.messageStubType = 135;
                break;
            case "COMMUNITY_LINK_SUB_GROUP":
            case 136:
                m.messageStubType = 136;
                break;
            case "COMMUNITY_UNLINK_PARENT_GROUP":
            case 137:
                m.messageStubType = 137;
                break;
            case "COMMUNITY_UNLINK_SIBLING_GROUP":
            case 138:
                m.messageStubType = 138;
                break;
            case "COMMUNITY_UNLINK_SUB_GROUP":
            case 139:
                m.messageStubType = 139;
                break;
            case "GROUP_PARTICIPANT_ACCEPT":
            case 140:
                m.messageStubType = 140;
                break;
            case "GROUP_PARTICIPANT_LINKED_GROUP_JOIN":
            case 141:
                m.messageStubType = 141;
                break;
            case "COMMUNITY_CREATE":
            case 142:
                m.messageStubType = 142;
                break;
            case "EPHEMERAL_KEEP_IN_CHAT":
            case 143:
                m.messageStubType = 143;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST":
            case 144:
                m.messageStubType = 144;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE":
            case 145:
                m.messageStubType = 145;
                break;
            case "INTEGRITY_UNLINK_PARENT_GROUP":
            case 146:
                m.messageStubType = 146;
                break;
            case "COMMUNITY_PARTICIPANT_PROMOTE":
            case 147:
                m.messageStubType = 147;
                break;
            case "COMMUNITY_PARTICIPANT_DEMOTE":
            case 148:
                m.messageStubType = 148;
                break;
            case "COMMUNITY_PARENT_GROUP_DELETED":
            case 149:
                m.messageStubType = 149;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL":
            case 150:
                m.messageStubType = 150;
                break;
            case "GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP":
            case 151:
                m.messageStubType = 151;
                break;
            case "MASKED_THREAD_CREATED":
            case 152:
                m.messageStubType = 152;
                break;
            case "MASKED_THREAD_UNMASKED":
            case 153:
                m.messageStubType = 153;
                break;
            case "BIZ_CHAT_ASSIGNMENT":
            case 154:
                m.messageStubType = 154;
                break;
            case "CHAT_PSA":
            case 155:
                m.messageStubType = 155;
                break;
            case "CHAT_POLL_CREATION_MESSAGE":
            case 156:
                m.messageStubType = 156;
                break;
            case "CAG_MASKED_THREAD_CREATED":
            case 157:
                m.messageStubType = 157;
                break;
            case "COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED":
            case 158:
                m.messageStubType = 158;
                break;
            case "CAG_INVITE_AUTO_ADD":
            case 159:
                m.messageStubType = 159;
                break;
            case "BIZ_CHAT_ASSIGNMENT_UNASSIGN":
            case 160:
                m.messageStubType = 160;
                break;
            case "CAG_INVITE_AUTO_JOINED":
            case 161:
                m.messageStubType = 161;
                break;
            case "SCHEDULED_CALL_START_MESSAGE":
            case 162:
                m.messageStubType = 162;
                break;
            case "COMMUNITY_INVITE_RICH":
            case 163:
                m.messageStubType = 163;
                break;
            case "COMMUNITY_INVITE_AUTO_ADD_RICH":
            case 164:
                m.messageStubType = 164;
                break;
            case "SUB_GROUP_INVITE_RICH":
            case 165:
                m.messageStubType = 165;
                break;
            case "SUB_GROUP_PARTICIPANT_ADD_RICH":
            case 166:
                m.messageStubType = 166;
                break;
            case "COMMUNITY_LINK_PARENT_GROUP_RICH":
            case 167:
                m.messageStubType = 167;
                break;
            case "COMMUNITY_PARTICIPANT_ADD_RICH":
            case 168:
                m.messageStubType = 168;
                break;
            case "SILENCED_UNKNOWN_CALLER_AUDIO":
            case 169:
                m.messageStubType = 169;
                break;
            case "SILENCED_UNKNOWN_CALLER_VIDEO":
            case 170:
                m.messageStubType = 170;
                break;
            case "GROUP_MEMBER_ADD_MODE":
            case 171:
                m.messageStubType = 171;
                break;
            case "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD":
            case 172:
                m.messageStubType = 172;
                break;
            case "COMMUNITY_CHANGE_DESCRIPTION":
            case 173:
                m.messageStubType = 173;
                break;
            case "SENDER_INVITE":
            case 174:
                m.messageStubType = 174;
                break;
            case "RECEIVER_INVITE":
            case 175:
                m.messageStubType = 175;
                break;
            case "COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS":
            case 176:
                m.messageStubType = 176;
                break;
            case "PINNED_MESSAGE_IN_CHAT":
            case 177:
                m.messageStubType = 177;
                break;
            case "PAYMENT_INVITE_SETUP_INVITER":
            case 178:
                m.messageStubType = 178;
                break;
            case "PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY":
            case 179:
                m.messageStubType = 179;
                break;
            case "PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE":
            case 180:
                m.messageStubType = 180;
                break;
            case "LINKED_GROUP_CALL_START":
            case 181:
                m.messageStubType = 181;
                break;
            case "REPORT_TO_ADMIN_ENABLED_STATUS":
            case 182:
                m.messageStubType = 182;
                break;
            case "EMPTY_SUBGROUP_CREATE":
            case 183:
                m.messageStubType = 183;
                break;
            case "SCHEDULED_CALL_CANCEL":
            case 184:
                m.messageStubType = 184;
                break;
            case "SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH":
            case 185:
                m.messageStubType = 185;
                break;
            case "GROUP_CHANGE_RECENT_HISTORY_SHARING":
            case 186:
                m.messageStubType = 186;
                break;
            case "PAID_MESSAGE_SERVER_CAMPAIGN_ID":
            case 187:
                m.messageStubType = 187;
                break;
            case "GENERAL_CHAT_CREATE":
            case 188:
                m.messageStubType = 188;
                break;
            case "GENERAL_CHAT_ADD":
            case 189:
                m.messageStubType = 189;
                break;
            case "GENERAL_CHAT_AUTO_ADD_DISABLED":
            case 190:
                m.messageStubType = 190;
                break;
            case "SUGGESTED_SUBGROUP_ANNOUNCE":
            case 191:
                m.messageStubType = 191;
                break;
            case "BIZ_BOT_1P_MESSAGING_ENABLED":
            case 192:
                m.messageStubType = 192;
                break;
            case "CHANGE_USERNAME":
            case 193:
                m.messageStubType = 193;
                break;
            case "BIZ_COEX_PRIVACY_INIT_SELF":
            case 194:
                m.messageStubType = 194;
                break;
            case "BIZ_COEX_PRIVACY_TRANSITION_SELF":
            case 195:
                m.messageStubType = 195;
                break;
            case "SUPPORT_AI_EDUCATION":
            case 196:
                m.messageStubType = 196;
                break;
            case "BIZ_BOT_3P_MESSAGING_ENABLED":
            case 197:
                m.messageStubType = 197;
                break;
            case "REMINDER_SETUP_MESSAGE":
            case 198:
                m.messageStubType = 198;
                break;
            case "REMINDER_SENT_MESSAGE":
            case 199:
                m.messageStubType = 199;
                break;
            case "REMINDER_CANCEL_MESSAGE":
            case 200:
                m.messageStubType = 200;
                break;
            case "BIZ_COEX_PRIVACY_INIT":
            case 201:
                m.messageStubType = 201;
                break;
            case "BIZ_COEX_PRIVACY_TRANSITION":
            case 202:
                m.messageStubType = 202;
                break;
            case "GROUP_DEACTIVATED":
            case 203:
                m.messageStubType = 203;
                break;
            case "COMMUNITY_DEACTIVATE_SIBLING_GROUP":
            case 204:
                m.messageStubType = 204;
                break;
            case "EVENT_UPDATED":
            case 205:
                m.messageStubType = 205;
                break;
            case "EVENT_CANCELED":
            case 206:
                m.messageStubType = 206;
                break;
            case "COMMUNITY_OWNER_UPDATED":
            case 207:
                m.messageStubType = 207;
                break;
            case "COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN":
            case 208:
                m.messageStubType = 208;
                break;
            case "CAPI_GROUP_NE2EE_SYSTEM_MESSAGE":
            case 209:
                m.messageStubType = 209;
                break;
            case "STATUS_MENTION":
            case 210:
                m.messageStubType = 210;
                break;
            case "USER_CONTROLS_SYSTEM_MESSAGE":
            case 211:
                m.messageStubType = 211;
                break;
            case "SUPPORT_SYSTEM_MESSAGE":
            case 212:
                m.messageStubType = 212;
                break;
            case "CHANGE_LID":
            case 213:
                m.messageStubType = 213;
                break;
            case "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE":
            case 214:
                m.messageStubType = 214;
                break;
            case "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE":
            case 215:
                m.messageStubType = 215;
                break;
            case "CHANGE_LIMIT_SHARING":
            case 216:
                m.messageStubType = 216;
                break;
            case "GROUP_MEMBER_LINK_MODE":
            case 217:
                m.messageStubType = 217;
                break;
            case "BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE":
            case 218:
                m.messageStubType = 218;
                break;
            case "PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE":
            case 219:
                m.messageStubType = 219;
                break;
            case "QUARANTINED_MESSAGE":
            case 220:
                m.messageStubType = 220;
                break;
            case "GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE":
            case 221:
                m.messageStubType = 221;
                break;
            case "GROUP_OPEN_BOT_ADDED":
            case 222:
                m.messageStubType = 222;
                break;
            case "GROUP_TEE_BOT_ADDED":
            case 223:
                m.messageStubType = 223;
                break;
            case "CONTACT_INFO":
            case 224:
                m.messageStubType = 224;
                break;
            case "SCHEDULED_MESSAGE_CREATED":
            case 225:
                m.messageStubType = 225;
                break;
            case "IDENTITY_TRUST_MARKED":
            case 226:
                m.messageStubType = 226;
                break;
            case "IDENTITY_TRUST_UNMARKED":
            case 227:
                m.messageStubType = 227;
                break;
            case "IDENTITY_TRUST_REVOKED":
            case 228:
                m.messageStubType = 228;
                break;
            case "CTWA_CONSUMER_DISCLOSURE":
            case 230:
                m.messageStubType = 230;
                break;
            default:
                if (typeof d.messageStubType === "number" && (d.messageStubType | 0) === d.messageStubType)
                    m.messageStubType = d.messageStubType;
            }
            if (d.clearMedia != null) {
                m.clearMedia = $Boolean(d.clearMedia);
            }
            if (d.messageStubParameters) {
                if (!$Array.isArray(d.messageStubParameters))
                    throw $TypeError(".proto.WebMessageInfo.messageStubParameters: array expected");
                m.messageStubParameters = $Array(d.messageStubParameters.length);
                for (var i = 0; i < d.messageStubParameters.length; ++i) {
                    m.messageStubParameters[i] = $String(d.messageStubParameters[i]);
                }
            }
            if (d.duration != null) {
                m.duration = d.duration >>> 0;
            }
            if (d.labels) {
                if (!$Array.isArray(d.labels))
                    throw $TypeError(".proto.WebMessageInfo.labels: array expected");
                m.labels = $Array(d.labels.length);
                for (var i = 0; i < d.labels.length; ++i) {
                    m.labels[i] = $String(d.labels[i]);
                }
            }
            if (d.paymentInfo != null) {
                if (!$util.isObject(d.paymentInfo))
                    throw $TypeError(".proto.WebMessageInfo.paymentInfo: object expected");
                m.paymentInfo = $root.proto.PaymentInfo.fromObject(d.paymentInfo, q + 1);
            }
            if (d.finalLiveLocation != null) {
                if (!$util.isObject(d.finalLiveLocation))
                    throw $TypeError(".proto.WebMessageInfo.finalLiveLocation: object expected");
                m.finalLiveLocation = $root.proto.Message.LiveLocationMessage.fromObject(d.finalLiveLocation, q + 1);
            }
            if (d.quotedPaymentInfo != null) {
                if (!$util.isObject(d.quotedPaymentInfo))
                    throw $TypeError(".proto.WebMessageInfo.quotedPaymentInfo: object expected");
                m.quotedPaymentInfo = $root.proto.PaymentInfo.fromObject(d.quotedPaymentInfo, q + 1);
            }
            if (d.ephemeralStartTimestamp != null) {
                if ($util.Long)
                    m.ephemeralStartTimestamp = $util.Long.fromValue(d.ephemeralStartTimestamp, true);
                else if (typeof d.ephemeralStartTimestamp === "string")
                    m.ephemeralStartTimestamp = $parseInt(d.ephemeralStartTimestamp, 10);
                else if (typeof d.ephemeralStartTimestamp === "number")
                    m.ephemeralStartTimestamp = d.ephemeralStartTimestamp;
                else if (typeof d.ephemeralStartTimestamp === "object")
                    m.ephemeralStartTimestamp = new $util.LongBits(d.ephemeralStartTimestamp.low >>> 0, d.ephemeralStartTimestamp.high >>> 0).toNumber(true);
            }
            if (d.ephemeralDuration != null) {
                m.ephemeralDuration = d.ephemeralDuration >>> 0;
            }
            if (d.ephemeralOffToOn != null) {
                m.ephemeralOffToOn = $Boolean(d.ephemeralOffToOn);
            }
            if (d.ephemeralOutOfSync != null) {
                m.ephemeralOutOfSync = $Boolean(d.ephemeralOutOfSync);
            }
            switch (d.bizPrivacyStatus) {
            case "E2EE":
            case 0:
                m.bizPrivacyStatus = 0;
                break;
            case "FB":
            case 2:
                m.bizPrivacyStatus = 2;
                break;
            case "BSP":
            case 1:
                m.bizPrivacyStatus = 1;
                break;
            case "BSP_AND_FB":
            case 3:
                m.bizPrivacyStatus = 3;
                break;
            default:
                if (typeof d.bizPrivacyStatus === "number" && (d.bizPrivacyStatus | 0) === d.bizPrivacyStatus)
                    m.bizPrivacyStatus = d.bizPrivacyStatus;
            }
            if (d.verifiedBizName != null) {
                m.verifiedBizName = $String(d.verifiedBizName);
            }
            if (d.mediaData != null) {
                if (!$util.isObject(d.mediaData))
                    throw $TypeError(".proto.WebMessageInfo.mediaData: object expected");
                m.mediaData = $root.proto.MediaData.fromObject(d.mediaData, q + 1);
            }
            if (d.photoChange != null) {
                if (!$util.isObject(d.photoChange))
                    throw $TypeError(".proto.WebMessageInfo.photoChange: object expected");
                m.photoChange = $root.proto.PhotoChange.fromObject(d.photoChange, q + 1);
            }
            if (d.userReceipt) {
                if (!$Array.isArray(d.userReceipt))
                    throw $TypeError(".proto.WebMessageInfo.userReceipt: array expected");
                m.userReceipt = $Array(d.userReceipt.length);
                for (var i = 0; i < d.userReceipt.length; ++i) {
                    if (!$util.isObject(d.userReceipt[i]))
                        throw $TypeError(".proto.WebMessageInfo.userReceipt: object expected");
                    m.userReceipt[i] = $root.proto.UserReceipt.fromObject(d.userReceipt[i], q + 1);
                }
            }
            if (d.reactions) {
                if (!$Array.isArray(d.reactions))
                    throw $TypeError(".proto.WebMessageInfo.reactions: array expected");
                m.reactions = $Array(d.reactions.length);
                for (var i = 0; i < d.reactions.length; ++i) {
                    if (!$util.isObject(d.reactions[i]))
                        throw $TypeError(".proto.WebMessageInfo.reactions: object expected");
                    m.reactions[i] = $root.proto.Reaction.fromObject(d.reactions[i], q + 1);
                }
            }
            if (d.quotedStickerData != null) {
                if (!$util.isObject(d.quotedStickerData))
                    throw $TypeError(".proto.WebMessageInfo.quotedStickerData: object expected");
                m.quotedStickerData = $root.proto.MediaData.fromObject(d.quotedStickerData, q + 1);
            }
            if (d.futureproofData != null) {
                if (typeof d.futureproofData === "string")
                    $util.base64.decode(d.futureproofData, m.futureproofData = $util.newBuffer($util.base64.length(d.futureproofData)), 0);
                else if (d.futureproofData.length >= 0)
                    m.futureproofData = d.futureproofData;
            }
            if (d.statusPsa != null) {
                if (!$util.isObject(d.statusPsa))
                    throw $TypeError(".proto.WebMessageInfo.statusPsa: object expected");
                m.statusPsa = $root.proto.StatusPSA.fromObject(d.statusPsa, q + 1);
            }
            if (d.pollUpdates) {
                if (!$Array.isArray(d.pollUpdates))
                    throw $TypeError(".proto.WebMessageInfo.pollUpdates: array expected");
                m.pollUpdates = $Array(d.pollUpdates.length);
                for (var i = 0; i < d.pollUpdates.length; ++i) {
                    if (!$util.isObject(d.pollUpdates[i]))
                        throw $TypeError(".proto.WebMessageInfo.pollUpdates: object expected");
                    m.pollUpdates[i] = $root.proto.PollUpdate.fromObject(d.pollUpdates[i], q + 1);
                }
            }
            if (d.pollAdditionalMetadata != null) {
                if (!$util.isObject(d.pollAdditionalMetadata))
                    throw $TypeError(".proto.WebMessageInfo.pollAdditionalMetadata: object expected");
                m.pollAdditionalMetadata = $root.proto.PollAdditionalMetadata.fromObject(d.pollAdditionalMetadata, q + 1);
            }
            if (d.agentId != null) {
                m.agentId = $String(d.agentId);
            }
            if (d.statusAlreadyViewed != null) {
                m.statusAlreadyViewed = $Boolean(d.statusAlreadyViewed);
            }
            if (d.messageSecret != null) {
                if (typeof d.messageSecret === "string")
                    $util.base64.decode(d.messageSecret, m.messageSecret = $util.newBuffer($util.base64.length(d.messageSecret)), 0);
                else if (d.messageSecret.length >= 0)
                    m.messageSecret = d.messageSecret;
            }
            if (d.keepInChat != null) {
                if (!$util.isObject(d.keepInChat))
                    throw $TypeError(".proto.WebMessageInfo.keepInChat: object expected");
                m.keepInChat = $root.proto.KeepInChat.fromObject(d.keepInChat, q + 1);
            }
            if (d.originalSelfAuthorUserJidString != null) {
                m.originalSelfAuthorUserJidString = $String(d.originalSelfAuthorUserJidString);
            }
            if (d.revokeMessageTimestamp != null) {
                if ($util.Long)
                    m.revokeMessageTimestamp = $util.Long.fromValue(d.revokeMessageTimestamp, true);
                else if (typeof d.revokeMessageTimestamp === "string")
                    m.revokeMessageTimestamp = $parseInt(d.revokeMessageTimestamp, 10);
                else if (typeof d.revokeMessageTimestamp === "number")
                    m.revokeMessageTimestamp = d.revokeMessageTimestamp;
                else if (typeof d.revokeMessageTimestamp === "object")
                    m.revokeMessageTimestamp = new $util.LongBits(d.revokeMessageTimestamp.low >>> 0, d.revokeMessageTimestamp.high >>> 0).toNumber(true);
            }
            if (d.pinInChat != null) {
                if (!$util.isObject(d.pinInChat))
                    throw $TypeError(".proto.WebMessageInfo.pinInChat: object expected");
                m.pinInChat = $root.proto.PinInChat.fromObject(d.pinInChat, q + 1);
            }
            if (d.premiumMessageInfo != null) {
                if (!$util.isObject(d.premiumMessageInfo))
                    throw $TypeError(".proto.WebMessageInfo.premiumMessageInfo: object expected");
                m.premiumMessageInfo = $root.proto.PremiumMessageInfo.fromObject(d.premiumMessageInfo, q + 1);
            }
            if (d.is1PBizBotMessage != null) {
                m.is1PBizBotMessage = $Boolean(d.is1PBizBotMessage);
            }
            if (d.isGroupHistoryMessage != null) {
                m.isGroupHistoryMessage = $Boolean(d.isGroupHistoryMessage);
            }
            if (d.botMessageInvokerJid != null) {
                m.botMessageInvokerJid = $String(d.botMessageInvokerJid);
            }
            if (d.commentMetadata != null) {
                if (!$util.isObject(d.commentMetadata))
                    throw $TypeError(".proto.WebMessageInfo.commentMetadata: object expected");
                m.commentMetadata = $root.proto.CommentMetadata.fromObject(d.commentMetadata, q + 1);
            }
            if (d.eventResponses) {
                if (!$Array.isArray(d.eventResponses))
                    throw $TypeError(".proto.WebMessageInfo.eventResponses: array expected");
                m.eventResponses = $Array(d.eventResponses.length);
                for (var i = 0; i < d.eventResponses.length; ++i) {
                    if (!$util.isObject(d.eventResponses[i]))
                        throw $TypeError(".proto.WebMessageInfo.eventResponses: object expected");
                    m.eventResponses[i] = $root.proto.EventResponse.fromObject(d.eventResponses[i], q + 1);
                }
            }
            if (d.reportingTokenInfo != null) {
                if (!$util.isObject(d.reportingTokenInfo))
                    throw $TypeError(".proto.WebMessageInfo.reportingTokenInfo: object expected");
                m.reportingTokenInfo = $root.proto.ReportingTokenInfo.fromObject(d.reportingTokenInfo, q + 1);
            }
            if (d.newsletterServerId != null) {
                if ($util.Long)
                    m.newsletterServerId = $util.Long.fromValue(d.newsletterServerId, true);
                else if (typeof d.newsletterServerId === "string")
                    m.newsletterServerId = $parseInt(d.newsletterServerId, 10);
                else if (typeof d.newsletterServerId === "number")
                    m.newsletterServerId = d.newsletterServerId;
                else if (typeof d.newsletterServerId === "object")
                    m.newsletterServerId = new $util.LongBits(d.newsletterServerId.low >>> 0, d.newsletterServerId.high >>> 0).toNumber(true);
            }
            if (d.eventAdditionalMetadata != null) {
                if (!$util.isObject(d.eventAdditionalMetadata))
                    throw $TypeError(".proto.WebMessageInfo.eventAdditionalMetadata: object expected");
                m.eventAdditionalMetadata = $root.proto.EventAdditionalMetadata.fromObject(d.eventAdditionalMetadata, q + 1);
            }
            if (d.isMentionedInStatus != null) {
                m.isMentionedInStatus = $Boolean(d.isMentionedInStatus);
            }
            if (d.statusMentions) {
                if (!$Array.isArray(d.statusMentions))
                    throw $TypeError(".proto.WebMessageInfo.statusMentions: array expected");
                m.statusMentions = $Array(d.statusMentions.length);
                for (var i = 0; i < d.statusMentions.length; ++i) {
                    m.statusMentions[i] = $String(d.statusMentions[i]);
                }
            }
            if (d.targetMessageId != null) {
                if (!$util.isObject(d.targetMessageId))
                    throw $TypeError(".proto.WebMessageInfo.targetMessageId: object expected");
                m.targetMessageId = $root.proto.MessageKey.fromObject(d.targetMessageId, q + 1);
            }
            if (d.messageAddOns) {
                if (!$Array.isArray(d.messageAddOns))
                    throw $TypeError(".proto.WebMessageInfo.messageAddOns: array expected");
                m.messageAddOns = $Array(d.messageAddOns.length);
                for (var i = 0; i < d.messageAddOns.length; ++i) {
                    if (!$util.isObject(d.messageAddOns[i]))
                        throw $TypeError(".proto.WebMessageInfo.messageAddOns: object expected");
                    m.messageAddOns[i] = $root.proto.MessageAddOn.fromObject(d.messageAddOns[i], q + 1);
                }
            }
            if (d.statusMentionMessageInfo != null) {
                if (!$util.isObject(d.statusMentionMessageInfo))
                    throw $TypeError(".proto.WebMessageInfo.statusMentionMessageInfo: object expected");
                m.statusMentionMessageInfo = $root.proto.StatusMentionMessage.fromObject(d.statusMentionMessageInfo, q + 1);
            }
            if (d.isSupportAiMessage != null) {
                m.isSupportAiMessage = $Boolean(d.isSupportAiMessage);
            }
            if (d.statusMentionSources) {
                if (!$Array.isArray(d.statusMentionSources))
                    throw $TypeError(".proto.WebMessageInfo.statusMentionSources: array expected");
                m.statusMentionSources = $Array(d.statusMentionSources.length);
                for (var i = 0; i < d.statusMentionSources.length; ++i) {
                    m.statusMentionSources[i] = $String(d.statusMentionSources[i]);
                }
            }
            if (d.supportAiCitations) {
                if (!$Array.isArray(d.supportAiCitations))
                    throw $TypeError(".proto.WebMessageInfo.supportAiCitations: array expected");
                m.supportAiCitations = $Array(d.supportAiCitations.length);
                for (var i = 0; i < d.supportAiCitations.length; ++i) {
                    if (!$util.isObject(d.supportAiCitations[i]))
                        throw $TypeError(".proto.WebMessageInfo.supportAiCitations: object expected");
                    m.supportAiCitations[i] = $root.proto.Citation.fromObject(d.supportAiCitations[i], q + 1);
                }
            }
            if (d.botTargetId != null) {
                m.botTargetId = $String(d.botTargetId);
            }
            if (d.groupHistoryIndividualMessageInfo != null) {
                if (!$util.isObject(d.groupHistoryIndividualMessageInfo))
                    throw $TypeError(".proto.WebMessageInfo.groupHistoryIndividualMessageInfo: object expected");
                m.groupHistoryIndividualMessageInfo = $root.proto.GroupHistoryIndividualMessageInfo.fromObject(d.groupHistoryIndividualMessageInfo, q + 1);
            }
            if (d.groupHistoryBundleInfo != null) {
                if (!$util.isObject(d.groupHistoryBundleInfo))
                    throw $TypeError(".proto.WebMessageInfo.groupHistoryBundleInfo: object expected");
                m.groupHistoryBundleInfo = $root.proto.GroupHistoryBundleInfo.fromObject(d.groupHistoryBundleInfo, q + 1);
            }
            if (d.interactiveMessageAdditionalMetadata != null) {
                if (!$util.isObject(d.interactiveMessageAdditionalMetadata))
                    throw $TypeError(".proto.WebMessageInfo.interactiveMessageAdditionalMetadata: object expected");
                m.interactiveMessageAdditionalMetadata = $root.proto.InteractiveMessageAdditionalMetadata.fromObject(d.interactiveMessageAdditionalMetadata, q + 1);
            }
            if (d.quarantinedMessage != null) {
                if (!$util.isObject(d.quarantinedMessage))
                    throw $TypeError(".proto.WebMessageInfo.quarantinedMessage: object expected");
                m.quarantinedMessage = $root.proto.QuarantinedMessage.fromObject(d.quarantinedMessage, q + 1);
            }
            if (d.nonJidMentions != null) {
                m.nonJidMentions = d.nonJidMentions >>> 0;
            }
            if (d.hsmTag != null) {
                m.hsmTag = $String(d.hsmTag);
            }
            if (d.ephemeralExpirationTimestamp != null) {
                if ($util.Long)
                    m.ephemeralExpirationTimestamp = $util.Long.fromValue(d.ephemeralExpirationTimestamp, true);
                else if (typeof d.ephemeralExpirationTimestamp === "string")
                    m.ephemeralExpirationTimestamp = $parseInt(d.ephemeralExpirationTimestamp, 10);
                else if (typeof d.ephemeralExpirationTimestamp === "number")
                    m.ephemeralExpirationTimestamp = d.ephemeralExpirationTimestamp;
                else if (typeof d.ephemeralExpirationTimestamp === "object")
                    m.ephemeralExpirationTimestamp = new $util.LongBits(d.ephemeralExpirationTimestamp.low >>> 0, d.ephemeralExpirationTimestamp.high >>> 0).toNumber(true);
            }
            if (d.scheduledMessageMetadata != null) {
                if (!$util.isObject(d.scheduledMessageMetadata))
                    throw $TypeError(".proto.WebMessageInfo.scheduledMessageMetadata: object expected");
                m.scheduledMessageMetadata = $root.proto.ScheduledMessageMetadata.fromObject(d.scheduledMessageMetadata, q + 1);
            }
            if (d.decisionId != null) {
                m.decisionId = $String(d.decisionId);
            }
            if (d.decisionSources) {
                if (!$Array.isArray(d.decisionSources))
                    throw $TypeError(".proto.WebMessageInfo.decisionSources: array expected");
                m.decisionSources = $Array(d.decisionSources.length);
                for (var i = 0; i < d.decisionSources.length; ++i) {
                    m.decisionSources[i] = $String(d.decisionSources[i]);
                }
            }
            return m;
        };

        WebMessageInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.messageStubParameters = [];
                d.labels = [];
                d.userReceipt = [];
                d.reactions = [];
                d.pollUpdates = [];
                d.eventResponses = [];
                d.statusMentions = [];
                d.messageAddOns = [];
                d.statusMentionSources = [];
                d.supportAiCitations = [];
                d.decisionSources = [];
            }
            if (o.defaults) {
                d.key = null;
            }
            if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
            }
            if (m.message != null && $Object.hasOwnProperty.call(m, "message")) {
                d.message = $root.proto.Message.toObject(m.message, o, q + 1);
            }
            if (m.messageTimestamp != null && $Object.hasOwnProperty.call(m, "messageTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.messageTimestamp = typeof m.messageTimestamp === "number" ? $BigInt(m.messageTimestamp) : $util.Long.fromBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.messageTimestamp === "number")
                    d.messageTimestamp = o.longs === $String ? $String(m.messageTimestamp) : m.messageTimestamp;
                else
                    d.messageTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.messageTimestamp) : o.longs === $Number ? new $util.LongBits(m.messageTimestamp.low >>> 0, m.messageTimestamp.high >>> 0).toNumber(true) : m.messageTimestamp;
            }
            if (m.status != null && $Object.hasOwnProperty.call(m, "status")) {
                d.status = o.enums === $String ? $root.proto.WebMessageInfo.Status[m.status] === $undefined ? m.status : $root.proto.WebMessageInfo.Status[m.status] : m.status;
            }
            if (m.participant != null && $Object.hasOwnProperty.call(m, "participant")) {
                d.participant = m.participant;
            }
            if (m.messageC2STimestamp != null && $Object.hasOwnProperty.call(m, "messageC2STimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.messageC2STimestamp = typeof m.messageC2STimestamp === "number" ? $BigInt(m.messageC2STimestamp) : $util.Long.fromBits(m.messageC2STimestamp.low >>> 0, m.messageC2STimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.messageC2STimestamp === "number")
                    d.messageC2STimestamp = o.longs === $String ? $String(m.messageC2STimestamp) : m.messageC2STimestamp;
                else
                    d.messageC2STimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.messageC2STimestamp) : o.longs === $Number ? new $util.LongBits(m.messageC2STimestamp.low >>> 0, m.messageC2STimestamp.high >>> 0).toNumber(true) : m.messageC2STimestamp;
            }
            if (m.ignore != null && $Object.hasOwnProperty.call(m, "ignore")) {
                d.ignore = m.ignore;
            }
            if (m.starred != null && $Object.hasOwnProperty.call(m, "starred")) {
                d.starred = m.starred;
            }
            if (m.broadcast != null && $Object.hasOwnProperty.call(m, "broadcast")) {
                d.broadcast = m.broadcast;
            }
            if (m.pushName != null && $Object.hasOwnProperty.call(m, "pushName")) {
                d.pushName = m.pushName;
            }
            if (m.mediaCiphertextSha256 != null && $Object.hasOwnProperty.call(m, "mediaCiphertextSha256")) {
                d.mediaCiphertextSha256 = o.bytes === $String ? $util.base64.encode(m.mediaCiphertextSha256, 0, m.mediaCiphertextSha256.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.mediaCiphertextSha256) : m.mediaCiphertextSha256;
            }
            if (m.multicast != null && $Object.hasOwnProperty.call(m, "multicast")) {
                d.multicast = m.multicast;
            }
            if (m.urlText != null && $Object.hasOwnProperty.call(m, "urlText")) {
                d.urlText = m.urlText;
            }
            if (m.urlNumber != null && $Object.hasOwnProperty.call(m, "urlNumber")) {
                d.urlNumber = m.urlNumber;
            }
            if (m.messageStubType != null && $Object.hasOwnProperty.call(m, "messageStubType")) {
                d.messageStubType = o.enums === $String ? $root.proto.WebMessageInfo.StubType[m.messageStubType] === $undefined ? m.messageStubType : $root.proto.WebMessageInfo.StubType[m.messageStubType] : m.messageStubType;
            }
            if (m.clearMedia != null && $Object.hasOwnProperty.call(m, "clearMedia")) {
                d.clearMedia = m.clearMedia;
            }
            if (m.messageStubParameters && m.messageStubParameters.length) {
                d.messageStubParameters = $Array(m.messageStubParameters.length);
                for (var j = 0; j < m.messageStubParameters.length; ++j) {
                    d.messageStubParameters[j] = m.messageStubParameters[j];
                }
            }
            if (m.duration != null && $Object.hasOwnProperty.call(m, "duration")) {
                d.duration = m.duration;
            }
            if (m.labels && m.labels.length) {
                d.labels = $Array(m.labels.length);
                for (var j = 0; j < m.labels.length; ++j) {
                    d.labels[j] = m.labels[j];
                }
            }
            if (m.paymentInfo != null && $Object.hasOwnProperty.call(m, "paymentInfo")) {
                d.paymentInfo = $root.proto.PaymentInfo.toObject(m.paymentInfo, o, q + 1);
            }
            if (m.finalLiveLocation != null && $Object.hasOwnProperty.call(m, "finalLiveLocation")) {
                d.finalLiveLocation = $root.proto.Message.LiveLocationMessage.toObject(m.finalLiveLocation, o, q + 1);
            }
            if (m.quotedPaymentInfo != null && $Object.hasOwnProperty.call(m, "quotedPaymentInfo")) {
                d.quotedPaymentInfo = $root.proto.PaymentInfo.toObject(m.quotedPaymentInfo, o, q + 1);
            }
            if (m.ephemeralStartTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralStartTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.ephemeralStartTimestamp = typeof m.ephemeralStartTimestamp === "number" ? $BigInt(m.ephemeralStartTimestamp) : $util.Long.fromBits(m.ephemeralStartTimestamp.low >>> 0, m.ephemeralStartTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.ephemeralStartTimestamp === "number")
                    d.ephemeralStartTimestamp = o.longs === $String ? $String(m.ephemeralStartTimestamp) : m.ephemeralStartTimestamp;
                else
                    d.ephemeralStartTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.ephemeralStartTimestamp) : o.longs === $Number ? new $util.LongBits(m.ephemeralStartTimestamp.low >>> 0, m.ephemeralStartTimestamp.high >>> 0).toNumber(true) : m.ephemeralStartTimestamp;
            }
            if (m.ephemeralDuration != null && $Object.hasOwnProperty.call(m, "ephemeralDuration")) {
                d.ephemeralDuration = m.ephemeralDuration;
            }
            if (m.ephemeralOffToOn != null && $Object.hasOwnProperty.call(m, "ephemeralOffToOn")) {
                d.ephemeralOffToOn = m.ephemeralOffToOn;
            }
            if (m.ephemeralOutOfSync != null && $Object.hasOwnProperty.call(m, "ephemeralOutOfSync")) {
                d.ephemeralOutOfSync = m.ephemeralOutOfSync;
            }
            if (m.bizPrivacyStatus != null && $Object.hasOwnProperty.call(m, "bizPrivacyStatus")) {
                d.bizPrivacyStatus = o.enums === $String ? $root.proto.WebMessageInfo.BizPrivacyStatus[m.bizPrivacyStatus] === $undefined ? m.bizPrivacyStatus : $root.proto.WebMessageInfo.BizPrivacyStatus[m.bizPrivacyStatus] : m.bizPrivacyStatus;
            }
            if (m.verifiedBizName != null && $Object.hasOwnProperty.call(m, "verifiedBizName")) {
                d.verifiedBizName = m.verifiedBizName;
            }
            if (m.mediaData != null && $Object.hasOwnProperty.call(m, "mediaData")) {
                d.mediaData = $root.proto.MediaData.toObject(m.mediaData, o, q + 1);
            }
            if (m.photoChange != null && $Object.hasOwnProperty.call(m, "photoChange")) {
                d.photoChange = $root.proto.PhotoChange.toObject(m.photoChange, o, q + 1);
            }
            if (m.userReceipt && m.userReceipt.length) {
                d.userReceipt = $Array(m.userReceipt.length);
                for (var j = 0; j < m.userReceipt.length; ++j) {
                    d.userReceipt[j] = $root.proto.UserReceipt.toObject(m.userReceipt[j], o, q + 1);
                }
            }
            if (m.reactions && m.reactions.length) {
                d.reactions = $Array(m.reactions.length);
                for (var j = 0; j < m.reactions.length; ++j) {
                    d.reactions[j] = $root.proto.Reaction.toObject(m.reactions[j], o, q + 1);
                }
            }
            if (m.quotedStickerData != null && $Object.hasOwnProperty.call(m, "quotedStickerData")) {
                d.quotedStickerData = $root.proto.MediaData.toObject(m.quotedStickerData, o, q + 1);
            }
            if (m.futureproofData != null && $Object.hasOwnProperty.call(m, "futureproofData")) {
                d.futureproofData = o.bytes === $String ? $util.base64.encode(m.futureproofData, 0, m.futureproofData.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.futureproofData) : m.futureproofData;
            }
            if (m.statusPsa != null && $Object.hasOwnProperty.call(m, "statusPsa")) {
                d.statusPsa = $root.proto.StatusPSA.toObject(m.statusPsa, o, q + 1);
            }
            if (m.pollUpdates && m.pollUpdates.length) {
                d.pollUpdates = $Array(m.pollUpdates.length);
                for (var j = 0; j < m.pollUpdates.length; ++j) {
                    d.pollUpdates[j] = $root.proto.PollUpdate.toObject(m.pollUpdates[j], o, q + 1);
                }
            }
            if (m.pollAdditionalMetadata != null && $Object.hasOwnProperty.call(m, "pollAdditionalMetadata")) {
                d.pollAdditionalMetadata = $root.proto.PollAdditionalMetadata.toObject(m.pollAdditionalMetadata, o, q + 1);
            }
            if (m.agentId != null && $Object.hasOwnProperty.call(m, "agentId")) {
                d.agentId = m.agentId;
            }
            if (m.statusAlreadyViewed != null && $Object.hasOwnProperty.call(m, "statusAlreadyViewed")) {
                d.statusAlreadyViewed = m.statusAlreadyViewed;
            }
            if (m.messageSecret != null && $Object.hasOwnProperty.call(m, "messageSecret")) {
                d.messageSecret = o.bytes === $String ? $util.base64.encode(m.messageSecret, 0, m.messageSecret.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageSecret) : m.messageSecret;
            }
            if (m.keepInChat != null && $Object.hasOwnProperty.call(m, "keepInChat")) {
                d.keepInChat = $root.proto.KeepInChat.toObject(m.keepInChat, o, q + 1);
            }
            if (m.originalSelfAuthorUserJidString != null && $Object.hasOwnProperty.call(m, "originalSelfAuthorUserJidString")) {
                d.originalSelfAuthorUserJidString = m.originalSelfAuthorUserJidString;
            }
            if (m.revokeMessageTimestamp != null && $Object.hasOwnProperty.call(m, "revokeMessageTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.revokeMessageTimestamp = typeof m.revokeMessageTimestamp === "number" ? $BigInt(m.revokeMessageTimestamp) : $util.Long.fromBits(m.revokeMessageTimestamp.low >>> 0, m.revokeMessageTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.revokeMessageTimestamp === "number")
                    d.revokeMessageTimestamp = o.longs === $String ? $String(m.revokeMessageTimestamp) : m.revokeMessageTimestamp;
                else
                    d.revokeMessageTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.revokeMessageTimestamp) : o.longs === $Number ? new $util.LongBits(m.revokeMessageTimestamp.low >>> 0, m.revokeMessageTimestamp.high >>> 0).toNumber(true) : m.revokeMessageTimestamp;
            }
            if (m.pinInChat != null && $Object.hasOwnProperty.call(m, "pinInChat")) {
                d.pinInChat = $root.proto.PinInChat.toObject(m.pinInChat, o, q + 1);
            }
            if (m.premiumMessageInfo != null && $Object.hasOwnProperty.call(m, "premiumMessageInfo")) {
                d.premiumMessageInfo = $root.proto.PremiumMessageInfo.toObject(m.premiumMessageInfo, o, q + 1);
            }
            if (m.is1PBizBotMessage != null && $Object.hasOwnProperty.call(m, "is1PBizBotMessage")) {
                d.is1PBizBotMessage = m.is1PBizBotMessage;
            }
            if (m.isGroupHistoryMessage != null && $Object.hasOwnProperty.call(m, "isGroupHistoryMessage")) {
                d.isGroupHistoryMessage = m.isGroupHistoryMessage;
            }
            if (m.botMessageInvokerJid != null && $Object.hasOwnProperty.call(m, "botMessageInvokerJid")) {
                d.botMessageInvokerJid = m.botMessageInvokerJid;
            }
            if (m.commentMetadata != null && $Object.hasOwnProperty.call(m, "commentMetadata")) {
                d.commentMetadata = $root.proto.CommentMetadata.toObject(m.commentMetadata, o, q + 1);
            }
            if (m.eventResponses && m.eventResponses.length) {
                d.eventResponses = $Array(m.eventResponses.length);
                for (var j = 0; j < m.eventResponses.length; ++j) {
                    d.eventResponses[j] = $root.proto.EventResponse.toObject(m.eventResponses[j], o, q + 1);
                }
            }
            if (m.reportingTokenInfo != null && $Object.hasOwnProperty.call(m, "reportingTokenInfo")) {
                d.reportingTokenInfo = $root.proto.ReportingTokenInfo.toObject(m.reportingTokenInfo, o, q + 1);
            }
            if (m.newsletterServerId != null && $Object.hasOwnProperty.call(m, "newsletterServerId")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.newsletterServerId = typeof m.newsletterServerId === "number" ? $BigInt(m.newsletterServerId) : $util.Long.fromBits(m.newsletterServerId.low >>> 0, m.newsletterServerId.high >>> 0, true).toBigInt();
                else if (typeof m.newsletterServerId === "number")
                    d.newsletterServerId = o.longs === $String ? $String(m.newsletterServerId) : m.newsletterServerId;
                else
                    d.newsletterServerId = o.longs === $String ? $util.Long.prototype.toString.call(m.newsletterServerId) : o.longs === $Number ? new $util.LongBits(m.newsletterServerId.low >>> 0, m.newsletterServerId.high >>> 0).toNumber(true) : m.newsletterServerId;
            }
            if (m.eventAdditionalMetadata != null && $Object.hasOwnProperty.call(m, "eventAdditionalMetadata")) {
                d.eventAdditionalMetadata = $root.proto.EventAdditionalMetadata.toObject(m.eventAdditionalMetadata, o, q + 1);
            }
            if (m.isMentionedInStatus != null && $Object.hasOwnProperty.call(m, "isMentionedInStatus")) {
                d.isMentionedInStatus = m.isMentionedInStatus;
            }
            if (m.statusMentions && m.statusMentions.length) {
                d.statusMentions = $Array(m.statusMentions.length);
                for (var j = 0; j < m.statusMentions.length; ++j) {
                    d.statusMentions[j] = m.statusMentions[j];
                }
            }
            if (m.targetMessageId != null && $Object.hasOwnProperty.call(m, "targetMessageId")) {
                d.targetMessageId = $root.proto.MessageKey.toObject(m.targetMessageId, o, q + 1);
            }
            if (m.messageAddOns && m.messageAddOns.length) {
                d.messageAddOns = $Array(m.messageAddOns.length);
                for (var j = 0; j < m.messageAddOns.length; ++j) {
                    d.messageAddOns[j] = $root.proto.MessageAddOn.toObject(m.messageAddOns[j], o, q + 1);
                }
            }
            if (m.statusMentionMessageInfo != null && $Object.hasOwnProperty.call(m, "statusMentionMessageInfo")) {
                d.statusMentionMessageInfo = $root.proto.StatusMentionMessage.toObject(m.statusMentionMessageInfo, o, q + 1);
            }
            if (m.isSupportAiMessage != null && $Object.hasOwnProperty.call(m, "isSupportAiMessage")) {
                d.isSupportAiMessage = m.isSupportAiMessage;
            }
            if (m.statusMentionSources && m.statusMentionSources.length) {
                d.statusMentionSources = $Array(m.statusMentionSources.length);
                for (var j = 0; j < m.statusMentionSources.length; ++j) {
                    d.statusMentionSources[j] = m.statusMentionSources[j];
                }
            }
            if (m.supportAiCitations && m.supportAiCitations.length) {
                d.supportAiCitations = $Array(m.supportAiCitations.length);
                for (var j = 0; j < m.supportAiCitations.length; ++j) {
                    d.supportAiCitations[j] = $root.proto.Citation.toObject(m.supportAiCitations[j], o, q + 1);
                }
            }
            if (m.botTargetId != null && $Object.hasOwnProperty.call(m, "botTargetId")) {
                d.botTargetId = m.botTargetId;
            }
            if (m.groupHistoryIndividualMessageInfo != null && $Object.hasOwnProperty.call(m, "groupHistoryIndividualMessageInfo")) {
                d.groupHistoryIndividualMessageInfo = $root.proto.GroupHistoryIndividualMessageInfo.toObject(m.groupHistoryIndividualMessageInfo, o, q + 1);
            }
            if (m.groupHistoryBundleInfo != null && $Object.hasOwnProperty.call(m, "groupHistoryBundleInfo")) {
                d.groupHistoryBundleInfo = $root.proto.GroupHistoryBundleInfo.toObject(m.groupHistoryBundleInfo, o, q + 1);
            }
            if (m.interactiveMessageAdditionalMetadata != null && $Object.hasOwnProperty.call(m, "interactiveMessageAdditionalMetadata")) {
                d.interactiveMessageAdditionalMetadata = $root.proto.InteractiveMessageAdditionalMetadata.toObject(m.interactiveMessageAdditionalMetadata, o, q + 1);
            }
            if (m.quarantinedMessage != null && $Object.hasOwnProperty.call(m, "quarantinedMessage")) {
                d.quarantinedMessage = $root.proto.QuarantinedMessage.toObject(m.quarantinedMessage, o, q + 1);
            }
            if (m.nonJidMentions != null && $Object.hasOwnProperty.call(m, "nonJidMentions")) {
                d.nonJidMentions = m.nonJidMentions;
            }
            if (m.hsmTag != null && $Object.hasOwnProperty.call(m, "hsmTag")) {
                d.hsmTag = m.hsmTag;
            }
            if (m.ephemeralExpirationTimestamp != null && $Object.hasOwnProperty.call(m, "ephemeralExpirationTimestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.ephemeralExpirationTimestamp = typeof m.ephemeralExpirationTimestamp === "number" ? $BigInt(m.ephemeralExpirationTimestamp) : $util.Long.fromBits(m.ephemeralExpirationTimestamp.low >>> 0, m.ephemeralExpirationTimestamp.high >>> 0, true).toBigInt();
                else if (typeof m.ephemeralExpirationTimestamp === "number")
                    d.ephemeralExpirationTimestamp = o.longs === $String ? $String(m.ephemeralExpirationTimestamp) : m.ephemeralExpirationTimestamp;
                else
                    d.ephemeralExpirationTimestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.ephemeralExpirationTimestamp) : o.longs === $Number ? new $util.LongBits(m.ephemeralExpirationTimestamp.low >>> 0, m.ephemeralExpirationTimestamp.high >>> 0).toNumber(true) : m.ephemeralExpirationTimestamp;
            }
            if (m.scheduledMessageMetadata != null && $Object.hasOwnProperty.call(m, "scheduledMessageMetadata")) {
                d.scheduledMessageMetadata = $root.proto.ScheduledMessageMetadata.toObject(m.scheduledMessageMetadata, o, q + 1);
            }
            if (m.decisionId != null && $Object.hasOwnProperty.call(m, "decisionId")) {
                d.decisionId = m.decisionId;
            }
            if (m.decisionSources && m.decisionSources.length) {
                d.decisionSources = $Array(m.decisionSources.length);
                for (var j = 0; j < m.decisionSources.length; ++j) {
                    d.decisionSources[j] = m.decisionSources[j];
                }
            }
            return d;
        };

        WebMessageInfo.prototype.toJSON = function() {
            return WebMessageInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebMessageInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.WebMessageInfo";
        };

        WebMessageInfo.BizPrivacyStatus = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "E2EE"] = 0;
            values[valuesById[2] = "FB"] = 2;
            values[valuesById[1] = "BSP"] = 1;
            values[valuesById[3] = "BSP_AND_FB"] = 3;
            return values;
        })();

        WebMessageInfo.Status = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "ERROR"] = 0;
            values[valuesById[1] = "PENDING"] = 1;
            values[valuesById[2] = "SERVER_ACK"] = 2;
            values[valuesById[3] = "DELIVERY_ACK"] = 3;
            values[valuesById[4] = "READ"] = 4;
            values[valuesById[5] = "PLAYED"] = 5;
            return values;
        })();

        WebMessageInfo.StubType = (function() {
            const valuesById = $Object.create(null), values = $Object.create(valuesById);
            values[valuesById[0] = "UNKNOWN"] = 0;
            values[valuesById[1] = "REVOKE"] = 1;
            values[valuesById[2] = "CIPHERTEXT"] = 2;
            values[valuesById[3] = "FUTUREPROOF"] = 3;
            values[valuesById[4] = "NON_VERIFIED_TRANSITION"] = 4;
            values[valuesById[5] = "UNVERIFIED_TRANSITION"] = 5;
            values[valuesById[6] = "VERIFIED_TRANSITION"] = 6;
            values[valuesById[7] = "VERIFIED_LOW_UNKNOWN"] = 7;
            values[valuesById[8] = "VERIFIED_HIGH"] = 8;
            values[valuesById[9] = "VERIFIED_INITIAL_UNKNOWN"] = 9;
            values[valuesById[10] = "VERIFIED_INITIAL_LOW"] = 10;
            values[valuesById[11] = "VERIFIED_INITIAL_HIGH"] = 11;
            values[valuesById[12] = "VERIFIED_TRANSITION_ANY_TO_NONE"] = 12;
            values[valuesById[13] = "VERIFIED_TRANSITION_ANY_TO_HIGH"] = 13;
            values[valuesById[14] = "VERIFIED_TRANSITION_HIGH_TO_LOW"] = 14;
            values[valuesById[15] = "VERIFIED_TRANSITION_HIGH_TO_UNKNOWN"] = 15;
            values[valuesById[16] = "VERIFIED_TRANSITION_UNKNOWN_TO_LOW"] = 16;
            values[valuesById[17] = "VERIFIED_TRANSITION_LOW_TO_UNKNOWN"] = 17;
            values[valuesById[18] = "VERIFIED_TRANSITION_NONE_TO_LOW"] = 18;
            values[valuesById[19] = "VERIFIED_TRANSITION_NONE_TO_UNKNOWN"] = 19;
            values[valuesById[20] = "GROUP_CREATE"] = 20;
            values[valuesById[21] = "GROUP_CHANGE_SUBJECT"] = 21;
            values[valuesById[22] = "GROUP_CHANGE_ICON"] = 22;
            values[valuesById[23] = "GROUP_CHANGE_INVITE_LINK"] = 23;
            values[valuesById[24] = "GROUP_CHANGE_DESCRIPTION"] = 24;
            values[valuesById[25] = "GROUP_CHANGE_RESTRICT"] = 25;
            values[valuesById[26] = "GROUP_CHANGE_ANNOUNCE"] = 26;
            values[valuesById[27] = "GROUP_PARTICIPANT_ADD"] = 27;
            values[valuesById[28] = "GROUP_PARTICIPANT_REMOVE"] = 28;
            values[valuesById[29] = "GROUP_PARTICIPANT_PROMOTE"] = 29;
            values[valuesById[30] = "GROUP_PARTICIPANT_DEMOTE"] = 30;
            values[valuesById[31] = "GROUP_PARTICIPANT_INVITE"] = 31;
            values[valuesById[32] = "GROUP_PARTICIPANT_LEAVE"] = 32;
            values[valuesById[33] = "GROUP_PARTICIPANT_CHANGE_NUMBER"] = 33;
            values[valuesById[34] = "BROADCAST_CREATE"] = 34;
            values[valuesById[35] = "BROADCAST_ADD"] = 35;
            values[valuesById[36] = "BROADCAST_REMOVE"] = 36;
            values[valuesById[37] = "GENERIC_NOTIFICATION"] = 37;
            values[valuesById[38] = "E2E_IDENTITY_CHANGED"] = 38;
            values[valuesById[39] = "E2E_ENCRYPTED"] = 39;
            values[valuesById[40] = "CALL_MISSED_VOICE"] = 40;
            values[valuesById[41] = "CALL_MISSED_VIDEO"] = 41;
            values[valuesById[42] = "INDIVIDUAL_CHANGE_NUMBER"] = 42;
            values[valuesById[43] = "GROUP_DELETE"] = 43;
            values[valuesById[44] = "GROUP_ANNOUNCE_MODE_MESSAGE_BOUNCE"] = 44;
            values[valuesById[45] = "CALL_MISSED_GROUP_VOICE"] = 45;
            values[valuesById[46] = "CALL_MISSED_GROUP_VIDEO"] = 46;
            values[valuesById[47] = "PAYMENT_CIPHERTEXT"] = 47;
            values[valuesById[48] = "PAYMENT_FUTUREPROOF"] = 48;
            values[valuesById[49] = "PAYMENT_TRANSACTION_STATUS_UPDATE_FAILED"] = 49;
            values[valuesById[50] = "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUNDED"] = 50;
            values[valuesById[51] = "PAYMENT_TRANSACTION_STATUS_UPDATE_REFUND_FAILED"] = 51;
            values[valuesById[52] = "PAYMENT_TRANSACTION_STATUS_RECEIVER_PENDING_SETUP"] = 52;
            values[valuesById[53] = "PAYMENT_TRANSACTION_STATUS_RECEIVER_SUCCESS_AFTER_HICCUP"] = 53;
            values[valuesById[54] = "PAYMENT_ACTION_ACCOUNT_SETUP_REMINDER"] = 54;
            values[valuesById[55] = "PAYMENT_ACTION_SEND_PAYMENT_REMINDER"] = 55;
            values[valuesById[56] = "PAYMENT_ACTION_SEND_PAYMENT_INVITATION"] = 56;
            values[valuesById[57] = "PAYMENT_ACTION_REQUEST_DECLINED"] = 57;
            values[valuesById[58] = "PAYMENT_ACTION_REQUEST_EXPIRED"] = 58;
            values[valuesById[59] = "PAYMENT_ACTION_REQUEST_CANCELLED"] = 59;
            values[valuesById[60] = "BIZ_VERIFIED_TRANSITION_TOP_TO_BOTTOM"] = 60;
            values[valuesById[61] = "BIZ_VERIFIED_TRANSITION_BOTTOM_TO_TOP"] = 61;
            values[valuesById[62] = "BIZ_INTRO_TOP"] = 62;
            values[valuesById[63] = "BIZ_INTRO_BOTTOM"] = 63;
            values[valuesById[64] = "BIZ_NAME_CHANGE"] = 64;
            values[valuesById[65] = "BIZ_MOVE_TO_CONSUMER_APP"] = 65;
            values[valuesById[66] = "BIZ_TWO_TIER_MIGRATION_TOP"] = 66;
            values[valuesById[67] = "BIZ_TWO_TIER_MIGRATION_BOTTOM"] = 67;
            values[valuesById[68] = "OVERSIZED"] = 68;
            values[valuesById[69] = "GROUP_CHANGE_NO_FREQUENTLY_FORWARDED"] = 69;
            values[valuesById[70] = "GROUP_V4_ADD_INVITE_SENT"] = 70;
            values[valuesById[71] = "GROUP_PARTICIPANT_ADD_REQUEST_JOIN"] = 71;
            values[valuesById[72] = "CHANGE_EPHEMERAL_SETTING"] = 72;
            values[valuesById[73] = "E2E_DEVICE_CHANGED"] = 73;
            values[valuesById[74] = "VIEWED_ONCE"] = 74;
            values[valuesById[75] = "E2E_ENCRYPTED_NOW"] = 75;
            values[valuesById[76] = "BLUE_MSG_BSP_FB_TO_BSP_PREMISE"] = 76;
            values[valuesById[77] = "BLUE_MSG_BSP_FB_TO_SELF_FB"] = 77;
            values[valuesById[78] = "BLUE_MSG_BSP_FB_TO_SELF_PREMISE"] = 78;
            values[valuesById[79] = "BLUE_MSG_BSP_FB_UNVERIFIED"] = 79;
            values[valuesById[80] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 80;
            values[valuesById[81] = "BLUE_MSG_BSP_FB_VERIFIED"] = 81;
            values[valuesById[82] = "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 82;
            values[valuesById[83] = "BLUE_MSG_BSP_PREMISE_TO_SELF_PREMISE"] = 83;
            values[valuesById[84] = "BLUE_MSG_BSP_PREMISE_UNVERIFIED"] = 84;
            values[valuesById[85] = "BLUE_MSG_BSP_PREMISE_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 85;
            values[valuesById[86] = "BLUE_MSG_BSP_PREMISE_VERIFIED"] = 86;
            values[valuesById[87] = "BLUE_MSG_BSP_PREMISE_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 87;
            values[valuesById[88] = "BLUE_MSG_CONSUMER_TO_BSP_FB_UNVERIFIED"] = 88;
            values[valuesById[89] = "BLUE_MSG_CONSUMER_TO_BSP_PREMISE_UNVERIFIED"] = 89;
            values[valuesById[90] = "BLUE_MSG_CONSUMER_TO_SELF_FB_UNVERIFIED"] = 90;
            values[valuesById[91] = "BLUE_MSG_CONSUMER_TO_SELF_PREMISE_UNVERIFIED"] = 91;
            values[valuesById[92] = "BLUE_MSG_SELF_FB_TO_BSP_PREMISE"] = 92;
            values[valuesById[93] = "BLUE_MSG_SELF_FB_TO_SELF_PREMISE"] = 93;
            values[valuesById[94] = "BLUE_MSG_SELF_FB_UNVERIFIED"] = 94;
            values[valuesById[95] = "BLUE_MSG_SELF_FB_UNVERIFIED_TO_SELF_PREMISE_VERIFIED"] = 95;
            values[valuesById[96] = "BLUE_MSG_SELF_FB_VERIFIED"] = 96;
            values[valuesById[97] = "BLUE_MSG_SELF_FB_VERIFIED_TO_SELF_PREMISE_UNVERIFIED"] = 97;
            values[valuesById[98] = "BLUE_MSG_SELF_PREMISE_TO_BSP_PREMISE"] = 98;
            values[valuesById[99] = "BLUE_MSG_SELF_PREMISE_UNVERIFIED"] = 99;
            values[valuesById[100] = "BLUE_MSG_SELF_PREMISE_VERIFIED"] = 100;
            values[valuesById[101] = "BLUE_MSG_TO_BSP_FB"] = 101;
            values[valuesById[102] = "BLUE_MSG_TO_CONSUMER"] = 102;
            values[valuesById[103] = "BLUE_MSG_TO_SELF_FB"] = 103;
            values[valuesById[104] = "BLUE_MSG_UNVERIFIED_TO_BSP_FB_VERIFIED"] = 104;
            values[valuesById[105] = "BLUE_MSG_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 105;
            values[valuesById[106] = "BLUE_MSG_UNVERIFIED_TO_SELF_FB_VERIFIED"] = 106;
            values[valuesById[107] = "BLUE_MSG_UNVERIFIED_TO_VERIFIED"] = 107;
            values[valuesById[108] = "BLUE_MSG_VERIFIED_TO_BSP_FB_UNVERIFIED"] = 108;
            values[valuesById[109] = "BLUE_MSG_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 109;
            values[valuesById[110] = "BLUE_MSG_VERIFIED_TO_SELF_FB_UNVERIFIED"] = 110;
            values[valuesById[111] = "BLUE_MSG_VERIFIED_TO_UNVERIFIED"] = 111;
            values[valuesById[112] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 112;
            values[valuesById[113] = "BLUE_MSG_BSP_FB_UNVERIFIED_TO_SELF_FB_VERIFIED"] = 113;
            values[valuesById[114] = "BLUE_MSG_BSP_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 114;
            values[valuesById[115] = "BLUE_MSG_BSP_FB_VERIFIED_TO_SELF_FB_UNVERIFIED"] = 115;
            values[valuesById[116] = "BLUE_MSG_SELF_FB_UNVERIFIED_TO_BSP_PREMISE_VERIFIED"] = 116;
            values[valuesById[117] = "BLUE_MSG_SELF_FB_VERIFIED_TO_BSP_PREMISE_UNVERIFIED"] = 117;
            values[valuesById[118] = "E2E_IDENTITY_UNAVAILABLE"] = 118;
            values[valuesById[119] = "GROUP_CREATING"] = 119;
            values[valuesById[120] = "GROUP_CREATE_FAILED"] = 120;
            values[valuesById[121] = "GROUP_BOUNCED"] = 121;
            values[valuesById[122] = "BLOCK_CONTACT"] = 122;
            values[valuesById[123] = "EPHEMERAL_SETTING_NOT_APPLIED"] = 123;
            values[valuesById[124] = "SYNC_FAILED"] = 124;
            values[valuesById[125] = "SYNCING"] = 125;
            values[valuesById[126] = "BIZ_PRIVACY_MODE_INIT_FB"] = 126;
            values[valuesById[127] = "BIZ_PRIVACY_MODE_INIT_BSP"] = 127;
            values[valuesById[128] = "BIZ_PRIVACY_MODE_TO_FB"] = 128;
            values[valuesById[129] = "BIZ_PRIVACY_MODE_TO_BSP"] = 129;
            values[valuesById[130] = "DISAPPEARING_MODE"] = 130;
            values[valuesById[131] = "E2E_DEVICE_FETCH_FAILED"] = 131;
            values[valuesById[132] = "ADMIN_REVOKE"] = 132;
            values[valuesById[133] = "GROUP_INVITE_LINK_GROWTH_LOCKED"] = 133;
            values[valuesById[134] = "COMMUNITY_LINK_PARENT_GROUP"] = 134;
            values[valuesById[135] = "COMMUNITY_LINK_SIBLING_GROUP"] = 135;
            values[valuesById[136] = "COMMUNITY_LINK_SUB_GROUP"] = 136;
            values[valuesById[137] = "COMMUNITY_UNLINK_PARENT_GROUP"] = 137;
            values[valuesById[138] = "COMMUNITY_UNLINK_SIBLING_GROUP"] = 138;
            values[valuesById[139] = "COMMUNITY_UNLINK_SUB_GROUP"] = 139;
            values[valuesById[140] = "GROUP_PARTICIPANT_ACCEPT"] = 140;
            values[valuesById[141] = "GROUP_PARTICIPANT_LINKED_GROUP_JOIN"] = 141;
            values[valuesById[142] = "COMMUNITY_CREATE"] = 142;
            values[valuesById[143] = "EPHEMERAL_KEEP_IN_CHAT"] = 143;
            values[valuesById[144] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST"] = 144;
            values[valuesById[145] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_MODE"] = 145;
            values[valuesById[146] = "INTEGRITY_UNLINK_PARENT_GROUP"] = 146;
            values[valuesById[147] = "COMMUNITY_PARTICIPANT_PROMOTE"] = 147;
            values[valuesById[148] = "COMMUNITY_PARTICIPANT_DEMOTE"] = 148;
            values[valuesById[149] = "COMMUNITY_PARENT_GROUP_DELETED"] = 149;
            values[valuesById[150] = "COMMUNITY_LINK_PARENT_GROUP_MEMBERSHIP_APPROVAL"] = 150;
            values[valuesById[151] = "GROUP_PARTICIPANT_JOINED_GROUP_AND_PARENT_GROUP"] = 151;
            values[valuesById[152] = "MASKED_THREAD_CREATED"] = 152;
            values[valuesById[153] = "MASKED_THREAD_UNMASKED"] = 153;
            values[valuesById[154] = "BIZ_CHAT_ASSIGNMENT"] = 154;
            values[valuesById[155] = "CHAT_PSA"] = 155;
            values[valuesById[156] = "CHAT_POLL_CREATION_MESSAGE"] = 156;
            values[valuesById[157] = "CAG_MASKED_THREAD_CREATED"] = 157;
            values[valuesById[158] = "COMMUNITY_PARENT_GROUP_SUBJECT_CHANGED"] = 158;
            values[valuesById[159] = "CAG_INVITE_AUTO_ADD"] = 159;
            values[valuesById[160] = "BIZ_CHAT_ASSIGNMENT_UNASSIGN"] = 160;
            values[valuesById[161] = "CAG_INVITE_AUTO_JOINED"] = 161;
            values[valuesById[162] = "SCHEDULED_CALL_START_MESSAGE"] = 162;
            values[valuesById[163] = "COMMUNITY_INVITE_RICH"] = 163;
            values[valuesById[164] = "COMMUNITY_INVITE_AUTO_ADD_RICH"] = 164;
            values[valuesById[165] = "SUB_GROUP_INVITE_RICH"] = 165;
            values[valuesById[166] = "SUB_GROUP_PARTICIPANT_ADD_RICH"] = 166;
            values[valuesById[167] = "COMMUNITY_LINK_PARENT_GROUP_RICH"] = 167;
            values[valuesById[168] = "COMMUNITY_PARTICIPANT_ADD_RICH"] = 168;
            values[valuesById[169] = "SILENCED_UNKNOWN_CALLER_AUDIO"] = 169;
            values[valuesById[170] = "SILENCED_UNKNOWN_CALLER_VIDEO"] = 170;
            values[valuesById[171] = "GROUP_MEMBER_ADD_MODE"] = 171;
            values[valuesById[172] = "GROUP_MEMBERSHIP_JOIN_APPROVAL_REQUEST_NON_ADMIN_ADD"] = 172;
            values[valuesById[173] = "COMMUNITY_CHANGE_DESCRIPTION"] = 173;
            values[valuesById[174] = "SENDER_INVITE"] = 174;
            values[valuesById[175] = "RECEIVER_INVITE"] = 175;
            values[valuesById[176] = "COMMUNITY_ALLOW_MEMBER_ADDED_GROUPS"] = 176;
            values[valuesById[177] = "PINNED_MESSAGE_IN_CHAT"] = 177;
            values[valuesById[178] = "PAYMENT_INVITE_SETUP_INVITER"] = 178;
            values[valuesById[179] = "PAYMENT_INVITE_SETUP_INVITEE_RECEIVE_ONLY"] = 179;
            values[valuesById[180] = "PAYMENT_INVITE_SETUP_INVITEE_SEND_AND_RECEIVE"] = 180;
            values[valuesById[181] = "LINKED_GROUP_CALL_START"] = 181;
            values[valuesById[182] = "REPORT_TO_ADMIN_ENABLED_STATUS"] = 182;
            values[valuesById[183] = "EMPTY_SUBGROUP_CREATE"] = 183;
            values[valuesById[184] = "SCHEDULED_CALL_CANCEL"] = 184;
            values[valuesById[185] = "SUBGROUP_ADMIN_TRIGGERED_AUTO_ADD_RICH"] = 185;
            values[valuesById[186] = "GROUP_CHANGE_RECENT_HISTORY_SHARING"] = 186;
            values[valuesById[187] = "PAID_MESSAGE_SERVER_CAMPAIGN_ID"] = 187;
            values[valuesById[188] = "GENERAL_CHAT_CREATE"] = 188;
            values[valuesById[189] = "GENERAL_CHAT_ADD"] = 189;
            values[valuesById[190] = "GENERAL_CHAT_AUTO_ADD_DISABLED"] = 190;
            values[valuesById[191] = "SUGGESTED_SUBGROUP_ANNOUNCE"] = 191;
            values[valuesById[192] = "BIZ_BOT_1P_MESSAGING_ENABLED"] = 192;
            values[valuesById[193] = "CHANGE_USERNAME"] = 193;
            values[valuesById[194] = "BIZ_COEX_PRIVACY_INIT_SELF"] = 194;
            values[valuesById[195] = "BIZ_COEX_PRIVACY_TRANSITION_SELF"] = 195;
            values[valuesById[196] = "SUPPORT_AI_EDUCATION"] = 196;
            values[valuesById[197] = "BIZ_BOT_3P_MESSAGING_ENABLED"] = 197;
            values[valuesById[198] = "REMINDER_SETUP_MESSAGE"] = 198;
            values[valuesById[199] = "REMINDER_SENT_MESSAGE"] = 199;
            values[valuesById[200] = "REMINDER_CANCEL_MESSAGE"] = 200;
            values[valuesById[201] = "BIZ_COEX_PRIVACY_INIT"] = 201;
            values[valuesById[202] = "BIZ_COEX_PRIVACY_TRANSITION"] = 202;
            values[valuesById[203] = "GROUP_DEACTIVATED"] = 203;
            values[valuesById[204] = "COMMUNITY_DEACTIVATE_SIBLING_GROUP"] = 204;
            values[valuesById[205] = "EVENT_UPDATED"] = 205;
            values[valuesById[206] = "EVENT_CANCELED"] = 206;
            values[valuesById[207] = "COMMUNITY_OWNER_UPDATED"] = 207;
            values[valuesById[208] = "COMMUNITY_SUB_GROUP_VISIBILITY_HIDDEN"] = 208;
            values[valuesById[209] = "CAPI_GROUP_NE2EE_SYSTEM_MESSAGE"] = 209;
            values[valuesById[210] = "STATUS_MENTION"] = 210;
            values[valuesById[211] = "USER_CONTROLS_SYSTEM_MESSAGE"] = 211;
            values[valuesById[212] = "SUPPORT_SYSTEM_MESSAGE"] = 212;
            values[valuesById[213] = "CHANGE_LID"] = 213;
            values[valuesById[214] = "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_IN_MESSAGE"] = 214;
            values[valuesById[215] = "BIZ_CUSTOMER_3PD_DATA_SHARING_OPT_OUT_MESSAGE"] = 215;
            values[valuesById[216] = "CHANGE_LIMIT_SHARING"] = 216;
            values[valuesById[217] = "GROUP_MEMBER_LINK_MODE"] = 217;
            values[valuesById[218] = "BIZ_AUTOMATICALLY_LABELED_CHAT_SYSTEM_MESSAGE"] = 218;
            values[valuesById[219] = "PHONE_NUMBER_HIDING_CHAT_DEPRECATED_MESSAGE"] = 219;
            values[valuesById[220] = "QUARANTINED_MESSAGE"] = 220;
            values[valuesById[221] = "GROUP_MEMBER_SHARE_GROUP_HISTORY_MODE"] = 221;
            values[valuesById[222] = "GROUP_OPEN_BOT_ADDED"] = 222;
            values[valuesById[223] = "GROUP_TEE_BOT_ADDED"] = 223;
            values[valuesById[224] = "CONTACT_INFO"] = 224;
            values[valuesById[225] = "SCHEDULED_MESSAGE_CREATED"] = 225;
            values[valuesById[226] = "IDENTITY_TRUST_MARKED"] = 226;
            values[valuesById[227] = "IDENTITY_TRUST_UNMARKED"] = 227;
            values[valuesById[228] = "IDENTITY_TRUST_REVOKED"] = 228;
            values[valuesById[230] = "CTWA_CONSUMER_DISCLOSURE"] = 230;
            return values;
        })();

        return WebMessageInfo;
    })();

    proto.WebMessageInfoWithMessageBytes = (function() {

        const WebMessageInfoWithMessageBytes = function (p) {
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        WebMessageInfoWithMessageBytes.prototype.key = null;
        WebMessageInfoWithMessageBytes.prototype.messageBytes = null;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfoWithMessageBytes.prototype, "_key", {
            get: $util.oneOfGetter($oneOfFields = ["key"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebMessageInfoWithMessageBytes.prototype, "_messageBytes", {
            get: $util.oneOfGetter($oneOfFields = ["messageBytes"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebMessageInfoWithMessageBytes.create = function(properties) {
            return new WebMessageInfoWithMessageBytes(properties);
        };

        WebMessageInfoWithMessageBytes.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.key != null && $Object.hasOwnProperty.call(m, "key"))
                $root.proto.MessageKey.encode(m.key, w.uint32(10).fork(), q + 1).ldelim();
            if (m.messageBytes != null && $Object.hasOwnProperty.call(m, "messageBytes"))
                w.uint32(18).bytes(m.messageBytes);
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        WebMessageInfoWithMessageBytes.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.WebMessageInfoWithMessageBytes();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 1: {
                        if (u !== 2)
                            break;
                        m.key = $root.proto.MessageKey.decode(r, r.uint32(), $undefined, q + 1, m.key);
                        m._key = "key";
                        continue;
                    }
                case 2: {
                        if (u !== 2)
                            break;
                        m.messageBytes = r.bytes();
                        m._messageBytes = "messageBytes";
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        WebMessageInfoWithMessageBytes.fromObject = function (d, q) {
            if (d instanceof $root.proto.WebMessageInfoWithMessageBytes)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.WebMessageInfoWithMessageBytes: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.WebMessageInfoWithMessageBytes();
            if (d.key != null) {
                if (!$util.isObject(d.key))
                    throw $TypeError(".proto.WebMessageInfoWithMessageBytes.key: object expected");
                m.key = $root.proto.MessageKey.fromObject(d.key, q + 1);
            }
            if (d.messageBytes != null) {
                if (typeof d.messageBytes === "string")
                    $util.base64.decode(d.messageBytes, m.messageBytes = $util.newBuffer($util.base64.length(d.messageBytes)), 0);
                else if (d.messageBytes.length >= 0)
                    m.messageBytes = d.messageBytes;
            }
            return m;
        };

        WebMessageInfoWithMessageBytes.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (m.key != null && $Object.hasOwnProperty.call(m, "key")) {
                d.key = $root.proto.MessageKey.toObject(m.key, o, q + 1);
            }
            if (m.messageBytes != null && $Object.hasOwnProperty.call(m, "messageBytes")) {
                d.messageBytes = o.bytes === $String ? $util.base64.encode(m.messageBytes, 0, m.messageBytes.length) : o.bytes === $Array ? $Array.prototype.slice.call(m.messageBytes) : m.messageBytes;
            }
            return d;
        };

        WebMessageInfoWithMessageBytes.prototype.toJSON = function() {
            return WebMessageInfoWithMessageBytes.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebMessageInfoWithMessageBytes.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.WebMessageInfoWithMessageBytes";
        };

        return WebMessageInfoWithMessageBytes;
    })();

    proto.WebNotificationsInfo = (function() {

        const WebNotificationsInfo = function (p) {
            this.notifyMessages = [];
            if (p)
                for (var ks = $Object.keys(p), i = 0; i < ks.length; ++i)
                    if (p[ks[i]] != null && ks[i] !== "__proto__")
                        this[ks[i]] = p[ks[i]];
        };

        WebNotificationsInfo.prototype.timestamp = null;
        WebNotificationsInfo.prototype.unreadChats = null;
        WebNotificationsInfo.prototype.notifyMessageCount = null;
        WebNotificationsInfo.prototype.notifyMessages = $util.emptyArray;

        let $oneOfFields;

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebNotificationsInfo.prototype, "_timestamp", {
            get: $util.oneOfGetter($oneOfFields = ["timestamp"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebNotificationsInfo.prototype, "_unreadChats", {
            get: $util.oneOfGetter($oneOfFields = ["unreadChats"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        // Virtual OneOf for proto3 optional field
        $Object.defineProperty(WebNotificationsInfo.prototype, "_notifyMessageCount", {
            get: $util.oneOfGetter($oneOfFields = ["notifyMessageCount"]),
            set: $util.oneOfSetter($oneOfFields)
        });

        WebNotificationsInfo.create = function(properties) {
            return new WebNotificationsInfo(properties);
        };

        WebNotificationsInfo.encode = function (m, w, q) {
            if (!w)
                w = $Writer.create();
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp"))
                w.uint32(16).uint64(m.timestamp);
            if (m.unreadChats != null && $Object.hasOwnProperty.call(m, "unreadChats"))
                w.uint32(24).uint32(m.unreadChats);
            if (m.notifyMessageCount != null && $Object.hasOwnProperty.call(m, "notifyMessageCount"))
                w.uint32(32).uint32(m.notifyMessageCount);
            if (m.notifyMessages != null && m.notifyMessages.length) {
                for (var i = 0; i < m.notifyMessages.length; ++i)
                    $root.proto.WebMessageInfo.encode(m.notifyMessages[i], w.uint32(42).fork(), q + 1).ldelim();
            }
            if (m.$unknowns != null && $Object.hasOwnProperty.call(m, "$unknowns"))
                for (var i = 0; i < m.$unknowns.length; ++i)
                    w.raw(m.$unknowns[i]);
            return w;
        };

        WebNotificationsInfo.decode = function (r, l, z, q, g) {
            if (!(r instanceof $Reader))
                r = $Reader.create(r);
            if (q === $undefined)
                q = 0;
            if (q > $Reader.recursionLimit)
                throw $Error("max depth exceeded");
            var c = l === $undefined ? r.len : r.pos + l, m = g || new $root.proto.WebNotificationsInfo();
            while (r.pos < c) {
                var s = r.pos;
                var t = r.tag();
                if (t === z) {
                    z = $undefined;
                    break;
                }
                var u = t & 7;
                switch (t >>>= 3) {
                case 2: {
                        if (u !== 0)
                            break;
                        m.timestamp = r.uint64();
                        m._timestamp = "timestamp";
                        continue;
                    }
                case 3: {
                        if (u !== 0)
                            break;
                        m.unreadChats = r.uint32();
                        m._unreadChats = "unreadChats";
                        continue;
                    }
                case 4: {
                        if (u !== 0)
                            break;
                        m.notifyMessageCount = r.uint32();
                        m._notifyMessageCount = "notifyMessageCount";
                        continue;
                    }
                case 5: {
                        if (u !== 2)
                            break;
                        if (!(m.notifyMessages && m.notifyMessages.length))
                            m.notifyMessages = [];
                        m.notifyMessages.push($root.proto.WebMessageInfo.decode(r, r.uint32(), $undefined, q + 1));
                        continue;
                    }
                }
                r.skipType(u, q, t);
                if (!r.discardUnknown) {
                    $util.makeProp(m, "$unknowns", false);
                    (m.$unknowns || (m.$unknowns = [])).push(r.raw(s, r.pos));
                }
            }
            if (z !== $undefined)
                throw $Error("missing end group");
            return m;
        };

        WebNotificationsInfo.fromObject = function (d, q) {
            if (d instanceof $root.proto.WebNotificationsInfo)
                return d;
            if (!$util.isObject(d))
                throw $TypeError(".proto.WebNotificationsInfo: object expected");
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var m = new $root.proto.WebNotificationsInfo();
            if (d.timestamp != null) {
                if ($util.Long)
                    m.timestamp = $util.Long.fromValue(d.timestamp, true);
                else if (typeof d.timestamp === "string")
                    m.timestamp = $parseInt(d.timestamp, 10);
                else if (typeof d.timestamp === "number")
                    m.timestamp = d.timestamp;
                else if (typeof d.timestamp === "object")
                    m.timestamp = new $util.LongBits(d.timestamp.low >>> 0, d.timestamp.high >>> 0).toNumber(true);
            }
            if (d.unreadChats != null) {
                m.unreadChats = d.unreadChats >>> 0;
            }
            if (d.notifyMessageCount != null) {
                m.notifyMessageCount = d.notifyMessageCount >>> 0;
            }
            if (d.notifyMessages) {
                if (!$Array.isArray(d.notifyMessages))
                    throw $TypeError(".proto.WebNotificationsInfo.notifyMessages: array expected");
                m.notifyMessages = $Array(d.notifyMessages.length);
                for (var i = 0; i < d.notifyMessages.length; ++i) {
                    if (!$util.isObject(d.notifyMessages[i]))
                        throw $TypeError(".proto.WebNotificationsInfo.notifyMessages: object expected");
                    m.notifyMessages[i] = $root.proto.WebMessageInfo.fromObject(d.notifyMessages[i], q + 1);
                }
            }
            return m;
        };

        WebNotificationsInfo.toObject = function (m, o, q) {
            if (!o)
                o = {};
            if (q === $undefined)
                q = 0;
            if (q > $util.recursionLimit)
                throw $Error("max depth exceeded");
            var d = {};
            if (o.arrays || o.defaults) {
                d.notifyMessages = [];
            }
            if (m.timestamp != null && $Object.hasOwnProperty.call(m, "timestamp")) {
                if (typeof $BigInt !== "undefined" && o.longs === $BigInt)
                    d.timestamp = typeof m.timestamp === "number" ? $BigInt(m.timestamp) : $util.Long.fromBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0, true).toBigInt();
                else if (typeof m.timestamp === "number")
                    d.timestamp = o.longs === $String ? $String(m.timestamp) : m.timestamp;
                else
                    d.timestamp = o.longs === $String ? $util.Long.prototype.toString.call(m.timestamp) : o.longs === $Number ? new $util.LongBits(m.timestamp.low >>> 0, m.timestamp.high >>> 0).toNumber(true) : m.timestamp;
            }
            if (m.unreadChats != null && $Object.hasOwnProperty.call(m, "unreadChats")) {
                d.unreadChats = m.unreadChats;
            }
            if (m.notifyMessageCount != null && $Object.hasOwnProperty.call(m, "notifyMessageCount")) {
                d.notifyMessageCount = m.notifyMessageCount;
            }
            if (m.notifyMessages && m.notifyMessages.length) {
                d.notifyMessages = $Array(m.notifyMessages.length);
                for (var j = 0; j < m.notifyMessages.length; ++j) {
                    d.notifyMessages[j] = $root.proto.WebMessageInfo.toObject(m.notifyMessages[j], o, q + 1);
                }
            }
            return d;
        };

        WebNotificationsInfo.prototype.toJSON = function() {
            return WebNotificationsInfo.toObject(this, $protobuf.util.toJSONOptions);
        };

        WebNotificationsInfo.getTypeUrl = function(prefix) {
            if (prefix === $undefined)
                prefix = "type.googleapis.com";
            return prefix + "/proto.WebNotificationsInfo";
        };

        return WebNotificationsInfo;
    })();

    return proto;
})();

export {
  $root as default
};
